# Data Model: Rewrite Codegen from grammar.js

**Feature**: 001-codegen-grammarjs-rewrite
**Date**: 2026-03-24
**Last Updated**: 2026-03-25

## Input Entities (read-only, from grammar packages)

### GrammarJson
Source: `grammar.json` (compiled from `grammar.js`)
- `name: string` — grammar language name (e.g., "rust")
- `rules: Record<string, GrammarRule>` — rule definitions per node kind

### GrammarRule (discriminated union)
Types: SEQ, CHOICE, STRING, FIELD, SYMBOL, BLANK, REPEAT, REPEAT1,
PREC, PREC_LEFT, PREC_RIGHT, ALIAS, TOKEN, IMMEDIATE_TOKEN, PATTERN

Key relationships:
- SEQ.members → ordered list of child rules (determines token order)
- FIELD.name → field name, FIELD.content → nested rule
- STRING.value → literal text (keywords, punctuation)
- SYMBOL.name → reference to another rule

### RawNodeEntry
Source: `node-types.json`
- `type: string` — node kind string
- `named: boolean` — whether this is a named node
- `fields?: Record<string, RawFieldEntry>` — field metadata
- `children?: RawFieldEntry` — unnamed children slot
- `subtypes?: Array<{ type: string; named: boolean }>` — for supertypes

### RawFieldEntry
- `required: boolean` — whether field must be present
- `multiple: boolean` — whether field accepts multiple values
- `types: Array<{ type: string; named: boolean }>` — accepted node kinds

## Derived Entities (computed by grammar-reader)

### KindMeta
Computed by: `readGrammarKind(grammar, kind)`
Merges `node-types.json` (field metadata) + `grammar.json` (rule structure)
- `kind: string` — node kind
- `fields: FieldMeta[]` — field metadata
- `hasChildren: boolean` — whether node has unnamed children
- `children?: ChildrenMeta` — unnamed children metadata

### FieldMeta
- `name: string` — field name (from grammar)
- `required: boolean` — from node-types.json (authoritative)
- `multiple: boolean` — from node-types.json (authoritative)
- `types: string[]` — all accepted type strings
- `namedTypes: string[]` — only named types (maps to factories)

### ChildrenMeta
- `required: boolean`
- `multiple: boolean`
- `types: string[]`
- `namedTypes: string[]`

### SupertypeInfo
- `name: string` — supertype kind (e.g., `_expression`)
- `subtypes: string[]` — concrete kind strings

## Output Entities (generated by codegen)

### NodeData<G, K> (type projection, in @sittir/types)
Grammar-derived plain object type. Two params always (grammar + kind).
- Branch nodes: `{ type: K; fields: { ... } }` — fields typed to grammar spec
- Leaf nodes: `{ type: K; text?: string }` — no `fields` property

### AnyNodeData (runtime type, in @sittir/types)
Loose structural supertype for grammar-agnostic `@sittir/core` functions.
- `type: string`
- `fields?: Record<string, unknown>` — optional (leaves don't have fields)
- `text?: string`

### TreeNode<G, K> (type projection, in @sittir/types)
Parsed tree node with typed `field()` navigation. Structurally compatible with ast-grep `SgNode`.
- `type: K`
- `field(name): TreeNode<G, FieldKinds> | null`
- `text(): string`
- `range(): { start: { index }, end: { index } }`

### Edit (interface, in @sittir/types)
- `startPos: number` — byte offset of replacement start
- `endPos: number` — byte offset of replacement end
- `insertedText: string` — replacement text

### GeneratedFiles (returned by generate())
- `grammar: string` — grammar.ts (type literal)
- `types: string` — types.ts (enums, interfaces, supertype unions)
- `rules: string` — rules.ts (S-expression templates)
- `joinBy: string` — joinby.ts (separator map)
- `factories: string` — factories.ts (unified factories + Config/FromInput/Node types)
- `from: string` — from.ts (.from() ergonomic resolution)
- `assign: string` — assign.ts (assignByKind + per-kind assign + edit())
- `utils: string` — utils.ts (isNodeData, _inferBranch, _BRANCH_FIELDS)
- `irNamespace: string` — ir.ts (developer namespace)
- `consts: string` — consts.ts (kind/keyword/operator discovery maps)
- `index: string` — index.ts (barrel exports)
- `tests: string` — nodes.test.ts (per-node tests)
- `config: string` — vitest.config.ts

## Entity Relationships

```
grammar.json ──┐
               ├──→ grammar-reader ──→ KindMeta[] ──→ codegen emitters ──→ GeneratedFiles
node-types.json┘
                                                           │
                                                           ├── grammar.ts (type literal)
                                                           ├── types.ts (interfaces, enums, unions)
                                                           ├── rules.ts (S-expression templates)
                                                           ├── joinby.ts (separator map)
                                                           ├── factories.ts (factories + type interfaces)
                                                           ├── from.ts (.from() resolution)
                                                           ├── assign.ts (tree hydration + edit())
                                                           ├── utils.ts (shared utilities)
                                                           ├── ir.ts (developer namespace)
                                                           ├── consts.ts (discovery maps)
                                                           ├── index.ts (barrel)
                                                           └── nodes.test.ts (per-node tests)
```

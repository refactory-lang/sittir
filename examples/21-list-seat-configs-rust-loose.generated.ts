// @generated by `sittir tool emit-factory-source --surface loose`; do not edit.
import { ir } from '@sittir/rust';

export function rebuildListSeatConfigsRustLoose() {
	return ir.sourceFile({
		statements: [ir.functionItem({
			name: "main",
			parameters: ir.parameters(),
			body: ir.block({
				statements: [ir.expressionStatement.withSemi(ir.callExpression({
					function: "call",
					arguments: [{
						attributeItem: [ir.attribute.input({
							path: "cfg",
							arguments: ir.delimTokenTree.paren(ir.identifier("a")),
						})],
						expression: "x",
					}, "y"],
				}))],
			}),
		})],
	});
}

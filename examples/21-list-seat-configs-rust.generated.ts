// @generated by `sittir tool emit-factory-source`; do not edit.
import { ir, Delimiter } from '@sittir/rust';

export function rebuildListSeatConfigsRustGenerated() {
	return ir.sourceFile.strict({
		statements: [ir.functionItem.strict({
			name: ir.identifier("main"),
			parameters: ir.parameters.strict(),
			body: ir.block.strict({
				statements: [ir.expressionStatement.withSemi.strict(ir.callExpression.strict({
					function: ir.identifier("call"),
					arguments: ir.arguments.strict(ir.argumentsElements.strict({ delimiter: Delimiter.None }, {
						attributeItem: [ir.attributeItem.strict(ir.attribute.input.strict({
							path: ir.identifier("cfg"),
							arguments: ir.delimTokenTree.paren.strict(ir.identifier("a")),
						}))],
						expression: ir.identifier("x"),
					}, {
						expression: ir.identifier("y"),
					})),
				}))],
			}),
		})],
	});
}

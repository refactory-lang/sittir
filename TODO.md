2. Get rid of all unused imports in the code. Run oxlint --fix on the code to remove unused imports and fix formatting issues. Run oxfmt . on the code to fix formatting issues.
4. Remove all calls to deprecated functions in code, and remove the deprecated functions themselves.
5. Remove all calls to js engine in code, and remove the js engine itself. (VERIFIED PARTIAL: createEngine() is native-only and the js engine is gone, but the GENERATED index.ts still re-exports `createRenderer` from @sittir/legacy-core — emitters/index-file.ts:40 — plus 7 tool/validator importers; the generated re-export is the concrete remnant to remove.)
6. Remove any silently deprecated features in code, and remove the deprecated features themselves.
7. Remove any silent failures in code, and remove the silent failures themselves. Remove any silent warnings in code, and remove the silent warnings themselves.
10. Remove references to "VARIANT" type in code, and remove the VARIANT type itself.
11. DONE: generated `wrap.ts` type errors are gone. The `SliceGroup1`/`Yield` missing-property class was closed by the order-independent supertype-union fix. The `TSKindId.CloseParen`/`Oror`/`_PropertyIdentifier` class needed the catalog's `findKindEntry` kind-name chain (exact key → parser symbolName → anon-scoped literal → hidden-named fallback): an exact-key catalog find misses entries reached through parser-symbol/literal aliases, so member names fell back to model typeName and referenced nonexistent enum members. Every `_wrapTable` key and `$type` stamp now resolves through that one chain, and hidden/alias pairs that resolve to the SAME catalog entry are deduped by a claim policy (the entry's own kind — the canonical storage kind — wins the dispatch id). All three packages pass `tsc -p tsconfig.build.json` with declaration emit, verified with the `tsbuildinfo` removed first (the factories' explicit `*Built` return types cured the companion TS7056 class; their `_NodeMethods` `$trivia` tail derives from grammar trivia roles so rust's `BlockComment | LineComment` surface matches `withMethods`).
12. Consolidate all single-phase methods (functions only ever called by one compiler phase — verify with infigraph's `find_all_references`/`trace_callers`, not grep) into the phase file they belong to. Use lsproxy (not manual edit) for the actual move. After each move, confirm the moved function is NOT exported from its new home unless something outside that phase genuinely still needs it.
13. NEW: Please see field override logic... doesn't appear to change name of existing fields (cf. attempt to change Parameter field _pattern to _name (from value in base grammar). )
13. NEW: Make examples realizable Refer to TODOs in example code
14. NEW: collapse logic for "unnamed" children vs. "named" children (from factory surface PoV), broadly simplify emitter logic. 
15. NEW: Refactor generated manifest logic to distinguish between native impacting codegen changes, and js impacting changes. js-only changes should not require trigger native rebuild. (Base on changes to generated output and/or core source, not changes to codegen source)
16. NEW: Consider using polymorph shape (perhaps resurrecting AssembledPolymorph as a subclass of AssembledBranch) to distinguish between top-level sequences and top-level choices (non-supertype), which permit distinct emissions for each choice arm. Consider collapsing AssembledGroup into branch.  Finally, consider using node.hidden as ssot for whether a factory emitted and SlotClass as ssot for emitting branch factories (getting rid of multiple varying interpretations of the same branch)
17. DONE: public_field_definition quintet collapsed to ONE kind with flat optional marker slots (declare/static/readonly/abstract/accessor booleans + accessibility_modifier/override_modifier node slots) — the permutation arms no longer mint (isPermutationChoice decline in enrich), the wrap-arity error class is gone (each marker singular), and the modifier stack renders in canonical flat order. Markers are per-slot booleans rather than one bitflag: the modifier set interleaves node members (accessibility_modifier, override_modifier), which the keyword-presence taxonomy excludes from bitflag packing — method_definition's flat marker shape is the precedent. Sweep across all three grammars found no other permutable-modifier split (macro_definition arms co-vary paired delimiters; with_clause arms differ structurally; _for_header arms differ at the kind literal AND the trailing member).
18. NEW: Port the exercise tool's render step off @sittir/legacy-core to the native engine (see `ki-exercise-legacy-renderer`).
19. NEW: Post-generate inline-integrity assertion — every wired inline: name must exist in the final rules (see `ki-inline-integrity-check`).
20. NEW: Triage the 18 dependabot vulnerabilities (13 high) on master.

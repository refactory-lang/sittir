node_modules/
dist/
build/
coverage/
.coverage/
*.log
.DS_Store
Thumbs.db
*.tmp
*.swp
.vscode/
.idea/
.env
.env*
.env.local
.env.*.local
.nyc_output/
.pnpm-debug.log*
*.tsbuildinfo
.claude/settings.local.json
# infigraph's local code-graph cache/index (embeddings, kuzu db, session logs)
.infigraph/
.claude/scheduled_tasks.lock
# `.sittir/` is the per-grammar generated-output staging area. Its CONTENTS
# are ignored, EXCEPT the well-known tracked artifacts un-ignored below — so
# they're first-class tracked (no more `git add -f`, no "ignored" warnings,
# `git status`/`git add` just work):
#   - grammar.js (tree-sitter grammar entry)
#   - package.json (codegen-emitted package manifest)
#   - tree-sitter.json (tree-sitter metadata)
#   - generated.manifest.json (codegen output fingerprint — see
#     packages/codegen/src/scripts/generated-manifest.ts)
# To track a new .sittir artifact, add a `!` negation below (must ignore the
# dir CONTENTS via `/*`, not the dir, or negations inside can't take effect).
packages/*/.sittir/*
!packages/*/.sittir/grammar.js
!packages/*/.sittir/package.json
!packages/*/.sittir/tree-sitter.json
!packages/*/.sittir/generated.manifest.json
!packages/*/.sittir/grammar-diagnostics.json


# Stray tree-sitter build artifacts (parser.wasm should be in .sittir/)
/tree-sitter-*.wasm
.worktrees/
scratch/

# Rust workspace (spec 012)
/target/
Cargo.lock

# napi-rs build outputs (spec 012 T036-T038): platform-specific .node
# binaries. Each developer rebuilds locally via
# `cd rust/crates/sittir-{lang} && napi build`.
rust/crates/sittir-*/*.node
.claude/worktrees/
tmp-scripts/
graphify-out/cache/**

# vitest JSON reporter side-artifact (scripts/test-and-record.ts)
/.vitest-report.json

# tsc build outputs accidentally landing alongside .ts source. These four
# packages have ZERO intentionally-tracked .js/.d.ts in src/ (verified
# 2026-05-15) — pure TS source. (packages/codegen and packages/validator
# DO track .js/.d.ts intentionally and are NOT covered by these rules.)
packages/common/src/**/*.js
packages/common/src/**/*.js.map
packages/common/src/**/*.d.ts
packages/common/src/**/*.d.ts.map
packages/legacy-core/src/**/*.js
packages/legacy-core/src/**/*.js.map
packages/legacy-core/src/**/*.d.ts
packages/legacy-core/src/**/*.d.ts.map
packages/types/src/**/*.js
packages/types/src/**/*.js.map
packages/types/src/**/*.d.ts
packages/types/src/**/*.d.ts.map
packages/tools/src/**/*.js
packages/tools/src/**/*.js.map
packages/tools/src/**/*.d.ts
packages/tools/src/**/*.d.ts.map

# Local cargo build tuning (e.g. lld linker) — per-machine, requires lld installed
.cargo/config.toml
**/.DS_Store

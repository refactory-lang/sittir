# Feature Specification: Nested-Supertype Alias Materialization

**Feature Branch**: `026-nested-supertype-alias-materialization`
**Created**: 2026-07-26
**Status**: Implemented (2026-07-27) — rust acceptance criteria met in full; see Progress below for the ts/py follow-on scope discovered along the way
**Input**: Follow-up from `variantrule-retype-cut` / PR #179 — landed alongside commit `ca27502a6` ("fix(evaluate): stop alphabetizing buildRuleCatalog's rule order"), which fixed the *naming* half of this bug. This spec covers the remaining *node-registration* half.

## Progress (2026-07-26)

Implemented and verified zero-regression:

- `assemble()`'s main loop (assemble.ts) gained a post-pass that walks every `SUPERTYPE` rule's `subtypeParseNames`, and for each entry whose target (`subName`) is *itself* classified `SUPERTYPE`, registers a real `AssembledSupertype` node under the alias name (`token_pattern_group1`), reusing the nested rule's own resolved subtypes. Guarded to skip non-SUPERTYPE targets (the `_primitive_type → primitive_type` ENUM case stays on the old flatten-through path, per the Non-Goals below).
- `resolveHiddenSubtypes` now walks a SUPERTYPE rule's own `subtypes` list directly (not `resolveHiddenRuleContent`'s fully-flattened output) and substitutes the alias name for nested-SUPERTYPE members, so parents correctly reference `token_pattern_group1` instead of flattening through it.
- Result: rust `read-render-parse` Pass 107→108 (`AstMatchPass` steady at 100), zero regressions across `validate:native` (ts/py byte-identical) and `pnpm vitest run` (37 pre-existing failures, byte-identical FAIL-line set before/after).
- **"Macro invocation - arbitrary tokens" now passes fully** (was previously `unknown kind id` crash).
- **"Attribute macros" still fails** — but this is NOT a regression: it was already failing on the ordering-fix-only baseline (with a different error, `unknown kind id 170`); post-this-fix it fails with a different error instead (`Missing field _content` on `DelimTokenTreeTransport._content`), same FAIL status throughout.

**Newly discovered, separate, deeper bug — root-caused and FIXED (2026-07-27)**: dispatched a `sittir-research` (fable) agent to diagnose the "Attribute macros" residual failure precisely. It falsified my initial "adjacency" framing (a composite element *before* the alias failed identically to one *after* — position is irrelevant) and found the true mechanism: the native reader collapses a node whose children are all anonymous tokens (e.g. a `token_pattern_group1` occurrence wrapping a bare `'`) into a text-only leaf with no named child and no `$other` bucket. `emitTransparentSupertypeWrap` (packages/codegen/src/emitters/wrap.ts) required a named child unconditionally and threw on that shape. Separately, `resolveWrappedStorageValue` (packages/tools/src/validate/common.ts) silently caught that throw and degraded the *entire* array slot to raw stubs — including perfectly-healthy sibling elements — which is why the error pointed at an innocent neighboring `token_tree` instead of the actual failing element, creating the adjacency illusion.

**Fix implemented**: (1) `emitTransparentSupertypeWrap` now falls back to treating the node itself as the resolved member when both the kind-keyed and `$other` probes come up empty and the node carries `$text` (a reader-collapsed leaf) — the transport side already accepted this bare-leaf shape (`Verbatim`/token-literal variants), only the wrap layer refused it. (2) `resolveWrappedStorageValue`'s accessor-throw diagnostic is now unconditional (was gated behind `SITTIR_VALIDATOR_DUMP_ACCESSOR_THROW`) so a masked slot is visible without needing to know to set that env var — the underlying fallback-to-raw-stubs behavior is left unchanged (lower-risk than rethrowing; the masking is a validator-diagnostics concern once the actual wrap bug is fixed, not something to change functionally without further dedicated investigation).

Result: rust `read-render-parse` Pass 108→109/136 (AstMatchPass steady at 100) — **both target cases now pass fully**: "Macro invocation - arbitrary tokens" and "Attribute macros". Verified zero regressions: typescript/python byte-identical to baseline, `pnpm vitest run` shows the same 37 pre-existing failures (22 files/29 tests) before and after.

**Confirmed applicability to typescript/python** (from the same agent's investigation, using a live accessor-throw probe on `let x!: number;` in typescript): the identical mechanism — an anonymous token (`!`) causing the same reader-collapse → wrap-throw → whole-slot-masking chain — is present in typescript's current rt failures too (ts has 15 "Missing field" render failures, python 10; ts confirmed via direct repro, python plausible-unconfirmed). However, this specific fix (`emitTransparentSupertypeWrap`) only covers the *transparent-supertype-wrapper* code path — the ts/py cases the agent found are in *ordinary* wrap functions (e.g. `wrapVariableDeclarator`'s `_content` slot), a different wrap-emitter code path that needs its own, separate audit for the same "required slot fed only by anonymous tokens" pattern. That audit — and any resulting fix — is now unmasked and inspectable via Part 2's always-on diagnostic, but is out of scope for this spec; treat as a new follow-up (**spec 027**, not yet filed).

The agent also checked whether ts/py have any *nested-supertype* rules that would need `afd2d90d0`'s materialization pass the way rust's `_non_special_token` did: **python has none**. **TypeScript has exactly one** — `_jsx_identifier` (nested inside `_jsx_attribute_name`) aliases to `property_identifier`, an *already-existing visible node name*, not a synthesized `groupN`. If tree-sitter ever routes through that alias at runtime, `afd2d90d0`'s materialization pass would need a collision-guard ("alias name already registered as a node — don't re-register") rather than fresh registration; not yet observed as an active failure, so not actioned.

**Root cause of `token_pattern_group1` fully traced (2026-07-27), and a separate factory/type-surface gap closed as a result.** Live-instrumented `enrich.ts` (a stack-capturing trace inside `makeVisibleGroupAlias`, per the user's "instrument the override" suggestion, then removed) to find exactly which enrich mechanism mints `token_pattern_group1` — the earlier "tree-sitter auto-derives this name" hypothesis was wrong (confirmed by diffing against upstream tree-sitter-rust's own precompiled `parser.c`, which has zero occurrences of the string). The real mechanism: `enrich(base)` runs over the *raw upstream* grammar in `overrides.ts` (`const enrichedBase = enrich(base)`), before any `transform()` override applies. At that point, `_non_special_token`'s body still has its *original* shape — position 7 is a bare `REPEAT1(CHOICE(...41 punctuation chars...))`, not yet replaced by the `_token_tree_punctuation` alias. `isSupertypeLike`'s test (`group-classify.ts`) requires every choice member to be SYMBOL/STRING/named-ALIAS; the raw `REPEAT1` fails that, so at enrich-time `_non_special_token` reads as structural (not a pure dispatch union) rather than as the declared-supertype-adjacent shape `mintStructuredChoiceArm`'s `isSupertypeLike(body)` gate exists to protect (the documented "python `_compound_statement` → 0/115" incident). So enrich mints it a real, tagged alias (`metadata.author: 'enrich'`) via `promoteExistingHiddenRuleName` → `makeVisibleGroupAlias`, matching the *original*, still-valid motivation on that rule (the punctuation run was otherwise unaddressable and silently dropped on render). link.ts does **not** independently predict this name (no duplicated logic to fix there) — `collectSubtypeNames` just reads the alias's `value` field directly off the already-minted rule tree node, identically to how it reads `_token_tree_punctuation`. Once the *later* override replaces the punctuation repeat1 with a clean alias, the bare `'`/keyword members simply ride along under the same already-minted `token_pattern_group1` wrapper — and tree-sitter's compiler still fuses that wrapper into a zero-child leaf when the matched alternative is one bare anonymous token, which is exactly what the Part 1 wrap.ts fix (above) handles. No enrich or link change was needed or made.

Separately, verified the **factory/type surface has a real, independent gap**: `Tokens`'s union type (`types.ts`) had zero representation for any of `_non_special_token`'s 29 bare-literal members (`'` plus 28 reserved keywords) — invisible to the type system, no factory function, no `from.ts` (FromNapiValue) handling. `TokenPatternGroup1Kind`'s enum acknowledged `'` (tagged `TokSq`) but had no corresponding type member for it, and no entries at all for the 28 keywords. Fixed by extracting the 28 keyword literals (positions 9-36 of `_non_special_token`'s upstream 37-member choice) into a new named rule, `_token_keywords`, referenced via a single `$._token_keywords` symbol instead of 28 flat bare-string arms (`packages/rust/overrides.ts`). Deliberately left `'` alone — it still hits the text-leaf-fusion path fixed in Part 1, and folding a single punctuation character into a "reserved word" enum would mix two unrelated cases for no benefit. Because `_token_keywords`'s own body is *pure* bare-STRING members with no symbol refs, `classifyHiddenChoiceRule`'s enum-admission check (checked before supertype classification) accepts it as an **ENUM**, not a nested SUPERTYPE — giving it the same well-supported type/factory/from-napi path `_token_tree_punctuation` already has. Confirmed via generated output: `Tokens` now includes a `TokenKeywords` member, `TokenPatternGroup1Kind` gained a `TokenKeywords` entry, `types.ts` gained a proper `TokenKeywords = Terminal<TSKindId.As | TSKindId.Async | ...>` type, and `factories.ts`/`from.ts` both gained real construct/read support (`buildTokenKeywords`). Verified zero regressions: rust `read-render-parse` steady at 109/136 (AstMatchPass 100, unchanged), fixture/kind counts identical to baseline (1045/80), `validate:native` byte-identical across all three grammars, `pnpm vitest run` byte-identical to the same 37-failure (22 files/29 tests) baseline. The pre-existing, unrelated `token_tree` fusion bug (`Missing field _content` on `MacroInvocationTransport._token_tree` for all-bare-keyword token-tree contents, e.g. `foo!(as, async)`) was checked and confirmed identical on the pre-change baseline — not something this change touches; a candidate for a future investigation, not filed as its own spec yet.

**Apostrophe also fixed at the grammar level (2026-07-27), and confirmed the wrap.ts fallback stays.** Following up on the `_token_keywords` extraction, gave `'` (position 8, deliberately left alone at the time) the same treatment: a new `_token_pattern_quote: ($) => "'"` rule, aliased in explicitly, replacing the bare literal. Confirmed via `probe-kind` on the original repro that this produces a real, non-fused child chain (`token_pattern_group1` → `token_pattern_quote` → `'`) with zero wrap errors. Verified zero regressions: `validate:native` byte-identical across all three grammars, rust rt Pass steady at 109/136. Note: `_token_pattern_quote`'s body is a single bare STRING (not a choice), so it doesn't hit `classifyHiddenChoiceRule`'s ENUM-admission path the way `_token_keywords` did — it classifies `modelType=token` instead, and (like the pre-fix state) gets no `factories.ts`/`from.ts` support and no member in `TokenPatternGroup1`'s type union. Smaller version of the same factory-surface gap; not fixed here.

With both remaining rust triggers fixed at the grammar level, tested directly (by temporarily making the wrap.ts fallback throw instead of return, regenerating+rebuilding all three grammars, then running both `validate:native` and the full `vitest run`) whether the fallback branch in `emitTransparentSupertypeWrap` is now dead code. **It is not** — `validate:native`'s corpus never hit it (no throws), but the full `vitest` suite did, once. **Correction to the initial attribution**: the throw fired for `_PropertyIdentifier`, not `_jsx_identifier`/`property_identifier` as first assumed (the two are unrelated — `_jsx_identifier`'s own subtypes, `["jsx_identifier", "identifier"]`, are both already-visible node kinds and were never actually at risk). The real trigger, traced via `classify`: `_property_identifier` (`modelType=supertype`, subtypes `["identifier", "_reserved_identifier"]`) nests `_reserved_identifier`, materialized as `statement_identifier_group1` via the same `afd2d90d0` mechanism as rust's `token_pattern_group1`.

**`_reserved_identifier` also fixed at the grammar level (2026-07-27), following the same pattern.** `_reserved_identifier`'s upstream shape — `(_, previous) => choice(...18 TS-specific bare strings, previous)`, where `previous` (the base JS grammar's own `_reserved_identifier`: `get`/`set`/`async`/`static`/`export`/`let`) is left as a **nested** sub-CHOICE rather than flattened — blocks `classifyHiddenChoiceRule`'s ENUM admission the exact same way rust's raw `REPEAT1` did (specs/026's root-cause trace above), leaving it `modelType=pattern` (unclassified, "mixed/structural — survive as-is") and inlining directly into `_property_identifier`'s occurrence with no node of its own. Fixed by overriding `_reserved_identifier` in `packages/typescript/overrides.ts` to flatten that nested sub-choice programmatically (reads `original.members`, spreads the last member's own `.members` in if it's a `CHOICE`, rather than hardcoding the string list — stays correct if upstream's own list ever changes). Confirmed via `classify`: `_reserved_identifier` now `modelType=enum`; `types.ts` gained a proper `ReservedIdentifier = Terminal<TSKindId.Declare | ...>` type and real factory support (auto-generated `nodes.test.ts` fixtures updated from the generic `'test'` placeholder to a real enum value `'declare'`, confirming the type actually took effect). Verified zero regressions: `validate:native` byte-identical across all three grammars, full `vitest run` back to the same 37-failure (22 files/29 tests) baseline (one hardcoded smoke-test assertion in `generate.test.ts` needed updating — it asserted the now-superseded `TokSq = "'"` string; updated to assert `TokenKeywords`'s new type instead).

**`_token_pattern_quote` superseded — folded into `_token_keywords` instead.** `'` is technically a lifetime token, close enough kin to the reserved words to just share their choice rather than get its own standalone rule — and doing so closes the smaller factory-surface gap `_token_pattern_quote` left behind (no type/factory/from-napi support, since a lone bare-STRING rule body doesn't hit the ENUM-admission path a multi-member bare-STRING choice does). `_token_keywords` is now `choice("'", 'as', 'async', ..., 'while')`, 29 members; `_token_pattern_quote` is removed. Confirmed via generated output: `TokenKeywords`'s `Terminal<...>` type now includes `TSKindId.Squote`, with full factory/from-napi support. Verified zero regressions: `validate:native` byte-identical, rust rt Pass steady at 109/136.

**Conclusion on the wrap.ts fallback**: it stays. It is not rust-specific dead code — it is the general, correct handling for tree-sitter fusing *any* nested-supertype alias occurrence into a zero-child leaf when its matched content is a single bare anonymous token, and that shape recurs independently across grammars. Both known triggers (rust's `_non_special_token`, ts's `_reserved_identifier`) are now fixed at the grammar level, but the fallback remains the right safety net for any future/undiscovered case of the same shape, in these or other grammars. Reverted the throw instrumentation and restored normal generated output before each commit in this line of investigation.

## Progress (2026-07-28) — rrp break sweep: fixes landed + remaining root-caused classes

Sweep of the remaining read-render-parse AST-match breaks across all three grammars (worktree `variantrule-retype-cut`). Baseline at sweep start: rust rrp 109/136 (astMatch 100), ts 84/111 (63), python 71/115 (41); vitest 25 failed files / 26 failed tests (note: the "22 files/37 tests" figure quoted in earlier progress notes above is stale).

**Fixed this sweep** (each verified via `validate counts` + full `vitest run`, zero new failures):

1. `collectFixedLiteral` multi-member-SEQ fixed text (codegen, node-map.ts) — python `'not in'`/`'is not'` rendered empty from the bare-kind-id wire path, breaking the whole "Comparison operators" corpus entry. Same latent hole closed for rust `_reference_expression_raw_const`/`unit_expression`/`unit_type` and ts `_meta_property_group1/2`.
2. python `case_as_pattern` — upstream puns TWO different rules onto the `as_pattern` kind (`_as_pattern` = `seq(case_pattern,'as',identifier)` aliased at `case_pattern` arm 0, vs the expression-context `as_pattern`); the reader used the expression shape and threw on every case-context occurrence. Real visible rule per the `case_tuple_pattern` precedent.
3. python `comprehension_clauses` — hidden mandatory-seq-member helper never populated its singular slot (no `optional(...)` reference site exists, so the Track-B content-alias mint can never fire for it). Real visible rule; body flattened to `repeat1(choice(...))` because the base `seq(first, repeat(...))` shape derives two slots that the reader can only fill as one (the generated render fn fed both template slots the same buffer → every clause duplicated on deep render).
4. python `import_from_statement` — flat `3: field('wildcard_import')` broadcast over the whole position-3 choice, stamping the parenthesized arm's anonymous `(`/`,`/`)` tokens; slot filtered them → throw. Path-scoped to `'3/0'`.
5. rust `last_match_arm` trailing `,` — pos-4 `optional(',')` was an uncaptured anonymous token, silently dropped on render (3 corpus AST mismatches). `'4/0': field('comma')`.

Result after sweep: python rrp 76/115 (astMatch 44, errors 43→38, from 107/120, cov 124/126); rust/ts numbers in the final validation rows of `packages/tools/validation-history.jsonl`.

**Remaining break classes, root-caused but NOT fixable at the overrides level** (documented here so they stop being re-diagnosed):

- **Ordinary-wrap "required slot fed only by anonymous tokens"** — the exact follow-up this spec's Part-1 note already predicted ("spec 027", still unfiled). The Part-1 fallback fix covered ONLY `emitTransparentSupertypeWrap`; ordinary wrap fns and polymorph content dispatch still require a named child. Confirmed instances: ts `variable_declarator` on `a!: b` (the enrich-minted `_variable_declarator_group1` hidden group materializes nothing when its occurrence content is the bare `!`; `singular slot "content" … got undefined`), rust `range_expression` bare `..` (polymorph `content` undefined — the `range_expression_bare` variant arm is all-anonymous), rust `_visibility_modifier_group1` on `pub(in crate::d)`. Fix belongs in `packages/codegen/src/emitters/wrap.ts`'s ordinary wrap emitters (same bare-leaf fallback the transparent-supertype path already has), NOT in overrides — a grammar-side rewrite would have to invent named wrappers for single anonymous tokens across dozens of rules.
- **rust `_let_chain` singular `right` slot** — upstream models let-chains via hidden LEFT RECURSION; tree-sitter flattens a chain of N `&&` arms into N same-level children, but sittir derives a singular `right` slot (`received 3 values`). Not override-fixable: rewriting the left recursion as `seq(first, repeat1(seq('&&', …)))` changes the LR interplay with `binary_expression`'s own `&&` precedence — high parser-divergence risk. Fix: assemble-level modeling of flattened left-recursive occurrences as repeated slots.
- **rust `string_literal` byte-/C-string prefixes** (`b"…"`/`c"…"` render as `"…"`, 1 corpus mismatch) — the opening token is `alias(/[bc]?"/, '"')`; slot classification keys off the ALIAS display string and mints a fixed `dquote` TERMINAL (presence-boolean wire), so the actual `c"` text never reaches the transport. Field-promoting the position was tried (2026-07-28) and does not help (see the comment in `packages/rust/overrides.ts`). Fix: classification must treat alias-of-PATTERN-whose-regex-is-not-the-alias-string as content-bearing.
- **ts `call_expression` / `instantiation_expression` `content` undefined** (`bar<T>()`, `import('x')`, `f<T>`) — upstream ships the variant split as `choice(alias(_call_expression_call, …), …)` pre-split alias arms; the `.claude/skills/sittir-transforms` skill (gotcha #7) already documents why no clean override-only fix exists (needs readNode field-hoisting + `AssembledGroup.renderParts` changes).
- **ts `public_field_definition` modifier runs** (`static readonly`, `readonly abstract` → `singular slot … received 2 values`) — the 4-arm modifier choice's polymorph arms each contain a seq of multiple optional modifier tokens, but the variant-arm slot is modeled singular. In-principle fixable by splitting the arms further in overrides, but that block is already entangled with the `inline:` LR-conflict workaround (see the long comment at `packages/typescript/overrides.ts` ~line 232) — deferred rather than risked blind.
- **ts `_export_statement_type_export` ASI semicolon** (`export type { UserType }` with no `;`) — the required singular `semicolon` slot is fed by `$._semicolon = choice($._automatic_semicolon, ';')` where `_automatic_semicolon` is a zero-width external token; at deep read the slot comes up undefined even though the override grammar surfaces a visible empty-text `automatic_semicolon` node. Same requiredness-vs-zero-width modeling gap class as the ordinary-wrap item above.
- **python Track-B promoted list kinds render empty** (`parameters`→`()`, `tuple_pattern`→`()` under lambda, list/tuple `childCount 3 ≠ 2` cluster — the largest remaining python mismatch class) — the content-alias visible-group slot-population gap already tracked in project memory ("Content-alias visible-group state — floor holds; slot-population gap remains"): deep wrap produces the `_patterns`/`element_list` node with its elements, but the transport receives an empty list. Not re-root-caused past that point this sweep.
- **python `block` ≠ `simple_statements` / MISSING-newline cluster** — compound-statement bodies render single-line so trailing statements escape the suite; interacts with the documented `alias_sym_*` vs `sym_*` kind-id gap for aliased `_suite` bodies (docs/KNOWN_ISSUES.md last entry).

## Summary

When a hidden rule is itself a nested `SUPERTYPE` (a supertype whose own subtype is *another* supertype — e.g. rust's `_non_delim_token`, whose subtype `_non_special_token` is a further supertype), and that nested rule materializes as a real, aliased node in tree-sitter's compiled parser (confirmed via `grammar.json`), sittir's own pipeline has no mechanism to register a corresponding node for it. `resolveHiddenSubtypes` (assemble.ts) currently flattens straight through the nested supertype to its eventual leaf kinds instead — which is *usually* harmless (tree-sitter also flattens in the common case) but is silently wrong for the subset of nested supertypes tree-sitter aliases into their own intermediate node. The result: content at that tree position is duplicated/corrupted at render time (confirmed for rust macro-invocation arguments — `foo!(a, b)` rendering as `foo!()`, and worse).

Landing the naming fix alone (commit `ca27502a6`) already resolves the *majority* of this bug's damage: 15+ rust test cases move from silently-duplicated content to byte-exact AST match. Two cases (`Attribute macros`, `Macro invocation - arbitrary tokens`) instead move from silently-wrong to a loud render crash (`unknown kind id N in DelimTokensTransport`), because attempting the correct fix — substituting the real alias name instead of flattening — hits a wall: **nothing in sittir's pipeline ever synthesizes an actual node for the alias**, so downstream emitters (`emitSupertypeUnionDeclarations` in types.ts, and presumably the Rust-side kindId catalog / wrap dispatch table) have nothing to reference.

## Problem Statement

### The concrete case (rust)

- `_non_special_token` is a hidden `SUPERTYPE` rule, referenced as a subtype from three different parents: `_tokens`, `_non_delim_token`, `_token_pattern`.
- Tree-sitter's real compile aliases this occurrence to a genuine, named CST node: `token_pattern_group1` — confirmed via `grammar.json`, which shows the alias at three positions: `_token_pattern/members[4]`, `_tokens/members[3]`, `_non_delim_token/members[0]`.
- As of commit `ca27502a6`, sittir's own `link()` phase correctly computes this SAME name (`_non_delim_token.subtypeParseNames = { "_non_special_token": "token_pattern_group1" }`, verified via the `classify` CLI tool) — the naming divergence that caused this to previously read `non_delim_token_group1` (wrong) is fixed.
- But `resolveHiddenSubtypes` (assemble.ts:823-905) still flattens `_non_delim_token`'s reference to `_non_special_token` straight through to leaf kinds (`string_literal`, `identifier`, `mutable_specifier`, `self`, `super`, `crate`, ...) via `resolveHiddenRuleContent`, discarding the alias entirely. This happens to produce a passable (if occasionally lossy) result for the *common* case, but is flatly wrong when tree-sitter's real compile inserts a genuine intermediate node — which the flattened view has no way to represent.

### What was tried and why it's incomplete

During investigation (this session), the following fix was drafted and reverted:

1. In `resolveHiddenSubtypes`'s `visit()`, when `rule.type === SUPERTYPE`, walk `rule.subtypes` directly (its own un-flattened list) instead of `resolveHiddenRuleContent`'s fully-flattened output.
2. For each direct subtype with an entry in `rule.subtypeParseNames`, substitute the alias name instead of recursing/flattening — but **only** when the referenced member is *itself* classified `SUPERTYPE` (an ENUM-shaped hidden rule with its own parse-name alias, e.g. `_primitive_type → primitive_type`, needed to keep flattening — see Non-Goals).

This correctly identifies *when* substitution should happen. But regenerating rust with this change throws:

```
Error: types: supertype '_delim_tokens' references subtype 'token_pattern_group1' which is not in NodeMap.
    at emitSupertypeUnionDeclarations (packages/codegen/src/emitters/types.ts:871)
```

Confirmed via the `classify` CLI tool: `token_pattern_group1` has no entry in `nodeMap.nodes` anywhere in sittir's own model. Enrich's clause-hoist promotion (`promoteExistingHiddenRuleName`, `dsl/enrich.ts`) computes the *name* tree-sitter will use, but nothing downstream of `link()`/`assemble()` ever mints an actual node, kindId, or Rust-side transport/dispatch-enum variant for it.

## Goals

- Given a nested-supertype rule (`_non_special_token`) that tree-sitter's real compile aliases into a distinct intermediate CST node (`token_pattern_group1`) at one or more parent occurrences, sittir's pipeline should:
  1. Register a real node for the alias in `nodeMap.nodes` (assemble phase).
  2. Give that node a `subtypes` union equal to the nested rule's own subtypes (recursively resolved the same way any other supertype's subtypes are — i.e. this node behaves exactly like any other supertype node, just reached via an alias rather than a bare hidden name).
  3. Emit the appropriate Rust-side kindId entry and dispatch-enum variant (transport.rs / kind_ids.rs) so a CST node arriving under that real compiled kind-id round-trips correctly instead of throwing "unknown kind id".
  4. Parents that reference the nested supertype (`_delim_tokens` via `_non_delim_token`, `_tokens`, `_token_pattern`) should list the alias (`token_pattern_group1`) as their resolved subtype, not the flattened leaf set.
- Fix should generalize: any nested-supertype-with-a-real-compiled-alias case, not just rust's `_non_special_token` — this pattern is plausible (if not yet observed) in typescript/python too.
- Zero regressions across the `validate:native` gate and the unit test suite for all three grammars, matching the discipline already established (this repo's baseline is VALIDATOR-RELATIVE — net win with zero *true* regressions, not raw-count chasing).

## Non-Goals

- **ENUM-shaped hidden rules with their own parse-name alias** (e.g. `_primitive_type`, an all-STRING `CHOICE` classified `modelType=enum`, aliased to `primitive_type` at its `_non_special_token` occurrence) are explicitly out of scope for this materialization. These do not get a separately-registered node in the same way — `primitive_type` is not currently registered as a standalone node either, and (unlike the nested-SUPERTYPE case) the existing flatten-through behavior for ENUMs has not been shown to cause the same class of content-loss bug. Confirm this assumption during implementation (open question below) rather than assuming it's identical to the SUPERTYPE case.
- **Auditing/fixing every other possible nested-supertype occurrence** across all three grammars is out of scope for the initial landing — ship the mechanism generally, but the validation gate (not an exhaustive manual audit) is the acceptance signal for "did this regress or improve anything else."
- **Re-litigating the ordering fix** (commit `ca27502a6`) — that fix is landed and stable; this spec is purely about the residual node-registration gap it exposed.
- **The already-landed `loadGrammarJsonAliasMap` infrastructure** (`inline-sets.ts`, `AssembleCtx.grammarJsonAliasMap`) is inert as of `ca27502a6` (the ordering fix made sittir's own `subtypeParseNames` agree with grammar.json's real names, so the read-back is no longer strictly necessary for the naming half of this bug). Whether it's still useful as a *validation cross-check* (assert sittir's computed name matches grammar.json's, and diagnose loudly if they ever diverge again) or should be removed as now-dead weight is an implementation-time call, not pre-decided here.

## Investigation Trail (for the implementer)

- `packages/codegen/src/compiler/assemble.ts` — `resolveHiddenSubtypes` (~line 823) is the exact site; its current comment block ("KNOWN GAP (not yet fixed)") documents this precise investigation and the drafted-then-reverted fix shape.
- `packages/codegen/src/types/rule.ts:525-546` — `SupertypeRule.subtypeParseNames` doc comment; confirms the field is stamped per-rule by `classifyHiddenChoiceRule` (link.ts) and is correctly populated post-`ca27502a6`.
- `packages/codegen/src/emitters/types.ts:848-880` (approx) — `emitSupertypeUnionDeclarations`, the function that throws "not in NodeMap" — the acceptance test for "is the node registered."
- Rust-side: `rust/crates/sittir-rust/src/render/transport.rs`'s `DelimTokensTransport` enum (and its `FromNapiValue`/kind-id match arms) is the concrete downstream artifact that needs a new variant + kind-id mapping once a node exists.
- `packages/tools/src/probe/` (`probe-kind` CLI tool) and `classify` CLI tool (`pnpm exec tsx packages/cli/src/cli.ts tool classify -g rust --kind <name>`) are the fastest empirical verification loop — used throughout this investigation to confirm rule shapes/subtypeParseNames/nodeMap membership without needing a full native rebuild.
- Reproduction: `pnpm exec tsx packages/cli/src/cli.ts tool probe-kind -g rust --kind macro_invocation --source 'foo!(a, b);'` (post-`ca27502a6`, pre-this-spec's-fix) should show the render succeeding structurally but the two named test cases in `packages/tools/src/__tests__/corpus-validation.test.ts`'s rust corpus ("Attribute macros", "Macro invocation - arbitrary tokens") will fail with `unknown kind id N in DelimTokensTransport`.

## Open Questions

1. Does `_primitive_type`'s alias (`primitive_type`) need the *same* treatment eventually (i.e. is there a latent, not-yet-observed content-loss bug there too), or is the ENUM case genuinely safe to keep flattening? The corpus doesn't currently exercise this path in a way that surfaces a failure, but that may just mean the test corpus doesn't cover it — not that it's safe.
2. Is `token_pattern_group1` (and any sibling cases discovered while implementing this) a **single node reused across all three referencing parents** (`_tokens`, `_non_delim_token`, `_token_pattern`), or could tree-sitter theoretically alias the same hidden rule to *different* names at different occurrences? (Not observed in practice — tree-sitter dedupes identical anonymous content to one shared alias, per the existing `loadGrammarJsonAliasMap` doc comment — but worth a defensive check.)
3. Where's the right place to synthesize the node — a new pass alongside `synthesizeInlineAliasSources`/`synthesizeFieldEnumRules` (evaluate.ts), or inside `assemble()` itself alongside the existing supertype-resolution logic? The former runs earlier and might let `token_pattern_group1` flow through the pipeline exactly like any other rule; the latter is more localized but may need to hand-roll what the earlier passes get for free.
4. Should this synthesis be conditioned on `AssembleCtx.grammarJsonAliasMap` (confirming the alias is real per the compiled grammar) as a safety gate, given that map is already threaded through and inert? Using it as a *gate* (only materialize when grammar.json confirms it) rather than a name *source* (now unnecessary post-`ca27502a6`) may be its best remaining use.

## Acceptance Criteria

- `pnpm run validate:native` shows rust `read-render-parse` Pass ≥ 109/136 (recovering the 2 cases this spec addresses on top of the 107/136 baseline `ca27502a6` leaves behind) with AstMatchPass ≥ 100, and zero new failures relative to that baseline for typescript/python.
- The two named rust corpus cases (`Attribute macros`, `Macro invocation - arbitrary tokens`) pass with byte-exact AST match, not just "no crash."
- `pnpm vitest run` shows no new failures beyond the existing 22-failed-files/29-failed-tests pre-existing baseline.
- `foo!(a, b)` (and other macro-invocation-with-arguments source) round-trips through render without content loss or duplication.

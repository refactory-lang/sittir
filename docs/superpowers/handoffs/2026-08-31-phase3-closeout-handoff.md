# Handoff — phase 3 closed; next: slot-accessor unification, then 3f static spacing (opening prompt for the next session)

Paste from the `---` line down.

---

Phase 3 of the sittir compiler rework is fully merged. `master` is at
`f9f16ebb2`; work from a fresh branch off it (always stack branches).
Session memory: `get_latest_session` (session_2026-08-31). Read
[docs/compiler-phase-glossary.md](../../compiler-phase-glossary.md) before
touching the pipeline.

## What phase 3 landed (all on master)

The builder/overlay stack, merged in order: phase-typed rule builders →
evaluate constructs through builders → typed builder outputs → assemble
reads the normalized tree → the alias-form flip (link's alias form is
canonical, `aliasedFrom` = SOURCE) → the byte-identity gate → the
catalog-anon gate → node-classes/node-types (3d) → the overlay surface
(3e): namespaced/variant/form constructors live in a generated overlay
module composed into `ir.ts` bundles, hoisted kinds dissolve into forms of
their parent, VARIANT/GROUP left the rule vocabulary, and core emitters no
longer read `hoisted`. On top: the review-triage fixes across the stack,
and the from() validator identity fix — read-node identity resolves
through storage kinds (`KIND_NAMES`), never the parse-face display map,
closing the last typescript from() divergence.

## Gates at master (exact — ratchets only tighten)

- `pnpm run validate:native` at floor: coverage 208/208 · 194/194 ·
  142/142; factory-render-parse 1519 · 1202 · 1390; read-render-parse
  134/137 · 112/114 · 115/116 (the three shortfalls are pre-existing
  floors); from() 149/149 · 145/145 · 126/126 (all clean — the ts
  `member_expression` case is CLOSED).
- Fixtures 1500 · 1486 · 1361.
- Codegen unit suite: 15 pre-existing failures; tools suite: 21
  pre-existing failures (isolate/CLI-harness/e2e-generate classes, incl.
  the `_keepModelledSlots` synthetic-wrap-module harness gap); tools tsc
  1 pre-existing. Baseline-diff by test NAME against a master run before
  attributing any failure.
- `pnpm run type-check`: treat the current master count as the baseline —
  measure it first, ratchet down only.

## Next work, in order

1. **Slot-accessor unification.** Fold `fields` / `slots` /
   `structuralFieldsOf` / `allSlotsOf` into ONE accessor — every slot is
   named after assemble, so the split no longer encodes a real
   distinction. This is a byte-identical refactor: regenerated packages
   must not change (the byte-identity invariant is the ONLY gate;
   internal unit tests get updated, never preserved). No spec needed.
2. **3f — normalize static spacing.** Per the master step sequence
   ([2026-08-28-assemble-off-the-simplified-tree.md](../specs/2026-08-28-assemble-off-the-simplified-tree.md)):
   relocate the already-computed static-spacing fact (baked at emit time
   since the static-seam-bake landed) into the tree as a stamped member,
   bounded by the seam census, plus diagnose why ~92–95% of boundaries
   classify runtime-varying (suspected over-conservative classifier).
   Byte-identical: 3c and 3f may not move generated bytes. Follow-on
   slice, ruled in after the relocation lands: the RawWriter downgrade —
   render fns whose seams are all statically decided take RawWriter,
   SpacingWriter survives only at dynamic seams. That slice's gate is
   behavioral: generated render modules may change, rendered output may
   not (validate floors exact). The full custom-sink end-state
   (mark_adjacent dissolution, coordinates in-band) stays with the
   text-content/provenance spec
   ([2026-08-26-text-content-vs-source-provenance.md](../specs/2026-08-26-text-content-vs-source-provenance.md)).

## Known opens (debt register — fix at the root or leave; never mask)

- Three read-render-parse floor shortfalls (rust 134/137, ts 112/114,
  python 115/116) — pre-existing, diagnosed classes in session memory.
- The tools-suite 21 and codegen-suite 15 pre-existing failures — never
  triaged as a body of work.
- Four review threads parked during the stack triage (see the merged
  stack's PR conversations) — judged stale-or-deferred, not fixed.
- Four pre-branch formatter offenders (enrich.ts, enrich-type.ts,
  transpile-overrides.ts, native-node-id.test.ts).
- The open-bug inventory lives in session memory and TODO.md (do not
  commit TODO.md).

## Standing constraints

Commit by pathspec (`git commit -- <paths>`); never commit `TODO.md`,
`examples/*`, `tsconfig.json`, `packages/tools/validation-report.json`
(deliberately dirty; root `incremental:false` is deliberate — never
"fix"). Comments never go in `packages/codegen/src` — rationale lives in
`docs/glossary/<dir>.md`. No spec/plan/PR/task numbers in docs or
comments. Generated outputs are never hand-edited. The manifest
pre-commit hook may trip on python's nondeterministic `.sittir/grammar.js`
reorder — regenerate python, restage, retry. `set -o pipefail` on gate
commands. After remote merges: `git fetch && git branch -f master
origin/master`; never `--delete-branch` on stacked PRs.

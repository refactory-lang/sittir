// Auto-generated by @sittir/codegen — do not edit
import { describe, it, expect } from 'vitest';
import { ir } from '../src/ir.js';

describe('program', () => {
  it('factory produces correct type', () => {
    const node = ir.program({ hashBangLine: { type: 'hash_bang_line', text: 'test' } as any, statements: 'test' as any });
    expect(node.type).toBe('program');
  });
  it('render produces non-empty string', () => {
    const node = ir.program({ hashBangLine: { type: 'hash_bang_line', text: 'test' } as any, statements: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('hash_bang_line', () => {
  it('factory produces correct type', () => {
    const node = ir.hashBangLine('test');
    expect(node.type).toBe('hash_bang_line');
    expect(node.text).toBe('test');
  });
});

describe('export_statement', () => {
  it('form_0 form produces correct type', () => {
    const node = ir.exportStatement.form_0({ declaration: { type: 'declaration', text: 'test' } as any, source: { type: 'export_clause', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('export_statement');
  });
  it('declaration form produces correct type', () => {
    const node = ir.exportStatement.declaration({ declaration: 'test' as any, source: 'test' as any });
    expect(node.type).toBe('export_statement');
  });
  it('declaration2 form produces correct type', () => {
    const node = ir.exportStatement.declaration2({ declaration: 'test' as any, source: 'test' as any });
    expect(node.type).toBe('export_statement');
  });
  it('namespace form produces correct type', () => {
    const node = ir.exportStatement.namespace({ declaration: 'test' as any, source: 'test' as any });
    expect(node.type).toBe('export_statement');
  });
});

describe('declaration', () => {
  it('form_0 form produces correct type', () => {
    const node = ir.declaration.form_0({});
    expect(node.type).toBe('declaration');
  });
  it('function_signature form produces correct type', () => {
    const node = ir.declaration.function_signature({});
    expect(node.type).toBe('declaration');
  });
  it('abstract_class_declaration form produces correct type', () => {
    const node = ir.declaration.abstract_class_declaration({});
    expect(node.type).toBe('declaration');
  });
  it('module form produces correct type', () => {
    const node = ir.declaration.module({});
    expect(node.type).toBe('declaration');
  });
  it('internal_module form produces correct type', () => {
    const node = ir.declaration.internal_module({});
    expect(node.type).toBe('declaration');
  });
  it('type_alias_declaration form produces correct type', () => {
    const node = ir.declaration.type_alias_declaration({});
    expect(node.type).toBe('declaration');
  });
  it('enum_declaration form produces correct type', () => {
    const node = ir.declaration.enum_declaration({});
    expect(node.type).toBe('declaration');
  });
  it('interface_declaration form produces correct type', () => {
    const node = ir.declaration.interface_declaration({});
    expect(node.type).toBe('declaration');
  });
  it('import_alias form produces correct type', () => {
    const node = ir.declaration.import_alias({});
    expect(node.type).toBe('declaration');
  });
  it('ambient_declaration form produces correct type', () => {
    const node = ir.declaration.ambient_declaration({});
    expect(node.type).toBe('declaration');
  });
});

describe('import', () => {
  it('factory produces keyword', () => {
    const node = ir.import();
    expect(node.type).toBe('import');
    expect(node.text).toBe('import');
  });
});

describe('import_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.importStatement({ importClause: 'test' as any, fromClause: { type: 'type', text: 'test' } as any, importAttribute: { type: 'import_require_clause', text: 'test' } as any, semicolon: { type: 'import_attribute', text: 'test' } as any });
    expect(node.type).toBe('import_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.importStatement({ importClause: 'test' as any, fromClause: { type: 'type', text: 'test' } as any, importAttribute: { type: 'import_require_clause', text: 'test' } as any, semicolon: { type: 'import_attribute', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('import_clause', () => {
  it('namespace_import form produces correct type', () => {
    const node = ir.importClause.namespace_import({});
    expect(node.type).toBe('import_clause');
  });
  it('named_imports form produces correct type', () => {
    const node = ir.importClause.named_imports({});
    expect(node.type).toBe('import_clause');
  });
  it('default_import form produces correct type', () => {
    const node = ir.importClause.default_import({ defaultImport: { type: '_import_identifier', text: 'test' } as any, namedImports: 'test' as any });
    expect(node.type).toBe('import_clause');
  });
});

describe('namespace_import', () => {
  it('factory produces correct type', () => {
    const node = ir.namespaceImport({ identifier: 'test' as any });
    expect(node.type).toBe('namespace_import');
  });
  it('render produces non-empty string', () => {
    const node = ir.namespaceImport({ identifier: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('import_attribute', () => {
  it('factory produces correct type', () => {
    const node = ir.importAttribute({ object: { type: 'with', text: 'test' } as any });
    expect(node.type).toBe('import_attribute');
  });
  it('render produces non-empty string', () => {
    const node = ir.importAttribute({ object: { type: 'with', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('statement', () => {
  it('export_statement form produces correct type', () => {
    const node = ir.statement.export_statement({});
    expect(node.type).toBe('statement');
  });
  it('import_statement form produces correct type', () => {
    const node = ir.statement.import_statement({});
    expect(node.type).toBe('statement');
  });
  it('debugger_statement form produces correct type', () => {
    const node = ir.statement.debugger_statement({});
    expect(node.type).toBe('statement');
  });
  it('expression_statement form produces correct type', () => {
    const node = ir.statement.expression_statement({});
    expect(node.type).toBe('statement');
  });
  it('declaration form produces correct type', () => {
    const node = ir.statement.declaration({});
    expect(node.type).toBe('statement');
  });
  it('statement_block form produces correct type', () => {
    const node = ir.statement.statement_block({});
    expect(node.type).toBe('statement');
  });
  it('if_statement form produces correct type', () => {
    const node = ir.statement.if_statement({});
    expect(node.type).toBe('statement');
  });
  it('switch_statement form produces correct type', () => {
    const node = ir.statement.switch_statement({});
    expect(node.type).toBe('statement');
  });
  it('for_statement form produces correct type', () => {
    const node = ir.statement.for_statement({});
    expect(node.type).toBe('statement');
  });
  it('for_in_statement form produces correct type', () => {
    const node = ir.statement.for_in_statement({});
    expect(node.type).toBe('statement');
  });
  it('while_statement form produces correct type', () => {
    const node = ir.statement.while_statement({});
    expect(node.type).toBe('statement');
  });
  it('do_statement form produces correct type', () => {
    const node = ir.statement.do_statement({});
    expect(node.type).toBe('statement');
  });
  it('try_statement form produces correct type', () => {
    const node = ir.statement.try_statement({});
    expect(node.type).toBe('statement');
  });
  it('with_statement form produces correct type', () => {
    const node = ir.statement.with_statement({});
    expect(node.type).toBe('statement');
  });
  it('break_statement form produces correct type', () => {
    const node = ir.statement.break_statement({});
    expect(node.type).toBe('statement');
  });
  it('continue_statement form produces correct type', () => {
    const node = ir.statement.continue_statement({});
    expect(node.type).toBe('statement');
  });
  it('return_statement form produces correct type', () => {
    const node = ir.statement.return_statement({});
    expect(node.type).toBe('statement');
  });
  it('throw_statement form produces correct type', () => {
    const node = ir.statement.throw_statement({});
    expect(node.type).toBe('statement');
  });
  it('empty_statement form produces correct type', () => {
    const node = ir.statement.empty_statement({});
    expect(node.type).toBe('statement');
  });
  it('labeled_statement form produces correct type', () => {
    const node = ir.statement.labeled_statement({});
    expect(node.type).toBe('statement');
  });
});

describe('variable_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.variableDeclaration({ declarators: 'test' as any, semicolon: 'test' as any });
    expect(node.type).toBe('variable_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.variableDeclaration({ declarators: 'test' as any, semicolon: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('lexical_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.lexicalDeclaration({ declarators: { type: 'let', text: 'test' } as any, semicolon: 'test' as any });
    expect(node.type).toBe('lexical_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.lexicalDeclaration({ declarators: { type: 'let', text: 'test' } as any, semicolon: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('variable_declarator', () => {
  it('factory produces correct type', () => {
    const node = ir.variableDeclarator({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('variable_declarator');
  });
  it('render produces non-empty string', () => {
    const node = ir.variableDeclarator({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('statement_block', () => {
  it('factory produces correct type', () => {
    const node = ir.statementBlock({ statements: 'test' as any, automaticSemicolon: 'test' as any });
    expect(node.type).toBe('statement_block');
  });
  it('render produces non-empty string', () => {
    const node = ir.statementBlock({ statements: 'test' as any, automaticSemicolon: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('else_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.elseClause({ statement: 'test' as any });
    expect(node.type).toBe('else_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.elseClause({ statement: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('if_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.ifStatement({ condition: { type: 'parenthesized_expression', text: 'test' } as any, consequence: { type: 'statement', text: 'test' } as any });
    expect(node.type).toBe('if_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.ifStatement({ condition: { type: 'parenthesized_expression', text: 'test' } as any, consequence: { type: 'statement', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('switch_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.switchStatement({ value: { type: 'parenthesized_expression', text: 'test' } as any, body: { type: 'switch_body', text: 'test' } as any });
    expect(node.type).toBe('switch_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.switchStatement({ value: { type: 'parenthesized_expression', text: 'test' } as any, body: { type: 'switch_body', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('for_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.forStatement({ initializer: { type: 'lexical_declaration', text: 'test' } as any, condition: { type: 'empty_statement', text: 'test' } as any, increment: { type: '_expressions', text: 'test' } as any, body: { type: 'statement', text: 'test' } as any });
    expect(node.type).toBe('for_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.forStatement({ initializer: { type: 'lexical_declaration', text: 'test' } as any, condition: { type: 'empty_statement', text: 'test' } as any, increment: { type: '_expressions', text: 'test' } as any, body: { type: 'statement', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('for_in_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.forInStatement({ body: { type: 'statement', text: 'test' } as any });
    expect(node.type).toBe('for_in_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.forInStatement({ body: { type: 'statement', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('while_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.whileStatement({ condition: { type: 'parenthesized_expression', text: 'test' } as any, body: { type: 'statement', text: 'test' } as any });
    expect(node.type).toBe('while_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.whileStatement({ condition: { type: 'parenthesized_expression', text: 'test' } as any, body: { type: 'statement', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('do_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.doStatement({ body: { type: 'statement', text: 'test' } as any, condition: { type: 'parenthesized_expression', text: 'test' } as any });
    expect(node.type).toBe('do_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.doStatement({ body: { type: 'statement', text: 'test' } as any, condition: { type: 'parenthesized_expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('try_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.tryStatement({ body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('try_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.tryStatement({ body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('with_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.withStatement({ object: { type: 'parenthesized_expression', text: 'test' } as any, body: { type: 'statement', text: 'test' } as any });
    expect(node.type).toBe('with_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.withStatement({ object: { type: 'parenthesized_expression', text: 'test' } as any, body: { type: 'statement', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('break_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.breakStatement({ label: { type: 'identifier', text: 'test' } as any });
    expect(node.type).toBe('break_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.breakStatement({ label: { type: 'identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('continue_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.continueStatement({ label: { type: 'identifier', text: 'test' } as any });
    expect(node.type).toBe('continue_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.continueStatement({ label: { type: 'identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('labeled_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.labeledStatement({ label: { type: 'identifier', text: 'test' } as any, body: { type: 'statement', text: 'test' } as any });
    expect(node.type).toBe('labeled_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.labeledStatement({ label: { type: 'identifier', text: 'test' } as any, body: { type: 'statement', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('switch_body', () => {
  it('factory produces correct type', () => {
    const node = ir.switchBody();
    expect(node.type).toBe('switch_body');
  });
});

describe('switch_case', () => {
  it('factory produces correct type', () => {
    const node = ir.switchCase({ value: { type: '_expressions', text: 'test' } as any, body: 'test' as any });
    expect(node.type).toBe('switch_case');
  });
  it('render produces non-empty string', () => {
    const node = ir.switchCase({ value: { type: '_expressions', text: 'test' } as any, body: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('switch_default', () => {
  it('factory produces correct type', () => {
    const node = ir.switchDefault({ body: 'test' as any });
    expect(node.type).toBe('switch_default');
  });
  it('render produces non-empty string', () => {
    const node = ir.switchDefault({ body: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('catch_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.catchClause({ body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('catch_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.catchClause({ body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('finally_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.finallyClause({ body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('finally_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.finallyClause({ body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('parenthesized_expression', () => {
});

describe('expression', () => {
  it('as_expression form produces correct type', () => {
    const node = ir.expression.as_expression({});
    expect(node.type).toBe('expression');
  });
  it('satisfies_expression form produces correct type', () => {
    const node = ir.expression.satisfies_expression({});
    expect(node.type).toBe('expression');
  });
  it('instantiation_expression form produces correct type', () => {
    const node = ir.expression.instantiation_expression({});
    expect(node.type).toBe('expression');
  });
  it('internal_module form produces correct type', () => {
    const node = ir.expression.internal_module({});
    expect(node.type).toBe('expression');
  });
  it('type_assertion form produces correct type', () => {
    const node = ir.expression.type_assertion({});
    expect(node.type).toBe('expression');
  });
  it('primary_expression form produces correct type', () => {
    const node = ir.expression.primary_expression({});
    expect(node.type).toBe('expression');
  });
  it('assignment_expression form produces correct type', () => {
    const node = ir.expression.assignment_expression({});
    expect(node.type).toBe('expression');
  });
  it('augmented_assignment_expression form produces correct type', () => {
    const node = ir.expression.augmented_assignment_expression({});
    expect(node.type).toBe('expression');
  });
  it('await_expression form produces correct type', () => {
    const node = ir.expression.await_expression({});
    expect(node.type).toBe('expression');
  });
  it('unary_expression form produces correct type', () => {
    const node = ir.expression.unary_expression({});
    expect(node.type).toBe('expression');
  });
  it('binary_expression form produces correct type', () => {
    const node = ir.expression.binary_expression({});
    expect(node.type).toBe('expression');
  });
  it('ternary_expression form produces correct type', () => {
    const node = ir.expression.ternary_expression({});
    expect(node.type).toBe('expression');
  });
  it('update_expression form produces correct type', () => {
    const node = ir.expression.update_expression({});
    expect(node.type).toBe('expression');
  });
  it('new_expression form produces correct type', () => {
    const node = ir.expression.new_expression({});
    expect(node.type).toBe('expression');
  });
  it('yield_expression form produces correct type', () => {
    const node = ir.expression.yield_expression({});
    expect(node.type).toBe('expression');
  });
});

describe('primary_expression', () => {
  it('form_0 form produces correct type', () => {
    const node = ir.primaryExpression.form_0({});
    expect(node.type).toBe('primary_expression');
  });
  it('non_null_expression form produces correct type', () => {
    const node = ir.primaryExpression.non_null_expression({});
    expect(node.type).toBe('primary_expression');
  });
});

describe('yield_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.yieldExpression({ expression: 'test' as any });
    expect(node.type).toBe('yield_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.yieldExpression({ expression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('assignment_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.assignmentPattern({ left: { type: 'pattern', text: 'test' } as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('assignment_pattern');
  });
  it('render produces non-empty string', () => {
    const node = ir.assignmentPattern({ left: { type: 'pattern', text: 'test' } as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('object_assignment_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.objectAssignmentPattern({ left: { type: '_reserved_identifier', text: 'test' } as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('object_assignment_pattern');
  });
  it('render produces non-empty string', () => {
    const node = ir.objectAssignmentPattern({ left: { type: '_reserved_identifier', text: 'test' } as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('jsx_element', () => {
  it('factory produces correct type', () => {
    const node = ir.jsxElement({ openTag: { type: 'jsx_opening_element', text: 'test' } as any, closeTag: { type: 'jsx_closing_element', text: 'test' } as any });
    expect(node.type).toBe('jsx_element');
  });
  it('render produces non-empty string', () => {
    const node = ir.jsxElement({ openTag: { type: 'jsx_opening_element', text: 'test' } as any, closeTag: { type: 'jsx_closing_element', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('html_character_reference', () => {
  it('factory produces correct type', () => {
    const node = ir.htmlCharacterReference('test');
    expect(node.type).toBe('html_character_reference');
    expect(node.text).toBe('test');
  });
});

describe('jsx_expression', () => {
});

describe('jsx_identifier', () => {
  it('factory produces correct type', () => {
    const node = ir.jsxIdentifier('test');
    expect(node.type).toBe('jsx_identifier');
    expect(node.text).toBe('test');
  });
});

describe('nested_identifier', () => {
  it('factory produces correct type', () => {
    const node = ir.nestedIdentifier({ object: { type: 'identifier', text: 'test' } as any, property: { type: 'identifier', text: 'test' } as any });
    expect(node.type).toBe('nested_identifier');
  });
  it('render produces non-empty string', () => {
    const node = ir.nestedIdentifier({ object: { type: 'identifier', text: 'test' } as any, property: { type: 'identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('jsx_closing_element', () => {
  it('factory produces correct type', () => {
    const node = ir.jsxClosingElement({});
    expect(node.type).toBe('jsx_closing_element');
  });
  it('render produces non-empty string', () => {
    const node = ir.jsxClosingElement({});
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('unescaped_double_jsx_string_fragment', () => {
  it('factory produces correct type', () => {
    const node = ir.unescapedDoubleJsxStringFragment('test');
    expect(node.type).toBe('unescaped_double_jsx_string_fragment');
    expect(node.text).toBe('test');
  });
});

describe('unescaped_single_jsx_string_fragment', () => {
  it('factory produces correct type', () => {
    const node = ir.unescapedSingleJsxStringFragment('test');
    expect(node.type).toBe('unescaped_single_jsx_string_fragment');
    expect(node.text).toBe('test');
  });
});

describe('class', () => {
  it('factory produces correct type', () => {
    const node = ir.class({ classHeritage: 'test' as any, name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, body: { type: 'class_body', text: 'test' } as any });
    expect(node.type).toBe('class');
  });
  it('render produces non-empty string', () => {
    const node = ir.class({ classHeritage: 'test' as any, name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, body: { type: 'class_body', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('class_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.classDeclaration({ classHeritage: 'test' as any, automaticSemicolon: 'test' as any, name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, body: { type: 'class_body', text: 'test' } as any });
    expect(node.type).toBe('class_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.classDeclaration({ classHeritage: 'test' as any, automaticSemicolon: 'test' as any, name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, body: { type: 'class_body', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('class_heritage', () => {
  it('extends_clause form produces correct type', () => {
    const node = ir.classHeritage.extends_clause({ extendsClause: { type: 'extends_clause', text: 'test' } as any, implementsClause: { type: 'implements_clause', text: 'test' } as any });
    expect(node.type).toBe('class_heritage');
  });
  it('implements_clause form produces correct type', () => {
    const node = ir.classHeritage.implements_clause({});
    expect(node.type).toBe('class_heritage');
  });
});

describe('function_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.functionExpression({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('function_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.functionExpression({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('function_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.functionDeclaration({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('function_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.functionDeclaration({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('generator_function', () => {
  it('factory produces correct type', () => {
    const node = ir.generatorFunction({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('generator_function');
  });
  it('render produces non-empty string', () => {
    const node = ir.generatorFunction({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('generator_function_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.generatorFunctionDeclaration({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('generator_function_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.generatorFunctionDeclaration({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('arrow_function', () => {
  it('factory produces correct type', () => {
    const node = ir.arrowFunction({ parameter: { type: '_reserved_identifier', text: 'test' } as any, body: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('arrow_function');
  });
  it('render produces non-empty string', () => {
    const node = ir.arrowFunction({ parameter: { type: '_reserved_identifier', text: 'test' } as any, body: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('call_expression', () => {
  it('function form produces correct type', () => {
    const node = ir.callExpression.function({ function: { type: 'expression', text: 'test' } as any, typeArguments: { type: 'type_arguments', text: 'test' } as any, arguments: { type: 'arguments', text: 'test' } as any });
    expect(node.type).toBe('call_expression');
  });
  it('function2 form produces correct type', () => {
    const node = ir.callExpression.function2({ function: { type: 'primary_expression', text: 'test' } as any, arguments: { type: 'template_string', text: 'test' } as any });
    expect(node.type).toBe('call_expression');
  });
  it('tok_q_dot form produces correct type', () => {
    const node = ir.callExpression.tok_q_dot({ function: { type: 'primary_expression', text: 'test' } as any, typeArguments: { type: 'type_arguments', text: 'test' } as any, arguments: { type: 'arguments', text: 'test' } as any });
    expect(node.type).toBe('call_expression');
  });
});

describe('new_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.newExpression({ constructor: { type: 'primary_expression', text: 'test' } as any, typeArguments: { type: 'type_arguments', text: 'test' } as any, arguments: { type: 'arguments', text: 'test' } as any });
    expect(node.type).toBe('new_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.newExpression({ constructor: { type: 'primary_expression', text: 'test' } as any, typeArguments: { type: 'type_arguments', text: 'test' } as any, arguments: { type: 'arguments', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('await_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.awaitExpression({ expression: 'test' as any });
    expect(node.type).toBe('await_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.awaitExpression({ expression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('member_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.memberExpression({ object: { type: 'expression', text: 'test' } as any, optionalChain: { type: 'optional_chain', text: 'test' } as any, property: { type: 'private_property_identifier', text: 'test' } as any });
    expect(node.type).toBe('member_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.memberExpression({ object: { type: 'expression', text: 'test' } as any, optionalChain: { type: 'optional_chain', text: 'test' } as any, property: { type: 'private_property_identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('subscript_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.subscriptExpression({ object: { type: 'expression', text: 'test' } as any, index: { type: '_expressions', text: 'test' } as any });
    expect(node.type).toBe('subscript_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.subscriptExpression({ object: { type: 'expression', text: 'test' } as any, index: { type: '_expressions', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('assignment_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.assignmentExpression({ left: { type: 'parenthesized_expression', text: 'test' } as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('assignment_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.assignmentExpression({ left: { type: 'parenthesized_expression', text: 'test' } as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('augmented_assignment_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.augmentedAssignmentExpression({ left: { type: '_augmented_assignment_lhs', text: 'test' } as any, operator: { type: '+=', text: 'test' } as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('augmented_assignment_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.augmentedAssignmentExpression({ left: { type: '_augmented_assignment_lhs', text: 'test' } as any, operator: { type: '+=', text: 'test' } as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('spread_element', () => {
  it('factory produces correct type', () => {
    const node = ir.spreadElement({ expression: 'test' as any });
    expect(node.type).toBe('spread_element');
  });
  it('render produces non-empty string', () => {
    const node = ir.spreadElement({ expression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('ternary_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.ternaryExpression({ condition: { type: 'expression', text: 'test' } as any, consequence: { type: 'expression', text: 'test' } as any, alternative: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('ternary_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.ternaryExpression({ condition: { type: 'expression', text: 'test' } as any, consequence: { type: 'expression', text: 'test' } as any, alternative: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('binary_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.binaryExpression({ left: { type: 'expression', text: 'test' } as any, operator: 'test' as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('binary_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.binaryExpression({ left: { type: 'expression', text: 'test' } as any, operator: 'test' as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('unary_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.unaryExpression({ operator: { type: '!', text: 'test' } as any, argument: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('unary_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.unaryExpression({ operator: { type: '!', text: 'test' } as any, argument: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('update_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.updateExpression({ argument: { type: 'expression', text: 'test' } as any, operator: { type: '++', text: 'test' } as any });
    expect(node.type).toBe('update_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.updateExpression({ argument: { type: 'expression', text: 'test' } as any, operator: { type: '++', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('string', () => {
  it('tok_dq form produces correct type', () => {
    const node = ir.string.tok_dq({});
    expect(node.type).toBe('string');
  });
  it('tok_sq form produces correct type', () => {
    const node = ir.string.tok_sq({});
    expect(node.type).toBe('string');
  });
});

describe('unescaped_double_string_fragment', () => {
  it('factory produces correct type', () => {
    const node = ir.unescapedDoubleStringFragment('test');
    expect(node.type).toBe('unescaped_double_string_fragment');
    expect(node.text).toBe('test');
  });
});

describe('unescaped_single_string_fragment', () => {
  it('factory produces correct type', () => {
    const node = ir.unescapedSingleStringFragment('test');
    expect(node.type).toBe('unescaped_single_string_fragment');
    expect(node.text).toBe('test');
  });
});

describe('escape_sequence', () => {
});

describe('comment', () => {
  it('slashslash form produces correct type', () => {
    const node = ir.comment.slashslash({});
    expect(node.type).toBe('comment');
  });
  it('tok_slash_star form produces correct type', () => {
    const node = ir.comment.tok_slash_star({});
    expect(node.type).toBe('comment');
  });
});

describe('template_string', () => {
  it('factory produces correct type', () => {
    const node = ir.templateString();
    expect(node.type).toBe('template_string');
  });
});

describe('regex', () => {
  it('factory produces correct type', () => {
    const node = ir.regex({ pattern: { type: 'regex_pattern', text: 'test' } as any });
    expect(node.type).toBe('regex');
  });
  it('render produces non-empty string', () => {
    const node = ir.regex({ pattern: { type: 'regex_pattern', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('regex_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.regexPattern();
    expect(node.type).toBe('regex_pattern');
  });
});

describe('regex_flags', () => {
  it('factory produces correct type', () => {
    const node = ir.regexFlags('test');
    expect(node.type).toBe('regex_flags');
    expect(node.text).toBe('test');
  });
});

describe('number', () => {
  it('form_0 form produces correct type', () => {
    const node = ir.number.form_0({});
    expect(node.type).toBe('number');
  });
  it('form_1 form produces correct type', () => {
    const node = ir.number.form_1({});
    expect(node.type).toBe('number');
  });
  it('form_2 form produces correct type', () => {
    const node = ir.number.form_2({});
    expect(node.type).toBe('number');
  });
  it('form_3 form produces correct type', () => {
    const node = ir.number.form_3({});
    expect(node.type).toBe('number');
  });
  it('n form produces correct type', () => {
    const node = ir.number.n({});
    expect(node.type).toBe('number');
  });
});

describe('meta_property', () => {
  it('new form produces correct type', () => {
    const node = ir.metaProperty.new({});
    expect(node.type).toBe('meta_property');
  });
  it('import form produces correct type', () => {
    const node = ir.metaProperty.import({});
    expect(node.type).toBe('meta_property');
  });
});

describe('this', () => {
  it('factory produces keyword', () => {
    const node = ir.this();
    expect(node.type).toBe('this');
    expect(node.text).toBe('this');
  });
});

describe('super', () => {
  it('factory produces keyword', () => {
    const node = ir.super();
    expect(node.type).toBe('super');
    expect(node.text).toBe('super');
  });
});

describe('true', () => {
  it('factory produces keyword', () => {
    const node = ir.true();
    expect(node.type).toBe('true');
    expect(node.text).toBe('true');
  });
});

describe('false', () => {
  it('factory produces keyword', () => {
    const node = ir.false();
    expect(node.type).toBe('false');
    expect(node.text).toBe('false');
  });
});

describe('null', () => {
  it('factory produces keyword', () => {
    const node = ir.null();
    expect(node.type).toBe('null');
    expect(node.text).toBe('null');
  });
});

describe('undefined', () => {
  it('factory produces keyword', () => {
    const node = ir.undefined();
    expect(node.type).toBe('undefined');
    expect(node.text).toBe('undefined');
  });
});

describe('decorator', () => {
});

describe('decorator_member_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.decoratorMemberExpression({ object: { type: 'identifier', text: 'test' } as any, property: { type: 'identifier', text: 'test' } as any });
    expect(node.type).toBe('decorator_member_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.decoratorMemberExpression({ object: { type: 'identifier', text: 'test' } as any, property: { type: 'identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('decorator_call_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.decoratorCallExpression({ function: { type: 'identifier', text: 'test' } as any, arguments: { type: 'arguments', text: 'test' } as any });
    expect(node.type).toBe('decorator_call_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.decoratorCallExpression({ function: { type: 'identifier', text: 'test' } as any, arguments: { type: 'arguments', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('class_body', () => {
  it('factory produces correct type', () => {
    const node = ir.classBody();
    expect(node.type).toBe('class_body');
  });
});

describe('field_definition', () => {
  it('factory produces correct type', () => {
    const node = ir.fieldDefinition({ decorator: [], property: { type: '_property_name', text: 'test' } as any });
    expect(node.type).toBe('field_definition');
  });
  it('render produces non-empty string', () => {
    const node = ir.fieldDefinition({ decorator: [], property: { type: '_property_name', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('class_static_block', () => {
  it('factory produces correct type', () => {
    const node = ir.classStaticBlock({ body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('class_static_block');
  });
  it('render produces non-empty string', () => {
    const node = ir.classStaticBlock({ body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('pattern', () => {
  it('_lhs_expression form produces correct type', () => {
    const node = ir.pattern._lhs_expression({});
    expect(node.type).toBe('pattern');
  });
  it('rest_pattern form produces correct type', () => {
    const node = ir.pattern.rest_pattern({});
    expect(node.type).toBe('pattern');
  });
});

describe('method_definition', () => {
  it('factory produces correct type', () => {
    const node = ir.methodDefinition({ accessibilityModifier: { type: 'accessibility_modifier', text: 'test' } as any, overrideModifier: 'test' as any, name: { type: '_property_name', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.type).toBe('method_definition');
  });
  it('render produces non-empty string', () => {
    const node = ir.methodDefinition({ accessibilityModifier: { type: 'accessibility_modifier', text: 'test' } as any, overrideModifier: 'test' as any, name: { type: '_property_name', text: 'test' } as any, body: { type: 'statement_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('pair', () => {
  it('factory produces correct type', () => {
    const node = ir.pair({ key: { type: '_property_name', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('pair');
  });
  it('render produces non-empty string', () => {
    const node = ir.pair({ key: { type: '_property_name', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('pair_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.pairPattern({ key: { type: '_property_name', text: 'test' } as any, value: { type: 'pattern', text: 'test' } as any });
    expect(node.type).toBe('pair_pattern');
  });
  it('render produces non-empty string', () => {
    const node = ir.pairPattern({ key: { type: '_property_name', text: 'test' } as any, value: { type: 'pattern', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('computed_property_name', () => {
  it('factory produces correct type', () => {
    const node = ir.computedPropertyName({ expression: 'test' as any });
    expect(node.type).toBe('computed_property_name');
  });
  it('render produces non-empty string', () => {
    const node = ir.computedPropertyName({ expression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('public_field_definition', () => {
  it('factory produces correct type', () => {
    const node = ir.publicFieldDefinition({ accessibilityModifier: 'test' as any, overrideModifier: 'test' as any, initializer: 'test' as any, name: { type: '_property_name', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('public_field_definition');
  });
  it('render produces non-empty string', () => {
    const node = ir.publicFieldDefinition({ accessibilityModifier: 'test' as any, overrideModifier: 'test' as any, initializer: 'test' as any, name: { type: '_property_name', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('non_null_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.nonNullExpression({ expression: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('non_null_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.nonNullExpression({ expression: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('method_signature', () => {
  it('factory produces correct type', () => {
    const node = ir.methodSignature({ accessibilityModifier: { type: 'accessibility_modifier', text: 'test' } as any, overrideModifier: 'test' as any, name: { type: '_property_name', text: 'test' } as any });
    expect(node.type).toBe('method_signature');
  });
  it('render produces non-empty string', () => {
    const node = ir.methodSignature({ accessibilityModifier: { type: 'accessibility_modifier', text: 'test' } as any, overrideModifier: 'test' as any, name: { type: '_property_name', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('abstract_method_signature', () => {
  it('factory produces correct type', () => {
    const node = ir.abstractMethodSignature({ accessibilityModifier: { type: 'accessibility_modifier', text: 'test' } as any, overrideModifier: 'test' as any, name: { type: '_property_name', text: 'test' } as any });
    expect(node.type).toBe('abstract_method_signature');
  });
  it('render produces non-empty string', () => {
    const node = ir.abstractMethodSignature({ accessibilityModifier: { type: 'accessibility_modifier', text: 'test' } as any, overrideModifier: 'test' as any, name: { type: '_property_name', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('function_signature', () => {
  it('factory produces correct type', () => {
    const node = ir.functionSignature({ name: { type: 'identifier', text: 'test' } as any });
    expect(node.type).toBe('function_signature');
  });
  it('render produces non-empty string', () => {
    const node = ir.functionSignature({ name: { type: 'identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('decorator_parenthesized_expression', () => {
});

describe('type_assertion', () => {
  it('factory produces correct type', () => {
    const node = ir.typeAssertion({ typeArguments: { type: 'type_arguments', text: 'test' } as any, expression: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('type_assertion');
  });
  it('render produces non-empty string', () => {
    const node = ir.typeAssertion({ typeArguments: { type: 'type_arguments', text: 'test' } as any, expression: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('as_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.asExpression({ expression: { type: 'expression', text: 'test' } as any, typeAnnotation: 'test' as any });
    expect(node.type).toBe('as_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.asExpression({ expression: { type: 'expression', text: 'test' } as any, typeAnnotation: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('satisfies_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.satisfiesExpression({ expression: { type: 'expression', text: 'test' } as any, typeAnnotation: 'test' as any });
    expect(node.type).toBe('satisfies_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.satisfiesExpression({ expression: { type: 'expression', text: 'test' } as any, typeAnnotation: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('instantiation_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.instantiationExpression({ expression: { type: 'expression', text: 'test' } as any, typeArguments: { type: 'type_arguments', text: 'test' } as any });
    expect(node.type).toBe('instantiation_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.instantiationExpression({ expression: { type: 'expression', text: 'test' } as any, typeArguments: { type: 'type_arguments', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('import_require_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.importRequireClause({ identifier: { type: 'identifier', text: 'test' } as any, source: { type: 'string', text: 'test' } as any });
    expect(node.type).toBe('import_require_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.importRequireClause({ identifier: { type: 'identifier', text: 'test' } as any, source: { type: 'string', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('ambient_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.ambientDeclaration({ declaration: 'test' as any, typeAnnotation: { type: 'declaration', text: 'test' } as any });
    expect(node.type).toBe('ambient_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.ambientDeclaration({ declaration: 'test' as any, typeAnnotation: { type: 'declaration', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('abstract_class_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.abstractClassDeclaration({ classHeritage: 'test' as any, name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, body: { type: 'class_body', text: 'test' } as any });
    expect(node.type).toBe('abstract_class_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.abstractClassDeclaration({ classHeritage: 'test' as any, name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, body: { type: 'class_body', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('import_alias', () => {
  it('factory produces correct type', () => {
    const node = ir.importAlias({ name: 'test' as any, value: { type: 'identifier', text: 'test' } as any, semicolon: 'test' as any });
    expect(node.type).toBe('import_alias');
  });
  it('render produces non-empty string', () => {
    const node = ir.importAlias({ name: 'test' as any, value: { type: 'identifier', text: 'test' } as any, semicolon: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('nested_type_identifier', () => {
  it('factory produces correct type', () => {
    const node = ir.nestedTypeIdentifier({ module: { type: 'identifier', text: 'test' } as any, name: { type: '_type_identifier', text: 'test' } as any });
    expect(node.type).toBe('nested_type_identifier');
  });
  it('render produces non-empty string', () => {
    const node = ir.nestedTypeIdentifier({ module: { type: 'identifier', text: 'test' } as any, name: { type: '_type_identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('interface_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.interfaceDeclaration({ extendsTypeClause: 'test' as any, name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, body: { type: 'object_type', text: 'test' } as any });
    expect(node.type).toBe('interface_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.interfaceDeclaration({ extendsTypeClause: 'test' as any, name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, body: { type: 'object_type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('enum_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.enumDeclaration({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'enum_body', text: 'test' } as any });
    expect(node.type).toBe('enum_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.enumDeclaration({ name: { type: 'identifier', text: 'test' } as any, body: { type: 'enum_body', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('enum_body', () => {
  it('factory produces correct type', () => {
    const node = ir.enumBody({ opening: 'test' as any, members: 'test' as any });
    expect(node.type).toBe('enum_body');
  });
  it('render produces non-empty string', () => {
    const node = ir.enumBody({ opening: 'test' as any, members: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('enum_assignment', () => {
  it('factory produces correct type', () => {
    const node = ir.enumAssignment({ name: { type: '_property_name', text: 'test' } as any });
    expect(node.type).toBe('enum_assignment');
  });
  it('render produces non-empty string', () => {
    const node = ir.enumAssignment({ name: { type: '_property_name', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('type_alias_declaration', () => {
  it('factory produces correct type', () => {
    const node = ir.typeAliasDeclaration({ name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, value: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('type_alias_declaration');
  });
  it('render produces non-empty string', () => {
    const node = ir.typeAliasDeclaration({ name: { type: '_type_identifier', text: 'test' } as any, typeParameters: { type: 'type_parameters', text: 'test' } as any, value: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('override_modifier', () => {
  it('factory produces keyword', () => {
    const node = ir.overrideModifier();
    expect(node.type).toBe('override_modifier');
    expect(node.text).toBe('override');
  });
});

describe('required_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.requiredParameter({ parameterName: { type: '_parameter_name', text: 'test' } as any, initializer: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('required_parameter');
  });
  it('render produces non-empty string', () => {
    const node = ir.requiredParameter({ parameterName: { type: '_parameter_name', text: 'test' } as any, initializer: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('optional_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.optionalParameter({ parameterName: { type: '_parameter_name', text: 'test' } as any, initializer: 'test' as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('optional_parameter');
  });
  it('render produces non-empty string', () => {
    const node = ir.optionalParameter({ parameterName: { type: '_parameter_name', text: 'test' } as any, initializer: 'test' as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('asserts_annotation', () => {
  it('factory produces correct type', () => {
    const node = ir.assertsAnnotation({ asserts: 'test' as any });
    expect(node.type).toBe('asserts_annotation');
  });
  it('render produces non-empty string', () => {
    const node = ir.assertsAnnotation({ asserts: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('type', () => {
  it('primary_type form produces correct type', () => {
    const node = ir.type.primary_type({});
    expect(node.type).toBe('type');
  });
  it('function_type form produces correct type', () => {
    const node = ir.type.function_type({});
    expect(node.type).toBe('type');
  });
  it('readonly_type form produces correct type', () => {
    const node = ir.type.readonly_type({});
    expect(node.type).toBe('type');
  });
  it('constructor_type form produces correct type', () => {
    const node = ir.type.constructor_type({});
    expect(node.type).toBe('type');
  });
  it('infer_type form produces correct type', () => {
    const node = ir.type.infer_type({});
    expect(node.type).toBe('type');
  });
  it('_type_query_member_expression_in_type_annotation form produces correct type', () => {
    const node = ir.type._type_query_member_expression_in_type_annotation({});
    expect(node.type).toBe('type');
  });
  it('_type_query_call_expression_in_type_annotation form produces correct type', () => {
    const node = ir.type._type_query_call_expression_in_type_annotation({});
    expect(node.type).toBe('type');
  });
});

describe('tuple_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.tupleParameter({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('tuple_parameter');
  });
  it('render produces non-empty string', () => {
    const node = ir.tupleParameter({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('optional_tuple_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.optionalTupleParameter({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('optional_tuple_parameter');
  });
  it('render produces non-empty string', () => {
    const node = ir.optionalTupleParameter({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('constructor_type', () => {
  it('factory produces correct type', () => {
    const node = ir.constructorType({ typeParameters: { type: 'type_parameters', text: 'test' } as any, parameters: { type: 'formal_parameters', text: 'test' } as any, type: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('constructor_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.constructorType({ typeParameters: { type: 'type_parameters', text: 'test' } as any, parameters: { type: 'formal_parameters', text: 'test' } as any, type: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('primary_type', () => {
  it('parenthesized_type form produces correct type', () => {
    const node = ir.primaryType.parenthesized_type({});
    expect(node.type).toBe('primary_type');
  });
  it('predefined_type form produces correct type', () => {
    const node = ir.primaryType.predefined_type({});
    expect(node.type).toBe('primary_type');
  });
  it('_type_identifier form produces correct type', () => {
    const node = ir.primaryType._type_identifier({});
    expect(node.type).toBe('primary_type');
  });
  it('nested_type_identifier form produces correct type', () => {
    const node = ir.primaryType.nested_type_identifier({});
    expect(node.type).toBe('primary_type');
  });
  it('generic_type form produces correct type', () => {
    const node = ir.primaryType.generic_type({});
    expect(node.type).toBe('primary_type');
  });
  it('object_type form produces correct type', () => {
    const node = ir.primaryType.object_type({});
    expect(node.type).toBe('primary_type');
  });
  it('array_type form produces correct type', () => {
    const node = ir.primaryType.array_type({});
    expect(node.type).toBe('primary_type');
  });
  it('tuple_type form produces correct type', () => {
    const node = ir.primaryType.tuple_type({});
    expect(node.type).toBe('primary_type');
  });
  it('flow_maybe_type form produces correct type', () => {
    const node = ir.primaryType.flow_maybe_type({});
    expect(node.type).toBe('primary_type');
  });
  it('type_query form produces correct type', () => {
    const node = ir.primaryType.type_query({});
    expect(node.type).toBe('primary_type');
  });
  it('index_type_query form produces correct type', () => {
    const node = ir.primaryType.index_type_query({});
    expect(node.type).toBe('primary_type');
  });
  it('this form produces correct type', () => {
    const node = ir.primaryType.this({});
    expect(node.type).toBe('primary_type');
  });
  it('existential_type form produces correct type', () => {
    const node = ir.primaryType.existential_type({});
    expect(node.type).toBe('primary_type');
  });
  it('literal_type form produces correct type', () => {
    const node = ir.primaryType.literal_type({});
    expect(node.type).toBe('primary_type');
  });
  it('lookup_type form produces correct type', () => {
    const node = ir.primaryType.lookup_type({});
    expect(node.type).toBe('primary_type');
  });
  it('conditional_type form produces correct type', () => {
    const node = ir.primaryType.conditional_type({});
    expect(node.type).toBe('primary_type');
  });
  it('template_literal_type form produces correct type', () => {
    const node = ir.primaryType.template_literal_type({});
    expect(node.type).toBe('primary_type');
  });
  it('intersection_type form produces correct type', () => {
    const node = ir.primaryType.intersection_type({});
    expect(node.type).toBe('primary_type');
  });
  it('union_type form produces correct type', () => {
    const node = ir.primaryType.union_type({});
    expect(node.type).toBe('primary_type');
  });
  it('const form produces correct type', () => {
    const node = ir.primaryType.const({});
    expect(node.type).toBe('primary_type');
  });
});

describe('template_type', () => {
});

describe('template_literal_type', () => {
  it('factory produces correct type', () => {
    const node = ir.templateLiteralType();
    expect(node.type).toBe('template_literal_type');
  });
});

describe('infer_type', () => {
  it('factory produces correct type', () => {
    const node = ir.inferType({ typeIdentifier: 'test' as any, constraint: { type: '_type_identifier', text: 'test' } as any });
    expect(node.type).toBe('infer_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.inferType({ typeIdentifier: 'test' as any, constraint: { type: '_type_identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('conditional_type', () => {
  it('factory produces correct type', () => {
    const node = ir.conditionalType({ left: { type: 'type', text: 'test' } as any, right: { type: 'type', text: 'test' } as any, consequence: { type: 'type', text: 'test' } as any, alternative: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('conditional_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.conditionalType({ left: { type: 'type', text: 'test' } as any, right: { type: 'type', text: 'test' } as any, consequence: { type: 'type', text: 'test' } as any, alternative: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('generic_type', () => {
  it('factory produces correct type', () => {
    const node = ir.genericType({ name: { type: '_type_identifier', text: 'test' } as any, typeArguments: { type: 'type_arguments', text: 'test' } as any });
    expect(node.type).toBe('generic_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.genericType({ name: { type: '_type_identifier', text: 'test' } as any, typeArguments: { type: 'type_arguments', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('type_predicate', () => {
  it('factory produces correct type', () => {
    const node = ir.typePredicate({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('type_predicate');
  });
  it('render produces non-empty string', () => {
    const node = ir.typePredicate({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('type_predicate_annotation', () => {
  it('factory produces correct type', () => {
    const node = ir.typePredicateAnnotation({ typePredicate: 'test' as any });
    expect(node.type).toBe('type_predicate_annotation');
  });
  it('render produces non-empty string', () => {
    const node = ir.typePredicateAnnotation({ typePredicate: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('index_type_query', () => {
  it('factory produces correct type', () => {
    const node = ir.indexTypeQuery({ primaryType: 'test' as any });
    expect(node.type).toBe('index_type_query');
  });
  it('render produces non-empty string', () => {
    const node = ir.indexTypeQuery({ primaryType: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('lookup_type', () => {
  it('factory produces correct type', () => {
    const node = ir.lookupType({ primaryType: { type: 'primary_type', text: 'test' } as any, indexType: 'test' as any });
    expect(node.type).toBe('lookup_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.lookupType({ primaryType: { type: 'primary_type', text: 'test' } as any, indexType: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('mapped_type_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.mappedTypeClause({ name: { type: '_type_identifier', text: 'test' } as any, type: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('mapped_type_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.mappedTypeClause({ name: { type: '_type_identifier', text: 'test' } as any, type: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('literal_type', () => {
  it('_number form produces correct type', () => {
    const node = ir.literalType._number({});
    expect(node.type).toBe('literal_type');
  });
  it('number form produces correct type', () => {
    const node = ir.literalType.number({});
    expect(node.type).toBe('literal_type');
  });
  it('string form produces correct type', () => {
    const node = ir.literalType.string({});
    expect(node.type).toBe('literal_type');
  });
  it('true form produces correct type', () => {
    const node = ir.literalType.true({});
    expect(node.type).toBe('literal_type');
  });
  it('false form produces correct type', () => {
    const node = ir.literalType.false({});
    expect(node.type).toBe('literal_type');
  });
  it('null form produces correct type', () => {
    const node = ir.literalType.null({});
    expect(node.type).toBe('literal_type');
  });
  it('undefined form produces correct type', () => {
    const node = ir.literalType.undefined({});
    expect(node.type).toBe('literal_type');
  });
});

describe('flow_maybe_type', () => {
  it('factory produces correct type', () => {
    const node = ir.flowMaybeType({ primaryType: 'test' as any });
    expect(node.type).toBe('flow_maybe_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.flowMaybeType({ primaryType: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('predefined_type', () => {
  it('any form produces correct type', () => {
    const node = ir.predefinedType.any({});
    expect(node.type).toBe('predefined_type');
  });
  it('number form produces correct type', () => {
    const node = ir.predefinedType.number({});
    expect(node.type).toBe('predefined_type');
  });
  it('boolean form produces correct type', () => {
    const node = ir.predefinedType.boolean({});
    expect(node.type).toBe('predefined_type');
  });
  it('string form produces correct type', () => {
    const node = ir.predefinedType.string({});
    expect(node.type).toBe('predefined_type');
  });
  it('symbol form produces correct type', () => {
    const node = ir.predefinedType.symbol({});
    expect(node.type).toBe('predefined_type');
  });
  it('unique form produces correct type', () => {
    const node = ir.predefinedType.unique({});
    expect(node.type).toBe('predefined_type');
  });
  it('void form produces correct type', () => {
    const node = ir.predefinedType.void({});
    expect(node.type).toBe('predefined_type');
  });
  it('unknown form produces correct type', () => {
    const node = ir.predefinedType.unknown({});
    expect(node.type).toBe('predefined_type');
  });
  it('never form produces correct type', () => {
    const node = ir.predefinedType.never({});
    expect(node.type).toBe('predefined_type');
  });
  it('object form produces correct type', () => {
    const node = ir.predefinedType.object({});
    expect(node.type).toBe('predefined_type');
  });
});

describe('object_type', () => {
  it('factory produces correct type', () => {
    const node = ir.objectType({ opening: { type: '{', text: 'test' } as any, members: 'test' as any, closing: { type: '}', text: 'test' } as any });
    expect(node.type).toBe('object_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.objectType({ opening: { type: '{', text: 'test' } as any, members: 'test' as any, closing: { type: '}', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('property_signature', () => {
  it('factory produces correct type', () => {
    const node = ir.propertySignature({ accessibilityModifier: { type: 'accessibility_modifier', text: 'test' } as any, overrideModifier: 'test' as any, name: { type: '_property_name', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('property_signature');
  });
  it('render produces non-empty string', () => {
    const node = ir.propertySignature({ accessibilityModifier: { type: 'accessibility_modifier', text: 'test' } as any, overrideModifier: 'test' as any, name: { type: '_property_name', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('type_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.typeParameter({ name: { type: '_type_identifier', text: 'test' } as any, constraint: { type: 'constraint', text: 'test' } as any, value: { type: 'default_type', text: 'test' } as any });
    expect(node.type).toBe('type_parameter');
  });
  it('render produces non-empty string', () => {
    const node = ir.typeParameter({ name: { type: '_type_identifier', text: 'test' } as any, constraint: { type: 'constraint', text: 'test' } as any, value: { type: 'default_type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('construct_signature', () => {
  it('factory produces correct type', () => {
    const node = ir.constructSignature({ typeParameters: { type: 'type_parameters', text: 'test' } as any, parameters: { type: 'formal_parameters', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('construct_signature');
  });
  it('render produces non-empty string', () => {
    const node = ir.constructSignature({ typeParameters: { type: 'type_parameters', text: 'test' } as any, parameters: { type: 'formal_parameters', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('index_signature', () => {
  it('factory produces correct type', () => {
    const node = ir.indexSignature({ mappedTypeClause: 'test' as any, name: { type: 'identifier', text: 'test' } as any, indexType: { type: 'type', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.type).toBe('index_signature');
  });
  it('render produces non-empty string', () => {
    const node = ir.indexSignature({ mappedTypeClause: 'test' as any, name: { type: 'identifier', text: 'test' } as any, indexType: { type: 'type', text: 'test' } as any, type: { type: 'type_annotation', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('array_type', () => {
  it('factory produces correct type', () => {
    const node = ir.arrayType({ primaryType: { type: 'primary_type', text: 'test' } as any });
    expect(node.type).toBe('array_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.arrayType({ primaryType: { type: 'primary_type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('union_type', () => {
  it('factory produces correct type', () => {
    const node = ir.unionType({ left: { type: 'type', text: 'test' } as any, right: 'test' as any });
    expect(node.type).toBe('union_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.unionType({ left: { type: 'type', text: 'test' } as any, right: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('intersection_type', () => {
  it('factory produces correct type', () => {
    const node = ir.intersectionType({ left: { type: 'type', text: 'test' } as any, right: 'test' as any });
    expect(node.type).toBe('intersection_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.intersectionType({ left: { type: 'type', text: 'test' } as any, right: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('function_type', () => {
  it('factory produces correct type', () => {
    const node = ir.functionType({ typeParameters: { type: 'type_parameters', text: 'test' } as any, parameters: { type: 'formal_parameters', text: 'test' } as any, returnType: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('function_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.functionType({ typeParameters: { type: 'type_parameters', text: 'test' } as any, parameters: { type: 'formal_parameters', text: 'test' } as any, returnType: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('shorthand_property_identifier', () => {
  it('identifier form produces correct type', () => {
    const node = ir.shorthandPropertyIdentifier.identifier({});
    expect(node.type).toBe('shorthand_property_identifier');
  });
  it('_reserved_identifier form produces correct type', () => {
    const node = ir.shorthandPropertyIdentifier._reserved_identifier({});
    expect(node.type).toBe('shorthand_property_identifier');
  });
});

describe('shorthand_property_identifier_pattern', () => {
  it('identifier form produces correct type', () => {
    const node = ir.shorthandPropertyIdentifierPattern.identifier({});
    expect(node.type).toBe('shorthand_property_identifier_pattern');
  });
  it('_reserved_identifier form produces correct type', () => {
    const node = ir.shorthandPropertyIdentifierPattern._reserved_identifier({});
    expect(node.type).toBe('shorthand_property_identifier_pattern');
  });
});

describe('interface_body', () => {
  it('factory produces correct type', () => {
    const node = ir.interfaceBody({ opening: { type: '{', text: 'test' } as any, members: 'test' as any, closing: { type: '}', text: 'test' } as any });
    expect(node.type).toBe('interface_body');
  });
  it('render produces non-empty string', () => {
    const node = ir.interfaceBody({ opening: { type: '{', text: 'test' } as any, members: 'test' as any, closing: { type: '}', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('this_type', () => {
  it('factory produces keyword', () => {
    const node = ir.thisType();
    expect(node.type).toBe('this_type');
    expect(node.text).toBe('this');
  });
});

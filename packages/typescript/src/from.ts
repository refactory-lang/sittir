// Auto-generated by @sittir/codegen — do not edit
// .from() API — shared resolvers, immutable ergonomic setters

import type { AbstractClassDeclaration, AbstractClassDeclarationConfig, AbstractMethodSignature, AbstractMethodSignatureConfig, AddingTypeAnnotation, AddingTypeAnnotationConfig, AmbientDeclaration, AmbientDeclarationConfig, AmbientDeclarationGlobal, AmbientDeclarationGlobalConfig, AmbientDeclarationModule, AmbientDeclarationModuleConfig, AmbientDeclarationV0, AmbientDeclarationV0Config, Arguments, ArgumentsConfig, Array, ArrayConfig, ArrayPattern, ArrayPatternConfig, ArrayType, ArrayTypeConfig, ArrowFunction, ArrowFunctionConfig, AsExpression, AsExpressionConfig, Asserts, AssertsAnnotation, AssertsAnnotationConfig, AssertsConfig, AssignmentExpression, AssignmentExpressionConfig, AssignmentPattern, AssignmentPatternConfig, AugmentedAssignmentExpression, AugmentedAssignmentExpressionConfig, AugmentedAssignmentLhs, AwaitExpression, AwaitExpressionConfig, BinaryExpression, BinaryExpressionAmp, BinaryExpressionAmpConfig, BinaryExpressionAngle, BinaryExpressionAngleConfig, BinaryExpressionCaret, BinaryExpressionCaretConfig, BinaryExpressionCloseAngle, BinaryExpressionCloseAngleConfig, BinaryExpressionConfig, BinaryExpressionIn, BinaryExpressionInConfig, BinaryExpressionInstanceof, BinaryExpressionInstanceofConfig, BinaryExpressionMinus, BinaryExpressionMinusConfig, BinaryExpressionPercent, BinaryExpressionPercentConfig, BinaryExpressionPipe, BinaryExpressionPipeConfig, BinaryExpressionPlus, BinaryExpressionPlusConfig, BinaryExpressionSlash, BinaryExpressionSlashConfig, BinaryExpressionStar, BinaryExpressionStarConfig, BinaryExpressionTok_213d, BinaryExpressionTok_213d3d, BinaryExpressionTok_213d3dConfig, BinaryExpressionTok_213dConfig, BinaryExpressionTok_2626, BinaryExpressionTok_2626Config, BinaryExpressionTok_2a2a, BinaryExpressionTok_2a2aConfig, BinaryExpressionTok_3c3c, BinaryExpressionTok_3c3cConfig, BinaryExpressionTok_3c3d, BinaryExpressionTok_3c3dConfig, BinaryExpressionTok_3d3d, BinaryExpressionTok_3d3d3d, BinaryExpressionTok_3d3d3dConfig, BinaryExpressionTok_3d3dConfig, BinaryExpressionTok_3e3d, BinaryExpressionTok_3e3dConfig, BinaryExpressionTok_3e3e, BinaryExpressionTok_3e3e3e, BinaryExpressionTok_3e3e3eConfig, BinaryExpressionTok_3e3eConfig, BinaryExpressionTok_3f3f, BinaryExpressionTok_3f3fConfig, BinaryExpressionTok_7c7c, BinaryExpressionTok_7c7cConfig, BreakStatement, BreakStatementConfig, CallExpression, CallExpressionConfig, CallExpressionTok_3f2e, CallExpressionTok_3f2eConfig, CallExpressionV0, CallExpressionV0Config, CallExpressionV1, CallExpressionV1Config, CallSignature, CallSignatureConfig, CatchClause, CatchClauseConfig, Class, ClassBody, ClassBodyComma, ClassBodyCommaConfig, ClassBodyConfig, ClassBodyDecorator, ClassBodyDecoratorConfig, ClassBodySemi, ClassBodySemiConfig, ClassBodyV2, ClassBodyV2Config, ClassConfig, ClassDeclaration, ClassDeclarationConfig, ClassHeritage, ClassHeritageConfig, ClassStaticBlock, ClassStaticBlockConfig, ComputedPropertyName, ComputedPropertyNameConfig, ConditionalType, ConditionalTypeConfig, Constraint, ConstraintConfig, ConstructSignature, ConstructSignatureConfig, ConstructorType, ConstructorTypeConfig, ContinueStatement, ContinueStatementConfig, Declaration, Decorator, DecoratorConfig, DefaultType, DefaultTypeConfig, DestructuringPattern, DoStatement, DoStatementConfig, ElseClause, ElseClauseConfig, EnumAssignment, EnumAssignmentConfig, EnumBody, EnumBodyConfig, EnumDeclaration, EnumDeclarationConfig, EscapeSequence, ExportClause, ExportClauseConfig, ExportSpecifier, ExportSpecifierConfig, ExportStatement, ExportStatementAs, ExportStatementAsConfig, ExportStatementConfig, ExportStatementEq, ExportStatementEqConfig, ExportStatementStar, ExportStatementStarConfig, ExportStatementTypeKw, ExportStatementTypeKwConfig, ExportStatementV1, ExportStatementV1Config, ExportStatementV2, ExportStatementV2Config, ExportStatementV3, ExportStatementV3Config, ExportStatementValue, ExportStatementValueConfig, Expression, ExpressionStatement, ExpressionStatementConfig, Expressions, ExtendsClause, ExtendsClauseConfig, ExtendsTypeClause, ExtendsTypeClauseConfig, False, FinallyClause, FinallyClauseConfig, FlowMaybeType, FlowMaybeTypeConfig, ForInStatement, ForInStatementConfig, ForStatement, ForStatementConfig, ForStatementV0, ForStatementV0Config, ForStatementV1, ForStatementV1Config, FormalParameters, FormalParametersConfig, FunctionDeclaration, FunctionDeclarationConfig, FunctionExpression, FunctionExpressionConfig, FunctionSignature, FunctionSignatureConfig, FunctionType, FunctionTypeConfig, GeneratorFunction, GeneratorFunctionConfig, GeneratorFunctionDeclaration, GeneratorFunctionDeclarationConfig, GenericType, GenericTypeConfig, Identifier, IfStatement, IfStatementConfig, ImplementsClause, ImplementsClauseConfig, ImportAlias, ImportAliasConfig, ImportAttribute, ImportAttributeConfig, ImportClause, ImportClauseComma, ImportClauseCommaConfig, ImportClauseConfig, ImportClauseV0, ImportClauseV0Config, ImportIdentifier, ImportRequireClause, ImportRequireClauseConfig, ImportSpecifier, ImportSpecifierAs, ImportSpecifierAsConfig, ImportSpecifierConfig, ImportSpecifierV0, ImportSpecifierV0Config, ImportStatement, ImportStatementConfig, ImportStatementFromClause, ImportStatementFromClauseConfig, ImportStatementSource, ImportStatementSourceConfig, ImportStatementV1, ImportStatementV1Config, IndexSignature, IndexSignatureColon, IndexSignatureColonConfig, IndexSignatureConfig, IndexSignatureMappedTypeClause, IndexSignatureMappedTypeClauseConfig, IndexTypeQuery, IndexTypeQueryConfig, InferType, InferTypeConfig, InstantiationExpression, InstantiationExpressionConfig, InterfaceBody, InterfaceBodyConfig, InterfaceDeclaration, InterfaceDeclarationConfig, InternalModule, InternalModuleConfig, IntersectionType, IntersectionTypeConfig, LabeledStatement, LabeledStatementConfig, LexicalDeclaration, LexicalDeclarationConfig, LhsExpression, LiteralType, LiteralTypeConfig, LookupType, LookupTypeConfig, MappedTypeClause, MappedTypeClauseConfig, MemberExpression, MemberExpressionConfig, MethodDefinition, MethodDefinitionConfig, MethodSignature, MethodSignatureConfig, Module, ModuleConfig, ModuleExportName, NamedImports, NamedImportsConfig, NamespaceExport, NamespaceExportConfig, NamespaceImport, NamespaceImportConfig, NestedIdentifier, NestedIdentifierConfig, NestedTypeIdentifier, NestedTypeIdentifierConfig, NewExpression, NewExpressionConfig, NonNullExpression, NonNullExpressionConfig, Null, Number, Object, ObjectAssignmentPattern, ObjectAssignmentPatternConfig, ObjectConfig, ObjectPattern, ObjectPatternConfig, ObjectType, ObjectTypeConfig, OmittingTypeAnnotation, OmittingTypeAnnotationConfig, OptingTypeAnnotation, OptingTypeAnnotationConfig, OptionalParameter, OptionalParameterConfig, OptionalType, OptionalTypeConfig, Pair, PairConfig, PairPattern, PairPatternConfig, ParameterName, ParenthesizedExpression, ParenthesizedExpressionConfig, ParenthesizedExpressionType, ParenthesizedExpressionTypeConfig, ParenthesizedExpressionV1, ParenthesizedExpressionV1Config, ParenthesizedType, ParenthesizedTypeConfig, Pattern, PrimaryExpression, PrimaryType, Program, ProgramConfig, PropertyName, PropertySignature, PropertySignatureConfig, PublicFieldDefinition, PublicFieldDefinitionAbstract, PublicFieldDefinitionAbstractConfig, PublicFieldDefinitionAccessor, PublicFieldDefinitionAccessorConfig, PublicFieldDefinitionConfig, PublicFieldDefinitionStatic, PublicFieldDefinitionStaticConfig, ReadonlyType, ReadonlyTypeConfig, Regex, RegexConfig, RequiredParameter, RequiredParameterConfig, RestPattern, RestPatternConfig, RestType, RestTypeConfig, ReturnStatement, ReturnStatementConfig, SatisfiesExpression, SatisfiesExpressionConfig, SequenceExpression, SequenceExpressionConfig, ShorthandPropertyIdentifier, ShorthandPropertyIdentifierPattern, SpreadElement, SpreadElementConfig, Statement, StatementBlock, StatementBlockConfig, String, StringConfig, StringFragment, SubscriptExpression, SubscriptExpressionConfig, SwitchBody, SwitchBodyConfig, SwitchCase, SwitchCaseConfig, SwitchDefault, SwitchDefaultConfig, SwitchStatement, SwitchStatementConfig, TemplateLiteralType, TemplateLiteralTypeConfig, TemplateString, TemplateStringConfig, TemplateSubstitution, TemplateSubstitutionConfig, TemplateType, TemplateTypeConfig, TernaryExpression, TernaryExpressionConfig, This, ThrowStatement, ThrowStatementConfig, True, TryStatement, TryStatementConfig, TupleType, TupleTypeConfig, Type, TypeAliasDeclaration, TypeAliasDeclarationConfig, TypeAnnotation, TypeAnnotationConfig, TypeArguments, TypeArgumentsConfig, TypeAssertion, TypeAssertionConfig, TypeIdentifier, TypeParameter, TypeParameterConfig, TypeParameters, TypeParametersConfig, TypePredicate, TypePredicateAnnotation, TypePredicateAnnotationConfig, TypePredicateConfig, TypeQuery, TypeQueryConfig, UnaryExpression, UnaryExpressionConfig, Undefined, UnionType, UnionTypeConfig, UpdateExpression, UpdateExpressionConfig, VariableDeclaration, VariableDeclarationConfig, VariableDeclarator, VariableDeclaratorBang, VariableDeclaratorBangConfig, VariableDeclaratorConfig, VariableDeclaratorV0, VariableDeclaratorV0Config, WhileStatement, WhileStatementConfig, WithStatement, WithStatementConfig, YieldExpression, YieldExpressionConfig, YieldExpressionStar, YieldExpressionStarConfig, YieldExpressionV1, YieldExpressionV1Config } from './types.js';
import type { KindMap, FromInputMap } from './types.js';
import type { RuntimeNodeOf } from '@sittir/types';
import { isNodeData, hasKind } from './utils.js';

import { abstract_class_declaration_, abstract_method_signature_, accessibility_modifier_, adding_type_annotation_, ambient_declaration_, ambient_declaration__global_, ambient_declaration__module_, ambient_declaration__v0_, arguments_, array_, array_pattern_, array_type_, arrow_function_, as_expression_, asserts_, asserts_annotation_, assignment_expression_, assignment_pattern_, augmented_assignment_expression_, await_expression_, binary_expression_, binary_expression__amp_, binary_expression__angle_, binary_expression__caret_, binary_expression__close_angle_, binary_expression__in_, binary_expression__instanceof_, binary_expression__minus_, binary_expression__percent_, binary_expression__pipe_, binary_expression__plus_, binary_expression__slash_, binary_expression__star_, binary_expression__tok_213d3d_, binary_expression__tok_213d_, binary_expression__tok_2626_, binary_expression__tok_2a2a_, binary_expression__tok_3c3c_, binary_expression__tok_3c3d_, binary_expression__tok_3d3d3d_, binary_expression__tok_3d3d_, binary_expression__tok_3e3d_, binary_expression__tok_3e3e3e_, binary_expression__tok_3e3e_, binary_expression__tok_3f3f_, binary_expression__tok_7c7c_, break_statement_, call_expression_, call_expression__tok_3f2e_, call_expression__v0_, call_expression__v1_, call_signature_, catch_clause_, class_, class_body_, class_body__comma_, class_body__decorator_, class_body__semi_, class_body__v2_, class_declaration_, class_heritage_, class_static_block_, comment_, computed_property_name_, conditional_type_, constraint_, construct_signature_, constructor_type_, continue_statement_, debugger_statement_, decorator_, default_type_, do_statement_, else_clause_, empty_statement_, enum_assignment_, enum_body_, enum_declaration_, escape_sequence_, existential_type_, export_clause_, export_specifier_, export_statement_, export_statement__as_, export_statement__eq_, export_statement__star_, export_statement__type_kw_, export_statement__v1_, export_statement__v2_, export_statement__v3_, export_statement__value_, expression_statement_, extends_clause_, extends_type_clause_, false_, finally_clause_, flow_maybe_type_, for_in_statement_, for_statement_, for_statement__v0_, for_statement__v1_, formal_parameters_, function_declaration_, function_expression_, function_signature_, function_type_, generator_function_, generator_function_declaration_, generic_type_, hash_bang_line_, html_comment_, identifier_, if_statement_, implements_clause_, import_, import_alias_, import_attribute_, import_clause_, import_clause__comma_, import_clause__v0_, import_require_clause_, import_specifier_, import_specifier__as_, import_specifier__v0_, import_statement_, import_statement__from_clause_, import_statement__source_, import_statement__v1_, index_signature_, index_signature__colon_, index_signature__mapped_type_clause_, index_type_query_, infer_type_, instantiation_expression_, interface_body_, interface_declaration_, internal_module_, intersection_type_, labeled_statement_, lexical_declaration_, literal_type_, lookup_type_, mapped_type_clause_, member_expression_, meta_property_, method_definition_, method_signature_, module_, named_imports_, namespace_export_, namespace_import_, nested_identifier_, nested_type_identifier_, new_expression_, non_null_expression_, null_, number_, object_, object_assignment_pattern_, object_pattern_, object_type_, omitting_type_annotation_, opting_type_annotation_, optional_chain_, optional_parameter_, optional_type_, override_modifier_, pair_, pair_pattern_, parenthesized_expression_, parenthesized_expression__type_, parenthesized_expression__v1_, parenthesized_type_, predefined_type_, private_property_identifier_, program_, property_identifier_, property_signature_, public_field_definition_, public_field_definition__abstract_, public_field_definition__accessor_, public_field_definition__static_, readonly_type_, regex_, regex_flags_, regex_pattern_, required_parameter_, rest_pattern_, rest_type_, return_statement_, satisfies_expression_, sequence_expression_, shorthand_property_identifier_, shorthand_property_identifier_pattern_, spread_element_, statement_block_, statement_identifier_, string_, string_fragment_, subscript_expression_, super_, switch_body_, switch_case_, switch_default_, switch_statement_, template_literal_type_, template_string_, template_substitution_, template_type_, ternary_expression_, this_, this_type_, throw_statement_, true_, try_statement_, tuple_type_, type_alias_declaration_, type_annotation_, type_arguments_, type_assertion_, type_identifier_, type_parameter_, type_parameters_, type_predicate_, type_predicate_annotation_, type_query_, unary_expression_, undefined_, union_type_, update_expression_, variable_declaration_, variable_declarator_, variable_declarator__bang_, variable_declarator__v0_, while_statement_, with_statement_, yield_expression_, yield_expression__star_, yield_expression__v1_ } from './factories.js';
import type { AbstractClassDeclarationFromInput, AbstractMethodSignatureFromInput, AddingTypeAnnotationFromInput, AmbientDeclarationFromInput, ArgumentsFromInput, ArrayFromInput, ArrayPatternFromInput, ArrayTypeFromInput, ArrowFunctionFromInput, AsExpressionFromInput, AssertsFromInput, AssertsAnnotationFromInput, AssignmentExpressionFromInput, AssignmentPatternFromInput, AugmentedAssignmentExpressionFromInput, AwaitExpressionFromInput, BinaryExpressionFromInput, BreakStatementFromInput, CallExpressionFromInput, CallSignatureFromInput, CatchClauseFromInput, ClassFromInput, ClassBodyFromInput, ClassDeclarationFromInput, ClassHeritageFromInput, ClassStaticBlockFromInput, ComputedPropertyNameFromInput, ConditionalTypeFromInput, ConstraintFromInput, ConstructSignatureFromInput, ConstructorTypeFromInput, ContinueStatementFromInput, DecoratorFromInput, DefaultTypeFromInput, DoStatementFromInput, ElseClauseFromInput, EnumAssignmentFromInput, EnumBodyFromInput, EnumDeclarationFromInput, ExportClauseFromInput, ExportSpecifierFromInput, ExportStatementFromInput, ExpressionStatementFromInput, ExtendsClauseFromInput, ExtendsTypeClauseFromInput, FinallyClauseFromInput, FlowMaybeTypeFromInput, ForInStatementFromInput, ForStatementFromInput, FormalParametersFromInput, FunctionDeclarationFromInput, FunctionExpressionFromInput, FunctionSignatureFromInput, FunctionTypeFromInput, GeneratorFunctionFromInput, GeneratorFunctionDeclarationFromInput, GenericTypeFromInput, IfStatementFromInput, ImplementsClauseFromInput, ImportAliasFromInput, ImportAttributeFromInput, ImportClauseFromInput, ImportRequireClauseFromInput, ImportSpecifierFromInput, ImportStatementFromInput, IndexSignatureFromInput, IndexTypeQueryFromInput, InferTypeFromInput, InstantiationExpressionFromInput, InterfaceBodyFromInput, InterfaceDeclarationFromInput, InternalModuleFromInput, IntersectionTypeFromInput, LabeledStatementFromInput, LexicalDeclarationFromInput, LiteralTypeFromInput, LookupTypeFromInput, MappedTypeClauseFromInput, MemberExpressionFromInput, MethodDefinitionFromInput, MethodSignatureFromInput, ModuleFromInput, NamedImportsFromInput, NamespaceExportFromInput, NamespaceImportFromInput, NestedIdentifierFromInput, NestedTypeIdentifierFromInput, NewExpressionFromInput, NonNullExpressionFromInput, ObjectFromInput, ObjectAssignmentPatternFromInput, ObjectPatternFromInput, ObjectTypeFromInput, OmittingTypeAnnotationFromInput, OptingTypeAnnotationFromInput, OptionalParameterFromInput, OptionalTypeFromInput, PairFromInput, PairPatternFromInput, ParenthesizedExpressionFromInput, ParenthesizedTypeFromInput, ProgramFromInput, PropertySignatureFromInput, PublicFieldDefinitionFromInput, ReadonlyTypeFromInput, RegexFromInput, RequiredParameterFromInput, RestPatternFromInput, RestTypeFromInput, ReturnStatementFromInput, SatisfiesExpressionFromInput, SequenceExpressionFromInput, SpreadElementFromInput, StatementBlockFromInput, StringFromInput, SubscriptExpressionFromInput, SwitchBodyFromInput, SwitchCaseFromInput, SwitchDefaultFromInput, SwitchStatementFromInput, TemplateLiteralTypeFromInput, TemplateStringFromInput, TemplateSubstitutionFromInput, TemplateTypeFromInput, TernaryExpressionFromInput, ThrowStatementFromInput, TryStatementFromInput, TupleTypeFromInput, TypeAliasDeclarationFromInput, TypeAnnotationFromInput, TypeArgumentsFromInput, TypeAssertionFromInput, TypeParameterFromInput, TypeParametersFromInput, TypePredicateFromInput, TypePredicateAnnotationFromInput, TypeQueryFromInput, UnaryExpressionFromInput, UnionTypeFromInput, UpdateExpressionFromInput, VariableDeclarationFromInput, VariableDeclaratorFromInput, WhileStatementFromInput, WithStatementFromInput, YieldExpressionFromInput, AmbientDeclarationV0FromInput, AmbientDeclarationGlobalFromInput, AmbientDeclarationModuleFromInput, BinaryExpressionTok_2626FromInput, BinaryExpressionTok_7c7cFromInput, BinaryExpressionTok_3e3eFromInput, BinaryExpressionTok_3e3e3eFromInput, BinaryExpressionTok_3c3cFromInput, BinaryExpressionAmpFromInput, BinaryExpressionCaretFromInput, BinaryExpressionPipeFromInput, BinaryExpressionPlusFromInput, BinaryExpressionMinusFromInput, BinaryExpressionStarFromInput, BinaryExpressionSlashFromInput, BinaryExpressionPercentFromInput, BinaryExpressionTok_2a2aFromInput, BinaryExpressionAngleFromInput, BinaryExpressionTok_3c3dFromInput, BinaryExpressionTok_3d3dFromInput, BinaryExpressionTok_3d3d3dFromInput, BinaryExpressionTok_213dFromInput, BinaryExpressionTok_213d3dFromInput, BinaryExpressionTok_3e3dFromInput, BinaryExpressionCloseAngleFromInput, BinaryExpressionTok_3f3fFromInput, BinaryExpressionInstanceofFromInput, BinaryExpressionInFromInput, CallExpressionV0FromInput, CallExpressionV1FromInput, CallExpressionTok_3f2eFromInput, ClassBodyDecoratorFromInput, ClassBodyCommaFromInput, ClassBodyV2FromInput, ClassBodySemiFromInput, ExportStatementStarFromInput, ExportStatementV1FromInput, ExportStatementV2FromInput, ExportStatementV3FromInput, ExportStatementValueFromInput, ExportStatementTypeKwFromInput, ExportStatementEqFromInput, ExportStatementAsFromInput, ForStatementV0FromInput, ForStatementV1FromInput, ImportClauseV0FromInput, ImportClauseCommaFromInput, ImportSpecifierV0FromInput, ImportSpecifierAsFromInput, ImportStatementFromClauseFromInput, ImportStatementV1FromInput, ImportStatementSourceFromInput, IndexSignatureColonFromInput, IndexSignatureMappedTypeClauseFromInput, ParenthesizedExpressionTypeFromInput, ParenthesizedExpressionV1FromInput, PublicFieldDefinitionStaticFromInput, PublicFieldDefinitionAbstractFromInput, PublicFieldDefinitionAccessorFromInput, VariableDeclaratorV0FromInput, VariableDeclaratorBangFromInput, YieldExpressionStarFromInput, YieldExpressionV1FromInput } from './types.js';

/** Maps kind string to its typed .from() resolver. */
export type FromMap = {
  [K in keyof FromInputMap]: (input: KindMap[K] | FromInputMap[K]) => RuntimeNodeOf<KindMap[K]>;
};

/** @internal Map of kind string to .from() resolver function. */
export const _fromMap = {
  'abstract_class_declaration': abstractClassDeclarationFrom,
  'abstract_method_signature': abstractMethodSignatureFrom,
  'adding_type_annotation': addingTypeAnnotationFrom,
  'ambient_declaration': ambientDeclarationFrom,
  'arguments': arguments_From,
  'array': arrayFrom,
  'array_pattern': arrayPatternFrom,
  'array_type': arrayTypeFrom,
  'arrow_function': arrowFunctionFrom,
  'as_expression': asExpressionFrom,
  'asserts': assertsFrom,
  'asserts_annotation': assertsAnnotationFrom,
  'assignment_expression': assignmentExpressionFrom,
  'assignment_pattern': assignmentPatternFrom,
  'augmented_assignment_expression': augmentedAssignmentExpressionFrom,
  'await_expression': awaitExpressionFrom,
  'binary_expression': binaryExpressionFrom,
  'break_statement': breakStatementFrom,
  'call_expression': callExpressionFrom,
  'call_signature': callSignatureFrom,
  'catch_clause': catchClauseFrom,
  'class': class_From,
  'class_body': classBodyFrom,
  'class_declaration': classDeclarationFrom,
  'class_heritage': classHeritageFrom,
  'class_static_block': classStaticBlockFrom,
  'computed_property_name': computedPropertyNameFrom,
  'conditional_type': conditionalTypeFrom,
  'constraint': constraintFrom,
  'construct_signature': constructSignatureFrom,
  'constructor_type': constructorTypeFrom,
  'continue_statement': continueStatementFrom,
  'decorator': decoratorFrom,
  'default_type': defaultTypeFrom,
  'do_statement': doStatementFrom,
  'else_clause': elseClauseFrom,
  'enum_assignment': enumAssignmentFrom,
  'enum_body': enumBodyFrom,
  'enum_declaration': enumDeclarationFrom,
  'export_clause': exportClauseFrom,
  'export_specifier': exportSpecifierFrom,
  'export_statement': exportStatementFrom,
  'expression_statement': expressionStatementFrom,
  'extends_clause': extendsClauseFrom,
  'extends_type_clause': extendsTypeClauseFrom,
  'finally_clause': finallyClauseFrom,
  'flow_maybe_type': flowMaybeTypeFrom,
  'for_in_statement': forInStatementFrom,
  'for_statement': forStatementFrom,
  'formal_parameters': formalParametersFrom,
  'function_declaration': functionDeclarationFrom,
  'function_expression': functionExpressionFrom,
  'function_signature': functionSignatureFrom,
  'function_type': functionTypeFrom,
  'generator_function': generatorFunctionFrom,
  'generator_function_declaration': generatorFunctionDeclarationFrom,
  'generic_type': genericTypeFrom,
  'if_statement': ifStatementFrom,
  'implements_clause': implementsClauseFrom,
  'import_alias': importAliasFrom,
  'import_attribute': importAttributeFrom,
  'import_clause': importClauseFrom,
  'import_require_clause': importRequireClauseFrom,
  'import_specifier': importSpecifierFrom,
  'import_statement': importStatementFrom,
  'index_signature': indexSignatureFrom,
  'index_type_query': indexTypeQueryFrom,
  'infer_type': inferTypeFrom,
  'instantiation_expression': instantiationExpressionFrom,
  'interface_body': interfaceBodyFrom,
  'interface_declaration': interfaceDeclarationFrom,
  'internal_module': internalModuleFrom,
  'intersection_type': intersectionTypeFrom,
  'labeled_statement': labeledStatementFrom,
  'lexical_declaration': lexicalDeclarationFrom,
  'literal_type': literalTypeFrom,
  'lookup_type': lookupTypeFrom,
  'mapped_type_clause': mappedTypeClauseFrom,
  'member_expression': memberExpressionFrom,
  'method_definition': methodDefinitionFrom,
  'method_signature': methodSignatureFrom,
  'module': moduleFrom,
  'named_imports': namedImportsFrom,
  'namespace_export': namespaceExportFrom,
  'namespace_import': namespaceImportFrom,
  'nested_identifier': nestedIdentifierFrom,
  'nested_type_identifier': nestedTypeIdentifierFrom,
  'new_expression': newExpressionFrom,
  'non_null_expression': nonNullExpressionFrom,
  'object': objectFrom,
  'object_assignment_pattern': objectAssignmentPatternFrom,
  'object_pattern': objectPatternFrom,
  'object_type': objectTypeFrom,
  'omitting_type_annotation': omittingTypeAnnotationFrom,
  'opting_type_annotation': optingTypeAnnotationFrom,
  'optional_parameter': optionalParameterFrom,
  'optional_type': optionalTypeFrom,
  'pair': pairFrom,
  'pair_pattern': pairPatternFrom,
  'parenthesized_expression': parenthesizedExpressionFrom,
  'parenthesized_type': parenthesizedTypeFrom,
  'program': programFrom,
  'property_signature': propertySignatureFrom,
  'public_field_definition': publicFieldDefinitionFrom,
  'readonly_type': readonlyTypeFrom,
  'regex': regexFrom,
  'required_parameter': requiredParameterFrom,
  'rest_pattern': restPatternFrom,
  'rest_type': restTypeFrom,
  'return_statement': returnStatementFrom,
  'satisfies_expression': satisfiesExpressionFrom,
  'sequence_expression': sequenceExpressionFrom,
  'spread_element': spreadElementFrom,
  'statement_block': statementBlockFrom,
  'string': stringFrom,
  'subscript_expression': subscriptExpressionFrom,
  'switch_body': switchBodyFrom,
  'switch_case': switchCaseFrom,
  'switch_default': switchDefaultFrom,
  'switch_statement': switchStatementFrom,
  'template_literal_type': templateLiteralTypeFrom,
  'template_string': templateStringFrom,
  'template_substitution': templateSubstitutionFrom,
  'template_type': templateTypeFrom,
  'ternary_expression': ternaryExpressionFrom,
  'throw_statement': throwStatementFrom,
  'try_statement': tryStatementFrom,
  'tuple_type': tupleTypeFrom,
  'type_alias_declaration': typeAliasDeclarationFrom,
  'type_annotation': typeAnnotationFrom,
  'type_arguments': typeArgumentsFrom,
  'type_assertion': typeAssertionFrom,
  'type_parameter': typeParameterFrom,
  'type_parameters': typeParametersFrom,
  'type_predicate': typePredicateFrom,
  'type_predicate_annotation': typePredicateAnnotationFrom,
  'type_query': typeQueryFrom,
  'unary_expression': unaryExpressionFrom,
  'union_type': unionTypeFrom,
  'update_expression': updateExpressionFrom,
  'variable_declaration': variableDeclarationFrom,
  'variable_declarator': variableDeclaratorFrom,
  'while_statement': whileStatementFrom,
  'with_statement': withStatementFrom,
  'yield_expression': yieldExpressionFrom,
};

export function _resolveByKind(kind: string, rest: unknown) {
  const fn = (_fromMap as Record<string, (input: any) => unknown>)[kind];
  if (fn) return fn(rest);
  throw new Error(`Unknown kind for .from(): '${kind}'`);
}

/** Leaf kind registry for string dispatch. */
const _leafRegistry: Record<string, { values?: string[]; pattern?: RegExp; factory: (text: string) => unknown }> = {
  'accessibility_modifier': { values: ["private","protected","public"], factory: accessibility_modifier_ },
  'debugger_statement': { factory: debugger_statement_ },
  'empty_statement': { values: [";"], factory: () => empty_statement_() },
  'existential_type': { values: ["*"], factory: () => existential_type_() },
  'identifier': { factory: identifier_ },
  'import': { values: ["import"], factory: () => import_() },
  'meta_property': { pattern: /^(?:new\.target|import\.meta)$/, factory: meta_property_ },
  'optional_chain': { values: ["?."], factory: () => optional_chain_() },
  'override_modifier': { values: ["override"], factory: () => override_modifier_() },
  'predefined_type': { values: ["any","boolean","never","number","object","string","string","symbol","unknown","void"], factory: predefined_type_ },
  'comment': { factory: comment_ },
  'escape_sequence': { pattern: /^\\(?:[^xu0-7]|[0-7]{1,3}|x[0-9a-fA-F]{2}|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|[\r?][\n\u2028\u2029])$/, factory: escape_sequence_ },
  'false': { values: ["false"], factory: () => false_() },
  'hash_bang_line': { pattern: /^#!.*$/, factory: hash_bang_line_ },
  'html_comment': { factory: html_comment_ },
  'null': { values: ["null"], factory: () => null_() },
  'number': { pattern: /^(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:(?:0|0?[1-9]_?\d(_?\d)*?)\.\d(_?\d)*?(?:e|E)(?:-|\+)?\d(_?\d)*?|\.\d(_?\d)*(?:e|E)(?:-|\+)?\d(_?\d)*?|(?:0|0?[1-9]_?\d(_?\d)*?)(?:e|E)(?:-|\+)?\d(_?\d)*|\d(_?\d)*)|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|\d(_?\d)*)n)$/, factory: number_ },
  'private_property_identifier': { factory: private_property_identifier_ },
  'property_identifier': { factory: property_identifier_ },
  'regex_flags': { pattern: /^[a-z]+$/, factory: regex_flags_ },
  'regex_pattern': { pattern: /^(?:(?:\[(?:(?:\\.|[^\]\n\\]))*\]|\\.|[^\/\\\[\n]))+$/, factory: regex_pattern_ },
  'shorthand_property_identifier': { factory: shorthand_property_identifier_ },
  'shorthand_property_identifier_pattern': { factory: shorthand_property_identifier_pattern_ },
  'statement_identifier': { factory: statement_identifier_ },
  'string_fragment': { factory: string_fragment_ },
  'super': { values: ["super"], factory: () => super_() },
  'this': { values: ["this"], factory: () => this_() },
  'this_type': { factory: this_type_ },
  'true': { values: ["true"], factory: () => true_() },
  'type_identifier': { factory: type_identifier_ },
  'undefined': { values: ["undefined"], factory: () => undefined_() },
};

/**
 * Resolve a string to the best matching leaf factory from a set of accepted kinds.
 * Returns undefined if no leaf kind matches.
 */
function _resolveLeafString(v: string, kinds: readonly string[]): unknown {
  for (const kind of kinds) {
    const entry = _leafRegistry[kind];
    if (!entry) continue;
    if (entry.values && entry.values.includes(v)) return entry.factory(v);
    if (entry.pattern && entry.pattern.test(v)) return entry.factory(v);
  }
  for (const kind of kinds) {
    const entry = _leafRegistry[kind];
    if (entry && !entry.values && !entry.pattern) return entry.factory(v);
  }
  return undefined;
}

function _resolveDeclaration(v: unknown): Declaration;
function _resolveDeclaration(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: abstract_class_declaration, ambient_declaration, class_declaration, enum_declaration, function_declaration, function_signature, generator_function_declaration, import_alias, interface_declaration, internal_module, lexical_declaration, module, type_alias_declaration, variable_declaration. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryExpression(v: unknown): PrimaryExpression;
function _resolvePrimaryExpression(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["false","identifier","meta_property","null","number","super","this","true","undefined"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpression(v: unknown): Expression;
function _resolveExpression(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["false","identifier","meta_property","null","number","super","this","true","undefined"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array, arrow_function, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, call_expression, class, function_expression, generator_function, instantiation_expression, internal_module, member_expression, new_expression, non_null_expression, object, parenthesized_expression, regex, satisfies_expression, string, subscript_expression, template_string, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePattern(v: unknown): Pattern;
function _resolvePattern(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","undefined"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_pattern, member_expression, non_null_expression, object_pattern, rest_pattern, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryType(v: unknown): PrimaryType;
function _resolvePrimaryType(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["existential_type","predefined_type","this_type","type_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_type, conditional_type, flow_maybe_type, generic_type, index_type_query, intersection_type, literal_type, lookup_type, nested_type_identifier, object_type, parenthesized_type, template_literal_type, tuple_type, type_query, union_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveStatement(v: unknown): Statement;
function _resolveStatement(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["debugger_statement","empty_statement"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: abstract_class_declaration, ambient_declaration, break_statement, class_declaration, continue_statement, do_statement, enum_declaration, export_statement, expression_statement, for_in_statement, for_statement, function_declaration, function_signature, generator_function_declaration, if_statement, import_alias, import_statement, interface_declaration, internal_module, labeled_statement, lexical_declaration, module, return_statement, statement_block, switch_statement, throw_statement, try_statement, type_alias_declaration, variable_declaration, while_statement, with_statement. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveType(v: unknown): Type;
function _resolveType(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["existential_type","predefined_type","this_type","type_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_type, call_expression, conditional_type, constructor_type, flow_maybe_type, function_type, generic_type, index_type_query, infer_type, intersection_type, literal_type, lookup_type, member_expression, nested_type_identifier, object_type, parenthesized_type, readonly_type, template_literal_type, tuple_type, type_query, union_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier(v: unknown): TypeIdentifier;
function _resolveTypeIdentifier(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["type_identifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePropertyName(v: unknown): PropertyName;
function _resolvePropertyName(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["number","private_property_identifier","property_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: computed_property_name, string. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveLhsExpression(v: unknown): LhsExpression;
function _resolveLhsExpression(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","undefined"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_pattern, member_expression, non_null_expression, object_pattern, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveAugmentedAssignmentLhs(v: unknown): AugmentedAssignmentLhs;
function _resolveAugmentedAssignmentLhs(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: member_expression, non_null_expression, parenthesized_expression, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveDestructuringPattern(v: unknown): DestructuringPattern;
function _resolveDestructuringPattern(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_pattern, object_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveModuleExportName(v: unknown): ModuleExportName;
function _resolveModuleExportName(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('string', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('string', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressions(v: unknown): Expressions;
function _resolveExpressions(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["false","identifier","meta_property","null","number","super","this","true","undefined"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array, arrow_function, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, call_expression, class, function_expression, generator_function, instantiation_expression, internal_module, member_expression, new_expression, non_null_expression, object, parenthesized_expression, regex, satisfies_expression, sequence_expression, string, subscript_expression, template_string, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveImportIdentifier(v: unknown): ImportIdentifier;
function _resolveImportIdentifier(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveParameterName(v: unknown): ParameterName;
function _resolveParameterName(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["accessibility_modifier","override_modifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rzpcalh(v: unknown): KindMap['class_body'];
function _rzpcalh(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('class_body', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('class_body', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1fl8679(v: unknown): KindMap['decorator'];
function _r1fl8679(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('decorator', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('decorator', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier2(v: unknown): KindMap['type_identifier'] | TypeIdentifier;
function _resolveTypeIdentifier2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["type_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveTypeIdentifier(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveTypeIdentifier(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r19n9507(v: unknown): KindMap['type_parameters'];
function _r19n9507(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('type_parameters', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('type_parameters', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1v8cdsw(v: unknown): KindMap['class_heritage'];
function _r1v8cdsw(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('class_heritage', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('class_heritage', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePropertyName2(v: unknown): KindMap['number'] | KindMap['private_property_identifier'] | KindMap['property_identifier'] | KindMap['computed_property_name'] | KindMap['string'] | PropertyName;
function _resolvePropertyName2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["number","private_property_identifier","property_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: computed_property_name, string. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1400tna(v: unknown): KindMap['formal_parameters'];
function _r1400tna(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('formal_parameters', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('formal_parameters', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r15nsthd(v: unknown): KindMap['asserts_annotation'] | KindMap['type_annotation'] | KindMap['type_predicate_annotation'];
function _r15nsthd(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: asserts_annotation, type_annotation, type_predicate_annotation. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveParameterName2(v: unknown): KindMap['accessibility_modifier'];
function _resolveParameterName2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["accessibility_modifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveParameterName3(v: unknown): KindMap['override_modifier'];
function _resolveParameterName3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["override_modifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier3(v: unknown): string;
function _resolveTypeIdentifier3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier4(v: unknown): Type;
function _resolveTypeIdentifier4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveType(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveType(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rca402w(v: unknown): KindMap['property_identifier'] | KindMap['statement_block'] | Declaration;
function _rca402w(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["property_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: statement_block, abstract_class_declaration, ambient_declaration, class_declaration, enum_declaration, function_declaration, function_signature, generator_function_declaration, import_alias, interface_declaration, internal_module, lexical_declaration, module, type_alias_declaration, variable_declaration. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rxueckq(v: unknown): KindMap['spread_element'] | Expression;
function _rxueckq(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: spread_element, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1lwpcqg(v: unknown): KindMap['assignment_pattern'] | Pattern;
function _r1lwpcqg(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: assignment_pattern, array_pattern, member_expression, non_null_expression, object_pattern, rest_pattern, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier5(v: unknown): PrimaryType;
function _resolveTypeIdentifier5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolvePrimaryType(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolvePrimaryType(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveStatement2(v: unknown): KindMap['statement_block'] | Expression;
function _resolveStatement2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: statement_block, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier6(v: unknown): Expression;
function _resolveTypeIdentifier6(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveExpression(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveExpression(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rc0mhwb(v: unknown): KindMap['identifier'] | KindMap['this'] | KindMap['type_predicate'];
function _rc0mhwb(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","this"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('type_predicate', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('type_predicate', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1n126sn(v: unknown): KindMap['asserts'];
function _r1n126sn(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('asserts', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('asserts', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rh2pegt(v: unknown): KindMap['identifier'] | KindMap['undefined'] | KindMap['array_pattern'] | KindMap['member_expression'] | KindMap['non_null_expression'] | KindMap['object_pattern'] | KindMap['parenthesized_expression'] | KindMap['subscript_expression'] | LhsExpression;
function _rh2pegt(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","undefined"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_pattern, member_expression, non_null_expression, object_pattern, parenthesized_expression, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier7(v: unknown): Pattern;
function _resolveTypeIdentifier7(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolvePattern(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolvePattern(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveAugmentedAssignmentLhs2(v: unknown): KindMap['identifier'] | KindMap['member_expression'] | KindMap['non_null_expression'] | KindMap['parenthesized_expression'] | KindMap['subscript_expression'] | AugmentedAssignmentLhs;
function _resolveAugmentedAssignmentLhs2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: member_expression, non_null_expression, parenthesized_expression, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier8(v: unknown): '%=' | '&&=' | '&=' | '**=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '>>>=' | '??=' | '^=' | '|=' | '||=';
function _resolveTypeIdentifier8(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['%=', '&&=', '&=', '**=', '*=', '+=', '-=', '/=', '<<=', '>>=', '>>>=', '??=', '^=', '|=', '||='].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePropertyName3(v: unknown): KindMap['private_property_identifier'] | Expression;
function _resolvePropertyName3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["private_property_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveExpression(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveExpression(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier9(v: unknown): '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||';
function _resolveTypeIdentifier9(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['!=', '!==', '%', '&', '&&', '*', '**', '+', '-', '/', '<', '<<', '<=', '==', '===', '>', '>=', '>>', '>>>', '??', '^', 'in', 'instanceof', '|', '||'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r11b7pfd(v: unknown): KindMap['statement_identifier'];
function _r11b7pfd(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["statement_identifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rq16gfq(v: unknown): KindMap['arguments'] | KindMap['template_string'];
function _rq16gfq(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: arguments, template_string. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r6g0yom(v: unknown): KindMap['import'] | KindMap['new_expression'] | Expression | PrimaryExpression;
function _r6g0yom(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["import"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: new_expression, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1uqebll(v: unknown): KindMap['type_arguments'];
function _r1uqebll(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('type_arguments', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('type_arguments', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveStatement3(v: unknown): KindMap['statement_block'];
function _resolveStatement3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('statement_block', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('statement_block', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveLhsExpression2(v: unknown): KindMap['identifier'] | KindMap['array_pattern'] | KindMap['object_pattern'] | DestructuringPattern;
function _resolveLhsExpression2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_pattern, object_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1s2j1z2(v: unknown): KindMap['type_annotation'];
function _r1s2j1z2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('type_annotation', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('type_annotation', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r19m37fc(v: unknown): KindMap['abstract_method_signature'] | KindMap['class_static_block'] | KindMap['index_signature'] | KindMap['method_definition'] | KindMap['method_signature'] | KindMap['public_field_definition'];
function _r19m37fc(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: abstract_method_signature, class_static_block, index_signature, method_definition, method_signature, public_field_definition. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1ja7eu7(v: unknown): KindMap['extends_clause'] | KindMap['implements_clause'];
function _r1ja7eu7(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: extends_clause, implements_clause. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r60me0s(v: unknown): KindMap['implements_clause'];
function _r60me0s(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('implements_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('implements_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryExpression2(v: unknown): KindMap['identifier'] | KindMap['call_expression'] | KindMap['member_expression'] | KindMap['parenthesized_expression'];
function _resolvePrimaryExpression2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: call_expression, member_expression, parenthesized_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier10(v: unknown): Statement;
function _resolveTypeIdentifier10(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveStatement(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveStatement(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveAugmentedAssignmentLhs3(v: unknown): KindMap['parenthesized_expression'];
function _resolveAugmentedAssignmentLhs3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('parenthesized_expression', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('parenthesized_expression', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1nm6gsv(v: unknown): KindMap['enum_assignment'];
function _r1nm6gsv(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('enum_assignment', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('enum_assignment', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1q8ojb8(v: unknown): KindMap['enum_body'];
function _r1q8ojb8(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('enum_body', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('enum_body', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r109ficd(v: unknown): KindMap['export_specifier'];
function _r109ficd(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('export_specifier', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('export_specifier', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveModuleExportName2(v: unknown): KindMap['identifier'] | KindMap['string'] | ModuleExportName;
function _resolveModuleExportName2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: string. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1tl6ta5(v: unknown): KindMap['identifier'] | KindMap['export_clause'] | KindMap['namespace_export'] | Declaration | Expression;
function _r1tl6ta5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: export_clause, namespace_export, abstract_class_declaration, ambient_declaration, class_declaration, enum_declaration, function_declaration, function_signature, generator_function_declaration, import_alias, interface_declaration, internal_module, lexical_declaration, module, type_alias_declaration, variable_declaration, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveModuleExportName3(v: unknown): KindMap['string'];
function _resolveModuleExportName3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('string', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('string', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1tl6ta52(v: unknown): KindMap['identifier'] | KindMap['export_clause'] | KindMap['namespace_export'] | Expression;
function _r1tl6ta52(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: export_clause, namespace_export, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressions2(v: unknown): KindMap['sequence_expression'] | Expression;
function _resolveExpressions2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: sequence_expression, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryType2(v: unknown): KindMap['type_identifier'] | KindMap['generic_type'] | KindMap['nested_type_identifier'] | TypeIdentifier;
function _resolvePrimaryType2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["type_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: generic_type, nested_type_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier11(v: unknown): 'const' | 'let' | 'var';
function _resolveTypeIdentifier11(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['const', 'let', 'var'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rh2pegt2(v: unknown): KindMap['identifier'] | KindMap['undefined'] | KindMap['array_pattern'] | KindMap['member_expression'] | KindMap['non_null_expression'] | KindMap['object_pattern'] | KindMap['parenthesized_expression'] | KindMap['subscript_expression'] | DestructuringPattern | LhsExpression;
function _rh2pegt2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","undefined"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_pattern, member_expression, non_null_expression, object_pattern, parenthesized_expression, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier12(v: unknown): 'in' | 'of';
function _resolveTypeIdentifier12(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['in', 'of'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressions3(v: unknown): KindMap['sequence_expression'] | Expressions | Expression;
function _resolveExpressions3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: sequence_expression, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1wds2lp(v: unknown): KindMap['empty_statement'] | KindMap['sequence_expression'] | Expressions | Expression;
function _r1wds2lp(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if ([';'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, ["empty_statement"]); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: sequence_expression, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1n4up13(v: unknown): KindMap['empty_statement'] | KindMap['lexical_declaration'] | KindMap['sequence_expression'] | KindMap['variable_declaration'] | Expressions | Expression;
function _r1n4up13(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["empty_statement"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: lexical_declaration, sequence_expression, variable_declaration, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1vnjrv9(v: unknown): KindMap['optional_parameter'] | KindMap['required_parameter'];
function _r1vnjrv9(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: optional_parameter, required_parameter. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rhgo79(v: unknown): KindMap['asserts'] | KindMap['type_predicate'] | Type;
function _rhgo79(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: asserts, type_predicate, call_expression, constructor_type, function_type, infer_type, member_expression, array_type, conditional_type, flow_maybe_type, generic_type, index_type_query, intersection_type, literal_type, lookup_type, nested_type_identifier, object_type, parenthesized_type, template_literal_type, tuple_type, type_query, union_type, readonly_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryType3(v: unknown): KindMap['type_identifier'] | KindMap['nested_type_identifier'] | TypeIdentifier;
function _resolvePrimaryType3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["type_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: nested_type_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1totjtz(v: unknown): KindMap['else_clause'];
function _r1totjtz(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('else_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('else_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1twh7u2(v: unknown): KindMap['identifier'] | KindMap['nested_identifier'];
function _r1twh7u2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('nested_identifier', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('nested_identifier', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryExpression3(v: unknown): KindMap['object'];
function _resolvePrimaryExpression3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('object', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('object', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1588e16(v: unknown): KindMap['identifier'] | KindMap['named_imports'] | KindMap['namespace_import'] | ImportIdentifier;
function _r1588e16(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: named_imports, namespace_import. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1588e162(v: unknown): KindMap['identifier'] | KindMap['named_imports'] | KindMap['namespace_import'];
function _r1588e162(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: named_imports, namespace_import. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveImportIdentifier2(v: unknown): KindMap['identifier'] | ImportIdentifier;
function _resolveImportIdentifier2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveImportIdentifier(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveImportIdentifier(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveModuleExportName4(v: unknown): KindMap['identifier'] | KindMap['string'] | ImportIdentifier | ModuleExportName;
function _resolveModuleExportName4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: string. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r113fjlk(v: unknown): KindMap['import_clause'] | KindMap['import_require_clause'];
function _r113fjlk(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: import_clause, import_require_clause. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1n7cbqo(v: unknown): KindMap['import_attribute'];
function _r1n7cbqo(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('import_attribute', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('import_attribute', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier13(v: unknown): '+' | '-';
function _resolveTypeIdentifier13(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['+', '-'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rc3yugm(v: unknown): KindMap['adding_type_annotation'] | KindMap['omitting_type_annotation'] | KindMap['opting_type_annotation'] | KindMap['type_annotation'];
function _rc3yugm(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: adding_type_annotation, omitting_type_annotation, opting_type_annotation, type_annotation. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _ro6lixi(v: unknown): KindMap['mapped_type_clause'];
function _ro6lixi(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('mapped_type_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('mapped_type_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier14(v: unknown): KindMap['type_identifier'] | Type;
function _resolveTypeIdentifier14(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["type_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveType(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveType(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r8pjzv1(v: unknown): KindMap['identifier'] | KindMap['import'] | KindMap['member_expression'] | KindMap['subscript_expression'];
function _r8pjzv1(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","import"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: member_expression, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _ra5cxfs(v: unknown): KindMap['call_signature'] | KindMap['construct_signature'] | KindMap['export_statement'] | KindMap['index_signature'] | KindMap['method_signature'] | KindMap['property_signature'];
function _ra5cxfs(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: call_signature, construct_signature, export_statement, index_signature, method_signature, property_signature. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rk01cxs(v: unknown): KindMap['interface_body'];
function _rk01cxs(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('interface_body', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('interface_body', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r163ugqi(v: unknown): KindMap['extends_type_clause'];
function _r163ugqi(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('extends_type_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('extends_type_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r17uug3x(v: unknown): KindMap['identifier'] | KindMap['nested_identifier'] | KindMap['string'];
function _r17uug3x(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: nested_identifier, string. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier15(v: unknown): 'const' | 'let';
function _resolveTypeIdentifier15(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['const', 'let'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1xs8n9k(v: unknown): KindMap['variable_declarator'];
function _r1xs8n9k(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('variable_declarator', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('variable_declarator', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpression2(v: unknown): KindMap['false'] | KindMap['null'] | KindMap['number'] | KindMap['true'] | KindMap['undefined'] | KindMap['string'] | KindMap['unary_expression'];
function _resolveExpression2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["false","null","number","true","undefined"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: string, unary_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r10xn03h(v: unknown): KindMap['import'] | Expression | PrimaryExpression;
function _r10xn03h(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["import"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rt995je(v: unknown): KindMap['optional_chain'];
function _rt995je(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["optional_chain"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePropertyName4(v: unknown): KindMap['private_property_identifier'] | KindMap['property_identifier'];
function _resolvePropertyName4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["private_property_identifier","property_identifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1cojdna(v: unknown): KindMap['import_specifier'];
function _r1cojdna(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('import_specifier', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('import_specifier', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveAugmentedAssignmentLhs4(v: unknown): KindMap['identifier'] | KindMap['member_expression'];
function _resolveAugmentedAssignmentLhs4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('member_expression', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('member_expression', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePropertyName5(v: unknown): KindMap['property_identifier'];
function _resolvePropertyName5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["property_identifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r17o033s(v: unknown): KindMap['arguments'];
function _r17o033s(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('arguments', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('arguments', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier16(v: unknown): PrimaryExpression;
function _resolveTypeIdentifier16(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolvePrimaryExpression(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolvePrimaryExpression(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _ri5vclk(v: unknown): KindMap['shorthand_property_identifier'] | KindMap['method_definition'] | KindMap['pair'] | KindMap['spread_element'];
function _ri5vclk(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["shorthand_property_identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: method_definition, pair, spread_element. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r60k8ng(v: unknown): KindMap['shorthand_property_identifier_pattern'] | KindMap['array_pattern'] | KindMap['object_pattern'] | DestructuringPattern;
function _r60k8ng(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["shorthand_property_identifier_pattern"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_pattern, object_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1r6luag(v: unknown): KindMap['shorthand_property_identifier_pattern'] | KindMap['object_assignment_pattern'] | KindMap['pair_pattern'] | KindMap['rest_pattern'];
function _r1r6luag(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["shorthand_property_identifier_pattern"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: object_assignment_pattern, pair_pattern, rest_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryExpression4(v: unknown): KindMap['this'] | Pattern;
function _resolvePrimaryExpression4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["this"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolvePattern(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolvePattern(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveParameterName4(v: unknown): KindMap['accessibility_modifier'] | KindMap['override_modifier'] | ParameterName;
function _resolveParameterName4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["accessibility_modifier","override_modifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveParameterName(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveParameterName(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressions4(v: unknown): KindMap['identifier'] | KindMap['call_expression'] | KindMap['member_expression'] | KindMap['sequence_expression'] | Expression;
function _resolveExpressions4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: call_expression, member_expression, sequence_expression, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, class, function_expression, generator_function, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1lt2x38(v: unknown): KindMap['hash_bang_line'];
function _r1lt2x38(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["hash_bang_line"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rup90p(v: unknown): KindMap['regex_flags'];
function _rup90p(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["regex_flags"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r13pt1fe(v: unknown): KindMap['regex_pattern'];
function _r13pt1fe(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["regex_pattern"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePattern2(v: unknown): KindMap['identifier'] | KindMap['rest_pattern'];
function _resolvePattern2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('rest_pattern', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('rest_pattern', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _ram3b4x(v: unknown): KindMap['escape_sequence'] | KindMap['string_fragment'];
function _ram3b4x(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["escape_sequence","string_fragment"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1yiq3fd(v: unknown): KindMap['number'] | KindMap['predefined_type'] | KindMap['sequence_expression'] | KindMap['string'] | Expressions | Expression;
function _r1yiq3fd(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["number","predefined_type"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: sequence_expression, string, as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier17(v: unknown): Expression | PrimaryExpression;
function _resolveTypeIdentifier17(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: as_expression, assignment_expression, augmented_assignment_expression, await_expression, binary_expression, instantiation_expression, internal_module, new_expression, array, arrow_function, call_expression, class, function_expression, generator_function, member_expression, non_null_expression, object, parenthesized_expression, regex, string, subscript_expression, template_string, satisfies_expression, ternary_expression, type_assertion, unary_expression, update_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r17pulrl(v: unknown): KindMap['switch_case'] | KindMap['switch_default'];
function _r17pulrl(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: switch_case, switch_default. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1u0kk81(v: unknown): KindMap['switch_body'];
function _r1u0kk81(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('switch_body', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('switch_body', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1clt4kp(v: unknown): KindMap['string_fragment'] | KindMap['template_type'];
function _r1clt4kp(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["string_fragment"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('template_type', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('template_type', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1wwgc7t(v: unknown): KindMap['escape_sequence'] | KindMap['string_fragment'] | KindMap['template_substitution'];
function _r1wwgc7t(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["escape_sequence","string_fragment"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('template_substitution', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('template_substitution', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveType2(v: unknown): KindMap['infer_type'] | PrimaryType;
function _resolveType2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: infer_type, array_type, conditional_type, flow_maybe_type, generic_type, index_type_query, intersection_type, literal_type, lookup_type, nested_type_identifier, object_type, parenthesized_type, template_literal_type, tuple_type, type_query, union_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rqhziy5(v: unknown): KindMap['finally_clause'];
function _rqhziy5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('finally_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('finally_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r120ygox(v: unknown): KindMap['catch_clause'];
function _r120ygox(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('catch_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('catch_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r108s583(v: unknown): KindMap['optional_parameter'] | KindMap['optional_type'] | KindMap['required_parameter'] | KindMap['rest_type'] | Type;
function _r108s583(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: optional_parameter, optional_type, required_parameter, rest_type, call_expression, constructor_type, function_type, infer_type, member_expression, array_type, conditional_type, flow_maybe_type, generic_type, index_type_query, intersection_type, literal_type, lookup_type, nested_type_identifier, object_type, parenthesized_type, template_literal_type, tuple_type, type_query, union_type, readonly_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1qgzxrb(v: unknown): KindMap['constraint'];
function _r1qgzxrb(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('constraint', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('constraint', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1gd522w(v: unknown): KindMap['default_type'];
function _r1gd522w(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('default_type', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('default_type', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _reaq3s4(v: unknown): KindMap['type_parameter'];
function _reaq3s4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('type_parameter', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('type_parameter', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryExpression5(v: unknown): KindMap['identifier'] | KindMap['this'];
function _resolvePrimaryExpression5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","this"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rj0vzo(v: unknown): KindMap['type_predicate'];
function _rj0vzo(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('type_predicate', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('type_predicate', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpression3(v: unknown): KindMap['identifier'] | KindMap['this'] | KindMap['call_expression'] | KindMap['instantiation_expression'] | KindMap['member_expression'] | KindMap['subscript_expression'];
function _resolveExpression3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","this"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: call_expression, instantiation_expression, member_expression, subscript_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePropertyName6(v: unknown): KindMap['number'] | Expression;
function _resolvePropertyName6(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["number"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveExpression(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveExpression(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier18(v: unknown): '!' | '+' | '-' | 'delete' | 'typeof' | 'void' | '~';
function _resolveTypeIdentifier18(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['!', '+', '-', 'delete', 'typeof', 'void', '~'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier19(v: unknown): '++' | '--';
function _resolveTypeIdentifier19(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['++', '--'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

/** Shared field resolver for hidden rule `_from_clause` (used by 2 nodes). */
function _resolveFromClauseFields(
  obj: Record<string, unknown>,
): { source?: KindMap['string'] } {
  return {
    source: _resolveModuleExportName3(obj.source),
  };
}

/** Shared field resolver for hidden rule `_call_signature` (used by 10 nodes). */
function _resolveCallSignatureFields(
  obj: Record<string, unknown>,
): { parameters: KindMap['formal_parameters']; returnType?: KindMap['asserts_annotation'] | KindMap['type_annotation'] | KindMap['type_predicate_annotation']; typeParameters?: KindMap['type_parameters'] } {
  return {
    parameters: _r1400tna(obj.parameters),
    returnType: _r15nsthd(obj.return_type),
    typeParameters: _r19n9507(obj.type_parameters),
  };
}

/** Shared field resolver for hidden rule `_initializer` (used by 5 nodes). */
function _resolveInitializerFields(
  obj: Record<string, unknown>,
): { value: Expression } {
  return {
    value: _resolveTypeIdentifier6(obj.value),
  };
}

/** Shared field resolver for hidden rule `_parameter_name` (used by 2 nodes). */
function _resolveParameterNameFields(
  obj: Record<string, unknown>,
): { decorator?: (KindMap['decorator'])[]; pattern?: KindMap['this'] | Pattern } {
  return {
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    pattern: _resolvePrimaryExpression4(obj.pattern),
  };
}

export function abstractClassDeclarationFrom(input: RuntimeNodeOf<AbstractClassDeclaration> | AbstractClassDeclarationFromInput) {
  if (isNodeData<'abstract_class_declaration'>(input)) {
    return abstract_class_declaration_({
      body: input.fields?.body,
      decorator: input.fields?.decorator,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      classHeritage: input.fields?.class_heritage,
    });
  }
  const obj = input as AbstractClassDeclarationFromInput;
  return abstract_class_declaration_({
    body: _rzpcalh(obj.body),
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    name: _resolveTypeIdentifier2(obj.name),
    typeParameters: _r19n9507(obj.type_parameters),
    classHeritage: _r1v8cdsw(obj.class_heritage),
  });
}

export function abstractMethodSignatureFrom(input: RuntimeNodeOf<AbstractMethodSignature> | AbstractMethodSignatureFromInput) {
  if (isNodeData<'abstract_method_signature'>(input)) {
    return abstract_method_signature_({
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
      accessibilityModifier: input.fields?.accessibility_modifier,
      overrideModifier: input.fields?.override_modifier,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as AbstractMethodSignatureFromInput;
  return abstract_method_signature_({
    ..._resolveCallSignatureFields(obj),
    name: _resolvePropertyName2(obj.name),
    accessibilityModifier: _resolveParameterName2(obj.accessibility_modifier),
    overrideModifier: _resolveParameterName3(obj.override_modifier),
  });
}

export function addingTypeAnnotationFrom(input: RuntimeNodeOf<AddingTypeAnnotation>): ReturnType<typeof adding_type_annotation_>;
export function addingTypeAnnotationFrom(child: AddingTypeAnnotationFromInput): ReturnType<typeof adding_type_annotation_>;
export function addingTypeAnnotationFrom(arg?: RuntimeNodeOf<AddingTypeAnnotation> | AddingTypeAnnotationFromInput) {
  if (isNodeData(arg) && arg.type === 'adding_type_annotation') {
    return adding_type_annotation_(arg.children?.[0] as Type);
  }
  return adding_type_annotation_(_resolveTypeIdentifier4(arg) as Type);
}

export function ambientDeclarationFrom(input: RuntimeNodeOf<AmbientDeclaration> | AmbientDeclarationFromInput) {
  if (isNodeData<'ambient_declaration'>(input)) {
    return ambient_declaration_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as AmbientDeclarationConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return ambient_declaration_(mapped as AmbientDeclarationConfig);
}

export function ambientDeclarationV0From(input: RuntimeNodeOf<AmbientDeclarationV0> | AmbientDeclarationV0FromInput) {
  if (isNodeData<'ambient_declaration'>(input)) {
    return ambient_declaration__v0_({
      declaration: input.fields?.declaration,
    });
  }
  const obj = input as AmbientDeclarationV0FromInput;
  return ambient_declaration__v0_({
    declaration: _rca402w(obj.declaration),
  });
}

export function ambientDeclarationGlobalFrom(input: RuntimeNodeOf<AmbientDeclarationGlobal> | AmbientDeclarationGlobalFromInput) {
  if (isNodeData<'ambient_declaration'>(input)) {
    return ambient_declaration__global_({
      declaration: input.fields?.declaration,
    });
  }
  const obj = input as AmbientDeclarationGlobalFromInput;
  return ambient_declaration__global_({
    declaration: _rca402w(obj.declaration),
  });
}

export function ambientDeclarationModuleFrom(input: RuntimeNodeOf<AmbientDeclarationModule> | AmbientDeclarationModuleFromInput) {
  if (isNodeData<'ambient_declaration'>(input)) {
    return ambient_declaration__module_({
      declaration: input.fields?.declaration,
      typeAnnotation: input.fields?.type_annotation,
      semicolon: input.fields?.semicolon,
    });
  }
  const obj = input as AmbientDeclarationModuleFromInput;
  return ambient_declaration__module_({
    declaration: _rca402w(obj.declaration),
    typeAnnotation: _resolveTypeIdentifier4(obj.type_annotation),
    semicolon: _resolveTypeIdentifier3(obj.semicolon),
  });
}

export function arguments_From(input: RuntimeNodeOf<Arguments>): ReturnType<typeof arguments_>;
export function arguments_From(...children: ArgumentsFromInput[]): ReturnType<typeof arguments_>;
export function arguments_From(...args: (RuntimeNodeOf<Arguments> | ArgumentsFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'arguments') {
    return arguments_(...(args[0].children ?? []) as (Expression | SpreadElement)[]);
  }
  return arguments_(...args.map(v => _rxueckq(v)) as (Expression | SpreadElement)[]);
}

export function arrayFrom(input: RuntimeNodeOf<Array>): ReturnType<typeof array_>;
export function arrayFrom(...children: ArrayFromInput[]): ReturnType<typeof array_>;
export function arrayFrom(...args: (RuntimeNodeOf<Array> | ArrayFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'array') {
    return array_(...(args[0].children ?? []) as (Expression | SpreadElement)[]);
  }
  return array_(...args.map(v => _rxueckq(v)) as (Expression | SpreadElement)[]);
}

export function arrayPatternFrom(input: RuntimeNodeOf<ArrayPattern>): ReturnType<typeof array_pattern_>;
export function arrayPatternFrom(...children: ArrayPatternFromInput[]): ReturnType<typeof array_pattern_>;
export function arrayPatternFrom(...args: (RuntimeNodeOf<ArrayPattern> | ArrayPatternFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'array_pattern') {
    return array_pattern_(...(args[0].children ?? []) as (AssignmentPattern | Pattern)[]);
  }
  return array_pattern_(...args.map(v => _r1lwpcqg(v)) as (AssignmentPattern | Pattern)[]);
}

export function arrayTypeFrom(input: RuntimeNodeOf<ArrayType> | ArrayTypeFromInput) {
  if (isNodeData<'array_type'>(input)) {
    return array_type_({
      primaryType: input.fields?.primary_type,
    });
  }
  const obj = input as ArrayTypeFromInput;
  return array_type_({
    primaryType: _resolveTypeIdentifier5(obj.primary_type),
  });
}

export function arrowFunctionFrom(input: RuntimeNodeOf<ArrowFunction> | ArrowFunctionFromInput) {
  if (isNodeData<'arrow_function'>(input)) {
    return arrow_function_({
      body: input.fields?.body,
      parameter: input.fields?.parameter,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as ArrowFunctionFromInput;
  return arrow_function_({
    ..._resolveCallSignatureFields(obj),
    body: _resolveStatement2(obj.body),
    parameter: _resolveImportIdentifier(obj.parameter),
  });
}

export function asExpressionFrom(input: RuntimeNodeOf<AsExpression> | AsExpressionFromInput) {
  if (isNodeData<'as_expression'>(input)) {
    return as_expression_({
      expression: input.fields?.expression,
      typeAnnotation: input.fields?.type_annotation,
    });
  }
  const obj = input as AsExpressionFromInput;
  return as_expression_({
    expression: _resolveTypeIdentifier6(obj.expression),
    typeAnnotation: _resolveTypeIdentifier4(obj.type_annotation),
  });
}

export function assertsFrom(input: RuntimeNodeOf<Asserts>): ReturnType<typeof asserts_>;
export function assertsFrom(child: AssertsFromInput): ReturnType<typeof asserts_>;
export function assertsFrom(arg?: RuntimeNodeOf<Asserts> | AssertsFromInput) {
  if (isNodeData(arg) && arg.type === 'asserts') {
    return asserts_(arg.children?.[0] as Identifier | This | TypePredicate);
  }
  return asserts_(_rc0mhwb(arg) as Identifier | This | TypePredicate);
}

export function assertsAnnotationFrom(input: RuntimeNodeOf<AssertsAnnotation> | AssertsAnnotationFromInput) {
  if (isNodeData<'asserts_annotation'>(input)) {
    return asserts_annotation_({
      asserts: input.fields?.asserts,
    });
  }
  const obj = input as AssertsAnnotationFromInput;
  return asserts_annotation_({
    asserts: _r1n126sn(obj.asserts),
  });
}

export function assignmentExpressionFrom(input: RuntimeNodeOf<AssignmentExpression> | AssignmentExpressionFromInput) {
  if (isNodeData<'assignment_expression'>(input)) {
    return assignment_expression_({
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as AssignmentExpressionFromInput;
  return assignment_expression_({
    left: _rh2pegt(obj.left),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function assignmentPatternFrom(input: RuntimeNodeOf<AssignmentPattern> | AssignmentPatternFromInput) {
  if (isNodeData<'assignment_pattern'>(input)) {
    return assignment_pattern_({
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as AssignmentPatternFromInput;
  return assignment_pattern_({
    left: _resolveTypeIdentifier7(obj.left),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function augmentedAssignmentExpressionFrom(input: RuntimeNodeOf<AugmentedAssignmentExpression> | AugmentedAssignmentExpressionFromInput) {
  if (isNodeData<'augmented_assignment_expression'>(input)) {
    return augmented_assignment_expression_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as AugmentedAssignmentExpressionFromInput;
  return augmented_assignment_expression_({
    left: _resolveAugmentedAssignmentLhs2(obj.left),
    operator: _resolveTypeIdentifier8(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function awaitExpressionFrom(input: RuntimeNodeOf<AwaitExpression> | AwaitExpressionFromInput) {
  if (isNodeData<'await_expression'>(input)) {
    return await_expression_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as AwaitExpressionFromInput;
  return await_expression_({
    expression: _resolveTypeIdentifier6(obj.expression),
  });
}

export function binaryExpressionFrom(input: RuntimeNodeOf<BinaryExpression> | BinaryExpressionFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression_({
      ...Object.fromEntries(Object.entries(input.fields ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as BinaryExpressionConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return binary_expression_(mapped as BinaryExpressionConfig);
}

export function binaryExpressionTok_2626From(input: RuntimeNodeOf<BinaryExpressionTok_2626> | BinaryExpressionTok_2626FromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_2626_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_2626FromInput;
  return binary_expression__tok_2626_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_7c7cFrom(input: RuntimeNodeOf<BinaryExpressionTok_7c7c> | BinaryExpressionTok_7c7cFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_7c7c_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_7c7cFromInput;
  return binary_expression__tok_7c7c_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_3e3eFrom(input: RuntimeNodeOf<BinaryExpressionTok_3e3e> | BinaryExpressionTok_3e3eFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_3e3e_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_3e3eFromInput;
  return binary_expression__tok_3e3e_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_3e3e3eFrom(input: RuntimeNodeOf<BinaryExpressionTok_3e3e3e> | BinaryExpressionTok_3e3e3eFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_3e3e3e_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_3e3e3eFromInput;
  return binary_expression__tok_3e3e3e_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_3c3cFrom(input: RuntimeNodeOf<BinaryExpressionTok_3c3c> | BinaryExpressionTok_3c3cFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_3c3c_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_3c3cFromInput;
  return binary_expression__tok_3c3c_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionAmpFrom(input: RuntimeNodeOf<BinaryExpressionAmp> | BinaryExpressionAmpFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__amp_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionAmpFromInput;
  return binary_expression__amp_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionCaretFrom(input: RuntimeNodeOf<BinaryExpressionCaret> | BinaryExpressionCaretFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__caret_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionCaretFromInput;
  return binary_expression__caret_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionPipeFrom(input: RuntimeNodeOf<BinaryExpressionPipe> | BinaryExpressionPipeFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__pipe_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionPipeFromInput;
  return binary_expression__pipe_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionPlusFrom(input: RuntimeNodeOf<BinaryExpressionPlus> | BinaryExpressionPlusFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__plus_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionPlusFromInput;
  return binary_expression__plus_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionMinusFrom(input: RuntimeNodeOf<BinaryExpressionMinus> | BinaryExpressionMinusFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__minus_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionMinusFromInput;
  return binary_expression__minus_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionStarFrom(input: RuntimeNodeOf<BinaryExpressionStar> | BinaryExpressionStarFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__star_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionStarFromInput;
  return binary_expression__star_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionSlashFrom(input: RuntimeNodeOf<BinaryExpressionSlash> | BinaryExpressionSlashFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__slash_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionSlashFromInput;
  return binary_expression__slash_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionPercentFrom(input: RuntimeNodeOf<BinaryExpressionPercent> | BinaryExpressionPercentFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__percent_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionPercentFromInput;
  return binary_expression__percent_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_2a2aFrom(input: RuntimeNodeOf<BinaryExpressionTok_2a2a> | BinaryExpressionTok_2a2aFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_2a2a_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_2a2aFromInput;
  return binary_expression__tok_2a2a_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionAngleFrom(input: RuntimeNodeOf<BinaryExpressionAngle> | BinaryExpressionAngleFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__angle_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionAngleFromInput;
  return binary_expression__angle_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_3c3dFrom(input: RuntimeNodeOf<BinaryExpressionTok_3c3d> | BinaryExpressionTok_3c3dFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_3c3d_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_3c3dFromInput;
  return binary_expression__tok_3c3d_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_3d3dFrom(input: RuntimeNodeOf<BinaryExpressionTok_3d3d> | BinaryExpressionTok_3d3dFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_3d3d_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_3d3dFromInput;
  return binary_expression__tok_3d3d_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_3d3d3dFrom(input: RuntimeNodeOf<BinaryExpressionTok_3d3d3d> | BinaryExpressionTok_3d3d3dFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_3d3d3d_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_3d3d3dFromInput;
  return binary_expression__tok_3d3d3d_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_213dFrom(input: RuntimeNodeOf<BinaryExpressionTok_213d> | BinaryExpressionTok_213dFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_213d_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_213dFromInput;
  return binary_expression__tok_213d_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_213d3dFrom(input: RuntimeNodeOf<BinaryExpressionTok_213d3d> | BinaryExpressionTok_213d3dFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_213d3d_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_213d3dFromInput;
  return binary_expression__tok_213d3d_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_3e3dFrom(input: RuntimeNodeOf<BinaryExpressionTok_3e3d> | BinaryExpressionTok_3e3dFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_3e3d_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_3e3dFromInput;
  return binary_expression__tok_3e3d_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionCloseAngleFrom(input: RuntimeNodeOf<BinaryExpressionCloseAngle> | BinaryExpressionCloseAngleFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__close_angle_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionCloseAngleFromInput;
  return binary_expression__close_angle_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionTok_3f3fFrom(input: RuntimeNodeOf<BinaryExpressionTok_3f3f> | BinaryExpressionTok_3f3fFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__tok_3f3f_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionTok_3f3fFromInput;
  return binary_expression__tok_3f3f_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionInstanceofFrom(input: RuntimeNodeOf<BinaryExpressionInstanceof> | BinaryExpressionInstanceofFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__instanceof_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionInstanceofFromInput;
  return binary_expression__instanceof_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function binaryExpressionInFrom(input: RuntimeNodeOf<BinaryExpressionIn> | BinaryExpressionInFromInput) {
  if (isNodeData<'binary_expression'>(input)) {
    return binary_expression__in_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as BinaryExpressionInFromInput;
  return binary_expression__in_({
    left: _resolvePropertyName3(obj.left),
    operator: _resolveTypeIdentifier9(obj.operator),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function breakStatementFrom(input: RuntimeNodeOf<BreakStatement> | BreakStatementFromInput) {
  if (isNodeData<'break_statement'>(input)) {
    return break_statement_({
      label: input.fields?.label,
    });
  }
  const obj = input as BreakStatementFromInput;
  return break_statement_({
    label: _r11b7pfd(obj.label),
  });
}

export function callExpressionFrom(input: RuntimeNodeOf<CallExpression> | CallExpressionFromInput) {
  if (isNodeData<'call_expression'>(input)) {
    return call_expression_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as CallExpressionConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return call_expression_(mapped as CallExpressionConfig);
}

export function callExpressionV0From(input: RuntimeNodeOf<CallExpressionV0> | CallExpressionV0FromInput) {
  if (isNodeData<'call_expression'>(input)) {
    return call_expression__v0_({
      arguments: input.fields?.arguments,
      function: input.fields?.function,
      typeArguments: input.fields?.type_arguments,
    });
  }
  const obj = input as CallExpressionV0FromInput;
  return call_expression__v0_({
    arguments: _rq16gfq(obj.arguments),
    function: _r6g0yom(obj.function),
    typeArguments: _r1uqebll(obj.type_arguments),
  });
}

export function callExpressionV1From(input: RuntimeNodeOf<CallExpressionV1> | CallExpressionV1FromInput) {
  if (isNodeData<'call_expression'>(input)) {
    return call_expression__v1_({
      arguments: input.fields?.arguments,
      function: input.fields?.function,
    });
  }
  const obj = input as CallExpressionV1FromInput;
  return call_expression__v1_({
    arguments: _rq16gfq(obj.arguments),
    function: _r6g0yom(obj.function),
  });
}

export function callExpressionTok_3f2eFrom(input: RuntimeNodeOf<CallExpressionTok_3f2e> | CallExpressionTok_3f2eFromInput) {
  if (isNodeData<'call_expression'>(input)) {
    return call_expression__tok_3f2e_({
      arguments: input.fields?.arguments,
      function: input.fields?.function,
      typeArguments: input.fields?.type_arguments,
    });
  }
  const obj = input as CallExpressionTok_3f2eFromInput;
  return call_expression__tok_3f2e_({
    arguments: _rq16gfq(obj.arguments),
    function: _r6g0yom(obj.function),
    typeArguments: _r1uqebll(obj.type_arguments),
  });
}

export function callSignatureFrom(input: RuntimeNodeOf<CallSignature> | CallSignatureFromInput) {
  if (isNodeData<'call_signature'>(input)) {
    return call_signature_({
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as CallSignatureFromInput;
  return call_signature_({
    ..._resolveCallSignatureFields(obj),
  });
}

export function catchClauseFrom(input: RuntimeNodeOf<CatchClause> | CatchClauseFromInput) {
  if (isNodeData<'catch_clause'>(input)) {
    return catch_clause_({
      body: input.fields?.body,
      parameter: input.fields?.parameter,
      type: input.fields?.type,
    });
  }
  const obj = input as CatchClauseFromInput;
  return catch_clause_({
    body: _resolveStatement3(obj.body),
    parameter: _resolveLhsExpression2(obj.parameter),
    type: _r1s2j1z2(obj.type),
  });
}

export function class_From(input: RuntimeNodeOf<Class> | ClassFromInput) {
  if (isNodeData<'class'>(input)) {
    return class_({
      body: input.fields?.body,
      decorator: input.fields?.decorator,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      classHeritage: input.fields?.class_heritage,
    });
  }
  const obj = input as ClassFromInput;
  return class_({
    body: _rzpcalh(obj.body),
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    name: _resolveTypeIdentifier2(obj.name),
    typeParameters: _r19n9507(obj.type_parameters),
    classHeritage: _r1v8cdsw(obj.class_heritage),
  });
}

export function classBodyFrom(input: RuntimeNodeOf<ClassBody> | ClassBodyFromInput) {
  if (isNodeData<'class_body'>(input)) {
    return class_body_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
      children: input.children,
    } as ClassBodyConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return class_body_(mapped as ClassBodyConfig);
}

export function classBodyDecoratorFrom(input: RuntimeNodeOf<ClassBodyDecorator> | ClassBodyDecoratorFromInput) {
  if (isNodeData<'class_body'>(input)) {
    return class_body__decorator_({
      decorator: input.fields?.decorator,
      children: input.children as ClassBodyDecoratorConfig['children'],
    });
  }
  const obj = input as ClassBodyDecoratorFromInput;
  return class_body__decorator_({
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    children: (obj as { children?: unknown }).children as ClassBodyDecoratorConfig['children'],
  });
}

export function classBodyCommaFrom(input: RuntimeNodeOf<ClassBodyComma> | ClassBodyCommaFromInput) {
  if (isNodeData<'class_body'>(input)) {
    return class_body__comma_({
      children: input.children as ClassBodyCommaConfig['children'],
    });
  }
  const obj = input as ClassBodyCommaFromInput;
  return class_body__comma_({
    children: (obj as { children?: unknown }).children as ClassBodyCommaConfig['children'],
  });
}

export function classBodyV2From(input: RuntimeNodeOf<ClassBodyV2> | ClassBodyV2FromInput) {
  if (isNodeData<'class_body'>(input)) {
    return class_body__v2_({
      children: input.children as ClassBodyV2Config['children'],
    });
  }
  const obj = input as ClassBodyV2FromInput;
  return class_body__v2_({
    children: (obj as { children?: unknown }).children as ClassBodyV2Config['children'],
  });
}

export function classBodySemiFrom(input: RuntimeNodeOf<ClassBodySemi> | ClassBodySemiFromInput) {
  if (isNodeData<'class_body'>(input)) {
    return class_body__semi_({
      children: input.children as ClassBodySemiConfig['children'],
    });
  }
  const obj = input as ClassBodySemiFromInput;
  return class_body__semi_({
    children: (obj as { children?: unknown }).children as ClassBodySemiConfig['children'],
  });
}

export function classDeclarationFrom(input: RuntimeNodeOf<ClassDeclaration> | ClassDeclarationFromInput) {
  if (isNodeData<'class_declaration'>(input)) {
    return class_declaration_({
      body: input.fields?.body,
      decorator: input.fields?.decorator,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      classHeritage: input.fields?.class_heritage,
      automaticSemicolon: input.fields?.automatic_semicolon,
    });
  }
  const obj = input as ClassDeclarationFromInput;
  return class_declaration_({
    body: _rzpcalh(obj.body),
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    name: _resolveTypeIdentifier2(obj.name),
    typeParameters: _r19n9507(obj.type_parameters),
    classHeritage: _r1v8cdsw(obj.class_heritage),
    automaticSemicolon: _resolveTypeIdentifier3(obj.automatic_semicolon),
  });
}

export function classHeritageFrom(input: RuntimeNodeOf<ClassHeritage> | ClassHeritageFromInput) {
  if (isNodeData<'class_heritage'>(input)) {
    return class_heritage_({
      extendsClause: input.fields?.extends_clause,
      implementsClause: input.fields?.implements_clause,
    });
  }
  const obj = input as ClassHeritageFromInput;
  return class_heritage_({
    extendsClause: _r1ja7eu7(obj.extends_clause),
    implementsClause: _r60me0s(obj.implements_clause),
  });
}

export function classStaticBlockFrom(input: RuntimeNodeOf<ClassStaticBlock> | ClassStaticBlockFromInput) {
  if (isNodeData<'class_static_block'>(input)) {
    return class_static_block_({
      body: input.fields?.body,
    });
  }
  const obj = input as ClassStaticBlockFromInput;
  return class_static_block_({
    body: _resolveStatement3(obj.body),
  });
}

export function computedPropertyNameFrom(input: RuntimeNodeOf<ComputedPropertyName> | ComputedPropertyNameFromInput) {
  if (isNodeData<'computed_property_name'>(input)) {
    return computed_property_name_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as ComputedPropertyNameFromInput;
  return computed_property_name_({
    expression: _resolveTypeIdentifier6(obj.expression),
  });
}

export function conditionalTypeFrom(input: RuntimeNodeOf<ConditionalType> | ConditionalTypeFromInput) {
  if (isNodeData<'conditional_type'>(input)) {
    return conditional_type_({
      alternative: input.fields?.alternative,
      consequence: input.fields?.consequence,
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as ConditionalTypeFromInput;
  return conditional_type_({
    alternative: _resolveTypeIdentifier4(obj.alternative),
    consequence: _resolveTypeIdentifier4(obj.consequence),
    left: _resolveTypeIdentifier4(obj.left),
    right: _resolveTypeIdentifier4(obj.right),
  });
}

export function constraintFrom(input: RuntimeNodeOf<Constraint>): ReturnType<typeof constraint_>;
export function constraintFrom(child: ConstraintFromInput): ReturnType<typeof constraint_>;
export function constraintFrom(arg?: RuntimeNodeOf<Constraint> | ConstraintFromInput) {
  if (isNodeData(arg) && arg.type === 'constraint') {
    return constraint_(arg.children?.[0] as Type);
  }
  return constraint_(_resolveTypeIdentifier4(arg) as Type);
}

export function constructSignatureFrom(input: RuntimeNodeOf<ConstructSignature> | ConstructSignatureFromInput) {
  if (isNodeData<'construct_signature'>(input)) {
    return construct_signature_({
      parameters: input.fields?.parameters,
      type: input.fields?.type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as ConstructSignatureFromInput;
  return construct_signature_({
    parameters: _r1400tna(obj.parameters),
    type: _r1s2j1z2(obj.type),
    typeParameters: _r19n9507(obj.type_parameters),
  });
}

export function constructorTypeFrom(input: RuntimeNodeOf<ConstructorType> | ConstructorTypeFromInput) {
  if (isNodeData<'constructor_type'>(input)) {
    return constructor_type_({
      parameters: input.fields?.parameters,
      type: input.fields?.type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as ConstructorTypeFromInput;
  return constructor_type_({
    parameters: _r1400tna(obj.parameters),
    type: _resolveTypeIdentifier4(obj.type),
    typeParameters: _r19n9507(obj.type_parameters),
  });
}

export function continueStatementFrom(input: RuntimeNodeOf<ContinueStatement> | ContinueStatementFromInput) {
  if (isNodeData<'continue_statement'>(input)) {
    return continue_statement_({
      label: input.fields?.label,
    });
  }
  const obj = input as ContinueStatementFromInput;
  return continue_statement_({
    label: _r11b7pfd(obj.label),
  });
}

export function decoratorFrom(input: RuntimeNodeOf<Decorator>): ReturnType<typeof decorator_>;
export function decoratorFrom(child: DecoratorFromInput): ReturnType<typeof decorator_>;
export function decoratorFrom(arg?: RuntimeNodeOf<Decorator> | DecoratorFromInput) {
  if (isNodeData(arg) && arg.type === 'decorator') {
    return decorator_(arg.children?.[0] as CallExpression | Identifier | MemberExpression | ParenthesizedExpression);
  }
  return decorator_(_resolvePrimaryExpression2(arg) as CallExpression | Identifier | MemberExpression | ParenthesizedExpression);
}

export function defaultTypeFrom(input: RuntimeNodeOf<DefaultType>): ReturnType<typeof default_type_>;
export function defaultTypeFrom(child: DefaultTypeFromInput): ReturnType<typeof default_type_>;
export function defaultTypeFrom(arg?: RuntimeNodeOf<DefaultType> | DefaultTypeFromInput) {
  if (isNodeData(arg) && arg.type === 'default_type') {
    return default_type_(arg.children?.[0] as Type);
  }
  return default_type_(_resolveTypeIdentifier4(arg) as Type);
}

export function doStatementFrom(input: RuntimeNodeOf<DoStatement> | DoStatementFromInput) {
  if (isNodeData<'do_statement'>(input)) {
    return do_statement_({
      body: input.fields?.body,
      condition: input.fields?.condition,
    });
  }
  const obj = input as DoStatementFromInput;
  return do_statement_({
    body: _resolveTypeIdentifier10(obj.body),
    condition: _resolveAugmentedAssignmentLhs3(obj.condition),
  });
}

export function elseClauseFrom(input: RuntimeNodeOf<ElseClause> | ElseClauseFromInput) {
  if (isNodeData<'else_clause'>(input)) {
    return else_clause_({
      statement: input.fields?.statement,
    });
  }
  const obj = input as ElseClauseFromInput;
  return else_clause_({
    statement: _resolveTypeIdentifier10(obj.statement),
  });
}

export function enumAssignmentFrom(input: RuntimeNodeOf<EnumAssignment> | EnumAssignmentFromInput) {
  if (isNodeData<'enum_assignment'>(input)) {
    return enum_assignment_({
      name: input.fields?.name,
      value: input.fields?.value,
    });
  }
  const obj = input as EnumAssignmentFromInput;
  return enum_assignment_({
    ..._resolveInitializerFields(obj),
    name: _resolvePropertyName2(obj.name),
  });
}

export function enumBodyFrom(input: RuntimeNodeOf<EnumBody> | EnumBodyFromInput) {
  if (isNodeData<'enum_body'>(input)) {
    return enum_body_({
      name: input.fields?.name,
      opening: input.fields?.opening,
      members: input.fields?.members,
    });
  }
  const obj = input as EnumBodyFromInput;
  return enum_body_({
    name: obj.name !== undefined ? ((Array.isArray(obj.name) ? obj.name : [obj.name])).map(_resolvePropertyName2) : undefined,
    opening: _r1nm6gsv(obj.opening),
    members: obj.members !== undefined ? ((Array.isArray(obj.members) ? obj.members : [obj.members])).map(_r1nm6gsv) : undefined,
  });
}

export function enumDeclarationFrom(input: RuntimeNodeOf<EnumDeclaration> | EnumDeclarationFromInput) {
  if (isNodeData<'enum_declaration'>(input)) {
    return enum_declaration_({
      body: input.fields?.body,
      name: input.fields?.name,
    });
  }
  const obj = input as EnumDeclarationFromInput;
  return enum_declaration_({
    body: _r1q8ojb8(obj.body),
    name: _resolveImportIdentifier(obj.name),
  });
}

export function exportClauseFrom(input: RuntimeNodeOf<ExportClause>): ReturnType<typeof export_clause_>;
export function exportClauseFrom(...children: ExportClauseFromInput[]): ReturnType<typeof export_clause_>;
export function exportClauseFrom(...args: (RuntimeNodeOf<ExportClause> | ExportClauseFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'export_clause') {
    return export_clause_(...(args[0].children ?? []) as (ExportSpecifier)[]);
  }
  return export_clause_(...args.map(v => _r109ficd(v)) as (ExportSpecifier)[]);
}

export function exportSpecifierFrom(input: RuntimeNodeOf<ExportSpecifier> | ExportSpecifierFromInput) {
  if (isNodeData<'export_specifier'>(input)) {
    return export_specifier_({
      alias: input.fields?.alias,
      name: input.fields?.name,
    });
  }
  const obj = input as ExportSpecifierFromInput;
  return export_specifier_({
    alias: _resolveModuleExportName2(obj.alias),
    name: _resolveModuleExportName2(obj.name),
  });
}

export function exportStatementFrom(input: RuntimeNodeOf<ExportStatement> | ExportStatementFromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as ExportStatementConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return export_statement_(mapped as ExportStatementConfig);
}

export function exportStatementStarFrom(input: RuntimeNodeOf<ExportStatementStar> | ExportStatementStarFromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement__star_({
      children: input.children?.[0] as ExportStatementStarConfig['children'],
    });
  }
  const obj = input as ExportStatementStarFromInput;
  return export_statement__star_({
    children: (obj as { children?: unknown }).children as ExportStatementStarConfig['children'],
  });
}

export function exportStatementV1From(input: RuntimeNodeOf<ExportStatementV1> | ExportStatementV1FromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement__v1_({
      declaration: input.fields?.declaration,
    });
  }
  const obj = input as ExportStatementV1FromInput;
  return export_statement__v1_({
    declaration: _r1tl6ta5(obj.declaration),
  });
}

export function exportStatementV2From(input: RuntimeNodeOf<ExportStatementV2> | ExportStatementV2FromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement__v2_({
      declaration: input.fields?.declaration,
      decorator: input.fields?.decorator,
    });
  }
  const obj = input as ExportStatementV2FromInput;
  return export_statement__v2_({
    declaration: _r1tl6ta5(obj.declaration),
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
  });
}

export function exportStatementV3From(input: RuntimeNodeOf<ExportStatementV3> | ExportStatementV3FromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement__v3_({
      declaration: input.fields?.declaration,
      decorator: input.fields?.decorator,
    });
  }
  const obj = input as ExportStatementV3FromInput;
  return export_statement__v3_({
    declaration: _r1tl6ta5(obj.declaration),
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
  });
}

export function exportStatementValueFrom(input: RuntimeNodeOf<ExportStatementValue> | ExportStatementValueFromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement__value_({
      declaration: input.fields?.declaration,
      decorator: input.fields?.decorator,
      value: input.fields?.value,
    });
  }
  const obj = input as ExportStatementValueFromInput;
  return export_statement__value_({
    declaration: _r1tl6ta5(obj.declaration),
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    value: _resolveTypeIdentifier6(obj.value),
  });
}

export function exportStatementTypeKwFrom(input: RuntimeNodeOf<ExportStatementTypeKw> | ExportStatementTypeKwFromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement__type_kw_({
      declaration: input.fields?.declaration,
    });
  }
  const obj = input as ExportStatementTypeKwFromInput;
  return export_statement__type_kw_({
    declaration: _r1tl6ta5(obj.declaration),
  });
}

export function exportStatementEqFrom(input: RuntimeNodeOf<ExportStatementEq> | ExportStatementEqFromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement__eq_({
      declaration: input.fields?.declaration,
    });
  }
  const obj = input as ExportStatementEqFromInput;
  return export_statement__eq_({
    declaration: _r1tl6ta5(obj.declaration),
  });
}

export function exportStatementAsFrom(input: RuntimeNodeOf<ExportStatementAs> | ExportStatementAsFromInput) {
  if (isNodeData<'export_statement'>(input)) {
    return export_statement__as_({
      declaration: input.fields?.declaration,
    });
  }
  const obj = input as ExportStatementAsFromInput;
  return export_statement__as_({
    declaration: _r1tl6ta5(obj.declaration),
  });
}

export function expressionStatementFrom(input: RuntimeNodeOf<ExpressionStatement>): ReturnType<typeof expression_statement_>;
export function expressionStatementFrom(child: ExpressionStatementFromInput): ReturnType<typeof expression_statement_>;
export function expressionStatementFrom(arg?: RuntimeNodeOf<ExpressionStatement> | ExpressionStatementFromInput) {
  if (isNodeData(arg) && arg.type === 'expression_statement') {
    return expression_statement_(arg.children?.[0] as Expression | SequenceExpression);
  }
  return expression_statement_(_resolveExpressions2(arg) as Expression | SequenceExpression);
}

export function extendsClauseFrom(input: RuntimeNodeOf<ExtendsClause> | ExtendsClauseFromInput) {
  if (isNodeData<'extends_clause'>(input)) {
    return extends_clause_({
      typeArguments: input.fields?.type_arguments,
      value: input.fields?.value,
    });
  }
  const obj = input as ExtendsClauseFromInput;
  return extends_clause_({
    typeArguments: obj.type_arguments !== undefined ? ((Array.isArray(obj.type_arguments) ? obj.type_arguments : [obj.type_arguments])).map(_r1uqebll) : undefined,
    value: obj.value !== undefined ? ((Array.isArray(obj.value) ? obj.value : [obj.value])).map(_resolveTypeIdentifier6) : [],
  });
}

export function extendsTypeClauseFrom(input: RuntimeNodeOf<ExtendsTypeClause> | ExtendsTypeClauseFromInput) {
  if (isNodeData<'extends_type_clause'>(input)) {
    return extends_type_clause_({
      type: input.fields?.type,
    });
  }
  const obj = input as ExtendsTypeClauseFromInput;
  return extends_type_clause_({
    type: obj.type !== undefined ? ((Array.isArray(obj.type) ? obj.type : [obj.type])).map(_resolvePrimaryType2) : [],
  });
}

export function finallyClauseFrom(input: RuntimeNodeOf<FinallyClause> | FinallyClauseFromInput) {
  if (isNodeData<'finally_clause'>(input)) {
    return finally_clause_({
      body: input.fields?.body,
    });
  }
  const obj = input as FinallyClauseFromInput;
  return finally_clause_({
    body: _resolveStatement3(obj.body),
  });
}

export function flowMaybeTypeFrom(input: RuntimeNodeOf<FlowMaybeType> | FlowMaybeTypeFromInput) {
  if (isNodeData<'flow_maybe_type'>(input)) {
    return flow_maybe_type_({
      primaryType: input.fields?.primary_type,
    });
  }
  const obj = input as FlowMaybeTypeFromInput;
  return flow_maybe_type_({
    primaryType: _resolveTypeIdentifier5(obj.primary_type),
  });
}

export function forInStatementFrom(input: RuntimeNodeOf<ForInStatement> | ForInStatementFromInput) {
  if (isNodeData<'for_in_statement'>(input)) {
    return for_in_statement_({
      body: input.fields?.body,
      kind: input.fields?.kind,
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
      value: input.fields?.value,
    });
  }
  const obj = input as ForInStatementFromInput;
  return for_in_statement_({
    body: _resolveTypeIdentifier10(obj.body),
    kind: _resolveTypeIdentifier11(obj.kind),
    left: _rh2pegt2(obj.left),
    operator: _resolveTypeIdentifier12(obj.operator),
    right: _resolveExpressions3(obj.right),
    value: _resolveTypeIdentifier6(obj.value),
  });
}

export function forStatementFrom(input: RuntimeNodeOf<ForStatement> | ForStatementFromInput) {
  if (isNodeData<'for_statement'>(input)) {
    return for_statement_({
      ...Object.fromEntries(Object.entries(input.fields ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as ForStatementConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return for_statement_(mapped as ForStatementConfig);
}

export function forStatementV0From(input: RuntimeNodeOf<ForStatementV0> | ForStatementV0FromInput) {
  if (isNodeData<'for_statement'>(input)) {
    return for_statement__v0_({
      body: input.fields?.body,
      condition: input.fields?.condition,
      increment: input.fields?.increment,
      initializer: input.fields?.initializer,
    });
  }
  const obj = input as ForStatementV0FromInput;
  return for_statement__v0_({
    body: _resolveTypeIdentifier10(obj.body),
    condition: obj.condition !== undefined ? ((Array.isArray(obj.condition) ? obj.condition : [obj.condition])).map(_r1wds2lp) : [],
    increment: _resolveExpressions3(obj.increment),
    initializer: _r1n4up13(obj.initializer),
  });
}

export function forStatementV1From(input: RuntimeNodeOf<ForStatementV1> | ForStatementV1FromInput) {
  if (isNodeData<'for_statement'>(input)) {
    return for_statement__v1_({
      body: input.fields?.body,
      condition: input.fields?.condition,
      increment: input.fields?.increment,
      initializer: input.fields?.initializer,
    });
  }
  const obj = input as ForStatementV1FromInput;
  return for_statement__v1_({
    body: _resolveTypeIdentifier10(obj.body),
    condition: obj.condition !== undefined ? ((Array.isArray(obj.condition) ? obj.condition : [obj.condition])).map(_r1wds2lp) : [],
    increment: _resolveExpressions3(obj.increment),
    initializer: _r1n4up13(obj.initializer),
  });
}

export function formalParametersFrom(input: RuntimeNodeOf<FormalParameters>): ReturnType<typeof formal_parameters_>;
export function formalParametersFrom(...children: FormalParametersFromInput[]): ReturnType<typeof formal_parameters_>;
export function formalParametersFrom(...args: (RuntimeNodeOf<FormalParameters> | FormalParametersFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'formal_parameters') {
    return formal_parameters_(...(args[0].children ?? []) as (OptionalParameter | RequiredParameter)[]);
  }
  return formal_parameters_(...args.map(v => _r1vnjrv9(v)) as (OptionalParameter | RequiredParameter)[]);
}

export function functionDeclarationFrom(input: RuntimeNodeOf<FunctionDeclaration> | FunctionDeclarationFromInput) {
  if (isNodeData<'function_declaration'>(input)) {
    return function_declaration_({
      body: input.fields?.body,
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as FunctionDeclarationFromInput;
  return function_declaration_({
    ..._resolveCallSignatureFields(obj),
    body: _resolveStatement3(obj.body),
    name: _resolveImportIdentifier(obj.name),
  });
}

export function functionExpressionFrom(input: RuntimeNodeOf<FunctionExpression> | FunctionExpressionFromInput) {
  if (isNodeData<'function_expression'>(input)) {
    return function_expression_({
      body: input.fields?.body,
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as FunctionExpressionFromInput;
  return function_expression_({
    ..._resolveCallSignatureFields(obj),
    body: _resolveStatement3(obj.body),
    name: _resolveImportIdentifier(obj.name),
  });
}

export function functionSignatureFrom(input: RuntimeNodeOf<FunctionSignature> | FunctionSignatureFromInput) {
  if (isNodeData<'function_signature'>(input)) {
    return function_signature_({
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as FunctionSignatureFromInput;
  return function_signature_({
    ..._resolveCallSignatureFields(obj),
    name: _resolveImportIdentifier(obj.name),
  });
}

export function functionTypeFrom(input: RuntimeNodeOf<FunctionType> | FunctionTypeFromInput) {
  if (isNodeData<'function_type'>(input)) {
    return function_type_({
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as FunctionTypeFromInput;
  return function_type_({
    parameters: _r1400tna(obj.parameters),
    returnType: _rhgo79(obj.return_type),
    typeParameters: _r19n9507(obj.type_parameters),
  });
}

export function generatorFunctionFrom(input: RuntimeNodeOf<GeneratorFunction> | GeneratorFunctionFromInput) {
  if (isNodeData<'generator_function'>(input)) {
    return generator_function_({
      body: input.fields?.body,
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as GeneratorFunctionFromInput;
  return generator_function_({
    ..._resolveCallSignatureFields(obj),
    body: _resolveStatement3(obj.body),
    name: _resolveImportIdentifier(obj.name),
  });
}

export function generatorFunctionDeclarationFrom(input: RuntimeNodeOf<GeneratorFunctionDeclaration> | GeneratorFunctionDeclarationFromInput) {
  if (isNodeData<'generator_function_declaration'>(input)) {
    return generator_function_declaration_({
      body: input.fields?.body,
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as GeneratorFunctionDeclarationFromInput;
  return generator_function_declaration_({
    ..._resolveCallSignatureFields(obj),
    body: _resolveStatement3(obj.body),
    name: _resolveImportIdentifier(obj.name),
  });
}

export function genericTypeFrom(input: RuntimeNodeOf<GenericType> | GenericTypeFromInput) {
  if (isNodeData<'generic_type'>(input)) {
    return generic_type_({
      name: input.fields?.name,
      typeArguments: input.fields?.type_arguments,
    });
  }
  const obj = input as GenericTypeFromInput;
  return generic_type_({
    name: _resolvePrimaryType3(obj.name),
    typeArguments: _r1uqebll(obj.type_arguments),
  });
}

export function ifStatementFrom(input: RuntimeNodeOf<IfStatement> | IfStatementFromInput) {
  if (isNodeData<'if_statement'>(input)) {
    return if_statement_({
      alternative: input.fields?.alternative,
      condition: input.fields?.condition,
      consequence: input.fields?.consequence,
    });
  }
  const obj = input as IfStatementFromInput;
  return if_statement_({
    alternative: _r1totjtz(obj.alternative),
    condition: _resolveAugmentedAssignmentLhs3(obj.condition),
    consequence: _resolveTypeIdentifier10(obj.consequence),
  });
}

export function implementsClauseFrom(input: RuntimeNodeOf<ImplementsClause>): ReturnType<typeof implements_clause_>;
export function implementsClauseFrom(...children: ImplementsClauseFromInput[]): ReturnType<typeof implements_clause_>;
export function implementsClauseFrom(...args: (RuntimeNodeOf<ImplementsClause> | ImplementsClauseFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'implements_clause') {
    return implements_clause_(...(args[0].children ?? []) as (Type)[]);
  }
  return implements_clause_(...args.map(v => _resolveTypeIdentifier4(v)) as (Type)[]);
}

export function importAliasFrom(input: RuntimeNodeOf<ImportAlias> | ImportAliasFromInput) {
  if (isNodeData<'import_alias'>(input)) {
    return import_alias_({
      name: input.fields?.name,
      value: input.fields?.value,
      semicolon: input.fields?.semicolon,
    });
  }
  const obj = input as ImportAliasFromInput;
  return import_alias_({
    name: _resolveImportIdentifier(obj.name),
    value: _r1twh7u2(obj.value),
    semicolon: _resolveTypeIdentifier3(obj.semicolon),
  });
}

export function importAttributeFrom(input: RuntimeNodeOf<ImportAttribute> | ImportAttributeFromInput) {
  if (isNodeData<'import_attribute'>(input)) {
    return import_attribute_({
      object: input.fields?.object,
    });
  }
  const obj = input as ImportAttributeFromInput;
  return import_attribute_({
    object: _resolvePrimaryExpression3(obj.object),
  });
}

export function importClauseFrom(input: RuntimeNodeOf<ImportClause> | ImportClauseFromInput) {
  if (isNodeData<'import_clause'>(input)) {
    return import_clause_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as ImportClauseConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return import_clause_(mapped as ImportClauseConfig);
}

export function importClauseV0From(input: RuntimeNodeOf<ImportClauseV0> | ImportClauseV0FromInput) {
  if (isNodeData<'import_clause'>(input)) {
    return import_clause__v0_({
      defaultImport: input.fields?.default_import,
    });
  }
  const obj = input as ImportClauseV0FromInput;
  return import_clause__v0_({
    defaultImport: _r1588e16(obj.default_import),
  });
}

export function importClauseCommaFrom(input: RuntimeNodeOf<ImportClauseComma> | ImportClauseCommaFromInput) {
  if (isNodeData<'import_clause'>(input)) {
    return import_clause__comma_({
      defaultImport: input.fields?.default_import,
    });
  }
  const obj = input as ImportClauseCommaFromInput;
  return import_clause__comma_({
    defaultImport: _r1588e16(obj.default_import),
  });
}

export function importRequireClauseFrom(input: RuntimeNodeOf<ImportRequireClause> | ImportRequireClauseFromInput) {
  if (isNodeData<'import_require_clause'>(input)) {
    return import_require_clause_({
      source: input.fields?.source,
      identifier: input.fields?.identifier,
    });
  }
  const obj = input as ImportRequireClauseFromInput;
  return import_require_clause_({
    source: _resolveModuleExportName3(obj.source),
    identifier: _resolveImportIdentifier(obj.identifier),
  });
}

export function importSpecifierFrom(input: RuntimeNodeOf<ImportSpecifier> | ImportSpecifierFromInput) {
  if (isNodeData<'import_specifier'>(input)) {
    return import_specifier_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as ImportSpecifierConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return import_specifier_(mapped as ImportSpecifierConfig);
}

export function importSpecifierV0From(input: RuntimeNodeOf<ImportSpecifierV0> | ImportSpecifierV0FromInput) {
  if (isNodeData<'import_specifier'>(input)) {
    return import_specifier__v0_({
      name: input.fields?.name,
    });
  }
  const obj = input as ImportSpecifierV0FromInput;
  return import_specifier__v0_({
    name: _resolveModuleExportName4(obj.name),
  });
}

export function importSpecifierAsFrom(input: RuntimeNodeOf<ImportSpecifierAs> | ImportSpecifierAsFromInput) {
  if (isNodeData<'import_specifier'>(input)) {
    return import_specifier__as_({
      alias: input.fields?.alias,
      name: input.fields?.name,
    });
  }
  const obj = input as ImportSpecifierAsFromInput;
  return import_specifier__as_({
    alias: _resolveImportIdentifier2(obj.alias),
    name: _resolveModuleExportName4(obj.name),
  });
}

export function importStatementFrom(input: RuntimeNodeOf<ImportStatement> | ImportStatementFromInput) {
  if (isNodeData<'import_statement'>(input)) {
    return import_statement_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as ImportStatementConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return import_statement_(mapped as ImportStatementConfig);
}

export function importStatementFromClauseFrom(input: RuntimeNodeOf<ImportStatementFromClause> | ImportStatementFromClauseFromInput) {
  if (isNodeData<'import_statement'>(input)) {
    return import_statement__from_clause_({
      importClause: input.fields?.import_clause,
      fromClause: input.fields?.from_clause,
      importAttribute: input.fields?.import_attribute,
      semicolon: input.fields?.semicolon,
    });
  }
  const obj = input as ImportStatementFromClauseFromInput;
  return import_statement__from_clause_({
    importClause: _r113fjlk(obj.import_clause),
    fromClause: _resolveTypeIdentifier3(obj.from_clause),
    importAttribute: _r1n7cbqo(obj.import_attribute),
    semicolon: _resolveTypeIdentifier3(obj.semicolon),
  });
}

export function importStatementV1From(input: RuntimeNodeOf<ImportStatementV1> | ImportStatementV1FromInput) {
  if (isNodeData<'import_statement'>(input)) {
    return import_statement__v1_({
      importClause: input.fields?.import_clause,
      importAttribute: input.fields?.import_attribute,
      semicolon: input.fields?.semicolon,
    });
  }
  const obj = input as ImportStatementV1FromInput;
  return import_statement__v1_({
    importClause: _r113fjlk(obj.import_clause),
    importAttribute: _r1n7cbqo(obj.import_attribute),
    semicolon: _resolveTypeIdentifier3(obj.semicolon),
  });
}

export function importStatementSourceFrom(input: RuntimeNodeOf<ImportStatementSource> | ImportStatementSourceFromInput) {
  if (isNodeData<'import_statement'>(input)) {
    return import_statement__source_({
      source: input.fields?.source,
      importAttribute: input.fields?.import_attribute,
      semicolon: input.fields?.semicolon,
    });
  }
  const obj = input as ImportStatementSourceFromInput;
  return import_statement__source_({
    source: _resolveModuleExportName3(obj.source),
    importAttribute: _r1n7cbqo(obj.import_attribute),
    semicolon: _resolveTypeIdentifier3(obj.semicolon),
  });
}

export function indexSignatureFrom(input: RuntimeNodeOf<IndexSignature> | IndexSignatureFromInput) {
  if (isNodeData<'index_signature'>(input)) {
    return index_signature_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as IndexSignatureConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return index_signature_(mapped as IndexSignatureConfig);
}

export function indexSignatureColonFrom(input: RuntimeNodeOf<IndexSignatureColon> | IndexSignatureColonFromInput) {
  if (isNodeData<'index_signature'>(input)) {
    return index_signature__colon_({
      indexType: input.fields?.index_type,
      name: input.fields?.name,
      sign: input.fields?.sign,
      type: input.fields?.type,
    });
  }
  const obj = input as IndexSignatureColonFromInput;
  return index_signature__colon_({
    indexType: _resolveTypeIdentifier4(obj.index_type),
    name: _resolveImportIdentifier(obj.name),
    sign: _resolveTypeIdentifier13(obj.sign),
    type: _rc3yugm(obj.type),
  });
}

export function indexSignatureMappedTypeClauseFrom(input: RuntimeNodeOf<IndexSignatureMappedTypeClause> | IndexSignatureMappedTypeClauseFromInput) {
  if (isNodeData<'index_signature'>(input)) {
    return index_signature__mapped_type_clause_({
      sign: input.fields?.sign,
      type: input.fields?.type,
      mappedTypeClause: input.fields?.mapped_type_clause,
    });
  }
  const obj = input as IndexSignatureMappedTypeClauseFromInput;
  return index_signature__mapped_type_clause_({
    sign: _resolveTypeIdentifier13(obj.sign),
    type: _rc3yugm(obj.type),
    mappedTypeClause: _ro6lixi(obj.mapped_type_clause),
  });
}

export function indexTypeQueryFrom(input: RuntimeNodeOf<IndexTypeQuery> | IndexTypeQueryFromInput) {
  if (isNodeData<'index_type_query'>(input)) {
    return index_type_query_({
      primaryType: input.fields?.primary_type,
    });
  }
  const obj = input as IndexTypeQueryFromInput;
  return index_type_query_({
    primaryType: _resolveTypeIdentifier5(obj.primary_type),
  });
}

export function inferTypeFrom(input: RuntimeNodeOf<InferType> | InferTypeFromInput) {
  if (isNodeData<'infer_type'>(input)) {
    return infer_type_({
      typeIdentifier: input.fields?.type_identifier,
      constraint: input.fields?.constraint,
    });
  }
  const obj = input as InferTypeFromInput;
  return infer_type_({
    typeIdentifier: _resolveTypeIdentifier2(obj.type_identifier),
    constraint: _resolveTypeIdentifier14(obj.constraint),
  });
}

export function instantiationExpressionFrom(input: RuntimeNodeOf<InstantiationExpression> | InstantiationExpressionFromInput) {
  if (isNodeData<'instantiation_expression'>(input)) {
    return instantiation_expression_({
      function: input.fields?.function,
      typeArguments: input.fields?.type_arguments,
      expression: input.fields?.expression,
    });
  }
  const obj = input as InstantiationExpressionFromInput;
  return instantiation_expression_({
    function: _r8pjzv1(obj.function),
    typeArguments: _r1uqebll(obj.type_arguments),
    expression: _resolveTypeIdentifier6(obj.expression),
  });
}

export function interfaceBodyFrom(input: RuntimeNodeOf<InterfaceBody>): ReturnType<typeof interface_body_>;
export function interfaceBodyFrom(...children: InterfaceBodyFromInput[]): ReturnType<typeof interface_body_>;
export function interfaceBodyFrom(...args: (RuntimeNodeOf<InterfaceBody> | InterfaceBodyFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'interface_body') {
    return interface_body_(...(args[0].children ?? []) as (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]);
  }
  return interface_body_(...args.map(v => _ra5cxfs(v)) as (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]);
}

export function interfaceDeclarationFrom(input: RuntimeNodeOf<InterfaceDeclaration> | InterfaceDeclarationFromInput) {
  if (isNodeData<'interface_declaration'>(input)) {
    return interface_declaration_({
      body: input.fields?.body,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      extendsTypeClause: input.fields?.extends_type_clause,
    });
  }
  const obj = input as InterfaceDeclarationFromInput;
  return interface_declaration_({
    body: _rk01cxs(obj.body),
    name: _resolveTypeIdentifier2(obj.name),
    typeParameters: _r19n9507(obj.type_parameters),
    extendsTypeClause: _r163ugqi(obj.extends_type_clause),
  });
}

export function internalModuleFrom(input: RuntimeNodeOf<InternalModule> | InternalModuleFromInput) {
  if (isNodeData<'internal_module'>(input)) {
    return internal_module_({
      body: input.fields?.body,
      name: input.fields?.name,
    });
  }
  const obj = input as InternalModuleFromInput;
  return internal_module_({
    body: _resolveStatement3(obj.body),
    name: _r17uug3x(obj.name),
  });
}

export function intersectionTypeFrom(input: RuntimeNodeOf<IntersectionType> | IntersectionTypeFromInput) {
  if (isNodeData<'intersection_type'>(input)) {
    return intersection_type_({
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as IntersectionTypeFromInput;
  return intersection_type_({
    left: _resolveTypeIdentifier4(obj.left),
    right: _resolveTypeIdentifier4(obj.right),
  });
}

export function labeledStatementFrom(input: RuntimeNodeOf<LabeledStatement> | LabeledStatementFromInput) {
  if (isNodeData<'labeled_statement'>(input)) {
    return labeled_statement_({
      body: input.fields?.body,
      label: input.fields?.label,
    });
  }
  const obj = input as LabeledStatementFromInput;
  return labeled_statement_({
    body: _resolveTypeIdentifier10(obj.body),
    label: _r11b7pfd(obj.label),
  });
}

export function lexicalDeclarationFrom(input: RuntimeNodeOf<LexicalDeclaration> | LexicalDeclarationFromInput) {
  if (isNodeData<'lexical_declaration'>(input)) {
    return lexical_declaration_({
      kind: input.fields?.kind,
      declarators: input.fields?.declarators,
      semicolon: input.fields?.semicolon,
    });
  }
  const obj = input as LexicalDeclarationFromInput;
  return lexical_declaration_({
    kind: _resolveTypeIdentifier15(obj.kind),
    declarators: obj.declarators !== undefined ? ((Array.isArray(obj.declarators) ? obj.declarators : [obj.declarators])).map(_r1xs8n9k) : [],
    semicolon: _resolveTypeIdentifier3(obj.semicolon),
  });
}

export function literalTypeFrom(input: RuntimeNodeOf<LiteralType>): ReturnType<typeof literal_type_>;
export function literalTypeFrom(child: LiteralTypeFromInput): ReturnType<typeof literal_type_>;
export function literalTypeFrom(arg?: RuntimeNodeOf<LiteralType> | LiteralTypeFromInput) {
  if (isNodeData(arg) && arg.type === 'literal_type') {
    return literal_type_(arg.children?.[0] as False | Null | Number | String | True | UnaryExpression | Undefined);
  }
  return literal_type_(_resolveExpression2(arg) as False | Null | Number | String | True | UnaryExpression | Undefined);
}

export function lookupTypeFrom(input: RuntimeNodeOf<LookupType> | LookupTypeFromInput) {
  if (isNodeData<'lookup_type'>(input)) {
    return lookup_type_({
      primaryType: input.fields?.primary_type,
      indexType: input.fields?.index_type,
    });
  }
  const obj = input as LookupTypeFromInput;
  return lookup_type_({
    primaryType: _resolveTypeIdentifier5(obj.primary_type),
    indexType: _resolveTypeIdentifier4(obj.index_type),
  });
}

export function mappedTypeClauseFrom(input: RuntimeNodeOf<MappedTypeClause> | MappedTypeClauseFromInput) {
  if (isNodeData<'mapped_type_clause'>(input)) {
    return mapped_type_clause_({
      alias: input.fields?.alias,
      name: input.fields?.name,
      type: input.fields?.type,
    });
  }
  const obj = input as MappedTypeClauseFromInput;
  return mapped_type_clause_({
    alias: _resolveTypeIdentifier4(obj.alias),
    name: _resolveTypeIdentifier2(obj.name),
    type: _resolveTypeIdentifier4(obj.type),
  });
}

export function memberExpressionFrom(input: RuntimeNodeOf<MemberExpression> | MemberExpressionFromInput) {
  if (isNodeData<'member_expression'>(input)) {
    return member_expression_({
      object: input.fields?.object,
      optionalChain: input.fields?.optional_chain,
      property: input.fields?.property,
    });
  }
  const obj = input as MemberExpressionFromInput;
  return member_expression_({
    object: _r10xn03h(obj.object),
    optionalChain: _rt995je(obj.optional_chain),
    property: _resolvePropertyName4(obj.property),
  });
}

export function methodDefinitionFrom(input: RuntimeNodeOf<MethodDefinition> | MethodDefinitionFromInput) {
  if (isNodeData<'method_definition'>(input)) {
    return method_definition_({
      body: input.fields?.body,
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
      accessibilityModifier: input.fields?.accessibility_modifier,
      overrideModifier: input.fields?.override_modifier,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as MethodDefinitionFromInput;
  return method_definition_({
    ..._resolveCallSignatureFields(obj),
    body: _resolveStatement3(obj.body),
    name: _resolvePropertyName2(obj.name),
    accessibilityModifier: _resolveParameterName2(obj.accessibility_modifier),
    overrideModifier: _resolveParameterName3(obj.override_modifier),
  });
}

export function methodSignatureFrom(input: RuntimeNodeOf<MethodSignature> | MethodSignatureFromInput) {
  if (isNodeData<'method_signature'>(input)) {
    return method_signature_({
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
      accessibilityModifier: input.fields?.accessibility_modifier,
      overrideModifier: input.fields?.override_modifier,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as MethodSignatureFromInput;
  return method_signature_({
    ..._resolveCallSignatureFields(obj),
    name: _resolvePropertyName2(obj.name),
    accessibilityModifier: _resolveParameterName2(obj.accessibility_modifier),
    overrideModifier: _resolveParameterName3(obj.override_modifier),
  });
}

export function moduleFrom(input: RuntimeNodeOf<Module> | ModuleFromInput) {
  if (isNodeData<'module'>(input)) {
    return module_({
      body: input.fields?.body,
      name: input.fields?.name,
    });
  }
  const obj = input as ModuleFromInput;
  return module_({
    body: _resolveStatement3(obj.body),
    name: _r17uug3x(obj.name),
  });
}

export function namedImportsFrom(input: RuntimeNodeOf<NamedImports>): ReturnType<typeof named_imports_>;
export function namedImportsFrom(...children: NamedImportsFromInput[]): ReturnType<typeof named_imports_>;
export function namedImportsFrom(...args: (RuntimeNodeOf<NamedImports> | NamedImportsFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'named_imports') {
    return named_imports_(...(args[0].children ?? []) as (ImportSpecifier)[]);
  }
  return named_imports_(...args.map(v => _r1cojdna(v)) as (ImportSpecifier)[]);
}

export function namespaceExportFrom(input: RuntimeNodeOf<NamespaceExport>): ReturnType<typeof namespace_export_>;
export function namespaceExportFrom(child: NamespaceExportFromInput): ReturnType<typeof namespace_export_>;
export function namespaceExportFrom(arg?: RuntimeNodeOf<NamespaceExport> | NamespaceExportFromInput) {
  if (isNodeData(arg) && arg.type === 'namespace_export') {
    return namespace_export_(arg.children?.[0] as ModuleExportName);
  }
  return namespace_export_(_resolveModuleExportName(arg) as ModuleExportName);
}

export function namespaceImportFrom(input: RuntimeNodeOf<NamespaceImport> | NamespaceImportFromInput) {
  if (isNodeData<'namespace_import'>(input)) {
    return namespace_import_({
      identifier: input.fields?.identifier,
    });
  }
  const obj = input as NamespaceImportFromInput;
  return namespace_import_({
    identifier: _resolveImportIdentifier(obj.identifier),
  });
}

export function nestedIdentifierFrom(input: RuntimeNodeOf<NestedIdentifier> | NestedIdentifierFromInput) {
  if (isNodeData<'nested_identifier'>(input)) {
    return nested_identifier_({
      object: input.fields?.object,
      property: input.fields?.property,
    });
  }
  const obj = input as NestedIdentifierFromInput;
  return nested_identifier_({
    object: _resolveAugmentedAssignmentLhs4(obj.object),
    property: _resolvePropertyName5(obj.property),
  });
}

export function nestedTypeIdentifierFrom(input: RuntimeNodeOf<NestedTypeIdentifier> | NestedTypeIdentifierFromInput) {
  if (isNodeData<'nested_type_identifier'>(input)) {
    return nested_type_identifier_({
      module: input.fields?.module,
      name: input.fields?.name,
    });
  }
  const obj = input as NestedTypeIdentifierFromInput;
  return nested_type_identifier_({
    module: _r1twh7u2(obj.module),
    name: _resolveTypeIdentifier2(obj.name),
  });
}

export function newExpressionFrom(input: RuntimeNodeOf<NewExpression> | NewExpressionFromInput) {
  if (isNodeData<'new_expression'>(input)) {
    return new_expression_({
      arguments: input.fields?.arguments,
      constructor: input.fields?.constructor,
      typeArguments: input.fields?.type_arguments,
    });
  }
  const obj = input as NewExpressionFromInput;
  return new_expression_({
    arguments: _r17o033s(obj.arguments),
    constructor: _resolveTypeIdentifier16(obj.constructor),
    typeArguments: _r1uqebll(obj.type_arguments),
  });
}

export function nonNullExpressionFrom(input: RuntimeNodeOf<NonNullExpression> | NonNullExpressionFromInput) {
  if (isNodeData<'non_null_expression'>(input)) {
    return non_null_expression_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as NonNullExpressionFromInput;
  return non_null_expression_({
    expression: _resolveTypeIdentifier6(obj.expression),
  });
}

export function objectFrom(input: RuntimeNodeOf<Object>): ReturnType<typeof object_>;
export function objectFrom(...children: ObjectFromInput[]): ReturnType<typeof object_>;
export function objectFrom(...args: (RuntimeNodeOf<Object> | ObjectFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'object') {
    return object_(...(args[0].children ?? []) as (MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement)[]);
  }
  return object_(...args.map(v => _ri5vclk(v)) as (MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement)[]);
}

export function objectAssignmentPatternFrom(input: RuntimeNodeOf<ObjectAssignmentPattern> | ObjectAssignmentPatternFromInput) {
  if (isNodeData<'object_assignment_pattern'>(input)) {
    return object_assignment_pattern_({
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as ObjectAssignmentPatternFromInput;
  return object_assignment_pattern_({
    left: _r60k8ng(obj.left),
    right: _resolveTypeIdentifier6(obj.right),
  });
}

export function objectPatternFrom(input: RuntimeNodeOf<ObjectPattern>): ReturnType<typeof object_pattern_>;
export function objectPatternFrom(...children: ObjectPatternFromInput[]): ReturnType<typeof object_pattern_>;
export function objectPatternFrom(...args: (RuntimeNodeOf<ObjectPattern> | ObjectPatternFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'object_pattern') {
    return object_pattern_(...(args[0].children ?? []) as (ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern)[]);
  }
  return object_pattern_(...args.map(v => _r1r6luag(v)) as (ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern)[]);
}

export function objectTypeFrom(input: RuntimeNodeOf<ObjectType> | ObjectTypeFromInput) {
  if (isNodeData<'object_type'>(input)) {
    return object_type_({
      opening: input.fields?.opening,
      members: input.fields?.members,
      closing: input.fields?.closing,
    });
  }
  const obj = input as ObjectTypeFromInput;
  return object_type_({
    opening: _ra5cxfs(obj.opening),
    members: obj.members !== undefined ? ((Array.isArray(obj.members) ? obj.members : [obj.members])).map(_ra5cxfs) : undefined,
    closing: _resolveTypeIdentifier3(obj.closing),
  });
}

export function omittingTypeAnnotationFrom(input: RuntimeNodeOf<OmittingTypeAnnotation>): ReturnType<typeof omitting_type_annotation_>;
export function omittingTypeAnnotationFrom(child: OmittingTypeAnnotationFromInput): ReturnType<typeof omitting_type_annotation_>;
export function omittingTypeAnnotationFrom(arg?: RuntimeNodeOf<OmittingTypeAnnotation> | OmittingTypeAnnotationFromInput) {
  if (isNodeData(arg) && arg.type === 'omitting_type_annotation') {
    return omitting_type_annotation_(arg.children?.[0] as Type);
  }
  return omitting_type_annotation_(_resolveTypeIdentifier4(arg) as Type);
}

export function optingTypeAnnotationFrom(input: RuntimeNodeOf<OptingTypeAnnotation>): ReturnType<typeof opting_type_annotation_>;
export function optingTypeAnnotationFrom(child: OptingTypeAnnotationFromInput): ReturnType<typeof opting_type_annotation_>;
export function optingTypeAnnotationFrom(arg?: RuntimeNodeOf<OptingTypeAnnotation> | OptingTypeAnnotationFromInput) {
  if (isNodeData(arg) && arg.type === 'opting_type_annotation') {
    return opting_type_annotation_(arg.children?.[0] as Type);
  }
  return opting_type_annotation_(_resolveTypeIdentifier4(arg) as Type);
}

export function optionalParameterFrom(input: RuntimeNodeOf<OptionalParameter> | OptionalParameterFromInput) {
  if (isNodeData<'optional_parameter'>(input)) {
    return optional_parameter_({
      decorator: input.fields?.decorator,
      name: input.fields?.name,
      pattern: input.fields?.pattern,
      type: input.fields?.type,
      value: input.fields?.value,
      parameterName: input.fields?.parameter_name,
      initializer: input.fields?.initializer,
    });
  }
  const obj = input as OptionalParameterFromInput;
  return optional_parameter_({
    ..._resolveInitializerFields(obj),
    ..._resolveParameterNameFields(obj),
    name: _resolveImportIdentifier(obj.name),
    type: _r1s2j1z2(obj.type),
    parameterName: _resolveParameterName4(obj.parameter_name),
    initializer: _resolveParameterName(obj.initializer),
  });
}

export function optionalTypeFrom(input: RuntimeNodeOf<OptionalType>): ReturnType<typeof optional_type_>;
export function optionalTypeFrom(child: OptionalTypeFromInput): ReturnType<typeof optional_type_>;
export function optionalTypeFrom(arg?: RuntimeNodeOf<OptionalType> | OptionalTypeFromInput) {
  if (isNodeData(arg) && arg.type === 'optional_type') {
    return optional_type_(arg.children?.[0] as Type);
  }
  return optional_type_(_resolveTypeIdentifier4(arg) as Type);
}

export function pairFrom(input: RuntimeNodeOf<Pair> | PairFromInput) {
  if (isNodeData<'pair'>(input)) {
    return pair_({
      key: input.fields?.key,
      value: input.fields?.value,
    });
  }
  const obj = input as PairFromInput;
  return pair_({
    key: _resolvePropertyName2(obj.key),
    value: _resolveTypeIdentifier6(obj.value),
  });
}

export function pairPatternFrom(input: RuntimeNodeOf<PairPattern> | PairPatternFromInput) {
  if (isNodeData<'pair_pattern'>(input)) {
    return pair_pattern_({
      key: input.fields?.key,
      value: input.fields?.value,
    });
  }
  const obj = input as PairPatternFromInput;
  return pair_pattern_({
    key: _resolvePropertyName2(obj.key),
    value: _r1lwpcqg(obj.value),
  });
}

export function parenthesizedExpressionFrom(input: RuntimeNodeOf<ParenthesizedExpression> | ParenthesizedExpressionFromInput) {
  if (isNodeData<'parenthesized_expression'>(input)) {
    return parenthesized_expression_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
      children: input.children?.[0],
    } as ParenthesizedExpressionConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return parenthesized_expression_(mapped as ParenthesizedExpressionConfig);
}

export function parenthesizedExpressionTypeFrom(input: RuntimeNodeOf<ParenthesizedExpressionType> | ParenthesizedExpressionTypeFromInput) {
  if (isNodeData<'parenthesized_expression'>(input)) {
    return parenthesized_expression__type_({
      type: input.fields?.type,
      children: input.children?.[0] as ParenthesizedExpressionTypeConfig['children'],
    });
  }
  const obj = input as ParenthesizedExpressionTypeFromInput;
  return parenthesized_expression__type_({
    type: _r1s2j1z2(obj.type),
    children: (obj as { children?: unknown }).children as ParenthesizedExpressionTypeConfig['children'],
  });
}

export function parenthesizedExpressionV1From(input: RuntimeNodeOf<ParenthesizedExpressionV1> | ParenthesizedExpressionV1FromInput) {
  if (isNodeData<'parenthesized_expression'>(input)) {
    return parenthesized_expression__v1_({
      children: input.children?.[0] as ParenthesizedExpressionV1Config['children'],
    });
  }
  const obj = input as ParenthesizedExpressionV1FromInput;
  return parenthesized_expression__v1_({
    children: (obj as { children?: unknown }).children as ParenthesizedExpressionV1Config['children'],
  });
}

export function parenthesizedTypeFrom(input: RuntimeNodeOf<ParenthesizedType>): ReturnType<typeof parenthesized_type_>;
export function parenthesizedTypeFrom(child: ParenthesizedTypeFromInput): ReturnType<typeof parenthesized_type_>;
export function parenthesizedTypeFrom(arg?: RuntimeNodeOf<ParenthesizedType> | ParenthesizedTypeFromInput) {
  if (isNodeData(arg) && arg.type === 'parenthesized_type') {
    return parenthesized_type_(arg.children?.[0] as Type);
  }
  return parenthesized_type_(_resolveTypeIdentifier4(arg) as Type);
}

export function programFrom(input: RuntimeNodeOf<Program> | ProgramFromInput) {
  if (isNodeData<'program'>(input)) {
    return program_({
      hashBangLine: input.fields?.hash_bang_line,
      statements: input.fields?.statements,
    });
  }
  const obj = input as ProgramFromInput;
  return program_({
    hashBangLine: _r1lt2x38(obj.hash_bang_line),
    statements: obj.statements !== undefined ? ((Array.isArray(obj.statements) ? obj.statements : [obj.statements])).map(_resolveTypeIdentifier10) : undefined,
  });
}

export function propertySignatureFrom(input: RuntimeNodeOf<PropertySignature> | PropertySignatureFromInput) {
  if (isNodeData<'property_signature'>(input)) {
    return property_signature_({
      name: input.fields?.name,
      type: input.fields?.type,
      accessibilityModifier: input.fields?.accessibility_modifier,
      overrideModifier: input.fields?.override_modifier,
    });
  }
  const obj = input as PropertySignatureFromInput;
  return property_signature_({
    name: _resolvePropertyName2(obj.name),
    type: _r1s2j1z2(obj.type),
    accessibilityModifier: _resolveParameterName2(obj.accessibility_modifier),
    overrideModifier: _resolveParameterName3(obj.override_modifier),
  });
}

export function publicFieldDefinitionFrom(input: RuntimeNodeOf<PublicFieldDefinition> | PublicFieldDefinitionFromInput) {
  if (isNodeData<'public_field_definition'>(input)) {
    return public_field_definition_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as PublicFieldDefinitionConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return public_field_definition_(mapped as PublicFieldDefinitionConfig);
}

export function publicFieldDefinitionStaticFrom(input: RuntimeNodeOf<PublicFieldDefinitionStatic> | PublicFieldDefinitionStaticFromInput) {
  if (isNodeData<'public_field_definition'>(input)) {
    return public_field_definition__static_({
      decorator: input.fields?.decorator,
      name: input.fields?.name,
      type: input.fields?.type,
      accessibilityModifier: input.fields?.accessibility_modifier,
      overrideModifier: input.fields?.override_modifier,
      initializer: input.fields?.initializer,
    });
  }
  const obj = input as PublicFieldDefinitionStaticFromInput;
  return public_field_definition__static_({
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    name: _resolvePropertyName2(obj.name),
    type: _r1s2j1z2(obj.type),
    accessibilityModifier: _resolveParameterName2(obj.accessibility_modifier),
    overrideModifier: _resolveParameterName3(obj.override_modifier),
    initializer: _resolveTypeIdentifier3(obj.initializer),
  });
}

export function publicFieldDefinitionAbstractFrom(input: RuntimeNodeOf<PublicFieldDefinitionAbstract> | PublicFieldDefinitionAbstractFromInput) {
  if (isNodeData<'public_field_definition'>(input)) {
    return public_field_definition__abstract_({
      decorator: input.fields?.decorator,
      name: input.fields?.name,
      type: input.fields?.type,
      accessibilityModifier: input.fields?.accessibility_modifier,
      initializer: input.fields?.initializer,
    });
  }
  const obj = input as PublicFieldDefinitionAbstractFromInput;
  return public_field_definition__abstract_({
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    name: _resolvePropertyName2(obj.name),
    type: _r1s2j1z2(obj.type),
    accessibilityModifier: _resolveParameterName2(obj.accessibility_modifier),
    initializer: _resolveTypeIdentifier3(obj.initializer),
  });
}

export function publicFieldDefinitionAccessorFrom(input: RuntimeNodeOf<PublicFieldDefinitionAccessor> | PublicFieldDefinitionAccessorFromInput) {
  if (isNodeData<'public_field_definition'>(input)) {
    return public_field_definition__accessor_({
      decorator: input.fields?.decorator,
      name: input.fields?.name,
      type: input.fields?.type,
      accessibilityModifier: input.fields?.accessibility_modifier,
      initializer: input.fields?.initializer,
    });
  }
  const obj = input as PublicFieldDefinitionAccessorFromInput;
  return public_field_definition__accessor_({
    decorator: obj.decorator !== undefined ? ((Array.isArray(obj.decorator) ? obj.decorator : [obj.decorator])).map(_r1fl8679) : undefined,
    name: _resolvePropertyName2(obj.name),
    type: _r1s2j1z2(obj.type),
    accessibilityModifier: _resolveParameterName2(obj.accessibility_modifier),
    initializer: _resolveTypeIdentifier3(obj.initializer),
  });
}

export function readonlyTypeFrom(input: RuntimeNodeOf<ReadonlyType>): ReturnType<typeof readonly_type_>;
export function readonlyTypeFrom(child: ReadonlyTypeFromInput): ReturnType<typeof readonly_type_>;
export function readonlyTypeFrom(arg?: RuntimeNodeOf<ReadonlyType> | ReadonlyTypeFromInput) {
  if (isNodeData(arg) && arg.type === 'readonly_type') {
    return readonly_type_(arg.children?.[0] as Type);
  }
  return readonly_type_(_resolveTypeIdentifier4(arg) as Type);
}

export function regexFrom(input: RuntimeNodeOf<Regex> | RegexFromInput) {
  if (isNodeData<'regex'>(input)) {
    return regex_({
      flags: input.fields?.flags,
      pattern: input.fields?.pattern,
    });
  }
  const obj = input as RegexFromInput;
  return regex_({
    flags: _rup90p(obj.flags),
    pattern: _r13pt1fe(obj.pattern),
  });
}

export function requiredParameterFrom(input: RuntimeNodeOf<RequiredParameter> | RequiredParameterFromInput) {
  if (isNodeData<'required_parameter'>(input)) {
    return required_parameter_({
      decorator: input.fields?.decorator,
      name: input.fields?.name,
      pattern: input.fields?.pattern,
      type: input.fields?.type,
      value: input.fields?.value,
      parameterName: input.fields?.parameter_name,
      initializer: input.fields?.initializer,
    });
  }
  const obj = input as RequiredParameterFromInput;
  return required_parameter_({
    ..._resolveInitializerFields(obj),
    ..._resolveParameterNameFields(obj),
    name: _resolvePattern2(obj.name),
    type: _r1s2j1z2(obj.type),
    parameterName: _resolveParameterName4(obj.parameter_name),
    initializer: _resolveParameterName(obj.initializer),
  });
}

export function restPatternFrom(input: RuntimeNodeOf<RestPattern>): ReturnType<typeof rest_pattern_>;
export function restPatternFrom(child: RestPatternFromInput): ReturnType<typeof rest_pattern_>;
export function restPatternFrom(arg?: RuntimeNodeOf<RestPattern> | RestPatternFromInput) {
  if (isNodeData(arg) && arg.type === 'rest_pattern') {
    return rest_pattern_(arg.children?.[0] as LhsExpression);
  }
  return rest_pattern_(_resolveLhsExpression(arg) as LhsExpression);
}

export function restTypeFrom(input: RuntimeNodeOf<RestType>): ReturnType<typeof rest_type_>;
export function restTypeFrom(child: RestTypeFromInput): ReturnType<typeof rest_type_>;
export function restTypeFrom(arg?: RuntimeNodeOf<RestType> | RestTypeFromInput) {
  if (isNodeData(arg) && arg.type === 'rest_type') {
    return rest_type_(arg.children?.[0] as Type);
  }
  return rest_type_(_resolveTypeIdentifier4(arg) as Type);
}

export function returnStatementFrom(input: RuntimeNodeOf<ReturnStatement>): ReturnType<typeof return_statement_>;
export function returnStatementFrom(child?: ReturnStatementFromInput): ReturnType<typeof return_statement_>;
export function returnStatementFrom(arg?: RuntimeNodeOf<ReturnStatement> | ReturnStatementFromInput) {
  if (isNodeData(arg) && arg.type === 'return_statement') {
    return return_statement_(arg.children?.[0] as Expression | SequenceExpression);
  }
  return return_statement_(_resolveExpressions2(arg) as Expression | SequenceExpression);
}

export function satisfiesExpressionFrom(input: RuntimeNodeOf<SatisfiesExpression> | SatisfiesExpressionFromInput) {
  if (isNodeData<'satisfies_expression'>(input)) {
    return satisfies_expression_({
      expression: input.fields?.expression,
      typeAnnotation: input.fields?.type_annotation,
    });
  }
  const obj = input as SatisfiesExpressionFromInput;
  return satisfies_expression_({
    expression: _resolveTypeIdentifier6(obj.expression),
    typeAnnotation: _resolveTypeIdentifier4(obj.type_annotation),
  });
}

export function sequenceExpressionFrom(input: RuntimeNodeOf<SequenceExpression>): ReturnType<typeof sequence_expression_>;
export function sequenceExpressionFrom(...children: SequenceExpressionFromInput[]): ReturnType<typeof sequence_expression_>;
export function sequenceExpressionFrom(...args: (RuntimeNodeOf<SequenceExpression> | SequenceExpressionFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'sequence_expression') {
    return sequence_expression_(...(args[0].children ?? []) as (Expression)[]);
  }
  return sequence_expression_(...args.map(v => _resolveTypeIdentifier6(v)) as (Expression)[]);
}

export function spreadElementFrom(input: RuntimeNodeOf<SpreadElement> | SpreadElementFromInput) {
  if (isNodeData<'spread_element'>(input)) {
    return spread_element_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as SpreadElementFromInput;
  return spread_element_({
    expression: _resolveTypeIdentifier6(obj.expression),
  });
}

export function statementBlockFrom(input: RuntimeNodeOf<StatementBlock> | StatementBlockFromInput) {
  if (isNodeData<'statement_block'>(input)) {
    return statement_block_({
      statements: input.fields?.statements,
      automaticSemicolon: input.fields?.automatic_semicolon,
    });
  }
  const obj = input as StatementBlockFromInput;
  return statement_block_({
    statements: obj.statements !== undefined ? ((Array.isArray(obj.statements) ? obj.statements : [obj.statements])).map(_resolveTypeIdentifier10) : undefined,
    automaticSemicolon: _resolveTypeIdentifier3(obj.automatic_semicolon),
  });
}

export function stringFrom(input: RuntimeNodeOf<String>): ReturnType<typeof string_>;
export function stringFrom(...children: StringFromInput[]): ReturnType<typeof string_>;
export function stringFrom(...args: (RuntimeNodeOf<String> | StringFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'string') {
    return string_(...(args[0].children ?? []) as (EscapeSequence | StringFragment)[]);
  }
  return string_(...args.map(v => _ram3b4x(v)) as (EscapeSequence | StringFragment)[]);
}

export function subscriptExpressionFrom(input: RuntimeNodeOf<SubscriptExpression> | SubscriptExpressionFromInput) {
  if (isNodeData<'subscript_expression'>(input)) {
    return subscript_expression_({
      index: input.fields?.index,
      object: input.fields?.object,
      optionalChain: input.fields?.optional_chain,
    });
  }
  const obj = input as SubscriptExpressionFromInput;
  return subscript_expression_({
    index: _r1yiq3fd(obj.index),
    object: _resolveTypeIdentifier17(obj.object),
    optionalChain: _rt995je(obj.optional_chain),
  });
}

export function switchBodyFrom(input: RuntimeNodeOf<SwitchBody>): ReturnType<typeof switch_body_>;
export function switchBodyFrom(...children: SwitchBodyFromInput[]): ReturnType<typeof switch_body_>;
export function switchBodyFrom(...args: (RuntimeNodeOf<SwitchBody> | SwitchBodyFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'switch_body') {
    return switch_body_(...(args[0].children ?? []) as (SwitchCase | SwitchDefault)[]);
  }
  return switch_body_(...args.map(v => _r17pulrl(v)) as (SwitchCase | SwitchDefault)[]);
}

export function switchCaseFrom(input: RuntimeNodeOf<SwitchCase> | SwitchCaseFromInput) {
  if (isNodeData<'switch_case'>(input)) {
    return switch_case_({
      body: input.fields?.body,
      value: input.fields?.value,
    });
  }
  const obj = input as SwitchCaseFromInput;
  return switch_case_({
    body: obj.body !== undefined ? ((Array.isArray(obj.body) ? obj.body : [obj.body])).map(_resolveTypeIdentifier10) : undefined,
    value: _resolveExpressions3(obj.value),
  });
}

export function switchDefaultFrom(input: RuntimeNodeOf<SwitchDefault> | SwitchDefaultFromInput) {
  if (isNodeData<'switch_default'>(input)) {
    return switch_default_({
      body: input.fields?.body,
    });
  }
  const obj = input as SwitchDefaultFromInput;
  return switch_default_({
    body: obj.body !== undefined ? ((Array.isArray(obj.body) ? obj.body : [obj.body])).map(_resolveTypeIdentifier10) : undefined,
  });
}

export function switchStatementFrom(input: RuntimeNodeOf<SwitchStatement> | SwitchStatementFromInput) {
  if (isNodeData<'switch_statement'>(input)) {
    return switch_statement_({
      body: input.fields?.body,
      value: input.fields?.value,
    });
  }
  const obj = input as SwitchStatementFromInput;
  return switch_statement_({
    body: _r1u0kk81(obj.body),
    value: _resolveAugmentedAssignmentLhs3(obj.value),
  });
}

export function templateLiteralTypeFrom(input: RuntimeNodeOf<TemplateLiteralType>): ReturnType<typeof template_literal_type_>;
export function templateLiteralTypeFrom(...children: TemplateLiteralTypeFromInput[]): ReturnType<typeof template_literal_type_>;
export function templateLiteralTypeFrom(...args: (RuntimeNodeOf<TemplateLiteralType> | TemplateLiteralTypeFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'template_literal_type') {
    return template_literal_type_(...(args[0].children ?? []) as (StringFragment | TemplateType)[]);
  }
  return template_literal_type_(...args.map(v => _r1clt4kp(v)) as (StringFragment | TemplateType)[]);
}

export function templateStringFrom(input: RuntimeNodeOf<TemplateString>): ReturnType<typeof template_string_>;
export function templateStringFrom(...children: TemplateStringFromInput[]): ReturnType<typeof template_string_>;
export function templateStringFrom(...args: (RuntimeNodeOf<TemplateString> | TemplateStringFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'template_string') {
    return template_string_(...(args[0].children ?? []) as (EscapeSequence | StringFragment | TemplateSubstitution)[]);
  }
  return template_string_(...args.map(v => _r1wwgc7t(v)) as (EscapeSequence | StringFragment | TemplateSubstitution)[]);
}

export function templateSubstitutionFrom(input: RuntimeNodeOf<TemplateSubstitution>): ReturnType<typeof template_substitution_>;
export function templateSubstitutionFrom(child: TemplateSubstitutionFromInput): ReturnType<typeof template_substitution_>;
export function templateSubstitutionFrom(arg?: RuntimeNodeOf<TemplateSubstitution> | TemplateSubstitutionFromInput) {
  if (isNodeData(arg) && arg.type === 'template_substitution') {
    return template_substitution_(arg.children?.[0] as Expression | SequenceExpression);
  }
  return template_substitution_(_resolveExpressions2(arg) as Expression | SequenceExpression);
}

export function templateTypeFrom(input: RuntimeNodeOf<TemplateType>): ReturnType<typeof template_type_>;
export function templateTypeFrom(child: TemplateTypeFromInput): ReturnType<typeof template_type_>;
export function templateTypeFrom(arg?: RuntimeNodeOf<TemplateType> | TemplateTypeFromInput) {
  if (isNodeData(arg) && arg.type === 'template_type') {
    return template_type_(arg.children?.[0] as InferType | PrimaryType);
  }
  return template_type_(_resolveType2(arg) as InferType | PrimaryType);
}

export function ternaryExpressionFrom(input: RuntimeNodeOf<TernaryExpression> | TernaryExpressionFromInput) {
  if (isNodeData<'ternary_expression'>(input)) {
    return ternary_expression_({
      alternative: input.fields?.alternative,
      condition: input.fields?.condition,
      consequence: input.fields?.consequence,
    });
  }
  const obj = input as TernaryExpressionFromInput;
  return ternary_expression_({
    alternative: _resolveTypeIdentifier6(obj.alternative),
    condition: _resolveTypeIdentifier6(obj.condition),
    consequence: _resolveTypeIdentifier6(obj.consequence),
  });
}

export function throwStatementFrom(input: RuntimeNodeOf<ThrowStatement>): ReturnType<typeof throw_statement_>;
export function throwStatementFrom(child: ThrowStatementFromInput): ReturnType<typeof throw_statement_>;
export function throwStatementFrom(arg?: RuntimeNodeOf<ThrowStatement> | ThrowStatementFromInput) {
  if (isNodeData(arg) && arg.type === 'throw_statement') {
    return throw_statement_(arg.children?.[0] as Expression | SequenceExpression);
  }
  return throw_statement_(_resolveExpressions2(arg) as Expression | SequenceExpression);
}

export function tryStatementFrom(input: RuntimeNodeOf<TryStatement> | TryStatementFromInput) {
  if (isNodeData<'try_statement'>(input)) {
    return try_statement_({
      body: input.fields?.body,
      finalizer: input.fields?.finalizer,
      handler: input.fields?.handler,
    });
  }
  const obj = input as TryStatementFromInput;
  return try_statement_({
    body: _resolveStatement3(obj.body),
    finalizer: _rqhziy5(obj.finalizer),
    handler: _r120ygox(obj.handler),
  });
}

export function tupleTypeFrom(input: RuntimeNodeOf<TupleType>): ReturnType<typeof tuple_type_>;
export function tupleTypeFrom(...children: TupleTypeFromInput[]): ReturnType<typeof tuple_type_>;
export function tupleTypeFrom(...args: (RuntimeNodeOf<TupleType> | TupleTypeFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'tuple_type') {
    return tuple_type_(...(args[0].children ?? []) as (OptionalParameter | OptionalType | RequiredParameter | RestType | Type)[]);
  }
  return tuple_type_(...args.map(v => _r108s583(v)) as (OptionalParameter | OptionalType | RequiredParameter | RestType | Type)[]);
}

export function typeAliasDeclarationFrom(input: RuntimeNodeOf<TypeAliasDeclaration> | TypeAliasDeclarationFromInput) {
  if (isNodeData<'type_alias_declaration'>(input)) {
    return type_alias_declaration_({
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      value: input.fields?.value,
    });
  }
  const obj = input as TypeAliasDeclarationFromInput;
  return type_alias_declaration_({
    name: _resolveTypeIdentifier2(obj.name),
    typeParameters: _r19n9507(obj.type_parameters),
    value: _resolveTypeIdentifier4(obj.value),
  });
}

export function typeAnnotationFrom(input: RuntimeNodeOf<TypeAnnotation>): ReturnType<typeof type_annotation_>;
export function typeAnnotationFrom(child: TypeAnnotationFromInput): ReturnType<typeof type_annotation_>;
export function typeAnnotationFrom(arg?: RuntimeNodeOf<TypeAnnotation> | TypeAnnotationFromInput) {
  if (isNodeData(arg) && arg.type === 'type_annotation') {
    return type_annotation_(arg.children?.[0] as Type);
  }
  return type_annotation_(_resolveTypeIdentifier4(arg) as Type);
}

export function typeArgumentsFrom(input: RuntimeNodeOf<TypeArguments>): ReturnType<typeof type_arguments_>;
export function typeArgumentsFrom(...children: TypeArgumentsFromInput[]): ReturnType<typeof type_arguments_>;
export function typeArgumentsFrom(...args: (RuntimeNodeOf<TypeArguments> | TypeArgumentsFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'type_arguments') {
    return type_arguments_(...(args[0].children ?? []) as (Type)[]);
  }
  return type_arguments_(...args.map(v => _resolveTypeIdentifier4(v)) as (Type)[]);
}

export function typeAssertionFrom(input: RuntimeNodeOf<TypeAssertion> | TypeAssertionFromInput) {
  if (isNodeData<'type_assertion'>(input)) {
    return type_assertion_({
      typeArguments: input.fields?.type_arguments,
      expression: input.fields?.expression,
    });
  }
  const obj = input as TypeAssertionFromInput;
  return type_assertion_({
    typeArguments: _r1uqebll(obj.type_arguments),
    expression: _resolveTypeIdentifier6(obj.expression),
  });
}

export function typeParameterFrom(input: RuntimeNodeOf<TypeParameter> | TypeParameterFromInput) {
  if (isNodeData<'type_parameter'>(input)) {
    return type_parameter_({
      constraint: input.fields?.constraint,
      name: input.fields?.name,
      value: input.fields?.value,
    });
  }
  const obj = input as TypeParameterFromInput;
  return type_parameter_({
    constraint: _r1qgzxrb(obj.constraint),
    name: _resolveTypeIdentifier2(obj.name),
    value: _r1gd522w(obj.value),
  });
}

export function typeParametersFrom(input: RuntimeNodeOf<TypeParameters>): ReturnType<typeof type_parameters_>;
export function typeParametersFrom(...children: TypeParametersFromInput[]): ReturnType<typeof type_parameters_>;
export function typeParametersFrom(...args: (RuntimeNodeOf<TypeParameters> | TypeParametersFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'type_parameters') {
    return type_parameters_(...(args[0].children ?? []) as (TypeParameter)[]);
  }
  return type_parameters_(...args.map(v => _reaq3s4(v)) as (TypeParameter)[]);
}

export function typePredicateFrom(input: RuntimeNodeOf<TypePredicate> | TypePredicateFromInput) {
  if (isNodeData<'type_predicate'>(input)) {
    return type_predicate_({
      name: input.fields?.name,
      type: input.fields?.type,
    });
  }
  const obj = input as TypePredicateFromInput;
  return type_predicate_({
    name: _resolvePrimaryExpression5(obj.name),
    type: _resolveTypeIdentifier4(obj.type),
  });
}

export function typePredicateAnnotationFrom(input: RuntimeNodeOf<TypePredicateAnnotation> | TypePredicateAnnotationFromInput) {
  if (isNodeData<'type_predicate_annotation'>(input)) {
    return type_predicate_annotation_({
      typePredicate: input.fields?.type_predicate,
    });
  }
  const obj = input as TypePredicateAnnotationFromInput;
  return type_predicate_annotation_({
    typePredicate: _rj0vzo(obj.type_predicate),
  });
}

export function typeQueryFrom(input: RuntimeNodeOf<TypeQuery>): ReturnType<typeof type_query_>;
export function typeQueryFrom(child: TypeQueryFromInput): ReturnType<typeof type_query_>;
export function typeQueryFrom(arg?: RuntimeNodeOf<TypeQuery> | TypeQueryFromInput) {
  if (isNodeData(arg) && arg.type === 'type_query') {
    return type_query_(arg.children?.[0] as CallExpression | Identifier | InstantiationExpression | MemberExpression | SubscriptExpression | This);
  }
  return type_query_(_resolveExpression3(arg) as CallExpression | Identifier | InstantiationExpression | MemberExpression | SubscriptExpression | This);
}

export function unaryExpressionFrom(input: RuntimeNodeOf<UnaryExpression> | UnaryExpressionFromInput) {
  if (isNodeData<'unary_expression'>(input)) {
    return unary_expression_({
      argument: input.fields?.argument,
      operator: input.fields?.operator,
    });
  }
  const obj = input as UnaryExpressionFromInput;
  return unary_expression_({
    argument: _resolvePropertyName6(obj.argument),
    operator: _resolveTypeIdentifier18(obj.operator),
  });
}

export function unionTypeFrom(input: RuntimeNodeOf<UnionType> | UnionTypeFromInput) {
  if (isNodeData<'union_type'>(input)) {
    return union_type_({
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as UnionTypeFromInput;
  return union_type_({
    left: _resolveTypeIdentifier4(obj.left),
    right: _resolveTypeIdentifier4(obj.right),
  });
}

export function updateExpressionFrom(input: RuntimeNodeOf<UpdateExpression> | UpdateExpressionFromInput) {
  if (isNodeData<'update_expression'>(input)) {
    return update_expression_({
      argument: input.fields?.argument,
      operator: input.fields?.operator,
    });
  }
  const obj = input as UpdateExpressionFromInput;
  return update_expression_({
    argument: _resolveTypeIdentifier6(obj.argument),
    operator: _resolveTypeIdentifier19(obj.operator),
  });
}

export function variableDeclarationFrom(input: RuntimeNodeOf<VariableDeclaration> | VariableDeclarationFromInput) {
  if (isNodeData<'variable_declaration'>(input)) {
    return variable_declaration_({
      declarators: input.fields?.declarators,
      semicolon: input.fields?.semicolon,
    });
  }
  const obj = input as VariableDeclarationFromInput;
  return variable_declaration_({
    declarators: obj.declarators !== undefined ? ((Array.isArray(obj.declarators) ? obj.declarators : [obj.declarators])).map(_r1xs8n9k) : [],
    semicolon: _resolveTypeIdentifier3(obj.semicolon),
  });
}

export function variableDeclaratorFrom(input: RuntimeNodeOf<VariableDeclarator> | VariableDeclaratorFromInput) {
  if (isNodeData<'variable_declarator'>(input)) {
    return variable_declarator_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as VariableDeclaratorConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return variable_declarator_(mapped as VariableDeclaratorConfig);
}

export function variableDeclaratorV0From(input: RuntimeNodeOf<VariableDeclaratorV0> | VariableDeclaratorV0FromInput) {
  if (isNodeData<'variable_declarator'>(input)) {
    return variable_declarator__v0_({
      name: input.fields?.name,
      type: input.fields?.type,
    });
  }
  const obj = input as VariableDeclaratorV0FromInput;
  return variable_declarator__v0_({
    name: _resolveLhsExpression2(obj.name),
    type: _r1s2j1z2(obj.type),
  });
}

export function variableDeclaratorBangFrom(input: RuntimeNodeOf<VariableDeclaratorBang> | VariableDeclaratorBangFromInput) {
  if (isNodeData<'variable_declarator'>(input)) {
    return variable_declarator__bang_({
      name: input.fields?.name,
      type: input.fields?.type,
    });
  }
  const obj = input as VariableDeclaratorBangFromInput;
  return variable_declarator__bang_({
    name: _resolveLhsExpression2(obj.name),
    type: _r1s2j1z2(obj.type),
  });
}

export function whileStatementFrom(input: RuntimeNodeOf<WhileStatement> | WhileStatementFromInput) {
  if (isNodeData<'while_statement'>(input)) {
    return while_statement_({
      body: input.fields?.body,
      condition: input.fields?.condition,
    });
  }
  const obj = input as WhileStatementFromInput;
  return while_statement_({
    body: _resolveTypeIdentifier10(obj.body),
    condition: _resolveAugmentedAssignmentLhs3(obj.condition),
  });
}

export function withStatementFrom(input: RuntimeNodeOf<WithStatement> | WithStatementFromInput) {
  if (isNodeData<'with_statement'>(input)) {
    return with_statement_({
      body: input.fields?.body,
      object: input.fields?.object,
    });
  }
  const obj = input as WithStatementFromInput;
  return with_statement_({
    body: _resolveTypeIdentifier10(obj.body),
    object: _resolveAugmentedAssignmentLhs3(obj.object),
  });
}

export function yieldExpressionFrom(input: RuntimeNodeOf<YieldExpression> | YieldExpressionFromInput) {
  if (isNodeData<'yield_expression'>(input)) {
    return yield_expression_({
      ...Object.fromEntries(Object.entries(input.fields ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as YieldExpressionConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return yield_expression_(mapped as YieldExpressionConfig);
}

export function yieldExpressionStarFrom(input: RuntimeNodeOf<YieldExpressionStar> | YieldExpressionStarFromInput) {
  if (isNodeData<'yield_expression'>(input)) {
    return yield_expression__star_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as YieldExpressionStarFromInput;
  return yield_expression__star_({
    expression: _resolveTypeIdentifier6(obj.expression),
  });
}

export function yieldExpressionV1From(input: RuntimeNodeOf<YieldExpressionV1> | YieldExpressionV1FromInput) {
  if (isNodeData<'yield_expression'>(input)) {
    return yield_expression__v1_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as YieldExpressionV1FromInput;
  return yield_expression__v1_({
    expression: _resolveTypeIdentifier6(obj.expression),
  });
}

// Auto-generated by @sittir/codegen — do not edit

import type { NodeData } from './types.js';
import type { AbstractClassDeclaration, AbstractClassDeclarationConfig, AbstractClassDeclarationTree, AbstractMethodSignature, AbstractMethodSignatureConfig, AbstractMethodSignatureTree, AccessibilityModifier, AccessibilityModifierTree, AddingTypeAnnotation, AddingTypeAnnotationConfig, AddingTypeAnnotationTree, AmbientDeclaration, AmbientDeclarationConfig, AmbientDeclarationTree, Arguments, ArgumentsConfig, ArgumentsTree, Array, ArrayConfig, ArrayPattern, ArrayPatternConfig, ArrayPatternTree, ArrayTree, ArrayType, ArrayTypeConfig, ArrayTypeTree, ArrowFunction, ArrowFunctionConfig, ArrowFunctionTree, AsExpression, AsExpressionConfig, AsExpressionTree, Asserts, AssertsAnnotation, AssertsAnnotationConfig, AssertsAnnotationTree, AssertsConfig, AssertsTree, AssignmentExpression, AssignmentExpressionConfig, AssignmentExpressionTree, AssignmentPattern, AssignmentPatternConfig, AssignmentPatternTree, AugmentedAssignmentExpression, AugmentedAssignmentExpressionConfig, AugmentedAssignmentExpressionTree, AwaitExpression, AwaitExpressionConfig, AwaitExpressionTree, BinaryExpression, BinaryExpressionConfig, BinaryExpressionTree, BreakStatement, BreakStatementConfig, BreakStatementTree, CallExpression, CallExpressionConfig, CallExpressionTree, CallSignature, CallSignatureConfig, CallSignatureTree, CatchClause, CatchClauseConfig, CatchClauseTree, Class, ClassBody, ClassBodyConfig, ClassBodyTree, ClassConfig, ClassDeclaration, ClassDeclarationConfig, ClassDeclarationTree, ClassHeritage, ClassHeritageConfig, ClassHeritageTree, ClassStaticBlock, ClassStaticBlockConfig, ClassStaticBlockTree, ClassTree, Comment, CommentTree, ComputedPropertyName, ComputedPropertyNameConfig, ComputedPropertyNameTree, ConditionalType, ConditionalTypeConfig, ConditionalTypeTree, Constraint, ConstraintConfig, ConstraintTree, ConstructSignature, ConstructSignatureConfig, ConstructSignatureTree, ConstructorType, ConstructorTypeConfig, ConstructorTypeTree, ContinueStatement, ContinueStatementConfig, ContinueStatementTree, DebuggerStatement, DebuggerStatementTree, Declaration, Decorator, DecoratorConfig, DecoratorTree, DefaultType, DefaultTypeConfig, DefaultTypeTree, DoStatement, DoStatementConfig, DoStatementTree, ElseClause, ElseClauseConfig, ElseClauseTree, EmptyStatement, EmptyStatementTree, EnumAssignment, EnumAssignmentConfig, EnumAssignmentTree, EnumBody, EnumBodyConfig, EnumBodyTree, EnumDeclaration, EnumDeclarationConfig, EnumDeclarationTree, EscapeSequence, EscapeSequenceTree, ExistentialType, ExistentialTypeTree, ExportClause, ExportClauseConfig, ExportClauseTree, ExportSpecifier, ExportSpecifierConfig, ExportSpecifierTree, ExportStatement, ExportStatementConfig, ExportStatementTree, Expression, ExpressionStatement, ExpressionStatementConfig, ExpressionStatementTree, ExtendsClause, ExtendsClauseConfig, ExtendsClauseTree, ExtendsTypeClause, ExtendsTypeClauseConfig, ExtendsTypeClauseTree, False, FalseTree, FinallyClause, FinallyClauseConfig, FinallyClauseTree, FlowMaybeType, FlowMaybeTypeConfig, FlowMaybeTypeTree, ForInStatement, ForInStatementConfig, ForInStatementTree, ForStatement, ForStatementConfig, ForStatementTree, FormalParameters, FormalParametersConfig, FormalParametersTree, FunctionDeclaration, FunctionDeclarationConfig, FunctionDeclarationTree, FunctionExpression, FunctionExpressionConfig, FunctionExpressionTree, FunctionSignature, FunctionSignatureConfig, FunctionSignatureTree, FunctionType, FunctionTypeConfig, FunctionTypeTree, GeneratorFunction, GeneratorFunctionConfig, GeneratorFunctionDeclaration, GeneratorFunctionDeclarationConfig, GeneratorFunctionDeclarationTree, GeneratorFunctionTree, GenericType, GenericTypeConfig, GenericTypeTree, HashBangLine, HashBangLineTree, HtmlComment, HtmlCommentTree, Identifier, IdentifierTree, IfStatement, IfStatementConfig, IfStatementTree, ImplementsClause, ImplementsClauseConfig, ImplementsClauseTree, Import, ImportAlias, ImportAliasConfig, ImportAliasTree, ImportAttribute, ImportAttributeConfig, ImportAttributeTree, ImportClause, ImportClauseConfig, ImportClauseTree, ImportRequireClause, ImportRequireClauseConfig, ImportRequireClauseTree, ImportSpecifier, ImportSpecifierConfig, ImportSpecifierTree, ImportStatement, ImportStatementConfig, ImportStatementTree, ImportTree, IndexSignature, IndexSignatureConfig, IndexSignatureTree, IndexTypeQuery, IndexTypeQueryConfig, IndexTypeQueryTree, InferType, InferTypeConfig, InferTypeTree, InstantiationExpression, InstantiationExpressionConfig, InstantiationExpressionTree, InterfaceBody, InterfaceBodyConfig, InterfaceBodyTree, InterfaceDeclaration, InterfaceDeclarationConfig, InterfaceDeclarationTree, InternalModule, InternalModuleConfig, InternalModuleTree, IntersectionType, IntersectionTypeConfig, IntersectionTypeTree, LabeledStatement, LabeledStatementConfig, LabeledStatementTree, LexicalDeclaration, LexicalDeclarationConfig, LexicalDeclarationTree, LiteralType, LiteralTypeConfig, LiteralTypeTree, LookupType, LookupTypeConfig, LookupTypeTree, MappedTypeClause, MappedTypeClauseConfig, MappedTypeClauseTree, MemberExpression, MemberExpressionConfig, MemberExpressionTree, MetaProperty, MetaPropertyTree, MethodDefinition, MethodDefinitionConfig, MethodDefinitionTree, MethodSignature, MethodSignatureConfig, MethodSignatureTree, Module, ModuleConfig, ModuleTree, NamedImports, NamedImportsConfig, NamedImportsTree, NamespaceExport, NamespaceExportConfig, NamespaceExportTree, NamespaceImport, NamespaceImportConfig, NamespaceImportTree, NestedIdentifier, NestedIdentifierConfig, NestedIdentifierTree, NestedTypeIdentifier, NestedTypeIdentifierConfig, NestedTypeIdentifierTree, NewExpression, NewExpressionConfig, NewExpressionTree, NonNullExpression, NonNullExpressionConfig, NonNullExpressionTree, Null, NullTree, Number, NumberTree, Object, ObjectAssignmentPattern, ObjectAssignmentPatternConfig, ObjectAssignmentPatternTree, ObjectConfig, ObjectPattern, ObjectPatternConfig, ObjectPatternTree, ObjectTree, ObjectType, ObjectTypeConfig, ObjectTypeTree, OmittingTypeAnnotation, OmittingTypeAnnotationConfig, OmittingTypeAnnotationTree, OptingTypeAnnotation, OptingTypeAnnotationConfig, OptingTypeAnnotationTree, OptionalChain, OptionalChainTree, OptionalParameter, OptionalParameterConfig, OptionalParameterTree, OptionalType, OptionalTypeConfig, OptionalTypeTree, OverrideModifier, OverrideModifierTree, Pair, PairConfig, PairPattern, PairPatternConfig, PairPatternTree, PairTree, ParenthesizedExpression, ParenthesizedExpressionConfig, ParenthesizedExpressionTree, ParenthesizedType, ParenthesizedTypeConfig, ParenthesizedTypeTree, Pattern, PredefinedType, PredefinedTypeTree, PrimaryExpression, PrimaryType, PrivatePropertyIdentifier, PrivatePropertyIdentifierTree, Program, ProgramConfig, ProgramTree, PropertyIdentifier, PropertyIdentifierTree, PropertySignature, PropertySignatureConfig, PropertySignatureTree, PublicFieldDefinition, PublicFieldDefinitionConfig, PublicFieldDefinitionTree, ReadonlyType, ReadonlyTypeConfig, ReadonlyTypeTree, Regex, RegexConfig, RegexFlags, RegexFlagsTree, RegexPattern, RegexPatternTree, RegexTree, RequiredParameter, RequiredParameterConfig, RequiredParameterTree, RestPattern, RestPatternConfig, RestPatternTree, RestType, RestTypeConfig, RestTypeTree, ReturnStatement, ReturnStatementConfig, ReturnStatementTree, SatisfiesExpression, SatisfiesExpressionConfig, SatisfiesExpressionTree, SequenceExpression, SequenceExpressionConfig, SequenceExpressionTree, ShorthandPropertyIdentifier, ShorthandPropertyIdentifierPattern, ShorthandPropertyIdentifierPatternTree, ShorthandPropertyIdentifierTree, SpreadElement, SpreadElementConfig, SpreadElementTree, Statement, StatementBlock, StatementBlockConfig, StatementBlockTree, StatementIdentifier, StatementIdentifierTree, String, StringConfig, StringFragment, StringFragmentTree, StringTree, SubscriptExpression, SubscriptExpressionConfig, SubscriptExpressionTree, Super, SuperTree, SwitchBody, SwitchBodyConfig, SwitchBodyTree, SwitchCase, SwitchCaseConfig, SwitchCaseTree, SwitchDefault, SwitchDefaultConfig, SwitchDefaultTree, SwitchStatement, SwitchStatementConfig, SwitchStatementTree, TemplateLiteralType, TemplateLiteralTypeConfig, TemplateLiteralTypeTree, TemplateString, TemplateStringConfig, TemplateStringTree, TemplateSubstitution, TemplateSubstitutionConfig, TemplateSubstitutionTree, TemplateType, TemplateTypeConfig, TemplateTypeTree, TernaryExpression, TernaryExpressionConfig, TernaryExpressionTree, This, ThisTree, ThisType, ThisTypeTree, ThrowStatement, ThrowStatementConfig, ThrowStatementTree, True, TrueTree, TryStatement, TryStatementConfig, TryStatementTree, TupleType, TupleTypeConfig, TupleTypeTree, Type, TypeAliasDeclaration, TypeAliasDeclarationConfig, TypeAliasDeclarationTree, TypeAnnotation, TypeAnnotationConfig, TypeAnnotationTree, TypeArguments, TypeArgumentsConfig, TypeArgumentsTree, TypeAssertion, TypeAssertionConfig, TypeAssertionTree, TypeIdentifier, TypeIdentifierTree, TypeParameter, TypeParameterConfig, TypeParameterTree, TypeParameters, TypeParametersConfig, TypeParametersTree, TypePredicate, TypePredicateAnnotation, TypePredicateAnnotationConfig, TypePredicateAnnotationTree, TypePredicateConfig, TypePredicateTree, TypeQuery, TypeQueryConfig, TypeQueryTree, UnaryExpression, UnaryExpressionConfig, UnaryExpressionTree, Undefined, UndefinedTree, UnionType, UnionTypeConfig, UnionTypeTree, UpdateExpression, UpdateExpressionConfig, UpdateExpressionTree, VariableDeclaration, VariableDeclarationConfig, VariableDeclarationTree, VariableDeclarator, VariableDeclaratorConfig, VariableDeclaratorTree, WhileStatement, WhileStatementConfig, WhileStatementTree, WithStatement, WithStatementConfig, WithStatementTree, YieldExpression, YieldExpressionConfig, YieldExpressionTree } from './types.js';
import type { Edit, AnyNodeData } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { rules } from './rules.js';
import { joinBy } from './joinby.js';

const { render, toEdit } = createRenderer(rules, joinBy);

const RESERVED_KEYWORDS = new Set([
  'abstract',
  'accessor',
  'any',
  'as',
  'assert',
  'async',
  'await',
  'boolean',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'delete',
  'do',
  'else',
  'enum',
  'export',
  'extends',
  'finally',
  'for',
  'from',
  'function',
  'get',
  'global',
  'if',
  'implements',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'let',
  'meta',
  'namespace',
  'never',
  'new',
  'of',
  'override',
  'private',
  'protected',
  'public',
  'readonly',
  'require',
  'return',
  'satisfies',
  'set',
  'static',
  'switch',
  'symbol',
  'target',
  'throw',
  'try',
  'typeof',
  'unknown',
  'using',
  'var',
  'void',
  'while',
  'with',
  'yield',
]);

export function abstract_class_declaration_(
  config: AbstractClassDeclarationConfig,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'abstract_class_declaration' as const,
    fields,
    children,
    body(v?: ClassBody): any { return v !== undefined ? abstract_class_declaration_({ ...config, body: v }) : fields.body; },
    decorator(...v: (Decorator)[]): any { return v.length ? abstract_class_declaration_({ ...config, decorator: v }) : fields.decorator; },
    name(v?: TypeIdentifier): any { return v !== undefined ? abstract_class_declaration_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? abstract_class_declaration_({ ...config, typeParameters: v }) : fields.type_parameters; },
    child(v?: ClassHeritage): any { return v !== undefined ? abstract_class_declaration_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function abstract_method_signature_(
  config: AbstractMethodSignatureConfig,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  const children = [config.accessibilityModifier, config.overrideModifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'abstract_method_signature' as const,
    fields,
    children,
    name(v?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String): any { return v !== undefined ? abstract_method_signature_({ ...config, name: v }) : fields.name; },
    parameters(v?: FormalParameters): any { return v !== undefined ? abstract_method_signature_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? abstract_method_signature_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? abstract_method_signature_({ ...config, typeParameters: v }) : fields.type_parameters; },
    accessibilityModifier(v?: AccessibilityModifier): any { return v !== undefined ? abstract_method_signature_({ ...config, accessibilityModifier: v }) : config?.accessibilityModifier; },
    overrideModifier(v?: OverrideModifier): any { return v !== undefined ? abstract_method_signature_({ ...config, overrideModifier: v }) : config?.overrideModifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function adding_type_annotation_(
  config: AddingTypeAnnotationConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'adding_type_annotation' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? adding_type_annotation_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ambient_declaration_(
  config: AmbientDeclarationConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'ambient_declaration' as const,
    fields,
    children,
    children0(v?: Declaration | PropertyIdentifier | StatementBlock): any { return v !== undefined ? ambient_declaration_({ ...config, children0: v }) : config?.children0; },
    children1(v?: Type): any { return v !== undefined ? ambient_declaration_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function arguments_(
  config?: ArgumentsConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'arguments' as const,
    fields,
    children,
    children0(v?: Expression | SpreadElement): any { return v !== undefined ? arguments_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (Expression | SpreadElement)[]): any { return v.length ? arguments_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_(
  config?: ArrayConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'array' as const,
    fields,
    children,
    children0(v?: Expression | SpreadElement): any { return v !== undefined ? array_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (Expression | SpreadElement)[]): any { return v.length ? array_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_pattern_(
  config?: ArrayPatternConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'array_pattern' as const,
    fields,
    children,
    children0(v?: AssignmentPattern | Pattern): any { return v !== undefined ? array_pattern_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (AssignmentPattern | Pattern)[]): any { return v.length ? array_pattern_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_type_(
  config: ArrayTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'array_type' as const,
    fields,
    children,
    child(v?: PrimaryType): any { return v !== undefined ? array_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function arrow_function_(
  config: ArrowFunctionConfig,
) {
  const fields = {
    body: config.body,
    parameter: config.parameter,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'arrow_function' as const,
    fields,
    body(v?: Expression | StatementBlock): any { return v !== undefined ? arrow_function_({ ...config, body: v }) : fields.body; },
    parameter(v?: Identifier): any { return v !== undefined ? arrow_function_({ ...config, parameter: v }) : fields.parameter; },
    parameters(v?: FormalParameters): any { return v !== undefined ? arrow_function_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? arrow_function_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? arrow_function_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function as_expression_(
  config: AsExpressionConfig,
) {
  const fields = {
  };
  const children = [config.expression, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'as_expression' as const,
    fields,
    children,
    expression(v?: Expression): any { return v !== undefined ? as_expression_({ ...config, expression: v }) : config?.expression; },
    children1(v?: Type): any { return v !== undefined ? as_expression_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function asserts_(
  config: AssertsConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'asserts' as const,
    fields,
    children,
    child(v?: Identifier | This | TypePredicate): any { return v !== undefined ? asserts_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function asserts_annotation_(
  config: AssertsAnnotationConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'asserts_annotation' as const,
    fields,
    children,
    child(v?: Asserts): any { return v !== undefined ? asserts_annotation_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_expression_(
  config: AssignmentExpressionConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment_expression' as const,
    fields,
    left(v?: ArrayPattern | Identifier | MemberExpression | NonNullExpression | ObjectPattern | ParenthesizedExpression | SubscriptExpression | Undefined): any { return v !== undefined ? assignment_expression_({ ...config, left: v }) : fields.left; },
    right(v?: Expression): any { return v !== undefined ? assignment_expression_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_pattern_(
  config: AssignmentPatternConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment_pattern' as const,
    fields,
    left(v?: Pattern): any { return v !== undefined ? assignment_pattern_({ ...config, left: v }) : fields.left; },
    right(v?: Expression): any { return v !== undefined ? assignment_pattern_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function augmented_assignment_expression_(
  config: AugmentedAssignmentExpressionConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment_expression' as const,
    fields,
    left(v?: Identifier | MemberExpression | NonNullExpression | ParenthesizedExpression | SubscriptExpression): any { return v !== undefined ? augmented_assignment_expression_({ ...config, left: v }) : fields.left; },
    operator(v?: '+=' | '-=' | '*=' | '/=' | '%=' | '^=' | '&=' | '|=' | '>>=' | '>>>=' | '<<=' | '**=' | '&&=' | '||=' | '??='): any { return v !== undefined ? augmented_assignment_expression_({ ...config, operator: v }) : fields.operator; },
    right(v?: Expression): any { return v !== undefined ? augmented_assignment_expression_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function await_expression_(
  config: AwaitExpressionConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'await_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? await_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function binary_expression_(
  config: BinaryExpressionConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    fields,
    left(v?: Expression | PrivatePropertyIdentifier): any { return v !== undefined ? binary_expression_({ ...config, left: v }) : fields.left; },
    operator(v?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||'): any { return v !== undefined ? binary_expression_({ ...config, operator: v }) : fields.operator; },
    right(v?: Expression): any { return v !== undefined ? binary_expression_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function break_statement_(
  config?: BreakStatementConfig,
) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'break_statement' as const,
    fields,
    label(v?: StatementIdentifier): any { return v !== undefined ? break_statement_({ ...config, label: v }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call_expression_(
  config: CallExpressionConfig,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'call_expression' as const,
    fields,
    arguments(v?: Arguments | TemplateString): any { return v !== undefined ? call_expression_({ ...config, arguments: v }) : fields.arguments; },
    function(v?: Expression | Import | NewExpression | PrimaryExpression): any { return v !== undefined ? call_expression_({ ...config, function: v }) : fields.function; },
    typeArguments(v?: TypeArguments): any { return v !== undefined ? call_expression_({ ...config, typeArguments: v }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call_signature_(
  config: CallSignatureConfig,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'call_signature' as const,
    fields,
    parameters(v?: FormalParameters): any { return v !== undefined ? call_signature_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? call_signature_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? call_signature_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function catch_clause_(
  config: CatchClauseConfig,
) {
  const fields = {
    body: config.body,
    parameter: config.parameter,
    type: config.type,
  };
  return {
    type: 'catch_clause' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? catch_clause_({ ...config, body: v }) : fields.body; },
    parameter(v?: ArrayPattern | Identifier | ObjectPattern): any { return v !== undefined ? catch_clause_({ ...config, parameter: v }) : fields.parameter; },
    typeField(v?: TypeAnnotation): any { return v !== undefined ? catch_clause_({ ...config, type: v }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_(
  config: ClassConfig,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'class' as const,
    fields,
    children,
    body(v?: ClassBody): any { return v !== undefined ? class_({ ...config, body: v }) : fields.body; },
    decorator(...v: (Decorator)[]): any { return v.length ? class_({ ...config, decorator: v }) : fields.decorator; },
    name(v?: TypeIdentifier): any { return v !== undefined ? class_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? class_({ ...config, typeParameters: v }) : fields.type_parameters; },
    child(v?: ClassHeritage): any { return v !== undefined ? class_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_body_(
  config?: ClassBodyConfig,
) {
  const fields = {
    decorator: config?.decorator,
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'class_body' as const,
    fields,
    children,
    decorator(...v: (Decorator)[]): any { return v.length ? class_body_({ ...config, decorator: v }) : fields.decorator; },
    getChildren(): any { return children; },
    setChildren(...v: (AbstractMethodSignature | ClassStaticBlock | IndexSignature | MethodDefinition | MethodSignature | PublicFieldDefinition)[]): any { return class_body_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_declaration_(
  config: ClassDeclarationConfig,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'class_declaration' as const,
    fields,
    children,
    body(v?: ClassBody): any { return v !== undefined ? class_declaration_({ ...config, body: v }) : fields.body; },
    decorator(...v: (Decorator)[]): any { return v.length ? class_declaration_({ ...config, decorator: v }) : fields.decorator; },
    name(v?: TypeIdentifier): any { return v !== undefined ? class_declaration_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? class_declaration_({ ...config, typeParameters: v }) : fields.type_parameters; },
    child(v?: ClassHeritage): any { return v !== undefined ? class_declaration_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_heritage_(
  config: ClassHeritageConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.implementsClause].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'class_heritage' as const,
    fields,
    children,
    children0(v?: ExtendsClause | ImplementsClause): any { return v !== undefined ? class_heritage_({ ...config, children0: v }) : config?.children0; },
    implementsClause(v?: ImplementsClause): any { return v !== undefined ? class_heritage_({ ...config, implementsClause: v }) : config?.implementsClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_static_block_(
  config: ClassStaticBlockConfig,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'class_static_block' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? class_static_block_({ ...config, body: v }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function computed_property_name_(
  config: ComputedPropertyNameConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'computed_property_name' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? computed_property_name_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function conditional_type_(
  config: ConditionalTypeConfig,
) {
  const fields = {
    alternative: config.alternative,
    consequence: config.consequence,
    left: config.left,
    right: config.right,
  };
  return {
    type: 'conditional_type' as const,
    fields,
    alternative(v?: Type): any { return v !== undefined ? conditional_type_({ ...config, alternative: v }) : fields.alternative; },
    consequence(v?: Type): any { return v !== undefined ? conditional_type_({ ...config, consequence: v }) : fields.consequence; },
    left(v?: Type): any { return v !== undefined ? conditional_type_({ ...config, left: v }) : fields.left; },
    right(v?: Type): any { return v !== undefined ? conditional_type_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constraint_(
  config: ConstraintConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'constraint' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? constraint_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function construct_signature_(
  config: ConstructSignatureConfig,
) {
  const fields = {
    parameters: config.parameters,
    type: config.type,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'construct_signature' as const,
    fields,
    parameters(v?: FormalParameters): any { return v !== undefined ? construct_signature_({ ...config, parameters: v }) : fields.parameters; },
    typeField(v?: TypeAnnotation): any { return v !== undefined ? construct_signature_({ ...config, type: v }) : fields.type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? construct_signature_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constructor_type_(
  config: ConstructorTypeConfig,
) {
  const fields = {
    parameters: config.parameters,
    type: config.type,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'constructor_type' as const,
    fields,
    parameters(v?: FormalParameters): any { return v !== undefined ? constructor_type_({ ...config, parameters: v }) : fields.parameters; },
    typeField(v?: Type): any { return v !== undefined ? constructor_type_({ ...config, type: v }) : fields.type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? constructor_type_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function continue_statement_(
  config?: ContinueStatementConfig,
) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'continue_statement' as const,
    fields,
    label(v?: StatementIdentifier): any { return v !== undefined ? continue_statement_({ ...config, label: v }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decorator_(
  config: DecoratorConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'decorator' as const,
    fields,
    children,
    child(v?: CallExpression | Identifier | MemberExpression | ParenthesizedExpression): any { return v !== undefined ? decorator_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function default_type_(
  config: DefaultTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'default_type' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? default_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function do_statement_(
  config: DoStatementConfig,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
  };
  return {
    type: 'do_statement' as const,
    fields,
    body(v?: Statement): any { return v !== undefined ? do_statement_({ ...config, body: v }) : fields.body; },
    condition(v?: ParenthesizedExpression): any { return v !== undefined ? do_statement_({ ...config, condition: v }) : fields.condition; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function else_clause_(
  config: ElseClauseConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'else_clause' as const,
    fields,
    children,
    child(v?: Statement): any { return v !== undefined ? else_clause_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_assignment_(
  config: EnumAssignmentConfig,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'enum_assignment' as const,
    fields,
    name(v?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String): any { return v !== undefined ? enum_assignment_({ ...config, name: v }) : fields.name; },
    value(v?: Expression): any { return v !== undefined ? enum_assignment_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_body_(
  config?: EnumBodyConfig,
) {
  const fields = {
    name: config?.name,
  };
  const children = [config?.enumAssignment1, ...(config?.enumAssignment2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'enum_body' as const,
    fields,
    children,
    name(...v: (ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String)[]): any { return v.length ? enum_body_({ ...config, name: v }) : fields.name; },
    enumAssignment1(v?: EnumAssignment): any { return v !== undefined ? enum_body_({ ...config, enumAssignment1: v }) : config?.enumAssignment1; },
    enumAssignment2(...v: (EnumAssignment)[]): any { return v.length ? enum_body_({ ...config, enumAssignment2: v }) : config?.enumAssignment2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_declaration_(
  config: EnumDeclarationConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'enum_declaration' as const,
    fields,
    body(v?: EnumBody): any { return v !== undefined ? enum_declaration_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? enum_declaration_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_clause_(
  config?: ExportClauseConfig,
) {
  const fields = {
  };
  const children = [config?.exportSpecifier1, ...(config?.exportSpecifier2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'export_clause' as const,
    fields,
    children,
    exportSpecifier1(v?: ExportSpecifier): any { return v !== undefined ? export_clause_({ ...config, exportSpecifier1: v }) : config?.exportSpecifier1; },
    exportSpecifier2(...v: (ExportSpecifier)[]): any { return v.length ? export_clause_({ ...config, exportSpecifier2: v }) : config?.exportSpecifier2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_specifier_(
  config: ExportSpecifierConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  return {
    type: 'export_specifier' as const,
    fields,
    alias(v?: Identifier | String): any { return v !== undefined ? export_specifier_({ ...config, alias: v }) : fields.alias; },
    name(v?: Identifier | String): any { return v !== undefined ? export_specifier_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_statement_(
  config?: ExportStatementConfig,
) {
  const fields = {
    declaration: config?.declaration,
    decorator: config?.decorator,
    source: config?.source,
    value: config?.value,
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'export_statement' as const,
    fields,
    children,
    declaration(v?: Declaration): any { return v !== undefined ? export_statement_({ ...config, declaration: v }) : fields.declaration; },
    decorator(...v: (Decorator)[]): any { return v.length ? export_statement_({ ...config, decorator: v }) : fields.decorator; },
    source(v?: String): any { return v !== undefined ? export_statement_({ ...config, source: v }) : fields.source; },
    value(v?: Expression): any { return v !== undefined ? export_statement_({ ...config, value: v }) : fields.value; },
    child(v?: ExportClause | Expression | Identifier | NamespaceExport): any { return v !== undefined ? export_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expression_statement_(
  config: ExpressionStatementConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'expression_statement' as const,
    fields,
    children,
    child(v?: Expression | SequenceExpression): any { return v !== undefined ? expression_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extends_clause_(
  config: ExtendsClauseConfig,
) {
  const fields = {
    type_arguments: config.typeArguments,
    value: config.value,
  };
  return {
    type: 'extends_clause' as const,
    fields,
    typeArguments(...v: (TypeArguments)[]): any { return v.length ? extends_clause_({ ...config, typeArguments: v }) : fields.type_arguments; },
    value(...v: (Expression)[]): any { return v.length ? extends_clause_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extends_type_clause_(
  config: ExtendsTypeClauseConfig,
) {
  const fields = {
    type: config.type,
  };
  return {
    type: 'extends_type_clause' as const,
    fields,
    typeField(...v: (GenericType | NestedTypeIdentifier | TypeIdentifier)[]): any { return v.length ? extends_type_clause_({ ...config, type: v }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function finally_clause_(
  config: FinallyClauseConfig,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'finally_clause' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? finally_clause_({ ...config, body: v }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function flow_maybe_type_(
  config: FlowMaybeTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'flow_maybe_type' as const,
    fields,
    children,
    child(v?: PrimaryType): any { return v !== undefined ? flow_maybe_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_in_statement_(
  config: ForInStatementConfig,
) {
  const fields = {
    body: config.body,
    kind: config.kind,
    left: config.left,
    operator: config.operator,
    right: config.right,
    value: config.value,
  };
  return {
    type: 'for_in_statement' as const,
    fields,
    body(v?: Statement): any { return v !== undefined ? for_in_statement_({ ...config, body: v }) : fields.body; },
    kind(v?: 'var' | 'let' | 'const'): any { return v !== undefined ? for_in_statement_({ ...config, kind: v }) : fields.kind; },
    left(v?: ArrayPattern | Identifier | MemberExpression | NonNullExpression | ObjectPattern | ParenthesizedExpression | SubscriptExpression | Undefined): any { return v !== undefined ? for_in_statement_({ ...config, left: v }) : fields.left; },
    operator(v?: 'in' | 'of'): any { return v !== undefined ? for_in_statement_({ ...config, operator: v }) : fields.operator; },
    right(v?: Expression | SequenceExpression): any { return v !== undefined ? for_in_statement_({ ...config, right: v }) : fields.right; },
    value(v?: Expression): any { return v !== undefined ? for_in_statement_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_statement_(
  config: ForStatementConfig,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
    increment: config.increment,
    initializer: config.initializer,
  };
  return {
    type: 'for_statement' as const,
    fields,
    body(v?: Statement): any { return v !== undefined ? for_statement_({ ...config, body: v }) : fields.body; },
    condition(...v: (EmptyStatement | Expression | SequenceExpression)[]): any { return v.length ? for_statement_({ ...config, condition: v }) : fields.condition; },
    increment(v?: Expression | SequenceExpression): any { return v !== undefined ? for_statement_({ ...config, increment: v }) : fields.increment; },
    initializer(v?: EmptyStatement | Expression | LexicalDeclaration | SequenceExpression | VariableDeclaration): any { return v !== undefined ? for_statement_({ ...config, initializer: v }) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function formal_parameters_(
  config?: FormalParametersConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'formal_parameters' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (OptionalParameter | RequiredParameter)[]): any { return formal_parameters_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_declaration_(
  config: FunctionDeclarationConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_declaration' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? function_declaration_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? function_declaration_({ ...config, name: v }) : fields.name; },
    parameters(v?: FormalParameters): any { return v !== undefined ? function_declaration_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? function_declaration_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? function_declaration_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_expression_(
  config: FunctionExpressionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_expression' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? function_expression_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? function_expression_({ ...config, name: v }) : fields.name; },
    parameters(v?: FormalParameters): any { return v !== undefined ? function_expression_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? function_expression_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? function_expression_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_signature_(
  config: FunctionSignatureConfig,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_signature' as const,
    fields,
    name(v?: Identifier): any { return v !== undefined ? function_signature_({ ...config, name: v }) : fields.name; },
    parameters(v?: FormalParameters): any { return v !== undefined ? function_signature_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? function_signature_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? function_signature_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_type_(
  config: FunctionTypeConfig,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_type' as const,
    fields,
    parameters(v?: FormalParameters): any { return v !== undefined ? function_type_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: Asserts | Type | TypePredicate): any { return v !== undefined ? function_type_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? function_type_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generator_function_(
  config: GeneratorFunctionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'generator_function' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? generator_function_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? generator_function_({ ...config, name: v }) : fields.name; },
    parameters(v?: FormalParameters): any { return v !== undefined ? generator_function_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? generator_function_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? generator_function_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generator_function_declaration_(
  config: GeneratorFunctionDeclarationConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'generator_function_declaration' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? generator_function_declaration_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? generator_function_declaration_({ ...config, name: v }) : fields.name; },
    parameters(v?: FormalParameters): any { return v !== undefined ? generator_function_declaration_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? generator_function_declaration_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? generator_function_declaration_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_(
  config: GenericTypeConfig,
) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    fields,
    name(v?: NestedTypeIdentifier | TypeIdentifier): any { return v !== undefined ? generic_type_({ ...config, name: v }) : fields.name; },
    typeArguments(v?: TypeArguments): any { return v !== undefined ? generic_type_({ ...config, typeArguments: v }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_statement_(
  config: IfStatementConfig,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'if_statement' as const,
    fields,
    alternative(v?: ElseClause): any { return v !== undefined ? if_statement_({ ...config, alternative: v }) : fields.alternative; },
    condition(v?: ParenthesizedExpression): any { return v !== undefined ? if_statement_({ ...config, condition: v }) : fields.condition; },
    consequence(v?: Statement): any { return v !== undefined ? if_statement_({ ...config, consequence: v }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function implements_clause_(
  config: ImplementsClauseConfig,
) {
  const fields = {
  };
  const children = [config.children0, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'implements_clause' as const,
    fields,
    children,
    children0(v?: Type): any { return v !== undefined ? implements_clause_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (Type)[]): any { return v.length ? implements_clause_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_alias_(
  config: ImportAliasConfig,
) {
  const fields = {
  };
  const children = [config.identifier, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'import_alias' as const,
    fields,
    children,
    identifier(v?: Identifier): any { return v !== undefined ? import_alias_({ ...config, identifier: v }) : config?.identifier; },
    children1(v?: Identifier | NestedIdentifier): any { return v !== undefined ? import_alias_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_attribute_(
  config: ImportAttributeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'import_attribute' as const,
    fields,
    children,
    child(v?: Object): any { return v !== undefined ? import_attribute_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_clause_(
  config: ImportClauseConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'import_clause' as const,
    fields,
    children,
    children0(v?: Identifier | NamedImports | NamespaceImport): any { return v !== undefined ? import_clause_({ ...config, children0: v }) : config?.children0; },
    children1(v?: NamedImports | NamespaceImport): any { return v !== undefined ? import_clause_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_require_clause_(
  config: ImportRequireClauseConfig,
) {
  const fields = {
    source: config.source,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'import_require_clause' as const,
    fields,
    children,
    source(v?: String): any { return v !== undefined ? import_require_clause_({ ...config, source: v }) : fields.source; },
    child(v?: Identifier): any { return v !== undefined ? import_require_clause_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_specifier_(
  config: ImportSpecifierConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  return {
    type: 'import_specifier' as const,
    fields,
    alias(v?: Identifier): any { return v !== undefined ? import_specifier_({ ...config, alias: v }) : fields.alias; },
    name(v?: Identifier | String): any { return v !== undefined ? import_specifier_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_statement_(
  config?: ImportStatementConfig,
) {
  const fields = {
    source: config?.source,
  };
  const children = [config?.children0, config?.importAttribute].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'import_statement' as const,
    fields,
    children,
    source(v?: String): any { return v !== undefined ? import_statement_({ ...config, source: v }) : fields.source; },
    children0(v?: ImportClause | ImportRequireClause): any { return v !== undefined ? import_statement_({ ...config, children0: v }) : config?.children0; },
    importAttribute(v?: ImportAttribute): any { return v !== undefined ? import_statement_({ ...config, importAttribute: v }) : config?.importAttribute; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function index_signature_(
  config: IndexSignatureConfig,
) {
  const fields = {
    index_type: config.indexType,
    name: config.name,
    sign: config.sign,
    type: config.type,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'index_signature' as const,
    fields,
    children,
    indexType(v?: Type): any { return v !== undefined ? index_signature_({ ...config, indexType: v }) : fields.index_type; },
    name(v?: Identifier): any { return v !== undefined ? index_signature_({ ...config, name: v }) : fields.name; },
    sign(v?: '-' | '+'): any { return v !== undefined ? index_signature_({ ...config, sign: v }) : fields.sign; },
    typeField(v?: AddingTypeAnnotation | OmittingTypeAnnotation | OptingTypeAnnotation | TypeAnnotation): any { return v !== undefined ? index_signature_({ ...config, type: v }) : fields.type; },
    child(v?: MappedTypeClause): any { return v !== undefined ? index_signature_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function index_type_query_(
  config: IndexTypeQueryConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'index_type_query' as const,
    fields,
    children,
    child(v?: PrimaryType): any { return v !== undefined ? index_type_query_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function infer_type_(
  config: InferTypeConfig,
) {
  const fields = {
  };
  const children = [config.typeIdentifier, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'infer_type' as const,
    fields,
    children,
    typeIdentifier(v?: TypeIdentifier): any { return v !== undefined ? infer_type_({ ...config, typeIdentifier: v }) : config?.typeIdentifier; },
    children1(v?: Type): any { return v !== undefined ? infer_type_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function instantiation_expression_(
  config: InstantiationExpressionConfig,
) {
  const fields = {
    function: config.function,
    type_arguments: config.typeArguments,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'instantiation_expression' as const,
    fields,
    children,
    function(v?: Identifier | Import | MemberExpression | SubscriptExpression): any { return v !== undefined ? instantiation_expression_({ ...config, function: v }) : fields.function; },
    typeArguments(v?: TypeArguments): any { return v !== undefined ? instantiation_expression_({ ...config, typeArguments: v }) : fields.type_arguments; },
    child(v?: Expression): any { return v !== undefined ? instantiation_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interface_body_(
  config?: InterfaceBodyConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'interface_body' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]): any { return interface_body_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interface_declaration_(
  config: InterfaceDeclarationConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'interface_declaration' as const,
    fields,
    children,
    body(v?: InterfaceBody): any { return v !== undefined ? interface_declaration_({ ...config, body: v }) : fields.body; },
    name(v?: TypeIdentifier): any { return v !== undefined ? interface_declaration_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? interface_declaration_({ ...config, typeParameters: v }) : fields.type_parameters; },
    child(v?: ExtendsTypeClause): any { return v !== undefined ? interface_declaration_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function internal_module_(
  config: InternalModuleConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'internal_module' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? internal_module_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier | NestedIdentifier | String): any { return v !== undefined ? internal_module_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function intersection_type_(
  config: IntersectionTypeConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'intersection_type' as const,
    fields,
    children,
    children0(v?: Type): any { return v !== undefined ? intersection_type_({ ...config, children0: v }) : config?.children0; },
    children1(v?: Type): any { return v !== undefined ? intersection_type_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function labeled_statement_(
  config: LabeledStatementConfig,
) {
  const fields = {
    body: config.body,
    label: config.label,
  };
  return {
    type: 'labeled_statement' as const,
    fields,
    body(v?: Statement): any { return v !== undefined ? labeled_statement_({ ...config, body: v }) : fields.body; },
    label(v?: StatementIdentifier): any { return v !== undefined ? labeled_statement_({ ...config, label: v }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lexical_declaration_(
  config: LexicalDeclarationConfig,
) {
  const fields = {
    kind: config.kind,
  };
  const children = [config.variableDeclarator1, ...(config.variableDeclarator2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'lexical_declaration' as const,
    fields,
    children,
    kind(v?: 'let' | 'const'): any { return v !== undefined ? lexical_declaration_({ ...config, kind: v }) : fields.kind; },
    variableDeclarator1(v?: VariableDeclarator): any { return v !== undefined ? lexical_declaration_({ ...config, variableDeclarator1: v }) : config?.variableDeclarator1; },
    variableDeclarator2(...v: (VariableDeclarator)[]): any { return v.length ? lexical_declaration_({ ...config, variableDeclarator2: v }) : config?.variableDeclarator2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function literal_type_(
  config: LiteralTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'literal_type' as const,
    fields,
    children,
    child(v?: False | Null | Number | String | True | UnaryExpression | Undefined): any { return v !== undefined ? literal_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lookup_type_(
  config: LookupTypeConfig,
) {
  const fields = {
  };
  const children = [config.primaryType, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'lookup_type' as const,
    fields,
    children,
    primaryType(v?: PrimaryType): any { return v !== undefined ? lookup_type_({ ...config, primaryType: v }) : config?.primaryType; },
    children1(v?: Type): any { return v !== undefined ? lookup_type_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function mapped_type_clause_(
  config: MappedTypeClauseConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
    type: config.type,
  };
  return {
    type: 'mapped_type_clause' as const,
    fields,
    alias(v?: Type): any { return v !== undefined ? mapped_type_clause_({ ...config, alias: v }) : fields.alias; },
    name(v?: TypeIdentifier): any { return v !== undefined ? mapped_type_clause_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? mapped_type_clause_({ ...config, type: v }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function member_expression_(
  config: MemberExpressionConfig,
) {
  const fields = {
    object: config.object,
    optional_chain: config.optionalChain,
    property: config.property,
  };
  return {
    type: 'member_expression' as const,
    fields,
    object(v?: Expression | Import | PrimaryExpression): any { return v !== undefined ? member_expression_({ ...config, object: v }) : fields.object; },
    optionalChain(v?: OptionalChain): any { return v !== undefined ? member_expression_({ ...config, optionalChain: v }) : fields.optional_chain; },
    property(v?: PrivatePropertyIdentifier | PropertyIdentifier): any { return v !== undefined ? member_expression_({ ...config, property: v }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function method_definition_(
  config: MethodDefinitionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  const children = [config.accessibilityModifier, config.overrideModifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'method_definition' as const,
    fields,
    children,
    body(v?: StatementBlock): any { return v !== undefined ? method_definition_({ ...config, body: v }) : fields.body; },
    name(v?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String): any { return v !== undefined ? method_definition_({ ...config, name: v }) : fields.name; },
    parameters(v?: FormalParameters): any { return v !== undefined ? method_definition_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? method_definition_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? method_definition_({ ...config, typeParameters: v }) : fields.type_parameters; },
    accessibilityModifier(v?: AccessibilityModifier): any { return v !== undefined ? method_definition_({ ...config, accessibilityModifier: v }) : config?.accessibilityModifier; },
    overrideModifier(v?: OverrideModifier): any { return v !== undefined ? method_definition_({ ...config, overrideModifier: v }) : config?.overrideModifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function method_signature_(
  config: MethodSignatureConfig,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  const children = [config.accessibilityModifier, config.overrideModifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'method_signature' as const,
    fields,
    children,
    name(v?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String): any { return v !== undefined ? method_signature_({ ...config, name: v }) : fields.name; },
    parameters(v?: FormalParameters): any { return v !== undefined ? method_signature_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation): any { return v !== undefined ? method_signature_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? method_signature_({ ...config, typeParameters: v }) : fields.type_parameters; },
    accessibilityModifier(v?: AccessibilityModifier): any { return v !== undefined ? method_signature_({ ...config, accessibilityModifier: v }) : config?.accessibilityModifier; },
    overrideModifier(v?: OverrideModifier): any { return v !== undefined ? method_signature_({ ...config, overrideModifier: v }) : config?.overrideModifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function module_(
  config: ModuleConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'module' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? module_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier | NestedIdentifier | String): any { return v !== undefined ? module_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function named_imports_(
  config?: NamedImportsConfig,
) {
  const fields = {
  };
  const children = [config?.importSpecifier1, ...(config?.importSpecifier2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'named_imports' as const,
    fields,
    children,
    importSpecifier1(v?: ImportSpecifier): any { return v !== undefined ? named_imports_({ ...config, importSpecifier1: v }) : config?.importSpecifier1; },
    importSpecifier2(...v: (ImportSpecifier)[]): any { return v.length ? named_imports_({ ...config, importSpecifier2: v }) : config?.importSpecifier2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function namespace_export_(
  config: NamespaceExportConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'namespace_export' as const,
    fields,
    children,
    child(v?: Identifier | String): any { return v !== undefined ? namespace_export_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function namespace_import_(
  config: NamespaceImportConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'namespace_import' as const,
    fields,
    children,
    child(v?: Identifier): any { return v !== undefined ? namespace_import_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nested_identifier_(
  config: NestedIdentifierConfig,
) {
  const fields = {
    object: config.object,
    property: config.property,
  };
  return {
    type: 'nested_identifier' as const,
    fields,
    object(v?: Identifier | MemberExpression): any { return v !== undefined ? nested_identifier_({ ...config, object: v }) : fields.object; },
    property(v?: PropertyIdentifier): any { return v !== undefined ? nested_identifier_({ ...config, property: v }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nested_type_identifier_(
  config: NestedTypeIdentifierConfig,
) {
  const fields = {
    module: config.module,
    name: config.name,
  };
  return {
    type: 'nested_type_identifier' as const,
    fields,
    module(v?: Identifier | NestedIdentifier): any { return v !== undefined ? nested_type_identifier_({ ...config, module: v }) : fields.module; },
    name(v?: TypeIdentifier): any { return v !== undefined ? nested_type_identifier_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function new_expression_(
  config: NewExpressionConfig,
) {
  const fields = {
    arguments: config.arguments,
    constructor: config.constructor,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'new_expression' as const,
    fields,
    arguments(v?: Arguments): any { return v !== undefined ? new_expression_({ ...config, arguments: v }) : fields.arguments; },
    constructor(v?: PrimaryExpression): any { return v !== undefined ? new_expression_({ ...config, constructor: v }) : fields.constructor; },
    typeArguments(v?: TypeArguments): any { return v !== undefined ? new_expression_({ ...config, typeArguments: v }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function non_null_expression_(
  config: NonNullExpressionConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'non_null_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? non_null_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_(
  config?: ObjectConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'object' as const,
    fields,
    children,
    children0(v?: MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement): any { return v !== undefined ? object_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement)[]): any { return v.length ? object_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_assignment_pattern_(
  config: ObjectAssignmentPatternConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'object_assignment_pattern' as const,
    fields,
    left(v?: ArrayPattern | ObjectPattern | ShorthandPropertyIdentifierPattern): any { return v !== undefined ? object_assignment_pattern_({ ...config, left: v }) : fields.left; },
    right(v?: Expression): any { return v !== undefined ? object_assignment_pattern_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_pattern_(
  config?: ObjectPatternConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'object_pattern' as const,
    fields,
    children,
    children0(v?: ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern): any { return v !== undefined ? object_pattern_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern)[]): any { return v.length ? object_pattern_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_type_(
  config?: ObjectTypeConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'object_type' as const,
    fields,
    children,
    children0(v?: CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature): any { return v !== undefined ? object_type_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]): any { return v.length ? object_type_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function omitting_type_annotation_(
  config: OmittingTypeAnnotationConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'omitting_type_annotation' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? omitting_type_annotation_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function opting_type_annotation_(
  config: OptingTypeAnnotationConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'opting_type_annotation' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? opting_type_annotation_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function optional_parameter_(
  config?: OptionalParameterConfig,
) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    pattern: config?.pattern,
    type: config?.type,
    value: config?.value,
  };
  const children = [config?.accessibilityModifier, config?.overrideModifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'optional_parameter' as const,
    fields,
    children,
    decorator(...v: (Decorator)[]): any { return v.length ? optional_parameter_({ ...config, decorator: v }) : fields.decorator; },
    name(v?: Identifier): any { return v !== undefined ? optional_parameter_({ ...config, name: v }) : fields.name; },
    pattern(v?: Pattern | This): any { return v !== undefined ? optional_parameter_({ ...config, pattern: v }) : fields.pattern; },
    typeField(v?: TypeAnnotation): any { return v !== undefined ? optional_parameter_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? optional_parameter_({ ...config, value: v }) : fields.value; },
    accessibilityModifier(v?: AccessibilityModifier): any { return v !== undefined ? optional_parameter_({ ...config, accessibilityModifier: v }) : config?.accessibilityModifier; },
    overrideModifier(v?: OverrideModifier): any { return v !== undefined ? optional_parameter_({ ...config, overrideModifier: v }) : config?.overrideModifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function optional_type_(
  config: OptionalTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'optional_type' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? optional_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair_(
  config: PairConfig,
) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    type: 'pair' as const,
    fields,
    key(v?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String): any { return v !== undefined ? pair_({ ...config, key: v }) : fields.key; },
    value(v?: Expression): any { return v !== undefined ? pair_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair_pattern_(
  config: PairPatternConfig,
) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    type: 'pair_pattern' as const,
    fields,
    key(v?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String): any { return v !== undefined ? pair_pattern_({ ...config, key: v }) : fields.key; },
    value(v?: AssignmentPattern | Pattern): any { return v !== undefined ? pair_pattern_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_expression_(
  config: ParenthesizedExpressionConfig,
) {
  const fields = {
    type: config.type,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'parenthesized_expression' as const,
    fields,
    children,
    typeField(v?: TypeAnnotation): any { return v !== undefined ? parenthesized_expression_({ ...config, type: v }) : fields.type; },
    child(v?: CallExpression | Expression | Identifier | MemberExpression | SequenceExpression): any { return v !== undefined ? parenthesized_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_type_(
  config: ParenthesizedTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'parenthesized_type' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? parenthesized_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function program_(
  config?: ProgramConfig,
) {
  const fields = {
  };
  const children = [config?.hashBangLine, ...(config?.statement ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'program' as const,
    fields,
    children,
    hashBangLine(v?: HashBangLine): any { return v !== undefined ? program_({ ...config, hashBangLine: v }) : config?.hashBangLine; },
    statement(...v: (Statement)[]): any { return v.length ? program_({ ...config, statement: v }) : config?.statement; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function property_signature_(
  config: PropertySignatureConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  const children = [config.accessibilityModifier, config.overrideModifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'property_signature' as const,
    fields,
    children,
    name(v?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String): any { return v !== undefined ? property_signature_({ ...config, name: v }) : fields.name; },
    typeField(v?: TypeAnnotation): any { return v !== undefined ? property_signature_({ ...config, type: v }) : fields.type; },
    accessibilityModifier(v?: AccessibilityModifier): any { return v !== undefined ? property_signature_({ ...config, accessibilityModifier: v }) : config?.accessibilityModifier; },
    overrideModifier(v?: OverrideModifier): any { return v !== undefined ? property_signature_({ ...config, overrideModifier: v }) : config?.overrideModifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function public_field_definition_(
  config: PublicFieldDefinitionConfig,
) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type: config.type,
    value: config.value,
  };
  const children = [config.accessibilityModifier, config.overrideModifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'public_field_definition' as const,
    fields,
    children,
    decorator(...v: (Decorator)[]): any { return v.length ? public_field_definition_({ ...config, decorator: v }) : fields.decorator; },
    name(v?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String): any { return v !== undefined ? public_field_definition_({ ...config, name: v }) : fields.name; },
    typeField(v?: TypeAnnotation): any { return v !== undefined ? public_field_definition_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? public_field_definition_({ ...config, value: v }) : fields.value; },
    accessibilityModifier(v?: AccessibilityModifier): any { return v !== undefined ? public_field_definition_({ ...config, accessibilityModifier: v }) : config?.accessibilityModifier; },
    overrideModifier(v?: OverrideModifier): any { return v !== undefined ? public_field_definition_({ ...config, overrideModifier: v }) : config?.overrideModifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function readonly_type_(
  config: ReadonlyTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'readonly_type' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? readonly_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function regex_(
  config: RegexConfig,
) {
  const fields = {
    flags: config.flags,
    pattern: config.pattern,
  };
  return {
    type: 'regex' as const,
    fields,
    flags(v?: RegexFlags): any { return v !== undefined ? regex_({ ...config, flags: v }) : fields.flags; },
    pattern(v?: RegexPattern): any { return v !== undefined ? regex_({ ...config, pattern: v }) : fields.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function required_parameter_(
  config?: RequiredParameterConfig,
) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    pattern: config?.pattern,
    type: config?.type,
    value: config?.value,
  };
  const children = [config?.accessibilityModifier, config?.overrideModifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'required_parameter' as const,
    fields,
    children,
    decorator(...v: (Decorator)[]): any { return v.length ? required_parameter_({ ...config, decorator: v }) : fields.decorator; },
    name(v?: Identifier | RestPattern): any { return v !== undefined ? required_parameter_({ ...config, name: v }) : fields.name; },
    pattern(v?: Pattern | This): any { return v !== undefined ? required_parameter_({ ...config, pattern: v }) : fields.pattern; },
    typeField(v?: TypeAnnotation): any { return v !== undefined ? required_parameter_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? required_parameter_({ ...config, value: v }) : fields.value; },
    accessibilityModifier(v?: AccessibilityModifier): any { return v !== undefined ? required_parameter_({ ...config, accessibilityModifier: v }) : config?.accessibilityModifier; },
    overrideModifier(v?: OverrideModifier): any { return v !== undefined ? required_parameter_({ ...config, overrideModifier: v }) : config?.overrideModifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function rest_pattern_(
  config: RestPatternConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'rest_pattern' as const,
    fields,
    children,
    child(v?: ArrayPattern | Identifier | MemberExpression | NonNullExpression | ObjectPattern | SubscriptExpression | Undefined): any { return v !== undefined ? rest_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function rest_type_(
  config: RestTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'rest_type' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? rest_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function return_statement_(
  config?: ReturnStatementConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'return_statement' as const,
    fields,
    children,
    child(v?: Expression | SequenceExpression): any { return v !== undefined ? return_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function satisfies_expression_(
  config: SatisfiesExpressionConfig,
) {
  const fields = {
  };
  const children = [config.expression, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'satisfies_expression' as const,
    fields,
    children,
    expression(v?: Expression): any { return v !== undefined ? satisfies_expression_({ ...config, expression: v }) : config?.expression; },
    children1(v?: Type): any { return v !== undefined ? satisfies_expression_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function sequence_expression_(
  config: SequenceExpressionConfig,
) {
  const fields = {
  };
  const children = [config.expression1, ...(config.expression2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'sequence_expression' as const,
    fields,
    children,
    expression1(v?: Expression): any { return v !== undefined ? sequence_expression_({ ...config, expression1: v }) : config?.expression1; },
    expression2(...v: (Expression)[]): any { return v.length ? sequence_expression_({ ...config, expression2: v }) : config?.expression2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function spread_element_(
  config: SpreadElementConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'spread_element' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? spread_element_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function statement_block_(
  config?: StatementBlockConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'statement_block' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Statement)[]): any { return statement_block_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string_(
  config?: StringConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'string' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (EscapeSequence | StringFragment)[]): any { return string_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function subscript_expression_(
  config: SubscriptExpressionConfig,
) {
  const fields = {
    index: config.index,
    object: config.object,
    optional_chain: config.optionalChain,
  };
  return {
    type: 'subscript_expression' as const,
    fields,
    index(v?: Expression | Number | PredefinedType | SequenceExpression | String): any { return v !== undefined ? subscript_expression_({ ...config, index: v }) : fields.index; },
    object(v?: Expression | PrimaryExpression): any { return v !== undefined ? subscript_expression_({ ...config, object: v }) : fields.object; },
    optionalChain(v?: OptionalChain): any { return v !== undefined ? subscript_expression_({ ...config, optionalChain: v }) : fields.optional_chain; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_body_(
  config?: SwitchBodyConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'switch_body' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (SwitchCase | SwitchDefault)[]): any { return switch_body_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_case_(
  config: SwitchCaseConfig,
) {
  const fields = {
    body: config.body,
    value: config.value,
  };
  return {
    type: 'switch_case' as const,
    fields,
    body(...v: (Statement)[]): any { return v.length ? switch_case_({ ...config, body: v }) : fields.body; },
    value(v?: Expression | SequenceExpression): any { return v !== undefined ? switch_case_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_default_(
  config?: SwitchDefaultConfig,
) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'switch_default' as const,
    fields,
    body(...v: (Statement)[]): any { return v.length ? switch_default_({ ...config, body: v }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_statement_(
  config: SwitchStatementConfig,
) {
  const fields = {
    body: config.body,
    value: config.value,
  };
  return {
    type: 'switch_statement' as const,
    fields,
    body(v?: SwitchBody): any { return v !== undefined ? switch_statement_({ ...config, body: v }) : fields.body; },
    value(v?: ParenthesizedExpression): any { return v !== undefined ? switch_statement_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_literal_type_(
  config?: TemplateLiteralTypeConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'template_literal_type' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (StringFragment | TemplateType)[]): any { return template_literal_type_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_string_(
  config?: TemplateStringConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'template_string' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (EscapeSequence | StringFragment | TemplateSubstitution)[]): any { return template_string_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_substitution_(
  config: TemplateSubstitutionConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'template_substitution' as const,
    fields,
    children,
    child(v?: Expression | SequenceExpression): any { return v !== undefined ? template_substitution_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_type_(
  config: TemplateTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'template_type' as const,
    fields,
    children,
    child(v?: InferType | PrimaryType): any { return v !== undefined ? template_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ternary_expression_(
  config: TernaryExpressionConfig,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'ternary_expression' as const,
    fields,
    alternative(v?: Expression): any { return v !== undefined ? ternary_expression_({ ...config, alternative: v }) : fields.alternative; },
    condition(v?: Expression): any { return v !== undefined ? ternary_expression_({ ...config, condition: v }) : fields.condition; },
    consequence(v?: Expression): any { return v !== undefined ? ternary_expression_({ ...config, consequence: v }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function throw_statement_(
  config: ThrowStatementConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'throw_statement' as const,
    fields,
    children,
    child(v?: Expression | SequenceExpression): any { return v !== undefined ? throw_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_statement_(
  config: TryStatementConfig,
) {
  const fields = {
    body: config.body,
    finalizer: config.finalizer,
    handler: config.handler,
  };
  return {
    type: 'try_statement' as const,
    fields,
    body(v?: StatementBlock): any { return v !== undefined ? try_statement_({ ...config, body: v }) : fields.body; },
    finalizer(v?: FinallyClause): any { return v !== undefined ? try_statement_({ ...config, finalizer: v }) : fields.finalizer; },
    handler(v?: CatchClause): any { return v !== undefined ? try_statement_({ ...config, handler: v }) : fields.handler; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_type_(
  config?: TupleTypeConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'tuple_type' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (OptionalParameter | OptionalType | RequiredParameter | RestType | Type)[]): any { return tuple_type_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_alias_declaration_(
  config: TypeAliasDeclarationConfig,
) {
  const fields = {
    name: config.name,
    type_parameters: config.typeParameters,
    value: config.value,
  };
  return {
    type: 'type_alias_declaration' as const,
    fields,
    name(v?: TypeIdentifier): any { return v !== undefined ? type_alias_declaration_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? type_alias_declaration_({ ...config, typeParameters: v }) : fields.type_parameters; },
    value(v?: Type): any { return v !== undefined ? type_alias_declaration_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_annotation_(
  config: TypeAnnotationConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_annotation' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? type_annotation_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_arguments_(
  config: TypeArgumentsConfig,
) {
  const fields = {
  };
  const children = [config.children0, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_arguments' as const,
    fields,
    children,
    children0(v?: Type): any { return v !== undefined ? type_arguments_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (Type)[]): any { return v.length ? type_arguments_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_assertion_(
  config: TypeAssertionConfig,
) {
  const fields = {
  };
  const children = [config.typeArguments, config.expression].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_assertion' as const,
    fields,
    children,
    typeArguments(v?: TypeArguments): any { return v !== undefined ? type_assertion_({ ...config, typeArguments: v }) : config?.typeArguments; },
    expression(v?: Expression): any { return v !== undefined ? type_assertion_({ ...config, expression: v }) : config?.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameter_(
  config: TypeParameterConfig,
) {
  const fields = {
    constraint: config.constraint,
    name: config.name,
    value: config.value,
  };
  return {
    type: 'type_parameter' as const,
    fields,
    constraint(v?: Constraint): any { return v !== undefined ? type_parameter_({ ...config, constraint: v }) : fields.constraint; },
    name(v?: TypeIdentifier): any { return v !== undefined ? type_parameter_({ ...config, name: v }) : fields.name; },
    value(v?: DefaultType): any { return v !== undefined ? type_parameter_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameters_(
  config: TypeParametersConfig,
) {
  const fields = {
  };
  const children = [config.typeParameter1, ...(config.typeParameter2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_parameters' as const,
    fields,
    children,
    typeParameter1(v?: TypeParameter): any { return v !== undefined ? type_parameters_({ ...config, typeParameter1: v }) : config?.typeParameter1; },
    typeParameter2(...v: (TypeParameter)[]): any { return v.length ? type_parameters_({ ...config, typeParameter2: v }) : config?.typeParameter2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_predicate_(
  config: TypePredicateConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    type: 'type_predicate' as const,
    fields,
    name(v?: Identifier | This): any { return v !== undefined ? type_predicate_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? type_predicate_({ ...config, type: v }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_predicate_annotation_(
  config: TypePredicateAnnotationConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_predicate_annotation' as const,
    fields,
    children,
    child(v?: TypePredicate): any { return v !== undefined ? type_predicate_annotation_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_query_(
  config: TypeQueryConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_query' as const,
    fields,
    children,
    child(v?: CallExpression | Identifier | InstantiationExpression | MemberExpression | SubscriptExpression | This): any { return v !== undefined ? type_query_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unary_expression_(
  config: UnaryExpressionConfig,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'unary_expression' as const,
    fields,
    argument(v?: Expression | Number): any { return v !== undefined ? unary_expression_({ ...config, argument: v }) : fields.argument; },
    operator(v?: '!' | '~' | '-' | '+' | 'typeof' | 'void' | 'delete'): any { return v !== undefined ? unary_expression_({ ...config, operator: v }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function union_type_(
  config: UnionTypeConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'union_type' as const,
    fields,
    children,
    children0(v?: Type): any { return v !== undefined ? union_type_({ ...config, children0: v }) : config?.children0; },
    children1(v?: Type): any { return v !== undefined ? union_type_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function update_expression_(
  config: UpdateExpressionConfig,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'update_expression' as const,
    fields,
    argument(v?: Expression): any { return v !== undefined ? update_expression_({ ...config, argument: v }) : fields.argument; },
    operator(v?: '++' | '--'): any { return v !== undefined ? update_expression_({ ...config, operator: v }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function variable_declaration_(
  config: VariableDeclarationConfig,
) {
  const fields = {
  };
  const children = [config.variableDeclarator1, ...(config.variableDeclarator2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'variable_declaration' as const,
    fields,
    children,
    variableDeclarator1(v?: VariableDeclarator): any { return v !== undefined ? variable_declaration_({ ...config, variableDeclarator1: v }) : config?.variableDeclarator1; },
    variableDeclarator2(...v: (VariableDeclarator)[]): any { return v.length ? variable_declaration_({ ...config, variableDeclarator2: v }) : config?.variableDeclarator2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function variable_declarator_(
  config: VariableDeclaratorConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  return {
    type: 'variable_declarator' as const,
    fields,
    name(v?: ArrayPattern | Identifier | ObjectPattern): any { return v !== undefined ? variable_declarator_({ ...config, name: v }) : fields.name; },
    typeField(v?: TypeAnnotation): any { return v !== undefined ? variable_declarator_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? variable_declarator_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function while_statement_(
  config: WhileStatementConfig,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
  };
  return {
    type: 'while_statement' as const,
    fields,
    body(v?: Statement): any { return v !== undefined ? while_statement_({ ...config, body: v }) : fields.body; },
    condition(v?: ParenthesizedExpression): any { return v !== undefined ? while_statement_({ ...config, condition: v }) : fields.condition; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function with_statement_(
  config: WithStatementConfig,
) {
  const fields = {
    body: config.body,
    object: config.object,
  };
  return {
    type: 'with_statement' as const,
    fields,
    body(v?: Statement): any { return v !== undefined ? with_statement_({ ...config, body: v }) : fields.body; },
    object(v?: ParenthesizedExpression): any { return v !== undefined ? with_statement_({ ...config, object: v }) : fields.object; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function yield_expression_(
  config?: YieldExpressionConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'yield_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? yield_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function accessibility_modifier_(text: 'private' | 'protected' | 'public') {
  return {
    type: 'accessibility_modifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: AccessibilityModifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function debugger_statement_(text: string) {
  return {
    type: 'debugger_statement' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: DebuggerStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function empty_statement_() {
  return {
    type: 'empty_statement' as const,
    text: ';',
    render: () => ';',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: ';' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: ';' };
    },
    replace: (target: EmptyStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: ';' }; },
  };
}

export function existential_type_() {
  return {
    type: 'existential_type' as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: ExistentialTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  return {
    type: 'identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function import_() {
  return {
    type: 'import' as const,
    text: 'import',
    render: () => 'import',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'import' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'import' };
    },
    replace: (target: ImportTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' }; },
  };
}

export function meta_property_(text: string) {
  if (!/^(?:new\.target|import\.meta)$/.test(text)) throw new Error(`Invalid meta_property: '${text}' does not match grammar pattern`);
  return {
    type: 'meta_property' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: MetaPropertyTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function optional_chain_() {
  return {
    type: 'optional_chain' as const,
    text: '?.',
    render: () => '?.',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '?.' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '?.' };
    },
    replace: (target: OptionalChainTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '?.' }; },
  };
}

export function override_modifier_() {
  return {
    type: 'override_modifier' as const,
    text: 'override',
    render: () => 'override',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'override' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'override' };
    },
    replace: (target: OverrideModifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' }; },
  };
}

export function predefined_type_(text: 'any' | 'boolean' | 'never' | 'number' | 'object' | 'string' | 'string' | 'symbol' | 'unknown' | 'void') {
  return {
    type: 'predefined_type' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PredefinedTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment_(text: string) {
  return {
    type: 'comment' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: CommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escape_sequence_(text: `\\${string}`) {
  return {
    type: 'escape_sequence' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeSequenceTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    text: 'false',
    render: () => 'false',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'false' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'false' };
    },
    replace: (target: FalseTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' }; },
  };
}

export function hash_bang_line_(text: string) {
  if (!/^#!.*$/.test(text)) throw new Error(`Invalid hash_bang_line: '${text}' does not match grammar pattern`);
  return {
    type: 'hash_bang_line' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: HashBangLineTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function html_comment_(text: string) {
  return {
    type: 'html_comment' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: HtmlCommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function null_() {
  return {
    type: 'null' as const,
    text: 'null',
    render: () => 'null',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'null' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'null' };
    },
    replace: (target: NullTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' }; },
  };
}

export function number_(text: string) {
  if (!/^(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:(?:0|0?[1-9]_?\d(_?\d)*?)\.\d(_?\d)*?(?:e|E)(?:-|\+)?\d(_?\d)*?|\.\d(_?\d)*(?:e|E)(?:-|\+)?\d(_?\d)*?|(?:0|0?[1-9]_?\d(_?\d)*?)(?:e|E)(?:-|\+)?\d(_?\d)*|\d(_?\d)*)|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|\d(_?\d)*)n)$/.test(text)) throw new Error(`Invalid number: '${text}' does not match grammar pattern`);
  return {
    type: 'number' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: NumberTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function private_property_identifier_(text: string) {
  return {
    type: 'private_property_identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PrivatePropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function property_identifier_(text: string) {
  return {
    type: 'property_identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regex_flags_(text: string) {
  if (!/^[a-z]+$/.test(text)) throw new Error(`Invalid regex_flags: '${text}' does not match grammar pattern`);
  return {
    type: 'regex_flags' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: RegexFlagsTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regex_pattern_(text: string) {
  if (!/^(?:(?:\[(?:(?:\\.|[^\]\n\\]))*\]|\\.|[^/\\\[\n]))+$/.test(text)) throw new Error(`Invalid regex_pattern: '${text}' does not match grammar pattern`);
  return {
    type: 'regex_pattern' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: RegexPatternTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthand_property_identifier_(text: string) {
  return {
    type: 'shorthand_property_identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShorthandPropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthand_property_identifier_pattern_(text: string) {
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShorthandPropertyIdentifierPatternTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function statement_identifier_(text: string) {
  return {
    type: 'statement_identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StatementIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function string_fragment_(text: string) {
  return {
    type: 'string_fragment' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringFragmentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    text: 'super',
    render: () => 'super',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'super' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'super' };
    },
    replace: (target: SuperTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; },
  };
}

export function this_() {
  return {
    type: 'this' as const,
    text: 'this',
    render: () => 'this',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'this' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'this' };
    },
    replace: (target: ThisTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' }; },
  };
}

export function this_type_(text: string) {
  return {
    type: 'this_type' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ThisTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    text: 'true',
    render: () => 'true',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'true' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'true' };
    },
    replace: (target: TrueTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' }; },
  };
}

export function type_identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid type_identifier`);
  return {
    type: 'type_identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: TypeIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function undefined_() {
  return {
    type: 'undefined' as const,
    text: 'undefined',
    render: () => 'undefined',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'undefined' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'undefined' };
    },
    replace: (target: UndefinedTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' }; },
  };
}


// Auto-generated by @sittir/codegen — do not edit

import type { AbstractClassDeclaration, AbstractClassDeclarationTree, AbstractMethodSignature, AbstractMethodSignatureTree, AccessibilityModifier, AccessibilityModifierTree, AddingTypeAnnotation, AddingTypeAnnotationTree, AmbientDeclaration, AmbientDeclarationTree, Arguments, ArgumentsTree, Array, ArrayPattern, ArrayPatternTree, ArrayTree, ArrayType, ArrayTypeTree, ArrowFunction, ArrowFunctionParameter, ArrowFunctionParameterConfig, ArrowFunctionParameterTree, ArrowFunctionTree, ArrowFunctionUCallSignature, ArrowFunctionUCallSignatureConfig, ArrowFunctionUCallSignatureTree, AsExpression, AsExpressionTree, Asserts, AssertsAnnotation, AssertsAnnotationTree, AssertsTree, AssignmentExpression, AssignmentExpressionTree, AssignmentPattern, AssignmentPatternTree, AugmentedAssignmentExpression, AugmentedAssignmentExpressionTree, AugmentedAssignmentLhs, AugmentedAssignmentLhsTree, AutomaticSemicolon, AutomaticSemicolonTree, AwaitExpression, AwaitExpressionTree, BinaryExpression, BinaryExpressionTree, BreakStatement, BreakStatementTree, CallExpression, CallExpressionFunction, CallExpressionFunction2, CallExpressionFunction2Config, CallExpressionFunction2Tree, CallExpressionFunctionConfig, CallExpressionFunctionTree, CallExpressionTokQDot, CallExpressionTokQDotConfig, CallExpressionTokQDotTree, CallExpressionTree, CallSignature, CallSignatureTree, CatchClause, CatchClauseTree, Class, ClassBody, ClassBodyTree, ClassDeclaration, ClassDeclarationTree, ClassHeritage, ClassHeritageTree, ClassStaticBlock, ClassStaticBlockTree, ClassTree, Comment, CommentTree, ComputedPropertyName, ComputedPropertyNameTree, ConditionalType, ConditionalTypeTree, ConfigMap, Constraint, ConstraintTree, ConstructSignature, ConstructSignatureTree, ConstructorType, ConstructorTypeTree, ContinueStatement, ContinueStatementTree, DebuggerStatement, DebuggerStatementTree, Declaration, DeclarationTree, Decorator, DecoratorCallExpression, DecoratorCallExpressionTree, DecoratorMemberExpression, DecoratorMemberExpressionTree, DecoratorParenthesizedExpression, DecoratorParenthesizedExpressionTree, DecoratorTree, DefaultType, DefaultTypeTree, DoStatement, DoStatementTree, ElseClause, ElseClauseTree, EnumAssignment, EnumAssignmentTree, EnumBody, EnumBodyTree, EnumDeclaration, EnumDeclarationTree, ErrorRecovery, ErrorRecoveryTree, EscapeSequence, EscapeSequenceTree, ExportClause, ExportClauseTree, ExportSpecifier, ExportSpecifierTree, ExportStatement, ExportStatementExport, ExportStatementExport2, ExportStatementExport2Config, ExportStatementExport2Tree, ExportStatementExport3, ExportStatementExport3Config, ExportStatementExport3Tree, ExportStatementExportConfig, ExportStatementExportTree, ExportStatementForm0, ExportStatementForm0Config, ExportStatementForm0Tree, ExportStatementTree, Expression, ExpressionStatement, ExpressionStatementTree, ExpressionTree, ExtendsClause, ExtendsClauseTree, ExtendsTypeClause, ExtendsTypeClauseTree, False, FalseTree, FieldDefinition, FieldDefinitionTree, FinallyClause, FinallyClauseTree, FlowMaybeType, FlowMaybeTypeTree, ForInStatement, ForInStatementTree, ForStatement, ForStatementTree, FormalParameters, FormalParametersTree, FunctionDeclaration, FunctionDeclarationTree, FunctionExpression, FunctionExpressionTree, FunctionSignature, FunctionSignatureAutomaticSemicolon, FunctionSignatureAutomaticSemicolonTree, FunctionSignatureTree, FunctionType, FunctionTypeTree, GeneratorFunction, GeneratorFunctionDeclaration, GeneratorFunctionDeclarationTree, GeneratorFunctionTree, GenericType, GenericTypeTree, HashBangLine, HashBangLineTree, HiddenTypeIdentifier, HiddenTypeIdentifierTree, HtmlCharacterReference, HtmlCharacterReferenceTree, HtmlComment, HtmlCommentTree, Identifier, IdentifierTree, IfStatement, IfStatementTree, ImplementsClause, ImplementsClauseTree, Import, ImportAlias, ImportAliasTree, ImportAttribute, ImportAttributeTree, ImportClause, ImportClauseTree, ImportIdentifier, ImportIdentifierTree, ImportRequireClause, ImportRequireClauseTree, ImportSpecifier, ImportSpecifierAs, ImportSpecifierAsConfig, ImportSpecifierAsTree, ImportSpecifierName, ImportSpecifierNameConfig, ImportSpecifierNameTree, ImportSpecifierTree, ImportStatement, ImportStatementTree, ImportTree, IndexSignature, IndexSignatureColon, IndexSignatureColonConfig, IndexSignatureColonTree, IndexSignatureMappedTypeClause, IndexSignatureMappedTypeClauseConfig, IndexSignatureMappedTypeClauseTree, IndexSignatureTree, IndexTypeQuery, IndexTypeQueryTree, InferType, InferTypeTree, InstantiationExpression, InstantiationExpressionTree, InterfaceBody, InterfaceBodyTree, InterfaceDeclaration, InterfaceDeclarationTree, InternalModule, InternalModuleTree, IntersectionType, IntersectionTypeTree, JsxAttribute, JsxAttributeTree, JsxClosingElement, JsxClosingElementTree, JsxElement, JsxElementTree, JsxExpression, JsxExpressionTree, JsxIdentifier, JsxIdentifierTree, JsxNamespaceName, JsxNamespaceNameTree, JsxOpeningElement, JsxOpeningElementTree, JsxSelfClosingElement, JsxSelfClosingElementTree, JsxStartOpeningElement, JsxStartOpeningElementTree, JsxString, JsxStringTree, JsxText, JsxTextTree, KindMap, LabeledStatement, LabeledStatementTree, LexicalDeclaration, LexicalDeclarationTree, LhsExpression, LhsExpressionTree, LiteralType, LiteralTypeTree, LookupType, LookupTypeTree, MappedTypeClause, MappedTypeClauseTree, MemberExpression, MemberExpressionTree, MetaProperty, MetaPropertyTree, MethodDefinition, MethodDefinitionTree, MethodSignature, MethodSignatureTree, Module, ModuleTree, NamedImports, NamedImportsTree, NamespaceExport, NamespaceExportTree, NamespaceImport, NamespaceImportTree, NestedIdentifier, NestedIdentifierTree, NestedTypeIdentifier, NestedTypeIdentifierTree, NewExpression, NewExpressionTree, NonNullExpression, NonNullExpressionTree, Null, NullTree, Number, NumberTree, Object, ObjectAssignmentPattern, ObjectAssignmentPatternTree, ObjectPattern, ObjectPatternTree, ObjectTree, ObjectType, ObjectTypeTree, OmittingTypeAnnotation, OmittingTypeAnnotationTree, OptingTypeAnnotation, OptingTypeAnnotationTree, OptionalParameter, OptionalParameterTree, OptionalTupleParameter, OptionalTupleParameterTree, OptionalType, OptionalTypeTree, Oror, OrorTree, OverrideModifier, OverrideModifierTree, Pair, PairPattern, PairPatternTree, PairTree, ParenthesizedExpression, ParenthesizedExpressionExpression, ParenthesizedExpressionExpressionConfig, ParenthesizedExpressionExpressionTree, ParenthesizedExpressionSequenceExpression, ParenthesizedExpressionSequenceExpressionConfig, ParenthesizedExpressionSequenceExpressionTree, ParenthesizedExpressionTree, ParenthesizedType, ParenthesizedTypeTree, Pattern, PatternTree, PredefinedType, PredefinedTypeTree, PrimaryExpression, PrimaryExpressionTree, PrimaryType, PrimaryTypeTree, PrivatePropertyIdentifier, PrivatePropertyIdentifierTree, Program, ProgramTree, PropertyIdentifier, PropertyIdentifierTree, PropertyName, PropertyNameTree, PropertySignature, PropertySignatureTree, PublicFieldDefinition, PublicFieldDefinitionTree, ReadonlyType, ReadonlyTypeTree, Regex, RegexFlags, RegexFlagsTree, RegexPattern, RegexPatternTree, RegexTree, RequiredParameter, RequiredParameterTree, ReservedIdentifier, ReservedIdentifierTree, RestPattern, RestPatternTree, RestType, RestTypeTree, ReturnStatement, ReturnStatementTree, SatisfiesExpression, SatisfiesExpressionTree, Semicolon, SemicolonTree, SequenceExpression, SequenceExpressionTree, ShorthandPropertyIdentifier, ShorthandPropertyIdentifierPattern, ShorthandPropertyIdentifierPatternTree, ShorthandPropertyIdentifierTree, SpreadElement, SpreadElementTree, Statement, StatementBlock, StatementBlockTree, StatementBody, StatementBodyConfig, StatementBodyTree, StatementBreakStatement, StatementBreakStatementConfig, StatementBreakStatementTree, StatementContinueStatement, StatementContinueStatementConfig, StatementContinueStatementTree, StatementDebuggerStatement, StatementDebuggerStatementConfig, StatementDebuggerStatementTree, StatementDeclaration, StatementDeclarationConfig, StatementDeclarationTree, StatementDoStatement, StatementDoStatementConfig, StatementDoStatementTree, StatementEmptyStatement, StatementEmptyStatementConfig, StatementEmptyStatementTree, StatementExportStatement, StatementExportStatementConfig, StatementExportStatementTree, StatementExpressionStatement, StatementExpressionStatementConfig, StatementExpressionStatementTree, StatementForInStatement, StatementForInStatementConfig, StatementForInStatementTree, StatementForStatement, StatementForStatementConfig, StatementForStatementTree, StatementIdentifier, StatementIdentifierTree, StatementIfStatement, StatementIfStatementConfig, StatementIfStatementTree, StatementImportStatement, StatementImportStatementConfig, StatementImportStatementTree, StatementLabeledStatement, StatementLabeledStatementConfig, StatementLabeledStatementTree, StatementReturnStatement, StatementReturnStatementConfig, StatementReturnStatementTree, StatementSwitchStatement, StatementSwitchStatementConfig, StatementSwitchStatementTree, StatementThrowStatement, StatementThrowStatementConfig, StatementThrowStatementTree, StatementTree, StatementTryStatement, StatementTryStatementConfig, StatementTryStatementTree, StatementWhileStatement, StatementWhileStatementConfig, StatementWhileStatementTree, StatementWithStatement, StatementWithStatementConfig, StatementWithStatementTree, String, StringFragment, StringFragmentTree, StringTree, SubscriptExpression, SubscriptExpressionTree, Super, SuperTree, SwitchBody, SwitchBodyTree, SwitchCase, SwitchCaseTree, SwitchDefault, SwitchDefaultTree, SwitchStatement, SwitchStatementTree, TemplateChars, TemplateCharsTree, TemplateLiteralType, TemplateLiteralTypeTree, TemplateString, TemplateStringTree, TemplateSubstitution, TemplateSubstitutionTree, TemplateType, TemplateTypeTree, TernaryExpression, TernaryExpressionTree, TernaryQmark, TernaryQmarkTree, This, ThisTree, ThisType, ThisTypeTree, ThrowStatement, ThrowStatementTree, True, TrueTree, TryStatement, TryStatementTree, TupleParameter, TupleParameterTree, TupleType, TupleTypeTree, Type, TypeAliasDeclaration, TypeAliasDeclarationTree, TypeAnnotation, TypeAnnotationTree, TypeArguments, TypeArgumentsTree, TypeAssertion, TypeAssertionTree, TypeIdentifier, TypeIdentifierTree, TypeParameter, TypeParameterTree, TypeParameters, TypeParametersTree, TypePredicate, TypePredicateAnnotation, TypePredicateAnnotationTree, TypePredicateTree, TypeQuery, TypeQueryTree, TypeTree, UnaryExpression, UnaryExpressionTree, Undefined, UndefinedTree, UnescapedDoubleJsxStringFragment, UnescapedDoubleJsxStringFragmentTree, UnescapedDoubleStringFragment, UnescapedDoubleStringFragmentTree, UnescapedSingleJsxStringFragment, UnescapedSingleJsxStringFragmentTree, UnescapedSingleStringFragment, UnescapedSingleStringFragmentTree, UnionType, UnionTypeTree, UpdateExpression, UpdateExpressionTree, VariableDeclaration, VariableDeclarationTree, VariableDeclarator, VariableDeclaratorTree, WhileStatement, WhileStatementTree, WithStatement, WithStatementTree, YieldExpression, YieldExpressionTree } from './types.js';
import type { Edit, ConfigOf, ChildOf, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  'abstract',
  'accessor',
  'as',
  'async',
  'await',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'do',
  'else',
  'enum',
  'extends',
  'false',
  'finally',
  'for',
  'from',
  'function',
  'global',
  'if',
  'implements',
  'import',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'namespace',
  'new',
  'null',
  'override',
  'override_modifier',
  'readonly',
  'require',
  'return',
  'satisfies',
  'static',
  'super',
  'switch',
  'this',
  'this_type',
  'throw',
  'true',
  'try',
  'typeof',
  'undefined',
  'using',
  'var',
  'while',
  'with',
  'yield',
]);

export function program(config: ConfigOf<Program>) {
  const fields = {
    hash_bang_line: config?.hashBangLine,
    statements: config?.statements,
  };
  return {
    type: 'program' as const,
    named: true as const,
    fields,
    hashBangLine(hashBangLine_?: ConfigOf<Program>['hashBangLine']) { return hashBangLine_ !== undefined ? program({ ...(config ?? {}), hashBangLine: hashBangLine_ }) : fields.hash_bang_line; },
    statements(...statements_: NonNullable<ConfigOf<Program>['statements']>) { return statements_.length ? program({ ...(config ?? {}), statements: statements_ }) : fields.statements; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hashBangLine(text: string) {
  return {
    type: 'hash_bang_line' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HashBangLineTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function exportStatement(config: ConfigOf<ExportStatementForm0> | ConfigOf<ExportStatementExport> | ConfigOf<ExportStatementExport2> | ConfigOf<ExportStatementExport3>) {
  if (config && 'decorator' in config && 'declaration' in config && 'value' in config) return exportStatementForm0(config as ConfigOf<ExportStatementForm0>);
  return exportStatementExport3(config as ConfigOf<ExportStatementExport3>);
}
export function exportStatementForm0(config: ConfigOf<ExportStatementForm0>) {
  const fields = {
    decorator: config?.decorator,
    declaration: config?.declaration,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: NonNullable<ConfigOf<ExportStatementForm0>['decorator']>) { return decorator_.length ? exportStatementForm0({ ...(config ?? {}), decorator: decorator_ }) : fields.decorator; },
    declaration(declaration_?: ConfigOf<ExportStatementForm0>['declaration']) { return declaration_ !== undefined ? exportStatementForm0({ ...(config ?? {}), declaration: declaration_ }) : fields.declaration; },
    value(value_?: ConfigOf<ExportStatementForm0>['value']) { return value_ !== undefined ? exportStatementForm0({ ...(config ?? {}), value: value_ }) : fields.value; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ExportStatementForm0>) { return exportStatementForm0({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport(config: ConfigOf<ExportStatementExport>) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<ExportStatementExport>) { return exportStatementExport({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport2(config: ConfigOf<ExportStatementExport2>) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<ExportStatementExport2>) { return exportStatementExport2({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExport2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport3(config: ConfigOf<ExportStatementExport3>) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<ExportStatementExport3>) { return exportStatementExport3({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExport3Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceExport(child: ChildOf<NamespaceExport>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<NamespaceExport>['children'];
  return {
    type: 'namespace_export' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportClause(..._children: ChildOf<ExportClause>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ExportClause>['children'];
  return {
    type: 'export_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportSpecifier(config: ConfigOf<ExportSpecifier>) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'export_specifier' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ExportSpecifier>['name']) { return name_ !== undefined ? exportSpecifier({ ...(config ?? {}), name: name_ }) : fields.name; },
    alias(alias_?: ConfigOf<ExportSpecifier>['alias']) { return alias_ !== undefined ? exportSpecifier({ ...(config ?? {}), alias: alias_ }) : fields.alias; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declaration(child: ChildOf<Declaration>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Declaration>['children'];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_() {
  return {
    type: 'import' as const,
    named: true as const,
    text: 'import' as const,
    render: () => 'import' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'import' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'import' as const },
    replace: (t: ImportTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' as const }; },
  };
}

export function importStatement(config: ConfigOf<ImportStatement>) {
  const fields = {
    import_clause: config?.importClause,
    from_clause: config?.fromClause,
    import_attribute: config?.importAttribute,
    semicolon: config?.semicolon,
  };
  return {
    type: 'import_statement' as const,
    named: true as const,
    fields,
    importClause(importClause_?: ConfigOf<ImportStatement>['importClause']) { return importClause_ !== undefined ? importStatement({ ...(config ?? {}), importClause: importClause_ }) : fields.import_clause; },
    fromClause(fromClause_?: ConfigOf<ImportStatement>['fromClause']) { return fromClause_ !== undefined ? importStatement({ ...(config ?? {}), fromClause: fromClause_ }) : fields.from_clause; },
    importAttribute(importAttribute_?: ConfigOf<ImportStatement>['importAttribute']) { return importAttribute_ !== undefined ? importStatement({ ...(config ?? {}), importAttribute: importAttribute_ }) : fields.import_attribute; },
    semicolon(semicolon_?: ConfigOf<ImportStatement>['semicolon']) { return semicolon_ !== undefined ? importStatement({ ...(config ?? {}), semicolon: semicolon_ }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClause(child?: ChildOf<ImportClause>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ImportClause>['children'];
  return {
    type: 'import_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceImport(config: ConfigOf<NamespaceImport>) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'namespace_import' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<NamespaceImport>['identifier']) { return identifier_ !== undefined ? namespaceImport({ ...(config ?? {}), identifier: identifier_ }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedImports(..._children: ChildOf<NamedImports>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<NamedImports>['children'];
  return {
    type: 'named_imports' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifier(config: ConfigOf<ImportSpecifierName> | ConfigOf<ImportSpecifierAs>) {
  if (config && 'name' in config && 'alias' in config) return importSpecifierAs(config as ConfigOf<ImportSpecifierAs>);
  return importSpecifierName(config as ConfigOf<ImportSpecifierName>);
}
export function importSpecifierName(config: ConfigOf<ImportSpecifierName>) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ImportSpecifierName>['name']) { return name_ !== undefined ? importSpecifierName({ ...(config ?? {}), name: name_ }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importSpecifierAs(config: ConfigOf<ImportSpecifierAs>) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ImportSpecifierAs>['name']) { return name_ !== undefined ? importSpecifierAs({ ...(config ?? {}), name: name_ }) : fields.name; },
    alias(alias_?: ConfigOf<ImportSpecifierAs>['alias']) { return alias_ !== undefined ? importSpecifierAs({ ...(config ?? {}), alias: alias_ }) : fields.alias; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierAsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAttribute(config: ConfigOf<ImportAttribute>) {
  const fields = {
    object: config?.object,
  };
  const children = config?.children ?? [];
  return {
    type: 'import_attribute' as const,
    named: true as const,
    fields,
    children,
    object(object_?: ConfigOf<ImportAttribute>['object']) { return object_ !== undefined ? importAttribute({ ...(config ?? {}), object: object_ }) : fields.object; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ImportAttribute>) { return importAttribute({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statement(config: ConfigOf<StatementExportStatement> | ConfigOf<StatementImportStatement> | ConfigOf<StatementDebuggerStatement> | ConfigOf<StatementExpressionStatement> | ConfigOf<StatementDeclaration> | ConfigOf<StatementBody> | ConfigOf<StatementIfStatement> | ConfigOf<StatementSwitchStatement> | ConfigOf<StatementForStatement> | ConfigOf<StatementForInStatement> | ConfigOf<StatementWhileStatement> | ConfigOf<StatementDoStatement> | ConfigOf<StatementTryStatement> | ConfigOf<StatementWithStatement> | ConfigOf<StatementBreakStatement> | ConfigOf<StatementContinueStatement> | ConfigOf<StatementReturnStatement> | ConfigOf<StatementThrowStatement> | ConfigOf<StatementEmptyStatement> | ConfigOf<StatementLabeledStatement>) {
  if (config && 'body' in config) return statementBody(config as ConfigOf<StatementBody>);
  return statementLabeledStatement(config as ConfigOf<StatementLabeledStatement>);
}
export function statementExportStatement(config: ConfigOf<StatementExportStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementExportStatement>) { return statementExportStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementImportStatement(config: ConfigOf<StatementImportStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementImportStatement>) { return statementImportStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDebuggerStatement(config: ConfigOf<StatementDebuggerStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementDebuggerStatement>) { return statementDebuggerStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementExpressionStatement(config: ConfigOf<StatementExpressionStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementExpressionStatement>) { return statementExpressionStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDeclaration(config: ConfigOf<StatementDeclaration>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementDeclaration>) { return statementDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementBody(config: ConfigOf<StatementBody>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'statement' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<StatementBody>['body']) { return body_ !== undefined ? statementBody({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementIfStatement(config: ConfigOf<StatementIfStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementIfStatement>) { return statementIfStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementIfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementSwitchStatement(config: ConfigOf<StatementSwitchStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementSwitchStatement>) { return statementSwitchStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementSwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementForStatement(config: ConfigOf<StatementForStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementForStatement>) { return statementForStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementForInStatement(config: ConfigOf<StatementForInStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementForInStatement>) { return statementForInStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementWhileStatement(config: ConfigOf<StatementWhileStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementWhileStatement>) { return statementWhileStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementWhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDoStatement(config: ConfigOf<StatementDoStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementDoStatement>) { return statementDoStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementTryStatement(config: ConfigOf<StatementTryStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementTryStatement>) { return statementTryStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementWithStatement(config: ConfigOf<StatementWithStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementWithStatement>) { return statementWithStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementWithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementBreakStatement(config: ConfigOf<StatementBreakStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementBreakStatement>) { return statementBreakStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementContinueStatement(config: ConfigOf<StatementContinueStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementContinueStatement>) { return statementContinueStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementReturnStatement(config: ConfigOf<StatementReturnStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementReturnStatement>) { return statementReturnStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementThrowStatement(config: ConfigOf<StatementThrowStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementThrowStatement>) { return statementThrowStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementEmptyStatement(config: ConfigOf<StatementEmptyStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementEmptyStatement>) { return statementEmptyStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementEmptyStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementLabeledStatement(config: ConfigOf<StatementLabeledStatement>) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<StatementLabeledStatement>) { return statementLabeledStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementLabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child: ChildOf<ExpressionStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ExpressionStatement>['children'];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclaration(config: ConfigOf<VariableDeclaration>) {
  const fields = {
    declarators: config?.declarators,
    semicolon: config?.semicolon,
  };
  return {
    type: 'variable_declaration' as const,
    named: true as const,
    fields,
    declarators(declarators_?: ConfigOf<VariableDeclaration>['declarators']) { return declarators_ !== undefined ? variableDeclaration({ ...(config ?? {}), declarators: declarators_ }) : fields.declarators; },
    semicolon(semicolon_?: ConfigOf<VariableDeclaration>['semicolon']) { return semicolon_ !== undefined ? variableDeclaration({ ...(config ?? {}), semicolon: semicolon_ }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lexicalDeclaration(config: ConfigOf<LexicalDeclaration>) {
  const fields = {
    kind: config?.kind,
    declarators: config?.declarators,
    semicolon: config?.semicolon,
  };
  return {
    type: 'lexical_declaration' as const,
    named: true as const,
    fields,
    kind(kind_?: ConfigOf<LexicalDeclaration>['kind']) { return kind_ !== undefined ? lexicalDeclaration({ ...(config ?? {}), kind: kind_ }) : fields.kind; },
    declarators(declarators_?: ConfigOf<LexicalDeclaration>['declarators']) { return declarators_ !== undefined ? lexicalDeclaration({ ...(config ?? {}), declarators: declarators_ }) : fields.declarators; },
    semicolon(semicolon_?: ConfigOf<LexicalDeclaration>['semicolon']) { return semicolon_ !== undefined ? lexicalDeclaration({ ...(config ?? {}), semicolon: semicolon_ }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclarator(config: ConfigOf<VariableDeclarator>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'variable_declarator' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<VariableDeclarator>['name']) { return name_ !== undefined ? variableDeclarator({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<VariableDeclarator>['type']) { return type !== undefined ? variableDeclarator({ ...(config ?? {}), type: type }) : fields.type; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<VariableDeclarator>) { return variableDeclarator({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statementBlock(config: ConfigOf<StatementBlock>) {
  const fields = {
    statements: config?.statements,
    automatic_semicolon: config?.automaticSemicolon,
  };
  return {
    type: 'statement_block' as const,
    named: true as const,
    fields,
    statements(...statements_: NonNullable<ConfigOf<StatementBlock>['statements']>) { return statements_.length ? statementBlock({ ...(config ?? {}), statements: statements_ }) : fields.statements; },
    automaticSemicolon(automaticSemicolon_?: ConfigOf<StatementBlock>['automaticSemicolon']) { return automaticSemicolon_ !== undefined ? statementBlock({ ...(config ?? {}), automaticSemicolon: automaticSemicolon_ }) : fields.automatic_semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: ConfigOf<ElseClause>) {
  const fields = {
    statement: config?.statement,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    statement(statement_?: ConfigOf<ElseClause>['statement']) { return statement_ !== undefined ? elseClause({ ...(config ?? {}), statement: statement_ }) : fields.statement; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifStatement(config: ConfigOf<IfStatement>) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: ConfigOf<IfStatement>['condition']) { return condition_ !== undefined ? ifStatement({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    consequence(consequence_?: ConfigOf<IfStatement>['consequence']) { return consequence_ !== undefined ? ifStatement({ ...(config ?? {}), consequence: consequence_ }) : fields.consequence; },
    alternative(alternative_?: ConfigOf<IfStatement>['alternative']) { return alternative_ !== undefined ? ifStatement({ ...(config ?? {}), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchStatement(config: ConfigOf<SwitchStatement>) {
  const fields = {
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'switch_statement' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<SwitchStatement>['value']) { return value_ !== undefined ? switchStatement({ ...(config ?? {}), value: value_ }) : fields.value; },
    body(body_?: ConfigOf<SwitchStatement>['body']) { return body_ !== undefined ? switchStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: ConfigOf<ForStatement>) {
  const fields = {
    initializer: config?.initializer,
    condition: config?.condition,
    increment: config?.increment,
    body: config?.body,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    initializer(initializer_?: ConfigOf<ForStatement>['initializer']) { return initializer_ !== undefined ? forStatement({ ...(config ?? {}), initializer: initializer_ }) : fields.initializer; },
    condition(condition_?: ConfigOf<ForStatement>['condition']) { return condition_ !== undefined ? forStatement({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    increment(increment_?: ConfigOf<ForStatement>['increment']) { return increment_ !== undefined ? forStatement({ ...(config ?? {}), increment: increment_ }) : fields.increment; },
    body(body_?: ConfigOf<ForStatement>['body']) { return body_ !== undefined ? forStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInStatement(config: ConfigOf<ForInStatement>) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<ForInStatement>['body']) { return body_ !== undefined ? forInStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ForInStatement>) { return forInStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: ConfigOf<WhileStatement>) {
  const fields = {
    condition: config?.condition,
    body: config?.body,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: ConfigOf<WhileStatement>['condition']) { return condition_ !== undefined ? whileStatement({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    body(body_?: ConfigOf<WhileStatement>['body']) { return body_ !== undefined ? whileStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function doStatement(config: ConfigOf<DoStatement>) {
  const fields = {
    body: config?.body,
    condition: config?.condition,
  };
  const children = config?.children ?? [];
  return {
    type: 'do_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<DoStatement>['body']) { return body_ !== undefined ? doStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    condition(condition_?: ConfigOf<DoStatement>['condition']) { return condition_ !== undefined ? doStatement({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<DoStatement>) { return doStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: ConfigOf<TryStatement>) {
  const fields = {
    body: config?.body,
    handler: config?.handler,
    finalizer: config?.finalizer,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<TryStatement>['body']) { return body_ !== undefined ? tryStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    handler(handler_?: ConfigOf<TryStatement>['handler']) { return handler_ !== undefined ? tryStatement({ ...(config ?? {}), handler: handler_ }) : fields.handler; },
    finalizer(finalizer_?: ConfigOf<TryStatement>['finalizer']) { return finalizer_ !== undefined ? tryStatement({ ...(config ?? {}), finalizer: finalizer_ }) : fields.finalizer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: ConfigOf<WithStatement>) {
  const fields = {
    object: config?.object,
    body: config?.body,
  };
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<WithStatement>['object']) { return object_ !== undefined ? withStatement({ ...(config ?? {}), object: object_ }) : fields.object; },
    body(body_?: ConfigOf<WithStatement>['body']) { return body_ !== undefined ? withStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakStatement(config: ConfigOf<BreakStatement>) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'break_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: ConfigOf<BreakStatement>['label']) { return label_ !== undefined ? breakStatement({ ...(config ?? {}), label: label_ }) : fields.label; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<BreakStatement>) { return breakStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueStatement(config: ConfigOf<ContinueStatement>) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'continue_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: ConfigOf<ContinueStatement>['label']) { return label_ !== undefined ? continueStatement({ ...(config ?? {}), label: label_ }) : fields.label; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ContinueStatement>) { return continueStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function debuggerStatement(child: ChildOf<DebuggerStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<DebuggerStatement>['children'];
  return {
    type: 'debugger_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(child?: ChildOf<ReturnStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ReturnStatement>['children'];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function throwStatement(child: ChildOf<ThrowStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ThrowStatement>['children'];
  return {
    type: 'throw_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function labeledStatement(config: ConfigOf<LabeledStatement>) {
  const fields = {
    label: config?.label,
    body: config?.body,
  };
  return {
    type: 'labeled_statement' as const,
    named: true as const,
    fields,
    label(label_?: ConfigOf<LabeledStatement>['label']) { return label_ !== undefined ? labeledStatement({ ...(config ?? {}), label: label_ }) : fields.label; },
    body(body_?: ConfigOf<LabeledStatement>['body']) { return body_ !== undefined ? labeledStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchBody(..._children: ChildOf<SwitchBody>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<SwitchBody>['children'];
  return {
    type: 'switch_body' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchCase(config: ConfigOf<SwitchCase>) {
  const fields = {
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'switch_case' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<SwitchCase>['value']) { return value_ !== undefined ? switchCase({ ...(config ?? {}), value: value_ }) : fields.value; },
    body(...body_: NonNullable<ConfigOf<SwitchCase>['body']>) { return body_.length ? switchCase({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchDefault(config: ConfigOf<SwitchDefault>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'switch_default' as const,
    named: true as const,
    fields,
    body(...body_: NonNullable<ConfigOf<SwitchDefault>['body']>) { return body_.length ? switchDefault({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function catchClause(config: ConfigOf<CatchClause>) {
  const fields = {
    parameter: config?.parameter,
    type: config?.type,
    body: config?.body,
  };
  return {
    type: 'catch_clause' as const,
    named: true as const,
    fields,
    parameter(parameter_?: ConfigOf<CatchClause>['parameter']) { return parameter_ !== undefined ? catchClause({ ...(config ?? {}), parameter: parameter_ }) : fields.parameter; },
    typeField(type?: ConfigOf<CatchClause>['type']) { return type !== undefined ? catchClause({ ...(config ?? {}), type: type }) : fields.type; },
    body(body_?: ConfigOf<CatchClause>['body']) { return body_ !== undefined ? catchClause({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: ConfigOf<FinallyClause>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<FinallyClause>['body']) { return body_ !== undefined ? finallyClause({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(config: ConfigOf<ParenthesizedExpressionExpression> | ConfigOf<ParenthesizedExpressionSequenceExpression>) {
  if (config && 'type' in config) return parenthesizedExpressionExpression(config as ConfigOf<ParenthesizedExpressionExpression>);
  return parenthesizedExpressionSequenceExpression(config as ConfigOf<ParenthesizedExpressionSequenceExpression>);
}
export function parenthesizedExpressionExpression(config: ConfigOf<ParenthesizedExpressionExpression>) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: ConfigOf<ParenthesizedExpressionExpression>['type']) { return type !== undefined ? parenthesizedExpressionExpression({ ...(config ?? {}), type: type }) : fields.type; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ParenthesizedExpressionExpression>) { return parenthesizedExpressionExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parenthesizedExpressionSequenceExpression(config: ConfigOf<ParenthesizedExpressionSequenceExpression>) {
  const children = config?.children ?? [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<ParenthesizedExpressionSequenceExpression>) { return parenthesizedExpressionSequenceExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionSequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expression(child: ChildOf<Expression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Expression>['children'];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function primaryExpression(child: ChildOf<PrimaryExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<PrimaryExpression>['children'];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(config: ConfigOf<YieldExpression>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'yield_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<YieldExpression>['expression']) { return expression_ !== undefined ? yieldExpression({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function object(..._children: ChildOf<Object>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Object>['children'];
  return {
    type: 'object' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectPattern(..._children: ChildOf<ObjectPattern>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ObjectPattern>['children'];
  return {
    type: 'object_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentPattern(config: ConfigOf<AssignmentPattern>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_pattern' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<AssignmentPattern>['left']) { return left_ !== undefined ? assignmentPattern({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<AssignmentPattern>['right']) { return right_ !== undefined ? assignmentPattern({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectAssignmentPattern(config: ConfigOf<ObjectAssignmentPattern>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'object_assignment_pattern' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<ObjectAssignmentPattern>['left']) { return left_ !== undefined ? objectAssignmentPattern({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<ObjectAssignmentPattern>['right']) { return right_ !== undefined ? objectAssignmentPattern({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function array(..._children: ChildOf<Array>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Array>['children'];
  return {
    type: 'array' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayPattern(..._children: ChildOf<ArrayPattern>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ArrayPattern>['children'];
  return {
    type: 'array_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxElement(config: ConfigOf<JsxElement>) {
  const fields = {
    open_tag: config?.openTag,
    close_tag: config?.closeTag,
  };
  const children = config?.children ?? [];
  return {
    type: 'jsx_element' as const,
    named: true as const,
    fields,
    children,
    openTag(openTag_?: ConfigOf<JsxElement>['openTag']) { return openTag_ !== undefined ? jsxElement({ ...(config ?? {}), openTag: openTag_ }) : fields.open_tag; },
    closeTag(closeTag_?: ConfigOf<JsxElement>['closeTag']) { return closeTag_ !== undefined ? jsxElement({ ...(config ?? {}), closeTag: closeTag_ }) : fields.close_tag; },
    getChildren() { return children; },
    setChildren(...items: ChildOf<JsxElement>[]) { return jsxElement({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlCharacterReference(text: string) {
  return {
    type: 'html_character_reference' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HtmlCharacterReferenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxExpression(child?: ChildOf<JsxExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<JsxExpression>['children'];
  return {
    type: 'jsx_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxOpeningElement(child: ChildOf<JsxOpeningElement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<JsxOpeningElement>['children'];
  return {
    type: 'jsx_opening_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxIdentifier(text: string) {
  return {
    type: 'jsx_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: JsxIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function nestedIdentifier(config: ConfigOf<NestedIdentifier>) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'nested_identifier' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<NestedIdentifier>['object']) { return object_ !== undefined ? nestedIdentifier({ ...(config ?? {}), object: object_ }) : fields.object; },
    property(property_?: ConfigOf<NestedIdentifier>['property']) { return property_ !== undefined ? nestedIdentifier({ ...(config ?? {}), property: property_ }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxNamespaceName(child: ChildOf<JsxNamespaceName>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<JsxNamespaceName>['children'];
  return {
    type: 'jsx_namespace_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxNamespaceNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxClosingElement(config?: ConfigOf<JsxClosingElement>) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'jsx_closing_element' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<JsxClosingElement>['name']) { return name_ !== undefined ? jsxClosingElement({ ...(config ?? {}), name: name_ }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxSelfClosingElement(child: ChildOf<JsxSelfClosingElement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<JsxSelfClosingElement>['children'];
  return {
    type: 'jsx_self_closing_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxSelfClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxAttribute(child: ChildOf<JsxAttribute>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<JsxAttribute>['children'];
  return {
    type: 'jsx_attribute' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxString(..._children: ChildOf<JsxString>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<JsxString>['children'];
  return {
    type: '_jsx_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleJsxStringFragment(text: string) {
  return {
    type: 'unescaped_double_jsx_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedDoubleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleJsxStringFragment(text: string) {
  return {
    type: 'unescaped_single_jsx_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedSingleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function class_(config: ConfigOf<Class>) {
  const fields = {
    class_heritage: config?.classHeritage,
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'class' as const,
    named: true as const,
    fields,
    children,
    classHeritage(...classHeritage_: NonNullable<ConfigOf<Class>['classHeritage']>) { return classHeritage_.length ? class_({ ...(config ?? {}), classHeritage: classHeritage_ }) : fields.class_heritage; },
    name(name_?: ConfigOf<Class>['name']) { return name_ !== undefined ? class_({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<Class>['typeParameters']) { return typeParameters_ !== undefined ? class_({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    body(body_?: ConfigOf<Class>['body']) { return body_ !== undefined ? class_({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<Class>) { return class_({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDeclaration(config: ConfigOf<ClassDeclaration>) {
  const fields = {
    class_heritage: config?.classHeritage,
    name: config?.name,
    type_parameters: config?.typeParameters,
    automatic_semicolon: config?.automaticSemicolon,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_declaration' as const,
    named: true as const,
    fields,
    children,
    classHeritage(...classHeritage_: NonNullable<ConfigOf<ClassDeclaration>['classHeritage']>) { return classHeritage_.length ? classDeclaration({ ...(config ?? {}), classHeritage: classHeritage_ }) : fields.class_heritage; },
    name(name_?: ConfigOf<ClassDeclaration>['name']) { return name_ !== undefined ? classDeclaration({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<ClassDeclaration>['typeParameters']) { return typeParameters_ !== undefined ? classDeclaration({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    automaticSemicolon(automaticSemicolon_?: ConfigOf<ClassDeclaration>['automaticSemicolon']) { return automaticSemicolon_ !== undefined ? classDeclaration({ ...(config ?? {}), automaticSemicolon: automaticSemicolon_ }) : fields.automatic_semicolon; },
    body(body_?: ConfigOf<ClassDeclaration>['body']) { return body_ !== undefined ? classDeclaration({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ClassDeclaration>) { return classDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritage(child: ChildOf<ClassHeritage>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ClassHeritage>['children'];
  return {
    type: 'class_heritage' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionExpression(config: ConfigOf<FunctionExpression>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_expression' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<FunctionExpression>['name']) { return name_ !== undefined ? functionExpression({ ...(config ?? {}), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<FunctionExpression>['body']) { return body_ !== undefined ? functionExpression({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<FunctionExpression>) { return functionExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDeclaration(config: ConfigOf<FunctionDeclaration>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<FunctionDeclaration>['name']) { return name_ !== undefined ? functionDeclaration({ ...(config ?? {}), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<FunctionDeclaration>['body']) { return body_ !== undefined ? functionDeclaration({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<FunctionDeclaration>) { return functionDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunction(config: ConfigOf<GeneratorFunction>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_function' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<GeneratorFunction>['name']) { return name_ !== undefined ? generatorFunction({ ...(config ?? {}), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<GeneratorFunction>['body']) { return body_ !== undefined ? generatorFunction({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<GeneratorFunction>) { return generatorFunction({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunctionDeclaration(config: ConfigOf<GeneratorFunctionDeclaration>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_function_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<GeneratorFunctionDeclaration>['name']) { return name_ !== undefined ? generatorFunctionDeclaration({ ...(config ?? {}), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<GeneratorFunctionDeclaration>['body']) { return body_ !== undefined ? generatorFunctionDeclaration({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<GeneratorFunctionDeclaration>) { return generatorFunctionDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunction(config: ConfigOf<ArrowFunctionParameter> | ConfigOf<ArrowFunctionUCallSignature>) {
  if (config && 'parameter' in config) return arrowFunctionParameter(config as ConfigOf<ArrowFunctionParameter>);
  return arrowFunctionUCallSignature(config as ConfigOf<ArrowFunctionUCallSignature>);
}
export function arrowFunctionParameter(config: ConfigOf<ArrowFunctionParameter>) {
  const fields = {
    parameter: config?.parameter,
  };
  return {
    type: 'arrow_function' as const,
    named: true as const,
    fields,
    parameter(parameter_?: ConfigOf<ArrowFunctionParameter>['parameter']) { return parameter_ !== undefined ? arrowFunctionParameter({ ...(config ?? {}), parameter: parameter_ }) : fields.parameter; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function arrowFunctionUCallSignature(config: ConfigOf<ArrowFunctionUCallSignature>) {
  const children = config?.children ?? [];
  return {
    type: 'arrow_function' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<ArrowFunctionUCallSignature>) { return arrowFunctionUCallSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionUCallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: ConfigOf<CallExpressionFunction> | ConfigOf<CallExpressionFunction2> | ConfigOf<CallExpressionTokQDot>) {
  if (config && 'function' in config && 'typeArguments' in config && 'arguments' in config) return callExpressionFunction(config as ConfigOf<CallExpressionFunction>);
  return callExpressionFunction2(config as ConfigOf<CallExpressionFunction2>);
}
export function callExpressionFunction(config: ConfigOf<CallExpressionFunction>) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<CallExpressionFunction>['function']) { return function_ !== undefined ? callExpressionFunction({ ...(config ?? {}), function: function_ }) : fields.function; },
    typeArguments(typeArguments_?: ConfigOf<CallExpressionFunction>['typeArguments']) { return typeArguments_ !== undefined ? callExpressionFunction({ ...(config ?? {}), typeArguments: typeArguments_ }) : fields.type_arguments; },
    arguments(arguments_?: ConfigOf<CallExpressionFunction>['arguments']) { return arguments_ !== undefined ? callExpressionFunction({ ...(config ?? {}), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionFunction2(config: ConfigOf<CallExpressionFunction2>) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<CallExpressionFunction2>['function']) { return function_ !== undefined ? callExpressionFunction2({ ...(config ?? {}), function: function_ }) : fields.function; },
    arguments(arguments_?: ConfigOf<CallExpressionFunction2>['arguments']) { return arguments_ !== undefined ? callExpressionFunction2({ ...(config ?? {}), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionFunction2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionTokQDot(config: ConfigOf<CallExpressionTokQDot>) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<CallExpressionTokQDot>['function']) { return function_ !== undefined ? callExpressionTokQDot({ ...(config ?? {}), function: function_ }) : fields.function; },
    typeArguments(typeArguments_?: ConfigOf<CallExpressionTokQDot>['typeArguments']) { return typeArguments_ !== undefined ? callExpressionTokQDot({ ...(config ?? {}), typeArguments: typeArguments_ }) : fields.type_arguments; },
    arguments(arguments_?: ConfigOf<CallExpressionTokQDot>['arguments']) { return arguments_ !== undefined ? callExpressionTokQDot({ ...(config ?? {}), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTokQDotTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function newExpression(config: ConfigOf<NewExpression>) {
  const fields = {
    constructor: config?.constructor,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'new_expression' as const,
    named: true as const,
    fields,
    constructor(constructor_?: ConfigOf<NewExpression>['constructor']) { return constructor_ !== undefined ? newExpression({ ...(config ?? {}), constructor: constructor_ }) : fields.constructor; },
    typeArguments(typeArguments_?: ConfigOf<NewExpression>['typeArguments']) { return typeArguments_ !== undefined ? newExpression({ ...(config ?? {}), typeArguments: typeArguments_ }) : fields.type_arguments; },
    arguments(arguments_?: ConfigOf<NewExpression>['arguments']) { return arguments_ !== undefined ? newExpression({ ...(config ?? {}), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(config: ConfigOf<AwaitExpression>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'await_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<AwaitExpression>['expression']) { return expression_ !== undefined ? awaitExpression({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberExpression(config: ConfigOf<MemberExpression>) {
  const fields = {
    object: config?.object,
    optional_chain: config?.optionalChain,
    property: config?.property,
  };
  return {
    type: 'member_expression' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<MemberExpression>['object']) { return object_ !== undefined ? memberExpression({ ...(config ?? {}), object: object_ }) : fields.object; },
    optionalChain(optionalChain_?: ConfigOf<MemberExpression>['optionalChain']) { return optionalChain_ !== undefined ? memberExpression({ ...(config ?? {}), optionalChain: optionalChain_ }) : fields.optional_chain; },
    property(property_?: ConfigOf<MemberExpression>['property']) { return property_ !== undefined ? memberExpression({ ...(config ?? {}), property: property_ }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscriptExpression(config: ConfigOf<SubscriptExpression>) {
  const fields = {
    object: config?.object,
    optional_chain: config?.optionalChain,
    index: config?.index,
  };
  return {
    type: 'subscript_expression' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<SubscriptExpression>['object']) { return object_ !== undefined ? subscriptExpression({ ...(config ?? {}), object: object_ }) : fields.object; },
    optionalChain(optionalChain_?: ConfigOf<SubscriptExpression>['optionalChain']) { return optionalChain_ !== undefined ? subscriptExpression({ ...(config ?? {}), optionalChain: optionalChain_ }) : fields.optional_chain; },
    index(index_?: ConfigOf<SubscriptExpression>['index']) { return index_ !== undefined ? subscriptExpression({ ...(config ?? {}), index: index_ }) : fields.index; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lhsExpression(child: ChildOf<LhsExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<LhsExpression>['children'];
  return {
    type: '_lhs_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LhsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: ConfigOf<AssignmentExpression>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<AssignmentExpression>['left']) { return left_ !== undefined ? assignmentExpression({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<AssignmentExpression>['right']) { return right_ !== undefined ? assignmentExpression({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignmentLhs(child: ChildOf<AugmentedAssignmentLhs>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<AugmentedAssignmentLhs>['children'];
  return {
    type: '_augmented_assignment_lhs' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentLhsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignmentExpression(config: ConfigOf<AugmentedAssignmentExpression>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'augmented_assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<AugmentedAssignmentExpression>['left']) { return left_ !== undefined ? augmentedAssignmentExpression({ ...(config ?? {}), left: left_ }) : fields.left; },
    operator(operator_?: ConfigOf<AugmentedAssignmentExpression>['operator']) { return operator_ !== undefined ? augmentedAssignmentExpression({ ...(config ?? {}), operator: operator_ }) : fields.operator; },
    right(right_?: ConfigOf<AugmentedAssignmentExpression>['right']) { return right_ !== undefined ? augmentedAssignmentExpression({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function spreadElement(config: ConfigOf<SpreadElement>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'spread_element' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<SpreadElement>['expression']) { return expression_ !== undefined ? spreadElement({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ternaryExpression(config: ConfigOf<TernaryExpression>) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  const children = config?.children ?? [];
  return {
    type: 'ternary_expression' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: ConfigOf<TernaryExpression>['condition']) { return condition_ !== undefined ? ternaryExpression({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    consequence(consequence_?: ConfigOf<TernaryExpression>['consequence']) { return consequence_ !== undefined ? ternaryExpression({ ...(config ?? {}), consequence: consequence_ }) : fields.consequence; },
    alternative(alternative_?: ConfigOf<TernaryExpression>['alternative']) { return alternative_ !== undefined ? ternaryExpression({ ...(config ?? {}), alternative: alternative_ }) : fields.alternative; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<TernaryExpression>) { return ternaryExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: ConfigOf<BinaryExpression>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<BinaryExpression>['left']) { return left_ !== undefined ? binaryExpression({ ...(config ?? {}), left: left_ }) : fields.left; },
    operator(operator_?: ConfigOf<BinaryExpression>['operator']) { return operator_ !== undefined ? binaryExpression({ ...(config ?? {}), operator: operator_ }) : fields.operator; },
    right(right_?: ConfigOf<BinaryExpression>['right']) { return right_ !== undefined ? binaryExpression({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: ConfigOf<UnaryExpression>) {
  const fields = {
    operator: config?.operator,
    argument: config?.argument,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    operator(operator_?: ConfigOf<UnaryExpression>['operator']) { return operator_ !== undefined ? unaryExpression({ ...(config ?? {}), operator: operator_ }) : fields.operator; },
    argument(argument_?: ConfigOf<UnaryExpression>['argument']) { return argument_ !== undefined ? unaryExpression({ ...(config ?? {}), argument: argument_ }) : fields.argument; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function updateExpression(config: ConfigOf<UpdateExpression>) {
  const fields = {
    argument: config?.argument,
    operator: config?.operator,
  };
  return {
    type: 'update_expression' as const,
    named: true as const,
    fields,
    argument(argument_?: ConfigOf<UpdateExpression>['argument']) { return argument_ !== undefined ? updateExpression({ ...(config ?? {}), argument: argument_ }) : fields.argument; },
    operator(operator_?: ConfigOf<UpdateExpression>['operator']) { return operator_ !== undefined ? updateExpression({ ...(config ?? {}), operator: operator_ }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function sequenceExpression(..._children: ChildOf<SequenceExpression>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<SequenceExpression>['children'];
  return {
    type: 'sequence_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(..._children: ChildOf<String>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<String>['children'];
  return {
    type: 'string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleStringFragment(text: string) {
  return {
    type: 'unescaped_double_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedDoubleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleStringFragment(text: string) {
  return {
    type: 'unescaped_single_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedSingleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(text: string) {
  return {
    type: 'comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function templateString(..._children: ChildOf<TemplateString>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TemplateString>['children'];
  return {
    type: 'template_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateSubstitution(child: ChildOf<TemplateSubstitution>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<TemplateSubstitution>['children'];
  return {
    type: 'template_substitution' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regex(config: ConfigOf<Regex>) {
  const fields = {
    pattern: config?.pattern,
    flags: config?.flags,
  };
  return {
    type: 'regex' as const,
    named: true as const,
    fields,
    pattern(pattern_?: ConfigOf<Regex>['pattern']) { return pattern_ !== undefined ? regex({ ...(config ?? {}), pattern: pattern_ }) : fields.pattern; },
    flags(flags_?: ConfigOf<Regex>['flags']) { return flags_ !== undefined ? regex({ ...(config ?? {}), flags: flags_ }) : fields.flags; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regexPattern(text: string) {
  return {
    type: 'regex_pattern' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RegexPatternTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regexFlags(text: string) {
  return {
    type: 'regex_flags' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RegexFlagsTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function number(text: string) {
  return {
    type: 'number' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: NumberTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function privatePropertyIdentifier(text: string) {
  return {
    type: 'private_property_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PrivatePropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function metaProperty(text: string) {
  return {
    type: 'meta_property' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: MetaPropertyTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function this_() {
  return {
    type: 'this' as const,
    named: true as const,
    text: 'this' as const,
    render: () => 'this' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' as const },
    replace: (t: ThisTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' as const }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'true' as const,
    render: () => 'true' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'true' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'true' as const },
    replace: (t: TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' as const }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'false' as const,
    render: () => 'false' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'false' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'false' as const },
    replace: (t: FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' as const }; },
  };
}

export function null_() {
  return {
    type: 'null' as const,
    named: true as const,
    text: 'null' as const,
    render: () => 'null' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'null' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'null' as const },
    replace: (t: NullTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' as const }; },
  };
}

export function undefined_() {
  return {
    type: 'undefined' as const,
    named: true as const,
    text: 'undefined' as const,
    render: () => 'undefined' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'undefined' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'undefined' as const },
    replace: (t: UndefinedTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' as const }; },
  };
}

export function arguments_(..._children: ChildOf<Arguments>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Arguments>['children'];
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(child: ChildOf<Decorator>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Decorator>['children'];
  return {
    type: 'decorator' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorMemberExpression(config: ConfigOf<DecoratorMemberExpression>) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'decorator_member_expression' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<DecoratorMemberExpression>['object']) { return object_ !== undefined ? decoratorMemberExpression({ ...(config ?? {}), object: object_ }) : fields.object; },
    property(property_?: ConfigOf<DecoratorMemberExpression>['property']) { return property_ !== undefined ? decoratorMemberExpression({ ...(config ?? {}), property: property_ }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorMemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorCallExpression(config: ConfigOf<DecoratorCallExpression>) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'decorator_call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<DecoratorCallExpression>['function']) { return function_ !== undefined ? decoratorCallExpression({ ...(config ?? {}), function: function_ }) : fields.function; },
    typeArguments(typeArguments_?: ConfigOf<DecoratorCallExpression>['typeArguments']) { return typeArguments_ !== undefined ? decoratorCallExpression({ ...(config ?? {}), typeArguments: typeArguments_ }) : fields.type_arguments; },
    arguments(arguments_?: ConfigOf<DecoratorCallExpression>['arguments']) { return arguments_ !== undefined ? decoratorCallExpression({ ...(config ?? {}), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorCallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBody(config: ConfigOf<ClassBody>) {
  const fields = {
    decorator: config?.decorator,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: NonNullable<ConfigOf<ClassBody>['decorator']>) { return decorator_.length ? classBody({ ...(config ?? {}), decorator: decorator_ }) : fields.decorator; },
    getChildren() { return children; },
    setChildren(...items: ChildOf<ClassBody>[]) { return classBody({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDefinition(config: ConfigOf<FieldDefinition>) {
  const fields = {
    decorator: config?.decorator,
    property: config?.property,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_definition' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: NonNullable<ConfigOf<FieldDefinition>['decorator']>) { return decorator_.length ? fieldDefinition({ ...(config ?? {}), decorator: decorator_ }) : fields.decorator; },
    property(property_?: ConfigOf<FieldDefinition>['property']) { return property_ !== undefined ? fieldDefinition({ ...(config ?? {}), property: property_ }) : fields.property; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<FieldDefinition>) { return fieldDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formalParameters(..._children: ChildOf<FormalParameters>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<FormalParameters>['children'];
  return {
    type: 'formal_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classStaticBlock(config: ConfigOf<ClassStaticBlock>) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_static_block' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<ClassStaticBlock>['body']) { return body_ !== undefined ? classStaticBlock({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ClassStaticBlock>) { return classStaticBlock({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pattern(child: ChildOf<Pattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Pattern>['children'];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restPattern(child: ChildOf<RestPattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<RestPattern>['children'];
  return {
    type: 'rest_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodDefinition(config: ConfigOf<MethodDefinition>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'method_definition' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<MethodDefinition>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? methodDefinition({ ...(config ?? {}), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<MethodDefinition>['overrideModifier']) { return overrideModifier_ !== undefined ? methodDefinition({ ...(config ?? {}), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    name(name_?: ConfigOf<MethodDefinition>['name']) { return name_ !== undefined ? methodDefinition({ ...(config ?? {}), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<MethodDefinition>['body']) { return body_ !== undefined ? methodDefinition({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<MethodDefinition>) { return methodDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: ConfigOf<Pair>) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key_?: ConfigOf<Pair>['key']) { return key_ !== undefined ? pair({ ...(config ?? {}), key: key_ }) : fields.key; },
    value(value_?: ConfigOf<Pair>['value']) { return value_ !== undefined ? pair({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pairPattern(config: ConfigOf<PairPattern>) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair_pattern' as const,
    named: true as const,
    fields,
    key(key_?: ConfigOf<PairPattern>['key']) { return key_ !== undefined ? pairPattern({ ...(config ?? {}), key: key_ }) : fields.key; },
    value(value_?: ConfigOf<PairPattern>['value']) { return value_ !== undefined ? pairPattern({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertyName(child: ChildOf<PropertyName>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<PropertyName>['children'];
  return {
    type: '_property_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function computedPropertyName(config: ConfigOf<ComputedPropertyName>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'computed_property_name' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<ComputedPropertyName>['expression']) { return expression_ !== undefined ? computedPropertyName({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function reservedIdentifier(text: string) {
  return {
    type: '_reserved_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ReservedIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function semicolon(child: ChildOf<Semicolon>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Semicolon>['children'];
  return {
    type: '_semicolon' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SemicolonTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function publicFieldDefinition(config: ConfigOf<PublicFieldDefinition>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    initializer: config?.initializer,
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(...accessibilityModifier_: NonNullable<ConfigOf<PublicFieldDefinition>['accessibilityModifier']>) { return accessibilityModifier_.length ? publicFieldDefinition({ ...(config ?? {}), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<PublicFieldDefinition>['overrideModifier']) { return overrideModifier_ !== undefined ? publicFieldDefinition({ ...(config ?? {}), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    initializer(initializer_?: ConfigOf<PublicFieldDefinition>['initializer']) { return initializer_ !== undefined ? publicFieldDefinition({ ...(config ?? {}), initializer: initializer_ }) : fields.initializer; },
    name(name_?: ConfigOf<PublicFieldDefinition>['name']) { return name_ !== undefined ? publicFieldDefinition({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<PublicFieldDefinition>['type']) { return type !== undefined ? publicFieldDefinition({ ...(config ?? {}), type: type }) : fields.type; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<PublicFieldDefinition>) { return publicFieldDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxStartOpeningElement(config?: ConfigOf<JsxStartOpeningElement>) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
    attribute: config?.attribute,
  };
  return {
    type: '_jsx_start_opening_element' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<JsxStartOpeningElement>['name']) { return name_ !== undefined ? jsxStartOpeningElement({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeArguments(typeArguments_?: ConfigOf<JsxStartOpeningElement>['typeArguments']) { return typeArguments_ !== undefined ? jsxStartOpeningElement({ ...(config ?? {}), typeArguments: typeArguments_ }) : fields.type_arguments; },
    attribute(...attribute_: NonNullable<ConfigOf<JsxStartOpeningElement>['attribute']>) { return attribute_.length ? jsxStartOpeningElement({ ...(config ?? {}), attribute: attribute_ }) : fields.attribute; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxStartOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importIdentifier(child: ChildOf<ImportIdentifier>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ImportIdentifier>['children'];
  return {
    type: '_import_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonNullExpression(config: ConfigOf<NonNullExpression>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'non_null_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<NonNullExpression>['expression']) { return expression_ !== undefined ? nonNullExpression({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodSignature(config: ConfigOf<MethodSignature>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<MethodSignature>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? methodSignature({ ...(config ?? {}), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<MethodSignature>['overrideModifier']) { return overrideModifier_ !== undefined ? methodSignature({ ...(config ?? {}), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    name(name_?: ConfigOf<MethodSignature>['name']) { return name_ !== undefined ? methodSignature({ ...(config ?? {}), name: name_ }) : fields.name; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<MethodSignature>) { return methodSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractMethodSignature(config: ConfigOf<AbstractMethodSignature>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'abstract_method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<AbstractMethodSignature>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? abstractMethodSignature({ ...(config ?? {}), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<AbstractMethodSignature>['overrideModifier']) { return overrideModifier_ !== undefined ? abstractMethodSignature({ ...(config ?? {}), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    name(name_?: ConfigOf<AbstractMethodSignature>['name']) { return name_ !== undefined ? abstractMethodSignature({ ...(config ?? {}), name: name_ }) : fields.name; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<AbstractMethodSignature>) { return abstractMethodSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignature(config: ConfigOf<FunctionSignature>) {
  const fields = {
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_signature' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<FunctionSignature>['name']) { return name_ !== undefined ? functionSignature({ ...(config ?? {}), name: name_ }) : fields.name; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<FunctionSignature>) { return functionSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorParenthesizedExpression(child: ChildOf<DecoratorParenthesizedExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<DecoratorParenthesizedExpression>['children'];
  return {
    type: 'decorator_parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAssertion(config: ConfigOf<TypeAssertion>) {
  const fields = {
    type_arguments: config?.typeArguments,
    expression: config?.expression,
  };
  return {
    type: 'type_assertion' as const,
    named: true as const,
    fields,
    typeArguments(typeArguments_?: ConfigOf<TypeAssertion>['typeArguments']) { return typeArguments_ !== undefined ? typeAssertion({ ...(config ?? {}), typeArguments: typeArguments_ }) : fields.type_arguments; },
    expression(expression_?: ConfigOf<TypeAssertion>['expression']) { return expression_ !== undefined ? typeAssertion({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asExpression(config: ConfigOf<AsExpression>) {
  const fields = {
    expression: config?.expression,
    type_annotation: config?.typeAnnotation,
  };
  return {
    type: 'as_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<AsExpression>['expression']) { return expression_ !== undefined ? asExpression({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    typeAnnotation(typeAnnotation_?: ConfigOf<AsExpression>['typeAnnotation']) { return typeAnnotation_ !== undefined ? asExpression({ ...(config ?? {}), typeAnnotation: typeAnnotation_ }) : fields.type_annotation; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function satisfiesExpression(config: ConfigOf<SatisfiesExpression>) {
  const fields = {
    expression: config?.expression,
    type_annotation: config?.typeAnnotation,
  };
  return {
    type: 'satisfies_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<SatisfiesExpression>['expression']) { return expression_ !== undefined ? satisfiesExpression({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    typeAnnotation(typeAnnotation_?: ConfigOf<SatisfiesExpression>['typeAnnotation']) { return typeAnnotation_ !== undefined ? satisfiesExpression({ ...(config ?? {}), typeAnnotation: typeAnnotation_ }) : fields.type_annotation; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function instantiationExpression(config: ConfigOf<InstantiationExpression>) {
  const fields = {
    expression: config?.expression,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'instantiation_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<InstantiationExpression>['expression']) { return expression_ !== undefined ? instantiationExpression({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    typeArguments(typeArguments_?: ConfigOf<InstantiationExpression>['typeArguments']) { return typeArguments_ !== undefined ? instantiationExpression({ ...(config ?? {}), typeArguments: typeArguments_ }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importRequireClause(config: ConfigOf<ImportRequireClause>) {
  const fields = {
    identifier: config?.identifier,
    source: config?.source,
  };
  return {
    type: 'import_require_clause' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<ImportRequireClause>['identifier']) { return identifier_ !== undefined ? importRequireClause({ ...(config ?? {}), identifier: identifier_ }) : fields.identifier; },
    source(source_?: ConfigOf<ImportRequireClause>['source']) { return source_ !== undefined ? importRequireClause({ ...(config ?? {}), source: source_ }) : fields.source; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsClause(..._children: ChildOf<ExtendsClause>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ExtendsClause>['children'];
  return {
    type: 'extends_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implementsClause(..._children: ChildOf<ImplementsClause>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ImplementsClause>['children'];
  return {
    type: 'implements_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ambientDeclaration(config: ConfigOf<AmbientDeclaration>) {
  const fields = {
    declaration: config?.declaration,
  };
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    fields,
    declaration(declaration_?: ConfigOf<AmbientDeclaration>['declaration']) { return declaration_ !== undefined ? ambientDeclaration({ ...(config ?? {}), declaration: declaration_ }) : fields.declaration; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractClassDeclaration(config: ConfigOf<AbstractClassDeclaration>) {
  const fields = {
    class_heritage: config?.classHeritage,
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'abstract_class_declaration' as const,
    named: true as const,
    fields,
    children,
    classHeritage(...classHeritage_: NonNullable<ConfigOf<AbstractClassDeclaration>['classHeritage']>) { return classHeritage_.length ? abstractClassDeclaration({ ...(config ?? {}), classHeritage: classHeritage_ }) : fields.class_heritage; },
    name(name_?: ConfigOf<AbstractClassDeclaration>['name']) { return name_ !== undefined ? abstractClassDeclaration({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<AbstractClassDeclaration>['typeParameters']) { return typeParameters_ !== undefined ? abstractClassDeclaration({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    body(body_?: ConfigOf<AbstractClassDeclaration>['body']) { return body_ !== undefined ? abstractClassDeclaration({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<AbstractClassDeclaration>) { return abstractClassDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function module(child: ChildOf<Module>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Module>['children'];
  return {
    type: 'module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function internalModule(child: ChildOf<InternalModule>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<InternalModule>['children'];
  return {
    type: 'internal_module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAlias(config: ConfigOf<ImportAlias>) {
  const fields = {
    name: config?.name,
    value: config?.value,
    semicolon: config?.semicolon,
  };
  return {
    type: 'import_alias' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ImportAlias>['name']) { return name_ !== undefined ? importAlias({ ...(config ?? {}), name: name_ }) : fields.name; },
    value(value_?: ConfigOf<ImportAlias>['value']) { return value_ !== undefined ? importAlias({ ...(config ?? {}), value: value_ }) : fields.value; },
    semicolon(semicolon_?: ConfigOf<ImportAlias>['semicolon']) { return semicolon_ !== undefined ? importAlias({ ...(config ?? {}), semicolon: semicolon_ }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nestedTypeIdentifier(config: ConfigOf<NestedTypeIdentifier>) {
  const fields = {
    module: config?.module,
    name: config?.name,
  };
  return {
    type: 'nested_type_identifier' as const,
    named: true as const,
    fields,
    module(module_?: ConfigOf<NestedTypeIdentifier>['module']) { return module_ !== undefined ? nestedTypeIdentifier({ ...(config ?? {}), module: module_ }) : fields.module; },
    name(name_?: ConfigOf<NestedTypeIdentifier>['name']) { return name_ !== undefined ? nestedTypeIdentifier({ ...(config ?? {}), name: name_ }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interfaceDeclaration(config: ConfigOf<InterfaceDeclaration>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    extends_type_clause: config?.extendsTypeClause,
    body: config?.body,
  };
  return {
    type: 'interface_declaration' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<InterfaceDeclaration>['name']) { return name_ !== undefined ? interfaceDeclaration({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<InterfaceDeclaration>['typeParameters']) { return typeParameters_ !== undefined ? interfaceDeclaration({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    extendsTypeClause(extendsTypeClause_?: ConfigOf<InterfaceDeclaration>['extendsTypeClause']) { return extendsTypeClause_ !== undefined ? interfaceDeclaration({ ...(config ?? {}), extendsTypeClause: extendsTypeClause_ }) : fields.extends_type_clause; },
    body(body_?: ConfigOf<InterfaceDeclaration>['body']) { return body_ !== undefined ? interfaceDeclaration({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsTypeClause(config: ConfigOf<ExtendsTypeClause>) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'extends_type_clause' as const,
    named: true as const,
    fields,
    typeField(...type: NonNullable<ConfigOf<ExtendsTypeClause>['type']>) { return type.length ? extendsTypeClause({ ...(config ?? {}), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumDeclaration(config: ConfigOf<EnumDeclaration>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'enum_declaration' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<EnumDeclaration>['name']) { return name_ !== undefined ? enumDeclaration({ ...(config ?? {}), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<EnumDeclaration>['body']) { return body_ !== undefined ? enumDeclaration({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumBody(config?: ConfigOf<EnumBody>) {
  const fields = {
    opening: config?.opening,
  };
  return {
    type: 'enum_body' as const,
    named: true as const,
    fields,
    opening(opening_?: ConfigOf<EnumBody>['opening']) { return opening_ !== undefined ? enumBody({ ...(config ?? {}), opening: opening_ }) : fields.opening; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumAssignment(config: ConfigOf<EnumAssignment>) {
  const fields = {
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'enum_assignment' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<EnumAssignment>['name']) { return name_ !== undefined ? enumAssignment({ ...(config ?? {}), name: name_ }) : fields.name; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<EnumAssignment>) { return enumAssignment({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasDeclaration(config: ConfigOf<TypeAliasDeclaration>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'type_alias_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<TypeAliasDeclaration>['name']) { return name_ !== undefined ? typeAliasDeclaration({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<TypeAliasDeclaration>['typeParameters']) { return typeParameters_ !== undefined ? typeAliasDeclaration({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    value(value_?: ConfigOf<TypeAliasDeclaration>['value']) { return value_ !== undefined ? typeAliasDeclaration({ ...(config ?? {}), value: value_ }) : fields.value; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<TypeAliasDeclaration>) { return typeAliasDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function accessibilityModifier(text: string) {
  return {
    type: 'accessibility_modifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: AccessibilityModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function overrideModifier() {
  return {
    type: 'override_modifier' as const,
    named: true as const,
    text: 'override' as const,
    render: () => 'override' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'override' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'override' as const },
    replace: (t: OverrideModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' as const }; },
  };
}

export function requiredParameter(config: ConfigOf<RequiredParameter>) {
  const fields = {
    parameter_name: config?.parameterName,
    type: config?.type,
    initializer: config?.initializer,
  };
  return {
    type: 'required_parameter' as const,
    named: true as const,
    fields,
    parameterName(parameterName_?: ConfigOf<RequiredParameter>['parameterName']) { return parameterName_ !== undefined ? requiredParameter({ ...(config ?? {}), parameterName: parameterName_ }) : fields.parameter_name; },
    typeField(type?: ConfigOf<RequiredParameter>['type']) { return type !== undefined ? requiredParameter({ ...(config ?? {}), type: type }) : fields.type; },
    initializer(initializer_?: ConfigOf<RequiredParameter>['initializer']) { return initializer_ !== undefined ? requiredParameter({ ...(config ?? {}), initializer: initializer_ }) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalParameter(config: ConfigOf<OptionalParameter>) {
  const fields = {
    parameter_name: config?.parameterName,
    type: config?.type,
    initializer: config?.initializer,
  };
  return {
    type: 'optional_parameter' as const,
    named: true as const,
    fields,
    parameterName(parameterName_?: ConfigOf<OptionalParameter>['parameterName']) { return parameterName_ !== undefined ? optionalParameter({ ...(config ?? {}), parameterName: parameterName_ }) : fields.parameter_name; },
    typeField(type?: ConfigOf<OptionalParameter>['type']) { return type !== undefined ? optionalParameter({ ...(config ?? {}), type: type }) : fields.type; },
    initializer(initializer_?: ConfigOf<OptionalParameter>['initializer']) { return initializer_ !== undefined ? optionalParameter({ ...(config ?? {}), initializer: initializer_ }) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function omittingTypeAnnotation(child: ChildOf<OmittingTypeAnnotation>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<OmittingTypeAnnotation>['children'];
  return {
    type: 'omitting_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function addingTypeAnnotation(child: ChildOf<AddingTypeAnnotation>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<AddingTypeAnnotation>['children'];
  return {
    type: 'adding_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optingTypeAnnotation(child: ChildOf<OptingTypeAnnotation>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<OptingTypeAnnotation>['children'];
  return {
    type: 'opting_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAnnotation(child: ChildOf<TypeAnnotation>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<TypeAnnotation>['children'];
  return {
    type: 'type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asserts(child: ChildOf<Asserts>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Asserts>['children'];
  return {
    type: 'asserts' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertsAnnotation(config: ConfigOf<AssertsAnnotation>) {
  const fields = {
    asserts: config?.asserts,
  };
  return {
    type: 'asserts_annotation' as const,
    named: true as const,
    fields,
    asserts(asserts_?: ConfigOf<AssertsAnnotation>['asserts']) { return asserts_ !== undefined ? assertsAnnotation({ ...(config ?? {}), asserts: asserts_ }) : fields.asserts; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function type(child: ChildOf<Type>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Type>['children'];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleParameter(config: ConfigOf<TupleParameter>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'tuple_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TupleParameter>['name']) { return name_ !== undefined ? tupleParameter({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<TupleParameter>['type']) { return type !== undefined ? tupleParameter({ ...(config ?? {}), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalTupleParameter(config: ConfigOf<OptionalTupleParameter>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'optional_tuple_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<OptionalTupleParameter>['name']) { return name_ !== undefined ? optionalTupleParameter({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<OptionalTupleParameter>['type']) { return type !== undefined ? optionalTupleParameter({ ...(config ?? {}), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalType(child: ChildOf<OptionalType>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<OptionalType>['children'];
  return {
    type: 'optional_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restType(child: ChildOf<RestType>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<RestType>['children'];
  return {
    type: 'rest_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructorType(config: ConfigOf<ConstructorType>) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    type: config?.type,
  };
  return {
    type: 'constructor_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: ConfigOf<ConstructorType>['typeParameters']) { return typeParameters_ !== undefined ? constructorType({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    parameters(parameters_?: ConfigOf<ConstructorType>['parameters']) { return parameters_ !== undefined ? constructorType({ ...(config ?? {}), parameters: parameters_ }) : fields.parameters; },
    typeField(type?: ConfigOf<ConstructorType>['type']) { return type !== undefined ? constructorType({ ...(config ?? {}), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function primaryType(config: ConfigOf<PrimaryType>) {
  const fields = {
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<PrimaryType>['name']) { return name_ !== undefined ? primaryType({ ...(config ?? {}), name: name_ }) : fields.name; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<PrimaryType>) { return primaryType({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateType(child: ChildOf<TemplateType>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<TemplateType>['children'];
  return {
    type: 'template_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateLiteralType(..._children: ChildOf<TemplateLiteralType>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TemplateLiteralType>['children'];
  return {
    type: 'template_literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function inferType(config: ConfigOf<InferType>) {
  const fields = {
    type_identifier: config?.typeIdentifier,
    constraint: config?.constraint,
  };
  return {
    type: 'infer_type' as const,
    named: true as const,
    fields,
    typeIdentifier(typeIdentifier_?: ConfigOf<InferType>['typeIdentifier']) { return typeIdentifier_ !== undefined ? inferType({ ...(config ?? {}), typeIdentifier: typeIdentifier_ }) : fields.type_identifier; },
    constraint(constraint_?: ConfigOf<InferType>['constraint']) { return constraint_ !== undefined ? inferType({ ...(config ?? {}), constraint: constraint_ }) : fields.constraint; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalType(config: ConfigOf<ConditionalType>) {
  const fields = {
    left: config?.left,
    right: config?.right,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'conditional_type' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<ConditionalType>['left']) { return left_ !== undefined ? conditionalType({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<ConditionalType>['right']) { return right_ !== undefined ? conditionalType({ ...(config ?? {}), right: right_ }) : fields.right; },
    consequence(consequence_?: ConfigOf<ConditionalType>['consequence']) { return consequence_ !== undefined ? conditionalType({ ...(config ?? {}), consequence: consequence_ }) : fields.consequence; },
    alternative(alternative_?: ConfigOf<ConditionalType>['alternative']) { return alternative_ !== undefined ? conditionalType({ ...(config ?? {}), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: ConfigOf<GenericType>) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<GenericType>['name']) { return name_ !== undefined ? genericType({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeArguments(typeArguments_?: ConfigOf<GenericType>['typeArguments']) { return typeArguments_ !== undefined ? genericType({ ...(config ?? {}), typeArguments: typeArguments_ }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicate(config: ConfigOf<TypePredicate>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'type_predicate' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TypePredicate>['name']) { return name_ !== undefined ? typePredicate({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<TypePredicate>['type']) { return type !== undefined ? typePredicate({ ...(config ?? {}), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicateAnnotation(config: ConfigOf<TypePredicateAnnotation>) {
  const fields = {
    type_predicate: config?.typePredicate,
  };
  return {
    type: 'type_predicate_annotation' as const,
    named: true as const,
    fields,
    typePredicate(typePredicate_?: ConfigOf<TypePredicateAnnotation>['typePredicate']) { return typePredicate_ !== undefined ? typePredicateAnnotation({ ...(config ?? {}), typePredicate: typePredicate_ }) : fields.type_predicate; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeQuery(child: ChildOf<TypeQuery>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<TypeQuery>['children'];
  return {
    type: 'type_query' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexTypeQuery(config: ConfigOf<IndexTypeQuery>) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'index_type_query' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: ConfigOf<IndexTypeQuery>['primaryType']) { return primaryType_ !== undefined ? indexTypeQuery({ ...(config ?? {}), primaryType: primaryType_ }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lookupType(config: ConfigOf<LookupType>) {
  const fields = {
    primary_type: config?.primaryType,
    index_type: config?.indexType,
  };
  return {
    type: 'lookup_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: ConfigOf<LookupType>['primaryType']) { return primaryType_ !== undefined ? lookupType({ ...(config ?? {}), primaryType: primaryType_ }) : fields.primary_type; },
    indexType(indexType_?: ConfigOf<LookupType>['indexType']) { return indexType_ !== undefined ? lookupType({ ...(config ?? {}), indexType: indexType_ }) : fields.index_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mappedTypeClause(config: ConfigOf<MappedTypeClause>) {
  const fields = {
    name: config?.name,
    type: config?.type,
    alias: config?.alias,
  };
  return {
    type: 'mapped_type_clause' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<MappedTypeClause>['name']) { return name_ !== undefined ? mappedTypeClause({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<MappedTypeClause>['type']) { return type !== undefined ? mappedTypeClause({ ...(config ?? {}), type: type }) : fields.type; },
    alias(alias_?: ConfigOf<MappedTypeClause>['alias']) { return alias_ !== undefined ? mappedTypeClause({ ...(config ?? {}), alias: alias_ }) : fields.alias; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function literalType(child: ChildOf<LiteralType>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<LiteralType>['children'];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function flowMaybeType(config: ConfigOf<FlowMaybeType>) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'flow_maybe_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: ConfigOf<FlowMaybeType>['primaryType']) { return primaryType_ !== undefined ? flowMaybeType({ ...(config ?? {}), primaryType: primaryType_ }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedType(child: ChildOf<ParenthesizedType>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ParenthesizedType>['children'];
  return {
    type: 'parenthesized_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function predefinedType(text: string) {
  return {
    type: 'predefined_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PredefinedTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeArguments(..._children: ChildOf<TypeArguments>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TypeArguments>['children'];
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectType(config: ConfigOf<ObjectType>) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'object_type' as const,
    named: true as const,
    fields,
    opening(opening_?: ConfigOf<ObjectType>['opening']) { return opening_ !== undefined ? objectType({ ...(config ?? {}), opening: opening_ }) : fields.opening; },
    members(members_?: ConfigOf<ObjectType>['members']) { return members_ !== undefined ? objectType({ ...(config ?? {}), members: members_ }) : fields.members; },
    closing(closing_?: ConfigOf<ObjectType>['closing']) { return closing_ !== undefined ? objectType({ ...(config ?? {}), closing: closing_ }) : fields.closing; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callSignature(child: ChildOf<CallSignature>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<CallSignature>['children'];
  return {
    type: 'call_signature' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertySignature(config: ConfigOf<PropertySignature>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'property_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<PropertySignature>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? propertySignature({ ...(config ?? {}), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<PropertySignature>['overrideModifier']) { return overrideModifier_ !== undefined ? propertySignature({ ...(config ?? {}), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    name(name_?: ConfigOf<PropertySignature>['name']) { return name_ !== undefined ? propertySignature({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<PropertySignature>['type']) { return type !== undefined ? propertySignature({ ...(config ?? {}), type: type }) : fields.type; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<PropertySignature>) { return propertySignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(..._children: ChildOf<TypeParameters>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TypeParameters>['children'];
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: ConfigOf<TypeParameter>) {
  const fields = {
    name: config?.name,
    constraint: config?.constraint,
    value: config?.value,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TypeParameter>['name']) { return name_ !== undefined ? typeParameter({ ...(config ?? {}), name: name_ }) : fields.name; },
    constraint(constraint_?: ConfigOf<TypeParameter>['constraint']) { return constraint_ !== undefined ? typeParameter({ ...(config ?? {}), constraint: constraint_ }) : fields.constraint; },
    value(value_?: ConfigOf<TypeParameter>['value']) { return value_ !== undefined ? typeParameter({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultType(child: ChildOf<DefaultType>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<DefaultType>['children'];
  return {
    type: 'default_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constraint(child: ChildOf<Constraint>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Constraint>['children'];
  return {
    type: 'constraint' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructSignature(config: ConfigOf<ConstructSignature>) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    type: config?.type,
  };
  return {
    type: 'construct_signature' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: ConfigOf<ConstructSignature>['typeParameters']) { return typeParameters_ !== undefined ? constructSignature({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    parameters(parameters_?: ConfigOf<ConstructSignature>['parameters']) { return parameters_ !== undefined ? constructSignature({ ...(config ?? {}), parameters: parameters_ }) : fields.parameters; },
    typeField(type?: ConfigOf<ConstructSignature>['type']) { return type !== undefined ? constructSignature({ ...(config ?? {}), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignature(config: ConfigOf<IndexSignatureColon> | ConfigOf<IndexSignatureMappedTypeClause>) {
  if (config && 'name' in config && 'indexType' in config) return indexSignatureColon(config as ConfigOf<IndexSignatureColon>);
  return indexSignatureMappedTypeClause(config as ConfigOf<IndexSignatureMappedTypeClause>);
}
export function indexSignatureColon(config: ConfigOf<IndexSignatureColon>) {
  const fields = {
    name: config?.name,
    index_type: config?.indexType,
  };
  return {
    type: 'index_signature' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<IndexSignatureColon>['name']) { return name_ !== undefined ? indexSignatureColon({ ...(config ?? {}), name: name_ }) : fields.name; },
    indexType(indexType_?: ConfigOf<IndexSignatureColon>['indexType']) { return indexType_ !== undefined ? indexSignatureColon({ ...(config ?? {}), indexType: indexType_ }) : fields.index_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureColonTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function indexSignatureMappedTypeClause(config: ConfigOf<IndexSignatureMappedTypeClause>) {
  const children = config?.children ?? [];
  return {
    type: 'index_signature' as const,
    named: true as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ChildOf<IndexSignatureMappedTypeClause>) { return indexSignatureMappedTypeClause({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureMappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: ConfigOf<ArrayType>) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: ConfigOf<ArrayType>['primaryType']) { return primaryType_ !== undefined ? arrayType({ ...(config ?? {}), primaryType: primaryType_ }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(..._children: ChildOf<TupleType>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TupleType>['children'];
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function readonlyType(child: ChildOf<ReadonlyType>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ReadonlyType>['children'];
  return {
    type: 'readonly_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: ConfigOf<UnionType>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<UnionType>['left']) { return left_ !== undefined ? unionType({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<UnionType>['right']) { return right_ !== undefined ? unionType({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function intersectionType(config: ConfigOf<IntersectionType>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'intersection_type' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<IntersectionType>['left']) { return left_ !== undefined ? intersectionType({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<IntersectionType>['right']) { return right_ !== undefined ? intersectionType({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: ConfigOf<FunctionType>) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: ConfigOf<FunctionType>['typeParameters']) { return typeParameters_ !== undefined ? functionType({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    parameters(parameters_?: ConfigOf<FunctionType>['parameters']) { return parameters_ !== undefined ? functionType({ ...(config ?? {}), parameters: parameters_ }) : fields.parameters; },
    returnType(returnType_?: ConfigOf<FunctionType>['returnType']) { return returnType_ !== undefined ? functionType({ ...(config ?? {}), returnType: returnType_ }) : fields.return_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenTypeIdentifier(child: ChildOf<HiddenTypeIdentifier>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<HiddenTypeIdentifier>['children'];
  return {
    type: '_type_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function automaticSemicolon(text: string) {
  return {
    type: '_automatic_semicolon' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: AutomaticSemicolonTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function templateChars(text: string) {
  return {
    type: '_template_chars' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TemplateCharsTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function ternaryQmark(text: string) {
  return {
    type: '_ternary_qmark' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TernaryQmarkTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function htmlComment(text: string) {
  return {
    type: 'html_comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HtmlCommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function oror(text: string) {
  return {
    type: '||' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: OrorTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxText(text: string) {
  return {
    type: 'jsx_text' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: JsxTextTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function functionSignatureAutomaticSemicolon(text: string) {
  return {
    type: '_function_signature_automatic_semicolon' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FunctionSignatureAutomaticSemicolonTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function errorRecovery(text: string) {
  return {
    type: '__error_recovery' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ErrorRecoveryTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function statementIdentifier(text: string) {
  return {
    type: 'statement_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StatementIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthandPropertyIdentifier(child: ChildOf<ShorthandPropertyIdentifier>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ShorthandPropertyIdentifier>['children'];
  return {
    type: 'shorthand_property_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandPropertyIdentifierPattern(child: ChildOf<ShorthandPropertyIdentifierPattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ShorthandPropertyIdentifierPattern>['children'];
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertyIdentifier(text: string) {
  return {
    type: 'property_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringFragment(text: string) {
  return {
    type: 'string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function interfaceBody(config: ConfigOf<InterfaceBody>) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'interface_body' as const,
    named: true as const,
    fields,
    opening(opening_?: ConfigOf<InterfaceBody>['opening']) { return opening_ !== undefined ? interfaceBody({ ...(config ?? {}), opening: opening_ }) : fields.opening; },
    members(members_?: ConfigOf<InterfaceBody>['members']) { return members_ !== undefined ? interfaceBody({ ...(config ?? {}), members: members_ }) : fields.members; },
    closing(closing_?: ConfigOf<InterfaceBody>['closing']) { return closing_ !== undefined ? interfaceBody({ ...(config ?? {}), closing: closing_ }) : fields.closing; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function thisType() {
  return {
    type: 'this_type' as const,
    named: true as const,
    text: 'this' as const,
    render: () => 'this' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' as const },
    replace: (t: ThisTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' as const }; },
  };
}

export function typeIdentifier(text: string) {
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "program": FluentNode<"program", ConfigOf<Program>>;
  "hash_bang_line": HashBangLine;
  "export_statement": FluentNode<"export_statement", ConfigOf<ExportStatement>>;
  "namespace_export": FluentNode<"namespace_export", ConfigOf<NamespaceExport>>;
  "export_clause": FluentNode<"export_clause", ConfigOf<ExportClause>>;
  "export_specifier": FluentNode<"export_specifier", ConfigOf<ExportSpecifier>>;
  "declaration": FluentNode<"declaration", ConfigOf<Declaration>>;
  "import": Import;
  "import_statement": FluentNode<"import_statement", ConfigOf<ImportStatement>>;
  "import_clause": FluentNode<"import_clause", ConfigOf<ImportClause>>;
  "namespace_import": FluentNode<"namespace_import", ConfigOf<NamespaceImport>>;
  "named_imports": FluentNode<"named_imports", ConfigOf<NamedImports>>;
  "import_specifier": FluentNode<"import_specifier", ConfigOf<ImportSpecifier>>;
  "import_attribute": FluentNode<"import_attribute", ConfigOf<ImportAttribute>>;
  "statement": FluentNode<"statement", ConfigOf<Statement>>;
  "expression_statement": FluentNode<"expression_statement", ConfigOf<ExpressionStatement>>;
  "variable_declaration": FluentNode<"variable_declaration", ConfigOf<VariableDeclaration>>;
  "lexical_declaration": FluentNode<"lexical_declaration", ConfigOf<LexicalDeclaration>>;
  "variable_declarator": FluentNode<"variable_declarator", ConfigOf<VariableDeclarator>>;
  "statement_block": FluentNode<"statement_block", ConfigOf<StatementBlock>>;
  "else_clause": FluentNode<"else_clause", ConfigOf<ElseClause>>;
  "if_statement": FluentNode<"if_statement", ConfigOf<IfStatement>>;
  "switch_statement": FluentNode<"switch_statement", ConfigOf<SwitchStatement>>;
  "for_statement": FluentNode<"for_statement", ConfigOf<ForStatement>>;
  "for_in_statement": FluentNode<"for_in_statement", ConfigOf<ForInStatement>>;
  "while_statement": FluentNode<"while_statement", ConfigOf<WhileStatement>>;
  "do_statement": FluentNode<"do_statement", ConfigOf<DoStatement>>;
  "try_statement": FluentNode<"try_statement", ConfigOf<TryStatement>>;
  "with_statement": FluentNode<"with_statement", ConfigOf<WithStatement>>;
  "break_statement": FluentNode<"break_statement", ConfigOf<BreakStatement>>;
  "continue_statement": FluentNode<"continue_statement", ConfigOf<ContinueStatement>>;
  "debugger_statement": FluentNode<"debugger_statement", ConfigOf<DebuggerStatement>>;
  "return_statement": FluentNode<"return_statement", ConfigOf<ReturnStatement>>;
  "throw_statement": FluentNode<"throw_statement", ConfigOf<ThrowStatement>>;
  "labeled_statement": FluentNode<"labeled_statement", ConfigOf<LabeledStatement>>;
  "switch_body": FluentNode<"switch_body", ConfigOf<SwitchBody>>;
  "switch_case": FluentNode<"switch_case", ConfigOf<SwitchCase>>;
  "switch_default": FluentNode<"switch_default", ConfigOf<SwitchDefault>>;
  "catch_clause": FluentNode<"catch_clause", ConfigOf<CatchClause>>;
  "finally_clause": FluentNode<"finally_clause", ConfigOf<FinallyClause>>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", ConfigOf<ParenthesizedExpression>>;
  "expression": FluentNode<"expression", ConfigOf<Expression>>;
  "primary_expression": FluentNode<"primary_expression", ConfigOf<PrimaryExpression>>;
  "yield_expression": FluentNode<"yield_expression", ConfigOf<YieldExpression>>;
  "object": FluentNode<"object", ConfigOf<Object>>;
  "object_pattern": FluentNode<"object_pattern", ConfigOf<ObjectPattern>>;
  "assignment_pattern": FluentNode<"assignment_pattern", ConfigOf<AssignmentPattern>>;
  "object_assignment_pattern": FluentNode<"object_assignment_pattern", ConfigOf<ObjectAssignmentPattern>>;
  "array": FluentNode<"array", ConfigOf<Array>>;
  "array_pattern": FluentNode<"array_pattern", ConfigOf<ArrayPattern>>;
  "jsx_element": FluentNode<"jsx_element", ConfigOf<JsxElement>>;
  "html_character_reference": HtmlCharacterReference;
  "jsx_expression": FluentNode<"jsx_expression", ConfigOf<JsxExpression>>;
  "jsx_opening_element": FluentNode<"jsx_opening_element", ConfigOf<JsxOpeningElement>>;
  "jsx_identifier": JsxIdentifier;
  "nested_identifier": FluentNode<"nested_identifier", ConfigOf<NestedIdentifier>>;
  "jsx_namespace_name": FluentNode<"jsx_namespace_name", ConfigOf<JsxNamespaceName>>;
  "jsx_closing_element": FluentNode<"jsx_closing_element", ConfigOf<JsxClosingElement>>;
  "jsx_self_closing_element": FluentNode<"jsx_self_closing_element", ConfigOf<JsxSelfClosingElement>>;
  "jsx_attribute": FluentNode<"jsx_attribute", ConfigOf<JsxAttribute>>;
  "_jsx_string": FluentNode<"_jsx_string", ConfigOf<JsxString>>;
  "unescaped_double_jsx_string_fragment": UnescapedDoubleJsxStringFragment;
  "unescaped_single_jsx_string_fragment": UnescapedSingleJsxStringFragment;
  "class": FluentNode<"class", ConfigOf<Class>>;
  "class_declaration": FluentNode<"class_declaration", ConfigOf<ClassDeclaration>>;
  "class_heritage": FluentNode<"class_heritage", ConfigOf<ClassHeritage>>;
  "function_expression": FluentNode<"function_expression", ConfigOf<FunctionExpression>>;
  "function_declaration": FluentNode<"function_declaration", ConfigOf<FunctionDeclaration>>;
  "generator_function": FluentNode<"generator_function", ConfigOf<GeneratorFunction>>;
  "generator_function_declaration": FluentNode<"generator_function_declaration", ConfigOf<GeneratorFunctionDeclaration>>;
  "arrow_function": FluentNode<"arrow_function", ConfigOf<ArrowFunction>>;
  "call_expression": FluentNode<"call_expression", ConfigOf<CallExpression>>;
  "new_expression": FluentNode<"new_expression", ConfigOf<NewExpression>>;
  "await_expression": FluentNode<"await_expression", ConfigOf<AwaitExpression>>;
  "member_expression": FluentNode<"member_expression", ConfigOf<MemberExpression>>;
  "subscript_expression": FluentNode<"subscript_expression", ConfigOf<SubscriptExpression>>;
  "_lhs_expression": FluentNode<"_lhs_expression", ConfigOf<LhsExpression>>;
  "assignment_expression": FluentNode<"assignment_expression", ConfigOf<AssignmentExpression>>;
  "_augmented_assignment_lhs": FluentNode<"_augmented_assignment_lhs", ConfigOf<AugmentedAssignmentLhs>>;
  "augmented_assignment_expression": FluentNode<"augmented_assignment_expression", ConfigOf<AugmentedAssignmentExpression>>;
  "spread_element": FluentNode<"spread_element", ConfigOf<SpreadElement>>;
  "ternary_expression": FluentNode<"ternary_expression", ConfigOf<TernaryExpression>>;
  "binary_expression": FluentNode<"binary_expression", ConfigOf<BinaryExpression>>;
  "unary_expression": FluentNode<"unary_expression", ConfigOf<UnaryExpression>>;
  "update_expression": FluentNode<"update_expression", ConfigOf<UpdateExpression>>;
  "sequence_expression": FluentNode<"sequence_expression", ConfigOf<SequenceExpression>>;
  "string": FluentNode<"string", ConfigOf<String>>;
  "unescaped_double_string_fragment": UnescapedDoubleStringFragment;
  "unescaped_single_string_fragment": UnescapedSingleStringFragment;
  "escape_sequence": EscapeSequence;
  "comment": Comment;
  "template_string": FluentNode<"template_string", ConfigOf<TemplateString>>;
  "template_substitution": FluentNode<"template_substitution", ConfigOf<TemplateSubstitution>>;
  "regex": FluentNode<"regex", ConfigOf<Regex>>;
  "regex_pattern": RegexPattern;
  "regex_flags": RegexFlags;
  "number": Number;
  "identifier": Identifier;
  "private_property_identifier": PrivatePropertyIdentifier;
  "meta_property": MetaProperty;
  "this": This;
  "super": Super;
  "true": True;
  "false": False;
  "null": Null;
  "undefined": Undefined;
  "arguments": FluentNode<"arguments", ConfigOf<Arguments>>;
  "decorator": FluentNode<"decorator", ConfigOf<Decorator>>;
  "decorator_member_expression": FluentNode<"decorator_member_expression", ConfigOf<DecoratorMemberExpression>>;
  "decorator_call_expression": FluentNode<"decorator_call_expression", ConfigOf<DecoratorCallExpression>>;
  "class_body": FluentNode<"class_body", ConfigOf<ClassBody>>;
  "field_definition": FluentNode<"field_definition", ConfigOf<FieldDefinition>>;
  "formal_parameters": FluentNode<"formal_parameters", ConfigOf<FormalParameters>>;
  "class_static_block": FluentNode<"class_static_block", ConfigOf<ClassStaticBlock>>;
  "pattern": FluentNode<"pattern", ConfigOf<Pattern>>;
  "rest_pattern": FluentNode<"rest_pattern", ConfigOf<RestPattern>>;
  "method_definition": FluentNode<"method_definition", ConfigOf<MethodDefinition>>;
  "pair": FluentNode<"pair", ConfigOf<Pair>>;
  "pair_pattern": FluentNode<"pair_pattern", ConfigOf<PairPattern>>;
  "_property_name": FluentNode<"_property_name", ConfigOf<PropertyName>>;
  "computed_property_name": FluentNode<"computed_property_name", ConfigOf<ComputedPropertyName>>;
  "_reserved_identifier": ReservedIdentifier;
  "_semicolon": FluentNode<"_semicolon", ConfigOf<Semicolon>>;
  "public_field_definition": FluentNode<"public_field_definition", ConfigOf<PublicFieldDefinition>>;
  "_jsx_start_opening_element": FluentNode<"_jsx_start_opening_element", ConfigOf<JsxStartOpeningElement>>;
  "_import_identifier": FluentNode<"_import_identifier", ConfigOf<ImportIdentifier>>;
  "non_null_expression": FluentNode<"non_null_expression", ConfigOf<NonNullExpression>>;
  "method_signature": FluentNode<"method_signature", ConfigOf<MethodSignature>>;
  "abstract_method_signature": FluentNode<"abstract_method_signature", ConfigOf<AbstractMethodSignature>>;
  "function_signature": FluentNode<"function_signature", ConfigOf<FunctionSignature>>;
  "decorator_parenthesized_expression": FluentNode<"decorator_parenthesized_expression", ConfigOf<DecoratorParenthesizedExpression>>;
  "type_assertion": FluentNode<"type_assertion", ConfigOf<TypeAssertion>>;
  "as_expression": FluentNode<"as_expression", ConfigOf<AsExpression>>;
  "satisfies_expression": FluentNode<"satisfies_expression", ConfigOf<SatisfiesExpression>>;
  "instantiation_expression": FluentNode<"instantiation_expression", ConfigOf<InstantiationExpression>>;
  "import_require_clause": FluentNode<"import_require_clause", ConfigOf<ImportRequireClause>>;
  "extends_clause": FluentNode<"extends_clause", ConfigOf<ExtendsClause>>;
  "implements_clause": FluentNode<"implements_clause", ConfigOf<ImplementsClause>>;
  "ambient_declaration": FluentNode<"ambient_declaration", ConfigOf<AmbientDeclaration>>;
  "abstract_class_declaration": FluentNode<"abstract_class_declaration", ConfigOf<AbstractClassDeclaration>>;
  "module": FluentNode<"module", ConfigOf<Module>>;
  "internal_module": FluentNode<"internal_module", ConfigOf<InternalModule>>;
  "import_alias": FluentNode<"import_alias", ConfigOf<ImportAlias>>;
  "nested_type_identifier": FluentNode<"nested_type_identifier", ConfigOf<NestedTypeIdentifier>>;
  "interface_declaration": FluentNode<"interface_declaration", ConfigOf<InterfaceDeclaration>>;
  "extends_type_clause": FluentNode<"extends_type_clause", ConfigOf<ExtendsTypeClause>>;
  "enum_declaration": FluentNode<"enum_declaration", ConfigOf<EnumDeclaration>>;
  "enum_body": FluentNode<"enum_body", ConfigOf<EnumBody>>;
  "enum_assignment": FluentNode<"enum_assignment", ConfigOf<EnumAssignment>>;
  "type_alias_declaration": FluentNode<"type_alias_declaration", ConfigOf<TypeAliasDeclaration>>;
  "accessibility_modifier": AccessibilityModifier;
  "override_modifier": OverrideModifier;
  "required_parameter": FluentNode<"required_parameter", ConfigOf<RequiredParameter>>;
  "optional_parameter": FluentNode<"optional_parameter", ConfigOf<OptionalParameter>>;
  "omitting_type_annotation": FluentNode<"omitting_type_annotation", ConfigOf<OmittingTypeAnnotation>>;
  "adding_type_annotation": FluentNode<"adding_type_annotation", ConfigOf<AddingTypeAnnotation>>;
  "opting_type_annotation": FluentNode<"opting_type_annotation", ConfigOf<OptingTypeAnnotation>>;
  "type_annotation": FluentNode<"type_annotation", ConfigOf<TypeAnnotation>>;
  "asserts": FluentNode<"asserts", ConfigOf<Asserts>>;
  "asserts_annotation": FluentNode<"asserts_annotation", ConfigOf<AssertsAnnotation>>;
  "type": FluentNode<"type", ConfigOf<Type>>;
  "tuple_parameter": FluentNode<"tuple_parameter", ConfigOf<TupleParameter>>;
  "optional_tuple_parameter": FluentNode<"optional_tuple_parameter", ConfigOf<OptionalTupleParameter>>;
  "optional_type": FluentNode<"optional_type", ConfigOf<OptionalType>>;
  "rest_type": FluentNode<"rest_type", ConfigOf<RestType>>;
  "constructor_type": FluentNode<"constructor_type", ConfigOf<ConstructorType>>;
  "primary_type": FluentNode<"primary_type", ConfigOf<PrimaryType>>;
  "template_type": FluentNode<"template_type", ConfigOf<TemplateType>>;
  "template_literal_type": FluentNode<"template_literal_type", ConfigOf<TemplateLiteralType>>;
  "infer_type": FluentNode<"infer_type", ConfigOf<InferType>>;
  "conditional_type": FluentNode<"conditional_type", ConfigOf<ConditionalType>>;
  "generic_type": FluentNode<"generic_type", ConfigOf<GenericType>>;
  "type_predicate": FluentNode<"type_predicate", ConfigOf<TypePredicate>>;
  "type_predicate_annotation": FluentNode<"type_predicate_annotation", ConfigOf<TypePredicateAnnotation>>;
  "type_query": FluentNode<"type_query", ConfigOf<TypeQuery>>;
  "index_type_query": FluentNode<"index_type_query", ConfigOf<IndexTypeQuery>>;
  "lookup_type": FluentNode<"lookup_type", ConfigOf<LookupType>>;
  "mapped_type_clause": FluentNode<"mapped_type_clause", ConfigOf<MappedTypeClause>>;
  "literal_type": FluentNode<"literal_type", ConfigOf<LiteralType>>;
  "flow_maybe_type": FluentNode<"flow_maybe_type", ConfigOf<FlowMaybeType>>;
  "parenthesized_type": FluentNode<"parenthesized_type", ConfigOf<ParenthesizedType>>;
  "predefined_type": PredefinedType;
  "type_arguments": FluentNode<"type_arguments", ConfigOf<TypeArguments>>;
  "object_type": FluentNode<"object_type", ConfigOf<ObjectType>>;
  "call_signature": FluentNode<"call_signature", ConfigOf<CallSignature>>;
  "property_signature": FluentNode<"property_signature", ConfigOf<PropertySignature>>;
  "type_parameters": FluentNode<"type_parameters", ConfigOf<TypeParameters>>;
  "type_parameter": FluentNode<"type_parameter", ConfigOf<TypeParameter>>;
  "default_type": FluentNode<"default_type", ConfigOf<DefaultType>>;
  "constraint": FluentNode<"constraint", ConfigOf<Constraint>>;
  "construct_signature": FluentNode<"construct_signature", ConfigOf<ConstructSignature>>;
  "index_signature": FluentNode<"index_signature", ConfigOf<IndexSignature>>;
  "array_type": FluentNode<"array_type", ConfigOf<ArrayType>>;
  "tuple_type": FluentNode<"tuple_type", ConfigOf<TupleType>>;
  "readonly_type": FluentNode<"readonly_type", ConfigOf<ReadonlyType>>;
  "union_type": FluentNode<"union_type", ConfigOf<UnionType>>;
  "intersection_type": FluentNode<"intersection_type", ConfigOf<IntersectionType>>;
  "function_type": FluentNode<"function_type", ConfigOf<FunctionType>>;
  "_type_identifier": FluentNode<"_type_identifier", ConfigOf<HiddenTypeIdentifier>>;
  "_automatic_semicolon": AutomaticSemicolon;
  "_template_chars": TemplateChars;
  "_ternary_qmark": TernaryQmark;
  "html_comment": HtmlComment;
  "||": Oror;
  "jsx_text": JsxText;
  "_function_signature_automatic_semicolon": FunctionSignatureAutomaticSemicolon;
  "__error_recovery": ErrorRecovery;
  "statement_identifier": StatementIdentifier;
  "shorthand_property_identifier": FluentNode<"shorthand_property_identifier", ConfigOf<ShorthandPropertyIdentifier>>;
  "shorthand_property_identifier_pattern": FluentNode<"shorthand_property_identifier_pattern", ConfigOf<ShorthandPropertyIdentifierPattern>>;
  "property_identifier": PropertyIdentifier;
  "string_fragment": StringFragment;
  "interface_body": FluentNode<"interface_body", ConfigOf<InterfaceBody>>;
  "this_type": ThisType;
  "type_identifier": TypeIdentifier;
};

export const _factoryMap = {
  "program": program,
  "hash_bang_line": hashBangLine,
  "export_statement": exportStatement,
  "namespace_export": namespaceExport,
  "export_clause": exportClause,
  "export_specifier": exportSpecifier,
  "declaration": declaration,
  "import": import_,
  "import_statement": importStatement,
  "import_clause": importClause,
  "namespace_import": namespaceImport,
  "named_imports": namedImports,
  "import_specifier": importSpecifier,
  "import_attribute": importAttribute,
  "statement": statement,
  "expression_statement": expressionStatement,
  "variable_declaration": variableDeclaration,
  "lexical_declaration": lexicalDeclaration,
  "variable_declarator": variableDeclarator,
  "statement_block": statementBlock,
  "else_clause": elseClause,
  "if_statement": ifStatement,
  "switch_statement": switchStatement,
  "for_statement": forStatement,
  "for_in_statement": forInStatement,
  "while_statement": whileStatement,
  "do_statement": doStatement,
  "try_statement": tryStatement,
  "with_statement": withStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "debugger_statement": debuggerStatement,
  "return_statement": returnStatement,
  "throw_statement": throwStatement,
  "labeled_statement": labeledStatement,
  "switch_body": switchBody,
  "switch_case": switchCase,
  "switch_default": switchDefault,
  "catch_clause": catchClause,
  "finally_clause": finallyClause,
  "parenthesized_expression": parenthesizedExpression,
  "expression": expression,
  "primary_expression": primaryExpression,
  "yield_expression": yieldExpression,
  "object": object,
  "object_pattern": objectPattern,
  "assignment_pattern": assignmentPattern,
  "object_assignment_pattern": objectAssignmentPattern,
  "array": array,
  "array_pattern": arrayPattern,
  "jsx_element": jsxElement,
  "html_character_reference": htmlCharacterReference,
  "jsx_expression": jsxExpression,
  "jsx_opening_element": jsxOpeningElement,
  "jsx_identifier": jsxIdentifier,
  "nested_identifier": nestedIdentifier,
  "jsx_namespace_name": jsxNamespaceName,
  "jsx_closing_element": jsxClosingElement,
  "jsx_self_closing_element": jsxSelfClosingElement,
  "jsx_attribute": jsxAttribute,
  "_jsx_string": jsxString,
  "unescaped_double_jsx_string_fragment": unescapedDoubleJsxStringFragment,
  "unescaped_single_jsx_string_fragment": unescapedSingleJsxStringFragment,
  "class": class_,
  "class_declaration": classDeclaration,
  "class_heritage": classHeritage,
  "function_expression": functionExpression,
  "function_declaration": functionDeclaration,
  "generator_function": generatorFunction,
  "generator_function_declaration": generatorFunctionDeclaration,
  "arrow_function": arrowFunction,
  "call_expression": callExpression,
  "new_expression": newExpression,
  "await_expression": awaitExpression,
  "member_expression": memberExpression,
  "subscript_expression": subscriptExpression,
  "_lhs_expression": lhsExpression,
  "assignment_expression": assignmentExpression,
  "_augmented_assignment_lhs": augmentedAssignmentLhs,
  "augmented_assignment_expression": augmentedAssignmentExpression,
  "spread_element": spreadElement,
  "ternary_expression": ternaryExpression,
  "binary_expression": binaryExpression,
  "unary_expression": unaryExpression,
  "update_expression": updateExpression,
  "sequence_expression": sequenceExpression,
  "string": string,
  "unescaped_double_string_fragment": unescapedDoubleStringFragment,
  "unescaped_single_string_fragment": unescapedSingleStringFragment,
  "escape_sequence": escapeSequence,
  "comment": comment,
  "template_string": templateString,
  "template_substitution": templateSubstitution,
  "regex": regex,
  "regex_pattern": regexPattern,
  "regex_flags": regexFlags,
  "number": number,
  "identifier": identifier,
  "private_property_identifier": privatePropertyIdentifier,
  "meta_property": metaProperty,
  "this": this_,
  "super": super_,
  "true": true_,
  "false": false_,
  "null": null_,
  "undefined": undefined_,
  "arguments": arguments_,
  "decorator": decorator,
  "decorator_member_expression": decoratorMemberExpression,
  "decorator_call_expression": decoratorCallExpression,
  "class_body": classBody,
  "field_definition": fieldDefinition,
  "formal_parameters": formalParameters,
  "class_static_block": classStaticBlock,
  "pattern": pattern,
  "rest_pattern": restPattern,
  "method_definition": methodDefinition,
  "pair": pair,
  "pair_pattern": pairPattern,
  "_property_name": propertyName,
  "computed_property_name": computedPropertyName,
  "_reserved_identifier": reservedIdentifier,
  "_semicolon": semicolon,
  "public_field_definition": publicFieldDefinition,
  "_jsx_start_opening_element": jsxStartOpeningElement,
  "_import_identifier": importIdentifier,
  "non_null_expression": nonNullExpression,
  "method_signature": methodSignature,
  "abstract_method_signature": abstractMethodSignature,
  "function_signature": functionSignature,
  "decorator_parenthesized_expression": decoratorParenthesizedExpression,
  "type_assertion": typeAssertion,
  "as_expression": asExpression,
  "satisfies_expression": satisfiesExpression,
  "instantiation_expression": instantiationExpression,
  "import_require_clause": importRequireClause,
  "extends_clause": extendsClause,
  "implements_clause": implementsClause,
  "ambient_declaration": ambientDeclaration,
  "abstract_class_declaration": abstractClassDeclaration,
  "module": module,
  "internal_module": internalModule,
  "import_alias": importAlias,
  "nested_type_identifier": nestedTypeIdentifier,
  "interface_declaration": interfaceDeclaration,
  "extends_type_clause": extendsTypeClause,
  "enum_declaration": enumDeclaration,
  "enum_body": enumBody,
  "enum_assignment": enumAssignment,
  "type_alias_declaration": typeAliasDeclaration,
  "accessibility_modifier": accessibilityModifier,
  "override_modifier": overrideModifier,
  "required_parameter": requiredParameter,
  "optional_parameter": optionalParameter,
  "omitting_type_annotation": omittingTypeAnnotation,
  "adding_type_annotation": addingTypeAnnotation,
  "opting_type_annotation": optingTypeAnnotation,
  "type_annotation": typeAnnotation,
  "asserts": asserts,
  "asserts_annotation": assertsAnnotation,
  "type": type,
  "tuple_parameter": tupleParameter,
  "optional_tuple_parameter": optionalTupleParameter,
  "optional_type": optionalType,
  "rest_type": restType,
  "constructor_type": constructorType,
  "primary_type": primaryType,
  "template_type": templateType,
  "template_literal_type": templateLiteralType,
  "infer_type": inferType,
  "conditional_type": conditionalType,
  "generic_type": genericType,
  "type_predicate": typePredicate,
  "type_predicate_annotation": typePredicateAnnotation,
  "type_query": typeQuery,
  "index_type_query": indexTypeQuery,
  "lookup_type": lookupType,
  "mapped_type_clause": mappedTypeClause,
  "literal_type": literalType,
  "flow_maybe_type": flowMaybeType,
  "parenthesized_type": parenthesizedType,
  "predefined_type": predefinedType,
  "type_arguments": typeArguments,
  "object_type": objectType,
  "call_signature": callSignature,
  "property_signature": propertySignature,
  "type_parameters": typeParameters,
  "type_parameter": typeParameter,
  "default_type": defaultType,
  "constraint": constraint,
  "construct_signature": constructSignature,
  "index_signature": indexSignature,
  "array_type": arrayType,
  "tuple_type": tupleType,
  "readonly_type": readonlyType,
  "union_type": unionType,
  "intersection_type": intersectionType,
  "function_type": functionType,
  "_type_identifier": hiddenTypeIdentifier,
  "_automatic_semicolon": automaticSemicolon,
  "_template_chars": templateChars,
  "_ternary_qmark": ternaryQmark,
  "html_comment": htmlComment,
  "||": oror,
  "jsx_text": jsxText,
  "_function_signature_automatic_semicolon": functionSignatureAutomaticSemicolon,
  "__error_recovery": errorRecovery,
  "statement_identifier": statementIdentifier,
  "shorthand_property_identifier": shorthandPropertyIdentifier,
  "shorthand_property_identifier_pattern": shorthandPropertyIdentifierPattern,
  "property_identifier": propertyIdentifier,
  "string_fragment": stringFragment,
  "interface_body": interfaceBody,
  "this_type": thisType,
  "type_identifier": typeIdentifier,
} as const;
export type _FactoryMap = typeof _factoryMap;

// Auto-generated by @sittir/codegen — do not edit

import type * as T from './types.js';
import type { AnyNodeData, ByteRange, ConfigOf, Edit, FluentNode, NonEmptyArray } from '@sittir/types';
import { render, toEdit } from './boundary.ts';

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const _leafRe_hashBangLine = /^(?:#!.*)/u;
const _leafRe_htmlCharacterReference = /^(?:&(#([xX][0-9a-fA-F]{1,6}|[0-9]{1,5})|[A-Za-z]{1,30});)/u;
const _leafRe_jsxIdentifier = /^(?:[a-zA-Z_$][a-zA-Z\d_$]*-[a-zA-Z\d_$-]*)/u;
const _leafRe_unescapedDoubleJsxStringFragment = /^(?:([^"&]|&[^#A-Za-z])+)/u;
const _leafRe_unescapedSingleJsxStringFragment = /^(?:([^'&]|&[^#A-Za-z])+)/u;
const _leafRe_unescapedDoubleStringFragment = /^(?:[^"\\\r\n]+)/u;
const _leafRe_unescapedSingleStringFragment = /^(?:[^'\\\r\n]+)/u;
const _leafRe_regexFlags = /^(?:[a-z]+)/u;

export function program(config: ConfigOf<T.Program>) {
  const fields = {
    hash_bang_line: config.hashBangLine,
    statements: config.statements,
  };
  return {
    $type: 'program' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    hashBangLine(value?: T.HashBangLine | undefined) { return _fs(config, program, 'hashBangLine', value, config?.hashBangLine); },
    statements(...values: T.Statement[]) { return _fsm(config, program, 'statements', values, config?.statements); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ProgramTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function hashBangLine(text: string) {
  if (text.length === 0) throw new Error(`hash_bang_line: text must be non-empty`); if (!_leafRe_hashBangLine.test(text)) throw new Error(`hash_bang_line: text does not match pattern: ${text}`);
  return {
    $type: 'hash_bang_line' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HashBangLineTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function exportStatement(config: ConfigOf<T.ExportStatementUFormDefault>): ReturnType<typeof exportStatementUFormDefault>;
export function exportStatement(config: ConfigOf<T.ExportStatementUFormTypeExport>): ReturnType<typeof exportStatementUFormTypeExport>;
export function exportStatement(config: ConfigOf<T.ExportStatementUFormEqualsExport>): ReturnType<typeof exportStatementUFormEqualsExport>;
export function exportStatement(config: ConfigOf<T.ExportStatementUFormNamespaceExport>): ReturnType<typeof exportStatementUFormNamespaceExport>;
export function exportStatement(config: ConfigOf<T.ExportStatementUFormDefault> | ConfigOf<T.ExportStatementUFormTypeExport> | ConfigOf<T.ExportStatementUFormEqualsExport> | ConfigOf<T.ExportStatementUFormNamespaceExport>) {
  switch (config.$variant) {
    case 'default': return exportStatementUFormDefault(config as Parameters<typeof exportStatementUFormDefault>[0]);
    case 'type_export': return exportStatementUFormTypeExport(config as Parameters<typeof exportStatementUFormTypeExport>[0]);
    case 'equals_export': return exportStatementUFormEqualsExport(config as Parameters<typeof exportStatementUFormEqualsExport>[0]);
    case 'namespace_export': return exportStatementUFormNamespaceExport(config as Parameters<typeof exportStatementUFormNamespaceExport>[0]);
  }
  throw new Error(`exportStatement: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'default' | 'type_export' | 'equals_export' | 'namespace_export'.`);
}
export function exportStatementUFormDefault(config: Omit<ConfigOf<T.ExportStatementUFormDefault>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'export_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'default' as const,
    $children: children,
    child(value?: T.ExportStatementDefault) {
      if (value === undefined) return children[0];
      return exportStatementUFormDefault({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementUFormDefaultTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementUFormTypeExport(config?: Omit<ConfigOf<T.ExportStatementUFormTypeExport>, '$variant'>) {
  const inner = exportStatementTypeExport(config as Parameters<typeof exportStatementTypeExport>[0]);
  const children = [inner] as const;
  return {
    $type: 'export_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'type_export' as const,
    $children: children,
    source(value?: T.String | undefined) {
      if (value === undefined) return inner.$fields.source;
      return exportStatementUFormTypeExport({ source: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementUFormTypeExportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementUFormEqualsExport(config?: Omit<ConfigOf<T.ExportStatementUFormEqualsExport>, '$variant'>) {
  const inner = exportStatementEqualsExport((config?.children ?? [])[0] as Parameters<typeof exportStatementEqualsExport>[0]);
  const children = [inner] as const;
  return {
    $type: 'export_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'equals_export' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementUFormEqualsExportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementUFormNamespaceExport(config?: Omit<ConfigOf<T.ExportStatementUFormNamespaceExport>, '$variant'>) {
  const inner = exportStatementNamespaceExport((config?.children ?? [])[0] as Parameters<typeof exportStatementNamespaceExport>[0]);
  const children = [inner] as const;
  return {
    $type: 'export_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'namespace_export' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementUFormNamespaceExportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceExport(child: T.ModuleExportName) {
  const children = [child];
  return {
    $type: 'namespace_export' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.NamespaceExportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportClause(...children: T.ExportSpecifier[]) {
  return {
    $type: 'export_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportSpecifier(config: ConfigOf<T.ExportSpecifier>) {
  const fields = {
    name: config.name,
    alias: config.alias,
  };
  return {
    $type: 'export_specifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.ModuleExportName) { return _fs(config, exportSpecifier, 'name', value, config?.name); },
    alias(value?: T.ModuleExportName | undefined) { return _fs(config, exportSpecifier, 'alias', value, config?.alias); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportSpecifierTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_() {
  return {
    $type: 'import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'import' as const,
    render: () => 'import' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'import' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'import' as const },
    replace: (t: T.ImportTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' as const }; },
  };
}

export function importStatement(config: ConfigOf<T.ImportStatement>) {
  const fields = {
    import_clause: config.importClause,
    from_clause: config.fromClause,
    import_attribute: config.importAttribute,
    semicolon: config.semicolon,
  };
  return {
    $type: 'import_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    importClause(value?: "type" | "typeof" | undefined) { return _fs(config, importStatement, 'importClause', value, config?.importClause); },
    fromClause(value?: T.ImportClause | "from" | T.String | T.ImportRequireClause) { return _fs(config, importStatement, 'fromClause', value, config?.fromClause); },
    importAttribute(value?: T.ImportAttribute | undefined) { return _fs(config, importStatement, 'importAttribute', value, config?.importAttribute); },
    semicolon(value?: T.Semicolon) { return _fs(config, importStatement, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClause(config: ConfigOf<T.ImportClauseUFormNamespaceImport>): ReturnType<typeof importClauseUFormNamespaceImport>;
export function importClause(config: ConfigOf<T.ImportClauseUFormNamedImports>): ReturnType<typeof importClauseUFormNamedImports>;
export function importClause(config: ConfigOf<T.ImportClauseUFormDefaultImport>): ReturnType<typeof importClauseUFormDefaultImport>;
export function importClause(config: ConfigOf<T.ImportClauseUFormNamespaceImport> | ConfigOf<T.ImportClauseUFormNamedImports> | ConfigOf<T.ImportClauseUFormDefaultImport>) {
  switch (config.$variant) {
    case 'namespace_import': return importClauseUFormNamespaceImport(config as Parameters<typeof importClauseUFormNamespaceImport>[0]);
    case 'named_imports': return importClauseUFormNamedImports(config as Parameters<typeof importClauseUFormNamedImports>[0]);
    case 'default_import': return importClauseUFormDefaultImport(config as Parameters<typeof importClauseUFormDefaultImport>[0]);
  }
  throw new Error(`importClause: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'namespace_import' | 'named_imports' | 'default_import'.`);
}
export function importClauseUFormNamespaceImport(config?: Omit<ConfigOf<T.ImportClauseUFormNamespaceImport>, '$variant'>) {
  const inner = importClauseNamespaceImport((config?.children ?? [])[0] as Parameters<typeof importClauseNamespaceImport>[0]);
  const children = [inner] as const;
  return {
    $type: 'import_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'namespace_import' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportClauseUFormNamespaceImportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseUFormNamedImports(config?: Omit<ConfigOf<T.ImportClauseUFormNamedImports>, '$variant'>) {
  const inner = importClauseNamedImports((config?.children ?? [])[0] as Parameters<typeof importClauseNamedImports>[0]);
  const children = [inner] as const;
  return {
    $type: 'import_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'named_imports' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportClauseUFormNamedImportsTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseUFormDefaultImport(config?: Omit<ConfigOf<T.ImportClauseUFormDefaultImport>, '$variant'>) {
  const inner = importClauseDefaultImport((config?.children ?? [])[0] as Parameters<typeof importClauseDefaultImport>[0]);
  const children = [inner] as const;
  return {
    $type: 'import_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'default_import' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportClauseUFormDefaultImportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceImport(config: ConfigOf<T.NamespaceImport>) {
  const fields = {
    identifier: config.identifier,
  };
  return {
    $type: 'namespace_import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    identifier(value?: T.Identifier) { return _fs(config, namespaceImport, 'identifier', value, config?.identifier); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.NamespaceImportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedImports(...children: T.ImportSpecifier[]) {
  return {
    $type: 'named_imports' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.NamedImportsTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifier(config: ConfigOf<T.ImportSpecifierUFormName>): ReturnType<typeof importSpecifierUFormName>;
export function importSpecifier(config: ConfigOf<T.ImportSpecifierUFormAs>): ReturnType<typeof importSpecifierUFormAs>;
export function importSpecifier(config: ConfigOf<T.ImportSpecifierUFormName> | ConfigOf<T.ImportSpecifierUFormAs>) {
  switch (config.$variant) {
    case 'name': return importSpecifierUFormName(config as Parameters<typeof importSpecifierUFormName>[0]);
    case 'as': return importSpecifierUFormAs(config as Parameters<typeof importSpecifierUFormAs>[0]);
  }
  throw new Error(`importSpecifier: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'name' | 'as'.`);
}
export function importSpecifierUFormName(config: Omit<ConfigOf<T.ImportSpecifierUFormName>, '$variant'>) {
  const inner = importSpecifierName(config);
  const children = [inner] as const;
  return {
    $type: 'import_specifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'name' as const,
    $children: children,
    name(value?: T.ImportIdentifier) {
      if (value === undefined) return inner.$fields.name;
      return importSpecifierUFormName({ name: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportSpecifierUFormNameTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function importSpecifierUFormAs(config: Omit<ConfigOf<T.ImportSpecifierUFormAs>, '$variant'>) {
  const inner = {
    $type: 'import_specifier_as' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: {
      name: config.name,
      alias: config.alias,
    },
  };
  const children = [inner] as const;
  return {
    $type: 'import_specifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'as' as const,
    $children: children,
    name(value?: T.ModuleExportName | T.Identifier) {
      if (value === undefined) return inner.$fields.name;
      return importSpecifierUFormAs({ alias: inner.$fields.alias, name: value });
    },
    alias(value?: T.ImportIdentifier) {
      if (value === undefined) return inner.$fields.alias;
      return importSpecifierUFormAs({ name: inner.$fields.name, alias: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportSpecifierUFormAsTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAttribute(config: ConfigOf<T.ImportAttribute>) {
  const fields = {
    object: config.object,
  };
  return {
    $type: 'import_attribute' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: "with" | "assert" | T.Object) { return _fs(config, importAttribute, 'object', value, config?.object); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportAttributeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(config: ConfigOf<T.ExpressionStatement>) {
  const fields = {
    semicolon: config.semicolon,
  };
  const children = config.children ?? [];
  return {
    $type: 'expression_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    semicolon(value?: T.Semicolon) { return _fs(config, expressionStatement, 'semicolon', value, config?.semicolon); },
    child(value?: T.Expressions) {
      if (value === undefined) return children[0];
      return expressionStatement({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExpressionStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclaration(config: ConfigOf<T.VariableDeclaration>) {
  const fields = {
    declarators: config.declarators,
    semicolon: config.semicolon,
  };
  return {
    $type: 'variable_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    declarators(...values: NonEmptyArray<T.VariableDeclarator>) { return _fsm(config, variableDeclaration, 'declarators', values, config?.declarators); },
    semicolon(value?: T.Semicolon) { return _fs(config, variableDeclaration, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.VariableDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function lexicalDeclaration(config: ConfigOf<T.LexicalDeclaration>) {
  const fields = {
    kind: config.kind,
    declarators: config.declarators,
    semicolon: config.semicolon,
  };
  return {
    $type: 'lexical_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    kind(value?: "let" | "const") { return _fs(config, lexicalDeclaration, 'kind', value, config?.kind); },
    declarators(...values: NonEmptyArray<T.VariableDeclarator>) { return _fsm(config, lexicalDeclaration, 'declarators', values, config?.declarators); },
    semicolon(value?: T.Semicolon) { return _fs(config, lexicalDeclaration, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.LexicalDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclarator(config: ConfigOf<T.VariableDeclarator>) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  return {
    $type: 'variable_declarator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier | T.DestructuringPattern) { return _fs(config, variableDeclarator, 'name', value, config?.name); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, variableDeclarator, 'type', value, config?.type); },
    value(value?: T.Expression | undefined) { return _fs(config, variableDeclarator, 'value', value, config?.value); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.VariableDeclaratorTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function statementBlock(config: ConfigOf<T.StatementBlock>) {
  const fields = {
    statements: config.statements,
    automatic_semicolon: config.automaticSemicolon,
  };
  return {
    $type: 'statement_block' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    statements(...values: T.Statement[]) { return _fsm(config, statementBlock, 'statements', values, config?.statements); },
    automaticSemicolon(value?: T.AutomaticSemicolon | undefined) { return _fs(config, statementBlock, 'automaticSemicolon', value, config?.automaticSemicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.StatementBlockTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: ConfigOf<T.ElseClause>) {
  const fields = {
    statement: config.statement,
  };
  return {
    $type: 'else_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    statement(value?: T.Statement) { return _fs(config, elseClause, 'statement', value, config?.statement); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ElseClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifStatement(config: ConfigOf<T.IfStatement>) {
  const fields = {
    condition: config.condition,
    consequence: config.consequence,
    alternative: config.alternative,
  };
  return {
    $type: 'if_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    condition(value?: T.ParenthesizedExpression) { return _fs(config, ifStatement, 'condition', value, config?.condition); },
    consequence(value?: T.Statement) { return _fs(config, ifStatement, 'consequence', value, config?.consequence); },
    alternative(value?: T.ElseClause | undefined) { return _fs(config, ifStatement, 'alternative', value, config?.alternative); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.IfStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchStatement(config: ConfigOf<T.SwitchStatement>) {
  const fields = {
    value: config.value,
    body: config.body,
  };
  return {
    $type: 'switch_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    value(value?: T.ParenthesizedExpression) { return _fs(config, switchStatement, 'value', value, config?.value); },
    body(value?: T.SwitchBody) { return _fs(config, switchStatement, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.SwitchStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: ConfigOf<T.ForStatement>) {
  const fields = {
    initializer: config.initializer,
    condition: config.condition,
    increment: config.increment,
    body: config.body,
  };
  return {
    $type: 'for_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    initializer(value?: T.LexicalDeclaration | T.VariableDeclaration | T.Expressions | T.EmptyStatement) { return _fs(config, forStatement, 'initializer', value, config?.initializer); },
    condition(value?: T.Expressions | T.EmptyStatement) { return _fs(config, forStatement, 'condition', value, config?.condition); },
    increment(value?: T.Expressions | undefined) { return _fs(config, forStatement, 'increment', value, config?.increment); },
    body(value?: T.Statement) { return _fs(config, forStatement, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ForStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInStatement(config: ConfigOf<T.ForInStatement>) {
  const fields = {
    await: config.await ? "await" as const : undefined,
    operator: config.operator,
    right: config.right,
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'for_in_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    await(value?: "await" | undefined) { return _fs(config, forInStatement, 'await', value, config?.await); },
    operator(value?: "in" | "of") { return _fs(config, forInStatement, 'operator', value, config?.operator); },
    right(value?: T.Expressions) { return _fs(config, forInStatement, 'right', value, config?.right); },
    body(value?: T.Statement) { return _fs(config, forInStatement, 'body', value, config?.body); },
    child(value?: (T.ForHeaderLhs | T.ForHeaderVarKind | T.ForHeaderLetConstKind)) {
      if (value === undefined) return children[0];
      return forInStatement({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ForInStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: ConfigOf<T.WhileStatement>) {
  const fields = {
    condition: config.condition,
    body: config.body,
  };
  return {
    $type: 'while_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    condition(value?: T.ParenthesizedExpression) { return _fs(config, whileStatement, 'condition', value, config?.condition); },
    body(value?: T.Statement) { return _fs(config, whileStatement, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.WhileStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function doStatement(config: ConfigOf<T.DoStatement>) {
  const fields = {
    body: config.body,
    condition: config.condition,
    semicolon: config.semicolon,
  };
  return {
    $type: 'do_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(value?: T.Statement) { return _fs(config, doStatement, 'body', value, config?.body); },
    condition(value?: T.ParenthesizedExpression) { return _fs(config, doStatement, 'condition', value, config?.condition); },
    semicolon(value?: T.Semicolon | undefined) { return _fs(config, doStatement, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.DoStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: ConfigOf<T.TryStatement>) {
  const fields = {
    body: config.body,
    handler: config.handler,
    finalizer: config.finalizer,
  };
  return {
    $type: 'try_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(value?: T.StatementBlock) { return _fs(config, tryStatement, 'body', value, config?.body); },
    handler(value?: T.CatchClause | undefined) { return _fs(config, tryStatement, 'handler', value, config?.handler); },
    finalizer(value?: T.FinallyClause | undefined) { return _fs(config, tryStatement, 'finalizer', value, config?.finalizer); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TryStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: ConfigOf<T.WithStatement>) {
  const fields = {
    object: config.object,
    body: config.body,
  };
  return {
    $type: 'with_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.ParenthesizedExpression) { return _fs(config, withStatement, 'object', value, config?.object); },
    body(value?: T.Statement) { return _fs(config, withStatement, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.WithStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakStatement(config: ConfigOf<T.BreakStatement>) {
  const fields = {
    label: config.label,
    semicolon: config.semicolon,
  };
  return {
    $type: 'break_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    label(value?: T.Identifier | undefined) { return _fs(config, breakStatement, 'label', value, config?.label); },
    semicolon(value?: T.Semicolon) { return _fs(config, breakStatement, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.BreakStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueStatement(config: ConfigOf<T.ContinueStatement>) {
  const fields = {
    label: config.label,
    semicolon: config.semicolon,
  };
  return {
    $type: 'continue_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    label(value?: T.Identifier | undefined) { return _fs(config, continueStatement, 'label', value, config?.label); },
    semicolon(value?: T.Semicolon) { return _fs(config, continueStatement, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ContinueStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function debuggerStatement(config: ConfigOf<T.DebuggerStatement>) {
  const fields = {
    semicolon: config.semicolon,
  };
  return {
    $type: 'debugger_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    semicolon(value?: T.Semicolon) { return _fs(config, debuggerStatement, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.DebuggerStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(config: ConfigOf<T.ReturnStatement>) {
  const fields = {
    semicolon: config.semicolon,
  };
  const children = config.children ?? [];
  return {
    $type: 'return_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    semicolon(value?: T.Semicolon) { return _fs(config, returnStatement, 'semicolon', value, config?.semicolon); },
    child(value?: T.Expressions) {
      if (value === undefined) return children[0];
      return returnStatement({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ReturnStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function throwStatement(config: ConfigOf<T.ThrowStatement>) {
  const fields = {
    semicolon: config.semicolon,
  };
  const children = config.children ?? [];
  return {
    $type: 'throw_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    semicolon(value?: T.Semicolon) { return _fs(config, throwStatement, 'semicolon', value, config?.semicolon); },
    child(value?: T.Expressions) {
      if (value === undefined) return children[0];
      return throwStatement({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ThrowStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function labeledStatement(config: ConfigOf<T.LabeledStatement>) {
  const fields = {
    label: config.label,
    body: config.body,
  };
  return {
    $type: 'labeled_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    label(value?: T.StatementIdentifier) { return _fs(config, labeledStatement, 'label', value, config?.label); },
    body(value?: T.Statement) { return _fs(config, labeledStatement, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.LabeledStatementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchBody(...children: (T.SwitchCase | T.SwitchDefault)[]) {
  return {
    $type: 'switch_body' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.SwitchBodyTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchCase(config: ConfigOf<T.SwitchCase>) {
  const fields = {
    value: config.value,
    body: config.body,
  };
  return {
    $type: 'switch_case' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    value(value?: T.Expressions) { return _fs(config, switchCase, 'value', value, config?.value); },
    body(...values: T.Statement[]) { return _fsm(config, switchCase, 'body', values, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.SwitchCaseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchDefault(config: ConfigOf<T.SwitchDefault>) {
  const fields = {
    body: config.body,
  };
  return {
    $type: 'switch_default' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(...values: T.Statement[]) { return _fsm(config, switchDefault, 'body', values, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.SwitchDefaultTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function catchClause(config: ConfigOf<T.CatchClause>) {
  const fields = {
    parameter: config.parameter,
    type: config.type,
    body: config.body,
  };
  return {
    $type: 'catch_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    parameter(value?: T.Identifier | T.DestructuringPattern | undefined) { return _fs(config, catchClause, 'parameter', value, config?.parameter); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, catchClause, 'type', value, config?.type); },
    body(value?: T.StatementBlock) { return _fs(config, catchClause, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.CatchClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: ConfigOf<T.FinallyClause>) {
  const fields = {
    body: config.body,
  };
  return {
    $type: 'finally_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(value?: T.StatementBlock) { return _fs(config, finallyClause, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.FinallyClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(config: ConfigOf<T.ParenthesizedExpressionUFormTyped>): ReturnType<typeof parenthesizedExpressionUFormTyped>;
export function parenthesizedExpression(config: ConfigOf<T.ParenthesizedExpressionUFormSequence>): ReturnType<typeof parenthesizedExpressionUFormSequence>;
export function parenthesizedExpression(config: ConfigOf<T.ParenthesizedExpressionUFormTyped> | ConfigOf<T.ParenthesizedExpressionUFormSequence>) {
  switch (config.$variant) {
    case 'typed': return parenthesizedExpressionUFormTyped(config as Parameters<typeof parenthesizedExpressionUFormTyped>[0]);
    case 'sequence': return parenthesizedExpressionUFormSequence(config as Parameters<typeof parenthesizedExpressionUFormSequence>[0]);
  }
  throw new Error(`parenthesizedExpression: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'typed' | 'sequence'.`);
}
export function parenthesizedExpressionUFormTyped(config?: Omit<ConfigOf<T.ParenthesizedExpressionUFormTyped>, '$variant'>) {
  const inner = {
    $type: 'parenthesized_expression_typed' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: {
      type: config?.type,
    },
  };
  const children = [inner] as const;
  return {
    $type: 'parenthesized_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'typed' as const,
    $children: children,
    typeField(value?: T.TypeAnnotation | undefined) {
      if (value === undefined) return inner.$fields.type;
      return parenthesizedExpressionUFormTyped({ type: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ParenthesizedExpressionUFormTypedTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function parenthesizedExpressionUFormSequence(config?: Omit<ConfigOf<T.ParenthesizedExpressionUFormSequence>, '$variant'>) {
  const inner = parenthesizedExpressionSequence((config?.children ?? [])[0] as Parameters<typeof parenthesizedExpressionSequence>[0]);
  const children = [inner] as const;
  return {
    $type: 'parenthesized_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'sequence' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ParenthesizedExpressionUFormSequenceTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(config?: ConfigOf<T.YieldExpression>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    $type: 'yield_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression | undefined) { return _fs(config, yieldExpression, 'expression', value, config?.expression); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.YieldExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function object(...children: (T.Pair | T.SpreadElement | T.MethodDefinition | T.ShorthandPropertyIdentifier)[]) {
  return {
    $type: 'object' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ObjectTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectPattern(...children: (T.PairPattern | T.RestPattern | T.ObjectAssignmentPattern | T.ShorthandPropertyIdentifierPattern)[]) {
  return {
    $type: 'object_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ObjectPatternTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentPattern(config: ConfigOf<T.AssignmentPattern>) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'assignment_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Pattern) { return _fs(config, assignmentPattern, 'left', value, config?.left); },
    right(value?: T.Expression) { return _fs(config, assignmentPattern, 'right', value, config?.right); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AssignmentPatternTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectAssignmentPattern(config: ConfigOf<T.ObjectAssignmentPattern>) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'object_assignment_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.ShorthandPropertyIdentifierPattern | T.DestructuringPattern) { return _fs(config, objectAssignmentPattern, 'left', value, config?.left); },
    right(value?: T.Expression) { return _fs(config, objectAssignmentPattern, 'right', value, config?.right); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ObjectAssignmentPatternTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function array(...children: (T.Expression | T.SpreadElement)[]) {
  return {
    $type: 'array' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ArrayTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayPattern(...children: (T.Pattern | T.AssignmentPattern)[]) {
  return {
    $type: 'array_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ArrayPatternTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxElement(config: ConfigOf<T.JsxElement>) {
  const fields = {
    open_tag: config.openTag,
    close_tag: config.closeTag,
  };
  const children = config.children ?? [];
  return {
    $type: 'jsx_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    openTag(value?: T.JsxOpeningElement) { return _fs(config, jsxElement, 'openTag', value, config?.openTag); },
    closeTag(value?: T.JsxClosingElement) { return _fs(config, jsxElement, 'closeTag', value, config?.closeTag); },
    children(...items: T.JsxChild[]) {
      if (items.length === 0) return children;
      return jsxElement({ ...config, children: items });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.JsxElementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlCharacterReference(text: string) {
  if (text.length === 0) throw new Error(`html_character_reference: text must be non-empty`); if (!_leafRe_htmlCharacterReference.test(text)) throw new Error(`html_character_reference: text does not match pattern: ${text}`);
  return {
    $type: 'html_character_reference' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HtmlCharacterReferenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxExpression(child?: (T.Expression | T.SequenceExpression | T.SpreadElement)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'jsx_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.JsxExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxOpeningElement(config: ConfigOf<T.JsxOpeningElement>) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
    attribute: config.attribute,
  };
  return {
    $type: 'jsx_opening_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T._JsxIdentifier | T.JsxNamespaceName | T.Identifier | T.NestedIdentifier | undefined) { return _fs(config, jsxOpeningElement, 'name', value, config?.name); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, jsxOpeningElement, 'typeArguments', value, config?.typeArguments); },
    attribute(...values: T._JsxAttribute[]) { return _fsm(config, jsxOpeningElement, 'attribute', values, config?.attribute); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.JsxOpeningElementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxIdentifier(text: string) {
  if (text.length === 0) throw new Error(`jsx_identifier: text must be non-empty`); if (!_leafRe_jsxIdentifier.test(text)) throw new Error(`jsx_identifier: text does not match pattern: ${text}`);
  return {
    $type: 'jsx_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.JsxIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function nestedIdentifier(config: ConfigOf<T.NestedIdentifier>) {
  const fields = {
    object: config.object,
    property: config.property,
  };
  return {
    $type: 'nested_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.Identifier | T.NestedIdentifier) { return _fs(config, nestedIdentifier, 'object', value, config?.object); },
    property(value?: T.Identifier) { return _fs(config, nestedIdentifier, 'property', value, config?.property); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.NestedIdentifierTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxNamespaceName(...children: T._JsxIdentifier[]) {
  return {
    $type: 'jsx_namespace_name' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.JsxNamespaceNameTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxClosingElement(config?: ConfigOf<T.JsxClosingElement>) {
  const fields = {
    name: config?.name,
  };
  return {
    $type: 'jsx_closing_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.JsxElementName | undefined) { return _fs(config, jsxClosingElement, 'name', value, config?.name); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.JsxClosingElementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxSelfClosingElement(config: ConfigOf<T.JsxSelfClosingElement>) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
    attribute: config.attribute,
  };
  return {
    $type: 'jsx_self_closing_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T._JsxIdentifier | T.JsxNamespaceName | T.Identifier | T.NestedIdentifier | undefined) { return _fs(config, jsxSelfClosingElement, 'name', value, config?.name); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, jsxSelfClosingElement, 'typeArguments', value, config?.typeArguments); },
    attribute(...values: T._JsxAttribute[]) { return _fsm(config, jsxSelfClosingElement, 'attribute', values, config?.attribute); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.JsxSelfClosingElementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxAttribute(child: (T.JsxAttributeName | T.JsxAttributeValue)) {
  const children = [child];
  return {
    $type: 'jsx_attribute' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.JsxAttributeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleJsxStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_double_jsx_string_fragment: text must be non-empty`); if (!_leafRe_unescapedDoubleJsxStringFragment.test(text)) throw new Error(`unescaped_double_jsx_string_fragment: text does not match pattern: ${text}`);
  return {
    $type: 'unescaped_double_jsx_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedDoubleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleJsxStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_single_jsx_string_fragment: text must be non-empty`); if (!_leafRe_unescapedSingleJsxStringFragment.test(text)) throw new Error(`unescaped_single_jsx_string_fragment: text does not match pattern: ${text}`);
  return {
    $type: 'unescaped_single_jsx_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedSingleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function class_(config: ConfigOf<T.Class>) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
    body: config.body,
  };
  return {
    $type: 'class' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    decorator(...values: T.Decorator[]) { return _fsm(config, class_, 'decorator', values, config?.decorator); },
    name(value?: T.TypeIdentifier | undefined) { return _fs(config, class_, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, class_, 'typeParameters', value, config?.typeParameters); },
    classHeritage(value?: T.ClassHeritage | undefined) { return _fs(config, class_, 'classHeritage', value, config?.classHeritage); },
    body(value?: T.ClassBody) { return _fs(config, class_, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDeclaration(config: ConfigOf<T.ClassDeclaration>) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
    body: config.body,
    automatic_semicolon: config.automaticSemicolon,
  };
  return {
    $type: 'class_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    decorator(...values: T.Decorator[]) { return _fsm(config, classDeclaration, 'decorator', values, config?.decorator); },
    name(value?: T.TypeIdentifier) { return _fs(config, classDeclaration, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, classDeclaration, 'typeParameters', value, config?.typeParameters); },
    classHeritage(value?: T.ClassHeritage | undefined) { return _fs(config, classDeclaration, 'classHeritage', value, config?.classHeritage); },
    body(value?: T.ClassBody) { return _fs(config, classDeclaration, 'body', value, config?.body); },
    automaticSemicolon(value?: T.AutomaticSemicolon | undefined) { return _fs(config, classDeclaration, 'automaticSemicolon', value, config?.automaticSemicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritage(config: ConfigOf<T.ClassHeritageUFormExtendsClause>): ReturnType<typeof classHeritageUFormExtendsClause>;
export function classHeritage(config: ConfigOf<T.ClassHeritageUFormImplementsClause>): ReturnType<typeof classHeritageUFormImplementsClause>;
export function classHeritage(config: ConfigOf<T.ClassHeritageUFormExtendsClause> | ConfigOf<T.ClassHeritageUFormImplementsClause>) {
  switch (config.$variant) {
    case 'extends_clause': return classHeritageUFormExtendsClause(config as Parameters<typeof classHeritageUFormExtendsClause>[0]);
    case 'implements_clause': return classHeritageUFormImplementsClause(config as Parameters<typeof classHeritageUFormImplementsClause>[0]);
  }
  throw new Error(`classHeritage: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'extends_clause' | 'implements_clause'.`);
}
export function classHeritageUFormExtendsClause(config?: Omit<ConfigOf<T.ClassHeritageUFormExtendsClause>, '$variant'>) {
  const inner = classHeritageExtendsClause((config?.children ?? [])[0] as Parameters<typeof classHeritageExtendsClause>[0]);
  const children = [inner] as const;
  return {
    $type: 'class_heritage' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'extends_clause' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassHeritageUFormExtendsClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function classHeritageUFormImplementsClause(config?: Omit<ConfigOf<T.ClassHeritageUFormImplementsClause>, '$variant'>) {
  const inner = classHeritageImplementsClause((config?.children ?? [])[0] as Parameters<typeof classHeritageImplementsClause>[0]);
  const children = [inner] as const;
  return {
    $type: 'class_heritage' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'implements_clause' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassHeritageUFormImplementsClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionExpression(config: ConfigOf<T.FunctionExpression>) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  return {
    $type: 'function_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: "async" | undefined) { return _fs(config, functionExpression, 'async', value, config?.async); },
    name(value?: T.Identifier | undefined) { return _fs(config, functionExpression, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionExpression, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, functionExpression, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, functionExpression, 'returnType', value, config?.returnType); },
    body(value?: T.StatementBlock) { return _fs(config, functionExpression, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.FunctionExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDeclaration(config: ConfigOf<T.FunctionDeclaration>) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'function_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    async(value?: "async" | undefined) { return _fs(config, functionDeclaration, 'async', value, config?.async); },
    name(value?: T.Identifier) { return _fs(config, functionDeclaration, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionDeclaration, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, functionDeclaration, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, functionDeclaration, 'returnType', value, config?.returnType); },
    body(value?: T.StatementBlock) { return _fs(config, functionDeclaration, 'body', value, config?.body); },
    child(value?: T.AutomaticSemicolon) {
      if (value === undefined) return children[0];
      return functionDeclaration({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.FunctionDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunction(config: ConfigOf<T.GeneratorFunction>) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  return {
    $type: 'generator_function' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: "async" | undefined) { return _fs(config, generatorFunction, 'async', value, config?.async); },
    name(value?: T.Identifier | undefined) { return _fs(config, generatorFunction, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, generatorFunction, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, generatorFunction, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, generatorFunction, 'returnType', value, config?.returnType); },
    body(value?: T.StatementBlock) { return _fs(config, generatorFunction, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.GeneratorFunctionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunctionDeclaration(config: ConfigOf<T.GeneratorFunctionDeclaration>) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'generator_function_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    async(value?: "async" | undefined) { return _fs(config, generatorFunctionDeclaration, 'async', value, config?.async); },
    name(value?: T.Identifier) { return _fs(config, generatorFunctionDeclaration, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, generatorFunctionDeclaration, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, generatorFunctionDeclaration, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, generatorFunctionDeclaration, 'returnType', value, config?.returnType); },
    body(value?: T.StatementBlock) { return _fs(config, generatorFunctionDeclaration, 'body', value, config?.body); },
    child(value?: T.AutomaticSemicolon) {
      if (value === undefined) return children[0];
      return generatorFunctionDeclaration({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.GeneratorFunctionDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunction(config: ConfigOf<T.ArrowFunctionUFormParameter>): ReturnType<typeof arrowFunctionUFormParameter>;
export function arrowFunction(config: ConfigOf<T.ArrowFunctionUFormUCallSignature>): ReturnType<typeof arrowFunctionUFormUCallSignature>;
export function arrowFunction(config: ConfigOf<T.ArrowFunctionUFormParameter> | ConfigOf<T.ArrowFunctionUFormUCallSignature>) {
  switch (config.$variant) {
    case 'parameter': return arrowFunctionUFormParameter(config as Parameters<typeof arrowFunctionUFormParameter>[0]);
    case '_call_signature': return arrowFunctionUFormUCallSignature(config as Parameters<typeof arrowFunctionUFormUCallSignature>[0]);
  }
  throw new Error(`arrowFunction: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'parameter' | '_call_signature'.`);
}
export function arrowFunctionUFormParameter(config: Omit<ConfigOf<T.ArrowFunctionUFormParameter>, '$variant'>) {
  const fields = {
    async: config.async,
    body: config.body,
  };
  const inner = arrowFunctionParameter(config);
  const children = [inner] as const;
  return {
    $type: 'arrow_function' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'parameter' as const,
    $fields: fields,
    $children: children,
    async(value?: "async" | undefined) {
      if (value === undefined) return fields.async;
      return arrowFunctionUFormParameter({ body: config.body, parameter: inner.$fields.parameter, async: value });
    },
    body(value?: T.Expression | T.StatementBlock) {
      if (value === undefined) return fields.body;
      return arrowFunctionUFormParameter({ async: config.async, parameter: inner.$fields.parameter, body: value });
    },
    parameter(value?: T.ReservedIdentifier) {
      if (value === undefined) return inner.$fields.parameter;
      return arrowFunctionUFormParameter({ async: config.async, body: config.body, parameter: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ArrowFunctionUFormParameterTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function arrowFunctionUFormUCallSignature(config: Omit<ConfigOf<T.ArrowFunctionUFormUCallSignature>, '$variant'>) {
  const fields = {
    async: config.async,
    body: config.body,
  };
  const inner = arrowFunctionUCallSignature(config);
  const children = [inner] as const;
  return {
    $type: 'arrow_function' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: '_call_signature' as const,
    $fields: fields,
    $children: children,
    async(value?: "async" | undefined) {
      if (value === undefined) return fields.async;
      return arrowFunctionUFormUCallSignature({ body: config.body, typeParameters: inner.$fields.type_parameters, parameters: inner.$fields.parameters, returnType: inner.$fields.return_type, async: value });
    },
    body(value?: T.Expression | T.StatementBlock) {
      if (value === undefined) return fields.body;
      return arrowFunctionUFormUCallSignature({ async: config.async, typeParameters: inner.$fields.type_parameters, parameters: inner.$fields.parameters, returnType: inner.$fields.return_type, body: value });
    },
    typeParameters(value?: T.TypeParameters | undefined) {
      if (value === undefined) return inner.$fields.type_parameters;
      return arrowFunctionUFormUCallSignature({ async: config.async, body: config.body, parameters: inner.$fields.parameters, returnType: inner.$fields.return_type, typeParameters: value });
    },
    parameters(value?: T.FormalParameters) {
      if (value === undefined) return inner.$fields.parameters;
      return arrowFunctionUFormUCallSignature({ async: config.async, body: config.body, typeParameters: inner.$fields.type_parameters, returnType: inner.$fields.return_type, parameters: value });
    },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) {
      if (value === undefined) return inner.$fields.return_type;
      return arrowFunctionUFormUCallSignature({ async: config.async, body: config.body, typeParameters: inner.$fields.type_parameters, parameters: inner.$fields.parameters, returnType: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ArrowFunctionUFormUCallSignatureTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: ConfigOf<T.CallExpressionUFormCall>): ReturnType<typeof callExpressionUFormCall>;
export function callExpression(config: ConfigOf<T.CallExpressionUFormTemplateCall>): ReturnType<typeof callExpressionUFormTemplateCall>;
export function callExpression(config: ConfigOf<T.CallExpressionUFormMember>): ReturnType<typeof callExpressionUFormMember>;
export function callExpression(config: ConfigOf<T.CallExpressionUFormCall> | ConfigOf<T.CallExpressionUFormTemplateCall> | ConfigOf<T.CallExpressionUFormMember>) {
  switch (config.$variant) {
    case 'call': return callExpressionUFormCall(config as Parameters<typeof callExpressionUFormCall>[0]);
    case 'template_call': return callExpressionUFormTemplateCall(config as Parameters<typeof callExpressionUFormTemplateCall>[0]);
    case 'member': return callExpressionUFormMember(config as Parameters<typeof callExpressionUFormMember>[0]);
  }
  throw new Error(`callExpression: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'call' | 'template_call' | 'member'.`);
}
export function callExpressionUFormCall(config: Omit<ConfigOf<T.CallExpressionUFormCall>, '$variant'>) {
  const inner = {
    $type: 'call_expression_call' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: {
      function: config.function,
      type_arguments: config.typeArguments,
      arguments: config.arguments,
    },
  };
  const children = [inner] as const;
  return {
    $type: 'call_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'call' as const,
    $children: children,
    function(value?: T.Expression | T.Import) {
      if (value === undefined) return inner.$fields.function;
      return callExpressionUFormCall({ typeArguments: inner.$fields.type_arguments, arguments: inner.$fields.arguments, function: value });
    },
    typeArguments(value?: T.TypeArguments | undefined) {
      if (value === undefined) return inner.$fields.type_arguments;
      return callExpressionUFormCall({ function: inner.$fields.function, arguments: inner.$fields.arguments, typeArguments: value });
    },
    arguments(value?: T.Arguments) {
      if (value === undefined) return inner.$fields.arguments;
      return callExpressionUFormCall({ function: inner.$fields.function, typeArguments: inner.$fields.type_arguments, arguments: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.CallExpressionUFormCallTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionUFormTemplateCall(config: Omit<ConfigOf<T.CallExpressionUFormTemplateCall>, '$variant'>) {
  const inner = {
    $type: 'call_expression_template_call' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: {
      function: config.function,
      arguments: config.arguments,
    },
  };
  const children = [inner] as const;
  return {
    $type: 'call_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'template_call' as const,
    $children: children,
    function(value?: T.PrimaryExpression | T.NewExpression) {
      if (value === undefined) return inner.$fields.function;
      return callExpressionUFormTemplateCall({ arguments: inner.$fields.arguments, function: value });
    },
    arguments(value?: T.TemplateString) {
      if (value === undefined) return inner.$fields.arguments;
      return callExpressionUFormTemplateCall({ function: inner.$fields.function, arguments: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.CallExpressionUFormTemplateCallTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionUFormMember(config: Omit<ConfigOf<T.CallExpressionUFormMember>, '$variant'>) {
  const inner = {
    $type: 'call_expression_member' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: {
      function: config.function,
      type_arguments: config.typeArguments,
      arguments: config.arguments,
    },
  };
  const children = [inner] as const;
  return {
    $type: 'call_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'member' as const,
    $children: children,
    function(value?: T.PrimaryExpression) {
      if (value === undefined) return inner.$fields.function;
      return callExpressionUFormMember({ typeArguments: inner.$fields.type_arguments, arguments: inner.$fields.arguments, function: value });
    },
    typeArguments(value?: T.TypeArguments | undefined) {
      if (value === undefined) return inner.$fields.type_arguments;
      return callExpressionUFormMember({ function: inner.$fields.function, arguments: inner.$fields.arguments, typeArguments: value });
    },
    arguments(value?: T.Arguments) {
      if (value === undefined) return inner.$fields.arguments;
      return callExpressionUFormMember({ function: inner.$fields.function, typeArguments: inner.$fields.type_arguments, arguments: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.CallExpressionUFormMemberTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function newExpression(config: ConfigOf<T.NewExpression>) {
  const fields = {
    constructor: config.constructor,
    type_arguments: config.typeArguments,
    arguments: config.arguments,
  };
  return {
    $type: 'new_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    constructor(value?: T.PrimaryExpression) { return _fs(config, newExpression, 'constructor', value, config?.constructor); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, newExpression, 'typeArguments', value, config?.typeArguments); },
    arguments(value?: T.Arguments | undefined) { return _fs(config, newExpression, 'arguments', value, config?.arguments); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.NewExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(config: ConfigOf<T.AwaitExpression>) {
  const fields = {
    expression: config.expression,
  };
  return {
    $type: 'await_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, awaitExpression, 'expression', value, config?.expression); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AwaitExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberExpression(config: ConfigOf<T.MemberExpression>) {
  const fields = {
    object: config.object,
    property: config.property,
  };
  return {
    $type: 'member_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.Expression | T.PrimaryExpression | T.Import) { return _fs(config, memberExpression, 'object', value, config?.object); },
    property(value?: T.PrivatePropertyIdentifier | T.Identifier) { return _fs(config, memberExpression, 'property', value, config?.property); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.MemberExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscriptExpression(config: ConfigOf<T.SubscriptExpression>) {
  const fields = {
    object: config.object,
    optional_chain: config.optionalChain ? "?." as const : undefined,
    index: config.index,
  };
  return {
    $type: 'subscript_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.Expression | T.PrimaryExpression) { return _fs(config, subscriptExpression, 'object', value, config?.object); },
    optionalChain(value?: T.OptionalChain | undefined) { return _fs(config, subscriptExpression, 'optionalChain', value, config?.optionalChain); },
    index(value?: T.Expressions) { return _fs(config, subscriptExpression, 'index', value, config?.index); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.SubscriptExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function lhsExpression(child: (T.MemberExpression | T.SubscriptExpression | T._Identifier | T.ReservedIdentifier | T.DestructuringPattern | T.NonNullExpression)) {
  const children = [child];
  return {
    $type: '_lhs_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.LhsExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: ConfigOf<T.AssignmentExpression>) {
  const fields = {
    using: config.using ? "using" as const : undefined,
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'assignment_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    using(value?: "using" | undefined) { return _fs(config, assignmentExpression, 'using', value, config?.using); },
    left(value?: T.ParenthesizedExpression | T.LhsExpression) { return _fs(config, assignmentExpression, 'left', value, config?.left); },
    right(value?: T.Expression) { return _fs(config, assignmentExpression, 'right', value, config?.right); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AssignmentExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignmentExpression(config: ConfigOf<T.AugmentedAssignmentExpression>) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    $type: 'augmented_assignment_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.MemberExpression | T.SubscriptExpression | T.ReservedIdentifier | T.ParenthesizedExpression | T.NonNullExpression) { return _fs(config, augmentedAssignmentExpression, 'left', value, config?.left); },
    operator(value?: "+=" | "-=" | "*=" | "/=" | "%=" | "^=" | "&=" | "|=" | ">>=" | ">>>=" | "<<=" | "**=" | "&&=" | "||=" | "??=") { return _fs(config, augmentedAssignmentExpression, 'operator', value, config?.operator); },
    right(value?: T.Expression) { return _fs(config, augmentedAssignmentExpression, 'right', value, config?.right); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AugmentedAssignmentExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function spreadElement(config: ConfigOf<T.SpreadElement>) {
  const fields = {
    expression: config.expression,
  };
  return {
    $type: 'spread_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, spreadElement, 'expression', value, config?.expression); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.SpreadElementTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function ternaryExpression(config: ConfigOf<T.TernaryExpression>) {
  const fields = {
    condition: config.condition,
    consequence: config.consequence,
    alternative: config.alternative,
  };
  return {
    $type: 'ternary_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    condition(value?: T.Expression) { return _fs(config, ternaryExpression, 'condition', value, config?.condition); },
    consequence(value?: T.Expression) { return _fs(config, ternaryExpression, 'consequence', value, config?.consequence); },
    alternative(value?: T.Expression) { return _fs(config, ternaryExpression, 'alternative', value, config?.alternative); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TernaryExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: ConfigOf<T.BinaryExpression>) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    $type: 'binary_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Expression | T.PrivatePropertyIdentifier) { return _fs(config, binaryExpression, 'left', value, config?.left); },
    operator(value?: "&&" | "||" | ">>" | ">>>" | "<<" | "&" | "^" | "|" | "+" | "-" | "*" | "/" | "%" | "**" | "<" | "<=" | "==" | "===" | "!=" | "!==" | ">=" | ">" | "??" | "instanceof" | "in") { return _fs(config, binaryExpression, 'operator', value, config?.operator); },
    right(value?: T.Expression) { return _fs(config, binaryExpression, 'right', value, config?.right); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.BinaryExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: ConfigOf<T.UnaryExpression>) {
  const fields = {
    operator: config.operator,
    argument: config.argument,
  };
  return {
    $type: 'unary_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    operator(value?: "!" | "~" | "-" | "+" | "typeof" | "void" | "delete") { return _fs(config, unaryExpression, 'operator', value, config?.operator); },
    argument(value?: T.Expression) { return _fs(config, unaryExpression, 'argument', value, config?.argument); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.UnaryExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function updateExpression(config: ConfigOf<T.UpdateExpressionUFormPostfix>): ReturnType<typeof updateExpressionUFormPostfix>;
export function updateExpression(config: ConfigOf<T.UpdateExpressionUFormPrefix>): ReturnType<typeof updateExpressionUFormPrefix>;
export function updateExpression(config: ConfigOf<T.UpdateExpressionUFormPostfix> | ConfigOf<T.UpdateExpressionUFormPrefix>) {
  switch (config.$variant) {
    case 'postfix': return updateExpressionUFormPostfix(config as Parameters<typeof updateExpressionUFormPostfix>[0]);
    case 'prefix': return updateExpressionUFormPrefix(config as Parameters<typeof updateExpressionUFormPrefix>[0]);
  }
  throw new Error(`updateExpression: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'postfix' | 'prefix'.`);
}
export function updateExpressionUFormPostfix(config: Omit<ConfigOf<T.UpdateExpressionUFormPostfix>, '$variant'>) {
  const inner = {
    $type: 'update_expression_postfix' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: {
      argument: config.argument,
      operator: config.operator,
    },
  };
  const children = [inner] as const;
  return {
    $type: 'update_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'postfix' as const,
    $children: children,
    argument(value?: T.Expression) {
      if (value === undefined) return inner.$fields.argument;
      return updateExpressionUFormPostfix({ operator: inner.$fields.operator, argument: value });
    },
    operator(value?: "++" | "--") {
      if (value === undefined) return inner.$fields.operator;
      return updateExpressionUFormPostfix({ argument: inner.$fields.argument, operator: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.UpdateExpressionUFormPostfixTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function updateExpressionUFormPrefix(config: Omit<ConfigOf<T.UpdateExpressionUFormPrefix>, '$variant'>) {
  const inner = {
    $type: 'update_expression_prefix' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: {
      operator: config.operator,
      argument: config.argument,
    },
  };
  const children = [inner] as const;
  return {
    $type: 'update_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'prefix' as const,
    $children: children,
    operator(value?: "++" | "--") {
      if (value === undefined) return inner.$fields.operator;
      return updateExpressionUFormPrefix({ argument: inner.$fields.argument, operator: value });
    },
    argument(value?: T.Expression) {
      if (value === undefined) return inner.$fields.argument;
      return updateExpressionUFormPrefix({ operator: inner.$fields.operator, argument: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.UpdateExpressionUFormPrefixTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function sequenceExpression(...children: T.Expression[]) {
  _assertNonEmpty(children, 'sequence_expression.children');
  return {
    $type: 'sequence_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.SequenceExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(config: ConfigOf<T.StringUFormDouble>): ReturnType<typeof stringUFormDouble>;
export function string(config: ConfigOf<T.StringUFormSingle>): ReturnType<typeof stringUFormSingle>;
export function string(config: ConfigOf<T.StringUFormDouble> | ConfigOf<T.StringUFormSingle>) {
  switch (config.$variant) {
    case 'double': return stringUFormDouble(config as Parameters<typeof stringUFormDouble>[0]);
    case 'single': return stringUFormSingle(config as Parameters<typeof stringUFormSingle>[0]);
  }
  throw new Error(`string: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'double' | 'single'.`);
}
export function stringUFormDouble(config?: Omit<ConfigOf<T.StringUFormDouble>, '$variant'>) {
  const inner = stringDouble(...(config?.children ?? []));
  const children = [inner] as const;
  return {
    $type: 'string' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'double' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.StringUFormDoubleTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function stringUFormSingle(config?: Omit<ConfigOf<T.StringUFormSingle>, '$variant'>) {
  const inner = stringSingle(...(config?.children ?? []));
  const children = [inner] as const;
  return {
    $type: 'string' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'single' as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.StringUFormSingleTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_double_string_fragment: text must be non-empty`); if (!_leafRe_unescapedDoubleStringFragment.test(text)) throw new Error(`unescaped_double_string_fragment: text does not match pattern: ${text}`);
  return {
    $type: 'unescaped_double_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedDoubleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_single_string_fragment: text must be non-empty`); if (!_leafRe_unescapedSingleStringFragment.test(text)) throw new Error(`unescaped_single_string_fragment: text does not match pattern: ${text}`);
  return {
    $type: 'unescaped_single_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedSingleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`);
  return {
    $type: 'escape_sequence' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(text: string) {
  if (text.length === 0) throw new Error(`comment: text must be non-empty`);
  return {
    $type: 'comment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function templateString(...children: (T.StringFragment | T.EscapeSequence | T.TemplateSubstitution)[]) {
  return {
    $type: 'template_string' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TemplateStringTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateSubstitution(child: T.Expressions) {
  const children = [child];
  return {
    $type: 'template_substitution' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TemplateSubstitutionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function regex(config: ConfigOf<T.Regex>) {
  const fields = {
    pattern: config.pattern,
    flags: config.flags,
  };
  return {
    $type: 'regex' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    pattern(value?: T.RegexPattern) { return _fs(config, regex, 'pattern', value, config?.pattern); },
    flags(value?: T.RegexFlags | undefined) { return _fs(config, regex, 'flags', value, config?.flags); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.RegexTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function regexPattern(text: string) {
  if (text.length === 0) throw new Error(`regex_pattern: text must be non-empty`);
  return {
    $type: 'regex_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.RegexPatternTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regexFlags(text: string) {
  if (text.length === 0) throw new Error(`regex_flags: text must be non-empty`); if (!_leafRe_regexFlags.test(text)) throw new Error(`regex_flags: text does not match pattern: ${text}`);
  return {
    $type: 'regex_flags' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.RegexFlagsTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function number(text: string) {
  if (text.length === 0) throw new Error(`number: text must be non-empty`);
  return {
    $type: 'number' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.NumberTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`);
  return {
    $type: 'identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function privatePropertyIdentifier(text: string) {
  if (text.length === 0) throw new Error(`private_property_identifier: text must be non-empty`);
  return {
    $type: 'private_property_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PrivatePropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function metaProperty(text: string) {
  if (text.length === 0) throw new Error(`meta_property: text must be non-empty`);
  return {
    $type: 'meta_property' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.MetaPropertyTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function this_() {
  return {
    $type: 'this' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'this' as const,
    render: () => 'this' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' as const },
    replace: (t: T.ThisTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' as const }; },
  };
}

export function super_() {
  return {
    $type: 'super' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: T.SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function true_() {
  return {
    $type: 'true' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'true' as const,
    render: () => 'true' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'true' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'true' as const },
    replace: (t: T.TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' as const }; },
  };
}

export function false_() {
  return {
    $type: 'false' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'false' as const,
    render: () => 'false' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'false' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'false' as const },
    replace: (t: T.FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' as const }; },
  };
}

export function null_() {
  return {
    $type: 'null' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'null' as const,
    render: () => 'null' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'null' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'null' as const },
    replace: (t: T.NullTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' as const }; },
  };
}

export function undefined_() {
  return {
    $type: 'undefined' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'undefined' as const,
    render: () => 'undefined' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'undefined' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'undefined' as const },
    replace: (t: T.UndefinedTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' as const }; },
  };
}

export function arguments_(...children: (T.Expression | T.SpreadElement)[]) {
  return {
    $type: 'arguments' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ArgumentsTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(child: (T.Identifier | T.DecoratorMemberExpression | T.DecoratorCallExpression | T.DecoratorParenthesizedExpression)) {
  const children = [child];
  return {
    $type: 'decorator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.DecoratorTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorMemberExpression(config: ConfigOf<T.DecoratorMemberExpression>) {
  const fields = {
    object: config.object,
    property: config.property,
  };
  return {
    $type: 'decorator_member_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.Identifier | T.DecoratorMemberExpression) { return _fs(config, decoratorMemberExpression, 'object', value, config?.object); },
    property(value?: T.Identifier) { return _fs(config, decoratorMemberExpression, 'property', value, config?.property); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.DecoratorMemberExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorCallExpression(config: ConfigOf<T.DecoratorCallExpression>) {
  const fields = {
    function: config.function,
    type_arguments: config.typeArguments,
    arguments: config.arguments,
  };
  return {
    $type: 'decorator_call_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    function(value?: T.Identifier | T.DecoratorMemberExpression) { return _fs(config, decoratorCallExpression, 'function', value, config?.function); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, decoratorCallExpression, 'typeArguments', value, config?.typeArguments); },
    arguments(value?: T.Arguments) { return _fs(config, decoratorCallExpression, 'arguments', value, config?.arguments); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.DecoratorCallExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBody(...children: (T.ClassBodyMethod | T.ClassBodyMethodSig | T.ClassStaticBlock | T.ClassBodyMember)[]) {
  return {
    $type: 'class_body' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassBodyTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDefinition(config: ConfigOf<T.FieldDefinition>) {
  const fields = {
    decorator: config.decorator,
    static: config.static ? "static" as const : undefined,
    property: config.property,
    value: config.value,
  };
  return {
    $type: 'field_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    decorator(...values: T.Decorator[]) { return _fsm(config, fieldDefinition, 'decorator', values, config?.decorator); },
    static(value?: "static" | undefined) { return _fs(config, fieldDefinition, 'static', value, config?.static); },
    property(value?: T.PropertyName) { return _fs(config, fieldDefinition, 'property', value, config?.property); },
    value(value?: T.Expression | undefined) { return _fs(config, fieldDefinition, 'value', value, config?.value); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.FieldDefinitionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function formalParameters(...children: T.FormalParameter[]) {
  _assertNonEmpty(children, 'formal_parameters.children');
  return {
    $type: 'formal_parameters' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.FormalParametersTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function classStaticBlock(config: ConfigOf<T.ClassStaticBlock>) {
  const fields = {
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'class_static_block' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    body(value?: T.StatementBlock) { return _fs(config, classStaticBlock, 'body', value, config?.body); },
    child(value?: T.AutomaticSemicolon) {
      if (value === undefined) return children[0];
      return classStaticBlock({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassStaticBlockTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function restPattern(child: T.LhsExpression) {
  const children = [child];
  return {
    $type: 'rest_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.RestPatternTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodDefinition(config: ConfigOf<T.MethodDefinition>) {
  const fields = {
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
    readonly: config.readonly ? "readonly" as const : undefined,
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  return {
    $type: 'method_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    accessibilityModifier(value?: T.AccessibilityModifier | undefined) { return _fs(config, methodDefinition, 'accessibilityModifier', value, config?.accessibilityModifier); },
    overrideModifier(value?: "static" | T.OverrideModifier | undefined) { return _fs(config, methodDefinition, 'overrideModifier', value, config?.overrideModifier); },
    readonly(value?: "readonly" | undefined) { return _fs(config, methodDefinition, 'readonly', value, config?.readonly); },
    async(value?: "async" | undefined) { return _fs(config, methodDefinition, 'async', value, config?.async); },
    name(value?: T.PropertyName) { return _fs(config, methodDefinition, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, methodDefinition, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, methodDefinition, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, methodDefinition, 'returnType', value, config?.returnType); },
    body(value?: T.StatementBlock) { return _fs(config, methodDefinition, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.MethodDefinitionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: ConfigOf<T.Pair>) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    $type: 'pair' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    key(value?: T.PropertyName) { return _fs(config, pair, 'key', value, config?.key); },
    value(value?: T.Expression) { return _fs(config, pair, 'value', value, config?.value); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.PairTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function pairPattern(config: ConfigOf<T.PairPattern>) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    $type: 'pair_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    key(value?: T.PropertyName) { return _fs(config, pairPattern, 'key', value, config?.key); },
    value(value?: T.Pattern | T.AssignmentPattern) { return _fs(config, pairPattern, 'value', value, config?.value); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.PairPatternTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function computedPropertyName(config: ConfigOf<T.ComputedPropertyName>) {
  const fields = {
    expression: config.expression,
  };
  return {
    $type: 'computed_property_name' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, computedPropertyName, 'expression', value, config?.expression); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ComputedPropertyNameTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function reservedIdentifier(text: string) {
  if (text.length === 0) throw new Error(`_reserved_identifier: text must be non-empty`);
  return {
    $type: '_reserved_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ReservedIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringFragment(text: string) {
  if (text.length === 0) throw new Error(`_string_fragment: text must be non-empty`);
  return {
    $type: '_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function publicFieldDefinition(config: ConfigOf<T.PublicFieldDefinition>) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type: config.type,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    $type: 'public_field_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    decorator(...values: T.Decorator[]) { return _fsm(config, publicFieldDefinition, 'decorator', values, config?.decorator); },
    name(value?: T.PropertyName) { return _fs(config, publicFieldDefinition, 'name', value, config?.name); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, publicFieldDefinition, 'type', value, config?.type); },
    value(value?: T.Expression | undefined) { return _fs(config, publicFieldDefinition, 'value', value, config?.value); },
    child(value?: (T.PublicFieldDefinitionDeclareFirst | T.PublicFieldDefinitionAccessFirst | T.PublicFieldDefinitionStaticMods | T.PublicFieldDefinitionAbstractFirst | T.PublicFieldDefinitionReadonlyFirst | T.PublicFieldDefinitionAccessorOpt)) {
      if (value === undefined) return children[0];
      return publicFieldDefinition({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.PublicFieldDefinitionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonNullExpression(config: ConfigOf<T.NonNullExpression>) {
  const fields = {
    expression: config.expression,
  };
  return {
    $type: 'non_null_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, nonNullExpression, 'expression', value, config?.expression); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.NonNullExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodSignature(config: ConfigOf<T.MethodSignature>) {
  const fields = {
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
    readonly: config.readonly ? "readonly" as const : undefined,
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: 'method_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    accessibilityModifier(value?: T.AccessibilityModifier | undefined) { return _fs(config, methodSignature, 'accessibilityModifier', value, config?.accessibilityModifier); },
    overrideModifier(value?: "static" | T.OverrideModifier | undefined) { return _fs(config, methodSignature, 'overrideModifier', value, config?.overrideModifier); },
    readonly(value?: "readonly" | undefined) { return _fs(config, methodSignature, 'readonly', value, config?.readonly); },
    async(value?: "async" | undefined) { return _fs(config, methodSignature, 'async', value, config?.async); },
    name(value?: T.PropertyName) { return _fs(config, methodSignature, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, methodSignature, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, methodSignature, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, methodSignature, 'returnType', value, config?.returnType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.MethodSignatureTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractMethodSignature(config: ConfigOf<T.AbstractMethodSignature>) {
  const fields = {
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier ? "override" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: 'abstract_method_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    accessibilityModifier(value?: T.AccessibilityModifier | undefined) { return _fs(config, abstractMethodSignature, 'accessibilityModifier', value, config?.accessibilityModifier); },
    overrideModifier(value?: T.OverrideModifier | undefined) { return _fs(config, abstractMethodSignature, 'overrideModifier', value, config?.overrideModifier); },
    name(value?: T.PropertyName) { return _fs(config, abstractMethodSignature, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, abstractMethodSignature, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, abstractMethodSignature, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, abstractMethodSignature, 'returnType', value, config?.returnType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AbstractMethodSignatureTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignature(config: ConfigOf<T.FunctionSignature>) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    semicolon: config.semicolon,
  };
  return {
    $type: 'function_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: "async" | undefined) { return _fs(config, functionSignature, 'async', value, config?.async); },
    name(value?: T.Identifier) { return _fs(config, functionSignature, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionSignature, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, functionSignature, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, functionSignature, 'returnType', value, config?.returnType); },
    semicolon(value?: T.Semicolon | T.FunctionSignatureAutomaticSemicolon) { return _fs(config, functionSignature, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.FunctionSignatureTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorParenthesizedExpression(child: (T.Identifier | T.DecoratorMemberExpression | T.DecoratorCallExpression)) {
  const children = [child];
  return {
    $type: 'decorator_parenthesized_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.DecoratorParenthesizedExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAssertion(config: ConfigOf<T.TypeAssertion>) {
  const fields = {
    type_arguments: config.typeArguments,
    expression: config.expression,
  };
  return {
    $type: 'type_assertion' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeArguments(value?: T.TypeArguments) { return _fs(config, typeAssertion, 'typeArguments', value, config?.typeArguments); },
    expression(value?: T.Expression) { return _fs(config, typeAssertion, 'expression', value, config?.expression); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypeAssertionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function asExpression(config: ConfigOf<T.AsExpression>) {
  const fields = {
    expression: config.expression,
    type_annotation: config.typeAnnotation,
  };
  return {
    $type: 'as_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, asExpression, 'expression', value, config?.expression); },
    typeAnnotation(value?: "const" | T.Type) { return _fs(config, asExpression, 'typeAnnotation', value, config?.typeAnnotation); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AsExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function satisfiesExpression(config: ConfigOf<T.SatisfiesExpression>) {
  const fields = {
    expression: config.expression,
    type_annotation: config.typeAnnotation,
  };
  return {
    $type: 'satisfies_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, satisfiesExpression, 'expression', value, config?.expression); },
    typeAnnotation(value?: T.Type) { return _fs(config, satisfiesExpression, 'typeAnnotation', value, config?.typeAnnotation); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.SatisfiesExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function instantiationExpression(config: ConfigOf<T.InstantiationExpression>) {
  const fields = {
    expression: config.expression,
    type_arguments: config.typeArguments,
  };
  return {
    $type: 'instantiation_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, instantiationExpression, 'expression', value, config?.expression); },
    typeArguments(value?: T.TypeArguments) { return _fs(config, instantiationExpression, 'typeArguments', value, config?.typeArguments); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.InstantiationExpressionTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importRequireClause(config: ConfigOf<T.ImportRequireClause>) {
  const fields = {
    identifier: config.identifier,
    source: config.source,
  };
  return {
    $type: 'import_require_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    identifier(value?: T.Identifier) { return _fs(config, importRequireClause, 'identifier', value, config?.identifier); },
    source(value?: T.String) { return _fs(config, importRequireClause, 'source', value, config?.source); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportRequireClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsClause(config: ConfigOf<T.ExtendsClause>) {
  const fields = {
    value: config.value,
    type_arguments: config.typeArguments,
  };
  return {
    $type: 'extends_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    value(...values: NonEmptyArray<T.Expression>) { return _fsm(config, extendsClause, 'value', values, config?.value); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, extendsClause, 'typeArguments', value, config?.typeArguments); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExtendsClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function implementsClause(...children: T.Type[]) {
  _assertNonEmpty(children, 'implements_clause.children');
  return {
    $type: 'implements_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImplementsClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function ambientDeclaration(config: ConfigOf<T.AmbientDeclaration>) {
  const fields = {
    declaration: config.declaration,
  };
  return {
    $type: 'ambient_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    declaration(value?: T.Declaration | "global" | T.StatementBlock | "module" | T.Identifier | T.Type | T.Semicolon) { return _fs(config, ambientDeclaration, 'declaration', value, config?.declaration); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AmbientDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractClassDeclaration(config: ConfigOf<T.AbstractClassDeclaration>) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
    body: config.body,
  };
  return {
    $type: 'abstract_class_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    decorator(...values: T.Decorator[]) { return _fsm(config, abstractClassDeclaration, 'decorator', values, config?.decorator); },
    name(value?: T.TypeIdentifier) { return _fs(config, abstractClassDeclaration, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, abstractClassDeclaration, 'typeParameters', value, config?.typeParameters); },
    classHeritage(value?: T.ClassHeritage | undefined) { return _fs(config, abstractClassDeclaration, 'classHeritage', value, config?.classHeritage); },
    body(value?: T.ClassBody) { return _fs(config, abstractClassDeclaration, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AbstractClassDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function module(config: ConfigOf<T.Module>) {
  const fields = {
    name: config.name,
    body: config.body,
  };
  return {
    $type: 'module' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.String | T.Identifier | T.NestedIdentifier) { return _fs(config, module, 'name', value, config?.name); },
    body(value?: T.StatementBlock | undefined) { return _fs(config, module, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ModuleTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function internalModule(config: ConfigOf<T.InternalModule>) {
  const fields = {
    name: config.name,
    body: config.body,
  };
  return {
    $type: 'internal_module' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.String | T.Identifier | T.NestedIdentifier) { return _fs(config, internalModule, 'name', value, config?.name); },
    body(value?: T.StatementBlock | undefined) { return _fs(config, internalModule, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.InternalModuleTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAlias(config: ConfigOf<T.ImportAlias>) {
  const fields = {
    name: config.name,
    value: config.value,
    semicolon: config.semicolon,
  };
  return {
    $type: 'import_alias' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier) { return _fs(config, importAlias, 'name', value, config?.name); },
    value(value?: T.Identifier | T.NestedIdentifier) { return _fs(config, importAlias, 'value', value, config?.value); },
    semicolon(value?: T.Semicolon) { return _fs(config, importAlias, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportAliasTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function nestedTypeIdentifier(config: ConfigOf<T.NestedTypeIdentifier>) {
  const fields = {
    module: config.module,
    name: config.name,
  };
  return {
    $type: 'nested_type_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    module(value?: T.Identifier | T.NestedIdentifier) { return _fs(config, nestedTypeIdentifier, 'module', value, config?.module); },
    name(value?: T.TypeIdentifier) { return _fs(config, nestedTypeIdentifier, 'name', value, config?.name); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.NestedTypeIdentifierTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function interfaceDeclaration(config: ConfigOf<T.InterfaceDeclaration>) {
  const fields = {
    name: config.name,
    type_parameters: config.typeParameters,
    extends_type_clause: config.extendsTypeClause,
    body: config.body,
  };
  return {
    $type: 'interface_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.TypeIdentifier) { return _fs(config, interfaceDeclaration, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, interfaceDeclaration, 'typeParameters', value, config?.typeParameters); },
    extendsTypeClause(value?: T.ExtendsTypeClause | undefined) { return _fs(config, interfaceDeclaration, 'extendsTypeClause', value, config?.extendsTypeClause); },
    body(value?: T.ObjectType) { return _fs(config, interfaceDeclaration, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.InterfaceDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsTypeClause(config: ConfigOf<T.ExtendsTypeClause>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'extends_type_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(...values: NonEmptyArray<T.TypeIdentifier | T.NestedTypeIdentifier | T.GenericType>) { return _fsm(config, extendsTypeClause, 'type', values, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExtendsTypeClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumDeclaration(config: ConfigOf<T.EnumDeclaration>) {
  const fields = {
    const: config.const ? "const" as const : undefined,
    name: config.name,
    body: config.body,
  };
  return {
    $type: 'enum_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    const(value?: "const" | undefined) { return _fs(config, enumDeclaration, 'const', value, config?.const); },
    name(value?: T.Identifier) { return _fs(config, enumDeclaration, 'name', value, config?.name); },
    body(value?: T.EnumBody) { return _fs(config, enumDeclaration, 'body', value, config?.body); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.EnumDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumBody(config: ConfigOf<T.EnumBody>) {
  const fields = {
    opening: config.opening,
  };
  return {
    $type: 'enum_body' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    opening(...values: NonEmptyArray<T.PropertyName | T.EnumAssignment>) { return _fsm(config, enumBody, 'opening', values, config?.opening); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.EnumBodyTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumAssignment(config: ConfigOf<T.EnumAssignment>) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    $type: 'enum_assignment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.PropertyName) { return _fs(config, enumAssignment, 'name', value, config?.name); },
    value(value?: T.Expression) { return _fs(config, enumAssignment, 'value', value, config?.value); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.EnumAssignmentTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasDeclaration(config: ConfigOf<T.TypeAliasDeclaration>) {
  const fields = {
    name: config.name,
    type_parameters: config.typeParameters,
    value: config.value,
    semicolon: config.semicolon,
  };
  return {
    $type: 'type_alias_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.TypeIdentifier) { return _fs(config, typeAliasDeclaration, 'name', value, config?.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, typeAliasDeclaration, 'typeParameters', value, config?.typeParameters); },
    value(value?: T.Type) { return _fs(config, typeAliasDeclaration, 'value', value, config?.value); },
    semicolon(value?: T.Semicolon) { return _fs(config, typeAliasDeclaration, 'semicolon', value, config?.semicolon); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypeAliasDeclarationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function accessibilityModifier(text: 'public' | 'private' | 'protected') {
  return {
    $type: 'accessibility_modifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.AccessibilityModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function overrideModifier() {
  return {
    $type: 'override_modifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'override' as const,
    render: () => 'override' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'override' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'override' as const },
    replace: (t: T.OverrideModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' as const }; },
  };
}

export function requiredParameter(config: ConfigOf<T.RequiredParameter>) {
  const fields = {
    decorator: config.decorator,
    readonly: config.readonly ? "readonly" as const : undefined,
    pattern: config.pattern,
    type: config.type,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    $type: 'required_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    decorator(...values: T.Decorator[]) { return _fsm(config, requiredParameter, 'decorator', values, config?.decorator); },
    readonly(value?: "readonly" | undefined) { return _fs(config, requiredParameter, 'readonly', value, config?.readonly); },
    pattern(value?: T.Pattern | T.This) { return _fs(config, requiredParameter, 'pattern', value, config?.pattern); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, requiredParameter, 'type', value, config?.type); },
    value(value?: T.Expression | undefined) { return _fs(config, requiredParameter, 'value', value, config?.value); },
    child(value?: (T.AccessibilityModifier | T.OverrideModifier)) {
      if (value === undefined) return children[0];
      return requiredParameter({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.RequiredParameterTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalParameter(config: ConfigOf<T.OptionalParameter>) {
  const fields = {
    decorator: config.decorator,
    readonly: config.readonly ? "readonly" as const : undefined,
    pattern: config.pattern,
    type: config.type,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    $type: 'optional_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    decorator(...values: T.Decorator[]) { return _fsm(config, optionalParameter, 'decorator', values, config?.decorator); },
    readonly(value?: "readonly" | undefined) { return _fs(config, optionalParameter, 'readonly', value, config?.readonly); },
    pattern(value?: T.Pattern | T.This) { return _fs(config, optionalParameter, 'pattern', value, config?.pattern); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, optionalParameter, 'type', value, config?.type); },
    value(value?: T.Expression | undefined) { return _fs(config, optionalParameter, 'value', value, config?.value); },
    child(value?: (T.AccessibilityModifier | T.OverrideModifier)) {
      if (value === undefined) return children[0];
      return optionalParameter({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.OptionalParameterTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function omittingTypeAnnotation(config: ConfigOf<T.OmittingTypeAnnotation>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'omitting_type_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, omittingTypeAnnotation, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.OmittingTypeAnnotationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function addingTypeAnnotation(config: ConfigOf<T.AddingTypeAnnotation>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'adding_type_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, addingTypeAnnotation, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AddingTypeAnnotationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function optingTypeAnnotation(config: ConfigOf<T.OptingTypeAnnotation>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'opting_type_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, optingTypeAnnotation, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.OptingTypeAnnotationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAnnotation(config: ConfigOf<T.TypeAnnotation>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'type_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, typeAnnotation, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypeAnnotationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function asserts(child: (T.TypePredicate | T.Identifier | T.This)) {
  const children = [child];
  return {
    $type: 'asserts' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AssertsTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertsAnnotation(config: ConfigOf<T.AssertsAnnotation>) {
  const fields = {
    asserts: config.asserts,
  };
  return {
    $type: 'asserts_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    asserts(value?: ":" | T.Asserts) { return _fs(config, assertsAnnotation, 'asserts', value, config?.asserts); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.AssertsAnnotationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleParameter(config: ConfigOf<T.TupleParameter>) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    $type: 'tuple_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier | T.RestPattern) { return _fs(config, tupleParameter, 'name', value, config?.name); },
    typeField(value?: T.TypeAnnotation) { return _fs(config, tupleParameter, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TupleParameterTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalTupleParameter(config: ConfigOf<T.OptionalTupleParameter>) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    $type: 'optional_tuple_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier) { return _fs(config, optionalTupleParameter, 'name', value, config?.name); },
    typeField(value?: T.TypeAnnotation) { return _fs(config, optionalTupleParameter, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.OptionalTupleParameterTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalType(config: ConfigOf<T.OptionalType>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'optional_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, optionalType, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.OptionalTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function restType(config: ConfigOf<T.RestType>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'rest_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, restType, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.RestTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructorType(config: ConfigOf<T.ConstructorType>) {
  const fields = {
    abstract: config.abstract ? "abstract" as const : undefined,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    type: config.type,
  };
  return {
    $type: 'constructor_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    abstract(value?: "abstract" | undefined) { return _fs(config, constructorType, 'abstract', value, config?.abstract); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, constructorType, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, constructorType, 'parameters', value, config?.parameters); },
    typeField(value?: T.Type) { return _fs(config, constructorType, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ConstructorTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateType(child: (T.PrimaryType | T.InferType)) {
  const children = [child];
  return {
    $type: 'template_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TemplateTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateLiteralType(...children: (T.StringFragment | T.TemplateType)[]) {
  return {
    $type: 'template_literal_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TemplateLiteralTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function inferType(config: ConfigOf<T.InferType>) {
  const fields = {
    type_identifier: config.typeIdentifier,
    constraint: config.constraint,
  };
  return {
    $type: 'infer_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeIdentifier(value?: T.TypeIdentifier) { return _fs(config, inferType, 'typeIdentifier', value, config?.typeIdentifier); },
    constraint(value?: "extends" | T.Type) { return _fs(config, inferType, 'constraint', value, config?.constraint); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.InferTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalType(config: ConfigOf<T.ConditionalType>) {
  const fields = {
    left: config.left,
    right: config.right,
    consequence: config.consequence,
    alternative: config.alternative,
  };
  return {
    $type: 'conditional_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Type) { return _fs(config, conditionalType, 'left', value, config?.left); },
    right(value?: T.Type) { return _fs(config, conditionalType, 'right', value, config?.right); },
    consequence(value?: T.Type) { return _fs(config, conditionalType, 'consequence', value, config?.consequence); },
    alternative(value?: T.Type) { return _fs(config, conditionalType, 'alternative', value, config?.alternative); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ConditionalTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: ConfigOf<T.GenericType>) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
  };
  return {
    $type: 'generic_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.TypeIdentifier | T.NestedTypeIdentifier) { return _fs(config, genericType, 'name', value, config?.name); },
    typeArguments(value?: T.TypeArguments) { return _fs(config, genericType, 'typeArguments', value, config?.typeArguments); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.GenericTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicate(config: ConfigOf<T.TypePredicate>) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    $type: 'type_predicate' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.PredefinedType | T.This) { return _fs(config, typePredicate, 'name', value, config?.name); },
    typeField(value?: T.Type) { return _fs(config, typePredicate, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypePredicateTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicateAnnotation(config: ConfigOf<T.TypePredicateAnnotation>) {
  const fields = {
    type_predicate: config.typePredicate,
  };
  return {
    $type: 'type_predicate_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typePredicate(value?: ":" | T.TypePredicate) { return _fs(config, typePredicateAnnotation, 'typePredicate', value, config?.typePredicate); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypePredicateAnnotationTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeQuery(child: (T.TypeQuerySubscriptExpression | T.TypeQueryMemberExpression | T.TypeQueryCallExpression | T.TypeQueryInstantiationExpression | T.Identifier | T.This)) {
  const children = [child];
  return {
    $type: 'type_query' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypeQueryTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexTypeQuery(config: ConfigOf<T.IndexTypeQuery>) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    $type: 'index_type_query' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryType(value?: T.PrimaryType) { return _fs(config, indexTypeQuery, 'primaryType', value, config?.primaryType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.IndexTypeQueryTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function lookupType(config: ConfigOf<T.LookupType>) {
  const fields = {
    primary_type: config.primaryType,
    index_type: config.indexType,
  };
  return {
    $type: 'lookup_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryType(value?: T.PrimaryType) { return _fs(config, lookupType, 'primaryType', value, config?.primaryType); },
    indexType(value?: T.Type) { return _fs(config, lookupType, 'indexType', value, config?.indexType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.LookupTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function mappedTypeClause(config: ConfigOf<T.MappedTypeClause>) {
  const fields = {
    name: config.name,
    type: config.type,
    alias: config.alias,
  };
  return {
    $type: 'mapped_type_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.TypeIdentifier) { return _fs(config, mappedTypeClause, 'name', value, config?.name); },
    typeField(value?: T.Type) { return _fs(config, mappedTypeClause, 'type', value, config?.type); },
    alias(value?: T.Type | undefined) { return _fs(config, mappedTypeClause, 'alias', value, config?.alias); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.MappedTypeClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function literalType(child: (T._Number | T.Number | T.String | T.True | T.False | T.Null | T.Undefined)) {
  const children = [child];
  return {
    $type: 'literal_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.LiteralTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function flowMaybeType(config: ConfigOf<T.FlowMaybeType>) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    $type: 'flow_maybe_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryType(value?: T.PrimaryType) { return _fs(config, flowMaybeType, 'primaryType', value, config?.primaryType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.FlowMaybeTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedType(config: ConfigOf<T.ParenthesizedType>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'parenthesized_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, parenthesizedType, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ParenthesizedTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function predefinedType(text: string) {
  if (text.length === 0) throw new Error(`predefined_type: text must be non-empty`);
  return {
    $type: 'predefined_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PredefinedTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeArguments(...children: T.Type[]) {
  _assertNonEmpty(children, 'type_arguments.children');
  return {
    $type: 'type_arguments' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypeArgumentsTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectType(config: ConfigOf<T.ObjectType>) {
  const fields = {
    opening: config.opening,
    members: config.members,
    closing: config.closing,
  };
  return {
    $type: 'object_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    opening(value?: "{" | "{|") { return _fs(config, objectType, 'opening', value, config?.opening); },
    members(...values: NonEmptyArray<"," | ";" | T.ExportStatement | T.PropertySignature | T.CallSignature | T.ConstructSignature | T.IndexSignature | T.MethodSignature | T.Semicolon>) { return _fsm(config, objectType, 'members', values, config?.members); },
    closing(value?: "}" | "|}") { return _fs(config, objectType, 'closing', value, config?.closing); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ObjectTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectTypeCurly(config?: T.ObjectType.Curly.Config) {
  const fields = {
    opening: "{" as const,
    members: config?.members,
    closing: "}" as const,
  };
  return {
    $type: 'object_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ObjectTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectTypeFlow(config?: T.ObjectType.Flow.Config) {
  const fields = {
    opening: "{|" as const,
    members: config?.members,
    closing: "|}" as const,
  };
  return {
    $type: 'object_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ObjectTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function callSignature(config: ConfigOf<T.CallSignature>) {
  const fields = {
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: 'call_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, callSignature, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, callSignature, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, callSignature, 'returnType', value, config?.returnType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.CallSignatureTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertySignature(config: ConfigOf<T.PropertySignature>) {
  const fields = {
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
    readonly: config.readonly ? "readonly" as const : undefined,
    name: config.name,
    type: config.type,
  };
  return {
    $type: 'property_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    accessibilityModifier(value?: T.AccessibilityModifier | undefined) { return _fs(config, propertySignature, 'accessibilityModifier', value, config?.accessibilityModifier); },
    overrideModifier(value?: "static" | T.OverrideModifier | undefined) { return _fs(config, propertySignature, 'overrideModifier', value, config?.overrideModifier); },
    readonly(value?: "readonly" | undefined) { return _fs(config, propertySignature, 'readonly', value, config?.readonly); },
    name(value?: T.PropertyName) { return _fs(config, propertySignature, 'name', value, config?.name); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, propertySignature, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.PropertySignatureTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: T.TypeParameter[]) {
  _assertNonEmpty(children, 'type_parameters.children');
  return {
    $type: 'type_parameters' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypeParametersTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: ConfigOf<T.TypeParameter>) {
  const fields = {
    const: config.const ? "const" as const : undefined,
    name: config.name,
    constraint: config.constraint,
    value: config.value,
  };
  return {
    $type: 'type_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    const(value?: "const" | undefined) { return _fs(config, typeParameter, 'const', value, config?.const); },
    name(value?: T.TypeIdentifier) { return _fs(config, typeParameter, 'name', value, config?.name); },
    constraint(value?: T.Constraint | undefined) { return _fs(config, typeParameter, 'constraint', value, config?.constraint); },
    value(value?: T.DefaultType | undefined) { return _fs(config, typeParameter, 'value', value, config?.value); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypeParameterTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultType(config: ConfigOf<T.DefaultType>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'default_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, defaultType, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.DefaultTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function constraint(config: ConfigOf<T.Constraint>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'constraint' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, constraint, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ConstraintTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructSignature(config: ConfigOf<T.ConstructSignature>) {
  const fields = {
    abstract: config.abstract ? "abstract" as const : undefined,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    type: config.type,
  };
  return {
    $type: 'construct_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    abstract(value?: "abstract" | undefined) { return _fs(config, constructSignature, 'abstract', value, config?.abstract); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, constructSignature, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, constructSignature, 'parameters', value, config?.parameters); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, constructSignature, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ConstructSignatureTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignature(config: ConfigOf<T.IndexSignatureUFormColon>): ReturnType<typeof indexSignatureUFormColon>;
export function indexSignature(config: ConfigOf<T.IndexSignatureUFormMappedTypeClause>): ReturnType<typeof indexSignatureUFormMappedTypeClause>;
export function indexSignature(config: ConfigOf<T.IndexSignatureUFormColon> | ConfigOf<T.IndexSignatureUFormMappedTypeClause>) {
  switch (config.$variant) {
    case 'colon': return indexSignatureUFormColon(config as Parameters<typeof indexSignatureUFormColon>[0]);
    case 'mapped_type_clause': return indexSignatureUFormMappedTypeClause(config as Parameters<typeof indexSignatureUFormMappedTypeClause>[0]);
  }
  throw new Error(`indexSignature: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'colon' | 'mapped_type_clause'.`);
}
export function indexSignatureUFormColon(config: Omit<ConfigOf<T.IndexSignatureUFormColon>, '$variant'>) {
  const fields = {
    sign: config.sign,
    type: config.type,
  };
  const inner = {
    $type: 'index_signature_colon' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: {
      name: config.name,
      index_type: config.indexType,
    },
  };
  const children = [inner] as const;
  return {
    $type: 'index_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'colon' as const,
    $fields: fields,
    $children: children,
    sign(value?: "-" | "+" | undefined) {
      if (value === undefined) return fields.sign;
      return indexSignatureUFormColon({ type: config.type, name: inner.$fields.name, indexType: inner.$fields.index_type, sign: value });
    },
    typeField(value?: T.TypeAnnotation | T.OmittingTypeAnnotation | T.AddingTypeAnnotation | T.OptingTypeAnnotation) {
      if (value === undefined) return fields.type;
      return indexSignatureUFormColon({ sign: config.sign, name: inner.$fields.name, indexType: inner.$fields.index_type, type: value });
    },
    name(value?: T.ReservedIdentifier) {
      if (value === undefined) return inner.$fields.name;
      return indexSignatureUFormColon({ sign: config.sign, type: config.type, indexType: inner.$fields.index_type, name: value });
    },
    indexType(value?: T.Type) {
      if (value === undefined) return inner.$fields.index_type;
      return indexSignatureUFormColon({ sign: config.sign, type: config.type, name: inner.$fields.name, indexType: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.IndexSignatureUFormColonTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}
export function indexSignatureUFormMappedTypeClause(config: Omit<ConfigOf<T.IndexSignatureUFormMappedTypeClause>, '$variant'>) {
  const fields = {
    sign: config.sign,
    type: config.type,
  };
  const inner = indexSignatureMappedTypeClause((config?.children ?? [])[0] as Parameters<typeof indexSignatureMappedTypeClause>[0]);
  const children = [inner] as const;
  return {
    $type: 'index_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'mapped_type_clause' as const,
    $fields: fields,
    $children: children,
    sign(value?: "-" | "+" | undefined) {
      if (value === undefined) return fields.sign;
      return indexSignatureUFormMappedTypeClause({ type: config.type, sign: value });
    },
    typeField(value?: T.TypeAnnotation | T.OmittingTypeAnnotation | T.AddingTypeAnnotation | T.OptingTypeAnnotation) {
      if (value === undefined) return fields.type;
      return indexSignatureUFormMappedTypeClause({ sign: config.sign, type: value });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.IndexSignatureUFormMappedTypeClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: ConfigOf<T.ArrayType>) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    $type: 'array_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryType(value?: T.PrimaryType) { return _fs(config, arrayType, 'primaryType', value, config?.primaryType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ArrayTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: T.TupleTypeMember[]) {
  return {
    $type: 'tuple_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TupleTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function readonlyType(config: ConfigOf<T.ReadonlyType>) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'readonly_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, readonlyType, 'type', value, config?.type); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ReadonlyTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: ConfigOf<T.UnionType>) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'union_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Type | undefined) { return _fs(config, unionType, 'left', value, config?.left); },
    right(value?: T.Type) { return _fs(config, unionType, 'right', value, config?.right); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.UnionTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function intersectionType(config: ConfigOf<T.IntersectionType>) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'intersection_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Type | undefined) { return _fs(config, intersectionType, 'left', value, config?.left); },
    right(value?: T.Type) { return _fs(config, intersectionType, 'right', value, config?.right); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.IntersectionTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: ConfigOf<T.FunctionType>) {
  const fields = {
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: 'function_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionType, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, functionType, 'parameters', value, config?.parameters); },
    returnType(value?: T.Type | T.Asserts | T.TypePredicate) { return _fs(config, functionType, 'returnType', value, config?.returnType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.FunctionTypeTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeIdentifier(child: T.Identifier) {
  const children = [child];
  return {
    $type: '_type_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.TypeIdentifierTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunctionParameter(config: ConfigOf<T.ArrowFunctionParameter>) {
  const fields = {
    parameter: config.parameter,
  };
  return {
    $type: '_arrow_function_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    parameter(value?: T.ReservedIdentifier) { return _fs(config, arrowFunctionParameter, 'parameter', value, config?.parameter); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ArrowFunctionParameterTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunctionUCallSignature(config: ConfigOf<T.ArrowFunctionUCallSignature>) {
  const fields = {
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: '_arrow_function__call_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, arrowFunctionUCallSignature, 'typeParameters', value, config?.typeParameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, arrowFunctionUCallSignature, 'parameters', value, config?.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, arrowFunctionUCallSignature, 'returnType', value, config?.returnType); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ArrowFunctionUCallSignatureTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritageExtendsClause(child: (T.ExtendsClause | T.ImplementsClause)) {
  const children = [child];
  return {
    $type: '_class_heritage_extends_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassHeritageExtendsClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritageImplementsClause(child: T.ImplementsClause) {
  const children = [child];
  return {
    $type: '_class_heritage_implements_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassHeritageImplementsClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClauseNamespaceImport(child: T.NamespaceImport) {
  const children = [child];
  return {
    $type: '_import_clause_namespace_import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportClauseNamespaceImportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClauseNamedImports(child: T.NamedImports) {
  const children = [child];
  return {
    $type: '_import_clause_named_imports' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportClauseNamedImportsTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClauseDefaultImport(child: (T.ImportIdentifier | T.NamespaceImport | T.NamedImports)) {
  const children = [child];
  return {
    $type: '_import_clause_default_import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportClauseDefaultImportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifierName(config: ConfigOf<T.ImportSpecifierName>) {
  const fields = {
    name: config.name,
  };
  return {
    $type: '_import_specifier_name' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.ImportIdentifier) { return _fs(config, importSpecifierName, 'name', value, config?.name); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ImportSpecifierNameTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignatureMappedTypeClause(child: T.MappedTypeClause) {
  const children = [child];
  return {
    $type: '_index_signature_mapped_type_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.IndexSignatureMappedTypeClauseTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementDefaultFromArmStarFrom(config: ConfigOf<T.ExportStatementDefaultFromArmStarFrom>) {
  const fields = {
    source: config.source,
  };
  return {
    $type: '_export_statement_default_from_arm_star_from' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    source(value?: T.String) { return _fs(config, exportStatementDefaultFromArmStarFrom, 'source', value, config?.source); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementDefaultFromArmStarFromTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementDefaultFromArmNsFrom(config: ConfigOf<T.ExportStatementDefaultFromArmNsFrom>) {
  const fields = {
    source: config.source,
  };
  const children = config.children ?? [];
  return {
    $type: '_export_statement_default_from_arm_ns_from' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    source(value?: T.String) { return _fs(config, exportStatementDefaultFromArmNsFrom, 'source', value, config?.source); },
    child(value?: T.NamespaceExport) {
      if (value === undefined) return children[0];
      return exportStatementDefaultFromArmNsFrom({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementDefaultFromArmNsFromTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementDefaultFromArmClauseFrom(config: ConfigOf<T.ExportStatementDefaultFromArmClauseFrom>) {
  const fields = {
    source: config.source,
  };
  const children = config.children ?? [];
  return {
    $type: '_export_statement_default_from_arm_clause_from' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    source(value?: T.String) { return _fs(config, exportStatementDefaultFromArmClauseFrom, 'source', value, config?.source); },
    child(value?: T.ExportClause) {
      if (value === undefined) return children[0];
      return exportStatementDefaultFromArmClauseFrom({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementDefaultFromArmClauseFromTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBodyMethodSig(child: (T.MethodSignature | T.FunctionSignatureAutomaticSemicolon)) {
  const children = [child];
  return {
    $type: '_class_body_method_sig' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassBodyMethodSigTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBodyMember(child: (T.AbstractMethodSignature | T.IndexSignature | T.MethodSignature | T.PublicFieldDefinition | T.Semicolon)) {
  const children = [child];
  return {
    $type: '_class_body_member' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ClassBodyMemberTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function forHeaderLhs(config: ConfigOf<T.ForHeaderLhs>) {
  const fields = {
    left: config.left,
  };
  return {
    $type: '_for_header_lhs' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.LhsExpression | T.ParenthesizedExpression) { return _fs(config, forHeaderLhs, 'left', value, config?.left); },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ForHeaderLhsTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function publicFieldDefinitionDeclareFirst(child?: T.AccessibilityModifier) {
  const children = child != null ? [child] : [];
  return {
    $type: '_public_field_definition_declare_first' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.PublicFieldDefinitionDeclareFirstTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function publicFieldDefinitionAccessorOpt(_config?: ConfigOf<T.PublicFieldDefinitionAccessorOpt>) {
  const fields = {
    accessor: "accessor" as const,
  };
  return {
    $type: '_public_field_definition_accessor_opt' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    get accessor() { return fields.accessor; },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.PublicFieldDefinitionAccessorOptTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpressionSequence(child: T.SequenceExpression) {
  const children = [child];
  return {
    $type: '_parenthesized_expression_sequence' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ParenthesizedExpressionSequenceTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementTypeExport(config: ConfigOf<T.ExportStatementTypeExport>) {
  const fields = {
    source: config.source,
  };
  const children = config.children ?? [];
  return {
    $type: '_export_statement_type_export' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    source(value?: T.String | undefined) { return _fs(config, exportStatementTypeExport, 'source', value, config?.source); },
    child(value?: (T.ExportClause | T.Semicolon)) {
      if (value === undefined) return children[0];
      return exportStatementTypeExport({ ...config, children: [value] });
    },
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementTypeExportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementEqualsExport(child: (T.Expression | T.Semicolon)) {
  const children = [child];
  return {
    $type: '_export_statement_equals_export' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementEqualsExportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementNamespaceExport(child: (T.Identifier | T.Semicolon)) {
  const children = [child];
  return {
    $type: '_export_statement_namespace_export' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.ExportStatementNamespaceExportTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringDouble(...children: (T.UnescapedDoubleStringFragment | T.EscapeSequence)[]) {
  return {
    $type: '_string_double' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.StringDoubleTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringSingle(...children: (T.UnescapedSingleStringFragment | T.EscapeSequence)[]) {
  return {
    $type: '_string_single' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render(this: AnyNodeData): string { return render(this); },
    toEdit(this: AnyNodeData, startOrRange: number | ByteRange, endPos?: number): Edit {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(this: AnyNodeData, target: T.StringSingleTree): Edit { const r = target.range(); return toEdit(this, r); },
  };
}

export function automaticSemicolon(text: string) {
  if (text.length === 0) throw new Error(`_automatic_semicolon: text must be non-empty`);
  return {
    $type: '_automatic_semicolon' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.AutomaticSemicolonTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function htmlComment(text: string) {
  if (text.length === 0) throw new Error(`html_comment: text must be non-empty`);
  return {
    $type: 'html_comment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HtmlCommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function oror(text: string) {
  if (text.length === 0) throw new Error(`||: text must be non-empty`);
  return {
    $type: '||' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.OrorTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxText(text: string) {
  if (text.length === 0) throw new Error(`jsx_text: text must be non-empty`);
  return {
    $type: 'jsx_text' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.JsxTextTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function functionSignatureAutomaticSemicolon(text: string) {
  if (text.length === 0) throw new Error(`_function_signature_automatic_semicolon: text must be non-empty`);
  return {
    $type: '_function_signature_automatic_semicolon' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FunctionSignatureAutomaticSemicolonTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "program": FluentNode<"program", T.Program.Config>;
  "hash_bang_line": T.HashBangLine;
  "export_statement": FluentNode<"export_statement", T.ExportStatement.Config>;
  "namespace_export": FluentNode<"namespace_export", T.NamespaceExport.Config>;
  "export_clause": FluentNode<"export_clause", T.ExportClause.Config>;
  "export_specifier": FluentNode<"export_specifier", T.ExportSpecifier.Config>;
  "import": T.Import;
  "import_statement": FluentNode<"import_statement", T.ImportStatement.Config>;
  "import_clause": FluentNode<"import_clause", T.ImportClause.Config>;
  "namespace_import": FluentNode<"namespace_import", T.NamespaceImport.Config>;
  "named_imports": FluentNode<"named_imports", T.NamedImports.Config>;
  "import_specifier": FluentNode<"import_specifier", T.ImportSpecifier.Config>;
  "import_attribute": FluentNode<"import_attribute", T.ImportAttribute.Config>;
  "expression_statement": FluentNode<"expression_statement", T.ExpressionStatement.Config>;
  "variable_declaration": FluentNode<"variable_declaration", T.VariableDeclaration.Config>;
  "lexical_declaration": FluentNode<"lexical_declaration", T.LexicalDeclaration.Config>;
  "variable_declarator": FluentNode<"variable_declarator", T.VariableDeclarator.Config>;
  "statement_block": FluentNode<"statement_block", T.StatementBlock.Config>;
  "else_clause": FluentNode<"else_clause", T.ElseClause.Config>;
  "if_statement": FluentNode<"if_statement", T.IfStatement.Config>;
  "switch_statement": FluentNode<"switch_statement", T.SwitchStatement.Config>;
  "for_statement": FluentNode<"for_statement", T.ForStatement.Config>;
  "for_in_statement": FluentNode<"for_in_statement", T.ForInStatement.Config>;
  "while_statement": FluentNode<"while_statement", T.WhileStatement.Config>;
  "do_statement": FluentNode<"do_statement", T.DoStatement.Config>;
  "try_statement": FluentNode<"try_statement", T.TryStatement.Config>;
  "with_statement": FluentNode<"with_statement", T.WithStatement.Config>;
  "break_statement": FluentNode<"break_statement", T.BreakStatement.Config>;
  "continue_statement": FluentNode<"continue_statement", T.ContinueStatement.Config>;
  "debugger_statement": FluentNode<"debugger_statement", T.DebuggerStatement.Config>;
  "return_statement": FluentNode<"return_statement", T.ReturnStatement.Config>;
  "throw_statement": FluentNode<"throw_statement", T.ThrowStatement.Config>;
  "labeled_statement": FluentNode<"labeled_statement", T.LabeledStatement.Config>;
  "switch_body": FluentNode<"switch_body", T.SwitchBody.Config>;
  "switch_case": FluentNode<"switch_case", T.SwitchCase.Config>;
  "switch_default": FluentNode<"switch_default", T.SwitchDefault.Config>;
  "catch_clause": FluentNode<"catch_clause", T.CatchClause.Config>;
  "finally_clause": FluentNode<"finally_clause", T.FinallyClause.Config>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", T.ParenthesizedExpression.Config>;
  "yield_expression": FluentNode<"yield_expression", T.YieldExpression.Config>;
  "object": FluentNode<"object", T.Object.Config>;
  "object_pattern": FluentNode<"object_pattern", T.ObjectPattern.Config>;
  "assignment_pattern": FluentNode<"assignment_pattern", T.AssignmentPattern.Config>;
  "object_assignment_pattern": FluentNode<"object_assignment_pattern", T.ObjectAssignmentPattern.Config>;
  "array": FluentNode<"array", T.Array.Config>;
  "array_pattern": FluentNode<"array_pattern", T.ArrayPattern.Config>;
  "jsx_element": FluentNode<"jsx_element", T.JsxElement.Config>;
  "html_character_reference": T.HtmlCharacterReference;
  "jsx_expression": FluentNode<"jsx_expression", T.JsxExpression.Config>;
  "jsx_opening_element": FluentNode<"jsx_opening_element", T.JsxOpeningElement.Config>;
  "jsx_identifier": T.JsxIdentifier;
  "nested_identifier": FluentNode<"nested_identifier", T.NestedIdentifier.Config>;
  "jsx_namespace_name": FluentNode<"jsx_namespace_name", T.JsxNamespaceName.Config>;
  "jsx_closing_element": FluentNode<"jsx_closing_element", T.JsxClosingElement.Config>;
  "jsx_self_closing_element": FluentNode<"jsx_self_closing_element", T.JsxSelfClosingElement.Config>;
  "jsx_attribute": FluentNode<"jsx_attribute", T.JsxAttribute.Config>;
  "unescaped_double_jsx_string_fragment": T.UnescapedDoubleJsxStringFragment;
  "unescaped_single_jsx_string_fragment": T.UnescapedSingleJsxStringFragment;
  "class": FluentNode<"class", T.Class.Config>;
  "class_declaration": FluentNode<"class_declaration", T.ClassDeclaration.Config>;
  "class_heritage": FluentNode<"class_heritage", T.ClassHeritage.Config>;
  "function_expression": FluentNode<"function_expression", T.FunctionExpression.Config>;
  "function_declaration": FluentNode<"function_declaration", T.FunctionDeclaration.Config>;
  "generator_function": FluentNode<"generator_function", T.GeneratorFunction.Config>;
  "generator_function_declaration": FluentNode<"generator_function_declaration", T.GeneratorFunctionDeclaration.Config>;
  "arrow_function": FluentNode<"arrow_function", T.ArrowFunction.Config>;
  "call_expression": FluentNode<"call_expression", T.CallExpression.Config>;
  "new_expression": FluentNode<"new_expression", T.NewExpression.Config>;
  "await_expression": FluentNode<"await_expression", T.AwaitExpression.Config>;
  "member_expression": FluentNode<"member_expression", T.MemberExpression.Config>;
  "subscript_expression": FluentNode<"subscript_expression", T.SubscriptExpression.Config>;
  "_lhs_expression": FluentNode<"_lhs_expression", T.LhsExpression.Config>;
  "assignment_expression": FluentNode<"assignment_expression", T.AssignmentExpression.Config>;
  "augmented_assignment_expression": FluentNode<"augmented_assignment_expression", T.AugmentedAssignmentExpression.Config>;
  "spread_element": FluentNode<"spread_element", T.SpreadElement.Config>;
  "ternary_expression": FluentNode<"ternary_expression", T.TernaryExpression.Config>;
  "binary_expression": FluentNode<"binary_expression", T.BinaryExpression.Config>;
  "unary_expression": FluentNode<"unary_expression", T.UnaryExpression.Config>;
  "update_expression": FluentNode<"update_expression", T.UpdateExpression.Config>;
  "sequence_expression": FluentNode<"sequence_expression", T.SequenceExpression.Config>;
  "string": FluentNode<"string", T.String.Config>;
  "unescaped_double_string_fragment": T.UnescapedDoubleStringFragment;
  "unescaped_single_string_fragment": T.UnescapedSingleStringFragment;
  "escape_sequence": T.EscapeSequence;
  "comment": T.Comment;
  "template_string": FluentNode<"template_string", T.TemplateString.Config>;
  "template_substitution": FluentNode<"template_substitution", T.TemplateSubstitution.Config>;
  "regex": FluentNode<"regex", T.Regex.Config>;
  "regex_pattern": T.RegexPattern;
  "regex_flags": T.RegexFlags;
  "number": T.Number;
  "identifier": T.Identifier;
  "private_property_identifier": T.PrivatePropertyIdentifier;
  "meta_property": T.MetaProperty;
  "this": T.This;
  "super": T.Super;
  "true": T.True;
  "false": T.False;
  "null": T.Null;
  "undefined": T.Undefined;
  "arguments": FluentNode<"arguments", T.Arguments.Config>;
  "decorator": FluentNode<"decorator", T.Decorator.Config>;
  "decorator_member_expression": FluentNode<"decorator_member_expression", T.DecoratorMemberExpression.Config>;
  "decorator_call_expression": FluentNode<"decorator_call_expression", T.DecoratorCallExpression.Config>;
  "class_body": FluentNode<"class_body", T.ClassBody.Config>;
  "field_definition": FluentNode<"field_definition", T.FieldDefinition.Config>;
  "formal_parameters": FluentNode<"formal_parameters", T.FormalParameters.Config>;
  "class_static_block": FluentNode<"class_static_block", T.ClassStaticBlock.Config>;
  "rest_pattern": FluentNode<"rest_pattern", T.RestPattern.Config>;
  "method_definition": FluentNode<"method_definition", T.MethodDefinition.Config>;
  "pair": FluentNode<"pair", T.Pair.Config>;
  "pair_pattern": FluentNode<"pair_pattern", T.PairPattern.Config>;
  "computed_property_name": FluentNode<"computed_property_name", T.ComputedPropertyName.Config>;
  "_reserved_identifier": T.ReservedIdentifier;
  "_string_fragment": T.StringFragment;
  "public_field_definition": FluentNode<"public_field_definition", T.PublicFieldDefinition.Config>;
  "non_null_expression": FluentNode<"non_null_expression", T.NonNullExpression.Config>;
  "method_signature": FluentNode<"method_signature", T.MethodSignature.Config>;
  "abstract_method_signature": FluentNode<"abstract_method_signature", T.AbstractMethodSignature.Config>;
  "function_signature": FluentNode<"function_signature", T.FunctionSignature.Config>;
  "decorator_parenthesized_expression": FluentNode<"decorator_parenthesized_expression", T.DecoratorParenthesizedExpression.Config>;
  "type_assertion": FluentNode<"type_assertion", T.TypeAssertion.Config>;
  "as_expression": FluentNode<"as_expression", T.AsExpression.Config>;
  "satisfies_expression": FluentNode<"satisfies_expression", T.SatisfiesExpression.Config>;
  "instantiation_expression": FluentNode<"instantiation_expression", T.InstantiationExpression.Config>;
  "import_require_clause": FluentNode<"import_require_clause", T.ImportRequireClause.Config>;
  "extends_clause": FluentNode<"extends_clause", T.ExtendsClause.Config>;
  "implements_clause": FluentNode<"implements_clause", T.ImplementsClause.Config>;
  "ambient_declaration": FluentNode<"ambient_declaration", T.AmbientDeclaration.Config>;
  "abstract_class_declaration": FluentNode<"abstract_class_declaration", T.AbstractClassDeclaration.Config>;
  "module": FluentNode<"module", T.Module.Config>;
  "internal_module": FluentNode<"internal_module", T.InternalModule.Config>;
  "import_alias": FluentNode<"import_alias", T.ImportAlias.Config>;
  "nested_type_identifier": FluentNode<"nested_type_identifier", T.NestedTypeIdentifier.Config>;
  "interface_declaration": FluentNode<"interface_declaration", T.InterfaceDeclaration.Config>;
  "extends_type_clause": FluentNode<"extends_type_clause", T.ExtendsTypeClause.Config>;
  "enum_declaration": FluentNode<"enum_declaration", T.EnumDeclaration.Config>;
  "enum_body": FluentNode<"enum_body", T.EnumBody.Config>;
  "enum_assignment": FluentNode<"enum_assignment", T.EnumAssignment.Config>;
  "type_alias_declaration": FluentNode<"type_alias_declaration", T.TypeAliasDeclaration.Config>;
  "accessibility_modifier": T.AccessibilityModifier;
  "override_modifier": T.OverrideModifier;
  "required_parameter": FluentNode<"required_parameter", T.RequiredParameter.Config>;
  "optional_parameter": FluentNode<"optional_parameter", T.OptionalParameter.Config>;
  "omitting_type_annotation": FluentNode<"omitting_type_annotation", T.OmittingTypeAnnotation.Config>;
  "adding_type_annotation": FluentNode<"adding_type_annotation", T.AddingTypeAnnotation.Config>;
  "opting_type_annotation": FluentNode<"opting_type_annotation", T.OptingTypeAnnotation.Config>;
  "type_annotation": FluentNode<"type_annotation", T.TypeAnnotation.Config>;
  "asserts": FluentNode<"asserts", T.Asserts.Config>;
  "asserts_annotation": FluentNode<"asserts_annotation", T.AssertsAnnotation.Config>;
  "tuple_parameter": FluentNode<"tuple_parameter", T.TupleParameter.Config>;
  "optional_tuple_parameter": FluentNode<"optional_tuple_parameter", T.OptionalTupleParameter.Config>;
  "optional_type": FluentNode<"optional_type", T.OptionalType.Config>;
  "rest_type": FluentNode<"rest_type", T.RestType.Config>;
  "constructor_type": FluentNode<"constructor_type", T.ConstructorType.Config>;
  "template_type": FluentNode<"template_type", T.TemplateType.Config>;
  "template_literal_type": FluentNode<"template_literal_type", T.TemplateLiteralType.Config>;
  "infer_type": FluentNode<"infer_type", T.InferType.Config>;
  "conditional_type": FluentNode<"conditional_type", T.ConditionalType.Config>;
  "generic_type": FluentNode<"generic_type", T.GenericType.Config>;
  "type_predicate": FluentNode<"type_predicate", T.TypePredicate.Config>;
  "type_predicate_annotation": FluentNode<"type_predicate_annotation", T.TypePredicateAnnotation.Config>;
  "type_query": FluentNode<"type_query", T.TypeQuery.Config>;
  "index_type_query": FluentNode<"index_type_query", T.IndexTypeQuery.Config>;
  "lookup_type": FluentNode<"lookup_type", T.LookupType.Config>;
  "mapped_type_clause": FluentNode<"mapped_type_clause", T.MappedTypeClause.Config>;
  "literal_type": FluentNode<"literal_type", T.LiteralType.Config>;
  "flow_maybe_type": FluentNode<"flow_maybe_type", T.FlowMaybeType.Config>;
  "parenthesized_type": FluentNode<"parenthesized_type", T.ParenthesizedType.Config>;
  "predefined_type": T.PredefinedType;
  "type_arguments": FluentNode<"type_arguments", T.TypeArguments.Config>;
  "object_type": FluentNode<"object_type", T.ObjectType.Config>;
  "call_signature": FluentNode<"call_signature", T.CallSignature.Config>;
  "property_signature": FluentNode<"property_signature", T.PropertySignature.Config>;
  "type_parameters": FluentNode<"type_parameters", T.TypeParameters.Config>;
  "type_parameter": FluentNode<"type_parameter", T.TypeParameter.Config>;
  "default_type": FluentNode<"default_type", T.DefaultType.Config>;
  "constraint": FluentNode<"constraint", T.Constraint.Config>;
  "construct_signature": FluentNode<"construct_signature", T.ConstructSignature.Config>;
  "index_signature": FluentNode<"index_signature", T.IndexSignature.Config>;
  "array_type": FluentNode<"array_type", T.ArrayType.Config>;
  "tuple_type": FluentNode<"tuple_type", T.TupleType.Config>;
  "readonly_type": FluentNode<"readonly_type", T.ReadonlyType.Config>;
  "union_type": FluentNode<"union_type", T.UnionType.Config>;
  "intersection_type": FluentNode<"intersection_type", T.IntersectionType.Config>;
  "function_type": FluentNode<"function_type", T.FunctionType.Config>;
  "_type_identifier": FluentNode<"_type_identifier", T.TypeIdentifier.Config>;
  "_arrow_function_parameter": FluentNode<"_arrow_function_parameter", T.ArrowFunctionParameter.Config>;
  "_arrow_function__call_signature": FluentNode<"_arrow_function__call_signature", T.ArrowFunctionUCallSignature.Config>;
  "_class_heritage_extends_clause": FluentNode<"_class_heritage_extends_clause", T.ClassHeritageExtendsClause.Config>;
  "_class_heritage_implements_clause": FluentNode<"_class_heritage_implements_clause", T.ClassHeritageImplementsClause.Config>;
  "_import_clause_namespace_import": FluentNode<"_import_clause_namespace_import", T.ImportClauseNamespaceImport.Config>;
  "_import_clause_named_imports": FluentNode<"_import_clause_named_imports", T.ImportClauseNamedImports.Config>;
  "_import_clause_default_import": FluentNode<"_import_clause_default_import", T.ImportClauseDefaultImport.Config>;
  "_import_specifier_name": FluentNode<"_import_specifier_name", T.ImportSpecifierName.Config>;
  "_index_signature_mapped_type_clause": FluentNode<"_index_signature_mapped_type_clause", T.IndexSignatureMappedTypeClause.Config>;
  "_export_statement_default_from_arm_star_from": FluentNode<"_export_statement_default_from_arm_star_from", T.ExportStatementDefaultFromArmStarFrom.Config>;
  "_export_statement_default_from_arm_ns_from": FluentNode<"_export_statement_default_from_arm_ns_from", T.ExportStatementDefaultFromArmNsFrom.Config>;
  "_export_statement_default_from_arm_clause_from": FluentNode<"_export_statement_default_from_arm_clause_from", T.ExportStatementDefaultFromArmClauseFrom.Config>;
  "_class_body_method_sig": FluentNode<"_class_body_method_sig", T.ClassBodyMethodSig.Config>;
  "_class_body_member": FluentNode<"_class_body_member", T.ClassBodyMember.Config>;
  "_for_header_lhs": FluentNode<"_for_header_lhs", T.ForHeaderLhs.Config>;
  "_public_field_definition_declare_first": FluentNode<"_public_field_definition_declare_first", T.PublicFieldDefinitionDeclareFirst.Config>;
  "_public_field_definition_accessor_opt": FluentNode<"_public_field_definition_accessor_opt", T.PublicFieldDefinitionAccessorOpt.Config>;
  "_parenthesized_expression_sequence": FluentNode<"_parenthesized_expression_sequence", T.ParenthesizedExpressionSequence.Config>;
  "_export_statement_type_export": FluentNode<"_export_statement_type_export", T.ExportStatementTypeExport.Config>;
  "_export_statement_equals_export": FluentNode<"_export_statement_equals_export", T.ExportStatementEqualsExport.Config>;
  "_export_statement_namespace_export": FluentNode<"_export_statement_namespace_export", T.ExportStatementNamespaceExport.Config>;
  "_string_double": FluentNode<"_string_double", T.StringDouble.Config>;
  "_string_single": FluentNode<"_string_single", T.StringSingle.Config>;
  "_automatic_semicolon": T.AutomaticSemicolon;
  "html_comment": T.HtmlComment;
  "||": T.Oror;
  "jsx_text": T.JsxText;
  "_function_signature_automatic_semicolon": T.FunctionSignatureAutomaticSemicolon;
};

export const _factoryMap = {
  "program": program,
  "hash_bang_line": hashBangLine,
  "export_statement": exportStatement,
  "namespace_export": namespaceExport,
  "export_clause": exportClause,
  "export_specifier": exportSpecifier,
  "import": import_,
  "import_statement": importStatement,
  "import_clause": importClause,
  "namespace_import": namespaceImport,
  "named_imports": namedImports,
  "import_specifier": importSpecifier,
  "import_attribute": importAttribute,
  "expression_statement": expressionStatement,
  "variable_declaration": variableDeclaration,
  "lexical_declaration": lexicalDeclaration,
  "variable_declarator": variableDeclarator,
  "statement_block": statementBlock,
  "else_clause": elseClause,
  "if_statement": ifStatement,
  "switch_statement": switchStatement,
  "for_statement": forStatement,
  "for_in_statement": forInStatement,
  "while_statement": whileStatement,
  "do_statement": doStatement,
  "try_statement": tryStatement,
  "with_statement": withStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "debugger_statement": debuggerStatement,
  "return_statement": returnStatement,
  "throw_statement": throwStatement,
  "labeled_statement": labeledStatement,
  "switch_body": switchBody,
  "switch_case": switchCase,
  "switch_default": switchDefault,
  "catch_clause": catchClause,
  "finally_clause": finallyClause,
  "parenthesized_expression": parenthesizedExpression,
  "yield_expression": yieldExpression,
  "object": object,
  "object_pattern": objectPattern,
  "assignment_pattern": assignmentPattern,
  "object_assignment_pattern": objectAssignmentPattern,
  "array": array,
  "array_pattern": arrayPattern,
  "jsx_element": jsxElement,
  "html_character_reference": htmlCharacterReference,
  "jsx_expression": jsxExpression,
  "jsx_opening_element": jsxOpeningElement,
  "jsx_identifier": jsxIdentifier,
  "nested_identifier": nestedIdentifier,
  "jsx_namespace_name": jsxNamespaceName,
  "jsx_closing_element": jsxClosingElement,
  "jsx_self_closing_element": jsxSelfClosingElement,
  "jsx_attribute": jsxAttribute,
  "unescaped_double_jsx_string_fragment": unescapedDoubleJsxStringFragment,
  "unescaped_single_jsx_string_fragment": unescapedSingleJsxStringFragment,
  "class": class_,
  "class_declaration": classDeclaration,
  "class_heritage": classHeritage,
  "function_expression": functionExpression,
  "function_declaration": functionDeclaration,
  "generator_function": generatorFunction,
  "generator_function_declaration": generatorFunctionDeclaration,
  "arrow_function": arrowFunction,
  "call_expression": callExpression,
  "new_expression": newExpression,
  "await_expression": awaitExpression,
  "member_expression": memberExpression,
  "subscript_expression": subscriptExpression,
  "_lhs_expression": lhsExpression,
  "assignment_expression": assignmentExpression,
  "augmented_assignment_expression": augmentedAssignmentExpression,
  "spread_element": spreadElement,
  "ternary_expression": ternaryExpression,
  "binary_expression": binaryExpression,
  "unary_expression": unaryExpression,
  "update_expression": updateExpression,
  "sequence_expression": sequenceExpression,
  "string": string,
  "unescaped_double_string_fragment": unescapedDoubleStringFragment,
  "unescaped_single_string_fragment": unescapedSingleStringFragment,
  "escape_sequence": escapeSequence,
  "comment": comment,
  "template_string": templateString,
  "template_substitution": templateSubstitution,
  "regex": regex,
  "regex_pattern": regexPattern,
  "regex_flags": regexFlags,
  "number": number,
  "identifier": identifier,
  "private_property_identifier": privatePropertyIdentifier,
  "meta_property": metaProperty,
  "this": this_,
  "super": super_,
  "true": true_,
  "false": false_,
  "null": null_,
  "undefined": undefined_,
  "arguments": arguments_,
  "decorator": decorator,
  "decorator_member_expression": decoratorMemberExpression,
  "decorator_call_expression": decoratorCallExpression,
  "class_body": classBody,
  "field_definition": fieldDefinition,
  "formal_parameters": formalParameters,
  "class_static_block": classStaticBlock,
  "rest_pattern": restPattern,
  "method_definition": methodDefinition,
  "pair": pair,
  "pair_pattern": pairPattern,
  "computed_property_name": computedPropertyName,
  "_reserved_identifier": reservedIdentifier,
  "_string_fragment": stringFragment,
  "public_field_definition": publicFieldDefinition,
  "non_null_expression": nonNullExpression,
  "method_signature": methodSignature,
  "abstract_method_signature": abstractMethodSignature,
  "function_signature": functionSignature,
  "decorator_parenthesized_expression": decoratorParenthesizedExpression,
  "type_assertion": typeAssertion,
  "as_expression": asExpression,
  "satisfies_expression": satisfiesExpression,
  "instantiation_expression": instantiationExpression,
  "import_require_clause": importRequireClause,
  "extends_clause": extendsClause,
  "implements_clause": implementsClause,
  "ambient_declaration": ambientDeclaration,
  "abstract_class_declaration": abstractClassDeclaration,
  "module": module,
  "internal_module": internalModule,
  "import_alias": importAlias,
  "nested_type_identifier": nestedTypeIdentifier,
  "interface_declaration": interfaceDeclaration,
  "extends_type_clause": extendsTypeClause,
  "enum_declaration": enumDeclaration,
  "enum_body": enumBody,
  "enum_assignment": enumAssignment,
  "type_alias_declaration": typeAliasDeclaration,
  "accessibility_modifier": accessibilityModifier,
  "override_modifier": overrideModifier,
  "required_parameter": requiredParameter,
  "optional_parameter": optionalParameter,
  "omitting_type_annotation": omittingTypeAnnotation,
  "adding_type_annotation": addingTypeAnnotation,
  "opting_type_annotation": optingTypeAnnotation,
  "type_annotation": typeAnnotation,
  "asserts": asserts,
  "asserts_annotation": assertsAnnotation,
  "tuple_parameter": tupleParameter,
  "optional_tuple_parameter": optionalTupleParameter,
  "optional_type": optionalType,
  "rest_type": restType,
  "constructor_type": constructorType,
  "template_type": templateType,
  "template_literal_type": templateLiteralType,
  "infer_type": inferType,
  "conditional_type": conditionalType,
  "generic_type": genericType,
  "type_predicate": typePredicate,
  "type_predicate_annotation": typePredicateAnnotation,
  "type_query": typeQuery,
  "index_type_query": indexTypeQuery,
  "lookup_type": lookupType,
  "mapped_type_clause": mappedTypeClause,
  "literal_type": literalType,
  "flow_maybe_type": flowMaybeType,
  "parenthesized_type": parenthesizedType,
  "predefined_type": predefinedType,
  "type_arguments": typeArguments,
  "object_type": objectType,
  "call_signature": callSignature,
  "property_signature": propertySignature,
  "type_parameters": typeParameters,
  "type_parameter": typeParameter,
  "default_type": defaultType,
  "constraint": constraint,
  "construct_signature": constructSignature,
  "index_signature": indexSignature,
  "array_type": arrayType,
  "tuple_type": tupleType,
  "readonly_type": readonlyType,
  "union_type": unionType,
  "intersection_type": intersectionType,
  "function_type": functionType,
  "_type_identifier": typeIdentifier,
  "_arrow_function_parameter": arrowFunctionParameter,
  "_arrow_function__call_signature": arrowFunctionUCallSignature,
  "_class_heritage_extends_clause": classHeritageExtendsClause,
  "_class_heritage_implements_clause": classHeritageImplementsClause,
  "_import_clause_namespace_import": importClauseNamespaceImport,
  "_import_clause_named_imports": importClauseNamedImports,
  "_import_clause_default_import": importClauseDefaultImport,
  "_import_specifier_name": importSpecifierName,
  "_index_signature_mapped_type_clause": indexSignatureMappedTypeClause,
  "_export_statement_default_from_arm_star_from": exportStatementDefaultFromArmStarFrom,
  "_export_statement_default_from_arm_ns_from": exportStatementDefaultFromArmNsFrom,
  "_export_statement_default_from_arm_clause_from": exportStatementDefaultFromArmClauseFrom,
  "_class_body_method_sig": classBodyMethodSig,
  "_class_body_member": classBodyMember,
  "_for_header_lhs": forHeaderLhs,
  "_public_field_definition_declare_first": publicFieldDefinitionDeclareFirst,
  "_public_field_definition_accessor_opt": publicFieldDefinitionAccessorOpt,
  "_parenthesized_expression_sequence": parenthesizedExpressionSequence,
  "_export_statement_type_export": exportStatementTypeExport,
  "_export_statement_equals_export": exportStatementEqualsExport,
  "_export_statement_namespace_export": exportStatementNamespaceExport,
  "_string_double": stringDouble,
  "_string_single": stringSingle,
  "_automatic_semicolon": automaticSemicolon,
  "html_comment": htmlComment,
  "||": oror,
  "jsx_text": jsxText,
  "_function_signature_automatic_semicolon": functionSignatureAutomaticSemicolon,
} as const;
export type _FactoryMap = typeof _factoryMap;

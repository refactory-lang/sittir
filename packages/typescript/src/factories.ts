// Auto-generated by @sittir/codegen — do not edit

import type { AbstractClassDeclaration, AbstractClassDeclarationTree, AbstractMethodSignature, AbstractMethodSignatureTree, AccessibilityModifier, AccessibilityModifierTree, AddingTypeAnnotation, AddingTypeAnnotationTree, AmbientDeclaration, AmbientDeclarationTree, Arguments, ArgumentsTree, Array, ArrayPattern, ArrayPatternTree, ArrayTree, ArrayType, ArrayTypeTree, ArrowFunction, ArrowFunctionParameter, ArrowFunctionParameterConfig, ArrowFunctionParameterTree, ArrowFunctionTree, ArrowFunctionUCallSignature, ArrowFunctionUCallSignatureConfig, ArrowFunctionUCallSignatureTree, AsExpression, AsExpressionTree, Asserts, AssertsAnnotation, AssertsAnnotationTree, AssertsTree, AssignmentExpression, AssignmentExpressionTree, AssignmentPattern, AssignmentPatternTree, AugmentedAssignmentExpression, AugmentedAssignmentExpressionTree, AwaitExpression, AwaitExpressionTree, BinaryExpression, BinaryExpressionTree, BreakStatement, BreakStatementTree, CallExpression, CallExpressionFunction, CallExpressionFunction2, CallExpressionFunction2Config, CallExpressionFunction2Tree, CallExpressionFunctionConfig, CallExpressionFunctionTree, CallExpressionTokQDot, CallExpressionTokQDotConfig, CallExpressionTokQDotTree, CallExpressionTree, CallSignature, CallSignatureTree, CatchClause, CatchClauseTree, Class, ClassBody, ClassBodyTree, ClassDeclaration, ClassDeclarationTree, ClassHeritage, ClassHeritageTree, ClassStaticBlock, ClassStaticBlockTree, ClassTree, Comment, CommentTree, ComputedPropertyName, ComputedPropertyNameTree, ConditionalType, ConditionalTypeTree, Constraint, ConstraintTree, ConstructSignature, ConstructSignatureTree, ConstructorType, ConstructorTypeTree, ContinueStatement, ContinueStatementTree, DebuggerStatement, DebuggerStatementTree, Declaration, DeclarationTree, Decorator, DecoratorCallExpression, DecoratorCallExpressionTree, DecoratorMemberExpression, DecoratorMemberExpressionTree, DecoratorParenthesizedExpression, DecoratorParenthesizedExpressionTree, DecoratorTree, DefaultType, DefaultTypeTree, DoStatement, DoStatementTree, ElseClause, ElseClauseTree, EnumAssignment, EnumAssignmentTree, EnumBody, EnumBodyTree, EnumDeclaration, EnumDeclarationTree, EscapeSequence, EscapeSequenceTree, ExportClause, ExportClauseTree, ExportSpecifier, ExportSpecifierTree, ExportStatement, ExportStatementExport, ExportStatementExport2, ExportStatementExport2Config, ExportStatementExport2Tree, ExportStatementExport3, ExportStatementExport3Config, ExportStatementExport3Tree, ExportStatementExportConfig, ExportStatementExportTree, ExportStatementForm0, ExportStatementForm0Config, ExportStatementForm0Tree, ExportStatementTree, Expression, ExpressionStatement, ExpressionStatementTree, ExpressionTree, ExtendsClause, ExtendsClauseTree, ExtendsTypeClause, ExtendsTypeClauseTree, False, FalseTree, FieldDefinition, FieldDefinitionTree, FinallyClause, FinallyClauseTree, FlowMaybeType, FlowMaybeTypeTree, ForInStatement, ForInStatementTree, ForStatement, ForStatementTree, FormalParameters, FormalParametersTree, FunctionDeclaration, FunctionDeclarationTree, FunctionExpression, FunctionExpressionTree, FunctionSignature, FunctionSignatureTree, FunctionType, FunctionTypeTree, GeneratorFunction, GeneratorFunctionDeclaration, GeneratorFunctionDeclarationTree, GeneratorFunctionTree, GenericType, GenericTypeTree, HashBangLine, HashBangLineTree, HiddenAugmentedAssignmentLhs, HiddenAugmentedAssignmentLhsTree, HiddenImportIdentifier, HiddenImportIdentifierTree, HiddenJsxStartOpeningElement, HiddenJsxStartOpeningElementTree, HiddenJsxString, HiddenJsxStringTree, HiddenLhsExpression, HiddenLhsExpressionTree, HiddenPropertyName, HiddenPropertyNameTree, HiddenReservedIdentifier, HiddenReservedIdentifierTree, HiddenSemicolon, HiddenSemicolonTree, HiddenTypeIdentifier, HiddenTypeIdentifierTree, HtmlCharacterReference, HtmlCharacterReferenceTree, Identifier, IdentifierTree, IfStatement, IfStatementTree, ImplementsClause, ImplementsClauseTree, Import, ImportAlias, ImportAliasTree, ImportAttribute, ImportAttributeTree, ImportClause, ImportClauseTree, ImportRequireClause, ImportRequireClauseTree, ImportSpecifier, ImportSpecifierAs, ImportSpecifierAsConfig, ImportSpecifierAsTree, ImportSpecifierName, ImportSpecifierNameConfig, ImportSpecifierNameTree, ImportSpecifierTree, ImportStatement, ImportStatementTree, ImportTree, IndexSignature, IndexSignatureColon, IndexSignatureColonConfig, IndexSignatureColonTree, IndexSignatureMappedTypeClause, IndexSignatureMappedTypeClauseConfig, IndexSignatureMappedTypeClauseTree, IndexSignatureTree, IndexTypeQuery, IndexTypeQueryTree, InferType, InferTypeTree, InstantiationExpression, InstantiationExpressionTree, InterfaceBody, InterfaceBodyTree, InterfaceDeclaration, InterfaceDeclarationTree, InternalModule, InternalModuleTree, IntersectionType, IntersectionTypeTree, JsxAttribute, JsxAttributeTree, JsxClosingElement, JsxClosingElementTree, JsxElement, JsxElementTree, JsxExpression, JsxExpressionTree, JsxIdentifier, JsxIdentifierTree, JsxNamespaceName, JsxNamespaceNameTree, JsxOpeningElement, JsxOpeningElementTree, JsxSelfClosingElement, JsxSelfClosingElementTree, LabeledStatement, LabeledStatementTree, LexicalDeclaration, LexicalDeclarationTree, LiteralType, LiteralTypeTree, LookupType, LookupTypeTree, MappedTypeClause, MappedTypeClauseTree, MemberExpression, MemberExpressionTree, MetaProperty, MetaPropertyTree, MethodDefinition, MethodDefinitionTree, MethodSignature, MethodSignatureTree, Module, ModuleTree, NamedImports, NamedImportsTree, NamespaceExport, NamespaceExportTree, NamespaceImport, NamespaceImportTree, NestedIdentifier, NestedIdentifierTree, NestedTypeIdentifier, NestedTypeIdentifierTree, NewExpression, NewExpressionTree, NonNullExpression, NonNullExpressionTree, Null, NullTree, Number, NumberTree, Object, ObjectAssignmentPattern, ObjectAssignmentPatternTree, ObjectPattern, ObjectPatternTree, ObjectTree, ObjectType, ObjectTypeTree, OmittingTypeAnnotation, OmittingTypeAnnotationTree, OptingTypeAnnotation, OptingTypeAnnotationTree, OptionalParameter, OptionalParameterTree, OptionalTupleParameter, OptionalTupleParameterTree, OptionalType, OptionalTypeTree, OverrideModifier, OverrideModifierTree, Pair, PairPattern, PairPatternTree, PairTree, ParenthesizedExpression, ParenthesizedExpressionExpression, ParenthesizedExpressionExpressionConfig, ParenthesizedExpressionExpressionTree, ParenthesizedExpressionSequenceExpression, ParenthesizedExpressionSequenceExpressionConfig, ParenthesizedExpressionSequenceExpressionTree, ParenthesizedExpressionTree, ParenthesizedType, ParenthesizedTypeTree, Pattern, PatternTree, PredefinedType, PredefinedTypeTree, PrimaryExpression, PrimaryExpressionTree, PrimaryType, PrimaryTypeTree, PrivatePropertyIdentifier, PrivatePropertyIdentifierTree, Program, ProgramTree, PropertyIdentifier, PropertyIdentifierTree, PropertySignature, PropertySignatureTree, PublicFieldDefinition, PublicFieldDefinitionTree, ReadonlyType, ReadonlyTypeTree, Regex, RegexFlags, RegexFlagsTree, RegexPattern, RegexPatternTree, RegexTree, RequiredParameter, RequiredParameterTree, RestPattern, RestPatternTree, RestType, RestTypeTree, ReturnStatement, ReturnStatementTree, SatisfiesExpression, SatisfiesExpressionTree, SequenceExpression, SequenceExpressionTree, ShorthandPropertyIdentifier, ShorthandPropertyIdentifierPattern, ShorthandPropertyIdentifierPatternTree, ShorthandPropertyIdentifierTree, SpreadElement, SpreadElementTree, Statement, StatementBlock, StatementBlockTree, StatementBody, StatementBodyConfig, StatementBodyTree, StatementBreakStatement, StatementBreakStatementConfig, StatementBreakStatementTree, StatementContinueStatement, StatementContinueStatementConfig, StatementContinueStatementTree, StatementDebuggerStatement, StatementDebuggerStatementConfig, StatementDebuggerStatementTree, StatementDeclaration, StatementDeclarationConfig, StatementDeclarationTree, StatementDoStatement, StatementDoStatementConfig, StatementDoStatementTree, StatementEmptyStatement, StatementEmptyStatementConfig, StatementEmptyStatementTree, StatementExportStatement, StatementExportStatementConfig, StatementExportStatementTree, StatementExpressionStatement, StatementExpressionStatementConfig, StatementExpressionStatementTree, StatementForInStatement, StatementForInStatementConfig, StatementForInStatementTree, StatementForStatement, StatementForStatementConfig, StatementForStatementTree, StatementIdentifier, StatementIdentifierTree, StatementIfStatement, StatementIfStatementConfig, StatementIfStatementTree, StatementImportStatement, StatementImportStatementConfig, StatementImportStatementTree, StatementLabeledStatement, StatementLabeledStatementConfig, StatementLabeledStatementTree, StatementReturnStatement, StatementReturnStatementConfig, StatementReturnStatementTree, StatementSwitchStatement, StatementSwitchStatementConfig, StatementSwitchStatementTree, StatementThrowStatement, StatementThrowStatementConfig, StatementThrowStatementTree, StatementTree, StatementTryStatement, StatementTryStatementConfig, StatementTryStatementTree, StatementWhileStatement, StatementWhileStatementConfig, StatementWhileStatementTree, StatementWithStatement, StatementWithStatementConfig, StatementWithStatementTree, String, StringFragment, StringFragmentTree, StringTree, SubscriptExpression, SubscriptExpressionTree, Super, SuperTree, SwitchBody, SwitchBodyTree, SwitchCase, SwitchCaseTree, SwitchDefault, SwitchDefaultTree, SwitchStatement, SwitchStatementTree, TemplateLiteralType, TemplateLiteralTypeTree, TemplateString, TemplateStringTree, TemplateSubstitution, TemplateSubstitutionTree, TemplateType, TemplateTypeTree, TernaryExpression, TernaryExpressionTree, This, ThisTree, ThisType, ThisTypeTree, ThrowStatement, ThrowStatementTree, True, TrueTree, TryStatement, TryStatementTree, TupleParameter, TupleParameterTree, TupleType, TupleTypeTree, Type, TypeAliasDeclaration, TypeAliasDeclarationTree, TypeAnnotation, TypeAnnotationTree, TypeArguments, TypeArgumentsTree, TypeAssertion, TypeAssertionTree, TypeIdentifier, TypeIdentifierTree, TypeParameter, TypeParameterTree, TypeParameters, TypeParametersTree, TypePredicate, TypePredicateAnnotation, TypePredicateAnnotationTree, TypePredicateTree, TypeQuery, TypeQueryTree, TypeTree, UnaryExpression, UnaryExpressionTree, Undefined, UndefinedTree, UnescapedDoubleJsxStringFragment, UnescapedDoubleJsxStringFragmentTree, UnescapedDoubleStringFragment, UnescapedDoubleStringFragmentTree, UnescapedSingleJsxStringFragment, UnescapedSingleJsxStringFragmentTree, UnescapedSingleStringFragment, UnescapedSingleStringFragmentTree, UnionType, UnionTypeTree, UpdateExpression, UpdateExpressionTree, VariableDeclaration, VariableDeclarationTree, VariableDeclarator, VariableDeclaratorTree, WhileStatement, WhileStatementTree, WithStatement, WithStatementTree, YieldExpression, YieldExpressionTree } from './types.js';
import type { Edit, AnyNodeData, ConfigOf } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  'abstract',
  'accessor',
  'as',
  'assert',
  'async',
  'await',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'delete',
  'do',
  'else',
  'enum',
  'extends',
  'false',
  'finally',
  'for',
  'from',
  'function',
  'get',
  'global',
  'if',
  'implements',
  'import',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'let',
  'namespace',
  'new',
  'null',
  'of',
  'override',
  'override_modifier',
  'private',
  'protected',
  'public',
  'readonly',
  'require',
  'return',
  'satisfies',
  'set',
  'static',
  'super',
  'switch',
  'this',
  'this_type',
  'throw',
  'true',
  'try',
  'typeof',
  'undefined',
  'using',
  'var',
  'void',
  'while',
  'with',
  'yield',
]);

export function program(config: ConfigOf<Program>) {
  const fields = {
    hash_bang_line: config?.hashBangLine,
    statements: config?.statements,
  };
  return {
    type: 'program' as const,
    named: true as const,
    fields,
    hashBangLine(hashBangLine_?: ConfigOf<Program>['hashBangLine']) { return hashBangLine_ !== undefined ? program({ ...(config ?? {} as ConfigOf<Program>), hashBangLine: hashBangLine_ }) : fields.hash_bang_line; },
    statements(statements_?: ConfigOf<Program>['statements']) { return statements_ !== undefined ? program({ ...(config ?? {} as ConfigOf<Program>), statements: statements_ }) : fields.statements; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hashBangLine(text: string) {
  return {
    type: 'hash_bang_line' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HashBangLineTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function exportStatement(config?: ConfigOf<ExportStatement>) {
  if (config && 'decorator' in config) return exportStatementForm0(config as any);
  return exportStatementExport3(config as any);
}
export function exportStatementForm0(config: ConfigOf<ExportStatementForm0>) {
  const fields = {
    decorator: config?.decorator,
    declaration: config?.declaration,
    value: config?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: NonNullable<ConfigOf<ExportStatementForm0>['decorator']>) { return decorator_.length ? exportStatementForm0({ ...(config ?? {} as ConfigOf<ExportStatementForm0>), decorator: decorator_ }) : fields.decorator; },
    declaration(declaration_?: ConfigOf<ExportStatementForm0>['declaration']) { return declaration_ !== undefined ? exportStatementForm0({ ...(config ?? {} as ConfigOf<ExportStatementForm0>), declaration: declaration_ }) : fields.declaration; },
    value(value_?: ConfigOf<ExportStatementForm0>['value']) { return value_ !== undefined ? exportStatementForm0({ ...(config ?? {} as ConfigOf<ExportStatementForm0>), value: value_ }) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportStatementForm0({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport(config?: ConfigOf<ExportStatementExport>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportStatementExport({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport2(config?: ConfigOf<ExportStatementExport2>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportStatementExport2({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExport2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport3(config?: ConfigOf<ExportStatementExport3>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportStatementExport3({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExport3Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceExport(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'namespace_export' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportClause(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'export_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportSpecifier(config: ConfigOf<ExportSpecifier>) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'export_specifier' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ExportSpecifier>['name']) { return name_ !== undefined ? exportSpecifier({ ...(config ?? {} as ConfigOf<ExportSpecifier>), name: name_ }) : fields.name; },
    alias(alias_?: ConfigOf<ExportSpecifier>['alias']) { return alias_ !== undefined ? exportSpecifier({ ...(config ?? {} as ConfigOf<ExportSpecifier>), alias: alias_ }) : fields.alias; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declaration(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_() {
  return {
    type: 'import' as const,
    named: true as const,
    text: 'import',
    render: () => 'import',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'import' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'import' },
    replace: (t: ImportTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' }; },
  };
}

export function importStatement(config: ConfigOf<ImportStatement>) {
  const fields = {
    import_clause: config?.importClause,
    from_clause: config?.fromClause,
    import_attribute: config?.importAttribute,
    semicolon: config?.semicolon,
  };
  return {
    type: 'import_statement' as const,
    named: true as const,
    fields,
    importClause(importClause_?: ConfigOf<ImportStatement>['importClause']) { return importClause_ !== undefined ? importStatement({ ...(config ?? {} as ConfigOf<ImportStatement>), importClause: importClause_ }) : fields.import_clause; },
    fromClause(fromClause_?: ConfigOf<ImportStatement>['fromClause']) { return fromClause_ !== undefined ? importStatement({ ...(config ?? {} as ConfigOf<ImportStatement>), fromClause: fromClause_ }) : fields.from_clause; },
    importAttribute(importAttribute_?: ConfigOf<ImportStatement>['importAttribute']) { return importAttribute_ !== undefined ? importStatement({ ...(config ?? {} as ConfigOf<ImportStatement>), importAttribute: importAttribute_ }) : fields.import_attribute; },
    semicolon(semicolon_?: ConfigOf<ImportStatement>['semicolon']) { return semicolon_ !== undefined ? importStatement({ ...(config ?? {} as ConfigOf<ImportStatement>), semicolon: semicolon_ }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClause(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'import_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceImport(config: ConfigOf<NamespaceImport>) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'namespace_import' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<NamespaceImport>['identifier']) { return identifier_ !== undefined ? namespaceImport({ ...(config ?? {} as ConfigOf<NamespaceImport>), identifier: identifier_ }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedImports(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'named_imports' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifier(config?: ConfigOf<ImportSpecifier>) {
  if (config && 'alias' in config) return importSpecifierAs(config as any);
  return importSpecifierName(config as any);
}
export function importSpecifierName(config: ConfigOf<ImportSpecifierName>) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ImportSpecifierName>['name']) { return name_ !== undefined ? importSpecifierName({ ...(config ?? {} as ConfigOf<ImportSpecifierName>), name: name_ }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importSpecifierAs(config: ConfigOf<ImportSpecifierAs>) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ImportSpecifierAs>['name']) { return name_ !== undefined ? importSpecifierAs({ ...(config ?? {} as ConfigOf<ImportSpecifierAs>), name: name_ }) : fields.name; },
    alias(alias_?: ConfigOf<ImportSpecifierAs>['alias']) { return alias_ !== undefined ? importSpecifierAs({ ...(config ?? {} as ConfigOf<ImportSpecifierAs>), alias: alias_ }) : fields.alias; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierAsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAttribute(config: ConfigOf<ImportAttribute>) {
  const fields = {
    object: config?.object,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_attribute' as const,
    named: true as const,
    fields,
    children,
    object(object_?: ConfigOf<ImportAttribute>['object']) { return object_ !== undefined ? importAttribute({ ...(config ?? {} as ConfigOf<ImportAttribute>), object: object_ }) : fields.object; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return importAttribute({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statement(config?: ConfigOf<Statement>) {
  if (config && 'body' in config) return statementBody(config as any);
  return statementLabeledStatement(config as any);
}
export function statementExportStatement(config?: ConfigOf<StatementExportStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementExportStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementImportStatement(config?: ConfigOf<StatementImportStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementImportStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDebuggerStatement(config?: ConfigOf<StatementDebuggerStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementDebuggerStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementExpressionStatement(config?: ConfigOf<StatementExpressionStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementExpressionStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDeclaration(config?: ConfigOf<StatementDeclaration>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementBody(config: ConfigOf<StatementBody>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'statement' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<StatementBody>['body']) { return body_ !== undefined ? statementBody({ ...(config ?? {} as ConfigOf<StatementBody>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementIfStatement(config?: ConfigOf<StatementIfStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementIfStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementIfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementSwitchStatement(config?: ConfigOf<StatementSwitchStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementSwitchStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementSwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementForStatement(config?: ConfigOf<StatementForStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementForStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementForInStatement(config?: ConfigOf<StatementForInStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementForInStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementWhileStatement(config?: ConfigOf<StatementWhileStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementWhileStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementWhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDoStatement(config?: ConfigOf<StatementDoStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementDoStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementTryStatement(config?: ConfigOf<StatementTryStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementTryStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementWithStatement(config?: ConfigOf<StatementWithStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementWithStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementWithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementBreakStatement(config?: ConfigOf<StatementBreakStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementBreakStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementContinueStatement(config?: ConfigOf<StatementContinueStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementContinueStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementReturnStatement(config?: ConfigOf<StatementReturnStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementReturnStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementThrowStatement(config?: ConfigOf<StatementThrowStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementThrowStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementEmptyStatement(config?: ConfigOf<StatementEmptyStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementEmptyStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementEmptyStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementLabeledStatement(config?: ConfigOf<StatementLabeledStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementLabeledStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementLabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclaration(config: ConfigOf<VariableDeclaration>) {
  const fields = {
    declarators: config?.declarators,
    semicolon: config?.semicolon,
  };
  return {
    type: 'variable_declaration' as const,
    named: true as const,
    fields,
    declarators(declarators_?: ConfigOf<VariableDeclaration>['declarators']) { return declarators_ !== undefined ? variableDeclaration({ ...(config ?? {} as ConfigOf<VariableDeclaration>), declarators: declarators_ }) : fields.declarators; },
    semicolon(semicolon_?: ConfigOf<VariableDeclaration>['semicolon']) { return semicolon_ !== undefined ? variableDeclaration({ ...(config ?? {} as ConfigOf<VariableDeclaration>), semicolon: semicolon_ }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lexicalDeclaration(config: ConfigOf<LexicalDeclaration>) {
  const fields = {
    kind: config?.kind,
    declarators: config?.declarators,
    semicolon: config?.semicolon,
  };
  return {
    type: 'lexical_declaration' as const,
    named: true as const,
    fields,
    kind(kind_?: ConfigOf<LexicalDeclaration>['kind']) { return kind_ !== undefined ? lexicalDeclaration({ ...(config ?? {} as ConfigOf<LexicalDeclaration>), kind: kind_ }) : fields.kind; },
    declarators(declarators_?: ConfigOf<LexicalDeclaration>['declarators']) { return declarators_ !== undefined ? lexicalDeclaration({ ...(config ?? {} as ConfigOf<LexicalDeclaration>), declarators: declarators_ }) : fields.declarators; },
    semicolon(semicolon_?: ConfigOf<LexicalDeclaration>['semicolon']) { return semicolon_ !== undefined ? lexicalDeclaration({ ...(config ?? {} as ConfigOf<LexicalDeclaration>), semicolon: semicolon_ }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclarator(config: ConfigOf<VariableDeclarator>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'variable_declarator' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<VariableDeclarator>['name']) { return name_ !== undefined ? variableDeclarator({ ...(config ?? {} as ConfigOf<VariableDeclarator>), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<VariableDeclarator>['type']) { return type !== undefined ? variableDeclarator({ ...(config ?? {} as ConfigOf<VariableDeclarator>), type: type }) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return variableDeclarator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statementBlock(config: ConfigOf<StatementBlock>) {
  const fields = {
    statements: config?.statements,
    automatic_semicolon: config?.automaticSemicolon,
  };
  return {
    type: 'statement_block' as const,
    named: true as const,
    fields,
    statements(statements_?: ConfigOf<StatementBlock>['statements']) { return statements_ !== undefined ? statementBlock({ ...(config ?? {} as ConfigOf<StatementBlock>), statements: statements_ }) : fields.statements; },
    automaticSemicolon(automaticSemicolon_?: ConfigOf<StatementBlock>['automaticSemicolon']) { return automaticSemicolon_ !== undefined ? statementBlock({ ...(config ?? {} as ConfigOf<StatementBlock>), automaticSemicolon: automaticSemicolon_ }) : fields.automatic_semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: ConfigOf<ElseClause>) {
  const fields = {
    statement: config?.statement,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    statement(statement_?: ConfigOf<ElseClause>['statement']) { return statement_ !== undefined ? elseClause({ ...(config ?? {} as ConfigOf<ElseClause>), statement: statement_ }) : fields.statement; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifStatement(config: ConfigOf<IfStatement>) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: ConfigOf<IfStatement>['condition']) { return condition_ !== undefined ? ifStatement({ ...(config ?? {} as ConfigOf<IfStatement>), condition: condition_ }) : fields.condition; },
    consequence(consequence_?: ConfigOf<IfStatement>['consequence']) { return consequence_ !== undefined ? ifStatement({ ...(config ?? {} as ConfigOf<IfStatement>), consequence: consequence_ }) : fields.consequence; },
    alternative(alternative_?: ConfigOf<IfStatement>['alternative']) { return alternative_ !== undefined ? ifStatement({ ...(config ?? {} as ConfigOf<IfStatement>), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchStatement(config: ConfigOf<SwitchStatement>) {
  const fields = {
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'switch_statement' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<SwitchStatement>['value']) { return value_ !== undefined ? switchStatement({ ...(config ?? {} as ConfigOf<SwitchStatement>), value: value_ }) : fields.value; },
    body(body_?: ConfigOf<SwitchStatement>['body']) { return body_ !== undefined ? switchStatement({ ...(config ?? {} as ConfigOf<SwitchStatement>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: ConfigOf<ForStatement>) {
  const fields = {
    initializer: config?.initializer,
    condition: config?.condition,
    increment: config?.increment,
    body: config?.body,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    initializer(initializer_?: ConfigOf<ForStatement>['initializer']) { return initializer_ !== undefined ? forStatement({ ...(config ?? {} as ConfigOf<ForStatement>), initializer: initializer_ }) : fields.initializer; },
    condition(condition_?: ConfigOf<ForStatement>['condition']) { return condition_ !== undefined ? forStatement({ ...(config ?? {} as ConfigOf<ForStatement>), condition: condition_ }) : fields.condition; },
    increment(increment_?: ConfigOf<ForStatement>['increment']) { return increment_ !== undefined ? forStatement({ ...(config ?? {} as ConfigOf<ForStatement>), increment: increment_ }) : fields.increment; },
    body(body_?: ConfigOf<ForStatement>['body']) { return body_ !== undefined ? forStatement({ ...(config ?? {} as ConfigOf<ForStatement>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInStatement(config: ConfigOf<ForInStatement>) {
  const fields = {
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<ForInStatement>['body']) { return body_ !== undefined ? forInStatement({ ...(config ?? {} as ConfigOf<ForInStatement>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return forInStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: ConfigOf<WhileStatement>) {
  const fields = {
    condition: config?.condition,
    body: config?.body,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: ConfigOf<WhileStatement>['condition']) { return condition_ !== undefined ? whileStatement({ ...(config ?? {} as ConfigOf<WhileStatement>), condition: condition_ }) : fields.condition; },
    body(body_?: ConfigOf<WhileStatement>['body']) { return body_ !== undefined ? whileStatement({ ...(config ?? {} as ConfigOf<WhileStatement>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function doStatement(config: ConfigOf<DoStatement>) {
  const fields = {
    body: config?.body,
    condition: config?.condition,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'do_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<DoStatement>['body']) { return body_ !== undefined ? doStatement({ ...(config ?? {} as ConfigOf<DoStatement>), body: body_ }) : fields.body; },
    condition(condition_?: ConfigOf<DoStatement>['condition']) { return condition_ !== undefined ? doStatement({ ...(config ?? {} as ConfigOf<DoStatement>), condition: condition_ }) : fields.condition; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return doStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: ConfigOf<TryStatement>) {
  const fields = {
    body: config?.body,
    handler: config?.handler,
    finalizer: config?.finalizer,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<TryStatement>['body']) { return body_ !== undefined ? tryStatement({ ...(config ?? {} as ConfigOf<TryStatement>), body: body_ }) : fields.body; },
    handler(handler_?: ConfigOf<TryStatement>['handler']) { return handler_ !== undefined ? tryStatement({ ...(config ?? {} as ConfigOf<TryStatement>), handler: handler_ }) : fields.handler; },
    finalizer(finalizer_?: ConfigOf<TryStatement>['finalizer']) { return finalizer_ !== undefined ? tryStatement({ ...(config ?? {} as ConfigOf<TryStatement>), finalizer: finalizer_ }) : fields.finalizer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: ConfigOf<WithStatement>) {
  const fields = {
    object: config?.object,
    body: config?.body,
  };
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<WithStatement>['object']) { return object_ !== undefined ? withStatement({ ...(config ?? {} as ConfigOf<WithStatement>), object: object_ }) : fields.object; },
    body(body_?: ConfigOf<WithStatement>['body']) { return body_ !== undefined ? withStatement({ ...(config ?? {} as ConfigOf<WithStatement>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakStatement(config: ConfigOf<BreakStatement>) {
  const fields = {
    label: config?.label,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'break_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: ConfigOf<BreakStatement>['label']) { return label_ !== undefined ? breakStatement({ ...(config ?? {} as ConfigOf<BreakStatement>), label: label_ }) : fields.label; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return breakStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueStatement(config: ConfigOf<ContinueStatement>) {
  const fields = {
    label: config?.label,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'continue_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: ConfigOf<ContinueStatement>['label']) { return label_ !== undefined ? continueStatement({ ...(config ?? {} as ConfigOf<ContinueStatement>), label: label_ }) : fields.label; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return continueStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function debuggerStatement(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'debugger_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function throwStatement(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'throw_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function labeledStatement(config: ConfigOf<LabeledStatement>) {
  const fields = {
    label: config?.label,
    body: config?.body,
  };
  return {
    type: 'labeled_statement' as const,
    named: true as const,
    fields,
    label(label_?: ConfigOf<LabeledStatement>['label']) { return label_ !== undefined ? labeledStatement({ ...(config ?? {} as ConfigOf<LabeledStatement>), label: label_ }) : fields.label; },
    body(body_?: ConfigOf<LabeledStatement>['body']) { return body_ !== undefined ? labeledStatement({ ...(config ?? {} as ConfigOf<LabeledStatement>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchBody(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'switch_body' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchCase(config: ConfigOf<SwitchCase>) {
  const fields = {
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'switch_case' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<SwitchCase>['value']) { return value_ !== undefined ? switchCase({ ...(config ?? {} as ConfigOf<SwitchCase>), value: value_ }) : fields.value; },
    body(body_?: ConfigOf<SwitchCase>['body']) { return body_ !== undefined ? switchCase({ ...(config ?? {} as ConfigOf<SwitchCase>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchDefault(config: ConfigOf<SwitchDefault>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'switch_default' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<SwitchDefault>['body']) { return body_ !== undefined ? switchDefault({ ...(config ?? {} as ConfigOf<SwitchDefault>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function catchClause(config: ConfigOf<CatchClause>) {
  const fields = {
    parameter: config?.parameter,
    type: config?.type,
    body: config?.body,
  };
  return {
    type: 'catch_clause' as const,
    named: true as const,
    fields,
    parameter(parameter_?: ConfigOf<CatchClause>['parameter']) { return parameter_ !== undefined ? catchClause({ ...(config ?? {} as ConfigOf<CatchClause>), parameter: parameter_ }) : fields.parameter; },
    typeField(type?: ConfigOf<CatchClause>['type']) { return type !== undefined ? catchClause({ ...(config ?? {} as ConfigOf<CatchClause>), type: type }) : fields.type; },
    body(body_?: ConfigOf<CatchClause>['body']) { return body_ !== undefined ? catchClause({ ...(config ?? {} as ConfigOf<CatchClause>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: ConfigOf<FinallyClause>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<FinallyClause>['body']) { return body_ !== undefined ? finallyClause({ ...(config ?? {} as ConfigOf<FinallyClause>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(config?: ConfigOf<ParenthesizedExpression>) {
  if (config && 'type' in config) return parenthesizedExpressionExpression(config as any);
  return parenthesizedExpressionSequenceExpression(config as any);
}
export function parenthesizedExpressionExpression(config: ConfigOf<ParenthesizedExpressionExpression>) {
  const fields = {
    type: config?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: ConfigOf<ParenthesizedExpressionExpression>['type']) { return type !== undefined ? parenthesizedExpressionExpression({ ...(config ?? {} as ConfigOf<ParenthesizedExpressionExpression>), type: type }) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return parenthesizedExpressionExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parenthesizedExpressionSequenceExpression(config?: ConfigOf<ParenthesizedExpressionSequenceExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parenthesizedExpressionSequenceExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionSequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expression(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function primaryExpression(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(config: ConfigOf<YieldExpression>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'yield_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<YieldExpression>['expression']) { return expression_ !== undefined ? yieldExpression({ ...(config ?? {} as ConfigOf<YieldExpression>), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function object(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'object' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectPattern(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'object_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentPattern(config: ConfigOf<AssignmentPattern>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_pattern' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<AssignmentPattern>['left']) { return left_ !== undefined ? assignmentPattern({ ...(config ?? {} as ConfigOf<AssignmentPattern>), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<AssignmentPattern>['right']) { return right_ !== undefined ? assignmentPattern({ ...(config ?? {} as ConfigOf<AssignmentPattern>), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectAssignmentPattern(config: ConfigOf<ObjectAssignmentPattern>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'object_assignment_pattern' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<ObjectAssignmentPattern>['left']) { return left_ !== undefined ? objectAssignmentPattern({ ...(config ?? {} as ConfigOf<ObjectAssignmentPattern>), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<ObjectAssignmentPattern>['right']) { return right_ !== undefined ? objectAssignmentPattern({ ...(config ?? {} as ConfigOf<ObjectAssignmentPattern>), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function array(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'array' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayPattern(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'array_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxElement(config: ConfigOf<JsxElement>) {
  const fields = {
    open_tag: config?.openTag,
    close_tag: config?.closeTag,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'jsx_element' as const,
    named: true as const,
    fields,
    children,
    openTag(openTag_?: ConfigOf<JsxElement>['openTag']) { return openTag_ !== undefined ? jsxElement({ ...(config ?? {} as ConfigOf<JsxElement>), openTag: openTag_ }) : fields.open_tag; },
    closeTag(closeTag_?: ConfigOf<JsxElement>['closeTag']) { return closeTag_ !== undefined ? jsxElement({ ...(config ?? {} as ConfigOf<JsxElement>), closeTag: closeTag_ }) : fields.close_tag; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return jsxElement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlCharacterReference(text: string) {
  return {
    type: 'html_character_reference' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HtmlCharacterReferenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxExpression(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'jsx_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxOpeningElement(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'jsx_opening_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxIdentifier(text: string) {
  return {
    type: 'jsx_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: JsxIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function nestedIdentifier(config: ConfigOf<NestedIdentifier>) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'nested_identifier' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<NestedIdentifier>['object']) { return object_ !== undefined ? nestedIdentifier({ ...(config ?? {} as ConfigOf<NestedIdentifier>), object: object_ }) : fields.object; },
    property(property_?: ConfigOf<NestedIdentifier>['property']) { return property_ !== undefined ? nestedIdentifier({ ...(config ?? {} as ConfigOf<NestedIdentifier>), property: property_ }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxNamespaceName(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'jsx_namespace_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxNamespaceNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxClosingElement(config?: ConfigOf<JsxClosingElement>) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'jsx_closing_element' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<JsxClosingElement>['name']) { return name_ !== undefined ? jsxClosingElement({ ...(config ?? {} as ConfigOf<JsxClosingElement>), name: name_ }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxSelfClosingElement(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'jsx_self_closing_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxSelfClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxAttribute(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'jsx_attribute' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenJsxString(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: '_jsx_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenJsxStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleJsxStringFragment(text: string) {
  return {
    type: 'unescaped_double_jsx_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedDoubleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleJsxStringFragment(text: string) {
  return {
    type: 'unescaped_single_jsx_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedSingleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function class_(config: ConfigOf<Class>) {
  const fields = {
    class_heritage: config?.classHeritage,
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class' as const,
    named: true as const,
    fields,
    children,
    classHeritage(classHeritage_?: ConfigOf<Class>['classHeritage']) { return classHeritage_ !== undefined ? class_({ ...(config ?? {} as ConfigOf<Class>), classHeritage: classHeritage_ }) : fields.class_heritage; },
    name(name_?: ConfigOf<Class>['name']) { return name_ !== undefined ? class_({ ...(config ?? {} as ConfigOf<Class>), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<Class>['typeParameters']) { return typeParameters_ !== undefined ? class_({ ...(config ?? {} as ConfigOf<Class>), typeParameters: typeParameters_ }) : fields.type_parameters; },
    body(body_?: ConfigOf<Class>['body']) { return body_ !== undefined ? class_({ ...(config ?? {} as ConfigOf<Class>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return class_({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDeclaration(config: ConfigOf<ClassDeclaration>) {
  const fields = {
    class_heritage: config?.classHeritage,
    name: config?.name,
    type_parameters: config?.typeParameters,
    automatic_semicolon: config?.automaticSemicolon,
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_declaration' as const,
    named: true as const,
    fields,
    children,
    classHeritage(classHeritage_?: ConfigOf<ClassDeclaration>['classHeritage']) { return classHeritage_ !== undefined ? classDeclaration({ ...(config ?? {} as ConfigOf<ClassDeclaration>), classHeritage: classHeritage_ }) : fields.class_heritage; },
    name(name_?: ConfigOf<ClassDeclaration>['name']) { return name_ !== undefined ? classDeclaration({ ...(config ?? {} as ConfigOf<ClassDeclaration>), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<ClassDeclaration>['typeParameters']) { return typeParameters_ !== undefined ? classDeclaration({ ...(config ?? {} as ConfigOf<ClassDeclaration>), typeParameters: typeParameters_ }) : fields.type_parameters; },
    automaticSemicolon(automaticSemicolon_?: ConfigOf<ClassDeclaration>['automaticSemicolon']) { return automaticSemicolon_ !== undefined ? classDeclaration({ ...(config ?? {} as ConfigOf<ClassDeclaration>), automaticSemicolon: automaticSemicolon_ }) : fields.automatic_semicolon; },
    body(body_?: ConfigOf<ClassDeclaration>['body']) { return body_ !== undefined ? classDeclaration({ ...(config ?? {} as ConfigOf<ClassDeclaration>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return classDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritage(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'class_heritage' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionExpression(config: ConfigOf<FunctionExpression>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_expression' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<FunctionExpression>['name']) { return name_ !== undefined ? functionExpression({ ...(config ?? {} as ConfigOf<FunctionExpression>), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<FunctionExpression>['body']) { return body_ !== undefined ? functionExpression({ ...(config ?? {} as ConfigOf<FunctionExpression>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDeclaration(config: ConfigOf<FunctionDeclaration>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<FunctionDeclaration>['name']) { return name_ !== undefined ? functionDeclaration({ ...(config ?? {} as ConfigOf<FunctionDeclaration>), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<FunctionDeclaration>['body']) { return body_ !== undefined ? functionDeclaration({ ...(config ?? {} as ConfigOf<FunctionDeclaration>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunction(config: ConfigOf<GeneratorFunction>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generator_function' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<GeneratorFunction>['name']) { return name_ !== undefined ? generatorFunction({ ...(config ?? {} as ConfigOf<GeneratorFunction>), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<GeneratorFunction>['body']) { return body_ !== undefined ? generatorFunction({ ...(config ?? {} as ConfigOf<GeneratorFunction>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return generatorFunction({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunctionDeclaration(config: ConfigOf<GeneratorFunctionDeclaration>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generator_function_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<GeneratorFunctionDeclaration>['name']) { return name_ !== undefined ? generatorFunctionDeclaration({ ...(config ?? {} as ConfigOf<GeneratorFunctionDeclaration>), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<GeneratorFunctionDeclaration>['body']) { return body_ !== undefined ? generatorFunctionDeclaration({ ...(config ?? {} as ConfigOf<GeneratorFunctionDeclaration>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return generatorFunctionDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunction(config?: ConfigOf<ArrowFunction>) {
  if (config && 'parameter' in config) return arrowFunctionParameter(config as any);
  return arrowFunctionUCallSignature(config as any);
}
export function arrowFunctionParameter(config: ConfigOf<ArrowFunctionParameter>) {
  const fields = {
    parameter: config?.parameter,
  };
  return {
    type: 'arrow_function' as const,
    named: true as const,
    fields,
    parameter(parameter_?: ConfigOf<ArrowFunctionParameter>['parameter']) { return parameter_ !== undefined ? arrowFunctionParameter({ ...(config ?? {} as ConfigOf<ArrowFunctionParameter>), parameter: parameter_ }) : fields.parameter; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function arrowFunctionUCallSignature(config?: ConfigOf<ArrowFunctionUCallSignature>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'arrow_function' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return arrowFunctionUCallSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionUCallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config?: ConfigOf<CallExpression>) {
  if (config && 'typeArguments' in config) return callExpressionFunction(config as any);
  if (config && 'typeArguments' in config) return callExpressionTokQDot(config as any);
  return callExpressionFunction2(config as any);
}
export function callExpressionFunction(config: ConfigOf<CallExpressionFunction>) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<CallExpressionFunction>['function']) { return function_ !== undefined ? callExpressionFunction({ ...(config ?? {} as ConfigOf<CallExpressionFunction>), function: function_ }) : fields.function; },
    typeArguments(typeArguments_?: ConfigOf<CallExpressionFunction>['typeArguments']) { return typeArguments_ !== undefined ? callExpressionFunction({ ...(config ?? {} as ConfigOf<CallExpressionFunction>), typeArguments: typeArguments_ }) : fields.type_arguments; },
    arguments(arguments_?: ConfigOf<CallExpressionFunction>['arguments']) { return arguments_ !== undefined ? callExpressionFunction({ ...(config ?? {} as ConfigOf<CallExpressionFunction>), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionFunction2(config: ConfigOf<CallExpressionFunction2>) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<CallExpressionFunction2>['function']) { return function_ !== undefined ? callExpressionFunction2({ ...(config ?? {} as ConfigOf<CallExpressionFunction2>), function: function_ }) : fields.function; },
    arguments(arguments_?: ConfigOf<CallExpressionFunction2>['arguments']) { return arguments_ !== undefined ? callExpressionFunction2({ ...(config ?? {} as ConfigOf<CallExpressionFunction2>), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionFunction2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionTokQDot(config: ConfigOf<CallExpressionTokQDot>) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<CallExpressionTokQDot>['function']) { return function_ !== undefined ? callExpressionTokQDot({ ...(config ?? {} as ConfigOf<CallExpressionTokQDot>), function: function_ }) : fields.function; },
    typeArguments(typeArguments_?: ConfigOf<CallExpressionTokQDot>['typeArguments']) { return typeArguments_ !== undefined ? callExpressionTokQDot({ ...(config ?? {} as ConfigOf<CallExpressionTokQDot>), typeArguments: typeArguments_ }) : fields.type_arguments; },
    arguments(arguments_?: ConfigOf<CallExpressionTokQDot>['arguments']) { return arguments_ !== undefined ? callExpressionTokQDot({ ...(config ?? {} as ConfigOf<CallExpressionTokQDot>), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTokQDotTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function newExpression(config: ConfigOf<NewExpression>) {
  const fields = {
    constructor: config?.constructor,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'new_expression' as const,
    named: true as const,
    fields,
    constructor(constructor_?: ConfigOf<NewExpression>['constructor']) { return constructor_ !== undefined ? newExpression({ ...(config ?? {} as ConfigOf<NewExpression>), constructor: constructor_ }) : fields.constructor; },
    typeArguments(typeArguments_?: ConfigOf<NewExpression>['typeArguments']) { return typeArguments_ !== undefined ? newExpression({ ...(config ?? {} as ConfigOf<NewExpression>), typeArguments: typeArguments_ }) : fields.type_arguments; },
    arguments(arguments_?: ConfigOf<NewExpression>['arguments']) { return arguments_ !== undefined ? newExpression({ ...(config ?? {} as ConfigOf<NewExpression>), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(config: ConfigOf<AwaitExpression>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'await_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<AwaitExpression>['expression']) { return expression_ !== undefined ? awaitExpression({ ...(config ?? {} as ConfigOf<AwaitExpression>), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberExpression(config: ConfigOf<MemberExpression>) {
  const fields = {
    object: config?.object,
    optional_chain: config?.optionalChain,
    property: config?.property,
  };
  return {
    type: 'member_expression' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<MemberExpression>['object']) { return object_ !== undefined ? memberExpression({ ...(config ?? {} as ConfigOf<MemberExpression>), object: object_ }) : fields.object; },
    optionalChain(optionalChain_?: ConfigOf<MemberExpression>['optionalChain']) { return optionalChain_ !== undefined ? memberExpression({ ...(config ?? {} as ConfigOf<MemberExpression>), optionalChain: optionalChain_ }) : fields.optional_chain; },
    property(property_?: ConfigOf<MemberExpression>['property']) { return property_ !== undefined ? memberExpression({ ...(config ?? {} as ConfigOf<MemberExpression>), property: property_ }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscriptExpression(config: ConfigOf<SubscriptExpression>) {
  const fields = {
    object: config?.object,
    optional_chain: config?.optionalChain,
    index: config?.index,
  };
  return {
    type: 'subscript_expression' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<SubscriptExpression>['object']) { return object_ !== undefined ? subscriptExpression({ ...(config ?? {} as ConfigOf<SubscriptExpression>), object: object_ }) : fields.object; },
    optionalChain(optionalChain_?: ConfigOf<SubscriptExpression>['optionalChain']) { return optionalChain_ !== undefined ? subscriptExpression({ ...(config ?? {} as ConfigOf<SubscriptExpression>), optionalChain: optionalChain_ }) : fields.optional_chain; },
    index(index_?: ConfigOf<SubscriptExpression>['index']) { return index_ !== undefined ? subscriptExpression({ ...(config ?? {} as ConfigOf<SubscriptExpression>), index: index_ }) : fields.index; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenLhsExpression(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: '_lhs_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenLhsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: ConfigOf<AssignmentExpression>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<AssignmentExpression>['left']) { return left_ !== undefined ? assignmentExpression({ ...(config ?? {} as ConfigOf<AssignmentExpression>), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<AssignmentExpression>['right']) { return right_ !== undefined ? assignmentExpression({ ...(config ?? {} as ConfigOf<AssignmentExpression>), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenAugmentedAssignmentLhs(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: '_augmented_assignment_lhs' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenAugmentedAssignmentLhsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignmentExpression(config: ConfigOf<AugmentedAssignmentExpression>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'augmented_assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<AugmentedAssignmentExpression>['left']) { return left_ !== undefined ? augmentedAssignmentExpression({ ...(config ?? {} as ConfigOf<AugmentedAssignmentExpression>), left: left_ }) : fields.left; },
    operator(operator_?: ConfigOf<AugmentedAssignmentExpression>['operator']) { return operator_ !== undefined ? augmentedAssignmentExpression({ ...(config ?? {} as ConfigOf<AugmentedAssignmentExpression>), operator: operator_ }) : fields.operator; },
    right(right_?: ConfigOf<AugmentedAssignmentExpression>['right']) { return right_ !== undefined ? augmentedAssignmentExpression({ ...(config ?? {} as ConfigOf<AugmentedAssignmentExpression>), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function spreadElement(config: ConfigOf<SpreadElement>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'spread_element' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<SpreadElement>['expression']) { return expression_ !== undefined ? spreadElement({ ...(config ?? {} as ConfigOf<SpreadElement>), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ternaryExpression(config: ConfigOf<TernaryExpression>) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'ternary_expression' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: ConfigOf<TernaryExpression>['condition']) { return condition_ !== undefined ? ternaryExpression({ ...(config ?? {} as ConfigOf<TernaryExpression>), condition: condition_ }) : fields.condition; },
    consequence(consequence_?: ConfigOf<TernaryExpression>['consequence']) { return consequence_ !== undefined ? ternaryExpression({ ...(config ?? {} as ConfigOf<TernaryExpression>), consequence: consequence_ }) : fields.consequence; },
    alternative(alternative_?: ConfigOf<TernaryExpression>['alternative']) { return alternative_ !== undefined ? ternaryExpression({ ...(config ?? {} as ConfigOf<TernaryExpression>), alternative: alternative_ }) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return ternaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: ConfigOf<BinaryExpression>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<BinaryExpression>['left']) { return left_ !== undefined ? binaryExpression({ ...(config ?? {} as ConfigOf<BinaryExpression>), left: left_ }) : fields.left; },
    operator(operator_?: ConfigOf<BinaryExpression>['operator']) { return operator_ !== undefined ? binaryExpression({ ...(config ?? {} as ConfigOf<BinaryExpression>), operator: operator_ }) : fields.operator; },
    right(right_?: ConfigOf<BinaryExpression>['right']) { return right_ !== undefined ? binaryExpression({ ...(config ?? {} as ConfigOf<BinaryExpression>), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: ConfigOf<UnaryExpression>) {
  const fields = {
    operator: config?.operator,
    argument: config?.argument,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    operator(operator_?: ConfigOf<UnaryExpression>['operator']) { return operator_ !== undefined ? unaryExpression({ ...(config ?? {} as ConfigOf<UnaryExpression>), operator: operator_ }) : fields.operator; },
    argument(argument_?: ConfigOf<UnaryExpression>['argument']) { return argument_ !== undefined ? unaryExpression({ ...(config ?? {} as ConfigOf<UnaryExpression>), argument: argument_ }) : fields.argument; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function updateExpression(config: ConfigOf<UpdateExpression>) {
  const fields = {
    argument: config?.argument,
    operator: config?.operator,
  };
  return {
    type: 'update_expression' as const,
    named: true as const,
    fields,
    argument(argument_?: ConfigOf<UpdateExpression>['argument']) { return argument_ !== undefined ? updateExpression({ ...(config ?? {} as ConfigOf<UpdateExpression>), argument: argument_ }) : fields.argument; },
    operator(operator_?: ConfigOf<UpdateExpression>['operator']) { return operator_ !== undefined ? updateExpression({ ...(config ?? {} as ConfigOf<UpdateExpression>), operator: operator_ }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function sequenceExpression(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'sequence_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleStringFragment(text: string) {
  return {
    type: 'unescaped_double_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedDoubleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleStringFragment(text: string) {
  return {
    type: 'unescaped_single_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedSingleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(text: string) {
  return {
    type: 'comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function templateString(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'template_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateSubstitution(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'template_substitution' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regex(config: ConfigOf<Regex>) {
  const fields = {
    pattern: config?.pattern,
    flags: config?.flags,
  };
  return {
    type: 'regex' as const,
    named: true as const,
    fields,
    pattern(pattern_?: ConfigOf<Regex>['pattern']) { return pattern_ !== undefined ? regex({ ...(config ?? {} as ConfigOf<Regex>), pattern: pattern_ }) : fields.pattern; },
    flags(flags_?: ConfigOf<Regex>['flags']) { return flags_ !== undefined ? regex({ ...(config ?? {} as ConfigOf<Regex>), flags: flags_ }) : fields.flags; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regexPattern(text: string) {
  return {
    type: 'regex_pattern' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RegexPatternTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regexFlags(text: string) {
  return {
    type: 'regex_flags' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RegexFlagsTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function number(text: string) {
  return {
    type: 'number' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: NumberTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function privatePropertyIdentifier(text: string) {
  return {
    type: 'private_property_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PrivatePropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function metaProperty(text: string) {
  return {
    type: 'meta_property' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: MetaPropertyTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function this_() {
  return {
    type: 'this' as const,
    named: true as const,
    text: 'this',
    render: () => 'this',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' },
    replace: (t: ThisTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super',
    render: () => 'super',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' },
    replace: (t: SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'true',
    render: () => 'true',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'true' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'true' },
    replace: (t: TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'false',
    render: () => 'false',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'false' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'false' },
    replace: (t: FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' }; },
  };
}

export function null_() {
  return {
    type: 'null' as const,
    named: true as const,
    text: 'null',
    render: () => 'null',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'null' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'null' },
    replace: (t: NullTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' }; },
  };
}

export function undefined_() {
  return {
    type: 'undefined' as const,
    named: true as const,
    text: 'undefined',
    render: () => 'undefined',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'undefined' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'undefined' },
    replace: (t: UndefinedTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' }; },
  };
}

export function arguments_(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'decorator' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorMemberExpression(config: ConfigOf<DecoratorMemberExpression>) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'decorator_member_expression' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<DecoratorMemberExpression>['object']) { return object_ !== undefined ? decoratorMemberExpression({ ...(config ?? {} as ConfigOf<DecoratorMemberExpression>), object: object_ }) : fields.object; },
    property(property_?: ConfigOf<DecoratorMemberExpression>['property']) { return property_ !== undefined ? decoratorMemberExpression({ ...(config ?? {} as ConfigOf<DecoratorMemberExpression>), property: property_ }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorMemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorCallExpression(config: ConfigOf<DecoratorCallExpression>) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'decorator_call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<DecoratorCallExpression>['function']) { return function_ !== undefined ? decoratorCallExpression({ ...(config ?? {} as ConfigOf<DecoratorCallExpression>), function: function_ }) : fields.function; },
    typeArguments(typeArguments_?: ConfigOf<DecoratorCallExpression>['typeArguments']) { return typeArguments_ !== undefined ? decoratorCallExpression({ ...(config ?? {} as ConfigOf<DecoratorCallExpression>), typeArguments: typeArguments_ }) : fields.type_arguments; },
    arguments(arguments_?: ConfigOf<DecoratorCallExpression>['arguments']) { return arguments_ !== undefined ? decoratorCallExpression({ ...(config ?? {} as ConfigOf<DecoratorCallExpression>), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorCallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBody(config: ConfigOf<ClassBody>) {
  const fields = {
    decorator: config?.decorator,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: NonNullable<ConfigOf<ClassBody>['decorator']>) { return decorator_.length ? classBody({ ...(config ?? {} as ConfigOf<ClassBody>), decorator: decorator_ }) : fields.decorator; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return classBody({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDefinition(config: ConfigOf<FieldDefinition>) {
  const fields = {
    decorator: config?.decorator,
    property: config?.property,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'field_definition' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: NonNullable<ConfigOf<FieldDefinition>['decorator']>) { return decorator_.length ? fieldDefinition({ ...(config ?? {} as ConfigOf<FieldDefinition>), decorator: decorator_ }) : fields.decorator; },
    property(property_?: ConfigOf<FieldDefinition>['property']) { return property_ !== undefined ? fieldDefinition({ ...(config ?? {} as ConfigOf<FieldDefinition>), property: property_ }) : fields.property; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return fieldDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formalParameters(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'formal_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classStaticBlock(config: ConfigOf<ClassStaticBlock>) {
  const fields = {
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_static_block' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<ClassStaticBlock>['body']) { return body_ !== undefined ? classStaticBlock({ ...(config ?? {} as ConfigOf<ClassStaticBlock>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return classStaticBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pattern(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restPattern(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'rest_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodDefinition(config: ConfigOf<MethodDefinition>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'method_definition' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<MethodDefinition>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? methodDefinition({ ...(config ?? {} as ConfigOf<MethodDefinition>), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<MethodDefinition>['overrideModifier']) { return overrideModifier_ !== undefined ? methodDefinition({ ...(config ?? {} as ConfigOf<MethodDefinition>), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    name(name_?: ConfigOf<MethodDefinition>['name']) { return name_ !== undefined ? methodDefinition({ ...(config ?? {} as ConfigOf<MethodDefinition>), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<MethodDefinition>['body']) { return body_ !== undefined ? methodDefinition({ ...(config ?? {} as ConfigOf<MethodDefinition>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return methodDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: ConfigOf<Pair>) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key_?: ConfigOf<Pair>['key']) { return key_ !== undefined ? pair({ ...(config ?? {} as ConfigOf<Pair>), key: key_ }) : fields.key; },
    value(value_?: ConfigOf<Pair>['value']) { return value_ !== undefined ? pair({ ...(config ?? {} as ConfigOf<Pair>), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pairPattern(config: ConfigOf<PairPattern>) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair_pattern' as const,
    named: true as const,
    fields,
    key(key_?: ConfigOf<PairPattern>['key']) { return key_ !== undefined ? pairPattern({ ...(config ?? {} as ConfigOf<PairPattern>), key: key_ }) : fields.key; },
    value(value_?: ConfigOf<PairPattern>['value']) { return value_ !== undefined ? pairPattern({ ...(config ?? {} as ConfigOf<PairPattern>), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenPropertyName(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: '_property_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function computedPropertyName(config: ConfigOf<ComputedPropertyName>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'computed_property_name' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<ComputedPropertyName>['expression']) { return expression_ !== undefined ? computedPropertyName({ ...(config ?? {} as ConfigOf<ComputedPropertyName>), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenReservedIdentifier(text: string) {
  return {
    type: '_reserved_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HiddenReservedIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function hiddenSemicolon(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: '_semicolon' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenSemicolonTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function publicFieldDefinition(config: ConfigOf<PublicFieldDefinition>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    initializer: config?.initializer,
    name: config?.name,
    type: config?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<PublicFieldDefinition>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? publicFieldDefinition({ ...(config ?? {} as ConfigOf<PublicFieldDefinition>), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<PublicFieldDefinition>['overrideModifier']) { return overrideModifier_ !== undefined ? publicFieldDefinition({ ...(config ?? {} as ConfigOf<PublicFieldDefinition>), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    initializer(initializer_?: ConfigOf<PublicFieldDefinition>['initializer']) { return initializer_ !== undefined ? publicFieldDefinition({ ...(config ?? {} as ConfigOf<PublicFieldDefinition>), initializer: initializer_ }) : fields.initializer; },
    name(name_?: ConfigOf<PublicFieldDefinition>['name']) { return name_ !== undefined ? publicFieldDefinition({ ...(config ?? {} as ConfigOf<PublicFieldDefinition>), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<PublicFieldDefinition>['type']) { return type !== undefined ? publicFieldDefinition({ ...(config ?? {} as ConfigOf<PublicFieldDefinition>), type: type }) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return publicFieldDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenJsxStartOpeningElement(config?: ConfigOf<HiddenJsxStartOpeningElement>) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
    attribute: config?.attribute,
  };
  return {
    type: '_jsx_start_opening_element' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<HiddenJsxStartOpeningElement>['name']) { return name_ !== undefined ? hiddenJsxStartOpeningElement({ ...(config ?? {} as ConfigOf<HiddenJsxStartOpeningElement>), name: name_ }) : fields.name; },
    typeArguments(typeArguments_?: ConfigOf<HiddenJsxStartOpeningElement>['typeArguments']) { return typeArguments_ !== undefined ? hiddenJsxStartOpeningElement({ ...(config ?? {} as ConfigOf<HiddenJsxStartOpeningElement>), typeArguments: typeArguments_ }) : fields.type_arguments; },
    attribute(...attribute_: NonNullable<ConfigOf<HiddenJsxStartOpeningElement>['attribute']>) { return attribute_.length ? hiddenJsxStartOpeningElement({ ...(config ?? {} as ConfigOf<HiddenJsxStartOpeningElement>), attribute: attribute_ }) : fields.attribute; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenJsxStartOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenImportIdentifier(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: '_import_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenImportIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonNullExpression(config: ConfigOf<NonNullExpression>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'non_null_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<NonNullExpression>['expression']) { return expression_ !== undefined ? nonNullExpression({ ...(config ?? {} as ConfigOf<NonNullExpression>), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodSignature(config: ConfigOf<MethodSignature>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<MethodSignature>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? methodSignature({ ...(config ?? {} as ConfigOf<MethodSignature>), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<MethodSignature>['overrideModifier']) { return overrideModifier_ !== undefined ? methodSignature({ ...(config ?? {} as ConfigOf<MethodSignature>), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    name(name_?: ConfigOf<MethodSignature>['name']) { return name_ !== undefined ? methodSignature({ ...(config ?? {} as ConfigOf<MethodSignature>), name: name_ }) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return methodSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractMethodSignature(config: ConfigOf<AbstractMethodSignature>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'abstract_method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<AbstractMethodSignature>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? abstractMethodSignature({ ...(config ?? {} as ConfigOf<AbstractMethodSignature>), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<AbstractMethodSignature>['overrideModifier']) { return overrideModifier_ !== undefined ? abstractMethodSignature({ ...(config ?? {} as ConfigOf<AbstractMethodSignature>), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    name(name_?: ConfigOf<AbstractMethodSignature>['name']) { return name_ !== undefined ? abstractMethodSignature({ ...(config ?? {} as ConfigOf<AbstractMethodSignature>), name: name_ }) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return abstractMethodSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignature(config: ConfigOf<FunctionSignature>) {
  const fields = {
    name: config?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_signature' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<FunctionSignature>['name']) { return name_ !== undefined ? functionSignature({ ...(config ?? {} as ConfigOf<FunctionSignature>), name: name_ }) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorParenthesizedExpression(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'decorator_parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAssertion(config: ConfigOf<TypeAssertion>) {
  const fields = {
    type_arguments: config?.typeArguments,
    expression: config?.expression,
  };
  return {
    type: 'type_assertion' as const,
    named: true as const,
    fields,
    typeArguments(typeArguments_?: ConfigOf<TypeAssertion>['typeArguments']) { return typeArguments_ !== undefined ? typeAssertion({ ...(config ?? {} as ConfigOf<TypeAssertion>), typeArguments: typeArguments_ }) : fields.type_arguments; },
    expression(expression_?: ConfigOf<TypeAssertion>['expression']) { return expression_ !== undefined ? typeAssertion({ ...(config ?? {} as ConfigOf<TypeAssertion>), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asExpression(config: ConfigOf<AsExpression>) {
  const fields = {
    expression: config?.expression,
    type_annotation: config?.typeAnnotation,
  };
  return {
    type: 'as_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<AsExpression>['expression']) { return expression_ !== undefined ? asExpression({ ...(config ?? {} as ConfigOf<AsExpression>), expression: expression_ }) : fields.expression; },
    typeAnnotation(typeAnnotation_?: ConfigOf<AsExpression>['typeAnnotation']) { return typeAnnotation_ !== undefined ? asExpression({ ...(config ?? {} as ConfigOf<AsExpression>), typeAnnotation: typeAnnotation_ }) : fields.type_annotation; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function satisfiesExpression(config: ConfigOf<SatisfiesExpression>) {
  const fields = {
    expression: config?.expression,
    type_annotation: config?.typeAnnotation,
  };
  return {
    type: 'satisfies_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<SatisfiesExpression>['expression']) { return expression_ !== undefined ? satisfiesExpression({ ...(config ?? {} as ConfigOf<SatisfiesExpression>), expression: expression_ }) : fields.expression; },
    typeAnnotation(typeAnnotation_?: ConfigOf<SatisfiesExpression>['typeAnnotation']) { return typeAnnotation_ !== undefined ? satisfiesExpression({ ...(config ?? {} as ConfigOf<SatisfiesExpression>), typeAnnotation: typeAnnotation_ }) : fields.type_annotation; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function instantiationExpression(config: ConfigOf<InstantiationExpression>) {
  const fields = {
    expression: config?.expression,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'instantiation_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<InstantiationExpression>['expression']) { return expression_ !== undefined ? instantiationExpression({ ...(config ?? {} as ConfigOf<InstantiationExpression>), expression: expression_ }) : fields.expression; },
    typeArguments(typeArguments_?: ConfigOf<InstantiationExpression>['typeArguments']) { return typeArguments_ !== undefined ? instantiationExpression({ ...(config ?? {} as ConfigOf<InstantiationExpression>), typeArguments: typeArguments_ }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importRequireClause(config: ConfigOf<ImportRequireClause>) {
  const fields = {
    identifier: config?.identifier,
    source: config?.source,
  };
  return {
    type: 'import_require_clause' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<ImportRequireClause>['identifier']) { return identifier_ !== undefined ? importRequireClause({ ...(config ?? {} as ConfigOf<ImportRequireClause>), identifier: identifier_ }) : fields.identifier; },
    source(source_?: ConfigOf<ImportRequireClause>['source']) { return source_ !== undefined ? importRequireClause({ ...(config ?? {} as ConfigOf<ImportRequireClause>), source: source_ }) : fields.source; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsClause(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'extends_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implementsClause(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'implements_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ambientDeclaration(config: ConfigOf<AmbientDeclaration>) {
  const fields = {
    declaration: config?.declaration,
  };
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    fields,
    declaration(declaration_?: ConfigOf<AmbientDeclaration>['declaration']) { return declaration_ !== undefined ? ambientDeclaration({ ...(config ?? {} as ConfigOf<AmbientDeclaration>), declaration: declaration_ }) : fields.declaration; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractClassDeclaration(config: ConfigOf<AbstractClassDeclaration>) {
  const fields = {
    class_heritage: config?.classHeritage,
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'abstract_class_declaration' as const,
    named: true as const,
    fields,
    children,
    classHeritage(classHeritage_?: ConfigOf<AbstractClassDeclaration>['classHeritage']) { return classHeritage_ !== undefined ? abstractClassDeclaration({ ...(config ?? {} as ConfigOf<AbstractClassDeclaration>), classHeritage: classHeritage_ }) : fields.class_heritage; },
    name(name_?: ConfigOf<AbstractClassDeclaration>['name']) { return name_ !== undefined ? abstractClassDeclaration({ ...(config ?? {} as ConfigOf<AbstractClassDeclaration>), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<AbstractClassDeclaration>['typeParameters']) { return typeParameters_ !== undefined ? abstractClassDeclaration({ ...(config ?? {} as ConfigOf<AbstractClassDeclaration>), typeParameters: typeParameters_ }) : fields.type_parameters; },
    body(body_?: ConfigOf<AbstractClassDeclaration>['body']) { return body_ !== undefined ? abstractClassDeclaration({ ...(config ?? {} as ConfigOf<AbstractClassDeclaration>), body: body_ }) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return abstractClassDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function module(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function internalModule(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'internal_module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAlias(config: ConfigOf<ImportAlias>) {
  const fields = {
    name: config?.name,
    value: config?.value,
    semicolon: config?.semicolon,
  };
  return {
    type: 'import_alias' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ImportAlias>['name']) { return name_ !== undefined ? importAlias({ ...(config ?? {} as ConfigOf<ImportAlias>), name: name_ }) : fields.name; },
    value(value_?: ConfigOf<ImportAlias>['value']) { return value_ !== undefined ? importAlias({ ...(config ?? {} as ConfigOf<ImportAlias>), value: value_ }) : fields.value; },
    semicolon(semicolon_?: ConfigOf<ImportAlias>['semicolon']) { return semicolon_ !== undefined ? importAlias({ ...(config ?? {} as ConfigOf<ImportAlias>), semicolon: semicolon_ }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nestedTypeIdentifier(config: ConfigOf<NestedTypeIdentifier>) {
  const fields = {
    module: config?.module,
    name: config?.name,
  };
  return {
    type: 'nested_type_identifier' as const,
    named: true as const,
    fields,
    module(module_?: ConfigOf<NestedTypeIdentifier>['module']) { return module_ !== undefined ? nestedTypeIdentifier({ ...(config ?? {} as ConfigOf<NestedTypeIdentifier>), module: module_ }) : fields.module; },
    name(name_?: ConfigOf<NestedTypeIdentifier>['name']) { return name_ !== undefined ? nestedTypeIdentifier({ ...(config ?? {} as ConfigOf<NestedTypeIdentifier>), name: name_ }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interfaceDeclaration(config: ConfigOf<InterfaceDeclaration>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    extends_type_clause: config?.extendsTypeClause,
    body: config?.body,
  };
  return {
    type: 'interface_declaration' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<InterfaceDeclaration>['name']) { return name_ !== undefined ? interfaceDeclaration({ ...(config ?? {} as ConfigOf<InterfaceDeclaration>), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<InterfaceDeclaration>['typeParameters']) { return typeParameters_ !== undefined ? interfaceDeclaration({ ...(config ?? {} as ConfigOf<InterfaceDeclaration>), typeParameters: typeParameters_ }) : fields.type_parameters; },
    extendsTypeClause(extendsTypeClause_?: ConfigOf<InterfaceDeclaration>['extendsTypeClause']) { return extendsTypeClause_ !== undefined ? interfaceDeclaration({ ...(config ?? {} as ConfigOf<InterfaceDeclaration>), extendsTypeClause: extendsTypeClause_ }) : fields.extends_type_clause; },
    body(body_?: ConfigOf<InterfaceDeclaration>['body']) { return body_ !== undefined ? interfaceDeclaration({ ...(config ?? {} as ConfigOf<InterfaceDeclaration>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsTypeClause(config: ConfigOf<ExtendsTypeClause>) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'extends_type_clause' as const,
    named: true as const,
    fields,
    typeField(...type: NonNullable<ConfigOf<ExtendsTypeClause>['type']>) { return type.length ? extendsTypeClause({ ...(config ?? {} as ConfigOf<ExtendsTypeClause>), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumDeclaration(config: ConfigOf<EnumDeclaration>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'enum_declaration' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<EnumDeclaration>['name']) { return name_ !== undefined ? enumDeclaration({ ...(config ?? {} as ConfigOf<EnumDeclaration>), name: name_ }) : fields.name; },
    body(body_?: ConfigOf<EnumDeclaration>['body']) { return body_ !== undefined ? enumDeclaration({ ...(config ?? {} as ConfigOf<EnumDeclaration>), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumBody(config: ConfigOf<EnumBody>) {
  const fields = {
    opening: config?.opening,
  };
  return {
    type: 'enum_body' as const,
    named: true as const,
    fields,
    opening(opening_?: ConfigOf<EnumBody>['opening']) { return opening_ !== undefined ? enumBody({ ...(config ?? {} as ConfigOf<EnumBody>), opening: opening_ }) : fields.opening; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumAssignment(config: ConfigOf<EnumAssignment>) {
  const fields = {
    name: config?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'enum_assignment' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<EnumAssignment>['name']) { return name_ !== undefined ? enumAssignment({ ...(config ?? {} as ConfigOf<EnumAssignment>), name: name_ }) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return enumAssignment({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasDeclaration(config: ConfigOf<TypeAliasDeclaration>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    value: config?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_alias_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<TypeAliasDeclaration>['name']) { return name_ !== undefined ? typeAliasDeclaration({ ...(config ?? {} as ConfigOf<TypeAliasDeclaration>), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<TypeAliasDeclaration>['typeParameters']) { return typeParameters_ !== undefined ? typeAliasDeclaration({ ...(config ?? {} as ConfigOf<TypeAliasDeclaration>), typeParameters: typeParameters_ }) : fields.type_parameters; },
    value(value_?: ConfigOf<TypeAliasDeclaration>['value']) { return value_ !== undefined ? typeAliasDeclaration({ ...(config ?? {} as ConfigOf<TypeAliasDeclaration>), value: value_ }) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeAliasDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function accessibilityModifier(text: string) {
  return {
    type: 'accessibility_modifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: AccessibilityModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function overrideModifier() {
  return {
    type: 'override_modifier' as const,
    named: true as const,
    text: 'override',
    render: () => 'override',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'override' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'override' },
    replace: (t: OverrideModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' }; },
  };
}

export function requiredParameter(config: ConfigOf<RequiredParameter>) {
  const fields = {
    parameter_name: config?.parameterName,
    type: config?.type,
    initializer: config?.initializer,
  };
  return {
    type: 'required_parameter' as const,
    named: true as const,
    fields,
    parameterName(parameterName_?: ConfigOf<RequiredParameter>['parameterName']) { return parameterName_ !== undefined ? requiredParameter({ ...(config ?? {} as ConfigOf<RequiredParameter>), parameterName: parameterName_ }) : fields.parameter_name; },
    typeField(type?: ConfigOf<RequiredParameter>['type']) { return type !== undefined ? requiredParameter({ ...(config ?? {} as ConfigOf<RequiredParameter>), type: type }) : fields.type; },
    initializer(initializer_?: ConfigOf<RequiredParameter>['initializer']) { return initializer_ !== undefined ? requiredParameter({ ...(config ?? {} as ConfigOf<RequiredParameter>), initializer: initializer_ }) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalParameter(config: ConfigOf<OptionalParameter>) {
  const fields = {
    parameter_name: config?.parameterName,
    type: config?.type,
    initializer: config?.initializer,
  };
  return {
    type: 'optional_parameter' as const,
    named: true as const,
    fields,
    parameterName(parameterName_?: ConfigOf<OptionalParameter>['parameterName']) { return parameterName_ !== undefined ? optionalParameter({ ...(config ?? {} as ConfigOf<OptionalParameter>), parameterName: parameterName_ }) : fields.parameter_name; },
    typeField(type?: ConfigOf<OptionalParameter>['type']) { return type !== undefined ? optionalParameter({ ...(config ?? {} as ConfigOf<OptionalParameter>), type: type }) : fields.type; },
    initializer(initializer_?: ConfigOf<OptionalParameter>['initializer']) { return initializer_ !== undefined ? optionalParameter({ ...(config ?? {} as ConfigOf<OptionalParameter>), initializer: initializer_ }) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function omittingTypeAnnotation(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'omitting_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function addingTypeAnnotation(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'adding_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optingTypeAnnotation(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'opting_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAnnotation(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asserts(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'asserts' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertsAnnotation(config: ConfigOf<AssertsAnnotation>) {
  const fields = {
    asserts: config?.asserts,
  };
  return {
    type: 'asserts_annotation' as const,
    named: true as const,
    fields,
    asserts(asserts_?: ConfigOf<AssertsAnnotation>['asserts']) { return asserts_ !== undefined ? assertsAnnotation({ ...(config ?? {} as ConfigOf<AssertsAnnotation>), asserts: asserts_ }) : fields.asserts; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function type(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleParameter(config: ConfigOf<TupleParameter>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'tuple_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TupleParameter>['name']) { return name_ !== undefined ? tupleParameter({ ...(config ?? {} as ConfigOf<TupleParameter>), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<TupleParameter>['type']) { return type !== undefined ? tupleParameter({ ...(config ?? {} as ConfigOf<TupleParameter>), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalTupleParameter(config: ConfigOf<OptionalTupleParameter>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'optional_tuple_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<OptionalTupleParameter>['name']) { return name_ !== undefined ? optionalTupleParameter({ ...(config ?? {} as ConfigOf<OptionalTupleParameter>), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<OptionalTupleParameter>['type']) { return type !== undefined ? optionalTupleParameter({ ...(config ?? {} as ConfigOf<OptionalTupleParameter>), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalType(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'optional_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restType(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'rest_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructorType(config: ConfigOf<ConstructorType>) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    type: config?.type,
  };
  return {
    type: 'constructor_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: ConfigOf<ConstructorType>['typeParameters']) { return typeParameters_ !== undefined ? constructorType({ ...(config ?? {} as ConfigOf<ConstructorType>), typeParameters: typeParameters_ }) : fields.type_parameters; },
    parameters(parameters_?: ConfigOf<ConstructorType>['parameters']) { return parameters_ !== undefined ? constructorType({ ...(config ?? {} as ConfigOf<ConstructorType>), parameters: parameters_ }) : fields.parameters; },
    typeField(type?: ConfigOf<ConstructorType>['type']) { return type !== undefined ? constructorType({ ...(config ?? {} as ConfigOf<ConstructorType>), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function primaryType(config: ConfigOf<PrimaryType>) {
  const fields = {
    name: config?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<PrimaryType>['name']) { return name_ !== undefined ? primaryType({ ...(config ?? {} as ConfigOf<PrimaryType>), name: name_ }) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateType(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'template_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateLiteralType(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'template_literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function inferType(config: ConfigOf<InferType>) {
  const fields = {
    type_identifier: config?.typeIdentifier,
    constraint: config?.constraint,
  };
  return {
    type: 'infer_type' as const,
    named: true as const,
    fields,
    typeIdentifier(typeIdentifier_?: ConfigOf<InferType>['typeIdentifier']) { return typeIdentifier_ !== undefined ? inferType({ ...(config ?? {} as ConfigOf<InferType>), typeIdentifier: typeIdentifier_ }) : fields.type_identifier; },
    constraint(constraint_?: ConfigOf<InferType>['constraint']) { return constraint_ !== undefined ? inferType({ ...(config ?? {} as ConfigOf<InferType>), constraint: constraint_ }) : fields.constraint; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalType(config: ConfigOf<ConditionalType>) {
  const fields = {
    left: config?.left,
    right: config?.right,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'conditional_type' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<ConditionalType>['left']) { return left_ !== undefined ? conditionalType({ ...(config ?? {} as ConfigOf<ConditionalType>), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<ConditionalType>['right']) { return right_ !== undefined ? conditionalType({ ...(config ?? {} as ConfigOf<ConditionalType>), right: right_ }) : fields.right; },
    consequence(consequence_?: ConfigOf<ConditionalType>['consequence']) { return consequence_ !== undefined ? conditionalType({ ...(config ?? {} as ConfigOf<ConditionalType>), consequence: consequence_ }) : fields.consequence; },
    alternative(alternative_?: ConfigOf<ConditionalType>['alternative']) { return alternative_ !== undefined ? conditionalType({ ...(config ?? {} as ConfigOf<ConditionalType>), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: ConfigOf<GenericType>) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<GenericType>['name']) { return name_ !== undefined ? genericType({ ...(config ?? {} as ConfigOf<GenericType>), name: name_ }) : fields.name; },
    typeArguments(typeArguments_?: ConfigOf<GenericType>['typeArguments']) { return typeArguments_ !== undefined ? genericType({ ...(config ?? {} as ConfigOf<GenericType>), typeArguments: typeArguments_ }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicate(config: ConfigOf<TypePredicate>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'type_predicate' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TypePredicate>['name']) { return name_ !== undefined ? typePredicate({ ...(config ?? {} as ConfigOf<TypePredicate>), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<TypePredicate>['type']) { return type !== undefined ? typePredicate({ ...(config ?? {} as ConfigOf<TypePredicate>), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicateAnnotation(config: ConfigOf<TypePredicateAnnotation>) {
  const fields = {
    type_predicate: config?.typePredicate,
  };
  return {
    type: 'type_predicate_annotation' as const,
    named: true as const,
    fields,
    typePredicate(typePredicate_?: ConfigOf<TypePredicateAnnotation>['typePredicate']) { return typePredicate_ !== undefined ? typePredicateAnnotation({ ...(config ?? {} as ConfigOf<TypePredicateAnnotation>), typePredicate: typePredicate_ }) : fields.type_predicate; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeQuery(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'type_query' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexTypeQuery(config: ConfigOf<IndexTypeQuery>) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'index_type_query' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: ConfigOf<IndexTypeQuery>['primaryType']) { return primaryType_ !== undefined ? indexTypeQuery({ ...(config ?? {} as ConfigOf<IndexTypeQuery>), primaryType: primaryType_ }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lookupType(config: ConfigOf<LookupType>) {
  const fields = {
    primary_type: config?.primaryType,
    index_type: config?.indexType,
  };
  return {
    type: 'lookup_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: ConfigOf<LookupType>['primaryType']) { return primaryType_ !== undefined ? lookupType({ ...(config ?? {} as ConfigOf<LookupType>), primaryType: primaryType_ }) : fields.primary_type; },
    indexType(indexType_?: ConfigOf<LookupType>['indexType']) { return indexType_ !== undefined ? lookupType({ ...(config ?? {} as ConfigOf<LookupType>), indexType: indexType_ }) : fields.index_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mappedTypeClause(config: ConfigOf<MappedTypeClause>) {
  const fields = {
    name: config?.name,
    type: config?.type,
    alias: config?.alias,
  };
  return {
    type: 'mapped_type_clause' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<MappedTypeClause>['name']) { return name_ !== undefined ? mappedTypeClause({ ...(config ?? {} as ConfigOf<MappedTypeClause>), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<MappedTypeClause>['type']) { return type !== undefined ? mappedTypeClause({ ...(config ?? {} as ConfigOf<MappedTypeClause>), type: type }) : fields.type; },
    alias(alias_?: ConfigOf<MappedTypeClause>['alias']) { return alias_ !== undefined ? mappedTypeClause({ ...(config ?? {} as ConfigOf<MappedTypeClause>), alias: alias_ }) : fields.alias; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function literalType(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function flowMaybeType(config: ConfigOf<FlowMaybeType>) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'flow_maybe_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: ConfigOf<FlowMaybeType>['primaryType']) { return primaryType_ !== undefined ? flowMaybeType({ ...(config ?? {} as ConfigOf<FlowMaybeType>), primaryType: primaryType_ }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedType(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'parenthesized_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function predefinedType(text: string) {
  return {
    type: 'predefined_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PredefinedTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeArguments(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectType(config: ConfigOf<ObjectType>) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'object_type' as const,
    named: true as const,
    fields,
    opening(opening_?: ConfigOf<ObjectType>['opening']) { return opening_ !== undefined ? objectType({ ...(config ?? {} as ConfigOf<ObjectType>), opening: opening_ }) : fields.opening; },
    members(members_?: ConfigOf<ObjectType>['members']) { return members_ !== undefined ? objectType({ ...(config ?? {} as ConfigOf<ObjectType>), members: members_ }) : fields.members; },
    closing(closing_?: ConfigOf<ObjectType>['closing']) { return closing_ !== undefined ? objectType({ ...(config ?? {} as ConfigOf<ObjectType>), closing: closing_ }) : fields.closing; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callSignature(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'call_signature' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertySignature(config: ConfigOf<PropertySignature>) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
    type: config?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'property_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: ConfigOf<PropertySignature>['accessibilityModifier']) { return accessibilityModifier_ !== undefined ? propertySignature({ ...(config ?? {} as ConfigOf<PropertySignature>), accessibilityModifier: accessibilityModifier_ }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: ConfigOf<PropertySignature>['overrideModifier']) { return overrideModifier_ !== undefined ? propertySignature({ ...(config ?? {} as ConfigOf<PropertySignature>), overrideModifier: overrideModifier_ }) : fields.override_modifier; },
    name(name_?: ConfigOf<PropertySignature>['name']) { return name_ !== undefined ? propertySignature({ ...(config ?? {} as ConfigOf<PropertySignature>), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<PropertySignature>['type']) { return type !== undefined ? propertySignature({ ...(config ?? {} as ConfigOf<PropertySignature>), type: type }) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return propertySignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: ConfigOf<TypeParameter>) {
  const fields = {
    name: config?.name,
    constraint: config?.constraint,
    value: config?.value,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TypeParameter>['name']) { return name_ !== undefined ? typeParameter({ ...(config ?? {} as ConfigOf<TypeParameter>), name: name_ }) : fields.name; },
    constraint(constraint_?: ConfigOf<TypeParameter>['constraint']) { return constraint_ !== undefined ? typeParameter({ ...(config ?? {} as ConfigOf<TypeParameter>), constraint: constraint_ }) : fields.constraint; },
    value(value_?: ConfigOf<TypeParameter>['value']) { return value_ !== undefined ? typeParameter({ ...(config ?? {} as ConfigOf<TypeParameter>), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultType(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'default_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constraint(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'constraint' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructSignature(config: ConfigOf<ConstructSignature>) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    type: config?.type,
  };
  return {
    type: 'construct_signature' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: ConfigOf<ConstructSignature>['typeParameters']) { return typeParameters_ !== undefined ? constructSignature({ ...(config ?? {} as ConfigOf<ConstructSignature>), typeParameters: typeParameters_ }) : fields.type_parameters; },
    parameters(parameters_?: ConfigOf<ConstructSignature>['parameters']) { return parameters_ !== undefined ? constructSignature({ ...(config ?? {} as ConfigOf<ConstructSignature>), parameters: parameters_ }) : fields.parameters; },
    typeField(type?: ConfigOf<ConstructSignature>['type']) { return type !== undefined ? constructSignature({ ...(config ?? {} as ConfigOf<ConstructSignature>), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignature(config?: ConfigOf<IndexSignature>) {
  if (config && 'name' in config) return indexSignatureColon(config as any);
  return indexSignatureMappedTypeClause(config as any);
}
export function indexSignatureColon(config: ConfigOf<IndexSignatureColon>) {
  const fields = {
    name: config?.name,
    index_type: config?.indexType,
  };
  return {
    type: 'index_signature' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<IndexSignatureColon>['name']) { return name_ !== undefined ? indexSignatureColon({ ...(config ?? {} as ConfigOf<IndexSignatureColon>), name: name_ }) : fields.name; },
    indexType(indexType_?: ConfigOf<IndexSignatureColon>['indexType']) { return indexType_ !== undefined ? indexSignatureColon({ ...(config ?? {} as ConfigOf<IndexSignatureColon>), indexType: indexType_ }) : fields.index_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureColonTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function indexSignatureMappedTypeClause(config?: ConfigOf<IndexSignatureMappedTypeClause>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'index_signature' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return indexSignatureMappedTypeClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureMappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: ConfigOf<ArrayType>) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: ConfigOf<ArrayType>['primaryType']) { return primaryType_ !== undefined ? arrayType({ ...(config ?? {} as ConfigOf<ArrayType>), primaryType: primaryType_ }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(..._children: any[]) {
  const children = _children.filter((c: any) => c && typeof c === "object" && "type" in c);
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function readonlyType(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'readonly_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: ConfigOf<UnionType>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<UnionType>['left']) { return left_ !== undefined ? unionType({ ...(config ?? {} as ConfigOf<UnionType>), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<UnionType>['right']) { return right_ !== undefined ? unionType({ ...(config ?? {} as ConfigOf<UnionType>), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function intersectionType(config: ConfigOf<IntersectionType>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'intersection_type' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<IntersectionType>['left']) { return left_ !== undefined ? intersectionType({ ...(config ?? {} as ConfigOf<IntersectionType>), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<IntersectionType>['right']) { return right_ !== undefined ? intersectionType({ ...(config ?? {} as ConfigOf<IntersectionType>), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: ConfigOf<FunctionType>) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: ConfigOf<FunctionType>['typeParameters']) { return typeParameters_ !== undefined ? functionType({ ...(config ?? {} as ConfigOf<FunctionType>), typeParameters: typeParameters_ }) : fields.type_parameters; },
    parameters(parameters_?: ConfigOf<FunctionType>['parameters']) { return parameters_ !== undefined ? functionType({ ...(config ?? {} as ConfigOf<FunctionType>), parameters: parameters_ }) : fields.parameters; },
    returnType(returnType_?: ConfigOf<FunctionType>['returnType']) { return returnType_ !== undefined ? functionType({ ...(config ?? {} as ConfigOf<FunctionType>), returnType: returnType_ }) : fields.return_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenTypeIdentifier(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: '_type_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statementIdentifier(text: string) {
  return {
    type: 'statement_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StatementIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthandPropertyIdentifier(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'shorthand_property_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandPropertyIdentifierPattern(child?: any) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = hasChild ? [child] : [];
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertyIdentifier(text: string) {
  return {
    type: 'property_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringFragment(text: string) {
  return {
    type: 'string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function interfaceBody(config: ConfigOf<InterfaceBody>) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'interface_body' as const,
    named: true as const,
    fields,
    opening(opening_?: ConfigOf<InterfaceBody>['opening']) { return opening_ !== undefined ? interfaceBody({ ...(config ?? {} as ConfigOf<InterfaceBody>), opening: opening_ }) : fields.opening; },
    members(members_?: ConfigOf<InterfaceBody>['members']) { return members_ !== undefined ? interfaceBody({ ...(config ?? {} as ConfigOf<InterfaceBody>), members: members_ }) : fields.members; },
    closing(closing_?: ConfigOf<InterfaceBody>['closing']) { return closing_ !== undefined ? interfaceBody({ ...(config ?? {} as ConfigOf<InterfaceBody>), closing: closing_ }) : fields.closing; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function thisType() {
  return {
    type: 'this_type' as const,
    named: true as const,
    text: 'this',
    render: () => 'this',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' },
    replace: (t: ThisTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' }; },
  };
}

export function typeIdentifier(text: string) {
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export const _factoryMap: Record<string, (config?: any) => unknown> = {
  'program': program,
  'hash_bang_line': hashBangLine,
  'export_statement_form_0': exportStatementForm0,
  'export_statement_export': exportStatementExport,
  'export_statement_export2': exportStatementExport2,
  'export_statement_export3': exportStatementExport3,
  'export_statement': exportStatement,
  'namespace_export': namespaceExport,
  'export_clause': exportClause,
  'export_specifier': exportSpecifier,
  'declaration': declaration,
  'import': import_,
  'import_statement': importStatement,
  'import_clause': importClause,
  'namespace_import': namespaceImport,
  'named_imports': namedImports,
  'import_specifier_name': importSpecifierName,
  'import_specifier_as': importSpecifierAs,
  'import_specifier': importSpecifier,
  'import_attribute': importAttribute,
  'statement_export_statement': statementExportStatement,
  'statement_import_statement': statementImportStatement,
  'statement_debugger_statement': statementDebuggerStatement,
  'statement_expression_statement': statementExpressionStatement,
  'statement_declaration': statementDeclaration,
  'statement_body': statementBody,
  'statement_if_statement': statementIfStatement,
  'statement_switch_statement': statementSwitchStatement,
  'statement_for_statement': statementForStatement,
  'statement_for_in_statement': statementForInStatement,
  'statement_while_statement': statementWhileStatement,
  'statement_do_statement': statementDoStatement,
  'statement_try_statement': statementTryStatement,
  'statement_with_statement': statementWithStatement,
  'statement_break_statement': statementBreakStatement,
  'statement_continue_statement': statementContinueStatement,
  'statement_return_statement': statementReturnStatement,
  'statement_throw_statement': statementThrowStatement,
  'statement_empty_statement': statementEmptyStatement,
  'statement_labeled_statement': statementLabeledStatement,
  'statement': statement,
  'expression_statement': expressionStatement,
  'variable_declaration': variableDeclaration,
  'lexical_declaration': lexicalDeclaration,
  'variable_declarator': variableDeclarator,
  'statement_block': statementBlock,
  'else_clause': elseClause,
  'if_statement': ifStatement,
  'switch_statement': switchStatement,
  'for_statement': forStatement,
  'for_in_statement': forInStatement,
  'while_statement': whileStatement,
  'do_statement': doStatement,
  'try_statement': tryStatement,
  'with_statement': withStatement,
  'break_statement': breakStatement,
  'continue_statement': continueStatement,
  'debugger_statement': debuggerStatement,
  'return_statement': returnStatement,
  'throw_statement': throwStatement,
  'labeled_statement': labeledStatement,
  'switch_body': switchBody,
  'switch_case': switchCase,
  'switch_default': switchDefault,
  'catch_clause': catchClause,
  'finally_clause': finallyClause,
  'parenthesized_expression_expression': parenthesizedExpressionExpression,
  'parenthesized_expression_sequence_expression': parenthesizedExpressionSequenceExpression,
  'parenthesized_expression': parenthesizedExpression,
  'expression': expression,
  'primary_expression': primaryExpression,
  'yield_expression': yieldExpression,
  'object': object,
  'object_pattern': objectPattern,
  'assignment_pattern': assignmentPattern,
  'object_assignment_pattern': objectAssignmentPattern,
  'array': array,
  'array_pattern': arrayPattern,
  'jsx_element': jsxElement,
  'html_character_reference': htmlCharacterReference,
  'jsx_expression': jsxExpression,
  'jsx_opening_element': jsxOpeningElement,
  'jsx_identifier': jsxIdentifier,
  'nested_identifier': nestedIdentifier,
  'jsx_namespace_name': jsxNamespaceName,
  'jsx_closing_element': jsxClosingElement,
  'jsx_self_closing_element': jsxSelfClosingElement,
  'jsx_attribute': jsxAttribute,
  '_jsx_string': hiddenJsxString,
  'unescaped_double_jsx_string_fragment': unescapedDoubleJsxStringFragment,
  'unescaped_single_jsx_string_fragment': unescapedSingleJsxStringFragment,
  'class': class_,
  'class_declaration': classDeclaration,
  'class_heritage': classHeritage,
  'function_expression': functionExpression,
  'function_declaration': functionDeclaration,
  'generator_function': generatorFunction,
  'generator_function_declaration': generatorFunctionDeclaration,
  'arrow_function_parameter': arrowFunctionParameter,
  'arrow_function__call_signature': arrowFunctionUCallSignature,
  'arrow_function': arrowFunction,
  'call_expression_function': callExpressionFunction,
  'call_expression_function2': callExpressionFunction2,
  'call_expression_tok_q_dot': callExpressionTokQDot,
  'call_expression': callExpression,
  'new_expression': newExpression,
  'await_expression': awaitExpression,
  'member_expression': memberExpression,
  'subscript_expression': subscriptExpression,
  '_lhs_expression': hiddenLhsExpression,
  'assignment_expression': assignmentExpression,
  '_augmented_assignment_lhs': hiddenAugmentedAssignmentLhs,
  'augmented_assignment_expression': augmentedAssignmentExpression,
  'spread_element': spreadElement,
  'ternary_expression': ternaryExpression,
  'binary_expression': binaryExpression,
  'unary_expression': unaryExpression,
  'update_expression': updateExpression,
  'sequence_expression': sequenceExpression,
  'string': string,
  'unescaped_double_string_fragment': unescapedDoubleStringFragment,
  'unescaped_single_string_fragment': unescapedSingleStringFragment,
  'escape_sequence': escapeSequence,
  'comment': comment,
  'template_string': templateString,
  'template_substitution': templateSubstitution,
  'regex': regex,
  'regex_pattern': regexPattern,
  'regex_flags': regexFlags,
  'number': number,
  'identifier': identifier,
  'private_property_identifier': privatePropertyIdentifier,
  'meta_property': metaProperty,
  'this': this_,
  'super': super_,
  'true': true_,
  'false': false_,
  'null': null_,
  'undefined': undefined_,
  'arguments': arguments_,
  'decorator': decorator,
  'decorator_member_expression': decoratorMemberExpression,
  'decorator_call_expression': decoratorCallExpression,
  'class_body': classBody,
  'field_definition': fieldDefinition,
  'formal_parameters': formalParameters,
  'class_static_block': classStaticBlock,
  'pattern': pattern,
  'rest_pattern': restPattern,
  'method_definition': methodDefinition,
  'pair': pair,
  'pair_pattern': pairPattern,
  '_property_name': hiddenPropertyName,
  'computed_property_name': computedPropertyName,
  '_reserved_identifier': hiddenReservedIdentifier,
  '_semicolon': hiddenSemicolon,
  'public_field_definition': publicFieldDefinition,
  '_jsx_start_opening_element': hiddenJsxStartOpeningElement,
  '_import_identifier': hiddenImportIdentifier,
  'non_null_expression': nonNullExpression,
  'method_signature': methodSignature,
  'abstract_method_signature': abstractMethodSignature,
  'function_signature': functionSignature,
  'decorator_parenthesized_expression': decoratorParenthesizedExpression,
  'type_assertion': typeAssertion,
  'as_expression': asExpression,
  'satisfies_expression': satisfiesExpression,
  'instantiation_expression': instantiationExpression,
  'import_require_clause': importRequireClause,
  'extends_clause': extendsClause,
  'implements_clause': implementsClause,
  'ambient_declaration': ambientDeclaration,
  'abstract_class_declaration': abstractClassDeclaration,
  'module': module,
  'internal_module': internalModule,
  'import_alias': importAlias,
  'nested_type_identifier': nestedTypeIdentifier,
  'interface_declaration': interfaceDeclaration,
  'extends_type_clause': extendsTypeClause,
  'enum_declaration': enumDeclaration,
  'enum_body': enumBody,
  'enum_assignment': enumAssignment,
  'type_alias_declaration': typeAliasDeclaration,
  'accessibility_modifier': accessibilityModifier,
  'override_modifier': overrideModifier,
  'required_parameter': requiredParameter,
  'optional_parameter': optionalParameter,
  'omitting_type_annotation': omittingTypeAnnotation,
  'adding_type_annotation': addingTypeAnnotation,
  'opting_type_annotation': optingTypeAnnotation,
  'type_annotation': typeAnnotation,
  'asserts': asserts,
  'asserts_annotation': assertsAnnotation,
  'type': type,
  'tuple_parameter': tupleParameter,
  'optional_tuple_parameter': optionalTupleParameter,
  'optional_type': optionalType,
  'rest_type': restType,
  'constructor_type': constructorType,
  'primary_type': primaryType,
  'template_type': templateType,
  'template_literal_type': templateLiteralType,
  'infer_type': inferType,
  'conditional_type': conditionalType,
  'generic_type': genericType,
  'type_predicate': typePredicate,
  'type_predicate_annotation': typePredicateAnnotation,
  'type_query': typeQuery,
  'index_type_query': indexTypeQuery,
  'lookup_type': lookupType,
  'mapped_type_clause': mappedTypeClause,
  'literal_type': literalType,
  'flow_maybe_type': flowMaybeType,
  'parenthesized_type': parenthesizedType,
  'predefined_type': predefinedType,
  'type_arguments': typeArguments,
  'object_type': objectType,
  'call_signature': callSignature,
  'property_signature': propertySignature,
  'type_parameters': typeParameters,
  'type_parameter': typeParameter,
  'default_type': defaultType,
  'constraint': constraint,
  'construct_signature': constructSignature,
  'index_signature_colon': indexSignatureColon,
  'index_signature_mapped_type_clause': indexSignatureMappedTypeClause,
  'index_signature': indexSignature,
  'array_type': arrayType,
  'tuple_type': tupleType,
  'readonly_type': readonlyType,
  'union_type': unionType,
  'intersection_type': intersectionType,
  'function_type': functionType,
  '_type_identifier': hiddenTypeIdentifier,
  'statement_identifier': statementIdentifier,
  'shorthand_property_identifier': shorthandPropertyIdentifier,
  'shorthand_property_identifier_pattern': shorthandPropertyIdentifierPattern,
  'property_identifier': propertyIdentifier,
  'string_fragment': stringFragment,
  'interface_body': interfaceBody,
  'this_type': thisType,
  'type_identifier': typeIdentifier,
};

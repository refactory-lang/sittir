// Auto-generated by @sittir/codegen — do not edit

import type {
  AbstractClassDeclaration,
  AbstractClassDeclarationConfig,
  AbstractClassDeclarationTree,
  AbstractMethodSignature,
  AbstractMethodSignatureConfig,
  AbstractMethodSignatureTree,
  AccessibilityModifier,
  AccessibilityModifierTree,
  AddingTypeAnnotation,
  AddingTypeAnnotationConfig,
  AddingTypeAnnotationTree,
  AmbientDeclaration,
  AmbientDeclarationConfig,
  AmbientDeclarationTree,
  Arguments,
  ArgumentsConfig,
  ArgumentsTree,
  Array,
  ArrayConfig,
  ArrayPattern,
  ArrayPatternConfig,
  ArrayPatternTree,
  ArrayTree,
  ArrayType,
  ArrayTypeConfig,
  ArrayTypeTree,
  ArrowFunction,
  ArrowFunctionConfig,
  ArrowFunctionParameter,
  ArrowFunctionParameterConfig,
  ArrowFunctionParameterTree,
  ArrowFunctionTree,
  ArrowFunctionUCallSignature,
  ArrowFunctionUCallSignatureConfig,
  ArrowFunctionUCallSignatureTree,
  AsExpression,
  AsExpressionConfig,
  AsExpressionTree,
  Asserts,
  AssertsAnnotation,
  AssertsAnnotationConfig,
  AssertsAnnotationTree,
  AssertsConfig,
  AssertsTree,
  AssignmentExpression,
  AssignmentExpressionConfig,
  AssignmentExpressionTree,
  AssignmentPattern,
  AssignmentPatternConfig,
  AssignmentPatternTree,
  AugmentedAssignmentExpression,
  AugmentedAssignmentExpressionConfig,
  AugmentedAssignmentExpressionTree,
  AutomaticSemicolon,
  AwaitExpression,
  AwaitExpressionConfig,
  AwaitExpressionTree,
  BinaryExpression,
  BinaryExpressionConfig,
  BinaryExpressionTree,
  BreakStatement,
  BreakStatementConfig,
  BreakStatementTree,
  CallExpression,
  CallExpressionConfig,
  CallExpressionFunction,
  CallExpressionFunction2,
  CallExpressionFunction2Config,
  CallExpressionFunction2Tree,
  CallExpressionFunctionConfig,
  CallExpressionFunctionTree,
  CallExpressionTokQDot,
  CallExpressionTokQDotConfig,
  CallExpressionTokQDotTree,
  CallExpressionTree,
  CallSignature,
  CallSignatureConfig,
  CallSignatureTree,
  CatchClause,
  CatchClauseConfig,
  CatchClauseTree,
  Class,
  ClassBody,
  ClassBodyConfig,
  ClassBodyTree,
  ClassConfig,
  ClassDeclaration,
  ClassDeclarationConfig,
  ClassDeclarationTree,
  ClassHeritage,
  ClassHeritageConfig,
  ClassHeritageExtendsClause,
  ClassHeritageExtendsClauseConfig,
  ClassHeritageExtendsClauseTree,
  ClassHeritageImplementsClause,
  ClassHeritageImplementsClauseConfig,
  ClassHeritageImplementsClauseTree,
  ClassHeritageTree,
  ClassStaticBlock,
  ClassStaticBlockConfig,
  ClassStaticBlockTree,
  ClassTree,
  Comment,
  CommentTree,
  ComputedPropertyName,
  ComputedPropertyNameConfig,
  ComputedPropertyNameTree,
  ConditionalType,
  ConditionalTypeConfig,
  ConditionalTypeTree,
  ConfigMap,
  Constraint,
  ConstraintConfig,
  ConstraintTree,
  ConstructSignature,
  ConstructSignatureConfig,
  ConstructSignatureTree,
  ConstructorType,
  ConstructorTypeConfig,
  ConstructorTypeTree,
  ContinueStatement,
  ContinueStatementConfig,
  ContinueStatementTree,
  DebuggerStatement,
  DebuggerStatementConfig,
  DebuggerStatementTree,
  Declaration,
  DeclarationConfig,
  DeclarationTree,
  Decorator,
  DecoratorCallExpression,
  DecoratorCallExpressionConfig,
  DecoratorCallExpressionTree,
  DecoratorConfig,
  DecoratorMemberExpression,
  DecoratorMemberExpressionConfig,
  DecoratorMemberExpressionTree,
  DecoratorParenthesizedExpression,
  DecoratorParenthesizedExpressionConfig,
  DecoratorParenthesizedExpressionTree,
  DecoratorTree,
  DefaultType,
  DefaultTypeConfig,
  DefaultTypeTree,
  DestructuringPattern,
  DoStatement,
  DoStatementConfig,
  DoStatementTree,
  ElseClause,
  ElseClauseConfig,
  ElseClauseTree,
  EmptyStatement,
  EnumAssignment,
  EnumAssignmentConfig,
  EnumAssignmentTree,
  EnumBody,
  EnumBodyConfig,
  EnumBodyTree,
  EnumDeclaration,
  EnumDeclarationConfig,
  EnumDeclarationTree,
  EscapeSequence,
  EscapeSequenceTree,
  ExportClause,
  ExportClauseConfig,
  ExportClauseTree,
  ExportSpecifier,
  ExportSpecifierConfig,
  ExportSpecifierTree,
  ExportStatement,
  ExportStatementConfig,
  ExportStatementExport,
  ExportStatementExport2,
  ExportStatementExport2Config,
  ExportStatementExport2Tree,
  ExportStatementExport3,
  ExportStatementExport3Config,
  ExportStatementExport3Tree,
  ExportStatementExportConfig,
  ExportStatementExportTree,
  ExportStatementForm0,
  ExportStatementForm0Config,
  ExportStatementForm0Tree,
  ExportStatementTree,
  Expression,
  ExpressionConfig,
  ExpressionStatement,
  ExpressionStatementConfig,
  ExpressionStatementTree,
  ExpressionTree,
  Expressions,
  ExtendsClause,
  ExtendsClauseConfig,
  ExtendsClauseSingle,
  ExtendsClauseTree,
  ExtendsTypeClause,
  ExtendsTypeClauseConfig,
  ExtendsTypeClauseTree,
  False,
  FalseTree,
  FieldDefinition,
  FieldDefinitionConfig,
  FieldDefinitionTree,
  FinallyClause,
  FinallyClauseConfig,
  FinallyClauseTree,
  FlowMaybeType,
  FlowMaybeTypeConfig,
  FlowMaybeTypeTree,
  ForHeader,
  ForInStatement,
  ForInStatementConfig,
  ForInStatementTree,
  ForStatement,
  ForStatementConfig,
  ForStatementTree,
  FormalParameter,
  FormalParameters,
  FormalParametersConfig,
  FormalParametersTree,
  FromClause,
  FunctionDeclaration,
  FunctionDeclarationConfig,
  FunctionDeclarationTree,
  FunctionExpression,
  FunctionExpressionConfig,
  FunctionExpressionTree,
  FunctionSignature,
  FunctionSignatureAutomaticSemicolon,
  FunctionSignatureConfig,
  FunctionSignatureTree,
  FunctionType,
  FunctionTypeConfig,
  FunctionTypeTree,
  GeneratorFunction,
  GeneratorFunctionConfig,
  GeneratorFunctionDeclaration,
  GeneratorFunctionDeclarationConfig,
  GeneratorFunctionDeclarationTree,
  GeneratorFunctionTree,
  GenericType,
  GenericTypeConfig,
  GenericTypeTree,
  HashBangLine,
  HashBangLineTree,
  HtmlCharacterReference,
  HtmlCharacterReferenceTree,
  HtmlComment,
  HtmlCommentTree,
  Identifier,
  IdentifierTree,
  IfStatement,
  IfStatementConfig,
  IfStatementTree,
  ImplementsClause,
  ImplementsClauseConfig,
  ImplementsClauseTree,
  Import,
  ImportAlias,
  ImportAliasConfig,
  ImportAliasTree,
  ImportAttribute,
  ImportAttributeConfig,
  ImportAttributeTree,
  ImportClause,
  ImportClauseConfig,
  ImportClauseDefaultImport,
  ImportClauseDefaultImportConfig,
  ImportClauseDefaultImportTree,
  ImportClauseNamedImports,
  ImportClauseNamedImportsConfig,
  ImportClauseNamedImportsTree,
  ImportClauseNamespaceImport,
  ImportClauseNamespaceImportConfig,
  ImportClauseNamespaceImportTree,
  ImportClauseTree,
  ImportIdentifier,
  ImportRequireClause,
  ImportRequireClauseConfig,
  ImportRequireClauseTree,
  ImportSpecifier,
  ImportSpecifierAs,
  ImportSpecifierAsConfig,
  ImportSpecifierAsTree,
  ImportSpecifierConfig,
  ImportSpecifierName,
  ImportSpecifierNameConfig,
  ImportSpecifierNameTree,
  ImportSpecifierTree,
  ImportStatement,
  ImportStatementConfig,
  ImportStatementTree,
  ImportTree,
  IndexSignature,
  IndexSignatureColon,
  IndexSignatureColonConfig,
  IndexSignatureColonTree,
  IndexSignatureConfig,
  IndexSignatureMappedTypeClause,
  IndexSignatureMappedTypeClauseConfig,
  IndexSignatureMappedTypeClauseTree,
  IndexSignatureTree,
  IndexTypeQuery,
  IndexTypeQueryConfig,
  IndexTypeQueryTree,
  InferType,
  InferTypeConfig,
  InferTypeTree,
  Initializer,
  InstantiationExpression,
  InstantiationExpressionConfig,
  InstantiationExpressionTree,
  InterfaceBody,
  InterfaceBodyConfig,
  InterfaceBodyTree,
  InterfaceDeclaration,
  InterfaceDeclarationConfig,
  InterfaceDeclarationTree,
  InternalModule,
  InternalModuleConfig,
  InternalModuleTree,
  IntersectionType,
  IntersectionTypeConfig,
  IntersectionTypeTree,
  JsxAttribute,
  JsxAttributeConfig,
  JsxAttributeName,
  JsxAttributeTree,
  JsxAttributeValue,
  JsxChild,
  JsxClosingElement,
  JsxClosingElementConfig,
  JsxClosingElementTree,
  JsxElement,
  JsxElementConfig,
  JsxElementName,
  JsxElementTree,
  JsxExpression,
  JsxExpressionConfig,
  JsxExpressionTree,
  JsxIdentifier,
  JsxIdentifierTree,
  JsxNamespaceName,
  JsxNamespaceNameConfig,
  JsxNamespaceNameTree,
  JsxOpeningElement,
  JsxOpeningElementConfig,
  JsxOpeningElementTree,
  JsxSelfClosingElement,
  JsxSelfClosingElementConfig,
  JsxSelfClosingElementTree,
  JsxStartOpeningElement,
  JsxText,
  JsxTextTree,
  KindMap,
  LabeledStatement,
  LabeledStatementConfig,
  LabeledStatementTree,
  LexicalDeclaration,
  LexicalDeclarationConfig,
  LexicalDeclarationTree,
  LhsExpression,
  LiteralType,
  LiteralTypeConfig,
  LiteralTypeTree,
  LookupType,
  LookupTypeConfig,
  LookupTypeTree,
  MappedTypeClause,
  MappedTypeClauseConfig,
  MappedTypeClauseTree,
  MemberExpression,
  MemberExpressionConfig,
  MemberExpressionDot,
  MemberExpressionDotConfig,
  MemberExpressionDotTree,
  MemberExpressionOptionalChain,
  MemberExpressionOptionalChainConfig,
  MemberExpressionOptionalChainTree,
  MemberExpressionTree,
  MetaProperty,
  MetaPropertyTree,
  MethodDefinition,
  MethodDefinitionConfig,
  MethodDefinitionTree,
  MethodSignature,
  MethodSignatureConfig,
  MethodSignatureTree,
  Module,
  ModuleConfig,
  ModuleExportName,
  ModuleTree,
  NamedImports,
  NamedImportsConfig,
  NamedImportsTree,
  NamespaceExport,
  NamespaceExportConfig,
  NamespaceExportTree,
  NamespaceImport,
  NamespaceImportConfig,
  NamespaceImportTree,
  NestedIdentifier,
  NestedIdentifierConfig,
  NestedIdentifierTree,
  NestedTypeIdentifier,
  NestedTypeIdentifierConfig,
  NestedTypeIdentifierTree,
  NewExpression,
  NewExpressionConfig,
  NewExpressionTree,
  NonNullExpression,
  NonNullExpressionConfig,
  NonNullExpressionTree,
  Null,
  NullTree,
  Number,
  NumberTree,
  Object,
  ObjectAssignmentPattern,
  ObjectAssignmentPatternConfig,
  ObjectAssignmentPatternTree,
  ObjectConfig,
  ObjectPattern,
  ObjectPatternConfig,
  ObjectPatternTree,
  ObjectTree,
  ObjectType,
  ObjectTypeConfig,
  ObjectTypeTree,
  OmittingTypeAnnotation,
  OmittingTypeAnnotationConfig,
  OmittingTypeAnnotationTree,
  OptingTypeAnnotation,
  OptingTypeAnnotationConfig,
  OptingTypeAnnotationTree,
  OptionalChain,
  OptionalParameter,
  OptionalParameterConfig,
  OptionalParameterTree,
  OptionalTupleParameter,
  OptionalTupleParameterConfig,
  OptionalTupleParameterTree,
  OptionalType,
  OptionalTypeConfig,
  OptionalTypeTree,
  Oror,
  OrorTree,
  OverrideModifier,
  OverrideModifierTree,
  Pair,
  PairConfig,
  PairPattern,
  PairPatternConfig,
  PairPatternTree,
  PairTree,
  ParameterName,
  ParenthesizedExpression,
  ParenthesizedExpressionConfig,
  ParenthesizedExpressionExpression,
  ParenthesizedExpressionExpressionConfig,
  ParenthesizedExpressionExpressionTree,
  ParenthesizedExpressionSequenceExpression,
  ParenthesizedExpressionSequenceExpressionConfig,
  ParenthesizedExpressionSequenceExpressionTree,
  ParenthesizedExpressionTree,
  ParenthesizedType,
  ParenthesizedTypeConfig,
  ParenthesizedTypeTree,
  Pattern,
  PatternConfig,
  PatternTree,
  PredefinedType,
  PredefinedTypeTree,
  PrimaryExpression,
  PrimaryExpressionConfig,
  PrimaryExpressionTree,
  PrimaryType,
  PrivatePropertyIdentifier,
  PrivatePropertyIdentifierTree,
  Program,
  ProgramConfig,
  ProgramTree,
  PropertyIdentifier,
  PropertyIdentifierTree,
  PropertyName,
  PropertySignature,
  PropertySignatureConfig,
  PropertySignatureTree,
  PublicFieldDefinition,
  PublicFieldDefinitionConfig,
  PublicFieldDefinitionTree,
  ReadonlyType,
  ReadonlyTypeConfig,
  ReadonlyTypeTree,
  Regex,
  RegexConfig,
  RegexFlags,
  RegexFlagsTree,
  RegexPattern,
  RegexPatternTree,
  RegexTree,
  RequiredParameter,
  RequiredParameterConfig,
  RequiredParameterTree,
  ReservedIdentifier,
  RestPattern,
  RestPatternConfig,
  RestPatternTree,
  RestType,
  RestTypeConfig,
  RestTypeTree,
  ReturnStatement,
  ReturnStatementConfig,
  ReturnStatementTree,
  SatisfiesExpression,
  SatisfiesExpressionConfig,
  SatisfiesExpressionTree,
  Semicolon,
  SequenceExpression,
  SequenceExpressionConfig,
  SequenceExpressionTree,
  ShorthandPropertyIdentifier,
  ShorthandPropertyIdentifierConfig,
  ShorthandPropertyIdentifierPattern,
  ShorthandPropertyIdentifierPatternConfig,
  ShorthandPropertyIdentifierPatternTree,
  ShorthandPropertyIdentifierTree,
  SpreadElement,
  SpreadElementConfig,
  SpreadElementTree,
  Statement,
  StatementBlock,
  StatementBlockConfig,
  StatementBlockTree,
  StatementBody,
  StatementBodyConfig,
  StatementBodyTree,
  StatementBreakStatement,
  StatementBreakStatementConfig,
  StatementBreakStatementTree,
  StatementConfig,
  StatementContinueStatement,
  StatementContinueStatementConfig,
  StatementContinueStatementTree,
  StatementDebuggerStatement,
  StatementDebuggerStatementConfig,
  StatementDebuggerStatementTree,
  StatementDeclaration,
  StatementDeclarationConfig,
  StatementDeclarationTree,
  StatementDoStatement,
  StatementDoStatementConfig,
  StatementDoStatementTree,
  StatementEmptyStatement,
  StatementEmptyStatementConfig,
  StatementEmptyStatementTree,
  StatementExportStatement,
  StatementExportStatementConfig,
  StatementExportStatementTree,
  StatementExpressionStatement,
  StatementExpressionStatementConfig,
  StatementExpressionStatementTree,
  StatementForInStatement,
  StatementForInStatementConfig,
  StatementForInStatementTree,
  StatementForStatement,
  StatementForStatementConfig,
  StatementForStatementTree,
  StatementIdentifier,
  StatementIdentifierTree,
  StatementIfStatement,
  StatementIfStatementConfig,
  StatementIfStatementTree,
  StatementImportStatement,
  StatementImportStatementConfig,
  StatementImportStatementTree,
  StatementLabeledStatement,
  StatementLabeledStatementConfig,
  StatementLabeledStatementTree,
  StatementReturnStatement,
  StatementReturnStatementConfig,
  StatementReturnStatementTree,
  StatementSwitchStatement,
  StatementSwitchStatementConfig,
  StatementSwitchStatementTree,
  StatementThrowStatement,
  StatementThrowStatementConfig,
  StatementThrowStatementTree,
  StatementTree,
  StatementTryStatement,
  StatementTryStatementConfig,
  StatementTryStatementTree,
  StatementWhileStatement,
  StatementWhileStatementConfig,
  StatementWhileStatementTree,
  StatementWithStatement,
  StatementWithStatementConfig,
  StatementWithStatementTree,
  String,
  StringConfig,
  StringFragment,
  StringFragmentTree,
  StringTree,
  SubscriptExpression,
  SubscriptExpressionConfig,
  SubscriptExpressionTree,
  Super,
  SuperTree,
  SwitchBody,
  SwitchBodyConfig,
  SwitchBodyTree,
  SwitchCase,
  SwitchCaseConfig,
  SwitchCaseTree,
  SwitchDefault,
  SwitchDefaultConfig,
  SwitchDefaultTree,
  SwitchStatement,
  SwitchStatementConfig,
  SwitchStatementTree,
  TemplateChars,
  TemplateLiteralType,
  TemplateLiteralTypeConfig,
  TemplateLiteralTypeTree,
  TemplateString,
  TemplateStringConfig,
  TemplateStringTree,
  TemplateSubstitution,
  TemplateSubstitutionConfig,
  TemplateSubstitutionTree,
  TemplateType,
  TemplateTypeConfig,
  TemplateTypeTree,
  TernaryExpression,
  TernaryExpressionConfig,
  TernaryExpressionTree,
  This,
  ThisTree,
  ThisType,
  ThisTypeTree,
  ThrowStatement,
  ThrowStatementConfig,
  ThrowStatementTree,
  True,
  TrueTree,
  TryStatement,
  TryStatementConfig,
  TryStatementTree,
  TupleParameter,
  TupleParameterConfig,
  TupleParameterTree,
  TupleType,
  TupleTypeConfig,
  TupleTypeMember,
  TupleTypeTree,
  Type,
  TypeAliasDeclaration,
  TypeAliasDeclarationConfig,
  TypeAliasDeclarationTree,
  TypeAnnotation,
  TypeAnnotationConfig,
  TypeAnnotationTree,
  TypeArguments,
  TypeArgumentsConfig,
  TypeArgumentsTree,
  TypeAssertion,
  TypeAssertionConfig,
  TypeAssertionTree,
  TypeIdentifier,
  TypeIdentifierTree,
  TypeParameter,
  TypeParameterConfig,
  TypeParameterTree,
  TypeParameters,
  TypeParametersConfig,
  TypeParametersTree,
  TypePredicate,
  TypePredicateAnnotation,
  TypePredicateAnnotationConfig,
  TypePredicateAnnotationTree,
  TypePredicateConfig,
  TypePredicateTree,
  TypeQuery,
  TypeQueryCallExpression,
  TypeQueryConfig,
  TypeQueryInstantiationExpression,
  TypeQueryMemberExpression,
  TypeQuerySubscriptExpression,
  TypeQueryTree,
  UnaryExpression,
  UnaryExpressionConfig,
  UnaryExpressionTree,
  Undefined,
  UndefinedTree,
  UnescapedDoubleJsxStringFragment,
  UnescapedDoubleJsxStringFragmentTree,
  UnescapedDoubleStringFragment,
  UnescapedDoubleStringFragmentTree,
  UnescapedSingleJsxStringFragment,
  UnescapedSingleJsxStringFragmentTree,
  UnescapedSingleStringFragment,
  UnescapedSingleStringFragmentTree,
  UnionType,
  UnionTypeConfig,
  UnionTypeTree,
  UpdateExpression,
  UpdateExpressionConfig,
  UpdateExpressionTree,
  VariableDeclaration,
  VariableDeclarationConfig,
  VariableDeclarationTree,
  VariableDeclarator,
  VariableDeclaratorConfig,
  VariableDeclaratorTree,
  WhileStatement,
  WhileStatementConfig,
  WhileStatementTree,
  WithStatement,
  WithStatementConfig,
  WithStatementTree,
  YieldExpression,
  YieldExpressionConfig,
  YieldExpressionTree,
  _CallSignature,
  _Identifier,
  _JsxElement,
  _JsxIdentifier,
  _Module,
  _Number,
  _TypeIdentifier,
} from './types.js';
import type { Edit, ByteRange, NonEmptyArray, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const RESERVED_KEYWORDS: ReadonlySet<string> = new Set([
  'abstract',
  'accessor',
  'as',
  'async',
  'await',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'do',
  'else',
  'enum',
  'extends',
  'false',
  'finally',
  'for',
  'from',
  'function',
  'global',
  'if',
  'implements',
  'import',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'namespace',
  'new',
  'null',
  'override',
  'readonly',
  'require',
  'return',
  'satisfies',
  'static',
  'super',
  'switch',
  'this',
  'throw',
  'true',
  'try',
  'typeof',
  'undefined',
  'using',
  'var',
  'while',
  'with',
  'yield',
]);

const _leafRe_hashBangLine = /^(?:#!.*)/u;
const _leafRe_htmlCharacterReference = /^(?:&(#([xX][0-9a-fA-F]{1,6}|[0-9]{1,5})|[A-Za-z]{1,30});)/u;
const _leafRe_jsxIdentifier = /^(?:[a-zA-Z_$][a-zA-Z\d_$]*-[a-zA-Z\d_$\-]*)/u;
const _leafRe_unescapedDoubleJsxStringFragment = /^(?:([^"&]|&[^#A-Za-z])+)/u;
const _leafRe_unescapedSingleJsxStringFragment = /^(?:([^'&]|&[^#A-Za-z])+)/u;
const _leafRe_unescapedDoubleStringFragment = /^(?:[^"\\\r\n]+)/u;
const _leafRe_unescapedSingleStringFragment = /^(?:[^'\\\r\n]+)/u;
const _leafRe_regexFlags = /^(?:[a-z]+)/u;

export function program(config: ProgramConfig) {
  const fields = {
    hash_bang_line: config?.hashBangLine,
    statements: config?.statements,
  };
  return {
    type: 'program' as const,
    named: true as const,
    fields,
    hashBangLine(hashBangLine_?: HashBangLine | undefined) { return _fs(config, program, 'hashBangLine', hashBangLine_, fields.hash_bang_line); },
    statements(...statements_: Statement[]) { return _fsm(config, program, 'statements', statements_, fields.statements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hashBangLine(text: string) {
  if (text.length === 0) throw new Error(`hash_bang_line: text must be non-empty`); if (!_leafRe_hashBangLine.test(text)) throw new Error(`hash_bang_line: text does not match pattern: ${text}`);
  return {
    type: 'hash_bang_line' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HashBangLineTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function exportStatement(config: ExportStatementForm0Config | ExportStatementExportConfig | ExportStatementExport2Config | ExportStatementExport3Config) {
  if (config && 'decorator' in config && 'declaration' in config && 'value' in config) return exportStatementForm0(config as ExportStatementForm0Config);
  return exportStatementExport3(config as ExportStatementExport3Config);
}
export function exportStatementForm0(config: ExportStatementForm0Config) {
  const fields = {
    decorator: config?.decorator,
    declaration: config?.declaration,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'form_0' as const,
    fields,
    children,
    decorator(...decorator_: Decorator[]) { return _fsm(config, exportStatementForm0, 'decorator', decorator_, fields.decorator); },
    declaration(declaration_?: Declaration) { return _fs(config, exportStatementForm0, 'declaration', declaration_, fields.declaration); },
    value(value_?: Expression) { return _fs(config, exportStatementForm0, 'value', value_, fields.value); },
    getChild() { return children[0]; },
    setChild(child: (FromClause | NamespaceExport | ExportClause | Semicolon)) { return exportStatementForm0({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport(config: ExportStatementExportConfig) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'export' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: (ExportClause | FromClause | Semicolon)) { return exportStatementExport({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport2(config: ExportStatementExport2Config) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'export2' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: (Expression | Semicolon)) { return exportStatementExport2({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExport2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementExport3(config: ExportStatementExport3Config) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'export3' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: (Identifier | Semicolon)) { return exportStatementExport3({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementExport3Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceExport(child: ModuleExportName) {
  const children = [child];
  return {
    type: 'namespace_export' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportClause(...children: ExportSpecifier[]) {
  return {
    type: 'export_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportSpecifier(config: ExportSpecifierConfig) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'export_specifier' as const,
    named: true as const,
    fields,
    name(name_?: ModuleExportName) { return _fs(config, exportSpecifier, 'name', name_, fields.name); },
    alias(alias_?: ModuleExportName | undefined) { return _fs(config, exportSpecifier, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declaration(child: (FunctionDeclaration | GeneratorFunctionDeclaration | ClassDeclaration | LexicalDeclaration | VariableDeclaration | FunctionSignature | AbstractClassDeclaration | Module | InternalModule | TypeAliasDeclaration | EnumDeclaration | InterfaceDeclaration | ImportAlias | AmbientDeclaration)) {
  const children = [child];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_() {
  return {
    type: 'import' as const,
    named: true as const,
    text: 'import' as const,
    render: () => 'import' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'import' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'import' as const },
    replace: (t: ImportTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' as const }; },
  };
}

export function importStatement(config: ImportStatementConfig) {
  const fields = {
    import_clause: config?.importClause,
    from_clause: config?.fromClause,
    import_attribute: config?.importAttribute,
    semicolon: config?.semicolon,
  };
  return {
    type: 'import_statement' as const,
    named: true as const,
    fields,
    importClause(importClause_?: "type" | "typeof" | undefined) { return _fs(config, importStatement, 'importClause', importClause_, fields.import_clause); },
    fromClause(fromClause_?: ImportClause | FromClause | ImportRequireClause | String) { return _fs(config, importStatement, 'fromClause', fromClause_, fields.from_clause); },
    importAttribute(importAttribute_?: ImportAttribute | undefined) { return _fs(config, importStatement, 'importAttribute', importAttribute_, fields.import_attribute); },
    semicolon(semicolon_?: Semicolon) { return _fs(config, importStatement, 'semicolon', semicolon_, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClause(config: ImportClauseNamespaceImportConfig | ImportClauseNamedImportsConfig | ImportClauseDefaultImportConfig) {
  if (config && 'defaultImport' in config && 'namedImports' in config) return importClauseDefaultImport(config as ImportClauseDefaultImportConfig);
  return importClauseNamedImports(config as ImportClauseNamedImportsConfig);
}
export function importClauseNamespaceImport(config: ImportClauseNamespaceImportConfig) {
  const children = config?.children ?? [];
  return {
    type: 'import_clause' as const,
    named: true as const,
    variant: 'namespace_import' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: NamespaceImport) { return importClauseNamespaceImport({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseNamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseNamedImports(config: ImportClauseNamedImportsConfig) {
  const children = config?.children ?? [];
  return {
    type: 'import_clause' as const,
    named: true as const,
    variant: 'named_imports' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: NamedImports) { return importClauseNamedImports({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseNamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseDefaultImport(config: ImportClauseDefaultImportConfig) {
  const fields = {
    default_import: config?.defaultImport,
    named_imports: config?.namedImports,
  };
  return {
    type: 'import_clause' as const,
    named: true as const,
    variant: 'default_import' as const,
    fields,
    defaultImport(defaultImport_?: ImportIdentifier) { return _fs(config, importClauseDefaultImport, 'defaultImport', defaultImport_, fields.default_import); },
    namedImports(namedImports_?: NamespaceImport | NamedImports | undefined) { return _fs(config, importClauseDefaultImport, 'namedImports', namedImports_, fields.named_imports); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseDefaultImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceImport(config: NamespaceImportConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'namespace_import' as const,
    named: true as const,
    fields,
    identifier(identifier_?: Identifier) { return _fs(config, namespaceImport, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedImports(...children: ImportSpecifier[]) {
  return {
    type: 'named_imports' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifier(config: ImportSpecifierNameConfig | ImportSpecifierAsConfig) {
  if (config && 'name' in config && 'alias' in config) return importSpecifierAs(config as ImportSpecifierAsConfig);
  return importSpecifierName(config as ImportSpecifierNameConfig);
}
export function importSpecifierName(config: ImportSpecifierNameConfig) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    variant: 'name' as const,
    fields,
    name(name_?: ImportIdentifier) { return _fs(config, importSpecifierName, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importSpecifierAs(config: ImportSpecifierAsConfig) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    variant: 'as' as const,
    fields,
    name(name_?: ModuleExportName) { return _fs(config, importSpecifierAs, 'name', name_, fields.name); },
    alias(alias_?: ImportIdentifier) { return _fs(config, importSpecifierAs, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierAsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAttribute(config: ImportAttributeConfig) {
  const fields = {
    object: config?.object,
  };
  const children = config?.children ?? [];
  return {
    type: 'import_attribute' as const,
    named: true as const,
    fields,
    children,
    object(object_?: "with" | "assert") { return _fs(config, importAttribute, 'object', object_, fields.object); },
    getChild() { return children[0]; },
    setChild(child: Object) { return importAttribute({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statement(config: StatementExportStatementConfig | StatementImportStatementConfig | StatementDebuggerStatementConfig | StatementExpressionStatementConfig | StatementDeclarationConfig | StatementBodyConfig | StatementIfStatementConfig | StatementSwitchStatementConfig | StatementForStatementConfig | StatementForInStatementConfig | StatementWhileStatementConfig | StatementDoStatementConfig | StatementTryStatementConfig | StatementWithStatementConfig | StatementBreakStatementConfig | StatementContinueStatementConfig | StatementReturnStatementConfig | StatementThrowStatementConfig | StatementEmptyStatementConfig | StatementLabeledStatementConfig) {
  if (config && 'body' in config) return statementBody(config as StatementBodyConfig);
  return statementLabeledStatement(config as StatementLabeledStatementConfig);
}
export function statementExportStatement(config: StatementExportStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'export_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ExportStatement) { return statementExportStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementImportStatement(config: StatementImportStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'import_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ImportStatement) { return statementImportStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDebuggerStatement(config: StatementDebuggerStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'debugger_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: DebuggerStatement) { return statementDebuggerStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementExpressionStatement(config: StatementExpressionStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'expression_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ExpressionStatement) { return statementExpressionStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDeclaration(config: StatementDeclarationConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'declaration' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: Declaration) { return statementDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementBody(config: StatementBodyConfig) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    body(body_?: StatementBlock) { return _fs(config, statementBody, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementIfStatement(config: StatementIfStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'if_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: IfStatement) { return statementIfStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementIfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementSwitchStatement(config: StatementSwitchStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'switch_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: SwitchStatement) { return statementSwitchStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementSwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementForStatement(config: StatementForStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'for_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ForStatement) { return statementForStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementForInStatement(config: StatementForInStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'for_in_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ForInStatement) { return statementForInStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementWhileStatement(config: StatementWhileStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'while_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: WhileStatement) { return statementWhileStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementWhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDoStatement(config: StatementDoStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'do_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: DoStatement) { return statementDoStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementDoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementTryStatement(config: StatementTryStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'try_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: TryStatement) { return statementTryStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementWithStatement(config: StatementWithStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'with_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: WithStatement) { return statementWithStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementWithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementBreakStatement(config: StatementBreakStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'break_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: BreakStatement) { return statementBreakStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementContinueStatement(config: StatementContinueStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'continue_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ContinueStatement) { return statementContinueStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementReturnStatement(config: StatementReturnStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'return_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ReturnStatement) { return statementReturnStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementThrowStatement(config: StatementThrowStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'throw_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ThrowStatement) { return statementThrowStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementEmptyStatement(config: StatementEmptyStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'empty_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: EmptyStatement) { return statementEmptyStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementEmptyStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementLabeledStatement(config: StatementLabeledStatementConfig) {
  const children = config?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    variant: 'labeled_statement' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: LabeledStatement) { return statementLabeledStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementLabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child: (Expressions | Semicolon)) {
  const children = [child];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclaration(config: VariableDeclarationConfig) {
  const fields = {
    declarators: config?.declarators,
    semicolon: config?.semicolon,
  };
  return {
    type: 'variable_declaration' as const,
    named: true as const,
    fields,
    declarators(...declarators_: NonEmptyArray<string>) { return _fsm(config, variableDeclaration, 'declarators', declarators_, fields.declarators); },
    semicolon(semicolon_?: Semicolon) { return _fs(config, variableDeclaration, 'semicolon', semicolon_, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lexicalDeclaration(config: LexicalDeclarationConfig) {
  const fields = {
    kind: config?.kind,
    declarators: config?.declarators,
    semicolon: config?.semicolon,
  };
  return {
    type: 'lexical_declaration' as const,
    named: true as const,
    fields,
    kind(kind_?: "let" | "const") { return _fs(config, lexicalDeclaration, 'kind', kind_, fields.kind); },
    declarators(...declarators_: NonEmptyArray<string>) { return _fsm(config, lexicalDeclaration, 'declarators', declarators_, fields.declarators); },
    semicolon(semicolon_?: Semicolon) { return _fs(config, lexicalDeclaration, 'semicolon', semicolon_, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclarator(config: VariableDeclaratorConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'variable_declarator' as const,
    named: true as const,
    fields,
    children,
    name(name_?: Identifier | DestructuringPattern) { return _fs(config, variableDeclarator, 'name', name_, fields.name); },
    typeField(type?: TypeAnnotation | undefined) { return _fs(config, variableDeclarator, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: Initializer) { return variableDeclarator({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statementBlock(config: StatementBlockConfig) {
  const fields = {
    statements: config?.statements,
    automatic_semicolon: config?.automaticSemicolon,
  };
  return {
    type: 'statement_block' as const,
    named: true as const,
    fields,
    statements(...statements_: Statement[]) { return _fsm(config, statementBlock, 'statements', statements_, fields.statements); },
    automaticSemicolon(automaticSemicolon_?: AutomaticSemicolon | undefined) { return _fs(config, statementBlock, 'automaticSemicolon', automaticSemicolon_, fields.automatic_semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: ElseClauseConfig) {
  const fields = {
    statement: config?.statement,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    statement(statement_?: Statement) { return _fs(config, elseClause, 'statement', statement_, fields.statement); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifStatement(config: IfStatementConfig) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: ParenthesizedExpression) { return _fs(config, ifStatement, 'condition', condition_, fields.condition); },
    consequence(consequence_?: Statement) { return _fs(config, ifStatement, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: ElseClause | undefined) { return _fs(config, ifStatement, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchStatement(config: SwitchStatementConfig) {
  const fields = {
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'switch_statement' as const,
    named: true as const,
    fields,
    value(value_?: ParenthesizedExpression) { return _fs(config, switchStatement, 'value', value_, fields.value); },
    body(body_?: SwitchBody) { return _fs(config, switchStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: ForStatementConfig) {
  const fields = {
    initializer: config?.initializer,
    condition: config?.condition,
    increment: config?.increment,
    body: config?.body,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    initializer(initializer_?: LexicalDeclaration | VariableDeclaration | Expressions | EmptyStatement) { return _fs(config, forStatement, 'initializer', initializer_, fields.initializer); },
    condition(condition_?: Expressions | EmptyStatement) { return _fs(config, forStatement, 'condition', condition_, fields.condition); },
    increment(increment_?: Expressions | undefined) { return _fs(config, forStatement, 'increment', increment_, fields.increment); },
    body(body_?: Statement) { return _fs(config, forStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInStatement(config: ForInStatementConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: Statement) { return _fs(config, forInStatement, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ForHeader) { return forInStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: WhileStatementConfig) {
  const fields = {
    condition: config?.condition,
    body: config?.body,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: ParenthesizedExpression) { return _fs(config, whileStatement, 'condition', condition_, fields.condition); },
    body(body_?: Statement) { return _fs(config, whileStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function doStatement(config: DoStatementConfig) {
  const fields = {
    body: config?.body,
    condition: config?.condition,
  };
  const children = config?.children ?? [];
  return {
    type: 'do_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: Statement) { return _fs(config, doStatement, 'body', body_, fields.body); },
    condition(condition_?: ParenthesizedExpression) { return _fs(config, doStatement, 'condition', condition_, fields.condition); },
    getChild() { return children[0]; },
    setChild(child: Semicolon) { return doStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: TryStatementConfig) {
  const fields = {
    body: config?.body,
    handler: config?.handler,
    finalizer: config?.finalizer,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    body(body_?: StatementBlock) { return _fs(config, tryStatement, 'body', body_, fields.body); },
    handler(handler_?: CatchClause | undefined) { return _fs(config, tryStatement, 'handler', handler_, fields.handler); },
    finalizer(finalizer_?: FinallyClause | undefined) { return _fs(config, tryStatement, 'finalizer', finalizer_, fields.finalizer); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: WithStatementConfig) {
  const fields = {
    object: config?.object,
    body: config?.body,
  };
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    object(object_?: ParenthesizedExpression) { return _fs(config, withStatement, 'object', object_, fields.object); },
    body(body_?: Statement) { return _fs(config, withStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakStatement(config: BreakStatementConfig) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'break_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: Identifier | undefined) { return _fs(config, breakStatement, 'label', label_, fields.label); },
    getChild() { return children[0]; },
    setChild(child: Semicolon) { return breakStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueStatement(config: ContinueStatementConfig) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'continue_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: Identifier | undefined) { return _fs(config, continueStatement, 'label', label_, fields.label); },
    getChild() { return children[0]; },
    setChild(child: Semicolon) { return continueStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function debuggerStatement(child: Semicolon) {
  const children = [child];
  return {
    type: 'debugger_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(child?: (Expressions | Semicolon)) {
  const children = child != null ? [child] : [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function throwStatement(child: (Expressions | Semicolon)) {
  const children = [child];
  return {
    type: 'throw_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function labeledStatement(config: LabeledStatementConfig) {
  const fields = {
    label: config?.label,
    body: config?.body,
  };
  return {
    type: 'labeled_statement' as const,
    named: true as const,
    fields,
    label(label_?: Identifier | ReservedIdentifier) { return _fs(config, labeledStatement, 'label', label_, fields.label); },
    body(body_?: Statement) { return _fs(config, labeledStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchBody(...children: (SwitchCase | SwitchDefault)[]) {
  return {
    type: 'switch_body' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchCase(config: SwitchCaseConfig) {
  const fields = {
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'switch_case' as const,
    named: true as const,
    fields,
    value(value_?: Expressions) { return _fs(config, switchCase, 'value', value_, fields.value); },
    body(...body_: Statement[]) { return _fsm(config, switchCase, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchDefault(config: SwitchDefaultConfig) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'switch_default' as const,
    named: true as const,
    fields,
    body(...body_: Statement[]) { return _fsm(config, switchDefault, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function catchClause(config: CatchClauseConfig) {
  const fields = {
    parameter: config?.parameter,
    type: config?.type,
    body: config?.body,
  };
  return {
    type: 'catch_clause' as const,
    named: true as const,
    fields,
    parameter(parameter_?: Identifier | DestructuringPattern | undefined) { return _fs(config, catchClause, 'parameter', parameter_, fields.parameter); },
    typeField(type?: TypeAnnotation | undefined) { return _fs(config, catchClause, 'type', type, fields.type); },
    body(body_?: StatementBlock) { return _fs(config, catchClause, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: FinallyClauseConfig) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    body(body_?: StatementBlock) { return _fs(config, finallyClause, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(config: ParenthesizedExpressionExpressionConfig | ParenthesizedExpressionSequenceExpressionConfig) {
  if (config && 'type' in config) return parenthesizedExpressionExpression(config as ParenthesizedExpressionExpressionConfig);
  return parenthesizedExpressionSequenceExpression(config as ParenthesizedExpressionSequenceExpressionConfig);
}
export function parenthesizedExpressionExpression(config: ParenthesizedExpressionExpressionConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    variant: 'expression' as const,
    fields,
    children,
    typeField(type?: TypeAnnotation | undefined) { return _fs(config, parenthesizedExpressionExpression, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: Expression) { return parenthesizedExpressionExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parenthesizedExpressionSequenceExpression(config: ParenthesizedExpressionSequenceExpressionConfig) {
  const children = config?.children ?? [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    variant: 'sequence_expression' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: SequenceExpression) { return parenthesizedExpressionSequenceExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionSequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expression(child: (AsExpression | SatisfiesExpression | InstantiationExpression | InternalModule | PrimaryExpression | _JsxElement | AssignmentExpression | AugmentedAssignmentExpression | AwaitExpression | UnaryExpression | BinaryExpression | TernaryExpression | UpdateExpression | NewExpression | YieldExpression)) {
  const children = [child];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function primaryExpression(child: (SubscriptExpression | MemberExpression | ParenthesizedExpression | _Identifier | ReservedIdentifier | This | Super | Number | String | TemplateString | Regex | True | False | Null | Object | Array | FunctionExpression | ArrowFunction | GeneratorFunction | Class | MetaProperty | CallExpression | NonNullExpression)) {
  const children = [child];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(config: YieldExpressionConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'yield_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, yieldExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function object(...children: (Pair | SpreadElement | MethodDefinition | Identifier | ReservedIdentifier)[]) {
  return {
    type: 'object' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectPattern(...children: (PairPattern | RestPattern | ObjectAssignmentPattern | Identifier | ReservedIdentifier)[]) {
  return {
    type: 'object_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentPattern(config: AssignmentPatternConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_pattern' as const,
    named: true as const,
    fields,
    left(left_?: Pattern) { return _fs(config, assignmentPattern, 'left', left_, fields.left); },
    right(right_?: Expression) { return _fs(config, assignmentPattern, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectAssignmentPattern(config: ObjectAssignmentPatternConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'object_assignment_pattern' as const,
    named: true as const,
    fields,
    left(left_?: ReservedIdentifier | Identifier | DestructuringPattern) { return _fs(config, objectAssignmentPattern, 'left', left_, fields.left); },
    right(right_?: Expression) { return _fs(config, objectAssignmentPattern, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function array(...children: (Expression | SpreadElement)[]) {
  return {
    type: 'array' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayPattern(...children: (Pattern | AssignmentPattern)[]) {
  return {
    type: 'array_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxElement(config: JsxElementConfig) {
  const fields = {
    open_tag: config?.openTag,
    close_tag: config?.closeTag,
  };
  const children = config?.children ?? [];
  return {
    type: 'jsx_element' as const,
    named: true as const,
    fields,
    children,
    openTag(openTag_?: JsxOpeningElement) { return _fs(config, jsxElement, 'openTag', openTag_, fields.open_tag); },
    closeTag(closeTag_?: JsxClosingElement) { return _fs(config, jsxElement, 'closeTag', closeTag_, fields.close_tag); },
    getChildren() { return children; },
    setChildren(...items: JsxChild[]) { return jsxElement({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlCharacterReference(text: string) {
  if (text.length === 0) throw new Error(`html_character_reference: text must be non-empty`); if (!_leafRe_htmlCharacterReference.test(text)) throw new Error(`html_character_reference: text does not match pattern: ${text}`);
  return {
    type: 'html_character_reference' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HtmlCharacterReferenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxExpression(child?: (Expression | SequenceExpression | SpreadElement)) {
  const children = child != null ? [child] : [];
  return {
    type: 'jsx_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxOpeningElement(child: JsxStartOpeningElement) {
  const children = [child];
  return {
    type: 'jsx_opening_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxIdentifier(text: string) {
  if (text.length === 0) throw new Error(`jsx_identifier: text must be non-empty`); if (!_leafRe_jsxIdentifier.test(text)) throw new Error(`jsx_identifier: text does not match pattern: ${text}`);
  return {
    type: 'jsx_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: JsxIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function nestedIdentifier(config: NestedIdentifierConfig) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'nested_identifier' as const,
    named: true as const,
    fields,
    object(object_?: Identifier | NestedIdentifier) { return _fs(config, nestedIdentifier, 'object', object_, fields.object); },
    property(property_?: Identifier) { return _fs(config, nestedIdentifier, 'property', property_, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxNamespaceName(...children: _JsxIdentifier[]) {
  return {
    type: 'jsx_namespace_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxNamespaceNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxClosingElement(config?: JsxClosingElementConfig) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'jsx_closing_element' as const,
    named: true as const,
    fields,
    name(name_?: JsxElementName | undefined) { return _fs(config, jsxClosingElement, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxSelfClosingElement(child: JsxStartOpeningElement) {
  const children = [child];
  return {
    type: 'jsx_self_closing_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxSelfClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxAttribute(child: (JsxAttributeName | JsxAttributeValue)) {
  const children = [child];
  return {
    type: 'jsx_attribute' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleJsxStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_double_jsx_string_fragment: text must be non-empty`); if (!_leafRe_unescapedDoubleJsxStringFragment.test(text)) throw new Error(`unescaped_double_jsx_string_fragment: text does not match pattern: ${text}`);
  return {
    type: 'unescaped_double_jsx_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedDoubleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleJsxStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_single_jsx_string_fragment: text must be non-empty`); if (!_leafRe_unescapedSingleJsxStringFragment.test(text)) throw new Error(`unescaped_single_jsx_string_fragment: text does not match pattern: ${text}`);
  return {
    type: 'unescaped_single_jsx_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedSingleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function class_(config: ClassConfig) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    type_parameters: config?.typeParameters,
    class_heritage: config?.classHeritage,
    body: config?.body,
  };
  return {
    type: 'class' as const,
    named: true as const,
    fields,
    decorator(...decorator_: Decorator[]) { return _fsm(config, class_, 'decorator', decorator_, fields.decorator); },
    name(name_?: _TypeIdentifier | undefined) { return _fs(config, class_, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, class_, 'typeParameters', typeParameters_, fields.type_parameters); },
    classHeritage(classHeritage_?: ClassHeritage | undefined) { return _fs(config, class_, 'classHeritage', classHeritage_, fields.class_heritage); },
    body(body_?: ClassBody) { return _fs(config, class_, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDeclaration(config: ClassDeclarationConfig) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    type_parameters: config?.typeParameters,
    class_heritage: config?.classHeritage,
    body: config?.body,
    automatic_semicolon: config?.automaticSemicolon,
  };
  return {
    type: 'class_declaration' as const,
    named: true as const,
    fields,
    decorator(...decorator_: Decorator[]) { return _fsm(config, classDeclaration, 'decorator', decorator_, fields.decorator); },
    name(name_?: _TypeIdentifier) { return _fs(config, classDeclaration, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, classDeclaration, 'typeParameters', typeParameters_, fields.type_parameters); },
    classHeritage(classHeritage_?: ClassHeritage | undefined) { return _fs(config, classDeclaration, 'classHeritage', classHeritage_, fields.class_heritage); },
    body(body_?: ClassBody) { return _fs(config, classDeclaration, 'body', body_, fields.body); },
    automaticSemicolon(automaticSemicolon_?: AutomaticSemicolon | undefined) { return _fs(config, classDeclaration, 'automaticSemicolon', automaticSemicolon_, fields.automatic_semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritage(config: ClassHeritageExtendsClauseConfig | ClassHeritageImplementsClauseConfig) {
  if (config && 'extendsClause' in config && 'implementsClause' in config) return classHeritageExtendsClause(config as ClassHeritageExtendsClauseConfig);
  return classHeritageImplementsClause(config as ClassHeritageImplementsClauseConfig);
}
export function classHeritageExtendsClause(config: ClassHeritageExtendsClauseConfig) {
  const fields = {
    extends_clause: config?.extendsClause,
    implements_clause: config?.implementsClause,
  };
  return {
    type: 'class_heritage' as const,
    named: true as const,
    variant: 'extends_clause' as const,
    fields,
    extendsClause(extendsClause_?: ExtendsClause) { return _fs(config, classHeritageExtendsClause, 'extendsClause', extendsClause_, fields.extends_clause); },
    implementsClause(implementsClause_?: ImplementsClause | undefined) { return _fs(config, classHeritageExtendsClause, 'implementsClause', implementsClause_, fields.implements_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function classHeritageImplementsClause(config: ClassHeritageImplementsClauseConfig) {
  const children = config?.children ?? [];
  return {
    type: 'class_heritage' as const,
    named: true as const,
    variant: 'implements_clause' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: ImplementsClause) { return classHeritageImplementsClause({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionExpression(config: FunctionExpressionConfig) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_expression' as const,
    named: true as const,
    fields,
    children,
    name(name_?: Identifier | undefined) { return _fs(config, functionExpression, 'name', name_, fields.name); },
    body(body_?: StatementBlock) { return _fs(config, functionExpression, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: _CallSignature) { return functionExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDeclaration(config: FunctionDeclarationConfig) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: Identifier) { return _fs(config, functionDeclaration, 'name', name_, fields.name); },
    body(body_?: StatementBlock) { return _fs(config, functionDeclaration, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (_CallSignature | AutomaticSemicolon)) { return functionDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunction(config: GeneratorFunctionConfig) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_function' as const,
    named: true as const,
    fields,
    children,
    name(name_?: Identifier | undefined) { return _fs(config, generatorFunction, 'name', name_, fields.name); },
    body(body_?: StatementBlock) { return _fs(config, generatorFunction, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: _CallSignature) { return generatorFunction({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunctionDeclaration(config: GeneratorFunctionDeclarationConfig) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_function_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: Identifier) { return _fs(config, generatorFunctionDeclaration, 'name', name_, fields.name); },
    body(body_?: StatementBlock) { return _fs(config, generatorFunctionDeclaration, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (_CallSignature | AutomaticSemicolon)) { return generatorFunctionDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunction(config: ArrowFunctionParameterConfig | ArrowFunctionUCallSignatureConfig) {
  if (config && 'parameter' in config && 'body' in config) return arrowFunctionParameter(config as ArrowFunctionParameterConfig);
  return arrowFunctionUCallSignature(config as ArrowFunctionUCallSignatureConfig);
}
export function arrowFunctionParameter(config: ArrowFunctionParameterConfig) {
  const fields = {
    parameter: config?.parameter,
    body: config?.body,
  };
  return {
    type: 'arrow_function' as const,
    named: true as const,
    variant: 'parameter' as const,
    fields,
    parameter(parameter_?: ReservedIdentifier | Identifier) { return _fs(config, arrowFunctionParameter, 'parameter', parameter_, fields.parameter); },
    body(body_?: Expression | StatementBlock) { return _fs(config, arrowFunctionParameter, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function arrowFunctionUCallSignature(config: ArrowFunctionUCallSignatureConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'arrow_function' as const,
    named: true as const,
    variant: '_call_signature' as const,
    fields,
    children,
    body(body_?: Expression | StatementBlock) { return _fs(config, arrowFunctionUCallSignature, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: _CallSignature) { return arrowFunctionUCallSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionUCallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: CallExpressionFunctionConfig | CallExpressionFunction2Config | CallExpressionTokQDotConfig) {
  if (config && 'function' in config && 'typeArguments' in config && 'arguments' in config) return callExpressionFunction(config as CallExpressionFunctionConfig);
  return callExpressionFunction2(config as CallExpressionFunction2Config);
}
export function callExpressionFunction(config: CallExpressionFunctionConfig) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'function' as const,
    fields,
    function(function_?: Expression | Import) { return _fs(config, callExpressionFunction, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: TypeArguments | undefined) { return _fs(config, callExpressionFunction, 'typeArguments', typeArguments_, fields.type_arguments); },
    arguments(arguments_?: Arguments) { return _fs(config, callExpressionFunction, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionFunction2(config: CallExpressionFunction2Config) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'function2' as const,
    fields,
    function(function_?: PrimaryExpression | NewExpression) { return _fs(config, callExpressionFunction2, 'function', function_, fields.function); },
    arguments(arguments_?: TemplateString) { return _fs(config, callExpressionFunction2, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionFunction2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionTokQDot(config: CallExpressionTokQDotConfig) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'tok_q_dot' as const,
    fields,
    function(function_?: PrimaryExpression) { return _fs(config, callExpressionTokQDot, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: TypeArguments | undefined) { return _fs(config, callExpressionTokQDot, 'typeArguments', typeArguments_, fields.type_arguments); },
    arguments(arguments_?: Arguments) { return _fs(config, callExpressionTokQDot, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTokQDotTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function newExpression(config: NewExpressionConfig) {
  const fields = {
    constructor: config?.constructor,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'new_expression' as const,
    named: true as const,
    fields,
    constructor(constructor_?: PrimaryExpression) { return _fs(config, newExpression, 'constructor', constructor_, fields.constructor); },
    typeArguments(typeArguments_?: TypeArguments | undefined) { return _fs(config, newExpression, 'typeArguments', typeArguments_, fields.type_arguments); },
    arguments(arguments_?: Arguments | undefined) { return _fs(config, newExpression, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(config: AwaitExpressionConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'await_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, awaitExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberExpression(config: MemberExpressionDotConfig | MemberExpressionOptionalChainConfig) {
  if (config && 'object' in config && 'optionalChain' in config && 'property' in config) return memberExpressionOptionalChain(config as MemberExpressionOptionalChainConfig);
  return memberExpressionDot(config as MemberExpressionDotConfig);
}
export function memberExpressionDot(config: MemberExpressionDotConfig) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'member_expression' as const,
    named: true as const,
    variant: 'dot' as const,
    fields,
    object(object_?: Expression | PrimaryExpression | Import) { return _fs(config, memberExpressionDot, 'object', object_, fields.object); },
    property(property_?: PrivatePropertyIdentifier | Identifier) { return _fs(config, memberExpressionDot, 'property', property_, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberExpressionDotTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function memberExpressionOptionalChain(config: MemberExpressionOptionalChainConfig) {
  const fields = {
    object: config?.object,
    optional_chain: config?.optionalChain,
    property: config?.property,
  };
  return {
    type: 'member_expression' as const,
    named: true as const,
    variant: 'optional_chain' as const,
    fields,
    object(object_?: Expression | PrimaryExpression | Import) { return _fs(config, memberExpressionOptionalChain, 'object', object_, fields.object); },
    optionalChain(optionalChain_?: OptionalChain) { return _fs(config, memberExpressionOptionalChain, 'optionalChain', optionalChain_, fields.optional_chain); },
    property(property_?: PrivatePropertyIdentifier | Identifier) { return _fs(config, memberExpressionOptionalChain, 'property', property_, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberExpressionOptionalChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscriptExpression(config: SubscriptExpressionConfig) {
  const fields = {
    object: config?.object,
    optional_chain: config?.optionalChain,
    index: config?.index,
  };
  return {
    type: 'subscript_expression' as const,
    named: true as const,
    fields,
    object(object_?: Expression | PrimaryExpression) { return _fs(config, subscriptExpression, 'object', object_, fields.object); },
    optionalChain(optionalChain_?: OptionalChain | undefined) { return _fs(config, subscriptExpression, 'optionalChain', optionalChain_, fields.optional_chain); },
    index(index_?: Expressions) { return _fs(config, subscriptExpression, 'index', index_, fields.index); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: AssignmentExpressionConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: ParenthesizedExpression | LhsExpression) { return _fs(config, assignmentExpression, 'left', left_, fields.left); },
    right(right_?: Expression) { return _fs(config, assignmentExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignmentExpression(config: AugmentedAssignmentExpressionConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'augmented_assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: MemberExpression | SubscriptExpression | ReservedIdentifier | Identifier | ParenthesizedExpression | NonNullExpression) { return _fs(config, augmentedAssignmentExpression, 'left', left_, fields.left); },
    operator(operator_?: "+=" | "-=" | "*=" | "/=" | "%=" | "^=" | "&=" | "|=" | ">>=" | ">>>=" | "<<=" | "**=" | "&&=" | "||=" | "??=") { return _fs(config, augmentedAssignmentExpression, 'operator', operator_, fields.operator); },
    right(right_?: Expression) { return _fs(config, augmentedAssignmentExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function spreadElement(config: SpreadElementConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'spread_element' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, spreadElement, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ternaryExpression(config: TernaryExpressionConfig) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'ternary_expression' as const,
    named: true as const,
    fields,
    condition(condition_?: Expression) { return _fs(config, ternaryExpression, 'condition', condition_, fields.condition); },
    consequence(consequence_?: Expression) { return _fs(config, ternaryExpression, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: Expression) { return _fs(config, ternaryExpression, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: BinaryExpressionConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(left_?: Expression | PrivatePropertyIdentifier) { return _fs(config, binaryExpression, 'left', left_, fields.left); },
    operator(operator_?: "&&" | "||" | ">>" | ">>>" | "<<" | "&" | "^" | "|" | "+" | "-" | "*" | "/" | "%" | "**" | "<" | "<=" | "==" | "===" | "!=" | "!==" | ">=" | ">" | "??" | "instanceof" | "in") { return _fs(config, binaryExpression, 'operator', operator_, fields.operator); },
    right(right_?: Expression) { return _fs(config, binaryExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: UnaryExpressionConfig) {
  const fields = {
    operator: config?.operator,
    argument: config?.argument,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    operator(operator_?: "!" | "~" | "-" | "+" | "typeof" | "void" | "delete") { return _fs(config, unaryExpression, 'operator', operator_, fields.operator); },
    argument(argument_?: Expression) { return _fs(config, unaryExpression, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function updateExpression(config: UpdateExpressionConfig) {
  const fields = {
    argument: config?.argument,
    operator: config?.operator,
  };
  return {
    type: 'update_expression' as const,
    named: true as const,
    fields,
    argument(argument_?: Expression) { return _fs(config, updateExpression, 'argument', argument_, fields.argument); },
    operator(operator_?: "++" | "--") { return _fs(config, updateExpression, 'operator', operator_, fields.operator); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function sequenceExpression(...children: Expression[]) {
  _assertNonEmpty(children, 'sequence_expression.children');
  return {
    type: 'sequence_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(...children: (UnescapedDoubleStringFragment | EscapeSequence | UnescapedSingleStringFragment)[]) {
  return {
    type: 'string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_double_string_fragment: text must be non-empty`); if (!_leafRe_unescapedDoubleStringFragment.test(text)) throw new Error(`unescaped_double_string_fragment: text does not match pattern: ${text}`);
  return {
    type: 'unescaped_double_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedDoubleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_single_string_fragment: text must be non-empty`); if (!_leafRe_unescapedSingleStringFragment.test(text)) throw new Error(`unescaped_single_string_fragment: text does not match pattern: ${text}`);
  return {
    type: 'unescaped_single_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedSingleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(text: string) {
  if (text.length === 0) throw new Error(`comment: text must be non-empty`);
  return {
    type: 'comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function templateString(...children: (TemplateChars | EscapeSequence | TemplateSubstitution)[]) {
  return {
    type: 'template_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateSubstitution(child: Expressions) {
  const children = [child];
  return {
    type: 'template_substitution' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regex(config: RegexConfig) {
  const fields = {
    pattern: config?.pattern,
    flags: config?.flags,
  };
  return {
    type: 'regex' as const,
    named: true as const,
    fields,
    pattern(pattern_?: RegexPattern) { return _fs(config, regex, 'pattern', pattern_, fields.pattern); },
    flags(flags_?: RegexFlags | undefined) { return _fs(config, regex, 'flags', flags_, fields.flags); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regexPattern(text: string) {
  if (text.length === 0) throw new Error(`regex_pattern: text must be non-empty`);
  return {
    type: 'regex_pattern' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RegexPatternTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regexFlags(text: string) {
  if (text.length === 0) throw new Error(`regex_flags: text must be non-empty`); if (!_leafRe_regexFlags.test(text)) throw new Error(`regex_flags: text does not match pattern: ${text}`);
  return {
    type: 'regex_flags' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RegexFlagsTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function number(text: string) {
  if (text.length === 0) throw new Error(`number: text must be non-empty`);
  return {
    type: 'number' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: NumberTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function privatePropertyIdentifier(text: string) {
  if (text.length === 0) throw new Error(`private_property_identifier: text must be non-empty`);
  return {
    type: 'private_property_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PrivatePropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function metaProperty(text: string) {
  if (text.length === 0) throw new Error(`meta_property: text must be non-empty`);
  return {
    type: 'meta_property' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: MetaPropertyTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function this_() {
  return {
    type: 'this' as const,
    named: true as const,
    text: 'this' as const,
    render: () => 'this' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' as const },
    replace: (t: ThisTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' as const }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'true' as const,
    render: () => 'true' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'true' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'true' as const },
    replace: (t: TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' as const }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'false' as const,
    render: () => 'false' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'false' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'false' as const },
    replace: (t: FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' as const }; },
  };
}

export function null_() {
  return {
    type: 'null' as const,
    named: true as const,
    text: 'null' as const,
    render: () => 'null' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'null' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'null' as const },
    replace: (t: NullTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' as const }; },
  };
}

export function undefined_() {
  return {
    type: 'undefined' as const,
    named: true as const,
    text: 'undefined' as const,
    render: () => 'undefined' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'undefined' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'undefined' as const },
    replace: (t: UndefinedTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' as const }; },
  };
}

export function arguments_(...children: (Expression | SpreadElement)[]) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(child: (Identifier | DecoratorMemberExpression | DecoratorCallExpression | DecoratorParenthesizedExpression)) {
  const children = [child];
  return {
    type: 'decorator' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorMemberExpression(config: DecoratorMemberExpressionConfig) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'decorator_member_expression' as const,
    named: true as const,
    fields,
    object(object_?: Identifier | DecoratorMemberExpression) { return _fs(config, decoratorMemberExpression, 'object', object_, fields.object); },
    property(property_?: Identifier) { return _fs(config, decoratorMemberExpression, 'property', property_, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorMemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorCallExpression(config: DecoratorCallExpressionConfig) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'decorator_call_expression' as const,
    named: true as const,
    fields,
    function(function_?: Identifier | DecoratorMemberExpression) { return _fs(config, decoratorCallExpression, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: TypeArguments | undefined) { return _fs(config, decoratorCallExpression, 'typeArguments', typeArguments_, fields.type_arguments); },
    arguments(arguments_?: Arguments) { return _fs(config, decoratorCallExpression, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorCallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBody(config: ClassBodyConfig) {
  const fields = {
    decorator: config?.decorator,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: Decorator[]) { return _fsm(config, classBody, 'decorator', decorator_, fields.decorator); },
    getChildren() { return children; },
    setChildren(...items: (MethodDefinition | Semicolon | MethodSignature | FunctionSignatureAutomaticSemicolon | ClassStaticBlock | AbstractMethodSignature | IndexSignature | PublicFieldDefinition)[]) { return classBody({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDefinition(config: FieldDefinitionConfig) {
  const fields = {
    decorator: config?.decorator,
    property: config?.property,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_definition' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: Decorator[]) { return _fsm(config, fieldDefinition, 'decorator', decorator_, fields.decorator); },
    property(property_?: PropertyName) { return _fs(config, fieldDefinition, 'property', property_, fields.property); },
    getChild() { return children[0]; },
    setChild(child: Initializer) { return fieldDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formalParameters(...children: FormalParameter[]) {
  return {
    type: 'formal_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classStaticBlock(config: ClassStaticBlockConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_static_block' as const,
    named: true as const,
    fields,
    children,
    body(body_?: StatementBlock) { return _fs(config, classStaticBlock, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: AutomaticSemicolon) { return classStaticBlock({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pattern(child: (LhsExpression | RestPattern)) {
  const children = [child];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restPattern(child: LhsExpression) {
  const children = [child];
  return {
    type: 'rest_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodDefinition(config: MethodDefinitionConfig) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'method_definition' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: AccessibilityModifier | undefined) { return _fs(config, methodDefinition, 'accessibilityModifier', accessibilityModifier_, fields.accessibility_modifier); },
    overrideModifier(overrideModifier_?: "static" | undefined) { return _fs(config, methodDefinition, 'overrideModifier', overrideModifier_, fields.override_modifier); },
    name(name_?: PropertyName) { return _fs(config, methodDefinition, 'name', name_, fields.name); },
    body(body_?: StatementBlock) { return _fs(config, methodDefinition, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (OverrideModifier | _CallSignature)) { return methodDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: PairConfig) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key_?: PropertyName) { return _fs(config, pair, 'key', key_, fields.key); },
    value(value_?: Expression) { return _fs(config, pair, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pairPattern(config: PairPatternConfig) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair_pattern' as const,
    named: true as const,
    fields,
    key(key_?: PropertyName) { return _fs(config, pairPattern, 'key', key_, fields.key); },
    value(value_?: Pattern | AssignmentPattern) { return _fs(config, pairPattern, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function computedPropertyName(config: ComputedPropertyNameConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'computed_property_name' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, computedPropertyName, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function publicFieldDefinition(config: PublicFieldDefinitionConfig) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: Decorator[]) { return _fsm(config, publicFieldDefinition, 'decorator', decorator_, fields.decorator); },
    name(name_?: PropertyName) { return _fs(config, publicFieldDefinition, 'name', name_, fields.name); },
    typeField(type?: TypeAnnotation | undefined) { return _fs(config, publicFieldDefinition, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (AccessibilityModifier | OverrideModifier | Initializer)) { return publicFieldDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonNullExpression(config: NonNullExpressionConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'non_null_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, nonNullExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodSignature(config: MethodSignatureConfig) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: AccessibilityModifier | undefined) { return _fs(config, methodSignature, 'accessibilityModifier', accessibilityModifier_, fields.accessibility_modifier); },
    overrideModifier(overrideModifier_?: "static" | undefined) { return _fs(config, methodSignature, 'overrideModifier', overrideModifier_, fields.override_modifier); },
    name(name_?: PropertyName) { return _fs(config, methodSignature, 'name', name_, fields.name); },
    getChild() { return children[0]; },
    setChild(child: (OverrideModifier | _CallSignature)) { return methodSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractMethodSignature(config: AbstractMethodSignatureConfig) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'abstract_method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: AccessibilityModifier | undefined) { return _fs(config, abstractMethodSignature, 'accessibilityModifier', accessibilityModifier_, fields.accessibility_modifier); },
    overrideModifier(overrideModifier_?: OverrideModifier | undefined) { return _fs(config, abstractMethodSignature, 'overrideModifier', overrideModifier_, fields.override_modifier); },
    name(name_?: PropertyName) { return _fs(config, abstractMethodSignature, 'name', name_, fields.name); },
    getChild() { return children[0]; },
    setChild(child: _CallSignature) { return abstractMethodSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignature(config: FunctionSignatureConfig) {
  const fields = {
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_signature' as const,
    named: true as const,
    fields,
    children,
    name(name_?: Identifier) { return _fs(config, functionSignature, 'name', name_, fields.name); },
    getChild() { return children[0]; },
    setChild(child: (_CallSignature | Semicolon | FunctionSignatureAutomaticSemicolon)) { return functionSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorParenthesizedExpression(child: (Identifier | DecoratorMemberExpression | DecoratorCallExpression)) {
  const children = [child];
  return {
    type: 'decorator_parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAssertion(config: TypeAssertionConfig) {
  const fields = {
    type_arguments: config?.typeArguments,
    expression: config?.expression,
  };
  return {
    type: 'type_assertion' as const,
    named: true as const,
    fields,
    typeArguments(typeArguments_?: TypeArguments) { return _fs(config, typeAssertion, 'typeArguments', typeArguments_, fields.type_arguments); },
    expression(expression_?: Expression) { return _fs(config, typeAssertion, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asExpression(config: AsExpressionConfig) {
  const fields = {
    expression: config?.expression,
    type_annotation: config?.typeAnnotation,
  };
  return {
    type: 'as_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, asExpression, 'expression', expression_, fields.expression); },
    typeAnnotation(typeAnnotation_?: Type) { return _fs(config, asExpression, 'typeAnnotation', typeAnnotation_, fields.type_annotation); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function satisfiesExpression(config: SatisfiesExpressionConfig) {
  const fields = {
    expression: config?.expression,
    type_annotation: config?.typeAnnotation,
  };
  return {
    type: 'satisfies_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, satisfiesExpression, 'expression', expression_, fields.expression); },
    typeAnnotation(typeAnnotation_?: Type) { return _fs(config, satisfiesExpression, 'typeAnnotation', typeAnnotation_, fields.type_annotation); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function instantiationExpression(config: InstantiationExpressionConfig) {
  const fields = {
    expression: config?.expression,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'instantiation_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, instantiationExpression, 'expression', expression_, fields.expression); },
    typeArguments(typeArguments_?: TypeArguments) { return _fs(config, instantiationExpression, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importRequireClause(config: ImportRequireClauseConfig) {
  const fields = {
    identifier: config?.identifier,
    source: config?.source,
  };
  return {
    type: 'import_require_clause' as const,
    named: true as const,
    fields,
    identifier(identifier_?: Identifier) { return _fs(config, importRequireClause, 'identifier', identifier_, fields.identifier); },
    source(source_?: String) { return _fs(config, importRequireClause, 'source', source_, fields.source); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsClause(...children: ExtendsClauseSingle[]) {
  _assertNonEmpty(children, 'extends_clause.children');
  return {
    type: 'extends_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implementsClause(...children: Type[]) {
  _assertNonEmpty(children, 'implements_clause.children');
  return {
    type: 'implements_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ambientDeclaration(config: AmbientDeclarationConfig) {
  const fields = {
    declaration: config?.declaration,
  };
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    fields,
    declaration(declaration_?: Declaration | StatementBlock | Identifier | Type | Semicolon) { return _fs(config, ambientDeclaration, 'declaration', declaration_, fields.declaration); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractClassDeclaration(config: AbstractClassDeclarationConfig) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    type_parameters: config?.typeParameters,
    class_heritage: config?.classHeritage,
    body: config?.body,
  };
  return {
    type: 'abstract_class_declaration' as const,
    named: true as const,
    fields,
    decorator(...decorator_: Decorator[]) { return _fsm(config, abstractClassDeclaration, 'decorator', decorator_, fields.decorator); },
    name(name_?: _TypeIdentifier) { return _fs(config, abstractClassDeclaration, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, abstractClassDeclaration, 'typeParameters', typeParameters_, fields.type_parameters); },
    classHeritage(classHeritage_?: ClassHeritage | undefined) { return _fs(config, abstractClassDeclaration, 'classHeritage', classHeritage_, fields.class_heritage); },
    body(body_?: ClassBody) { return _fs(config, abstractClassDeclaration, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function module(child: _Module) {
  const children = [child];
  return {
    type: 'module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function internalModule(child: _Module) {
  const children = [child];
  return {
    type: 'internal_module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAlias(config: ImportAliasConfig) {
  const fields = {
    name: config?.name,
    value: config?.value,
    semicolon: config?.semicolon,
  };
  return {
    type: 'import_alias' as const,
    named: true as const,
    fields,
    name(name_?: Identifier) { return _fs(config, importAlias, 'name', name_, fields.name); },
    value(value_?: Identifier | NestedIdentifier) { return _fs(config, importAlias, 'value', value_, fields.value); },
    semicolon(semicolon_?: Semicolon) { return _fs(config, importAlias, 'semicolon', semicolon_, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nestedTypeIdentifier(config: NestedTypeIdentifierConfig) {
  const fields = {
    module: config?.module,
    name: config?.name,
  };
  return {
    type: 'nested_type_identifier' as const,
    named: true as const,
    fields,
    module(module_?: Identifier | NestedIdentifier) { return _fs(config, nestedTypeIdentifier, 'module', module_, fields.module); },
    name(name_?: _TypeIdentifier) { return _fs(config, nestedTypeIdentifier, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interfaceDeclaration(config: InterfaceDeclarationConfig) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    extends_type_clause: config?.extendsTypeClause,
    body: config?.body,
  };
  return {
    type: 'interface_declaration' as const,
    named: true as const,
    fields,
    name(name_?: _TypeIdentifier) { return _fs(config, interfaceDeclaration, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, interfaceDeclaration, 'typeParameters', typeParameters_, fields.type_parameters); },
    extendsTypeClause(extendsTypeClause_?: ExtendsTypeClause | undefined) { return _fs(config, interfaceDeclaration, 'extendsTypeClause', extendsTypeClause_, fields.extends_type_clause); },
    body(body_?: ObjectType) { return _fs(config, interfaceDeclaration, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsTypeClause(config: ExtendsTypeClauseConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'extends_type_clause' as const,
    named: true as const,
    fields,
    typeField(...type: NonEmptyArray<_TypeIdentifier | NestedTypeIdentifier | GenericType>) { return _fsm(config, extendsTypeClause, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumDeclaration(config: EnumDeclarationConfig) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'enum_declaration' as const,
    named: true as const,
    fields,
    name(name_?: Identifier) { return _fs(config, enumDeclaration, 'name', name_, fields.name); },
    body(body_?: EnumBody) { return _fs(config, enumDeclaration, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumBody(config?: EnumBodyConfig) {
  const fields = {
    opening: config?.opening,
  };
  return {
    type: 'enum_body' as const,
    named: true as const,
    fields,
    opening(...opening_: string[]) { return _fsm(config, enumBody, 'opening', opening_, fields.opening); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumAssignment(config: EnumAssignmentConfig) {
  const fields = {
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'enum_assignment' as const,
    named: true as const,
    fields,
    children,
    name(name_?: PropertyName) { return _fs(config, enumAssignment, 'name', name_, fields.name); },
    getChild() { return children[0]; },
    setChild(child: Initializer) { return enumAssignment({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasDeclaration(config: TypeAliasDeclarationConfig) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'type_alias_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: _TypeIdentifier) { return _fs(config, typeAliasDeclaration, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, typeAliasDeclaration, 'typeParameters', typeParameters_, fields.type_parameters); },
    value(value_?: Type) { return _fs(config, typeAliasDeclaration, 'value', value_, fields.value); },
    getChild() { return children[0]; },
    setChild(child: Semicolon) { return typeAliasDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function accessibilityModifier(text: 'public' | 'private' | 'protected') {
  return {
    type: 'accessibility_modifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: AccessibilityModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function overrideModifier() {
  return {
    type: 'override_modifier' as const,
    named: true as const,
    text: 'override' as const,
    render: () => 'override' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'override' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'override' as const },
    replace: (t: OverrideModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' as const }; },
  };
}

export function requiredParameter(config: RequiredParameterConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'required_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: TypeAnnotation | undefined) { return _fs(config, requiredParameter, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (ParameterName | Initializer)) { return requiredParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalParameter(config: OptionalParameterConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'optional_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: TypeAnnotation | undefined) { return _fs(config, optionalParameter, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (ParameterName | Initializer)) { return optionalParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function omittingTypeAnnotation(child: Type) {
  const children = [child];
  return {
    type: 'omitting_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function addingTypeAnnotation(child: Type) {
  const children = [child];
  return {
    type: 'adding_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optingTypeAnnotation(child: Type) {
  const children = [child];
  return {
    type: 'opting_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAnnotation(child: Type) {
  const children = [child];
  return {
    type: 'type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asserts(child: (TypePredicate | Identifier | This)) {
  const children = [child];
  return {
    type: 'asserts' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertsAnnotation(config: AssertsAnnotationConfig) {
  const fields = {
    asserts: config?.asserts,
  };
  const children = config?.children ?? [];
  return {
    type: 'asserts_annotation' as const,
    named: true as const,
    fields,
    children,
    asserts(asserts_?: ":") { return _fs(config, assertsAnnotation, 'asserts', asserts_, fields.asserts); },
    getChild() { return children[0]; },
    setChild(child: Asserts) { return assertsAnnotation({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleParameter(config: TupleParameterConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'tuple_parameter' as const,
    named: true as const,
    fields,
    name(name_?: Identifier | RestPattern) { return _fs(config, tupleParameter, 'name', name_, fields.name); },
    typeField(type?: TypeAnnotation) { return _fs(config, tupleParameter, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalTupleParameter(config: OptionalTupleParameterConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'optional_tuple_parameter' as const,
    named: true as const,
    fields,
    name(name_?: Identifier) { return _fs(config, optionalTupleParameter, 'name', name_, fields.name); },
    typeField(type?: TypeAnnotation) { return _fs(config, optionalTupleParameter, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalType(child: Type) {
  const children = [child];
  return {
    type: 'optional_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restType(child: Type) {
  const children = [child];
  return {
    type: 'rest_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructorType(config: ConstructorTypeConfig) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    type: config?.type,
  };
  return {
    type: 'constructor_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, constructorType, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: FormalParameters) { return _fs(config, constructorType, 'parameters', parameters_, fields.parameters); },
    typeField(type?: Type) { return _fs(config, constructorType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateType(child: (PrimaryType | InferType)) {
  const children = [child];
  return {
    type: 'template_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateLiteralType(...children: (TemplateChars | TemplateType)[]) {
  return {
    type: 'template_literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function inferType(config: InferTypeConfig) {
  const fields = {
    type_identifier: config?.typeIdentifier,
    constraint: config?.constraint,
  };
  return {
    type: 'infer_type' as const,
    named: true as const,
    fields,
    typeIdentifier(typeIdentifier_?: _TypeIdentifier) { return _fs(config, inferType, 'typeIdentifier', typeIdentifier_, fields.type_identifier); },
    constraint(constraint_?: Type | undefined) { return _fs(config, inferType, 'constraint', constraint_, fields.constraint); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalType(config: ConditionalTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'conditional_type' as const,
    named: true as const,
    fields,
    left(left_?: Type) { return _fs(config, conditionalType, 'left', left_, fields.left); },
    right(right_?: Type) { return _fs(config, conditionalType, 'right', right_, fields.right); },
    consequence(consequence_?: Type) { return _fs(config, conditionalType, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: Type) { return _fs(config, conditionalType, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: GenericTypeConfig) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    name(name_?: _TypeIdentifier | NestedTypeIdentifier) { return _fs(config, genericType, 'name', name_, fields.name); },
    typeArguments(typeArguments_?: TypeArguments) { return _fs(config, genericType, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicate(config: TypePredicateConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'type_predicate' as const,
    named: true as const,
    fields,
    name(name_?: Identifier | This | PredefinedType) { return _fs(config, typePredicate, 'name', name_, fields.name); },
    typeField(type?: Type) { return _fs(config, typePredicate, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicateAnnotation(config: TypePredicateAnnotationConfig) {
  const fields = {
    type_predicate: config?.typePredicate,
  };
  const children = config?.children ?? [];
  return {
    type: 'type_predicate_annotation' as const,
    named: true as const,
    fields,
    children,
    typePredicate(typePredicate_?: ":") { return _fs(config, typePredicateAnnotation, 'typePredicate', typePredicate_, fields.type_predicate); },
    getChild() { return children[0]; },
    setChild(child: TypePredicate) { return typePredicateAnnotation({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeQuery(child: (TypeQuerySubscriptExpression | TypeQueryMemberExpression | TypeQueryCallExpression | TypeQueryInstantiationExpression | Identifier | This)) {
  const children = [child];
  return {
    type: 'type_query' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexTypeQuery(config: IndexTypeQueryConfig) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'index_type_query' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: PrimaryType) { return _fs(config, indexTypeQuery, 'primaryType', primaryType_, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lookupType(config: LookupTypeConfig) {
  const fields = {
    primary_type: config?.primaryType,
    index_type: config?.indexType,
  };
  return {
    type: 'lookup_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: PrimaryType) { return _fs(config, lookupType, 'primaryType', primaryType_, fields.primary_type); },
    indexType(indexType_?: Type) { return _fs(config, lookupType, 'indexType', indexType_, fields.index_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mappedTypeClause(config: MappedTypeClauseConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
    alias: config?.alias,
  };
  return {
    type: 'mapped_type_clause' as const,
    named: true as const,
    fields,
    name(name_?: _TypeIdentifier) { return _fs(config, mappedTypeClause, 'name', name_, fields.name); },
    typeField(type?: Type) { return _fs(config, mappedTypeClause, 'type', type, fields.type); },
    alias(alias_?: Type | undefined) { return _fs(config, mappedTypeClause, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function literalType(child: (_Number | Number | String | True | False | Null | Undefined)) {
  const children = [child];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function flowMaybeType(config: FlowMaybeTypeConfig) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'flow_maybe_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: PrimaryType) { return _fs(config, flowMaybeType, 'primaryType', primaryType_, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedType(child: Type) {
  const children = [child];
  return {
    type: 'parenthesized_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function predefinedType(text: string) {
  if (text.length === 0) throw new Error(`predefined_type: text must be non-empty`);
  return {
    type: 'predefined_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PredefinedTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeArguments(...children: Type[]) {
  _assertNonEmpty(children, 'type_arguments.children');
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectType(config: ObjectTypeConfig) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'object_type' as const,
    named: true as const,
    fields,
    opening(opening_?: "{" | "{|") { return _fs(config, objectType, 'opening', opening_, fields.opening); },
    members(members_?: ExportStatement | Semicolon | PropertySignature | CallSignature | ConstructSignature | IndexSignature | MethodSignature | undefined) { return _fs(config, objectType, 'members', members_, fields.members); },
    closing(closing_?: "}" | "|}") { return _fs(config, objectType, 'closing', closing_, fields.closing); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callSignature(child: _CallSignature) {
  const children = [child];
  return {
    type: 'call_signature' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertySignature(config: PropertySignatureConfig) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'property_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: AccessibilityModifier | undefined) { return _fs(config, propertySignature, 'accessibilityModifier', accessibilityModifier_, fields.accessibility_modifier); },
    overrideModifier(overrideModifier_?: "static" | undefined) { return _fs(config, propertySignature, 'overrideModifier', overrideModifier_, fields.override_modifier); },
    name(name_?: PropertyName) { return _fs(config, propertySignature, 'name', name_, fields.name); },
    typeField(type?: TypeAnnotation | undefined) { return _fs(config, propertySignature, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: OverrideModifier) { return propertySignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: TypeParameter[]) {
  _assertNonEmpty(children, 'type_parameters.children');
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: TypeParameterConfig) {
  const fields = {
    name: config?.name,
    constraint: config?.constraint,
    value: config?.value,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    name(name_?: _TypeIdentifier) { return _fs(config, typeParameter, 'name', name_, fields.name); },
    constraint(constraint_?: Constraint | undefined) { return _fs(config, typeParameter, 'constraint', constraint_, fields.constraint); },
    value(value_?: DefaultType | undefined) { return _fs(config, typeParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultType(child: Type) {
  const children = [child];
  return {
    type: 'default_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constraint(child: Type) {
  const children = [child];
  return {
    type: 'constraint' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructSignature(config: ConstructSignatureConfig) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    type: config?.type,
  };
  return {
    type: 'construct_signature' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, constructSignature, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: FormalParameters) { return _fs(config, constructSignature, 'parameters', parameters_, fields.parameters); },
    typeField(type?: TypeAnnotation | undefined) { return _fs(config, constructSignature, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignature(config: IndexSignatureColonConfig | IndexSignatureMappedTypeClauseConfig) {
  if (config && 'mappedTypeClause' in config && 'name' in config && 'indexType' in config && 'type' in config) return indexSignatureColon(config as IndexSignatureColonConfig);
  return indexSignatureMappedTypeClause(config as IndexSignatureMappedTypeClauseConfig);
}
export function indexSignatureColon(config: IndexSignatureColonConfig) {
  const fields = {
    mapped_type_clause: config?.mappedTypeClause,
    name: config?.name,
    index_type: config?.indexType,
    type: config?.type,
  };
  return {
    type: 'index_signature' as const,
    named: true as const,
    variant: 'colon' as const,
    fields,
    mappedTypeClause(mappedTypeClause_?: string) { return _fs(config, indexSignatureColon, 'mappedTypeClause', mappedTypeClause_, fields.mapped_type_clause); },
    name(name_?: Identifier | ReservedIdentifier) { return _fs(config, indexSignatureColon, 'name', name_, fields.name); },
    indexType(indexType_?: Type) { return _fs(config, indexSignatureColon, 'indexType', indexType_, fields.index_type); },
    typeField(type?: TypeAnnotation | OmittingTypeAnnotation | AddingTypeAnnotation | OptingTypeAnnotation) { return _fs(config, indexSignatureColon, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureColonTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function indexSignatureMappedTypeClause(config: IndexSignatureMappedTypeClauseConfig) {
  const fields = {
    mapped_type_clause: config?.mappedTypeClause,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'index_signature' as const,
    named: true as const,
    variant: 'mapped_type_clause' as const,
    fields,
    children,
    mappedTypeClause(mappedTypeClause_?: string) { return _fs(config, indexSignatureMappedTypeClause, 'mappedTypeClause', mappedTypeClause_, fields.mapped_type_clause); },
    typeField(type?: TypeAnnotation | OmittingTypeAnnotation | AddingTypeAnnotation | OptingTypeAnnotation) { return _fs(config, indexSignatureMappedTypeClause, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: MappedTypeClause) { return indexSignatureMappedTypeClause({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureMappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: ArrayTypeConfig) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: PrimaryType) { return _fs(config, arrayType, 'primaryType', primaryType_, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: TupleTypeMember[]) {
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function readonlyType(child: Type) {
  const children = [child];
  return {
    type: 'readonly_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: UnionTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left_?: Type | undefined) { return _fs(config, unionType, 'left', left_, fields.left); },
    right(right_?: Type) { return _fs(config, unionType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function intersectionType(config: IntersectionTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'intersection_type' as const,
    named: true as const,
    fields,
    left(left_?: Type | undefined) { return _fs(config, intersectionType, 'left', left_, fields.left); },
    right(right_?: Type) { return _fs(config, intersectionType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: FunctionTypeConfig) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, functionType, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: FormalParameters) { return _fs(config, functionType, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: Type | Asserts | TypePredicate) { return _fs(config, functionType, 'returnType', returnType_, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlComment(text: string) {
  if (text.length === 0) throw new Error(`html_comment: text must be non-empty`);
  return {
    type: 'html_comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HtmlCommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function oror(text: string) {
  if (text.length === 0) throw new Error(`||: text must be non-empty`);
  return {
    type: '||' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: OrorTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxText(text: string) {
  if (text.length === 0) throw new Error(`jsx_text: text must be non-empty`);
  return {
    type: 'jsx_text' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: JsxTextTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function statementIdentifier(text: string) {
  if (text.length === 0) throw new Error(`statement_identifier: text must be non-empty`);
  return {
    type: 'statement_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StatementIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthandPropertyIdentifier(child: (Identifier | ReservedIdentifier)) {
  const children = [child];
  return {
    type: 'shorthand_property_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandPropertyIdentifierPattern(child: (Identifier | ReservedIdentifier)) {
  const children = [child];
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertyIdentifier(text: string) {
  if (text.length === 0) throw new Error(`property_identifier: text must be non-empty`);
  return {
    type: 'property_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringFragment(text: string) {
  if (text.length === 0) throw new Error(`string_fragment: text must be non-empty`);
  return {
    type: 'string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function interfaceBody(config: InterfaceBodyConfig) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'interface_body' as const,
    named: true as const,
    fields,
    opening(opening_?: "{" | "{|") { return _fs(config, interfaceBody, 'opening', opening_, fields.opening); },
    members(members_?: ExportStatement | Semicolon | PropertySignature | CallSignature | ConstructSignature | IndexSignature | MethodSignature | undefined) { return _fs(config, interfaceBody, 'members', members_, fields.members); },
    closing(closing_?: "}" | "|}") { return _fs(config, interfaceBody, 'closing', closing_, fields.closing); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function thisType() {
  return {
    type: 'this_type' as const,
    named: true as const,
    text: 'this' as const,
    render: () => 'this' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' as const },
    replace: (t: ThisTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' as const }; },
  };
}

export function typeIdentifier(text: string) {
  if (text.length === 0) throw new Error(`type_identifier: text must be non-empty`);
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "program": FluentNode<"program", ProgramConfig>;
  "hash_bang_line": HashBangLine;
  "export_statement": FluentNode<"export_statement", ExportStatementConfig>;
  "namespace_export": FluentNode<"namespace_export", NamespaceExportConfig>;
  "export_clause": FluentNode<"export_clause", ExportClauseConfig>;
  "export_specifier": FluentNode<"export_specifier", ExportSpecifierConfig>;
  "declaration": FluentNode<"declaration", DeclarationConfig>;
  "import": Import;
  "import_statement": FluentNode<"import_statement", ImportStatementConfig>;
  "import_clause": FluentNode<"import_clause", ImportClauseConfig>;
  "namespace_import": FluentNode<"namespace_import", NamespaceImportConfig>;
  "named_imports": FluentNode<"named_imports", NamedImportsConfig>;
  "import_specifier": FluentNode<"import_specifier", ImportSpecifierConfig>;
  "import_attribute": FluentNode<"import_attribute", ImportAttributeConfig>;
  "statement": FluentNode<"statement", StatementConfig>;
  "expression_statement": FluentNode<"expression_statement", ExpressionStatementConfig>;
  "variable_declaration": FluentNode<"variable_declaration", VariableDeclarationConfig>;
  "lexical_declaration": FluentNode<"lexical_declaration", LexicalDeclarationConfig>;
  "variable_declarator": FluentNode<"variable_declarator", VariableDeclaratorConfig>;
  "statement_block": FluentNode<"statement_block", StatementBlockConfig>;
  "else_clause": FluentNode<"else_clause", ElseClauseConfig>;
  "if_statement": FluentNode<"if_statement", IfStatementConfig>;
  "switch_statement": FluentNode<"switch_statement", SwitchStatementConfig>;
  "for_statement": FluentNode<"for_statement", ForStatementConfig>;
  "for_in_statement": FluentNode<"for_in_statement", ForInStatementConfig>;
  "while_statement": FluentNode<"while_statement", WhileStatementConfig>;
  "do_statement": FluentNode<"do_statement", DoStatementConfig>;
  "try_statement": FluentNode<"try_statement", TryStatementConfig>;
  "with_statement": FluentNode<"with_statement", WithStatementConfig>;
  "break_statement": FluentNode<"break_statement", BreakStatementConfig>;
  "continue_statement": FluentNode<"continue_statement", ContinueStatementConfig>;
  "debugger_statement": FluentNode<"debugger_statement", DebuggerStatementConfig>;
  "return_statement": FluentNode<"return_statement", ReturnStatementConfig>;
  "throw_statement": FluentNode<"throw_statement", ThrowStatementConfig>;
  "labeled_statement": FluentNode<"labeled_statement", LabeledStatementConfig>;
  "switch_body": FluentNode<"switch_body", SwitchBodyConfig>;
  "switch_case": FluentNode<"switch_case", SwitchCaseConfig>;
  "switch_default": FluentNode<"switch_default", SwitchDefaultConfig>;
  "catch_clause": FluentNode<"catch_clause", CatchClauseConfig>;
  "finally_clause": FluentNode<"finally_clause", FinallyClauseConfig>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", ParenthesizedExpressionConfig>;
  "expression": FluentNode<"expression", ExpressionConfig>;
  "primary_expression": FluentNode<"primary_expression", PrimaryExpressionConfig>;
  "yield_expression": FluentNode<"yield_expression", YieldExpressionConfig>;
  "object": FluentNode<"object", ObjectConfig>;
  "object_pattern": FluentNode<"object_pattern", ObjectPatternConfig>;
  "assignment_pattern": FluentNode<"assignment_pattern", AssignmentPatternConfig>;
  "object_assignment_pattern": FluentNode<"object_assignment_pattern", ObjectAssignmentPatternConfig>;
  "array": FluentNode<"array", ArrayConfig>;
  "array_pattern": FluentNode<"array_pattern", ArrayPatternConfig>;
  "jsx_element": FluentNode<"jsx_element", JsxElementConfig>;
  "html_character_reference": HtmlCharacterReference;
  "jsx_expression": FluentNode<"jsx_expression", JsxExpressionConfig>;
  "jsx_opening_element": FluentNode<"jsx_opening_element", JsxOpeningElementConfig>;
  "jsx_identifier": JsxIdentifier;
  "nested_identifier": FluentNode<"nested_identifier", NestedIdentifierConfig>;
  "jsx_namespace_name": FluentNode<"jsx_namespace_name", JsxNamespaceNameConfig>;
  "jsx_closing_element": FluentNode<"jsx_closing_element", JsxClosingElementConfig>;
  "jsx_self_closing_element": FluentNode<"jsx_self_closing_element", JsxSelfClosingElementConfig>;
  "jsx_attribute": FluentNode<"jsx_attribute", JsxAttributeConfig>;
  "unescaped_double_jsx_string_fragment": UnescapedDoubleJsxStringFragment;
  "unescaped_single_jsx_string_fragment": UnescapedSingleJsxStringFragment;
  "class": FluentNode<"class", ClassConfig>;
  "class_declaration": FluentNode<"class_declaration", ClassDeclarationConfig>;
  "class_heritage": FluentNode<"class_heritage", ClassHeritageConfig>;
  "function_expression": FluentNode<"function_expression", FunctionExpressionConfig>;
  "function_declaration": FluentNode<"function_declaration", FunctionDeclarationConfig>;
  "generator_function": FluentNode<"generator_function", GeneratorFunctionConfig>;
  "generator_function_declaration": FluentNode<"generator_function_declaration", GeneratorFunctionDeclarationConfig>;
  "arrow_function": FluentNode<"arrow_function", ArrowFunctionConfig>;
  "call_expression": FluentNode<"call_expression", CallExpressionConfig>;
  "new_expression": FluentNode<"new_expression", NewExpressionConfig>;
  "await_expression": FluentNode<"await_expression", AwaitExpressionConfig>;
  "member_expression": FluentNode<"member_expression", MemberExpressionConfig>;
  "subscript_expression": FluentNode<"subscript_expression", SubscriptExpressionConfig>;
  "assignment_expression": FluentNode<"assignment_expression", AssignmentExpressionConfig>;
  "augmented_assignment_expression": FluentNode<"augmented_assignment_expression", AugmentedAssignmentExpressionConfig>;
  "spread_element": FluentNode<"spread_element", SpreadElementConfig>;
  "ternary_expression": FluentNode<"ternary_expression", TernaryExpressionConfig>;
  "binary_expression": FluentNode<"binary_expression", BinaryExpressionConfig>;
  "unary_expression": FluentNode<"unary_expression", UnaryExpressionConfig>;
  "update_expression": FluentNode<"update_expression", UpdateExpressionConfig>;
  "sequence_expression": FluentNode<"sequence_expression", SequenceExpressionConfig>;
  "string": FluentNode<"string", StringConfig>;
  "unescaped_double_string_fragment": UnescapedDoubleStringFragment;
  "unescaped_single_string_fragment": UnescapedSingleStringFragment;
  "escape_sequence": EscapeSequence;
  "comment": Comment;
  "template_string": FluentNode<"template_string", TemplateStringConfig>;
  "template_substitution": FluentNode<"template_substitution", TemplateSubstitutionConfig>;
  "regex": FluentNode<"regex", RegexConfig>;
  "regex_pattern": RegexPattern;
  "regex_flags": RegexFlags;
  "number": Number;
  "identifier": Identifier;
  "private_property_identifier": PrivatePropertyIdentifier;
  "meta_property": MetaProperty;
  "this": This;
  "super": Super;
  "true": True;
  "false": False;
  "null": Null;
  "undefined": Undefined;
  "arguments": FluentNode<"arguments", ArgumentsConfig>;
  "decorator": FluentNode<"decorator", DecoratorConfig>;
  "decorator_member_expression": FluentNode<"decorator_member_expression", DecoratorMemberExpressionConfig>;
  "decorator_call_expression": FluentNode<"decorator_call_expression", DecoratorCallExpressionConfig>;
  "class_body": FluentNode<"class_body", ClassBodyConfig>;
  "field_definition": FluentNode<"field_definition", FieldDefinitionConfig>;
  "formal_parameters": FluentNode<"formal_parameters", FormalParametersConfig>;
  "class_static_block": FluentNode<"class_static_block", ClassStaticBlockConfig>;
  "pattern": FluentNode<"pattern", PatternConfig>;
  "rest_pattern": FluentNode<"rest_pattern", RestPatternConfig>;
  "method_definition": FluentNode<"method_definition", MethodDefinitionConfig>;
  "pair": FluentNode<"pair", PairConfig>;
  "pair_pattern": FluentNode<"pair_pattern", PairPatternConfig>;
  "computed_property_name": FluentNode<"computed_property_name", ComputedPropertyNameConfig>;
  "public_field_definition": FluentNode<"public_field_definition", PublicFieldDefinitionConfig>;
  "non_null_expression": FluentNode<"non_null_expression", NonNullExpressionConfig>;
  "method_signature": FluentNode<"method_signature", MethodSignatureConfig>;
  "abstract_method_signature": FluentNode<"abstract_method_signature", AbstractMethodSignatureConfig>;
  "function_signature": FluentNode<"function_signature", FunctionSignatureConfig>;
  "decorator_parenthesized_expression": FluentNode<"decorator_parenthesized_expression", DecoratorParenthesizedExpressionConfig>;
  "type_assertion": FluentNode<"type_assertion", TypeAssertionConfig>;
  "as_expression": FluentNode<"as_expression", AsExpressionConfig>;
  "satisfies_expression": FluentNode<"satisfies_expression", SatisfiesExpressionConfig>;
  "instantiation_expression": FluentNode<"instantiation_expression", InstantiationExpressionConfig>;
  "import_require_clause": FluentNode<"import_require_clause", ImportRequireClauseConfig>;
  "extends_clause": FluentNode<"extends_clause", ExtendsClauseConfig>;
  "implements_clause": FluentNode<"implements_clause", ImplementsClauseConfig>;
  "ambient_declaration": FluentNode<"ambient_declaration", AmbientDeclarationConfig>;
  "abstract_class_declaration": FluentNode<"abstract_class_declaration", AbstractClassDeclarationConfig>;
  "module": FluentNode<"module", ModuleConfig>;
  "internal_module": FluentNode<"internal_module", InternalModuleConfig>;
  "import_alias": FluentNode<"import_alias", ImportAliasConfig>;
  "nested_type_identifier": FluentNode<"nested_type_identifier", NestedTypeIdentifierConfig>;
  "interface_declaration": FluentNode<"interface_declaration", InterfaceDeclarationConfig>;
  "extends_type_clause": FluentNode<"extends_type_clause", ExtendsTypeClauseConfig>;
  "enum_declaration": FluentNode<"enum_declaration", EnumDeclarationConfig>;
  "enum_body": FluentNode<"enum_body", EnumBodyConfig>;
  "enum_assignment": FluentNode<"enum_assignment", EnumAssignmentConfig>;
  "type_alias_declaration": FluentNode<"type_alias_declaration", TypeAliasDeclarationConfig>;
  "accessibility_modifier": AccessibilityModifier;
  "override_modifier": OverrideModifier;
  "required_parameter": FluentNode<"required_parameter", RequiredParameterConfig>;
  "optional_parameter": FluentNode<"optional_parameter", OptionalParameterConfig>;
  "omitting_type_annotation": FluentNode<"omitting_type_annotation", OmittingTypeAnnotationConfig>;
  "adding_type_annotation": FluentNode<"adding_type_annotation", AddingTypeAnnotationConfig>;
  "opting_type_annotation": FluentNode<"opting_type_annotation", OptingTypeAnnotationConfig>;
  "type_annotation": FluentNode<"type_annotation", TypeAnnotationConfig>;
  "asserts": FluentNode<"asserts", AssertsConfig>;
  "asserts_annotation": FluentNode<"asserts_annotation", AssertsAnnotationConfig>;
  "tuple_parameter": FluentNode<"tuple_parameter", TupleParameterConfig>;
  "optional_tuple_parameter": FluentNode<"optional_tuple_parameter", OptionalTupleParameterConfig>;
  "optional_type": FluentNode<"optional_type", OptionalTypeConfig>;
  "rest_type": FluentNode<"rest_type", RestTypeConfig>;
  "constructor_type": FluentNode<"constructor_type", ConstructorTypeConfig>;
  "template_type": FluentNode<"template_type", TemplateTypeConfig>;
  "template_literal_type": FluentNode<"template_literal_type", TemplateLiteralTypeConfig>;
  "infer_type": FluentNode<"infer_type", InferTypeConfig>;
  "conditional_type": FluentNode<"conditional_type", ConditionalTypeConfig>;
  "generic_type": FluentNode<"generic_type", GenericTypeConfig>;
  "type_predicate": FluentNode<"type_predicate", TypePredicateConfig>;
  "type_predicate_annotation": FluentNode<"type_predicate_annotation", TypePredicateAnnotationConfig>;
  "type_query": FluentNode<"type_query", TypeQueryConfig>;
  "index_type_query": FluentNode<"index_type_query", IndexTypeQueryConfig>;
  "lookup_type": FluentNode<"lookup_type", LookupTypeConfig>;
  "mapped_type_clause": FluentNode<"mapped_type_clause", MappedTypeClauseConfig>;
  "literal_type": FluentNode<"literal_type", LiteralTypeConfig>;
  "flow_maybe_type": FluentNode<"flow_maybe_type", FlowMaybeTypeConfig>;
  "parenthesized_type": FluentNode<"parenthesized_type", ParenthesizedTypeConfig>;
  "predefined_type": PredefinedType;
  "type_arguments": FluentNode<"type_arguments", TypeArgumentsConfig>;
  "object_type": FluentNode<"object_type", ObjectTypeConfig>;
  "call_signature": FluentNode<"call_signature", CallSignatureConfig>;
  "property_signature": FluentNode<"property_signature", PropertySignatureConfig>;
  "type_parameters": FluentNode<"type_parameters", TypeParametersConfig>;
  "type_parameter": FluentNode<"type_parameter", TypeParameterConfig>;
  "default_type": FluentNode<"default_type", DefaultTypeConfig>;
  "constraint": FluentNode<"constraint", ConstraintConfig>;
  "construct_signature": FluentNode<"construct_signature", ConstructSignatureConfig>;
  "index_signature": FluentNode<"index_signature", IndexSignatureConfig>;
  "array_type": FluentNode<"array_type", ArrayTypeConfig>;
  "tuple_type": FluentNode<"tuple_type", TupleTypeConfig>;
  "readonly_type": FluentNode<"readonly_type", ReadonlyTypeConfig>;
  "union_type": FluentNode<"union_type", UnionTypeConfig>;
  "intersection_type": FluentNode<"intersection_type", IntersectionTypeConfig>;
  "function_type": FluentNode<"function_type", FunctionTypeConfig>;
  "html_comment": HtmlComment;
  "||": Oror;
  "jsx_text": JsxText;
  "statement_identifier": StatementIdentifier;
  "shorthand_property_identifier": FluentNode<"shorthand_property_identifier", ShorthandPropertyIdentifierConfig>;
  "shorthand_property_identifier_pattern": FluentNode<"shorthand_property_identifier_pattern", ShorthandPropertyIdentifierPatternConfig>;
  "property_identifier": PropertyIdentifier;
  "string_fragment": StringFragment;
  "interface_body": FluentNode<"interface_body", InterfaceBodyConfig>;
  "this_type": ThisType;
  "type_identifier": TypeIdentifier;
};

export const _factoryMap = {
  "program": program,
  "hash_bang_line": hashBangLine,
  "export_statement": exportStatement,
  "namespace_export": namespaceExport,
  "export_clause": exportClause,
  "export_specifier": exportSpecifier,
  "declaration": declaration,
  "import": import_,
  "import_statement": importStatement,
  "import_clause": importClause,
  "namespace_import": namespaceImport,
  "named_imports": namedImports,
  "import_specifier": importSpecifier,
  "import_attribute": importAttribute,
  "statement": statement,
  "expression_statement": expressionStatement,
  "variable_declaration": variableDeclaration,
  "lexical_declaration": lexicalDeclaration,
  "variable_declarator": variableDeclarator,
  "statement_block": statementBlock,
  "else_clause": elseClause,
  "if_statement": ifStatement,
  "switch_statement": switchStatement,
  "for_statement": forStatement,
  "for_in_statement": forInStatement,
  "while_statement": whileStatement,
  "do_statement": doStatement,
  "try_statement": tryStatement,
  "with_statement": withStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "debugger_statement": debuggerStatement,
  "return_statement": returnStatement,
  "throw_statement": throwStatement,
  "labeled_statement": labeledStatement,
  "switch_body": switchBody,
  "switch_case": switchCase,
  "switch_default": switchDefault,
  "catch_clause": catchClause,
  "finally_clause": finallyClause,
  "parenthesized_expression": parenthesizedExpression,
  "expression": expression,
  "primary_expression": primaryExpression,
  "yield_expression": yieldExpression,
  "object": object,
  "object_pattern": objectPattern,
  "assignment_pattern": assignmentPattern,
  "object_assignment_pattern": objectAssignmentPattern,
  "array": array,
  "array_pattern": arrayPattern,
  "jsx_element": jsxElement,
  "html_character_reference": htmlCharacterReference,
  "jsx_expression": jsxExpression,
  "jsx_opening_element": jsxOpeningElement,
  "jsx_identifier": jsxIdentifier,
  "nested_identifier": nestedIdentifier,
  "jsx_namespace_name": jsxNamespaceName,
  "jsx_closing_element": jsxClosingElement,
  "jsx_self_closing_element": jsxSelfClosingElement,
  "jsx_attribute": jsxAttribute,
  "unescaped_double_jsx_string_fragment": unescapedDoubleJsxStringFragment,
  "unescaped_single_jsx_string_fragment": unescapedSingleJsxStringFragment,
  "class": class_,
  "class_declaration": classDeclaration,
  "class_heritage": classHeritage,
  "function_expression": functionExpression,
  "function_declaration": functionDeclaration,
  "generator_function": generatorFunction,
  "generator_function_declaration": generatorFunctionDeclaration,
  "arrow_function": arrowFunction,
  "call_expression": callExpression,
  "new_expression": newExpression,
  "await_expression": awaitExpression,
  "member_expression": memberExpression,
  "subscript_expression": subscriptExpression,
  "assignment_expression": assignmentExpression,
  "augmented_assignment_expression": augmentedAssignmentExpression,
  "spread_element": spreadElement,
  "ternary_expression": ternaryExpression,
  "binary_expression": binaryExpression,
  "unary_expression": unaryExpression,
  "update_expression": updateExpression,
  "sequence_expression": sequenceExpression,
  "string": string,
  "unescaped_double_string_fragment": unescapedDoubleStringFragment,
  "unescaped_single_string_fragment": unescapedSingleStringFragment,
  "escape_sequence": escapeSequence,
  "comment": comment,
  "template_string": templateString,
  "template_substitution": templateSubstitution,
  "regex": regex,
  "regex_pattern": regexPattern,
  "regex_flags": regexFlags,
  "number": number,
  "identifier": identifier,
  "private_property_identifier": privatePropertyIdentifier,
  "meta_property": metaProperty,
  "this": this_,
  "super": super_,
  "true": true_,
  "false": false_,
  "null": null_,
  "undefined": undefined_,
  "arguments": arguments_,
  "decorator": decorator,
  "decorator_member_expression": decoratorMemberExpression,
  "decorator_call_expression": decoratorCallExpression,
  "class_body": classBody,
  "field_definition": fieldDefinition,
  "formal_parameters": formalParameters,
  "class_static_block": classStaticBlock,
  "pattern": pattern,
  "rest_pattern": restPattern,
  "method_definition": methodDefinition,
  "pair": pair,
  "pair_pattern": pairPattern,
  "computed_property_name": computedPropertyName,
  "public_field_definition": publicFieldDefinition,
  "non_null_expression": nonNullExpression,
  "method_signature": methodSignature,
  "abstract_method_signature": abstractMethodSignature,
  "function_signature": functionSignature,
  "decorator_parenthesized_expression": decoratorParenthesizedExpression,
  "type_assertion": typeAssertion,
  "as_expression": asExpression,
  "satisfies_expression": satisfiesExpression,
  "instantiation_expression": instantiationExpression,
  "import_require_clause": importRequireClause,
  "extends_clause": extendsClause,
  "implements_clause": implementsClause,
  "ambient_declaration": ambientDeclaration,
  "abstract_class_declaration": abstractClassDeclaration,
  "module": module,
  "internal_module": internalModule,
  "import_alias": importAlias,
  "nested_type_identifier": nestedTypeIdentifier,
  "interface_declaration": interfaceDeclaration,
  "extends_type_clause": extendsTypeClause,
  "enum_declaration": enumDeclaration,
  "enum_body": enumBody,
  "enum_assignment": enumAssignment,
  "type_alias_declaration": typeAliasDeclaration,
  "accessibility_modifier": accessibilityModifier,
  "override_modifier": overrideModifier,
  "required_parameter": requiredParameter,
  "optional_parameter": optionalParameter,
  "omitting_type_annotation": omittingTypeAnnotation,
  "adding_type_annotation": addingTypeAnnotation,
  "opting_type_annotation": optingTypeAnnotation,
  "type_annotation": typeAnnotation,
  "asserts": asserts,
  "asserts_annotation": assertsAnnotation,
  "tuple_parameter": tupleParameter,
  "optional_tuple_parameter": optionalTupleParameter,
  "optional_type": optionalType,
  "rest_type": restType,
  "constructor_type": constructorType,
  "template_type": templateType,
  "template_literal_type": templateLiteralType,
  "infer_type": inferType,
  "conditional_type": conditionalType,
  "generic_type": genericType,
  "type_predicate": typePredicate,
  "type_predicate_annotation": typePredicateAnnotation,
  "type_query": typeQuery,
  "index_type_query": indexTypeQuery,
  "lookup_type": lookupType,
  "mapped_type_clause": mappedTypeClause,
  "literal_type": literalType,
  "flow_maybe_type": flowMaybeType,
  "parenthesized_type": parenthesizedType,
  "predefined_type": predefinedType,
  "type_arguments": typeArguments,
  "object_type": objectType,
  "call_signature": callSignature,
  "property_signature": propertySignature,
  "type_parameters": typeParameters,
  "type_parameter": typeParameter,
  "default_type": defaultType,
  "constraint": constraint,
  "construct_signature": constructSignature,
  "index_signature": indexSignature,
  "array_type": arrayType,
  "tuple_type": tupleType,
  "readonly_type": readonlyType,
  "union_type": unionType,
  "intersection_type": intersectionType,
  "function_type": functionType,
  "html_comment": htmlComment,
  "||": oror,
  "jsx_text": jsxText,
  "statement_identifier": statementIdentifier,
  "shorthand_property_identifier": shorthandPropertyIdentifier,
  "shorthand_property_identifier_pattern": shorthandPropertyIdentifierPattern,
  "property_identifier": propertyIdentifier,
  "string_fragment": stringFragment,
  "interface_body": interfaceBody,
  "this_type": thisType,
  "type_identifier": typeIdentifier,
} as const;
export type _FactoryMap = typeof _factoryMap;

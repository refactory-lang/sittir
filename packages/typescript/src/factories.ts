// Auto-generated by @sittir/codegen — do not edit

import type { NodeData } from './types.js';
import type { AbstractClassDeclaration, AbstractClassDeclarationConfig, AbstractClassDeclarationTree, AbstractMethodSignature, AbstractMethodSignatureConfig, AbstractMethodSignatureTree, AccessibilityModifier, AccessibilityModifierTree, AddingTypeAnnotation, AddingTypeAnnotationConfig, AddingTypeAnnotationTree, AmbientDeclaration, AmbientDeclarationConfig, AmbientDeclarationTree, Arguments, ArgumentsConfig, ArgumentsTree, Array, ArrayConfig, ArrayPattern, ArrayPatternConfig, ArrayPatternTree, ArrayTree, ArrayType, ArrayTypeConfig, ArrayTypeTree, ArrowFunction, ArrowFunctionConfig, ArrowFunctionTree, AsExpression, AsExpressionConfig, AsExpressionTree, Asserts, AssertsAnnotation, AssertsAnnotationConfig, AssertsAnnotationTree, AssertsConfig, AssertsTree, AssignmentExpression, AssignmentExpressionConfig, AssignmentExpressionTree, AssignmentPattern, AssignmentPatternConfig, AssignmentPatternTree, AugmentedAssignmentExpression, AugmentedAssignmentExpressionConfig, AugmentedAssignmentExpressionTree, AwaitExpression, AwaitExpressionConfig, AwaitExpressionTree, BinaryExpression, BinaryExpressionConfig, BinaryExpressionTree, BreakStatement, BreakStatementConfig, BreakStatementTree, CallExpression, CallExpressionConfig, CallExpressionTree, CallSignature, CallSignatureConfig, CallSignatureTree, CatchClause, CatchClauseConfig, CatchClauseTree, Class, ClassBody, ClassBodyConfig, ClassBodyTree, ClassConfig, ClassDeclaration, ClassDeclarationConfig, ClassDeclarationTree, ClassHeritage, ClassHeritageConfig, ClassHeritageTree, ClassStaticBlock, ClassStaticBlockConfig, ClassStaticBlockTree, ClassTree, Comment, CommentTree, ComputedPropertyName, ComputedPropertyNameConfig, ComputedPropertyNameTree, ConditionalType, ConditionalTypeConfig, ConditionalTypeTree, Constraint, ConstraintConfig, ConstraintTree, ConstructSignature, ConstructSignatureConfig, ConstructSignatureTree, ConstructorType, ConstructorTypeConfig, ConstructorTypeTree, ContinueStatement, ContinueStatementConfig, ContinueStatementTree, DebuggerStatement, DebuggerStatementTree, Declaration, Decorator, DecoratorConfig, DecoratorTree, DefaultType, DefaultTypeConfig, DefaultTypeTree, DoStatement, DoStatementConfig, DoStatementTree, ElseClause, ElseClauseConfig, ElseClauseTree, EmptyStatement, EmptyStatementTree, EnumAssignment, EnumAssignmentConfig, EnumAssignmentTree, EnumBody, EnumBodyConfig, EnumBodyTree, EnumDeclaration, EnumDeclarationConfig, EnumDeclarationTree, EscapeSequence, EscapeSequenceTree, ExistentialType, ExistentialTypeTree, ExportClause, ExportClauseConfig, ExportClauseTree, ExportSpecifier, ExportSpecifierConfig, ExportSpecifierTree, ExportStatement, ExportStatementConfig, ExportStatementTree, Expression, ExpressionStatement, ExpressionStatementConfig, ExpressionStatementTree, ExtendsClause, ExtendsClauseConfig, ExtendsClauseTree, ExtendsTypeClause, ExtendsTypeClauseConfig, ExtendsTypeClauseTree, False, FalseTree, FinallyClause, FinallyClauseConfig, FinallyClauseTree, FlowMaybeType, FlowMaybeTypeConfig, FlowMaybeTypeTree, ForInStatement, ForInStatementConfig, ForInStatementTree, ForStatement, ForStatementConfig, ForStatementTree, FormalParameters, FormalParametersConfig, FormalParametersTree, FunctionDeclaration, FunctionDeclarationConfig, FunctionDeclarationTree, FunctionExpression, FunctionExpressionConfig, FunctionExpressionTree, FunctionSignature, FunctionSignatureConfig, FunctionSignatureTree, FunctionType, FunctionTypeConfig, FunctionTypeTree, GeneratorFunction, GeneratorFunctionConfig, GeneratorFunctionDeclaration, GeneratorFunctionDeclarationConfig, GeneratorFunctionDeclarationTree, GeneratorFunctionTree, GenericType, GenericTypeConfig, GenericTypeTree, HashBangLine, HashBangLineTree, HtmlComment, HtmlCommentTree, Identifier, IdentifierTree, IfStatement, IfStatementConfig, IfStatementTree, ImplementsClause, ImplementsClauseConfig, ImplementsClauseTree, Import, ImportAlias, ImportAliasConfig, ImportAliasTree, ImportAttribute, ImportAttributeConfig, ImportAttributeTree, ImportClause, ImportClauseConfig, ImportClauseTree, ImportRequireClause, ImportRequireClauseConfig, ImportRequireClauseTree, ImportSpecifier, ImportSpecifierConfig, ImportSpecifierTree, ImportStatement, ImportStatementConfig, ImportStatementTree, ImportTree, IndexSignature, IndexSignatureConfig, IndexSignatureTree, IndexTypeQuery, IndexTypeQueryConfig, IndexTypeQueryTree, InferType, InferTypeConfig, InferTypeTree, InstantiationExpression, InstantiationExpressionConfig, InstantiationExpressionTree, InterfaceBody, InterfaceBodyConfig, InterfaceBodyTree, InterfaceDeclaration, InterfaceDeclarationConfig, InterfaceDeclarationTree, InternalModule, InternalModuleConfig, InternalModuleTree, IntersectionType, IntersectionTypeConfig, IntersectionTypeTree, LabeledStatement, LabeledStatementConfig, LabeledStatementTree, LexicalDeclaration, LexicalDeclarationConfig, LexicalDeclarationTree, LiteralType, LiteralTypeConfig, LiteralTypeTree, LookupType, LookupTypeConfig, LookupTypeTree, MappedTypeClause, MappedTypeClauseConfig, MappedTypeClauseTree, MemberExpression, MemberExpressionConfig, MemberExpressionTree, MetaProperty, MetaPropertyTree, MethodDefinition, MethodDefinitionConfig, MethodDefinitionTree, MethodSignature, MethodSignatureConfig, MethodSignatureTree, Module, ModuleConfig, ModuleTree, NamedImports, NamedImportsConfig, NamedImportsTree, NamespaceExport, NamespaceExportConfig, NamespaceExportTree, NamespaceImport, NamespaceImportConfig, NamespaceImportTree, NestedIdentifier, NestedIdentifierConfig, NestedIdentifierTree, NestedTypeIdentifier, NestedTypeIdentifierConfig, NestedTypeIdentifierTree, NewExpression, NewExpressionConfig, NewExpressionTree, NonNullExpression, NonNullExpressionConfig, NonNullExpressionTree, Null, NullTree, Number, NumberTree, Object, ObjectAssignmentPattern, ObjectAssignmentPatternConfig, ObjectAssignmentPatternTree, ObjectConfig, ObjectPattern, ObjectPatternConfig, ObjectPatternTree, ObjectTree, ObjectType, ObjectTypeConfig, ObjectTypeTree, OmittingTypeAnnotation, OmittingTypeAnnotationConfig, OmittingTypeAnnotationTree, OptingTypeAnnotation, OptingTypeAnnotationConfig, OptingTypeAnnotationTree, OptionalChain, OptionalChainTree, OptionalParameter, OptionalParameterConfig, OptionalParameterTree, OptionalType, OptionalTypeConfig, OptionalTypeTree, OverrideModifier, OverrideModifierTree, Pair, PairConfig, PairPattern, PairPatternConfig, PairPatternTree, PairTree, ParenthesizedExpression, ParenthesizedExpressionConfig, ParenthesizedExpressionTree, ParenthesizedType, ParenthesizedTypeConfig, ParenthesizedTypeTree, Pattern, PredefinedType, PredefinedTypeTree, PrimaryExpression, PrimaryType, PrivatePropertyIdentifier, PrivatePropertyIdentifierTree, Program, ProgramConfig, ProgramTree, PropertyIdentifier, PropertyIdentifierTree, PropertySignature, PropertySignatureConfig, PropertySignatureTree, PublicFieldDefinition, PublicFieldDefinitionConfig, PublicFieldDefinitionTree, ReadonlyType, ReadonlyTypeConfig, ReadonlyTypeTree, Regex, RegexConfig, RegexFlags, RegexFlagsTree, RegexPattern, RegexPatternTree, RegexTree, RequiredParameter, RequiredParameterConfig, RequiredParameterTree, RestPattern, RestPatternConfig, RestPatternTree, RestType, RestTypeConfig, RestTypeTree, ReturnStatement, ReturnStatementConfig, ReturnStatementTree, SatisfiesExpression, SatisfiesExpressionConfig, SatisfiesExpressionTree, SequenceExpression, SequenceExpressionConfig, SequenceExpressionTree, ShorthandPropertyIdentifier, ShorthandPropertyIdentifierPattern, ShorthandPropertyIdentifierPatternTree, ShorthandPropertyIdentifierTree, SpreadElement, SpreadElementConfig, SpreadElementTree, Statement, StatementBlock, StatementBlockConfig, StatementBlockTree, StatementIdentifier, StatementIdentifierTree, String, StringConfig, StringFragment, StringFragmentTree, StringTree, SubscriptExpression, SubscriptExpressionConfig, SubscriptExpressionTree, Super, SuperTree, SwitchBody, SwitchBodyConfig, SwitchBodyTree, SwitchCase, SwitchCaseConfig, SwitchCaseTree, SwitchDefault, SwitchDefaultConfig, SwitchDefaultTree, SwitchStatement, SwitchStatementConfig, SwitchStatementTree, TemplateLiteralType, TemplateLiteralTypeConfig, TemplateLiteralTypeTree, TemplateString, TemplateStringConfig, TemplateStringTree, TemplateSubstitution, TemplateSubstitutionConfig, TemplateSubstitutionTree, TemplateType, TemplateTypeConfig, TemplateTypeTree, TernaryExpression, TernaryExpressionConfig, TernaryExpressionTree, This, ThisTree, ThisType, ThisTypeTree, ThrowStatement, ThrowStatementConfig, ThrowStatementTree, True, TrueTree, TryStatement, TryStatementConfig, TryStatementTree, TupleType, TupleTypeConfig, TupleTypeTree, Type, TypeAliasDeclaration, TypeAliasDeclarationConfig, TypeAliasDeclarationTree, TypeAnnotation, TypeAnnotationConfig, TypeAnnotationTree, TypeArguments, TypeArgumentsConfig, TypeArgumentsTree, TypeAssertion, TypeAssertionConfig, TypeAssertionTree, TypeIdentifier, TypeIdentifierTree, TypeParameter, TypeParameterConfig, TypeParameterTree, TypeParameters, TypeParametersConfig, TypeParametersTree, TypePredicate, TypePredicateAnnotation, TypePredicateAnnotationConfig, TypePredicateAnnotationTree, TypePredicateConfig, TypePredicateTree, TypeQuery, TypeQueryConfig, TypeQueryTree, UnaryExpression, UnaryExpressionConfig, UnaryExpressionTree, Undefined, UndefinedTree, UnionType, UnionTypeConfig, UnionTypeTree, UpdateExpression, UpdateExpressionConfig, UpdateExpressionTree, VariableDeclaration, VariableDeclarationConfig, VariableDeclarationTree, VariableDeclarator, VariableDeclaratorConfig, VariableDeclaratorTree, WhileStatement, WhileStatementConfig, WhileStatementTree, WithStatement, WithStatementConfig, WithStatementTree, YieldExpression, YieldExpressionConfig, YieldExpressionTree } from './types.js';
import type { Edit, AnyNodeData } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  'abstract',
  'accessor',
  'any',
  'as',
  'assert',
  'async',
  'await',
  'boolean',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'delete',
  'do',
  'else',
  'enum',
  'export',
  'extends',
  'finally',
  'for',
  'from',
  'function',
  'get',
  'global',
  'if',
  'implements',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'let',
  'meta',
  'namespace',
  'never',
  'new',
  'of',
  'override',
  'private',
  'protected',
  'public',
  'readonly',
  'require',
  'return',
  'satisfies',
  'set',
  'static',
  'switch',
  'symbol',
  'target',
  'throw',
  'try',
  'typeof',
  'unknown',
  'using',
  'var',
  'void',
  'while',
  'with',
  'yield',
]);

export function abstract_class_declaration_(
  config: AbstractClassDeclarationConfig,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'abstract_class_declaration' as const,
    named: true as const,
    fields,
    children,
    body(body?: ClassBody) { return body !== undefined ? abstract_class_declaration_({ ...config, body: body }) : fields.body; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? abstract_class_declaration_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: TypeIdentifier) { return name !== undefined ? abstract_class_declaration_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? abstract_class_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    child(child?: ClassHeritage) { return child !== undefined ? abstract_class_declaration_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function abstract_method_signature_(
  config: AbstractMethodSignatureConfig,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  const children = [...(config.accessibilityModifier ? [config.accessibilityModifier] : []), ...(config.overrideModifier ? [config.overrideModifier] : [])];
  return {
    type: 'abstract_method_signature' as const,
    named: true as const,
    fields,
    children,
    name(name?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String) { return name !== undefined ? abstract_method_signature_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? abstract_method_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? abstract_method_signature_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? abstract_method_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? abstract_method_signature_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? abstract_method_signature_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function adding_type_annotation_(
  config: AddingTypeAnnotationConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'adding_type_annotation' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? adding_type_annotation_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ambient_declaration_(
  config: AmbientDeclarationConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    type: config.type,
  };
  const children = [...(config.children1 ? [config.children1] : []), ...(config.childType ? [config.childType] : [])];
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Declaration | PropertyIdentifier | StatementBlock) { return NEEDS_NAME_0 !== undefined ? ambient_declaration_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    typeField(type_?: Type) { return type_ !== undefined ? ambient_declaration_({ ...config, type: type_ }) : fields.type; },
    children1(children1?: Declaration | PropertyIdentifier | StatementBlock) { return children1 !== undefined ? ambient_declaration_({ ...config, children1 }) : config?.children1; },
    childType(childType?: Type) { return childType !== undefined ? ambient_declaration_({ ...config, childType }) : config?.childType; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function arguments_(
  config?: ArgumentsConfig,
) {
  const fields = {
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.expressionOrSpreadElement1 ? [config?.expressionOrSpreadElement1] : []), ...(config?.expressionOrSpreadElement2 ?? [])];
  return {
    type: 'arguments' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Expression | SpreadElement) { return NEEDS_NAME_0 !== undefined ? arguments_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (Expression | SpreadElement)[]) { return NEEDS_NAME_1.length ? arguments_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    expressionOrSpreadElement1(expressionOrSpreadElement1?: Expression | SpreadElement) { return expressionOrSpreadElement1 !== undefined ? arguments_({ ...config, expressionOrSpreadElement1 }) : config?.expressionOrSpreadElement1; },
    expressionOrSpreadElement2(...expressionOrSpreadElement2: (Expression | SpreadElement)[]) { return expressionOrSpreadElement2.length ? arguments_({ ...config, expressionOrSpreadElement2 }) : config?.expressionOrSpreadElement2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_(
  config?: ArrayConfig,
) {
  const fields = {
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.expressionOrSpreadElement1 ? [config?.expressionOrSpreadElement1] : []), ...(config?.expressionOrSpreadElement2 ?? [])];
  return {
    type: 'array' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Expression | SpreadElement) { return NEEDS_NAME_0 !== undefined ? array_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (Expression | SpreadElement)[]) { return NEEDS_NAME_1.length ? array_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    expressionOrSpreadElement1(expressionOrSpreadElement1?: Expression | SpreadElement) { return expressionOrSpreadElement1 !== undefined ? array_({ ...config, expressionOrSpreadElement1 }) : config?.expressionOrSpreadElement1; },
    expressionOrSpreadElement2(...expressionOrSpreadElement2: (Expression | SpreadElement)[]) { return expressionOrSpreadElement2.length ? array_({ ...config, expressionOrSpreadElement2 }) : config?.expressionOrSpreadElement2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_pattern_(
  config?: ArrayPatternConfig,
) {
  const fields = {
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.assignmentPatternOrPattern1 ? [config?.assignmentPatternOrPattern1] : []), ...(config?.assignmentPatternOrPattern2 ?? [])];
  return {
    type: 'array_pattern' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: AssignmentPattern | Pattern) { return NEEDS_NAME_0 !== undefined ? array_pattern_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (AssignmentPattern | Pattern)[]) { return NEEDS_NAME_1.length ? array_pattern_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    assignmentPatternOrPattern1(assignmentPatternOrPattern1?: AssignmentPattern | Pattern) { return assignmentPatternOrPattern1 !== undefined ? array_pattern_({ ...config, assignmentPatternOrPattern1 }) : config?.assignmentPatternOrPattern1; },
    assignmentPatternOrPattern2(...assignmentPatternOrPattern2: (AssignmentPattern | Pattern)[]) { return assignmentPatternOrPattern2.length ? array_pattern_({ ...config, assignmentPatternOrPattern2 }) : config?.assignmentPatternOrPattern2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_type_(
  config: ArrayTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'array_type' as const,
    named: true as const,
    children,
    child(child?: PrimaryType) { return child !== undefined ? array_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function arrow_function_(
  config: ArrowFunctionConfig,
) {
  const fields = {
    body: config.body,
    parameter: config.parameter,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'arrow_function' as const,
    named: true as const,
    fields,
    body(body?: Expression | StatementBlock) { return body !== undefined ? arrow_function_({ ...config, body: body }) : fields.body; },
    parameter(parameter?: Identifier) { return parameter !== undefined ? arrow_function_({ ...config, parameter: parameter }) : fields.parameter; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? arrow_function_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? arrow_function_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? arrow_function_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function as_expression_(
  config: AsExpressionConfig,
) {
  const fields = {
    expression: config.expression,
    type: config.type,
  };
  const children = [...(config.expression ? [config.expression] : []), ...(config.childType ? [config.childType] : [])];
  return {
    type: 'as_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression?: Expression) { return expression !== undefined ? as_expression_({ ...config, expression: expression }) : fields.expression; },
    typeField(type_?: Type) { return type_ !== undefined ? as_expression_({ ...config, type: type_ }) : fields.type; },
    childType(childType?: Type) { return childType !== undefined ? as_expression_({ ...config, childType }) : config?.childType; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function asserts_(
  config: AssertsConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'asserts' as const,
    named: true as const,
    children,
    child(child?: Identifier | This | TypePredicate) { return child !== undefined ? asserts_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function asserts_annotation_(
  config: AssertsAnnotationConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'asserts_annotation' as const,
    named: true as const,
    children,
    child(child?: Asserts) { return child !== undefined ? asserts_annotation_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_expression_(
  config: AssignmentExpressionConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left?: ArrayPattern | Identifier | MemberExpression | NonNullExpression | ObjectPattern | ParenthesizedExpression | SubscriptExpression | Undefined) { return left !== undefined ? assignment_expression_({ ...config, left: left }) : fields.left; },
    right(right?: Expression) { return right !== undefined ? assignment_expression_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_pattern_(
  config: AssignmentPatternConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment_pattern' as const,
    named: true as const,
    fields,
    left(left?: Pattern) { return left !== undefined ? assignment_pattern_({ ...config, left: left }) : fields.left; },
    right(right?: Expression) { return right !== undefined ? assignment_pattern_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function augmented_assignment_expression_(
  config: AugmentedAssignmentExpressionConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment_expression' as const,
    named: true as const,
    fields,
    left(left?: Identifier | MemberExpression | NonNullExpression | ParenthesizedExpression | SubscriptExpression) { return left !== undefined ? augmented_assignment_expression_({ ...config, left: left }) : fields.left; },
    operator(operator?: '+=' | '-=' | '*=' | '/=' | '%=' | '^=' | '&=' | '|=' | '>>=' | '>>>=' | '<<=' | '**=' | '&&=' | '||=' | '??=') { return operator !== undefined ? augmented_assignment_expression_({ ...config, operator: operator }) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? augmented_assignment_expression_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function await_expression_(
  config: AwaitExpressionConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'await_expression' as const,
    named: true as const,
    children,
    child(child?: Expression) { return child !== undefined ? await_expression_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function binary_expression_(
  config: BinaryExpressionConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression_({ ...config, left: left }) : fields.left; },
    operator(operator?: '&&' | '||' | '>>' | '>>>' | '<<' | '&' | '^' | '|' | '+' | '-' | '*' | '/' | '%' | '**' | '<' | '<=' | '==' | '===' | '!=' | '!==' | '>=' | '>' | '??' | 'instanceof' | 'in') { return operator !== undefined ? binary_expression_({ ...config, operator: operator }) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function break_statement_(
  config?: BreakStatementConfig,
) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'break_statement' as const,
    named: true as const,
    fields,
    label(label?: StatementIdentifier) { return label !== undefined ? break_statement_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call_expression_(
  config: CallExpressionConfig,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    arguments(arguments_?: Arguments | TemplateString) { return arguments_ !== undefined ? call_expression_({ ...config, arguments: arguments_ }) : fields.arguments; },
    function(function_?: Expression | Import | NewExpression | PrimaryExpression) { return function_ !== undefined ? call_expression_({ ...config, function: function_ }) : fields.function; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? call_expression_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call_signature_(
  config: CallSignatureConfig,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'call_signature' as const,
    named: true as const,
    fields,
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? call_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? call_signature_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? call_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function catch_clause_(
  config: CatchClauseConfig,
) {
  const fields = {
    body: config.body,
    parameter: config.parameter,
    type: config.type,
  };
  return {
    type: 'catch_clause' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? catch_clause_({ ...config, body: body }) : fields.body; },
    parameter(parameter?: ArrayPattern | Identifier | ObjectPattern) { return parameter !== undefined ? catch_clause_({ ...config, parameter: parameter }) : fields.parameter; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? catch_clause_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_(
  config: ClassConfig,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'class' as const,
    named: true as const,
    fields,
    children,
    body(body?: ClassBody) { return body !== undefined ? class_({ ...config, body: body }) : fields.body; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? class_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: TypeIdentifier) { return name !== undefined ? class_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? class_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    child(child?: ClassHeritage) { return child !== undefined ? class_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_body_(
  config?: ClassBodyConfig,
) {
  const fields = {
    decorator: config?.decorator,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? class_body_({ ...config, decorator: decorator }) : fields.decorator; },
    getChildren() { return children; },
    setChildren(...children: (AbstractMethodSignature | ClassStaticBlock | IndexSignature | MethodDefinition | MethodSignature | PublicFieldDefinition)[]) { return class_body_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_declaration_(
  config: ClassDeclarationConfig,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'class_declaration' as const,
    named: true as const,
    fields,
    children,
    body(body?: ClassBody) { return body !== undefined ? class_declaration_({ ...config, body: body }) : fields.body; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? class_declaration_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: TypeIdentifier) { return name !== undefined ? class_declaration_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? class_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    child(child?: ClassHeritage) { return child !== undefined ? class_declaration_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_heritage_(
  config: ClassHeritageConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.extendsClauseOrImplementsClause ? [config.extendsClauseOrImplementsClause] : []), ...(config.implementsClause ? [config.implementsClause] : [])];
  return {
    type: 'class_heritage' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: ExtendsClause | ImplementsClause) { return NEEDS_NAME_0 !== undefined ? class_heritage_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(NEEDS_NAME_1?: ImplementsClause) { return NEEDS_NAME_1 !== undefined ? class_heritage_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    extendsClauseOrImplementsClause(extendsClauseOrImplementsClause?: ExtendsClause | ImplementsClause) { return extendsClauseOrImplementsClause !== undefined ? class_heritage_({ ...config, extendsClauseOrImplementsClause }) : config?.extendsClauseOrImplementsClause; },
    implementsClause(implementsClause?: ImplementsClause) { return implementsClause !== undefined ? class_heritage_({ ...config, implementsClause }) : config?.implementsClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_static_block_(
  config: ClassStaticBlockConfig,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'class_static_block' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? class_static_block_({ ...config, body: body }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function computed_property_name_(
  config: ComputedPropertyNameConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'computed_property_name' as const,
    named: true as const,
    children,
    child(child?: Expression) { return child !== undefined ? computed_property_name_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function conditional_type_(
  config: ConditionalTypeConfig,
) {
  const fields = {
    alternative: config.alternative,
    consequence: config.consequence,
    left: config.left,
    right: config.right,
  };
  return {
    type: 'conditional_type' as const,
    named: true as const,
    fields,
    alternative(alternative?: Type) { return alternative !== undefined ? conditional_type_({ ...config, alternative: alternative }) : fields.alternative; },
    consequence(consequence?: Type) { return consequence !== undefined ? conditional_type_({ ...config, consequence: consequence }) : fields.consequence; },
    left(left?: Type) { return left !== undefined ? conditional_type_({ ...config, left: left }) : fields.left; },
    right(right?: Type) { return right !== undefined ? conditional_type_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constraint_(
  config: ConstraintConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'constraint' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? constraint_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function construct_signature_(
  config: ConstructSignatureConfig,
) {
  const fields = {
    parameters: config.parameters,
    type: config.type,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'construct_signature' as const,
    named: true as const,
    fields,
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? construct_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? construct_signature_({ ...config, type: type_ }) : fields.type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? construct_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constructor_type_(
  config: ConstructorTypeConfig,
) {
  const fields = {
    parameters: config.parameters,
    type: config.type,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'constructor_type' as const,
    named: true as const,
    fields,
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? constructor_type_({ ...config, parameters: parameters }) : fields.parameters; },
    typeField(type_?: Type) { return type_ !== undefined ? constructor_type_({ ...config, type: type_ }) : fields.type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? constructor_type_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function continue_statement_(
  config?: ContinueStatementConfig,
) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'continue_statement' as const,
    named: true as const,
    fields,
    label(label?: StatementIdentifier) { return label !== undefined ? continue_statement_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decorator_(
  config: DecoratorConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'decorator' as const,
    named: true as const,
    children,
    child(child?: CallExpression | Identifier | MemberExpression | ParenthesizedExpression) { return child !== undefined ? decorator_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function default_type_(
  config: DefaultTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'default_type' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? default_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function do_statement_(
  config: DoStatementConfig,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
  };
  return {
    type: 'do_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? do_statement_({ ...config, body: body }) : fields.body; },
    condition(condition?: ParenthesizedExpression) { return condition !== undefined ? do_statement_({ ...config, condition: condition }) : fields.condition; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function else_clause_(
  config: ElseClauseConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'else_clause' as const,
    named: true as const,
    children,
    child(child?: Statement) { return child !== undefined ? else_clause_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_assignment_(
  config: EnumAssignmentConfig,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'enum_assignment' as const,
    named: true as const,
    fields,
    name(name?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String) { return name !== undefined ? enum_assignment_({ ...config, name: name }) : fields.name; },
    value(value?: Expression) { return value !== undefined ? enum_assignment_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_body_(
  config?: EnumBodyConfig,
) {
  const fields = {
    name: config?.name,
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.enumAssignment1 ? [config?.enumAssignment1] : []), ...(config?.enumAssignment2 ?? [])];
  return {
    type: 'enum_body' as const,
    named: true as const,
    fields,
    children,
    name(...name: (ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String)[]) { return name.length ? enum_body_({ ...config, name: name }) : fields.name; },
    NEEDS_NAME_0(NEEDS_NAME_0?: EnumAssignment) { return NEEDS_NAME_0 !== undefined ? enum_body_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (EnumAssignment)[]) { return NEEDS_NAME_1.length ? enum_body_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    enumAssignment1(enumAssignment1?: EnumAssignment) { return enumAssignment1 !== undefined ? enum_body_({ ...config, enumAssignment1 }) : config?.enumAssignment1; },
    enumAssignment2(...enumAssignment2: (EnumAssignment)[]) { return enumAssignment2.length ? enum_body_({ ...config, enumAssignment2 }) : config?.enumAssignment2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_declaration_(
  config: EnumDeclarationConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'enum_declaration' as const,
    named: true as const,
    fields,
    body(body?: EnumBody) { return body !== undefined ? enum_declaration_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? enum_declaration_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_clause_(
  config?: ExportClauseConfig,
) {
  const fields = {
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.exportSpecifier1 ? [config?.exportSpecifier1] : []), ...(config?.exportSpecifier2 ?? [])];
  return {
    type: 'export_clause' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: ExportSpecifier) { return NEEDS_NAME_0 !== undefined ? export_clause_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (ExportSpecifier)[]) { return NEEDS_NAME_1.length ? export_clause_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    exportSpecifier1(exportSpecifier1?: ExportSpecifier) { return exportSpecifier1 !== undefined ? export_clause_({ ...config, exportSpecifier1 }) : config?.exportSpecifier1; },
    exportSpecifier2(...exportSpecifier2: (ExportSpecifier)[]) { return exportSpecifier2.length ? export_clause_({ ...config, exportSpecifier2 }) : config?.exportSpecifier2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_specifier_(
  config: ExportSpecifierConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  return {
    type: 'export_specifier' as const,
    named: true as const,
    fields,
    alias(alias?: Identifier | String) { return alias !== undefined ? export_specifier_({ ...config, alias: alias }) : fields.alias; },
    name(name?: Identifier | String) { return name !== undefined ? export_specifier_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_statement_(
  config?: ExportStatementConfig,
) {
  const fields = {
    declaration: config?.declaration,
    decorator: config?.decorator,
    source: config?.source,
    value: config?.value,
  };
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    fields,
    children,
    declaration(declaration?: Declaration) { return declaration !== undefined ? export_statement_({ ...config, declaration: declaration }) : fields.declaration; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? export_statement_({ ...config, decorator: decorator }) : fields.decorator; },
    source(source?: String) { return source !== undefined ? export_statement_({ ...config, source: source }) : fields.source; },
    value(value?: Expression) { return value !== undefined ? export_statement_({ ...config, value: value }) : fields.value; },
    child(child?: ExportClause | Expression | Identifier | NamespaceExport) { return child !== undefined ? export_statement_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expression_statement_(
  config: ExpressionStatementConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    child(child?: Expression | SequenceExpression) { return child !== undefined ? expression_statement_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extends_clause_(
  config: ExtendsClauseConfig,
) {
  const fields = {
    type_arguments: config.typeArguments,
    value: config.value,
  };
  return {
    type: 'extends_clause' as const,
    named: true as const,
    fields,
    typeArguments(...typeArguments: (TypeArguments)[]) { return typeArguments.length ? extends_clause_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    value(...value: (Expression)[]) { return value.length ? extends_clause_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extends_type_clause_(
  config: ExtendsTypeClauseConfig,
) {
  const fields = {
    type: config.type,
  };
  return {
    type: 'extends_type_clause' as const,
    named: true as const,
    fields,
    typeField(...type_: (GenericType | NestedTypeIdentifier | TypeIdentifier)[]) { return type_.length ? extends_type_clause_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function finally_clause_(
  config: FinallyClauseConfig,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? finally_clause_({ ...config, body: body }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function flow_maybe_type_(
  config: FlowMaybeTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'flow_maybe_type' as const,
    named: true as const,
    children,
    child(child?: PrimaryType) { return child !== undefined ? flow_maybe_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_in_statement_(
  config: ForInStatementConfig,
) {
  const fields = {
    body: config.body,
    kind: config.kind,
    left: config.left,
    operator: config.operator,
    right: config.right,
    value: config.value,
  };
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? for_in_statement_({ ...config, body: body }) : fields.body; },
    kind(kind?: 'var' | 'let' | 'const') { return kind !== undefined ? for_in_statement_({ ...config, kind: kind }) : fields.kind; },
    left(left?: ArrayPattern | Identifier | MemberExpression | NonNullExpression | ObjectPattern | ParenthesizedExpression | SubscriptExpression | Undefined) { return left !== undefined ? for_in_statement_({ ...config, left: left }) : fields.left; },
    operator(operator?: 'in' | 'of') { return operator !== undefined ? for_in_statement_({ ...config, operator: operator }) : fields.operator; },
    right(right?: Expression | SequenceExpression) { return right !== undefined ? for_in_statement_({ ...config, right: right }) : fields.right; },
    value(value?: Expression) { return value !== undefined ? for_in_statement_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_statement_(
  config: ForStatementConfig,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
    increment: config.increment,
    initializer: config.initializer,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? for_statement_({ ...config, body: body }) : fields.body; },
    condition(...condition: (EmptyStatement | Expression | SequenceExpression)[]) { return condition.length ? for_statement_({ ...config, condition: condition }) : fields.condition; },
    increment(increment?: Expression | SequenceExpression) { return increment !== undefined ? for_statement_({ ...config, increment: increment }) : fields.increment; },
    initializer(initializer?: EmptyStatement | Expression | LexicalDeclaration | SequenceExpression | VariableDeclaration) { return initializer !== undefined ? for_statement_({ ...config, initializer: initializer }) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function formal_parameters_(
  config?: FormalParametersConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'formal_parameters' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (OptionalParameter | RequiredParameter)[]) { return formal_parameters_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_declaration_(
  config: FunctionDeclarationConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_declaration' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? function_declaration_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? function_declaration_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? function_declaration_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? function_declaration_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_expression_(
  config: FunctionExpressionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_expression' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? function_expression_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? function_expression_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? function_expression_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? function_expression_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_expression_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_signature_(
  config: FunctionSignatureConfig,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_signature' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? function_signature_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? function_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? function_signature_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_type_(
  config: FunctionTypeConfig,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? function_type_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: Asserts | Type | TypePredicate) { return returnType !== undefined ? function_type_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_type_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generator_function_(
  config: GeneratorFunctionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'generator_function' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? generator_function_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? generator_function_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? generator_function_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? generator_function_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? generator_function_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generator_function_declaration_(
  config: GeneratorFunctionDeclarationConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'generator_function_declaration' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? generator_function_declaration_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? generator_function_declaration_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? generator_function_declaration_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? generator_function_declaration_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? generator_function_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_(
  config: GenericTypeConfig,
) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    name(name?: NestedTypeIdentifier | TypeIdentifier) { return name !== undefined ? generic_type_({ ...config, name: name }) : fields.name; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? generic_type_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_statement_(
  config: IfStatementConfig,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    alternative(alternative?: ElseClause) { return alternative !== undefined ? if_statement_({ ...config, alternative: alternative }) : fields.alternative; },
    condition(condition?: ParenthesizedExpression) { return condition !== undefined ? if_statement_({ ...config, condition: condition }) : fields.condition; },
    consequence(consequence?: Statement) { return consequence !== undefined ? if_statement_({ ...config, consequence: consequence }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function implements_clause_(
  config: ImplementsClauseConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.childType1 ? [config.childType1] : []), ...(config.childType2 ?? [])];
  return {
    type: 'implements_clause' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Type) { return NEEDS_NAME_0 !== undefined ? implements_clause_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (Type)[]) { return NEEDS_NAME_1.length ? implements_clause_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    childType1(childType1?: Type) { return childType1 !== undefined ? implements_clause_({ ...config, childType1 }) : config?.childType1; },
    childType2(...childType2: (Type)[]) { return childType2.length ? implements_clause_({ ...config, childType2 }) : config?.childType2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_alias_(
  config: ImportAliasConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.identifier ? [config.identifier] : []), ...(config.identifierOrNestedIdentifier ? [config.identifierOrNestedIdentifier] : [])];
  return {
    type: 'import_alias' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Identifier) { return NEEDS_NAME_0 !== undefined ? import_alias_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(NEEDS_NAME_1?: Identifier | NestedIdentifier) { return NEEDS_NAME_1 !== undefined ? import_alias_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    identifier(identifier?: Identifier) { return identifier !== undefined ? import_alias_({ ...config, identifier }) : config?.identifier; },
    identifierOrNestedIdentifier(identifierOrNestedIdentifier?: Identifier | NestedIdentifier) { return identifierOrNestedIdentifier !== undefined ? import_alias_({ ...config, identifierOrNestedIdentifier }) : config?.identifierOrNestedIdentifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_attribute_(
  config: ImportAttributeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'import_attribute' as const,
    named: true as const,
    children,
    child(child?: Object) { return child !== undefined ? import_attribute_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_clause_(
  config: ImportClauseConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.children1 ? [config.children1] : []), ...(config.namedImportsOrNamespaceImport ? [config.namedImportsOrNamespaceImport] : [])];
  return {
    type: 'import_clause' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Identifier | NamedImports | NamespaceImport) { return NEEDS_NAME_0 !== undefined ? import_clause_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(NEEDS_NAME_1?: NamedImports | NamespaceImport) { return NEEDS_NAME_1 !== undefined ? import_clause_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    children1(children1?: Identifier | NamedImports | NamespaceImport) { return children1 !== undefined ? import_clause_({ ...config, children1 }) : config?.children1; },
    namedImportsOrNamespaceImport(namedImportsOrNamespaceImport?: NamedImports | NamespaceImport) { return namedImportsOrNamespaceImport !== undefined ? import_clause_({ ...config, namedImportsOrNamespaceImport }) : config?.namedImportsOrNamespaceImport; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_require_clause_(
  config: ImportRequireClauseConfig,
) {
  const fields = {
    source: config.source,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'import_require_clause' as const,
    named: true as const,
    fields,
    children,
    source(source?: String) { return source !== undefined ? import_require_clause_({ ...config, source: source }) : fields.source; },
    child(child?: Identifier) { return child !== undefined ? import_require_clause_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_specifier_(
  config: ImportSpecifierConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    fields,
    alias(alias?: Identifier) { return alias !== undefined ? import_specifier_({ ...config, alias: alias }) : fields.alias; },
    name(name?: Identifier | String) { return name !== undefined ? import_specifier_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_statement_(
  config?: ImportStatementConfig,
) {
  const fields = {
    source: config?.source,
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    import_attribute: config?.importAttribute,
  };
  const children = [...(config?.importClauseOrImportRequireClause ? [config?.importClauseOrImportRequireClause] : []), ...(config?.importAttribute ? [config?.importAttribute] : [])];
  return {
    type: 'import_statement' as const,
    named: true as const,
    fields,
    children,
    source(source?: String) { return source !== undefined ? import_statement_({ ...config, source: source }) : fields.source; },
    NEEDS_NAME_0(NEEDS_NAME_0?: ImportClause | ImportRequireClause) { return NEEDS_NAME_0 !== undefined ? import_statement_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    importAttribute(importAttribute?: ImportAttribute) { return importAttribute !== undefined ? import_statement_({ ...config, importAttribute: importAttribute }) : fields.import_attribute; },
    importClauseOrImportRequireClause(importClauseOrImportRequireClause?: ImportClause | ImportRequireClause) { return importClauseOrImportRequireClause !== undefined ? import_statement_({ ...config, importClauseOrImportRequireClause }) : config?.importClauseOrImportRequireClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function index_signature_(
  config: IndexSignatureConfig,
) {
  const fields = {
    index_type: config.indexType,
    name: config.name,
    sign: config.sign,
    type: config.type,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'index_signature' as const,
    named: true as const,
    fields,
    children,
    indexType(indexType?: Type) { return indexType !== undefined ? index_signature_({ ...config, indexType: indexType }) : fields.index_type; },
    name(name?: Identifier) { return name !== undefined ? index_signature_({ ...config, name: name }) : fields.name; },
    sign(sign?: '-' | '+') { return sign !== undefined ? index_signature_({ ...config, sign: sign }) : fields.sign; },
    typeField(type_?: AddingTypeAnnotation | OmittingTypeAnnotation | OptingTypeAnnotation | TypeAnnotation) { return type_ !== undefined ? index_signature_({ ...config, type: type_ }) : fields.type; },
    child(child?: MappedTypeClause) { return child !== undefined ? index_signature_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function index_type_query_(
  config: IndexTypeQueryConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'index_type_query' as const,
    named: true as const,
    children,
    child(child?: PrimaryType) { return child !== undefined ? index_type_query_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function infer_type_(
  config: InferTypeConfig,
) {
  const fields = {
    type_identifier: config.typeIdentifier,
    type: config.type,
  };
  const children = [...(config.typeIdentifier ? [config.typeIdentifier] : []), ...(config.childType ? [config.childType] : [])];
  return {
    type: 'infer_type' as const,
    named: true as const,
    fields,
    children,
    typeIdentifier(typeIdentifier?: TypeIdentifier) { return typeIdentifier !== undefined ? infer_type_({ ...config, typeIdentifier: typeIdentifier }) : fields.type_identifier; },
    typeField(type_?: Type) { return type_ !== undefined ? infer_type_({ ...config, type: type_ }) : fields.type; },
    childType(childType?: Type) { return childType !== undefined ? infer_type_({ ...config, childType }) : config?.childType; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function instantiation_expression_(
  config: InstantiationExpressionConfig,
) {
  const fields = {
    function: config.function,
    type_arguments: config.typeArguments,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'instantiation_expression' as const,
    named: true as const,
    fields,
    children,
    function(function_?: Identifier | Import | MemberExpression | SubscriptExpression) { return function_ !== undefined ? instantiation_expression_({ ...config, function: function_ }) : fields.function; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? instantiation_expression_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    child(child?: Expression) { return child !== undefined ? instantiation_expression_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interface_body_(
  config?: InterfaceBodyConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'interface_body' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]) { return interface_body_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interface_declaration_(
  config: InterfaceDeclarationConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'interface_declaration' as const,
    named: true as const,
    fields,
    children,
    body(body?: InterfaceBody) { return body !== undefined ? interface_declaration_({ ...config, body: body }) : fields.body; },
    name(name?: TypeIdentifier) { return name !== undefined ? interface_declaration_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? interface_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    child(child?: ExtendsTypeClause) { return child !== undefined ? interface_declaration_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function internal_module_(
  config: InternalModuleConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'internal_module' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? internal_module_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier | NestedIdentifier | String) { return name !== undefined ? internal_module_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function intersection_type_(
  config: IntersectionTypeConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.childType1 ? [config.childType1] : []), ...(config.childType2 ? [config.childType2] : [])];
  return {
    type: 'intersection_type' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Type) { return NEEDS_NAME_0 !== undefined ? intersection_type_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(NEEDS_NAME_1?: Type) { return NEEDS_NAME_1 !== undefined ? intersection_type_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    childType1(childType1?: Type) { return childType1 !== undefined ? intersection_type_({ ...config, childType1 }) : config?.childType1; },
    childType2(childType2?: Type) { return childType2 !== undefined ? intersection_type_({ ...config, childType2 }) : config?.childType2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function labeled_statement_(
  config: LabeledStatementConfig,
) {
  const fields = {
    body: config.body,
    label: config.label,
  };
  return {
    type: 'labeled_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? labeled_statement_({ ...config, body: body }) : fields.body; },
    label(label?: StatementIdentifier) { return label !== undefined ? labeled_statement_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lexical_declaration_(
  config: LexicalDeclarationConfig,
) {
  const fields = {
    kind: config.kind,
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.variableDeclarator1 ? [config.variableDeclarator1] : []), ...(config.variableDeclarator2 ?? [])];
  return {
    type: 'lexical_declaration' as const,
    named: true as const,
    fields,
    children,
    kind(kind?: 'let' | 'const') { return kind !== undefined ? lexical_declaration_({ ...config, kind: kind }) : fields.kind; },
    NEEDS_NAME_0(NEEDS_NAME_0?: VariableDeclarator) { return NEEDS_NAME_0 !== undefined ? lexical_declaration_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (VariableDeclarator)[]) { return NEEDS_NAME_1.length ? lexical_declaration_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    variableDeclarator1(variableDeclarator1?: VariableDeclarator) { return variableDeclarator1 !== undefined ? lexical_declaration_({ ...config, variableDeclarator1 }) : config?.variableDeclarator1; },
    variableDeclarator2(...variableDeclarator2: (VariableDeclarator)[]) { return variableDeclarator2.length ? lexical_declaration_({ ...config, variableDeclarator2 }) : config?.variableDeclarator2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function literal_type_(
  config: LiteralTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    child(child?: False | Null | Number | String | True | UnaryExpression | Undefined) { return child !== undefined ? literal_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lookup_type_(
  config: LookupTypeConfig,
) {
  const fields = {
    primary_type: config.primaryType,
    type: config.type,
  };
  const children = [...(config.primaryType ? [config.primaryType] : []), ...(config.childType ? [config.childType] : [])];
  return {
    type: 'lookup_type' as const,
    named: true as const,
    fields,
    children,
    primaryType(primaryType?: PrimaryType) { return primaryType !== undefined ? lookup_type_({ ...config, primaryType: primaryType }) : fields.primary_type; },
    typeField(type_?: Type) { return type_ !== undefined ? lookup_type_({ ...config, type: type_ }) : fields.type; },
    childType(childType?: Type) { return childType !== undefined ? lookup_type_({ ...config, childType }) : config?.childType; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function mapped_type_clause_(
  config: MappedTypeClauseConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
    type: config.type,
  };
  return {
    type: 'mapped_type_clause' as const,
    named: true as const,
    fields,
    alias(alias?: Type) { return alias !== undefined ? mapped_type_clause_({ ...config, alias: alias }) : fields.alias; },
    name(name?: TypeIdentifier) { return name !== undefined ? mapped_type_clause_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? mapped_type_clause_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function member_expression_(
  config: MemberExpressionConfig,
) {
  const fields = {
    object: config.object,
    optional_chain: config.optionalChain,
    property: config.property,
  };
  return {
    type: 'member_expression' as const,
    named: true as const,
    fields,
    object(object?: Expression | Import | PrimaryExpression) { return object !== undefined ? member_expression_({ ...config, object: object }) : fields.object; },
    optionalChain(optionalChain?: OptionalChain) { return optionalChain !== undefined ? member_expression_({ ...config, optionalChain: optionalChain }) : fields.optional_chain; },
    property(property?: PrivatePropertyIdentifier | PropertyIdentifier) { return property !== undefined ? member_expression_({ ...config, property: property }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function method_definition_(
  config: MethodDefinitionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  const children = [...(config.accessibilityModifier ? [config.accessibilityModifier] : []), ...(config.overrideModifier ? [config.overrideModifier] : [])];
  return {
    type: 'method_definition' as const,
    named: true as const,
    fields,
    children,
    body(body?: StatementBlock) { return body !== undefined ? method_definition_({ ...config, body: body }) : fields.body; },
    name(name?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String) { return name !== undefined ? method_definition_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? method_definition_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? method_definition_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? method_definition_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? method_definition_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? method_definition_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function method_signature_(
  config: MethodSignatureConfig,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  const children = [...(config.accessibilityModifier ? [config.accessibilityModifier] : []), ...(config.overrideModifier ? [config.overrideModifier] : [])];
  return {
    type: 'method_signature' as const,
    named: true as const,
    fields,
    children,
    name(name?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String) { return name !== undefined ? method_signature_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? method_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? method_signature_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? method_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? method_signature_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? method_signature_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function module_(
  config: ModuleConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'module' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? module_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier | NestedIdentifier | String) { return name !== undefined ? module_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function named_imports_(
  config?: NamedImportsConfig,
) {
  const fields = {
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.importSpecifier1 ? [config?.importSpecifier1] : []), ...(config?.importSpecifier2 ?? [])];
  return {
    type: 'named_imports' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: ImportSpecifier) { return NEEDS_NAME_0 !== undefined ? named_imports_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (ImportSpecifier)[]) { return NEEDS_NAME_1.length ? named_imports_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    importSpecifier1(importSpecifier1?: ImportSpecifier) { return importSpecifier1 !== undefined ? named_imports_({ ...config, importSpecifier1 }) : config?.importSpecifier1; },
    importSpecifier2(...importSpecifier2: (ImportSpecifier)[]) { return importSpecifier2.length ? named_imports_({ ...config, importSpecifier2 }) : config?.importSpecifier2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function namespace_export_(
  config: NamespaceExportConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'namespace_export' as const,
    named: true as const,
    children,
    child(child?: Identifier | String) { return child !== undefined ? namespace_export_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function namespace_import_(
  config: NamespaceImportConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'namespace_import' as const,
    named: true as const,
    children,
    child(child?: Identifier) { return child !== undefined ? namespace_import_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nested_identifier_(
  config: NestedIdentifierConfig,
) {
  const fields = {
    object: config.object,
    property: config.property,
  };
  return {
    type: 'nested_identifier' as const,
    named: true as const,
    fields,
    object(object?: Identifier | MemberExpression) { return object !== undefined ? nested_identifier_({ ...config, object: object }) : fields.object; },
    property(property?: PropertyIdentifier) { return property !== undefined ? nested_identifier_({ ...config, property: property }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nested_type_identifier_(
  config: NestedTypeIdentifierConfig,
) {
  const fields = {
    module: config.module,
    name: config.name,
  };
  return {
    type: 'nested_type_identifier' as const,
    named: true as const,
    fields,
    module(module?: Identifier | NestedIdentifier) { return module !== undefined ? nested_type_identifier_({ ...config, module: module }) : fields.module; },
    name(name?: TypeIdentifier) { return name !== undefined ? nested_type_identifier_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function new_expression_(
  config: NewExpressionConfig,
) {
  const fields = {
    arguments: config.arguments,
    constructor: config.constructor,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'new_expression' as const,
    named: true as const,
    fields,
    arguments(arguments_?: Arguments) { return arguments_ !== undefined ? new_expression_({ ...config, arguments: arguments_ }) : fields.arguments; },
    constructor(constructor?: PrimaryExpression) { return constructor !== undefined ? new_expression_({ ...config, constructor: constructor }) : fields.constructor; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? new_expression_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function non_null_expression_(
  config: NonNullExpressionConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'non_null_expression' as const,
    named: true as const,
    children,
    child(child?: Expression) { return child !== undefined ? non_null_expression_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_(
  config?: ObjectConfig,
) {
  const fields = {
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.children1 ? [config?.children1] : []), ...(config?.children2 ?? [])];
  return {
    type: 'object' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement) { return NEEDS_NAME_0 !== undefined ? object_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement)[]) { return NEEDS_NAME_1.length ? object_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    children1(children1?: MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement) { return children1 !== undefined ? object_({ ...config, children1 }) : config?.children1; },
    children2(...children2: (MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement)[]) { return children2.length ? object_({ ...config, children2 }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_assignment_pattern_(
  config: ObjectAssignmentPatternConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'object_assignment_pattern' as const,
    named: true as const,
    fields,
    left(left?: ArrayPattern | ObjectPattern | ShorthandPropertyIdentifierPattern) { return left !== undefined ? object_assignment_pattern_({ ...config, left: left }) : fields.left; },
    right(right?: Expression) { return right !== undefined ? object_assignment_pattern_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_pattern_(
  config?: ObjectPatternConfig,
) {
  const fields = {
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.children1 ? [config?.children1] : []), ...(config?.children2 ?? [])];
  return {
    type: 'object_pattern' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern) { return NEEDS_NAME_0 !== undefined ? object_pattern_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern)[]) { return NEEDS_NAME_1.length ? object_pattern_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    children1(children1?: ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern) { return children1 !== undefined ? object_pattern_({ ...config, children1 }) : config?.children1; },
    children2(...children2: (ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern)[]) { return children2.length ? object_pattern_({ ...config, children2 }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_type_(
  config?: ObjectTypeConfig,
) {
  const fields = {
    NEEDS_NAME_0: config?.NEEDS_NAME_0,
    NEEDS_NAME_1: config?.NEEDS_NAME_1,
  };
  const children = [...(config?.children1 ? [config?.children1] : []), ...(config?.children2 ?? [])];
  return {
    type: 'object_type' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature) { return NEEDS_NAME_0 !== undefined ? object_type_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]) { return NEEDS_NAME_1.length ? object_type_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    children1(children1?: CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature) { return children1 !== undefined ? object_type_({ ...config, children1 }) : config?.children1; },
    children2(...children2: (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]) { return children2.length ? object_type_({ ...config, children2 }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function omitting_type_annotation_(
  config: OmittingTypeAnnotationConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'omitting_type_annotation' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? omitting_type_annotation_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function opting_type_annotation_(
  config: OptingTypeAnnotationConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'opting_type_annotation' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? opting_type_annotation_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function optional_parameter_(
  config?: OptionalParameterConfig,
) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    pattern: config?.pattern,
    type: config?.type,
    value: config?.value,
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
  };
  const children = [...(config?.accessibilityModifier ? [config?.accessibilityModifier] : []), ...(config?.overrideModifier ? [config?.overrideModifier] : [])];
  return {
    type: 'optional_parameter' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? optional_parameter_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: Identifier) { return name !== undefined ? optional_parameter_({ ...config, name: name }) : fields.name; },
    pattern(pattern?: Pattern | This) { return pattern !== undefined ? optional_parameter_({ ...config, pattern: pattern }) : fields.pattern; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? optional_parameter_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? optional_parameter_({ ...config, value: value }) : fields.value; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? optional_parameter_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? optional_parameter_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function optional_type_(
  config: OptionalTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'optional_type' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? optional_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair_(
  config: PairConfig,
) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String) { return key !== undefined ? pair_({ ...config, key: key }) : fields.key; },
    value(value?: Expression) { return value !== undefined ? pair_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair_pattern_(
  config: PairPatternConfig,
) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    type: 'pair_pattern' as const,
    named: true as const,
    fields,
    key(key?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String) { return key !== undefined ? pair_pattern_({ ...config, key: key }) : fields.key; },
    value(value?: AssignmentPattern | Pattern) { return value !== undefined ? pair_pattern_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_expression_(
  config: ParenthesizedExpressionConfig,
) {
  const fields = {
    type: config.type,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    fields,
    children,
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? parenthesized_expression_({ ...config, type: type_ }) : fields.type; },
    child(child?: CallExpression | Expression | Identifier | MemberExpression | SequenceExpression) { return child !== undefined ? parenthesized_expression_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_type_(
  config: ParenthesizedTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'parenthesized_type' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? parenthesized_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function program_(
  config?: ProgramConfig,
) {
  const fields = {
    hash_bang_line: config?.hashBangLine,
    statement: config?.statement,
  };
  const children = [...(config?.hashBangLine ? [config?.hashBangLine] : []), ...(config?.statement ?? [])];
  return {
    type: 'program' as const,
    named: true as const,
    fields,
    children,
    hashBangLine(hashBangLine?: HashBangLine) { return hashBangLine !== undefined ? program_({ ...config, hashBangLine: hashBangLine }) : fields.hash_bang_line; },
    statement(...statement: (Statement)[]) { return statement.length ? program_({ ...config, statement: statement }) : fields.statement; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function property_signature_(
  config: PropertySignatureConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  const children = [...(config.accessibilityModifier ? [config.accessibilityModifier] : []), ...(config.overrideModifier ? [config.overrideModifier] : [])];
  return {
    type: 'property_signature' as const,
    named: true as const,
    fields,
    children,
    name(name?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String) { return name !== undefined ? property_signature_({ ...config, name: name }) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? property_signature_({ ...config, type: type_ }) : fields.type; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? property_signature_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? property_signature_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function public_field_definition_(
  config: PublicFieldDefinitionConfig,
) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type: config.type,
    value: config.value,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  const children = [...(config.accessibilityModifier ? [config.accessibilityModifier] : []), ...(config.overrideModifier ? [config.overrideModifier] : [])];
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? public_field_definition_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: ComputedPropertyName | Number | PrivatePropertyIdentifier | PropertyIdentifier | String) { return name !== undefined ? public_field_definition_({ ...config, name: name }) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? public_field_definition_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? public_field_definition_({ ...config, value: value }) : fields.value; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? public_field_definition_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? public_field_definition_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function readonly_type_(
  config: ReadonlyTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'readonly_type' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? readonly_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function regex_(
  config: RegexConfig,
) {
  const fields = {
    flags: config.flags,
    pattern: config.pattern,
  };
  return {
    type: 'regex' as const,
    named: true as const,
    fields,
    flags(flags?: RegexFlags) { return flags !== undefined ? regex_({ ...config, flags: flags }) : fields.flags; },
    pattern(pattern?: RegexPattern) { return pattern !== undefined ? regex_({ ...config, pattern: pattern }) : fields.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function required_parameter_(
  config?: RequiredParameterConfig,
) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    pattern: config?.pattern,
    type: config?.type,
    value: config?.value,
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
  };
  const children = [...(config?.accessibilityModifier ? [config?.accessibilityModifier] : []), ...(config?.overrideModifier ? [config?.overrideModifier] : [])];
  return {
    type: 'required_parameter' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? required_parameter_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: Identifier | RestPattern) { return name !== undefined ? required_parameter_({ ...config, name: name }) : fields.name; },
    pattern(pattern?: Pattern | This) { return pattern !== undefined ? required_parameter_({ ...config, pattern: pattern }) : fields.pattern; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? required_parameter_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? required_parameter_({ ...config, value: value }) : fields.value; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? required_parameter_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? required_parameter_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function rest_pattern_(
  config: RestPatternConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'rest_pattern' as const,
    named: true as const,
    children,
    child(child?: ArrayPattern | Identifier | MemberExpression | NonNullExpression | ObjectPattern | SubscriptExpression | Undefined) { return child !== undefined ? rest_pattern_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function rest_type_(
  config: RestTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'rest_type' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? rest_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function return_statement_(
  config?: ReturnStatementConfig,
) {
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    child(child?: Expression | SequenceExpression) { return child !== undefined ? return_statement_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function satisfies_expression_(
  config: SatisfiesExpressionConfig,
) {
  const fields = {
    expression: config.expression,
    type: config.type,
  };
  const children = [...(config.expression ? [config.expression] : []), ...(config.childType ? [config.childType] : [])];
  return {
    type: 'satisfies_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression?: Expression) { return expression !== undefined ? satisfies_expression_({ ...config, expression: expression }) : fields.expression; },
    typeField(type_?: Type) { return type_ !== undefined ? satisfies_expression_({ ...config, type: type_ }) : fields.type; },
    childType(childType?: Type) { return childType !== undefined ? satisfies_expression_({ ...config, childType }) : config?.childType; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function sequence_expression_(
  config: SequenceExpressionConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.expression1 ? [config.expression1] : []), ...(config.expression2 ?? [])];
  return {
    type: 'sequence_expression' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Expression) { return NEEDS_NAME_0 !== undefined ? sequence_expression_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (Expression)[]) { return NEEDS_NAME_1.length ? sequence_expression_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    expression1(expression1?: Expression) { return expression1 !== undefined ? sequence_expression_({ ...config, expression1 }) : config?.expression1; },
    expression2(...expression2: (Expression)[]) { return expression2.length ? sequence_expression_({ ...config, expression2 }) : config?.expression2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function spread_element_(
  config: SpreadElementConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'spread_element' as const,
    named: true as const,
    children,
    child(child?: Expression) { return child !== undefined ? spread_element_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function statement_block_(
  config?: StatementBlockConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'statement_block' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Statement)[]) { return statement_block_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string_(
  config?: StringConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'string' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (EscapeSequence | StringFragment)[]) { return string_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function subscript_expression_(
  config: SubscriptExpressionConfig,
) {
  const fields = {
    index: config.index,
    object: config.object,
    optional_chain: config.optionalChain,
  };
  return {
    type: 'subscript_expression' as const,
    named: true as const,
    fields,
    index(index?: Expression | Number | PredefinedType | SequenceExpression | String) { return index !== undefined ? subscript_expression_({ ...config, index: index }) : fields.index; },
    object(object?: Expression | PrimaryExpression) { return object !== undefined ? subscript_expression_({ ...config, object: object }) : fields.object; },
    optionalChain(optionalChain?: OptionalChain) { return optionalChain !== undefined ? subscript_expression_({ ...config, optionalChain: optionalChain }) : fields.optional_chain; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_body_(
  config?: SwitchBodyConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'switch_body' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (SwitchCase | SwitchDefault)[]) { return switch_body_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_case_(
  config: SwitchCaseConfig,
) {
  const fields = {
    body: config.body,
    value: config.value,
  };
  return {
    type: 'switch_case' as const,
    named: true as const,
    fields,
    body(...body: (Statement)[]) { return body.length ? switch_case_({ ...config, body: body }) : fields.body; },
    value(value?: Expression | SequenceExpression) { return value !== undefined ? switch_case_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_default_(
  config?: SwitchDefaultConfig,
) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'switch_default' as const,
    named: true as const,
    fields,
    body(...body: (Statement)[]) { return body.length ? switch_default_({ ...config, body: body }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_statement_(
  config: SwitchStatementConfig,
) {
  const fields = {
    body: config.body,
    value: config.value,
  };
  return {
    type: 'switch_statement' as const,
    named: true as const,
    fields,
    body(body?: SwitchBody) { return body !== undefined ? switch_statement_({ ...config, body: body }) : fields.body; },
    value(value?: ParenthesizedExpression) { return value !== undefined ? switch_statement_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_literal_type_(
  config?: TemplateLiteralTypeConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'template_literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (StringFragment | TemplateType)[]) { return template_literal_type_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_string_(
  config?: TemplateStringConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'template_string' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (EscapeSequence | StringFragment | TemplateSubstitution)[]) { return template_string_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_substitution_(
  config: TemplateSubstitutionConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'template_substitution' as const,
    named: true as const,
    children,
    child(child?: Expression | SequenceExpression) { return child !== undefined ? template_substitution_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_type_(
  config: TemplateTypeConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'template_type' as const,
    named: true as const,
    children,
    child(child?: InferType | PrimaryType) { return child !== undefined ? template_type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ternary_expression_(
  config: TernaryExpressionConfig,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'ternary_expression' as const,
    named: true as const,
    fields,
    alternative(alternative?: Expression) { return alternative !== undefined ? ternary_expression_({ ...config, alternative: alternative }) : fields.alternative; },
    condition(condition?: Expression) { return condition !== undefined ? ternary_expression_({ ...config, condition: condition }) : fields.condition; },
    consequence(consequence?: Expression) { return consequence !== undefined ? ternary_expression_({ ...config, consequence: consequence }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function throw_statement_(
  config: ThrowStatementConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'throw_statement' as const,
    named: true as const,
    children,
    child(child?: Expression | SequenceExpression) { return child !== undefined ? throw_statement_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_statement_(
  config: TryStatementConfig,
) {
  const fields = {
    body: config.body,
    finalizer: config.finalizer,
    handler: config.handler,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? try_statement_({ ...config, body: body }) : fields.body; },
    finalizer(finalizer?: FinallyClause) { return finalizer !== undefined ? try_statement_({ ...config, finalizer: finalizer }) : fields.finalizer; },
    handler(handler?: CatchClause) { return handler !== undefined ? try_statement_({ ...config, handler: handler }) : fields.handler; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_type_(
  config?: TupleTypeConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (OptionalParameter | OptionalType | RequiredParameter | RestType | Type)[]) { return tuple_type_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_alias_declaration_(
  config: TypeAliasDeclarationConfig,
) {
  const fields = {
    name: config.name,
    type_parameters: config.typeParameters,
    value: config.value,
  };
  return {
    type: 'type_alias_declaration' as const,
    named: true as const,
    fields,
    name(name?: TypeIdentifier) { return name !== undefined ? type_alias_declaration_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? type_alias_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    value(value?: Type) { return value !== undefined ? type_alias_declaration_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_annotation_(
  config: TypeAnnotationConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'type_annotation' as const,
    named: true as const,
    children,
    child(child?: Type) { return child !== undefined ? type_annotation_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_arguments_(
  config: TypeArgumentsConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.childType1 ? [config.childType1] : []), ...(config.childType2 ?? [])];
  return {
    type: 'type_arguments' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Type) { return NEEDS_NAME_0 !== undefined ? type_arguments_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (Type)[]) { return NEEDS_NAME_1.length ? type_arguments_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    childType1(childType1?: Type) { return childType1 !== undefined ? type_arguments_({ ...config, childType1 }) : config?.childType1; },
    childType2(...childType2: (Type)[]) { return childType2.length ? type_arguments_({ ...config, childType2 }) : config?.childType2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_assertion_(
  config: TypeAssertionConfig,
) {
  const fields = {
    type_arguments: config.typeArguments,
    expression: config.expression,
  };
  const children = [...(config.typeArguments ? [config.typeArguments] : []), ...(config.expression ? [config.expression] : [])];
  return {
    type: 'type_assertion' as const,
    named: true as const,
    fields,
    children,
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? type_assertion_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    expression(expression?: Expression) { return expression !== undefined ? type_assertion_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameter_(
  config: TypeParameterConfig,
) {
  const fields = {
    constraint: config.constraint,
    name: config.name,
    value: config.value,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    constraint(constraint?: Constraint) { return constraint !== undefined ? type_parameter_({ ...config, constraint: constraint }) : fields.constraint; },
    name(name?: TypeIdentifier) { return name !== undefined ? type_parameter_({ ...config, name: name }) : fields.name; },
    value(value?: DefaultType) { return value !== undefined ? type_parameter_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameters_(
  config: TypeParametersConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.typeParameter1 ? [config.typeParameter1] : []), ...(config.typeParameter2 ?? [])];
  return {
    type: 'type_parameters' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: TypeParameter) { return NEEDS_NAME_0 !== undefined ? type_parameters_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (TypeParameter)[]) { return NEEDS_NAME_1.length ? type_parameters_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    typeParameter1(typeParameter1?: TypeParameter) { return typeParameter1 !== undefined ? type_parameters_({ ...config, typeParameter1 }) : config?.typeParameter1; },
    typeParameter2(...typeParameter2: (TypeParameter)[]) { return typeParameter2.length ? type_parameters_({ ...config, typeParameter2 }) : config?.typeParameter2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_predicate_(
  config: TypePredicateConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    type: 'type_predicate' as const,
    named: true as const,
    fields,
    name(name?: Identifier | This) { return name !== undefined ? type_predicate_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? type_predicate_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_predicate_annotation_(
  config: TypePredicateAnnotationConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'type_predicate_annotation' as const,
    named: true as const,
    children,
    child(child?: TypePredicate) { return child !== undefined ? type_predicate_annotation_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_query_(
  config: TypeQueryConfig,
) {
  const children = config.children ? [config.children] : [];
  return {
    type: 'type_query' as const,
    named: true as const,
    children,
    child(child?: CallExpression | Identifier | InstantiationExpression | MemberExpression | SubscriptExpression | This) { return child !== undefined ? type_query_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unary_expression_(
  config: UnaryExpressionConfig,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    argument(argument?: Expression | Number) { return argument !== undefined ? unary_expression_({ ...config, argument: argument }) : fields.argument; },
    operator(operator?: '!' | '~' | '-' | '+' | 'typeof' | 'void' | 'delete') { return operator !== undefined ? unary_expression_({ ...config, operator: operator }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function union_type_(
  config: UnionTypeConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.childType1 ? [config.childType1] : []), ...(config.childType2 ? [config.childType2] : [])];
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: Type) { return NEEDS_NAME_0 !== undefined ? union_type_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(NEEDS_NAME_1?: Type) { return NEEDS_NAME_1 !== undefined ? union_type_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    childType1(childType1?: Type) { return childType1 !== undefined ? union_type_({ ...config, childType1 }) : config?.childType1; },
    childType2(childType2?: Type) { return childType2 !== undefined ? union_type_({ ...config, childType2 }) : config?.childType2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function update_expression_(
  config: UpdateExpressionConfig,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'update_expression' as const,
    named: true as const,
    fields,
    argument(argument?: Expression) { return argument !== undefined ? update_expression_({ ...config, argument: argument }) : fields.argument; },
    operator(operator?: '++' | '--') { return operator !== undefined ? update_expression_({ ...config, operator: operator }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function variable_declaration_(
  config: VariableDeclarationConfig,
) {
  const fields = {
    NEEDS_NAME_0: config.NEEDS_NAME_0,
    NEEDS_NAME_1: config.NEEDS_NAME_1,
  };
  const children = [...(config.variableDeclarator1 ? [config.variableDeclarator1] : []), ...(config.variableDeclarator2 ?? [])];
  return {
    type: 'variable_declaration' as const,
    named: true as const,
    fields,
    children,
    NEEDS_NAME_0(NEEDS_NAME_0?: VariableDeclarator) { return NEEDS_NAME_0 !== undefined ? variable_declaration_({ ...config, NEEDS_NAME_0: NEEDS_NAME_0 }) : fields.NEEDS_NAME_0; },
    NEEDS_NAME_1(...NEEDS_NAME_1: (VariableDeclarator)[]) { return NEEDS_NAME_1.length ? variable_declaration_({ ...config, NEEDS_NAME_1: NEEDS_NAME_1 }) : fields.NEEDS_NAME_1; },
    variableDeclarator1(variableDeclarator1?: VariableDeclarator) { return variableDeclarator1 !== undefined ? variable_declaration_({ ...config, variableDeclarator1 }) : config?.variableDeclarator1; },
    variableDeclarator2(...variableDeclarator2: (VariableDeclarator)[]) { return variableDeclarator2.length ? variable_declaration_({ ...config, variableDeclarator2 }) : config?.variableDeclarator2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function variable_declarator_(
  config: VariableDeclaratorConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  return {
    type: 'variable_declarator' as const,
    named: true as const,
    fields,
    name(name?: ArrayPattern | Identifier | ObjectPattern) { return name !== undefined ? variable_declarator_({ ...config, name: name }) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? variable_declarator_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? variable_declarator_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function while_statement_(
  config: WhileStatementConfig,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? while_statement_({ ...config, body: body }) : fields.body; },
    condition(condition?: ParenthesizedExpression) { return condition !== undefined ? while_statement_({ ...config, condition: condition }) : fields.condition; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function with_statement_(
  config: WithStatementConfig,
) {
  const fields = {
    body: config.body,
    object: config.object,
  };
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? with_statement_({ ...config, body: body }) : fields.body; },
    object(object?: ParenthesizedExpression) { return object !== undefined ? with_statement_({ ...config, object: object }) : fields.object; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function yield_expression_(
  config?: YieldExpressionConfig,
) {
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'yield_expression' as const,
    named: true as const,
    children,
    child(child?: Expression) { return child !== undefined ? yield_expression_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function accessibility_modifier_(text: string) {
  return {
    type: 'accessibility_modifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: AccessibilityModifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function debugger_statement_(text: string) {
  return {
    type: 'debugger_statement' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: DebuggerStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function empty_statement_() {
  return {
    type: 'empty_statement' as const,
    named: true as const,
    text: ';',
    render: () => ';',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: ';' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: ';' };
    },
    replace: (target: EmptyStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: ';' }; },
  };
}

export function existential_type_() {
  return {
    type: 'existential_type' as const,
    named: true as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: ExistentialTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function import_() {
  return {
    type: 'import' as const,
    named: true as const,
    text: 'import',
    render: () => 'import',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'import' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'import' };
    },
    replace: (target: ImportTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' }; },
  };
}

export function meta_property_(text: string) {
  if (!/^(?:new\.target|import\.meta)$/.test(text)) throw new Error(`Invalid meta_property: '${text}' does not match grammar pattern`);
  return {
    type: 'meta_property' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: MetaPropertyTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function optional_chain_() {
  return {
    type: 'optional_chain' as const,
    named: true as const,
    text: '?.',
    render: () => '?.',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '?.' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '?.' };
    },
    replace: (target: OptionalChainTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '?.' }; },
  };
}

export function override_modifier_() {
  return {
    type: 'override_modifier' as const,
    named: true as const,
    text: 'override',
    render: () => 'override',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'override' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'override' };
    },
    replace: (target: OverrideModifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' }; },
  };
}

export function predefined_type_(text: string) {
  return {
    type: 'predefined_type' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PredefinedTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment_(text: string) {
  return {
    type: 'comment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: CommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escape_sequence_(text: string) {
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeSequenceTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'false',
    render: () => 'false',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'false' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'false' };
    },
    replace: (target: FalseTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' }; },
  };
}

export function hash_bang_line_(text: string) {
  if (!/^#!.*$/.test(text)) throw new Error(`Invalid hash_bang_line: '${text}' does not match grammar pattern`);
  return {
    type: 'hash_bang_line' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: HashBangLineTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function html_comment_(text: string) {
  return {
    type: 'html_comment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: HtmlCommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function null_() {
  return {
    type: 'null' as const,
    named: true as const,
    text: 'null',
    render: () => 'null',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'null' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'null' };
    },
    replace: (target: NullTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' }; },
  };
}

export function number_(text: string) {
  if (!/^(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:(?:0|0?[1-9]_?\d(_?\d)*?)\.\d(_?\d)*?(?:e|E)(?:-|\+)?\d(_?\d)*?|\.\d(_?\d)*(?:e|E)(?:-|\+)?\d(_?\d)*?|(?:0|0?[1-9]_?\d(_?\d)*?)(?:e|E)(?:-|\+)?\d(_?\d)*|\d(_?\d)*)|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|\d(_?\d)*)n)$/.test(text)) throw new Error(`Invalid number: '${text}' does not match grammar pattern`);
  return {
    type: 'number' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: NumberTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function private_property_identifier_(text: string) {
  return {
    type: 'private_property_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PrivatePropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function property_identifier_(text: string) {
  return {
    type: 'property_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regex_flags_(text: string) {
  if (!/^[a-z]+$/.test(text)) throw new Error(`Invalid regex_flags: '${text}' does not match grammar pattern`);
  return {
    type: 'regex_flags' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: RegexFlagsTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regex_pattern_(text: string) {
  if (!/^(?:(?:\[(?:(?:\\.|[^\]\n\\]))*\]|\\.|[^/\\\[\n]))+$/.test(text)) throw new Error(`Invalid regex_pattern: '${text}' does not match grammar pattern`);
  return {
    type: 'regex_pattern' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: RegexPatternTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthand_property_identifier_(text: string) {
  return {
    type: 'shorthand_property_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShorthandPropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthand_property_identifier_pattern_(text: string) {
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShorthandPropertyIdentifierPatternTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function statement_identifier_(text: string) {
  return {
    type: 'statement_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StatementIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function string_fragment_(text: string) {
  return {
    type: 'string_fragment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringFragmentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super',
    render: () => 'super',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'super' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'super' };
    },
    replace: (target: SuperTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; },
  };
}

export function this_() {
  return {
    type: 'this' as const,
    named: true as const,
    text: 'this',
    render: () => 'this',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'this' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'this' };
    },
    replace: (target: ThisTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' }; },
  };
}

export function this_type_(text: string) {
  return {
    type: 'this_type' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ThisTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'true',
    render: () => 'true',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'true' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'true' };
    },
    replace: (target: TrueTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' }; },
  };
}

export function type_identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid type_identifier`);
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: TypeIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function undefined_() {
  return {
    type: 'undefined' as const,
    named: true as const,
    text: 'undefined',
    render: () => 'undefined',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'undefined' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'undefined' };
    },
    replace: (target: UndefinedTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' }; },
  };
}


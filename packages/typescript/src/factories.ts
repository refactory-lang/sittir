// Auto-generated by @sittir/codegen — do not edit

import type * as T from './types.js';
import type { ByteRange, ConfigOf, FluentNode, NonEmptyArray } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const _leafRe_hashBangLine = /^(?:#!.*)/u;
const _leafRe_htmlCharacterReference = /^(?:&(#([xX][0-9a-fA-F]{1,6}|[0-9]{1,5})|[A-Za-z]{1,30});)/u;
const _leafRe_jsxIdentifier = /^(?:[a-zA-Z_$][a-zA-Z\d_$]*-[a-zA-Z\d_$-]*)/u;
const _leafRe_unescapedDoubleJsxStringFragment = /^(?:([^"&]|&[^#A-Za-z])+)/u;
const _leafRe_unescapedSingleJsxStringFragment = /^(?:([^'&]|&[^#A-Za-z])+)/u;
const _leafRe_unescapedDoubleStringFragment = /^(?:[^"\\\r\n]+)/u;
const _leafRe_unescapedSingleStringFragment = /^(?:[^'\\\r\n]+)/u;
const _leafRe_regexFlags = /^(?:[a-z]+)/u;

export function program(config: T.Program.Config) {
  const fields = {
    hash_bang_line: config.hashBangLine,
    statements: config.statements,
  };
  return {
    $type: 'program' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    hashBangLine(value?: T.HashBangLine | undefined) { return _fs(config, program, 'hashBangLine', value, fields.hash_bang_line); },
    statements(...values: T.Statement[]) { return _fsm(config, program, 'statements', values, fields.statements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hashBangLine(text: string) {
  if (text.length === 0) throw new Error(`hash_bang_line: text must be non-empty`); if (!_leafRe_hashBangLine.test(text)) throw new Error(`hash_bang_line: text does not match pattern: ${text}`);
  return {
    $type: 'hash_bang_line' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HashBangLineTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function exportStatement(config: ConfigOf<T.ExportStatementUFormDefault>): ReturnType<typeof exportStatementUFormDefault>;
export function exportStatement(config: ConfigOf<T.ExportStatementUFormTypeExport>): ReturnType<typeof exportStatementUFormTypeExport>;
export function exportStatement(config: ConfigOf<T.ExportStatementUFormEqualsExport>): ReturnType<typeof exportStatementUFormEqualsExport>;
export function exportStatement(config: ConfigOf<T.ExportStatementUFormNamespaceExport>): ReturnType<typeof exportStatementUFormNamespaceExport>;
export function exportStatement(config: ConfigOf<T.ExportStatementUFormDefault> | ConfigOf<T.ExportStatementUFormTypeExport> | ConfigOf<T.ExportStatementUFormEqualsExport> | ConfigOf<T.ExportStatementUFormNamespaceExport>) {
  switch (config.$variant) {
    case 'default': return exportStatementUFormDefault(config as Parameters<typeof exportStatementUFormDefault>[0]);
    case 'type_export': return exportStatementUFormTypeExport(config as Parameters<typeof exportStatementUFormTypeExport>[0]);
    case 'equals_export': return exportStatementUFormEqualsExport(config as Parameters<typeof exportStatementUFormEqualsExport>[0]);
    case 'namespace_export': return exportStatementUFormNamespaceExport(config as Parameters<typeof exportStatementUFormNamespaceExport>[0]);
  }
  throw new Error(`exportStatement: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'default' | 'type_export' | 'equals_export' | 'namespace_export'.`);
}
export function exportStatementUFormDefault(config: Omit<ConfigOf<T.ExportStatementUFormDefault>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'export_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'default' as const,
    $children: children,
    child(value?: T.ExportStatementDefault) {
      if (value === undefined) return children[0];
      return exportStatementUFormDefault({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementUFormDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementUFormTypeExport(config?: Omit<ConfigOf<T.ExportStatementUFormTypeExport>, '$variant'>) {
  const inner = exportStatementTypeExport(config);
  const children = [inner] as const;
  return {
    $type: 'export_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'type_export' as const,
    $children: children,
    source(value?: T.String | undefined) {
      if (value === undefined) return inner.$fields.source;
      return exportStatementUFormTypeExport({ source: value });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementUFormTypeExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementUFormEqualsExport(config: Omit<ConfigOf<T.ExportStatementUFormEqualsExport>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'export_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'equals_export' as const,
    $children: children,
    child(value?: T.ExportStatementEqualsExport) {
      if (value === undefined) return children[0];
      return exportStatementUFormEqualsExport({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementUFormEqualsExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementUFormNamespaceExport(config: Omit<ConfigOf<T.ExportStatementUFormNamespaceExport>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'export_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'namespace_export' as const,
    $children: children,
    child(value?: T.ExportStatementNamespaceExport) {
      if (value === undefined) return children[0];
      return exportStatementUFormNamespaceExport({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementUFormNamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceExport(child: T.ModuleExportName) {
  const children = [child];
  return {
    $type: 'namespace_export' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportClause(...children: T.ExportSpecifier[]) {
  return {
    $type: 'export_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportSpecifier(config: T.ExportSpecifier.Config) {
  const fields = {
    name: config.name,
    alias: config.alias,
  };
  return {
    $type: 'export_specifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.ModuleExportName) { return _fs(config, exportSpecifier, 'name', value, fields.name); },
    alias(value?: T.ModuleExportName | undefined) { return _fs(config, exportSpecifier, 'alias', value, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declaration(child?: (T.FunctionDeclaration | T.GeneratorFunctionDeclaration | T.ClassDeclaration | T.LexicalDeclaration | T.VariableDeclaration | T.FunctionSignature | T.AbstractClassDeclaration | T.Module | T.InternalModule | T.TypeAliasDeclaration | T.EnumDeclaration | T.InterfaceDeclaration | T.ImportAlias | T.AmbientDeclaration)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_() {
  return {
    $type: 'import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'import' as const,
    render: () => 'import' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'import' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'import' as const },
    replace: (t: T.ImportTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' as const }; },
  };
}

export function importStatement(config: T.ImportStatement.Config) {
  const fields = {
    import_clause: config.importClause,
    from_clause: config.fromClause,
    source: config.source,
    import_attribute: config.importAttribute,
    semicolon: config.semicolon,
  };
  return {
    $type: 'import_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    importClause(value?: "type" | "typeof" | undefined) { return _fs(config, importStatement, 'importClause', value, fields.import_clause); },
    fromClause(value?: T.ImportClause | "from" | T.String | T.ImportRequireClause) { return _fs(config, importStatement, 'fromClause', value, fields.from_clause); },
    source(value?: T.String | undefined) { return _fs(config, importStatement, 'source', value, fields.source); },
    importAttribute(value?: T.ImportAttribute | undefined) { return _fs(config, importStatement, 'importAttribute', value, fields.import_attribute); },
    semicolon(value?: T.Semicolon) { return _fs(config, importStatement, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClause(config: ConfigOf<T.ImportClauseUFormNamespaceImport>): ReturnType<typeof importClauseUFormNamespaceImport>;
export function importClause(config: ConfigOf<T.ImportClauseUFormNamedImports>): ReturnType<typeof importClauseUFormNamedImports>;
export function importClause(config: ConfigOf<T.ImportClauseUFormDefaultImport>): ReturnType<typeof importClauseUFormDefaultImport>;
export function importClause(config: ConfigOf<T.ImportClauseUFormNamespaceImport> | ConfigOf<T.ImportClauseUFormNamedImports> | ConfigOf<T.ImportClauseUFormDefaultImport>) {
  switch (config.$variant) {
    case 'namespace_import': return importClauseUFormNamespaceImport(config as Parameters<typeof importClauseUFormNamespaceImport>[0]);
    case 'named_imports': return importClauseUFormNamedImports(config as Parameters<typeof importClauseUFormNamedImports>[0]);
    case 'default_import': return importClauseUFormDefaultImport(config as Parameters<typeof importClauseUFormDefaultImport>[0]);
  }
  throw new Error(`importClause: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'namespace_import' | 'named_imports' | 'default_import'.`);
}
export function importClauseUFormNamespaceImport(config: Omit<ConfigOf<T.ImportClauseUFormNamespaceImport>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'import_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'namespace_import' as const,
    $children: children,
    child(value?: T.ImportClauseNamespaceImport) {
      if (value === undefined) return children[0];
      return importClauseUFormNamespaceImport({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseUFormNamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseUFormNamedImports(config: Omit<ConfigOf<T.ImportClauseUFormNamedImports>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'import_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'named_imports' as const,
    $children: children,
    child(value?: T.ImportClauseNamedImports) {
      if (value === undefined) return children[0];
      return importClauseUFormNamedImports({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseUFormNamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseUFormDefaultImport(config: Omit<ConfigOf<T.ImportClauseUFormDefaultImport>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'import_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'default_import' as const,
    $children: children,
    child(value?: T.ImportClauseDefaultImport) {
      if (value === undefined) return children[0];
      return importClauseUFormDefaultImport({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseUFormDefaultImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceImport(config: T.NamespaceImport.Config) {
  const fields = {
    identifier: config.identifier,
  };
  return {
    $type: 'namespace_import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    identifier(value?: T.Identifier) { return _fs(config, namespaceImport, 'identifier', value, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedImports(...children: T.ImportSpecifier[]) {
  return {
    $type: 'named_imports' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifier(config: ConfigOf<T.ImportSpecifierUFormName>): ReturnType<typeof importSpecifierUFormName>;
export function importSpecifier(config: ConfigOf<T.ImportSpecifierUFormAs>): ReturnType<typeof importSpecifierUFormAs>;
export function importSpecifier(config: ConfigOf<T.ImportSpecifierUFormName> | ConfigOf<T.ImportSpecifierUFormAs>) {
  switch (config.$variant) {
    case 'name': return importSpecifierUFormName(config as Parameters<typeof importSpecifierUFormName>[0]);
    case 'as': return importSpecifierUFormAs(config as Parameters<typeof importSpecifierUFormAs>[0]);
  }
  throw new Error(`importSpecifier: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'name' | 'as'.`);
}
export function importSpecifierUFormName(config: Omit<ConfigOf<T.ImportSpecifierUFormName>, '$variant'>) {
  const inner = importSpecifierName(config);
  const children = [inner] as const;
  return {
    $type: 'import_specifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'name' as const,
    $children: children,
    name(value?: T.ImportIdentifier) {
      if (value === undefined) return inner.$fields.name;
      return importSpecifierUFormName({ name: value });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportSpecifierUFormNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importSpecifierUFormAs(config: Omit<ConfigOf<T.ImportSpecifierUFormAs>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'import_specifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'as' as const,
    $children: children,
    child(value?: T.ImportSpecifierAs) {
      if (value === undefined) return children[0];
      return importSpecifierUFormAs({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportSpecifierUFormAsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAttribute(config: T.ImportAttribute.Config) {
  const fields = {
    object: config.object,
  };
  return {
    $type: 'import_attribute' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: "with" | "assert" | T.Object) { return _fs(config, importAttribute, 'object', value, fields.object); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statement(child?: (T.ExportStatement | T.ImportStatement | T.DebuggerStatement | T.ExpressionStatement | T.Declaration | T.StatementBlock | T.IfStatement | T.SwitchStatement | T.ForStatement | T.ForInStatement | T.WhileStatement | T.DoStatement | T.TryStatement | T.WithStatement | T.BreakStatement | T.ContinueStatement | T.ReturnStatement | T.ThrowStatement | T.EmptyStatement | T.LabeledStatement)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(config: T.ExpressionStatement.Config) {
  const fields = {
    semicolon: config.semicolon,
  };
  const children = config.children ?? [];
  return {
    $type: 'expression_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    semicolon(value?: T.Semicolon) { return _fs(config, expressionStatement, 'semicolon', value, fields.semicolon); },
    child(value?: T.Expressions) {
      if (value === undefined) return children[0];
      return expressionStatement({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclaration(config: T.VariableDeclaration.Config) {
  const fields = {
    declarators: config.declarators,
    semicolon: config.semicolon,
  };
  return {
    $type: 'variable_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    declarators(...values: NonEmptyArray<T.VariableDeclarator>) { return _fsm(config, variableDeclaration, 'declarators', values, fields.declarators); },
    semicolon(value?: T.Semicolon) { return _fs(config, variableDeclaration, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lexicalDeclaration(config: T.LexicalDeclaration.Config) {
  const fields = {
    kind: config.kind,
    declarators: config.declarators,
    semicolon: config.semicolon,
  };
  return {
    $type: 'lexical_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    kind(value?: "let" | "const") { return _fs(config, lexicalDeclaration, 'kind', value, fields.kind); },
    declarators(...values: NonEmptyArray<T.VariableDeclarator>) { return _fsm(config, lexicalDeclaration, 'declarators', values, fields.declarators); },
    semicolon(value?: T.Semicolon) { return _fs(config, lexicalDeclaration, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclarator(config: ConfigOf<T.VariableDeclaratorForm0>): ReturnType<typeof variableDeclaratorForm0>;
export function variableDeclarator(config: ConfigOf<T.VariableDeclaratorForm1>): ReturnType<typeof variableDeclaratorForm1>;
export function variableDeclarator(config: ConfigOf<T.VariableDeclaratorForm0> | ConfigOf<T.VariableDeclaratorForm1>) {
  switch (config.$variant) {
    case 'form0': return variableDeclaratorForm0(config as Parameters<typeof variableDeclaratorForm0>[0]);
    case 'form1': return variableDeclaratorForm1(config as Parameters<typeof variableDeclaratorForm1>[0]);
  }
  throw new Error(`variableDeclarator: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'form0' | 'form1'.`);
}
export function variableDeclaratorForm0(config: Omit<ConfigOf<T.VariableDeclaratorForm0>, '$variant'>) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  const children = config.children ?? [];
  return {
    $type: 'variable_declarator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'form0' as const,
    $fields: fields,
    $children: children,
    name(value?: T.Identifier | T.DestructuringPattern) { return _fs(config, variableDeclaratorForm0, 'name', value, fields.name); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, variableDeclaratorForm0, 'type', value, fields.type); },
    child(value?: T.Initializer) {
      if (value === undefined) return children[0];
      return variableDeclaratorForm0({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VariableDeclaratorForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function variableDeclaratorForm1(config: Omit<ConfigOf<T.VariableDeclaratorForm1>, '$variant'>) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    $type: 'variable_declarator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'form1' as const,
    $fields: fields,
    name(value?: T.Identifier) { return _fs(config, variableDeclaratorForm1, 'name', value, fields.name); },
    typeField(value?: T.TypeAnnotation) { return _fs(config, variableDeclaratorForm1, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VariableDeclaratorForm1Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statementBlock(config: T.StatementBlock.Config) {
  const fields = {
    statements: config.statements,
    automatic_semicolon: config.automaticSemicolon,
  };
  return {
    $type: 'statement_block' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    statements(...values: T.Statement[]) { return _fsm(config, statementBlock, 'statements', values, fields.statements); },
    automaticSemicolon(value?: T.AutomaticSemicolon | undefined) { return _fs(config, statementBlock, 'automaticSemicolon', value, fields.automatic_semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: T.ElseClause.Config) {
  const fields = {
    statement: config.statement,
  };
  return {
    $type: 'else_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    statement(value?: T.Statement) { return _fs(config, elseClause, 'statement', value, fields.statement); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifStatement(config: T.IfStatement.Config) {
  const fields = {
    condition: config.condition,
    consequence: config.consequence,
    alternative: config.alternative,
  };
  return {
    $type: 'if_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    condition(value?: T.ParenthesizedExpression) { return _fs(config, ifStatement, 'condition', value, fields.condition); },
    consequence(value?: T.Statement) { return _fs(config, ifStatement, 'consequence', value, fields.consequence); },
    alternative(value?: T.ElseClause | undefined) { return _fs(config, ifStatement, 'alternative', value, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchStatement(config: T.SwitchStatement.Config) {
  const fields = {
    value: config.value,
    body: config.body,
  };
  return {
    $type: 'switch_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    value(value?: T.ParenthesizedExpression) { return _fs(config, switchStatement, 'value', value, fields.value); },
    body(value?: T.SwitchBody) { return _fs(config, switchStatement, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: T.ForStatement.Config) {
  const fields = {
    initializer: config.initializer,
    condition: config.condition,
    increment: config.increment,
    body: config.body,
  };
  return {
    $type: 'for_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    initializer(value?: T.LexicalDeclaration | T.VariableDeclaration | T.Expressions | T.EmptyStatement) { return _fs(config, forStatement, 'initializer', value, fields.initializer); },
    condition(value?: T.Expressions | T.EmptyStatement) { return _fs(config, forStatement, 'condition', value, fields.condition); },
    increment(value?: T.Expressions | undefined) { return _fs(config, forStatement, 'increment', value, fields.increment); },
    body(value?: T.Statement) { return _fs(config, forStatement, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInStatement(config: T.ForInStatement.Config) {
  const fields = {
    await: config.await ? "await" as const : undefined,
    left: config.left,
    kind: config.kind ? "var" as const : undefined,
    value: config.value,
    operator: config.operator,
    right: config.right,
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'for_in_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    await(value?: "await" | undefined) { return _fs(config, forInStatement, 'await', value, fields.await); },
    left(value?: T.LhsExpression | T.ParenthesizedExpression) { return _fs(config, forInStatement, 'left', value, fields.left); },
    kind(value?: "var" | undefined) { return _fs(config, forInStatement, 'kind', value, fields.kind); },
    value(value?: T.Expression | undefined) { return _fs(config, forInStatement, 'value', value, fields.value); },
    operator(value?: "in" | "of") { return _fs(config, forInStatement, 'operator', value, fields.operator); },
    right(value?: T.Expressions) { return _fs(config, forInStatement, 'right', value, fields.right); },
    body(value?: T.Statement) { return _fs(config, forInStatement, 'body', value, fields.body); },
    child(value?: T.AutomaticSemicolon) {
      if (value === undefined) return children[0];
      return forInStatement({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: T.WhileStatement.Config) {
  const fields = {
    condition: config.condition,
    body: config.body,
  };
  return {
    $type: 'while_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    condition(value?: T.ParenthesizedExpression) { return _fs(config, whileStatement, 'condition', value, fields.condition); },
    body(value?: T.Statement) { return _fs(config, whileStatement, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function doStatement(config: T.DoStatement.Config) {
  const fields = {
    body: config.body,
    condition: config.condition,
    semicolon: config.semicolon,
  };
  return {
    $type: 'do_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(value?: T.Statement) { return _fs(config, doStatement, 'body', value, fields.body); },
    condition(value?: T.ParenthesizedExpression) { return _fs(config, doStatement, 'condition', value, fields.condition); },
    semicolon(value?: T.Semicolon | undefined) { return _fs(config, doStatement, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: T.TryStatement.Config) {
  const fields = {
    body: config.body,
    handler: config.handler,
    finalizer: config.finalizer,
  };
  return {
    $type: 'try_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(value?: T.StatementBlock) { return _fs(config, tryStatement, 'body', value, fields.body); },
    handler(value?: T.CatchClause | undefined) { return _fs(config, tryStatement, 'handler', value, fields.handler); },
    finalizer(value?: T.FinallyClause | undefined) { return _fs(config, tryStatement, 'finalizer', value, fields.finalizer); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: T.WithStatement.Config) {
  const fields = {
    object: config.object,
    body: config.body,
  };
  return {
    $type: 'with_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.ParenthesizedExpression) { return _fs(config, withStatement, 'object', value, fields.object); },
    body(value?: T.Statement) { return _fs(config, withStatement, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakStatement(config: T.BreakStatement.Config) {
  const fields = {
    label: config.label,
    semicolon: config.semicolon,
  };
  return {
    $type: 'break_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    label(value?: T.Identifier | undefined) { return _fs(config, breakStatement, 'label', value, fields.label); },
    semicolon(value?: T.Semicolon) { return _fs(config, breakStatement, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueStatement(config: T.ContinueStatement.Config) {
  const fields = {
    label: config.label,
    semicolon: config.semicolon,
  };
  return {
    $type: 'continue_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    label(value?: T.Identifier | undefined) { return _fs(config, continueStatement, 'label', value, fields.label); },
    semicolon(value?: T.Semicolon) { return _fs(config, continueStatement, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function debuggerStatement(config: T.DebuggerStatement.Config) {
  const fields = {
    semicolon: config.semicolon,
  };
  return {
    $type: 'debugger_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    semicolon(value?: T.Semicolon) { return _fs(config, debuggerStatement, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(config: T.ReturnStatement.Config) {
  const fields = {
    semicolon: config.semicolon,
  };
  const children = config.children ?? [];
  return {
    $type: 'return_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    semicolon(value?: T.Semicolon) { return _fs(config, returnStatement, 'semicolon', value, fields.semicolon); },
    child(value?: T.Expressions) {
      if (value === undefined) return children[0];
      return returnStatement({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function throwStatement(config: T.ThrowStatement.Config) {
  const fields = {
    semicolon: config.semicolon,
  };
  const children = config.children ?? [];
  return {
    $type: 'throw_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    semicolon(value?: T.Semicolon) { return _fs(config, throwStatement, 'semicolon', value, fields.semicolon); },
    child(value?: T.Expressions) {
      if (value === undefined) return children[0];
      return throwStatement({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function labeledStatement(config: T.LabeledStatement.Config) {
  const fields = {
    label: config.label,
    body: config.body,
  };
  return {
    $type: 'labeled_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    label(value?: T.StatementIdentifier) { return _fs(config, labeledStatement, 'label', value, fields.label); },
    body(value?: T.Statement) { return _fs(config, labeledStatement, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchBody(...children: (T.SwitchCase | T.SwitchDefault)[]) {
  return {
    $type: 'switch_body' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchCase(config: T.SwitchCase.Config) {
  const fields = {
    value: config.value,
    body: config.body,
  };
  return {
    $type: 'switch_case' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    value(value?: T.Expressions) { return _fs(config, switchCase, 'value', value, fields.value); },
    body(...values: T.Statement[]) { return _fsm(config, switchCase, 'body', values, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchDefault(config: T.SwitchDefault.Config) {
  const fields = {
    body: config.body,
  };
  return {
    $type: 'switch_default' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(...values: T.Statement[]) { return _fsm(config, switchDefault, 'body', values, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function catchClause(config: T.CatchClause.Config) {
  const fields = {
    parameter: config.parameter,
    type: config.type,
    body: config.body,
  };
  return {
    $type: 'catch_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    parameter(value?: T.Identifier | T.DestructuringPattern | undefined) { return _fs(config, catchClause, 'parameter', value, fields.parameter); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, catchClause, 'type', value, fields.type); },
    body(value?: T.StatementBlock) { return _fs(config, catchClause, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: T.FinallyClause.Config) {
  const fields = {
    body: config.body,
  };
  return {
    $type: 'finally_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(value?: T.StatementBlock) { return _fs(config, finallyClause, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(config: ConfigOf<T.ParenthesizedExpressionUFormTyped>): ReturnType<typeof parenthesizedExpressionUFormTyped>;
export function parenthesizedExpression(config: ConfigOf<T.ParenthesizedExpressionUFormSequence>): ReturnType<typeof parenthesizedExpressionUFormSequence>;
export function parenthesizedExpression(config: ConfigOf<T.ParenthesizedExpressionUFormTyped> | ConfigOf<T.ParenthesizedExpressionUFormSequence>) {
  switch (config.$variant) {
    case 'typed': return parenthesizedExpressionUFormTyped(config as Parameters<typeof parenthesizedExpressionUFormTyped>[0]);
    case 'sequence': return parenthesizedExpressionUFormSequence(config as Parameters<typeof parenthesizedExpressionUFormSequence>[0]);
  }
  throw new Error(`parenthesizedExpression: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'typed' | 'sequence'.`);
}
export function parenthesizedExpressionUFormTyped(config: Omit<ConfigOf<T.ParenthesizedExpressionUFormTyped>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'parenthesized_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'typed' as const,
    $children: children,
    child(value?: T.ParenthesizedExpressionTyped) {
      if (value === undefined) return children[0];
      return parenthesizedExpressionUFormTyped({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedExpressionUFormTypedTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parenthesizedExpressionUFormSequence(config: Omit<ConfigOf<T.ParenthesizedExpressionUFormSequence>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'parenthesized_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'sequence' as const,
    $children: children,
    child(value?: T.ParenthesizedExpressionSequence) {
      if (value === undefined) return children[0];
      return parenthesizedExpressionUFormSequence({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedExpressionUFormSequenceTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expression(child?: (T.AsExpression | T.SatisfiesExpression | T.InstantiationExpression | T.InternalModule | T.TypeAssertion | T.PrimaryExpression | T.AssignmentExpression | T.AugmentedAssignmentExpression | T.AwaitExpression | T.UnaryExpression | T.BinaryExpression | T.TernaryExpression | T.UpdateExpression | T.NewExpression | T.YieldExpression)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function primaryExpression(child?: (T.SubscriptExpression | T.MemberExpression | T.ParenthesizedExpression | T._Identifier | T.This | T.Super | T.Number | T.String | T.TemplateString | T.Regex | T.True | T.False | T.Null | T.Object | T.Array | T.FunctionExpression | T.ArrowFunction | T.GeneratorFunction | T.Class | T.MetaProperty | T.CallExpression | T.NonNullExpression)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'primary_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(config?: T.YieldExpression.Config) {
  const fields = {
    expression: config?.expression,
  };
  return {
    $type: 'yield_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression | undefined) { return _fs(config, yieldExpression, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function object(...children: (T.Pair | T.SpreadElement | T.MethodDefinition | T.ShorthandPropertyIdentifier)[]) {
  return {
    $type: 'object' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectPattern(...children: (T.PairPattern | T.RestPattern | T.ObjectAssignmentPattern | T.ShorthandPropertyIdentifierPattern)[]) {
  return {
    $type: 'object_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentPattern(config: T.AssignmentPattern.Config) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'assignment_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Pattern) { return _fs(config, assignmentPattern, 'left', value, fields.left); },
    right(value?: T.Expression) { return _fs(config, assignmentPattern, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectAssignmentPattern(config: T.ObjectAssignmentPattern.Config) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'object_assignment_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.ShorthandPropertyIdentifierPattern | T.DestructuringPattern) { return _fs(config, objectAssignmentPattern, 'left', value, fields.left); },
    right(value?: T.Expression) { return _fs(config, objectAssignmentPattern, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function array(...children: (T.Expression | T.SpreadElement)[]) {
  return {
    $type: 'array' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayPattern(...children: (T.Pattern | T.AssignmentPattern)[]) {
  return {
    $type: 'array_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxElement(config: T.JsxElement.Config) {
  const fields = {
    open_tag: config.openTag,
    close_tag: config.closeTag,
  };
  const children = config.children ?? [];
  return {
    $type: 'jsx_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    openTag(value?: T.JsxOpeningElement) { return _fs(config, jsxElement, 'openTag', value, fields.open_tag); },
    closeTag(value?: T.JsxClosingElement) { return _fs(config, jsxElement, 'closeTag', value, fields.close_tag); },
    children(...items: T.JsxChild[]) {
      if (items.length === 0) return children;
      return jsxElement({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlCharacterReference(text: string) {
  if (text.length === 0) throw new Error(`html_character_reference: text must be non-empty`); if (!_leafRe_htmlCharacterReference.test(text)) throw new Error(`html_character_reference: text does not match pattern: ${text}`);
  return {
    $type: 'html_character_reference' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HtmlCharacterReferenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxExpression(child?: (T.Expression | T.SequenceExpression | T.SpreadElement)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'jsx_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxOpeningElement(config: T.JsxOpeningElement.Config) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
    attribute: config.attribute,
  };
  return {
    $type: 'jsx_opening_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T._JsxIdentifier | T.JsxNamespaceName | undefined) { return _fs(config, jsxOpeningElement, 'name', value, fields.name); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, jsxOpeningElement, 'typeArguments', value, fields.type_arguments); },
    attribute(...values: T._JsxAttribute[]) { return _fsm(config, jsxOpeningElement, 'attribute', values, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxIdentifier(text: string) {
  if (text.length === 0) throw new Error(`jsx_identifier: text must be non-empty`); if (!_leafRe_jsxIdentifier.test(text)) throw new Error(`jsx_identifier: text does not match pattern: ${text}`);
  return {
    $type: 'jsx_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.JsxIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function nestedIdentifier(config: T.NestedIdentifier.Config) {
  const fields = {
    object: config.object,
    property: config.property,
  };
  return {
    $type: 'nested_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.Identifier | T.NestedIdentifier) { return _fs(config, nestedIdentifier, 'object', value, fields.object); },
    property(value?: T.Identifier) { return _fs(config, nestedIdentifier, 'property', value, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxNamespaceName(...children: T._JsxIdentifier[]) {
  return {
    $type: 'jsx_namespace_name' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxNamespaceNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxClosingElement(config?: T.JsxClosingElement.Config) {
  const fields = {
    name: config?.name,
  };
  return {
    $type: 'jsx_closing_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.JsxElementName | undefined) { return _fs(config, jsxClosingElement, 'name', value, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxSelfClosingElement(config: T.JsxSelfClosingElement.Config) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
    attribute: config.attribute,
  };
  return {
    $type: 'jsx_self_closing_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T._JsxIdentifier | T.JsxNamespaceName | undefined) { return _fs(config, jsxSelfClosingElement, 'name', value, fields.name); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, jsxSelfClosingElement, 'typeArguments', value, fields.type_arguments); },
    attribute(...values: T._JsxAttribute[]) { return _fsm(config, jsxSelfClosingElement, 'attribute', values, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxSelfClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxAttribute(child: (T.JsxAttributeName | T.JsxAttributeValue)) {
  const children = [child];
  return {
    $type: 'jsx_attribute' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleJsxStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_double_jsx_string_fragment: text must be non-empty`); if (!_leafRe_unescapedDoubleJsxStringFragment.test(text)) throw new Error(`unescaped_double_jsx_string_fragment: text does not match pattern: ${text}`);
  return {
    $type: 'unescaped_double_jsx_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedDoubleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleJsxStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_single_jsx_string_fragment: text must be non-empty`); if (!_leafRe_unescapedSingleJsxStringFragment.test(text)) throw new Error(`unescaped_single_jsx_string_fragment: text does not match pattern: ${text}`);
  return {
    $type: 'unescaped_single_jsx_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedSingleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function class_(config: T.Class.Config) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
    body: config.body,
  };
  return {
    $type: 'class' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    decorator(...values: T.Decorator[]) { return _fsm(config, class_, 'decorator', values, fields.decorator); },
    name(value?: T.TypeIdentifier | undefined) { return _fs(config, class_, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, class_, 'typeParameters', value, fields.type_parameters); },
    classHeritage(value?: T.ClassHeritage | undefined) { return _fs(config, class_, 'classHeritage', value, fields.class_heritage); },
    body(value?: T.ClassBody) { return _fs(config, class_, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDeclaration(config: T.ClassDeclaration.Config) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
    body: config.body,
    automatic_semicolon: config.automaticSemicolon,
  };
  return {
    $type: 'class_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    decorator(...values: T.Decorator[]) { return _fsm(config, classDeclaration, 'decorator', values, fields.decorator); },
    name(value?: T.TypeIdentifier) { return _fs(config, classDeclaration, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, classDeclaration, 'typeParameters', value, fields.type_parameters); },
    classHeritage(value?: T.ClassHeritage | undefined) { return _fs(config, classDeclaration, 'classHeritage', value, fields.class_heritage); },
    body(value?: T.ClassBody) { return _fs(config, classDeclaration, 'body', value, fields.body); },
    automaticSemicolon(value?: T.AutomaticSemicolon | undefined) { return _fs(config, classDeclaration, 'automaticSemicolon', value, fields.automatic_semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritage(config: ConfigOf<T.ClassHeritageUFormExtendsClause>): ReturnType<typeof classHeritageUFormExtendsClause>;
export function classHeritage(config: ConfigOf<T.ClassHeritageUFormImplementsClause>): ReturnType<typeof classHeritageUFormImplementsClause>;
export function classHeritage(config: ConfigOf<T.ClassHeritageUFormExtendsClause> | ConfigOf<T.ClassHeritageUFormImplementsClause>) {
  switch (config.$variant) {
    case 'extends_clause': return classHeritageUFormExtendsClause(config as Parameters<typeof classHeritageUFormExtendsClause>[0]);
    case 'implements_clause': return classHeritageUFormImplementsClause(config as Parameters<typeof classHeritageUFormImplementsClause>[0]);
  }
  throw new Error(`classHeritage: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'extends_clause' | 'implements_clause'.`);
}
export function classHeritageUFormExtendsClause(config: Omit<ConfigOf<T.ClassHeritageUFormExtendsClause>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'class_heritage' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'extends_clause' as const,
    $children: children,
    child(value?: T.ClassHeritageExtendsClause) {
      if (value === undefined) return children[0];
      return classHeritageUFormExtendsClause({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassHeritageUFormExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function classHeritageUFormImplementsClause(config: Omit<ConfigOf<T.ClassHeritageUFormImplementsClause>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'class_heritage' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'implements_clause' as const,
    $children: children,
    child(value?: T.ClassHeritageImplementsClause) {
      if (value === undefined) return children[0];
      return classHeritageUFormImplementsClause({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassHeritageUFormImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionExpression(config: T.FunctionExpression.Config) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  return {
    $type: 'function_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: "async" | undefined) { return _fs(config, functionExpression, 'async', value, fields.async); },
    name(value?: T.Identifier | undefined) { return _fs(config, functionExpression, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionExpression, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, functionExpression, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, functionExpression, 'returnType', value, fields.return_type); },
    body(value?: T.StatementBlock) { return _fs(config, functionExpression, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDeclaration(config: T.FunctionDeclaration.Config) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'function_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    async(value?: "async" | undefined) { return _fs(config, functionDeclaration, 'async', value, fields.async); },
    name(value?: T.Identifier) { return _fs(config, functionDeclaration, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionDeclaration, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, functionDeclaration, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, functionDeclaration, 'returnType', value, fields.return_type); },
    body(value?: T.StatementBlock) { return _fs(config, functionDeclaration, 'body', value, fields.body); },
    child(value?: T.AutomaticSemicolon) {
      if (value === undefined) return children[0];
      return functionDeclaration({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunction(config: T.GeneratorFunction.Config) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  return {
    $type: 'generator_function' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: "async" | undefined) { return _fs(config, generatorFunction, 'async', value, fields.async); },
    name(value?: T.Identifier | undefined) { return _fs(config, generatorFunction, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, generatorFunction, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, generatorFunction, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, generatorFunction, 'returnType', value, fields.return_type); },
    body(value?: T.StatementBlock) { return _fs(config, generatorFunction, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunctionDeclaration(config: T.GeneratorFunctionDeclaration.Config) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'generator_function_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    async(value?: "async" | undefined) { return _fs(config, generatorFunctionDeclaration, 'async', value, fields.async); },
    name(value?: T.Identifier) { return _fs(config, generatorFunctionDeclaration, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, generatorFunctionDeclaration, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, generatorFunctionDeclaration, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, generatorFunctionDeclaration, 'returnType', value, fields.return_type); },
    body(value?: T.StatementBlock) { return _fs(config, generatorFunctionDeclaration, 'body', value, fields.body); },
    child(value?: T.AutomaticSemicolon) {
      if (value === undefined) return children[0];
      return generatorFunctionDeclaration({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunction(config: ConfigOf<T.ArrowFunctionUFormParameter>): ReturnType<typeof arrowFunctionUFormParameter>;
export function arrowFunction(config: ConfigOf<T.ArrowFunctionUFormUCallSignature>): ReturnType<typeof arrowFunctionUFormUCallSignature>;
export function arrowFunction(config: ConfigOf<T.ArrowFunctionUFormParameter> | ConfigOf<T.ArrowFunctionUFormUCallSignature>) {
  switch (config.$variant) {
    case 'parameter': return arrowFunctionUFormParameter(config as Parameters<typeof arrowFunctionUFormParameter>[0]);
    case '_call_signature': return arrowFunctionUFormUCallSignature(config as Parameters<typeof arrowFunctionUFormUCallSignature>[0]);
  }
  throw new Error(`arrowFunction: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'parameter' | '_call_signature'.`);
}
export function arrowFunctionUFormParameter(config: Omit<ConfigOf<T.ArrowFunctionUFormParameter>, '$variant'>) {
  const fields = {
    async: config.async,
    body: config.body,
  };
  const inner = arrowFunctionParameter(config);
  const children = [inner] as const;
  return {
    $type: 'arrow_function' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'parameter' as const,
    $fields: fields,
    $children: children,
    async(value?: "async" | undefined) {
      if (value === undefined) return fields.async;
      return arrowFunctionUFormParameter({ body: config.body, parameter: inner.$fields.parameter, async: value });
    },
    body(value?: T.Expression | T.StatementBlock) {
      if (value === undefined) return fields.body;
      return arrowFunctionUFormParameter({ async: config.async, parameter: inner.$fields.parameter, body: value });
    },
    parameter(value?: T.ReservedIdentifier) {
      if (value === undefined) return inner.$fields.parameter;
      return arrowFunctionUFormParameter({ async: config.async, body: config.body, parameter: value });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrowFunctionUFormParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function arrowFunctionUFormUCallSignature(config: Omit<ConfigOf<T.ArrowFunctionUFormUCallSignature>, '$variant'>) {
  const fields = {
    async: config.async,
    body: config.body,
  };
  const inner = arrowFunctionUCallSignature(config);
  const children = [inner] as const;
  return {
    $type: 'arrow_function' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: '_call_signature' as const,
    $fields: fields,
    $children: children,
    async(value?: "async" | undefined) {
      if (value === undefined) return fields.async;
      return arrowFunctionUFormUCallSignature({ body: config.body, typeParameters: inner.$fields.type_parameters, parameters: inner.$fields.parameters, returnType: inner.$fields.return_type, async: value });
    },
    body(value?: T.Expression | T.StatementBlock) {
      if (value === undefined) return fields.body;
      return arrowFunctionUFormUCallSignature({ async: config.async, typeParameters: inner.$fields.type_parameters, parameters: inner.$fields.parameters, returnType: inner.$fields.return_type, body: value });
    },
    typeParameters(value?: T.TypeParameters | undefined) {
      if (value === undefined) return inner.$fields.type_parameters;
      return arrowFunctionUFormUCallSignature({ async: config.async, body: config.body, parameters: inner.$fields.parameters, returnType: inner.$fields.return_type, typeParameters: value });
    },
    parameters(value?: T.FormalParameters) {
      if (value === undefined) return inner.$fields.parameters;
      return arrowFunctionUFormUCallSignature({ async: config.async, body: config.body, typeParameters: inner.$fields.type_parameters, returnType: inner.$fields.return_type, parameters: value });
    },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) {
      if (value === undefined) return inner.$fields.return_type;
      return arrowFunctionUFormUCallSignature({ async: config.async, body: config.body, typeParameters: inner.$fields.type_parameters, parameters: inner.$fields.parameters, returnType: value });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrowFunctionUFormUCallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: ConfigOf<T.CallExpressionUFormCall>): ReturnType<typeof callExpressionUFormCall>;
export function callExpression(config: ConfigOf<T.CallExpressionUFormTemplateCall>): ReturnType<typeof callExpressionUFormTemplateCall>;
export function callExpression(config: ConfigOf<T.CallExpressionUFormMember>): ReturnType<typeof callExpressionUFormMember>;
export function callExpression(config: ConfigOf<T.CallExpressionUFormCall> | ConfigOf<T.CallExpressionUFormTemplateCall> | ConfigOf<T.CallExpressionUFormMember>) {
  switch (config.$variant) {
    case 'call': return callExpressionUFormCall(config as Parameters<typeof callExpressionUFormCall>[0]);
    case 'template_call': return callExpressionUFormTemplateCall(config as Parameters<typeof callExpressionUFormTemplateCall>[0]);
    case 'member': return callExpressionUFormMember(config as Parameters<typeof callExpressionUFormMember>[0]);
  }
  throw new Error(`callExpression: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'call' | 'template_call' | 'member'.`);
}
export function callExpressionUFormCall(config: Omit<ConfigOf<T.CallExpressionUFormCall>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'call_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'call' as const,
    $children: children,
    child(value?: T.CallExpressionCall) {
      if (value === undefined) return children[0];
      return callExpressionUFormCall({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallExpressionUFormCallTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionUFormTemplateCall(config: Omit<ConfigOf<T.CallExpressionUFormTemplateCall>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'call_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'template_call' as const,
    $children: children,
    child(value?: T.CallExpressionTemplateCall) {
      if (value === undefined) return children[0];
      return callExpressionUFormTemplateCall({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallExpressionUFormTemplateCallTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionUFormMember(config: Omit<ConfigOf<T.CallExpressionUFormMember>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'call_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'member' as const,
    $children: children,
    child(value?: T.CallExpressionMember) {
      if (value === undefined) return children[0];
      return callExpressionUFormMember({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallExpressionUFormMemberTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function newExpression(config: T.NewExpression.Config) {
  const fields = {
    constructor: config.constructor,
    type_arguments: config.typeArguments,
    arguments: config.arguments,
  };
  return {
    $type: 'new_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    constructor(value?: T.PrimaryExpression) { return _fs(config, newExpression, 'constructor', value, fields.constructor); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, newExpression, 'typeArguments', value, fields.type_arguments); },
    arguments(value?: T.Arguments | undefined) { return _fs(config, newExpression, 'arguments', value, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(config: T.AwaitExpression.Config) {
  const fields = {
    expression: config.expression,
  };
  return {
    $type: 'await_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, awaitExpression, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberExpression(config: T.MemberExpression.Config) {
  const fields = {
    object: config.object,
    optional_chain: config.optionalChain ? "?." as const : undefined,
    property: config.property,
  };
  return {
    $type: 'member_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.Expression | T.PrimaryExpression | T.Import) { return _fs(config, memberExpression, 'object', value, fields.object); },
    optionalChain(value?: T.OptionalChain | undefined) { return _fs(config, memberExpression, 'optionalChain', value, fields.optional_chain); },
    property(value?: T.PrivatePropertyIdentifier | T.Identifier) { return _fs(config, memberExpression, 'property', value, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscriptExpression(config: T.SubscriptExpression.Config) {
  const fields = {
    object: config.object,
    optional_chain: config.optionalChain ? "?." as const : undefined,
    index: config.index,
  };
  return {
    $type: 'subscript_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.Expression | T.PrimaryExpression) { return _fs(config, subscriptExpression, 'object', value, fields.object); },
    optionalChain(value?: T.OptionalChain | undefined) { return _fs(config, subscriptExpression, 'optionalChain', value, fields.optional_chain); },
    index(value?: T.Expressions) { return _fs(config, subscriptExpression, 'index', value, fields.index); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lhsExpression(child?: (T.MemberExpression | T.SubscriptExpression | T._Identifier | T.DestructuringPattern | T.NonNullExpression)) {
  const children = child != null ? [child] : [];
  return {
    $type: '_lhs_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LhsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: T.AssignmentExpression.Config) {
  const fields = {
    using: config.using ? "using" as const : undefined,
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'assignment_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    using(value?: "using" | undefined) { return _fs(config, assignmentExpression, 'using', value, fields.using); },
    left(value?: T.ParenthesizedExpression | T.LhsExpression) { return _fs(config, assignmentExpression, 'left', value, fields.left); },
    right(value?: T.Expression) { return _fs(config, assignmentExpression, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignmentExpression(config: T.AugmentedAssignmentExpression.Config) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    $type: 'augmented_assignment_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.MemberExpression | T.SubscriptExpression | T.ReservedIdentifier | T.ParenthesizedExpression | T.NonNullExpression) { return _fs(config, augmentedAssignmentExpression, 'left', value, fields.left); },
    operator(value?: "+=" | "-=" | "*=" | "/=" | "%=" | "^=" | "&=" | "|=" | ">>=" | ">>>=" | "<<=" | "**=" | "&&=" | "||=" | "??=") { return _fs(config, augmentedAssignmentExpression, 'operator', value, fields.operator); },
    right(value?: T.Expression) { return _fs(config, augmentedAssignmentExpression, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function spreadElement(config: T.SpreadElement.Config) {
  const fields = {
    expression: config.expression,
  };
  return {
    $type: 'spread_element' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, spreadElement, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ternaryExpression(config: T.TernaryExpression.Config) {
  const fields = {
    condition: config.condition,
    consequence: config.consequence,
    alternative: config.alternative,
  };
  return {
    $type: 'ternary_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    condition(value?: T.Expression) { return _fs(config, ternaryExpression, 'condition', value, fields.condition); },
    consequence(value?: T.Expression) { return _fs(config, ternaryExpression, 'consequence', value, fields.consequence); },
    alternative(value?: T.Expression) { return _fs(config, ternaryExpression, 'alternative', value, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: T.BinaryExpression.Config) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    $type: 'binary_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Expression | T.PrivatePropertyIdentifier) { return _fs(config, binaryExpression, 'left', value, fields.left); },
    operator(value?: "&&" | "||" | ">>" | ">>>" | "<<" | "&" | "^" | "|" | "+" | "-" | "*" | "/" | "%" | "**" | "<" | "<=" | "==" | "===" | "!=" | "!==" | ">=" | ">" | "??" | "instanceof" | "in") { return _fs(config, binaryExpression, 'operator', value, fields.operator); },
    right(value?: T.Expression) { return _fs(config, binaryExpression, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: T.UnaryExpression.Config) {
  const fields = {
    operator: config.operator,
    argument: config.argument,
  };
  return {
    $type: 'unary_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    operator(value?: "!" | "~" | "-" | "+" | "typeof" | "void" | "delete") { return _fs(config, unaryExpression, 'operator', value, fields.operator); },
    argument(value?: T.Expression) { return _fs(config, unaryExpression, 'argument', value, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function updateExpression(config: T.UpdateExpression.Config) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    $type: 'update_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    argument(value?: T.Expression) { return _fs(config, updateExpression, 'argument', value, fields.argument); },
    operator(value?: "++" | "--") { return _fs(config, updateExpression, 'operator', value, fields.operator); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function sequenceExpression(...children: T.Expression[]) {
  _assertNonEmpty(children, 'sequence_expression.children');
  return {
    $type: 'sequence_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(config: ConfigOf<T.StringUFormDouble>): ReturnType<typeof stringUFormDouble>;
export function string(config: ConfigOf<T.StringUFormSingle>): ReturnType<typeof stringUFormSingle>;
export function string(config: ConfigOf<T.StringUFormDouble> | ConfigOf<T.StringUFormSingle>) {
  switch (config.$variant) {
    case 'double': return stringUFormDouble(config as Parameters<typeof stringUFormDouble>[0]);
    case 'single': return stringUFormSingle(config as Parameters<typeof stringUFormSingle>[0]);
  }
  throw new Error(`string: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'double' | 'single'.`);
}
export function stringUFormDouble(config: Omit<ConfigOf<T.StringUFormDouble>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'string' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'double' as const,
    $children: children,
    child(value?: T.StringDouble) {
      if (value === undefined) return children[0];
      return stringUFormDouble({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringUFormDoubleTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function stringUFormSingle(config: Omit<ConfigOf<T.StringUFormSingle>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: 'string' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'single' as const,
    $children: children,
    child(value?: T.StringSingle) {
      if (value === undefined) return children[0];
      return stringUFormSingle({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringUFormSingleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_double_string_fragment: text must be non-empty`); if (!_leafRe_unescapedDoubleStringFragment.test(text)) throw new Error(`unescaped_double_string_fragment: text does not match pattern: ${text}`);
  return {
    $type: 'unescaped_double_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedDoubleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_single_string_fragment: text must be non-empty`); if (!_leafRe_unescapedSingleStringFragment.test(text)) throw new Error(`unescaped_single_string_fragment: text does not match pattern: ${text}`);
  return {
    $type: 'unescaped_single_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedSingleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`);
  return {
    $type: 'escape_sequence' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(text: string) {
  if (text.length === 0) throw new Error(`comment: text must be non-empty`);
  return {
    $type: 'comment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function templateString(...children: (T.StringFragment | T.EscapeSequence | T.TemplateSubstitution)[]) {
  return {
    $type: 'template_string' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateSubstitution(child: T.Expressions) {
  const children = [child];
  return {
    $type: 'template_substitution' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regex(config: T.Regex.Config) {
  const fields = {
    pattern: config.pattern,
    flags: config.flags,
  };
  return {
    $type: 'regex' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    pattern(value?: T.RegexPattern) { return _fs(config, regex, 'pattern', value, fields.pattern); },
    flags(value?: T.RegexFlags | undefined) { return _fs(config, regex, 'flags', value, fields.flags); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regexPattern(text: string) {
  if (text.length === 0) throw new Error(`regex_pattern: text must be non-empty`);
  return {
    $type: 'regex_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.RegexPatternTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regexFlags(text: string) {
  if (text.length === 0) throw new Error(`regex_flags: text must be non-empty`); if (!_leafRe_regexFlags.test(text)) throw new Error(`regex_flags: text does not match pattern: ${text}`);
  return {
    $type: 'regex_flags' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.RegexFlagsTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function number(text: string) {
  if (text.length === 0) throw new Error(`number: text must be non-empty`);
  return {
    $type: 'number' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.NumberTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`);
  return {
    $type: 'identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function privatePropertyIdentifier(text: string) {
  if (text.length === 0) throw new Error(`private_property_identifier: text must be non-empty`);
  return {
    $type: 'private_property_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PrivatePropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function metaProperty(text: string) {
  if (text.length === 0) throw new Error(`meta_property: text must be non-empty`);
  return {
    $type: 'meta_property' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.MetaPropertyTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function this_() {
  return {
    $type: 'this' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'this' as const,
    render: () => 'this' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' as const },
    replace: (t: T.ThisTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' as const }; },
  };
}

export function super_() {
  return {
    $type: 'super' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: T.SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function true_() {
  return {
    $type: 'true' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'true' as const,
    render: () => 'true' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'true' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'true' as const },
    replace: (t: T.TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' as const }; },
  };
}

export function false_() {
  return {
    $type: 'false' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'false' as const,
    render: () => 'false' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'false' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'false' as const },
    replace: (t: T.FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' as const }; },
  };
}

export function null_() {
  return {
    $type: 'null' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'null' as const,
    render: () => 'null' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'null' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'null' as const },
    replace: (t: T.NullTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' as const }; },
  };
}

export function undefined_() {
  return {
    $type: 'undefined' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'undefined' as const,
    render: () => 'undefined' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'undefined' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'undefined' as const },
    replace: (t: T.UndefinedTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' as const }; },
  };
}

export function arguments_(...children: (T.Expression | T.SpreadElement)[]) {
  return {
    $type: 'arguments' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(child?: (T.Identifier | T.DecoratorMemberExpression | T.DecoratorCallExpression | T.DecoratorParenthesizedExpression)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'decorator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorMemberExpression(config: T.DecoratorMemberExpression.Config) {
  const fields = {
    object: config.object,
    property: config.property,
  };
  return {
    $type: 'decorator_member_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.Identifier | T.DecoratorMemberExpression) { return _fs(config, decoratorMemberExpression, 'object', value, fields.object); },
    property(value?: T.Identifier) { return _fs(config, decoratorMemberExpression, 'property', value, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorMemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorCallExpression(config: T.DecoratorCallExpression.Config) {
  const fields = {
    function: config.function,
    type_arguments: config.typeArguments,
    arguments: config.arguments,
  };
  return {
    $type: 'decorator_call_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    function(value?: T.Identifier | T.DecoratorMemberExpression) { return _fs(config, decoratorCallExpression, 'function', value, fields.function); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, decoratorCallExpression, 'typeArguments', value, fields.type_arguments); },
    arguments(value?: T.Arguments) { return _fs(config, decoratorCallExpression, 'arguments', value, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorCallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBody(config: T.ClassBody.Config) {
  const fields = {
    decorator: config.decorator,
  };
  const children = config.children ?? [];
  return {
    $type: 'class_body' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    decorator(...values: T.Decorator[]) { return _fsm(config, classBody, 'decorator', values, fields.decorator); },
    children(...items: (T.MethodDefinition | T.Semicolon | T.MethodSignature | T.FunctionSignatureAutomaticSemicolon | T.ClassStaticBlock | T.AbstractMethodSignature | T.IndexSignature | T.PublicFieldDefinition)[]) {
      if (items.length === 0) return children;
      return classBody({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDefinition(config: T.FieldDefinition.Config) {
  const fields = {
    decorator: config.decorator,
    static: config.static ? "static" as const : undefined,
    property: config.property,
    value: config.value,
  };
  return {
    $type: 'field_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    decorator(...values: T.Decorator[]) { return _fsm(config, fieldDefinition, 'decorator', values, fields.decorator); },
    static(value?: "static" | undefined) { return _fs(config, fieldDefinition, 'static', value, fields.static); },
    property(value?: T.PropertyName) { return _fs(config, fieldDefinition, 'property', value, fields.property); },
    value(value?: T.Expression | undefined) { return _fs(config, fieldDefinition, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formalParameters(...children: T.FormalParameter[]) {
  _assertNonEmpty(children, 'formal_parameters.children');
  return {
    $type: 'formal_parameters' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classStaticBlock(config: T.ClassStaticBlock.Config) {
  const fields = {
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'class_static_block' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    body(value?: T.StatementBlock) { return _fs(config, classStaticBlock, 'body', value, fields.body); },
    child(value?: T.AutomaticSemicolon) {
      if (value === undefined) return children[0];
      return classStaticBlock({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pattern(child?: (T.LhsExpression | T.RestPattern)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restPattern(child: T.LhsExpression) {
  const children = [child];
  return {
    $type: 'rest_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodDefinition(config: T.MethodDefinition.Config) {
  const fields = {
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier ? "static" as const : undefined,
    readonly: config.readonly ? "readonly" as const : undefined,
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    body: config.body,
  };
  const children = config.children ?? [];
  return {
    $type: 'method_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    accessibilityModifier(value?: T.AccessibilityModifier | undefined) { return _fs(config, methodDefinition, 'accessibilityModifier', value, fields.accessibility_modifier); },
    overrideModifier(value?: "static" | undefined) { return _fs(config, methodDefinition, 'overrideModifier', value, fields.override_modifier); },
    readonly(value?: "readonly" | undefined) { return _fs(config, methodDefinition, 'readonly', value, fields.readonly); },
    async(value?: "async" | undefined) { return _fs(config, methodDefinition, 'async', value, fields.async); },
    name(value?: T.PropertyName) { return _fs(config, methodDefinition, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, methodDefinition, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, methodDefinition, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, methodDefinition, 'returnType', value, fields.return_type); },
    body(value?: T.StatementBlock) { return _fs(config, methodDefinition, 'body', value, fields.body); },
    child(value?: T.OverrideModifier) {
      if (value === undefined) return children[0];
      return methodDefinition({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: T.Pair.Config) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    $type: 'pair' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    key(value?: T.PropertyName) { return _fs(config, pair, 'key', value, fields.key); },
    value(value?: T.Expression) { return _fs(config, pair, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pairPattern(config: T.PairPattern.Config) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    $type: 'pair_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    key(value?: T.PropertyName) { return _fs(config, pairPattern, 'key', value, fields.key); },
    value(value?: T.Pattern | T.AssignmentPattern) { return _fs(config, pairPattern, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function computedPropertyName(config: T.ComputedPropertyName.Config) {
  const fields = {
    expression: config.expression,
  };
  return {
    $type: 'computed_property_name' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, computedPropertyName, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function reservedIdentifier(text: string) {
  if (text.length === 0) throw new Error(`_reserved_identifier: text must be non-empty`);
  return {
    $type: '_reserved_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ReservedIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringFragment(text: string) {
  if (text.length === 0) throw new Error(`_string_fragment: text must be non-empty`);
  return {
    $type: '_string_fragment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function publicFieldDefinition(config: T.PublicFieldDefinition.Config) {
  const fields = {
    decorator: config.decorator,
    declare: config.declare ? "declare" as const : undefined,
    static: config.static ? "static" as const : undefined,
    readonly: config.readonly ? "readonly" as const : undefined,
    abstract: config.abstract ? "abstract" as const : undefined,
    accessor: config.accessor ? "accessor" as const : undefined,
    name: config.name,
    type: config.type,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    $type: 'public_field_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    decorator(...values: T.Decorator[]) { return _fsm(config, publicFieldDefinition, 'decorator', values, fields.decorator); },
    declare(value?: "declare" | undefined) { return _fs(config, publicFieldDefinition, 'declare', value, fields.declare); },
    static(value?: "static" | undefined) { return _fs(config, publicFieldDefinition, 'static', value, fields.static); },
    readonly(value?: "readonly" | undefined) { return _fs(config, publicFieldDefinition, 'readonly', value, fields.readonly); },
    abstract(value?: "abstract" | undefined) { return _fs(config, publicFieldDefinition, 'abstract', value, fields.abstract); },
    accessor(value?: "accessor" | undefined) { return _fs(config, publicFieldDefinition, 'accessor', value, fields.accessor); },
    name(value?: T.PropertyName) { return _fs(config, publicFieldDefinition, 'name', value, fields.name); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, publicFieldDefinition, 'type', value, fields.type); },
    value(value?: T.Expression | undefined) { return _fs(config, publicFieldDefinition, 'value', value, fields.value); },
    child(value?: (T.AccessibilityModifier | T.OverrideModifier)) {
      if (value === undefined) return children[0];
      return publicFieldDefinition({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonNullExpression(config: T.NonNullExpression.Config) {
  const fields = {
    expression: config.expression,
  };
  return {
    $type: 'non_null_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, nonNullExpression, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodSignature(config: T.MethodSignature.Config) {
  const fields = {
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier ? "static" as const : undefined,
    readonly: config.readonly ? "readonly" as const : undefined,
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  const children = config.children ?? [];
  return {
    $type: 'method_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    accessibilityModifier(value?: T.AccessibilityModifier | undefined) { return _fs(config, methodSignature, 'accessibilityModifier', value, fields.accessibility_modifier); },
    overrideModifier(value?: "static" | undefined) { return _fs(config, methodSignature, 'overrideModifier', value, fields.override_modifier); },
    readonly(value?: "readonly" | undefined) { return _fs(config, methodSignature, 'readonly', value, fields.readonly); },
    async(value?: "async" | undefined) { return _fs(config, methodSignature, 'async', value, fields.async); },
    name(value?: T.PropertyName) { return _fs(config, methodSignature, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, methodSignature, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, methodSignature, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, methodSignature, 'returnType', value, fields.return_type); },
    child(value?: T.OverrideModifier) {
      if (value === undefined) return children[0];
      return methodSignature({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractMethodSignature(config: T.AbstractMethodSignature.Config) {
  const fields = {
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier ? "override" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: 'abstract_method_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    accessibilityModifier(value?: T.AccessibilityModifier | undefined) { return _fs(config, abstractMethodSignature, 'accessibilityModifier', value, fields.accessibility_modifier); },
    overrideModifier(value?: T.OverrideModifier | undefined) { return _fs(config, abstractMethodSignature, 'overrideModifier', value, fields.override_modifier); },
    name(value?: T.PropertyName) { return _fs(config, abstractMethodSignature, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, abstractMethodSignature, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, abstractMethodSignature, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, abstractMethodSignature, 'returnType', value, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignature(config: T.FunctionSignature.Config) {
  const fields = {
    async: config.async ? "async" as const : undefined,
    name: config.name,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
    semicolon: config.semicolon,
  };
  return {
    $type: 'function_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: "async" | undefined) { return _fs(config, functionSignature, 'async', value, fields.async); },
    name(value?: T.Identifier) { return _fs(config, functionSignature, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionSignature, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, functionSignature, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, functionSignature, 'returnType', value, fields.return_type); },
    semicolon(value?: T.Semicolon | T.FunctionSignatureAutomaticSemicolon) { return _fs(config, functionSignature, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorParenthesizedExpression(child?: (T.Identifier | T.DecoratorMemberExpression | T.DecoratorCallExpression)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'decorator_parenthesized_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAssertion(config: T.TypeAssertion.Config) {
  const fields = {
    type_arguments: config.typeArguments,
    expression: config.expression,
  };
  return {
    $type: 'type_assertion' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeArguments(value?: T.TypeArguments) { return _fs(config, typeAssertion, 'typeArguments', value, fields.type_arguments); },
    expression(value?: T.Expression) { return _fs(config, typeAssertion, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asExpression(config: T.AsExpression.Config) {
  const fields = {
    expression: config.expression,
    type_annotation: config.typeAnnotation,
  };
  return {
    $type: 'as_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, asExpression, 'expression', value, fields.expression); },
    typeAnnotation(value?: "const" | T.Type) { return _fs(config, asExpression, 'typeAnnotation', value, fields.type_annotation); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function satisfiesExpression(config: T.SatisfiesExpression.Config) {
  const fields = {
    expression: config.expression,
    type_annotation: config.typeAnnotation,
  };
  return {
    $type: 'satisfies_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, satisfiesExpression, 'expression', value, fields.expression); },
    typeAnnotation(value?: T.Type) { return _fs(config, satisfiesExpression, 'typeAnnotation', value, fields.type_annotation); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function instantiationExpression(config: T.InstantiationExpression.Config) {
  const fields = {
    expression: config.expression,
    type_arguments: config.typeArguments,
  };
  return {
    $type: 'instantiation_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, instantiationExpression, 'expression', value, fields.expression); },
    typeArguments(value?: T.TypeArguments) { return _fs(config, instantiationExpression, 'typeArguments', value, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importRequireClause(config: T.ImportRequireClause.Config) {
  const fields = {
    identifier: config.identifier,
    source: config.source,
  };
  return {
    $type: 'import_require_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    identifier(value?: T.Identifier) { return _fs(config, importRequireClause, 'identifier', value, fields.identifier); },
    source(value?: T.String) { return _fs(config, importRequireClause, 'source', value, fields.source); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsClause(config: T.ExtendsClause.Config) {
  const fields = {
    value: config.value,
    type_arguments: config.typeArguments,
  };
  return {
    $type: 'extends_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    value(...values: NonEmptyArray<T.Expression>) { return _fsm(config, extendsClause, 'value', values, fields.value); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, extendsClause, 'typeArguments', value, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implementsClause(...children: T.Type[]) {
  _assertNonEmpty(children, 'implements_clause.children');
  return {
    $type: 'implements_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ambientDeclaration(config: T.AmbientDeclaration.Config) {
  const fields = {
    declaration: config.declaration,
  };
  return {
    $type: 'ambient_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    declaration(value?: T.Declaration | "global" | T.StatementBlock | "module" | T.Identifier | T.Type | T.Semicolon) { return _fs(config, ambientDeclaration, 'declaration', value, fields.declaration); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractClassDeclaration(config: T.AbstractClassDeclaration.Config) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
    body: config.body,
  };
  return {
    $type: 'abstract_class_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    decorator(...values: T.Decorator[]) { return _fsm(config, abstractClassDeclaration, 'decorator', values, fields.decorator); },
    name(value?: T.TypeIdentifier) { return _fs(config, abstractClassDeclaration, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, abstractClassDeclaration, 'typeParameters', value, fields.type_parameters); },
    classHeritage(value?: T.ClassHeritage | undefined) { return _fs(config, abstractClassDeclaration, 'classHeritage', value, fields.class_heritage); },
    body(value?: T.ClassBody) { return _fs(config, abstractClassDeclaration, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function module(config: T.Module.Config) {
  const fields = {
    name: config.name,
    body: config.body,
  };
  return {
    $type: 'module' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.String | T.Identifier | T.NestedIdentifier) { return _fs(config, module, 'name', value, fields.name); },
    body(value?: T.StatementBlock | undefined) { return _fs(config, module, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function internalModule(config: T.InternalModule.Config) {
  const fields = {
    name: config.name,
    body: config.body,
  };
  return {
    $type: 'internal_module' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.String | T.Identifier | T.NestedIdentifier) { return _fs(config, internalModule, 'name', value, fields.name); },
    body(value?: T.StatementBlock | undefined) { return _fs(config, internalModule, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAlias(config: T.ImportAlias.Config) {
  const fields = {
    name: config.name,
    value: config.value,
    semicolon: config.semicolon,
  };
  return {
    $type: 'import_alias' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier) { return _fs(config, importAlias, 'name', value, fields.name); },
    value(value?: T.Identifier | T.NestedIdentifier) { return _fs(config, importAlias, 'value', value, fields.value); },
    semicolon(value?: T.Semicolon) { return _fs(config, importAlias, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nestedTypeIdentifier(config: T.NestedTypeIdentifier.Config) {
  const fields = {
    module: config.module,
    name: config.name,
  };
  return {
    $type: 'nested_type_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    module(value?: T.Identifier | T.NestedIdentifier) { return _fs(config, nestedTypeIdentifier, 'module', value, fields.module); },
    name(value?: T.TypeIdentifier) { return _fs(config, nestedTypeIdentifier, 'name', value, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interfaceDeclaration(config: T.InterfaceDeclaration.Config) {
  const fields = {
    name: config.name,
    type_parameters: config.typeParameters,
    extends_type_clause: config.extendsTypeClause,
    body: config.body,
  };
  return {
    $type: 'interface_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.TypeIdentifier) { return _fs(config, interfaceDeclaration, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, interfaceDeclaration, 'typeParameters', value, fields.type_parameters); },
    extendsTypeClause(value?: T.ExtendsTypeClause | undefined) { return _fs(config, interfaceDeclaration, 'extendsTypeClause', value, fields.extends_type_clause); },
    body(value?: T.ObjectType) { return _fs(config, interfaceDeclaration, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsTypeClause(config: T.ExtendsTypeClause.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'extends_type_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(...values: NonEmptyArray<T.TypeIdentifier | T.NestedTypeIdentifier | T.GenericType>) { return _fsm(config, extendsTypeClause, 'type', values, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumDeclaration(config: T.EnumDeclaration.Config) {
  const fields = {
    const: config.const ? "const" as const : undefined,
    name: config.name,
    body: config.body,
  };
  return {
    $type: 'enum_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    const(value?: "const" | undefined) { return _fs(config, enumDeclaration, 'const', value, fields.const); },
    name(value?: T.Identifier) { return _fs(config, enumDeclaration, 'name', value, fields.name); },
    body(value?: T.EnumBody) { return _fs(config, enumDeclaration, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumBody(config: T.EnumBody.Config) {
  const fields = {
    opening: config.opening,
  };
  return {
    $type: 'enum_body' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    opening(...values: NonEmptyArray<T.PropertyName | T.EnumAssignment>) { return _fsm(config, enumBody, 'opening', values, fields.opening); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumAssignment(config: T.EnumAssignment.Config) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    $type: 'enum_assignment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.PropertyName) { return _fs(config, enumAssignment, 'name', value, fields.name); },
    value(value?: T.Expression) { return _fs(config, enumAssignment, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasDeclaration(config: T.TypeAliasDeclaration.Config) {
  const fields = {
    name: config.name,
    type_parameters: config.typeParameters,
    value: config.value,
    semicolon: config.semicolon,
  };
  return {
    $type: 'type_alias_declaration' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.TypeIdentifier) { return _fs(config, typeAliasDeclaration, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, typeAliasDeclaration, 'typeParameters', value, fields.type_parameters); },
    value(value?: T.Type) { return _fs(config, typeAliasDeclaration, 'value', value, fields.value); },
    semicolon(value?: T.Semicolon) { return _fs(config, typeAliasDeclaration, 'semicolon', value, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function accessibilityModifier(text: 'public' | 'private' | 'protected') {
  return {
    $type: 'accessibility_modifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.AccessibilityModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function overrideModifier() {
  return {
    $type: 'override_modifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'override' as const,
    render: () => 'override' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'override' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'override' as const },
    replace: (t: T.OverrideModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' as const }; },
  };
}

export function requiredParameter(config: T.RequiredParameter.Config) {
  const fields = {
    decorator: config.decorator,
    readonly: config.readonly ? "readonly" as const : undefined,
    pattern: config.pattern,
    type: config.type,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    $type: 'required_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    decorator(...values: T.Decorator[]) { return _fsm(config, requiredParameter, 'decorator', values, fields.decorator); },
    readonly(value?: "readonly" | undefined) { return _fs(config, requiredParameter, 'readonly', value, fields.readonly); },
    pattern(value?: T.Pattern | T.This) { return _fs(config, requiredParameter, 'pattern', value, fields.pattern); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, requiredParameter, 'type', value, fields.type); },
    value(value?: T.Expression | undefined) { return _fs(config, requiredParameter, 'value', value, fields.value); },
    child(value?: (T.AccessibilityModifier | T.OverrideModifier)) {
      if (value === undefined) return children[0];
      return requiredParameter({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalParameter(config: T.OptionalParameter.Config) {
  const fields = {
    decorator: config.decorator,
    readonly: config.readonly ? "readonly" as const : undefined,
    pattern: config.pattern,
    type: config.type,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    $type: 'optional_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    decorator(...values: T.Decorator[]) { return _fsm(config, optionalParameter, 'decorator', values, fields.decorator); },
    readonly(value?: "readonly" | undefined) { return _fs(config, optionalParameter, 'readonly', value, fields.readonly); },
    pattern(value?: T.Pattern | T.This) { return _fs(config, optionalParameter, 'pattern', value, fields.pattern); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, optionalParameter, 'type', value, fields.type); },
    value(value?: T.Expression | undefined) { return _fs(config, optionalParameter, 'value', value, fields.value); },
    child(value?: (T.AccessibilityModifier | T.OverrideModifier)) {
      if (value === undefined) return children[0];
      return optionalParameter({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function omittingTypeAnnotation(config: T.OmittingTypeAnnotation.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'omitting_type_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, omittingTypeAnnotation, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function addingTypeAnnotation(config: T.AddingTypeAnnotation.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'adding_type_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, addingTypeAnnotation, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optingTypeAnnotation(config: T.OptingTypeAnnotation.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'opting_type_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, optingTypeAnnotation, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAnnotation(config: T.TypeAnnotation.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'type_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, typeAnnotation, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asserts(child?: (T.TypePredicate | T.Identifier | T.This)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'asserts' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertsAnnotation(config: T.AssertsAnnotation.Config) {
  const fields = {
    asserts: config.asserts,
  };
  return {
    $type: 'asserts_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    asserts(value?: ":" | T.Asserts) { return _fs(config, assertsAnnotation, 'asserts', value, fields.asserts); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleParameter(config: T.TupleParameter.Config) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    $type: 'tuple_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier | T.RestPattern) { return _fs(config, tupleParameter, 'name', value, fields.name); },
    typeField(value?: T.TypeAnnotation) { return _fs(config, tupleParameter, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalTupleParameter(config: T.OptionalTupleParameter.Config) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    $type: 'optional_tuple_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier) { return _fs(config, optionalTupleParameter, 'name', value, fields.name); },
    typeField(value?: T.TypeAnnotation) { return _fs(config, optionalTupleParameter, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OptionalTupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalType(config: T.OptionalType.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'optional_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, optionalType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restType(config: T.RestType.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'rest_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, restType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructorType(config: T.ConstructorType.Config) {
  const fields = {
    abstract: config.abstract ? "abstract" as const : undefined,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    type: config.type,
  };
  return {
    $type: 'constructor_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    abstract(value?: "abstract" | undefined) { return _fs(config, constructorType, 'abstract', value, fields.abstract); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, constructorType, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, constructorType, 'parameters', value, fields.parameters); },
    typeField(value?: T.Type) { return _fs(config, constructorType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateType(child?: (T.PrimaryType | T.InferType)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'template_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TemplateTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateLiteralType(...children: (T.StringFragment | T.TemplateType)[]) {
  return {
    $type: 'template_literal_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function inferType(config: T.InferType.Config) {
  const fields = {
    type_identifier: config.typeIdentifier,
    constraint: config.constraint,
  };
  return {
    $type: 'infer_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeIdentifier(value?: T.TypeIdentifier) { return _fs(config, inferType, 'typeIdentifier', value, fields.type_identifier); },
    constraint(value?: "extends" | T.Type | undefined) { return _fs(config, inferType, 'constraint', value, fields.constraint); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalType(config: T.ConditionalType.Config) {
  const fields = {
    left: config.left,
    right: config.right,
    consequence: config.consequence,
    alternative: config.alternative,
  };
  return {
    $type: 'conditional_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Type) { return _fs(config, conditionalType, 'left', value, fields.left); },
    right(value?: T.Type) { return _fs(config, conditionalType, 'right', value, fields.right); },
    consequence(value?: T.Type) { return _fs(config, conditionalType, 'consequence', value, fields.consequence); },
    alternative(value?: T.Type) { return _fs(config, conditionalType, 'alternative', value, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: T.GenericType.Config) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
  };
  return {
    $type: 'generic_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.TypeIdentifier | T.NestedTypeIdentifier) { return _fs(config, genericType, 'name', value, fields.name); },
    typeArguments(value?: T.TypeArguments) { return _fs(config, genericType, 'typeArguments', value, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicate(config: T.TypePredicate.Config) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    $type: 'type_predicate' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier | T.This) { return _fs(config, typePredicate, 'name', value, fields.name); },
    typeField(value?: T.Type) { return _fs(config, typePredicate, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicateAnnotation(config: T.TypePredicateAnnotation.Config) {
  const fields = {
    type_predicate: config.typePredicate,
  };
  return {
    $type: 'type_predicate_annotation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typePredicate(value?: ":" | T.TypePredicate) { return _fs(config, typePredicateAnnotation, 'typePredicate', value, fields.type_predicate); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeQuery(child?: (T.TypeQuerySubscriptExpression | T.TypeQueryMemberExpression | T.TypeQueryCallExpression | T.TypeQueryInstantiationExpression | T.Identifier | T.This)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'type_query' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexTypeQuery(config: T.IndexTypeQuery.Config) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    $type: 'index_type_query' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryType(value?: T.PrimaryType) { return _fs(config, indexTypeQuery, 'primaryType', value, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lookupType(config: T.LookupType.Config) {
  const fields = {
    primary_type: config.primaryType,
    index_type: config.indexType,
  };
  return {
    $type: 'lookup_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryType(value?: T.PrimaryType) { return _fs(config, lookupType, 'primaryType', value, fields.primary_type); },
    indexType(value?: T.Type) { return _fs(config, lookupType, 'indexType', value, fields.index_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mappedTypeClause(config: T.MappedTypeClause.Config) {
  const fields = {
    name: config.name,
    type: config.type,
    alias: config.alias,
  };
  return {
    $type: 'mapped_type_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.TypeIdentifier) { return _fs(config, mappedTypeClause, 'name', value, fields.name); },
    typeField(value?: T.Type) { return _fs(config, mappedTypeClause, 'type', value, fields.type); },
    alias(value?: T.Type | undefined) { return _fs(config, mappedTypeClause, 'alias', value, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function literalType(child?: (T._Number | T.Number | T.String | T.True | T.False | T.Null | T.Undefined)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'literal_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function flowMaybeType(config: T.FlowMaybeType.Config) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    $type: 'flow_maybe_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryType(value?: T.PrimaryType) { return _fs(config, flowMaybeType, 'primaryType', value, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedType(config: T.ParenthesizedType.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'parenthesized_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, parenthesizedType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function predefinedType(text: string) {
  if (text.length === 0) throw new Error(`predefined_type: text must be non-empty`);
  return {
    $type: 'predefined_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PredefinedTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeArguments(...children: T.Type[]) {
  _assertNonEmpty(children, 'type_arguments.children');
  return {
    $type: 'type_arguments' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectType(config: T.ObjectType.Config) {
  const fields = {
    opening: config.opening,
    members: config.members,
    closing: config.closing,
  };
  return {
    $type: 'object_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    opening(value?: "{" | "{|") { return _fs(config, objectType, 'opening', value, fields.opening); },
    members(...values: NonEmptyArray<"," | ";" | T.ExportStatement | T.PropertySignature | T.CallSignature | T.ConstructSignature | T.IndexSignature | T.MethodSignature | T.Semicolon>) { return _fsm(config, objectType, 'members', values, fields.members); },
    closing(value?: "}" | "|}") { return _fs(config, objectType, 'closing', value, fields.closing); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectTypeCurly(config?: T.ObjectTypeCurly.Config) {
  const fields = {
    opening: "{" as const,
    members: config?.members,
    closing: "}" as const,
  };
  return {
    $type: 'object_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectTypeFlow(config?: T.ObjectTypeFlow.Config) {
  const fields = {
    opening: "{|" as const,
    members: config?.members,
    closing: "|}" as const,
  };
  return {
    $type: 'object_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callSignature(config: T.CallSignature.Config) {
  const fields = {
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: 'call_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, callSignature, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, callSignature, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, callSignature, 'returnType', value, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertySignature(config: T.PropertySignature.Config) {
  const fields = {
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier ? "static" as const : undefined,
    readonly: config.readonly ? "readonly" as const : undefined,
    name: config.name,
    type: config.type,
  };
  const children = config.children ?? [];
  return {
    $type: 'property_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    accessibilityModifier(value?: T.AccessibilityModifier | undefined) { return _fs(config, propertySignature, 'accessibilityModifier', value, fields.accessibility_modifier); },
    overrideModifier(value?: "static" | undefined) { return _fs(config, propertySignature, 'overrideModifier', value, fields.override_modifier); },
    readonly(value?: "readonly" | undefined) { return _fs(config, propertySignature, 'readonly', value, fields.readonly); },
    name(value?: T.PropertyName) { return _fs(config, propertySignature, 'name', value, fields.name); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, propertySignature, 'type', value, fields.type); },
    child(value?: T.OverrideModifier) {
      if (value === undefined) return children[0];
      return propertySignature({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: T.TypeParameter[]) {
  _assertNonEmpty(children, 'type_parameters.children');
  return {
    $type: 'type_parameters' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: T.TypeParameter.Config) {
  const fields = {
    const: config.const ? "const" as const : undefined,
    name: config.name,
    constraint: config.constraint,
    value: config.value,
  };
  return {
    $type: 'type_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    const(value?: "const" | undefined) { return _fs(config, typeParameter, 'const', value, fields.const); },
    name(value?: T.TypeIdentifier) { return _fs(config, typeParameter, 'name', value, fields.name); },
    constraint(value?: T.Constraint | undefined) { return _fs(config, typeParameter, 'constraint', value, fields.constraint); },
    value(value?: T.DefaultType | undefined) { return _fs(config, typeParameter, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultType(config: T.DefaultType.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'default_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, defaultType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constraint(config: T.Constraint.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'constraint' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, constraint, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructSignature(config: T.ConstructSignature.Config) {
  const fields = {
    abstract: config.abstract ? "abstract" as const : undefined,
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    type: config.type,
  };
  return {
    $type: 'construct_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    abstract(value?: "abstract" | undefined) { return _fs(config, constructSignature, 'abstract', value, fields.abstract); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, constructSignature, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, constructSignature, 'parameters', value, fields.parameters); },
    typeField(value?: T.TypeAnnotation | undefined) { return _fs(config, constructSignature, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignature(config: ConfigOf<T.IndexSignatureUFormColon>): ReturnType<typeof indexSignatureUFormColon>;
export function indexSignature(config: ConfigOf<T.IndexSignatureUFormMappedTypeClause>): ReturnType<typeof indexSignatureUFormMappedTypeClause>;
export function indexSignature(config: ConfigOf<T.IndexSignatureUFormColon> | ConfigOf<T.IndexSignatureUFormMappedTypeClause>) {
  switch (config.$variant) {
    case 'colon': return indexSignatureUFormColon(config as Parameters<typeof indexSignatureUFormColon>[0]);
    case 'mapped_type_clause': return indexSignatureUFormMappedTypeClause(config as Parameters<typeof indexSignatureUFormMappedTypeClause>[0]);
  }
  throw new Error(`indexSignature: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'colon' | 'mapped_type_clause'.`);
}
export function indexSignatureUFormColon(config: Omit<ConfigOf<T.IndexSignatureUFormColon>, '$variant'>) {
  const fields = {
    sign: config.sign,
    type: config.type,
  };
  const children = config.children ?? [];
  return {
    $type: 'index_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'colon' as const,
    $fields: fields,
    $children: children,
    sign(value?: "-" | "+" | undefined) { return _fs(config, indexSignatureUFormColon, 'sign', value, fields.sign); },
    typeField(value?: T.TypeAnnotation | T.OmittingTypeAnnotation | T.AddingTypeAnnotation | T.OptingTypeAnnotation) { return _fs(config, indexSignatureUFormColon, 'type', value, fields.type); },
    child(value?: T.IndexSignatureColon) {
      if (value === undefined) return children[0];
      return indexSignatureUFormColon({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IndexSignatureUFormColonTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function indexSignatureUFormMappedTypeClause(config: Omit<ConfigOf<T.IndexSignatureUFormMappedTypeClause>, '$variant'>) {
  const fields = {
    sign: config.sign,
    type: config.type,
  };
  const children = config.children ?? [];
  return {
    $type: 'index_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'mapped_type_clause' as const,
    $fields: fields,
    $children: children,
    sign(value?: "-" | "+" | undefined) { return _fs(config, indexSignatureUFormMappedTypeClause, 'sign', value, fields.sign); },
    typeField(value?: T.TypeAnnotation | T.OmittingTypeAnnotation | T.AddingTypeAnnotation | T.OptingTypeAnnotation) { return _fs(config, indexSignatureUFormMappedTypeClause, 'type', value, fields.type); },
    child(value?: T.IndexSignatureMappedTypeClause) {
      if (value === undefined) return children[0];
      return indexSignatureUFormMappedTypeClause({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IndexSignatureUFormMappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: T.ArrayType.Config) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    $type: 'array_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryType(value?: T.PrimaryType) { return _fs(config, arrayType, 'primaryType', value, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: T.TupleTypeMember[]) {
  return {
    $type: 'tuple_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function readonlyType(config: T.ReadonlyType.Config) {
  const fields = {
    type: config.type,
  };
  return {
    $type: 'readonly_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, readonlyType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: T.UnionType.Config) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'union_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Type | undefined) { return _fs(config, unionType, 'left', value, fields.left); },
    right(value?: T.Type) { return _fs(config, unionType, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function intersectionType(config: T.IntersectionType.Config) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    $type: 'intersection_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Type | undefined) { return _fs(config, intersectionType, 'left', value, fields.left); },
    right(value?: T.Type) { return _fs(config, intersectionType, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: T.FunctionType.Config) {
  const fields = {
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: 'function_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionType, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, functionType, 'parameters', value, fields.parameters); },
    returnType(value?: T.Type | T.Asserts | T.TypePredicate) { return _fs(config, functionType, 'returnType', value, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeIdentifier(child: T.Identifier) {
  const children = [child];
  return {
    $type: '_type_identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunctionParameter(config: T.ArrowFunctionParameter.Config) {
  const fields = {
    parameter: config.parameter,
  };
  return {
    $type: '_arrow_function_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    parameter(value?: T.ReservedIdentifier) { return _fs(config, arrowFunctionParameter, 'parameter', value, fields.parameter); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrowFunctionParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunctionUCallSignature(config: T.ArrowFunctionUCallSignature.Config) {
  const fields = {
    type_parameters: config.typeParameters,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    $type: '_arrow_function__call_signature' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, arrowFunctionUCallSignature, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.FormalParameters) { return _fs(config, arrowFunctionUCallSignature, 'parameters', value, fields.parameters); },
    returnType(value?: T.TypeAnnotation | T.AssertsAnnotation | T.TypePredicateAnnotation | undefined) { return _fs(config, arrowFunctionUCallSignature, 'returnType', value, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrowFunctionUCallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritageExtendsClause(child: (T.ExtendsClause | T.ImplementsClause)) {
  const children = [child];
  return {
    $type: '_class_heritage_extends_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassHeritageExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritageImplementsClause(child: T.ImplementsClause) {
  const children = [child];
  return {
    $type: '_class_heritage_implements_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassHeritageImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClauseNamespaceImport(child: T.NamespaceImport) {
  const children = [child];
  return {
    $type: '_import_clause_namespace_import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseNamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClauseNamedImports(child: T.NamedImports) {
  const children = [child];
  return {
    $type: '_import_clause_named_imports' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseNamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClauseDefaultImport(child: (T.ImportIdentifier | T.NamespaceImport | T.NamedImports)) {
  const children = [child];
  return {
    $type: '_import_clause_default_import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseDefaultImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifierName(config: T.ImportSpecifierName.Config) {
  const fields = {
    name: config.name,
  };
  return {
    $type: '_import_specifier_name' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.ImportIdentifier) { return _fs(config, importSpecifierName, 'name', value, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportSpecifierNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignatureMappedTypeClause(child: T.MappedTypeClause) {
  const children = [child];
  return {
    $type: '_index_signature_mapped_type_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IndexSignatureMappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpressionSequence(child: T.SequenceExpression) {
  const children = [child];
  return {
    $type: '_parenthesized_expression_sequence' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedExpressionSequenceTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementDefault(config: ConfigOf<T.ExportStatementDefaultForm0>): ReturnType<typeof exportStatementDefaultForm0>;
export function exportStatementDefault(config: ConfigOf<T.ExportStatementDefaultForm1>): ReturnType<typeof exportStatementDefaultForm1>;
export function exportStatementDefault(config: ConfigOf<T.ExportStatementDefaultForm0> | ConfigOf<T.ExportStatementDefaultForm1>) {
  switch (config.$variant) {
    case 'form0': return exportStatementDefaultForm0(config as Parameters<typeof exportStatementDefaultForm0>[0]);
    case 'form1': return exportStatementDefaultForm1(config as Parameters<typeof exportStatementDefaultForm1>[0]);
  }
  throw new Error(`exportStatementDefault: unknown $variant '${(config as { $variant?: string }).$variant}' — expected one of 'form0' | 'form1'.`);
}
export function exportStatementDefaultForm0(config: Omit<ConfigOf<T.ExportStatementDefaultForm0>, '$variant'>) {
  const children = config.children ?? [];
  return {
    $type: '_export_statement_default' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'form0' as const,
    $children: children,
    child(value?: (T.FromClause | T.NamespaceExport | T.ExportClause | T.Semicolon)) {
      if (value === undefined) return children[0];
      return exportStatementDefaultForm0({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementDefaultForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementDefaultForm1(config: Omit<ConfigOf<T.ExportStatementDefaultForm1>, '$variant'>) {
  const fields = {
    decorator: config.decorator,
    declaration: config.declaration,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    $type: '_export_statement_default' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: 'form1' as const,
    $fields: fields,
    $children: children,
    decorator(...values: T.Decorator[]) { return _fsm(config, exportStatementDefaultForm1, 'decorator', values, fields.decorator); },
    declaration(value?: T.Declaration) { return _fs(config, exportStatementDefaultForm1, 'declaration', value, fields.declaration); },
    value(value?: T.Expression | undefined) { return _fs(config, exportStatementDefaultForm1, 'value', value, fields.value); },
    child(value?: T.Semicolon) {
      if (value === undefined) return children[0];
      return exportStatementDefaultForm1({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementDefaultForm1Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementTypeExport(config: T.ExportStatementTypeExport.Config) {
  const fields = {
    source: config.source,
  };
  const children = config.children ?? [];
  return {
    $type: '_export_statement_type_export' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    source(value?: T.String | undefined) { return _fs(config, exportStatementTypeExport, 'source', value, fields.source); },
    child(value?: (T.ExportClause | T.Semicolon)) {
      if (value === undefined) return children[0];
      return exportStatementTypeExport({ ...config, children: [value] });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementTypeExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementEqualsExport(child: (T.Expression | T.Semicolon)) {
  const children = [child];
  return {
    $type: '_export_statement_equals_export' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementEqualsExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportStatementNamespaceExport(child: (T.Identifier | T.Semicolon)) {
  const children = [child];
  return {
    $type: '_export_statement_namespace_export' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementNamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringDouble(...children: (T.UnescapedDoubleStringFragment | T.EscapeSequence)[]) {
  return {
    $type: '_string_double' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringDoubleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringSingle(...children: (T.UnescapedSingleStringFragment | T.EscapeSequence)[]) {
  return {
    $type: '_string_single' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringSingleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function automaticSemicolon(text: string) {
  if (text.length === 0) throw new Error(`_automatic_semicolon: text must be non-empty`);
  return {
    $type: '_automatic_semicolon' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.AutomaticSemicolonTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function htmlComment(text: string) {
  if (text.length === 0) throw new Error(`html_comment: text must be non-empty`);
  return {
    $type: 'html_comment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HtmlCommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function oror(text: string) {
  if (text.length === 0) throw new Error(`||: text must be non-empty`);
  return {
    $type: '||' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.OrorTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxText(text: string) {
  if (text.length === 0) throw new Error(`jsx_text: text must be non-empty`);
  return {
    $type: 'jsx_text' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.JsxTextTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function functionSignatureAutomaticSemicolon(text: string) {
  if (text.length === 0) throw new Error(`_function_signature_automatic_semicolon: text must be non-empty`);
  return {
    $type: '_function_signature_automatic_semicolon' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FunctionSignatureAutomaticSemicolonTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "program": FluentNode<"program", T.Program.Config>;
  "hash_bang_line": T.HashBangLine;
  "export_statement": FluentNode<"export_statement", T.ExportStatement.Config>;
  "namespace_export": FluentNode<"namespace_export", T.NamespaceExport.Config>;
  "export_clause": FluentNode<"export_clause", T.ExportClause.Config>;
  "export_specifier": FluentNode<"export_specifier", T.ExportSpecifier.Config>;
  "declaration": FluentNode<"declaration", T.Declaration.Config>;
  "import": T.Import;
  "import_statement": FluentNode<"import_statement", T.ImportStatement.Config>;
  "import_clause": FluentNode<"import_clause", T.ImportClause.Config>;
  "namespace_import": FluentNode<"namespace_import", T.NamespaceImport.Config>;
  "named_imports": FluentNode<"named_imports", T.NamedImports.Config>;
  "import_specifier": FluentNode<"import_specifier", T.ImportSpecifier.Config>;
  "import_attribute": FluentNode<"import_attribute", T.ImportAttribute.Config>;
  "statement": FluentNode<"statement", T.Statement.Config>;
  "expression_statement": FluentNode<"expression_statement", T.ExpressionStatement.Config>;
  "variable_declaration": FluentNode<"variable_declaration", T.VariableDeclaration.Config>;
  "lexical_declaration": FluentNode<"lexical_declaration", T.LexicalDeclaration.Config>;
  "variable_declarator": FluentNode<"variable_declarator", T.VariableDeclarator.Config>;
  "statement_block": FluentNode<"statement_block", T.StatementBlock.Config>;
  "else_clause": FluentNode<"else_clause", T.ElseClause.Config>;
  "if_statement": FluentNode<"if_statement", T.IfStatement.Config>;
  "switch_statement": FluentNode<"switch_statement", T.SwitchStatement.Config>;
  "for_statement": FluentNode<"for_statement", T.ForStatement.Config>;
  "for_in_statement": FluentNode<"for_in_statement", T.ForInStatement.Config>;
  "while_statement": FluentNode<"while_statement", T.WhileStatement.Config>;
  "do_statement": FluentNode<"do_statement", T.DoStatement.Config>;
  "try_statement": FluentNode<"try_statement", T.TryStatement.Config>;
  "with_statement": FluentNode<"with_statement", T.WithStatement.Config>;
  "break_statement": FluentNode<"break_statement", T.BreakStatement.Config>;
  "continue_statement": FluentNode<"continue_statement", T.ContinueStatement.Config>;
  "debugger_statement": FluentNode<"debugger_statement", T.DebuggerStatement.Config>;
  "return_statement": FluentNode<"return_statement", T.ReturnStatement.Config>;
  "throw_statement": FluentNode<"throw_statement", T.ThrowStatement.Config>;
  "labeled_statement": FluentNode<"labeled_statement", T.LabeledStatement.Config>;
  "switch_body": FluentNode<"switch_body", T.SwitchBody.Config>;
  "switch_case": FluentNode<"switch_case", T.SwitchCase.Config>;
  "switch_default": FluentNode<"switch_default", T.SwitchDefault.Config>;
  "catch_clause": FluentNode<"catch_clause", T.CatchClause.Config>;
  "finally_clause": FluentNode<"finally_clause", T.FinallyClause.Config>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", T.ParenthesizedExpression.Config>;
  "expression": FluentNode<"expression", T.Expression.Config>;
  "primary_expression": FluentNode<"primary_expression", T.PrimaryExpression.Config>;
  "yield_expression": FluentNode<"yield_expression", T.YieldExpression.Config>;
  "object": FluentNode<"object", T.Object.Config>;
  "object_pattern": FluentNode<"object_pattern", T.ObjectPattern.Config>;
  "assignment_pattern": FluentNode<"assignment_pattern", T.AssignmentPattern.Config>;
  "object_assignment_pattern": FluentNode<"object_assignment_pattern", T.ObjectAssignmentPattern.Config>;
  "array": FluentNode<"array", T.Array.Config>;
  "array_pattern": FluentNode<"array_pattern", T.ArrayPattern.Config>;
  "jsx_element": FluentNode<"jsx_element", T.JsxElement.Config>;
  "html_character_reference": T.HtmlCharacterReference;
  "jsx_expression": FluentNode<"jsx_expression", T.JsxExpression.Config>;
  "jsx_opening_element": FluentNode<"jsx_opening_element", T.JsxOpeningElement.Config>;
  "jsx_identifier": T.JsxIdentifier;
  "nested_identifier": FluentNode<"nested_identifier", T.NestedIdentifier.Config>;
  "jsx_namespace_name": FluentNode<"jsx_namespace_name", T.JsxNamespaceName.Config>;
  "jsx_closing_element": FluentNode<"jsx_closing_element", T.JsxClosingElement.Config>;
  "jsx_self_closing_element": FluentNode<"jsx_self_closing_element", T.JsxSelfClosingElement.Config>;
  "jsx_attribute": FluentNode<"jsx_attribute", T.JsxAttribute.Config>;
  "unescaped_double_jsx_string_fragment": T.UnescapedDoubleJsxStringFragment;
  "unescaped_single_jsx_string_fragment": T.UnescapedSingleJsxStringFragment;
  "class": FluentNode<"class", T.Class.Config>;
  "class_declaration": FluentNode<"class_declaration", T.ClassDeclaration.Config>;
  "class_heritage": FluentNode<"class_heritage", T.ClassHeritage.Config>;
  "function_expression": FluentNode<"function_expression", T.FunctionExpression.Config>;
  "function_declaration": FluentNode<"function_declaration", T.FunctionDeclaration.Config>;
  "generator_function": FluentNode<"generator_function", T.GeneratorFunction.Config>;
  "generator_function_declaration": FluentNode<"generator_function_declaration", T.GeneratorFunctionDeclaration.Config>;
  "arrow_function": FluentNode<"arrow_function", T.ArrowFunction.Config>;
  "call_expression": FluentNode<"call_expression", T.CallExpression.Config>;
  "new_expression": FluentNode<"new_expression", T.NewExpression.Config>;
  "await_expression": FluentNode<"await_expression", T.AwaitExpression.Config>;
  "member_expression": FluentNode<"member_expression", T.MemberExpression.Config>;
  "subscript_expression": FluentNode<"subscript_expression", T.SubscriptExpression.Config>;
  "_lhs_expression": FluentNode<"_lhs_expression", T.LhsExpression.Config>;
  "assignment_expression": FluentNode<"assignment_expression", T.AssignmentExpression.Config>;
  "augmented_assignment_expression": FluentNode<"augmented_assignment_expression", T.AugmentedAssignmentExpression.Config>;
  "spread_element": FluentNode<"spread_element", T.SpreadElement.Config>;
  "ternary_expression": FluentNode<"ternary_expression", T.TernaryExpression.Config>;
  "binary_expression": FluentNode<"binary_expression", T.BinaryExpression.Config>;
  "unary_expression": FluentNode<"unary_expression", T.UnaryExpression.Config>;
  "update_expression": FluentNode<"update_expression", T.UpdateExpression.Config>;
  "sequence_expression": FluentNode<"sequence_expression", T.SequenceExpression.Config>;
  "string": FluentNode<"string", T.String.Config>;
  "unescaped_double_string_fragment": T.UnescapedDoubleStringFragment;
  "unescaped_single_string_fragment": T.UnescapedSingleStringFragment;
  "escape_sequence": T.EscapeSequence;
  "comment": T.Comment;
  "template_string": FluentNode<"template_string", T.TemplateString.Config>;
  "template_substitution": FluentNode<"template_substitution", T.TemplateSubstitution.Config>;
  "regex": FluentNode<"regex", T.Regex.Config>;
  "regex_pattern": T.RegexPattern;
  "regex_flags": T.RegexFlags;
  "number": T.Number;
  "identifier": T.Identifier;
  "private_property_identifier": T.PrivatePropertyIdentifier;
  "meta_property": T.MetaProperty;
  "this": T.This;
  "super": T.Super;
  "true": T.True;
  "false": T.False;
  "null": T.Null;
  "undefined": T.Undefined;
  "arguments": FluentNode<"arguments", T.Arguments.Config>;
  "decorator": FluentNode<"decorator", T.Decorator.Config>;
  "decorator_member_expression": FluentNode<"decorator_member_expression", T.DecoratorMemberExpression.Config>;
  "decorator_call_expression": FluentNode<"decorator_call_expression", T.DecoratorCallExpression.Config>;
  "class_body": FluentNode<"class_body", T.ClassBody.Config>;
  "field_definition": FluentNode<"field_definition", T.FieldDefinition.Config>;
  "formal_parameters": FluentNode<"formal_parameters", T.FormalParameters.Config>;
  "class_static_block": FluentNode<"class_static_block", T.ClassStaticBlock.Config>;
  "pattern": FluentNode<"pattern", T.Pattern.Config>;
  "rest_pattern": FluentNode<"rest_pattern", T.RestPattern.Config>;
  "method_definition": FluentNode<"method_definition", T.MethodDefinition.Config>;
  "pair": FluentNode<"pair", T.Pair.Config>;
  "pair_pattern": FluentNode<"pair_pattern", T.PairPattern.Config>;
  "computed_property_name": FluentNode<"computed_property_name", T.ComputedPropertyName.Config>;
  "_reserved_identifier": T.ReservedIdentifier;
  "_string_fragment": T.StringFragment;
  "public_field_definition": FluentNode<"public_field_definition", T.PublicFieldDefinition.Config>;
  "non_null_expression": FluentNode<"non_null_expression", T.NonNullExpression.Config>;
  "method_signature": FluentNode<"method_signature", T.MethodSignature.Config>;
  "abstract_method_signature": FluentNode<"abstract_method_signature", T.AbstractMethodSignature.Config>;
  "function_signature": FluentNode<"function_signature", T.FunctionSignature.Config>;
  "decorator_parenthesized_expression": FluentNode<"decorator_parenthesized_expression", T.DecoratorParenthesizedExpression.Config>;
  "type_assertion": FluentNode<"type_assertion", T.TypeAssertion.Config>;
  "as_expression": FluentNode<"as_expression", T.AsExpression.Config>;
  "satisfies_expression": FluentNode<"satisfies_expression", T.SatisfiesExpression.Config>;
  "instantiation_expression": FluentNode<"instantiation_expression", T.InstantiationExpression.Config>;
  "import_require_clause": FluentNode<"import_require_clause", T.ImportRequireClause.Config>;
  "extends_clause": FluentNode<"extends_clause", T.ExtendsClause.Config>;
  "implements_clause": FluentNode<"implements_clause", T.ImplementsClause.Config>;
  "ambient_declaration": FluentNode<"ambient_declaration", T.AmbientDeclaration.Config>;
  "abstract_class_declaration": FluentNode<"abstract_class_declaration", T.AbstractClassDeclaration.Config>;
  "module": FluentNode<"module", T.Module.Config>;
  "internal_module": FluentNode<"internal_module", T.InternalModule.Config>;
  "import_alias": FluentNode<"import_alias", T.ImportAlias.Config>;
  "nested_type_identifier": FluentNode<"nested_type_identifier", T.NestedTypeIdentifier.Config>;
  "interface_declaration": FluentNode<"interface_declaration", T.InterfaceDeclaration.Config>;
  "extends_type_clause": FluentNode<"extends_type_clause", T.ExtendsTypeClause.Config>;
  "enum_declaration": FluentNode<"enum_declaration", T.EnumDeclaration.Config>;
  "enum_body": FluentNode<"enum_body", T.EnumBody.Config>;
  "enum_assignment": FluentNode<"enum_assignment", T.EnumAssignment.Config>;
  "type_alias_declaration": FluentNode<"type_alias_declaration", T.TypeAliasDeclaration.Config>;
  "accessibility_modifier": T.AccessibilityModifier;
  "override_modifier": T.OverrideModifier;
  "required_parameter": FluentNode<"required_parameter", T.RequiredParameter.Config>;
  "optional_parameter": FluentNode<"optional_parameter", T.OptionalParameter.Config>;
  "omitting_type_annotation": FluentNode<"omitting_type_annotation", T.OmittingTypeAnnotation.Config>;
  "adding_type_annotation": FluentNode<"adding_type_annotation", T.AddingTypeAnnotation.Config>;
  "opting_type_annotation": FluentNode<"opting_type_annotation", T.OptingTypeAnnotation.Config>;
  "type_annotation": FluentNode<"type_annotation", T.TypeAnnotation.Config>;
  "asserts": FluentNode<"asserts", T.Asserts.Config>;
  "asserts_annotation": FluentNode<"asserts_annotation", T.AssertsAnnotation.Config>;
  "tuple_parameter": FluentNode<"tuple_parameter", T.TupleParameter.Config>;
  "optional_tuple_parameter": FluentNode<"optional_tuple_parameter", T.OptionalTupleParameter.Config>;
  "optional_type": FluentNode<"optional_type", T.OptionalType.Config>;
  "rest_type": FluentNode<"rest_type", T.RestType.Config>;
  "constructor_type": FluentNode<"constructor_type", T.ConstructorType.Config>;
  "template_type": FluentNode<"template_type", T.TemplateType.Config>;
  "template_literal_type": FluentNode<"template_literal_type", T.TemplateLiteralType.Config>;
  "infer_type": FluentNode<"infer_type", T.InferType.Config>;
  "conditional_type": FluentNode<"conditional_type", T.ConditionalType.Config>;
  "generic_type": FluentNode<"generic_type", T.GenericType.Config>;
  "type_predicate": FluentNode<"type_predicate", T.TypePredicate.Config>;
  "type_predicate_annotation": FluentNode<"type_predicate_annotation", T.TypePredicateAnnotation.Config>;
  "type_query": FluentNode<"type_query", T.TypeQuery.Config>;
  "index_type_query": FluentNode<"index_type_query", T.IndexTypeQuery.Config>;
  "lookup_type": FluentNode<"lookup_type", T.LookupType.Config>;
  "mapped_type_clause": FluentNode<"mapped_type_clause", T.MappedTypeClause.Config>;
  "literal_type": FluentNode<"literal_type", T.LiteralType.Config>;
  "flow_maybe_type": FluentNode<"flow_maybe_type", T.FlowMaybeType.Config>;
  "parenthesized_type": FluentNode<"parenthesized_type", T.ParenthesizedType.Config>;
  "predefined_type": T.PredefinedType;
  "type_arguments": FluentNode<"type_arguments", T.TypeArguments.Config>;
  "object_type": FluentNode<"object_type", T.ObjectType.Config>;
  "call_signature": FluentNode<"call_signature", T.CallSignature.Config>;
  "property_signature": FluentNode<"property_signature", T.PropertySignature.Config>;
  "type_parameters": FluentNode<"type_parameters", T.TypeParameters.Config>;
  "type_parameter": FluentNode<"type_parameter", T.TypeParameter.Config>;
  "default_type": FluentNode<"default_type", T.DefaultType.Config>;
  "constraint": FluentNode<"constraint", T.Constraint.Config>;
  "construct_signature": FluentNode<"construct_signature", T.ConstructSignature.Config>;
  "index_signature": FluentNode<"index_signature", T.IndexSignature.Config>;
  "array_type": FluentNode<"array_type", T.ArrayType.Config>;
  "tuple_type": FluentNode<"tuple_type", T.TupleType.Config>;
  "readonly_type": FluentNode<"readonly_type", T.ReadonlyType.Config>;
  "union_type": FluentNode<"union_type", T.UnionType.Config>;
  "intersection_type": FluentNode<"intersection_type", T.IntersectionType.Config>;
  "function_type": FluentNode<"function_type", T.FunctionType.Config>;
  "_type_identifier": FluentNode<"_type_identifier", T.TypeIdentifier.Config>;
  "_arrow_function_parameter": FluentNode<"_arrow_function_parameter", T.ArrowFunctionParameter.Config>;
  "_arrow_function__call_signature": FluentNode<"_arrow_function__call_signature", T.ArrowFunctionUCallSignature.Config>;
  "_class_heritage_extends_clause": FluentNode<"_class_heritage_extends_clause", T.ClassHeritageExtendsClause.Config>;
  "_class_heritage_implements_clause": FluentNode<"_class_heritage_implements_clause", T.ClassHeritageImplementsClause.Config>;
  "_import_clause_namespace_import": FluentNode<"_import_clause_namespace_import", T.ImportClauseNamespaceImport.Config>;
  "_import_clause_named_imports": FluentNode<"_import_clause_named_imports", T.ImportClauseNamedImports.Config>;
  "_import_clause_default_import": FluentNode<"_import_clause_default_import", T.ImportClauseDefaultImport.Config>;
  "_import_specifier_name": FluentNode<"_import_specifier_name", T.ImportSpecifierName.Config>;
  "_index_signature_mapped_type_clause": FluentNode<"_index_signature_mapped_type_clause", T.IndexSignatureMappedTypeClause.Config>;
  "_parenthesized_expression_sequence": FluentNode<"_parenthesized_expression_sequence", T.ParenthesizedExpressionSequence.Config>;
  "_export_statement_default": FluentNode<"_export_statement_default", T.ExportStatementDefault.Config>;
  "_export_statement_type_export": FluentNode<"_export_statement_type_export", T.ExportStatementTypeExport.Config>;
  "_export_statement_equals_export": FluentNode<"_export_statement_equals_export", T.ExportStatementEqualsExport.Config>;
  "_export_statement_namespace_export": FluentNode<"_export_statement_namespace_export", T.ExportStatementNamespaceExport.Config>;
  "_string_double": FluentNode<"_string_double", T.StringDouble.Config>;
  "_string_single": FluentNode<"_string_single", T.StringSingle.Config>;
  "_automatic_semicolon": T.AutomaticSemicolon;
  "html_comment": T.HtmlComment;
  "||": T.Oror;
  "jsx_text": T.JsxText;
  "_function_signature_automatic_semicolon": T.FunctionSignatureAutomaticSemicolon;
};

export const _factoryMap = {
  "program": program,
  "hash_bang_line": hashBangLine,
  "export_statement": exportStatement,
  "namespace_export": namespaceExport,
  "export_clause": exportClause,
  "export_specifier": exportSpecifier,
  "declaration": declaration,
  "import": import_,
  "import_statement": importStatement,
  "import_clause": importClause,
  "namespace_import": namespaceImport,
  "named_imports": namedImports,
  "import_specifier": importSpecifier,
  "import_attribute": importAttribute,
  "statement": statement,
  "expression_statement": expressionStatement,
  "variable_declaration": variableDeclaration,
  "lexical_declaration": lexicalDeclaration,
  "variable_declarator": variableDeclarator,
  "statement_block": statementBlock,
  "else_clause": elseClause,
  "if_statement": ifStatement,
  "switch_statement": switchStatement,
  "for_statement": forStatement,
  "for_in_statement": forInStatement,
  "while_statement": whileStatement,
  "do_statement": doStatement,
  "try_statement": tryStatement,
  "with_statement": withStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "debugger_statement": debuggerStatement,
  "return_statement": returnStatement,
  "throw_statement": throwStatement,
  "labeled_statement": labeledStatement,
  "switch_body": switchBody,
  "switch_case": switchCase,
  "switch_default": switchDefault,
  "catch_clause": catchClause,
  "finally_clause": finallyClause,
  "parenthesized_expression": parenthesizedExpression,
  "expression": expression,
  "primary_expression": primaryExpression,
  "yield_expression": yieldExpression,
  "object": object,
  "object_pattern": objectPattern,
  "assignment_pattern": assignmentPattern,
  "object_assignment_pattern": objectAssignmentPattern,
  "array": array,
  "array_pattern": arrayPattern,
  "jsx_element": jsxElement,
  "html_character_reference": htmlCharacterReference,
  "jsx_expression": jsxExpression,
  "jsx_opening_element": jsxOpeningElement,
  "jsx_identifier": jsxIdentifier,
  "nested_identifier": nestedIdentifier,
  "jsx_namespace_name": jsxNamespaceName,
  "jsx_closing_element": jsxClosingElement,
  "jsx_self_closing_element": jsxSelfClosingElement,
  "jsx_attribute": jsxAttribute,
  "unescaped_double_jsx_string_fragment": unescapedDoubleJsxStringFragment,
  "unescaped_single_jsx_string_fragment": unescapedSingleJsxStringFragment,
  "class": class_,
  "class_declaration": classDeclaration,
  "class_heritage": classHeritage,
  "function_expression": functionExpression,
  "function_declaration": functionDeclaration,
  "generator_function": generatorFunction,
  "generator_function_declaration": generatorFunctionDeclaration,
  "arrow_function": arrowFunction,
  "call_expression": callExpression,
  "new_expression": newExpression,
  "await_expression": awaitExpression,
  "member_expression": memberExpression,
  "subscript_expression": subscriptExpression,
  "_lhs_expression": lhsExpression,
  "assignment_expression": assignmentExpression,
  "augmented_assignment_expression": augmentedAssignmentExpression,
  "spread_element": spreadElement,
  "ternary_expression": ternaryExpression,
  "binary_expression": binaryExpression,
  "unary_expression": unaryExpression,
  "update_expression": updateExpression,
  "sequence_expression": sequenceExpression,
  "string": string,
  "unescaped_double_string_fragment": unescapedDoubleStringFragment,
  "unescaped_single_string_fragment": unescapedSingleStringFragment,
  "escape_sequence": escapeSequence,
  "comment": comment,
  "template_string": templateString,
  "template_substitution": templateSubstitution,
  "regex": regex,
  "regex_pattern": regexPattern,
  "regex_flags": regexFlags,
  "number": number,
  "identifier": identifier,
  "private_property_identifier": privatePropertyIdentifier,
  "meta_property": metaProperty,
  "this": this_,
  "super": super_,
  "true": true_,
  "false": false_,
  "null": null_,
  "undefined": undefined_,
  "arguments": arguments_,
  "decorator": decorator,
  "decorator_member_expression": decoratorMemberExpression,
  "decorator_call_expression": decoratorCallExpression,
  "class_body": classBody,
  "field_definition": fieldDefinition,
  "formal_parameters": formalParameters,
  "class_static_block": classStaticBlock,
  "pattern": pattern,
  "rest_pattern": restPattern,
  "method_definition": methodDefinition,
  "pair": pair,
  "pair_pattern": pairPattern,
  "computed_property_name": computedPropertyName,
  "_reserved_identifier": reservedIdentifier,
  "_string_fragment": stringFragment,
  "public_field_definition": publicFieldDefinition,
  "non_null_expression": nonNullExpression,
  "method_signature": methodSignature,
  "abstract_method_signature": abstractMethodSignature,
  "function_signature": functionSignature,
  "decorator_parenthesized_expression": decoratorParenthesizedExpression,
  "type_assertion": typeAssertion,
  "as_expression": asExpression,
  "satisfies_expression": satisfiesExpression,
  "instantiation_expression": instantiationExpression,
  "import_require_clause": importRequireClause,
  "extends_clause": extendsClause,
  "implements_clause": implementsClause,
  "ambient_declaration": ambientDeclaration,
  "abstract_class_declaration": abstractClassDeclaration,
  "module": module,
  "internal_module": internalModule,
  "import_alias": importAlias,
  "nested_type_identifier": nestedTypeIdentifier,
  "interface_declaration": interfaceDeclaration,
  "extends_type_clause": extendsTypeClause,
  "enum_declaration": enumDeclaration,
  "enum_body": enumBody,
  "enum_assignment": enumAssignment,
  "type_alias_declaration": typeAliasDeclaration,
  "accessibility_modifier": accessibilityModifier,
  "override_modifier": overrideModifier,
  "required_parameter": requiredParameter,
  "optional_parameter": optionalParameter,
  "omitting_type_annotation": omittingTypeAnnotation,
  "adding_type_annotation": addingTypeAnnotation,
  "opting_type_annotation": optingTypeAnnotation,
  "type_annotation": typeAnnotation,
  "asserts": asserts,
  "asserts_annotation": assertsAnnotation,
  "tuple_parameter": tupleParameter,
  "optional_tuple_parameter": optionalTupleParameter,
  "optional_type": optionalType,
  "rest_type": restType,
  "constructor_type": constructorType,
  "template_type": templateType,
  "template_literal_type": templateLiteralType,
  "infer_type": inferType,
  "conditional_type": conditionalType,
  "generic_type": genericType,
  "type_predicate": typePredicate,
  "type_predicate_annotation": typePredicateAnnotation,
  "type_query": typeQuery,
  "index_type_query": indexTypeQuery,
  "lookup_type": lookupType,
  "mapped_type_clause": mappedTypeClause,
  "literal_type": literalType,
  "flow_maybe_type": flowMaybeType,
  "parenthesized_type": parenthesizedType,
  "predefined_type": predefinedType,
  "type_arguments": typeArguments,
  "object_type": objectType,
  "call_signature": callSignature,
  "property_signature": propertySignature,
  "type_parameters": typeParameters,
  "type_parameter": typeParameter,
  "default_type": defaultType,
  "constraint": constraint,
  "construct_signature": constructSignature,
  "index_signature": indexSignature,
  "array_type": arrayType,
  "tuple_type": tupleType,
  "readonly_type": readonlyType,
  "union_type": unionType,
  "intersection_type": intersectionType,
  "function_type": functionType,
  "_type_identifier": typeIdentifier,
  "_arrow_function_parameter": arrowFunctionParameter,
  "_arrow_function__call_signature": arrowFunctionUCallSignature,
  "_class_heritage_extends_clause": classHeritageExtendsClause,
  "_class_heritage_implements_clause": classHeritageImplementsClause,
  "_import_clause_namespace_import": importClauseNamespaceImport,
  "_import_clause_named_imports": importClauseNamedImports,
  "_import_clause_default_import": importClauseDefaultImport,
  "_import_specifier_name": importSpecifierName,
  "_index_signature_mapped_type_clause": indexSignatureMappedTypeClause,
  "_parenthesized_expression_sequence": parenthesizedExpressionSequence,
  "_export_statement_default": exportStatementDefault,
  "_export_statement_type_export": exportStatementTypeExport,
  "_export_statement_equals_export": exportStatementEqualsExport,
  "_export_statement_namespace_export": exportStatementNamespaceExport,
  "_string_double": stringDouble,
  "_string_single": stringSingle,
  "_automatic_semicolon": automaticSemicolon,
  "html_comment": htmlComment,
  "||": oror,
  "jsx_text": jsxText,
  "_function_signature_automatic_semicolon": functionSignatureAutomaticSemicolon,
} as const;
export type _FactoryMap = typeof _factoryMap;

// Auto-generated by @sittir/codegen — do not edit

import type { NodeData } from './types.js';
import type { AbstractClassDeclaration, AbstractClassDeclarationTree, AbstractMethodSignature, AbstractMethodSignatureTree, AccessibilityModifier, AccessibilityModifierTree, AddingTypeAnnotation, AddingTypeAnnotationTree, AmbientDeclaration, AmbientDeclarationGlobal, AmbientDeclarationGlobalConfig, AmbientDeclarationModule, AmbientDeclarationModuleConfig, AmbientDeclarationTree, AmbientDeclarationV0, AmbientDeclarationV0Config, Arguments, ArgumentsTree, Array, ArrayPattern, ArrayPatternTree, ArrayTree, ArrayType, ArrayTypeTree, ArrowFunction, ArrowFunctionTree, AsExpression, AsExpressionTree, Asserts, AssertsAnnotation, AssertsAnnotationTree, AssertsTree, AssignmentExpression, AssignmentExpressionTree, AssignmentPattern, AssignmentPatternTree, AugmentedAssignmentExpression, AugmentedAssignmentExpressionTree, AugmentedAssignmentLhs, AwaitExpression, AwaitExpressionTree, BinaryExpression, BinaryExpressionAmp, BinaryExpressionAmpConfig, BinaryExpressionAngle, BinaryExpressionAngleConfig, BinaryExpressionCaret, BinaryExpressionCaretConfig, BinaryExpressionCloseAngle, BinaryExpressionCloseAngleConfig, BinaryExpressionIn, BinaryExpressionInConfig, BinaryExpressionInstanceof, BinaryExpressionInstanceofConfig, BinaryExpressionMinus, BinaryExpressionMinusConfig, BinaryExpressionPercent, BinaryExpressionPercentConfig, BinaryExpressionPipe, BinaryExpressionPipeConfig, BinaryExpressionPlus, BinaryExpressionPlusConfig, BinaryExpressionSlash, BinaryExpressionSlashConfig, BinaryExpressionStar, BinaryExpressionStarConfig, BinaryExpressionTok_213d, BinaryExpressionTok_213d3d, BinaryExpressionTok_213d3dConfig, BinaryExpressionTok_213dConfig, BinaryExpressionTok_2626, BinaryExpressionTok_2626Config, BinaryExpressionTok_2a2a, BinaryExpressionTok_2a2aConfig, BinaryExpressionTok_3c3c, BinaryExpressionTok_3c3cConfig, BinaryExpressionTok_3c3d, BinaryExpressionTok_3c3dConfig, BinaryExpressionTok_3d3d, BinaryExpressionTok_3d3d3d, BinaryExpressionTok_3d3d3dConfig, BinaryExpressionTok_3d3dConfig, BinaryExpressionTok_3e3d, BinaryExpressionTok_3e3dConfig, BinaryExpressionTok_3e3e, BinaryExpressionTok_3e3e3e, BinaryExpressionTok_3e3e3eConfig, BinaryExpressionTok_3e3eConfig, BinaryExpressionTok_3f3f, BinaryExpressionTok_3f3fConfig, BinaryExpressionTok_7c7c, BinaryExpressionTok_7c7cConfig, BinaryExpressionTree, BreakStatement, BreakStatementTree, CallExpression, CallExpressionTok_3f2e, CallExpressionTok_3f2eConfig, CallExpressionTree, CallExpressionV0, CallExpressionV0Config, CallExpressionV1, CallExpressionV1Config, CallSignature, CallSignatureTree, CatchClause, CatchClauseTree, Class, ClassBody, ClassBodyComma, ClassBodyCommaConfig, ClassBodyDecorator, ClassBodyDecoratorConfig, ClassBodySemi, ClassBodySemiConfig, ClassBodyTree, ClassBodyV2, ClassBodyV2Config, ClassDeclaration, ClassDeclarationTree, ClassHeritage, ClassHeritageTree, ClassStaticBlock, ClassStaticBlockTree, ClassTree, Comment, CommentTree, ComputedPropertyName, ComputedPropertyNameTree, ConditionalType, ConditionalTypeTree, Constraint, ConstraintTree, ConstructSignature, ConstructSignatureTree, ConstructorType, ConstructorTypeTree, ContinueStatement, ContinueStatementTree, DebuggerStatement, DebuggerStatementTree, Declaration, Decorator, DecoratorTree, DefaultType, DefaultTypeTree, DestructuringPattern, DoStatement, DoStatementTree, ElseClause, ElseClauseTree, EmptyStatement, EmptyStatementTree, EnumAssignment, EnumAssignmentTree, EnumBody, EnumBodyTree, EnumDeclaration, EnumDeclarationTree, EscapeSequence, EscapeSequenceTree, ExistentialType, ExistentialTypeTree, ExportClause, ExportClauseTree, ExportSpecifier, ExportSpecifierTree, ExportStatement, ExportStatementAs, ExportStatementAsConfig, ExportStatementEq, ExportStatementEqConfig, ExportStatementStar, ExportStatementStarConfig, ExportStatementTree, ExportStatementTypeKw, ExportStatementTypeKwConfig, ExportStatementV1, ExportStatementV1Config, ExportStatementV2, ExportStatementV2Config, ExportStatementV3, ExportStatementV3Config, ExportStatementV4, ExportStatementV4Config, ExportStatementValue, ExportStatementValueConfig, Expression, ExpressionStatement, ExpressionStatementTree, Expressions, ExtendsClause, ExtendsClauseTree, ExtendsTypeClause, ExtendsTypeClauseTree, False, FalseTree, FinallyClause, FinallyClauseTree, FlowMaybeType, FlowMaybeTypeTree, ForInStatement, ForInStatementLet, ForInStatementLetConfig, ForInStatementTree, ForInStatementV0, ForInStatementV0Config, ForInStatementVar, ForInStatementVarConfig, ForStatement, ForStatementTree, ForStatementV0, ForStatementV0Config, ForStatementV1, ForStatementV1Config, FormalParameters, FormalParametersTree, FunctionDeclaration, FunctionDeclarationTree, FunctionExpression, FunctionExpressionTree, FunctionSignature, FunctionSignatureTree, FunctionType, FunctionTypeTree, GeneratorFunction, GeneratorFunctionDeclaration, GeneratorFunctionDeclarationTree, GeneratorFunctionTree, GenericType, GenericTypeTree, HashBangLine, HashBangLineTree, HtmlComment, HtmlCommentTree, Identifier, IdentifierTree, IfStatement, IfStatementTree, ImplementsClause, ImplementsClauseTree, Import, ImportAlias, ImportAliasTree, ImportAttribute, ImportAttributeTree, ImportClause, ImportClauseComma, ImportClauseCommaConfig, ImportClauseTree, ImportClauseV0, ImportClauseV0Config, ImportIdentifier, ImportRequireClause, ImportRequireClauseTree, ImportSpecifier, ImportSpecifierAs, ImportSpecifierAsConfig, ImportSpecifierTree, ImportSpecifierV0, ImportSpecifierV0Config, ImportStatement, ImportStatementFrom, ImportStatementFromConfig, ImportStatementTree, ImportStatementV1, ImportStatementV1Config, ImportStatementV2, ImportStatementV2Config, ImportTree, IndexSignature, IndexSignatureColon, IndexSignatureColonConfig, IndexSignatureMappedTypeClause, IndexSignatureMappedTypeClauseConfig, IndexSignatureTree, IndexTypeQuery, IndexTypeQueryTree, InferType, InferTypeTree, InstantiationExpression, InstantiationExpressionTree, InterfaceBody, InterfaceBodyTree, InterfaceDeclaration, InterfaceDeclarationTree, InternalModule, InternalModuleTree, IntersectionType, IntersectionTypeTree, LabeledStatement, LabeledStatementTree, LexicalDeclaration, LexicalDeclarationTree, LhsExpression, LiteralType, LiteralTypeTree, LookupType, LookupTypeTree, MappedTypeClause, MappedTypeClauseTree, MemberExpression, MemberExpressionTree, MetaProperty, MetaPropertyTree, MethodDefinition, MethodDefinitionTree, MethodSignature, MethodSignatureTree, Module, ModuleExportName, ModuleTree, NamedImports, NamedImportsTree, NamespaceExport, NamespaceExportTree, NamespaceImport, NamespaceImportTree, NestedIdentifier, NestedIdentifierTree, NestedTypeIdentifier, NestedTypeIdentifierTree, NewExpression, NewExpressionTree, NonNullExpression, NonNullExpressionTree, Null, NullTree, Number, NumberTree, Object, ObjectAssignmentPattern, ObjectAssignmentPatternTree, ObjectPattern, ObjectPatternTree, ObjectTree, ObjectType, ObjectTypeTree, OmittingTypeAnnotation, OmittingTypeAnnotationTree, OptingTypeAnnotation, OptingTypeAnnotationTree, OptionalChain, OptionalChainTree, OptionalParameter, OptionalParameterTree, OptionalType, OptionalTypeTree, OverrideModifier, OverrideModifierTree, Pair, PairPattern, PairPatternTree, PairTree, ParameterName, ParenthesizedExpression, ParenthesizedExpressionTree, ParenthesizedExpressionType, ParenthesizedExpressionTypeConfig, ParenthesizedExpressionV1, ParenthesizedExpressionV1Config, ParenthesizedType, ParenthesizedTypeTree, Pattern, PredefinedType, PredefinedTypeTree, PrimaryExpression, PrimaryType, PrivatePropertyIdentifier, PrivatePropertyIdentifierTree, Program, ProgramTree, PropertyIdentifier, PropertyIdentifierTree, PropertyName, PropertySignature, PropertySignatureTree, PublicFieldDefinition, PublicFieldDefinitionAbstract, PublicFieldDefinitionAbstractConfig, PublicFieldDefinitionAccessor, PublicFieldDefinitionAccessorConfig, PublicFieldDefinitionStatic, PublicFieldDefinitionStaticConfig, PublicFieldDefinitionTree, ReadonlyType, ReadonlyTypeTree, Regex, RegexFlags, RegexFlagsTree, RegexPattern, RegexPatternTree, RegexTree, RequiredParameter, RequiredParameterTree, RestPattern, RestPatternTree, RestType, RestTypeTree, ReturnStatement, ReturnStatementTree, SatisfiesExpression, SatisfiesExpressionTree, SequenceExpression, SequenceExpressionTree, ShorthandPropertyIdentifier, ShorthandPropertyIdentifierPattern, ShorthandPropertyIdentifierPatternTree, ShorthandPropertyIdentifierTree, SpreadElement, SpreadElementTree, Statement, StatementBlock, StatementBlockTree, StatementIdentifier, StatementIdentifierTree, String, StringFragment, StringFragmentTree, StringTok_22, StringTok_22Config, StringTok_27, StringTok_27Config, StringTree, SubscriptExpression, SubscriptExpressionTree, Super, SuperTree, SwitchBody, SwitchBodyTree, SwitchCase, SwitchCaseTree, SwitchDefault, SwitchDefaultTree, SwitchStatement, SwitchStatementTree, TemplateLiteralType, TemplateLiteralTypeTree, TemplateString, TemplateStringTree, TemplateSubstitution, TemplateSubstitutionTree, TemplateType, TemplateTypeTree, TernaryExpression, TernaryExpressionTree, This, ThisTree, ThisType, ThisTypeTree, ThrowStatement, ThrowStatementTree, True, TrueTree, TryStatement, TryStatementTree, TupleType, TupleTypeTree, Type, TypeAliasDeclaration, TypeAliasDeclarationTree, TypeAnnotation, TypeAnnotationTree, TypeArguments, TypeArgumentsTree, TypeAssertion, TypeAssertionTree, TypeIdentifier, TypeIdentifierTree, TypeParameter, TypeParameterTree, TypeParameters, TypeParametersTree, TypePredicate, TypePredicateAnnotation, TypePredicateAnnotationTree, TypePredicateTree, TypeQuery, TypeQueryTree, UnaryExpression, UnaryExpressionTree, Undefined, UndefinedTree, UnionType, UnionTypeTree, UpdateExpression, UpdateExpressionTree, VariableDeclaration, VariableDeclarationTree, VariableDeclarator, VariableDeclaratorBang, VariableDeclaratorBangConfig, VariableDeclaratorEq, VariableDeclaratorEqConfig, VariableDeclaratorTree, WhileStatement, WhileStatementTree, WithStatement, WithStatementTree, YieldExpression, YieldExpressionStar, YieldExpressionStarConfig, YieldExpressionTree, YieldExpressionV1, YieldExpressionV1Config } from './types.js';
import type { Edit, AnyNodeData, ConfigOf } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  'abstract',
  'accessor',
  'any',
  'as',
  'assert',
  'async',
  'await',
  'boolean',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'delete',
  'do',
  'else',
  'enum',
  'export',
  'extends',
  'finally',
  'for',
  'from',
  'function',
  'get',
  'global',
  'if',
  'implements',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'let',
  'meta',
  'namespace',
  'never',
  'new',
  'of',
  'override',
  'private',
  'protected',
  'public',
  'readonly',
  'require',
  'return',
  'satisfies',
  'set',
  'static',
  'switch',
  'symbol',
  'target',
  'throw',
  'try',
  'typeof',
  'unknown',
  'using',
  'var',
  'void',
  'while',
  'with',
  'yield',
]);

export function abstract_class_declaration_(
  config: ConfigOf<AbstractClassDeclaration>,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
  };
  return {
    type: 'abstract_class_declaration' as const,
    named: true as const,
    fields,
    body(body?: ClassBody) { return body !== undefined ? abstract_class_declaration_({ ...config, body: body }) : fields.body; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? abstract_class_declaration_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: TypeIdentifier) { return name !== undefined ? abstract_class_declaration_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? abstract_class_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    classHeritage(classHeritage?: ClassHeritage) { return classHeritage !== undefined ? abstract_class_declaration_({ ...config, classHeritage: classHeritage }) : fields.class_heritage; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function abstract_method_signature_(
  config: ConfigOf<AbstractMethodSignature>,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  const children: unknown[] = [];
  return {
    type: 'abstract_method_signature' as const,
    named: true as const,
    fields,
    children,
    name(name?: PropertyName) { return name !== undefined ? abstract_method_signature_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? abstract_method_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? abstract_method_signature_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? abstract_method_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? abstract_method_signature_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? abstract_method_signature_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function adding_type_annotation_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'adding_type_annotation' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? adding_type_annotation_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ambient_declaration_(
  config: ConfigOf<AmbientDeclaration>,
) {
  if ('typeAnnotation' in config && config.typeAnnotation !== undefined) return ambient_declaration__module_(config as AmbientDeclarationModuleConfig);
  return ambient_declaration__v0_(config as AmbientDeclarationV0Config);
}

/** Variant factory: `ambient_declaration` — v0 form. */
export function ambient_declaration__v0_(
  config: AmbientDeclarationV0Config,
) {
  const fields = {
    declaration: config.declaration,
  };
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    declaration(declaration?: Declaration | PropertyIdentifier | StatementBlock) { return declaration !== undefined ? ambient_declaration__v0_({ ...config, declaration: declaration } as AmbientDeclarationV0Config) : fields.declaration; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `ambient_declaration` — global form. */
export function ambient_declaration__global_(
  config: AmbientDeclarationGlobalConfig,
) {
  const fields = {
    declaration: config.declaration,
  };
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    variant: 'global' as const,
    fields,
    declaration(declaration?: Declaration | PropertyIdentifier | StatementBlock) { return declaration !== undefined ? ambient_declaration__global_({ ...config, declaration: declaration } as AmbientDeclarationGlobalConfig) : fields.declaration; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `ambient_declaration` — module form. */
export function ambient_declaration__module_(
  config: AmbientDeclarationModuleConfig,
) {
  const fields = {
    declaration: config.declaration,
    type_annotation: config.typeAnnotation,
    semicolon: config.semicolon,
  };
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    variant: 'module' as const,
    fields,
    declaration(declaration?: Declaration | PropertyIdentifier | StatementBlock) { return declaration !== undefined ? ambient_declaration__module_({ ...config, declaration: declaration } as AmbientDeclarationModuleConfig) : fields.declaration; },
    typeAnnotation(typeAnnotation?: Type) { return typeAnnotation !== undefined ? ambient_declaration__module_({ ...config, typeAnnotation: typeAnnotation } as AmbientDeclarationModuleConfig) : fields.type_annotation; },
    semicolon(semicolon?: string) { return semicolon !== undefined ? ambient_declaration__module_({ ...config, semicolon: semicolon } as AmbientDeclarationModuleConfig) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arguments_(
  ...children: (Expression | SpreadElement)[]
) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression | SpreadElement)[]) { return arguments_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_(
  ...children: (Expression | SpreadElement)[]
) {
  return {
    type: 'array' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression | SpreadElement)[]) { return array_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_pattern_(
  ...children: (AssignmentPattern | Pattern)[]
) {
  return {
    type: 'array_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (AssignmentPattern | Pattern)[]) { return array_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_type_(
  config: ConfigOf<ArrayType>,
) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType?: PrimaryType) { return primaryType !== undefined ? array_type_({ ...config, primaryType: primaryType }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function arrow_function_(
  config: ConfigOf<ArrowFunction>,
) {
  const fields = {
    body: config.body,
    parameter: config.parameter,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'arrow_function' as const,
    named: true as const,
    fields,
    body(body?: Expression | StatementBlock) { return body !== undefined ? arrow_function_({ ...config, body: body }) : fields.body; },
    parameter(parameter?: Identifier) { return parameter !== undefined ? arrow_function_({ ...config, parameter: parameter }) : fields.parameter; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? arrow_function_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? arrow_function_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? arrow_function_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function as_expression_(
  config: ConfigOf<AsExpression>,
) {
  const fields = {
    expression: config.expression,
    type_annotation: config.typeAnnotation,
  };
  return {
    type: 'as_expression' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? as_expression_({ ...config, expression: expression }) : fields.expression; },
    typeAnnotation(typeAnnotation?: Type) { return typeAnnotation !== undefined ? as_expression_({ ...config, typeAnnotation: typeAnnotation }) : fields.type_annotation; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function asserts_(
  child: Identifier | This | TypePredicate,
) {
  const children = child ? [child] : [];
  return {
    type: 'asserts' as const,
    named: true as const,
    children,
    child(c?: Identifier | This | TypePredicate) { return c !== undefined ? asserts_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function asserts_annotation_(
  config: ConfigOf<AssertsAnnotation>,
) {
  const fields = {
    asserts: config.asserts,
  };
  return {
    type: 'asserts_annotation' as const,
    named: true as const,
    fields,
    asserts(asserts?: Asserts) { return asserts !== undefined ? asserts_annotation_({ ...config, asserts: asserts }) : fields.asserts; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_expression_(
  config: ConfigOf<AssignmentExpression>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left?: AugmentedAssignmentLhs | LhsExpression) { return left !== undefined ? assignment_expression_({ ...config, left: left }) : fields.left; },
    right(right?: Expression) { return right !== undefined ? assignment_expression_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_pattern_(
  config: ConfigOf<AssignmentPattern>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment_pattern' as const,
    named: true as const,
    fields,
    left(left?: Pattern) { return left !== undefined ? assignment_pattern_({ ...config, left: left }) : fields.left; },
    right(right?: Expression) { return right !== undefined ? assignment_pattern_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function augmented_assignment_expression_(
  config: ConfigOf<AugmentedAssignmentExpression>,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment_expression' as const,
    named: true as const,
    fields,
    left(left?: AugmentedAssignmentLhs) { return left !== undefined ? augmented_assignment_expression_({ ...config, left: left }) : fields.left; },
    operator(operator?: '%=' | '&&=' | '&=' | '**=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '>>>=' | '??=' | '^=' | '|=' | '||=') { return operator !== undefined ? augmented_assignment_expression_({ ...config, operator: operator }) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? augmented_assignment_expression_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function await_expression_(
  config: ConfigOf<AwaitExpression>,
) {
  const fields = {
    expression: config.expression,
  };
  return {
    type: 'await_expression' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? await_expression_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function binary_expression_(
  config: ConfigOf<BinaryExpression>,
) {
  return binary_expression__tok_2626_(config as BinaryExpressionTok_2626Config);
}

/** Variant factory: `binary_expression` — tok_2626 form. */
export function binary_expression__tok_2626_(
  config: BinaryExpressionTok_2626Config,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_2626' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_2626_({ ...config, left: left } as BinaryExpressionTok_2626Config) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_2626_({ ...config, operator: operator } as BinaryExpressionTok_2626Config) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_2626_({ ...config, right: right } as BinaryExpressionTok_2626Config) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_7c7c form. */
export function binary_expression__tok_7c7c_(
  config: BinaryExpressionTok_7c7cConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_7c7c' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_7c7c_({ ...config, left: left } as BinaryExpressionTok_7c7cConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_7c7c_({ ...config, operator: operator } as BinaryExpressionTok_7c7cConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_7c7c_({ ...config, right: right } as BinaryExpressionTok_7c7cConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3e3e form. */
export function binary_expression__tok_3e3e_(
  config: BinaryExpressionTok_3e3eConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3e3e' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_3e3e_({ ...config, left: left } as BinaryExpressionTok_3e3eConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3e3e_({ ...config, operator: operator } as BinaryExpressionTok_3e3eConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3e3e_({ ...config, right: right } as BinaryExpressionTok_3e3eConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3e3e3e form. */
export function binary_expression__tok_3e3e3e_(
  config: BinaryExpressionTok_3e3e3eConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3e3e3e' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_3e3e3e_({ ...config, left: left } as BinaryExpressionTok_3e3e3eConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3e3e3e_({ ...config, operator: operator } as BinaryExpressionTok_3e3e3eConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3e3e3e_({ ...config, right: right } as BinaryExpressionTok_3e3e3eConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3c3c form. */
export function binary_expression__tok_3c3c_(
  config: BinaryExpressionTok_3c3cConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3c3c' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_3c3c_({ ...config, left: left } as BinaryExpressionTok_3c3cConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3c3c_({ ...config, operator: operator } as BinaryExpressionTok_3c3cConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3c3c_({ ...config, right: right } as BinaryExpressionTok_3c3cConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — amp form. */
export function binary_expression__amp_(
  config: BinaryExpressionAmpConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'amp' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__amp_({ ...config, left: left } as BinaryExpressionAmpConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__amp_({ ...config, operator: operator } as BinaryExpressionAmpConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__amp_({ ...config, right: right } as BinaryExpressionAmpConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — caret form. */
export function binary_expression__caret_(
  config: BinaryExpressionCaretConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'caret' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__caret_({ ...config, left: left } as BinaryExpressionCaretConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__caret_({ ...config, operator: operator } as BinaryExpressionCaretConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__caret_({ ...config, right: right } as BinaryExpressionCaretConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — pipe form. */
export function binary_expression__pipe_(
  config: BinaryExpressionPipeConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'pipe' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__pipe_({ ...config, left: left } as BinaryExpressionPipeConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__pipe_({ ...config, operator: operator } as BinaryExpressionPipeConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__pipe_({ ...config, right: right } as BinaryExpressionPipeConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — plus form. */
export function binary_expression__plus_(
  config: BinaryExpressionPlusConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'plus' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__plus_({ ...config, left: left } as BinaryExpressionPlusConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__plus_({ ...config, operator: operator } as BinaryExpressionPlusConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__plus_({ ...config, right: right } as BinaryExpressionPlusConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — minus form. */
export function binary_expression__minus_(
  config: BinaryExpressionMinusConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'minus' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__minus_({ ...config, left: left } as BinaryExpressionMinusConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__minus_({ ...config, operator: operator } as BinaryExpressionMinusConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__minus_({ ...config, right: right } as BinaryExpressionMinusConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — star form. */
export function binary_expression__star_(
  config: BinaryExpressionStarConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'star' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__star_({ ...config, left: left } as BinaryExpressionStarConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__star_({ ...config, operator: operator } as BinaryExpressionStarConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__star_({ ...config, right: right } as BinaryExpressionStarConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — slash form. */
export function binary_expression__slash_(
  config: BinaryExpressionSlashConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'slash' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__slash_({ ...config, left: left } as BinaryExpressionSlashConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__slash_({ ...config, operator: operator } as BinaryExpressionSlashConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__slash_({ ...config, right: right } as BinaryExpressionSlashConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — percent form. */
export function binary_expression__percent_(
  config: BinaryExpressionPercentConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'percent' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__percent_({ ...config, left: left } as BinaryExpressionPercentConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__percent_({ ...config, operator: operator } as BinaryExpressionPercentConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__percent_({ ...config, right: right } as BinaryExpressionPercentConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_2a2a form. */
export function binary_expression__tok_2a2a_(
  config: BinaryExpressionTok_2a2aConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_2a2a' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_2a2a_({ ...config, left: left } as BinaryExpressionTok_2a2aConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_2a2a_({ ...config, operator: operator } as BinaryExpressionTok_2a2aConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_2a2a_({ ...config, right: right } as BinaryExpressionTok_2a2aConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — angle form. */
export function binary_expression__angle_(
  config: BinaryExpressionAngleConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'angle' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__angle_({ ...config, left: left } as BinaryExpressionAngleConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__angle_({ ...config, operator: operator } as BinaryExpressionAngleConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__angle_({ ...config, right: right } as BinaryExpressionAngleConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3c3d form. */
export function binary_expression__tok_3c3d_(
  config: BinaryExpressionTok_3c3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3c3d' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_3c3d_({ ...config, left: left } as BinaryExpressionTok_3c3dConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3c3d_({ ...config, operator: operator } as BinaryExpressionTok_3c3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3c3d_({ ...config, right: right } as BinaryExpressionTok_3c3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3d3d form. */
export function binary_expression__tok_3d3d_(
  config: BinaryExpressionTok_3d3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3d3d' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_3d3d_({ ...config, left: left } as BinaryExpressionTok_3d3dConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3d3d_({ ...config, operator: operator } as BinaryExpressionTok_3d3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3d3d_({ ...config, right: right } as BinaryExpressionTok_3d3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3d3d3d form. */
export function binary_expression__tok_3d3d3d_(
  config: BinaryExpressionTok_3d3d3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3d3d3d' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_3d3d3d_({ ...config, left: left } as BinaryExpressionTok_3d3d3dConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3d3d3d_({ ...config, operator: operator } as BinaryExpressionTok_3d3d3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3d3d3d_({ ...config, right: right } as BinaryExpressionTok_3d3d3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_213d form. */
export function binary_expression__tok_213d_(
  config: BinaryExpressionTok_213dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_213d' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_213d_({ ...config, left: left } as BinaryExpressionTok_213dConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_213d_({ ...config, operator: operator } as BinaryExpressionTok_213dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_213d_({ ...config, right: right } as BinaryExpressionTok_213dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_213d3d form. */
export function binary_expression__tok_213d3d_(
  config: BinaryExpressionTok_213d3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_213d3d' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_213d3d_({ ...config, left: left } as BinaryExpressionTok_213d3dConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_213d3d_({ ...config, operator: operator } as BinaryExpressionTok_213d3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_213d3d_({ ...config, right: right } as BinaryExpressionTok_213d3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3e3d form. */
export function binary_expression__tok_3e3d_(
  config: BinaryExpressionTok_3e3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3e3d' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_3e3d_({ ...config, left: left } as BinaryExpressionTok_3e3dConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3e3d_({ ...config, operator: operator } as BinaryExpressionTok_3e3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3e3d_({ ...config, right: right } as BinaryExpressionTok_3e3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — close_angle form. */
export function binary_expression__close_angle_(
  config: BinaryExpressionCloseAngleConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'close_angle' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__close_angle_({ ...config, left: left } as BinaryExpressionCloseAngleConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__close_angle_({ ...config, operator: operator } as BinaryExpressionCloseAngleConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__close_angle_({ ...config, right: right } as BinaryExpressionCloseAngleConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3f3f form. */
export function binary_expression__tok_3f3f_(
  config: BinaryExpressionTok_3f3fConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3f3f' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__tok_3f3f_({ ...config, left: left } as BinaryExpressionTok_3f3fConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3f3f_({ ...config, operator: operator } as BinaryExpressionTok_3f3fConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3f3f_({ ...config, right: right } as BinaryExpressionTok_3f3fConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — instanceof form. */
export function binary_expression__instanceof_(
  config: BinaryExpressionInstanceofConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'instanceof' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__instanceof_({ ...config, left: left } as BinaryExpressionInstanceofConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__instanceof_({ ...config, operator: operator } as BinaryExpressionInstanceofConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__instanceof_({ ...config, right: right } as BinaryExpressionInstanceofConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — in form. */
export function binary_expression__in_(
  config: BinaryExpressionInConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'in' as const,
    fields,
    left(left?: Expression | PrivatePropertyIdentifier) { return left !== undefined ? binary_expression__in_({ ...config, left: left } as BinaryExpressionInConfig) : fields.left; },
    operator(operator?: '!=' | '!==' | '%' | '&' | '&&' | '*' | '**' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '===' | '>' | '>=' | '>>' | '>>>' | '??' | '^' | 'in' | 'instanceof' | '|' | '||') { return operator !== undefined ? binary_expression__in_({ ...config, operator: operator } as BinaryExpressionInConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__in_({ ...config, right: right } as BinaryExpressionInConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function break_statement_(
  config?: ConfigOf<BreakStatement>,
) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'break_statement' as const,
    named: true as const,
    fields,
    label(label?: StatementIdentifier) { return label !== undefined ? break_statement_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call_expression_(
  config: ConfigOf<CallExpression>,
) {
  return call_expression__v0_(config as CallExpressionV0Config);
}

/** Variant factory: `call_expression` — v0 form. */
export function call_expression__v0_(
  config: CallExpressionV0Config,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    arguments(arguments_?: Arguments | TemplateString) { return arguments_ !== undefined ? call_expression__v0_({ ...config, arguments: arguments_ } as CallExpressionV0Config) : fields.arguments; },
    function(function_?: Expression | Import | NewExpression | PrimaryExpression) { return function_ !== undefined ? call_expression__v0_({ ...config, function: function_ } as CallExpressionV0Config) : fields.function; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? call_expression__v0_({ ...config, typeArguments: typeArguments } as CallExpressionV0Config) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `call_expression` — v1 form. */
export function call_expression__v1_(
  config: CallExpressionV1Config,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    arguments(arguments_?: Arguments | TemplateString) { return arguments_ !== undefined ? call_expression__v1_({ ...config, arguments: arguments_ } as CallExpressionV1Config) : fields.arguments; },
    function(function_?: Expression | Import | NewExpression | PrimaryExpression) { return function_ !== undefined ? call_expression__v1_({ ...config, function: function_ } as CallExpressionV1Config) : fields.function; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `call_expression` — tok_3f2e form. */
export function call_expression__tok_3f2e_(
  config: CallExpressionTok_3f2eConfig,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'tok_3f2e' as const,
    fields,
    arguments(arguments_?: Arguments | TemplateString) { return arguments_ !== undefined ? call_expression__tok_3f2e_({ ...config, arguments: arguments_ } as CallExpressionTok_3f2eConfig) : fields.arguments; },
    function(function_?: Expression | Import | NewExpression | PrimaryExpression) { return function_ !== undefined ? call_expression__tok_3f2e_({ ...config, function: function_ } as CallExpressionTok_3f2eConfig) : fields.function; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? call_expression__tok_3f2e_({ ...config, typeArguments: typeArguments } as CallExpressionTok_3f2eConfig) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function call_signature_(
  config: ConfigOf<CallSignature>,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'call_signature' as const,
    named: true as const,
    fields,
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? call_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? call_signature_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? call_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function catch_clause_(
  config: ConfigOf<CatchClause>,
) {
  const fields = {
    body: config.body,
    parameter: config.parameter,
    type: config.type,
  };
  return {
    type: 'catch_clause' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? catch_clause_({ ...config, body: body }) : fields.body; },
    parameter(parameter?: DestructuringPattern | Identifier) { return parameter !== undefined ? catch_clause_({ ...config, parameter: parameter }) : fields.parameter; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? catch_clause_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_(
  config: ConfigOf<Class>,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
  };
  return {
    type: 'class' as const,
    named: true as const,
    fields,
    body(body?: ClassBody) { return body !== undefined ? class_({ ...config, body: body }) : fields.body; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? class_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: TypeIdentifier) { return name !== undefined ? class_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? class_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    classHeritage(classHeritage?: ClassHeritage) { return classHeritage !== undefined ? class_({ ...config, classHeritage: classHeritage }) : fields.class_heritage; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_body_(
  config?: ConfigOf<ClassBody>,
) {
  if (config && 'decorator' in config && config.decorator !== undefined) return class_body__decorator_(config as ClassBodyDecoratorConfig);
  return class_body__comma_(config as ClassBodyCommaConfig);
}

/** Variant factory: `class_body` — decorator form. */
export function class_body__decorator_(
  config?: ClassBodyDecoratorConfig,
) {
  const fields = {
    decorator: config?.decorator,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    variant: 'decorator' as const,
    fields,
    children,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? class_body__decorator_({ ...config, decorator: decorator } as ClassBodyDecoratorConfig) : fields.decorator; },
    getChildren() { return children; },
    setChildren(...children: (AbstractMethodSignature | ClassStaticBlock | IndexSignature | MethodDefinition | MethodSignature | PublicFieldDefinition)[]) { return class_body__decorator_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `class_body` — comma form. */
export function class_body__comma_(
  config?: ClassBodyCommaConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    variant: 'comma' as const,
    fields: {},
    children,
    getChildren() { return children; },
    setChildren(...children: (AbstractMethodSignature | ClassStaticBlock | IndexSignature | MethodDefinition | MethodSignature | PublicFieldDefinition)[]) { return class_body__comma_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `class_body` — v2 form. */
export function class_body__v2_(
  config?: ClassBodyV2Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    variant: 'v2' as const,
    fields: {},
    children,
    getChildren() { return children; },
    setChildren(...children: (AbstractMethodSignature | ClassStaticBlock | IndexSignature | MethodDefinition | MethodSignature | PublicFieldDefinition)[]) { return class_body__v2_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `class_body` — semi form. */
export function class_body__semi_(
  config?: ClassBodySemiConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    variant: 'semi' as const,
    fields: {},
    children,
    getChildren() { return children; },
    setChildren(...children: (AbstractMethodSignature | ClassStaticBlock | IndexSignature | MethodDefinition | MethodSignature | PublicFieldDefinition)[]) { return class_body__semi_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function class_declaration_(
  config: ConfigOf<ClassDeclaration>,
) {
  const fields = {
    body: config.body,
    decorator: config.decorator,
    name: config.name,
    type_parameters: config.typeParameters,
    class_heritage: config.classHeritage,
    automatic_semicolon: config.automaticSemicolon,
  };
  return {
    type: 'class_declaration' as const,
    named: true as const,
    fields,
    body(body?: ClassBody) { return body !== undefined ? class_declaration_({ ...config, body: body }) : fields.body; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? class_declaration_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: TypeIdentifier) { return name !== undefined ? class_declaration_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? class_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    classHeritage(classHeritage?: ClassHeritage) { return classHeritage !== undefined ? class_declaration_({ ...config, classHeritage: classHeritage }) : fields.class_heritage; },
    automaticSemicolon(automaticSemicolon?: string) { return automaticSemicolon !== undefined ? class_declaration_({ ...config, automaticSemicolon: automaticSemicolon }) : fields.automatic_semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_heritage_(
  config: ConfigOf<ClassHeritage>,
) {
  const fields = {
    extends_clause: config.extendsClause,
    implements_clause: config.implementsClause,
  };
  return {
    type: 'class_heritage' as const,
    named: true as const,
    fields,
    extendsClause(extendsClause?: ExtendsClause | ImplementsClause) { return extendsClause !== undefined ? class_heritage_({ ...config, extendsClause: extendsClause }) : fields.extends_clause; },
    implementsClause(implementsClause?: ImplementsClause) { return implementsClause !== undefined ? class_heritage_({ ...config, implementsClause: implementsClause }) : fields.implements_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_static_block_(
  config: ConfigOf<ClassStaticBlock>,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'class_static_block' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? class_static_block_({ ...config, body: body }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function computed_property_name_(
  config: ConfigOf<ComputedPropertyName>,
) {
  const fields = {
    expression: config.expression,
  };
  return {
    type: 'computed_property_name' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? computed_property_name_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function conditional_type_(
  config: ConfigOf<ConditionalType>,
) {
  const fields = {
    alternative: config.alternative,
    consequence: config.consequence,
    left: config.left,
    right: config.right,
  };
  return {
    type: 'conditional_type' as const,
    named: true as const,
    fields,
    alternative(alternative?: Type) { return alternative !== undefined ? conditional_type_({ ...config, alternative: alternative }) : fields.alternative; },
    consequence(consequence?: Type) { return consequence !== undefined ? conditional_type_({ ...config, consequence: consequence }) : fields.consequence; },
    left(left?: Type) { return left !== undefined ? conditional_type_({ ...config, left: left }) : fields.left; },
    right(right?: Type) { return right !== undefined ? conditional_type_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constraint_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'constraint' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? constraint_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function construct_signature_(
  config: ConfigOf<ConstructSignature>,
) {
  const fields = {
    parameters: config.parameters,
    type: config.type,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'construct_signature' as const,
    named: true as const,
    fields,
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? construct_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? construct_signature_({ ...config, type: type_ }) : fields.type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? construct_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constructor_type_(
  config: ConfigOf<ConstructorType>,
) {
  const fields = {
    parameters: config.parameters,
    type: config.type,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'constructor_type' as const,
    named: true as const,
    fields,
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? constructor_type_({ ...config, parameters: parameters }) : fields.parameters; },
    typeField(type_?: Type) { return type_ !== undefined ? constructor_type_({ ...config, type: type_ }) : fields.type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? constructor_type_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function continue_statement_(
  config?: ConfigOf<ContinueStatement>,
) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'continue_statement' as const,
    named: true as const,
    fields,
    label(label?: StatementIdentifier) { return label !== undefined ? continue_statement_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decorator_(
  child: CallExpression | Identifier | MemberExpression | ParenthesizedExpression,
) {
  const children = child ? [child] : [];
  return {
    type: 'decorator' as const,
    named: true as const,
    children,
    child(c?: CallExpression | Identifier | MemberExpression | ParenthesizedExpression) { return c !== undefined ? decorator_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function default_type_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'default_type' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? default_type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function do_statement_(
  config: ConfigOf<DoStatement>,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
  };
  return {
    type: 'do_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? do_statement_({ ...config, body: body }) : fields.body; },
    condition(condition?: ParenthesizedExpression) { return condition !== undefined ? do_statement_({ ...config, condition: condition }) : fields.condition; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function else_clause_(
  config: ConfigOf<ElseClause>,
) {
  const fields = {
    statement: config.statement,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    statement(statement?: Statement) { return statement !== undefined ? else_clause_({ ...config, statement: statement }) : fields.statement; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_assignment_(
  config: ConfigOf<EnumAssignment>,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'enum_assignment' as const,
    named: true as const,
    fields,
    name(name?: PropertyName) { return name !== undefined ? enum_assignment_({ ...config, name: name }) : fields.name; },
    value(value?: Expression) { return value !== undefined ? enum_assignment_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_body_(
  config?: ConfigOf<EnumBody>,
) {
  const fields = {
    name: config?.name,
    opening: config?.opening,
    members: config?.members,
  };
  return {
    type: 'enum_body' as const,
    named: true as const,
    fields,
    name(...name: (PropertyName)[]) { return name.length ? enum_body_({ ...config, name: name }) : fields.name; },
    opening(opening?: EnumAssignment) { return opening !== undefined ? enum_body_({ ...config, opening: opening }) : fields.opening; },
    members(...members: (EnumAssignment)[]) { return members.length ? enum_body_({ ...config, members: members }) : fields.members; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_declaration_(
  config: ConfigOf<EnumDeclaration>,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'enum_declaration' as const,
    named: true as const,
    fields,
    body(body?: EnumBody) { return body !== undefined ? enum_declaration_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? enum_declaration_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_clause_(
  ...children: (ExportSpecifier)[]
) {
  return {
    type: 'export_clause' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (ExportSpecifier)[]) { return export_clause_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_specifier_(
  config: ConfigOf<ExportSpecifier>,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  return {
    type: 'export_specifier' as const,
    named: true as const,
    fields,
    alias(alias?: ModuleExportName) { return alias !== undefined ? export_specifier_({ ...config, alias: alias }) : fields.alias; },
    name(name?: ModuleExportName) { return name !== undefined ? export_specifier_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function export_statement_(
  config?: ConfigOf<ExportStatement>,
) {
  if (config && 'value' in config && config.value !== undefined) return export_statement__value_(config as ExportStatementValueConfig);
  return export_statement__v1_(config as ExportStatementV1Config);
}

/** Variant factory: `export_statement` — star form. */
export function export_statement__star_(
  config?: ExportStatementStarConfig,
) {
  const fields = {
    source: config?.source,
  };
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'star' as const,
    fields,
    children,
    source(source?: String) { return source !== undefined ? export_statement__star_({ ...config, source: source } as ExportStatementStarConfig) : fields.source; },
    child(child?: ExportClause | Expression | Identifier | NamespaceExport) { return child !== undefined ? export_statement__star_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `export_statement` — v1 form. */
export function export_statement__v1_(
  config?: ExportStatementV1Config,
) {
  const fields = {
    declaration: config?.declaration,
    source: config?.source,
  };
  const children: unknown[] = [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    children,
    declaration(declaration?: Declaration | ExportClause | Expression | Identifier | NamespaceExport) { return declaration !== undefined ? export_statement__v1_({ ...config, declaration: declaration } as ExportStatementV1Config) : fields.declaration; },
    source(source?: String) { return source !== undefined ? export_statement__v1_({ ...config, source: source } as ExportStatementV1Config) : fields.source; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `export_statement` — v2 form. */
export function export_statement__v2_(
  config?: ExportStatementV2Config,
) {
  const fields = {
    declaration: config?.declaration,
  };
  const children: unknown[] = [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'v2' as const,
    fields,
    children,
    declaration(declaration?: Declaration | ExportClause | Expression | Identifier | NamespaceExport) { return declaration !== undefined ? export_statement__v2_({ ...config, declaration: declaration } as ExportStatementV2Config) : fields.declaration; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `export_statement` — v3 form. */
export function export_statement__v3_(
  config?: ExportStatementV3Config,
) {
  const fields = {
    declaration: config?.declaration,
    decorator: config?.decorator,
  };
  const children: unknown[] = [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'v3' as const,
    fields,
    children,
    declaration(declaration?: Declaration | ExportClause | Expression | Identifier | NamespaceExport) { return declaration !== undefined ? export_statement__v3_({ ...config, declaration: declaration } as ExportStatementV3Config) : fields.declaration; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? export_statement__v3_({ ...config, decorator: decorator } as ExportStatementV3Config) : fields.decorator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `export_statement` — v4 form. */
export function export_statement__v4_(
  config?: ExportStatementV4Config,
) {
  const fields = {
    declaration: config?.declaration,
    decorator: config?.decorator,
  };
  const children: unknown[] = [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'v4' as const,
    fields,
    children,
    declaration(declaration?: Declaration | ExportClause | Expression | Identifier | NamespaceExport) { return declaration !== undefined ? export_statement__v4_({ ...config, declaration: declaration } as ExportStatementV4Config) : fields.declaration; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? export_statement__v4_({ ...config, decorator: decorator } as ExportStatementV4Config) : fields.decorator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `export_statement` — value form. */
export function export_statement__value_(
  config?: ExportStatementValueConfig,
) {
  const fields = {
    declaration: config?.declaration,
    decorator: config?.decorator,
    value: config?.value,
  };
  const children: unknown[] = [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'value' as const,
    fields,
    children,
    declaration(declaration?: Declaration | ExportClause | Expression | Identifier | NamespaceExport) { return declaration !== undefined ? export_statement__value_({ ...config, declaration: declaration } as ExportStatementValueConfig) : fields.declaration; },
    decorator(...decorator: (Decorator)[]) { return decorator.length ? export_statement__value_({ ...config, decorator: decorator } as ExportStatementValueConfig) : fields.decorator; },
    value(value?: Expression) { return value !== undefined ? export_statement__value_({ ...config, value: value } as ExportStatementValueConfig) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `export_statement` — type_kw form. */
export function export_statement__type_kw_(
  config?: ExportStatementTypeKwConfig,
) {
  const fields = {
    declaration: config?.declaration,
    source: config?.source,
  };
  const children: unknown[] = [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'type_kw' as const,
    fields,
    children,
    declaration(declaration?: Declaration | ExportClause | Expression | Identifier | NamespaceExport) { return declaration !== undefined ? export_statement__type_kw_({ ...config, declaration: declaration } as ExportStatementTypeKwConfig) : fields.declaration; },
    source(source?: String) { return source !== undefined ? export_statement__type_kw_({ ...config, source: source } as ExportStatementTypeKwConfig) : fields.source; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `export_statement` — eq form. */
export function export_statement__eq_(
  config?: ExportStatementEqConfig,
) {
  const fields = {
    declaration: config?.declaration,
  };
  const children: unknown[] = [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'eq' as const,
    fields,
    children,
    declaration(declaration?: Declaration | ExportClause | Expression | Identifier | NamespaceExport) { return declaration !== undefined ? export_statement__eq_({ ...config, declaration: declaration } as ExportStatementEqConfig) : fields.declaration; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `export_statement` — as form. */
export function export_statement__as_(
  config?: ExportStatementAsConfig,
) {
  const fields = {
    declaration: config?.declaration,
  };
  const children: unknown[] = [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'as' as const,
    fields,
    children,
    declaration(declaration?: Declaration | ExportClause | Expression | Identifier | NamespaceExport) { return declaration !== undefined ? export_statement__as_({ ...config, declaration: declaration } as ExportStatementAsConfig) : fields.declaration; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expression_statement_(
  child: Expression | SequenceExpression,
) {
  const children = child ? [child] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    child(c?: Expression | SequenceExpression) { return c !== undefined ? expression_statement_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extends_clause_(
  config: ConfigOf<ExtendsClause>,
) {
  const fields = {
    type_arguments: config.typeArguments,
    value: config.value,
  };
  return {
    type: 'extends_clause' as const,
    named: true as const,
    fields,
    typeArguments(...typeArguments: (TypeArguments)[]) { return typeArguments.length ? extends_clause_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    value(...value: (Expression)[]) { return value.length ? extends_clause_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extends_type_clause_(
  config: ConfigOf<ExtendsTypeClause>,
) {
  const fields = {
    type: config.type,
  };
  return {
    type: 'extends_type_clause' as const,
    named: true as const,
    fields,
    typeField(...type_: (GenericType | NestedTypeIdentifier | TypeIdentifier)[]) { return type_.length ? extends_type_clause_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function finally_clause_(
  config: ConfigOf<FinallyClause>,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? finally_clause_({ ...config, body: body }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function flow_maybe_type_(
  config: ConfigOf<FlowMaybeType>,
) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    type: 'flow_maybe_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType?: PrimaryType) { return primaryType !== undefined ? flow_maybe_type_({ ...config, primaryType: primaryType }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_in_statement_(
  config: ConfigOf<ForInStatement>,
) {
  if ('value' in config && config.value !== undefined) return for_in_statement__var_(config as ForInStatementVarConfig);
  return for_in_statement__let_(config as ForInStatementLetConfig);
}

/** Variant factory: `for_in_statement` — v0 form. */
export function for_in_statement__v0_(
  config: ForInStatementV0Config,
) {
  const fields = {
    body: config.body,
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    body(body?: Statement) { return body !== undefined ? for_in_statement__v0_({ ...config, body: body } as ForInStatementV0Config) : fields.body; },
    left(left?: AugmentedAssignmentLhs | LhsExpression) { return left !== undefined ? for_in_statement__v0_({ ...config, left: left } as ForInStatementV0Config) : fields.left; },
    operator(operator?: 'in' | 'of') { return operator !== undefined ? for_in_statement__v0_({ ...config, operator: operator } as ForInStatementV0Config) : fields.operator; },
    right(right?: Expression | Expressions | SequenceExpression) { return right !== undefined ? for_in_statement__v0_({ ...config, right: right } as ForInStatementV0Config) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `for_in_statement` — var form. */
export function for_in_statement__var_(
  config: ForInStatementVarConfig,
) {
  const fields = {
    body: config.body,
    kind: config.kind,
    left: config.left,
    operator: config.operator,
    right: config.right,
    value: config.value,
  };
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    variant: 'var' as const,
    fields,
    body(body?: Statement) { return body !== undefined ? for_in_statement__var_({ ...config, body: body } as ForInStatementVarConfig) : fields.body; },
    kind(kind?: 'const' | 'let' | 'var') { return kind !== undefined ? for_in_statement__var_({ ...config, kind: kind } as ForInStatementVarConfig) : fields.kind; },
    left(left?: AugmentedAssignmentLhs | LhsExpression) { return left !== undefined ? for_in_statement__var_({ ...config, left: left } as ForInStatementVarConfig) : fields.left; },
    operator(operator?: 'in' | 'of') { return operator !== undefined ? for_in_statement__var_({ ...config, operator: operator } as ForInStatementVarConfig) : fields.operator; },
    right(right?: Expression | Expressions | SequenceExpression) { return right !== undefined ? for_in_statement__var_({ ...config, right: right } as ForInStatementVarConfig) : fields.right; },
    value(value?: Expression) { return value !== undefined ? for_in_statement__var_({ ...config, value: value } as ForInStatementVarConfig) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `for_in_statement` — let form. */
export function for_in_statement__let_(
  config: ForInStatementLetConfig,
) {
  const fields = {
    body: config.body,
    kind: config.kind,
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    variant: 'let' as const,
    fields,
    body(body?: Statement) { return body !== undefined ? for_in_statement__let_({ ...config, body: body } as ForInStatementLetConfig) : fields.body; },
    kind(kind?: 'const' | 'let' | 'var') { return kind !== undefined ? for_in_statement__let_({ ...config, kind: kind } as ForInStatementLetConfig) : fields.kind; },
    left(left?: AugmentedAssignmentLhs | LhsExpression) { return left !== undefined ? for_in_statement__let_({ ...config, left: left } as ForInStatementLetConfig) : fields.left; },
    operator(operator?: 'in' | 'of') { return operator !== undefined ? for_in_statement__let_({ ...config, operator: operator } as ForInStatementLetConfig) : fields.operator; },
    right(right?: Expression | Expressions | SequenceExpression) { return right !== undefined ? for_in_statement__let_({ ...config, right: right } as ForInStatementLetConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function for_statement_(
  config: ConfigOf<ForStatement>,
) {
  return for_statement__v0_(config as ForStatementV0Config);
}

/** Variant factory: `for_statement` — v0 form. */
export function for_statement__v0_(
  config: ForStatementV0Config,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
    increment: config.increment,
    initializer: config.initializer,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    body(body?: Statement) { return body !== undefined ? for_statement__v0_({ ...config, body: body } as ForStatementV0Config) : fields.body; },
    condition(...condition: (EmptyStatement | Expression | Expressions | SequenceExpression)[]) { return condition.length ? for_statement__v0_({ ...config, condition: condition } as ForStatementV0Config) : fields.condition; },
    increment(increment?: Expression | Expressions | SequenceExpression) { return increment !== undefined ? for_statement__v0_({ ...config, increment: increment } as ForStatementV0Config) : fields.increment; },
    initializer(initializer?: EmptyStatement | Expression | Expressions | LexicalDeclaration | SequenceExpression | VariableDeclaration) { return initializer !== undefined ? for_statement__v0_({ ...config, initializer: initializer } as ForStatementV0Config) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `for_statement` — v1 form. */
export function for_statement__v1_(
  config: ForStatementV1Config,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
    increment: config.increment,
    initializer: config.initializer,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    body(body?: Statement) { return body !== undefined ? for_statement__v1_({ ...config, body: body } as ForStatementV1Config) : fields.body; },
    condition(...condition: (EmptyStatement | Expression | Expressions | SequenceExpression)[]) { return condition.length ? for_statement__v1_({ ...config, condition: condition } as ForStatementV1Config) : fields.condition; },
    increment(increment?: Expression | Expressions | SequenceExpression) { return increment !== undefined ? for_statement__v1_({ ...config, increment: increment } as ForStatementV1Config) : fields.increment; },
    initializer(initializer?: EmptyStatement | Expression | Expressions | LexicalDeclaration | SequenceExpression | VariableDeclaration) { return initializer !== undefined ? for_statement__v1_({ ...config, initializer: initializer } as ForStatementV1Config) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formal_parameters_(
  ...children: (OptionalParameter | RequiredParameter)[]
) {
  return {
    type: 'formal_parameters' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (OptionalParameter | RequiredParameter)[]) { return formal_parameters_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_declaration_(
  config: ConfigOf<FunctionDeclaration>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_declaration' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? function_declaration_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? function_declaration_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? function_declaration_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? function_declaration_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_expression_(
  config: ConfigOf<FunctionExpression>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_expression' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? function_expression_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? function_expression_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? function_expression_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? function_expression_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_expression_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_signature_(
  config: ConfigOf<FunctionSignature>,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_signature' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? function_signature_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? function_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? function_signature_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_type_(
  config: ConfigOf<FunctionType>,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? function_type_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: Asserts | Type | TypePredicate) { return returnType !== undefined ? function_type_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_type_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generator_function_(
  config: ConfigOf<GeneratorFunction>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'generator_function' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? generator_function_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? generator_function_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? generator_function_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? generator_function_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? generator_function_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generator_function_declaration_(
  config: ConfigOf<GeneratorFunctionDeclaration>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'generator_function_declaration' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? generator_function_declaration_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? generator_function_declaration_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? generator_function_declaration_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? generator_function_declaration_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? generator_function_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_(
  config: ConfigOf<GenericType>,
) {
  const fields = {
    name: config.name,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    name(name?: NestedTypeIdentifier | TypeIdentifier) { return name !== undefined ? generic_type_({ ...config, name: name }) : fields.name; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? generic_type_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_statement_(
  config: ConfigOf<IfStatement>,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    alternative(alternative?: ElseClause) { return alternative !== undefined ? if_statement_({ ...config, alternative: alternative }) : fields.alternative; },
    condition(condition?: ParenthesizedExpression) { return condition !== undefined ? if_statement_({ ...config, condition: condition }) : fields.condition; },
    consequence(consequence?: Statement) { return consequence !== undefined ? if_statement_({ ...config, consequence: consequence }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function implements_clause_(
  ...children: (Type)[]
) {
  return {
    type: 'implements_clause' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Type)[]) { return implements_clause_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_alias_(
  config: ConfigOf<ImportAlias>,
) {
  const fields = {
    name: config.name,
    value: config.value,
    semicolon: config.semicolon,
  };
  return {
    type: 'import_alias' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? import_alias_({ ...config, name: name }) : fields.name; },
    value(value?: Identifier | NestedIdentifier) { return value !== undefined ? import_alias_({ ...config, value: value }) : fields.value; },
    semicolon(semicolon?: string) { return semicolon !== undefined ? import_alias_({ ...config, semicolon: semicolon }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_attribute_(
  config: ConfigOf<ImportAttribute>,
) {
  const fields = {
    object: config.object,
  };
  return {
    type: 'import_attribute' as const,
    named: true as const,
    fields,
    object(object?: Object) { return object !== undefined ? import_attribute_({ ...config, object: object }) : fields.object; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_clause_(
  config: ConfigOf<ImportClause>,
) {
  return import_clause__v0_(config as ImportClauseV0Config);
}

/** Variant factory: `import_clause` — v0 form. */
export function import_clause__v0_(
  config: ImportClauseV0Config,
) {
  const fields = {
    default_import: config.defaultImport,
  };
  return {
    type: 'import_clause' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    defaultImport(defaultImport?: Identifier | NamedImports | NamespaceImport) { return defaultImport !== undefined ? import_clause__v0_({ ...config, defaultImport: defaultImport } as ImportClauseV0Config) : fields.default_import; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `import_clause` — comma form. */
export function import_clause__comma_(
  config: ImportClauseCommaConfig,
) {
  const fields = {
    default_import: config.defaultImport,
  };
  return {
    type: 'import_clause' as const,
    named: true as const,
    variant: 'comma' as const,
    fields,
    defaultImport(defaultImport?: Identifier | NamedImports | NamespaceImport) { return defaultImport !== undefined ? import_clause__comma_({ ...config, defaultImport: defaultImport } as ImportClauseCommaConfig) : fields.default_import; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_require_clause_(
  config: ConfigOf<ImportRequireClause>,
) {
  const fields = {
    source: config.source,
    identifier: config.identifier,
  };
  return {
    type: 'import_require_clause' as const,
    named: true as const,
    fields,
    source(source?: String) { return source !== undefined ? import_require_clause_({ ...config, source: source }) : fields.source; },
    identifier(identifier?: Identifier) { return identifier !== undefined ? import_require_clause_({ ...config, identifier: identifier }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_specifier_(
  config: ConfigOf<ImportSpecifier>,
) {
  if ('alias' in config && config.alias !== undefined) return import_specifier__as_(config as ImportSpecifierAsConfig);
  return import_specifier__v0_(config as ImportSpecifierV0Config);
}

/** Variant factory: `import_specifier` — v0 form. */
export function import_specifier__v0_(
  config: ImportSpecifierV0Config,
) {
  const fields = {
    name: config.name,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    name(name?: ModuleExportName) { return name !== undefined ? import_specifier__v0_({ ...config, name: name } as ImportSpecifierV0Config) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `import_specifier` — as form. */
export function import_specifier__as_(
  config: ImportSpecifierAsConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    variant: 'as' as const,
    fields,
    alias(alias?: Identifier) { return alias !== undefined ? import_specifier__as_({ ...config, alias: alias } as ImportSpecifierAsConfig) : fields.alias; },
    name(name?: ModuleExportName) { return name !== undefined ? import_specifier__as_({ ...config, name: name } as ImportSpecifierAsConfig) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_statement_(
  config: ConfigOf<ImportStatement>,
) {
  if ('fromClause' in config && config.fromClause !== undefined) return import_statement__from_(config as ImportStatementFromConfig);
  return import_statement__v1_(config as ImportStatementV1Config);
}

/** Variant factory: `import_statement` — from form. */
export function import_statement__from_(
  config: ImportStatementFromConfig,
) {
  const fields = {
    source: config.source,
    import_clause: config.importClause,
    from_clause: config.fromClause,
    import_attribute: config.importAttribute,
    semicolon: config.semicolon,
  };
  return {
    type: 'import_statement' as const,
    named: true as const,
    variant: 'from' as const,
    fields,
    source(source?: String) { return source !== undefined ? import_statement__from_({ ...config, source: source } as ImportStatementFromConfig) : fields.source; },
    importClause(importClause?: ImportClause | ImportRequireClause) { return importClause !== undefined ? import_statement__from_({ ...config, importClause: importClause } as ImportStatementFromConfig) : fields.import_clause; },
    fromClause(fromClause?: string) { return fromClause !== undefined ? import_statement__from_({ ...config, fromClause: fromClause } as ImportStatementFromConfig) : fields.from_clause; },
    importAttribute(importAttribute?: ImportAttribute) { return importAttribute !== undefined ? import_statement__from_({ ...config, importAttribute: importAttribute } as ImportStatementFromConfig) : fields.import_attribute; },
    semicolon(semicolon?: string) { return semicolon !== undefined ? import_statement__from_({ ...config, semicolon: semicolon } as ImportStatementFromConfig) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `import_statement` — v1 form. */
export function import_statement__v1_(
  config: ImportStatementV1Config,
) {
  const fields = {
    import_clause: config.importClause,
    import_attribute: config.importAttribute,
    semicolon: config.semicolon,
  };
  return {
    type: 'import_statement' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    importClause(importClause?: ImportClause | ImportRequireClause) { return importClause !== undefined ? import_statement__v1_({ ...config, importClause: importClause } as ImportStatementV1Config) : fields.import_clause; },
    importAttribute(importAttribute?: ImportAttribute) { return importAttribute !== undefined ? import_statement__v1_({ ...config, importAttribute: importAttribute } as ImportStatementV1Config) : fields.import_attribute; },
    semicolon(semicolon?: string) { return semicolon !== undefined ? import_statement__v1_({ ...config, semicolon: semicolon } as ImportStatementV1Config) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `import_statement` — v2 form. */
export function import_statement__v2_(
  config: ImportStatementV2Config,
) {
  const fields = {
    source: config.source,
    import_attribute: config.importAttribute,
    semicolon: config.semicolon,
  };
  return {
    type: 'import_statement' as const,
    named: true as const,
    variant: 'v2' as const,
    fields,
    source(source?: String) { return source !== undefined ? import_statement__v2_({ ...config, source: source } as ImportStatementV2Config) : fields.source; },
    importAttribute(importAttribute?: ImportAttribute) { return importAttribute !== undefined ? import_statement__v2_({ ...config, importAttribute: importAttribute } as ImportStatementV2Config) : fields.import_attribute; },
    semicolon(semicolon?: string) { return semicolon !== undefined ? import_statement__v2_({ ...config, semicolon: semicolon } as ImportStatementV2Config) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function index_signature_(
  config: ConfigOf<IndexSignature>,
) {
  if ('name' in config && config.name !== undefined) return index_signature__colon_(config as IndexSignatureColonConfig);
  else if ('mappedTypeClause' in config && config.mappedTypeClause !== undefined) return index_signature__mapped_type_clause_(config as IndexSignatureMappedTypeClauseConfig);
  return index_signature__mapped_type_clause_(config as IndexSignatureMappedTypeClauseConfig);
}

/** Variant factory: `index_signature` — colon form. */
export function index_signature__colon_(
  config: IndexSignatureColonConfig,
) {
  const fields = {
    index_type: config.indexType,
    name: config.name,
    sign: config.sign,
    type: config.type,
  };
  return {
    type: 'index_signature' as const,
    named: true as const,
    variant: 'colon' as const,
    fields,
    indexType(indexType?: Type) { return indexType !== undefined ? index_signature__colon_({ ...config, indexType: indexType } as IndexSignatureColonConfig) : fields.index_type; },
    name(name?: Identifier) { return name !== undefined ? index_signature__colon_({ ...config, name: name } as IndexSignatureColonConfig) : fields.name; },
    sign(sign?: '+' | '-') { return sign !== undefined ? index_signature__colon_({ ...config, sign: sign } as IndexSignatureColonConfig) : fields.sign; },
    typeField(type_?: AddingTypeAnnotation | OmittingTypeAnnotation | OptingTypeAnnotation | TypeAnnotation) { return type_ !== undefined ? index_signature__colon_({ ...config, type: type_ } as IndexSignatureColonConfig) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `index_signature` — mapped_type_clause form. */
export function index_signature__mapped_type_clause_(
  config: IndexSignatureMappedTypeClauseConfig,
) {
  const fields = {
    sign: config.sign,
    type: config.type,
    mapped_type_clause: config.mappedTypeClause,
  };
  return {
    type: 'index_signature' as const,
    named: true as const,
    variant: 'mapped_type_clause' as const,
    fields,
    sign(sign?: '+' | '-') { return sign !== undefined ? index_signature__mapped_type_clause_({ ...config, sign: sign } as IndexSignatureMappedTypeClauseConfig) : fields.sign; },
    typeField(type_?: AddingTypeAnnotation | OmittingTypeAnnotation | OptingTypeAnnotation | TypeAnnotation) { return type_ !== undefined ? index_signature__mapped_type_clause_({ ...config, type: type_ } as IndexSignatureMappedTypeClauseConfig) : fields.type; },
    mappedTypeClause(mappedTypeClause?: MappedTypeClause) { return mappedTypeClause !== undefined ? index_signature__mapped_type_clause_({ ...config, mappedTypeClause: mappedTypeClause } as IndexSignatureMappedTypeClauseConfig) : fields.mapped_type_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function index_type_query_(
  config: ConfigOf<IndexTypeQuery>,
) {
  const fields = {
    primary_type: config.primaryType,
  };
  return {
    type: 'index_type_query' as const,
    named: true as const,
    fields,
    primaryType(primaryType?: PrimaryType) { return primaryType !== undefined ? index_type_query_({ ...config, primaryType: primaryType }) : fields.primary_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function infer_type_(
  config: ConfigOf<InferType>,
) {
  const fields = {
    type_identifier: config.typeIdentifier,
    constraint: config.constraint,
  };
  return {
    type: 'infer_type' as const,
    named: true as const,
    fields,
    typeIdentifier(typeIdentifier?: TypeIdentifier) { return typeIdentifier !== undefined ? infer_type_({ ...config, typeIdentifier: typeIdentifier }) : fields.type_identifier; },
    constraint(constraint?: Type | TypeIdentifier) { return constraint !== undefined ? infer_type_({ ...config, constraint: constraint }) : fields.constraint; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function instantiation_expression_(
  config: ConfigOf<InstantiationExpression>,
) {
  const fields = {
    function: config.function,
    type_arguments: config.typeArguments,
    expression: config.expression,
  };
  return {
    type: 'instantiation_expression' as const,
    named: true as const,
    fields,
    function(function_?: Identifier | Import | MemberExpression | SubscriptExpression) { return function_ !== undefined ? instantiation_expression_({ ...config, function: function_ }) : fields.function; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? instantiation_expression_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    expression(expression?: Expression) { return expression !== undefined ? instantiation_expression_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interface_body_(
  ...children: (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]
) {
  return {
    type: 'interface_body' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]) { return interface_body_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interface_declaration_(
  config: ConfigOf<InterfaceDeclaration>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
    extends_type_clause: config.extendsTypeClause,
  };
  return {
    type: 'interface_declaration' as const,
    named: true as const,
    fields,
    body(body?: InterfaceBody) { return body !== undefined ? interface_declaration_({ ...config, body: body }) : fields.body; },
    name(name?: TypeIdentifier) { return name !== undefined ? interface_declaration_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? interface_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    extendsTypeClause(extendsTypeClause?: ExtendsTypeClause) { return extendsTypeClause !== undefined ? interface_declaration_({ ...config, extendsTypeClause: extendsTypeClause }) : fields.extends_type_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function internal_module_(
  config: ConfigOf<InternalModule>,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'internal_module' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? internal_module_({ ...config, body: body }) : fields.body; },
    name(name?: ModuleExportName | NestedIdentifier) { return name !== undefined ? internal_module_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function intersection_type_(
  config: ConfigOf<IntersectionType>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'intersection_type' as const,
    named: true as const,
    fields,
    left(left?: Type) { return left !== undefined ? intersection_type_({ ...config, left: left }) : fields.left; },
    right(right?: Type) { return right !== undefined ? intersection_type_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function labeled_statement_(
  config: ConfigOf<LabeledStatement>,
) {
  const fields = {
    body: config.body,
    label: config.label,
  };
  return {
    type: 'labeled_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? labeled_statement_({ ...config, body: body }) : fields.body; },
    label(label?: StatementIdentifier) { return label !== undefined ? labeled_statement_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lexical_declaration_(
  config: ConfigOf<LexicalDeclaration>,
) {
  const fields = {
    kind: config.kind,
    declarators: config.declarators,
    semicolon: config.semicolon,
  };
  return {
    type: 'lexical_declaration' as const,
    named: true as const,
    fields,
    kind(kind?: 'const' | 'let') { return kind !== undefined ? lexical_declaration_({ ...config, kind: kind }) : fields.kind; },
    declarators(...declarators: (VariableDeclarator)[]) { return declarators.length ? lexical_declaration_({ ...config, declarators: declarators }) : fields.declarators; },
    semicolon(semicolon?: string) { return semicolon !== undefined ? lexical_declaration_({ ...config, semicolon: semicolon }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function literal_type_(
  child: False | Null | Number | String | True | UnaryExpression | Undefined,
) {
  const children = child ? [child] : [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    child(c?: False | Null | Number | String | True | UnaryExpression | Undefined) { return c !== undefined ? literal_type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lookup_type_(
  config: ConfigOf<LookupType>,
) {
  const fields = {
    primary_type: config.primaryType,
    index_type: config.indexType,
  };
  return {
    type: 'lookup_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType?: PrimaryType) { return primaryType !== undefined ? lookup_type_({ ...config, primaryType: primaryType }) : fields.primary_type; },
    indexType(indexType?: Type) { return indexType !== undefined ? lookup_type_({ ...config, indexType: indexType }) : fields.index_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function mapped_type_clause_(
  config: ConfigOf<MappedTypeClause>,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
    type: config.type,
  };
  return {
    type: 'mapped_type_clause' as const,
    named: true as const,
    fields,
    alias(alias?: Type) { return alias !== undefined ? mapped_type_clause_({ ...config, alias: alias }) : fields.alias; },
    name(name?: TypeIdentifier) { return name !== undefined ? mapped_type_clause_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? mapped_type_clause_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function member_expression_(
  config: ConfigOf<MemberExpression>,
) {
  const fields = {
    object: config.object,
    optional_chain: config.optionalChain,
    property: config.property,
  };
  return {
    type: 'member_expression' as const,
    named: true as const,
    fields,
    object(object?: Expression | Import | PrimaryExpression) { return object !== undefined ? member_expression_({ ...config, object: object }) : fields.object; },
    optionalChain(optionalChain?: OptionalChain) { return optionalChain !== undefined ? member_expression_({ ...config, optionalChain: optionalChain }) : fields.optional_chain; },
    property(property?: PrivatePropertyIdentifier | PropertyIdentifier) { return property !== undefined ? member_expression_({ ...config, property: property }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function method_definition_(
  config: ConfigOf<MethodDefinition>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  const children: unknown[] = [];
  return {
    type: 'method_definition' as const,
    named: true as const,
    fields,
    children,
    body(body?: StatementBlock) { return body !== undefined ? method_definition_({ ...config, body: body }) : fields.body; },
    name(name?: PropertyName) { return name !== undefined ? method_definition_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? method_definition_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? method_definition_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? method_definition_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? method_definition_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? method_definition_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function method_signature_(
  config: ConfigOf<MethodSignature>,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  const children: unknown[] = [];
  return {
    type: 'method_signature' as const,
    named: true as const,
    fields,
    children,
    name(name?: PropertyName) { return name !== undefined ? method_signature_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: FormalParameters) { return parameters !== undefined ? method_signature_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: AssertsAnnotation | TypeAnnotation | TypePredicateAnnotation) { return returnType !== undefined ? method_signature_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? method_signature_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? method_signature_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? method_signature_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function module_(
  config: ConfigOf<Module>,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'module' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? module_({ ...config, body: body }) : fields.body; },
    name(name?: ModuleExportName | NestedIdentifier) { return name !== undefined ? module_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function named_imports_(
  ...children: (ImportSpecifier)[]
) {
  return {
    type: 'named_imports' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (ImportSpecifier)[]) { return named_imports_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function namespace_export_(
  child: ModuleExportName,
) {
  const children = child ? [child] : [];
  return {
    type: 'namespace_export' as const,
    named: true as const,
    children,
    child(c?: ModuleExportName) { return c !== undefined ? namespace_export_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function namespace_import_(
  config: ConfigOf<NamespaceImport>,
) {
  const fields = {
    identifier: config.identifier,
  };
  return {
    type: 'namespace_import' as const,
    named: true as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? namespace_import_({ ...config, identifier: identifier }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nested_identifier_(
  config: ConfigOf<NestedIdentifier>,
) {
  const fields = {
    object: config.object,
    property: config.property,
  };
  return {
    type: 'nested_identifier' as const,
    named: true as const,
    fields,
    object(object?: Identifier | MemberExpression) { return object !== undefined ? nested_identifier_({ ...config, object: object }) : fields.object; },
    property(property?: PropertyIdentifier) { return property !== undefined ? nested_identifier_({ ...config, property: property }) : fields.property; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nested_type_identifier_(
  config: ConfigOf<NestedTypeIdentifier>,
) {
  const fields = {
    module: config.module,
    name: config.name,
  };
  return {
    type: 'nested_type_identifier' as const,
    named: true as const,
    fields,
    module(module?: Identifier | NestedIdentifier) { return module !== undefined ? nested_type_identifier_({ ...config, module: module }) : fields.module; },
    name(name?: TypeIdentifier) { return name !== undefined ? nested_type_identifier_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function new_expression_(
  config: ConfigOf<NewExpression>,
) {
  const fields = {
    arguments: config.arguments,
    constructor: config.constructor,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'new_expression' as const,
    named: true as const,
    fields,
    arguments(arguments_?: Arguments) { return arguments_ !== undefined ? new_expression_({ ...config, arguments: arguments_ }) : fields.arguments; },
    constructor(constructor?: PrimaryExpression) { return constructor !== undefined ? new_expression_({ ...config, constructor: constructor }) : fields.constructor; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? new_expression_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function non_null_expression_(
  config: ConfigOf<NonNullExpression>,
) {
  const fields = {
    expression: config.expression,
  };
  return {
    type: 'non_null_expression' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? non_null_expression_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_(
  ...children: (MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement)[]
) {
  return {
    type: 'object' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (MethodDefinition | Pair | ShorthandPropertyIdentifier | SpreadElement)[]) { return object_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_assignment_pattern_(
  config: ConfigOf<ObjectAssignmentPattern>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'object_assignment_pattern' as const,
    named: true as const,
    fields,
    left(left?: DestructuringPattern | ShorthandPropertyIdentifierPattern) { return left !== undefined ? object_assignment_pattern_({ ...config, left: left }) : fields.left; },
    right(right?: Expression) { return right !== undefined ? object_assignment_pattern_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_pattern_(
  ...children: (ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern)[]
) {
  return {
    type: 'object_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (ObjectAssignmentPattern | PairPattern | RestPattern | ShorthandPropertyIdentifierPattern)[]) { return object_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function object_type_(
  config?: ConfigOf<ObjectType>,
) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'object_type' as const,
    named: true as const,
    fields,
    opening(opening?: CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature) { return opening !== undefined ? object_type_({ ...config, opening: opening }) : fields.opening; },
    members(...members: (CallSignature | ConstructSignature | ExportStatement | IndexSignature | MethodSignature | PropertySignature)[]) { return members.length ? object_type_({ ...config, members: members }) : fields.members; },
    closing(closing?: string) { return closing !== undefined ? object_type_({ ...config, closing: closing }) : fields.closing; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function omitting_type_annotation_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'omitting_type_annotation' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? omitting_type_annotation_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function opting_type_annotation_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'opting_type_annotation' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? opting_type_annotation_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function optional_parameter_(
  config: ConfigOf<OptionalParameter>,
) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    pattern: config.pattern,
    type: config.type,
    value: config.value,
    parameter_name: config.parameterName,
    initializer: config.initializer,
  };
  return {
    type: 'optional_parameter' as const,
    named: true as const,
    fields,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? optional_parameter_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: Identifier) { return name !== undefined ? optional_parameter_({ ...config, name: name }) : fields.name; },
    pattern(pattern?: Pattern | This) { return pattern !== undefined ? optional_parameter_({ ...config, pattern: pattern }) : fields.pattern; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? optional_parameter_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? optional_parameter_({ ...config, value: value }) : fields.value; },
    parameterName(parameterName?: ParameterName) { return parameterName !== undefined ? optional_parameter_({ ...config, parameterName: parameterName }) : fields.parameter_name; },
    initializer(initializer?: ParameterName) { return initializer !== undefined ? optional_parameter_({ ...config, initializer: initializer }) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function optional_type_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'optional_type' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? optional_type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair_(
  config: ConfigOf<Pair>,
) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key?: PropertyName) { return key !== undefined ? pair_({ ...config, key: key }) : fields.key; },
    value(value?: Expression) { return value !== undefined ? pair_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair_pattern_(
  config: ConfigOf<PairPattern>,
) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    type: 'pair_pattern' as const,
    named: true as const,
    fields,
    key(key?: PropertyName) { return key !== undefined ? pair_pattern_({ ...config, key: key }) : fields.key; },
    value(value?: AssignmentPattern | Pattern) { return value !== undefined ? pair_pattern_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_expression_(
  config: ConfigOf<ParenthesizedExpression>,
) {
  if ('type' in config && config.type !== undefined) return parenthesized_expression__type_(config as ParenthesizedExpressionTypeConfig);
  return parenthesized_expression__v1_(config as ParenthesizedExpressionV1Config);
}

/** Variant factory: `parenthesized_expression` — type form. */
export function parenthesized_expression__type_(
  config?: ParenthesizedExpressionTypeConfig,
) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    variant: 'type' as const,
    fields,
    children,
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? parenthesized_expression__type_({ ...config, type: type_ } as ParenthesizedExpressionTypeConfig) : fields.type; },
    child(child?: CallExpression | Expression | Identifier | MemberExpression | SequenceExpression) { return child !== undefined ? parenthesized_expression__type_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `parenthesized_expression` — v1 form. */
export function parenthesized_expression__v1_(
  config?: ParenthesizedExpressionV1Config,
) {
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    variant: 'v1' as const,
    fields: {},
    children,
    child(child?: CallExpression | Expression | Identifier | MemberExpression | SequenceExpression) { return child !== undefined ? parenthesized_expression__v1_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesized_type_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'parenthesized_type' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? parenthesized_type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function program_(
  config?: ConfigOf<Program>,
) {
  const fields = {
    hash_bang_line: config?.hashBangLine,
    statements: config?.statements,
  };
  return {
    type: 'program' as const,
    named: true as const,
    fields,
    hashBangLine(hashBangLine?: HashBangLine) { return hashBangLine !== undefined ? program_({ ...config, hashBangLine: hashBangLine }) : fields.hash_bang_line; },
    statements(...statements: (Statement)[]) { return statements.length ? program_({ ...config, statements: statements }) : fields.statements; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function property_signature_(
  config: ConfigOf<PropertySignature>,
) {
  const fields = {
    name: config.name,
    type: config.type,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
  };
  return {
    type: 'property_signature' as const,
    named: true as const,
    fields,
    name(name?: PropertyName) { return name !== undefined ? property_signature_({ ...config, name: name }) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? property_signature_({ ...config, type: type_ }) : fields.type; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? property_signature_({ ...config, accessibilityModifier: accessibilityModifier }) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? property_signature_({ ...config, overrideModifier: overrideModifier }) : fields.override_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function public_field_definition_(
  config: ConfigOf<PublicFieldDefinition>,
) {
  if ('overrideModifier' in config && config.overrideModifier !== undefined) return public_field_definition__static_(config as PublicFieldDefinitionStaticConfig);
  return public_field_definition__abstract_(config as PublicFieldDefinitionAbstractConfig);
}

/** Variant factory: `public_field_definition` — static form. */
export function public_field_definition__static_(
  config: PublicFieldDefinitionStaticConfig,
) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type: config.type,
    value: config.value,
    accessibility_modifier: config.accessibilityModifier,
    override_modifier: config.overrideModifier,
    initializer: config.initializer,
  };
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    variant: 'static' as const,
    fields,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? public_field_definition__static_({ ...config, decorator: decorator } as PublicFieldDefinitionStaticConfig) : fields.decorator; },
    name(name?: PropertyName) { return name !== undefined ? public_field_definition__static_({ ...config, name: name } as PublicFieldDefinitionStaticConfig) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? public_field_definition__static_({ ...config, type: type_ } as PublicFieldDefinitionStaticConfig) : fields.type; },
    value(value?: Expression) { return value !== undefined ? public_field_definition__static_({ ...config, value: value } as PublicFieldDefinitionStaticConfig) : fields.value; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? public_field_definition__static_({ ...config, accessibilityModifier: accessibilityModifier } as PublicFieldDefinitionStaticConfig) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier?: OverrideModifier) { return overrideModifier !== undefined ? public_field_definition__static_({ ...config, overrideModifier: overrideModifier } as PublicFieldDefinitionStaticConfig) : fields.override_modifier; },
    initializer(initializer?: string) { return initializer !== undefined ? public_field_definition__static_({ ...config, initializer: initializer } as PublicFieldDefinitionStaticConfig) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `public_field_definition` — abstract form. */
export function public_field_definition__abstract_(
  config: PublicFieldDefinitionAbstractConfig,
) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type: config.type,
    value: config.value,
    accessibility_modifier: config.accessibilityModifier,
    initializer: config.initializer,
  };
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    variant: 'abstract' as const,
    fields,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? public_field_definition__abstract_({ ...config, decorator: decorator } as PublicFieldDefinitionAbstractConfig) : fields.decorator; },
    name(name?: PropertyName) { return name !== undefined ? public_field_definition__abstract_({ ...config, name: name } as PublicFieldDefinitionAbstractConfig) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? public_field_definition__abstract_({ ...config, type: type_ } as PublicFieldDefinitionAbstractConfig) : fields.type; },
    value(value?: Expression) { return value !== undefined ? public_field_definition__abstract_({ ...config, value: value } as PublicFieldDefinitionAbstractConfig) : fields.value; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? public_field_definition__abstract_({ ...config, accessibilityModifier: accessibilityModifier } as PublicFieldDefinitionAbstractConfig) : fields.accessibility_modifier; },
    initializer(initializer?: string) { return initializer !== undefined ? public_field_definition__abstract_({ ...config, initializer: initializer } as PublicFieldDefinitionAbstractConfig) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `public_field_definition` — accessor form. */
export function public_field_definition__accessor_(
  config: PublicFieldDefinitionAccessorConfig,
) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    type: config.type,
    value: config.value,
    accessibility_modifier: config.accessibilityModifier,
    initializer: config.initializer,
  };
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    variant: 'accessor' as const,
    fields,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? public_field_definition__accessor_({ ...config, decorator: decorator } as PublicFieldDefinitionAccessorConfig) : fields.decorator; },
    name(name?: PropertyName) { return name !== undefined ? public_field_definition__accessor_({ ...config, name: name } as PublicFieldDefinitionAccessorConfig) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? public_field_definition__accessor_({ ...config, type: type_ } as PublicFieldDefinitionAccessorConfig) : fields.type; },
    value(value?: Expression) { return value !== undefined ? public_field_definition__accessor_({ ...config, value: value } as PublicFieldDefinitionAccessorConfig) : fields.value; },
    accessibilityModifier(accessibilityModifier?: AccessibilityModifier) { return accessibilityModifier !== undefined ? public_field_definition__accessor_({ ...config, accessibilityModifier: accessibilityModifier } as PublicFieldDefinitionAccessorConfig) : fields.accessibility_modifier; },
    initializer(initializer?: string) { return initializer !== undefined ? public_field_definition__accessor_({ ...config, initializer: initializer } as PublicFieldDefinitionAccessorConfig) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function readonly_type_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'readonly_type' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? readonly_type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function regex_(
  config: ConfigOf<Regex>,
) {
  const fields = {
    flags: config.flags,
    pattern: config.pattern,
  };
  return {
    type: 'regex' as const,
    named: true as const,
    fields,
    flags(flags?: RegexFlags) { return flags !== undefined ? regex_({ ...config, flags: flags }) : fields.flags; },
    pattern(pattern?: RegexPattern) { return pattern !== undefined ? regex_({ ...config, pattern: pattern }) : fields.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function required_parameter_(
  config: ConfigOf<RequiredParameter>,
) {
  const fields = {
    decorator: config.decorator,
    name: config.name,
    pattern: config.pattern,
    type: config.type,
    value: config.value,
    parameter_name: config.parameterName,
    initializer: config.initializer,
  };
  return {
    type: 'required_parameter' as const,
    named: true as const,
    fields,
    decorator(...decorator: (Decorator)[]) { return decorator.length ? required_parameter_({ ...config, decorator: decorator }) : fields.decorator; },
    name(name?: Identifier | RestPattern) { return name !== undefined ? required_parameter_({ ...config, name: name }) : fields.name; },
    pattern(pattern?: Pattern | This) { return pattern !== undefined ? required_parameter_({ ...config, pattern: pattern }) : fields.pattern; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? required_parameter_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? required_parameter_({ ...config, value: value }) : fields.value; },
    parameterName(parameterName?: ParameterName) { return parameterName !== undefined ? required_parameter_({ ...config, parameterName: parameterName }) : fields.parameter_name; },
    initializer(initializer?: ParameterName) { return initializer !== undefined ? required_parameter_({ ...config, initializer: initializer }) : fields.initializer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function rest_pattern_(
  child: LhsExpression,
) {
  const children = child ? [child] : [];
  return {
    type: 'rest_pattern' as const,
    named: true as const,
    children,
    child(c?: LhsExpression) { return c !== undefined ? rest_pattern_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function rest_type_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'rest_type' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? rest_type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function return_statement_(
  child?: Expression | SequenceExpression,
) {
  const children = child ? [child] : [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    child(c?: Expression | SequenceExpression) { return c !== undefined ? return_statement_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function satisfies_expression_(
  config: ConfigOf<SatisfiesExpression>,
) {
  const fields = {
    expression: config.expression,
    type_annotation: config.typeAnnotation,
  };
  return {
    type: 'satisfies_expression' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? satisfies_expression_({ ...config, expression: expression }) : fields.expression; },
    typeAnnotation(typeAnnotation?: Type) { return typeAnnotation !== undefined ? satisfies_expression_({ ...config, typeAnnotation: typeAnnotation }) : fields.type_annotation; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function sequence_expression_(
  ...children: (Expression)[]
) {
  return {
    type: 'sequence_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression)[]) { return sequence_expression_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function spread_element_(
  config: ConfigOf<SpreadElement>,
) {
  const fields = {
    expression: config.expression,
  };
  return {
    type: 'spread_element' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? spread_element_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function statement_block_(
  config?: ConfigOf<StatementBlock>,
) {
  const fields = {
    statements: config?.statements,
    automatic_semicolon: config?.automaticSemicolon,
  };
  return {
    type: 'statement_block' as const,
    named: true as const,
    fields,
    statements(...statements: (Statement)[]) { return statements.length ? statement_block_({ ...config, statements: statements }) : fields.statements; },
    automaticSemicolon(automaticSemicolon?: string) { return automaticSemicolon !== undefined ? statement_block_({ ...config, automaticSemicolon: automaticSemicolon }) : fields.automatic_semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string_(
  ...children: (EscapeSequence | StringFragment)[]
) {
  return {
    type: 'string' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (EscapeSequence | StringFragment)[]) { return string_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `string` — tok_22 form. */
export function string__tok_22_(
  config?: StringTok_22Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'string' as const,
    named: true as const,
    variant: 'tok_22' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (EscapeSequence | StringFragment)[]) { return string__tok_22_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `string` — tok_27 form. */
export function string__tok_27_(
  config?: StringTok_27Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'string' as const,
    named: true as const,
    variant: 'tok_27' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (EscapeSequence | StringFragment)[]) { return string__tok_27_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscript_expression_(
  config: ConfigOf<SubscriptExpression>,
) {
  const fields = {
    index: config.index,
    object: config.object,
    optional_chain: config.optionalChain,
  };
  return {
    type: 'subscript_expression' as const,
    named: true as const,
    fields,
    index(index?: Expression | Expressions | Number | PredefinedType | SequenceExpression | String) { return index !== undefined ? subscript_expression_({ ...config, index: index }) : fields.index; },
    object(object?: Expression | PrimaryExpression) { return object !== undefined ? subscript_expression_({ ...config, object: object }) : fields.object; },
    optionalChain(optionalChain?: OptionalChain) { return optionalChain !== undefined ? subscript_expression_({ ...config, optionalChain: optionalChain }) : fields.optional_chain; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_body_(
  ...children: (SwitchCase | SwitchDefault)[]
) {
  return {
    type: 'switch_body' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (SwitchCase | SwitchDefault)[]) { return switch_body_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_case_(
  config: ConfigOf<SwitchCase>,
) {
  const fields = {
    body: config.body,
    value: config.value,
  };
  return {
    type: 'switch_case' as const,
    named: true as const,
    fields,
    body(...body: (Statement)[]) { return body.length ? switch_case_({ ...config, body: body }) : fields.body; },
    value(value?: Expression | Expressions | SequenceExpression) { return value !== undefined ? switch_case_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_default_(
  config?: ConfigOf<SwitchDefault>,
) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'switch_default' as const,
    named: true as const,
    fields,
    body(...body: (Statement)[]) { return body.length ? switch_default_({ ...config, body: body }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function switch_statement_(
  config: ConfigOf<SwitchStatement>,
) {
  const fields = {
    body: config.body,
    value: config.value,
  };
  return {
    type: 'switch_statement' as const,
    named: true as const,
    fields,
    body(body?: SwitchBody) { return body !== undefined ? switch_statement_({ ...config, body: body }) : fields.body; },
    value(value?: ParenthesizedExpression) { return value !== undefined ? switch_statement_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_literal_type_(
  ...children: (StringFragment | TemplateType)[]
) {
  return {
    type: 'template_literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (StringFragment | TemplateType)[]) { return template_literal_type_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_string_(
  ...children: (EscapeSequence | StringFragment | TemplateSubstitution)[]
) {
  return {
    type: 'template_string' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (EscapeSequence | StringFragment | TemplateSubstitution)[]) { return template_string_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_substitution_(
  child: Expression | SequenceExpression,
) {
  const children = child ? [child] : [];
  return {
    type: 'template_substitution' as const,
    named: true as const,
    children,
    child(c?: Expression | SequenceExpression) { return c !== undefined ? template_substitution_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function template_type_(
  child: InferType | PrimaryType,
) {
  const children = child ? [child] : [];
  return {
    type: 'template_type' as const,
    named: true as const,
    children,
    child(c?: InferType | PrimaryType) { return c !== undefined ? template_type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ternary_expression_(
  config: ConfigOf<TernaryExpression>,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'ternary_expression' as const,
    named: true as const,
    fields,
    alternative(alternative?: Expression) { return alternative !== undefined ? ternary_expression_({ ...config, alternative: alternative }) : fields.alternative; },
    condition(condition?: Expression) { return condition !== undefined ? ternary_expression_({ ...config, condition: condition }) : fields.condition; },
    consequence(consequence?: Expression) { return consequence !== undefined ? ternary_expression_({ ...config, consequence: consequence }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function throw_statement_(
  child: Expression | SequenceExpression,
) {
  const children = child ? [child] : [];
  return {
    type: 'throw_statement' as const,
    named: true as const,
    children,
    child(c?: Expression | SequenceExpression) { return c !== undefined ? throw_statement_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_statement_(
  config: ConfigOf<TryStatement>,
) {
  const fields = {
    body: config.body,
    finalizer: config.finalizer,
    handler: config.handler,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    body(body?: StatementBlock) { return body !== undefined ? try_statement_({ ...config, body: body }) : fields.body; },
    finalizer(finalizer?: FinallyClause) { return finalizer !== undefined ? try_statement_({ ...config, finalizer: finalizer }) : fields.finalizer; },
    handler(handler?: CatchClause) { return handler !== undefined ? try_statement_({ ...config, handler: handler }) : fields.handler; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_type_(
  ...children: (OptionalParameter | OptionalType | RequiredParameter | RestType | Type)[]
) {
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (OptionalParameter | OptionalType | RequiredParameter | RestType | Type)[]) { return tuple_type_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_alias_declaration_(
  config: ConfigOf<TypeAliasDeclaration>,
) {
  const fields = {
    name: config.name,
    type_parameters: config.typeParameters,
    value: config.value,
  };
  return {
    type: 'type_alias_declaration' as const,
    named: true as const,
    fields,
    name(name?: TypeIdentifier) { return name !== undefined ? type_alias_declaration_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? type_alias_declaration_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    value(value?: Type) { return value !== undefined ? type_alias_declaration_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_annotation_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'type_annotation' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? type_annotation_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_arguments_(
  ...children: (Type)[]
) {
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Type)[]) { return type_arguments_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_assertion_(
  config: ConfigOf<TypeAssertion>,
) {
  const fields = {
    type_arguments: config.typeArguments,
    expression: config.expression,
  };
  return {
    type: 'type_assertion' as const,
    named: true as const,
    fields,
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? type_assertion_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    expression(expression?: Expression) { return expression !== undefined ? type_assertion_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameter_(
  config: ConfigOf<TypeParameter>,
) {
  const fields = {
    constraint: config.constraint,
    name: config.name,
    value: config.value,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    constraint(constraint?: Constraint) { return constraint !== undefined ? type_parameter_({ ...config, constraint: constraint }) : fields.constraint; },
    name(name?: TypeIdentifier) { return name !== undefined ? type_parameter_({ ...config, name: name }) : fields.name; },
    value(value?: DefaultType) { return value !== undefined ? type_parameter_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameters_(
  ...children: (TypeParameter)[]
) {
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (TypeParameter)[]) { return type_parameters_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_predicate_(
  config: ConfigOf<TypePredicate>,
) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    type: 'type_predicate' as const,
    named: true as const,
    fields,
    name(name?: Identifier | This) { return name !== undefined ? type_predicate_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? type_predicate_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_predicate_annotation_(
  config: ConfigOf<TypePredicateAnnotation>,
) {
  const fields = {
    type_predicate: config.typePredicate,
  };
  return {
    type: 'type_predicate_annotation' as const,
    named: true as const,
    fields,
    typePredicate(typePredicate?: TypePredicate) { return typePredicate !== undefined ? type_predicate_annotation_({ ...config, typePredicate: typePredicate }) : fields.type_predicate; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_query_(
  child: CallExpression | Identifier | InstantiationExpression | MemberExpression | SubscriptExpression | This,
) {
  const children = child ? [child] : [];
  return {
    type: 'type_query' as const,
    named: true as const,
    children,
    child(c?: CallExpression | Identifier | InstantiationExpression | MemberExpression | SubscriptExpression | This) { return c !== undefined ? type_query_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unary_expression_(
  config: ConfigOf<UnaryExpression>,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    argument(argument?: Expression | Number) { return argument !== undefined ? unary_expression_({ ...config, argument: argument }) : fields.argument; },
    operator(operator?: '!' | '+' | '-' | 'delete' | 'typeof' | 'void' | '~') { return operator !== undefined ? unary_expression_({ ...config, operator: operator }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function union_type_(
  config: ConfigOf<UnionType>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left?: Type) { return left !== undefined ? union_type_({ ...config, left: left }) : fields.left; },
    right(right?: Type) { return right !== undefined ? union_type_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function update_expression_(
  config: ConfigOf<UpdateExpression>,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'update_expression' as const,
    named: true as const,
    fields,
    argument(argument?: Expression) { return argument !== undefined ? update_expression_({ ...config, argument: argument }) : fields.argument; },
    operator(operator?: '++' | '--') { return operator !== undefined ? update_expression_({ ...config, operator: operator }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function variable_declaration_(
  config: ConfigOf<VariableDeclaration>,
) {
  const fields = {
    declarators: config.declarators,
    semicolon: config.semicolon,
  };
  return {
    type: 'variable_declaration' as const,
    named: true as const,
    fields,
    declarators(...declarators: (VariableDeclarator)[]) { return declarators.length ? variable_declaration_({ ...config, declarators: declarators }) : fields.declarators; },
    semicolon(semicolon?: string) { return semicolon !== undefined ? variable_declaration_({ ...config, semicolon: semicolon }) : fields.semicolon; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function variable_declarator_(
  config: ConfigOf<VariableDeclarator>,
) {
  if ('value' in config && config.value !== undefined) return variable_declarator__eq_(config as VariableDeclaratorEqConfig);
  return variable_declarator__bang_(config as VariableDeclaratorBangConfig);
}

/** Variant factory: `variable_declarator` — eq form. */
export function variable_declarator__eq_(
  config: VariableDeclaratorEqConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  return {
    type: 'variable_declarator' as const,
    named: true as const,
    variant: 'eq' as const,
    fields,
    name(name?: DestructuringPattern | Identifier) { return name !== undefined ? variable_declarator__eq_({ ...config, name: name } as VariableDeclaratorEqConfig) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? variable_declarator__eq_({ ...config, type: type_ } as VariableDeclaratorEqConfig) : fields.type; },
    value(value?: Expression) { return value !== undefined ? variable_declarator__eq_({ ...config, value: value } as VariableDeclaratorEqConfig) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `variable_declarator` — bang form. */
export function variable_declarator__bang_(
  config: VariableDeclaratorBangConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    type: 'variable_declarator' as const,
    named: true as const,
    variant: 'bang' as const,
    fields,
    name(name?: DestructuringPattern | Identifier) { return name !== undefined ? variable_declarator__bang_({ ...config, name: name } as VariableDeclaratorBangConfig) : fields.name; },
    typeField(type_?: TypeAnnotation) { return type_ !== undefined ? variable_declarator__bang_({ ...config, type: type_ } as VariableDeclaratorBangConfig) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function while_statement_(
  config: ConfigOf<WhileStatement>,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? while_statement_({ ...config, body: body }) : fields.body; },
    condition(condition?: ParenthesizedExpression) { return condition !== undefined ? while_statement_({ ...config, condition: condition }) : fields.condition; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function with_statement_(
  config: ConfigOf<WithStatement>,
) {
  const fields = {
    body: config.body,
    object: config.object,
  };
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    body(body?: Statement) { return body !== undefined ? with_statement_({ ...config, body: body }) : fields.body; },
    object(object?: ParenthesizedExpression) { return object !== undefined ? with_statement_({ ...config, object: object }) : fields.object; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function yield_expression_(
  config?: ConfigOf<YieldExpression>,
) {
  return yield_expression__star_(config as YieldExpressionStarConfig);
}

/** Variant factory: `yield_expression` — star form. */
export function yield_expression__star_(
  config?: YieldExpressionStarConfig,
) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'yield_expression' as const,
    named: true as const,
    variant: 'star' as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? yield_expression__star_({ ...config, expression: expression } as YieldExpressionStarConfig) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `yield_expression` — v1 form. */
export function yield_expression__v1_(
  config?: YieldExpressionV1Config,
) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'yield_expression' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? yield_expression__v1_({ ...config, expression: expression } as YieldExpressionV1Config) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function accessibility_modifier_(text: string) {
  return {
    type: 'accessibility_modifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: AccessibilityModifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function debugger_statement_(text: string) {
  return {
    type: 'debugger_statement' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: DebuggerStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function empty_statement_() {
  return {
    type: 'empty_statement' as const,
    named: true as const,
    text: ';',
    render: () => ';',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: ';' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: ';' };
    },
    replace: (target: EmptyStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: ';' }; },
  };
}

export function existential_type_() {
  return {
    type: 'existential_type' as const,
    named: true as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: ExistentialTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function import_() {
  return {
    type: 'import' as const,
    named: true as const,
    text: 'import',
    render: () => 'import',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'import' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'import' };
    },
    replace: (target: ImportTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' }; },
  };
}

export function meta_property_(text: string) {
  if (!/^(?:new\.target|import\.meta)$/.test(text)) throw new Error(`Invalid meta_property: '${text}' does not match grammar pattern`);
  return {
    type: 'meta_property' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: MetaPropertyTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function optional_chain_() {
  return {
    type: 'optional_chain' as const,
    named: true as const,
    text: '?.',
    render: () => '?.',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '?.' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '?.' };
    },
    replace: (target: OptionalChainTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '?.' }; },
  };
}

export function override_modifier_() {
  return {
    type: 'override_modifier' as const,
    named: true as const,
    text: 'override',
    render: () => 'override',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'override' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'override' };
    },
    replace: (target: OverrideModifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' }; },
  };
}

export function predefined_type_(text: string) {
  return {
    type: 'predefined_type' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PredefinedTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment_(text: string) {
  return {
    type: 'comment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: CommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escape_sequence_(text: string) {
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeSequenceTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'false',
    render: () => 'false',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'false' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'false' };
    },
    replace: (target: FalseTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' }; },
  };
}

export function hash_bang_line_(text: string) {
  if (!/^#!.*$/.test(text)) throw new Error(`Invalid hash_bang_line: '${text}' does not match grammar pattern`);
  return {
    type: 'hash_bang_line' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: HashBangLineTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function html_comment_(text: string) {
  return {
    type: 'html_comment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: HtmlCommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function null_() {
  return {
    type: 'null' as const,
    named: true as const,
    text: 'null',
    render: () => 'null',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'null' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'null' };
    },
    replace: (target: NullTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' }; },
  };
}

export function number_(text: string) {
  if (!/^(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:(?:0|0?[1-9]_?\d(_?\d)*?)\.\d(_?\d)*?(?:e|E)(?:-|\+)?\d(_?\d)*?|\.\d(_?\d)*(?:e|E)(?:-|\+)?\d(_?\d)*?|(?:0|0?[1-9]_?\d(_?\d)*?)(?:e|E)(?:-|\+)?\d(_?\d)*|\d(_?\d)*)|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|\d(_?\d)*)n)$/.test(text)) throw new Error(`Invalid number: '${text}' does not match grammar pattern`);
  return {
    type: 'number' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: NumberTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function private_property_identifier_(text: string) {
  return {
    type: 'private_property_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PrivatePropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function property_identifier_(text: string) {
  return {
    type: 'property_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regex_flags_(text: string) {
  if (!/^[a-z]+$/.test(text)) throw new Error(`Invalid regex_flags: '${text}' does not match grammar pattern`);
  return {
    type: 'regex_flags' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: RegexFlagsTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regex_pattern_(text: string) {
  if (!/^(?:(?:\[(?:(?:\\.|[^\]\n\\]))*\]|\\.|[^/\\\[\n]))+$/.test(text)) throw new Error(`Invalid regex_pattern: '${text}' does not match grammar pattern`);
  return {
    type: 'regex_pattern' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: RegexPatternTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthand_property_identifier_(text: string) {
  return {
    type: 'shorthand_property_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShorthandPropertyIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthand_property_identifier_pattern_(text: string) {
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShorthandPropertyIdentifierPatternTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function statement_identifier_(text: string) {
  return {
    type: 'statement_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StatementIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function string_fragment_(text: string) {
  return {
    type: 'string_fragment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringFragmentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super',
    render: () => 'super',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'super' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'super' };
    },
    replace: (target: SuperTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; },
  };
}

export function this_() {
  return {
    type: 'this' as const,
    named: true as const,
    text: 'this',
    render: () => 'this',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'this' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'this' };
    },
    replace: (target: ThisTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' }; },
  };
}

export function this_type_(text: string) {
  return {
    type: 'this_type' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ThisTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'true',
    render: () => 'true',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'true' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'true' };
    },
    replace: (target: TrueTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' }; },
  };
}

export function type_identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid type_identifier`);
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: TypeIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function undefined_() {
  return {
    type: 'undefined' as const,
    named: true as const,
    text: 'undefined',
    render: () => 'undefined',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'undefined' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'undefined' };
    },
    replace: (target: UndefinedTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' }; },
  };
}


/** @internal Map of kind string to factory function. */
export const _factoryMap: Record<string, (config?: any) => unknown> = {
  'abstract_class_declaration': abstract_class_declaration_,
  'abstract_method_signature': abstract_method_signature_,
  'adding_type_annotation': adding_type_annotation_,
  'ambient_declaration': ambient_declaration_,
  'arguments': arguments_,
  'array': array_,
  'array_pattern': array_pattern_,
  'array_type': array_type_,
  'arrow_function': arrow_function_,
  'as_expression': as_expression_,
  'asserts': asserts_,
  'asserts_annotation': asserts_annotation_,
  'assignment_expression': assignment_expression_,
  'assignment_pattern': assignment_pattern_,
  'augmented_assignment_expression': augmented_assignment_expression_,
  'await_expression': await_expression_,
  'binary_expression': binary_expression_,
  'break_statement': break_statement_,
  'call_expression': call_expression_,
  'call_signature': call_signature_,
  'catch_clause': catch_clause_,
  'class': class_,
  'class_body': class_body_,
  'class_declaration': class_declaration_,
  'class_heritage': class_heritage_,
  'class_static_block': class_static_block_,
  'computed_property_name': computed_property_name_,
  'conditional_type': conditional_type_,
  'constraint': constraint_,
  'construct_signature': construct_signature_,
  'constructor_type': constructor_type_,
  'continue_statement': continue_statement_,
  'decorator': decorator_,
  'default_type': default_type_,
  'do_statement': do_statement_,
  'else_clause': else_clause_,
  'enum_assignment': enum_assignment_,
  'enum_body': enum_body_,
  'enum_declaration': enum_declaration_,
  'export_clause': export_clause_,
  'export_specifier': export_specifier_,
  'export_statement': export_statement_,
  'expression_statement': expression_statement_,
  'extends_clause': extends_clause_,
  'extends_type_clause': extends_type_clause_,
  'finally_clause': finally_clause_,
  'flow_maybe_type': flow_maybe_type_,
  'for_in_statement': for_in_statement_,
  'for_statement': for_statement_,
  'formal_parameters': formal_parameters_,
  'function_declaration': function_declaration_,
  'function_expression': function_expression_,
  'function_signature': function_signature_,
  'function_type': function_type_,
  'generator_function': generator_function_,
  'generator_function_declaration': generator_function_declaration_,
  'generic_type': generic_type_,
  'if_statement': if_statement_,
  'implements_clause': implements_clause_,
  'import_alias': import_alias_,
  'import_attribute': import_attribute_,
  'import_clause': import_clause_,
  'import_require_clause': import_require_clause_,
  'import_specifier': import_specifier_,
  'import_statement': import_statement_,
  'index_signature': index_signature_,
  'index_type_query': index_type_query_,
  'infer_type': infer_type_,
  'instantiation_expression': instantiation_expression_,
  'interface_body': interface_body_,
  'interface_declaration': interface_declaration_,
  'internal_module': internal_module_,
  'intersection_type': intersection_type_,
  'labeled_statement': labeled_statement_,
  'lexical_declaration': lexical_declaration_,
  'literal_type': literal_type_,
  'lookup_type': lookup_type_,
  'mapped_type_clause': mapped_type_clause_,
  'member_expression': member_expression_,
  'method_definition': method_definition_,
  'method_signature': method_signature_,
  'module': module_,
  'named_imports': named_imports_,
  'namespace_export': namespace_export_,
  'namespace_import': namespace_import_,
  'nested_identifier': nested_identifier_,
  'nested_type_identifier': nested_type_identifier_,
  'new_expression': new_expression_,
  'non_null_expression': non_null_expression_,
  'object': object_,
  'object_assignment_pattern': object_assignment_pattern_,
  'object_pattern': object_pattern_,
  'object_type': object_type_,
  'omitting_type_annotation': omitting_type_annotation_,
  'opting_type_annotation': opting_type_annotation_,
  'optional_parameter': optional_parameter_,
  'optional_type': optional_type_,
  'pair': pair_,
  'pair_pattern': pair_pattern_,
  'parenthesized_expression': parenthesized_expression_,
  'parenthesized_type': parenthesized_type_,
  'program': program_,
  'property_signature': property_signature_,
  'public_field_definition': public_field_definition_,
  'readonly_type': readonly_type_,
  'regex': regex_,
  'required_parameter': required_parameter_,
  'rest_pattern': rest_pattern_,
  'rest_type': rest_type_,
  'return_statement': return_statement_,
  'satisfies_expression': satisfies_expression_,
  'sequence_expression': sequence_expression_,
  'spread_element': spread_element_,
  'statement_block': statement_block_,
  'string': string_,
  'subscript_expression': subscript_expression_,
  'switch_body': switch_body_,
  'switch_case': switch_case_,
  'switch_default': switch_default_,
  'switch_statement': switch_statement_,
  'template_literal_type': template_literal_type_,
  'template_string': template_string_,
  'template_substitution': template_substitution_,
  'template_type': template_type_,
  'ternary_expression': ternary_expression_,
  'throw_statement': throw_statement_,
  'try_statement': try_statement_,
  'tuple_type': tuple_type_,
  'type_alias_declaration': type_alias_declaration_,
  'type_annotation': type_annotation_,
  'type_arguments': type_arguments_,
  'type_assertion': type_assertion_,
  'type_parameter': type_parameter_,
  'type_parameters': type_parameters_,
  'type_predicate': type_predicate_,
  'type_predicate_annotation': type_predicate_annotation_,
  'type_query': type_query_,
  'unary_expression': unary_expression_,
  'union_type': union_type_,
  'update_expression': update_expression_,
  'variable_declaration': variable_declaration_,
  'variable_declarator': variable_declarator_,
  'while_statement': while_statement_,
  'with_statement': with_statement_,
  'yield_expression': yield_expression_,
};

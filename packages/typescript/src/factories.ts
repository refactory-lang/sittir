// Auto-generated by @sittir/codegen — do not edit

import type { NodeData, Edit, AssignableNode } from '@sittir/core';
import { render, toEdit } from '@sittir/core';
import { isNodeData, type FromValue } from './utils.js';
import { rules } from './rules.js';
import { joinBy } from './joinby.js';

const RESERVED_KEYWORDS = new Set([
  'abstract',
  'accessor',
  'any',
  'as',
  'assert',
  'async',
  'await',
  'boolean',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'delete',
  'do',
  'else',
  'enum',
  'export',
  'extends',
  'finally',
  'for',
  'from',
  'function',
  'get',
  'global',
  'if',
  'implements',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'let',
  'meta',
  'namespace',
  'never',
  'new',
  'of',
  'override',
  'private',
  'protected',
  'public',
  'readonly',
  'require',
  'return',
  'satisfies',
  'set',
  'static',
  'switch',
  'symbol',
  'target',
  'throw',
  'try',
  'typeof',
  'unknown',
  'using',
  'var',
  'void',
  'while',
  'with',
  'yield',
]);

export interface AbstractClassDeclarationConfig {
  decorator?: NodeData<'decorator'>[];
  name: NodeData<'type_identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  body: NodeData<'class_body'>;
  children?: NodeData[];
}

export interface AbstractClassDeclarationFromInput {
  decorator?: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput;
  name: NodeData<'type_identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  body: NodeData<'class_body'> | ClassBodyFromInput | FromValue[];
  children?: (NodeData<'class_heritage'> | ClassHeritageFromInput)[];
}

export type AbstractClassDeclarationNode = NodeData<'abstract_class_declaration'> & {
  decorator(value: NodeData<'decorator'>[]): AbstractClassDeclarationNode;
  typeParameters(value: NodeData<'type_parameters'>): AbstractClassDeclarationNode;
  body(value: NodeData<'class_body'>): AbstractClassDeclarationNode;
  children(...value: NodeData[]): AbstractClassDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'abstract_class_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AbstractClassDeclarationFromNode = NodeData<'abstract_class_declaration'> & {
  decorator(value: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput): AbstractClassDeclarationFromNode;
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): AbstractClassDeclarationFromNode;
  body(value: NodeData<'class_body'> | ClassBodyFromInput | FromValue[]): AbstractClassDeclarationFromNode;
  children(...value: FromValue[]): AbstractClassDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'abstract_class_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function abstractClassDeclaration(
  nameOrConfig: NodeData<'type_identifier'> | AbstractClassDeclarationConfig,
  config?: Partial<AbstractClassDeclarationConfig>,
): AbstractClassDeclarationNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'abstract_class_declaration', fields };
  node.decorator = (...v: any[]) => { fields['decorator'] = v; return node; };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

abstractClassDeclaration.assign = function(target: AssignableNode<'abstract_class_declaration'>): AbstractClassDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['decorator', 'name', 'type_parameters', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'abstract_class_declaration'; }, get fields() { return getFields(); } };
  node.decorator = (...v: any[]) => { overrides['decorator'] = v; return node; };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AbstractMethodSignatureConfig {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
  children?: NodeData[];
}

export interface AbstractMethodSignatureFromInput {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput) | FromValue[];
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
  children?: (NodeData<'accessibility_modifier' | 'override_modifier'> | string)[];
}

export type AbstractMethodSignatureNode = NodeData<'abstract_method_signature'> & {
  typeParameters(value: NodeData<'type_parameters'>): AbstractMethodSignatureNode;
  parameters(value: NodeData<'formal_parameters'>): AbstractMethodSignatureNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): AbstractMethodSignatureNode;
  children(...value: NodeData[]): AbstractMethodSignatureNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'abstract_method_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AbstractMethodSignatureFromNode = NodeData<'abstract_method_signature'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): AbstractMethodSignatureFromNode;
  parameters(value: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[]): AbstractMethodSignatureFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): AbstractMethodSignatureFromNode;
  children(...value: FromValue[]): AbstractMethodSignatureFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'abstract_method_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function abstractMethodSignature(
  nameOrConfig: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | AbstractMethodSignatureConfig,
  config?: Partial<AbstractMethodSignatureConfig>,
): AbstractMethodSignatureNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'abstract_method_signature', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

abstractMethodSignature.assign = function(target: AssignableNode<'abstract_method_signature'>): AbstractMethodSignatureNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'abstract_method_signature'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AddingTypeAnnotationConfig {
  children?: NodeData[];
}

export interface AddingTypeAnnotationFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type AddingTypeAnnotationNode = NodeData<'adding_type_annotation'> & {
  children(...value: NodeData[]): AddingTypeAnnotationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'adding_type_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AddingTypeAnnotationFromNode = NodeData<'adding_type_annotation'> & {
  children(...value: FromValue[]): AddingTypeAnnotationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'adding_type_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function addingTypeAnnotation(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | AddingTypeAnnotationConfig,
): AddingTypeAnnotationNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'adding_type_annotation', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

addingTypeAnnotation.assign = function(target: AssignableNode<'adding_type_annotation'>): AddingTypeAnnotationNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'adding_type_annotation'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AmbientDeclarationConfig {
  children?: NodeData[];
}

export interface AmbientDeclarationFromInput {
  children?: (NodeData<'declaration' | 'statement_block' | 'property_identifier' | 'type'> | string | ({ kind: 'declaration' } & DeclarationFromInput) | ({ kind: 'statement_block' } & StatementBlockFromInput) | ({ kind: 'type' } & TypeFromInput))[];
}

export type AmbientDeclarationNode = NodeData<'ambient_declaration'> & {
  children(...value: NodeData[]): AmbientDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'ambient_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AmbientDeclarationFromNode = NodeData<'ambient_declaration'> & {
  children(...value: FromValue[]): AmbientDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'ambient_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function ambientDeclaration(
  childrenOrConfig?: NodeData<'declaration' | 'statement_block' | 'property_identifier' | 'type'> | NodeData<'declaration' | 'statement_block' | 'property_identifier' | 'type'>[] | AmbientDeclarationConfig,
): AmbientDeclarationNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'ambient_declaration', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

ambientDeclaration.assign = function(target: AssignableNode<'ambient_declaration'>): AmbientDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'ambient_declaration'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArgumentsConfig {
  children?: NodeData[];
}

export interface ArgumentsFromInput {
  children?: (NodeData<'expression' | 'spread_element'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'spread_element' } & SpreadElementFromInput))[];
}

export type ArgumentsNode = NodeData<'arguments'> & {
  children(...value: NodeData[]): ArgumentsNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'arguments'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ArgumentsFromNode = NodeData<'arguments'> & {
  children(...value: FromValue[]): ArgumentsFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'arguments'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function arguments_(
  childrenOrConfig?: NodeData<'expression' | 'spread_element'> | NodeData<'expression' | 'spread_element'>[] | ArgumentsConfig,
): ArgumentsNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'arguments', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

arguments_.assign = function(target: AssignableNode<'arguments'>): ArgumentsNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'arguments'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArrayConfig {
  children?: NodeData[];
}

export interface ArrayFromInput {
  children?: (NodeData<'expression' | 'spread_element'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'spread_element' } & SpreadElementFromInput))[];
}

export type ArrayNode = NodeData<'array'> & {
  children(...value: NodeData[]): ArrayNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'array'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ArrayFromNode = NodeData<'array'> & {
  children(...value: FromValue[]): ArrayFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'array'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function array(
  childrenOrConfig?: NodeData<'expression' | 'spread_element'> | NodeData<'expression' | 'spread_element'>[] | ArrayConfig,
): ArrayNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'array', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

array.assign = function(target: AssignableNode<'array'>): ArrayNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'array'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArrayPatternConfig {
  children?: NodeData[];
}

export interface ArrayPatternFromInput {
  children?: (NodeData<'pattern' | 'assignment_pattern'> | ({ kind: 'pattern' } & PatternFromInput) | ({ kind: 'assignment_pattern' } & AssignmentPatternFromInput))[];
}

export type ArrayPatternNode = NodeData<'array_pattern'> & {
  children(...value: NodeData[]): ArrayPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'array_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ArrayPatternFromNode = NodeData<'array_pattern'> & {
  children(...value: FromValue[]): ArrayPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'array_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function arrayPattern(
  childrenOrConfig?: NodeData<'pattern' | 'assignment_pattern'> | NodeData<'pattern' | 'assignment_pattern'>[] | ArrayPatternConfig,
): ArrayPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'array_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

arrayPattern.assign = function(target: AssignableNode<'array_pattern'>): ArrayPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'array_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArrayTypeConfig {
  children?: NodeData[];
}

export interface ArrayTypeFromInput {
  children?: (NodeData<'primary_type'> | PrimaryTypeFromInput)[];
}

export type ArrayTypeNode = NodeData<'array_type'> & {
  children(...value: NodeData[]): ArrayTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'array_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ArrayTypeFromNode = NodeData<'array_type'> & {
  children(...value: FromValue[]): ArrayTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'array_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function arrayType(
  childrenOrConfig?: NodeData<'primary_type'> | NodeData<'primary_type'>[] | ArrayTypeConfig,
): ArrayTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'array_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

arrayType.assign = function(target: AssignableNode<'array_type'>): ArrayTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'array_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArrowFunctionConfig {
  parameter?: NodeData<'identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters?: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
  body: NodeData<'expression' | 'statement_block'>;
}

export interface ArrowFunctionFromInput {
  parameter?: NodeData<'identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters?: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
  body: NodeData<'expression' | 'statement_block'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'statement_block' } & StatementBlockFromInput) | FromValue[];
}

export type ArrowFunctionNode = NodeData<'arrow_function'> & {
  parameter(value: NodeData<'identifier'>): ArrowFunctionNode;
  typeParameters(value: NodeData<'type_parameters'>): ArrowFunctionNode;
  parameters(value: NodeData<'formal_parameters'>): ArrowFunctionNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): ArrowFunctionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'arrow_function'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ArrowFunctionFromNode = NodeData<'arrow_function'> & {
  parameter(value: NodeData<'identifier'> | string): ArrowFunctionFromNode;
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): ArrowFunctionFromNode;
  parameters(value: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[]): ArrowFunctionFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): ArrowFunctionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'arrow_function'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function arrowFunction(
  bodyOrConfig: NodeData<'expression' | 'statement_block'> | ArrowFunctionConfig,
  config?: Partial<ArrowFunctionConfig>,
): ArrowFunctionNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'arrow_function', fields };
  node.parameter = (v: any) => { fields['parameter'] = v; return node; };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

arrowFunction.assign = function(target: AssignableNode<'arrow_function'>): ArrowFunctionNode {
  const overrides: any = {};
  const fieldList: string[] = ['parameter', 'type_parameters', 'parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'arrow_function'; }, get fields() { return getFields(); } };
  node.parameter = (v: any) => { overrides['parameter'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AsExpressionConfig {
  children?: NodeData[];
}

export interface AsExpressionFromInput {
  children?: (NodeData<'expression' | 'type'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'type' } & TypeFromInput))[];
}

export type AsExpressionNode = NodeData<'as_expression'> & {
  children(...value: NodeData[]): AsExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'as_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AsExpressionFromNode = NodeData<'as_expression'> & {
  children(...value: FromValue[]): AsExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'as_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function asExpression(
  childrenOrConfig?: NodeData<'expression' | 'type'> | NodeData<'expression' | 'type'>[] | AsExpressionConfig,
): AsExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'as_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

asExpression.assign = function(target: AssignableNode<'as_expression'>): AsExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'as_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AssertsConfig {
  children?: NodeData[];
}

export interface AssertsFromInput {
  children?: (NodeData<'type_predicate' | 'identifier' | 'this'> | string | TypePredicateFromInput)[];
}

export type AssertsNode = NodeData<'asserts'> & {
  children(...value: NodeData[]): AssertsNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'asserts'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AssertsFromNode = NodeData<'asserts'> & {
  children(...value: FromValue[]): AssertsFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'asserts'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function asserts(
  childrenOrConfig?: NodeData<'type_predicate' | 'identifier' | 'this'> | NodeData<'type_predicate' | 'identifier' | 'this'>[] | AssertsConfig,
): AssertsNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'asserts', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

asserts.assign = function(target: AssignableNode<'asserts'>): AssertsNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'asserts'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AssertsAnnotationConfig {
  children?: NodeData[];
}

export interface AssertsAnnotationFromInput {
  children?: (NodeData<'asserts'> | AssertsFromInput)[];
}

export type AssertsAnnotationNode = NodeData<'asserts_annotation'> & {
  children(...value: NodeData[]): AssertsAnnotationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'asserts_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AssertsAnnotationFromNode = NodeData<'asserts_annotation'> & {
  children(...value: FromValue[]): AssertsAnnotationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'asserts_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function assertsAnnotation(
  childrenOrConfig?: NodeData<'asserts'> | NodeData<'asserts'>[] | AssertsAnnotationConfig,
): AssertsAnnotationNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'asserts_annotation', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

assertsAnnotation.assign = function(target: AssignableNode<'asserts_annotation'>): AssertsAnnotationNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'asserts_annotation'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AssignmentExpressionConfig {
  left: NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression' | 'parenthesized_expression'>;
  right: NodeData<'expression'>;
}

export interface AssignmentExpressionFromInput {
  left: NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression' | 'parenthesized_expression'> | string | ({ kind: 'member_expression' } & MemberExpressionFromInput) | ({ kind: 'subscript_expression' } & SubscriptExpressionFromInput) | ({ kind: 'object_pattern' } & ObjectPatternFromInput) | ({ kind: 'array_pattern' } & ArrayPatternFromInput) | ({ kind: 'non_null_expression' } & NonNullExpressionFromInput) | ({ kind: 'parenthesized_expression' } & ParenthesizedExpressionFromInput) | FromValue[];
  right: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type AssignmentExpressionNode = NodeData<'assignment_expression'> & {
  right(value: NodeData<'expression'>): AssignmentExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assignment_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AssignmentExpressionFromNode = NodeData<'assignment_expression'> & {
  right(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): AssignmentExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assignment_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function assignmentExpression(
  leftOrConfig: NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression' | 'parenthesized_expression'> | AssignmentExpressionConfig,
  config?: Partial<AssignmentExpressionConfig>,
): AssignmentExpressionNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'assignment_expression', fields };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

assignmentExpression.assign = function(target: AssignableNode<'assignment_expression'>): AssignmentExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'assignment_expression'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AssignmentPatternConfig {
  left: NodeData<'pattern'>;
  right: NodeData<'expression'>;
}

export interface AssignmentPatternFromInput {
  left: NodeData<'pattern'> | PatternFromInput | FromValue[];
  right: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type AssignmentPatternNode = NodeData<'assignment_pattern'> & {
  right(value: NodeData<'expression'>): AssignmentPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assignment_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AssignmentPatternFromNode = NodeData<'assignment_pattern'> & {
  right(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): AssignmentPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assignment_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function assignmentPattern(
  leftOrConfig: NodeData<'pattern'> | AssignmentPatternConfig,
  config?: Partial<AssignmentPatternConfig>,
): AssignmentPatternNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'assignment_pattern', fields };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

assignmentPattern.assign = function(target: AssignableNode<'assignment_pattern'>): AssignmentPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'assignment_pattern'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AugmentedAssignmentExpressionConfig {
  left: NodeData<'member_expression' | 'subscript_expression' | 'identifier' | 'parenthesized_expression' | 'non_null_expression'>;
  operator: NodeData<string>;
  right: NodeData<'expression'>;
}

export interface AugmentedAssignmentExpressionFromInput {
  left: NodeData<'member_expression' | 'subscript_expression' | 'identifier' | 'parenthesized_expression' | 'non_null_expression'> | string | ({ kind: 'member_expression' } & MemberExpressionFromInput) | ({ kind: 'subscript_expression' } & SubscriptExpressionFromInput) | ({ kind: 'parenthesized_expression' } & ParenthesizedExpressionFromInput) | ({ kind: 'non_null_expression' } & NonNullExpressionFromInput) | FromValue[];
  operator: NodeData | '+=' | '-=' | '*=' | '/=' | '%=' | '^=' | '&=' | '|=' | '>>=' | '>>>=' | '<<=' | '**=' | '&&=' | '||=' | '??=';
  right: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type AugmentedAssignmentExpressionNode = NodeData<'augmented_assignment_expression'> & {
  operator(value: NodeData<string>): AugmentedAssignmentExpressionNode;
  right(value: NodeData<'expression'>): AugmentedAssignmentExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'augmented_assignment_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AugmentedAssignmentExpressionFromNode = NodeData<'augmented_assignment_expression'> & {
  operator(value: NodeData | '+=' | '-=' | '*=' | '/=' | '%=' | '^=' | '&=' | '|=' | '>>=' | '>>>=' | '<<=' | '**=' | '&&=' | '||=' | '??='): AugmentedAssignmentExpressionFromNode;
  right(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): AugmentedAssignmentExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'augmented_assignment_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function augmentedAssignmentExpression(
  leftOrConfig: NodeData<'member_expression' | 'subscript_expression' | 'identifier' | 'parenthesized_expression' | 'non_null_expression'> | AugmentedAssignmentExpressionConfig,
  config?: Partial<AugmentedAssignmentExpressionConfig>,
): AugmentedAssignmentExpressionNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'augmented_assignment_expression', fields };
  node.operator = (v: any) => { fields['operator'] = v; return node; };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

augmentedAssignmentExpression.assign = function(target: AssignableNode<'augmented_assignment_expression'>): AugmentedAssignmentExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'operator', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'augmented_assignment_expression'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AwaitExpressionConfig {
  children?: NodeData[];
}

export interface AwaitExpressionFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type AwaitExpressionNode = NodeData<'await_expression'> & {
  children(...value: NodeData[]): AwaitExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'await_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AwaitExpressionFromNode = NodeData<'await_expression'> & {
  children(...value: FromValue[]): AwaitExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'await_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function awaitExpression(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | AwaitExpressionConfig,
): AwaitExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'await_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

awaitExpression.assign = function(target: AssignableNode<'await_expression'>): AwaitExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'await_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BinaryExpressionConfig {
  left: NodeData<'expression' | 'private_property_identifier'>;
  operator: NodeData<string>;
  right: NodeData<'expression'>;
}

export interface BinaryExpressionFromInput {
  left: NodeData<'expression' | 'private_property_identifier'> | string | ExpressionFromInput | FromValue[];
  operator: NodeData | '&&' | '||' | '>>' | '>>>' | '<<' | '&' | '^' | '|' | '+' | '-' | '*' | '/' | '%' | '**' | '<' | '<=' | '==' | '===' | '!=' | '!==' | '>=' | '>' | '??' | 'instanceof' | 'in';
  right: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type BinaryExpressionNode = NodeData<'binary_expression'> & {
  operator(value: NodeData<string>): BinaryExpressionNode;
  right(value: NodeData<'expression'>): BinaryExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'binary_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type BinaryExpressionFromNode = NodeData<'binary_expression'> & {
  operator(value: NodeData | '&&' | '||' | '>>' | '>>>' | '<<' | '&' | '^' | '|' | '+' | '-' | '*' | '/' | '%' | '**' | '<' | '<=' | '==' | '===' | '!=' | '!==' | '>=' | '>' | '??' | 'instanceof' | 'in'): BinaryExpressionFromNode;
  right(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): BinaryExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'binary_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function binaryExpression(
  leftOrConfig: NodeData<'expression' | 'private_property_identifier'> | BinaryExpressionConfig,
  config?: Partial<BinaryExpressionConfig>,
): BinaryExpressionNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'binary_expression', fields };
  node.operator = (v: any) => { fields['operator'] = v; return node; };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

binaryExpression.assign = function(target: AssignableNode<'binary_expression'>): BinaryExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'operator', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'binary_expression'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BreakStatementConfig {
  label?: NodeData<'statement_identifier'>;
}

export interface BreakStatementFromInput {
  label?: NodeData<'statement_identifier'> | string;
}

export type BreakStatementNode = NodeData<'break_statement'> & {
  label(value: NodeData<'statement_identifier'>): BreakStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'break_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type BreakStatementFromNode = NodeData<'break_statement'> & {
  label(value: NodeData<'statement_identifier'> | string): BreakStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'break_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function breakStatement(
  config?: BreakStatementConfig,
): BreakStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'break_statement', fields };
  node.label = (v: any) => { fields['label'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

breakStatement.assign = function(target: AssignableNode<'break_statement'>): BreakStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['label'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'break_statement'; }, get fields() { return getFields(); } };
  node.label = (v: any) => { overrides['label'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CallExpressionConfig {
  function: NodeData<'expression' | 'import' | 'primary_expression' | 'new_expression'>;
  type_arguments?: NodeData<'type_arguments'>;
  arguments: NodeData<'arguments' | 'template_string'>;
}

export interface CallExpressionFromInput {
  function: NodeData<'expression' | 'import' | 'primary_expression' | 'new_expression'> | string | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'primary_expression' } & PrimaryExpressionFromInput) | ({ kind: 'new_expression' } & NewExpressionFromInput) | FromValue[];
  type_arguments?: NodeData<'type_arguments'> | TypeArgumentsFromInput | FromValue[];
  arguments: NodeData<'arguments' | 'template_string'> | ({ kind: 'arguments' } & ArgumentsFromInput) | ({ kind: 'template_string' } & TemplateStringFromInput) | FromValue[];
}

export type CallExpressionNode = NodeData<'call_expression'> & {
  typeArguments(value: NodeData<'type_arguments'>): CallExpressionNode;
  arguments(value: NodeData<'arguments' | 'template_string'>): CallExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'call_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type CallExpressionFromNode = NodeData<'call_expression'> & {
  typeArguments(value: NodeData<'type_arguments'> | TypeArgumentsFromInput | FromValue[]): CallExpressionFromNode;
  arguments(value: NodeData<'arguments' | 'template_string'> | ({ kind: 'arguments' } & ArgumentsFromInput) | ({ kind: 'template_string' } & TemplateStringFromInput) | FromValue[]): CallExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'call_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function callExpression(
  functionOrConfig: NodeData<'expression' | 'import' | 'primary_expression' | 'new_expression'> | CallExpressionConfig,
  config?: Partial<CallExpressionConfig>,
): CallExpressionNode {
  const fields: any = isNodeData(functionOrConfig)
    ? { 'function': functionOrConfig, ...config }
    : functionOrConfig;
  const node: any = { type: 'call_expression', fields };
  node.typeArguments = (v: any) => { fields['type_arguments'] = v; return node; };
  node.arguments = (v: any) => { fields['arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

callExpression.assign = function(target: AssignableNode<'call_expression'>): CallExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['function', 'type_arguments', 'arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'call_expression'; }, get fields() { return getFields(); } };
  node.function = (v: any) => { overrides['function'] = v; return node; };
  node.typeArguments = (v: any) => { overrides['type_arguments'] = v; return node; };
  node.arguments = (v: any) => { overrides['arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CallSignatureConfig {
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
}

export interface CallSignatureFromInput {
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
}

export type CallSignatureNode = NodeData<'call_signature'> & {
  typeParameters(value: NodeData<'type_parameters'>): CallSignatureNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): CallSignatureNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'call_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type CallSignatureFromNode = NodeData<'call_signature'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): CallSignatureFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): CallSignatureFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'call_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function callSignature(
  parametersOrConfig: NodeData<'formal_parameters'> | CallSignatureConfig,
  config?: Partial<CallSignatureConfig>,
): CallSignatureNode {
  const fields: any = isNodeData(parametersOrConfig)
    ? { 'parameters': parametersOrConfig, ...config }
    : parametersOrConfig;
  const node: any = { type: 'call_signature', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

callSignature.assign = function(target: AssignableNode<'call_signature'>): CallSignatureNode {
  const overrides: any = {};
  const fieldList: string[] = ['type_parameters', 'parameters', 'return_type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'call_signature'; }, get fields() { return getFields(); } };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CatchClauseConfig {
  parameter?: NodeData<'object_pattern' | 'array_pattern' | 'identifier'>;
  type?: NodeData<'type_annotation'>;
  body: NodeData<'statement_block'>;
}

export interface CatchClauseFromInput {
  parameter?: NodeData<'object_pattern' | 'array_pattern' | 'identifier'> | string | ({ kind: 'object_pattern' } & ObjectPatternFromInput) | ({ kind: 'array_pattern' } & ArrayPatternFromInput) | FromValue[];
  type?: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[];
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type CatchClauseNode = NodeData<'catch_clause'> & {
  parameter(value: NodeData<'object_pattern' | 'array_pattern' | 'identifier'>): CatchClauseNode;
  typeField(value: NodeData<'type_annotation'>): CatchClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'catch_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type CatchClauseFromNode = NodeData<'catch_clause'> & {
  parameter(value: NodeData<'object_pattern' | 'array_pattern' | 'identifier'> | string | ({ kind: 'object_pattern' } & ObjectPatternFromInput) | ({ kind: 'array_pattern' } & ArrayPatternFromInput) | FromValue[]): CatchClauseFromNode;
  typeField(value: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[]): CatchClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'catch_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function catchClause(
  bodyOrConfig: NodeData<'statement_block'> | CatchClauseConfig,
  config?: Partial<CatchClauseConfig>,
): CatchClauseNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'catch_clause', fields };
  node.parameter = (v: any) => { fields['parameter'] = v; return node; };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

catchClause.assign = function(target: AssignableNode<'catch_clause'>): CatchClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['parameter', 'type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'catch_clause'; }, get fields() { return getFields(); } };
  node.parameter = (v: any) => { overrides['parameter'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClassConfig {
  decorator?: NodeData<'decorator'>[];
  name?: NodeData<'type_identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  body: NodeData<'class_body'>;
  children?: NodeData[];
}

export interface ClassFromInput {
  decorator?: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput;
  name?: NodeData<'type_identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  body: NodeData<'class_body'> | ClassBodyFromInput | FromValue[];
  children?: (NodeData<'class_heritage'> | ClassHeritageFromInput)[];
}

export type ClassNode = NodeData<'class'> & {
  decorator(value: NodeData<'decorator'>[]): ClassNode;
  name(value: NodeData<'type_identifier'>): ClassNode;
  typeParameters(value: NodeData<'type_parameters'>): ClassNode;
  children(...value: NodeData[]): ClassNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ClassFromNode = NodeData<'class'> & {
  decorator(value: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput): ClassFromNode;
  name(value: NodeData<'type_identifier'> | string): ClassFromNode;
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): ClassFromNode;
  children(...value: FromValue[]): ClassFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function class_(
  bodyOrConfig: NodeData<'class_body'> | ClassConfig,
  config?: Partial<ClassConfig>,
): ClassNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'class', fields };
  node.decorator = (...v: any[]) => { fields['decorator'] = v; return node; };
  node.name = (v: any) => { fields['name'] = v; return node; };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

class_.assign = function(target: AssignableNode<'class'>): ClassNode {
  const overrides: any = {};
  const fieldList: string[] = ['decorator', 'name', 'type_parameters', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'class'; }, get fields() { return getFields(); } };
  node.decorator = (...v: any[]) => { overrides['decorator'] = v; return node; };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClassBodyConfig {
  decorator?: NodeData<'decorator'>[];
  children?: NodeData[];
}

export interface ClassBodyFromInput {
  decorator?: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput;
  children?: (NodeData<'method_definition' | 'method_signature' | 'class_static_block' | 'abstract_method_signature' | 'index_signature' | 'public_field_definition'> | ({ kind: 'method_definition' } & MethodDefinitionFromInput) | ({ kind: 'method_signature' } & MethodSignatureFromInput) | ({ kind: 'class_static_block' } & ClassStaticBlockFromInput) | ({ kind: 'abstract_method_signature' } & AbstractMethodSignatureFromInput) | ({ kind: 'index_signature' } & IndexSignatureFromInput) | ({ kind: 'public_field_definition' } & PublicFieldDefinitionFromInput))[];
}

export type ClassBodyNode = NodeData<'class_body'> & {
  decorator(value: NodeData<'decorator'>[]): ClassBodyNode;
  children(...value: NodeData[]): ClassBodyNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_body'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ClassBodyFromNode = NodeData<'class_body'> & {
  decorator(value: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput): ClassBodyFromNode;
  children(...value: FromValue[]): ClassBodyFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_body'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function classBody(
  childrenOrConfig?: NodeData<'method_definition' | 'method_signature' | 'class_static_block' | 'abstract_method_signature' | 'index_signature' | 'public_field_definition'> | NodeData<'method_definition' | 'method_signature' | 'class_static_block' | 'abstract_method_signature' | 'index_signature' | 'public_field_definition'>[] | ClassBodyConfig,
): ClassBodyNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'class_body', fields };
  node.decorator = (...v: any[]) => { fields['decorator'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

classBody.assign = function(target: AssignableNode<'class_body'>): ClassBodyNode {
  const overrides: any = {};
  const fieldList: string[] = ['decorator'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'class_body'; }, get fields() { return getFields(); } };
  node.decorator = (...v: any[]) => { overrides['decorator'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClassDeclarationConfig {
  decorator?: NodeData<'decorator'>[];
  name: NodeData<'type_identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  body: NodeData<'class_body'>;
  children?: NodeData[];
}

export interface ClassDeclarationFromInput {
  decorator?: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput;
  name: NodeData<'type_identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  body: NodeData<'class_body'> | ClassBodyFromInput | FromValue[];
  children?: (NodeData<'class_heritage'> | ClassHeritageFromInput)[];
}

export type ClassDeclarationNode = NodeData<'class_declaration'> & {
  decorator(value: NodeData<'decorator'>[]): ClassDeclarationNode;
  typeParameters(value: NodeData<'type_parameters'>): ClassDeclarationNode;
  body(value: NodeData<'class_body'>): ClassDeclarationNode;
  children(...value: NodeData[]): ClassDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ClassDeclarationFromNode = NodeData<'class_declaration'> & {
  decorator(value: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput): ClassDeclarationFromNode;
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): ClassDeclarationFromNode;
  body(value: NodeData<'class_body'> | ClassBodyFromInput | FromValue[]): ClassDeclarationFromNode;
  children(...value: FromValue[]): ClassDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function classDeclaration(
  nameOrConfig: NodeData<'type_identifier'> | ClassDeclarationConfig,
  config?: Partial<ClassDeclarationConfig>,
): ClassDeclarationNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'class_declaration', fields };
  node.decorator = (...v: any[]) => { fields['decorator'] = v; return node; };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

classDeclaration.assign = function(target: AssignableNode<'class_declaration'>): ClassDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['decorator', 'name', 'type_parameters', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'class_declaration'; }, get fields() { return getFields(); } };
  node.decorator = (...v: any[]) => { overrides['decorator'] = v; return node; };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClassHeritageConfig {
  children?: NodeData[];
}

export interface ClassHeritageFromInput {
  children?: (NodeData<'extends_clause' | 'implements_clause'> | ({ kind: 'extends_clause' } & ExtendsClauseFromInput) | ({ kind: 'implements_clause' } & ImplementsClauseFromInput))[];
}

export type ClassHeritageNode = NodeData<'class_heritage'> & {
  children(...value: NodeData[]): ClassHeritageNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_heritage'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ClassHeritageFromNode = NodeData<'class_heritage'> & {
  children(...value: FromValue[]): ClassHeritageFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_heritage'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function classHeritage(
  childrenOrConfig?: NodeData<'extends_clause' | 'implements_clause'> | NodeData<'extends_clause' | 'implements_clause'>[] | ClassHeritageConfig,
): ClassHeritageNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'class_heritage', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

classHeritage.assign = function(target: AssignableNode<'class_heritage'>): ClassHeritageNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'class_heritage'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClassStaticBlockConfig {
  body: NodeData<'statement_block'>;
}

export interface ClassStaticBlockFromInput {
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type ClassStaticBlockNode = NodeData<'class_static_block'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_static_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ClassStaticBlockFromNode = NodeData<'class_static_block'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_static_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function classStaticBlock(
  bodyOrConfig: NodeData<'statement_block'> | ClassStaticBlockConfig,
  config?: Partial<ClassStaticBlockConfig>,
): ClassStaticBlockNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'class_static_block', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

classStaticBlock.assign = function(target: AssignableNode<'class_static_block'>): ClassStaticBlockNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'class_static_block'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ComputedPropertyNameConfig {
  children?: NodeData[];
}

export interface ComputedPropertyNameFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type ComputedPropertyNameNode = NodeData<'computed_property_name'> & {
  children(...value: NodeData[]): ComputedPropertyNameNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'computed_property_name'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ComputedPropertyNameFromNode = NodeData<'computed_property_name'> & {
  children(...value: FromValue[]): ComputedPropertyNameFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'computed_property_name'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function computedPropertyName(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | ComputedPropertyNameConfig,
): ComputedPropertyNameNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'computed_property_name', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

computedPropertyName.assign = function(target: AssignableNode<'computed_property_name'>): ComputedPropertyNameNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'computed_property_name'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConditionalTypeConfig {
  left: NodeData<'type'>;
  right: NodeData<'type'>;
  consequence: NodeData<'type'>;
  alternative: NodeData<'type'>;
}

export interface ConditionalTypeFromInput {
  left: NodeData<'type'> | TypeFromInput | FromValue[];
  right: NodeData<'type'> | TypeFromInput | FromValue[];
  consequence: NodeData<'type'> | TypeFromInput | FromValue[];
  alternative: NodeData<'type'> | TypeFromInput | FromValue[];
}

export type ConditionalTypeNode = NodeData<'conditional_type'> & {
  right(value: NodeData<'type'>): ConditionalTypeNode;
  consequence(value: NodeData<'type'>): ConditionalTypeNode;
  alternative(value: NodeData<'type'>): ConditionalTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'conditional_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ConditionalTypeFromNode = NodeData<'conditional_type'> & {
  right(value: NodeData<'type'> | TypeFromInput | FromValue[]): ConditionalTypeFromNode;
  consequence(value: NodeData<'type'> | TypeFromInput | FromValue[]): ConditionalTypeFromNode;
  alternative(value: NodeData<'type'> | TypeFromInput | FromValue[]): ConditionalTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'conditional_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function conditionalType(
  leftOrConfig: NodeData<'type'> | ConditionalTypeConfig,
  config?: Partial<ConditionalTypeConfig>,
): ConditionalTypeNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'conditional_type', fields };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.consequence = (v: any) => { fields['consequence'] = v; return node; };
  node.alternative = (v: any) => { fields['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

conditionalType.assign = function(target: AssignableNode<'conditional_type'>): ConditionalTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right', 'consequence', 'alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'conditional_type'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.consequence = (v: any) => { overrides['consequence'] = v; return node; };
  node.alternative = (v: any) => { overrides['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConstraintConfig {
  children?: NodeData[];
}

export interface ConstraintFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type ConstraintNode = NodeData<'constraint'> & {
  children(...value: NodeData[]): ConstraintNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'constraint'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ConstraintFromNode = NodeData<'constraint'> & {
  children(...value: FromValue[]): ConstraintFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'constraint'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function constraint(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | ConstraintConfig,
): ConstraintNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'constraint', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

constraint.assign = function(target: AssignableNode<'constraint'>): ConstraintNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'constraint'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConstructSignatureConfig {
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  type?: NodeData<'type_annotation'>;
}

export interface ConstructSignatureFromInput {
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  type?: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[];
}

export type ConstructSignatureNode = NodeData<'construct_signature'> & {
  typeParameters(value: NodeData<'type_parameters'>): ConstructSignatureNode;
  typeField(value: NodeData<'type_annotation'>): ConstructSignatureNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'construct_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ConstructSignatureFromNode = NodeData<'construct_signature'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): ConstructSignatureFromNode;
  typeField(value: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[]): ConstructSignatureFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'construct_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function constructSignature(
  parametersOrConfig: NodeData<'formal_parameters'> | ConstructSignatureConfig,
  config?: Partial<ConstructSignatureConfig>,
): ConstructSignatureNode {
  const fields: any = isNodeData(parametersOrConfig)
    ? { 'parameters': parametersOrConfig, ...config }
    : parametersOrConfig;
  const node: any = { type: 'construct_signature', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

constructSignature.assign = function(target: AssignableNode<'construct_signature'>): ConstructSignatureNode {
  const overrides: any = {};
  const fieldList: string[] = ['type_parameters', 'parameters', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'construct_signature'; }, get fields() { return getFields(); } };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConstructorTypeConfig {
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  type: NodeData<'type'>;
}

export interface ConstructorTypeFromInput {
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  type: NodeData<'type'> | TypeFromInput | FromValue[];
}

export type ConstructorTypeNode = NodeData<'constructor_type'> & {
  typeParameters(value: NodeData<'type_parameters'>): ConstructorTypeNode;
  typeField(value: NodeData<'type'>): ConstructorTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'constructor_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ConstructorTypeFromNode = NodeData<'constructor_type'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): ConstructorTypeFromNode;
  typeField(value: NodeData<'type'> | TypeFromInput | FromValue[]): ConstructorTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'constructor_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function constructorType(
  parametersOrConfig: NodeData<'formal_parameters'> | ConstructorTypeConfig,
  config?: Partial<ConstructorTypeConfig>,
): ConstructorTypeNode {
  const fields: any = isNodeData(parametersOrConfig)
    ? { 'parameters': parametersOrConfig, ...config }
    : parametersOrConfig;
  const node: any = { type: 'constructor_type', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

constructorType.assign = function(target: AssignableNode<'constructor_type'>): ConstructorTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['type_parameters', 'parameters', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'constructor_type'; }, get fields() { return getFields(); } };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ContinueStatementConfig {
  label?: NodeData<'statement_identifier'>;
}

export interface ContinueStatementFromInput {
  label?: NodeData<'statement_identifier'> | string;
}

export type ContinueStatementNode = NodeData<'continue_statement'> & {
  label(value: NodeData<'statement_identifier'>): ContinueStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'continue_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ContinueStatementFromNode = NodeData<'continue_statement'> & {
  label(value: NodeData<'statement_identifier'> | string): ContinueStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'continue_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function continueStatement(
  config?: ContinueStatementConfig,
): ContinueStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'continue_statement', fields };
  node.label = (v: any) => { fields['label'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

continueStatement.assign = function(target: AssignableNode<'continue_statement'>): ContinueStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['label'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'continue_statement'; }, get fields() { return getFields(); } };
  node.label = (v: any) => { overrides['label'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DecoratorConfig {
  children?: NodeData[];
}

export interface DecoratorFromInput {
  children?: (NodeData<'identifier' | 'member_expression' | 'call_expression' | 'parenthesized_expression'> | string | ({ kind: 'member_expression' } & MemberExpressionFromInput) | ({ kind: 'call_expression' } & CallExpressionFromInput) | ({ kind: 'parenthesized_expression' } & ParenthesizedExpressionFromInput))[];
}

export type DecoratorNode = NodeData<'decorator'> & {
  children(...value: NodeData[]): DecoratorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'decorator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DecoratorFromNode = NodeData<'decorator'> & {
  children(...value: FromValue[]): DecoratorFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'decorator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function decorator(
  childrenOrConfig?: NodeData<'identifier' | 'member_expression' | 'call_expression' | 'parenthesized_expression'> | NodeData<'identifier' | 'member_expression' | 'call_expression' | 'parenthesized_expression'>[] | DecoratorConfig,
): DecoratorNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'decorator', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

decorator.assign = function(target: AssignableNode<'decorator'>): DecoratorNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'decorator'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DefaultTypeConfig {
  children?: NodeData[];
}

export interface DefaultTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type DefaultTypeNode = NodeData<'default_type'> & {
  children(...value: NodeData[]): DefaultTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'default_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DefaultTypeFromNode = NodeData<'default_type'> & {
  children(...value: FromValue[]): DefaultTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'default_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function defaultType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | DefaultTypeConfig,
): DefaultTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'default_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

defaultType.assign = function(target: AssignableNode<'default_type'>): DefaultTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'default_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DoStatementConfig {
  body: NodeData<'statement'>;
  condition: NodeData<'parenthesized_expression'>;
}

export interface DoStatementFromInput {
  body: NodeData<'statement'> | StatementFromInput | FromValue[];
  condition: NodeData<'parenthesized_expression'> | ParenthesizedExpressionFromInput | FromValue[];
}

export type DoStatementNode = NodeData<'do_statement'> & {
  condition(value: NodeData<'parenthesized_expression'>): DoStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'do_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DoStatementFromNode = NodeData<'do_statement'> & {
  condition(value: NodeData<'parenthesized_expression'> | ParenthesizedExpressionFromInput | FromValue[]): DoStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'do_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function doStatement(
  bodyOrConfig: NodeData<'statement'> | DoStatementConfig,
  config?: Partial<DoStatementConfig>,
): DoStatementNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'do_statement', fields };
  node.condition = (v: any) => { fields['condition'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

doStatement.assign = function(target: AssignableNode<'do_statement'>): DoStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['body', 'condition'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'do_statement'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.condition = (v: any) => { overrides['condition'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ElseClauseConfig {
  children?: NodeData[];
}

export interface ElseClauseFromInput {
  children?: (NodeData<'statement'> | StatementFromInput)[];
}

export type ElseClauseNode = NodeData<'else_clause'> & {
  children(...value: NodeData[]): ElseClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'else_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ElseClauseFromNode = NodeData<'else_clause'> & {
  children(...value: FromValue[]): ElseClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'else_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function elseClause(
  childrenOrConfig?: NodeData<'statement'> | NodeData<'statement'>[] | ElseClauseConfig,
): ElseClauseNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'else_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

elseClause.assign = function(target: AssignableNode<'else_clause'>): ElseClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'else_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface EnumAssignmentConfig {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>;
  value: NodeData<'expression'>;
}

export interface EnumAssignmentFromInput {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput) | FromValue[];
  value: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type EnumAssignmentNode = NodeData<'enum_assignment'> & {
  value(value: NodeData<'expression'>): EnumAssignmentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_assignment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type EnumAssignmentFromNode = NodeData<'enum_assignment'> & {
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): EnumAssignmentFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_assignment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function enumAssignment(
  nameOrConfig: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | EnumAssignmentConfig,
  config?: Partial<EnumAssignmentConfig>,
): EnumAssignmentNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'enum_assignment', fields };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

enumAssignment.assign = function(target: AssignableNode<'enum_assignment'>): EnumAssignmentNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'enum_assignment'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface EnumBodyConfig {
  name?: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>[];
  children?: NodeData[];
}

export interface EnumBodyFromInput {
  name?: (NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput))[] | NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput);
  children?: (NodeData<'enum_assignment'> | EnumAssignmentFromInput)[];
}

export type EnumBodyNode = NodeData<'enum_body'> & {
  name(value: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>[]): EnumBodyNode;
  children(...value: NodeData[]): EnumBodyNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_body'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type EnumBodyFromNode = NodeData<'enum_body'> & {
  name(value: (NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput))[] | NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput)): EnumBodyFromNode;
  children(...value: FromValue[]): EnumBodyFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_body'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function enumBody(
  childrenOrConfig?: NodeData<'enum_assignment'> | NodeData<'enum_assignment'>[] | EnumBodyConfig,
): EnumBodyNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'enum_body', fields };
  node.name = (...v: any[]) => { fields['name'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

enumBody.assign = function(target: AssignableNode<'enum_body'>): EnumBodyNode {
  const overrides: any = {};
  const fieldList: string[] = ['name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'enum_body'; }, get fields() { return getFields(); } };
  node.name = (...v: any[]) => { overrides['name'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface EnumDeclarationConfig {
  name: NodeData<'identifier'>;
  body: NodeData<'enum_body'>;
}

export interface EnumDeclarationFromInput {
  name: NodeData<'identifier'> | string;
  body: NodeData<'enum_body'> | EnumBodyFromInput | FromValue[];
}

export type EnumDeclarationNode = NodeData<'enum_declaration'> & {
  body(value: NodeData<'enum_body'>): EnumDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type EnumDeclarationFromNode = NodeData<'enum_declaration'> & {
  body(value: NodeData<'enum_body'> | EnumBodyFromInput | FromValue[]): EnumDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function enumDeclaration(
  nameOrConfig: NodeData<'identifier'> | EnumDeclarationConfig,
  config?: Partial<EnumDeclarationConfig>,
): EnumDeclarationNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'enum_declaration', fields };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

enumDeclaration.assign = function(target: AssignableNode<'enum_declaration'>): EnumDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'enum_declaration'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExportClauseConfig {
  children?: NodeData[];
}

export interface ExportClauseFromInput {
  children?: (NodeData<'export_specifier'> | ExportSpecifierFromInput)[];
}

export type ExportClauseNode = NodeData<'export_clause'> & {
  children(...value: NodeData[]): ExportClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'export_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExportClauseFromNode = NodeData<'export_clause'> & {
  children(...value: FromValue[]): ExportClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'export_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function exportClause(
  childrenOrConfig?: NodeData<'export_specifier'> | NodeData<'export_specifier'>[] | ExportClauseConfig,
): ExportClauseNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'export_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

exportClause.assign = function(target: AssignableNode<'export_clause'>): ExportClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'export_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExportSpecifierConfig {
  name: NodeData<'identifier' | 'string'>;
  alias?: NodeData<'identifier' | 'string'>;
}

export interface ExportSpecifierFromInput {
  name: NodeData<'identifier' | 'string'> | string | StringFromInput | FromValue[];
  alias?: NodeData<'identifier' | 'string'> | string | StringFromInput | FromValue[];
}

export type ExportSpecifierNode = NodeData<'export_specifier'> & {
  alias(value: NodeData<'identifier' | 'string'>): ExportSpecifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'export_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExportSpecifierFromNode = NodeData<'export_specifier'> & {
  alias(value: NodeData<'identifier' | 'string'> | string | StringFromInput | FromValue[]): ExportSpecifierFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'export_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function exportSpecifier(
  nameOrConfig: NodeData<'identifier' | 'string'> | ExportSpecifierConfig,
  config?: Partial<ExportSpecifierConfig>,
): ExportSpecifierNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'export_specifier', fields };
  node.alias = (v: any) => { fields['alias'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

exportSpecifier.assign = function(target: AssignableNode<'export_specifier'>): ExportSpecifierNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'export_specifier'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.alias = (v: any) => { overrides['alias'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExportStatementConfig {
  source?: NodeData<'string'>;
  decorator?: NodeData<'decorator'>[];
  declaration?: NodeData<'declaration'>;
  value?: NodeData<'expression'>;
  children?: NodeData[];
}

export interface ExportStatementFromInput {
  source?: NodeData<'string'> | StringFromInput | FromValue[];
  decorator?: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput;
  declaration?: NodeData<'declaration'> | DeclarationFromInput | FromValue[];
  value?: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  children?: (NodeData<'namespace_export' | 'export_clause' | 'expression' | 'identifier'> | string | ({ kind: 'namespace_export' } & NamespaceExportFromInput) | ({ kind: 'export_clause' } & ExportClauseFromInput) | ({ kind: 'expression' } & ExpressionFromInput))[];
}

export type ExportStatementNode = NodeData<'export_statement'> & {
  source(value: NodeData<'string'>): ExportStatementNode;
  decorator(value: NodeData<'decorator'>[]): ExportStatementNode;
  declaration(value: NodeData<'declaration'>): ExportStatementNode;
  value(value: NodeData<'expression'>): ExportStatementNode;
  children(...value: NodeData[]): ExportStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'export_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExportStatementFromNode = NodeData<'export_statement'> & {
  source(value: NodeData<'string'> | StringFromInput | FromValue[]): ExportStatementFromNode;
  decorator(value: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput): ExportStatementFromNode;
  declaration(value: NodeData<'declaration'> | DeclarationFromInput | FromValue[]): ExportStatementFromNode;
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): ExportStatementFromNode;
  children(...value: FromValue[]): ExportStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'export_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function exportStatement(
  config?: ExportStatementConfig,
): ExportStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'export_statement', fields };
  node.source = (v: any) => { fields['source'] = v; return node; };
  node.decorator = (...v: any[]) => { fields['decorator'] = v; return node; };
  node.declaration = (v: any) => { fields['declaration'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

exportStatement.assign = function(target: AssignableNode<'export_statement'>): ExportStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['source', 'decorator', 'declaration', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'export_statement'; }, get fields() { return getFields(); } };
  node.source = (v: any) => { overrides['source'] = v; return node; };
  node.decorator = (...v: any[]) => { overrides['decorator'] = v; return node; };
  node.declaration = (v: any) => { overrides['declaration'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExpressionStatementConfig {
  children?: NodeData[];
}

export interface ExpressionStatementFromInput {
  children?: (NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput))[];
}

export type ExpressionStatementNode = NodeData<'expression_statement'> & {
  children(...value: NodeData[]): ExpressionStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExpressionStatementFromNode = NodeData<'expression_statement'> & {
  children(...value: FromValue[]): ExpressionStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function expressionStatement(
  childrenOrConfig?: NodeData<'expression' | 'sequence_expression'> | NodeData<'expression' | 'sequence_expression'>[] | ExpressionStatementConfig,
): ExpressionStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'expression_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

expressionStatement.assign = function(target: AssignableNode<'expression_statement'>): ExpressionStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'expression_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExtendsClauseConfig {
  value: NodeData<'expression'>[];
  type_arguments?: NodeData<'type_arguments'>[];
}

export interface ExtendsClauseFromInput {
  value: (NodeData<'expression'> | ExpressionFromInput)[] | NodeData<'expression'> | ExpressionFromInput;
  type_arguments?: (NodeData<'type_arguments'> | TypeArgumentsFromInput)[] | NodeData<'type_arguments'> | TypeArgumentsFromInput;
}

export type ExtendsClauseNode = NodeData<'extends_clause'> & {
  value(value: NodeData<'expression'>[]): ExtendsClauseNode;
  typeArguments(value: NodeData<'type_arguments'>[]): ExtendsClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'extends_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExtendsClauseFromNode = NodeData<'extends_clause'> & {
  value(value: (NodeData<'expression'> | ExpressionFromInput)[] | NodeData<'expression'> | ExpressionFromInput): ExtendsClauseFromNode;
  typeArguments(value: (NodeData<'type_arguments'> | TypeArgumentsFromInput)[] | NodeData<'type_arguments'> | TypeArgumentsFromInput): ExtendsClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'extends_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function extendsClause(
  config?: ExtendsClauseConfig,
): ExtendsClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'extends_clause', fields };
  node.value = (...v: any[]) => { fields['value'] = v; return node; };
  node.typeArguments = (...v: any[]) => { fields['type_arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

extendsClause.assign = function(target: AssignableNode<'extends_clause'>): ExtendsClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'type_arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'extends_clause'; }, get fields() { return getFields(); } };
  node.value = (...v: any[]) => { overrides['value'] = v; return node; };
  node.typeArguments = (...v: any[]) => { overrides['type_arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExtendsTypeClauseConfig {
  type: NodeData<'type_identifier' | 'nested_type_identifier' | 'generic_type'>[];
}

export interface ExtendsTypeClauseFromInput {
  type: (NodeData<'type_identifier' | 'nested_type_identifier' | 'generic_type'> | string | ({ kind: 'nested_type_identifier' } & NestedTypeIdentifierFromInput) | ({ kind: 'generic_type' } & GenericTypeFromInput))[] | NodeData<'type_identifier' | 'nested_type_identifier' | 'generic_type'> | string | ({ kind: 'nested_type_identifier' } & NestedTypeIdentifierFromInput) | ({ kind: 'generic_type' } & GenericTypeFromInput);
}

export type ExtendsTypeClauseNode = NodeData<'extends_type_clause'> & {
  typeField(value: NodeData<'type_identifier' | 'nested_type_identifier' | 'generic_type'>[]): ExtendsTypeClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'extends_type_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExtendsTypeClauseFromNode = NodeData<'extends_type_clause'> & {
  typeField(value: (NodeData<'type_identifier' | 'nested_type_identifier' | 'generic_type'> | string | ({ kind: 'nested_type_identifier' } & NestedTypeIdentifierFromInput) | ({ kind: 'generic_type' } & GenericTypeFromInput))[] | NodeData<'type_identifier' | 'nested_type_identifier' | 'generic_type'> | string | ({ kind: 'nested_type_identifier' } & NestedTypeIdentifierFromInput) | ({ kind: 'generic_type' } & GenericTypeFromInput)): ExtendsTypeClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'extends_type_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function extendsTypeClause(
  config?: ExtendsTypeClauseConfig,
): ExtendsTypeClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'extends_type_clause', fields };
  node.typeField = (...v: any[]) => { fields['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

extendsTypeClause.assign = function(target: AssignableNode<'extends_type_clause'>): ExtendsTypeClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'extends_type_clause'; }, get fields() { return getFields(); } };
  node.typeField = (...v: any[]) => { overrides['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FinallyClauseConfig {
  body: NodeData<'statement_block'>;
}

export interface FinallyClauseFromInput {
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type FinallyClauseNode = NodeData<'finally_clause'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'finally_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FinallyClauseFromNode = NodeData<'finally_clause'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'finally_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function finallyClause(
  bodyOrConfig: NodeData<'statement_block'> | FinallyClauseConfig,
  config?: Partial<FinallyClauseConfig>,
): FinallyClauseNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'finally_clause', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

finallyClause.assign = function(target: AssignableNode<'finally_clause'>): FinallyClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'finally_clause'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FlowMaybeTypeConfig {
  children?: NodeData[];
}

export interface FlowMaybeTypeFromInput {
  children?: (NodeData<'primary_type'> | PrimaryTypeFromInput)[];
}

export type FlowMaybeTypeNode = NodeData<'flow_maybe_type'> & {
  children(...value: NodeData[]): FlowMaybeTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'flow_maybe_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FlowMaybeTypeFromNode = NodeData<'flow_maybe_type'> & {
  children(...value: FromValue[]): FlowMaybeTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'flow_maybe_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function flowMaybeType(
  childrenOrConfig?: NodeData<'primary_type'> | NodeData<'primary_type'>[] | FlowMaybeTypeConfig,
): FlowMaybeTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'flow_maybe_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

flowMaybeType.assign = function(target: AssignableNode<'flow_maybe_type'>): FlowMaybeTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'flow_maybe_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ForInStatementConfig {
  left: NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression' | 'parenthesized_expression'>;
  kind?: NodeData<string>;
  value?: NodeData<'expression'>;
  operator: NodeData<string>;
  right: NodeData<'expression' | 'sequence_expression'>;
  body: NodeData<'statement'>;
}

export interface ForInStatementFromInput {
  left: NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression' | 'parenthesized_expression'> | string | ({ kind: 'member_expression' } & MemberExpressionFromInput) | ({ kind: 'subscript_expression' } & SubscriptExpressionFromInput) | ({ kind: 'object_pattern' } & ObjectPatternFromInput) | ({ kind: 'array_pattern' } & ArrayPatternFromInput) | ({ kind: 'non_null_expression' } & NonNullExpressionFromInput) | ({ kind: 'parenthesized_expression' } & ParenthesizedExpressionFromInput) | FromValue[];
  kind?: NodeData | 'var' | 'let' | 'const';
  value?: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  operator: NodeData | 'in' | 'of';
  right: NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput) | FromValue[];
  body: NodeData<'statement'> | StatementFromInput | FromValue[];
}

export type ForInStatementNode = NodeData<'for_in_statement'> & {
  kind(value: NodeData<string>): ForInStatementNode;
  value(value: NodeData<'expression'>): ForInStatementNode;
  operator(value: NodeData<string>): ForInStatementNode;
  right(value: NodeData<'expression' | 'sequence_expression'>): ForInStatementNode;
  body(value: NodeData<'statement'>): ForInStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_in_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ForInStatementFromNode = NodeData<'for_in_statement'> & {
  kind(value: NodeData | 'var' | 'let' | 'const'): ForInStatementFromNode;
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): ForInStatementFromNode;
  operator(value: NodeData | 'in' | 'of'): ForInStatementFromNode;
  right(value: NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput) | FromValue[]): ForInStatementFromNode;
  body(value: NodeData<'statement'> | StatementFromInput | FromValue[]): ForInStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_in_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function forInStatement(
  leftOrConfig: NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression' | 'parenthesized_expression'> | ForInStatementConfig,
  config?: Partial<ForInStatementConfig>,
): ForInStatementNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'for_in_statement', fields };
  node.kind = (v: any) => { fields['kind'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.operator = (v: any) => { fields['operator'] = v; return node; };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

forInStatement.assign = function(target: AssignableNode<'for_in_statement'>): ForInStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'kind', 'value', 'operator', 'right', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'for_in_statement'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.kind = (v: any) => { overrides['kind'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ForStatementConfig {
  initializer: NodeData<'expression' | 'sequence_expression' | 'lexical_declaration' | 'variable_declaration' | 'empty_statement'>;
  condition: NodeData<'expression' | 'sequence_expression' | 'empty_statement'>[];
  increment?: NodeData<'expression' | 'sequence_expression'>;
  body: NodeData<'statement'>;
}

export interface ForStatementFromInput {
  initializer: NodeData<'expression' | 'sequence_expression' | 'lexical_declaration' | 'variable_declaration' | 'empty_statement'> | string | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput) | ({ kind: 'lexical_declaration' } & LexicalDeclarationFromInput) | ({ kind: 'variable_declaration' } & VariableDeclarationFromInput) | FromValue[];
  condition: (NodeData<'expression' | 'sequence_expression' | 'empty_statement'> | string | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput))[] | NodeData<'expression' | 'sequence_expression' | 'empty_statement'> | string | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput);
  increment?: NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput) | FromValue[];
  body: NodeData<'statement'> | StatementFromInput | FromValue[];
}

export type ForStatementNode = NodeData<'for_statement'> & {
  condition(value: NodeData<'expression' | 'sequence_expression' | 'empty_statement'>[]): ForStatementNode;
  increment(value: NodeData<'expression' | 'sequence_expression'>): ForStatementNode;
  body(value: NodeData<'statement'>): ForStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ForStatementFromNode = NodeData<'for_statement'> & {
  condition(value: (NodeData<'expression' | 'sequence_expression' | 'empty_statement'> | string | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput))[] | NodeData<'expression' | 'sequence_expression' | 'empty_statement'> | string | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput)): ForStatementFromNode;
  increment(value: NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput) | FromValue[]): ForStatementFromNode;
  body(value: NodeData<'statement'> | StatementFromInput | FromValue[]): ForStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function forStatement(
  initializerOrConfig: NodeData<'expression' | 'sequence_expression' | 'lexical_declaration' | 'variable_declaration' | 'empty_statement'> | ForStatementConfig,
  config?: Partial<ForStatementConfig>,
): ForStatementNode {
  const fields: any = isNodeData(initializerOrConfig)
    ? { 'initializer': initializerOrConfig, ...config }
    : initializerOrConfig;
  const node: any = { type: 'for_statement', fields };
  node.condition = (...v: any[]) => { fields['condition'] = v; return node; };
  node.increment = (v: any) => { fields['increment'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

forStatement.assign = function(target: AssignableNode<'for_statement'>): ForStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['initializer', 'condition', 'increment', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'for_statement'; }, get fields() { return getFields(); } };
  node.initializer = (v: any) => { overrides['initializer'] = v; return node; };
  node.condition = (...v: any[]) => { overrides['condition'] = v; return node; };
  node.increment = (v: any) => { overrides['increment'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FormalParametersConfig {
  children?: NodeData[];
}

export interface FormalParametersFromInput {
  children?: (NodeData<'required_parameter' | 'optional_parameter'> | ({ kind: 'required_parameter' } & RequiredParameterFromInput) | ({ kind: 'optional_parameter' } & OptionalParameterFromInput))[];
}

export type FormalParametersNode = NodeData<'formal_parameters'> & {
  children(...value: NodeData[]): FormalParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'formal_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FormalParametersFromNode = NodeData<'formal_parameters'> & {
  children(...value: FromValue[]): FormalParametersFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'formal_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function formalParameters(
  childrenOrConfig?: NodeData<'required_parameter' | 'optional_parameter'> | NodeData<'required_parameter' | 'optional_parameter'>[] | FormalParametersConfig,
): FormalParametersNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'formal_parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

formalParameters.assign = function(target: AssignableNode<'formal_parameters'>): FormalParametersNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'formal_parameters'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionDeclarationConfig {
  name: NodeData<'identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
  body: NodeData<'statement_block'>;
}

export interface FunctionDeclarationFromInput {
  name: NodeData<'identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type FunctionDeclarationNode = NodeData<'function_declaration'> & {
  typeParameters(value: NodeData<'type_parameters'>): FunctionDeclarationNode;
  parameters(value: NodeData<'formal_parameters'>): FunctionDeclarationNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): FunctionDeclarationNode;
  body(value: NodeData<'statement_block'>): FunctionDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FunctionDeclarationFromNode = NodeData<'function_declaration'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): FunctionDeclarationFromNode;
  parameters(value: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[]): FunctionDeclarationFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): FunctionDeclarationFromNode;
  body(value: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[]): FunctionDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionDeclaration(
  nameOrConfig: NodeData<'identifier'> | FunctionDeclarationConfig,
  config?: Partial<FunctionDeclarationConfig>,
): FunctionDeclarationNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'function_declaration', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionDeclaration.assign = function(target: AssignableNode<'function_declaration'>): FunctionDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'function_declaration'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionExpressionConfig {
  name?: NodeData<'identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
  body: NodeData<'statement_block'>;
}

export interface FunctionExpressionFromInput {
  name?: NodeData<'identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type FunctionExpressionNode = NodeData<'function_expression'> & {
  name(value: NodeData<'identifier'>): FunctionExpressionNode;
  typeParameters(value: NodeData<'type_parameters'>): FunctionExpressionNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): FunctionExpressionNode;
  body(value: NodeData<'statement_block'>): FunctionExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FunctionExpressionFromNode = NodeData<'function_expression'> & {
  name(value: NodeData<'identifier'> | string): FunctionExpressionFromNode;
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): FunctionExpressionFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): FunctionExpressionFromNode;
  body(value: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[]): FunctionExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionExpression(
  parametersOrConfig: NodeData<'formal_parameters'> | FunctionExpressionConfig,
  config?: Partial<FunctionExpressionConfig>,
): FunctionExpressionNode {
  const fields: any = isNodeData(parametersOrConfig)
    ? { 'parameters': parametersOrConfig, ...config }
    : parametersOrConfig;
  const node: any = { type: 'function_expression', fields };
  node.name = (v: any) => { fields['name'] = v; return node; };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionExpression.assign = function(target: AssignableNode<'function_expression'>): FunctionExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'function_expression'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionSignatureConfig {
  name: NodeData<'identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
}

export interface FunctionSignatureFromInput {
  name: NodeData<'identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
}

export type FunctionSignatureNode = NodeData<'function_signature'> & {
  typeParameters(value: NodeData<'type_parameters'>): FunctionSignatureNode;
  parameters(value: NodeData<'formal_parameters'>): FunctionSignatureNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): FunctionSignatureNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FunctionSignatureFromNode = NodeData<'function_signature'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): FunctionSignatureFromNode;
  parameters(value: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[]): FunctionSignatureFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): FunctionSignatureFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionSignature(
  nameOrConfig: NodeData<'identifier'> | FunctionSignatureConfig,
  config?: Partial<FunctionSignatureConfig>,
): FunctionSignatureNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'function_signature', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionSignature.assign = function(target: AssignableNode<'function_signature'>): FunctionSignatureNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'function_signature'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionTypeConfig {
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type: NodeData<'type' | 'asserts' | 'type_predicate'>;
}

export interface FunctionTypeFromInput {
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type: NodeData<'type' | 'asserts' | 'type_predicate'> | ({ kind: 'type' } & TypeFromInput) | ({ kind: 'asserts' } & AssertsFromInput) | ({ kind: 'type_predicate' } & TypePredicateFromInput) | FromValue[];
}

export type FunctionTypeNode = NodeData<'function_type'> & {
  typeParameters(value: NodeData<'type_parameters'>): FunctionTypeNode;
  returnType(value: NodeData<'type' | 'asserts' | 'type_predicate'>): FunctionTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FunctionTypeFromNode = NodeData<'function_type'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): FunctionTypeFromNode;
  returnType(value: NodeData<'type' | 'asserts' | 'type_predicate'> | ({ kind: 'type' } & TypeFromInput) | ({ kind: 'asserts' } & AssertsFromInput) | ({ kind: 'type_predicate' } & TypePredicateFromInput) | FromValue[]): FunctionTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionType(
  parametersOrConfig: NodeData<'formal_parameters'> | FunctionTypeConfig,
  config?: Partial<FunctionTypeConfig>,
): FunctionTypeNode {
  const fields: any = isNodeData(parametersOrConfig)
    ? { 'parameters': parametersOrConfig, ...config }
    : parametersOrConfig;
  const node: any = { type: 'function_type', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionType.assign = function(target: AssignableNode<'function_type'>): FunctionTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['type_parameters', 'parameters', 'return_type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'function_type'; }, get fields() { return getFields(); } };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GeneratorFunctionConfig {
  name?: NodeData<'identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
  body: NodeData<'statement_block'>;
}

export interface GeneratorFunctionFromInput {
  name?: NodeData<'identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type GeneratorFunctionNode = NodeData<'generator_function'> & {
  name(value: NodeData<'identifier'>): GeneratorFunctionNode;
  typeParameters(value: NodeData<'type_parameters'>): GeneratorFunctionNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): GeneratorFunctionNode;
  body(value: NodeData<'statement_block'>): GeneratorFunctionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generator_function'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type GeneratorFunctionFromNode = NodeData<'generator_function'> & {
  name(value: NodeData<'identifier'> | string): GeneratorFunctionFromNode;
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): GeneratorFunctionFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): GeneratorFunctionFromNode;
  body(value: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[]): GeneratorFunctionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generator_function'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function generatorFunction(
  parametersOrConfig: NodeData<'formal_parameters'> | GeneratorFunctionConfig,
  config?: Partial<GeneratorFunctionConfig>,
): GeneratorFunctionNode {
  const fields: any = isNodeData(parametersOrConfig)
    ? { 'parameters': parametersOrConfig, ...config }
    : parametersOrConfig;
  const node: any = { type: 'generator_function', fields };
  node.name = (v: any) => { fields['name'] = v; return node; };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

generatorFunction.assign = function(target: AssignableNode<'generator_function'>): GeneratorFunctionNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'generator_function'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GeneratorFunctionDeclarationConfig {
  name: NodeData<'identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
  body: NodeData<'statement_block'>;
}

export interface GeneratorFunctionDeclarationFromInput {
  name: NodeData<'identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type GeneratorFunctionDeclarationNode = NodeData<'generator_function_declaration'> & {
  typeParameters(value: NodeData<'type_parameters'>): GeneratorFunctionDeclarationNode;
  parameters(value: NodeData<'formal_parameters'>): GeneratorFunctionDeclarationNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): GeneratorFunctionDeclarationNode;
  body(value: NodeData<'statement_block'>): GeneratorFunctionDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generator_function_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type GeneratorFunctionDeclarationFromNode = NodeData<'generator_function_declaration'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): GeneratorFunctionDeclarationFromNode;
  parameters(value: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[]): GeneratorFunctionDeclarationFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): GeneratorFunctionDeclarationFromNode;
  body(value: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[]): GeneratorFunctionDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generator_function_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function generatorFunctionDeclaration(
  nameOrConfig: NodeData<'identifier'> | GeneratorFunctionDeclarationConfig,
  config?: Partial<GeneratorFunctionDeclarationConfig>,
): GeneratorFunctionDeclarationNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'generator_function_declaration', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

generatorFunctionDeclaration.assign = function(target: AssignableNode<'generator_function_declaration'>): GeneratorFunctionDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'generator_function_declaration'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GenericTypeConfig {
  name: NodeData<'type_identifier' | 'nested_type_identifier'>;
  type_arguments: NodeData<'type_arguments'>;
}

export interface GenericTypeFromInput {
  name: NodeData<'type_identifier' | 'nested_type_identifier'> | string | NestedTypeIdentifierFromInput | FromValue[];
  type_arguments: NodeData<'type_arguments'> | TypeArgumentsFromInput | FromValue[];
}

export type GenericTypeNode = NodeData<'generic_type'> & {
  typeArguments(value: NodeData<'type_arguments'>): GenericTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type GenericTypeFromNode = NodeData<'generic_type'> & {
  typeArguments(value: NodeData<'type_arguments'> | TypeArgumentsFromInput | FromValue[]): GenericTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function genericType(
  nameOrConfig: NodeData<'type_identifier' | 'nested_type_identifier'> | GenericTypeConfig,
  config?: Partial<GenericTypeConfig>,
): GenericTypeNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'generic_type', fields };
  node.typeArguments = (v: any) => { fields['type_arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

genericType.assign = function(target: AssignableNode<'generic_type'>): GenericTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'generic_type'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeArguments = (v: any) => { overrides['type_arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface IfStatementConfig {
  condition: NodeData<'parenthesized_expression'>;
  consequence: NodeData<'statement'>;
  alternative?: NodeData<'else_clause'>;
}

export interface IfStatementFromInput {
  condition: NodeData<'parenthesized_expression'> | ParenthesizedExpressionFromInput | FromValue[];
  consequence: NodeData<'statement'> | StatementFromInput | FromValue[];
  alternative?: NodeData<'else_clause'> | ElseClauseFromInput | FromValue[];
}

export type IfStatementNode = NodeData<'if_statement'> & {
  consequence(value: NodeData<'statement'>): IfStatementNode;
  alternative(value: NodeData<'else_clause'>): IfStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type IfStatementFromNode = NodeData<'if_statement'> & {
  consequence(value: NodeData<'statement'> | StatementFromInput | FromValue[]): IfStatementFromNode;
  alternative(value: NodeData<'else_clause'> | ElseClauseFromInput | FromValue[]): IfStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function ifStatement(
  conditionOrConfig: NodeData<'parenthesized_expression'> | IfStatementConfig,
  config?: Partial<IfStatementConfig>,
): IfStatementNode {
  const fields: any = isNodeData(conditionOrConfig)
    ? { 'condition': conditionOrConfig, ...config }
    : conditionOrConfig;
  const node: any = { type: 'if_statement', fields };
  node.consequence = (v: any) => { fields['consequence'] = v; return node; };
  node.alternative = (v: any) => { fields['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

ifStatement.assign = function(target: AssignableNode<'if_statement'>): IfStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition', 'consequence', 'alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'if_statement'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = v; return node; };
  node.consequence = (v: any) => { overrides['consequence'] = v; return node; };
  node.alternative = (v: any) => { overrides['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImplementsClauseConfig {
  children?: NodeData[];
}

export interface ImplementsClauseFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type ImplementsClauseNode = NodeData<'implements_clause'> & {
  children(...value: NodeData[]): ImplementsClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'implements_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImplementsClauseFromNode = NodeData<'implements_clause'> & {
  children(...value: FromValue[]): ImplementsClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'implements_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function implementsClause(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | ImplementsClauseConfig,
): ImplementsClauseNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'implements_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

implementsClause.assign = function(target: AssignableNode<'implements_clause'>): ImplementsClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'implements_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImportAliasConfig {
  children?: NodeData[];
}

export interface ImportAliasFromInput {
  children?: (NodeData<'identifier' | 'nested_identifier'> | string | NestedIdentifierFromInput)[];
}

export type ImportAliasNode = NodeData<'import_alias'> & {
  children(...value: NodeData[]): ImportAliasNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_alias'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImportAliasFromNode = NodeData<'import_alias'> & {
  children(...value: FromValue[]): ImportAliasFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_alias'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importAlias(
  childrenOrConfig?: NodeData<'identifier' | 'nested_identifier'> | NodeData<'identifier' | 'nested_identifier'>[] | ImportAliasConfig,
): ImportAliasNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'import_alias', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

importAlias.assign = function(target: AssignableNode<'import_alias'>): ImportAliasNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'import_alias'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImportAttributeConfig {
  children?: NodeData[];
}

export interface ImportAttributeFromInput {
  children?: (NodeData<'object'> | ObjectFromInput)[];
}

export type ImportAttributeNode = NodeData<'import_attribute'> & {
  children(...value: NodeData[]): ImportAttributeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_attribute'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImportAttributeFromNode = NodeData<'import_attribute'> & {
  children(...value: FromValue[]): ImportAttributeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_attribute'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importAttribute(
  childrenOrConfig?: NodeData<'object'> | NodeData<'object'>[] | ImportAttributeConfig,
): ImportAttributeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'import_attribute', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

importAttribute.assign = function(target: AssignableNode<'import_attribute'>): ImportAttributeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'import_attribute'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImportClauseConfig {
  children?: NodeData[];
}

export interface ImportClauseFromInput {
  children?: (NodeData<'namespace_import' | 'named_imports' | 'identifier'> | string | ({ kind: 'namespace_import' } & NamespaceImportFromInput) | ({ kind: 'named_imports' } & NamedImportsFromInput))[];
}

export type ImportClauseNode = NodeData<'import_clause'> & {
  children(...value: NodeData[]): ImportClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImportClauseFromNode = NodeData<'import_clause'> & {
  children(...value: FromValue[]): ImportClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importClause(
  childrenOrConfig?: NodeData<'namespace_import' | 'named_imports' | 'identifier'> | NodeData<'namespace_import' | 'named_imports' | 'identifier'>[] | ImportClauseConfig,
): ImportClauseNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'import_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

importClause.assign = function(target: AssignableNode<'import_clause'>): ImportClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'import_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImportRequireClauseConfig {
  source: NodeData<'string'>;
  children?: NodeData[];
}

export interface ImportRequireClauseFromInput {
  source: NodeData<'string'> | StringFromInput | FromValue[];
  children?: (NodeData<'identifier'> | string)[];
}

export type ImportRequireClauseNode = NodeData<'import_require_clause'> & {
  children(...value: NodeData[]): ImportRequireClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_require_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImportRequireClauseFromNode = NodeData<'import_require_clause'> & {
  children(...value: FromValue[]): ImportRequireClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_require_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importRequireClause(
  sourceOrConfig: NodeData<'string'> | ImportRequireClauseConfig,
  config?: Partial<ImportRequireClauseConfig>,
): ImportRequireClauseNode {
  const fields: any = isNodeData(sourceOrConfig)
    ? { 'source': sourceOrConfig, ...config }
    : sourceOrConfig;
  const node: any = { type: 'import_require_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

importRequireClause.assign = function(target: AssignableNode<'import_require_clause'>): ImportRequireClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['source'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'import_require_clause'; }, get fields() { return getFields(); } };
  node.source = (v: any) => { overrides['source'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImportSpecifierConfig {
  name: NodeData<'identifier' | 'string'>;
  alias?: NodeData<'identifier'>;
}

export interface ImportSpecifierFromInput {
  name: NodeData<'identifier' | 'string'> | string | StringFromInput | FromValue[];
  alias?: NodeData<'identifier'> | string;
}

export type ImportSpecifierNode = NodeData<'import_specifier'> & {
  alias(value: NodeData<'identifier'>): ImportSpecifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImportSpecifierFromNode = NodeData<'import_specifier'> & {
  alias(value: NodeData<'identifier'> | string): ImportSpecifierFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importSpecifier(
  nameOrConfig: NodeData<'identifier' | 'string'> | ImportSpecifierConfig,
  config?: Partial<ImportSpecifierConfig>,
): ImportSpecifierNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'import_specifier', fields };
  node.alias = (v: any) => { fields['alias'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

importSpecifier.assign = function(target: AssignableNode<'import_specifier'>): ImportSpecifierNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'import_specifier'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.alias = (v: any) => { overrides['alias'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImportStatementConfig {
  source?: NodeData<'string'>;
  children?: NodeData[];
}

export interface ImportStatementFromInput {
  source?: NodeData<'string'> | StringFromInput | FromValue[];
  children?: (NodeData<'import_clause' | 'import_require_clause' | 'import_attribute'> | ({ kind: 'import_clause' } & ImportClauseFromInput) | ({ kind: 'import_require_clause' } & ImportRequireClauseFromInput) | ({ kind: 'import_attribute' } & ImportAttributeFromInput))[];
}

export type ImportStatementNode = NodeData<'import_statement'> & {
  source(value: NodeData<'string'>): ImportStatementNode;
  children(...value: NodeData[]): ImportStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImportStatementFromNode = NodeData<'import_statement'> & {
  source(value: NodeData<'string'> | StringFromInput | FromValue[]): ImportStatementFromNode;
  children(...value: FromValue[]): ImportStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importStatement(
  childrenOrConfig?: NodeData<'import_clause' | 'import_require_clause' | 'import_attribute'> | NodeData<'import_clause' | 'import_require_clause' | 'import_attribute'>[] | ImportStatementConfig,
): ImportStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'import_statement', fields };
  node.source = (v: any) => { fields['source'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

importStatement.assign = function(target: AssignableNode<'import_statement'>): ImportStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['source'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'import_statement'; }, get fields() { return getFields(); } };
  node.source = (v: any) => { overrides['source'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface IndexSignatureConfig {
  sign?: NodeData<string>;
  name?: NodeData<'identifier'>;
  index_type?: NodeData<'type'>;
  type: NodeData<'type_annotation' | 'omitting_type_annotation' | 'adding_type_annotation' | 'opting_type_annotation'>;
  children?: NodeData[];
}

export interface IndexSignatureFromInput {
  sign?: NodeData | '-' | '+';
  name?: NodeData<'identifier'> | string;
  index_type?: NodeData<'type'> | TypeFromInput | FromValue[];
  type: NodeData<'type_annotation' | 'omitting_type_annotation' | 'adding_type_annotation' | 'opting_type_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'omitting_type_annotation' } & OmittingTypeAnnotationFromInput) | ({ kind: 'adding_type_annotation' } & AddingTypeAnnotationFromInput) | ({ kind: 'opting_type_annotation' } & OptingTypeAnnotationFromInput) | FromValue[];
  children?: (NodeData<'mapped_type_clause'> | MappedTypeClauseFromInput)[];
}

export type IndexSignatureNode = NodeData<'index_signature'> & {
  sign(value: NodeData<string>): IndexSignatureNode;
  name(value: NodeData<'identifier'>): IndexSignatureNode;
  indexType(value: NodeData<'type'>): IndexSignatureNode;
  children(...value: NodeData[]): IndexSignatureNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'index_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type IndexSignatureFromNode = NodeData<'index_signature'> & {
  sign(value: NodeData | '-' | '+'): IndexSignatureFromNode;
  name(value: NodeData<'identifier'> | string): IndexSignatureFromNode;
  indexType(value: NodeData<'type'> | TypeFromInput | FromValue[]): IndexSignatureFromNode;
  children(...value: FromValue[]): IndexSignatureFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'index_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function indexSignature(
  typeOrConfig: NodeData<'type_annotation' | 'omitting_type_annotation' | 'adding_type_annotation' | 'opting_type_annotation'> | IndexSignatureConfig,
  config?: Partial<IndexSignatureConfig>,
): IndexSignatureNode {
  const fields: any = isNodeData(typeOrConfig)
    ? { 'type': typeOrConfig, ...config }
    : typeOrConfig;
  const node: any = { type: 'index_signature', fields };
  node.sign = (v: any) => { fields['sign'] = v; return node; };
  node.name = (v: any) => { fields['name'] = v; return node; };
  node.indexType = (v: any) => { fields['index_type'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

indexSignature.assign = function(target: AssignableNode<'index_signature'>): IndexSignatureNode {
  const overrides: any = {};
  const fieldList: string[] = ['sign', 'name', 'index_type', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'index_signature'; }, get fields() { return getFields(); } };
  node.sign = (v: any) => { overrides['sign'] = v; return node; };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.indexType = (v: any) => { overrides['index_type'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface IndexTypeQueryConfig {
  children?: NodeData[];
}

export interface IndexTypeQueryFromInput {
  children?: (NodeData<'primary_type'> | PrimaryTypeFromInput)[];
}

export type IndexTypeQueryNode = NodeData<'index_type_query'> & {
  children(...value: NodeData[]): IndexTypeQueryNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'index_type_query'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type IndexTypeQueryFromNode = NodeData<'index_type_query'> & {
  children(...value: FromValue[]): IndexTypeQueryFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'index_type_query'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function indexTypeQuery(
  childrenOrConfig?: NodeData<'primary_type'> | NodeData<'primary_type'>[] | IndexTypeQueryConfig,
): IndexTypeQueryNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'index_type_query', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

indexTypeQuery.assign = function(target: AssignableNode<'index_type_query'>): IndexTypeQueryNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'index_type_query'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface InferTypeConfig {
  children?: NodeData[];
}

export interface InferTypeFromInput {
  children?: (NodeData<'type_identifier' | 'type'> | string | TypeFromInput)[];
}

export type InferTypeNode = NodeData<'infer_type'> & {
  children(...value: NodeData[]): InferTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'infer_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type InferTypeFromNode = NodeData<'infer_type'> & {
  children(...value: FromValue[]): InferTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'infer_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function inferType(
  childrenOrConfig?: NodeData<'type_identifier' | 'type'> | NodeData<'type_identifier' | 'type'>[] | InferTypeConfig,
): InferTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'infer_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

inferType.assign = function(target: AssignableNode<'infer_type'>): InferTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'infer_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface InstantiationExpressionConfig {
  type_arguments: NodeData<'type_arguments'>;
  function?: NodeData<'identifier' | 'import' | 'member_expression' | 'subscript_expression'>;
  children?: NodeData[];
}

export interface InstantiationExpressionFromInput {
  type_arguments: NodeData<'type_arguments'> | TypeArgumentsFromInput | FromValue[];
  function?: NodeData<'identifier' | 'import' | 'member_expression' | 'subscript_expression'> | string | ({ kind: 'member_expression' } & MemberExpressionFromInput) | ({ kind: 'subscript_expression' } & SubscriptExpressionFromInput) | FromValue[];
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type InstantiationExpressionNode = NodeData<'instantiation_expression'> & {
  function(value: NodeData<'identifier' | 'import' | 'member_expression' | 'subscript_expression'>): InstantiationExpressionNode;
  children(...value: NodeData[]): InstantiationExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'instantiation_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type InstantiationExpressionFromNode = NodeData<'instantiation_expression'> & {
  function(value: NodeData<'identifier' | 'import' | 'member_expression' | 'subscript_expression'> | string | ({ kind: 'member_expression' } & MemberExpressionFromInput) | ({ kind: 'subscript_expression' } & SubscriptExpressionFromInput) | FromValue[]): InstantiationExpressionFromNode;
  children(...value: FromValue[]): InstantiationExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'instantiation_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function instantiationExpression(
  typeArgumentsOrConfig: NodeData<'type_arguments'> | InstantiationExpressionConfig,
  config?: Partial<InstantiationExpressionConfig>,
): InstantiationExpressionNode {
  const fields: any = isNodeData(typeArgumentsOrConfig)
    ? { 'type_arguments': typeArgumentsOrConfig, ...config }
    : typeArgumentsOrConfig;
  const node: any = { type: 'instantiation_expression', fields };
  node.function = (v: any) => { fields['function'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

instantiationExpression.assign = function(target: AssignableNode<'instantiation_expression'>): InstantiationExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['type_arguments', 'function'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'instantiation_expression'; }, get fields() { return getFields(); } };
  node.typeArguments = (v: any) => { overrides['type_arguments'] = v; return node; };
  node.function = (v: any) => { overrides['function'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface InterfaceBodyConfig {
  children?: NodeData[];
}

export interface InterfaceBodyFromInput {
  children?: (NodeData<'call_signature' | 'construct_signature' | 'export_statement' | 'index_signature' | 'method_signature' | 'property_signature'> | ({ kind: 'call_signature' } & CallSignatureFromInput) | ({ kind: 'construct_signature' } & ConstructSignatureFromInput) | ({ kind: 'export_statement' } & ExportStatementFromInput) | ({ kind: 'index_signature' } & IndexSignatureFromInput) | ({ kind: 'method_signature' } & MethodSignatureFromInput) | ({ kind: 'property_signature' } & PropertySignatureFromInput))[];
}

export type InterfaceBodyNode = NodeData<'interface_body'> & {
  children(...value: NodeData[]): InterfaceBodyNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'interface_body'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type InterfaceBodyFromNode = NodeData<'interface_body'> & {
  children(...value: FromValue[]): InterfaceBodyFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'interface_body'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function interfaceBody(
  childrenOrConfig?: NodeData<'call_signature' | 'construct_signature' | 'export_statement' | 'index_signature' | 'method_signature' | 'property_signature'> | NodeData<'call_signature' | 'construct_signature' | 'export_statement' | 'index_signature' | 'method_signature' | 'property_signature'>[] | InterfaceBodyConfig,
): InterfaceBodyNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'interface_body', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

interfaceBody.assign = function(target: AssignableNode<'interface_body'>): InterfaceBodyNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'interface_body'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface InterfaceDeclarationConfig {
  name: NodeData<'type_identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  body: NodeData<'interface_body'>;
  children?: NodeData[];
}

export interface InterfaceDeclarationFromInput {
  name: NodeData<'type_identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  body: NodeData<'interface_body'> | InterfaceBodyFromInput | FromValue[];
  children?: (NodeData<'extends_type_clause'> | ExtendsTypeClauseFromInput)[];
}

export type InterfaceDeclarationNode = NodeData<'interface_declaration'> & {
  typeParameters(value: NodeData<'type_parameters'>): InterfaceDeclarationNode;
  body(value: NodeData<'interface_body'>): InterfaceDeclarationNode;
  children(...value: NodeData[]): InterfaceDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'interface_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type InterfaceDeclarationFromNode = NodeData<'interface_declaration'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): InterfaceDeclarationFromNode;
  body(value: NodeData<'interface_body'> | InterfaceBodyFromInput | FromValue[]): InterfaceDeclarationFromNode;
  children(...value: FromValue[]): InterfaceDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'interface_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function interfaceDeclaration(
  nameOrConfig: NodeData<'type_identifier'> | InterfaceDeclarationConfig,
  config?: Partial<InterfaceDeclarationConfig>,
): InterfaceDeclarationNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'interface_declaration', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

interfaceDeclaration.assign = function(target: AssignableNode<'interface_declaration'>): InterfaceDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'interface_declaration'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface InternalModuleConfig {
  name: NodeData<'string' | 'identifier' | 'nested_identifier'>;
  body?: NodeData<'statement_block'>;
}

export interface InternalModuleFromInput {
  name: NodeData<'string' | 'identifier' | 'nested_identifier'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'nested_identifier' } & NestedIdentifierFromInput) | FromValue[];
  body?: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type InternalModuleNode = NodeData<'internal_module'> & {
  body(value: NodeData<'statement_block'>): InternalModuleNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'internal_module'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type InternalModuleFromNode = NodeData<'internal_module'> & {
  body(value: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[]): InternalModuleFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'internal_module'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function internalModule(
  nameOrConfig: NodeData<'string' | 'identifier' | 'nested_identifier'> | InternalModuleConfig,
  config?: Partial<InternalModuleConfig>,
): InternalModuleNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'internal_module', fields };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

internalModule.assign = function(target: AssignableNode<'internal_module'>): InternalModuleNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'internal_module'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface IntersectionTypeConfig {
  children?: NodeData[];
}

export interface IntersectionTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type IntersectionTypeNode = NodeData<'intersection_type'> & {
  children(...value: NodeData[]): IntersectionTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'intersection_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type IntersectionTypeFromNode = NodeData<'intersection_type'> & {
  children(...value: FromValue[]): IntersectionTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'intersection_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function intersectionType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | IntersectionTypeConfig,
): IntersectionTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'intersection_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

intersectionType.assign = function(target: AssignableNode<'intersection_type'>): IntersectionTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'intersection_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LabeledStatementConfig {
  body: NodeData<'statement'>;
  label: NodeData<'statement_identifier'>;
}

export interface LabeledStatementFromInput {
  body: NodeData<'statement'> | StatementFromInput | FromValue[];
  label: NodeData<'statement_identifier'> | string;
}

export type LabeledStatementNode = NodeData<'labeled_statement'> & {
  label(value: NodeData<'statement_identifier'>): LabeledStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'labeled_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type LabeledStatementFromNode = NodeData<'labeled_statement'> & {
  label(value: NodeData<'statement_identifier'> | string): LabeledStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'labeled_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function labeledStatement(
  bodyOrConfig: NodeData<'statement'> | LabeledStatementConfig,
  config?: Partial<LabeledStatementConfig>,
): LabeledStatementNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'labeled_statement', fields };
  node.label = (v: any) => { fields['label'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

labeledStatement.assign = function(target: AssignableNode<'labeled_statement'>): LabeledStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['body', 'label'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'labeled_statement'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.label = (v: any) => { overrides['label'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LexicalDeclarationConfig {
  kind: NodeData<string>;
  children?: NodeData[];
}

export interface LexicalDeclarationFromInput {
  kind: NodeData | 'let' | 'const';
  children?: (NodeData<'variable_declarator'> | VariableDeclaratorFromInput)[];
}

export type LexicalDeclarationNode = NodeData<'lexical_declaration'> & {
  children(...value: NodeData[]): LexicalDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lexical_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type LexicalDeclarationFromNode = NodeData<'lexical_declaration'> & {
  children(...value: FromValue[]): LexicalDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lexical_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lexicalDeclaration(
  kindOrConfig: NodeData<string> | LexicalDeclarationConfig,
  config?: Partial<LexicalDeclarationConfig>,
): LexicalDeclarationNode {
  const fields: any = isNodeData(kindOrConfig)
    ? { 'kind': kindOrConfig, ...config }
    : kindOrConfig;
  const node: any = { type: 'lexical_declaration', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

lexicalDeclaration.assign = function(target: AssignableNode<'lexical_declaration'>): LexicalDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['kind'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'lexical_declaration'; }, get fields() { return getFields(); } };
  node.kind = (v: any) => { overrides['kind'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LiteralTypeConfig {
  children?: NodeData[];
}

export interface LiteralTypeFromInput {
  children?: (NodeData<'unary_expression' | 'number' | 'string' | 'true' | 'false' | 'null' | 'undefined'> | string | ({ kind: 'unary_expression' } & UnaryExpressionFromInput) | ({ kind: 'string' } & StringFromInput))[];
}

export type LiteralTypeNode = NodeData<'literal_type'> & {
  children(...value: NodeData[]): LiteralTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'literal_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type LiteralTypeFromNode = NodeData<'literal_type'> & {
  children(...value: FromValue[]): LiteralTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'literal_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function literalType(
  childrenOrConfig?: NodeData<'unary_expression' | 'number' | 'string' | 'true' | 'false' | 'null' | 'undefined'> | NodeData<'unary_expression' | 'number' | 'string' | 'true' | 'false' | 'null' | 'undefined'>[] | LiteralTypeConfig,
): LiteralTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'literal_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

literalType.assign = function(target: AssignableNode<'literal_type'>): LiteralTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'literal_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LookupTypeConfig {
  children?: NodeData[];
}

export interface LookupTypeFromInput {
  children?: (NodeData<'primary_type' | 'type'> | ({ kind: 'primary_type' } & PrimaryTypeFromInput) | ({ kind: 'type' } & TypeFromInput))[];
}

export type LookupTypeNode = NodeData<'lookup_type'> & {
  children(...value: NodeData[]): LookupTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lookup_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type LookupTypeFromNode = NodeData<'lookup_type'> & {
  children(...value: FromValue[]): LookupTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lookup_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lookupType(
  childrenOrConfig?: NodeData<'primary_type' | 'type'> | NodeData<'primary_type' | 'type'>[] | LookupTypeConfig,
): LookupTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'lookup_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

lookupType.assign = function(target: AssignableNode<'lookup_type'>): LookupTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'lookup_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MappedTypeClauseConfig {
  name: NodeData<'type_identifier'>;
  type: NodeData<'type'>;
  alias?: NodeData<'type'>;
}

export interface MappedTypeClauseFromInput {
  name: NodeData<'type_identifier'> | string;
  type: NodeData<'type'> | TypeFromInput | FromValue[];
  alias?: NodeData<'type'> | TypeFromInput | FromValue[];
}

export type MappedTypeClauseNode = NodeData<'mapped_type_clause'> & {
  typeField(value: NodeData<'type'>): MappedTypeClauseNode;
  alias(value: NodeData<'type'>): MappedTypeClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'mapped_type_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type MappedTypeClauseFromNode = NodeData<'mapped_type_clause'> & {
  typeField(value: NodeData<'type'> | TypeFromInput | FromValue[]): MappedTypeClauseFromNode;
  alias(value: NodeData<'type'> | TypeFromInput | FromValue[]): MappedTypeClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'mapped_type_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function mappedTypeClause(
  nameOrConfig: NodeData<'type_identifier'> | MappedTypeClauseConfig,
  config?: Partial<MappedTypeClauseConfig>,
): MappedTypeClauseNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'mapped_type_clause', fields };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.alias = (v: any) => { fields['alias'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

mappedTypeClause.assign = function(target: AssignableNode<'mapped_type_clause'>): MappedTypeClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type', 'alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'mapped_type_clause'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.alias = (v: any) => { overrides['alias'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MemberExpressionConfig {
  object: NodeData<'expression' | 'primary_expression' | 'import'>;
  optional_chain?: NodeData<'optional_chain'>;
  property: NodeData<'private_property_identifier' | 'property_identifier'>;
}

export interface MemberExpressionFromInput {
  object: NodeData<'expression' | 'primary_expression' | 'import'> | string | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'primary_expression' } & PrimaryExpressionFromInput) | FromValue[];
  optional_chain?: NodeData<'optional_chain'> | string;
  property: NodeData<'private_property_identifier' | 'property_identifier'> | string;
}

export type MemberExpressionNode = NodeData<'member_expression'> & {
  optionalChain(value: NodeData<'optional_chain'>): MemberExpressionNode;
  property(value: NodeData<'private_property_identifier' | 'property_identifier'>): MemberExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'member_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type MemberExpressionFromNode = NodeData<'member_expression'> & {
  optionalChain(value: NodeData<'optional_chain'> | string): MemberExpressionFromNode;
  property(value: NodeData<'private_property_identifier' | 'property_identifier'> | string): MemberExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'member_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function memberExpression(
  objectOrConfig: NodeData<'expression' | 'primary_expression' | 'import'> | MemberExpressionConfig,
  config?: Partial<MemberExpressionConfig>,
): MemberExpressionNode {
  const fields: any = isNodeData(objectOrConfig)
    ? { 'object': objectOrConfig, ...config }
    : objectOrConfig;
  const node: any = { type: 'member_expression', fields };
  node.optionalChain = (v: any) => { fields['optional_chain'] = v; return node; };
  node.property = (v: any) => { fields['property'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

memberExpression.assign = function(target: AssignableNode<'member_expression'>): MemberExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['object', 'optional_chain', 'property'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'member_expression'; }, get fields() { return getFields(); } };
  node.object = (v: any) => { overrides['object'] = v; return node; };
  node.optionalChain = (v: any) => { overrides['optional_chain'] = v; return node; };
  node.property = (v: any) => { overrides['property'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MethodDefinitionConfig {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
  body: NodeData<'statement_block'>;
  children?: NodeData[];
}

export interface MethodDefinitionFromInput {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput) | FromValue[];
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
  children?: (NodeData<'accessibility_modifier' | 'override_modifier'> | string)[];
}

export type MethodDefinitionNode = NodeData<'method_definition'> & {
  typeParameters(value: NodeData<'type_parameters'>): MethodDefinitionNode;
  parameters(value: NodeData<'formal_parameters'>): MethodDefinitionNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): MethodDefinitionNode;
  body(value: NodeData<'statement_block'>): MethodDefinitionNode;
  children(...value: NodeData[]): MethodDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'method_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type MethodDefinitionFromNode = NodeData<'method_definition'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): MethodDefinitionFromNode;
  parameters(value: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[]): MethodDefinitionFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): MethodDefinitionFromNode;
  body(value: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[]): MethodDefinitionFromNode;
  children(...value: FromValue[]): MethodDefinitionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'method_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function methodDefinition(
  nameOrConfig: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | MethodDefinitionConfig,
  config?: Partial<MethodDefinitionConfig>,
): MethodDefinitionNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'method_definition', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

methodDefinition.assign = function(target: AssignableNode<'method_definition'>): MethodDefinitionNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'method_definition'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MethodSignatureConfig {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>;
  type_parameters?: NodeData<'type_parameters'>;
  parameters: NodeData<'formal_parameters'>;
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>;
  children?: NodeData[];
}

export interface MethodSignatureFromInput {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput) | FromValue[];
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  parameters: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[];
  return_type?: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[];
  children?: (NodeData<'accessibility_modifier' | 'override_modifier'> | string)[];
}

export type MethodSignatureNode = NodeData<'method_signature'> & {
  typeParameters(value: NodeData<'type_parameters'>): MethodSignatureNode;
  parameters(value: NodeData<'formal_parameters'>): MethodSignatureNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'>): MethodSignatureNode;
  children(...value: NodeData[]): MethodSignatureNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'method_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type MethodSignatureFromNode = NodeData<'method_signature'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): MethodSignatureFromNode;
  parameters(value: NodeData<'formal_parameters'> | FormalParametersFromInput | FromValue[]): MethodSignatureFromNode;
  returnType(value: NodeData<'type_annotation' | 'asserts_annotation' | 'type_predicate_annotation'> | ({ kind: 'type_annotation' } & TypeAnnotationFromInput) | ({ kind: 'asserts_annotation' } & AssertsAnnotationFromInput) | ({ kind: 'type_predicate_annotation' } & TypePredicateAnnotationFromInput) | FromValue[]): MethodSignatureFromNode;
  children(...value: FromValue[]): MethodSignatureFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'method_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function methodSignature(
  nameOrConfig: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | MethodSignatureConfig,
  config?: Partial<MethodSignatureConfig>,
): MethodSignatureNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'method_signature', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

methodSignature.assign = function(target: AssignableNode<'method_signature'>): MethodSignatureNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'method_signature'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ModuleConfig {
  name: NodeData<'string' | 'identifier' | 'nested_identifier'>;
  body?: NodeData<'statement_block'>;
}

export interface ModuleFromInput {
  name: NodeData<'string' | 'identifier' | 'nested_identifier'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'nested_identifier' } & NestedIdentifierFromInput) | FromValue[];
  body?: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
}

export type ModuleNode = NodeData<'module'> & {
  body(value: NodeData<'statement_block'>): ModuleNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'module'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ModuleFromNode = NodeData<'module'> & {
  body(value: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[]): ModuleFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'module'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function module(
  nameOrConfig: NodeData<'string' | 'identifier' | 'nested_identifier'> | ModuleConfig,
  config?: Partial<ModuleConfig>,
): ModuleNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'module', fields };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

module.assign = function(target: AssignableNode<'module'>): ModuleNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'module'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NamedImportsConfig {
  children?: NodeData[];
}

export interface NamedImportsFromInput {
  children?: (NodeData<'import_specifier'> | ImportSpecifierFromInput)[];
}

export type NamedImportsNode = NodeData<'named_imports'> & {
  children(...value: NodeData[]): NamedImportsNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'named_imports'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NamedImportsFromNode = NodeData<'named_imports'> & {
  children(...value: FromValue[]): NamedImportsFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'named_imports'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function namedImports(
  childrenOrConfig?: NodeData<'import_specifier'> | NodeData<'import_specifier'>[] | NamedImportsConfig,
): NamedImportsNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'named_imports', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

namedImports.assign = function(target: AssignableNode<'named_imports'>): NamedImportsNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'named_imports'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NamespaceExportConfig {
  children?: NodeData[];
}

export interface NamespaceExportFromInput {
  children?: (NodeData<'identifier' | 'string'> | string | StringFromInput)[];
}

export type NamespaceExportNode = NodeData<'namespace_export'> & {
  children(...value: NodeData[]): NamespaceExportNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'namespace_export'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NamespaceExportFromNode = NodeData<'namespace_export'> & {
  children(...value: FromValue[]): NamespaceExportFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'namespace_export'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function namespaceExport(
  childrenOrConfig?: NodeData<'identifier' | 'string'> | NodeData<'identifier' | 'string'>[] | NamespaceExportConfig,
): NamespaceExportNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'namespace_export', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

namespaceExport.assign = function(target: AssignableNode<'namespace_export'>): NamespaceExportNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'namespace_export'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NamespaceImportConfig {
  children?: NodeData[];
}

export interface NamespaceImportFromInput {
  children?: (NodeData<'identifier'> | string)[];
}

export type NamespaceImportNode = NodeData<'namespace_import'> & {
  children(...value: NodeData[]): NamespaceImportNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'namespace_import'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NamespaceImportFromNode = NodeData<'namespace_import'> & {
  children(...value: FromValue[]): NamespaceImportFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'namespace_import'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function namespaceImport(
  childrenOrConfig?: NodeData<'identifier'> | NodeData<'identifier'>[] | NamespaceImportConfig,
): NamespaceImportNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'namespace_import', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

namespaceImport.assign = function(target: AssignableNode<'namespace_import'>): NamespaceImportNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'namespace_import'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NestedIdentifierConfig {
  object: NodeData<'identifier' | 'member_expression'>;
  property: NodeData<'property_identifier'>;
}

export interface NestedIdentifierFromInput {
  object: NodeData<'identifier' | 'member_expression'> | string | MemberExpressionFromInput | FromValue[];
  property: NodeData<'property_identifier'> | string;
}

export type NestedIdentifierNode = NodeData<'nested_identifier'> & {
  property(value: NodeData<'property_identifier'>): NestedIdentifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'nested_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NestedIdentifierFromNode = NodeData<'nested_identifier'> & {
  property(value: NodeData<'property_identifier'> | string): NestedIdentifierFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'nested_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function nestedIdentifier(
  objectOrConfig: NodeData<'identifier' | 'member_expression'> | NestedIdentifierConfig,
  config?: Partial<NestedIdentifierConfig>,
): NestedIdentifierNode {
  const fields: any = isNodeData(objectOrConfig)
    ? { 'object': objectOrConfig, ...config }
    : objectOrConfig;
  const node: any = { type: 'nested_identifier', fields };
  node.property = (v: any) => { fields['property'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

nestedIdentifier.assign = function(target: AssignableNode<'nested_identifier'>): NestedIdentifierNode {
  const overrides: any = {};
  const fieldList: string[] = ['object', 'property'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'nested_identifier'; }, get fields() { return getFields(); } };
  node.object = (v: any) => { overrides['object'] = v; return node; };
  node.property = (v: any) => { overrides['property'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NestedTypeIdentifierConfig {
  module: NodeData<'identifier' | 'nested_identifier'>;
  name: NodeData<'type_identifier'>;
}

export interface NestedTypeIdentifierFromInput {
  module: NodeData<'identifier' | 'nested_identifier'> | string | NestedIdentifierFromInput | FromValue[];
  name: NodeData<'type_identifier'> | string;
}

export type NestedTypeIdentifierNode = NodeData<'nested_type_identifier'> & {
  module(value: NodeData<'identifier' | 'nested_identifier'>): NestedTypeIdentifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'nested_type_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NestedTypeIdentifierFromNode = NodeData<'nested_type_identifier'> & {
  module(value: NodeData<'identifier' | 'nested_identifier'> | string | NestedIdentifierFromInput | FromValue[]): NestedTypeIdentifierFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'nested_type_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function nestedTypeIdentifier(
  nameOrConfig: NodeData<'type_identifier'> | NestedTypeIdentifierConfig,
  config?: Partial<NestedTypeIdentifierConfig>,
): NestedTypeIdentifierNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'nested_type_identifier', fields };
  node.module = (v: any) => { fields['module'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

nestedTypeIdentifier.assign = function(target: AssignableNode<'nested_type_identifier'>): NestedTypeIdentifierNode {
  const overrides: any = {};
  const fieldList: string[] = ['module', 'name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'nested_type_identifier'; }, get fields() { return getFields(); } };
  node.module = (v: any) => { overrides['module'] = v; return node; };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NewExpressionConfig {
  constructor: NodeData<'primary_expression'>;
  type_arguments?: NodeData<'type_arguments'>;
  arguments?: NodeData<'arguments'>;
}

export interface NewExpressionFromInput {
  constructor: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[];
  type_arguments?: NodeData<'type_arguments'> | TypeArgumentsFromInput | FromValue[];
  arguments?: NodeData<'arguments'> | ArgumentsFromInput | FromValue[];
}

export type NewExpressionNode = NodeData<'new_expression'> & {
  typeArguments(value: NodeData<'type_arguments'>): NewExpressionNode;
  arguments(value: NodeData<'arguments'>): NewExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'new_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NewExpressionFromNode = NodeData<'new_expression'> & {
  typeArguments(value: NodeData<'type_arguments'> | TypeArgumentsFromInput | FromValue[]): NewExpressionFromNode;
  arguments(value: NodeData<'arguments'> | ArgumentsFromInput | FromValue[]): NewExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'new_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function newExpression(
  constructorOrConfig: NodeData<'primary_expression'> | NewExpressionConfig,
  config?: Partial<NewExpressionConfig>,
): NewExpressionNode {
  const fields: any = isNodeData(constructorOrConfig)
    ? { 'constructor': constructorOrConfig, ...config }
    : constructorOrConfig;
  const node: any = { type: 'new_expression', fields };
  node.typeArguments = (v: any) => { fields['type_arguments'] = v; return node; };
  node.arguments = (v: any) => { fields['arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

newExpression.assign = function(target: AssignableNode<'new_expression'>): NewExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['constructor', 'type_arguments', 'arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'new_expression'; }, get fields() { return getFields(); } };
  node.constructor = (v: any) => { overrides['constructor'] = v; return node; };
  node.typeArguments = (v: any) => { overrides['type_arguments'] = v; return node; };
  node.arguments = (v: any) => { overrides['arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NonNullExpressionConfig {
  children?: NodeData[];
}

export interface NonNullExpressionFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type NonNullExpressionNode = NodeData<'non_null_expression'> & {
  children(...value: NodeData[]): NonNullExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'non_null_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NonNullExpressionFromNode = NodeData<'non_null_expression'> & {
  children(...value: FromValue[]): NonNullExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'non_null_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function nonNullExpression(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | NonNullExpressionConfig,
): NonNullExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'non_null_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

nonNullExpression.assign = function(target: AssignableNode<'non_null_expression'>): NonNullExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'non_null_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ObjectConfig {
  children?: NodeData[];
}

export interface ObjectFromInput {
  children?: (NodeData<'pair' | 'spread_element' | 'method_definition' | 'shorthand_property_identifier'> | string | ({ kind: 'pair' } & PairFromInput) | ({ kind: 'spread_element' } & SpreadElementFromInput) | ({ kind: 'method_definition' } & MethodDefinitionFromInput))[];
}

export type ObjectNode = NodeData<'object'> & {
  children(...value: NodeData[]): ObjectNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'object'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ObjectFromNode = NodeData<'object'> & {
  children(...value: FromValue[]): ObjectFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'object'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function object(
  childrenOrConfig?: NodeData<'pair' | 'spread_element' | 'method_definition' | 'shorthand_property_identifier'> | NodeData<'pair' | 'spread_element' | 'method_definition' | 'shorthand_property_identifier'>[] | ObjectConfig,
): ObjectNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'object', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

object.assign = function(target: AssignableNode<'object'>): ObjectNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'object'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ObjectAssignmentPatternConfig {
  left: NodeData<'object_pattern' | 'array_pattern' | 'shorthand_property_identifier_pattern'>;
  right: NodeData<'expression'>;
}

export interface ObjectAssignmentPatternFromInput {
  left: NodeData<'object_pattern' | 'array_pattern' | 'shorthand_property_identifier_pattern'> | string | ({ kind: 'object_pattern' } & ObjectPatternFromInput) | ({ kind: 'array_pattern' } & ArrayPatternFromInput) | FromValue[];
  right: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type ObjectAssignmentPatternNode = NodeData<'object_assignment_pattern'> & {
  right(value: NodeData<'expression'>): ObjectAssignmentPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'object_assignment_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ObjectAssignmentPatternFromNode = NodeData<'object_assignment_pattern'> & {
  right(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): ObjectAssignmentPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'object_assignment_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function objectAssignmentPattern(
  leftOrConfig: NodeData<'object_pattern' | 'array_pattern' | 'shorthand_property_identifier_pattern'> | ObjectAssignmentPatternConfig,
  config?: Partial<ObjectAssignmentPatternConfig>,
): ObjectAssignmentPatternNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'object_assignment_pattern', fields };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

objectAssignmentPattern.assign = function(target: AssignableNode<'object_assignment_pattern'>): ObjectAssignmentPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'object_assignment_pattern'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ObjectPatternConfig {
  children?: NodeData[];
}

export interface ObjectPatternFromInput {
  children?: (NodeData<'pair_pattern' | 'rest_pattern' | 'object_assignment_pattern' | 'shorthand_property_identifier_pattern'> | string | ({ kind: 'pair_pattern' } & PairPatternFromInput) | ({ kind: 'rest_pattern' } & RestPatternFromInput) | ({ kind: 'object_assignment_pattern' } & ObjectAssignmentPatternFromInput))[];
}

export type ObjectPatternNode = NodeData<'object_pattern'> & {
  children(...value: NodeData[]): ObjectPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'object_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ObjectPatternFromNode = NodeData<'object_pattern'> & {
  children(...value: FromValue[]): ObjectPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'object_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function objectPattern(
  childrenOrConfig?: NodeData<'pair_pattern' | 'rest_pattern' | 'object_assignment_pattern' | 'shorthand_property_identifier_pattern'> | NodeData<'pair_pattern' | 'rest_pattern' | 'object_assignment_pattern' | 'shorthand_property_identifier_pattern'>[] | ObjectPatternConfig,
): ObjectPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'object_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

objectPattern.assign = function(target: AssignableNode<'object_pattern'>): ObjectPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'object_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ObjectTypeConfig {
  children?: NodeData[];
}

export interface ObjectTypeFromInput {
  children?: (NodeData<'export_statement' | 'property_signature' | 'call_signature' | 'construct_signature' | 'index_signature' | 'method_signature'> | ({ kind: 'export_statement' } & ExportStatementFromInput) | ({ kind: 'property_signature' } & PropertySignatureFromInput) | ({ kind: 'call_signature' } & CallSignatureFromInput) | ({ kind: 'construct_signature' } & ConstructSignatureFromInput) | ({ kind: 'index_signature' } & IndexSignatureFromInput) | ({ kind: 'method_signature' } & MethodSignatureFromInput))[];
}

export type ObjectTypeNode = NodeData<'object_type'> & {
  children(...value: NodeData[]): ObjectTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'object_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ObjectTypeFromNode = NodeData<'object_type'> & {
  children(...value: FromValue[]): ObjectTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'object_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function objectType(
  childrenOrConfig?: NodeData<'export_statement' | 'property_signature' | 'call_signature' | 'construct_signature' | 'index_signature' | 'method_signature'> | NodeData<'export_statement' | 'property_signature' | 'call_signature' | 'construct_signature' | 'index_signature' | 'method_signature'>[] | ObjectTypeConfig,
): ObjectTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'object_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

objectType.assign = function(target: AssignableNode<'object_type'>): ObjectTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'object_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface OmittingTypeAnnotationConfig {
  children?: NodeData[];
}

export interface OmittingTypeAnnotationFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type OmittingTypeAnnotationNode = NodeData<'omitting_type_annotation'> & {
  children(...value: NodeData[]): OmittingTypeAnnotationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'omitting_type_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type OmittingTypeAnnotationFromNode = NodeData<'omitting_type_annotation'> & {
  children(...value: FromValue[]): OmittingTypeAnnotationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'omitting_type_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function omittingTypeAnnotation(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | OmittingTypeAnnotationConfig,
): OmittingTypeAnnotationNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'omitting_type_annotation', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

omittingTypeAnnotation.assign = function(target: AssignableNode<'omitting_type_annotation'>): OmittingTypeAnnotationNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'omitting_type_annotation'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface OptingTypeAnnotationConfig {
  children?: NodeData[];
}

export interface OptingTypeAnnotationFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type OptingTypeAnnotationNode = NodeData<'opting_type_annotation'> & {
  children(...value: NodeData[]): OptingTypeAnnotationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'opting_type_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type OptingTypeAnnotationFromNode = NodeData<'opting_type_annotation'> & {
  children(...value: FromValue[]): OptingTypeAnnotationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'opting_type_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function optingTypeAnnotation(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | OptingTypeAnnotationConfig,
): OptingTypeAnnotationNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'opting_type_annotation', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

optingTypeAnnotation.assign = function(target: AssignableNode<'opting_type_annotation'>): OptingTypeAnnotationNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'opting_type_annotation'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface OptionalParameterConfig {
  decorator?: NodeData<'decorator'>[];
  pattern?: NodeData<'pattern' | 'this'>;
  type?: NodeData<'type_annotation'>;
  value?: NodeData<'expression'>;
  name?: NodeData<'identifier'>;
  children?: NodeData[];
}

export interface OptionalParameterFromInput {
  decorator?: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput;
  pattern?: NodeData<'pattern' | 'this'> | string | PatternFromInput | FromValue[];
  type?: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[];
  value?: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  name?: NodeData<'identifier'> | string;
  children?: (NodeData<'accessibility_modifier' | 'override_modifier'> | string)[];
}

export type OptionalParameterNode = NodeData<'optional_parameter'> & {
  decorator(value: NodeData<'decorator'>[]): OptionalParameterNode;
  pattern(value: NodeData<'pattern' | 'this'>): OptionalParameterNode;
  typeField(value: NodeData<'type_annotation'>): OptionalParameterNode;
  value(value: NodeData<'expression'>): OptionalParameterNode;
  name(value: NodeData<'identifier'>): OptionalParameterNode;
  children(...value: NodeData[]): OptionalParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'optional_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type OptionalParameterFromNode = NodeData<'optional_parameter'> & {
  decorator(value: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput): OptionalParameterFromNode;
  pattern(value: NodeData<'pattern' | 'this'> | string | PatternFromInput | FromValue[]): OptionalParameterFromNode;
  typeField(value: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[]): OptionalParameterFromNode;
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): OptionalParameterFromNode;
  name(value: NodeData<'identifier'> | string): OptionalParameterFromNode;
  children(...value: FromValue[]): OptionalParameterFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'optional_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function optionalParameter(
  config?: OptionalParameterConfig,
): OptionalParameterNode {
  const fields: any = config ?? {};
  const node: any = { type: 'optional_parameter', fields };
  node.decorator = (...v: any[]) => { fields['decorator'] = v; return node; };
  node.pattern = (v: any) => { fields['pattern'] = v; return node; };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.name = (v: any) => { fields['name'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

optionalParameter.assign = function(target: AssignableNode<'optional_parameter'>): OptionalParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['decorator', 'pattern', 'type', 'value', 'name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'optional_parameter'; }, get fields() { return getFields(); } };
  node.decorator = (...v: any[]) => { overrides['decorator'] = v; return node; };
  node.pattern = (v: any) => { overrides['pattern'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface OptionalTypeConfig {
  children?: NodeData[];
}

export interface OptionalTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type OptionalTypeNode = NodeData<'optional_type'> & {
  children(...value: NodeData[]): OptionalTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'optional_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type OptionalTypeFromNode = NodeData<'optional_type'> & {
  children(...value: FromValue[]): OptionalTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'optional_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function optionalType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | OptionalTypeConfig,
): OptionalTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'optional_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

optionalType.assign = function(target: AssignableNode<'optional_type'>): OptionalTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'optional_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface PairConfig {
  key: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>;
  value: NodeData<'expression'>;
}

export interface PairFromInput {
  key: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput) | FromValue[];
  value: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type PairNode = NodeData<'pair'> & {
  value(value: NodeData<'expression'>): PairNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pair'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type PairFromNode = NodeData<'pair'> & {
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): PairFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pair'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function pair(
  keyOrConfig: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | PairConfig,
  config?: Partial<PairConfig>,
): PairNode {
  const fields: any = isNodeData(keyOrConfig)
    ? { 'key': keyOrConfig, ...config }
    : keyOrConfig;
  const node: any = { type: 'pair', fields };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

pair.assign = function(target: AssignableNode<'pair'>): PairNode {
  const overrides: any = {};
  const fieldList: string[] = ['key', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'pair'; }, get fields() { return getFields(); } };
  node.key = (v: any) => { overrides['key'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface PairPatternConfig {
  key: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>;
  value: NodeData<'pattern' | 'assignment_pattern'>;
}

export interface PairPatternFromInput {
  key: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput) | FromValue[];
  value: NodeData<'pattern' | 'assignment_pattern'> | ({ kind: 'pattern' } & PatternFromInput) | ({ kind: 'assignment_pattern' } & AssignmentPatternFromInput) | FromValue[];
}

export type PairPatternNode = NodeData<'pair_pattern'> & {
  value(value: NodeData<'pattern' | 'assignment_pattern'>): PairPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pair_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type PairPatternFromNode = NodeData<'pair_pattern'> & {
  value(value: NodeData<'pattern' | 'assignment_pattern'> | ({ kind: 'pattern' } & PatternFromInput) | ({ kind: 'assignment_pattern' } & AssignmentPatternFromInput) | FromValue[]): PairPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pair_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function pairPattern(
  keyOrConfig: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | PairPatternConfig,
  config?: Partial<PairPatternConfig>,
): PairPatternNode {
  const fields: any = isNodeData(keyOrConfig)
    ? { 'key': keyOrConfig, ...config }
    : keyOrConfig;
  const node: any = { type: 'pair_pattern', fields };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

pairPattern.assign = function(target: AssignableNode<'pair_pattern'>): PairPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['key', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'pair_pattern'; }, get fields() { return getFields(); } };
  node.key = (v: any) => { overrides['key'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ParenthesizedExpressionConfig {
  type?: NodeData<'type_annotation'>;
  children?: NodeData[];
}

export interface ParenthesizedExpressionFromInput {
  type?: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[];
  children?: (NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput))[];
}

export type ParenthesizedExpressionNode = NodeData<'parenthesized_expression'> & {
  typeField(value: NodeData<'type_annotation'>): ParenthesizedExpressionNode;
  children(...value: NodeData[]): ParenthesizedExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ParenthesizedExpressionFromNode = NodeData<'parenthesized_expression'> & {
  typeField(value: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[]): ParenthesizedExpressionFromNode;
  children(...value: FromValue[]): ParenthesizedExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parenthesizedExpression(
  childrenOrConfig?: NodeData<'expression' | 'sequence_expression'> | NodeData<'expression' | 'sequence_expression'>[] | ParenthesizedExpressionConfig,
): ParenthesizedExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'parenthesized_expression', fields };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

parenthesizedExpression.assign = function(target: AssignableNode<'parenthesized_expression'>): ParenthesizedExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'parenthesized_expression'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ParenthesizedTypeConfig {
  children?: NodeData[];
}

export interface ParenthesizedTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type ParenthesizedTypeNode = NodeData<'parenthesized_type'> & {
  children(...value: NodeData[]): ParenthesizedTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ParenthesizedTypeFromNode = NodeData<'parenthesized_type'> & {
  children(...value: FromValue[]): ParenthesizedTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parenthesizedType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | ParenthesizedTypeConfig,
): ParenthesizedTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'parenthesized_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

parenthesizedType.assign = function(target: AssignableNode<'parenthesized_type'>): ParenthesizedTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'parenthesized_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ProgramConfig {
  children?: NodeData[];
}

export interface ProgramFromInput {
  children?: (NodeData<'hash_bang_line' | 'statement'> | string | StatementFromInput)[];
}

export type ProgramNode = NodeData<'program'> & {
  children(...value: NodeData[]): ProgramNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'program'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ProgramFromNode = NodeData<'program'> & {
  children(...value: FromValue[]): ProgramFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'program'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function program(
  childrenOrConfig?: NodeData<'hash_bang_line' | 'statement'> | NodeData<'hash_bang_line' | 'statement'>[] | ProgramConfig,
): ProgramNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'program', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

program.assign = function(target: AssignableNode<'program'>): ProgramNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'program'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface PropertySignatureConfig {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>;
  type?: NodeData<'type_annotation'>;
  children?: NodeData[];
}

export interface PropertySignatureFromInput {
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput) | FromValue[];
  type?: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[];
  children?: (NodeData<'accessibility_modifier' | 'override_modifier'> | string)[];
}

export type PropertySignatureNode = NodeData<'property_signature'> & {
  typeField(value: NodeData<'type_annotation'>): PropertySignatureNode;
  children(...value: NodeData[]): PropertySignatureNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'property_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type PropertySignatureFromNode = NodeData<'property_signature'> & {
  typeField(value: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[]): PropertySignatureFromNode;
  children(...value: FromValue[]): PropertySignatureFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'property_signature'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function propertySignature(
  nameOrConfig: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | PropertySignatureConfig,
  config?: Partial<PropertySignatureConfig>,
): PropertySignatureNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'property_signature', fields };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

propertySignature.assign = function(target: AssignableNode<'property_signature'>): PropertySignatureNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'property_signature'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface PublicFieldDefinitionConfig {
  decorator?: NodeData<'decorator'>[];
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'>;
  type?: NodeData<'type_annotation'>;
  value?: NodeData<'expression'>;
  children?: NodeData[];
}

export interface PublicFieldDefinitionFromInput {
  decorator?: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput;
  name: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | string | ({ kind: 'string' } & StringFromInput) | ({ kind: 'computed_property_name' } & ComputedPropertyNameFromInput) | FromValue[];
  type?: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[];
  value?: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  children?: (NodeData<'accessibility_modifier' | 'override_modifier'> | string)[];
}

export type PublicFieldDefinitionNode = NodeData<'public_field_definition'> & {
  decorator(value: NodeData<'decorator'>[]): PublicFieldDefinitionNode;
  typeField(value: NodeData<'type_annotation'>): PublicFieldDefinitionNode;
  value(value: NodeData<'expression'>): PublicFieldDefinitionNode;
  children(...value: NodeData[]): PublicFieldDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'public_field_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type PublicFieldDefinitionFromNode = NodeData<'public_field_definition'> & {
  decorator(value: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput): PublicFieldDefinitionFromNode;
  typeField(value: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[]): PublicFieldDefinitionFromNode;
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): PublicFieldDefinitionFromNode;
  children(...value: FromValue[]): PublicFieldDefinitionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'public_field_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function publicFieldDefinition(
  nameOrConfig: NodeData<'property_identifier' | 'private_property_identifier' | 'string' | 'number' | 'computed_property_name'> | PublicFieldDefinitionConfig,
  config?: Partial<PublicFieldDefinitionConfig>,
): PublicFieldDefinitionNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'public_field_definition', fields };
  node.decorator = (...v: any[]) => { fields['decorator'] = v; return node; };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

publicFieldDefinition.assign = function(target: AssignableNode<'public_field_definition'>): PublicFieldDefinitionNode {
  const overrides: any = {};
  const fieldList: string[] = ['decorator', 'name', 'type', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'public_field_definition'; }, get fields() { return getFields(); } };
  node.decorator = (...v: any[]) => { overrides['decorator'] = v; return node; };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ReadonlyTypeConfig {
  children?: NodeData[];
}

export interface ReadonlyTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type ReadonlyTypeNode = NodeData<'readonly_type'> & {
  children(...value: NodeData[]): ReadonlyTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'readonly_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ReadonlyTypeFromNode = NodeData<'readonly_type'> & {
  children(...value: FromValue[]): ReadonlyTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'readonly_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function readonlyType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | ReadonlyTypeConfig,
): ReadonlyTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'readonly_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

readonlyType.assign = function(target: AssignableNode<'readonly_type'>): ReadonlyTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'readonly_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RegexConfig {
  pattern: NodeData<'regex_pattern'>;
  flags?: NodeData<'regex_flags'>;
}

export interface RegexFromInput {
  pattern: NodeData<'regex_pattern'> | string;
  flags?: NodeData<'regex_flags'> | string;
}

export type RegexNode = NodeData<'regex'> & {
  flags(value: NodeData<'regex_flags'>): RegexNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'regex'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type RegexFromNode = NodeData<'regex'> & {
  flags(value: NodeData<'regex_flags'> | string): RegexFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'regex'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function regex(
  patternOrConfig: NodeData<'regex_pattern'> | RegexConfig,
  config?: Partial<RegexConfig>,
): RegexNode {
  const fields: any = isNodeData(patternOrConfig)
    ? { 'pattern': patternOrConfig, ...config }
    : patternOrConfig;
  const node: any = { type: 'regex', fields };
  node.flags = (v: any) => { fields['flags'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

regex.assign = function(target: AssignableNode<'regex'>): RegexNode {
  const overrides: any = {};
  const fieldList: string[] = ['pattern', 'flags'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'regex'; }, get fields() { return getFields(); } };
  node.pattern = (v: any) => { overrides['pattern'] = v; return node; };
  node.flags = (v: any) => { overrides['flags'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RequiredParameterConfig {
  decorator?: NodeData<'decorator'>[];
  pattern?: NodeData<'pattern' | 'this'>;
  type?: NodeData<'type_annotation'>;
  value?: NodeData<'expression'>;
  name?: NodeData<'identifier' | 'rest_pattern'>;
  children?: NodeData[];
}

export interface RequiredParameterFromInput {
  decorator?: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput;
  pattern?: NodeData<'pattern' | 'this'> | string | PatternFromInput | FromValue[];
  type?: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[];
  value?: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  name?: NodeData<'identifier' | 'rest_pattern'> | string | RestPatternFromInput | FromValue[];
  children?: (NodeData<'accessibility_modifier' | 'override_modifier'> | string)[];
}

export type RequiredParameterNode = NodeData<'required_parameter'> & {
  decorator(value: NodeData<'decorator'>[]): RequiredParameterNode;
  pattern(value: NodeData<'pattern' | 'this'>): RequiredParameterNode;
  typeField(value: NodeData<'type_annotation'>): RequiredParameterNode;
  value(value: NodeData<'expression'>): RequiredParameterNode;
  name(value: NodeData<'identifier' | 'rest_pattern'>): RequiredParameterNode;
  children(...value: NodeData[]): RequiredParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'required_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type RequiredParameterFromNode = NodeData<'required_parameter'> & {
  decorator(value: (NodeData<'decorator'> | DecoratorFromInput)[] | NodeData<'decorator'> | DecoratorFromInput): RequiredParameterFromNode;
  pattern(value: NodeData<'pattern' | 'this'> | string | PatternFromInput | FromValue[]): RequiredParameterFromNode;
  typeField(value: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[]): RequiredParameterFromNode;
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): RequiredParameterFromNode;
  name(value: NodeData<'identifier' | 'rest_pattern'> | string | RestPatternFromInput | FromValue[]): RequiredParameterFromNode;
  children(...value: FromValue[]): RequiredParameterFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'required_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function requiredParameter(
  config?: RequiredParameterConfig,
): RequiredParameterNode {
  const fields: any = config ?? {};
  const node: any = { type: 'required_parameter', fields };
  node.decorator = (...v: any[]) => { fields['decorator'] = v; return node; };
  node.pattern = (v: any) => { fields['pattern'] = v; return node; };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.name = (v: any) => { fields['name'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

requiredParameter.assign = function(target: AssignableNode<'required_parameter'>): RequiredParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['decorator', 'pattern', 'type', 'value', 'name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'required_parameter'; }, get fields() { return getFields(); } };
  node.decorator = (...v: any[]) => { overrides['decorator'] = v; return node; };
  node.pattern = (v: any) => { overrides['pattern'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RestPatternConfig {
  children?: NodeData[];
}

export interface RestPatternFromInput {
  children?: (NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression'> | string | ({ kind: 'member_expression' } & MemberExpressionFromInput) | ({ kind: 'subscript_expression' } & SubscriptExpressionFromInput) | ({ kind: 'object_pattern' } & ObjectPatternFromInput) | ({ kind: 'array_pattern' } & ArrayPatternFromInput) | ({ kind: 'non_null_expression' } & NonNullExpressionFromInput))[];
}

export type RestPatternNode = NodeData<'rest_pattern'> & {
  children(...value: NodeData[]): RestPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'rest_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type RestPatternFromNode = NodeData<'rest_pattern'> & {
  children(...value: FromValue[]): RestPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'rest_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function restPattern(
  childrenOrConfig?: NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression'> | NodeData<'member_expression' | 'subscript_expression' | 'undefined' | 'identifier' | 'object_pattern' | 'array_pattern' | 'non_null_expression'>[] | RestPatternConfig,
): RestPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'rest_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

restPattern.assign = function(target: AssignableNode<'rest_pattern'>): RestPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'rest_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RestTypeConfig {
  children?: NodeData[];
}

export interface RestTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type RestTypeNode = NodeData<'rest_type'> & {
  children(...value: NodeData[]): RestTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'rest_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type RestTypeFromNode = NodeData<'rest_type'> & {
  children(...value: FromValue[]): RestTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'rest_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function restType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | RestTypeConfig,
): RestTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'rest_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

restType.assign = function(target: AssignableNode<'rest_type'>): RestTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'rest_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ReturnStatementConfig {
  children?: NodeData[];
}

export interface ReturnStatementFromInput {
  children?: (NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput))[];
}

export type ReturnStatementNode = NodeData<'return_statement'> & {
  children(...value: NodeData[]): ReturnStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'return_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ReturnStatementFromNode = NodeData<'return_statement'> & {
  children(...value: FromValue[]): ReturnStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'return_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function returnStatement(
  childrenOrConfig?: NodeData<'expression' | 'sequence_expression'> | NodeData<'expression' | 'sequence_expression'>[] | ReturnStatementConfig,
): ReturnStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'return_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

returnStatement.assign = function(target: AssignableNode<'return_statement'>): ReturnStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'return_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SatisfiesExpressionConfig {
  children?: NodeData[];
}

export interface SatisfiesExpressionFromInput {
  children?: (NodeData<'expression' | 'type'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'type' } & TypeFromInput))[];
}

export type SatisfiesExpressionNode = NodeData<'satisfies_expression'> & {
  children(...value: NodeData[]): SatisfiesExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'satisfies_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SatisfiesExpressionFromNode = NodeData<'satisfies_expression'> & {
  children(...value: FromValue[]): SatisfiesExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'satisfies_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function satisfiesExpression(
  childrenOrConfig?: NodeData<'expression' | 'type'> | NodeData<'expression' | 'type'>[] | SatisfiesExpressionConfig,
): SatisfiesExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'satisfies_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

satisfiesExpression.assign = function(target: AssignableNode<'satisfies_expression'>): SatisfiesExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'satisfies_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SequenceExpressionConfig {
  children?: NodeData[];
}

export interface SequenceExpressionFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type SequenceExpressionNode = NodeData<'sequence_expression'> & {
  children(...value: NodeData[]): SequenceExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'sequence_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SequenceExpressionFromNode = NodeData<'sequence_expression'> & {
  children(...value: FromValue[]): SequenceExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'sequence_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function sequenceExpression(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | SequenceExpressionConfig,
): SequenceExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'sequence_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

sequenceExpression.assign = function(target: AssignableNode<'sequence_expression'>): SequenceExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'sequence_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SpreadElementConfig {
  children?: NodeData[];
}

export interface SpreadElementFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type SpreadElementNode = NodeData<'spread_element'> & {
  children(...value: NodeData[]): SpreadElementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'spread_element'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SpreadElementFromNode = NodeData<'spread_element'> & {
  children(...value: FromValue[]): SpreadElementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'spread_element'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function spreadElement(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | SpreadElementConfig,
): SpreadElementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'spread_element', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

spreadElement.assign = function(target: AssignableNode<'spread_element'>): SpreadElementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'spread_element'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StatementBlockConfig {
  children?: NodeData[];
}

export interface StatementBlockFromInput {
  children?: (NodeData<'statement'> | StatementFromInput)[];
}

export type StatementBlockNode = NodeData<'statement_block'> & {
  children(...value: NodeData[]): StatementBlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'statement_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type StatementBlockFromNode = NodeData<'statement_block'> & {
  children(...value: FromValue[]): StatementBlockFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'statement_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function statementBlock(
  childrenOrConfig?: NodeData<'statement'> | NodeData<'statement'>[] | StatementBlockConfig,
): StatementBlockNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'statement_block', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

statementBlock.assign = function(target: AssignableNode<'statement_block'>): StatementBlockNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'statement_block'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StringConfig {
  children?: NodeData[];
}

export interface StringFromInput {
  children?: (NodeData<'string_fragment' | 'escape_sequence'> | string)[];
}

export type StringNode = NodeData<'string'> & {
  children(...value: NodeData[]): StringNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type StringFromNode = NodeData<'string'> & {
  children(...value: FromValue[]): StringFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function string(
  childrenOrConfig?: NodeData<'string_fragment' | 'escape_sequence'> | NodeData<'string_fragment' | 'escape_sequence'>[] | StringConfig,
): StringNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'string', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

string.assign = function(target: AssignableNode<'string'>): StringNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'string'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SubscriptExpressionConfig {
  object: NodeData<'expression' | 'primary_expression'>;
  optional_chain?: NodeData<'optional_chain'>;
  index: NodeData<'expression' | 'sequence_expression'>;
}

export interface SubscriptExpressionFromInput {
  object: NodeData<'expression' | 'primary_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'primary_expression' } & PrimaryExpressionFromInput) | FromValue[];
  optional_chain?: NodeData<'optional_chain'> | string;
  index: NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput) | FromValue[];
}

export type SubscriptExpressionNode = NodeData<'subscript_expression'> & {
  optionalChain(value: NodeData<'optional_chain'>): SubscriptExpressionNode;
  index(value: NodeData<'expression' | 'sequence_expression'>): SubscriptExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'subscript_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SubscriptExpressionFromNode = NodeData<'subscript_expression'> & {
  optionalChain(value: NodeData<'optional_chain'> | string): SubscriptExpressionFromNode;
  index(value: NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput) | FromValue[]): SubscriptExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'subscript_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function subscriptExpression(
  objectOrConfig: NodeData<'expression' | 'primary_expression'> | SubscriptExpressionConfig,
  config?: Partial<SubscriptExpressionConfig>,
): SubscriptExpressionNode {
  const fields: any = isNodeData(objectOrConfig)
    ? { 'object': objectOrConfig, ...config }
    : objectOrConfig;
  const node: any = { type: 'subscript_expression', fields };
  node.optionalChain = (v: any) => { fields['optional_chain'] = v; return node; };
  node.index = (v: any) => { fields['index'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

subscriptExpression.assign = function(target: AssignableNode<'subscript_expression'>): SubscriptExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['object', 'optional_chain', 'index'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'subscript_expression'; }, get fields() { return getFields(); } };
  node.object = (v: any) => { overrides['object'] = v; return node; };
  node.optionalChain = (v: any) => { overrides['optional_chain'] = v; return node; };
  node.index = (v: any) => { overrides['index'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SwitchBodyConfig {
  children?: NodeData[];
}

export interface SwitchBodyFromInput {
  children?: (NodeData<'switch_case' | 'switch_default'> | ({ kind: 'switch_case' } & SwitchCaseFromInput) | ({ kind: 'switch_default' } & SwitchDefaultFromInput))[];
}

export type SwitchBodyNode = NodeData<'switch_body'> & {
  children(...value: NodeData[]): SwitchBodyNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'switch_body'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SwitchBodyFromNode = NodeData<'switch_body'> & {
  children(...value: FromValue[]): SwitchBodyFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'switch_body'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function switchBody(
  childrenOrConfig?: NodeData<'switch_case' | 'switch_default'> | NodeData<'switch_case' | 'switch_default'>[] | SwitchBodyConfig,
): SwitchBodyNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'switch_body', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

switchBody.assign = function(target: AssignableNode<'switch_body'>): SwitchBodyNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'switch_body'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SwitchCaseConfig {
  value: NodeData<'expression' | 'sequence_expression'>;
  body?: NodeData<'statement'>[];
}

export interface SwitchCaseFromInput {
  value: NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput) | FromValue[];
  body?: (NodeData<'statement'> | StatementFromInput)[] | NodeData<'statement'> | StatementFromInput;
}

export type SwitchCaseNode = NodeData<'switch_case'> & {
  body(value: NodeData<'statement'>[]): SwitchCaseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'switch_case'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SwitchCaseFromNode = NodeData<'switch_case'> & {
  body(value: (NodeData<'statement'> | StatementFromInput)[] | NodeData<'statement'> | StatementFromInput): SwitchCaseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'switch_case'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function switchCase(
  valueOrConfig: NodeData<'expression' | 'sequence_expression'> | SwitchCaseConfig,
  config?: Partial<SwitchCaseConfig>,
): SwitchCaseNode {
  const fields: any = isNodeData(valueOrConfig)
    ? { 'value': valueOrConfig, ...config }
    : valueOrConfig;
  const node: any = { type: 'switch_case', fields };
  node.body = (...v: any[]) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

switchCase.assign = function(target: AssignableNode<'switch_case'>): SwitchCaseNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'switch_case'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.body = (...v: any[]) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SwitchDefaultConfig {
  body?: NodeData<'statement'>[];
}

export interface SwitchDefaultFromInput {
  body?: (NodeData<'statement'> | StatementFromInput)[] | NodeData<'statement'> | StatementFromInput;
}

export type SwitchDefaultNode = NodeData<'switch_default'> & {
  body(value: NodeData<'statement'>[]): SwitchDefaultNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'switch_default'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SwitchDefaultFromNode = NodeData<'switch_default'> & {
  body(value: (NodeData<'statement'> | StatementFromInput)[] | NodeData<'statement'> | StatementFromInput): SwitchDefaultFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'switch_default'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function switchDefault(
  config?: SwitchDefaultConfig,
): SwitchDefaultNode {
  const fields: any = config ?? {};
  const node: any = { type: 'switch_default', fields };
  node.body = (...v: any[]) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

switchDefault.assign = function(target: AssignableNode<'switch_default'>): SwitchDefaultNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'switch_default'; }, get fields() { return getFields(); } };
  node.body = (...v: any[]) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SwitchStatementConfig {
  value: NodeData<'parenthesized_expression'>;
  body: NodeData<'switch_body'>;
}

export interface SwitchStatementFromInput {
  value: NodeData<'parenthesized_expression'> | ParenthesizedExpressionFromInput | FromValue[];
  body: NodeData<'switch_body'> | SwitchBodyFromInput | FromValue[];
}

export type SwitchStatementNode = NodeData<'switch_statement'> & {
  body(value: NodeData<'switch_body'>): SwitchStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'switch_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SwitchStatementFromNode = NodeData<'switch_statement'> & {
  body(value: NodeData<'switch_body'> | SwitchBodyFromInput | FromValue[]): SwitchStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'switch_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function switchStatement(
  valueOrConfig: NodeData<'parenthesized_expression'> | SwitchStatementConfig,
  config?: Partial<SwitchStatementConfig>,
): SwitchStatementNode {
  const fields: any = isNodeData(valueOrConfig)
    ? { 'value': valueOrConfig, ...config }
    : valueOrConfig;
  const node: any = { type: 'switch_statement', fields };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

switchStatement.assign = function(target: AssignableNode<'switch_statement'>): SwitchStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'switch_statement'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TemplateLiteralTypeConfig {
  children?: NodeData[];
}

export interface TemplateLiteralTypeFromInput {
  children?: (NodeData<'string_fragment' | 'template_type'> | string | TemplateTypeFromInput)[];
}

export type TemplateLiteralTypeNode = NodeData<'template_literal_type'> & {
  children(...value: NodeData[]): TemplateLiteralTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'template_literal_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TemplateLiteralTypeFromNode = NodeData<'template_literal_type'> & {
  children(...value: FromValue[]): TemplateLiteralTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'template_literal_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function templateLiteralType(
  childrenOrConfig?: NodeData<'string_fragment' | 'template_type'> | NodeData<'string_fragment' | 'template_type'>[] | TemplateLiteralTypeConfig,
): TemplateLiteralTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'template_literal_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

templateLiteralType.assign = function(target: AssignableNode<'template_literal_type'>): TemplateLiteralTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'template_literal_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TemplateStringConfig {
  children?: NodeData[];
}

export interface TemplateStringFromInput {
  children?: (NodeData<'string_fragment' | 'escape_sequence' | 'template_substitution'> | string | TemplateSubstitutionFromInput)[];
}

export type TemplateStringNode = NodeData<'template_string'> & {
  children(...value: NodeData[]): TemplateStringNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'template_string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TemplateStringFromNode = NodeData<'template_string'> & {
  children(...value: FromValue[]): TemplateStringFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'template_string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function templateString(
  childrenOrConfig?: NodeData<'string_fragment' | 'escape_sequence' | 'template_substitution'> | NodeData<'string_fragment' | 'escape_sequence' | 'template_substitution'>[] | TemplateStringConfig,
): TemplateStringNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'template_string', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

templateString.assign = function(target: AssignableNode<'template_string'>): TemplateStringNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'template_string'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TemplateSubstitutionConfig {
  children?: NodeData[];
}

export interface TemplateSubstitutionFromInput {
  children?: (NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput))[];
}

export type TemplateSubstitutionNode = NodeData<'template_substitution'> & {
  children(...value: NodeData[]): TemplateSubstitutionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'template_substitution'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TemplateSubstitutionFromNode = NodeData<'template_substitution'> & {
  children(...value: FromValue[]): TemplateSubstitutionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'template_substitution'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function templateSubstitution(
  childrenOrConfig?: NodeData<'expression' | 'sequence_expression'> | NodeData<'expression' | 'sequence_expression'>[] | TemplateSubstitutionConfig,
): TemplateSubstitutionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'template_substitution', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

templateSubstitution.assign = function(target: AssignableNode<'template_substitution'>): TemplateSubstitutionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'template_substitution'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TemplateTypeConfig {
  children?: NodeData[];
}

export interface TemplateTypeFromInput {
  children?: (NodeData<'primary_type' | 'infer_type'> | ({ kind: 'primary_type' } & PrimaryTypeFromInput) | ({ kind: 'infer_type' } & InferTypeFromInput))[];
}

export type TemplateTypeNode = NodeData<'template_type'> & {
  children(...value: NodeData[]): TemplateTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'template_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TemplateTypeFromNode = NodeData<'template_type'> & {
  children(...value: FromValue[]): TemplateTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'template_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function templateType(
  childrenOrConfig?: NodeData<'primary_type' | 'infer_type'> | NodeData<'primary_type' | 'infer_type'>[] | TemplateTypeConfig,
): TemplateTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'template_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

templateType.assign = function(target: AssignableNode<'template_type'>): TemplateTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'template_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TernaryExpressionConfig {
  condition: NodeData<'expression'>;
  consequence: NodeData<'expression'>;
  alternative: NodeData<'expression'>;
}

export interface TernaryExpressionFromInput {
  condition: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  consequence: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  alternative: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type TernaryExpressionNode = NodeData<'ternary_expression'> & {
  consequence(value: NodeData<'expression'>): TernaryExpressionNode;
  alternative(value: NodeData<'expression'>): TernaryExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'ternary_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TernaryExpressionFromNode = NodeData<'ternary_expression'> & {
  consequence(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): TernaryExpressionFromNode;
  alternative(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): TernaryExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'ternary_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function ternaryExpression(
  conditionOrConfig: NodeData<'expression'> | TernaryExpressionConfig,
  config?: Partial<TernaryExpressionConfig>,
): TernaryExpressionNode {
  const fields: any = isNodeData(conditionOrConfig)
    ? { 'condition': conditionOrConfig, ...config }
    : conditionOrConfig;
  const node: any = { type: 'ternary_expression', fields };
  node.consequence = (v: any) => { fields['consequence'] = v; return node; };
  node.alternative = (v: any) => { fields['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

ternaryExpression.assign = function(target: AssignableNode<'ternary_expression'>): TernaryExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition', 'consequence', 'alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'ternary_expression'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = v; return node; };
  node.consequence = (v: any) => { overrides['consequence'] = v; return node; };
  node.alternative = (v: any) => { overrides['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ThrowStatementConfig {
  children?: NodeData[];
}

export interface ThrowStatementFromInput {
  children?: (NodeData<'expression' | 'sequence_expression'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'sequence_expression' } & SequenceExpressionFromInput))[];
}

export type ThrowStatementNode = NodeData<'throw_statement'> & {
  children(...value: NodeData[]): ThrowStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'throw_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ThrowStatementFromNode = NodeData<'throw_statement'> & {
  children(...value: FromValue[]): ThrowStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'throw_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function throwStatement(
  childrenOrConfig?: NodeData<'expression' | 'sequence_expression'> | NodeData<'expression' | 'sequence_expression'>[] | ThrowStatementConfig,
): ThrowStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'throw_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

throwStatement.assign = function(target: AssignableNode<'throw_statement'>): ThrowStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'throw_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TryStatementConfig {
  body: NodeData<'statement_block'>;
  handler?: NodeData<'catch_clause'>;
  finalizer?: NodeData<'finally_clause'>;
}

export interface TryStatementFromInput {
  body: NodeData<'statement_block'> | StatementBlockFromInput | FromValue[];
  handler?: NodeData<'catch_clause'> | CatchClauseFromInput | FromValue[];
  finalizer?: NodeData<'finally_clause'> | FinallyClauseFromInput | FromValue[];
}

export type TryStatementNode = NodeData<'try_statement'> & {
  handler(value: NodeData<'catch_clause'>): TryStatementNode;
  finalizer(value: NodeData<'finally_clause'>): TryStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'try_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TryStatementFromNode = NodeData<'try_statement'> & {
  handler(value: NodeData<'catch_clause'> | CatchClauseFromInput | FromValue[]): TryStatementFromNode;
  finalizer(value: NodeData<'finally_clause'> | FinallyClauseFromInput | FromValue[]): TryStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'try_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tryStatement(
  bodyOrConfig: NodeData<'statement_block'> | TryStatementConfig,
  config?: Partial<TryStatementConfig>,
): TryStatementNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'try_statement', fields };
  node.handler = (v: any) => { fields['handler'] = v; return node; };
  node.finalizer = (v: any) => { fields['finalizer'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tryStatement.assign = function(target: AssignableNode<'try_statement'>): TryStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['body', 'handler', 'finalizer'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'try_statement'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.handler = (v: any) => { overrides['handler'] = v; return node; };
  node.finalizer = (v: any) => { overrides['finalizer'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TupleTypeConfig {
  children?: NodeData[];
}

export interface TupleTypeFromInput {
  children?: (NodeData<'required_parameter' | 'optional_parameter' | 'optional_type' | 'rest_type' | 'type'> | ({ kind: 'required_parameter' } & RequiredParameterFromInput) | ({ kind: 'optional_parameter' } & OptionalParameterFromInput) | ({ kind: 'optional_type' } & OptionalTypeFromInput) | ({ kind: 'rest_type' } & RestTypeFromInput) | ({ kind: 'type' } & TypeFromInput))[];
}

export type TupleTypeNode = NodeData<'tuple_type'> & {
  children(...value: NodeData[]): TupleTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TupleTypeFromNode = NodeData<'tuple_type'> & {
  children(...value: FromValue[]): TupleTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tupleType(
  childrenOrConfig?: NodeData<'required_parameter' | 'optional_parameter' | 'optional_type' | 'rest_type' | 'type'> | NodeData<'required_parameter' | 'optional_parameter' | 'optional_type' | 'rest_type' | 'type'>[] | TupleTypeConfig,
): TupleTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'tuple_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tupleType.assign = function(target: AssignableNode<'tuple_type'>): TupleTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'tuple_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeAliasDeclarationConfig {
  name: NodeData<'type_identifier'>;
  type_parameters?: NodeData<'type_parameters'>;
  value: NodeData<'type'>;
}

export interface TypeAliasDeclarationFromInput {
  name: NodeData<'type_identifier'> | string;
  type_parameters?: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[];
  value: NodeData<'type'> | TypeFromInput | FromValue[];
}

export type TypeAliasDeclarationNode = NodeData<'type_alias_declaration'> & {
  typeParameters(value: NodeData<'type_parameters'>): TypeAliasDeclarationNode;
  value(value: NodeData<'type'>): TypeAliasDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_alias_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeAliasDeclarationFromNode = NodeData<'type_alias_declaration'> & {
  typeParameters(value: NodeData<'type_parameters'> | TypeParametersFromInput | FromValue[]): TypeAliasDeclarationFromNode;
  value(value: NodeData<'type'> | TypeFromInput | FromValue[]): TypeAliasDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_alias_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeAliasDeclaration(
  nameOrConfig: NodeData<'type_identifier'> | TypeAliasDeclarationConfig,
  config?: Partial<TypeAliasDeclarationConfig>,
): TypeAliasDeclarationNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'type_alias_declaration', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeAliasDeclaration.assign = function(target: AssignableNode<'type_alias_declaration'>): TypeAliasDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'type_alias_declaration'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeAnnotationConfig {
  children?: NodeData[];
}

export interface TypeAnnotationFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type TypeAnnotationNode = NodeData<'type_annotation'> & {
  children(...value: NodeData[]): TypeAnnotationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeAnnotationFromNode = NodeData<'type_annotation'> & {
  children(...value: FromValue[]): TypeAnnotationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeAnnotation(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | TypeAnnotationConfig,
): TypeAnnotationNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'type_annotation', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeAnnotation.assign = function(target: AssignableNode<'type_annotation'>): TypeAnnotationNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_annotation'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeArgumentsConfig {
  children?: NodeData[];
}

export interface TypeArgumentsFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type TypeArgumentsNode = NodeData<'type_arguments'> & {
  children(...value: NodeData[]): TypeArgumentsNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_arguments'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeArgumentsFromNode = NodeData<'type_arguments'> & {
  children(...value: FromValue[]): TypeArgumentsFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_arguments'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeArguments(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | TypeArgumentsConfig,
): TypeArgumentsNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'type_arguments', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeArguments.assign = function(target: AssignableNode<'type_arguments'>): TypeArgumentsNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_arguments'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeAssertionConfig {
  children?: NodeData[];
}

export interface TypeAssertionFromInput {
  children?: (NodeData<'type_arguments' | 'expression'> | ({ kind: 'type_arguments' } & TypeArgumentsFromInput) | ({ kind: 'expression' } & ExpressionFromInput))[];
}

export type TypeAssertionNode = NodeData<'type_assertion'> & {
  children(...value: NodeData[]): TypeAssertionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_assertion'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeAssertionFromNode = NodeData<'type_assertion'> & {
  children(...value: FromValue[]): TypeAssertionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_assertion'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeAssertion(
  childrenOrConfig?: NodeData<'type_arguments' | 'expression'> | NodeData<'type_arguments' | 'expression'>[] | TypeAssertionConfig,
): TypeAssertionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'type_assertion', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeAssertion.assign = function(target: AssignableNode<'type_assertion'>): TypeAssertionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_assertion'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeParameterConfig {
  name: NodeData<'type_identifier'>;
  constraint?: NodeData<'constraint'>;
  value?: NodeData<'default_type'>;
}

export interface TypeParameterFromInput {
  name: NodeData<'type_identifier'> | string;
  constraint?: NodeData<'constraint'> | ConstraintFromInput | FromValue[];
  value?: NodeData<'default_type'> | DefaultTypeFromInput | FromValue[];
}

export type TypeParameterNode = NodeData<'type_parameter'> & {
  constraint(value: NodeData<'constraint'>): TypeParameterNode;
  value(value: NodeData<'default_type'>): TypeParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeParameterFromNode = NodeData<'type_parameter'> & {
  constraint(value: NodeData<'constraint'> | ConstraintFromInput | FromValue[]): TypeParameterFromNode;
  value(value: NodeData<'default_type'> | DefaultTypeFromInput | FromValue[]): TypeParameterFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeParameter(
  nameOrConfig: NodeData<'type_identifier'> | TypeParameterConfig,
  config?: Partial<TypeParameterConfig>,
): TypeParameterNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'type_parameter', fields };
  node.constraint = (v: any) => { fields['constraint'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeParameter.assign = function(target: AssignableNode<'type_parameter'>): TypeParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'constraint', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'type_parameter'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.constraint = (v: any) => { overrides['constraint'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeParametersConfig {
  children?: NodeData[];
}

export interface TypeParametersFromInput {
  children?: (NodeData<'type_parameter'> | TypeParameterFromInput)[];
}

export type TypeParametersNode = NodeData<'type_parameters'> & {
  children(...value: NodeData[]): TypeParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeParametersFromNode = NodeData<'type_parameters'> & {
  children(...value: FromValue[]): TypeParametersFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeParameters(
  childrenOrConfig?: NodeData<'type_parameter'> | NodeData<'type_parameter'>[] | TypeParametersConfig,
): TypeParametersNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'type_parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeParameters.assign = function(target: AssignableNode<'type_parameters'>): TypeParametersNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_parameters'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypePredicateConfig {
  name: NodeData<'identifier' | 'this'>;
  type: NodeData<'type'>;
}

export interface TypePredicateFromInput {
  name: NodeData<'identifier' | 'this'> | string;
  type: NodeData<'type'> | TypeFromInput | FromValue[];
}

export type TypePredicateNode = NodeData<'type_predicate'> & {
  typeField(value: NodeData<'type'>): TypePredicateNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_predicate'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypePredicateFromNode = NodeData<'type_predicate'> & {
  typeField(value: NodeData<'type'> | TypeFromInput | FromValue[]): TypePredicateFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_predicate'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typePredicate(
  nameOrConfig: NodeData<'identifier' | 'this'> | TypePredicateConfig,
  config?: Partial<TypePredicateConfig>,
): TypePredicateNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'type_predicate', fields };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typePredicate.assign = function(target: AssignableNode<'type_predicate'>): TypePredicateNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'type_predicate'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypePredicateAnnotationConfig {
  children?: NodeData[];
}

export interface TypePredicateAnnotationFromInput {
  children?: (NodeData<'type_predicate'> | TypePredicateFromInput)[];
}

export type TypePredicateAnnotationNode = NodeData<'type_predicate_annotation'> & {
  children(...value: NodeData[]): TypePredicateAnnotationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_predicate_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypePredicateAnnotationFromNode = NodeData<'type_predicate_annotation'> & {
  children(...value: FromValue[]): TypePredicateAnnotationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_predicate_annotation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typePredicateAnnotation(
  childrenOrConfig?: NodeData<'type_predicate'> | NodeData<'type_predicate'>[] | TypePredicateAnnotationConfig,
): TypePredicateAnnotationNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'type_predicate_annotation', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typePredicateAnnotation.assign = function(target: AssignableNode<'type_predicate_annotation'>): TypePredicateAnnotationNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_predicate_annotation'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeQueryConfig {
  children?: NodeData[];
}

export interface TypeQueryFromInput {
  children?: (NodeData<'subscript_expression' | 'member_expression' | 'call_expression' | 'instantiation_expression' | 'identifier' | 'this'> | string | ({ kind: 'subscript_expression' } & SubscriptExpressionFromInput) | ({ kind: 'member_expression' } & MemberExpressionFromInput) | ({ kind: 'call_expression' } & CallExpressionFromInput) | ({ kind: 'instantiation_expression' } & InstantiationExpressionFromInput))[];
}

export type TypeQueryNode = NodeData<'type_query'> & {
  children(...value: NodeData[]): TypeQueryNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_query'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeQueryFromNode = NodeData<'type_query'> & {
  children(...value: FromValue[]): TypeQueryFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_query'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeQuery(
  childrenOrConfig?: NodeData<'subscript_expression' | 'member_expression' | 'call_expression' | 'instantiation_expression' | 'identifier' | 'this'> | NodeData<'subscript_expression' | 'member_expression' | 'call_expression' | 'instantiation_expression' | 'identifier' | 'this'>[] | TypeQueryConfig,
): TypeQueryNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'type_query', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeQuery.assign = function(target: AssignableNode<'type_query'>): TypeQueryNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_query'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UnaryExpressionConfig {
  operator: NodeData<string>;
  argument: NodeData<'expression'>;
}

export interface UnaryExpressionFromInput {
  operator: NodeData | '!' | '~' | '-' | '+' | 'typeof' | 'void' | 'delete';
  argument: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type UnaryExpressionNode = NodeData<'unary_expression'> & {
  argument(value: NodeData<'expression'>): UnaryExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'unary_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type UnaryExpressionFromNode = NodeData<'unary_expression'> & {
  argument(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): UnaryExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'unary_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unaryExpression(
  operatorOrConfig: NodeData<string> | UnaryExpressionConfig,
  config?: Partial<UnaryExpressionConfig>,
): UnaryExpressionNode {
  const fields: any = isNodeData(operatorOrConfig)
    ? { 'operator': operatorOrConfig, ...config }
    : operatorOrConfig;
  const node: any = { type: 'unary_expression', fields };
  node.argument = (v: any) => { fields['argument'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

unaryExpression.assign = function(target: AssignableNode<'unary_expression'>): UnaryExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['operator', 'argument'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'unary_expression'; }, get fields() { return getFields(); } };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.argument = (v: any) => { overrides['argument'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UnionTypeConfig {
  children?: NodeData[];
}

export interface UnionTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type UnionTypeNode = NodeData<'union_type'> & {
  children(...value: NodeData[]): UnionTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type UnionTypeFromNode = NodeData<'union_type'> & {
  children(...value: FromValue[]): UnionTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unionType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | UnionTypeConfig,
): UnionTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'union_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

unionType.assign = function(target: AssignableNode<'union_type'>): UnionTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'union_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UpdateExpressionConfig {
  argument: NodeData<'expression'>;
  operator: NodeData<string>;
}

export interface UpdateExpressionFromInput {
  argument: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  operator: NodeData | '++' | '--';
}

export type UpdateExpressionNode = NodeData<'update_expression'> & {
  operator(value: NodeData<string>): UpdateExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'update_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type UpdateExpressionFromNode = NodeData<'update_expression'> & {
  operator(value: NodeData | '++' | '--'): UpdateExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'update_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function updateExpression(
  argumentOrConfig: NodeData<'expression'> | UpdateExpressionConfig,
  config?: Partial<UpdateExpressionConfig>,
): UpdateExpressionNode {
  const fields: any = isNodeData(argumentOrConfig)
    ? { 'argument': argumentOrConfig, ...config }
    : argumentOrConfig;
  const node: any = { type: 'update_expression', fields };
  node.operator = (v: any) => { fields['operator'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

updateExpression.assign = function(target: AssignableNode<'update_expression'>): UpdateExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['argument', 'operator'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'update_expression'; }, get fields() { return getFields(); } };
  node.argument = (v: any) => { overrides['argument'] = v; return node; };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface VariableDeclarationConfig {
  children?: NodeData[];
}

export interface VariableDeclarationFromInput {
  children?: (NodeData<'variable_declarator'> | VariableDeclaratorFromInput)[];
}

export type VariableDeclarationNode = NodeData<'variable_declaration'> & {
  children(...value: NodeData[]): VariableDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'variable_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type VariableDeclarationFromNode = NodeData<'variable_declaration'> & {
  children(...value: FromValue[]): VariableDeclarationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'variable_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function variableDeclaration(
  childrenOrConfig?: NodeData<'variable_declarator'> | NodeData<'variable_declarator'>[] | VariableDeclarationConfig,
): VariableDeclarationNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'variable_declaration', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

variableDeclaration.assign = function(target: AssignableNode<'variable_declaration'>): VariableDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'variable_declaration'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface VariableDeclaratorConfig {
  name: NodeData<'object_pattern' | 'array_pattern' | 'identifier'>;
  type?: NodeData<'type_annotation'>;
  value?: NodeData<'expression'>;
}

export interface VariableDeclaratorFromInput {
  name: NodeData<'object_pattern' | 'array_pattern' | 'identifier'> | string | ({ kind: 'object_pattern' } & ObjectPatternFromInput) | ({ kind: 'array_pattern' } & ArrayPatternFromInput) | FromValue[];
  type?: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[];
  value?: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type VariableDeclaratorNode = NodeData<'variable_declarator'> & {
  typeField(value: NodeData<'type_annotation'>): VariableDeclaratorNode;
  value(value: NodeData<'expression'>): VariableDeclaratorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'variable_declarator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type VariableDeclaratorFromNode = NodeData<'variable_declarator'> & {
  typeField(value: NodeData<'type_annotation'> | TypeAnnotationFromInput | FromValue[]): VariableDeclaratorFromNode;
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): VariableDeclaratorFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'variable_declarator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function variableDeclarator(
  nameOrConfig: NodeData<'object_pattern' | 'array_pattern' | 'identifier'> | VariableDeclaratorConfig,
  config?: Partial<VariableDeclaratorConfig>,
): VariableDeclaratorNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'variable_declarator', fields };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

variableDeclarator.assign = function(target: AssignableNode<'variable_declarator'>): VariableDeclaratorNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'variable_declarator'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WhileStatementConfig {
  condition: NodeData<'parenthesized_expression'>;
  body: NodeData<'statement'>;
}

export interface WhileStatementFromInput {
  condition: NodeData<'parenthesized_expression'> | ParenthesizedExpressionFromInput | FromValue[];
  body: NodeData<'statement'> | StatementFromInput | FromValue[];
}

export type WhileStatementNode = NodeData<'while_statement'> & {
  body(value: NodeData<'statement'>): WhileStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'while_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type WhileStatementFromNode = NodeData<'while_statement'> & {
  body(value: NodeData<'statement'> | StatementFromInput | FromValue[]): WhileStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'while_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function whileStatement(
  conditionOrConfig: NodeData<'parenthesized_expression'> | WhileStatementConfig,
  config?: Partial<WhileStatementConfig>,
): WhileStatementNode {
  const fields: any = isNodeData(conditionOrConfig)
    ? { 'condition': conditionOrConfig, ...config }
    : conditionOrConfig;
  const node: any = { type: 'while_statement', fields };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

whileStatement.assign = function(target: AssignableNode<'while_statement'>): WhileStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'while_statement'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WithStatementConfig {
  object: NodeData<'parenthesized_expression'>;
  body: NodeData<'statement'>;
}

export interface WithStatementFromInput {
  object: NodeData<'parenthesized_expression'> | ParenthesizedExpressionFromInput | FromValue[];
  body: NodeData<'statement'> | StatementFromInput | FromValue[];
}

export type WithStatementNode = NodeData<'with_statement'> & {
  body(value: NodeData<'statement'>): WithStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type WithStatementFromNode = NodeData<'with_statement'> & {
  body(value: NodeData<'statement'> | StatementFromInput | FromValue[]): WithStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function withStatement(
  objectOrConfig: NodeData<'parenthesized_expression'> | WithStatementConfig,
  config?: Partial<WithStatementConfig>,
): WithStatementNode {
  const fields: any = isNodeData(objectOrConfig)
    ? { 'object': objectOrConfig, ...config }
    : objectOrConfig;
  const node: any = { type: 'with_statement', fields };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

withStatement.assign = function(target: AssignableNode<'with_statement'>): WithStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['object', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'with_statement'; }, get fields() { return getFields(); } };
  node.object = (v: any) => { overrides['object'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface YieldExpressionConfig {
  children?: NodeData[];
}

export interface YieldExpressionFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type YieldExpressionNode = NodeData<'yield_expression'> & {
  children(...value: NodeData[]): YieldExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'yield_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type YieldExpressionFromNode = NodeData<'yield_expression'> & {
  children(...value: FromValue[]): YieldExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'yield_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function yieldExpression(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | YieldExpressionConfig,
): YieldExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'yield_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

yieldExpression.assign = function(target: AssignableNode<'yield_expression'>): YieldExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'yield_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export function accessibilityModifier(text: 'private' | 'protected' | 'public'): NodeData<'accessibility_modifier'> & { render(): string; replace(target: { type: 'accessibility_modifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'accessibility_modifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function debuggerStatement(text: string): NodeData<'debugger_statement'> & { render(): string; replace(target: { type: 'debugger_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'debugger_statement', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function emptyStatement(): NodeData<'empty_statement'> & { render(): string; replace(target: { type: 'empty_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'empty_statement', fields: {}, text: ';' };
  node.render = () => ';';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: ';' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: ';' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: ';' }; };
  return node;
}

export function existentialType(): NodeData<'existential_type'> & { render(): string; replace(target: { type: 'existential_type'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'existential_type', fields: {}, text: '*' };
  node.render = () => '*';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '*' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; };
  return node;
}

export function identifier(text: string): NodeData<'identifier'> & { render(): string; replace(target: { type: 'identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  const node: any = { type: 'identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function import_(): NodeData<'import'> & { render(): string; replace(target: { type: 'import'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'import', fields: {}, text: 'import' };
  node.render = () => 'import';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'import' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'import' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' }; };
  return node;
}

export function metaProperty(text: string): NodeData<'meta_property'> & { render(): string; replace(target: { type: 'meta_property'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^(?:new\.target|import\.meta)$/.test(text)) throw new Error(`Invalid meta_property: '${text}' does not match grammar pattern`);
  const node: any = { type: 'meta_property', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function optionalChain(): NodeData<'optional_chain'> & { render(): string; replace(target: { type: 'optional_chain'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'optional_chain', fields: {}, text: '?.' };
  node.render = () => '?.';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '?.' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '?.' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '?.' }; };
  return node;
}

export function overrideModifier(): NodeData<'override_modifier'> & { render(): string; replace(target: { type: 'override_modifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'override_modifier', fields: {}, text: 'override' };
  node.render = () => 'override';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'override' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'override' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' }; };
  return node;
}

export function predefinedType(text: 'any' | 'boolean' | 'never' | 'number' | 'object' | 'string' | 'string' | 'symbol' | 'unknown' | 'void'): NodeData<'predefined_type'> & { render(): string; replace(target: { type: 'predefined_type'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'predefined_type', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function comment(text: string): NodeData<'comment'> & { render(): string; replace(target: { type: 'comment'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'comment', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function escapeSequence(text: `\\${string}`): NodeData<'escape_sequence'> & { render(): string; replace(target: { type: 'escape_sequence'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'escape_sequence', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function false_(): NodeData<'false'> & { render(): string; replace(target: { type: 'false'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'false', fields: {}, text: 'false' };
  node.render = () => 'false';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'false' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'false' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' }; };
  return node;
}

export function hashBangLine(text: string): NodeData<'hash_bang_line'> & { render(): string; replace(target: { type: 'hash_bang_line'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^#!.*$/.test(text)) throw new Error(`Invalid hash_bang_line: '${text}' does not match grammar pattern`);
  const node: any = { type: 'hash_bang_line', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function htmlComment(text: string): NodeData<'html_comment'> & { render(): string; replace(target: { type: 'html_comment'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'html_comment', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function null_(): NodeData<'null'> & { render(): string; replace(target: { type: 'null'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'null', fields: {}, text: 'null' };
  node.render = () => 'null';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'null' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'null' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' }; };
  return node;
}

export function number(text: string): NodeData<'number'> & { render(): string; replace(target: { type: 'number'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:(?:0|0?[1-9]_?\d(_?\d)*?)\.\d(_?\d)*?(?:e|E)(?:-|\+)?\d(_?\d)*?|\.\d(_?\d)*(?:e|E)(?:-|\+)?\d(_?\d)*?|(?:0|0?[1-9]_?\d(_?\d)*?)(?:e|E)(?:-|\+)?\d(_?\d)*|\d(_?\d)*)|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|(?:(?:0x|0X)[\da-fA-F](_?[\da-fA-F])*|(?:0b|0B)[0-1](_?[0-1])*|(?:0o|0O)[0-7](_?[0-7])*|\d(_?\d)*)n)$/.test(text)) throw new Error(`Invalid number: '${text}' does not match grammar pattern`);
  const node: any = { type: 'number', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function privatePropertyIdentifier(text: string): NodeData<'private_property_identifier'> & { render(): string; replace(target: { type: 'private_property_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'private_property_identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function propertyIdentifier(text: string): NodeData<'property_identifier'> & { render(): string; replace(target: { type: 'property_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'property_identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function regexFlags(text: string): NodeData<'regex_flags'> & { render(): string; replace(target: { type: 'regex_flags'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^[a-z]+$/.test(text)) throw new Error(`Invalid regex_flags: '${text}' does not match grammar pattern`);
  const node: any = { type: 'regex_flags', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function regexPattern(text: string): NodeData<'regex_pattern'> & { render(): string; replace(target: { type: 'regex_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^(?:(?:\[(?:(?:\\.|[^\]\n\\]))*\]|\\.|[^/\\\[\n]))+$/.test(text)) throw new Error(`Invalid regex_pattern: '${text}' does not match grammar pattern`);
  const node: any = { type: 'regex_pattern', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function shorthandPropertyIdentifier(text: string): NodeData<'shorthand_property_identifier'> & { render(): string; replace(target: { type: 'shorthand_property_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'shorthand_property_identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function shorthandPropertyIdentifierPattern(text: string): NodeData<'shorthand_property_identifier_pattern'> & { render(): string; replace(target: { type: 'shorthand_property_identifier_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'shorthand_property_identifier_pattern', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function statementIdentifier(text: string): NodeData<'statement_identifier'> & { render(): string; replace(target: { type: 'statement_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'statement_identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function stringFragment(text: string): NodeData<'string_fragment'> & { render(): string; replace(target: { type: 'string_fragment'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'string_fragment', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function super_(): NodeData<'super'> & { render(): string; replace(target: { type: 'super'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'super', fields: {}, text: 'super' };
  node.render = () => 'super';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'super' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'super' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; };
  return node;
}

export function this_(): NodeData<'this'> & { render(): string; replace(target: { type: 'this'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'this', fields: {}, text: 'this' };
  node.render = () => 'this';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'this' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'this' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' }; };
  return node;
}

export function thisType(text: string): NodeData<'this_type'> & { render(): string; replace(target: { type: 'this_type'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'this_type', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function true_(): NodeData<'true'> & { render(): string; replace(target: { type: 'true'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'true', fields: {}, text: 'true' };
  node.render = () => 'true';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'true' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'true' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' }; };
  return node;
}

export function typeIdentifier(text: string): NodeData<'type_identifier'> & { render(): string; replace(target: { type: 'type_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid type_identifier`);
  const node: any = { type: 'type_identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function undefined_(): NodeData<'undefined'> & { render(): string; replace(target: { type: 'undefined'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'undefined', fields: {}, text: 'undefined' };
  node.render = () => 'undefined';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'undefined' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'undefined' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' }; };
  return node;
}

/**
 * Create an in-place editor for a parsed tree node.
 * Hydrates a factory from the target node's fields,
 * then allows fluent modification. .toEdit() uses the target's range.
 *
 * Usage: edit(matchedNode).returnType(ir.primitiveType("i64")).toEdit()
 */
export function edit<K extends string>(target: AssignableNode<K>): any {
  const factories: Record<string, { assign: (t: any) => any }> = {
    'abstract_class_declaration': abstractClassDeclaration,
    'abstract_method_signature': abstractMethodSignature,
    'adding_type_annotation': addingTypeAnnotation,
    'ambient_declaration': ambientDeclaration,
    'arguments': arguments_,
    'array': array,
    'array_pattern': arrayPattern,
    'array_type': arrayType,
    'arrow_function': arrowFunction,
    'as_expression': asExpression,
    'asserts': asserts,
    'asserts_annotation': assertsAnnotation,
    'assignment_expression': assignmentExpression,
    'assignment_pattern': assignmentPattern,
    'augmented_assignment_expression': augmentedAssignmentExpression,
    'await_expression': awaitExpression,
    'binary_expression': binaryExpression,
    'break_statement': breakStatement,
    'call_expression': callExpression,
    'call_signature': callSignature,
    'catch_clause': catchClause,
    'class': class_,
    'class_body': classBody,
    'class_declaration': classDeclaration,
    'class_heritage': classHeritage,
    'class_static_block': classStaticBlock,
    'computed_property_name': computedPropertyName,
    'conditional_type': conditionalType,
    'constraint': constraint,
    'construct_signature': constructSignature,
    'constructor_type': constructorType,
    'continue_statement': continueStatement,
    'decorator': decorator,
    'default_type': defaultType,
    'do_statement': doStatement,
    'else_clause': elseClause,
    'enum_assignment': enumAssignment,
    'enum_body': enumBody,
    'enum_declaration': enumDeclaration,
    'export_clause': exportClause,
    'export_specifier': exportSpecifier,
    'export_statement': exportStatement,
    'expression_statement': expressionStatement,
    'extends_clause': extendsClause,
    'extends_type_clause': extendsTypeClause,
    'finally_clause': finallyClause,
    'flow_maybe_type': flowMaybeType,
    'for_in_statement': forInStatement,
    'for_statement': forStatement,
    'formal_parameters': formalParameters,
    'function_declaration': functionDeclaration,
    'function_expression': functionExpression,
    'function_signature': functionSignature,
    'function_type': functionType,
    'generator_function': generatorFunction,
    'generator_function_declaration': generatorFunctionDeclaration,
    'generic_type': genericType,
    'if_statement': ifStatement,
    'implements_clause': implementsClause,
    'import_alias': importAlias,
    'import_attribute': importAttribute,
    'import_clause': importClause,
    'import_require_clause': importRequireClause,
    'import_specifier': importSpecifier,
    'import_statement': importStatement,
    'index_signature': indexSignature,
    'index_type_query': indexTypeQuery,
    'infer_type': inferType,
    'instantiation_expression': instantiationExpression,
    'interface_body': interfaceBody,
    'interface_declaration': interfaceDeclaration,
    'internal_module': internalModule,
    'intersection_type': intersectionType,
    'labeled_statement': labeledStatement,
    'lexical_declaration': lexicalDeclaration,
    'literal_type': literalType,
    'lookup_type': lookupType,
    'mapped_type_clause': mappedTypeClause,
    'member_expression': memberExpression,
    'method_definition': methodDefinition,
    'method_signature': methodSignature,
    'module': module,
    'named_imports': namedImports,
    'namespace_export': namespaceExport,
    'namespace_import': namespaceImport,
    'nested_identifier': nestedIdentifier,
    'nested_type_identifier': nestedTypeIdentifier,
    'new_expression': newExpression,
    'non_null_expression': nonNullExpression,
    'object': object,
    'object_assignment_pattern': objectAssignmentPattern,
    'object_pattern': objectPattern,
    'object_type': objectType,
    'omitting_type_annotation': omittingTypeAnnotation,
    'opting_type_annotation': optingTypeAnnotation,
    'optional_parameter': optionalParameter,
    'optional_type': optionalType,
    'pair': pair,
    'pair_pattern': pairPattern,
    'parenthesized_expression': parenthesizedExpression,
    'parenthesized_type': parenthesizedType,
    'program': program,
    'property_signature': propertySignature,
    'public_field_definition': publicFieldDefinition,
    'readonly_type': readonlyType,
    'regex': regex,
    'required_parameter': requiredParameter,
    'rest_pattern': restPattern,
    'rest_type': restType,
    'return_statement': returnStatement,
    'satisfies_expression': satisfiesExpression,
    'sequence_expression': sequenceExpression,
    'spread_element': spreadElement,
    'statement_block': statementBlock,
    'string': string,
    'subscript_expression': subscriptExpression,
    'switch_body': switchBody,
    'switch_case': switchCase,
    'switch_default': switchDefault,
    'switch_statement': switchStatement,
    'template_literal_type': templateLiteralType,
    'template_string': templateString,
    'template_substitution': templateSubstitution,
    'template_type': templateType,
    'ternary_expression': ternaryExpression,
    'throw_statement': throwStatement,
    'try_statement': tryStatement,
    'tuple_type': tupleType,
    'type_alias_declaration': typeAliasDeclaration,
    'type_annotation': typeAnnotation,
    'type_arguments': typeArguments,
    'type_assertion': typeAssertion,
    'type_parameter': typeParameter,
    'type_parameters': typeParameters,
    'type_predicate': typePredicate,
    'type_predicate_annotation': typePredicateAnnotation,
    'type_query': typeQuery,
    'unary_expression': unaryExpression,
    'union_type': unionType,
    'update_expression': updateExpression,
    'variable_declaration': variableDeclaration,
    'variable_declarator': variableDeclarator,
    'while_statement': whileStatement,
    'with_statement': withStatement,
    'yield_expression': yieldExpression,
  };
  const factory = factories[target.type];
  if (!factory) throw new Error(`No factory for kind '${target.type}'`);
  return factory.assign(target);
}

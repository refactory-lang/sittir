// Auto-generated by @sittir/codegen — do not edit

import type { AbstractClassDeclaration, AbstractClassDeclarationTree, AbstractMethodSignature, AbstractMethodSignatureTree, AddingTypeAnnotation, AddingTypeAnnotationTree, AmbientDeclaration, AmbientDeclarationTree, Arguments, ArgumentsTree, Array, ArrayPattern, ArrayPatternTree, ArrayTree, ArrayType, ArrayTypeTree, ArrowFunction, ArrowFunctionTree, AsExpression, AsExpressionTree, Asserts, AssertsAnnotation, AssertsAnnotationTree, AssertsTree, AssignmentExpression, AssignmentExpressionTree, AssignmentPattern, AssignmentPatternTree, AugmentedAssignmentExpression, AugmentedAssignmentExpressionTree, AwaitExpression, AwaitExpressionTree, BinaryExpression, BinaryExpressionTree, BreakStatement, BreakStatementTree, CallExpression, CallExpressionFunction, CallExpressionFunction2, CallExpressionFunction2Config, CallExpressionFunction2Tree, CallExpressionFunctionConfig, CallExpressionFunctionTree, CallExpressionTokQDot, CallExpressionTokQDotConfig, CallExpressionTokQDotTree, CallExpressionTree, CallSignature, CallSignatureTree, CatchClause, CatchClauseTree, Class, ClassBody, ClassBodyTree, ClassDeclaration, ClassDeclarationTree, ClassHeritage, ClassHeritageExtendsClause, ClassHeritageExtendsClauseConfig, ClassHeritageExtendsClauseTree, ClassHeritageImplementsClause, ClassHeritageImplementsClauseConfig, ClassHeritageImplementsClauseTree, ClassHeritageTree, ClassStaticBlock, ClassStaticBlockTree, ClassTree, Comment, CommentSlashslash, CommentSlashslashConfig, CommentSlashslashTree, CommentTokSlashStar, CommentTokSlashStarConfig, CommentTokSlashStarTree, CommentTree, ComputedPropertyName, ComputedPropertyNameTree, ConditionalType, ConditionalTypeTree, Constraint, ConstraintTree, ConstructSignature, ConstructSignatureTree, ConstructorType, ConstructorTypeTree, ContinueStatement, ContinueStatementTree, DebuggerStatement, DebuggerStatementTree, Declaration, DeclarationAbstractClassDeclaration, DeclarationAbstractClassDeclarationConfig, DeclarationAbstractClassDeclarationTree, DeclarationAmbientDeclaration, DeclarationAmbientDeclarationConfig, DeclarationAmbientDeclarationTree, DeclarationEnumDeclaration, DeclarationEnumDeclarationConfig, DeclarationEnumDeclarationTree, DeclarationForm0, DeclarationForm0Config, DeclarationForm0Tree, DeclarationFunctionSignature, DeclarationFunctionSignatureConfig, DeclarationFunctionSignatureTree, DeclarationImportAlias, DeclarationImportAliasConfig, DeclarationImportAliasTree, DeclarationInterfaceDeclaration, DeclarationInterfaceDeclarationConfig, DeclarationInterfaceDeclarationTree, DeclarationInternalModule, DeclarationInternalModuleConfig, DeclarationInternalModuleTree, DeclarationModule, DeclarationModuleConfig, DeclarationModuleTree, DeclarationTree, DeclarationTypeAliasDeclaration, DeclarationTypeAliasDeclarationConfig, DeclarationTypeAliasDeclarationTree, Decorator, DecoratorCallExpression, DecoratorCallExpressionTree, DecoratorMemberExpression, DecoratorMemberExpressionTree, DecoratorParenthesizedExpression, DecoratorParenthesizedExpressionTree, DecoratorTree, DefaultType, DefaultTypeTree, DoStatement, DoStatementTree, ElseClause, ElseClauseTree, EnumAssignment, EnumAssignmentTree, EnumBody, EnumBodyTree, EnumDeclaration, EnumDeclarationTree, EscapeSequence, EscapeSequenceTree, ExportClause, ExportClauseTree, ExportSpecifier, ExportSpecifierTree, ExportStatement, ExportStatementDeclaration, ExportStatementDeclaration2, ExportStatementDeclaration2Config, ExportStatementDeclaration2Tree, ExportStatementDeclarationConfig, ExportStatementDeclarationTree, ExportStatementForm0, ExportStatementForm0Config, ExportStatementForm0Tree, ExportStatementNamespace, ExportStatementNamespaceConfig, ExportStatementNamespaceTree, ExportStatementTree, Expression, ExpressionAsExpression, ExpressionAsExpressionConfig, ExpressionAsExpressionTree, ExpressionAssignmentExpression, ExpressionAssignmentExpressionConfig, ExpressionAssignmentExpressionTree, ExpressionAugmentedAssignmentExpression, ExpressionAugmentedAssignmentExpressionConfig, ExpressionAugmentedAssignmentExpressionTree, ExpressionAwaitExpression, ExpressionAwaitExpressionConfig, ExpressionAwaitExpressionTree, ExpressionBinaryExpression, ExpressionBinaryExpressionConfig, ExpressionBinaryExpressionTree, ExpressionInstantiationExpression, ExpressionInstantiationExpressionConfig, ExpressionInstantiationExpressionTree, ExpressionInternalModule, ExpressionInternalModuleConfig, ExpressionInternalModuleTree, ExpressionNewExpression, ExpressionNewExpressionConfig, ExpressionNewExpressionTree, ExpressionPrimaryExpression, ExpressionPrimaryExpressionConfig, ExpressionPrimaryExpressionTree, ExpressionSatisfiesExpression, ExpressionSatisfiesExpressionConfig, ExpressionSatisfiesExpressionTree, ExpressionStatement, ExpressionStatementTree, ExpressionTernaryExpression, ExpressionTernaryExpressionConfig, ExpressionTernaryExpressionTree, ExpressionTree, ExpressionTypeAssertion, ExpressionTypeAssertionConfig, ExpressionTypeAssertionTree, ExpressionUnaryExpression, ExpressionUnaryExpressionConfig, ExpressionUnaryExpressionTree, ExpressionUpdateExpression, ExpressionUpdateExpressionConfig, ExpressionUpdateExpressionTree, ExpressionYieldExpression, ExpressionYieldExpressionConfig, ExpressionYieldExpressionTree, ExtendsClause, ExtendsClauseTree, ExtendsTypeClause, ExtendsTypeClauseTree, False, FalseTree, FieldDefinition, FieldDefinitionTree, FinallyClause, FinallyClauseTree, FlowMaybeType, FlowMaybeTypeTree, ForInStatement, ForInStatementTree, ForStatement, ForStatementTree, FormalParameters, FormalParametersTree, FunctionDeclaration, FunctionDeclarationTree, FunctionExpression, FunctionExpressionTree, FunctionSignature, FunctionSignatureTree, FunctionType, FunctionTypeTree, GeneratorFunction, GeneratorFunctionDeclaration, GeneratorFunctionDeclarationTree, GeneratorFunctionTree, GenericType, GenericTypeTree, HashBangLine, HashBangLineTree, HiddenJsxStartOpeningElement, HiddenJsxStartOpeningElementTree, HiddenTypeIdentifier, HiddenTypeIdentifierTree, HtmlCharacterReference, HtmlCharacterReferenceTree, IfStatement, IfStatementTree, ImplementsClause, ImplementsClauseTree, Import, ImportAlias, ImportAliasTree, ImportAttribute, ImportAttributeTree, ImportClause, ImportClauseDefaultImport, ImportClauseDefaultImportConfig, ImportClauseDefaultImportTree, ImportClauseNamedImports, ImportClauseNamedImportsConfig, ImportClauseNamedImportsTree, ImportClauseNamespaceImport, ImportClauseNamespaceImportConfig, ImportClauseNamespaceImportTree, ImportClauseTree, ImportRequireClause, ImportRequireClauseTree, ImportSpecifier, ImportSpecifierTree, ImportStatement, ImportStatementTree, ImportTree, IndexSignature, IndexSignatureTree, IndexTypeQuery, IndexTypeQueryTree, InferType, InferTypeTree, InstantiationExpression, InstantiationExpressionTree, InterfaceBody, InterfaceBodyTree, InterfaceDeclaration, InterfaceDeclarationTree, InternalModule, InternalModuleTree, IntersectionType, IntersectionTypeTree, JsxAttribute, JsxAttributeTree, JsxClosingElement, JsxClosingElementTree, JsxElement, JsxElementTree, JsxExpression, JsxExpressionTree, JsxIdentifier, JsxIdentifierTree, JsxNamespaceName, JsxNamespaceNameTree, JsxOpeningElement, JsxOpeningElementTree, JsxSelfClosingElement, JsxSelfClosingElementTree, LabeledStatement, LabeledStatementTree, LexicalDeclaration, LexicalDeclarationTree, LiteralType, LiteralTypeFalse, LiteralTypeFalseConfig, LiteralTypeFalseTree, LiteralTypeNull, LiteralTypeNullConfig, LiteralTypeNullTree, LiteralTypeNumber, LiteralTypeNumberConfig, LiteralTypeNumberTree, LiteralTypeString, LiteralTypeStringConfig, LiteralTypeStringTree, LiteralTypeTree, LiteralTypeTrue, LiteralTypeTrueConfig, LiteralTypeTrueTree, LiteralTypeUNumber, LiteralTypeUNumberConfig, LiteralTypeUNumberTree, LiteralTypeUndefined, LiteralTypeUndefinedConfig, LiteralTypeUndefinedTree, LookupType, LookupTypeTree, MappedTypeClause, MappedTypeClauseTree, MemberExpression, MemberExpressionTree, MetaProperty, MetaPropertyImport, MetaPropertyImportConfig, MetaPropertyImportTree, MetaPropertyNew, MetaPropertyNewConfig, MetaPropertyNewTree, MetaPropertyTree, MethodDefinition, MethodDefinitionTree, MethodSignature, MethodSignatureTree, Module, ModuleTree, NamedImports, NamedImportsTree, NamespaceExport, NamespaceExportTree, NamespaceImport, NamespaceImportTree, NestedIdentifier, NestedIdentifierTree, NestedTypeIdentifier, NestedTypeIdentifierTree, NewExpression, NewExpressionTree, NonNullExpression, NonNullExpressionTree, Null, NullTree, Number, NumberForm0, NumberForm0Config, NumberForm0Tree, NumberForm1, NumberForm1Config, NumberForm1Tree, NumberForm2, NumberForm2Config, NumberForm2Tree, NumberForm3, NumberForm3Config, NumberForm3Tree, NumberN, NumberNConfig, NumberNTree, NumberTree, Object, ObjectAssignmentPattern, ObjectAssignmentPatternTree, ObjectPattern, ObjectPatternTree, ObjectTree, ObjectType, ObjectTypeTree, OmittingTypeAnnotation, OmittingTypeAnnotationTree, OptingTypeAnnotation, OptingTypeAnnotationTree, OptionalParameter, OptionalParameterTree, OptionalTupleParameter, OptionalTupleParameterTree, OptionalType, OptionalTypeTree, OverrideModifier, OverrideModifierTree, Pair, PairPattern, PairPatternTree, PairTree, ParenthesizedExpression, ParenthesizedExpressionTree, ParenthesizedType, ParenthesizedTypeTree, Pattern, PatternRestPattern, PatternRestPatternConfig, PatternRestPatternTree, PatternTree, PatternULhsExpression, PatternULhsExpressionConfig, PatternULhsExpressionTree, PredefinedType, PredefinedTypeAny, PredefinedTypeAnyConfig, PredefinedTypeAnyTree, PredefinedTypeBoolean, PredefinedTypeBooleanConfig, PredefinedTypeBooleanTree, PredefinedTypeNever, PredefinedTypeNeverConfig, PredefinedTypeNeverTree, PredefinedTypeNumber, PredefinedTypeNumberConfig, PredefinedTypeNumberTree, PredefinedTypeObject, PredefinedTypeObjectConfig, PredefinedTypeObjectTree, PredefinedTypeString, PredefinedTypeStringConfig, PredefinedTypeStringTree, PredefinedTypeSymbol, PredefinedTypeSymbolConfig, PredefinedTypeSymbolTree, PredefinedTypeTree, PredefinedTypeUnique, PredefinedTypeUniqueConfig, PredefinedTypeUniqueTree, PredefinedTypeUnknown, PredefinedTypeUnknownConfig, PredefinedTypeUnknownTree, PredefinedTypeVoid, PredefinedTypeVoidConfig, PredefinedTypeVoidTree, PrimaryExpression, PrimaryExpressionForm0, PrimaryExpressionForm0Config, PrimaryExpressionForm0Tree, PrimaryExpressionNonNullExpression, PrimaryExpressionNonNullExpressionConfig, PrimaryExpressionNonNullExpressionTree, PrimaryExpressionTree, PrimaryType, PrimaryTypeArrayType, PrimaryTypeArrayTypeConfig, PrimaryTypeArrayTypeTree, PrimaryTypeConditionalType, PrimaryTypeConditionalTypeConfig, PrimaryTypeConditionalTypeTree, PrimaryTypeConst, PrimaryTypeConstConfig, PrimaryTypeConstTree, PrimaryTypeExistentialType, PrimaryTypeExistentialTypeConfig, PrimaryTypeExistentialTypeTree, PrimaryTypeFlowMaybeType, PrimaryTypeFlowMaybeTypeConfig, PrimaryTypeFlowMaybeTypeTree, PrimaryTypeGenericType, PrimaryTypeGenericTypeConfig, PrimaryTypeGenericTypeTree, PrimaryTypeIndexTypeQuery, PrimaryTypeIndexTypeQueryConfig, PrimaryTypeIndexTypeQueryTree, PrimaryTypeIntersectionType, PrimaryTypeIntersectionTypeConfig, PrimaryTypeIntersectionTypeTree, PrimaryTypeLiteralType, PrimaryTypeLiteralTypeConfig, PrimaryTypeLiteralTypeTree, PrimaryTypeLookupType, PrimaryTypeLookupTypeConfig, PrimaryTypeLookupTypeTree, PrimaryTypeNestedTypeIdentifier, PrimaryTypeNestedTypeIdentifierConfig, PrimaryTypeNestedTypeIdentifierTree, PrimaryTypeObjectType, PrimaryTypeObjectTypeConfig, PrimaryTypeObjectTypeTree, PrimaryTypeParenthesizedType, PrimaryTypeParenthesizedTypeConfig, PrimaryTypeParenthesizedTypeTree, PrimaryTypePredefinedType, PrimaryTypePredefinedTypeConfig, PrimaryTypePredefinedTypeTree, PrimaryTypeTemplateLiteralType, PrimaryTypeTemplateLiteralTypeConfig, PrimaryTypeTemplateLiteralTypeTree, PrimaryTypeThis, PrimaryTypeThisConfig, PrimaryTypeThisTree, PrimaryTypeTree, PrimaryTypeTupleType, PrimaryTypeTupleTypeConfig, PrimaryTypeTupleTypeTree, PrimaryTypeTypeQuery, PrimaryTypeTypeQueryConfig, PrimaryTypeTypeQueryTree, PrimaryTypeUTypeIdentifier, PrimaryTypeUTypeIdentifierConfig, PrimaryTypeUTypeIdentifierTree, PrimaryTypeUnionType, PrimaryTypeUnionTypeConfig, PrimaryTypeUnionTypeTree, Program, ProgramTree, PropertySignature, PropertySignatureTree, PublicFieldDefinition, PublicFieldDefinitionTree, ReadonlyType, ReadonlyTypeTree, Regex, RegexFlags, RegexFlagsTree, RegexPattern, RegexPatternTree, RegexTree, RequiredParameter, RequiredParameterTree, RestPattern, RestPatternTree, RestType, RestTypeTree, ReturnStatement, ReturnStatementTree, SatisfiesExpression, SatisfiesExpressionTree, SequenceExpression, SequenceExpressionTree, ShorthandPropertyIdentifier, ShorthandPropertyIdentifierIdentifier, ShorthandPropertyIdentifierIdentifierConfig, ShorthandPropertyIdentifierIdentifierTree, ShorthandPropertyIdentifierPattern, ShorthandPropertyIdentifierPatternIdentifier, ShorthandPropertyIdentifierPatternIdentifierConfig, ShorthandPropertyIdentifierPatternIdentifierTree, ShorthandPropertyIdentifierPatternTree, ShorthandPropertyIdentifierPatternUReservedIdentifier, ShorthandPropertyIdentifierPatternUReservedIdentifierConfig, ShorthandPropertyIdentifierPatternUReservedIdentifierTree, ShorthandPropertyIdentifierTree, ShorthandPropertyIdentifierUReservedIdentifier, ShorthandPropertyIdentifierUReservedIdentifierConfig, ShorthandPropertyIdentifierUReservedIdentifierTree, SpreadElement, SpreadElementTree, Statement, StatementBlock, StatementBlockTree, StatementBreakStatement, StatementBreakStatementConfig, StatementBreakStatementTree, StatementContinueStatement, StatementContinueStatementConfig, StatementContinueStatementTree, StatementDebuggerStatement, StatementDebuggerStatementConfig, StatementDebuggerStatementTree, StatementDeclaration, StatementDeclarationConfig, StatementDeclarationTree, StatementDoStatement, StatementDoStatementConfig, StatementDoStatementTree, StatementEmptyStatement, StatementEmptyStatementConfig, StatementEmptyStatementTree, StatementExportStatement, StatementExportStatementConfig, StatementExportStatementTree, StatementExpressionStatement, StatementExpressionStatementConfig, StatementExpressionStatementTree, StatementForInStatement, StatementForInStatementConfig, StatementForInStatementTree, StatementForStatement, StatementForStatementConfig, StatementForStatementTree, StatementIfStatement, StatementIfStatementConfig, StatementIfStatementTree, StatementImportStatement, StatementImportStatementConfig, StatementImportStatementTree, StatementLabeledStatement, StatementLabeledStatementConfig, StatementLabeledStatementTree, StatementReturnStatement, StatementReturnStatementConfig, StatementReturnStatementTree, StatementStatementBlock, StatementStatementBlockConfig, StatementStatementBlockTree, StatementSwitchStatement, StatementSwitchStatementConfig, StatementSwitchStatementTree, StatementThrowStatement, StatementThrowStatementConfig, StatementThrowStatementTree, StatementTree, StatementTryStatement, StatementTryStatementConfig, StatementTryStatementTree, StatementWhileStatement, StatementWhileStatementConfig, StatementWhileStatementTree, StatementWithStatement, StatementWithStatementConfig, StatementWithStatementTree, String, StringTokDq, StringTokDqConfig, StringTokDqTree, StringTokSq, StringTokSqConfig, StringTokSqTree, StringTree, SubscriptExpression, SubscriptExpressionTree, Super, SuperTree, SwitchBody, SwitchBodyTree, SwitchCase, SwitchCaseTree, SwitchDefault, SwitchDefaultTree, SwitchStatement, SwitchStatementTree, TemplateLiteralType, TemplateLiteralTypeTree, TemplateString, TemplateStringTree, TemplateSubstitution, TemplateSubstitutionTree, TemplateType, TemplateTypeTree, TernaryExpression, TernaryExpressionTree, This, ThisTree, ThisType, ThisTypeTree, ThrowStatement, ThrowStatementTree, True, TrueTree, TryStatement, TryStatementTree, TupleParameter, TupleParameterTree, TupleType, TupleTypeTree, Type, TypeAliasDeclaration, TypeAliasDeclarationTree, TypeAnnotation, TypeAnnotationTree, TypeArguments, TypeArgumentsTree, TypeAssertion, TypeAssertionTree, TypeConstructorType, TypeConstructorTypeConfig, TypeConstructorTypeTree, TypeFunctionType, TypeFunctionTypeConfig, TypeFunctionTypeTree, TypeInferType, TypeInferTypeConfig, TypeInferTypeTree, TypeParameter, TypeParameterTree, TypeParameters, TypeParametersTree, TypePredicate, TypePredicateAnnotation, TypePredicateAnnotationTree, TypePredicateTree, TypePrimaryType, TypePrimaryTypeConfig, TypePrimaryTypeTree, TypeQuery, TypeQueryTree, TypeReadonlyType, TypeReadonlyTypeConfig, TypeReadonlyTypeTree, TypeTree, TypeUTypeQueryCallExpressionInTypeAnnotation, TypeUTypeQueryCallExpressionInTypeAnnotationConfig, TypeUTypeQueryCallExpressionInTypeAnnotationTree, TypeUTypeQueryMemberExpressionInTypeAnnotation, TypeUTypeQueryMemberExpressionInTypeAnnotationConfig, TypeUTypeQueryMemberExpressionInTypeAnnotationTree, UnaryExpression, UnaryExpressionTree, Undefined, UndefinedTree, UnescapedDoubleJsxStringFragment, UnescapedDoubleJsxStringFragmentTree, UnescapedDoubleStringFragment, UnescapedDoubleStringFragmentTree, UnescapedSingleJsxStringFragment, UnescapedSingleJsxStringFragmentTree, UnescapedSingleStringFragment, UnescapedSingleStringFragmentTree, UnionType, UnionTypeTree, UpdateExpression, UpdateExpressionTree, VariableDeclaration, VariableDeclarationTree, VariableDeclarator, VariableDeclaratorTree, WhileStatement, WhileStatementTree, WithStatement, WithStatementTree, YieldExpression, YieldExpressionTree } from './types.js';
import type { Edit, AnyNodeData, ConfigOf } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  '0',
  '0B',
  '0O',
  '0X',
  '0b',
  '0o',
  '0x',
  'E',
  '_',
  'abstract',
  'accessor',
  'any',
  'as',
  'assert',
  'async',
  'await',
  'boolean',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'delete',
  'do',
  'e',
  'else',
  'enum',
  'export',
  'extends',
  'false',
  'finally',
  'for',
  'from',
  'function',
  'get',
  'global',
  'if',
  'implements',
  'import',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'let',
  'meta',
  'n',
  'namespace',
  'never',
  'new',
  'null',
  'of',
  'override',
  'override_modifier',
  'private',
  'protected',
  'public',
  'readonly',
  'require',
  'return',
  'satisfies',
  'set',
  'static',
  'super',
  'switch',
  'symbol',
  'target',
  'this',
  'this_type',
  'throw',
  'true',
  'try',
  'typeof',
  'undefined',
  'unique',
  'unknown',
  'using',
  'var',
  'void',
  'while',
  'with',
  'yield',
]);

export function program(config: ConfigOf<Program>) {
  const fields = {
    hash_bang_line: (config as any)?.hashBangLine,
    statements: (config as any)?.statements,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'program' as const,
    named: true as const,
    fields,
    children,
    hashBangLine(hashBangLine_?: any) { return hashBangLine_ !== undefined ? program({ ...(config as any), hashBangLine: hashBangLine_ } as any) : fields.hash_bang_line; },
    statements(statements_?: any) { return statements_ !== undefined ? program({ ...(config as any), statements: statements_ } as any) : fields.statements; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return program({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hashBangLine(text: string) {
  return {
    type: 'hash_bang_line' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HashBangLineTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}





export function exportStatement(config?: ConfigOf<ExportStatement>) {
  if (config && 'value' in config) return exportStatementForm0(config as any);
  return exportStatementNamespace(config as any);
}
export function exportStatementForm0(config: ConfigOf<ExportStatementForm0>) {
  const fields = {
    declaration: (config as any)?.declaration,
    source: (config as any)?.source,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    fields,
    children,
    declaration(declaration_?: any) { return declaration_ !== undefined ? exportStatementForm0({ ...(config as any), declaration: declaration_ } as any) : fields.declaration; },
    source(source_?: any) { return source_ !== undefined ? exportStatementForm0({ ...(config as any), source: source_ } as any) : fields.source; },
    value(value_?: any) { return value_ !== undefined ? exportStatementForm0({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportStatementForm0({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementDeclaration(config: ConfigOf<ExportStatementDeclaration>) {
  const fields = {
    declaration: (config as any)?.declaration,
    source: (config as any)?.source,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    fields,
    children,
    declaration(declaration_?: any) { return declaration_ !== undefined ? exportStatementDeclaration({ ...(config as any), declaration: declaration_ } as any) : fields.declaration; },
    source(source_?: any) { return source_ !== undefined ? exportStatementDeclaration({ ...(config as any), source: source_ } as any) : fields.source; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportStatementDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementDeclaration2(config: ConfigOf<ExportStatementDeclaration2>) {
  const fields = {
    declaration: (config as any)?.declaration,
    source: (config as any)?.source,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    fields,
    children,
    declaration(declaration_?: any) { return declaration_ !== undefined ? exportStatementDeclaration2({ ...(config as any), declaration: declaration_ } as any) : fields.declaration; },
    source(source_?: any) { return source_ !== undefined ? exportStatementDeclaration2({ ...(config as any), source: source_ } as any) : fields.source; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportStatementDeclaration2({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementNamespace(config: ConfigOf<ExportStatementNamespace>) {
  const fields = {
    declaration: (config as any)?.declaration,
    source: (config as any)?.source,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    fields,
    children,
    declaration(declaration_?: any) { return declaration_ !== undefined ? exportStatementNamespace({ ...(config as any), declaration: declaration_ } as any) : fields.declaration; },
    source(source_?: any) { return source_ !== undefined ? exportStatementNamespace({ ...(config as any), source: source_ } as any) : fields.source; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportStatementNamespace({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceExport(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'namespace_export' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportClause(...children: any[]) {
  return {
    type: 'export_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportSpecifier(config: ConfigOf<ExportSpecifier>) {
  const fields = {
    name: (config as any)?.name,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'export_specifier' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? exportSpecifier({ ...(config as any), name: name_ } as any) : fields.name; },
    alias(alias_?: any) { return alias_ !== undefined ? exportSpecifier({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return exportSpecifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}











export function declaration(config?: ConfigOf<Declaration>) {
  return declarationAmbientDeclaration(config as any);
}
export function declarationForm0(config?: ConfigOf<DeclarationForm0>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationForm0({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationFunctionSignature(config?: ConfigOf<DeclarationFunctionSignature>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationFunctionSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationAbstractClassDeclaration(config?: ConfigOf<DeclarationAbstractClassDeclaration>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationAbstractClassDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationModule(config?: ConfigOf<DeclarationModule>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationModule({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationInternalModule(config?: ConfigOf<DeclarationInternalModule>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationInternalModule({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationTypeAliasDeclaration(config?: ConfigOf<DeclarationTypeAliasDeclaration>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationTypeAliasDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationEnumDeclaration(config?: ConfigOf<DeclarationEnumDeclaration>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationEnumDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationInterfaceDeclaration(config?: ConfigOf<DeclarationInterfaceDeclaration>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationInterfaceDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationImportAlias(config?: ConfigOf<DeclarationImportAlias>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationImportAlias({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function declarationAmbientDeclaration(config?: ConfigOf<DeclarationAmbientDeclaration>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return declarationAmbientDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_() {
  return {
    type: 'import' as const,
    named: true as const,
    text: 'import',
    render: () => 'import',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'import' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'import' },
    replace: (t: ImportTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' }; },
  };
}

export function importStatement(config: ConfigOf<ImportStatement>) {
  const fields = {
    import_clause: (config as any)?.importClause,
    from_clause: (config as any)?.fromClause,
    import_attribute: (config as any)?.importAttribute,
    semicolon: (config as any)?.semicolon,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_statement' as const,
    named: true as const,
    fields,
    children,
    importClause(importClause_?: any) { return importClause_ !== undefined ? importStatement({ ...(config as any), importClause: importClause_ } as any) : fields.import_clause; },
    fromClause(fromClause_?: any) { return fromClause_ !== undefined ? importStatement({ ...(config as any), fromClause: fromClause_ } as any) : fields.from_clause; },
    importAttribute(importAttribute_?: any) { return importAttribute_ !== undefined ? importStatement({ ...(config as any), importAttribute: importAttribute_ } as any) : fields.import_attribute; },
    semicolon(semicolon_?: any) { return semicolon_ !== undefined ? importStatement({ ...(config as any), semicolon: semicolon_ } as any) : fields.semicolon; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return importStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}




export function importClause(config?: ConfigOf<ImportClause>) {
  if (config && 'defaultImport' in config) return importClauseDefaultImport(config as any);
  return importClauseNamedImports(config as any);
}
export function importClauseNamespaceImport(config?: ConfigOf<ImportClauseNamespaceImport>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_clause' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return importClauseNamespaceImport({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseNamedImports(config?: ConfigOf<ImportClauseNamedImports>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_clause' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return importClauseNamedImports({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseDefaultImport(config: ConfigOf<ImportClauseDefaultImport>) {
  const fields = {
    default_import: (config as any)?.defaultImport,
    named_imports: (config as any)?.namedImports,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_clause' as const,
    named: true as const,
    fields,
    children,
    defaultImport(defaultImport_?: any) { return defaultImport_ !== undefined ? importClauseDefaultImport({ ...(config as any), defaultImport: defaultImport_ } as any) : fields.default_import; },
    namedImports(namedImports_?: any) { return namedImports_ !== undefined ? importClauseDefaultImport({ ...(config as any), namedImports: namedImports_ } as any) : fields.named_imports; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return importClauseDefaultImport({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceImport(config: ConfigOf<NamespaceImport>) {
  const fields = {
    identifier: (config as any)?.identifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'namespace_import' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? namespaceImport({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return namespaceImport({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedImports(...children: any[]) {
  return {
    type: 'named_imports' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifier(config: ConfigOf<ImportSpecifier>) {
  const fields = {
    name: (config as any)?.name,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_specifier' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? importSpecifier({ ...(config as any), name: name_ } as any) : fields.name; },
    alias(alias_?: any) { return alias_ !== undefined ? importSpecifier({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return importSpecifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAttribute(config: ConfigOf<ImportAttribute>) {
  const fields = {
    object: (config as any)?.object,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_attribute' as const,
    named: true as const,
    fields,
    children,
    object(object_?: any) { return object_ !== undefined ? importAttribute({ ...(config as any), object: object_ } as any) : fields.object; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return importAttribute({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}





















export function statement(config?: ConfigOf<Statement>) {
  return statementLabeledStatement(config as any);
}
export function statementExportStatement(config?: ConfigOf<StatementExportStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementExportStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementImportStatement(config?: ConfigOf<StatementImportStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementImportStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDebuggerStatement(config?: ConfigOf<StatementDebuggerStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementDebuggerStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementExpressionStatement(config?: ConfigOf<StatementExpressionStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementExpressionStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDeclaration(config?: ConfigOf<StatementDeclaration>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementStatementBlock(config?: ConfigOf<StatementStatementBlock>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementStatementBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementIfStatement(config?: ConfigOf<StatementIfStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementIfStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementSwitchStatement(config?: ConfigOf<StatementSwitchStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementSwitchStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementForStatement(config?: ConfigOf<StatementForStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementForStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementForInStatement(config?: ConfigOf<StatementForInStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementForInStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementWhileStatement(config?: ConfigOf<StatementWhileStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementWhileStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementDoStatement(config?: ConfigOf<StatementDoStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementDoStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementTryStatement(config?: ConfigOf<StatementTryStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementTryStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementWithStatement(config?: ConfigOf<StatementWithStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementWithStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementBreakStatement(config?: ConfigOf<StatementBreakStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementBreakStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementContinueStatement(config?: ConfigOf<StatementContinueStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementContinueStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementReturnStatement(config?: ConfigOf<StatementReturnStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementReturnStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementThrowStatement(config?: ConfigOf<StatementThrowStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementThrowStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementEmptyStatement(config?: ConfigOf<StatementEmptyStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementEmptyStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function statementLabeledStatement(config?: ConfigOf<StatementLabeledStatement>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementLabeledStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclaration(config: ConfigOf<VariableDeclaration>) {
  const fields = {
    declarators: (config as any)?.declarators,
    semicolon: (config as any)?.semicolon,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'variable_declaration' as const,
    named: true as const,
    fields,
    children,
    declarators(declarators_?: any) { return declarators_ !== undefined ? variableDeclaration({ ...(config as any), declarators: declarators_ } as any) : fields.declarators; },
    semicolon(semicolon_?: any) { return semicolon_ !== undefined ? variableDeclaration({ ...(config as any), semicolon: semicolon_ } as any) : fields.semicolon; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return variableDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lexicalDeclaration(config: ConfigOf<LexicalDeclaration>) {
  const fields = {
    declarators: (config as any)?.declarators,
    semicolon: (config as any)?.semicolon,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'lexical_declaration' as const,
    named: true as const,
    fields,
    children,
    declarators(declarators_?: any) { return declarators_ !== undefined ? lexicalDeclaration({ ...(config as any), declarators: declarators_ } as any) : fields.declarators; },
    semicolon(semicolon_?: any) { return semicolon_ !== undefined ? lexicalDeclaration({ ...(config as any), semicolon: semicolon_ } as any) : fields.semicolon; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return lexicalDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclarator(config: ConfigOf<VariableDeclarator>) {
  const fields = {
    name: (config as any)?.name,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'variable_declarator' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? variableDeclarator({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? variableDeclarator({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return variableDeclarator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statementBlock(config: ConfigOf<StatementBlock>) {
  const fields = {
    statements: (config as any)?.statements,
    automatic_semicolon: (config as any)?.automaticSemicolon,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'statement_block' as const,
    named: true as const,
    fields,
    children,
    statements(statements_?: any) { return statements_ !== undefined ? statementBlock({ ...(config as any), statements: statements_ } as any) : fields.statements; },
    automaticSemicolon(automaticSemicolon_?: any) { return automaticSemicolon_ !== undefined ? statementBlock({ ...(config as any), automaticSemicolon: automaticSemicolon_ } as any) : fields.automatic_semicolon; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return statementBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: ConfigOf<ElseClause>) {
  const fields = {
    statement: (config as any)?.statement,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    children,
    statement(statement_?: any) { return statement_ !== undefined ? elseClause({ ...(config as any), statement: statement_ } as any) : fields.statement; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return elseClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifStatement(config: ConfigOf<IfStatement>) {
  const fields = {
    condition: (config as any)?.condition,
    consequence: (config as any)?.consequence,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: any) { return condition_ !== undefined ? ifStatement({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    consequence(consequence_?: any) { return consequence_ !== undefined ? ifStatement({ ...(config as any), consequence: consequence_ } as any) : fields.consequence; },
    alternative(alternative_?: any) { return alternative_ !== undefined ? ifStatement({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return ifStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchStatement(config: ConfigOf<SwitchStatement>) {
  const fields = {
    value: (config as any)?.value,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'switch_statement' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? switchStatement({ ...(config as any), value: value_ } as any) : fields.value; },
    body(body_?: any) { return body_ !== undefined ? switchStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return switchStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: ConfigOf<ForStatement>) {
  const fields = {
    initializer: (config as any)?.initializer,
    condition: (config as any)?.condition,
    increment: (config as any)?.increment,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    children,
    initializer(initializer_?: any) { return initializer_ !== undefined ? forStatement({ ...(config as any), initializer: initializer_ } as any) : fields.initializer; },
    condition(condition_?: any) { return condition_ !== undefined ? forStatement({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    increment(increment_?: any) { return increment_ !== undefined ? forStatement({ ...(config as any), increment: increment_ } as any) : fields.increment; },
    body(body_?: any) { return body_ !== undefined ? forStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return forStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInStatement(config: ConfigOf<ForInStatement>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? forInStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return forInStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: ConfigOf<WhileStatement>) {
  const fields = {
    condition: (config as any)?.condition,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: any) { return condition_ !== undefined ? whileStatement({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    body(body_?: any) { return body_ !== undefined ? whileStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return whileStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function doStatement(config: ConfigOf<DoStatement>) {
  const fields = {
    body: (config as any)?.body,
    condition: (config as any)?.condition,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'do_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? doStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    condition(condition_?: any) { return condition_ !== undefined ? doStatement({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return doStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: ConfigOf<TryStatement>) {
  const fields = {
    body: (config as any)?.body,
    handler: (config as any)?.handler,
    finalizer: (config as any)?.finalizer,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? tryStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    handler(handler_?: any) { return handler_ !== undefined ? tryStatement({ ...(config as any), handler: handler_ } as any) : fields.handler; },
    finalizer(finalizer_?: any) { return finalizer_ !== undefined ? tryStatement({ ...(config as any), finalizer: finalizer_ } as any) : fields.finalizer; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return tryStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: ConfigOf<WithStatement>) {
  const fields = {
    object: (config as any)?.object,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    children,
    object(object_?: any) { return object_ !== undefined ? withStatement({ ...(config as any), object: object_ } as any) : fields.object; },
    body(body_?: any) { return body_ !== undefined ? withStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return withStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakStatement(config: ConfigOf<BreakStatement>) {
  const fields = {
    label: (config as any)?.label,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'break_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? breakStatement({ ...(config as any), label: label_ } as any) : fields.label; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return breakStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueStatement(config: ConfigOf<ContinueStatement>) {
  const fields = {
    label: (config as any)?.label,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'continue_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? continueStatement({ ...(config as any), label: label_ } as any) : fields.label; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return continueStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function debuggerStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'debugger_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function throwStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'throw_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function labeledStatement(config: ConfigOf<LabeledStatement>) {
  const fields = {
    label: (config as any)?.label,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'labeled_statement' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? labeledStatement({ ...(config as any), label: label_ } as any) : fields.label; },
    body(body_?: any) { return body_ !== undefined ? labeledStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return labeledStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchBody(...children: any[]) {
  return {
    type: 'switch_body' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchCase(config: ConfigOf<SwitchCase>) {
  const fields = {
    value: (config as any)?.value,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'switch_case' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? switchCase({ ...(config as any), value: value_ } as any) : fields.value; },
    body(body_?: any) { return body_ !== undefined ? switchCase({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return switchCase({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchDefault(config: ConfigOf<SwitchDefault>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'switch_default' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? switchDefault({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return switchDefault({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function catchClause(config: ConfigOf<CatchClause>) {
  const fields = {
    parameter: (config as any)?.parameter,
    type: (config as any)?.type,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'catch_clause' as const,
    named: true as const,
    fields,
    children,
    parameter(parameter_?: any) { return parameter_ !== undefined ? catchClause({ ...(config as any), parameter: parameter_ } as any) : fields.parameter; },
    typeField(type?: any) { return type !== undefined ? catchClause({ ...(config as any), type: type } as any) : fields.type; },
    body(body_?: any) { return body_ !== undefined ? catchClause({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return catchClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: ConfigOf<FinallyClause>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? finallyClause({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return finallyClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(config?: any) { return { type: 'parenthesized_expression' as const, named: true as const, render() { return render(this); }, toEdit(s: any, e?: any) { return typeof s === 'number' ? toEdit(this, s, e!) : toEdit(this, s); }, replace(t: ParenthesizedExpressionTree) { const r = t.range(); return toEdit(this, r); } }; }
















export function expression(config?: ConfigOf<Expression>) {
  return expressionYieldExpression(config as any);
}
export function expressionAsExpression(config?: ConfigOf<ExpressionAsExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionAsExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionSatisfiesExpression(config?: ConfigOf<ExpressionSatisfiesExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionSatisfiesExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionInstantiationExpression(config?: ConfigOf<ExpressionInstantiationExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionInstantiationExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionInternalModule(config?: ConfigOf<ExpressionInternalModule>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionInternalModule({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionTypeAssertion(config?: ConfigOf<ExpressionTypeAssertion>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionTypeAssertion({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionPrimaryExpression(config?: ConfigOf<ExpressionPrimaryExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionPrimaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionAssignmentExpression(config?: ConfigOf<ExpressionAssignmentExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionAssignmentExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionAugmentedAssignmentExpression(config?: ConfigOf<ExpressionAugmentedAssignmentExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionAugmentedAssignmentExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionAwaitExpression(config?: ConfigOf<ExpressionAwaitExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionAwaitExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionUnaryExpression(config?: ConfigOf<ExpressionUnaryExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionUnaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionBinaryExpression(config?: ConfigOf<ExpressionBinaryExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionBinaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionTernaryExpression(config?: ConfigOf<ExpressionTernaryExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionTernaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionUpdateExpression(config?: ConfigOf<ExpressionUpdateExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionUpdateExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionNewExpression(config?: ConfigOf<ExpressionNewExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionNewExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionYieldExpression(config?: ConfigOf<ExpressionYieldExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionYieldExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function primaryExpression(config?: ConfigOf<PrimaryExpression>) {
  return primaryExpressionNonNullExpression(config as any);
}
export function primaryExpressionForm0(config?: ConfigOf<PrimaryExpressionForm0>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionForm0({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionNonNullExpression(config?: ConfigOf<PrimaryExpressionNonNullExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionNonNullExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(config: ConfigOf<YieldExpression>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'yield_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? yieldExpression({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return yieldExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function object(...children: any[]) {
  return {
    type: 'object' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectPattern(...children: any[]) {
  return {
    type: 'object_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentPattern(config: ConfigOf<AssignmentPattern>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'assignment_pattern' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? assignmentPattern({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? assignmentPattern({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return assignmentPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectAssignmentPattern(config: ConfigOf<ObjectAssignmentPattern>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'object_assignment_pattern' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? objectAssignmentPattern({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? objectAssignmentPattern({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return objectAssignmentPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function array(...children: any[]) {
  return {
    type: 'array' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayPattern(...children: any[]) {
  return {
    type: 'array_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxElement(config: ConfigOf<JsxElement>) {
  const fields = {
    open_tag: (config as any)?.openTag,
    close_tag: (config as any)?.closeTag,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'jsx_element' as const,
    named: true as const,
    fields,
    children,
    openTag(openTag_?: any) { return openTag_ !== undefined ? jsxElement({ ...(config as any), openTag: openTag_ } as any) : fields.open_tag; },
    closeTag(closeTag_?: any) { return closeTag_ !== undefined ? jsxElement({ ...(config as any), closeTag: closeTag_ } as any) : fields.close_tag; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return jsxElement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlCharacterReference(text: string) {
  return {
    type: 'html_character_reference' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HtmlCharacterReferenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxExpression(config?: any) { return { type: 'jsx_expression' as const, named: true as const, render() { return render(this); }, toEdit(s: any, e?: any) { return typeof s === 'number' ? toEdit(this, s, e!) : toEdit(this, s); }, replace(t: JsxExpressionTree) { const r = t.range(); return toEdit(this, r); } }; }

export function jsxOpeningElement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'jsx_opening_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxIdentifier(text: string) {
  return {
    type: 'jsx_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: JsxIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function nestedIdentifier(config: ConfigOf<NestedIdentifier>) {
  const fields = {
    object: (config as any)?.object,
    property: (config as any)?.property,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'nested_identifier' as const,
    named: true as const,
    fields,
    children,
    object(object_?: any) { return object_ !== undefined ? nestedIdentifier({ ...(config as any), object: object_ } as any) : fields.object; },
    property(property_?: any) { return property_ !== undefined ? nestedIdentifier({ ...(config as any), property: property_ } as any) : fields.property; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return nestedIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxNamespaceName(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'jsx_namespace_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxNamespaceNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxClosingElement(config?: ConfigOf<JsxClosingElement>) {
  const fields = {
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'jsx_closing_element' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? jsxClosingElement({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return jsxClosingElement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxSelfClosingElement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'jsx_self_closing_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxSelfClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxAttribute(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'jsx_attribute' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: JsxAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleJsxStringFragment(text: string) {
  return {
    type: 'unescaped_double_jsx_string_fragment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedDoubleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleJsxStringFragment(text: string) {
  return {
    type: 'unescaped_single_jsx_string_fragment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedSingleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function class_(config: ConfigOf<Class>) {
  const fields = {
    class_heritage: (config as any)?.classHeritage,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class' as const,
    named: true as const,
    fields,
    children,
    classHeritage(classHeritage_?: any) { return classHeritage_ !== undefined ? class_({ ...(config as any), classHeritage: classHeritage_ } as any) : fields.class_heritage; },
    name(name_?: any) { return name_ !== undefined ? class_({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? class_({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    body(body_?: any) { return body_ !== undefined ? class_({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return class_({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDeclaration(config: ConfigOf<ClassDeclaration>) {
  const fields = {
    class_heritage: (config as any)?.classHeritage,
    automatic_semicolon: (config as any)?.automaticSemicolon,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_declaration' as const,
    named: true as const,
    fields,
    children,
    classHeritage(classHeritage_?: any) { return classHeritage_ !== undefined ? classDeclaration({ ...(config as any), classHeritage: classHeritage_ } as any) : fields.class_heritage; },
    automaticSemicolon(automaticSemicolon_?: any) { return automaticSemicolon_ !== undefined ? classDeclaration({ ...(config as any), automaticSemicolon: automaticSemicolon_ } as any) : fields.automatic_semicolon; },
    name(name_?: any) { return name_ !== undefined ? classDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? classDeclaration({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    body(body_?: any) { return body_ !== undefined ? classDeclaration({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return classDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function classHeritage(config?: ConfigOf<ClassHeritage>) {
  if (config && 'extendsClause' in config) return classHeritageExtendsClause(config as any);
  return classHeritageImplementsClause(config as any);
}
export function classHeritageExtendsClause(config: ConfigOf<ClassHeritageExtendsClause>) {
  const fields = {
    extends_clause: (config as any)?.extendsClause,
    implements_clause: (config as any)?.implementsClause,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_heritage' as const,
    named: true as const,
    fields,
    children,
    extendsClause(extendsClause_?: any) { return extendsClause_ !== undefined ? classHeritageExtendsClause({ ...(config as any), extendsClause: extendsClause_ } as any) : fields.extends_clause; },
    implementsClause(implementsClause_?: any) { return implementsClause_ !== undefined ? classHeritageExtendsClause({ ...(config as any), implementsClause: implementsClause_ } as any) : fields.implements_clause; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return classHeritageExtendsClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function classHeritageImplementsClause(config?: ConfigOf<ClassHeritageImplementsClause>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_heritage' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return classHeritageImplementsClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassHeritageTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionExpression(config: ConfigOf<FunctionExpression>) {
  const fields = {
    name: (config as any)?.name,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_expression' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? functionExpression({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? functionExpression({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDeclaration(config: ConfigOf<FunctionDeclaration>) {
  const fields = {
    name: (config as any)?.name,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? functionDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? functionDeclaration({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunction(config: ConfigOf<GeneratorFunction>) {
  const fields = {
    name: (config as any)?.name,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generator_function' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? generatorFunction({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? generatorFunction({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return generatorFunction({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunctionDeclaration(config: ConfigOf<GeneratorFunctionDeclaration>) {
  const fields = {
    name: (config as any)?.name,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generator_function_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? generatorFunctionDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? generatorFunctionDeclaration({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return generatorFunctionDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunction(config: ConfigOf<ArrowFunction>) {
  const fields = {
    parameter: (config as any)?.parameter,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'arrow_function' as const,
    named: true as const,
    fields,
    children,
    parameter(parameter_?: any) { return parameter_ !== undefined ? arrowFunction({ ...(config as any), parameter: parameter_ } as any) : fields.parameter; },
    body(body_?: any) { return body_ !== undefined ? arrowFunction({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return arrowFunction({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrowFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}




export function callExpression(config?: ConfigOf<CallExpression>) {
  if (config && 'typeArguments' in config) return callExpressionFunction(config as any);
  if (config && 'typeArguments' in config) return callExpressionTokQDot(config as any);
  return callExpressionFunction2(config as any);
}
export function callExpressionFunction(config: ConfigOf<CallExpressionFunction>) {
  const fields = {
    function: (config as any)?.function,
    type_arguments: (config as any)?.typeArguments,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    children,
    function(function_?: any) { return function_ !== undefined ? callExpressionFunction({ ...(config as any), function: function_ } as any) : fields.function; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? callExpressionFunction({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? callExpressionFunction({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return callExpressionFunction({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionFunction2(config: ConfigOf<CallExpressionFunction2>) {
  const fields = {
    function: (config as any)?.function,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    children,
    function(function_?: any) { return function_ !== undefined ? callExpressionFunction2({ ...(config as any), function: function_ } as any) : fields.function; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? callExpressionFunction2({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return callExpressionFunction2({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionTokQDot(config: ConfigOf<CallExpressionTokQDot>) {
  const fields = {
    function: (config as any)?.function,
    type_arguments: (config as any)?.typeArguments,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    children,
    function(function_?: any) { return function_ !== undefined ? callExpressionTokQDot({ ...(config as any), function: function_ } as any) : fields.function; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? callExpressionTokQDot({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? callExpressionTokQDot({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return callExpressionTokQDot({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function newExpression(config: ConfigOf<NewExpression>) {
  const fields = {
    constructor: (config as any)?.constructor,
    type_arguments: (config as any)?.typeArguments,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'new_expression' as const,
    named: true as const,
    fields,
    children,
    constructor(constructor_?: any) { return constructor_ !== undefined ? newExpression({ ...(config as any), constructor: constructor_ } as any) : fields.constructor; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? newExpression({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? newExpression({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return newExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(config: ConfigOf<AwaitExpression>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'await_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? awaitExpression({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return awaitExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberExpression(config: ConfigOf<MemberExpression>) {
  const fields = {
    object: (config as any)?.object,
    optional_chain: (config as any)?.optionalChain,
    property: (config as any)?.property,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'member_expression' as const,
    named: true as const,
    fields,
    children,
    object(object_?: any) { return object_ !== undefined ? memberExpression({ ...(config as any), object: object_ } as any) : fields.object; },
    optionalChain(optionalChain_?: any) { return optionalChain_ !== undefined ? memberExpression({ ...(config as any), optionalChain: optionalChain_ } as any) : fields.optional_chain; },
    property(property_?: any) { return property_ !== undefined ? memberExpression({ ...(config as any), property: property_ } as any) : fields.property; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return memberExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscriptExpression(config: ConfigOf<SubscriptExpression>) {
  const fields = {
    object: (config as any)?.object,
    optional_chain: (config as any)?.optionalChain,
    index: (config as any)?.index,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'subscript_expression' as const,
    named: true as const,
    fields,
    children,
    object(object_?: any) { return object_ !== undefined ? subscriptExpression({ ...(config as any), object: object_ } as any) : fields.object; },
    optionalChain(optionalChain_?: any) { return optionalChain_ !== undefined ? subscriptExpression({ ...(config as any), optionalChain: optionalChain_ } as any) : fields.optional_chain; },
    index(index_?: any) { return index_ !== undefined ? subscriptExpression({ ...(config as any), index: index_ } as any) : fields.index; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return subscriptExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: ConfigOf<AssignmentExpression>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? assignmentExpression({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? assignmentExpression({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return assignmentExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignmentExpression(config: ConfigOf<AugmentedAssignmentExpression>) {
  const fields = {
    left: (config as any)?.left,
    operator: (config as any)?.operator,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'augmented_assignment_expression' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? augmentedAssignmentExpression({ ...(config as any), left: left_ } as any) : fields.left; },
    operator(operator_?: any) { return operator_ !== undefined ? augmentedAssignmentExpression({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    right(right_?: any) { return right_ !== undefined ? augmentedAssignmentExpression({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return augmentedAssignmentExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function spreadElement(config: ConfigOf<SpreadElement>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'spread_element' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? spreadElement({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return spreadElement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ternaryExpression(config: ConfigOf<TernaryExpression>) {
  const fields = {
    condition: (config as any)?.condition,
    consequence: (config as any)?.consequence,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'ternary_expression' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: any) { return condition_ !== undefined ? ternaryExpression({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    consequence(consequence_?: any) { return consequence_ !== undefined ? ternaryExpression({ ...(config as any), consequence: consequence_ } as any) : fields.consequence; },
    alternative(alternative_?: any) { return alternative_ !== undefined ? ternaryExpression({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return ternaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: ConfigOf<BinaryExpression>) {
  const fields = {
    left: (config as any)?.left,
    operator: (config as any)?.operator,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? binaryExpression({ ...(config as any), left: left_ } as any) : fields.left; },
    operator(operator_?: any) { return operator_ !== undefined ? binaryExpression({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    right(right_?: any) { return right_ !== undefined ? binaryExpression({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return binaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: ConfigOf<UnaryExpression>) {
  const fields = {
    operator: (config as any)?.operator,
    argument: (config as any)?.argument,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    children,
    operator(operator_?: any) { return operator_ !== undefined ? unaryExpression({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    argument(argument_?: any) { return argument_ !== undefined ? unaryExpression({ ...(config as any), argument: argument_ } as any) : fields.argument; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return unaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function updateExpression(config: ConfigOf<UpdateExpression>) {
  const fields = {
    argument: (config as any)?.argument,
    operator: (config as any)?.operator,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'update_expression' as const,
    named: true as const,
    fields,
    children,
    argument(argument_?: any) { return argument_ !== undefined ? updateExpression({ ...(config as any), argument: argument_ } as any) : fields.argument; },
    operator(operator_?: any) { return operator_ !== undefined ? updateExpression({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return updateExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function sequenceExpression(...children: any[]) {
  return {
    type: 'sequence_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function string(config?: ConfigOf<String>) {
  return stringTokSq(config as any);
}
export function stringTokDq(config?: ConfigOf<StringTokDq>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'string' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return stringTokDq({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function stringTokSq(config?: ConfigOf<StringTokSq>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'string' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return stringTokSq({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleStringFragment(text: string) {
  return {
    type: 'unescaped_double_string_fragment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedDoubleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleStringFragment(text: string) {
  return {
    type: 'unescaped_single_string_fragment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnescapedSingleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(config?: any) { return { type: 'escape_sequence' as const, named: true as const, render() { return render(this); }, toEdit(s: any, e?: any) { return typeof s === 'number' ? toEdit(this, s, e!) : toEdit(this, s); }, replace(t: EscapeSequenceTree) { const r = t.range(); return toEdit(this, r); } }; }



export function comment(config?: ConfigOf<Comment>) {
  return commentTokSlashStar(config as any);
}
export function commentSlashslash(config?: ConfigOf<CommentSlashslash>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'comment' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return commentSlashslash({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function commentTokSlashStar(config?: ConfigOf<CommentTokSlashStar>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'comment' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return commentTokSlashStar({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateString(...children: any[]) {
  return {
    type: 'template_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateSubstitution(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'template_substitution' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regex(config: ConfigOf<Regex>) {
  const fields = {
    pattern: (config as any)?.pattern,
    flags: (config as any)?.flags,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'regex' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: any) { return pattern_ !== undefined ? regex({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    flags(flags_?: any) { return flags_ !== undefined ? regex({ ...(config as any), flags: flags_ } as any) : fields.flags; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return regex({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regexPattern(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'regex_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RegexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regexFlags(text: string) {
  return {
    type: 'regex_flags' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RegexFlagsTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}






export function number(config?: ConfigOf<Number>) {
  return numberN(config as any);
}
export function numberForm0(config?: ConfigOf<NumberForm0>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'number' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return numberForm0({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NumberTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function numberForm1(config?: ConfigOf<NumberForm1>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'number' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return numberForm1({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NumberTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function numberForm2(config?: ConfigOf<NumberForm2>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'number' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return numberForm2({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NumberTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function numberForm3(config?: ConfigOf<NumberForm3>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'number' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return numberForm3({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NumberTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function numberN(config?: ConfigOf<NumberN>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'number' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return numberN({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NumberTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function metaProperty(config?: ConfigOf<MetaProperty>) {
  return metaPropertyImport(config as any);
}
export function metaPropertyNew(config?: ConfigOf<MetaPropertyNew>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'meta_property' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return metaPropertyNew({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MetaPropertyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function metaPropertyImport(config?: ConfigOf<MetaPropertyImport>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'meta_property' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return metaPropertyImport({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MetaPropertyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function this_() {
  return {
    type: 'this' as const,
    named: true as const,
    text: 'this',
    render: () => 'this',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' },
    replace: (t: ThisTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super',
    render: () => 'super',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' },
    replace: (t: SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'true',
    render: () => 'true',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'true' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'true' },
    replace: (t: TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'false',
    render: () => 'false',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'false' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'false' },
    replace: (t: FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' }; },
  };
}

export function null_() {
  return {
    type: 'null' as const,
    named: true as const,
    text: 'null',
    render: () => 'null',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'null' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'null' },
    replace: (t: NullTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' }; },
  };
}

export function undefined_() {
  return {
    type: 'undefined' as const,
    named: true as const,
    text: 'undefined',
    render: () => 'undefined',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'undefined' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'undefined' },
    replace: (t: UndefinedTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' }; },
  };
}

export function arguments_(...children: any[]) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(config?: any) { return { type: 'decorator' as const, named: true as const, render() { return render(this); }, toEdit(s: any, e?: any) { return typeof s === 'number' ? toEdit(this, s, e!) : toEdit(this, s); }, replace(t: DecoratorTree) { const r = t.range(); return toEdit(this, r); } }; }

export function decoratorMemberExpression(config: ConfigOf<DecoratorMemberExpression>) {
  const fields = {
    object: (config as any)?.object,
    property: (config as any)?.property,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'decorator_member_expression' as const,
    named: true as const,
    fields,
    children,
    object(object_?: any) { return object_ !== undefined ? decoratorMemberExpression({ ...(config as any), object: object_ } as any) : fields.object; },
    property(property_?: any) { return property_ !== undefined ? decoratorMemberExpression({ ...(config as any), property: property_ } as any) : fields.property; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return decoratorMemberExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorMemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorCallExpression(config: ConfigOf<DecoratorCallExpression>) {
  const fields = {
    function: (config as any)?.function,
    type_arguments: (config as any)?.typeArguments,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'decorator_call_expression' as const,
    named: true as const,
    fields,
    children,
    function(function_?: any) { return function_ !== undefined ? decoratorCallExpression({ ...(config as any), function: function_ } as any) : fields.function; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? decoratorCallExpression({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? decoratorCallExpression({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return decoratorCallExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorCallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBody(...children: any[]) {
  return {
    type: 'class_body' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDefinition(config: ConfigOf<FieldDefinition>) {
  const fields = {
    decorator: (config as any)?.decorator,
    property: (config as any)?.property,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'field_definition' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: any[]) { return decorator_.length ? fieldDefinition({ ...(config as any), decorator: decorator_ } as any) : fields.decorator; },
    property(property_?: any) { return property_ !== undefined ? fieldDefinition({ ...(config as any), property: property_ } as any) : fields.property; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return fieldDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formalParameters(...children: any[]) {
  return {
    type: 'formal_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classStaticBlock(config: ConfigOf<ClassStaticBlock>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_static_block' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? classStaticBlock({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return classStaticBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function pattern(config?: ConfigOf<Pattern>) {
  return patternRestPattern(config as any);
}
export function patternULhsExpression(config?: ConfigOf<PatternULhsExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternULhsExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function patternRestPattern(config?: ConfigOf<PatternRestPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternRestPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restPattern(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'rest_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodDefinition(config: ConfigOf<MethodDefinition>) {
  const fields = {
    accessibility_modifier: (config as any)?.accessibilityModifier,
    override_modifier: (config as any)?.overrideModifier,
    name: (config as any)?.name,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'method_definition' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: any) { return accessibilityModifier_ !== undefined ? methodDefinition({ ...(config as any), accessibilityModifier: accessibilityModifier_ } as any) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: any) { return overrideModifier_ !== undefined ? methodDefinition({ ...(config as any), overrideModifier: overrideModifier_ } as any) : fields.override_modifier; },
    name(name_?: any) { return name_ !== undefined ? methodDefinition({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? methodDefinition({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return methodDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: ConfigOf<Pair>) {
  const fields = {
    key: (config as any)?.key,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    children,
    key(key_?: any) { return key_ !== undefined ? pair({ ...(config as any), key: key_ } as any) : fields.key; },
    value(value_?: any) { return value_ !== undefined ? pair({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return pair({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pairPattern(config: ConfigOf<PairPattern>) {
  const fields = {
    key: (config as any)?.key,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'pair_pattern' as const,
    named: true as const,
    fields,
    children,
    key(key_?: any) { return key_ !== undefined ? pairPattern({ ...(config as any), key: key_ } as any) : fields.key; },
    value(value_?: any) { return value_ !== undefined ? pairPattern({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return pairPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function computedPropertyName(config: ConfigOf<ComputedPropertyName>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'computed_property_name' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? computedPropertyName({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return computedPropertyName({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function publicFieldDefinition(config: ConfigOf<PublicFieldDefinition>) {
  const fields = {
    accessibility_modifier: (config as any)?.accessibilityModifier,
    override_modifier: (config as any)?.overrideModifier,
    initializer: (config as any)?.initializer,
    name: (config as any)?.name,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: any) { return accessibilityModifier_ !== undefined ? publicFieldDefinition({ ...(config as any), accessibilityModifier: accessibilityModifier_ } as any) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: any) { return overrideModifier_ !== undefined ? publicFieldDefinition({ ...(config as any), overrideModifier: overrideModifier_ } as any) : fields.override_modifier; },
    initializer(initializer_?: any) { return initializer_ !== undefined ? publicFieldDefinition({ ...(config as any), initializer: initializer_ } as any) : fields.initializer; },
    name(name_?: any) { return name_ !== undefined ? publicFieldDefinition({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? publicFieldDefinition({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return publicFieldDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenJsxStartOpeningElement(config?: ConfigOf<HiddenJsxStartOpeningElement>) {
  const fields = {
    name: (config as any)?.name,
    type_arguments: (config as any)?.typeArguments,
    attribute: (config as any)?.attribute,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: '_jsx_start_opening_element' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? hiddenJsxStartOpeningElement({ ...(config as any), name: name_ } as any) : fields.name; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? hiddenJsxStartOpeningElement({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    attribute(...attribute_: any[]) { return attribute_.length ? hiddenJsxStartOpeningElement({ ...(config as any), attribute: attribute_ } as any) : fields.attribute; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return hiddenJsxStartOpeningElement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenJsxStartOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonNullExpression(config: ConfigOf<NonNullExpression>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'non_null_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? nonNullExpression({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return nonNullExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodSignature(config: ConfigOf<MethodSignature>) {
  const fields = {
    accessibility_modifier: (config as any)?.accessibilityModifier,
    override_modifier: (config as any)?.overrideModifier,
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: any) { return accessibilityModifier_ !== undefined ? methodSignature({ ...(config as any), accessibilityModifier: accessibilityModifier_ } as any) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: any) { return overrideModifier_ !== undefined ? methodSignature({ ...(config as any), overrideModifier: overrideModifier_ } as any) : fields.override_modifier; },
    name(name_?: any) { return name_ !== undefined ? methodSignature({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return methodSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractMethodSignature(config: ConfigOf<AbstractMethodSignature>) {
  const fields = {
    accessibility_modifier: (config as any)?.accessibilityModifier,
    override_modifier: (config as any)?.overrideModifier,
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'abstract_method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: any) { return accessibilityModifier_ !== undefined ? abstractMethodSignature({ ...(config as any), accessibilityModifier: accessibilityModifier_ } as any) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: any) { return overrideModifier_ !== undefined ? abstractMethodSignature({ ...(config as any), overrideModifier: overrideModifier_ } as any) : fields.override_modifier; },
    name(name_?: any) { return name_ !== undefined ? abstractMethodSignature({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return abstractMethodSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignature(config: ConfigOf<FunctionSignature>) {
  const fields = {
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_signature' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? functionSignature({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorParenthesizedExpression(config?: any) { return { type: 'decorator_parenthesized_expression' as const, named: true as const, render() { return render(this); }, toEdit(s: any, e?: any) { return typeof s === 'number' ? toEdit(this, s, e!) : toEdit(this, s); }, replace(t: DecoratorParenthesizedExpressionTree) { const r = t.range(); return toEdit(this, r); } }; }

export function typeAssertion(config: ConfigOf<TypeAssertion>) {
  const fields = {
    type_arguments: (config as any)?.typeArguments,
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_assertion' as const,
    named: true as const,
    fields,
    children,
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? typeAssertion({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    expression(expression_?: any) { return expression_ !== undefined ? typeAssertion({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeAssertion({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asExpression(config: ConfigOf<AsExpression>) {
  const fields = {
    expression: (config as any)?.expression,
    type_annotation: (config as any)?.typeAnnotation,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? asExpression({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    typeAnnotation(typeAnnotation_?: any) { return typeAnnotation_ !== undefined ? asExpression({ ...(config as any), typeAnnotation: typeAnnotation_ } as any) : fields.type_annotation; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return asExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function satisfiesExpression(config: ConfigOf<SatisfiesExpression>) {
  const fields = {
    expression: (config as any)?.expression,
    type_annotation: (config as any)?.typeAnnotation,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'satisfies_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? satisfiesExpression({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    typeAnnotation(typeAnnotation_?: any) { return typeAnnotation_ !== undefined ? satisfiesExpression({ ...(config as any), typeAnnotation: typeAnnotation_ } as any) : fields.type_annotation; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return satisfiesExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function instantiationExpression(config: ConfigOf<InstantiationExpression>) {
  const fields = {
    expression: (config as any)?.expression,
    type_arguments: (config as any)?.typeArguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'instantiation_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? instantiationExpression({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? instantiationExpression({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return instantiationExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importRequireClause(config: ConfigOf<ImportRequireClause>) {
  const fields = {
    identifier: (config as any)?.identifier,
    source: (config as any)?.source,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_require_clause' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? importRequireClause({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    source(source_?: any) { return source_ !== undefined ? importRequireClause({ ...(config as any), source: source_ } as any) : fields.source; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return importRequireClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsClause(...children: any[]) {
  return {
    type: 'extends_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implementsClause(...children: any[]) {
  return {
    type: 'implements_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ambientDeclaration(config: ConfigOf<AmbientDeclaration>) {
  const fields = {
    declaration: (config as any)?.declaration,
    type_annotation: (config as any)?.typeAnnotation,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    fields,
    children,
    declaration(declaration_?: any) { return declaration_ !== undefined ? ambientDeclaration({ ...(config as any), declaration: declaration_ } as any) : fields.declaration; },
    typeAnnotation(typeAnnotation_?: any) { return typeAnnotation_ !== undefined ? ambientDeclaration({ ...(config as any), typeAnnotation: typeAnnotation_ } as any) : fields.type_annotation; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return ambientDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractClassDeclaration(config: ConfigOf<AbstractClassDeclaration>) {
  const fields = {
    class_heritage: (config as any)?.classHeritage,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'abstract_class_declaration' as const,
    named: true as const,
    fields,
    children,
    classHeritage(classHeritage_?: any) { return classHeritage_ !== undefined ? abstractClassDeclaration({ ...(config as any), classHeritage: classHeritage_ } as any) : fields.class_heritage; },
    name(name_?: any) { return name_ !== undefined ? abstractClassDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? abstractClassDeclaration({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    body(body_?: any) { return body_ !== undefined ? abstractClassDeclaration({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return abstractClassDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function module_(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function internalModule(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'internal_module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAlias(config: ConfigOf<ImportAlias>) {
  const fields = {
    name: (config as any)?.name,
    value: (config as any)?.value,
    semicolon: (config as any)?.semicolon,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_alias' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? importAlias({ ...(config as any), name: name_ } as any) : fields.name; },
    value(value_?: any) { return value_ !== undefined ? importAlias({ ...(config as any), value: value_ } as any) : fields.value; },
    semicolon(semicolon_?: any) { return semicolon_ !== undefined ? importAlias({ ...(config as any), semicolon: semicolon_ } as any) : fields.semicolon; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return importAlias({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nestedTypeIdentifier(config: ConfigOf<NestedTypeIdentifier>) {
  const fields = {
    module: (config as any)?.module,
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'nested_type_identifier' as const,
    named: true as const,
    fields,
    children,
    module(module_?: any) { return module_ !== undefined ? nestedTypeIdentifier({ ...(config as any), module: module_ } as any) : fields.module; },
    name(name_?: any) { return name_ !== undefined ? nestedTypeIdentifier({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return nestedTypeIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interfaceDeclaration(config: ConfigOf<InterfaceDeclaration>) {
  const fields = {
    extends_type_clause: (config as any)?.extendsTypeClause,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'interface_declaration' as const,
    named: true as const,
    fields,
    children,
    extendsTypeClause(extendsTypeClause_?: any) { return extendsTypeClause_ !== undefined ? interfaceDeclaration({ ...(config as any), extendsTypeClause: extendsTypeClause_ } as any) : fields.extends_type_clause; },
    name(name_?: any) { return name_ !== undefined ? interfaceDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? interfaceDeclaration({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    body(body_?: any) { return body_ !== undefined ? interfaceDeclaration({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return interfaceDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsTypeClause(config: ConfigOf<ExtendsTypeClause>) {
  const fields = {
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'extends_type_clause' as const,
    named: true as const,
    fields,
    children,
    typeField(...type: any[]) { return type.length ? extendsTypeClause({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return extendsTypeClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumDeclaration(config: ConfigOf<EnumDeclaration>) {
  const fields = {
    name: (config as any)?.name,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'enum_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? enumDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? enumDeclaration({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return enumDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumBody(config: ConfigOf<EnumBody>) {
  const fields = {
    opening: (config as any)?.opening,
    members: (config as any)?.members,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'enum_body' as const,
    named: true as const,
    fields,
    children,
    opening(opening_?: any) { return opening_ !== undefined ? enumBody({ ...(config as any), opening: opening_ } as any) : fields.opening; },
    members(members_?: any) { return members_ !== undefined ? enumBody({ ...(config as any), members: members_ } as any) : fields.members; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return enumBody({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumAssignment(config: ConfigOf<EnumAssignment>) {
  const fields = {
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'enum_assignment' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? enumAssignment({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return enumAssignment({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasDeclaration(config: ConfigOf<TypeAliasDeclaration>) {
  const fields = {
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_alias_declaration' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? typeAliasDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? typeAliasDeclaration({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    value(value_?: any) { return value_ !== undefined ? typeAliasDeclaration({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeAliasDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function overrideModifier() {
  return {
    type: 'override_modifier' as const,
    named: true as const,
    text: 'override',
    render: () => 'override',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'override' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'override' },
    replace: (t: OverrideModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' }; },
  };
}

export function requiredParameter(config: ConfigOf<RequiredParameter>) {
  const fields = {
    parameter_name: (config as any)?.parameterName,
    initializer: (config as any)?.initializer,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'required_parameter' as const,
    named: true as const,
    fields,
    children,
    parameterName(parameterName_?: any) { return parameterName_ !== undefined ? requiredParameter({ ...(config as any), parameterName: parameterName_ } as any) : fields.parameter_name; },
    initializer(initializer_?: any) { return initializer_ !== undefined ? requiredParameter({ ...(config as any), initializer: initializer_ } as any) : fields.initializer; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return requiredParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalParameter(config: ConfigOf<OptionalParameter>) {
  const fields = {
    parameter_name: (config as any)?.parameterName,
    initializer: (config as any)?.initializer,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'optional_parameter' as const,
    named: true as const,
    fields,
    children,
    parameterName(parameterName_?: any) { return parameterName_ !== undefined ? optionalParameter({ ...(config as any), parameterName: parameterName_ } as any) : fields.parameter_name; },
    initializer(initializer_?: any) { return initializer_ !== undefined ? optionalParameter({ ...(config as any), initializer: initializer_ } as any) : fields.initializer; },
    typeField(type?: any) { return type !== undefined ? optionalParameter({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return optionalParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function omittingTypeAnnotation(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'omitting_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function addingTypeAnnotation(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'adding_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optingTypeAnnotation(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'opting_type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAnnotation(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'type_annotation' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asserts(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'asserts' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertsAnnotation(config: ConfigOf<AssertsAnnotation>) {
  const fields = {
    asserts: (config as any)?.asserts,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'asserts_annotation' as const,
    named: true as const,
    fields,
    children,
    asserts(asserts_?: any) { return asserts_ !== undefined ? assertsAnnotation({ ...(config as any), asserts: asserts_ } as any) : fields.asserts; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return assertsAnnotation({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}








export function type_(config?: ConfigOf<Type>) {
  return typeUTypeQueryCallExpressionInTypeAnnotation(config as any);
}
export function typePrimaryType(config?: ConfigOf<TypePrimaryType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typePrimaryType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeFunctionType(config?: ConfigOf<TypeFunctionType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeFunctionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeReadonlyType(config?: ConfigOf<TypeReadonlyType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeReadonlyType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeConstructorType(config?: ConfigOf<TypeConstructorType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeConstructorType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeInferType(config?: ConfigOf<TypeInferType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeInferType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeUTypeQueryMemberExpressionInTypeAnnotation(config?: ConfigOf<TypeUTypeQueryMemberExpressionInTypeAnnotation>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeUTypeQueryMemberExpressionInTypeAnnotation({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeUTypeQueryCallExpressionInTypeAnnotation(config?: ConfigOf<TypeUTypeQueryCallExpressionInTypeAnnotation>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeUTypeQueryCallExpressionInTypeAnnotation({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleParameter(config: ConfigOf<TupleParameter>) {
  const fields = {
    name: (config as any)?.name,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'tuple_parameter' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? tupleParameter({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? tupleParameter({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return tupleParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalTupleParameter(config: ConfigOf<OptionalTupleParameter>) {
  const fields = {
    name: (config as any)?.name,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'optional_tuple_parameter' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? optionalTupleParameter({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? optionalTupleParameter({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return optionalTupleParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalType(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'optional_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restType(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'rest_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructorType(config: ConfigOf<ConstructorType>) {
  const fields = {
    type_parameters: (config as any)?.typeParameters,
    parameters: (config as any)?.parameters,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'constructor_type' as const,
    named: true as const,
    fields,
    children,
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? constructorType({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    parameters(parameters_?: any) { return parameters_ !== undefined ? constructorType({ ...(config as any), parameters: parameters_ } as any) : fields.parameters; },
    typeField(type?: any) { return type !== undefined ? constructorType({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return constructorType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}





















export function primaryType(config?: ConfigOf<PrimaryType>) {
  return primaryTypeConst(config as any);
}
export function primaryTypeParenthesizedType(config?: ConfigOf<PrimaryTypeParenthesizedType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeParenthesizedType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypePredefinedType(config?: ConfigOf<PrimaryTypePredefinedType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypePredefinedType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeUTypeIdentifier(config?: ConfigOf<PrimaryTypeUTypeIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeUTypeIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeNestedTypeIdentifier(config?: ConfigOf<PrimaryTypeNestedTypeIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeNestedTypeIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeGenericType(config?: ConfigOf<PrimaryTypeGenericType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeGenericType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeObjectType(config?: ConfigOf<PrimaryTypeObjectType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeObjectType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeArrayType(config?: ConfigOf<PrimaryTypeArrayType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeArrayType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeTupleType(config?: ConfigOf<PrimaryTypeTupleType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeTupleType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeFlowMaybeType(config?: ConfigOf<PrimaryTypeFlowMaybeType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeFlowMaybeType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeTypeQuery(config?: ConfigOf<PrimaryTypeTypeQuery>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeTypeQuery({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeIndexTypeQuery(config?: ConfigOf<PrimaryTypeIndexTypeQuery>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeIndexTypeQuery({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeThis(config?: ConfigOf<PrimaryTypeThis>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeThis({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeExistentialType(config?: ConfigOf<PrimaryTypeExistentialType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeExistentialType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeLiteralType(config?: ConfigOf<PrimaryTypeLiteralType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeLiteralType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeLookupType(config?: ConfigOf<PrimaryTypeLookupType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeLookupType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeConditionalType(config?: ConfigOf<PrimaryTypeConditionalType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeConditionalType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeTemplateLiteralType(config?: ConfigOf<PrimaryTypeTemplateLiteralType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeTemplateLiteralType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeIntersectionType(config?: ConfigOf<PrimaryTypeIntersectionType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeIntersectionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeUnionType(config?: ConfigOf<PrimaryTypeUnionType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeUnionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryTypeConst(config?: ConfigOf<PrimaryTypeConst>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryTypeConst({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateType(config?: any) { return { type: 'template_type' as const, named: true as const, render() { return render(this); }, toEdit(s: any, e?: any) { return typeof s === 'number' ? toEdit(this, s, e!) : toEdit(this, s); }, replace(t: TemplateTypeTree) { const r = t.range(); return toEdit(this, r); } }; }

export function templateLiteralType(...children: any[]) {
  return {
    type: 'template_literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function inferType(config: ConfigOf<InferType>) {
  const fields = {
    type_identifier: (config as any)?.typeIdentifier,
    constraint: (config as any)?.constraint,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'infer_type' as const,
    named: true as const,
    fields,
    children,
    typeIdentifier(typeIdentifier_?: any) { return typeIdentifier_ !== undefined ? inferType({ ...(config as any), typeIdentifier: typeIdentifier_ } as any) : fields.type_identifier; },
    constraint(constraint_?: any) { return constraint_ !== undefined ? inferType({ ...(config as any), constraint: constraint_ } as any) : fields.constraint; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return inferType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalType(config: ConfigOf<ConditionalType>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
    consequence: (config as any)?.consequence,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'conditional_type' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? conditionalType({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? conditionalType({ ...(config as any), right: right_ } as any) : fields.right; },
    consequence(consequence_?: any) { return consequence_ !== undefined ? conditionalType({ ...(config as any), consequence: consequence_ } as any) : fields.consequence; },
    alternative(alternative_?: any) { return alternative_ !== undefined ? conditionalType({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return conditionalType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: ConfigOf<GenericType>) {
  const fields = {
    name: (config as any)?.name,
    type_arguments: (config as any)?.typeArguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? genericType({ ...(config as any), name: name_ } as any) : fields.name; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? genericType({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return genericType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicate(config: ConfigOf<TypePredicate>) {
  const fields = {
    name: (config as any)?.name,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_predicate' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? typePredicate({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? typePredicate({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typePredicate({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicateAnnotation(config: ConfigOf<TypePredicateAnnotation>) {
  const fields = {
    type_predicate: (config as any)?.typePredicate,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_predicate_annotation' as const,
    named: true as const,
    fields,
    children,
    typePredicate(typePredicate_?: any) { return typePredicate_ !== undefined ? typePredicateAnnotation({ ...(config as any), typePredicate: typePredicate_ } as any) : fields.type_predicate; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typePredicateAnnotation({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeQuery(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'type_query' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexTypeQuery(config: ConfigOf<IndexTypeQuery>) {
  const fields = {
    primary_type: (config as any)?.primaryType,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'index_type_query' as const,
    named: true as const,
    fields,
    children,
    primaryType(primaryType_?: any) { return primaryType_ !== undefined ? indexTypeQuery({ ...(config as any), primaryType: primaryType_ } as any) : fields.primary_type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return indexTypeQuery({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lookupType(config: ConfigOf<LookupType>) {
  const fields = {
    primary_type: (config as any)?.primaryType,
    index_type: (config as any)?.indexType,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'lookup_type' as const,
    named: true as const,
    fields,
    children,
    primaryType(primaryType_?: any) { return primaryType_ !== undefined ? lookupType({ ...(config as any), primaryType: primaryType_ } as any) : fields.primary_type; },
    indexType(indexType_?: any) { return indexType_ !== undefined ? lookupType({ ...(config as any), indexType: indexType_ } as any) : fields.index_type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return lookupType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mappedTypeClause(config: ConfigOf<MappedTypeClause>) {
  const fields = {
    name: (config as any)?.name,
    type: (config as any)?.type,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'mapped_type_clause' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? mappedTypeClause({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? mappedTypeClause({ ...(config as any), type: type } as any) : fields.type; },
    alias(alias_?: any) { return alias_ !== undefined ? mappedTypeClause({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return mappedTypeClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}








export function literalType(config?: ConfigOf<LiteralType>) {
  return literalTypeUndefined(config as any);
}
export function literalTypeUNumber(config?: ConfigOf<LiteralTypeUNumber>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return literalTypeUNumber({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function literalTypeNumber(config?: ConfigOf<LiteralTypeNumber>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return literalTypeNumber({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function literalTypeString(config?: ConfigOf<LiteralTypeString>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return literalTypeString({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function literalTypeTrue(config?: ConfigOf<LiteralTypeTrue>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return literalTypeTrue({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function literalTypeFalse(config?: ConfigOf<LiteralTypeFalse>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return literalTypeFalse({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function literalTypeNull(config?: ConfigOf<LiteralTypeNull>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return literalTypeNull({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function literalTypeUndefined(config?: ConfigOf<LiteralTypeUndefined>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return literalTypeUndefined({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function flowMaybeType(config: ConfigOf<FlowMaybeType>) {
  const fields = {
    primary_type: (config as any)?.primaryType,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'flow_maybe_type' as const,
    named: true as const,
    fields,
    children,
    primaryType(primaryType_?: any) { return primaryType_ !== undefined ? flowMaybeType({ ...(config as any), primaryType: primaryType_ } as any) : fields.primary_type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return flowMaybeType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedType(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'parenthesized_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}











export function predefinedType(config?: ConfigOf<PredefinedType>) {
  return predefinedTypeObject(config as any);
}
export function predefinedTypeAny(config?: ConfigOf<PredefinedTypeAny>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeAny({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeNumber(config?: ConfigOf<PredefinedTypeNumber>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeNumber({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeBoolean(config?: ConfigOf<PredefinedTypeBoolean>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeBoolean({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeString(config?: ConfigOf<PredefinedTypeString>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeString({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeSymbol(config?: ConfigOf<PredefinedTypeSymbol>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeSymbol({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeUnique(config?: ConfigOf<PredefinedTypeUnique>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeUnique({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeVoid(config?: ConfigOf<PredefinedTypeVoid>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeVoid({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeUnknown(config?: ConfigOf<PredefinedTypeUnknown>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeUnknown({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeNever(config?: ConfigOf<PredefinedTypeNever>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeNever({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function predefinedTypeObject(config?: ConfigOf<PredefinedTypeObject>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'predefined_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return predefinedTypeObject({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PredefinedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeArguments(...children: any[]) {
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectType(config: ConfigOf<ObjectType>) {
  const fields = {
    opening: (config as any)?.opening,
    members: (config as any)?.members,
    closing: (config as any)?.closing,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'object_type' as const,
    named: true as const,
    fields,
    children,
    opening(opening_?: any) { return opening_ !== undefined ? objectType({ ...(config as any), opening: opening_ } as any) : fields.opening; },
    members(members_?: any) { return members_ !== undefined ? objectType({ ...(config as any), members: members_ } as any) : fields.members; },
    closing(closing_?: any) { return closing_ !== undefined ? objectType({ ...(config as any), closing: closing_ } as any) : fields.closing; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return objectType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callSignature(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'call_signature' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertySignature(config: ConfigOf<PropertySignature>) {
  const fields = {
    accessibility_modifier: (config as any)?.accessibilityModifier,
    override_modifier: (config as any)?.overrideModifier,
    name: (config as any)?.name,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'property_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: any) { return accessibilityModifier_ !== undefined ? propertySignature({ ...(config as any), accessibilityModifier: accessibilityModifier_ } as any) : fields.accessibility_modifier; },
    overrideModifier(overrideModifier_?: any) { return overrideModifier_ !== undefined ? propertySignature({ ...(config as any), overrideModifier: overrideModifier_ } as any) : fields.override_modifier; },
    name(name_?: any) { return name_ !== undefined ? propertySignature({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? propertySignature({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return propertySignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: any[]) {
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: ConfigOf<TypeParameter>) {
  const fields = {
    name: (config as any)?.name,
    constraint: (config as any)?.constraint,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? typeParameter({ ...(config as any), name: name_ } as any) : fields.name; },
    constraint(constraint_?: any) { return constraint_ !== undefined ? typeParameter({ ...(config as any), constraint: constraint_ } as any) : fields.constraint; },
    value(value_?: any) { return value_ !== undefined ? typeParameter({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultType(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'default_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constraint(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'constraint' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructSignature(config: ConfigOf<ConstructSignature>) {
  const fields = {
    type_parameters: (config as any)?.typeParameters,
    parameters: (config as any)?.parameters,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'construct_signature' as const,
    named: true as const,
    fields,
    children,
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? constructSignature({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    parameters(parameters_?: any) { return parameters_ !== undefined ? constructSignature({ ...(config as any), parameters: parameters_ } as any) : fields.parameters; },
    typeField(type?: any) { return type !== undefined ? constructSignature({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return constructSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignature(config: ConfigOf<IndexSignature>) {
  const fields = {
    mapped_type_clause: (config as any)?.mappedTypeClause,
    name: (config as any)?.name,
    index_type: (config as any)?.indexType,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'index_signature' as const,
    named: true as const,
    fields,
    children,
    mappedTypeClause(mappedTypeClause_?: any) { return mappedTypeClause_ !== undefined ? indexSignature({ ...(config as any), mappedTypeClause: mappedTypeClause_ } as any) : fields.mapped_type_clause; },
    name(name_?: any) { return name_ !== undefined ? indexSignature({ ...(config as any), name: name_ } as any) : fields.name; },
    indexType(indexType_?: any) { return indexType_ !== undefined ? indexSignature({ ...(config as any), indexType: indexType_ } as any) : fields.index_type; },
    typeField(type?: any) { return type !== undefined ? indexSignature({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return indexSignature({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: ConfigOf<ArrayType>) {
  const fields = {
    primary_type: (config as any)?.primaryType,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    children,
    primaryType(primaryType_?: any) { return primaryType_ !== undefined ? arrayType({ ...(config as any), primaryType: primaryType_ } as any) : fields.primary_type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return arrayType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: any[]) {
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function readonlyType(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'readonly_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: ConfigOf<UnionType>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? unionType({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? unionType({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return unionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function intersectionType(config: ConfigOf<IntersectionType>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'intersection_type' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? intersectionType({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? intersectionType({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return intersectionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: ConfigOf<FunctionType>) {
  const fields = {
    type_parameters: (config as any)?.typeParameters,
    parameters: (config as any)?.parameters,
    return_type: (config as any)?.returnType,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    children,
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? functionType({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    parameters(parameters_?: any) { return parameters_ !== undefined ? functionType({ ...(config as any), parameters: parameters_ } as any) : fields.parameters; },
    returnType(returnType_?: any) { return returnType_ !== undefined ? functionType({ ...(config as any), returnType: returnType_ } as any) : fields.return_type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenTypeIdentifier(child?: any) {
  const children = child ? [child] : [];
  return {
    type: '_type_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function shorthandPropertyIdentifier(config?: ConfigOf<ShorthandPropertyIdentifier>) {
  return shorthandPropertyIdentifierUReservedIdentifier(config as any);
}
export function shorthandPropertyIdentifierIdentifier(config?: ConfigOf<ShorthandPropertyIdentifierIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'shorthand_property_identifier' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return shorthandPropertyIdentifierIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function shorthandPropertyIdentifierUReservedIdentifier(config?: ConfigOf<ShorthandPropertyIdentifierUReservedIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'shorthand_property_identifier' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return shorthandPropertyIdentifierUReservedIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function shorthandPropertyIdentifierPattern(config?: ConfigOf<ShorthandPropertyIdentifierPattern>) {
  return shorthandPropertyIdentifierPatternUReservedIdentifier(config as any);
}
export function shorthandPropertyIdentifierPatternIdentifier(config?: ConfigOf<ShorthandPropertyIdentifierPatternIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return shorthandPropertyIdentifierPatternIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function shorthandPropertyIdentifierPatternUReservedIdentifier(config?: ConfigOf<ShorthandPropertyIdentifierPatternUReservedIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return shorthandPropertyIdentifierPatternUReservedIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandPropertyIdentifierPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interfaceBody(config: ConfigOf<InterfaceBody>) {
  const fields = {
    opening: (config as any)?.opening,
    members: (config as any)?.members,
    closing: (config as any)?.closing,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'interface_body' as const,
    named: true as const,
    fields,
    children,
    opening(opening_?: any) { return opening_ !== undefined ? interfaceBody({ ...(config as any), opening: opening_ } as any) : fields.opening; },
    members(members_?: any) { return members_ !== undefined ? interfaceBody({ ...(config as any), members: members_ } as any) : fields.members; },
    closing(closing_?: any) { return closing_ !== undefined ? interfaceBody({ ...(config as any), closing: closing_ } as any) : fields.closing; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return interfaceBody({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterfaceBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function thisType() {
  return {
    type: 'this_type' as const,
    named: true as const,
    text: 'this',
    render: () => 'this',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' },
    replace: (t: ThisTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' }; },
  };
}

export const _factoryMap: Record<string, (config?: any) => unknown> = {
  'program': program,
  'hash_bang_line': hashBangLine,
  'export_statement_form_0': exportStatementForm0,
  'export_statement_declaration': exportStatementDeclaration,
  'export_statement_declaration2': exportStatementDeclaration2,
  'export_statement_namespace': exportStatementNamespace,
  'export_statement': exportStatement,
  'namespace_export': namespaceExport,
  'export_clause': exportClause,
  'export_specifier': exportSpecifier,
  'declaration_form_0': declarationForm0,
  'declaration_function_signature': declarationFunctionSignature,
  'declaration_abstract_class_declaration': declarationAbstractClassDeclaration,
  'declaration_module': declarationModule,
  'declaration_internal_module': declarationInternalModule,
  'declaration_type_alias_declaration': declarationTypeAliasDeclaration,
  'declaration_enum_declaration': declarationEnumDeclaration,
  'declaration_interface_declaration': declarationInterfaceDeclaration,
  'declaration_import_alias': declarationImportAlias,
  'declaration_ambient_declaration': declarationAmbientDeclaration,
  'declaration': declaration,
  'import': import_,
  'import_statement': importStatement,
  'import_clause_namespace_import': importClauseNamespaceImport,
  'import_clause_named_imports': importClauseNamedImports,
  'import_clause_default_import': importClauseDefaultImport,
  'import_clause': importClause,
  'namespace_import': namespaceImport,
  'named_imports': namedImports,
  'import_specifier': importSpecifier,
  'import_attribute': importAttribute,
  'statement_export_statement': statementExportStatement,
  'statement_import_statement': statementImportStatement,
  'statement_debugger_statement': statementDebuggerStatement,
  'statement_expression_statement': statementExpressionStatement,
  'statement_declaration': statementDeclaration,
  'statement_statement_block': statementStatementBlock,
  'statement_if_statement': statementIfStatement,
  'statement_switch_statement': statementSwitchStatement,
  'statement_for_statement': statementForStatement,
  'statement_for_in_statement': statementForInStatement,
  'statement_while_statement': statementWhileStatement,
  'statement_do_statement': statementDoStatement,
  'statement_try_statement': statementTryStatement,
  'statement_with_statement': statementWithStatement,
  'statement_break_statement': statementBreakStatement,
  'statement_continue_statement': statementContinueStatement,
  'statement_return_statement': statementReturnStatement,
  'statement_throw_statement': statementThrowStatement,
  'statement_empty_statement': statementEmptyStatement,
  'statement_labeled_statement': statementLabeledStatement,
  'statement': statement,
  'expression_statement': expressionStatement,
  'variable_declaration': variableDeclaration,
  'lexical_declaration': lexicalDeclaration,
  'variable_declarator': variableDeclarator,
  'statement_block': statementBlock,
  'else_clause': elseClause,
  'if_statement': ifStatement,
  'switch_statement': switchStatement,
  'for_statement': forStatement,
  'for_in_statement': forInStatement,
  'while_statement': whileStatement,
  'do_statement': doStatement,
  'try_statement': tryStatement,
  'with_statement': withStatement,
  'break_statement': breakStatement,
  'continue_statement': continueStatement,
  'debugger_statement': debuggerStatement,
  'return_statement': returnStatement,
  'throw_statement': throwStatement,
  'labeled_statement': labeledStatement,
  'switch_body': switchBody,
  'switch_case': switchCase,
  'switch_default': switchDefault,
  'catch_clause': catchClause,
  'finally_clause': finallyClause,
  'parenthesized_expression': parenthesizedExpression,
  'expression_as_expression': expressionAsExpression,
  'expression_satisfies_expression': expressionSatisfiesExpression,
  'expression_instantiation_expression': expressionInstantiationExpression,
  'expression_internal_module': expressionInternalModule,
  'expression_type_assertion': expressionTypeAssertion,
  'expression_primary_expression': expressionPrimaryExpression,
  'expression_assignment_expression': expressionAssignmentExpression,
  'expression_augmented_assignment_expression': expressionAugmentedAssignmentExpression,
  'expression_await_expression': expressionAwaitExpression,
  'expression_unary_expression': expressionUnaryExpression,
  'expression_binary_expression': expressionBinaryExpression,
  'expression_ternary_expression': expressionTernaryExpression,
  'expression_update_expression': expressionUpdateExpression,
  'expression_new_expression': expressionNewExpression,
  'expression_yield_expression': expressionYieldExpression,
  'expression': expression,
  'primary_expression_form_0': primaryExpressionForm0,
  'primary_expression_non_null_expression': primaryExpressionNonNullExpression,
  'primary_expression': primaryExpression,
  'yield_expression': yieldExpression,
  'object': object,
  'object_pattern': objectPattern,
  'assignment_pattern': assignmentPattern,
  'object_assignment_pattern': objectAssignmentPattern,
  'array': array,
  'array_pattern': arrayPattern,
  'jsx_element': jsxElement,
  'html_character_reference': htmlCharacterReference,
  'jsx_expression': jsxExpression,
  'jsx_opening_element': jsxOpeningElement,
  'jsx_identifier': jsxIdentifier,
  'nested_identifier': nestedIdentifier,
  'jsx_namespace_name': jsxNamespaceName,
  'jsx_closing_element': jsxClosingElement,
  'jsx_self_closing_element': jsxSelfClosingElement,
  'jsx_attribute': jsxAttribute,
  'unescaped_double_jsx_string_fragment': unescapedDoubleJsxStringFragment,
  'unescaped_single_jsx_string_fragment': unescapedSingleJsxStringFragment,
  'class': class_,
  'class_declaration': classDeclaration,
  'class_heritage_extends_clause': classHeritageExtendsClause,
  'class_heritage_implements_clause': classHeritageImplementsClause,
  'class_heritage': classHeritage,
  'function_expression': functionExpression,
  'function_declaration': functionDeclaration,
  'generator_function': generatorFunction,
  'generator_function_declaration': generatorFunctionDeclaration,
  'arrow_function': arrowFunction,
  'call_expression_function': callExpressionFunction,
  'call_expression_function2': callExpressionFunction2,
  'call_expression_tok_q_dot': callExpressionTokQDot,
  'call_expression': callExpression,
  'new_expression': newExpression,
  'await_expression': awaitExpression,
  'member_expression': memberExpression,
  'subscript_expression': subscriptExpression,
  'assignment_expression': assignmentExpression,
  'augmented_assignment_expression': augmentedAssignmentExpression,
  'spread_element': spreadElement,
  'ternary_expression': ternaryExpression,
  'binary_expression': binaryExpression,
  'unary_expression': unaryExpression,
  'update_expression': updateExpression,
  'sequence_expression': sequenceExpression,
  'string_tok_dq': stringTokDq,
  'string_tok_sq': stringTokSq,
  'string': string,
  'unescaped_double_string_fragment': unescapedDoubleStringFragment,
  'unescaped_single_string_fragment': unescapedSingleStringFragment,
  'escape_sequence': escapeSequence,
  'comment_slashslash': commentSlashslash,
  'comment_tok_slash_star': commentTokSlashStar,
  'comment': comment,
  'template_string': templateString,
  'template_substitution': templateSubstitution,
  'regex': regex,
  'regex_pattern': regexPattern,
  'regex_flags': regexFlags,
  'number_form_0': numberForm0,
  'number_form_1': numberForm1,
  'number_form_2': numberForm2,
  'number_form_3': numberForm3,
  'number_n': numberN,
  'number': number,
  'meta_property_new': metaPropertyNew,
  'meta_property_import': metaPropertyImport,
  'meta_property': metaProperty,
  'this': this_,
  'super': super_,
  'true': true_,
  'false': false_,
  'null': null_,
  'undefined': undefined_,
  'arguments': arguments_,
  'decorator': decorator,
  'decorator_member_expression': decoratorMemberExpression,
  'decorator_call_expression': decoratorCallExpression,
  'class_body': classBody,
  'field_definition': fieldDefinition,
  'formal_parameters': formalParameters,
  'class_static_block': classStaticBlock,
  'pattern__lhs_expression': patternULhsExpression,
  'pattern_rest_pattern': patternRestPattern,
  'pattern': pattern,
  'rest_pattern': restPattern,
  'method_definition': methodDefinition,
  'pair': pair,
  'pair_pattern': pairPattern,
  'computed_property_name': computedPropertyName,
  'public_field_definition': publicFieldDefinition,
  '_jsx_start_opening_element': hiddenJsxStartOpeningElement,
  'non_null_expression': nonNullExpression,
  'method_signature': methodSignature,
  'abstract_method_signature': abstractMethodSignature,
  'function_signature': functionSignature,
  'decorator_parenthesized_expression': decoratorParenthesizedExpression,
  'type_assertion': typeAssertion,
  'as_expression': asExpression,
  'satisfies_expression': satisfiesExpression,
  'instantiation_expression': instantiationExpression,
  'import_require_clause': importRequireClause,
  'extends_clause': extendsClause,
  'implements_clause': implementsClause,
  'ambient_declaration': ambientDeclaration,
  'abstract_class_declaration': abstractClassDeclaration,
  'module': module_,
  'internal_module': internalModule,
  'import_alias': importAlias,
  'nested_type_identifier': nestedTypeIdentifier,
  'interface_declaration': interfaceDeclaration,
  'extends_type_clause': extendsTypeClause,
  'enum_declaration': enumDeclaration,
  'enum_body': enumBody,
  'enum_assignment': enumAssignment,
  'type_alias_declaration': typeAliasDeclaration,
  'override_modifier': overrideModifier,
  'required_parameter': requiredParameter,
  'optional_parameter': optionalParameter,
  'omitting_type_annotation': omittingTypeAnnotation,
  'adding_type_annotation': addingTypeAnnotation,
  'opting_type_annotation': optingTypeAnnotation,
  'type_annotation': typeAnnotation,
  'asserts': asserts,
  'asserts_annotation': assertsAnnotation,
  'type_primary_type': typePrimaryType,
  'type_function_type': typeFunctionType,
  'type_readonly_type': typeReadonlyType,
  'type_constructor_type': typeConstructorType,
  'type_infer_type': typeInferType,
  'type__type_query_member_expression_in_type_annotation': typeUTypeQueryMemberExpressionInTypeAnnotation,
  'type__type_query_call_expression_in_type_annotation': typeUTypeQueryCallExpressionInTypeAnnotation,
  'type': type_,
  'tuple_parameter': tupleParameter,
  'optional_tuple_parameter': optionalTupleParameter,
  'optional_type': optionalType,
  'rest_type': restType,
  'constructor_type': constructorType,
  'primary_type_parenthesized_type': primaryTypeParenthesizedType,
  'primary_type_predefined_type': primaryTypePredefinedType,
  'primary_type__type_identifier': primaryTypeUTypeIdentifier,
  'primary_type_nested_type_identifier': primaryTypeNestedTypeIdentifier,
  'primary_type_generic_type': primaryTypeGenericType,
  'primary_type_object_type': primaryTypeObjectType,
  'primary_type_array_type': primaryTypeArrayType,
  'primary_type_tuple_type': primaryTypeTupleType,
  'primary_type_flow_maybe_type': primaryTypeFlowMaybeType,
  'primary_type_type_query': primaryTypeTypeQuery,
  'primary_type_index_type_query': primaryTypeIndexTypeQuery,
  'primary_type_this': primaryTypeThis,
  'primary_type_existential_type': primaryTypeExistentialType,
  'primary_type_literal_type': primaryTypeLiteralType,
  'primary_type_lookup_type': primaryTypeLookupType,
  'primary_type_conditional_type': primaryTypeConditionalType,
  'primary_type_template_literal_type': primaryTypeTemplateLiteralType,
  'primary_type_intersection_type': primaryTypeIntersectionType,
  'primary_type_union_type': primaryTypeUnionType,
  'primary_type_const': primaryTypeConst,
  'primary_type': primaryType,
  'template_type': templateType,
  'template_literal_type': templateLiteralType,
  'infer_type': inferType,
  'conditional_type': conditionalType,
  'generic_type': genericType,
  'type_predicate': typePredicate,
  'type_predicate_annotation': typePredicateAnnotation,
  'type_query': typeQuery,
  'index_type_query': indexTypeQuery,
  'lookup_type': lookupType,
  'mapped_type_clause': mappedTypeClause,
  'literal_type__number': literalTypeUNumber,
  'literal_type_number': literalTypeNumber,
  'literal_type_string': literalTypeString,
  'literal_type_true': literalTypeTrue,
  'literal_type_false': literalTypeFalse,
  'literal_type_null': literalTypeNull,
  'literal_type_undefined': literalTypeUndefined,
  'literal_type': literalType,
  'flow_maybe_type': flowMaybeType,
  'parenthesized_type': parenthesizedType,
  'predefined_type_any': predefinedTypeAny,
  'predefined_type_number': predefinedTypeNumber,
  'predefined_type_boolean': predefinedTypeBoolean,
  'predefined_type_string': predefinedTypeString,
  'predefined_type_symbol': predefinedTypeSymbol,
  'predefined_type_unique': predefinedTypeUnique,
  'predefined_type_void': predefinedTypeVoid,
  'predefined_type_unknown': predefinedTypeUnknown,
  'predefined_type_never': predefinedTypeNever,
  'predefined_type_object': predefinedTypeObject,
  'predefined_type': predefinedType,
  'type_arguments': typeArguments,
  'object_type': objectType,
  'call_signature': callSignature,
  'property_signature': propertySignature,
  'type_parameters': typeParameters,
  'type_parameter': typeParameter,
  'default_type': defaultType,
  'constraint': constraint,
  'construct_signature': constructSignature,
  'index_signature': indexSignature,
  'array_type': arrayType,
  'tuple_type': tupleType,
  'readonly_type': readonlyType,
  'union_type': unionType,
  'intersection_type': intersectionType,
  'function_type': functionType,
  '_type_identifier': hiddenTypeIdentifier,
  'shorthand_property_identifier_identifier': shorthandPropertyIdentifierIdentifier,
  'shorthand_property_identifier__reserved_identifier': shorthandPropertyIdentifierUReservedIdentifier,
  'shorthand_property_identifier': shorthandPropertyIdentifier,
  'shorthand_property_identifier_pattern_identifier': shorthandPropertyIdentifierPatternIdentifier,
  'shorthand_property_identifier_pattern__reserved_identifier': shorthandPropertyIdentifierPatternUReservedIdentifier,
  'shorthand_property_identifier_pattern': shorthandPropertyIdentifierPattern,
  'interface_body': interfaceBody,
  'this_type': thisType,
};

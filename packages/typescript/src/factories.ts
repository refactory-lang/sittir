// Auto-generated by @sittir/codegen — do not edit

import type * as T from './types.js';
import type { Edit, ByteRange, NonEmptyArray, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const RESERVED_KEYWORDS: ReadonlySet<string> = new Set([
  'abstract',
  'accessor',
  'as',
  'async',
  'await',
  'break',
  'case',
  'catch',
  'const',
  'continue',
  'debugger',
  'declare',
  'default',
  'do',
  'else',
  'enum',
  'export',
  'extends',
  'false',
  'finally',
  'for',
  'from',
  'function',
  'global',
  'if',
  'implements',
  'import',
  'in',
  'infer',
  'instanceof',
  'interface',
  'is',
  'keyof',
  'namespace',
  'new',
  'null',
  'override',
  'readonly',
  'require',
  'return',
  'satisfies',
  'static',
  'super',
  'switch',
  'this',
  'throw',
  'true',
  'try',
  'typeof',
  'undefined',
  'using',
  'var',
  'while',
  'with',
  'yield',
]);

const _leafRe_hashBangLine = /^(?:#!.*)/u;
const _leafRe_htmlCharacterReference = /^(?:&(#([xX][0-9a-fA-F]{1,6}|[0-9]{1,5})|[A-Za-z]{1,30});)/u;
const _leafRe_jsxIdentifier = /^(?:[a-zA-Z_$][a-zA-Z\d_$]*-[a-zA-Z\d_$\-]*)/u;
const _leafRe_unescapedDoubleJsxStringFragment = /^(?:([^"&]|&[^#A-Za-z])+)/u;
const _leafRe_unescapedSingleJsxStringFragment = /^(?:([^'&]|&[^#A-Za-z])+)/u;
const _leafRe_unescapedDoubleStringFragment = /^(?:[^"\\\r\n]+)/u;
const _leafRe_unescapedSingleStringFragment = /^(?:[^'\\\r\n]+)/u;
const _leafRe_regexFlags = /^(?:[a-z]+)/u;

export function program(config: T.ProgramConfig) {
  const fields = {
    hash_bang_line: config?.hashBangLine,
    statements: config?.statements,
  };
  return {
    type: 'program' as const,
    named: true as const,
    fields,
    hashBangLine(hashBangLine_?: T.HashBangLine | undefined) { return _fs(config, program, 'hashBangLine', hashBangLine_, fields.hash_bang_line); },
    statements(...statements_: T.Statement[]) { return _fsm(config, program, 'statements', statements_, fields.statements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ProgramTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hashBangLine(text: string) {
  if (text.length === 0) throw new Error(`hash_bang_line: text must be non-empty`); if (!_leafRe_hashBangLine.test(text)) throw new Error(`hash_bang_line: text does not match pattern: ${text}`);
  return {
    type: 'hash_bang_line' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HashBangLineTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function exportStatement(config: T.ExportStatementForm0Config | T.ExportStatementForm1Config | T.ExportStatementForm2Config | T.ExportStatementForm3Config) {
  if (config && 'decorator' in config && 'declaration' in config && 'value' in config) return exportStatementForm0(config as T.ExportStatementForm0Config);
  return exportStatementForm3(config as T.ExportStatementForm3Config);
}
export function exportStatementForm0(config: T.ExportStatementForm0Config) {
  const fields = {
    decorator: config?.decorator,
    declaration: config?.declaration,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'form0' as const,
    fields,
    children,
    decorator(...decorator_: T.Decorator[]) { return _fsm(config, exportStatementForm0, 'decorator', decorator_, fields.decorator); },
    declaration(declaration_?: T.Declaration | undefined) { return _fs(config, exportStatementForm0, 'declaration', declaration_, fields.declaration); },
    value(value_?: T.Expression | undefined) { return _fs(config, exportStatementForm0, 'value', value_, fields.value); },
    getChild() { return children[0]; },
    setChild(child: (T.FromClause | T.NamespaceExport | T.ExportClause | T.Semicolon)) { return exportStatementForm0({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementForm1(config: T.ExportStatementForm1Config) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'form1' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: (T.ExportClause | T.FromClause | T.Semicolon)) { return exportStatementForm1({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementForm1Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementForm2(config: T.ExportStatementForm2Config) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'form2' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: (T.Expression | T.Semicolon)) { return exportStatementForm2({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementForm2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function exportStatementForm3(config: T.ExportStatementForm3Config) {
  const children = config?.children ?? [];
  return {
    type: 'export_statement' as const,
    named: true as const,
    variant: 'form3' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: (T.Identifier | T.Semicolon)) { return exportStatementForm3({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportStatementForm3Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceExport(child: T.ModuleExportName) {
  const children = [child];
  return {
    type: 'namespace_export' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NamespaceExportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportClause(...children: T.ExportSpecifier[]) {
  return {
    type: 'export_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exportSpecifier(config: T.ExportSpecifierConfig) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'export_specifier' as const,
    named: true as const,
    fields,
    name(name_?: T.ModuleExportName) { return _fs(config, exportSpecifier, 'name', name_, fields.name); },
    alias(alias_?: T.ModuleExportName | undefined) { return _fs(config, exportSpecifier, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declaration(child?: (T.FunctionDeclaration | T.GeneratorFunctionDeclaration | T.ClassDeclaration | T.LexicalDeclaration | T.VariableDeclaration | T.FunctionSignature | T.AbstractClassDeclaration | T.Module | T.InternalModule | T.TypeAliasDeclaration | T.EnumDeclaration | T.InterfaceDeclaration | T.ImportAlias | T.AmbientDeclaration)) {
  const children = child != null ? [child] : [];
  return {
    type: 'declaration' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_() {
  return {
    type: 'import' as const,
    named: true as const,
    text: 'import' as const,
    render: () => 'import' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'import' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'import' as const },
    replace: (t: T.ImportTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'import' as const }; },
  };
}

export function importStatement(config: T.ImportStatementConfig) {
  const fields = {
    import: config?.import,
    import_clause: config?.importClause,
    from_clause: config?.fromClause,
    import_attribute: config?.importAttribute,
    semicolon: config?.semicolon,
  };
  return {
    type: 'import_statement' as const,
    named: true as const,
    fields,
    import(import_?: "import") { return _fs(config, importStatement, 'import', import_, fields.import); },
    importClause(importClause_?: "type" | "typeof" | undefined) { return _fs(config, importStatement, 'importClause', importClause_, fields.import_clause); },
    fromClause(fromClause_?: T.ImportClause | T.FromClause | T.ImportRequireClause | T.String) { return _fs(config, importStatement, 'fromClause', fromClause_, fields.from_clause); },
    importAttribute(importAttribute_?: T.ImportAttribute | undefined) { return _fs(config, importStatement, 'importAttribute', importAttribute_, fields.import_attribute); },
    semicolon(semicolon_?: T.Semicolon) { return _fs(config, importStatement, 'semicolon', semicolon_, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importClause(config: T.ImportClauseForm0Config | T.ImportClauseForm1Config | T.ImportClauseForm2Config) {
  if (config && 'defaultImport' in config && 'namedImports' in config) return importClauseForm2(config as T.ImportClauseForm2Config);
  return importClauseForm1(config as T.ImportClauseForm1Config);
}
export function importClauseForm0(config: T.ImportClauseForm0Config) {
  const children = config?.children ?? [];
  return {
    type: 'import_clause' as const,
    named: true as const,
    variant: 'form0' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.NamespaceImport) { return importClauseForm0({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseForm1(config: T.ImportClauseForm1Config) {
  const children = config?.children ?? [];
  return {
    type: 'import_clause' as const,
    named: true as const,
    variant: 'form1' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.NamedImports) { return importClauseForm1({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseForm1Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function importClauseForm2(config: T.ImportClauseForm2Config) {
  const fields = {
    default_import: config?.defaultImport,
    named_imports: config?.namedImports,
  };
  return {
    type: 'import_clause' as const,
    named: true as const,
    variant: 'form2' as const,
    fields,
    defaultImport(defaultImport_?: T.ImportIdentifier) { return _fs(config, importClauseForm2, 'defaultImport', defaultImport_, fields.default_import); },
    namedImports(namedImports_?: T.NamespaceImport | T.NamedImports | undefined) { return _fs(config, importClauseForm2, 'namedImports', namedImports_, fields.named_imports); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportClauseForm2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namespaceImport(config: T.NamespaceImportConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'namespace_import' as const,
    named: true as const,
    fields,
    identifier(identifier_?: T.Identifier) { return _fs(config, namespaceImport, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NamespaceImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedImports(...children: T.ImportSpecifier[]) {
  return {
    type: 'named_imports' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NamedImportsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importSpecifier(config: T.ImportSpecifierConfig) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'import_specifier' as const,
    named: true as const,
    fields,
    name(name_?: T.ImportIdentifier) { return _fs(config, importSpecifier, 'name', name_, fields.name); },
    alias(alias_?: T.ImportIdentifier | undefined) { return _fs(config, importSpecifier, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAttribute(config: T.ImportAttributeConfig) {
  const fields = {
    object: config?.object,
  };
  return {
    type: 'import_attribute' as const,
    named: true as const,
    fields,
    object(object_?: "with" | "assert") { return _fs(config, importAttribute, 'object', object_, fields.object); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statement(child?: (T.ExportStatement | T.ImportStatement | T.DebuggerStatement | T.ExpressionStatement | T.Declaration | T.StatementBlock | T.IfStatement | T.SwitchStatement | T.ForStatement | T.ForInStatement | T.WhileStatement | T.DoStatement | T.TryStatement | T.WithStatement | T.BreakStatement | T.ContinueStatement | T.ReturnStatement | T.ThrowStatement | T.EmptyStatement | T.LabeledStatement)) {
  const children = child != null ? [child] : [];
  return {
    type: 'statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child: (T.Expressions | T.Semicolon)) {
  const children = [child];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclaration(config: T.VariableDeclarationConfig) {
  const fields = {
    var: config?.var,
    declarators: config?.declarators,
    semicolon: config?.semicolon,
  };
  return {
    type: 'variable_declaration' as const,
    named: true as const,
    fields,
    var(var_?: "var") { return _fs(config, variableDeclaration, 'var', var_, fields.var); },
    declarators(...declarators_: NonEmptyArray<string>) { return _fsm(config, variableDeclaration, 'declarators', declarators_, fields.declarators); },
    semicolon(semicolon_?: T.Semicolon) { return _fs(config, variableDeclaration, 'semicolon', semicolon_, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VariableDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lexicalDeclaration(config: T.LexicalDeclarationConfig) {
  const fields = {
    kind: config?.kind,
    declarators: config?.declarators,
    semicolon: config?.semicolon,
  };
  return {
    type: 'lexical_declaration' as const,
    named: true as const,
    fields,
    kind(kind_?: "let" | "const") { return _fs(config, lexicalDeclaration, 'kind', kind_, fields.kind); },
    declarators(...declarators_: NonEmptyArray<string>) { return _fsm(config, lexicalDeclaration, 'declarators', declarators_, fields.declarators); },
    semicolon(semicolon_?: T.Semicolon) { return _fs(config, lexicalDeclaration, 'semicolon', semicolon_, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LexicalDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variableDeclarator(config: T.VariableDeclaratorConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'variable_declarator' as const,
    named: true as const,
    fields,
    children,
    name(name_?: T.Identifier | T.DestructuringPattern) { return _fs(config, variableDeclarator, 'name', name_, fields.name); },
    typeField(type?: T.TypeAnnotation | undefined) { return _fs(config, variableDeclarator, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: T.Initializer) { return variableDeclarator({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VariableDeclaratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function statementBlock(config: T.StatementBlockConfig) {
  const fields = {
    statements: config?.statements,
    automatic_semicolon: config?.automaticSemicolon,
  };
  return {
    type: 'statement_block' as const,
    named: true as const,
    fields,
    statements(...statements_: T.Statement[]) { return _fsm(config, statementBlock, 'statements', statements_, fields.statements); },
    automaticSemicolon(automaticSemicolon_?: T.AutomaticSemicolon | undefined) { return _fs(config, statementBlock, 'automaticSemicolon', automaticSemicolon_, fields.automatic_semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StatementBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: T.ElseClauseConfig) {
  const fields = {
    else: config?.else,
    statement: config?.statement,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    else(else_?: "else") { return _fs(config, elseClause, 'else', else_, fields.else); },
    statement(statement_?: T.Statement) { return _fs(config, elseClause, 'statement', statement_, fields.statement); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifStatement(config: T.IfStatementConfig) {
  const fields = {
    if: config?.if,
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    if(if_?: "if") { return _fs(config, ifStatement, 'if', if_, fields.if); },
    condition(condition_?: T.ParenthesizedExpression) { return _fs(config, ifStatement, 'condition', condition_, fields.condition); },
    consequence(consequence_?: T.Statement) { return _fs(config, ifStatement, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: T.ElseClause | undefined) { return _fs(config, ifStatement, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchStatement(config: T.SwitchStatementConfig) {
  const fields = {
    switch: config?.switch,
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'switch_statement' as const,
    named: true as const,
    fields,
    switch(switch_?: "switch") { return _fs(config, switchStatement, 'switch', switch_, fields.switch); },
    value(value_?: T.ParenthesizedExpression) { return _fs(config, switchStatement, 'value', value_, fields.value); },
    body(body_?: T.SwitchBody) { return _fs(config, switchStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SwitchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: T.ForStatementConfig) {
  const fields = {
    for: config?.for,
    initializer: config?.initializer,
    condition: config?.condition,
    increment: config?.increment,
    body: config?.body,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    for(for_?: "for") { return _fs(config, forStatement, 'for', for_, fields.for); },
    initializer(initializer_?: T.LexicalDeclaration | T.VariableDeclaration) { return _fs(config, forStatement, 'initializer', initializer_, fields.initializer); },
    condition(condition_?: T.Expressions | T.EmptyStatement) { return _fs(config, forStatement, 'condition', condition_, fields.condition); },
    increment(increment_?: T.Expressions | undefined) { return _fs(config, forStatement, 'increment', increment_, fields.increment); },
    body(body_?: T.Statement) { return _fs(config, forStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInStatement(config: T.ForInStatementConfig) {
  const fields = {
    for: config?.for,
    await: config?.await,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'for_in_statement' as const,
    named: true as const,
    fields,
    children,
    for(for_?: "for") { return _fs(config, forInStatement, 'for', for_, fields.for); },
    await(await_?: "await" | undefined) { return _fs(config, forInStatement, 'await', await_, fields.await); },
    body(body_?: T.Statement) { return _fs(config, forInStatement, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ForHeader) { return forInStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForInStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: T.WhileStatementConfig) {
  const fields = {
    while: config?.while,
    condition: config?.condition,
    body: config?.body,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    while(while_?: "while") { return _fs(config, whileStatement, 'while', while_, fields.while); },
    condition(condition_?: T.ParenthesizedExpression) { return _fs(config, whileStatement, 'condition', condition_, fields.condition); },
    body(body_?: T.Statement) { return _fs(config, whileStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function doStatement(config: T.DoStatementConfig) {
  const fields = {
    do: config?.do,
    body: config?.body,
    condition: config?.condition,
  };
  const children = config?.children ?? [];
  return {
    type: 'do_statement' as const,
    named: true as const,
    fields,
    children,
    do(do_?: "do") { return _fs(config, doStatement, 'do', do_, fields.do); },
    body(body_?: T.Statement) { return _fs(config, doStatement, 'body', body_, fields.body); },
    condition(condition_?: T.ParenthesizedExpression) { return _fs(config, doStatement, 'condition', condition_, fields.condition); },
    getChild() { return children[0]; },
    setChild(child: T.Semicolon) { return doStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DoStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: T.TryStatementConfig) {
  const fields = {
    try: config?.try,
    body: config?.body,
    handler: config?.handler,
    finalizer: config?.finalizer,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    try(try_?: "try") { return _fs(config, tryStatement, 'try', try_, fields.try); },
    body(body_?: T.StatementBlock) { return _fs(config, tryStatement, 'body', body_, fields.body); },
    handler(handler_?: T.CatchClause | undefined) { return _fs(config, tryStatement, 'handler', handler_, fields.handler); },
    finalizer(finalizer_?: T.FinallyClause | undefined) { return _fs(config, tryStatement, 'finalizer', finalizer_, fields.finalizer); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: T.WithStatementConfig) {
  const fields = {
    with: config?.with,
    object: config?.object,
    body: config?.body,
  };
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    with(with_?: "with") { return _fs(config, withStatement, 'with', with_, fields.with); },
    object(object_?: T.ParenthesizedExpression) { return _fs(config, withStatement, 'object', object_, fields.object); },
    body(body_?: T.Statement) { return _fs(config, withStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakStatement(config: T.BreakStatementConfig) {
  const fields = {
    break: config?.break,
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'break_statement' as const,
    named: true as const,
    fields,
    children,
    break(break_?: "break") { return _fs(config, breakStatement, 'break', break_, fields.break); },
    label(label_?: T.StatementIdentifier | undefined) { return _fs(config, breakStatement, 'label', label_, fields.label); },
    getChild() { return children[0]; },
    setChild(child: T.Semicolon) { return breakStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BreakStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueStatement(config: T.ContinueStatementConfig) {
  const fields = {
    continue: config?.continue,
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'continue_statement' as const,
    named: true as const,
    fields,
    children,
    continue(continue_?: "continue") { return _fs(config, continueStatement, 'continue', continue_, fields.continue); },
    label(label_?: T.StatementIdentifier | undefined) { return _fs(config, continueStatement, 'label', label_, fields.label); },
    getChild() { return children[0]; },
    setChild(child: T.Semicolon) { return continueStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ContinueStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function debuggerStatement(config: T.DebuggerStatementConfig) {
  const fields = {
    debugger: config?.debugger,
  };
  const children = config?.children ?? [];
  return {
    type: 'debugger_statement' as const,
    named: true as const,
    fields,
    children,
    debugger(debugger_?: "debugger") { return _fs(config, debuggerStatement, 'debugger', debugger_, fields.debugger); },
    getChild() { return children[0]; },
    setChild(child: T.Semicolon) { return debuggerStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DebuggerStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(config: T.ReturnStatementConfig) {
  const fields = {
    return: config?.return,
  };
  const children = config?.children ?? [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    fields,
    children,
    return(return_?: "return") { return _fs(config, returnStatement, 'return', return_, fields.return); },
    getChild() { return children[0]; },
    setChild(child: (T.Expressions | T.Semicolon)) { return returnStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function throwStatement(config: T.ThrowStatementConfig) {
  const fields = {
    throw: config?.throw,
  };
  const children = config?.children ?? [];
  return {
    type: 'throw_statement' as const,
    named: true as const,
    fields,
    children,
    throw(throw_?: "throw") { return _fs(config, throwStatement, 'throw', throw_, fields.throw); },
    getChild() { return children[0]; },
    setChild(child: (T.Expressions | T.Semicolon)) { return throwStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ThrowStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function labeledStatement(config: T.LabeledStatementConfig) {
  const fields = {
    label: config?.label,
    body: config?.body,
  };
  return {
    type: 'labeled_statement' as const,
    named: true as const,
    fields,
    label(label_?: T.StatementIdentifier) { return _fs(config, labeledStatement, 'label', label_, fields.label); },
    body(body_?: T.Statement) { return _fs(config, labeledStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LabeledStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchBody(...children: (T.SwitchCase | T.SwitchDefault)[]) {
  return {
    type: 'switch_body' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SwitchBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchCase(config: T.SwitchCaseConfig) {
  const fields = {
    case: config?.case,
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'switch_case' as const,
    named: true as const,
    fields,
    case(case_?: "case") { return _fs(config, switchCase, 'case', case_, fields.case); },
    value(value_?: T.Expressions) { return _fs(config, switchCase, 'value', value_, fields.value); },
    body(...body_: T.Statement[]) { return _fsm(config, switchCase, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SwitchCaseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function switchDefault(config: T.SwitchDefaultConfig) {
  const fields = {
    default: config?.default,
    body: config?.body,
  };
  return {
    type: 'switch_default' as const,
    named: true as const,
    fields,
    default(default_?: "default") { return _fs(config, switchDefault, 'default', default_, fields.default); },
    body(...body_: T.Statement[]) { return _fsm(config, switchDefault, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SwitchDefaultTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function catchClause(config: T.CatchClauseConfig) {
  const fields = {
    catch: config?.catch,
    parameter: config?.parameter,
    type: config?.type,
    body: config?.body,
  };
  return {
    type: 'catch_clause' as const,
    named: true as const,
    fields,
    catch(catch_?: "catch") { return _fs(config, catchClause, 'catch', catch_, fields.catch); },
    parameter(parameter_?: T.Identifier | T.DestructuringPattern | undefined) { return _fs(config, catchClause, 'parameter', parameter_, fields.parameter); },
    typeField(type?: T.TypeAnnotation | undefined) { return _fs(config, catchClause, 'type', type, fields.type); },
    body(body_?: T.StatementBlock) { return _fs(config, catchClause, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CatchClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: T.FinallyClauseConfig) {
  const fields = {
    finally: config?.finally,
    body: config?.body,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    finally(finally_?: "finally") { return _fs(config, finallyClause, 'finally', finally_, fields.finally); },
    body(body_?: T.StatementBlock) { return _fs(config, finallyClause, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(config?: T.ParenthesizedExpressionConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: T.TypeAnnotation | undefined) { return _fs(config, parenthesizedExpression, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (T.Expression | T.SequenceExpression)) { return parenthesizedExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expression(child?: (T.AsExpression | T.SatisfiesExpression | T.InstantiationExpression | T.InternalModule | T.PrimaryExpression | T._JsxElement | T.AssignmentExpression | T.AugmentedAssignmentExpression | T.AwaitExpression | T.UnaryExpression | T.BinaryExpression | T.TernaryExpression | T.UpdateExpression | T.NewExpression | T.YieldExpression)) {
  const children = child != null ? [child] : [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function primaryExpression(child?: (T.SubscriptExpression | T.MemberExpression | T.ParenthesizedExpression | T._Identifier | T.This | T.Super | T.Number | T.String | T.TemplateString | T.Regex | T.True | T.False | T.Null | T.Object | T.Array | T.FunctionExpression | T.ArrowFunction | T.GeneratorFunction | T.Class | T.MetaProperty | T.CallExpression | T.NonNullExpression)) {
  const children = child != null ? [child] : [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(config: T.YieldExpressionConfig) {
  const fields = {
    yield: config?.yield,
    expression: config?.expression,
  };
  return {
    type: 'yield_expression' as const,
    named: true as const,
    fields,
    yield(yield_?: "yield") { return _fs(config, yieldExpression, 'yield', yield_, fields.yield); },
    expression(expression_?: T.Expression) { return _fs(config, yieldExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function object(...children: (T.Pair | T.SpreadElement | T.MethodDefinition | T.ShorthandPropertyIdentifier)[]) {
  return {
    type: 'object' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectPattern(...children: (T.PairPattern | T.RestPattern | T.ObjectAssignmentPattern | T.ShorthandPropertyIdentifierPattern)[]) {
  return {
    type: 'object_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentPattern(config: T.AssignmentPatternConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_pattern' as const,
    named: true as const,
    fields,
    left(left_?: T.Pattern) { return _fs(config, assignmentPattern, 'left', left_, fields.left); },
    right(right_?: T.Expression) { return _fs(config, assignmentPattern, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectAssignmentPattern(config: T.ObjectAssignmentPatternConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'object_assignment_pattern' as const,
    named: true as const,
    fields,
    left(left_?: T.ShorthandPropertyIdentifierPattern | T.DestructuringPattern) { return _fs(config, objectAssignmentPattern, 'left', left_, fields.left); },
    right(right_?: T.Expression) { return _fs(config, objectAssignmentPattern, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectAssignmentPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function array(...children: (T.Expression | T.SpreadElement)[]) {
  return {
    type: 'array' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayPattern(...children: (T.Pattern | T.AssignmentPattern)[]) {
  return {
    type: 'array_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxElement(config: T.JsxElementConfig) {
  const fields = {
    open_tag: config?.openTag,
    close_tag: config?.closeTag,
  };
  const children = config?.children ?? [];
  return {
    type: 'jsx_element' as const,
    named: true as const,
    fields,
    children,
    openTag(openTag_?: T.JsxOpeningElement) { return _fs(config, jsxElement, 'openTag', openTag_, fields.open_tag); },
    closeTag(closeTag_?: T.JsxClosingElement) { return _fs(config, jsxElement, 'closeTag', closeTag_, fields.close_tag); },
    getChildren() { return children; },
    setChildren(...items: T.JsxChild[]) { return jsxElement({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlCharacterReference(text: string) {
  if (text.length === 0) throw new Error(`html_character_reference: text must be non-empty`); if (!_leafRe_htmlCharacterReference.test(text)) throw new Error(`html_character_reference: text does not match pattern: ${text}`);
  return {
    type: 'html_character_reference' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HtmlCharacterReferenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxExpression(child?: (T.Expression | T.SequenceExpression | T.SpreadElement)) {
  const children = child != null ? [child] : [];
  return {
    type: 'jsx_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxOpeningElement(child: T.JsxStartOpeningElement) {
  const children = [child];
  return {
    type: 'jsx_opening_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxOpeningElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxIdentifier(text: string) {
  if (text.length === 0) throw new Error(`jsx_identifier: text must be non-empty`); if (!_leafRe_jsxIdentifier.test(text)) throw new Error(`jsx_identifier: text does not match pattern: ${text}`);
  return {
    type: 'jsx_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.JsxIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function nestedIdentifier(config: T.NestedIdentifierConfig) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'nested_identifier' as const,
    named: true as const,
    fields,
    object(object_?: T.Identifier | T.MemberExpression) { return _fs(config, nestedIdentifier, 'object', object_, fields.object); },
    property(property_?: T.PropertyIdentifier) { return _fs(config, nestedIdentifier, 'property', property_, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NestedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxNamespaceName(...children: T._JsxIdentifier[]) {
  return {
    type: 'jsx_namespace_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxNamespaceNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxClosingElement(config?: T.JsxClosingElementConfig) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'jsx_closing_element' as const,
    named: true as const,
    fields,
    name(name_?: T.JsxElementName | undefined) { return _fs(config, jsxClosingElement, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxSelfClosingElement(child: T.JsxStartOpeningElement) {
  const children = [child];
  return {
    type: 'jsx_self_closing_element' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxSelfClosingElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function jsxAttribute(child: (T.JsxAttributeName | T.JsxAttributeValue)) {
  const children = [child];
  return {
    type: 'jsx_attribute' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.JsxAttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleJsxStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_double_jsx_string_fragment: text must be non-empty`); if (!_leafRe_unescapedDoubleJsxStringFragment.test(text)) throw new Error(`unescaped_double_jsx_string_fragment: text does not match pattern: ${text}`);
  return {
    type: 'unescaped_double_jsx_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedDoubleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleJsxStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_single_jsx_string_fragment: text must be non-empty`); if (!_leafRe_unescapedSingleJsxStringFragment.test(text)) throw new Error(`unescaped_single_jsx_string_fragment: text does not match pattern: ${text}`);
  return {
    type: 'unescaped_single_jsx_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedSingleJsxStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function class_(config: T.ClassConfig) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    type_parameters: config?.typeParameters,
    class_heritage: config?.classHeritage,
    body: config?.body,
  };
  return {
    type: 'class' as const,
    named: true as const,
    fields,
    decorator(...decorator_: T.Decorator[]) { return _fsm(config, class_, 'decorator', decorator_, fields.decorator); },
    name(name_?: T._TypeIdentifier | undefined) { return _fs(config, class_, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, class_, 'typeParameters', typeParameters_, fields.type_parameters); },
    classHeritage(classHeritage_?: T.ClassHeritage | undefined) { return _fs(config, class_, 'classHeritage', classHeritage_, fields.class_heritage); },
    body(body_?: T.ClassBody) { return _fs(config, class_, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDeclaration(config: T.ClassDeclarationConfig) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    type_parameters: config?.typeParameters,
    class_heritage: config?.classHeritage,
    body: config?.body,
    automatic_semicolon: config?.automaticSemicolon,
  };
  return {
    type: 'class_declaration' as const,
    named: true as const,
    fields,
    decorator(...decorator_: T.Decorator[]) { return _fsm(config, classDeclaration, 'decorator', decorator_, fields.decorator); },
    name(name_?: T._TypeIdentifier) { return _fs(config, classDeclaration, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, classDeclaration, 'typeParameters', typeParameters_, fields.type_parameters); },
    classHeritage(classHeritage_?: T.ClassHeritage | undefined) { return _fs(config, classDeclaration, 'classHeritage', classHeritage_, fields.class_heritage); },
    body(body_?: T.ClassBody) { return _fs(config, classDeclaration, 'body', body_, fields.body); },
    automaticSemicolon(automaticSemicolon_?: T.AutomaticSemicolon | undefined) { return _fs(config, classDeclaration, 'automaticSemicolon', automaticSemicolon_, fields.automatic_semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classHeritage(config: T.ClassHeritageForm0Config | T.ClassHeritageForm1Config) {
  if (config && 'extendsClause' in config && 'implementsClause' in config) return classHeritageForm0(config as T.ClassHeritageForm0Config);
  return classHeritageForm1(config as T.ClassHeritageForm1Config);
}
export function classHeritageForm0(config: T.ClassHeritageForm0Config) {
  const fields = {
    extends_clause: config?.extendsClause,
    implements_clause: config?.implementsClause,
  };
  return {
    type: 'class_heritage' as const,
    named: true as const,
    variant: 'form0' as const,
    fields,
    extendsClause(extendsClause_?: T.ExtendsClause) { return _fs(config, classHeritageForm0, 'extendsClause', extendsClause_, fields.extends_clause); },
    implementsClause(implementsClause_?: T.ImplementsClause | undefined) { return _fs(config, classHeritageForm0, 'implementsClause', implementsClause_, fields.implements_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassHeritageForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function classHeritageForm1(config: T.ClassHeritageForm1Config) {
  const children = config?.children ?? [];
  return {
    type: 'class_heritage' as const,
    named: true as const,
    variant: 'form1' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.ImplementsClause) { return classHeritageForm1({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassHeritageForm1Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionExpression(config: T.FunctionExpressionConfig) {
  const fields = {
    async: config?.async,
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_expression' as const,
    named: true as const,
    fields,
    children,
    async(async_?: "async" | undefined) { return _fs(config, functionExpression, 'async', async_, fields.async); },
    name(name_?: T.Identifier | undefined) { return _fs(config, functionExpression, 'name', name_, fields.name); },
    body(body_?: T.StatementBlock) { return _fs(config, functionExpression, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T._CallSignature) { return functionExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDeclaration(config: T.FunctionDeclarationConfig) {
  const fields = {
    async: config?.async,
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_declaration' as const,
    named: true as const,
    fields,
    children,
    async(async_?: "async" | undefined) { return _fs(config, functionDeclaration, 'async', async_, fields.async); },
    name(name_?: T.Identifier) { return _fs(config, functionDeclaration, 'name', name_, fields.name); },
    body(body_?: T.StatementBlock) { return _fs(config, functionDeclaration, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (T._CallSignature | T.AutomaticSemicolon)) { return functionDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunction(config: T.GeneratorFunctionConfig) {
  const fields = {
    async: config?.async,
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_function' as const,
    named: true as const,
    fields,
    children,
    async(async_?: "async" | undefined) { return _fs(config, generatorFunction, 'async', async_, fields.async); },
    name(name_?: T.Identifier | undefined) { return _fs(config, generatorFunction, 'name', name_, fields.name); },
    body(body_?: T.StatementBlock) { return _fs(config, generatorFunction, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T._CallSignature) { return generatorFunction({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GeneratorFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorFunctionDeclaration(config: T.GeneratorFunctionDeclarationConfig) {
  const fields = {
    async: config?.async,
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_function_declaration' as const,
    named: true as const,
    fields,
    children,
    async(async_?: "async" | undefined) { return _fs(config, generatorFunctionDeclaration, 'async', async_, fields.async); },
    name(name_?: T.Identifier) { return _fs(config, generatorFunctionDeclaration, 'name', name_, fields.name); },
    body(body_?: T.StatementBlock) { return _fs(config, generatorFunctionDeclaration, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (T._CallSignature | T.AutomaticSemicolon)) { return generatorFunctionDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GeneratorFunctionDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrowFunction(config: T.ArrowFunctionConfig) {
  const fields = {
    async: config?.async,
    parameter: config?.parameter,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'arrow_function' as const,
    named: true as const,
    fields,
    children,
    async(async_?: "async" | undefined) { return _fs(config, arrowFunction, 'async', async_, fields.async); },
    parameter(parameter_?: T.Identifier | undefined) { return _fs(config, arrowFunction, 'parameter', parameter_, fields.parameter); },
    body(body_?: T.Expression | T.StatementBlock) { return _fs(config, arrowFunction, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T._CallSignature) { return arrowFunction({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrowFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: T.CallExpressionForm0Config | T.CallExpressionForm1Config | T.CallExpressionForm2Config) {
  if (config && 'function' in config && 'typeArguments' in config && 'arguments' in config) return callExpressionForm0(config as T.CallExpressionForm0Config);
  return callExpressionForm1(config as T.CallExpressionForm1Config);
}
export function callExpressionForm0(config: T.CallExpressionForm0Config) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'form0' as const,
    fields,
    function(function_?: T.Expression | T.Import) { return _fs(config, callExpressionForm0, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: T.TypeArguments | undefined) { return _fs(config, callExpressionForm0, 'typeArguments', typeArguments_, fields.type_arguments); },
    arguments(arguments_?: T.Arguments) { return _fs(config, callExpressionForm0, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallExpressionForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionForm1(config: T.CallExpressionForm1Config) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'form1' as const,
    fields,
    function(function_?: T.PrimaryExpression | T.NewExpression) { return _fs(config, callExpressionForm1, 'function', function_, fields.function); },
    arguments(arguments_?: T.TemplateString) { return _fs(config, callExpressionForm1, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallExpressionForm1Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function callExpressionForm2(config: T.CallExpressionForm2Config) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    variant: 'form2' as const,
    fields,
    function(function_?: T.PrimaryExpression) { return _fs(config, callExpressionForm2, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: T.TypeArguments | undefined) { return _fs(config, callExpressionForm2, 'typeArguments', typeArguments_, fields.type_arguments); },
    arguments(arguments_?: T.Arguments) { return _fs(config, callExpressionForm2, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallExpressionForm2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function newExpression(config: T.NewExpressionConfig) {
  const fields = {
    new: config?.new,
    constructor: config?.constructor,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'new_expression' as const,
    named: true as const,
    fields,
    new(new_?: "new") { return _fs(config, newExpression, 'new', new_, fields.new); },
    constructor(constructor_?: T.PrimaryExpression) { return _fs(config, newExpression, 'constructor', constructor_, fields.constructor); },
    typeArguments(typeArguments_?: T.TypeArguments | undefined) { return _fs(config, newExpression, 'typeArguments', typeArguments_, fields.type_arguments); },
    arguments(arguments_?: T.Arguments | undefined) { return _fs(config, newExpression, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NewExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(config: T.AwaitExpressionConfig) {
  const fields = {
    await: config?.await,
    expression: config?.expression,
  };
  return {
    type: 'await_expression' as const,
    named: true as const,
    fields,
    await(await_?: "await") { return _fs(config, awaitExpression, 'await', await_, fields.await); },
    expression(expression_?: T.Expression) { return _fs(config, awaitExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberExpression(config: T.MemberExpressionConfig) {
  const fields = {
    object: config?.object,
    optional_chain: config?.optionalChain,
    property: config?.property,
  };
  return {
    type: 'member_expression' as const,
    named: true as const,
    fields,
    object(object_?: T.Expression | T.PrimaryExpression | T.Import) { return _fs(config, memberExpression, 'object', object_, fields.object); },
    optionalChain(optionalChain_?: T.OptionalChain | undefined) { return _fs(config, memberExpression, 'optionalChain', optionalChain_, fields.optional_chain); },
    property(property_?: T.PrivatePropertyIdentifier | T.PropertyIdentifier) { return _fs(config, memberExpression, 'property', property_, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscriptExpression(config: T.SubscriptExpressionConfig) {
  const fields = {
    object: config?.object,
    optional_chain: config?.optionalChain,
    index: config?.index,
  };
  return {
    type: 'subscript_expression' as const,
    named: true as const,
    fields,
    object(object_?: T.Expression | T.PrimaryExpression) { return _fs(config, subscriptExpression, 'object', object_, fields.object); },
    optionalChain(optionalChain_?: T.OptionalChain | undefined) { return _fs(config, subscriptExpression, 'optionalChain', optionalChain_, fields.optional_chain); },
    index(index_?: T.Expressions) { return _fs(config, subscriptExpression, 'index', index_, fields.index); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SubscriptExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: T.AssignmentExpressionConfig) {
  const fields = {
    using: config?.using,
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    using(using_?: "using" | undefined) { return _fs(config, assignmentExpression, 'using', using_, fields.using); },
    left(left_?: T.ParenthesizedExpression | T.LhsExpression) { return _fs(config, assignmentExpression, 'left', left_, fields.left); },
    right(right_?: T.Expression) { return _fs(config, assignmentExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignmentExpression(config: T.AugmentedAssignmentExpressionConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'augmented_assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: T.MemberExpression | T.SubscriptExpression | T.Identifier | T.ParenthesizedExpression | T.NonNullExpression) { return _fs(config, augmentedAssignmentExpression, 'left', left_, fields.left); },
    operator(operator_?: "+=" | "-=" | "*=" | "/=" | "%=" | "^=" | "&=" | "|=" | ">>=" | ">>>=" | "<<=" | "**=" | "&&=" | "||=" | "??=") { return _fs(config, augmentedAssignmentExpression, 'operator', operator_, fields.operator); },
    right(right_?: T.Expression) { return _fs(config, augmentedAssignmentExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AugmentedAssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function spreadElement(config: T.SpreadElementConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'spread_element' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, spreadElement, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SpreadElementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ternaryExpression(config: T.TernaryExpressionConfig) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'ternary_expression' as const,
    named: true as const,
    fields,
    condition(condition_?: T.Expression) { return _fs(config, ternaryExpression, 'condition', condition_, fields.condition); },
    consequence(consequence_?: T.Expression) { return _fs(config, ternaryExpression, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: T.Expression) { return _fs(config, ternaryExpression, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TernaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: T.BinaryExpressionConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(left_?: T.Expression) { return _fs(config, binaryExpression, 'left', left_, fields.left); },
    operator(operator_?: "&&") { return _fs(config, binaryExpression, 'operator', operator_, fields.operator); },
    right(right_?: T.Expression) { return _fs(config, binaryExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: T.UnaryExpressionConfig) {
  const fields = {
    operator: config?.operator,
    argument: config?.argument,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    operator(operator_?: "!" | "~" | "-" | "+" | "typeof" | "void" | "delete") { return _fs(config, unaryExpression, 'operator', operator_, fields.operator); },
    argument(argument_?: T.Expression) { return _fs(config, unaryExpression, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function updateExpression(config: T.UpdateExpressionConfig) {
  const fields = {
    argument: config?.argument,
    operator: config?.operator,
  };
  return {
    type: 'update_expression' as const,
    named: true as const,
    fields,
    argument(argument_?: T.Expression) { return _fs(config, updateExpression, 'argument', argument_, fields.argument); },
    operator(operator_?: "++" | "--") { return _fs(config, updateExpression, 'operator', operator_, fields.operator); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UpdateExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function sequenceExpression(...children: T.Expression[]) {
  _assertNonEmpty(children, 'sequence_expression.children');
  return {
    type: 'sequence_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SequenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(...children: (T.StringFragment | T.EscapeSequence)[]) {
  return {
    type: 'string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unescapedDoubleStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_double_string_fragment: text must be non-empty`); if (!_leafRe_unescapedDoubleStringFragment.test(text)) throw new Error(`unescaped_double_string_fragment: text does not match pattern: ${text}`);
  return {
    type: 'unescaped_double_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedDoubleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function unescapedSingleStringFragment(text: string) {
  if (text.length === 0) throw new Error(`unescaped_single_string_fragment: text must be non-empty`); if (!_leafRe_unescapedSingleStringFragment.test(text)) throw new Error(`unescaped_single_string_fragment: text does not match pattern: ${text}`);
  return {
    type: 'unescaped_single_string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnescapedSingleStringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(text: string) {
  if (text.length === 0) throw new Error(`comment: text must be non-empty`);
  return {
    type: 'comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function templateString(...children: (T.StringFragment | T.EscapeSequence | T.TemplateSubstitution)[]) {
  return {
    type: 'template_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TemplateStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateSubstitution(child: T.Expressions) {
  const children = [child];
  return {
    type: 'template_substitution' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TemplateSubstitutionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regex(config: T.RegexConfig) {
  const fields = {
    pattern: config?.pattern,
    flags: config?.flags,
  };
  return {
    type: 'regex' as const,
    named: true as const,
    fields,
    pattern(pattern_?: T.RegexPattern) { return _fs(config, regex, 'pattern', pattern_, fields.pattern); },
    flags(flags_?: T.RegexFlags | undefined) { return _fs(config, regex, 'flags', flags_, fields.flags); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RegexTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function regexPattern(text: string) {
  if (text.length === 0) throw new Error(`regex_pattern: text must be non-empty`);
  return {
    type: 'regex_pattern' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.RegexPatternTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function regexFlags(text: string) {
  if (text.length === 0) throw new Error(`regex_flags: text must be non-empty`); if (!_leafRe_regexFlags.test(text)) throw new Error(`regex_flags: text does not match pattern: ${text}`);
  return {
    type: 'regex_flags' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.RegexFlagsTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function number(text: string) {
  if (text.length === 0) throw new Error(`number: text must be non-empty`);
  return {
    type: 'number' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.NumberTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function privatePropertyIdentifier(text: string) {
  if (text.length === 0) throw new Error(`private_property_identifier: text must be non-empty`);
  return {
    type: 'private_property_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PrivatePropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function metaProperty(text: string) {
  if (text.length === 0) throw new Error(`meta_property: text must be non-empty`);
  return {
    type: 'meta_property' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.MetaPropertyTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function this_() {
  return {
    type: 'this' as const,
    named: true as const,
    text: 'this' as const,
    render: () => 'this' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' as const },
    replace: (t: T.ThisTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' as const }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: T.SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'true' as const,
    render: () => 'true' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'true' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'true' as const },
    replace: (t: T.TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'true' as const }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'false' as const,
    render: () => 'false' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'false' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'false' as const },
    replace: (t: T.FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'false' as const }; },
  };
}

export function null_() {
  return {
    type: 'null' as const,
    named: true as const,
    text: 'null' as const,
    render: () => 'null' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'null' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'null' as const },
    replace: (t: T.NullTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'null' as const }; },
  };
}

export function undefined_() {
  return {
    type: 'undefined' as const,
    named: true as const,
    text: 'undefined' as const,
    render: () => 'undefined' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'undefined' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'undefined' as const },
    replace: (t: T.UndefinedTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'undefined' as const }; },
  };
}

export function arguments_(...children: (T.Expression | T.SpreadElement)[]) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(child?: (T.Identifier | T.MemberExpression | T.CallExpression | T.ParenthesizedExpression)) {
  const children = child != null ? [child] : [];
  return {
    type: 'decorator' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorMemberExpression(config: T.DecoratorMemberExpressionConfig) {
  const fields = {
    object: config?.object,
    property: config?.property,
  };
  return {
    type: 'decorator_member_expression' as const,
    named: true as const,
    fields,
    object(object_?: T.Identifier | T.MemberExpression) { return _fs(config, decoratorMemberExpression, 'object', object_, fields.object); },
    property(property_?: T.PropertyIdentifier) { return _fs(config, decoratorMemberExpression, 'property', property_, fields.property); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorMemberExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorCallExpression(config: T.DecoratorCallExpressionConfig) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
    arguments: config?.arguments,
  };
  return {
    type: 'decorator_call_expression' as const,
    named: true as const,
    fields,
    function(function_?: T.Identifier | T.MemberExpression) { return _fs(config, decoratorCallExpression, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: T.TypeArguments | undefined) { return _fs(config, decoratorCallExpression, 'typeArguments', typeArguments_, fields.type_arguments); },
    arguments(arguments_?: T.Arguments) { return _fs(config, decoratorCallExpression, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorCallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classBody(config?: T.ClassBodyConfig) {
  const fields = {
    decorator: config?.decorator,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_body' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: T.Decorator[]) { return _fsm(config, classBody, 'decorator', decorator_, fields.decorator); },
    getChildren() { return children; },
    setChildren(...items: (T.MethodDefinition | T.Semicolon | T.MethodSignature | T.FunctionSignatureAutomaticSemicolon | T.ClassStaticBlock | T.AbstractMethodSignature | T.IndexSignature | T.PublicFieldDefinition)[]) { return classBody({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDefinition(config: T.FieldDefinitionConfig) {
  const fields = {
    decorator: config?.decorator,
    static: config?.static,
    property: config?.property,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_definition' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: T.Decorator[]) { return _fsm(config, fieldDefinition, 'decorator', decorator_, fields.decorator); },
    static(static_?: "static" | undefined) { return _fs(config, fieldDefinition, 'static', static_, fields.static); },
    property(property_?: T.PropertyName) { return _fs(config, fieldDefinition, 'property', property_, fields.property); },
    getChild() { return children[0]; },
    setChild(child: T.Initializer) { return fieldDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formalParameters(...children: T.FormalParameter[]) {
  return {
    type: 'formal_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FormalParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classStaticBlock(config: T.ClassStaticBlockConfig) {
  const fields = {
    static: config?.static,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'class_static_block' as const,
    named: true as const,
    fields,
    children,
    static(static_?: "static") { return _fs(config, classStaticBlock, 'static', static_, fields.static); },
    body(body_?: T.StatementBlock) { return _fs(config, classStaticBlock, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.AutomaticSemicolon) { return classStaticBlock({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassStaticBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pattern(child?: (T.LhsExpression | T.RestPattern)) {
  const children = child != null ? [child] : [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restPattern(child: T.LhsExpression) {
  const children = [child];
  return {
    type: 'rest_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RestPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodDefinition(config: T.MethodDefinitionConfig) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    readonly: config?.readonly,
    async: config?.async,
    name: config?.name,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'method_definition' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: T.AccessibilityModifier | undefined) { return _fs(config, methodDefinition, 'accessibilityModifier', accessibilityModifier_, fields.accessibility_modifier); },
    overrideModifier(overrideModifier_?: "static" | undefined) { return _fs(config, methodDefinition, 'overrideModifier', overrideModifier_, fields.override_modifier); },
    readonly(readonly_?: "readonly" | undefined) { return _fs(config, methodDefinition, 'readonly', readonly_, fields.readonly); },
    async(async_?: "async" | undefined) { return _fs(config, methodDefinition, 'async', async_, fields.async); },
    name(name_?: T.PropertyName) { return _fs(config, methodDefinition, 'name', name_, fields.name); },
    body(body_?: T.StatementBlock) { return _fs(config, methodDefinition, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (T.OverrideModifier | T._CallSignature)) { return methodDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MethodDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: T.PairConfig) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key_?: T.PropertyName) { return _fs(config, pair, 'key', key_, fields.key); },
    value(value_?: T.Expression) { return _fs(config, pair, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pairPattern(config: T.PairPatternConfig) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair_pattern' as const,
    named: true as const,
    fields,
    key(key_?: T.PropertyName) { return _fs(config, pairPattern, 'key', key_, fields.key); },
    value(value_?: T.Pattern | T.AssignmentPattern) { return _fs(config, pairPattern, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PairPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function computedPropertyName(config: T.ComputedPropertyNameConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'computed_property_name' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, computedPropertyName, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ComputedPropertyNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function publicFieldDefinition(config: T.PublicFieldDefinitionConfig) {
  const fields = {
    decorator: config?.decorator,
    declare: config?.declare,
    static: config?.static,
    readonly: config?.readonly,
    abstract: config?.abstract,
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'public_field_definition' as const,
    named: true as const,
    fields,
    children,
    decorator(...decorator_: T.Decorator[]) { return _fsm(config, publicFieldDefinition, 'decorator', decorator_, fields.decorator); },
    declare(declare_?: "declare" | undefined) { return _fs(config, publicFieldDefinition, 'declare', declare_, fields.declare); },
    static(static_?: "static" | undefined) { return _fs(config, publicFieldDefinition, 'static', static_, fields.static); },
    readonly(readonly_?: "readonly" | undefined) { return _fs(config, publicFieldDefinition, 'readonly', readonly_, fields.readonly); },
    abstract(abstract_?: "abstract" | undefined) { return _fs(config, publicFieldDefinition, 'abstract', abstract_, fields.abstract); },
    name(name_?: T.PropertyName) { return _fs(config, publicFieldDefinition, 'name', name_, fields.name); },
    typeField(type?: T.TypeAnnotation | undefined) { return _fs(config, publicFieldDefinition, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (T.AccessibilityModifier | T.OverrideModifier | T.Initializer)) { return publicFieldDefinition({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PublicFieldDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonNullExpression(config: T.NonNullExpressionConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'non_null_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, nonNullExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NonNullExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function methodSignature(config: T.MethodSignatureConfig) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    readonly: config?.readonly,
    async: config?.async,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: T.AccessibilityModifier | undefined) { return _fs(config, methodSignature, 'accessibilityModifier', accessibilityModifier_, fields.accessibility_modifier); },
    overrideModifier(overrideModifier_?: "static" | undefined) { return _fs(config, methodSignature, 'overrideModifier', overrideModifier_, fields.override_modifier); },
    readonly(readonly_?: "readonly" | undefined) { return _fs(config, methodSignature, 'readonly', readonly_, fields.readonly); },
    async(async_?: "async" | undefined) { return _fs(config, methodSignature, 'async', async_, fields.async); },
    name(name_?: T.PropertyName) { return _fs(config, methodSignature, 'name', name_, fields.name); },
    getChild() { return children[0]; },
    setChild(child: (T.OverrideModifier | T._CallSignature)) { return methodSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractMethodSignature(config: T.AbstractMethodSignatureConfig) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'abstract_method_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: T.AccessibilityModifier | undefined) { return _fs(config, abstractMethodSignature, 'accessibilityModifier', accessibilityModifier_, fields.accessibility_modifier); },
    overrideModifier(overrideModifier_?: T.OverrideModifier | undefined) { return _fs(config, abstractMethodSignature, 'overrideModifier', overrideModifier_, fields.override_modifier); },
    name(name_?: T.PropertyName) { return _fs(config, abstractMethodSignature, 'name', name_, fields.name); },
    getChild() { return children[0]; },
    setChild(child: T._CallSignature) { return abstractMethodSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AbstractMethodSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignature(config: T.FunctionSignatureConfig) {
  const fields = {
    async: config?.async,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_signature' as const,
    named: true as const,
    fields,
    children,
    async(async_?: "async" | undefined) { return _fs(config, functionSignature, 'async', async_, fields.async); },
    name(name_?: T.Identifier) { return _fs(config, functionSignature, 'name', name_, fields.name); },
    getChild() { return children[0]; },
    setChild(child: (T._CallSignature | T.Semicolon | T.FunctionSignatureAutomaticSemicolon)) { return functionSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratorParenthesizedExpression(child?: (T.Identifier | T.MemberExpression | T.CallExpression)) {
  const children = child != null ? [child] : [];
  return {
    type: 'decorator_parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAssertion(config: T.TypeAssertionConfig) {
  const fields = {
    type_arguments: config?.typeArguments,
    expression: config?.expression,
  };
  return {
    type: 'type_assertion' as const,
    named: true as const,
    fields,
    typeArguments(typeArguments_?: T.TypeArguments) { return _fs(config, typeAssertion, 'typeArguments', typeArguments_, fields.type_arguments); },
    expression(expression_?: T.Expression) { return _fs(config, typeAssertion, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeAssertionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asExpression(config: T.AsExpressionConfig) {
  const fields = {
    expression: config?.expression,
    type_annotation: config?.typeAnnotation,
  };
  return {
    type: 'as_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, asExpression, 'expression', expression_, fields.expression); },
    typeAnnotation(typeAnnotation_?: T.Type) { return _fs(config, asExpression, 'typeAnnotation', typeAnnotation_, fields.type_annotation); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AsExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function satisfiesExpression(config: T.SatisfiesExpressionConfig) {
  const fields = {
    expression: config?.expression,
    type_annotation: config?.typeAnnotation,
  };
  return {
    type: 'satisfies_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, satisfiesExpression, 'expression', expression_, fields.expression); },
    typeAnnotation(typeAnnotation_?: T.Type) { return _fs(config, satisfiesExpression, 'typeAnnotation', typeAnnotation_, fields.type_annotation); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SatisfiesExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function instantiationExpression(config: T.InstantiationExpressionConfig) {
  const fields = {
    expression: config?.expression,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'instantiation_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, instantiationExpression, 'expression', expression_, fields.expression); },
    typeArguments(typeArguments_?: T.TypeArguments) { return _fs(config, instantiationExpression, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InstantiationExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importRequireClause(config: T.ImportRequireClauseConfig) {
  const fields = {
    identifier: config?.identifier,
    source: config?.source,
  };
  return {
    type: 'import_require_clause' as const,
    named: true as const,
    fields,
    identifier(identifier_?: T.Identifier) { return _fs(config, importRequireClause, 'identifier', identifier_, fields.identifier); },
    source(source_?: T.String) { return _fs(config, importRequireClause, 'source', source_, fields.source); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportRequireClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsClause(config: T.ExtendsClauseConfig) {
  const fields = {
    extends: config?.extends,
  };
  const children = config?.children ?? [];
  return {
    type: 'extends_clause' as const,
    named: true as const,
    fields,
    children,
    extends(extends_?: "extends") { return _fs(config, extendsClause, 'extends', extends_, fields.extends); },
    getChildren() { return children; },
    setChildren(...items: T.ExtendsClauseSingle[]) {
      _assertNonEmpty(items, 'extends_clause.children');
      return extendsClause({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExtendsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implementsClause(config: T.ImplementsClauseConfig) {
  const fields = {
    implements: config?.implements,
  };
  const children = config?.children ?? [];
  return {
    type: 'implements_clause' as const,
    named: true as const,
    fields,
    children,
    implements(implements_?: "implements") { return _fs(config, implementsClause, 'implements', implements_, fields.implements); },
    getChildren() { return children; },
    setChildren(...items: T.Type[]) {
      _assertNonEmpty(items, 'implements_clause.children');
      return implementsClause({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImplementsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ambientDeclaration(config: T.AmbientDeclarationConfig) {
  const fields = {
    declare: config?.declare,
    declaration: config?.declaration,
  };
  return {
    type: 'ambient_declaration' as const,
    named: true as const,
    fields,
    declare(declare_?: "declare") { return _fs(config, ambientDeclaration, 'declare', declare_, fields.declare); },
    declaration(declaration_?: T.Declaration | T.StatementBlock | T.PropertyIdentifier | T.Type | T.Semicolon) { return _fs(config, ambientDeclaration, 'declaration', declaration_, fields.declaration); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AmbientDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractClassDeclaration(config: T.AbstractClassDeclarationConfig) {
  const fields = {
    decorator: config?.decorator,
    name: config?.name,
    type_parameters: config?.typeParameters,
    class_heritage: config?.classHeritage,
    body: config?.body,
  };
  return {
    type: 'abstract_class_declaration' as const,
    named: true as const,
    fields,
    decorator(...decorator_: T.Decorator[]) { return _fsm(config, abstractClassDeclaration, 'decorator', decorator_, fields.decorator); },
    name(name_?: T._TypeIdentifier) { return _fs(config, abstractClassDeclaration, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, abstractClassDeclaration, 'typeParameters', typeParameters_, fields.type_parameters); },
    classHeritage(classHeritage_?: T.ClassHeritage | undefined) { return _fs(config, abstractClassDeclaration, 'classHeritage', classHeritage_, fields.class_heritage); },
    body(body_?: T.ClassBody) { return _fs(config, abstractClassDeclaration, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AbstractClassDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function module(config: T.ModuleConfig) {
  const fields = {
    module: config?.module,
  };
  const children = config?.children ?? [];
  return {
    type: 'module' as const,
    named: true as const,
    fields,
    children,
    module(module_?: "module") { return _fs(config, module, 'module', module_, fields.module); },
    getChild() { return children[0]; },
    setChild(child: T._Module) { return module({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function internalModule(config: T.InternalModuleConfig) {
  const fields = {
    namespace: config?.namespace,
  };
  const children = config?.children ?? [];
  return {
    type: 'internal_module' as const,
    named: true as const,
    fields,
    children,
    namespace(namespace_?: "namespace") { return _fs(config, internalModule, 'namespace', namespace_, fields.namespace); },
    getChild() { return children[0]; },
    setChild(child: T._Module) { return internalModule({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InternalModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importAlias(config: T.ImportAliasConfig) {
  const fields = {
    import: config?.import,
    name: config?.name,
    value: config?.value,
    semicolon: config?.semicolon,
  };
  return {
    type: 'import_alias' as const,
    named: true as const,
    fields,
    import(import_?: "import") { return _fs(config, importAlias, 'import', import_, fields.import); },
    name(name_?: T.Identifier) { return _fs(config, importAlias, 'name', name_, fields.name); },
    value(value_?: T.Identifier | T.NestedIdentifier) { return _fs(config, importAlias, 'value', value_, fields.value); },
    semicolon(semicolon_?: T.Semicolon) { return _fs(config, importAlias, 'semicolon', semicolon_, fields.semicolon); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportAliasTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nestedTypeIdentifier(config: T.NestedTypeIdentifierConfig) {
  const fields = {
    module: config?.module,
    name: config?.name,
  };
  return {
    type: 'nested_type_identifier' as const,
    named: true as const,
    fields,
    module(module_?: T.Identifier | T.NestedIdentifier) { return _fs(config, nestedTypeIdentifier, 'module', module_, fields.module); },
    name(name_?: T._TypeIdentifier) { return _fs(config, nestedTypeIdentifier, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NestedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interfaceDeclaration(config: T.InterfaceDeclarationConfig) {
  const fields = {
    interface: config?.interface,
    name: config?.name,
    type_parameters: config?.typeParameters,
    extends_type_clause: config?.extendsTypeClause,
    body: config?.body,
  };
  return {
    type: 'interface_declaration' as const,
    named: true as const,
    fields,
    interface(interface_?: "interface") { return _fs(config, interfaceDeclaration, 'interface', interface_, fields.interface); },
    name(name_?: T._TypeIdentifier) { return _fs(config, interfaceDeclaration, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, interfaceDeclaration, 'typeParameters', typeParameters_, fields.type_parameters); },
    extendsTypeClause(extendsTypeClause_?: T.ExtendsTypeClause | undefined) { return _fs(config, interfaceDeclaration, 'extendsTypeClause', extendsTypeClause_, fields.extends_type_clause); },
    body(body_?: T.InterfaceBody) { return _fs(config, interfaceDeclaration, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InterfaceDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extendsTypeClause(config: T.ExtendsTypeClauseConfig) {
  const fields = {
    extends: config?.extends,
    type: config?.type,
  };
  return {
    type: 'extends_type_clause' as const,
    named: true as const,
    fields,
    extends(extends_?: "extends") { return _fs(config, extendsTypeClause, 'extends', extends_, fields.extends); },
    typeField(...type: NonEmptyArray<T._TypeIdentifier | T.NestedTypeIdentifier | T.GenericType>) { return _fsm(config, extendsTypeClause, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExtendsTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumDeclaration(config: T.EnumDeclarationConfig) {
  const fields = {
    const: config?.const,
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'enum_declaration' as const,
    named: true as const,
    fields,
    const(const_?: "const" | undefined) { return _fs(config, enumDeclaration, 'const', const_, fields.const); },
    name(name_?: T.Identifier) { return _fs(config, enumDeclaration, 'name', name_, fields.name); },
    body(body_?: T.EnumBody) { return _fs(config, enumDeclaration, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumBody(config?: T.EnumBodyConfig) {
  const fields = {
    opening: config?.opening,
  };
  return {
    type: 'enum_body' as const,
    named: true as const,
    fields,
    opening(...opening_: string[]) { return _fsm(config, enumBody, 'opening', opening_, fields.opening); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumAssignment(config: T.EnumAssignmentConfig) {
  const fields = {
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'enum_assignment' as const,
    named: true as const,
    fields,
    children,
    name(name_?: T.PropertyName) { return _fs(config, enumAssignment, 'name', name_, fields.name); },
    getChild() { return children[0]; },
    setChild(child: T.Initializer) { return enumAssignment({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasDeclaration(config: T.TypeAliasDeclarationConfig) {
  const fields = {
    type: config?.type,
    name: config?.name,
    type_parameters: config?.typeParameters,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'type_alias_declaration' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: "type") { return _fs(config, typeAliasDeclaration, 'type', type, fields.type); },
    name(name_?: T._TypeIdentifier) { return _fs(config, typeAliasDeclaration, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, typeAliasDeclaration, 'typeParameters', typeParameters_, fields.type_parameters); },
    value(value_?: T.Type) { return _fs(config, typeAliasDeclaration, 'value', value_, fields.value); },
    getChild() { return children[0]; },
    setChild(child: T.Semicolon) { return typeAliasDeclaration({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeAliasDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function accessibilityModifier(text: 'public' | 'private' | 'protected') {
  return {
    type: 'accessibility_modifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.AccessibilityModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function overrideModifier() {
  return {
    type: 'override_modifier' as const,
    named: true as const,
    text: 'override' as const,
    render: () => 'override' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'override' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'override' as const },
    replace: (t: T.OverrideModifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'override' as const }; },
  };
}

export function requiredParameter(config: T.RequiredParameterConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'required_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: T.TypeAnnotation | undefined) { return _fs(config, requiredParameter, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (T.ParameterName | T.Initializer)) { return requiredParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RequiredParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalParameter(config: T.OptionalParameterConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'optional_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: T.TypeAnnotation | undefined) { return _fs(config, optionalParameter, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (T.ParameterName | T.Initializer)) { return optionalParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OptionalParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function omittingTypeAnnotation(config: T.OmittingTypeAnnotationConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'omitting_type_annotation' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, omittingTypeAnnotation, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OmittingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function addingTypeAnnotation(config: T.AddingTypeAnnotationConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'adding_type_annotation' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, addingTypeAnnotation, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AddingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optingTypeAnnotation(config: T.OptingTypeAnnotationConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'opting_type_annotation' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, optingTypeAnnotation, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OptingTypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAnnotation(config: T.TypeAnnotationConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'type_annotation' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, typeAnnotation, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asserts(config: T.AssertsConfig) {
  const fields = {
    asserts: config?.asserts,
  };
  const children = config?.children ?? [];
  return {
    type: 'asserts' as const,
    named: true as const,
    fields,
    children,
    asserts(asserts_?: "asserts") { return _fs(config, asserts, 'asserts', asserts_, fields.asserts); },
    getChild() { return children[0]; },
    setChild(child: (T.TypePredicate | T.Identifier | T.This)) { return asserts({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssertsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertsAnnotation(config: T.AssertsAnnotationConfig) {
  const fields = {
    asserts: config?.asserts,
  };
  return {
    type: 'asserts_annotation' as const,
    named: true as const,
    fields,
    asserts(asserts_?: ":") { return _fs(config, assertsAnnotation, 'asserts', asserts_, fields.asserts); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssertsAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleParameter(config: T.TupleParameterConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'tuple_parameter' as const,
    named: true as const,
    fields,
    name(name_?: T.Identifier | T.RestPattern) { return _fs(config, tupleParameter, 'name', name_, fields.name); },
    typeField(type?: T.TypeAnnotation) { return _fs(config, tupleParameter, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalTupleParameter(config: T.OptionalTupleParameterConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'optional_tuple_parameter' as const,
    named: true as const,
    fields,
    name(name_?: T.Identifier) { return _fs(config, optionalTupleParameter, 'name', name_, fields.name); },
    typeField(type?: T.TypeAnnotation) { return _fs(config, optionalTupleParameter, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OptionalTupleParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function optionalType(config: T.OptionalTypeConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'optional_type' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, optionalType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OptionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function restType(config: T.RestTypeConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'rest_type' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, restType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RestTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructorType(config: T.ConstructorTypeConfig) {
  const fields = {
    abstract: config?.abstract,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    type: config?.type,
  };
  return {
    type: 'constructor_type' as const,
    named: true as const,
    fields,
    abstract(abstract_?: "abstract" | undefined) { return _fs(config, constructorType, 'abstract', abstract_, fields.abstract); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, constructorType, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: T.FormalParameters) { return _fs(config, constructorType, 'parameters', parameters_, fields.parameters); },
    typeField(type?: T.Type) { return _fs(config, constructorType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstructorTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateType(child?: (T.PrimaryType | T.InferType)) {
  const children = child != null ? [child] : [];
  return {
    type: 'template_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TemplateTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function templateLiteralType(...children: (T.StringFragment | T.TemplateType)[]) {
  return {
    type: 'template_literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TemplateLiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function inferType(config: T.InferTypeConfig) {
  const fields = {
    infer: config?.infer,
    type_identifier: config?.typeIdentifier,
    constraint: config?.constraint,
  };
  return {
    type: 'infer_type' as const,
    named: true as const,
    fields,
    infer(infer_?: "infer") { return _fs(config, inferType, 'infer', infer_, fields.infer); },
    typeIdentifier(typeIdentifier_?: T._TypeIdentifier) { return _fs(config, inferType, 'typeIdentifier', typeIdentifier_, fields.type_identifier); },
    constraint(constraint_?: T.Type | undefined) { return _fs(config, inferType, 'constraint', constraint_, fields.constraint); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InferTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalType(config: T.ConditionalTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'conditional_type' as const,
    named: true as const,
    fields,
    left(left_?: T.Type) { return _fs(config, conditionalType, 'left', left_, fields.left); },
    right(right_?: T.Type) { return _fs(config, conditionalType, 'right', right_, fields.right); },
    consequence(consequence_?: T.Type) { return _fs(config, conditionalType, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: T.Type) { return _fs(config, conditionalType, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConditionalTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: T.GenericTypeConfig) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    name(name_?: T._TypeIdentifier | T.NestedTypeIdentifier) { return _fs(config, genericType, 'name', name_, fields.name); },
    typeArguments(typeArguments_?: T.TypeArguments) { return _fs(config, genericType, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicate(config: T.TypePredicateConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'type_predicate' as const,
    named: true as const,
    fields,
    name(name_?: T.Identifier | T.This) { return _fs(config, typePredicate, 'name', name_, fields.name); },
    typeField(type?: T.Type) { return _fs(config, typePredicate, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typePredicateAnnotation(config: T.TypePredicateAnnotationConfig) {
  const fields = {
    type_predicate: config?.typePredicate,
  };
  return {
    type: 'type_predicate_annotation' as const,
    named: true as const,
    fields,
    typePredicate(typePredicate_?: ":") { return _fs(config, typePredicateAnnotation, 'typePredicate', typePredicate_, fields.type_predicate); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypePredicateAnnotationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeQuery(config: T.TypeQueryConfig) {
  const fields = {
    typeof: config?.typeof,
  };
  const children = config?.children ?? [];
  return {
    type: 'type_query' as const,
    named: true as const,
    fields,
    children,
    typeof(typeof_?: "typeof") { return _fs(config, typeQuery, 'typeof', typeof_, fields.typeof); },
    getChild() { return children[0]; },
    setChild(child: (T.SubscriptExpression | T.MemberExpression | T.CallExpression | T.InstantiationExpression | T.Identifier | T.This)) { return typeQuery({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexTypeQuery(config: T.IndexTypeQueryConfig) {
  const fields = {
    keyof: config?.keyof,
    primary_type: config?.primaryType,
  };
  return {
    type: 'index_type_query' as const,
    named: true as const,
    fields,
    keyof(keyof_?: "keyof") { return _fs(config, indexTypeQuery, 'keyof', keyof_, fields.keyof); },
    primaryType(primaryType_?: T.PrimaryType) { return _fs(config, indexTypeQuery, 'primaryType', primaryType_, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IndexTypeQueryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lookupType(config: T.LookupTypeConfig) {
  const fields = {
    primary_type: config?.primaryType,
    index_type: config?.indexType,
  };
  return {
    type: 'lookup_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: T.PrimaryType) { return _fs(config, lookupType, 'primaryType', primaryType_, fields.primary_type); },
    indexType(indexType_?: T.Type) { return _fs(config, lookupType, 'indexType', indexType_, fields.index_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LookupTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mappedTypeClause(config: T.MappedTypeClauseConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
    alias: config?.alias,
  };
  return {
    type: 'mapped_type_clause' as const,
    named: true as const,
    fields,
    name(name_?: T._TypeIdentifier) { return _fs(config, mappedTypeClause, 'name', name_, fields.name); },
    typeField(type?: T.Type) { return _fs(config, mappedTypeClause, 'type', type, fields.type); },
    alias(alias_?: T.Type | undefined) { return _fs(config, mappedTypeClause, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MappedTypeClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function literalType(child?: (T.UnaryExpression | T.Number | T.String | T.True | T.False | T.Null | T.Undefined)) {
  const children = child != null ? [child] : [];
  return {
    type: 'literal_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LiteralTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function flowMaybeType(config: T.FlowMaybeTypeConfig) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'flow_maybe_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: T.PrimaryType) { return _fs(config, flowMaybeType, 'primaryType', primaryType_, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FlowMaybeTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedType(config: T.ParenthesizedTypeConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'parenthesized_type' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, parenthesizedType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function predefinedType(text: string) {
  if (text.length === 0) throw new Error(`predefined_type: text must be non-empty`);
  return {
    type: 'predefined_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PredefinedTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeArguments(...children: T.Type[]) {
  _assertNonEmpty(children, 'type_arguments.children');
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function objectType(config: T.ObjectTypeConfig) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'object_type' as const,
    named: true as const,
    fields,
    opening(opening_?: "{" | "{|") { return _fs(config, objectType, 'opening', opening_, fields.opening); },
    members(members_?: T.ExportStatement | T.Semicolon | T.PropertySignature | T.CallSignature | T.ConstructSignature | T.IndexSignature | T.MethodSignature | undefined) { return _fs(config, objectType, 'members', members_, fields.members); },
    closing(closing_?: "}" | "|}") { return _fs(config, objectType, 'closing', closing_, fields.closing); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ObjectTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callSignature(child: T._CallSignature) {
  const children = [child];
  return {
    type: 'call_signature' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertySignature(config: T.PropertySignatureConfig) {
  const fields = {
    accessibility_modifier: config?.accessibilityModifier,
    override_modifier: config?.overrideModifier,
    readonly: config?.readonly,
    name: config?.name,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'property_signature' as const,
    named: true as const,
    fields,
    children,
    accessibilityModifier(accessibilityModifier_?: T.AccessibilityModifier | undefined) { return _fs(config, propertySignature, 'accessibilityModifier', accessibilityModifier_, fields.accessibility_modifier); },
    overrideModifier(overrideModifier_?: "static" | undefined) { return _fs(config, propertySignature, 'overrideModifier', overrideModifier_, fields.override_modifier); },
    readonly(readonly_?: "readonly" | undefined) { return _fs(config, propertySignature, 'readonly', readonly_, fields.readonly); },
    name(name_?: T.PropertyName) { return _fs(config, propertySignature, 'name', name_, fields.name); },
    typeField(type?: T.TypeAnnotation | undefined) { return _fs(config, propertySignature, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: T.OverrideModifier) { return propertySignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PropertySignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: T.TypeParameter[]) {
  _assertNonEmpty(children, 'type_parameters.children');
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: T.TypeParameterConfig) {
  const fields = {
    const: config?.const,
    name: config?.name,
    constraint: config?.constraint,
    value: config?.value,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    const(const_?: "const" | undefined) { return _fs(config, typeParameter, 'const', const_, fields.const); },
    name(name_?: T._TypeIdentifier) { return _fs(config, typeParameter, 'name', name_, fields.name); },
    constraint(constraint_?: T.Constraint | undefined) { return _fs(config, typeParameter, 'constraint', constraint_, fields.constraint); },
    value(value_?: T.DefaultType | undefined) { return _fs(config, typeParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultType(config: T.DefaultTypeConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'default_type' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, defaultType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DefaultTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constraint(config: T.ConstraintConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'constraint' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, constraint, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstraintTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constructSignature(config: T.ConstructSignatureConfig) {
  const fields = {
    abstract: config?.abstract,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    type: config?.type,
  };
  return {
    type: 'construct_signature' as const,
    named: true as const,
    fields,
    abstract(abstract_?: "abstract" | undefined) { return _fs(config, constructSignature, 'abstract', abstract_, fields.abstract); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, constructSignature, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: T.FormalParameters) { return _fs(config, constructSignature, 'parameters', parameters_, fields.parameters); },
    typeField(type?: T.TypeAnnotation | undefined) { return _fs(config, constructSignature, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstructSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexSignature(config: T.IndexSignatureConfig) {
  const fields = {
    mapped_type_clause: config?.mappedTypeClause,
    name: config?.name,
    index_type: config?.indexType,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'index_signature' as const,
    named: true as const,
    fields,
    children,
    mappedTypeClause(mappedTypeClause_?: string) { return _fs(config, indexSignature, 'mappedTypeClause', mappedTypeClause_, fields.mapped_type_clause); },
    name(name_?: T.Identifier | undefined) { return _fs(config, indexSignature, 'name', name_, fields.name); },
    indexType(indexType_?: T.Type | undefined) { return _fs(config, indexSignature, 'indexType', indexType_, fields.index_type); },
    typeField(type?: T.TypeAnnotation | T.OmittingTypeAnnotation | T.AddingTypeAnnotation | T.OptingTypeAnnotation) { return _fs(config, indexSignature, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: T.MappedTypeClause) { return indexSignature({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IndexSignatureTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: T.ArrayTypeConfig) {
  const fields = {
    primary_type: config?.primaryType,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    primaryType(primaryType_?: T.PrimaryType) { return _fs(config, arrayType, 'primaryType', primaryType_, fields.primary_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: T.TupleTypeMember[]) {
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function readonlyType(config: T.ReadonlyTypeConfig) {
  const fields = {
    readonly: config?.readonly,
    type: config?.type,
  };
  return {
    type: 'readonly_type' as const,
    named: true as const,
    fields,
    readonly(readonly_?: "readonly") { return _fs(config, readonlyType, 'readonly', readonly_, fields.readonly); },
    typeField(type?: T.Type) { return _fs(config, readonlyType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReadonlyTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: T.UnionTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left_?: T.Type | undefined) { return _fs(config, unionType, 'left', left_, fields.left); },
    right(right_?: T.Type) { return _fs(config, unionType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function intersectionType(config: T.IntersectionTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'intersection_type' as const,
    named: true as const,
    fields,
    left(left_?: T.Type | undefined) { return _fs(config, intersectionType, 'left', left_, fields.left); },
    right(right_?: T.Type) { return _fs(config, intersectionType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IntersectionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: T.FunctionTypeConfig) {
  const fields = {
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, functionType, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: T.FormalParameters) { return _fs(config, functionType, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: T.Type | T.Asserts | T.TypePredicate) { return _fs(config, functionType, 'returnType', returnType_, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function htmlComment(text: string) {
  if (text.length === 0) throw new Error(`html_comment: text must be non-empty`);
  return {
    type: 'html_comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.HtmlCommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function oror(text: string) {
  if (text.length === 0) throw new Error(`||: text must be non-empty`);
  return {
    type: '||' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.OrorTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function jsxText(text: string) {
  if (text.length === 0) throw new Error(`jsx_text: text must be non-empty`);
  return {
    type: 'jsx_text' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.JsxTextTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function statementIdentifier(text: string) {
  if (text.length === 0) throw new Error(`statement_identifier: text must be non-empty`);
  return {
    type: 'statement_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StatementIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthandPropertyIdentifier(child?: (T.Identifier | T.ReservedIdentifier)) {
  const children = child != null ? [child] : [];
  return {
    type: 'shorthand_property_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ShorthandPropertyIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandPropertyIdentifierPattern(child?: (T.Identifier | T.ReservedIdentifier)) {
  const children = child != null ? [child] : [];
  return {
    type: 'shorthand_property_identifier_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ShorthandPropertyIdentifierPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function propertyIdentifier(text: string) {
  if (text.length === 0) throw new Error(`property_identifier: text must be non-empty`);
  return {
    type: 'property_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PropertyIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringFragment(text: string) {
  if (text.length === 0) throw new Error(`string_fragment: text must be non-empty`);
  return {
    type: 'string_fragment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringFragmentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function interfaceBody(config: T.InterfaceBodyConfig) {
  const fields = {
    opening: config?.opening,
    members: config?.members,
    closing: config?.closing,
  };
  return {
    type: 'interface_body' as const,
    named: true as const,
    fields,
    opening(opening_?: "{" | "{|") { return _fs(config, interfaceBody, 'opening', opening_, fields.opening); },
    members(members_?: T.ExportStatement | T.Semicolon | T.PropertySignature | T.CallSignature | T.ConstructSignature | T.IndexSignature | T.MethodSignature | undefined) { return _fs(config, interfaceBody, 'members', members_, fields.members); },
    closing(closing_?: "}" | "|}") { return _fs(config, interfaceBody, 'closing', closing_, fields.closing); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InterfaceBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function thisType() {
  return {
    type: 'this_type' as const,
    named: true as const,
    text: 'this' as const,
    render: () => 'this' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'this' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'this' as const },
    replace: (t: T.ThisTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'this' as const }; },
  };
}

export function typeIdentifier(text: string) {
  if (text.length === 0) throw new Error(`type_identifier: text must be non-empty`);
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "program": FluentNode<"program", T.ProgramConfig>;
  "hash_bang_line": T.HashBangLine;
  "export_statement": FluentNode<"export_statement", T.ExportStatementConfig>;
  "namespace_export": FluentNode<"namespace_export", T.NamespaceExportConfig>;
  "export_clause": FluentNode<"export_clause", T.ExportClauseConfig>;
  "export_specifier": FluentNode<"export_specifier", T.ExportSpecifierConfig>;
  "declaration": FluentNode<"declaration", T.DeclarationConfig>;
  "import": T.Import;
  "import_statement": FluentNode<"import_statement", T.ImportStatementConfig>;
  "import_clause": FluentNode<"import_clause", T.ImportClauseConfig>;
  "namespace_import": FluentNode<"namespace_import", T.NamespaceImportConfig>;
  "named_imports": FluentNode<"named_imports", T.NamedImportsConfig>;
  "import_specifier": FluentNode<"import_specifier", T.ImportSpecifierConfig>;
  "import_attribute": FluentNode<"import_attribute", T.ImportAttributeConfig>;
  "statement": FluentNode<"statement", T.StatementConfig>;
  "expression_statement": FluentNode<"expression_statement", T.ExpressionStatementConfig>;
  "variable_declaration": FluentNode<"variable_declaration", T.VariableDeclarationConfig>;
  "lexical_declaration": FluentNode<"lexical_declaration", T.LexicalDeclarationConfig>;
  "variable_declarator": FluentNode<"variable_declarator", T.VariableDeclaratorConfig>;
  "statement_block": FluentNode<"statement_block", T.StatementBlockConfig>;
  "else_clause": FluentNode<"else_clause", T.ElseClauseConfig>;
  "if_statement": FluentNode<"if_statement", T.IfStatementConfig>;
  "switch_statement": FluentNode<"switch_statement", T.SwitchStatementConfig>;
  "for_statement": FluentNode<"for_statement", T.ForStatementConfig>;
  "for_in_statement": FluentNode<"for_in_statement", T.ForInStatementConfig>;
  "while_statement": FluentNode<"while_statement", T.WhileStatementConfig>;
  "do_statement": FluentNode<"do_statement", T.DoStatementConfig>;
  "try_statement": FluentNode<"try_statement", T.TryStatementConfig>;
  "with_statement": FluentNode<"with_statement", T.WithStatementConfig>;
  "break_statement": FluentNode<"break_statement", T.BreakStatementConfig>;
  "continue_statement": FluentNode<"continue_statement", T.ContinueStatementConfig>;
  "debugger_statement": FluentNode<"debugger_statement", T.DebuggerStatementConfig>;
  "return_statement": FluentNode<"return_statement", T.ReturnStatementConfig>;
  "throw_statement": FluentNode<"throw_statement", T.ThrowStatementConfig>;
  "labeled_statement": FluentNode<"labeled_statement", T.LabeledStatementConfig>;
  "switch_body": FluentNode<"switch_body", T.SwitchBodyConfig>;
  "switch_case": FluentNode<"switch_case", T.SwitchCaseConfig>;
  "switch_default": FluentNode<"switch_default", T.SwitchDefaultConfig>;
  "catch_clause": FluentNode<"catch_clause", T.CatchClauseConfig>;
  "finally_clause": FluentNode<"finally_clause", T.FinallyClauseConfig>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", T.ParenthesizedExpressionConfig>;
  "expression": FluentNode<"expression", T.ExpressionConfig>;
  "primary_expression": FluentNode<"primary_expression", T.PrimaryExpressionConfig>;
  "yield_expression": FluentNode<"yield_expression", T.YieldExpressionConfig>;
  "object": FluentNode<"object", T.ObjectConfig>;
  "object_pattern": FluentNode<"object_pattern", T.ObjectPatternConfig>;
  "assignment_pattern": FluentNode<"assignment_pattern", T.AssignmentPatternConfig>;
  "object_assignment_pattern": FluentNode<"object_assignment_pattern", T.ObjectAssignmentPatternConfig>;
  "array": FluentNode<"array", T.ArrayConfig>;
  "array_pattern": FluentNode<"array_pattern", T.ArrayPatternConfig>;
  "jsx_element": FluentNode<"jsx_element", T.JsxElementConfig>;
  "html_character_reference": T.HtmlCharacterReference;
  "jsx_expression": FluentNode<"jsx_expression", T.JsxExpressionConfig>;
  "jsx_opening_element": FluentNode<"jsx_opening_element", T.JsxOpeningElementConfig>;
  "jsx_identifier": T.JsxIdentifier;
  "nested_identifier": FluentNode<"nested_identifier", T.NestedIdentifierConfig>;
  "jsx_namespace_name": FluentNode<"jsx_namespace_name", T.JsxNamespaceNameConfig>;
  "jsx_closing_element": FluentNode<"jsx_closing_element", T.JsxClosingElementConfig>;
  "jsx_self_closing_element": FluentNode<"jsx_self_closing_element", T.JsxSelfClosingElementConfig>;
  "jsx_attribute": FluentNode<"jsx_attribute", T.JsxAttributeConfig>;
  "unescaped_double_jsx_string_fragment": T.UnescapedDoubleJsxStringFragment;
  "unescaped_single_jsx_string_fragment": T.UnescapedSingleJsxStringFragment;
  "class": FluentNode<"class", T.ClassConfig>;
  "class_declaration": FluentNode<"class_declaration", T.ClassDeclarationConfig>;
  "class_heritage": FluentNode<"class_heritage", T.ClassHeritageConfig>;
  "function_expression": FluentNode<"function_expression", T.FunctionExpressionConfig>;
  "function_declaration": FluentNode<"function_declaration", T.FunctionDeclarationConfig>;
  "generator_function": FluentNode<"generator_function", T.GeneratorFunctionConfig>;
  "generator_function_declaration": FluentNode<"generator_function_declaration", T.GeneratorFunctionDeclarationConfig>;
  "arrow_function": FluentNode<"arrow_function", T.ArrowFunctionConfig>;
  "call_expression": FluentNode<"call_expression", T.CallExpressionConfig>;
  "new_expression": FluentNode<"new_expression", T.NewExpressionConfig>;
  "await_expression": FluentNode<"await_expression", T.AwaitExpressionConfig>;
  "member_expression": FluentNode<"member_expression", T.MemberExpressionConfig>;
  "subscript_expression": FluentNode<"subscript_expression", T.SubscriptExpressionConfig>;
  "assignment_expression": FluentNode<"assignment_expression", T.AssignmentExpressionConfig>;
  "augmented_assignment_expression": FluentNode<"augmented_assignment_expression", T.AugmentedAssignmentExpressionConfig>;
  "spread_element": FluentNode<"spread_element", T.SpreadElementConfig>;
  "ternary_expression": FluentNode<"ternary_expression", T.TernaryExpressionConfig>;
  "binary_expression": FluentNode<"binary_expression", T.BinaryExpressionConfig>;
  "unary_expression": FluentNode<"unary_expression", T.UnaryExpressionConfig>;
  "update_expression": FluentNode<"update_expression", T.UpdateExpressionConfig>;
  "sequence_expression": FluentNode<"sequence_expression", T.SequenceExpressionConfig>;
  "string": FluentNode<"string", T.StringConfig>;
  "unescaped_double_string_fragment": T.UnescapedDoubleStringFragment;
  "unescaped_single_string_fragment": T.UnescapedSingleStringFragment;
  "escape_sequence": T.EscapeSequence;
  "comment": T.Comment;
  "template_string": FluentNode<"template_string", T.TemplateStringConfig>;
  "template_substitution": FluentNode<"template_substitution", T.TemplateSubstitutionConfig>;
  "regex": FluentNode<"regex", T.RegexConfig>;
  "regex_pattern": T.RegexPattern;
  "regex_flags": T.RegexFlags;
  "number": T.Number;
  "identifier": T.Identifier;
  "private_property_identifier": T.PrivatePropertyIdentifier;
  "meta_property": T.MetaProperty;
  "this": T.This;
  "super": T.Super;
  "true": T.True;
  "false": T.False;
  "null": T.Null;
  "undefined": T.Undefined;
  "arguments": FluentNode<"arguments", T.ArgumentsConfig>;
  "decorator": FluentNode<"decorator", T.DecoratorConfig>;
  "decorator_member_expression": FluentNode<"decorator_member_expression", T.DecoratorMemberExpressionConfig>;
  "decorator_call_expression": FluentNode<"decorator_call_expression", T.DecoratorCallExpressionConfig>;
  "class_body": FluentNode<"class_body", T.ClassBodyConfig>;
  "field_definition": FluentNode<"field_definition", T.FieldDefinitionConfig>;
  "formal_parameters": FluentNode<"formal_parameters", T.FormalParametersConfig>;
  "class_static_block": FluentNode<"class_static_block", T.ClassStaticBlockConfig>;
  "pattern": FluentNode<"pattern", T.PatternConfig>;
  "rest_pattern": FluentNode<"rest_pattern", T.RestPatternConfig>;
  "method_definition": FluentNode<"method_definition", T.MethodDefinitionConfig>;
  "pair": FluentNode<"pair", T.PairConfig>;
  "pair_pattern": FluentNode<"pair_pattern", T.PairPatternConfig>;
  "computed_property_name": FluentNode<"computed_property_name", T.ComputedPropertyNameConfig>;
  "public_field_definition": FluentNode<"public_field_definition", T.PublicFieldDefinitionConfig>;
  "non_null_expression": FluentNode<"non_null_expression", T.NonNullExpressionConfig>;
  "method_signature": FluentNode<"method_signature", T.MethodSignatureConfig>;
  "abstract_method_signature": FluentNode<"abstract_method_signature", T.AbstractMethodSignatureConfig>;
  "function_signature": FluentNode<"function_signature", T.FunctionSignatureConfig>;
  "decorator_parenthesized_expression": FluentNode<"decorator_parenthesized_expression", T.DecoratorParenthesizedExpressionConfig>;
  "type_assertion": FluentNode<"type_assertion", T.TypeAssertionConfig>;
  "as_expression": FluentNode<"as_expression", T.AsExpressionConfig>;
  "satisfies_expression": FluentNode<"satisfies_expression", T.SatisfiesExpressionConfig>;
  "instantiation_expression": FluentNode<"instantiation_expression", T.InstantiationExpressionConfig>;
  "import_require_clause": FluentNode<"import_require_clause", T.ImportRequireClauseConfig>;
  "extends_clause": FluentNode<"extends_clause", T.ExtendsClauseConfig>;
  "implements_clause": FluentNode<"implements_clause", T.ImplementsClauseConfig>;
  "ambient_declaration": FluentNode<"ambient_declaration", T.AmbientDeclarationConfig>;
  "abstract_class_declaration": FluentNode<"abstract_class_declaration", T.AbstractClassDeclarationConfig>;
  "module": FluentNode<"module", T.ModuleConfig>;
  "internal_module": FluentNode<"internal_module", T.InternalModuleConfig>;
  "import_alias": FluentNode<"import_alias", T.ImportAliasConfig>;
  "nested_type_identifier": FluentNode<"nested_type_identifier", T.NestedTypeIdentifierConfig>;
  "interface_declaration": FluentNode<"interface_declaration", T.InterfaceDeclarationConfig>;
  "extends_type_clause": FluentNode<"extends_type_clause", T.ExtendsTypeClauseConfig>;
  "enum_declaration": FluentNode<"enum_declaration", T.EnumDeclarationConfig>;
  "enum_body": FluentNode<"enum_body", T.EnumBodyConfig>;
  "enum_assignment": FluentNode<"enum_assignment", T.EnumAssignmentConfig>;
  "type_alias_declaration": FluentNode<"type_alias_declaration", T.TypeAliasDeclarationConfig>;
  "accessibility_modifier": T.AccessibilityModifier;
  "override_modifier": T.OverrideModifier;
  "required_parameter": FluentNode<"required_parameter", T.RequiredParameterConfig>;
  "optional_parameter": FluentNode<"optional_parameter", T.OptionalParameterConfig>;
  "omitting_type_annotation": FluentNode<"omitting_type_annotation", T.OmittingTypeAnnotationConfig>;
  "adding_type_annotation": FluentNode<"adding_type_annotation", T.AddingTypeAnnotationConfig>;
  "opting_type_annotation": FluentNode<"opting_type_annotation", T.OptingTypeAnnotationConfig>;
  "type_annotation": FluentNode<"type_annotation", T.TypeAnnotationConfig>;
  "asserts": FluentNode<"asserts", T.AssertsConfig>;
  "asserts_annotation": FluentNode<"asserts_annotation", T.AssertsAnnotationConfig>;
  "tuple_parameter": FluentNode<"tuple_parameter", T.TupleParameterConfig>;
  "optional_tuple_parameter": FluentNode<"optional_tuple_parameter", T.OptionalTupleParameterConfig>;
  "optional_type": FluentNode<"optional_type", T.OptionalTypeConfig>;
  "rest_type": FluentNode<"rest_type", T.RestTypeConfig>;
  "constructor_type": FluentNode<"constructor_type", T.ConstructorTypeConfig>;
  "template_type": FluentNode<"template_type", T.TemplateTypeConfig>;
  "template_literal_type": FluentNode<"template_literal_type", T.TemplateLiteralTypeConfig>;
  "infer_type": FluentNode<"infer_type", T.InferTypeConfig>;
  "conditional_type": FluentNode<"conditional_type", T.ConditionalTypeConfig>;
  "generic_type": FluentNode<"generic_type", T.GenericTypeConfig>;
  "type_predicate": FluentNode<"type_predicate", T.TypePredicateConfig>;
  "type_predicate_annotation": FluentNode<"type_predicate_annotation", T.TypePredicateAnnotationConfig>;
  "type_query": FluentNode<"type_query", T.TypeQueryConfig>;
  "index_type_query": FluentNode<"index_type_query", T.IndexTypeQueryConfig>;
  "lookup_type": FluentNode<"lookup_type", T.LookupTypeConfig>;
  "mapped_type_clause": FluentNode<"mapped_type_clause", T.MappedTypeClauseConfig>;
  "literal_type": FluentNode<"literal_type", T.LiteralTypeConfig>;
  "flow_maybe_type": FluentNode<"flow_maybe_type", T.FlowMaybeTypeConfig>;
  "parenthesized_type": FluentNode<"parenthesized_type", T.ParenthesizedTypeConfig>;
  "predefined_type": T.PredefinedType;
  "type_arguments": FluentNode<"type_arguments", T.TypeArgumentsConfig>;
  "object_type": FluentNode<"object_type", T.ObjectTypeConfig>;
  "call_signature": FluentNode<"call_signature", T.CallSignatureConfig>;
  "property_signature": FluentNode<"property_signature", T.PropertySignatureConfig>;
  "type_parameters": FluentNode<"type_parameters", T.TypeParametersConfig>;
  "type_parameter": FluentNode<"type_parameter", T.TypeParameterConfig>;
  "default_type": FluentNode<"default_type", T.DefaultTypeConfig>;
  "constraint": FluentNode<"constraint", T.ConstraintConfig>;
  "construct_signature": FluentNode<"construct_signature", T.ConstructSignatureConfig>;
  "index_signature": FluentNode<"index_signature", T.IndexSignatureConfig>;
  "array_type": FluentNode<"array_type", T.ArrayTypeConfig>;
  "tuple_type": FluentNode<"tuple_type", T.TupleTypeConfig>;
  "readonly_type": FluentNode<"readonly_type", T.ReadonlyTypeConfig>;
  "union_type": FluentNode<"union_type", T.UnionTypeConfig>;
  "intersection_type": FluentNode<"intersection_type", T.IntersectionTypeConfig>;
  "function_type": FluentNode<"function_type", T.FunctionTypeConfig>;
  "html_comment": T.HtmlComment;
  "||": T.Oror;
  "jsx_text": T.JsxText;
  "statement_identifier": T.StatementIdentifier;
  "shorthand_property_identifier": FluentNode<"shorthand_property_identifier", T.ShorthandPropertyIdentifierConfig>;
  "shorthand_property_identifier_pattern": FluentNode<"shorthand_property_identifier_pattern", T.ShorthandPropertyIdentifierPatternConfig>;
  "property_identifier": T.PropertyIdentifier;
  "string_fragment": T.StringFragment;
  "interface_body": FluentNode<"interface_body", T.InterfaceBodyConfig>;
  "this_type": T.ThisType;
  "type_identifier": T.TypeIdentifier;
};

export const _factoryMap = {
  "program": program,
  "hash_bang_line": hashBangLine,
  "export_statement": exportStatement,
  "namespace_export": namespaceExport,
  "export_clause": exportClause,
  "export_specifier": exportSpecifier,
  "declaration": declaration,
  "import": import_,
  "import_statement": importStatement,
  "import_clause": importClause,
  "namespace_import": namespaceImport,
  "named_imports": namedImports,
  "import_specifier": importSpecifier,
  "import_attribute": importAttribute,
  "statement": statement,
  "expression_statement": expressionStatement,
  "variable_declaration": variableDeclaration,
  "lexical_declaration": lexicalDeclaration,
  "variable_declarator": variableDeclarator,
  "statement_block": statementBlock,
  "else_clause": elseClause,
  "if_statement": ifStatement,
  "switch_statement": switchStatement,
  "for_statement": forStatement,
  "for_in_statement": forInStatement,
  "while_statement": whileStatement,
  "do_statement": doStatement,
  "try_statement": tryStatement,
  "with_statement": withStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "debugger_statement": debuggerStatement,
  "return_statement": returnStatement,
  "throw_statement": throwStatement,
  "labeled_statement": labeledStatement,
  "switch_body": switchBody,
  "switch_case": switchCase,
  "switch_default": switchDefault,
  "catch_clause": catchClause,
  "finally_clause": finallyClause,
  "parenthesized_expression": parenthesizedExpression,
  "expression": expression,
  "primary_expression": primaryExpression,
  "yield_expression": yieldExpression,
  "object": object,
  "object_pattern": objectPattern,
  "assignment_pattern": assignmentPattern,
  "object_assignment_pattern": objectAssignmentPattern,
  "array": array,
  "array_pattern": arrayPattern,
  "jsx_element": jsxElement,
  "html_character_reference": htmlCharacterReference,
  "jsx_expression": jsxExpression,
  "jsx_opening_element": jsxOpeningElement,
  "jsx_identifier": jsxIdentifier,
  "nested_identifier": nestedIdentifier,
  "jsx_namespace_name": jsxNamespaceName,
  "jsx_closing_element": jsxClosingElement,
  "jsx_self_closing_element": jsxSelfClosingElement,
  "jsx_attribute": jsxAttribute,
  "unescaped_double_jsx_string_fragment": unescapedDoubleJsxStringFragment,
  "unescaped_single_jsx_string_fragment": unescapedSingleJsxStringFragment,
  "class": class_,
  "class_declaration": classDeclaration,
  "class_heritage": classHeritage,
  "function_expression": functionExpression,
  "function_declaration": functionDeclaration,
  "generator_function": generatorFunction,
  "generator_function_declaration": generatorFunctionDeclaration,
  "arrow_function": arrowFunction,
  "call_expression": callExpression,
  "new_expression": newExpression,
  "await_expression": awaitExpression,
  "member_expression": memberExpression,
  "subscript_expression": subscriptExpression,
  "assignment_expression": assignmentExpression,
  "augmented_assignment_expression": augmentedAssignmentExpression,
  "spread_element": spreadElement,
  "ternary_expression": ternaryExpression,
  "binary_expression": binaryExpression,
  "unary_expression": unaryExpression,
  "update_expression": updateExpression,
  "sequence_expression": sequenceExpression,
  "string": string,
  "unescaped_double_string_fragment": unescapedDoubleStringFragment,
  "unescaped_single_string_fragment": unescapedSingleStringFragment,
  "escape_sequence": escapeSequence,
  "comment": comment,
  "template_string": templateString,
  "template_substitution": templateSubstitution,
  "regex": regex,
  "regex_pattern": regexPattern,
  "regex_flags": regexFlags,
  "number": number,
  "identifier": identifier,
  "private_property_identifier": privatePropertyIdentifier,
  "meta_property": metaProperty,
  "this": this_,
  "super": super_,
  "true": true_,
  "false": false_,
  "null": null_,
  "undefined": undefined_,
  "arguments": arguments_,
  "decorator": decorator,
  "decorator_member_expression": decoratorMemberExpression,
  "decorator_call_expression": decoratorCallExpression,
  "class_body": classBody,
  "field_definition": fieldDefinition,
  "formal_parameters": formalParameters,
  "class_static_block": classStaticBlock,
  "pattern": pattern,
  "rest_pattern": restPattern,
  "method_definition": methodDefinition,
  "pair": pair,
  "pair_pattern": pairPattern,
  "computed_property_name": computedPropertyName,
  "public_field_definition": publicFieldDefinition,
  "non_null_expression": nonNullExpression,
  "method_signature": methodSignature,
  "abstract_method_signature": abstractMethodSignature,
  "function_signature": functionSignature,
  "decorator_parenthesized_expression": decoratorParenthesizedExpression,
  "type_assertion": typeAssertion,
  "as_expression": asExpression,
  "satisfies_expression": satisfiesExpression,
  "instantiation_expression": instantiationExpression,
  "import_require_clause": importRequireClause,
  "extends_clause": extendsClause,
  "implements_clause": implementsClause,
  "ambient_declaration": ambientDeclaration,
  "abstract_class_declaration": abstractClassDeclaration,
  "module": module,
  "internal_module": internalModule,
  "import_alias": importAlias,
  "nested_type_identifier": nestedTypeIdentifier,
  "interface_declaration": interfaceDeclaration,
  "extends_type_clause": extendsTypeClause,
  "enum_declaration": enumDeclaration,
  "enum_body": enumBody,
  "enum_assignment": enumAssignment,
  "type_alias_declaration": typeAliasDeclaration,
  "accessibility_modifier": accessibilityModifier,
  "override_modifier": overrideModifier,
  "required_parameter": requiredParameter,
  "optional_parameter": optionalParameter,
  "omitting_type_annotation": omittingTypeAnnotation,
  "adding_type_annotation": addingTypeAnnotation,
  "opting_type_annotation": optingTypeAnnotation,
  "type_annotation": typeAnnotation,
  "asserts": asserts,
  "asserts_annotation": assertsAnnotation,
  "tuple_parameter": tupleParameter,
  "optional_tuple_parameter": optionalTupleParameter,
  "optional_type": optionalType,
  "rest_type": restType,
  "constructor_type": constructorType,
  "template_type": templateType,
  "template_literal_type": templateLiteralType,
  "infer_type": inferType,
  "conditional_type": conditionalType,
  "generic_type": genericType,
  "type_predicate": typePredicate,
  "type_predicate_annotation": typePredicateAnnotation,
  "type_query": typeQuery,
  "index_type_query": indexTypeQuery,
  "lookup_type": lookupType,
  "mapped_type_clause": mappedTypeClause,
  "literal_type": literalType,
  "flow_maybe_type": flowMaybeType,
  "parenthesized_type": parenthesizedType,
  "predefined_type": predefinedType,
  "type_arguments": typeArguments,
  "object_type": objectType,
  "call_signature": callSignature,
  "property_signature": propertySignature,
  "type_parameters": typeParameters,
  "type_parameter": typeParameter,
  "default_type": defaultType,
  "constraint": constraint,
  "construct_signature": constructSignature,
  "index_signature": indexSignature,
  "array_type": arrayType,
  "tuple_type": tupleType,
  "readonly_type": readonlyType,
  "union_type": unionType,
  "intersection_type": intersectionType,
  "function_type": functionType,
  "html_comment": htmlComment,
  "||": oror,
  "jsx_text": jsxText,
  "statement_identifier": statementIdentifier,
  "shorthand_property_identifier": shorthandPropertyIdentifier,
  "shorthand_property_identifier_pattern": shorthandPropertyIdentifierPattern,
  "property_identifier": propertyIdentifier,
  "string_fragment": stringFragment,
  "interface_body": interfaceBody,
  "this_type": thisType,
  "type_identifier": typeIdentifier,
} as const;
export type _FactoryMap = typeof _factoryMap;

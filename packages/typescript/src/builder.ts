// Auto-generated by ir-codegen — do not edit

import { LeafBuilder } from '@sittir/types';
import { program } from './nodes/program.js';
import { export_statement } from './nodes/export-statement.js';
import { namespace_export } from './nodes/namespace-export.js';
import { export_clause } from './nodes/export-clause.js';
import { export_specifier } from './nodes/export-specifier.js';
import { import_statement } from './nodes/import-statement.js';
import { import_clause } from './nodes/import-clause.js';
import { namespace_import } from './nodes/namespace-import.js';
import { named_imports } from './nodes/named-imports.js';
import { import_specifier } from './nodes/import-specifier.js';
import { import_attribute } from './nodes/import-attribute.js';
import { expression_statement } from './nodes/expression-statement.js';
import { variable_declaration } from './nodes/variable-declaration.js';
import { lexical_declaration } from './nodes/lexical-declaration.js';
import { variable_declarator } from './nodes/variable-declarator.js';
import { statement_block } from './nodes/statement-block.js';
import { else_clause } from './nodes/else-clause.js';
import { if_statement } from './nodes/if-statement.js';
import { switch_statement } from './nodes/switch-statement.js';
import { for_statement } from './nodes/for-statement.js';
import { for_in_statement } from './nodes/for-in-statement.js';
import { while_statement } from './nodes/while-statement.js';
import { do_statement } from './nodes/do-statement.js';
import { try_statement } from './nodes/try-statement.js';
import { with_statement } from './nodes/with-statement.js';
import { break_statement } from './nodes/break-statement.js';
import { continue_statement } from './nodes/continue-statement.js';
import { return_statement } from './nodes/return-statement.js';
import { throw_statement } from './nodes/throw-statement.js';
import { switch_body } from './nodes/switch-body.js';
import { switch_case } from './nodes/switch-case.js';
import { switch_default } from './nodes/switch-default.js';
import { catch_clause } from './nodes/catch-clause.js';
import { finally_clause } from './nodes/finally-clause.js';
import { parenthesized_expression } from './nodes/parenthesized-expression.js';
import { yield_expression } from './nodes/yield-expression.js';
import { object } from './nodes/object.js';
import { object_pattern } from './nodes/object-pattern.js';
import { assignment_pattern } from './nodes/assignment-pattern.js';
import { object_assignment_pattern } from './nodes/object-assignment-pattern.js';
import { array } from './nodes/array.js';
import { array_pattern } from './nodes/array-pattern.js';
import { jsx_element } from './nodes/jsx-element.js';
import { jsx_expression } from './nodes/jsx-expression.js';
import { nested_identifier } from './nodes/nested-identifier.js';
import { jsx_namespace_name } from './nodes/jsx-namespace-name.js';
import { jsx_closing_element } from './nodes/jsx-closing-element.js';
import { jsx_attribute } from './nodes/jsx-attribute.js';
import { class_ } from './nodes/class.js';
import { class_declaration } from './nodes/class-declaration.js';
import { class_heritage } from './nodes/class-heritage.js';
import { function_expression } from './nodes/function-expression.js';
import { function_declaration } from './nodes/function-declaration.js';
import { generator_function } from './nodes/generator-function.js';
import { generator_function_declaration } from './nodes/generator-function-declaration.js';
import { arrow_function } from './nodes/arrow-function.js';
import { call_expression } from './nodes/call-expression.js';
import { new_expression } from './nodes/new-expression.js';
import { await_expression } from './nodes/await-expression.js';
import { member_expression } from './nodes/member-expression.js';
import { subscript_expression } from './nodes/subscript-expression.js';
import { assignment_expression } from './nodes/assignment-expression.js';
import { augmented_assignment_expression } from './nodes/augmented-assignment-expression.js';
import { spread_element } from './nodes/spread-element.js';
import { ternary_expression } from './nodes/ternary-expression.js';
import { binary_expression } from './nodes/binary-expression.js';
import { unary_expression } from './nodes/unary-expression.js';
import { update_expression } from './nodes/update-expression.js';
import { sequence_expression } from './nodes/sequence-expression.js';
import { string } from './nodes/string.js';
import { template_string } from './nodes/template-string.js';
import { template_substitution } from './nodes/template-substitution.js';
import { regex } from './nodes/regex.js';
import { arguments_ } from './nodes/arguments.js';
import { decorator } from './nodes/decorator.js';
import { decorator_member_expression } from './nodes/decorator-member-expression.js';
import { decorator_call_expression } from './nodes/decorator-call-expression.js';
import { class_body } from './nodes/class-body.js';
import { field_definition } from './nodes/field-definition.js';
import { formal_parameters } from './nodes/formal-parameters.js';
import { class_static_block } from './nodes/class-static-block.js';
import { rest_pattern } from './nodes/rest-pattern.js';
import { method_definition } from './nodes/method-definition.js';
import { pair } from './nodes/pair.js';
import { pair_pattern } from './nodes/pair-pattern.js';
import { computed_property_name } from './nodes/computed-property-name.js';
import { public_field_definition } from './nodes/public-field-definition.js';
import { non_null_expression } from './nodes/non-null-expression.js';
import { method_signature } from './nodes/method-signature.js';
import { abstract_method_signature } from './nodes/abstract-method-signature.js';
import { function_signature } from './nodes/function-signature.js';
import { decorator_parenthesized_expression } from './nodes/decorator-parenthesized-expression.js';
import { type_assertion } from './nodes/type-assertion.js';
import { as_expression } from './nodes/as-expression.js';
import { satisfies_expression } from './nodes/satisfies-expression.js';
import { instantiation_expression } from './nodes/instantiation-expression.js';
import { import_require_clause } from './nodes/import-require-clause.js';
import { extends_clause } from './nodes/extends-clause.js';
import { implements_clause } from './nodes/implements-clause.js';
import { ambient_declaration } from './nodes/ambient-declaration.js';
import { abstract_class_declaration } from './nodes/abstract-class-declaration.js';
import { module } from './nodes/module.js';
import { internal_module } from './nodes/internal-module.js';
import { import_alias } from './nodes/import-alias.js';
import { nested_type_identifier } from './nodes/nested-type-identifier.js';
import { interface_declaration } from './nodes/interface-declaration.js';
import { extends_type_clause } from './nodes/extends-type-clause.js';
import { enum_declaration } from './nodes/enum-declaration.js';
import { enum_body } from './nodes/enum-body.js';
import { enum_assignment } from './nodes/enum-assignment.js';
import { type_alias_declaration } from './nodes/type-alias-declaration.js';
import { required_parameter } from './nodes/required-parameter.js';
import { optional_parameter } from './nodes/optional-parameter.js';
import { omitting_type_annotation } from './nodes/omitting-type-annotation.js';
import { adding_type_annotation } from './nodes/adding-type-annotation.js';
import { opting_type_annotation } from './nodes/opting-type-annotation.js';
import { type_annotation } from './nodes/type-annotation.js';
import { asserts } from './nodes/asserts.js';
import { asserts_annotation } from './nodes/asserts-annotation.js';
import { tuple_parameter } from './nodes/tuple-parameter.js';
import { optional_tuple_parameter } from './nodes/optional-tuple-parameter.js';
import { optional_type } from './nodes/optional-type.js';
import { rest_type } from './nodes/rest-type.js';
import { constructor_type } from './nodes/constructor-type.js';
import { template_type } from './nodes/template-type.js';
import { template_literal_type } from './nodes/template-literal-type.js';
import { infer_type } from './nodes/infer-type.js';
import { conditional_type } from './nodes/conditional-type.js';
import { generic_type } from './nodes/generic-type.js';
import { type_predicate } from './nodes/type-predicate.js';
import { type_predicate_annotation } from './nodes/type-predicate-annotation.js';
import { type_query } from './nodes/type-query.js';
import { index_type_query } from './nodes/index-type-query.js';
import { lookup_type } from './nodes/lookup-type.js';
import { mapped_type_clause } from './nodes/mapped-type-clause.js';
import { literal_type } from './nodes/literal-type.js';
import { flow_maybe_type } from './nodes/flow-maybe-type.js';
import { parenthesized_type } from './nodes/parenthesized-type.js';
import { type_arguments } from './nodes/type-arguments.js';
import { object_type } from './nodes/object-type.js';
import { call_signature } from './nodes/call-signature.js';
import { property_signature } from './nodes/property-signature.js';
import { type_parameters } from './nodes/type-parameters.js';
import { type_parameter } from './nodes/type-parameter.js';
import { default_type } from './nodes/default-type.js';
import { constraint } from './nodes/constraint.js';
import { construct_signature } from './nodes/construct-signature.js';
import { index_signature } from './nodes/index-signature.js';
import { array_type } from './nodes/array-type.js';
import { tuple_type } from './nodes/tuple-type.js';
import { readonly_type } from './nodes/readonly-type.js';
import { union_type } from './nodes/union-type.js';
import { intersection_type } from './nodes/intersection-type.js';
import { function_type } from './nodes/function-type.js';
import { interface_body } from './nodes/interface-body.js';
import { labeled_statement } from './nodes/labeled-statement.js';

export const ir = {
  program,
  exportStatement: export_statement,
  namespaceExport: namespace_export,
  exportClause: export_clause,
  exportSpecifier: export_specifier,
  importStatement: import_statement,
  importClause: import_clause,
  namespaceImport: namespace_import,
  namedImports: named_imports,
  importSpecifier: import_specifier,
  importAttribute: import_attribute,
  expressionStatement: expression_statement,
  variableDeclaration: variable_declaration,
  lexicalDeclaration: lexical_declaration,
  variableDeclarator: variable_declarator,
  statementBlock: statement_block,
  elseClause: else_clause,
  ifStatement: if_statement,
  switchStatement: switch_statement,
  forStatement: for_statement,
  forInStatement: for_in_statement,
  whileStatement: while_statement,
  doStatement: do_statement,
  tryStatement: try_statement,
  withStatement: with_statement,
  breakStatement: break_statement,
  continueStatement: continue_statement,
  returnStatement: return_statement,
  throwStatement: throw_statement,
  switchBody: switch_body,
  switchCase: switch_case,
  switchDefault: switch_default,
  catchClause: catch_clause,
  finallyClause: finally_clause,
  parenthesizedExpression: parenthesized_expression,
  yieldExpression: yield_expression,
  object,
  objectPattern: object_pattern,
  assignmentPattern: assignment_pattern,
  objectAssignmentPattern: object_assignment_pattern,
  array,
  arrayPattern: array_pattern,
  jsxElement: jsx_element,
  jsxExpression: jsx_expression,
  nestedIdentifier: nested_identifier,
  jsxNamespaceName: jsx_namespace_name,
  jsxClosingElement: jsx_closing_element,
  jsxAttribute: jsx_attribute,
  class: class_,
  classDeclaration: class_declaration,
  classHeritage: class_heritage,
  functionExpression: function_expression,
  functionDeclaration: function_declaration,
  generatorFunction: generator_function,
  generatorFunctionDeclaration: generator_function_declaration,
  arrowFunction: arrow_function,
  callExpression: call_expression,
  newExpression: new_expression,
  awaitExpression: await_expression,
  memberExpression: member_expression,
  subscriptExpression: subscript_expression,
  assignmentExpression: assignment_expression,
  augmentedAssignmentExpression: augmented_assignment_expression,
  spreadElement: spread_element,
  ternaryExpression: ternary_expression,
  binaryExpression: binary_expression,
  unaryExpression: unary_expression,
  updateExpression: update_expression,
  sequenceExpression: sequence_expression,
  string,
  templateString: template_string,
  templateSubstitution: template_substitution,
  regex,
  arguments: arguments_,
  decorator,
  decoratorMemberExpression: decorator_member_expression,
  decoratorCallExpression: decorator_call_expression,
  classBody: class_body,
  fieldDefinition: field_definition,
  formalParameters: formal_parameters,
  classStaticBlock: class_static_block,
  restPattern: rest_pattern,
  methodDefinition: method_definition,
  pair,
  pairPattern: pair_pattern,
  computedPropertyName: computed_property_name,
  publicFieldDefinition: public_field_definition,
  nonNullExpression: non_null_expression,
  methodSignature: method_signature,
  abstractMethodSignature: abstract_method_signature,
  functionSignature: function_signature,
  decoratorParenthesizedExpression: decorator_parenthesized_expression,
  typeAssertion: type_assertion,
  asExpression: as_expression,
  satisfiesExpression: satisfies_expression,
  instantiationExpression: instantiation_expression,
  importRequireClause: import_require_clause,
  extendsClause: extends_clause,
  implementsClause: implements_clause,
  ambientDeclaration: ambient_declaration,
  abstractClassDeclaration: abstract_class_declaration,
  module,
  internalModule: internal_module,
  importAlias: import_alias,
  nestedTypeIdentifier: nested_type_identifier,
  interfaceDeclaration: interface_declaration,
  extendsTypeClause: extends_type_clause,
  enumDeclaration: enum_declaration,
  enumBody: enum_body,
  enumAssignment: enum_assignment,
  typeAliasDeclaration: type_alias_declaration,
  requiredParameter: required_parameter,
  optionalParameter: optional_parameter,
  omittingTypeAnnotation: omitting_type_annotation,
  addingTypeAnnotation: adding_type_annotation,
  optingTypeAnnotation: opting_type_annotation,
  typeAnnotation: type_annotation,
  asserts,
  assertsAnnotation: asserts_annotation,
  tupleParameter: tuple_parameter,
  optionalTupleParameter: optional_tuple_parameter,
  optionalType: optional_type,
  restType: rest_type,
  constructorType: constructor_type,
  templateType: template_type,
  templateLiteralType: template_literal_type,
  inferType: infer_type,
  conditionalType: conditional_type,
  genericType: generic_type,
  typePredicate: type_predicate,
  typePredicateAnnotation: type_predicate_annotation,
  typeQuery: type_query,
  indexTypeQuery: index_type_query,
  lookupType: lookup_type,
  mappedTypeClause: mapped_type_clause,
  literalType: literal_type,
  flowMaybeType: flow_maybe_type,
  parenthesizedType: parenthesized_type,
  typeArguments: type_arguments,
  objectType: object_type,
  callSignature: call_signature,
  propertySignature: property_signature,
  typeParameters: type_parameters,
  typeParameter: type_parameter,
  defaultType: default_type,
  constraint,
  constructSignature: construct_signature,
  indexSignature: index_signature,
  arrayType: array_type,
  tupleType: tuple_type,
  readonlyType: readonly_type,
  unionType: union_type,
  intersectionType: intersection_type,
  functionType: function_type,
  interfaceBody: interface_body,
  labeledStatement: labeled_statement,

  // Leaf node builders
  hashBangLine: (text: string) => new LeafBuilder('hash_bang_line', text),
  import: (text: string) => new LeafBuilder('import', text),
  emptyStatement: (text: string) => new LeafBuilder('empty_statement', text),
  htmlCharacterReference: (text: string) => new LeafBuilder('html_character_reference', text),
  jsxIdentifier: (text: string) => new LeafBuilder('jsx_identifier', text),
  unescapedDoubleJsxStringFragment: (text: string) => new LeafBuilder('unescaped_double_jsx_string_fragment', text),
  unescapedSingleJsxStringFragment: (text: string) => new LeafBuilder('unescaped_single_jsx_string_fragment', text),
  optionalChain: (text: string) => new LeafBuilder('optional_chain', text),
  unescapedDoubleStringFragment: (text: string) => new LeafBuilder('unescaped_double_string_fragment', text),
  unescapedSingleStringFragment: (text: string) => new LeafBuilder('unescaped_single_string_fragment', text),
  escapeSequence: (text: string) => new LeafBuilder('escape_sequence', text),
  comment: (text: string) => new LeafBuilder('comment', text),
  regexPattern: (text: string) => new LeafBuilder('regex_pattern', text),
  regexFlags: (text: string) => new LeafBuilder('regex_flags', text),
  number: (text: string) => new LeafBuilder('number', text),
  identifier: (text: string) => new LeafBuilder('identifier', text),
  privatePropertyIdentifier: (text: string) => new LeafBuilder('private_property_identifier', text),
  metaProperty: (text: string) => new LeafBuilder('meta_property', text),
  this: (text: string) => new LeafBuilder('this', text),
  super: (text: string) => new LeafBuilder('super', text),
  true: (text: string) => new LeafBuilder('true', text),
  false: (text: string) => new LeafBuilder('false', text),
  null: (text: string) => new LeafBuilder('null', text),
  undefined: (text: string) => new LeafBuilder('undefined', text),
  accessibilityModifier: (text: string) => new LeafBuilder('accessibility_modifier', text),
  overrideModifier: (text: string) => new LeafBuilder('override_modifier', text),
  existentialType: (text: string) => new LeafBuilder('existential_type', text),
  debuggerStatement: (text: string) => new LeafBuilder('debugger_statement', text),
  predefinedType: (text: string) => new LeafBuilder('predefined_type', text),
  htmlComment: (text: string) => new LeafBuilder('html_comment', text),
  propertyIdentifier: (text: string) => new LeafBuilder('property_identifier', text),
  shorthandPropertyIdentifier: (text: string) => new LeafBuilder('shorthand_property_identifier', text),
  shorthandPropertyIdentifierPattern: (text: string) => new LeafBuilder('shorthand_property_identifier_pattern', text),
  statementIdentifier: (text: string) => new LeafBuilder('statement_identifier', text),
  stringFragment: (text: string) => new LeafBuilder('string_fragment', text),
  thisType: (text: string) => new LeafBuilder('this_type', text),
  typeIdentifier: (text: string) => new LeafBuilder('type_identifier', text),

  // Semantic operator aliases
  and: () => new LeafBuilder('binary_expression_operator', '&&'),
  or: () => new LeafBuilder('binary_expression_operator', '||'),
  shr: () => new LeafBuilder('binary_expression_operator', '>>'),
  shl: () => new LeafBuilder('binary_expression_operator', '<<'),
  bitAnd: () => new LeafBuilder('binary_expression_operator', '&'),
  bitXor: () => new LeafBuilder('binary_expression_operator', '^'),
  bitOr: () => new LeafBuilder('binary_expression_operator', '|'),
  add: () => new LeafBuilder('binary_expression_operator', '+'),
  sub: () => new LeafBuilder('binary_expression_operator', '-'),
  mul: () => new LeafBuilder('binary_expression_operator', '*'),
  div: () => new LeafBuilder('binary_expression_operator', '/'),
  mod: () => new LeafBuilder('binary_expression_operator', '%'),
  lt: () => new LeafBuilder('binary_expression_operator', '<'),
  lte: () => new LeafBuilder('binary_expression_operator', '<='),
  eq: () => new LeafBuilder('binary_expression_operator', '=='),
  neq: () => new LeafBuilder('binary_expression_operator', '!='),
  gte: () => new LeafBuilder('binary_expression_operator', '>='),
  gt: () => new LeafBuilder('binary_expression_operator', '>'),

  // Namespaced access via supertypes
  declaration: {
    abstractClass: abstract_class_declaration,
    ambient: ambient_declaration,
    class: class_declaration,
    enum: enum_declaration,
    function: function_declaration,
    functionSignature: function_signature,
    generatorFunction: generator_function_declaration,
    importAlias: import_alias,
    interface: interface_declaration,
    internalModule: internal_module,
    lexical: lexical_declaration,
    module: module,
    typeAlias: type_alias_declaration,
    variable: variable_declaration,
  },
  expression: {
    as: as_expression,
    assignment: assignment_expression,
    augmentedAssignment: augmented_assignment_expression,
    await: await_expression,
    binary: binary_expression,
    instantiation: instantiation_expression,
    internalModule: internal_module,
    new: new_expression,
    satisfies: satisfies_expression,
    ternary: ternary_expression,
    typeAssertion: type_assertion,
    unary: unary_expression,
    update: update_expression,
    yield: yield_expression,
  },
  pattern: {
    array: array_pattern,
    memberExpression: member_expression,
    nonNullExpression: non_null_expression,
    object: object_pattern,
    rest: rest_pattern,
    subscriptExpression: subscript_expression,
  },
  primaryExpression: {
    array: array,
    arrowFunction: arrow_function,
    callExpression: call_expression,
    class: class_,
    functionExpression: function_expression,
    generatorFunction: generator_function,
    memberExpression: member_expression,
    nonNullExpression: non_null_expression,
    object: object,
    parenthesizedExpression: parenthesized_expression,
    regex: regex,
    string: string,
    subscriptExpression: subscript_expression,
    templateString: template_string,
  },
  primaryType: {
    arrayType: array_type,
    conditionalType: conditional_type,
    flowMaybeType: flow_maybe_type,
    genericType: generic_type,
    indexTypeQuery: index_type_query,
    intersectionType: intersection_type,
    literalType: literal_type,
    lookupType: lookup_type,
    nestedTypeIdentifier: nested_type_identifier,
    objectType: object_type,
    parenthesizedType: parenthesized_type,
    templateLiteralType: template_literal_type,
    tupleType: tuple_type,
    typeQuery: type_query,
    unionType: union_type,
  },
  statement: {
    break: break_statement,
    continue: continue_statement,
    do: do_statement,
    export: export_statement,
    expression: expression_statement,
    forIn: for_in_statement,
    for: for_statement,
    if: if_statement,
    import: import_statement,
    labeled: labeled_statement,
    return: return_statement,
    statementBlock: statement_block,
    switch: switch_statement,
    throw: throw_statement,
    try: try_statement,
    while: while_statement,
    with: with_statement,
  },
  type: {
    callExpression: call_expression,
    constructor: constructor_type,
    function: function_type,
    infer: infer_type,
    memberExpression: member_expression,
    readonly: readonly_type,
  },
} as const;

export type TypescriptIr = typeof ir;

import type { Builder, Edit, FieldKinds, FieldName, NodeTransform, NodeType } from '@sittir/types';
import type { TypescriptGrammar } from './types.js';

/** Tree-sitter CST node interface, parameterized by grammar node kind. */
export interface CSTNode<K extends keyof TypescriptGrammar = keyof TypescriptGrammar> {
  type: K;
  text: string;
  isNamed: boolean;
  startIndex: number;
  endIndex: number;
  namedChildren: CSTNode<keyof TypescriptGrammar>[];
  childForFieldName<F extends FieldName<TypescriptGrammar, K & string>>(name: F): CSTNode<FieldKinds<TypescriptGrammar, K & string, F> & keyof TypescriptGrammar> | null;
  childForFieldName(name: string): CSTNode<keyof TypescriptGrammar> | null;
  childrenForFieldName<F extends FieldName<TypescriptGrammar, K & string>>(name: F): CSTNode<FieldKinds<TypescriptGrammar, K & string, F> & keyof TypescriptGrammar>[];
  childrenForFieldName(name: string): CSTNode<keyof TypescriptGrammar>[];
}

/**
 * Hydrate a tree-sitter CST node into a builder tree.
 * Walks the CST recursively — no parsing needed.
 */
export function fromCST<K extends keyof TypescriptGrammar>(node: CSTNode<K>): Builder<NodeType<TypescriptGrammar, K>> {
  // Leaf / unknown nodes → LeafBuilder
  const builder = resolveBuilder(node);
  if (!builder) return new LeafBuilder(node.type, node.text) as any;
  return builder as any;
}

function resolveBuilder(node: CSTNode): Builder | null {
  switch (node.type) {
    case 'program': {
      const n = node as CSTNode<'program'>;
      const b = ir.program();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'export_statement': {
      const n = node as CSTNode<'export_statement'>;
      const b = ir.exportStatement(...n.namedChildren.map(c => fromCST(c) as any));
      const sourceChild = n.childForFieldName('source');
      if (sourceChild) b.source(fromCST(sourceChild) as any);
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const declarationChild = n.childForFieldName('declaration');
      if (declarationChild) b.declaration(fromCST(declarationChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'namespace_export': {
      const n = node as CSTNode<'namespace_export'>;
      const b = ir.namespaceExport(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'export_clause': {
      const n = node as CSTNode<'export_clause'>;
      const b = ir.exportClause();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'export_specifier': {
      const n = node as CSTNode<'export_specifier'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.exportSpecifier(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const aliasChild = n.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild) as any);
      return b;
    }
    case 'import_statement': {
      const n = node as CSTNode<'import_statement'>;
      const b = ir.importStatement(...n.namedChildren.map(c => fromCST(c) as any));
      const sourceChild = n.childForFieldName('source');
      if (sourceChild) b.source(fromCST(sourceChild) as any);
      return b;
    }
    case 'import_clause': {
      const n = node as CSTNode<'import_clause'>;
      const b = ir.importClause(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'namespace_import': {
      const n = node as CSTNode<'namespace_import'>;
      const firstChild = n.namedChildren[0];
      const b = ir.namespaceImport(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'named_imports': {
      const n = node as CSTNode<'named_imports'>;
      const b = ir.namedImports();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'import_specifier': {
      const n = node as CSTNode<'import_specifier'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.importSpecifier(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const aliasChild = n.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild) as any);
      return b;
    }
    case 'import_attribute': {
      const n = node as CSTNode<'import_attribute'>;
      const firstChild = n.namedChildren[0];
      const b = ir.importAttribute(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'expression_statement': {
      const n = node as CSTNode<'expression_statement'>;
      const b = ir.expressionStatement(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'variable_declaration': {
      const n = node as CSTNode<'variable_declaration'>;
      const b = ir.variableDeclaration(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'lexical_declaration': {
      const n = node as CSTNode<'lexical_declaration'>;
      const ctorChild = n.childForFieldName('kind');
      const b = ir.lexicalDeclaration(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('kind', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['kind'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'variable_declarator': {
      const n = node as CSTNode<'variable_declarator'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.variableDeclarator(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'statement_block': {
      const n = node as CSTNode<'statement_block'>;
      const b = ir.statementBlock();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'else_clause': {
      const n = node as CSTNode<'else_clause'>;
      const firstChild = n.namedChildren[0];
      const b = ir.elseClause(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'if_statement': {
      const n = node as CSTNode<'if_statement'>;
      const ctorChild = n.childForFieldName('condition');
      const b = ir.ifStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('condition', '') as any);
      const consequenceChild = n.childForFieldName('consequence');
      if (consequenceChild) b.consequence(fromCST(consequenceChild) as any);
      const alternativeChild = n.childForFieldName('alternative');
      if (alternativeChild) b.alternative(fromCST(alternativeChild) as any);
      return b;
    }
    case 'switch_statement': {
      const n = node as CSTNode<'switch_statement'>;
      const ctorChild = n.childForFieldName('value');
      const b = ir.switchStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('value', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'for_statement': {
      const n = node as CSTNode<'for_statement'>;
      const ctorChild = n.childForFieldName('initializer');
      const b = ir.forStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('initializer', '') as any);
      const conditionChild = n.childForFieldName('condition');
      if (conditionChild) b.condition(fromCST(conditionChild) as any);
      const incrementChild = n.childForFieldName('increment');
      if (incrementChild) b.increment(fromCST(incrementChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'for_in_statement': {
      const n = node as CSTNode<'for_in_statement'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.forInStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const kindChild = n.childForFieldName('kind');
      if (kindChild) b.kind(fromCST(kindChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'while_statement': {
      const n = node as CSTNode<'while_statement'>;
      const ctorChild = n.childForFieldName('condition');
      const b = ir.whileStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('condition', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'do_statement': {
      const n = node as CSTNode<'do_statement'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.doStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const conditionChild = n.childForFieldName('condition');
      if (conditionChild) b.condition(fromCST(conditionChild) as any);
      return b;
    }
    case 'try_statement': {
      const n = node as CSTNode<'try_statement'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.tryStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const handlerChild = n.childForFieldName('handler');
      if (handlerChild) b.handler(fromCST(handlerChild) as any);
      const finalizerChild = n.childForFieldName('finalizer');
      if (finalizerChild) b.finalizer(fromCST(finalizerChild) as any);
      return b;
    }
    case 'with_statement': {
      const n = node as CSTNode<'with_statement'>;
      const ctorChild = n.childForFieldName('object');
      const b = ir.withStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('object', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'break_statement': {
      const n = node as CSTNode<'break_statement'>;
      const b = ir.breakStatement();
      const labelChild = n.childForFieldName('label');
      if (labelChild) b.label(fromCST(labelChild) as any);
      return b;
    }
    case 'continue_statement': {
      const n = node as CSTNode<'continue_statement'>;
      const b = ir.continueStatement();
      const labelChild = n.childForFieldName('label');
      if (labelChild) b.label(fromCST(labelChild) as any);
      return b;
    }
    case 'return_statement': {
      const n = node as CSTNode<'return_statement'>;
      const b = ir.returnStatement();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'throw_statement': {
      const n = node as CSTNode<'throw_statement'>;
      const b = ir.throwStatement(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'switch_body': {
      const n = node as CSTNode<'switch_body'>;
      const b = ir.switchBody();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'switch_case': {
      const n = node as CSTNode<'switch_case'>;
      const ctorChild = n.childForFieldName('value');
      const b = ir.switchCase(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('value', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'switch_default': {
      const n = node as CSTNode<'switch_default'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.switchDefault(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      return b;
    }
    case 'catch_clause': {
      const n = node as CSTNode<'catch_clause'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.catchClause(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const parameterChild = n.childForFieldName('parameter');
      if (parameterChild) b.parameter(fromCST(parameterChild) as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      return b;
    }
    case 'finally_clause': {
      const n = node as CSTNode<'finally_clause'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.finallyClause(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      return b;
    }
    case 'parenthesized_expression': {
      const n = node as CSTNode<'parenthesized_expression'>;
      const b = ir.parenthesizedExpression(...n.namedChildren.map(c => fromCST(c) as any));
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      return b;
    }
    case 'yield_expression': {
      const n = node as CSTNode<'yield_expression'>;
      const b = ir.yieldExpression(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'object': {
      const n = node as CSTNode<'object'>;
      const b = ir.object();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'object_pattern': {
      const n = node as CSTNode<'object_pattern'>;
      const b = ir.objectPattern();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'assignment_pattern': {
      const n = node as CSTNode<'assignment_pattern'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.assignmentPattern(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'object_assignment_pattern': {
      const n = node as CSTNode<'object_assignment_pattern'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.objectAssignmentPattern(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'array': {
      const n = node as CSTNode<'array'>;
      const b = ir.array();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'array_pattern': {
      const n = node as CSTNode<'array_pattern'>;
      const b = ir.arrayPattern();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'jsx_element': {
      const n = node as CSTNode<'jsx_element'>;
      const ctorChild = n.childForFieldName('open_tag');
      const b = ir.jsxElement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('open_tag', '') as any);
      const closeTagChild = n.childForFieldName('close_tag');
      if (closeTagChild) b.closeTag(fromCST(closeTagChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['open_tag', 'close_tag'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'jsx_expression': {
      const n = node as CSTNode<'jsx_expression'>;
      const b = ir.jsxExpression();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'nested_identifier': {
      const n = node as CSTNode<'nested_identifier'>;
      const ctorChild = n.childForFieldName('object');
      const b = ir.nestedIdentifier(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('object', '') as any);
      const propertyChild = n.childForFieldName('property');
      if (propertyChild) b.property(fromCST(propertyChild) as any);
      return b;
    }
    case 'jsx_namespace_name': {
      const n = node as CSTNode<'jsx_namespace_name'>;
      const b = ir.jsxNamespaceName(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'jsx_closing_element': {
      const n = node as CSTNode<'jsx_closing_element'>;
      const b = ir.jsxClosingElement();
      const nameChild = n.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild) as any);
      return b;
    }
    case 'jsx_attribute': {
      const n = node as CSTNode<'jsx_attribute'>;
      const b = ir.jsxAttribute(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'class': {
      const n = node as CSTNode<'class'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.class(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const nameChild = n.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild) as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'name', 'type_parameters', 'body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'class_declaration': {
      const n = node as CSTNode<'class_declaration'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.classDeclaration(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'name', 'type_parameters', 'body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'class_heritage': {
      const n = node as CSTNode<'class_heritage'>;
      const b = ir.classHeritage(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'function_expression': {
      const n = node as CSTNode<'function_expression'>;
      const ctorChild = n.childForFieldName('parameters');
      const b = ir.functionExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('parameters', '') as any);
      const nameChild = n.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild) as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'function_declaration': {
      const n = node as CSTNode<'function_declaration'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.functionDeclaration(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const parametersChild = n.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'generator_function': {
      const n = node as CSTNode<'generator_function'>;
      const ctorChild = n.childForFieldName('parameters');
      const b = ir.generatorFunction(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('parameters', '') as any);
      const nameChild = n.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild) as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'generator_function_declaration': {
      const n = node as CSTNode<'generator_function_declaration'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.generatorFunctionDeclaration(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const parametersChild = n.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'arrow_function': {
      const n = node as CSTNode<'arrow_function'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.arrowFunction(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const parameterChild = n.childForFieldName('parameter');
      if (parameterChild) b.parameter(fromCST(parameterChild) as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const parametersChild = n.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      return b;
    }
    case 'call_expression': {
      const n = node as CSTNode<'call_expression'>;
      const ctorChild = n.childForFieldName('function');
      const b = ir.callExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('function', '') as any);
      const typeArgumentsChild = n.childForFieldName('type_arguments');
      if (typeArgumentsChild) b.typeArguments(fromCST(typeArgumentsChild) as any);
      const argumentsChild = n.childForFieldName('arguments');
      if (argumentsChild) b.arguments(fromCST(argumentsChild) as any);
      return b;
    }
    case 'new_expression': {
      const n = node as CSTNode<'new_expression'>;
      const ctorChild = n.childForFieldName('constructor');
      const b = ir.newExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('constructor', '') as any);
      const typeArgumentsChild = n.childForFieldName('type_arguments');
      if (typeArgumentsChild) b.typeArguments(fromCST(typeArgumentsChild) as any);
      const argumentsChild = n.childForFieldName('arguments');
      if (argumentsChild) b.arguments(fromCST(argumentsChild) as any);
      return b;
    }
    case 'await_expression': {
      const n = node as CSTNode<'await_expression'>;
      const firstChild = n.namedChildren[0];
      const b = ir.awaitExpression(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'member_expression': {
      const n = node as CSTNode<'member_expression'>;
      const ctorChild = n.childForFieldName('object');
      const b = ir.memberExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('object', '') as any);
      const optionalChainChild = n.childForFieldName('optional_chain');
      if (optionalChainChild) b.optionalChain(fromCST(optionalChainChild) as any);
      const propertyChild = n.childForFieldName('property');
      if (propertyChild) b.property(fromCST(propertyChild) as any);
      return b;
    }
    case 'subscript_expression': {
      const n = node as CSTNode<'subscript_expression'>;
      const ctorChild = n.childForFieldName('object');
      const b = ir.subscriptExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('object', '') as any);
      const optionalChainChild = n.childForFieldName('optional_chain');
      if (optionalChainChild) b.optionalChain(fromCST(optionalChainChild) as any);
      const indexChild = n.childForFieldName('index');
      if (indexChild) b.index(fromCST(indexChild) as any);
      return b;
    }
    case 'assignment_expression': {
      const n = node as CSTNode<'assignment_expression'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.assignmentExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'augmented_assignment_expression': {
      const n = node as CSTNode<'augmented_assignment_expression'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.augmentedAssignmentExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'spread_element': {
      const n = node as CSTNode<'spread_element'>;
      const firstChild = n.namedChildren[0];
      const b = ir.spreadElement(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'ternary_expression': {
      const n = node as CSTNode<'ternary_expression'>;
      const ctorChild = n.childForFieldName('condition');
      const b = ir.ternaryExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('condition', '') as any);
      const consequenceChild = n.childForFieldName('consequence');
      if (consequenceChild) b.consequence(fromCST(consequenceChild) as any);
      const alternativeChild = n.childForFieldName('alternative');
      if (alternativeChild) b.alternative(fromCST(alternativeChild) as any);
      return b;
    }
    case 'binary_expression': {
      const n = node as CSTNode<'binary_expression'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.binaryExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'unary_expression': {
      const n = node as CSTNode<'unary_expression'>;
      const ctorChild = n.childForFieldName('argument');
      const b = ir.unaryExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('argument', '') as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      return b;
    }
    case 'update_expression': {
      const n = node as CSTNode<'update_expression'>;
      const ctorChild = n.childForFieldName('argument');
      const b = ir.updateExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('argument', '') as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      return b;
    }
    case 'sequence_expression': {
      const n = node as CSTNode<'sequence_expression'>;
      const b = ir.sequenceExpression(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'string': {
      const n = node as CSTNode<'string'>;
      const b = ir.string();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'template_string': {
      const n = node as CSTNode<'template_string'>;
      const b = ir.templateString();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'template_substitution': {
      const n = node as CSTNode<'template_substitution'>;
      const b = ir.templateSubstitution(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'regex': {
      const n = node as CSTNode<'regex'>;
      const ctorChild = n.childForFieldName('pattern');
      const b = ir.regex(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('pattern', '') as any);
      const flagsChild = n.childForFieldName('flags');
      if (flagsChild) b.flags(fromCST(flagsChild) as any);
      return b;
    }
    case 'arguments': {
      const n = node as CSTNode<'arguments'>;
      const b = ir.arguments();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'decorator': {
      const n = node as CSTNode<'decorator'>;
      const b = ir.decorator(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'decorator_member_expression': {
      const n = node as CSTNode<'decorator_member_expression'>;
      const ctorChild = n.childForFieldName('object');
      const b = ir.decoratorMemberExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('object', '') as any);
      const propertyChild = n.childForFieldName('property');
      if (propertyChild) b.property(fromCST(propertyChild) as any);
      return b;
    }
    case 'decorator_call_expression': {
      const n = node as CSTNode<'decorator_call_expression'>;
      const ctorChild = n.childForFieldName('function');
      const b = ir.decoratorCallExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('function', '') as any);
      const typeArgumentsChild = n.childForFieldName('type_arguments');
      if (typeArgumentsChild) b.typeArguments(fromCST(typeArgumentsChild) as any);
      const argumentsChild = n.childForFieldName('arguments');
      if (argumentsChild) b.arguments(fromCST(argumentsChild) as any);
      return b;
    }
    case 'class_body': {
      const n = node as CSTNode<'class_body'>;
      const b = ir.classBody();
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['decorator'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'field_definition': {
      const n = node as CSTNode<'field_definition'>;
      const ctorChild = n.childForFieldName('property');
      const b = ir.fieldDefinition(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('property', '') as any);
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'formal_parameters': {
      const n = node as CSTNode<'formal_parameters'>;
      const b = ir.formalParameters();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'class_static_block': {
      const n = node as CSTNode<'class_static_block'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.classStaticBlock(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      return b;
    }
    case 'rest_pattern': {
      const n = node as CSTNode<'rest_pattern'>;
      const b = ir.restPattern(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'method_definition': {
      const n = node as CSTNode<'method_definition'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.methodDefinition(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const parametersChild = n.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['name', 'type_parameters', 'parameters', 'return_type', 'body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'pair': {
      const n = node as CSTNode<'pair'>;
      const ctorChild = n.childForFieldName('key');
      const b = ir.pair(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('key', '') as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'pair_pattern': {
      const n = node as CSTNode<'pair_pattern'>;
      const ctorChild = n.childForFieldName('key');
      const b = ir.pairPattern(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('key', '') as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'computed_property_name': {
      const n = node as CSTNode<'computed_property_name'>;
      const firstChild = n.namedChildren[0];
      const b = ir.computedPropertyName(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'public_field_definition': {
      const n = node as CSTNode<'public_field_definition'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.publicFieldDefinition(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'name', 'type', 'value'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'non_null_expression': {
      const n = node as CSTNode<'non_null_expression'>;
      const firstChild = n.namedChildren[0];
      const b = ir.nonNullExpression(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'method_signature': {
      const n = node as CSTNode<'method_signature'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.methodSignature(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const parametersChild = n.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['name', 'type_parameters', 'parameters', 'return_type'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'abstract_method_signature': {
      const n = node as CSTNode<'abstract_method_signature'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.abstractMethodSignature(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const parametersChild = n.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['name', 'type_parameters', 'parameters', 'return_type'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'function_signature': {
      const n = node as CSTNode<'function_signature'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.functionSignature(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const parametersChild = n.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      return b;
    }
    case 'decorator_parenthesized_expression': {
      const n = node as CSTNode<'decorator_parenthesized_expression'>;
      const b = ir.decoratorParenthesizedExpression(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'type_assertion': {
      const n = node as CSTNode<'type_assertion'>;
      const b = ir.typeAssertion(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'as_expression': {
      const n = node as CSTNode<'as_expression'>;
      const b = ir.asExpression(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'satisfies_expression': {
      const n = node as CSTNode<'satisfies_expression'>;
      const b = ir.satisfiesExpression(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'instantiation_expression': {
      const n = node as CSTNode<'instantiation_expression'>;
      const ctorChild = n.childForFieldName('type_arguments');
      const b = ir.instantiationExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('type_arguments', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['type_arguments'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'import_require_clause': {
      const n = node as CSTNode<'import_require_clause'>;
      const ctorChild = n.childForFieldName('source');
      const b = ir.importRequireClause(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('source', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['source'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'extends_clause': {
      const n = node as CSTNode<'extends_clause'>;
      const b = ir.extendsClause();
      const valueChildren = n.childrenForFieldName('value');
      if (valueChildren.length > 0) b.value(...valueChildren.map(c => fromCST(c) as any));
      const typeArgumentsChildren = n.childrenForFieldName('type_arguments');
      if (typeArgumentsChildren.length > 0) b.typeArguments(...typeArgumentsChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'implements_clause': {
      const n = node as CSTNode<'implements_clause'>;
      const b = ir.implementsClause(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'ambient_declaration': {
      const n = node as CSTNode<'ambient_declaration'>;
      const b = ir.ambientDeclaration(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'abstract_class_declaration': {
      const n = node as CSTNode<'abstract_class_declaration'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.abstractClassDeclaration(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'name', 'type_parameters', 'body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'module': {
      const n = node as CSTNode<'module'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.module(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'internal_module': {
      const n = node as CSTNode<'internal_module'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.internalModule(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'import_alias': {
      const n = node as CSTNode<'import_alias'>;
      const b = ir.importAlias(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'nested_type_identifier': {
      const n = node as CSTNode<'nested_type_identifier'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.nestedTypeIdentifier(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const moduleChild = n.childForFieldName('module');
      if (moduleChild) b.module(fromCST(moduleChild) as any);
      return b;
    }
    case 'interface_declaration': {
      const n = node as CSTNode<'interface_declaration'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.interfaceDeclaration(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['name', 'type_parameters', 'body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'extends_type_clause': {
      const n = node as CSTNode<'extends_type_clause'>;
      const b = ir.extendsTypeClause();
      const typeChildren = n.childrenForFieldName('type');
      if (typeChildren.length > 0) b.type(...typeChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'enum_declaration': {
      const n = node as CSTNode<'enum_declaration'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.enumDeclaration(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'enum_body': {
      const n = node as CSTNode<'enum_body'>;
      const b = ir.enumBody();
      const nameChildren = n.childrenForFieldName('name');
      if (nameChildren.length > 0) b.name(...nameChildren.map(c => fromCST(c) as any));
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['name'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'enum_assignment': {
      const n = node as CSTNode<'enum_assignment'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.enumAssignment(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'type_alias_declaration': {
      const n = node as CSTNode<'type_alias_declaration'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.typeAliasDeclaration(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'required_parameter': {
      const n = node as CSTNode<'required_parameter'>;
      const ctorChild = n.childForFieldName('pattern');
      const b = ir.requiredParameter(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('pattern', '') as any);
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'pattern', 'type', 'value'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'optional_parameter': {
      const n = node as CSTNode<'optional_parameter'>;
      const ctorChild = n.childForFieldName('pattern');
      const b = ir.optionalParameter(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('pattern', '') as any);
      const decoratorChildren = n.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(...decoratorChildren.map(c => fromCST(c) as any));
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'pattern', 'type', 'value'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'omitting_type_annotation': {
      const n = node as CSTNode<'omitting_type_annotation'>;
      const firstChild = n.namedChildren[0];
      const b = ir.omittingTypeAnnotation(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'adding_type_annotation': {
      const n = node as CSTNode<'adding_type_annotation'>;
      const firstChild = n.namedChildren[0];
      const b = ir.addingTypeAnnotation(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'opting_type_annotation': {
      const n = node as CSTNode<'opting_type_annotation'>;
      const firstChild = n.namedChildren[0];
      const b = ir.optingTypeAnnotation(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'type_annotation': {
      const n = node as CSTNode<'type_annotation'>;
      const firstChild = n.namedChildren[0];
      const b = ir.typeAnnotation(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'asserts': {
      const n = node as CSTNode<'asserts'>;
      const b = ir.asserts(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'asserts_annotation': {
      const n = node as CSTNode<'asserts_annotation'>;
      const firstChild = n.namedChildren[0];
      const b = ir.assertsAnnotation(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'tuple_parameter': {
      const n = node as CSTNode<'tuple_parameter'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.tupleParameter(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      return b;
    }
    case 'optional_tuple_parameter': {
      const n = node as CSTNode<'optional_tuple_parameter'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.optionalTupleParameter(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      return b;
    }
    case 'optional_type': {
      const n = node as CSTNode<'optional_type'>;
      const firstChild = n.namedChildren[0];
      const b = ir.optionalType(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'rest_type': {
      const n = node as CSTNode<'rest_type'>;
      const firstChild = n.namedChildren[0];
      const b = ir.restType(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'constructor_type': {
      const n = node as CSTNode<'constructor_type'>;
      const ctorChild = n.childForFieldName('parameters');
      const b = ir.constructorType(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('parameters', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      return b;
    }
    case 'template_type': {
      const n = node as CSTNode<'template_type'>;
      const b = ir.templateType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'template_literal_type': {
      const n = node as CSTNode<'template_literal_type'>;
      const b = ir.templateLiteralType();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'infer_type': {
      const n = node as CSTNode<'infer_type'>;
      const b = ir.inferType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'conditional_type': {
      const n = node as CSTNode<'conditional_type'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.conditionalType(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      const consequenceChild = n.childForFieldName('consequence');
      if (consequenceChild) b.consequence(fromCST(consequenceChild) as any);
      const alternativeChild = n.childForFieldName('alternative');
      if (alternativeChild) b.alternative(fromCST(alternativeChild) as any);
      return b;
    }
    case 'generic_type': {
      const n = node as CSTNode<'generic_type'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.genericType(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeArgumentsChild = n.childForFieldName('type_arguments');
      if (typeArgumentsChild) b.typeArguments(fromCST(typeArgumentsChild) as any);
      return b;
    }
    case 'type_predicate': {
      const n = node as CSTNode<'type_predicate'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.typePredicate(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      return b;
    }
    case 'type_predicate_annotation': {
      const n = node as CSTNode<'type_predicate_annotation'>;
      const firstChild = n.namedChildren[0];
      const b = ir.typePredicateAnnotation(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'type_query': {
      const n = node as CSTNode<'type_query'>;
      const b = ir.typeQuery(...n.namedChildren.map(c => fromCST(c) as any));
      const objectChild = n.childForFieldName('object');
      if (objectChild) b.object(fromCST(objectChild) as any);
      const indexChild = n.childForFieldName('index');
      if (indexChild) b.index(fromCST(indexChild) as any);
      const propertyChild = n.childForFieldName('property');
      if (propertyChild) b.property(fromCST(propertyChild) as any);
      const functionChild = n.childForFieldName('function');
      if (functionChild) b.function(fromCST(functionChild) as any);
      const argumentsChild = n.childForFieldName('arguments');
      if (argumentsChild) b.arguments(fromCST(argumentsChild) as any);
      const typeArgumentsChild = n.childForFieldName('type_arguments');
      if (typeArgumentsChild) b.typeArguments(fromCST(typeArgumentsChild) as any);
      return b;
    }
    case 'index_type_query': {
      const n = node as CSTNode<'index_type_query'>;
      const firstChild = n.namedChildren[0];
      const b = ir.indexTypeQuery(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'lookup_type': {
      const n = node as CSTNode<'lookup_type'>;
      const b = ir.lookupType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'mapped_type_clause': {
      const n = node as CSTNode<'mapped_type_clause'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.mappedTypeClause(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      const aliasChild = n.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild) as any);
      return b;
    }
    case 'literal_type': {
      const n = node as CSTNode<'literal_type'>;
      const b = ir.literalType(...n.namedChildren.map(c => fromCST(c) as any));
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      const argumentChild = n.childForFieldName('argument');
      if (argumentChild) b.argument(fromCST(argumentChild) as any);
      return b;
    }
    case 'flow_maybe_type': {
      const n = node as CSTNode<'flow_maybe_type'>;
      const firstChild = n.namedChildren[0];
      const b = ir.flowMaybeType(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'parenthesized_type': {
      const n = node as CSTNode<'parenthesized_type'>;
      const firstChild = n.namedChildren[0];
      const b = ir.parenthesizedType(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'type_arguments': {
      const n = node as CSTNode<'type_arguments'>;
      const b = ir.typeArguments(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'object_type': {
      const n = node as CSTNode<'object_type'>;
      const b = ir.objectType();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'call_signature': {
      const n = node as CSTNode<'call_signature'>;
      const ctorChild = n.childForFieldName('parameters');
      const b = ir.callSignature(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('parameters', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      return b;
    }
    case 'property_signature': {
      const n = node as CSTNode<'property_signature'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.propertySignature(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['name', 'type'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'type_parameters': {
      const n = node as CSTNode<'type_parameters'>;
      const b = ir.typeParameters(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'type_parameter': {
      const n = node as CSTNode<'type_parameter'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.typeParameter(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const constraintChild = n.childForFieldName('constraint');
      if (constraintChild) b.constraint(fromCST(constraintChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'default_type': {
      const n = node as CSTNode<'default_type'>;
      const firstChild = n.namedChildren[0];
      const b = ir.defaultType(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'constraint': {
      const n = node as CSTNode<'constraint'>;
      const firstChild = n.namedChildren[0];
      const b = ir.constraint(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'construct_signature': {
      const n = node as CSTNode<'construct_signature'>;
      const ctorChild = n.childForFieldName('parameters');
      const b = ir.constructSignature(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('parameters', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      return b;
    }
    case 'index_signature': {
      const n = node as CSTNode<'index_signature'>;
      const ctorChild = n.childForFieldName('type');
      const b = ir.indexSignature(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('type', '') as any);
      const signChild = n.childForFieldName('sign');
      if (signChild) b.sign(fromCST(signChild) as any);
      const nameChild = n.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild) as any);
      const indexTypeChild = n.childForFieldName('index_type');
      if (indexTypeChild) b.indexType(fromCST(indexTypeChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['sign', 'name', 'index_type', 'type'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'array_type': {
      const n = node as CSTNode<'array_type'>;
      const firstChild = n.namedChildren[0];
      const b = ir.arrayType(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'tuple_type': {
      const n = node as CSTNode<'tuple_type'>;
      const b = ir.tupleType();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'readonly_type': {
      const n = node as CSTNode<'readonly_type'>;
      const firstChild = n.namedChildren[0];
      const b = ir.readonlyType(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'union_type': {
      const n = node as CSTNode<'union_type'>;
      const b = ir.unionType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'intersection_type': {
      const n = node as CSTNode<'intersection_type'>;
      const b = ir.intersectionType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'function_type': {
      const n = node as CSTNode<'function_type'>;
      const ctorChild = n.childForFieldName('parameters');
      const b = ir.functionType(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('parameters', '') as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      return b;
    }
    case 'interface_body': {
      const n = node as CSTNode<'interface_body'>;
      const b = ir.interfaceBody();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'labeled_statement': {
      const n = node as CSTNode<'labeled_statement'>;
      const b = ir.labeledStatement();
      return b;
    }
    default: return null;
  }
}

/**
 * Create a codemod-compatible Edit from a tree-sitter node.
 * Hydrates the node into a builder, passes it to the transform,
 * then renders the result and wraps it with byte positions.
 */
export function edit<K extends keyof TypescriptGrammar>(
  node: CSTNode<K>,
  transform: NodeTransform<NodeType<TypescriptGrammar, K>>,
): Edit {
  const builder = fromCST(node);
  const result = transform(builder);
  return {
    startPos: node.startIndex,
    endPos: node.endIndex,
    insertedText: result.renderImpl(),
  };
}

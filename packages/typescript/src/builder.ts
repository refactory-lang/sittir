// Auto-generated by ir-codegen — do not edit

import { LeafBuilder } from '@sittir/types';
import { abstract_class } from './nodes/abstract-class.js';
import { abstract_method_signature } from './nodes/abstract-method-signature.js';
import { adding_type_annotation } from './nodes/adding-type-annotation.js';
import { ambient } from './nodes/ambient.js';
import { arguments_ } from './nodes/arguments.js';
import { array } from './nodes/array.js';
import { array_pattern } from './nodes/array-pattern.js';
import { array_type } from './nodes/array-type.js';
import { arrow_function } from './nodes/arrow-function.js';
import { as } from './nodes/as.js';
import { asserts_annotation } from './nodes/asserts-annotation.js';
import { assignment } from './nodes/assignment.js';
import { assignment_pattern } from './nodes/assignment-pattern.js';
import { augmented_assignment } from './nodes/augmented-assignment.js';
import { await_ } from './nodes/await.js';
import { binary } from './nodes/binary.js';
import { break_ } from './nodes/break.js';
import { call } from './nodes/call.js';
import { call_signature } from './nodes/call-signature.js';
import { catch_clause } from './nodes/catch-clause.js';
import { class_body } from './nodes/class-body.js';
import { class_ } from './nodes/class.js';
import { class_heritage } from './nodes/class-heritage.js';
import { class_static_block } from './nodes/class-static-block.js';
import { computed_property_name } from './nodes/computed-property-name.js';
import { conditional_type } from './nodes/conditional-type.js';
import { constraint } from './nodes/constraint.js';
import { construct_signature } from './nodes/construct-signature.js';
import { constructor_type } from './nodes/constructor-type.js';
import { continue_ } from './nodes/continue.js';
import { decorator } from './nodes/decorator.js';
import { default_type } from './nodes/default-type.js';
import { do_ } from './nodes/do.js';
import { else_clause } from './nodes/else-clause.js';
import { enum_assignment } from './nodes/enum-assignment.js';
import { enum_body } from './nodes/enum-body.js';
import { enum_ } from './nodes/enum.js';
import { export_clause } from './nodes/export-clause.js';
import { export_specifier } from './nodes/export-specifier.js';
import { export_ } from './nodes/export.js';
import { expression } from './nodes/expression.js';
import { extends_clause } from './nodes/extends-clause.js';
import { extends_type_clause } from './nodes/extends-type-clause.js';
import { finally_clause } from './nodes/finally-clause.js';
import { flow_maybe_type } from './nodes/flow-maybe-type.js';
import { for_in } from './nodes/for-in.js';
import { for_ } from './nodes/for.js';
import { formal_parameters } from './nodes/formal-parameters.js';
import { function_ as functionDeclaration } from './nodes/function-declaration.js';
import { function_ as functionExpression } from './nodes/function-expression.js';
import { function_signature } from './nodes/function-signature.js';
import { function_type } from './nodes/function-type.js';
import { generator_function as generatorFunction } from './nodes/generator-function.js';
import { generator_function as generatorFunctionDeclaration } from './nodes/generator-function-declaration.js';
import { generic_type } from './nodes/generic-type.js';
import { if_ } from './nodes/if.js';
import { implements_clause } from './nodes/implements-clause.js';
import { import_alias } from './nodes/import-alias.js';
import { import_attribute } from './nodes/import-attribute.js';
import { import_clause } from './nodes/import-clause.js';
import { import_require_clause } from './nodes/import-require-clause.js';
import { import_specifier } from './nodes/import-specifier.js';
import { import_ } from './nodes/import.js';
import { index_signature } from './nodes/index-signature.js';
import { index_type_query } from './nodes/index-type-query.js';
import { infer_type } from './nodes/infer-type.js';
import { instantiation } from './nodes/instantiation.js';
import { interface_body } from './nodes/interface-body.js';
import { interface_ } from './nodes/interface.js';
import { internal_module } from './nodes/internal-module.js';
import { intersection_type } from './nodes/intersection-type.js';
import { labeled } from './nodes/labeled.js';
import { lexical } from './nodes/lexical.js';
import { literal_type } from './nodes/literal-type.js';
import { lookup_type } from './nodes/lookup-type.js';
import { mapped_type_clause } from './nodes/mapped-type-clause.js';
import { member } from './nodes/member.js';
import { method_definition } from './nodes/method-definition.js';
import { method_signature } from './nodes/method-signature.js';
import { named_imports } from './nodes/named-imports.js';
import { namespace_export } from './nodes/namespace-export.js';
import { namespace_import } from './nodes/namespace-import.js';
import { nested_identifier } from './nodes/nested-identifier.js';
import { nested_type_identifier } from './nodes/nested-type-identifier.js';
import { new_ } from './nodes/new.js';
import { non_null } from './nodes/non-null.js';
import { object_assignment_pattern } from './nodes/object-assignment-pattern.js';
import { object_pattern } from './nodes/object-pattern.js';
import { object_type } from './nodes/object-type.js';
import { omitting_type_annotation } from './nodes/omitting-type-annotation.js';
import { opting_type_annotation } from './nodes/opting-type-annotation.js';
import { optional_parameter } from './nodes/optional-parameter.js';
import { optional_type } from './nodes/optional-type.js';
import { pair } from './nodes/pair.js';
import { pair_pattern } from './nodes/pair-pattern.js';
import { parenthesized } from './nodes/parenthesized.js';
import { parenthesized_type } from './nodes/parenthesized-type.js';
import { file } from './nodes/program.js';
import { property_signature } from './nodes/property-signature.js';
import { public_field_definition } from './nodes/public-field-definition.js';
import { readonly_type } from './nodes/readonly-type.js';
import { regex } from './nodes/regex.js';
import { required_parameter } from './nodes/required-parameter.js';
import { rest_pattern } from './nodes/rest-pattern.js';
import { rest_type } from './nodes/rest-type.js';
import { return_ } from './nodes/return.js';
import { satisfies } from './nodes/satisfies.js';
import { sequence } from './nodes/sequence.js';
import { spread_element } from './nodes/spread-element.js';
import { statement_block } from './nodes/statement-block.js';
import { subscript } from './nodes/subscript.js';
import { switch_body } from './nodes/switch-body.js';
import { switch_case } from './nodes/switch-case.js';
import { switch_default } from './nodes/switch-default.js';
import { switch_ } from './nodes/switch.js';
import { template_literal_type } from './nodes/template-literal-type.js';
import { template_string } from './nodes/template-string.js';
import { template_substitution } from './nodes/template-substitution.js';
import { template_type } from './nodes/template-type.js';
import { ternary } from './nodes/ternary.js';
import { throw_ } from './nodes/throw.js';
import { try_ } from './nodes/try.js';
import { tuple_type } from './nodes/tuple-type.js';
import { type_alias } from './nodes/type-alias.js';
import { type_annotation } from './nodes/type-annotation.js';
import { type_arguments } from './nodes/type-arguments.js';
import { type_assertion } from './nodes/type-assertion.js';
import { type_parameter } from './nodes/type-parameter.js';
import { type_parameters } from './nodes/type-parameters.js';
import { type_predicate } from './nodes/type-predicate.js';
import { type_predicate_annotation } from './nodes/type-predicate-annotation.js';
import { type_query } from './nodes/type-query.js';
import { unary } from './nodes/unary.js';
import { union_type } from './nodes/union-type.js';
import { update } from './nodes/update.js';
import { variable } from './nodes/variable.js';
import { variable_declarator } from './nodes/variable-declarator.js';
import { while_ } from './nodes/while.js';
import { with_ } from './nodes/with.js';
import { yield_ } from './nodes/yield.js';

export const ir = {
  abstract_class,
  abstract_method_signature,
  adding_type_annotation,
  ambient,
  arguments: arguments_,
  array,
  array_pattern,
  array_type,
  arrow_function,
  as,
  asserts_annotation,
  assignment,
  assignment_pattern,
  augmented_assignment,
  await: await_,
  binary,
  break: break_,
  call,
  call_signature,
  catch_clause,
  class_body,
  class: class_,
  class_heritage,
  class_static_block,
  computed_property_name,
  conditional_type,
  constraint,
  construct_signature,
  constructor_type,
  continue: continue_,
  decorator,
  default_type,
  do: do_,
  else_clause,
  enum_assignment,
  enum_body,
  enum: enum_,
  export_clause,
  export_specifier,
  export: export_,
  expression,
  extends_clause,
  extends_type_clause,
  finally_clause,
  flow_maybe_type,
  for_in,
  for: for_,
  formal_parameters,
  functionDeclaration,
  functionExpression,
  function_signature,
  function_type,
  generatorFunction,
  generatorFunctionDeclaration,
  generic_type,
  if: if_,
  implements_clause,
  import_alias,
  import_attribute,
  import_clause,
  import_require_clause,
  import_specifier,
  import: import_,
  index_signature,
  index_type_query,
  infer_type,
  instantiation,
  interface_body,
  interface: interface_,
  internal_module,
  intersection_type,
  labeled,
  lexical,
  literal_type,
  lookup_type,
  mapped_type_clause,
  member,
  method_definition,
  method_signature,
  named_imports,
  namespace_export,
  namespace_import,
  nested_identifier,
  nested_type_identifier,
  new: new_,
  non_null,
  object_assignment_pattern,
  object_pattern,
  object_type,
  omitting_type_annotation,
  opting_type_annotation,
  optional_parameter,
  optional_type,
  pair,
  pair_pattern,
  parenthesized,
  parenthesized_type,
  file,
  property_signature,
  public_field_definition,
  readonly_type,
  regex,
  required_parameter,
  rest_pattern,
  rest_type,
  return: return_,
  satisfies,
  sequence,
  spread_element,
  statement_block,
  subscript,
  switch_body,
  switch_case,
  switch_default,
  switch: switch_,
  template_literal_type,
  template_string,
  template_substitution,
  template_type,
  ternary,
  throw: throw_,
  try: try_,
  tuple_type,
  type_alias,
  type_annotation,
  type_arguments,
  type_assertion,
  type_parameter,
  type_parameters,
  type_predicate,
  type_predicate_annotation,
  type_query,
  unary,
  union_type,
  update,
  variable,
  variable_declarator,
  while: while_,
  with: with_,
  yield: yield_,

  // Leaf node builders
  accessibilityModifier: (text: string) => new LeafBuilder('accessibility_modifier', text),
  debuggerStatement: (text: string) => new LeafBuilder('debugger_statement', text),
  emptyStatement: (text: string) => new LeafBuilder('empty_statement', text),
  existentialType: (text: string) => new LeafBuilder('existential_type', text),
  identifier: (text: string) => new LeafBuilder('identifier', text),
  metaProperty: (text: string) => new LeafBuilder('meta_property', text),
  optionalChain: (text: string) => new LeafBuilder('optional_chain', text),
  overrideModifier: (text: string) => new LeafBuilder('override_modifier', text),
  predefinedType: (text: string) => new LeafBuilder('predefined_type', text),
  comment: (text: string) => new LeafBuilder('comment', text),
  escapeSequence: (text: string) => new LeafBuilder('escape_sequence', text),
  false: (text: string) => new LeafBuilder('false', text),
  hashBangLine: (text: string) => new LeafBuilder('hash_bang_line', text),
  htmlComment: (text: string) => new LeafBuilder('html_comment', text),
  null: (text: string) => new LeafBuilder('null', text),
  privatePropertyIdentifier: (text: string) => new LeafBuilder('private_property_identifier', text),
  propertyIdentifier: (text: string) => new LeafBuilder('property_identifier', text),
  regexFlags: (text: string) => new LeafBuilder('regex_flags', text),
  regexPattern: (text: string) => new LeafBuilder('regex_pattern', text),
  shorthandPropertyIdentifier: (text: string) => new LeafBuilder('shorthand_property_identifier', text),
  shorthandPropertyIdentifierPattern: (text: string) => new LeafBuilder('shorthand_property_identifier_pattern', text),
  statementIdentifier: (text: string) => new LeafBuilder('statement_identifier', text),
  stringFragment: (text: string) => new LeafBuilder('string_fragment', text),
  super: (text: string) => new LeafBuilder('super', text),
  this: (text: string) => new LeafBuilder('this', text),
  thisType: (text: string) => new LeafBuilder('this_type', text),
  true: (text: string) => new LeafBuilder('true', text),
  typeIdentifier: (text: string) => new LeafBuilder('type_identifier', text),
  undefined: (text: string) => new LeafBuilder('undefined', text),

  // Semantic operator aliases
  neq: () => new LeafBuilder('binary_expression_operator', '!='),
  mod: () => new LeafBuilder('binary_expression_operator', '%'),
  bitAnd: () => new LeafBuilder('binary_expression_operator', '&'),
  and: () => new LeafBuilder('binary_expression_operator', '&&'),
  mul: () => new LeafBuilder('binary_expression_operator', '*'),
  add: () => new LeafBuilder('binary_expression_operator', '+'),
  sub: () => new LeafBuilder('binary_expression_operator', '-'),
  div: () => new LeafBuilder('binary_expression_operator', '/'),
  lt: () => new LeafBuilder('binary_expression_operator', '<'),
  shl: () => new LeafBuilder('binary_expression_operator', '<<'),
  lte: () => new LeafBuilder('binary_expression_operator', '<='),
  eq: () => new LeafBuilder('binary_expression_operator', '=='),
  gt: () => new LeafBuilder('binary_expression_operator', '>'),
  gte: () => new LeafBuilder('binary_expression_operator', '>='),
  shr: () => new LeafBuilder('binary_expression_operator', '>>'),
  bitXor: () => new LeafBuilder('binary_expression_operator', '^'),
  bitOr: () => new LeafBuilder('binary_expression_operator', '|'),
  or: () => new LeafBuilder('binary_expression_operator', '||'),
} as const;

export type TypescriptIr = typeof ir;

import type { BaseBuilder } from '@sittir/types';

/** Tree-sitter CST node interface (minimal shape). */
interface CSTInput {
  type: string;
  text: string;
  isNamed: boolean;
  namedChildren: CSTInput[];
  childForFieldName(name: string): CSTInput | null;
  childrenForFieldName(name: string): CSTInput[];
}

/**
 * Hydrate a tree-sitter CST node into a builder tree.
 * Walks the CST recursively — no parsing needed.
 */
export function fromCST(node: CSTInput): BaseBuilder<{ kind: string }> {
  // Leaf / unknown nodes → LeafBuilder
  const builder = resolveBuilder(node);
  if (!builder) return new LeafBuilder(node.type, node.text);
  return builder;
}

function resolveBuilder(node: CSTInput): BaseBuilder<{ kind: string }> | null {
  switch (node.type) {
    case 'abstract_class_declaration': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.abstract_class(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const bodyChild = node.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild));
      const decoratorChildren = node.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(decoratorChildren.map(c => fromCST(c)));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['body', 'decorator', 'name', 'type_parameters'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'abstract_method_signature': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.abstract_method_signature(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['name', 'parameters', 'return_type', 'type_parameters'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'adding_type_annotation': {
      const firstChild = node.namedChildren[0];
      const b = ir.adding_type_annotation(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'ambient_declaration': {
      const b = ir.ambient(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'arguments': {
      const b = ir.arguments();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'array': {
      const b = ir.array();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'array_pattern': {
      const b = ir.array_pattern();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'array_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.array_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'arrow_function': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.arrow_function(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const parameterChild = node.childForFieldName('parameter');
      if (parameterChild) b.parameter(fromCST(parameterChild));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'as_expression': {
      const b = ir.as(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'asserts_annotation': {
      const firstChild = node.namedChildren[0];
      const b = ir.asserts_annotation(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'assignment_expression': {
      const ctorChild = node.childForFieldName('left');
      const b = ir.assignment(ctorChild ? fromCST(ctorChild) : new LeafBuilder('left', ''));
      const rightChild = node.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild));
      return b;
    }
    case 'assignment_pattern': {
      const ctorChild = node.childForFieldName('left');
      const b = ir.assignment_pattern(ctorChild ? fromCST(ctorChild) : new LeafBuilder('left', ''));
      const rightChild = node.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild));
      return b;
    }
    case 'augmented_assignment_expression': {
      const ctorChild = node.childForFieldName('left');
      const b = ir.augmented_assignment(ctorChild ? fromCST(ctorChild) : new LeafBuilder('left', ''));
      const operatorChild = node.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild));
      const rightChild = node.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild));
      return b;
    }
    case 'await_expression': {
      const firstChild = node.namedChildren[0];
      const b = ir.await(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'binary_expression': {
      const ctorChild = node.childForFieldName('left');
      const b = ir.binary(ctorChild ? fromCST(ctorChild) : new LeafBuilder('left', ''));
      const operatorChild = node.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild));
      const rightChild = node.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild));
      return b;
    }
    case 'break_statement': {
      const b = ir.break();
      const labelChild = node.childForFieldName('label');
      if (labelChild) b.label(fromCST(labelChild));
      return b;
    }
    case 'call_expression': {
      const ctorChild = node.childForFieldName('arguments');
      const b = ir.call(ctorChild ? fromCST(ctorChild) : new LeafBuilder('arguments', ''));
      const functionChild = node.childForFieldName('function');
      if (functionChild) b.function(fromCST(functionChild));
      const typeArgumentsChild = node.childForFieldName('type_arguments');
      if (typeArgumentsChild) b.typeArguments(fromCST(typeArgumentsChild));
      return b;
    }
    case 'call_signature': {
      const ctorChild = node.childForFieldName('parameters');
      const b = ir.call_signature(ctorChild ? fromCST(ctorChild) : new LeafBuilder('parameters', ''));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'catch_clause': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.catch_clause(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const parameterChild = node.childForFieldName('parameter');
      if (parameterChild) b.parameter(fromCST(parameterChild));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      return b;
    }
    case 'class_body': {
      const b = ir.class_body();
      const decoratorChildren = node.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(decoratorChildren.map(c => fromCST(c)));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['decorator'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'class_declaration': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.class(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const bodyChild = node.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild));
      const decoratorChildren = node.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(decoratorChildren.map(c => fromCST(c)));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['body', 'decorator', 'name', 'type_parameters'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'class_heritage': {
      const b = ir.class_heritage(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'class_static_block': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.class_static_block(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      return b;
    }
    case 'computed_property_name': {
      const firstChild = node.namedChildren[0];
      const b = ir.computed_property_name(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'conditional_type': {
      const ctorChild = node.childForFieldName('alternative');
      const b = ir.conditional_type(ctorChild ? fromCST(ctorChild) : new LeafBuilder('alternative', ''));
      const consequenceChild = node.childForFieldName('consequence');
      if (consequenceChild) b.consequence(fromCST(consequenceChild));
      const leftChild = node.childForFieldName('left');
      if (leftChild) b.left(fromCST(leftChild));
      const rightChild = node.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild));
      return b;
    }
    case 'constraint': {
      const firstChild = node.namedChildren[0];
      const b = ir.constraint(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'construct_signature': {
      const ctorChild = node.childForFieldName('parameters');
      const b = ir.construct_signature(ctorChild ? fromCST(ctorChild) : new LeafBuilder('parameters', ''));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'constructor_type': {
      const ctorChild = node.childForFieldName('parameters');
      const b = ir.constructor_type(ctorChild ? fromCST(ctorChild) : new LeafBuilder('parameters', ''));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'continue_statement': {
      const b = ir.continue();
      const labelChild = node.childForFieldName('label');
      if (labelChild) b.label(fromCST(labelChild));
      return b;
    }
    case 'decorator': {
      const firstChild = node.namedChildren[0];
      const b = ir.decorator(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'default_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.default_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'do_statement': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.do(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const conditionChild = node.childForFieldName('condition');
      if (conditionChild) b.condition(fromCST(conditionChild));
      return b;
    }
    case 'else_clause': {
      const firstChild = node.namedChildren[0];
      const b = ir.else_clause(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'enum_assignment': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.enum_assignment(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      return b;
    }
    case 'enum_body': {
      const b = ir.enum_body();
      const nameChildren = node.childrenForFieldName('name');
      if (nameChildren.length > 0) b.name(nameChildren.map(c => fromCST(c)));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['name'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'enum_declaration': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.enum(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const bodyChild = node.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild));
      return b;
    }
    case 'export_clause': {
      const b = ir.export_clause();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'export_specifier': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.export_specifier(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const aliasChild = node.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild));
      return b;
    }
    case 'export_statement': {
      const b = ir.export();
      const declarationChild = node.childForFieldName('declaration');
      if (declarationChild) b.declaration(fromCST(declarationChild));
      const decoratorChildren = node.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(decoratorChildren.map(c => fromCST(c)));
      const sourceChild = node.childForFieldName('source');
      if (sourceChild) b.source(fromCST(sourceChild));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['declaration', 'decorator', 'source', 'value'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'expression_statement': {
      const firstChild = node.namedChildren[0];
      const b = ir.expression(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'extends_clause': {
      const ctorChildren = node.childrenForFieldName('value');
      const b = ir.extends_clause(ctorChildren.map(c => fromCST(c)));
      const typeArgumentsChildren = node.childrenForFieldName('type_arguments');
      if (typeArgumentsChildren.length > 0) b.typeArguments(typeArgumentsChildren.map(c => fromCST(c)));
      return b;
    }
    case 'extends_type_clause': {
      const ctorChildren = node.childrenForFieldName('type');
      const b = ir.extends_type_clause(ctorChildren.map(c => fromCST(c)));
      return b;
    }
    case 'finally_clause': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.finally_clause(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      return b;
    }
    case 'flow_maybe_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.flow_maybe_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'for_in_statement': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.for_in(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const kindChild = node.childForFieldName('kind');
      if (kindChild) b.kind(fromCST(kindChild));
      const leftChild = node.childForFieldName('left');
      if (leftChild) b.left(fromCST(leftChild));
      const operatorChild = node.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild));
      const rightChild = node.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      return b;
    }
    case 'for_statement': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.for(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const conditionChildren = node.childrenForFieldName('condition');
      if (conditionChildren.length > 0) b.condition(conditionChildren.map(c => fromCST(c)));
      const incrementChild = node.childForFieldName('increment');
      if (incrementChild) b.increment(fromCST(incrementChild));
      const initializerChild = node.childForFieldName('initializer');
      if (initializerChild) b.initializer(fromCST(initializerChild));
      return b;
    }
    case 'formal_parameters': {
      const b = ir.formal_parameters();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'function_declaration': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.functionDeclaration(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const bodyChild = node.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'function_expression': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.functionExpression(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const nameChild = node.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'function_signature': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.function_signature(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'function_type': {
      const ctorChild = node.childForFieldName('parameters');
      const b = ir.function_type(ctorChild ? fromCST(ctorChild) : new LeafBuilder('parameters', ''));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'generator_function': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.generatorFunction(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const nameChild = node.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'generator_function_declaration': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.generatorFunctionDeclaration(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const bodyChild = node.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      return b;
    }
    case 'generic_type': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.generic_type(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const typeArgumentsChild = node.childForFieldName('type_arguments');
      if (typeArgumentsChild) b.typeArguments(fromCST(typeArgumentsChild));
      return b;
    }
    case 'if_statement': {
      const ctorChild = node.childForFieldName('condition');
      const b = ir.if(ctorChild ? fromCST(ctorChild) : new LeafBuilder('condition', ''));
      const alternativeChild = node.childForFieldName('alternative');
      if (alternativeChild) b.alternative(fromCST(alternativeChild));
      const consequenceChild = node.childForFieldName('consequence');
      if (consequenceChild) b.consequence(fromCST(consequenceChild));
      return b;
    }
    case 'implements_clause': {
      const b = ir.implements_clause(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'import_alias': {
      const b = ir.import_alias(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'import_attribute': {
      const firstChild = node.namedChildren[0];
      const b = ir.import_attribute(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'import_clause': {
      const b = ir.import_clause(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'import_require_clause': {
      const ctorChild = node.childForFieldName('source');
      const b = ir.import_require_clause(ctorChild ? fromCST(ctorChild) : new LeafBuilder('source', ''));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['source'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'import_specifier': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.import_specifier(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const aliasChild = node.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild));
      return b;
    }
    case 'import_statement': {
      const b = ir.import();
      const sourceChild = node.childForFieldName('source');
      if (sourceChild) b.source(fromCST(sourceChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['source'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'index_signature': {
      const ctorChild = node.childForFieldName('type');
      const b = ir.index_signature(ctorChild ? fromCST(ctorChild) : new LeafBuilder('type', ''));
      const indexTypeChild = node.childForFieldName('index_type');
      if (indexTypeChild) b.indexType(fromCST(indexTypeChild));
      const nameChild = node.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild));
      const signChild = node.childForFieldName('sign');
      if (signChild) b.sign(fromCST(signChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['index_type', 'name', 'sign', 'type'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'index_type_query': {
      const firstChild = node.namedChildren[0];
      const b = ir.index_type_query(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'infer_type': {
      const b = ir.infer_type(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'instantiation_expression': {
      const ctorChild = node.childForFieldName('type_arguments');
      const b = ir.instantiation(ctorChild ? fromCST(ctorChild) : new LeafBuilder('type_arguments', ''));
      const functionChild = node.childForFieldName('function');
      if (functionChild) b.function(fromCST(functionChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['function', 'type_arguments'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'interface_body': {
      const b = ir.interface_body();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'interface_declaration': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.interface(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const bodyChild = node.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['body', 'name', 'type_parameters'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'internal_module': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.internal_module(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const bodyChild = node.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild));
      return b;
    }
    case 'intersection_type': {
      const b = ir.intersection_type(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'labeled_statement': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.labeled(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const labelChild = node.childForFieldName('label');
      if (labelChild) b.label(fromCST(labelChild));
      return b;
    }
    case 'lexical_declaration': {
      const ctorChild = node.childForFieldName('kind');
      const b = ir.lexical(ctorChild ? fromCST(ctorChild) : new LeafBuilder('kind', ''));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['kind'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'literal_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.literal_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'lookup_type': {
      const b = ir.lookup_type(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'mapped_type_clause': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.mapped_type_clause(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const aliasChild = node.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      return b;
    }
    case 'member_expression': {
      const ctorChild = node.childForFieldName('object');
      const b = ir.member(ctorChild ? fromCST(ctorChild) : new LeafBuilder('object', ''));
      const optionalChainChild = node.childForFieldName('optional_chain');
      if (optionalChainChild) b.optionalChain(fromCST(optionalChainChild));
      const propertyChild = node.childForFieldName('property');
      if (propertyChild) b.property(fromCST(propertyChild));
      return b;
    }
    case 'method_definition': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.method_definition(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const bodyChild = node.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['body', 'name', 'parameters', 'return_type', 'type_parameters'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'method_signature': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.method_signature(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const parametersChild = node.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild));
      const returnTypeChild = node.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['name', 'parameters', 'return_type', 'type_parameters'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'named_imports': {
      const b = ir.named_imports();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'namespace_export': {
      const firstChild = node.namedChildren[0];
      const b = ir.namespace_export(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'namespace_import': {
      const firstChild = node.namedChildren[0];
      const b = ir.namespace_import(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'nested_identifier': {
      const ctorChild = node.childForFieldName('object');
      const b = ir.nested_identifier(ctorChild ? fromCST(ctorChild) : new LeafBuilder('object', ''));
      const propertyChild = node.childForFieldName('property');
      if (propertyChild) b.property(fromCST(propertyChild));
      return b;
    }
    case 'nested_type_identifier': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.nested_type_identifier(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const moduleChild = node.childForFieldName('module');
      if (moduleChild) b.module(fromCST(moduleChild));
      return b;
    }
    case 'new_expression': {
      const ctorChild = node.childForFieldName('constructor');
      const b = ir.new(ctorChild ? fromCST(ctorChild) : new LeafBuilder('constructor', ''));
      const argumentsChild = node.childForFieldName('arguments');
      if (argumentsChild) b.arguments(fromCST(argumentsChild));
      const typeArgumentsChild = node.childForFieldName('type_arguments');
      if (typeArgumentsChild) b.typeArguments(fromCST(typeArgumentsChild));
      return b;
    }
    case 'non_null_expression': {
      const firstChild = node.namedChildren[0];
      const b = ir.non_null(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'object_assignment_pattern': {
      const ctorChild = node.childForFieldName('left');
      const b = ir.object_assignment_pattern(ctorChild ? fromCST(ctorChild) : new LeafBuilder('left', ''));
      const rightChild = node.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild));
      return b;
    }
    case 'object_pattern': {
      const b = ir.object_pattern();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'object_type': {
      const b = ir.object_type();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'omitting_type_annotation': {
      const firstChild = node.namedChildren[0];
      const b = ir.omitting_type_annotation(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'opting_type_annotation': {
      const firstChild = node.namedChildren[0];
      const b = ir.opting_type_annotation(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'optional_parameter': {
      const b = ir.optional_parameter();
      const decoratorChildren = node.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(decoratorChildren.map(c => fromCST(c)));
      const nameChild = node.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild));
      const patternChild = node.childForFieldName('pattern');
      if (patternChild) b.pattern(fromCST(patternChild));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'name', 'pattern', 'type', 'value'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'optional_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.optional_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'pair': {
      const ctorChild = node.childForFieldName('key');
      const b = ir.pair(ctorChild ? fromCST(ctorChild) : new LeafBuilder('key', ''));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      return b;
    }
    case 'pair_pattern': {
      const ctorChild = node.childForFieldName('key');
      const b = ir.pair_pattern(ctorChild ? fromCST(ctorChild) : new LeafBuilder('key', ''));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      return b;
    }
    case 'parenthesized_expression': {
      const firstChild = node.namedChildren[0];
      const b = ir.parenthesized(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      return b;
    }
    case 'parenthesized_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.parenthesized_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'program': {
      const b = ir.file();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'property_signature': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.property_signature(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['name', 'type'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'public_field_definition': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.public_field_definition(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const decoratorChildren = node.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(decoratorChildren.map(c => fromCST(c)));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'name', 'type', 'value'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'readonly_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.readonly_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'regex': {
      const ctorChild = node.childForFieldName('pattern');
      const b = ir.regex(ctorChild ? fromCST(ctorChild) : new LeafBuilder('pattern', ''));
      const flagsChild = node.childForFieldName('flags');
      if (flagsChild) b.flags(fromCST(flagsChild));
      return b;
    }
    case 'required_parameter': {
      const b = ir.required_parameter();
      const decoratorChildren = node.childrenForFieldName('decorator');
      if (decoratorChildren.length > 0) b.decorator(decoratorChildren.map(c => fromCST(c)));
      const nameChild = node.childForFieldName('name');
      if (nameChild) b.name(fromCST(nameChild));
      const patternChild = node.childForFieldName('pattern');
      if (patternChild) b.pattern(fromCST(patternChild));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      const remainingChildren = node.namedChildren.filter(c => {
        const fieldNames = ['decorator', 'name', 'pattern', 'type', 'value'];
        return !fieldNames.some(fn => node.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'rest_pattern': {
      const firstChild = node.namedChildren[0];
      const b = ir.rest_pattern(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'rest_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.rest_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'return_statement': {
      const b = ir.return();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'satisfies_expression': {
      const b = ir.satisfies(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'sequence_expression': {
      const b = ir.sequence(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'spread_element': {
      const firstChild = node.namedChildren[0];
      const b = ir.spread_element(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'statement_block': {
      const b = ir.statement_block();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'subscript_expression': {
      const ctorChild = node.childForFieldName('index');
      const b = ir.subscript(ctorChild ? fromCST(ctorChild) : new LeafBuilder('index', ''));
      const objectChild = node.childForFieldName('object');
      if (objectChild) b.object(fromCST(objectChild));
      const optionalChainChild = node.childForFieldName('optional_chain');
      if (optionalChainChild) b.optionalChain(fromCST(optionalChainChild));
      return b;
    }
    case 'switch_body': {
      const b = ir.switch_body();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'switch_case': {
      const ctorChild = node.childForFieldName('value');
      const b = ir.switch_case(ctorChild ? fromCST(ctorChild) : new LeafBuilder('value', ''));
      const bodyChildren = node.childrenForFieldName('body');
      if (bodyChildren.length > 0) b.body(bodyChildren.map(c => fromCST(c)));
      return b;
    }
    case 'switch_default': {
      const b = ir.switch_default();
      const bodyChildren = node.childrenForFieldName('body');
      if (bodyChildren.length > 0) b.body(bodyChildren.map(c => fromCST(c)));
      return b;
    }
    case 'switch_statement': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.switch(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      return b;
    }
    case 'template_literal_type': {
      const b = ir.template_literal_type();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'template_string': {
      const b = ir.template_string();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'template_substitution': {
      const firstChild = node.namedChildren[0];
      const b = ir.template_substitution(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'template_type': {
      const firstChild = node.namedChildren[0];
      const b = ir.template_type(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'ternary_expression': {
      const ctorChild = node.childForFieldName('alternative');
      const b = ir.ternary(ctorChild ? fromCST(ctorChild) : new LeafBuilder('alternative', ''));
      const conditionChild = node.childForFieldName('condition');
      if (conditionChild) b.condition(fromCST(conditionChild));
      const consequenceChild = node.childForFieldName('consequence');
      if (consequenceChild) b.consequence(fromCST(consequenceChild));
      return b;
    }
    case 'throw_statement': {
      const firstChild = node.namedChildren[0];
      const b = ir.throw(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'try_statement': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.try(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const finalizerChild = node.childForFieldName('finalizer');
      if (finalizerChild) b.finalizer(fromCST(finalizerChild));
      const handlerChild = node.childForFieldName('handler');
      if (handlerChild) b.handler(fromCST(handlerChild));
      return b;
    }
    case 'tuple_type': {
      const b = ir.tuple_type();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    case 'type_alias_declaration': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.type_alias(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const typeParametersChild = node.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      return b;
    }
    case 'type_annotation': {
      const firstChild = node.namedChildren[0];
      const b = ir.type_annotation(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'type_arguments': {
      const b = ir.type_arguments(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'type_assertion': {
      const b = ir.type_assertion(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'type_parameter': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.type_parameter(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const constraintChild = node.childForFieldName('constraint');
      if (constraintChild) b.constraint(fromCST(constraintChild));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      return b;
    }
    case 'type_parameters': {
      const b = ir.type_parameters(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'type_predicate': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.type_predicate(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      return b;
    }
    case 'type_predicate_annotation': {
      const firstChild = node.namedChildren[0];
      const b = ir.type_predicate_annotation(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'type_query': {
      const firstChild = node.namedChildren[0];
      const b = ir.type_query(firstChild ? fromCST(firstChild) : new LeafBuilder('unknown', ''));
      return b;
    }
    case 'unary_expression': {
      const ctorChild = node.childForFieldName('argument');
      const b = ir.unary(ctorChild ? fromCST(ctorChild) : new LeafBuilder('argument', ''));
      const operatorChild = node.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild));
      return b;
    }
    case 'union_type': {
      const b = ir.union_type(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'update_expression': {
      const ctorChild = node.childForFieldName('argument');
      const b = ir.update(ctorChild ? fromCST(ctorChild) : new LeafBuilder('argument', ''));
      const operatorChild = node.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild));
      return b;
    }
    case 'variable_declaration': {
      const b = ir.variable(node.namedChildren.map(c => fromCST(c)));
      return b;
    }
    case 'variable_declarator': {
      const ctorChild = node.childForFieldName('name');
      const b = ir.variable_declarator(ctorChild ? fromCST(ctorChild) : new LeafBuilder('name', ''));
      const typeChild = node.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild));
      const valueChild = node.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild));
      return b;
    }
    case 'while_statement': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.while(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const conditionChild = node.childForFieldName('condition');
      if (conditionChild) b.condition(fromCST(conditionChild));
      return b;
    }
    case 'with_statement': {
      const ctorChild = node.childForFieldName('body');
      const b = ir.with(ctorChild ? fromCST(ctorChild) : new LeafBuilder('body', ''));
      const objectChild = node.childForFieldName('object');
      if (objectChild) b.object(fromCST(objectChild));
      return b;
    }
    case 'yield_expression': {
      const b = ir.yield();
      const remainingChildren = node.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(remainingChildren.map(c => fromCST(c)));
      return b;
    }
    default: return null;
  }
}

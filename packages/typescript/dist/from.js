// Auto-generated by @sittir/codegen — do not edit
import * as F from './factories.js';
import { TSKindId, kindIdFromName } from './types.js';
import { coerceKindEnumStorage, hasKind, isNodeData } from './utils.js';
export const _fromMap = {
    "abstract_class_declaration": abstractClassDeclarationFrom,
    "abstract_method_signature": abstractMethodSignatureFrom,
    "accessibility_modifier": accessibilityModifierFrom,
    "adding_type_annotation": addingTypeAnnotationFrom,
    "ambient_declaration_declaration": ambientDeclarationDeclarationFrom,
    "ambient_declaration": ambientDeclarationFrom,
    "arguments": arguments_From,
    "array": arrayFrom,
    "array_pattern": arrayPatternFrom,
    "array_type": arrayTypeFrom,
    "arrow_function_parameter": arrowFunctionParameterFrom,
    "arrow_function__call_signature": arrowFunctionUCallSignatureFrom,
    "arrow_function": arrowFunctionFrom,
    "as_expression": asExpressionFrom,
    "asserts": assertsFrom,
    "asserts_annotation": assertsAnnotationFrom,
    "assignment_expression": assignmentExpressionFrom,
    "assignment_pattern": assignmentPatternFrom,
    "augmented_assignment_expression": augmentedAssignmentExpressionFrom,
    "await_expression": awaitExpressionFrom,
    "binary_expression": binaryExpressionFrom,
    "break_statement": breakStatementFrom,
    "call_expression": callExpressionFrom,
    "call_signature": callSignatureFrom,
    "catch_clause": catchClauseFrom,
    "class": class_From,
    "class_body": classBodyFrom,
    "class_declaration": classDeclarationFrom,
    "class_heritage_extends_clause": classHeritageExtendsClauseFrom,
    "class_heritage_implements_clause": classHeritageImplementsClauseFrom,
    "class_heritage": classHeritageFrom,
    "class_static_block": classStaticBlockFrom,
    "comment": commentFrom,
    "computed_property_name": computedPropertyNameFrom,
    "conditional_type": conditionalTypeFrom,
    "constraint": constraintFrom,
    "construct_signature": constructSignatureFrom,
    "constructor_type": constructorTypeFrom,
    "continue_statement": continueStatementFrom,
    "debugger_statement": debuggerStatementFrom,
    "decorator": decoratorFrom,
    "decorator_call_expression": decoratorCallExpressionFrom,
    "decorator_member_expression": decoratorMemberExpressionFrom,
    "decorator_parenthesized_expression": decoratorParenthesizedExpressionFrom,
    "default_type": defaultTypeFrom,
    "do_statement": doStatementFrom,
    "else_clause": elseClauseFrom,
    "enum_assignment": enumAssignmentFrom,
    "enum_body": enumBodyFrom,
    "enum_declaration": enumDeclarationFrom,
    "escape_sequence": escapeSequenceFrom,
    "export_clause": exportClauseFrom,
    "export_specifier": exportSpecifierFrom,
    "export_statement_type_export": exportStatementTypeExportFrom,
    "export_statement_equals_export": exportStatementEqualsExportFrom,
    "export_statement_namespace_export": exportStatementNamespaceExportFrom,
    "export_statement": exportStatementFrom,
    "expression_statement": expressionStatementFrom,
    "extends_clause": extendsClauseFrom,
    "extends_type_clause": extendsTypeClauseFrom,
    "false": false_From,
    "finally_clause": finallyClauseFrom,
    "flow_maybe_type": flowMaybeTypeFrom,
    "for_in_statement": forInStatementFrom,
    "for_statement": forStatementFrom,
    "formal_parameters": formalParametersFrom,
    "function_declaration": functionDeclarationFrom,
    "function_expression": functionExpressionFrom,
    "function_signature": functionSignatureFrom,
    "function_type": functionTypeFrom,
    "generator_function": generatorFunctionFrom,
    "generator_function_declaration": generatorFunctionDeclarationFrom,
    "generic_type": genericTypeFrom,
    "hash_bang_line": hashBangLineFrom,
    "identifier": identifierFrom,
    "if_statement": ifStatementFrom,
    "implements_clause": implementsClauseFrom,
    "import": import_From,
    "import_alias": importAliasFrom,
    "import_attribute": importAttributeFrom,
    "import_clause_namespace_import": importClauseNamespaceImportFrom,
    "import_clause_named_imports": importClauseNamedImportsFrom,
    "import_clause_default_import": importClauseDefaultImportFrom,
    "import_clause": importClauseFrom,
    "import_require_clause": importRequireClauseFrom,
    "import_specifier_name": importSpecifierNameFrom,
    "import_specifier": importSpecifierFrom,
    "import_statement": importStatementFrom,
    "index_signature_mapped_type_clause": indexSignatureMappedTypeClauseFrom,
    "index_signature": indexSignatureFrom,
    "index_type_query": indexTypeQueryFrom,
    "infer_type": inferTypeFrom,
    "instantiation_expression": instantiationExpressionFrom,
    "interface_declaration": interfaceDeclarationFrom,
    "internal_module": internalModuleFrom,
    "intersection_type": intersectionTypeFrom,
    "labeled_statement": labeledStatementFrom,
    "lexical_declaration": lexicalDeclarationFrom,
    "literal_type": literalTypeFrom,
    "lookup_type": lookupTypeFrom,
    "mapped_type_clause": mappedTypeClauseFrom,
    "member_expression": memberExpressionFrom,
    "meta_property": metaPropertyFrom,
    "method_definition": methodDefinitionFrom,
    "method_signature": methodSignatureFrom,
    "module": moduleFrom,
    "named_imports": namedImportsFrom,
    "namespace_export": namespaceExportFrom,
    "namespace_import": namespaceImportFrom,
    "nested_identifier": nestedIdentifierFrom,
    "nested_type_identifier": nestedTypeIdentifierFrom,
    "new_expression": newExpressionFrom,
    "non_null_expression": nonNullExpressionFrom,
    "null": null_From,
    "number": numberFrom,
    "object": objectFrom,
    "object_assignment_pattern": objectAssignmentPatternFrom,
    "object_pattern": objectPatternFrom,
    "object_type": objectTypeFrom,
    "omitting_type_annotation": omittingTypeAnnotationFrom,
    "opting_type_annotation": optingTypeAnnotationFrom,
    "optional_parameter": optionalParameterFrom,
    "optional_tuple_parameter": optionalTupleParameterFrom,
    "optional_type": optionalTypeFrom,
    "override_modifier": overrideModifierFrom,
    "pair": pairFrom,
    "pair_pattern": pairPatternFrom,
    "parenthesized_expression_sequence": parenthesizedExpressionSequenceFrom,
    "parenthesized_expression": parenthesizedExpressionFrom,
    "parenthesized_type": parenthesizedTypeFrom,
    "predefined_type": predefinedTypeFrom,
    "private_property_identifier": privatePropertyIdentifierFrom,
    "program": programFrom,
    "property_signature": propertySignatureFrom,
    "public_field_definition": publicFieldDefinitionFrom,
    "readonly_type": readonlyTypeFrom,
    "regex": regexFrom,
    "regex_flags": regexFlagsFrom,
    "regex_pattern": regexPatternFrom,
    "required_parameter": requiredParameterFrom,
    "rest_pattern": restPatternFrom,
    "rest_type": restTypeFrom,
    "return_statement": returnStatementFrom,
    "satisfies_expression": satisfiesExpressionFrom,
    "sequence_expression": sequenceExpressionFrom,
    "spread_element": spreadElementFrom,
    "statement_block": statementBlockFrom,
    "string": stringFrom,
    "subscript_expression": subscriptExpressionFrom,
    "super": superFrom,
    "switch_body": switchBodyFrom,
    "switch_case": switchCaseFrom,
    "switch_default": switchDefaultFrom,
    "switch_statement": switchStatementFrom,
    "template_literal_type": templateLiteralTypeFrom,
    "template_string": templateStringFrom,
    "template_substitution": templateSubstitutionFrom,
    "template_type": templateTypeFrom,
    "ternary_expression": ternaryExpressionFrom,
    "this": thisFrom,
    "throw_statement": throwStatementFrom,
    "true": true_From,
    "try_statement": tryStatementFrom,
    "tuple_parameter": tupleParameterFrom,
    "tuple_type": tupleTypeFrom,
    "type_alias_declaration": typeAliasDeclarationFrom,
    "type_annotation": typeAnnotationFrom,
    "type_arguments": typeArgumentsFrom,
    "type_assertion": typeAssertionFrom,
    "type_parameter": typeParameterFrom,
    "type_parameters": typeParametersFrom,
    "type_predicate": typePredicateFrom,
    "type_predicate_annotation": typePredicateAnnotationFrom,
    "type_query": typeQueryFrom,
    "unary_expression": unaryExpressionFrom,
    "undefined": undefined_From,
    "unescaped_double_string_fragment": unescapedDoubleStringFragmentFrom,
    "unescaped_single_string_fragment": unescapedSingleStringFragmentFrom,
    "union_type": unionTypeFrom,
    "update_expression": updateExpressionFrom,
    "variable_declaration": variableDeclarationFrom,
    "variable_declarator": variableDeclaratorFrom,
    "while_statement": whileStatementFrom,
    "with_statement": withStatementFrom,
    "yield_expression": yieldExpressionFrom,
    "html_comment": htmlCommentFrom,
    "||": ororFrom,
    "jsx_text": jsxTextFrom,
};
const _leafRegistry = {
    "accessibility_modifier": { values: ["public", "private", "protected"], factory: (text) => F.accessibilityModifier(text) },
    "comment": { factory: F.comment },
    "escape_sequence": { factory: F.escapeSequence },
    "false": { values: ["false"], factory: () => F.false_() },
    "hash_bang_line": { factory: F.hashBangLine },
    "identifier": { factory: F.identifier },
    "import": { values: ["import"], factory: () => F.import_() },
    "meta_property": { factory: F.metaProperty },
    "null": { values: ["null"], factory: () => F.null_() },
    "number": { factory: F.number },
    "override_modifier": { values: ["override"], factory: () => F.overrideModifier() },
    "predefined_type": { factory: F.predefinedType },
    "private_property_identifier": { factory: F.privatePropertyIdentifier },
    "regex_flags": { factory: F.regexFlags },
    "regex_pattern": { factory: F.regexPattern },
    "super": { values: ["super"], factory: () => F.super_() },
    "this": { values: ["this"], factory: () => F.this_() },
    "true": { values: ["true"], factory: () => F.true_() },
    "undefined": { values: ["undefined"], factory: () => F.undefined_() },
    "unescaped_double_string_fragment": { factory: F.unescapedDoubleStringFragment },
    "unescaped_single_string_fragment": { factory: F.unescapedSingleStringFragment },
    "html_comment": { factory: F.htmlComment },
    "||": { factory: F.oror },
    "jsx_text": { factory: F.jsxText },
};
function _resolveLeafString(v, kinds) {
    for (const kind of kinds) {
        const entry = _leafRegistry[kind];
        if (!entry)
            continue;
        if (entry.values && entry.values.includes(v))
            return entry.factory(v);
        if (entry.pattern && entry.pattern.test(v))
            return entry.factory(v);
    }
    for (const kind of kinds) {
        const entry = _leafRegistry[kind];
        if (entry && !entry.values && !entry.pattern)
            return entry.factory(v);
    }
    return undefined;
}
function _isFromKind(k) {
    return k in _fromMap;
}
function _resolveByKind(kind, rest) {
    const fn = _fromMap[kind];
    return fn(rest);
}
function _resolveScalar(_v) {
    return undefined;
}
function _resolveOne(v, leafKinds, branchKinds) {
    if (v === undefined || v === null)
        return v;
    if (isNodeData(v))
        return v;
    if (typeof v === "boolean" || typeof v === "number") {
        const scalar = _resolveScalar(v);
        if (scalar !== undefined)
            return scalar;
    }
    if (typeof v === "string" && leafKinds.length > 0) {
        const leaf = _resolveLeafString(v, leafKinds);
        if (leaf !== undefined)
            return leaf;
    }
    if (typeof v === "object" && !Array.isArray(v) && "kind" in v) {
        const { kind, ...rest } = v;
        if (typeof kind === "string" && _isFromKind(kind))
            return _resolveByKind(kind, rest);
    }
    if (branchKinds.length === 1 && typeof v === "object" && !Array.isArray(v)) {
        const bk = branchKinds[0];
        if (_isFromKind(bk))
            return _resolveByKind(bk, v);
    }
    return v;
}
function _resolveMany(v, leafKinds, branchKinds) {
    if (v === undefined || v === null)
        return [];
    const arr = Array.isArray(v) ? v : [v];
    return arr.map(e => _resolveOne(e, leafKinds, branchKinds));
}
function _resolveOneLeaf(v, kind) {
    if (v === undefined || v === null)
        return v;
    if (isNodeData(v))
        return v;
    if (typeof v === "boolean" || typeof v === "number") {
        const scalar = _resolveScalar(v);
        if (scalar !== undefined)
            return scalar;
    }
    if (typeof v === "string") {
        const e = _leafRegistry[kind];
        if (e !== undefined)
            return e.factory(v);
    }
    if (typeof v === "object" && !Array.isArray(v) && "kind" in v) {
        const { kind: k, ...rest } = v;
        if (typeof k === "string" && _isFromKind(k))
            return _resolveByKind(k, rest);
    }
    return v;
}
const _wrapKindIds = {
    "_ambient_declaration_declaration": TSKindId._AmbientDeclarationDeclaration,
    "_class_heritage_implements_clause": TSKindId._ClassHeritageImplementsClause,
    "_import_clause_named_imports": TSKindId._ImportClauseNamedImports,
    "_import_clause_namespace_import": TSKindId._ImportClauseNamespaceImport,
    "_index_signature_mapped_type_clause": TSKindId._IndexSignatureMappedTypeClause,
    "_parenthesized_expression_sequence": TSKindId._ParenthesizedExpressionSequence,
    "_public_field_definition_declare_first": TSKindId.PublicFieldDefinitionDeclareFirst,
    "ambient_declaration_declaration": TSKindId._AmbientDeclarationDeclaration,
    "arguments": TSKindId.Arguments,
    "array": TSKindId.Array,
    "array_pattern": TSKindId.ArrayPattern,
    "asserts": TSKindId.Asserts,
    "class_body": TSKindId.ClassBody,
    "class_heritage_implements_clause": TSKindId._ClassHeritageImplementsClause,
    "decorator": TSKindId.Decorator,
    "decorator_parenthesized_expression": TSKindId.DecoratorParenthesizedExpression,
    "export_clause": TSKindId.ExportClause,
    "formal_parameters": TSKindId.FormalParameters,
    "implements_clause": TSKindId.ImplementsClause,
    "import_clause_namespace_import": TSKindId._ImportClauseNamespaceImport,
    "import_clause_named_imports": TSKindId._ImportClauseNamedImports,
    "index_signature_mapped_type_clause": TSKindId._IndexSignatureMappedTypeClause,
    "literal_type": TSKindId.LiteralType,
    "named_imports": TSKindId.NamedImports,
    "namespace_export": TSKindId.NamespaceExport,
    "object": TSKindId.Object,
    "object_pattern": TSKindId.ObjectPattern,
    "parenthesized_expression_sequence": TSKindId._ParenthesizedExpressionSequence,
    "rest_pattern": TSKindId.RestPattern,
    "sequence_expression": TSKindId.SequenceExpression,
    "switch_body": TSKindId.SwitchBody,
    "template_literal_type": TSKindId.TemplateLiteralType,
    "template_string": TSKindId.TemplateString,
    "template_substitution": TSKindId.TemplateSubstitution,
    "template_type": TSKindId.TemplateType,
    "tuple_type": TSKindId.TupleType,
    "type_arguments": TSKindId.TypeArguments,
    "type_parameters": TSKindId.TypeParameters,
    "type_query": TSKindId.TypeQuery,
};
function _wrapWithChildren(kind, children) {
    switch (kind) {
        case "_ambient_declaration_declaration": return F._ambientDeclarationDeclaration(...children);
        case "_class_heritage_implements_clause": return F._classHeritageImplementsClause(...children);
        case "_import_clause_named_imports": return F._importClauseNamedImports(...children);
        case "_import_clause_namespace_import": return F._importClauseNamespaceImport(...children);
        case "_index_signature_mapped_type_clause": return F._indexSignatureMappedTypeClause(...children);
        case "_parenthesized_expression_sequence": return F._parenthesizedExpressionSequence(...children);
        case "_public_field_definition_declare_first": return F.publicFieldDefinitionDeclareFirst(...children);
        case "ambient_declaration_declaration": return F.ambientDeclarationDeclaration(children[0]);
        case "arguments": return F.arguments_(...children);
        case "array": return F.array(...children);
        case "array_pattern": return F.arrayPattern(...children);
        case "asserts": return F.asserts(children[0]);
        case "class_body": return F.classBody(...children);
        case "class_heritage_implements_clause": return F.classHeritageImplementsClause(children[0]);
        case "decorator": return F.decorator(children[0]);
        case "decorator_parenthesized_expression": return F.decoratorParenthesizedExpression(children[0]);
        case "export_clause": return F.exportClause(...children);
        case "formal_parameters": return F.formalParameters(...children);
        case "implements_clause": return F.implementsClause(...children);
        case "import_clause_namespace_import": return F.importClauseNamespaceImport(children[0]);
        case "import_clause_named_imports": return F.importClauseNamedImports(children[0]);
        case "index_signature_mapped_type_clause": return F.indexSignatureMappedTypeClause(children[0]);
        case "literal_type": return F.literalType(children[0]);
        case "named_imports": return F.namedImports(...children);
        case "namespace_export": return F.namespaceExport(children[0]);
        case "object": return F.object(...children);
        case "object_pattern": return F.objectPattern(...children);
        case "parenthesized_expression_sequence": return F.parenthesizedExpressionSequence(children[0]);
        case "rest_pattern": return F.restPattern(children[0]);
        case "sequence_expression": return F.sequenceExpression(...children);
        case "switch_body": return F.switchBody(...children);
        case "template_literal_type": return F.templateLiteralType(...children);
        case "template_string": return F.templateString(...children);
        case "template_substitution": return F.templateSubstitution(children[0]);
        case "template_type": return F.templateType(children[0]);
        case "tuple_type": return F.tupleType(...children);
        case "type_arguments": return F.typeArguments(...children);
        case "type_parameters": return F.typeParameters(...children);
        case "type_query": return F.typeQuery(children[0]);
        default: return undefined;
    }
}
function _resolveOneBranch(v, kind) {
    if (v === undefined || v === null)
        return v;
    if (isNodeData(v)) {
        const wrapId = _wrapKindIds[kind];
        if (wrapId !== undefined && v.$type !== wrapId) {
            return _wrapWithChildren(kind, [v]);
        }
        return v;
    }
    if (Array.isArray(v) && kind in _wrapKindIds) {
        const resolved = v.map(e => {
            if (typeof e === "string" || typeof e === "number")
                return e;
            if (isNodeData(e))
                return e;
            if (typeof e === "object" && e !== null && !Array.isArray(e)) {
                if ("kind" in e) {
                    const { kind: k, ...rest } = e;
                    if (typeof k === "string" && _isFromKind(k))
                        return _resolveByKind(k, rest);
                }
                if (_isFromKind(kind))
                    return _resolveByKind(kind, e);
            }
            return e;
        });
        return _wrapWithChildren(kind, resolved);
    }
    if ((typeof v === "string" || typeof v === "number" || typeof v === "boolean") && _isFromKind(kind)) {
        return _resolveByKind(kind, v);
    }
    if (typeof v === "object" && !Array.isArray(v)) {
        if ("kind" in v) {
            const { kind: k, ...rest } = v;
            if (typeof k === "string" && _isFromKind(k))
                return _resolveByKind(k, rest);
        }
        if (_isFromKind(kind))
            return _resolveByKind(kind, v);
    }
    return v;
}
function _resolveManyLeaf(v, kind) {
    if (v === undefined || v === null)
        return [];
    const arr = Array.isArray(v) ? v : [v];
    return arr.map(e => _resolveOneLeaf(e, kind));
}
function _resolveManyBranch(v, kind) {
    if (v === undefined || v === null)
        return [];
    const arr = Array.isArray(v) ? v : [v];
    return arr.map(e => _resolveOneBranch(e, kind));
}
function _resolveBooleanKeyword(v) {
    if (v === undefined || v === null)
        return v;
    if (v === true || v === false)
        return v;
    if (isNodeData(v))
        return v;
    if (Array.isArray(v))
        return v;
    return v;
}
function _resolveBitflag(v) {
    if (v === undefined || v === null)
        return v;
    if (typeof v === "number")
        return v;
    if (typeof v === "string")
        return v;
    if (Array.isArray(v))
        return v;
    if (isNodeData(v))
        return v;
    return v;
}
function _assertNonEmpty(arr, label) {
    if (arr.length === 0) {
        throw new Error(`${label}: requires at least one element`);
    }
}
/** @internal — call an overloaded factory bypassing overload resolution. */
function _applyFactory(fn, ...args) {
    return Reflect.apply(fn, undefined, args);
}
/** @internal — treat a bare child input like `{ children: ... }` when polymorph forms allow it. */
function _childrenInput(input) {
    if (input !== null && input !== undefined && typeof input === "object" && !Array.isArray(input) && !isNodeData(input) && "children" in input) {
        return input.children;
    }
    return input;
}
// Interned resolver kind lists (dedup)
const _super_property_identifier = ["_reserved_identifier", "identifier"];
const _super_identifier = ["undefined", "identifier"];
const _super_import_identifier = ["identifier"];
const _super_destructuring_pattern = ["object_pattern", "array_pattern"];
const _super_export_statement_default = ["_export_statement_default_from_arm", "_export_statement_default_decl_arm"];
const _K0 = ["identifier", "_reserved_identifier", "private_property_identifier", "number"];
const _K1 = ["string", "computed_property_name"];
const _K2 = [];
const _K3 = ["type_annotation", "asserts_annotation", "type_predicate_annotation"];
const _K4 = ["predefined_type", "_type_identifier", "this"];
const _K5 = ["parenthesized_type", "nested_type_identifier", "generic_type", "object_type", "array_type", "tuple_type", "flow_maybe_type", "type_query", "index_type_query", "literal_type", "lookup_type", "conditional_type", "template_literal_type", "intersection_type", "union_type", "function_type", "readonly_type", "constructor_type", "infer_type", "_type_query_member_expression_in_type_annotation", "_type_query_call_expression_in_type_annotation"];
const _K6 = ["parenthesized_type", "nested_type_identifier", "generic_type", "object_type", "array_type", "tuple_type", "flow_maybe_type", "type_query", "index_type_query", "literal_type", "lookup_type", "conditional_type", "template_literal_type", "intersection_type", "union_type"];
const _K7 = ["undefined", "identifier", "_reserved_identifier", "this", "super", "number", "true", "false", "null", "meta_property"];
const _K8 = ["as_expression", "satisfies_expression", "instantiation_expression", "internal_module", "type_assertion", "subscript_expression", "member_expression", "parenthesized_expression", "string", "template_string", "regex", "object", "array", "function_expression", "arrow_function", "generator_function", "class", "call_expression", "non_null_expression", "assignment_expression", "augmented_assignment_expression", "await_expression", "unary_expression", "binary_expression", "ternary_expression", "update_expression", "new_expression", "yield_expression", "statement_block"];
const _K9 = ["as_expression", "satisfies_expression", "instantiation_expression", "internal_module", "type_assertion", "subscript_expression", "member_expression", "parenthesized_expression", "string", "template_string", "regex", "object", "array", "function_expression", "arrow_function", "generator_function", "class", "call_expression", "non_null_expression", "assignment_expression", "augmented_assignment_expression", "await_expression", "unary_expression", "binary_expression", "ternary_expression", "update_expression", "new_expression", "yield_expression"];
const _K10 = ["const", "predefined_type", "_type_identifier", "this"];
const _K11 = ["parenthesized_expression", "_lhs_expression"];
const _K12 = ["member_expression", "subscript_expression", "object_pattern", "array_pattern", "non_null_expression", "rest_pattern"];
const _K13 = ["member_expression", "subscript_expression", "parenthesized_expression", "non_null_expression"];
const _K14 = ["undefined", "identifier", "_reserved_identifier", "this", "super", "number", "true", "false", "null", "meta_property", "private_property_identifier"];
const _K15 = ["instanceof", "in"];
const _K16 = ["decorator_member_expression"];
const _K17 = ["export_statement", "import_statement", "debugger_statement", "expression_statement", "function_declaration", "generator_function_declaration", "class_declaration", "lexical_declaration", "variable_declaration", "function_signature", "abstract_class_declaration", "module", "internal_module", "type_alias_declaration", "enum_declaration", "interface_declaration", "import_alias", "ambient_declaration", "statement_block", "if_statement", "switch_statement", "for_statement", "for_in_statement", "while_statement", "do_statement", "try_statement", "with_statement", "break_statement", "continue_statement", "return_statement", "throw_statement", "labeled_statement"];
const _K18 = ["string"];
const _K19 = ["as_expression", "satisfies_expression", "instantiation_expression", "internal_module", "type_assertion", "subscript_expression", "member_expression", "parenthesized_expression", "string", "template_string", "regex", "object", "array", "function_expression", "arrow_function", "generator_function", "class", "call_expression", "non_null_expression", "assignment_expression", "augmented_assignment_expression", "await_expression", "unary_expression", "binary_expression", "ternary_expression", "update_expression", "new_expression", "yield_expression", "sequence_expression"];
const _K20 = ["_type_identifier"];
const _K21 = ["nested_type_identifier", "generic_type"];
const _K22 = ["_for_header_lhs", "_for_header_var_kind", "_for_header_let_const_kind"];
const _K23 = ["lexical_declaration", "variable_declaration", "as_expression", "satisfies_expression", "instantiation_expression", "internal_module", "type_assertion", "subscript_expression", "member_expression", "parenthesized_expression", "string", "template_string", "regex", "object", "array", "function_expression", "arrow_function", "generator_function", "class", "call_expression", "non_null_expression", "assignment_expression", "augmented_assignment_expression", "await_expression", "unary_expression", "binary_expression", "ternary_expression", "update_expression", "new_expression", "yield_expression", "sequence_expression"];
const _K24 = ["_automatic_semicolon", "_function_signature_automatic_semicolon"];
const _K25 = ["parenthesized_type", "nested_type_identifier", "generic_type", "object_type", "array_type", "tuple_type", "flow_maybe_type", "type_query", "index_type_query", "literal_type", "lookup_type", "conditional_type", "template_literal_type", "intersection_type", "union_type", "function_type", "readonly_type", "constructor_type", "infer_type", "_type_query_member_expression_in_type_annotation", "_type_query_call_expression_in_type_annotation", "asserts", "type_predicate"];
const _K26 = ["nested_type_identifier"];
const _K27 = ["nested_identifier"];
const _K28 = ["_import_attribute_object"];
const _K29 = ["object"];
const _K30 = ["namespace_import", "named_imports"];
const _K31 = ["import_clause", "string", "import_require_clause"];
const _K32 = ["type_annotation", "omitting_type_annotation", "adding_type_annotation", "opting_type_annotation"];
const _K33 = ["string", "nested_identifier"];
const _K34 = ["undefined", "identifier", "_reserved_identifier", "this", "super", "number", "true", "false", "null", "meta_property", "import"];
const _K35 = ["private_property_identifier", "identifier"];
const _K36 = ["subscript_expression", "member_expression", "parenthesized_expression", "string", "template_string", "regex", "object", "array", "function_expression", "arrow_function", "generator_function", "class", "call_expression", "non_null_expression"];
const _K37 = ["_automatic_semicolon"];
const _K38 = ["export_statement", "property_signature", "call_signature", "construct_signature", "index_signature", "method_signature"];
const _K39 = ["undefined", "identifier", "this"];
const _K40 = ["member_expression", "subscript_expression", "object_pattern", "array_pattern", "non_null_expression", "rest_pattern", "assignment_pattern"];
const _K41 = ["_public_field_definition_declare_first", "_public_field_definition_access_first"];
const _K42 = ["_public_field_definition_static_mods", "_public_field_definition_abstract_first", "_public_field_definition_readonly_first", "_public_field_definition_accessor_opt"];
const _K43 = ["unescaped_double_string_fragment", "escape_sequence", "unescaped_single_string_fragment"];
const _K44 = ["rest_pattern"];
const _K45 = ["identifier", "this", "predefined_type"];
export function abstractClassDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    return F.abstractClassDeclaration({
        decorator: _resolveManyBranch(input.decorator, "decorator"),
        name: _resolveOneLeaf(input.name, "_type_identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        classHeritage: _resolveOneBranch(input.classHeritage, "class_heritage"),
        body: _resolveOneBranch(input.body, "class_body") ?? F.classBody(),
    });
}
export function abstractMethodSignatureFrom(input) {
    if (isNodeData(input))
        return input;
    return F.abstractMethodSignature({
        accessibilityModifier: coerceKindEnumStorage(_resolveOneLeaf(input.accessibilityModifier, "_accessibility_modifier"), [["public", kindIdFromName("public")], ["private", kindIdFromName("private")], ["protected", kindIdFromName("protected")]]),
        overrideModifier: _resolveBooleanKeyword(input.overrideModifier),
        accessorKind: coerceKindEnumStorage(_resolveOneLeaf(input.accessorKind, "_accessor_kind"), [["get", kindIdFromName("get")], ["set", kindIdFromName("set")], ["*", kindIdFromName("*")]]),
        name: _resolveOne(input.name, _K0, _K1),
        optionalMarker: _resolveBooleanKeyword(input.optionalMarker),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
    });
}
export function accessibilityModifierFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.accessibilityModifier(input);
}
export function addingTypeAnnotationFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("adding_type_annotation"))
        return input;
    return F.addingTypeAnnotation(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function ambientDeclarationDeclarationFrom(input) {
    return F.ambientDeclarationDeclaration(input);
}
export function ambientDeclarationFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("ambient_declaration"))
            return input;
    }
    return _applyFactory(F.ambientDeclaration, input);
}
export function ambientDeclarationUFormDeclarationFrom(input) {
    return F.ambientDeclarationUFormDeclaration({
        ambientDeclarationDeclaration: _resolveOneBranch(input.ambientDeclarationDeclaration, "_ambient_declaration_declaration"),
    });
}
export function ambientDeclarationUFormGlobalFrom(input) {
    return F.ambientDeclarationUFormGlobal({
        ambientDeclarationGlobal: _resolveOneBranch(input.ambientDeclarationGlobal, "_ambient_declaration_global"),
    });
}
export function ambientDeclarationUFormModuleFrom(input) {
    return F.ambientDeclarationUFormModule({
        ambientDeclarationModule: _resolveOneBranch(input.ambientDeclarationModule, "_ambient_declaration_module"),
    });
}
export function arguments_From(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.Arguments) {
        const data = input[0];
        const stored = data._expression;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.arguments_(...children);
    }
    return F.arguments_(...input);
}
export function arrayFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.Array) {
        const data = input[0];
        const stored = data._expression;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.array(...children);
    }
    return F.array(...input);
}
export function arrayPatternFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.ArrayPattern) {
        const data = input[0];
        const stored = data._pattern;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.arrayPattern(...children);
    }
    return F.arrayPattern(...input);
}
export function arrayTypeFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("array_type"))
        return input;
    return F.arrayType(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "primaryType" in input ? input.primaryType : input), _K4, _K6));
}
export function arrowFunctionParameterFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("arrow_function_parameter"))
        return input;
    return F.arrowFunctionParameter(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "parameter" in input ? input.parameter : input), _super_property_identifier, _K2));
}
export function arrowFunctionUCallSignatureFrom(input) {
    if (isNodeData(input))
        return input;
    return F.arrowFunctionUCallSignature({
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
    });
}
export function arrowFunctionFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("arrow_function"))
            return input;
    }
    return _applyFactory(F.arrowFunction, input);
}
export function arrowFunctionUFormParameterFrom(input) {
    return F.arrowFunctionUFormParameter({
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        arrowFunctionParameter: _resolveOneBranch(input.arrowFunctionParameter, "_arrow_function_parameter"),
        body: _resolveOne(input.body, _K7, _K8),
    });
}
export function arrowFunctionUFormUCallSignatureFrom(input) {
    return F.arrowFunctionUFormUCallSignature({
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        arrowFunction_CallSignature: _resolveOneBranch(input.arrowFunction_CallSignature, "_arrow_function__call_signature"),
        body: _resolveOne(input.body, _K7, _K8),
    });
}
export function asExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.asExpression({
        expression: _resolveOne(input.expression, _K7, _K9),
        typeAnnotation: _resolveOne(input.typeAnnotation, _K10, _K5),
    });
}
export function assertsFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.Asserts) {
        const data = input;
        const child = data._type_predicate;
        return F.asserts(child);
    }
    return F.asserts(input);
}
export function assertsAnnotationFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("asserts_annotation"))
        return input;
    return F.assertsAnnotation(_resolveOneBranch((input !== null && typeof input === 'object' && !isNodeData(input) && "asserts" in input ? input.asserts : input), "asserts"));
}
export function assignmentExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.assignmentExpression({
        usingMarker: _resolveBooleanKeyword(input.usingMarker),
        left: _resolveOne(input.left, _K2, _K11),
        right: _resolveOne(input.right, _K7, _K9),
    });
}
export function assignmentPatternFrom(input) {
    if (isNodeData(input))
        return input;
    return F.assignmentPattern({
        left: _resolveOne(input.left, _super_identifier, _K12),
        right: _resolveOne(input.right, _K7, _K9),
    });
}
export function augmentedAssignmentExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.augmentedAssignmentExpression({
        left: _resolveOne(input.left, _super_property_identifier, _K13),
        operator: coerceKindEnumStorage(_resolveOneLeaf(input.operator, "_augmented_assignment_expression_operator"), [["+=", kindIdFromName("+=")], ["-=", kindIdFromName("-=")], ["*=", kindIdFromName("*=")], ["/=", kindIdFromName("/=")], ["%=", kindIdFromName("%=")], ["^=", kindIdFromName("^=")], ["&=", kindIdFromName("&=")], ["|=", kindIdFromName("|=")], [">>=", kindIdFromName(">>=")], [">>>=", kindIdFromName(">>>=")], ["<<=", kindIdFromName("<<=")], ["**=", kindIdFromName("**=")], ["&&=", kindIdFromName("&&=")], ["||=", kindIdFromName("||=")], ["??=", kindIdFromName("??=")]]),
        right: _resolveOne(input.right, _K7, _K9),
    });
}
export function awaitExpressionFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("await_expression"))
        return input;
    return F.awaitExpression(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "expression" in input ? input.expression : input), _K7, _K9));
}
export function binaryExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.binaryExpression({
        left: _resolveOne(input.left, _K14, _K9),
        operator: coerceKindEnumStorage(_resolveOne(input.operator, _K15, _K2), []),
        right: _resolveOne(input.right, _K7, _K9),
    });
}
export function breakStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.breakStatement({
        label: _resolveOneLeaf(input.label, "identifier"),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function callExpressionFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("call_expression"))
            return input;
    }
    return _applyFactory(F.callExpression, input);
}
export function callExpressionUFormCallFrom(input) {
    return F.callExpressionUFormCall({
        callExpressionCall: _resolveOneBranch(input.callExpressionCall, "_call_expression_call"),
    });
}
export function callExpressionUFormTemplateCallFrom(input) {
    return F.callExpressionUFormTemplateCall({
        callExpressionTemplateCall: _resolveOneBranch(input.callExpressionTemplateCall, "_call_expression_template_call"),
    });
}
export function callExpressionUFormMemberFrom(input) {
    return F.callExpressionUFormMember({
        callExpressionMember: _resolveOneBranch(input.callExpressionMember, "_call_expression_member"),
    });
}
export function callSignatureFrom(input) {
    if (isNodeData(input))
        return input;
    return F.callSignature({
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
    });
}
export function catchClauseFrom(input) {
    if (isNodeData(input))
        return input;
    return F.catchClause({
        parameter: _resolveOne(input.parameter, _super_import_identifier, _super_destructuring_pattern),
        type: _resolveOneBranch(input.type, "type_annotation"),
        body: _resolveOneBranch(input.body, "statement_block") ?? F.statementBlock(),
    });
}
export function class_From(input) {
    if (isNodeData(input))
        return input;
    return F.class_({
        decorator: _resolveManyBranch(input.decorator, "decorator"),
        name: _resolveOneLeaf(input.name, "_type_identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        classHeritage: _resolveOneBranch(input.classHeritage, "class_heritage"),
        body: _resolveOneBranch(input.body, "class_body") ?? F.classBody(),
    });
}
export function classBodyFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.ClassBody) {
        const data = input[0];
        const stored = data._class_body_method;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.classBody(...children);
    }
    return F.classBody(...input);
}
export function classDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    return F.classDeclaration({
        decorator: _resolveManyBranch(input.decorator, "decorator"),
        name: _resolveOneLeaf(input.name, "_type_identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        classHeritage: _resolveOneBranch(input.classHeritage, "class_heritage"),
        body: _resolveOneBranch(input.body, "class_body") ?? F.classBody(),
        automaticSemicolon: _resolveOneLeaf(input.automaticSemicolon, "_automatic_semicolon"),
    });
}
export function classHeritageExtendsClauseFrom(input) {
    if (isNodeData(input))
        return input;
    return F.classHeritageExtendsClause({
        extendsClause: _resolveOneBranch(input.extendsClause, "extends_clause"),
        implementsClause: _resolveOneBranch(input.implementsClause, "implements_clause"),
    });
}
export function classHeritageImplementsClauseFrom(input) {
    return F.classHeritageImplementsClause(input);
}
export function classHeritageFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("class_heritage"))
            return input;
    }
    return _applyFactory(F.classHeritage, input);
}
export function classHeritageUFormExtendsClauseFrom(input) {
    return F.classHeritageUFormExtendsClause({
        classHeritageExtendsClause: _resolveOneBranch(input.classHeritageExtendsClause, "_class_heritage_extends_clause"),
    });
}
export function classHeritageUFormImplementsClauseFrom(input) {
    return F.classHeritageUFormImplementsClause({
        classHeritageImplementsClause: _resolveOneBranch(input.classHeritageImplementsClause, "_class_heritage_implements_clause"),
    });
}
export function classStaticBlockFrom(input) {
    if (isNodeData(input))
        return input;
    return F.classStaticBlock({
        automaticSemicolon: _resolveOneLeaf(input.automaticSemicolon, "_automatic_semicolon"),
        body: _resolveOneBranch(input.body, "statement_block") ?? F.statementBlock(),
    });
}
export function commentFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.comment(input);
}
export function computedPropertyNameFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("computed_property_name"))
        return input;
    return F.computedPropertyName(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "expression" in input ? input.expression : input), _K7, _K9));
}
export function conditionalTypeFrom(input) {
    if (isNodeData(input))
        return input;
    return F.conditionalType({
        left: _resolveOne(input.left, _K4, _K5),
        right: _resolveOne(input.right, _K4, _K5),
        consequence: _resolveOne(input.consequence, _K4, _K5),
        alternative: _resolveOne(input.alternative, _K4, _K5),
    });
}
export function constraintFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("constraint"))
        return input;
    return F.constraint(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function constructSignatureFrom(input) {
    if (isNodeData(input))
        return input;
    return F.constructSignature({
        abstractMarker: _resolveBooleanKeyword(input.abstractMarker),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        type: _resolveOneBranch(input.type, "type_annotation"),
    });
}
export function constructorTypeFrom(input) {
    if (isNodeData(input))
        return input;
    return F.constructorType({
        abstractMarker: _resolveBooleanKeyword(input.abstractMarker),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        type: _resolveOne(input.type, _K4, _K5),
    });
}
export function continueStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.continueStatement({
        label: _resolveOneLeaf(input.label, "identifier"),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function debuggerStatementFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("debugger_statement"))
        return input;
    return F.debuggerStatement(_resolveOneLeaf((input !== null && typeof input === 'object' && !isNodeData(input) && "semicolon" in input ? input.semicolon : input), "_automatic_semicolon"));
}
export function decoratorFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.Decorator) {
        const data = input;
        const child = data._identifier;
        return F.decorator(child);
    }
    return F.decorator(input);
}
export function decoratorCallExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.decoratorCallExpression({
        function: _resolveOne(input.function, _super_import_identifier, _K16),
        typeArguments: _resolveOneBranch(input.typeArguments, "type_arguments"),
        arguments: _resolveOneBranch(input.arguments, "arguments") ?? F.arguments_(),
    });
}
export function decoratorMemberExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.decoratorMemberExpression({
        object: _resolveOne(input.object, _super_import_identifier, _K16),
        property: _resolveOneLeaf(input.property, "identifier"),
    });
}
export function decoratorParenthesizedExpressionFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.DecoratorParenthesizedExpression) {
        const data = input;
        const child = data._identifier;
        return F.decoratorParenthesizedExpression(child);
    }
    return F.decoratorParenthesizedExpression(input);
}
export function defaultTypeFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("default_type"))
        return input;
    return F.defaultType(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function doStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.doStatement({
        body: _resolveOne(input.body, _K2, _K17),
        condition: _resolveOneBranch(input.condition, "parenthesized_expression"),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function elseClauseFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("else_clause"))
        return input;
    return F.elseClause(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "statement" in input ? input.statement : input), _K2, _K17));
}
export function enumAssignmentFrom(input) {
    if (isNodeData(input))
        return input;
    return F.enumAssignment({
        name: _resolveOne(input.name, _K0, _K1),
        value: _resolveOne(input.value, _K7, _K9),
    });
}
export function enumBodyFrom(input) {
    if (input !== undefined && isNodeData(input))
        return input;
    return F.enumBody({
        name: _resolveMany(input?.name, _K0, _K1),
        enumAssignment: _resolveManyBranch(input?.enumAssignment, "enum_assignment"),
    });
}
export function enumDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    return F.enumDeclaration({
        constMarker: _resolveBooleanKeyword(input.constMarker),
        name: _resolveOneLeaf(input.name, "identifier"),
        body: _resolveOneBranch(input.body, "enum_body") ?? F.enumBody(),
    });
}
export function escapeSequenceFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.escapeSequence(input);
}
export function exportClauseFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.ExportClause) {
        const data = input[0];
        const stored = data._export_specifier;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.exportClause(...children);
    }
    return F.exportClause(...input);
}
export function exportSpecifierFrom(input) {
    if (isNodeData(input))
        return input;
    return F.exportSpecifier({
        exportKind: coerceKindEnumStorage(_resolveOneLeaf(input.exportKind, "_export_specifier_export_kind"), [["type", kindIdFromName("type")], ["typeof", kindIdFromName("typeof")]]),
        name: _resolveOne(input.name, _super_import_identifier, _K18),
        alias: _resolveOne(input.alias, _super_import_identifier, _K18),
    });
}
export function exportStatementTypeExportFrom(input) {
    if (isNodeData(input))
        return input;
    return F.exportStatementTypeExport({
        exportClause: _resolveOneBranch(input.exportClause, "export_clause") ?? F.exportClause(),
        source: _resolveOneBranch(input.source, "string"),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function exportStatementEqualsExportFrom(input) {
    if (isNodeData(input))
        return input;
    return F.exportStatementEqualsExport({
        expression: _resolveOne(input.expression, _K7, _K9),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function exportStatementNamespaceExportFrom(input) {
    if (isNodeData(input))
        return input;
    return F.exportStatementNamespaceExport({
        identifier: _resolveOneLeaf(input.identifier, "identifier"),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function exportStatementFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("export_statement"))
            return input;
    }
    return _applyFactory(F.exportStatement, input);
}
export function exportStatementUFormDefaultFrom(input) {
    return F.exportStatementUFormDefault({
        exportStatementDefault: _resolveOne(input.exportStatementDefault, _K2, _super_export_statement_default),
    });
}
export function exportStatementUFormTypeExportFrom(input) {
    return F.exportStatementUFormTypeExport({
        exportStatementTypeExport: _resolveOneBranch(input.exportStatementTypeExport, "_export_statement_type_export"),
    });
}
export function exportStatementUFormEqualsExportFrom(input) {
    return F.exportStatementUFormEqualsExport({
        exportStatementEqualsExport: _resolveOneBranch(input.exportStatementEqualsExport, "_export_statement_equals_export"),
    });
}
export function exportStatementUFormNamespaceExportFrom(input) {
    return F.exportStatementUFormNamespaceExport({
        exportStatementNamespaceExport: _resolveOneBranch(input.exportStatementNamespaceExport, "_export_statement_namespace_export"),
    });
}
export function expressionStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.expressionStatement({
        expressions: _resolveOne(input.expressions, _K7, _K19),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function extendsClauseFrom(input) {
    if (isNodeData(input))
        return input;
    const _ne_values = _resolveMany(input.value, _K7, _K9);
    _assertNonEmpty(_ne_values, 'extends_clause.values');
    return F.extendsClause({
        value: _ne_values,
        typeArguments: _resolveOneBranch(input.typeArguments, "type_arguments"),
    });
}
export function extendsTypeClauseFrom(input) {
    if (isNodeData(input))
        return input;
    const _ne_types = _resolveMany(input.type, _K20, _K21);
    _assertNonEmpty(_ne_types, 'extends_type_clause.types');
    return F.extendsTypeClause({
        type: _ne_types,
    });
}
export function false_From(input) {
    if (isNodeData(input))
        return input;
    return F.false_();
}
export function finallyClauseFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("finally_clause"))
        return input;
    return F.finallyClause(_resolveOneBranch((input !== null && typeof input === 'object' && !isNodeData(input) && "body" in input ? input.body : input), "statement_block"));
}
export function flowMaybeTypeFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("flow_maybe_type"))
        return input;
    return F.flowMaybeType(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "primaryType" in input ? input.primaryType : input), _K4, _K6));
}
export function forInStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.forInStatement({
        awaitMarker: _resolveBooleanKeyword(input.awaitMarker),
        forHeaderLhs: _resolveOne(input.forHeaderLhs, _K2, _K22),
        operator: coerceKindEnumStorage(_resolveOneLeaf(input.operator, "__for_header_operator"), [["in", kindIdFromName("in")], ["of", kindIdFromName("of")]]),
        right: _resolveOne(input.right, _K7, _K19),
        body: _resolveOne(input.body, _K2, _K17),
    });
}
export function forStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.forStatement({
        initializer: _resolveOne(input.initializer, _K7, _K23),
        condition: _resolveOne(input.condition, _K7, _K19),
        increment: _resolveOne(input.increment, _K7, _K19),
        body: _resolveOne(input.body, _K2, _K17),
    });
}
export function formalParametersFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.FormalParameters) {
        const data = input[0];
        const stored = data._formal_parameter;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.formalParameters(...children);
    }
    return F.formalParameters(...input);
}
export function functionDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    return F.functionDeclaration({
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        name: _resolveOneLeaf(input.name, "identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
        body: _resolveOneBranch(input.body, "statement_block") ?? F.statementBlock(),
        automaticSemicolon: _resolveOneLeaf(input.automaticSemicolon, "_automatic_semicolon"),
    });
}
export function functionExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.functionExpression({
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        name: _resolveOneLeaf(input.name, "identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
        body: _resolveOneBranch(input.body, "statement_block") ?? F.statementBlock(),
    });
}
export function functionSignatureFrom(input) {
    if (isNodeData(input))
        return input;
    return F.functionSignature({
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        name: _resolveOneLeaf(input.name, "identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
        semicolon: _resolveOne(input.semicolon, _K24, _K2),
    });
}
export function functionTypeFrom(input) {
    if (isNodeData(input))
        return input;
    return F.functionType({
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K4, _K25),
    });
}
export function generatorFunctionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.generatorFunction({
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        name: _resolveOneLeaf(input.name, "identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
        body: _resolveOneBranch(input.body, "statement_block") ?? F.statementBlock(),
    });
}
export function generatorFunctionDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    return F.generatorFunctionDeclaration({
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        name: _resolveOneLeaf(input.name, "identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
        body: _resolveOneBranch(input.body, "statement_block") ?? F.statementBlock(),
        automaticSemicolon: _resolveOneLeaf(input.automaticSemicolon, "_automatic_semicolon"),
    });
}
export function genericTypeFrom(input) {
    if (isNodeData(input))
        return input;
    return F.genericType({
        name: _resolveOne(input.name, _K20, _K26),
        typeArguments: _resolveOneBranch(input.typeArguments, "type_arguments") ?? F.typeArguments(),
    });
}
export function hashBangLineFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.hashBangLine(input);
}
export function identifierFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.identifier(input);
}
export function ifStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.ifStatement({
        condition: _resolveOneBranch(input.condition, "parenthesized_expression"),
        consequence: _resolveOne(input.consequence, _K2, _K17),
        alternative: _resolveOneBranch(input.alternative, "else_clause"),
    });
}
export function implementsClauseFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.ImplementsClause) {
        const data = input[0];
        const stored = data._type;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.implementsClause(...children);
    }
    return F.implementsClause(...input);
}
export function import_From(input) {
    if (isNodeData(input))
        return input;
    return F.import_();
}
export function importAliasFrom(input) {
    if (isNodeData(input))
        return input;
    return F.importAlias({
        name: _resolveOneLeaf(input.name, "identifier"),
        value: _resolveOne(input.value, _super_import_identifier, _K27),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function importAttributeFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("import_attribute"))
        return input;
    return F.importAttribute(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "object" in input ? input.object : input), _K28, _K29));
}
export function importClauseNamespaceImportFrom(input) {
    return F.importClauseNamespaceImport(input);
}
export function importClauseNamedImportsFrom(input) {
    return F.importClauseNamedImports(input);
}
export function importClauseDefaultImportFrom(input) {
    if (isNodeData(input))
        return input;
    return F.importClauseDefaultImport({
        importIdentifier: _resolveOneLeaf(input.importIdentifier, "identifier"),
        namespaceImport: _resolveOne(input.namespaceImport, _K2, _K30),
    });
}
export function importClauseFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("import_clause"))
            return input;
    }
    return _applyFactory(F.importClause, input);
}
export function importClauseUFormNamespaceImportFrom(input) {
    return F.importClauseUFormNamespaceImport({
        importClauseNamespaceImport: _resolveOneBranch(input.importClauseNamespaceImport, "_import_clause_namespace_import"),
    });
}
export function importClauseUFormNamedImportsFrom(input) {
    return F.importClauseUFormNamedImports({
        importClauseNamedImports: _resolveOneBranch(input.importClauseNamedImports, "_import_clause_named_imports"),
    });
}
export function importClauseUFormDefaultImportFrom(input) {
    return F.importClauseUFormDefaultImport({
        importClauseDefaultImport: _resolveOneBranch(input.importClauseDefaultImport, "_import_clause_default_import"),
    });
}
export function importRequireClauseFrom(input) {
    if (isNodeData(input))
        return input;
    return F.importRequireClause({
        identifier: _resolveOneLeaf(input.identifier, "identifier"),
        source: _resolveOneBranch(input.source, "string"),
    });
}
export function importSpecifierNameFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("import_specifier_name"))
        return input;
    return F.importSpecifierName(_resolveOneLeaf((input !== null && typeof input === 'object' && !isNodeData(input) && "name" in input ? input.name : input), "identifier"));
}
export function importSpecifierFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("import_specifier"))
            return input;
    }
    return _applyFactory(F.importSpecifier, input);
}
export function importSpecifierUFormNameFrom(input) {
    return F.importSpecifierUFormName({
        importKind: coerceKindEnumStorage(_resolveOneLeaf(input.importKind, "_export_specifier_export_kind"), [["type", kindIdFromName("type")], ["typeof", kindIdFromName("typeof")]]),
        importSpecifierName: _resolveOneBranch(input.importSpecifierName, "_import_specifier_name"),
    });
}
export function importSpecifierUFormAsFrom(input) {
    return F.importSpecifierUFormAs({
        importKind: coerceKindEnumStorage(_resolveOneLeaf(input.importKind, "_export_specifier_export_kind"), [["type", kindIdFromName("type")], ["typeof", kindIdFromName("typeof")]]),
        importSpecifierAs: _resolveOneBranch(input.importSpecifierAs, "_import_specifier_as"),
    });
}
export function importStatementFrom(input) {
    if (isNodeData(input))
        return input;
    const _ne_fromClauses = _resolveMany(input.fromClause, _K2, _K31);
    _assertNonEmpty(_ne_fromClauses, 'import_statement.fromClauses');
    return F.importStatement({
        importClause: coerceKindEnumStorage(_resolveOne(input.importClause, _K2, _K2), [["type", kindIdFromName("type")], ["typeof", kindIdFromName("typeof")]]),
        fromClause: _ne_fromClauses,
        importAttribute: _resolveOneBranch(input.importAttribute, "import_attribute"),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function indexSignatureMappedTypeClauseFrom(input) {
    return F.indexSignatureMappedTypeClause(input);
}
export function indexSignatureFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("index_signature"))
            return input;
    }
    return _applyFactory(F.indexSignature, input);
}
export function indexSignatureUFormColonFrom(input) {
    return F.indexSignatureUFormColon({
        sign: coerceKindEnumStorage(_resolveOne(input.sign, _K2, _K2), [["-", kindIdFromName("-")], ["+", kindIdFromName("+")]]),
        indexSignatureColon: _resolveOneBranch(input.indexSignatureColon, "_index_signature_colon"),
        type: _resolveOne(input.type, _K2, _K32),
    });
}
export function indexSignatureUFormMappedTypeClauseFrom(input) {
    return F.indexSignatureUFormMappedTypeClause({
        sign: coerceKindEnumStorage(_resolveOne(input.sign, _K2, _K2), [["-", kindIdFromName("-")], ["+", kindIdFromName("+")]]),
        indexSignatureMappedTypeClause: _resolveOneBranch(input.indexSignatureMappedTypeClause, "_index_signature_mapped_type_clause"),
        type: _resolveOne(input.type, _K2, _K32),
    });
}
export function indexTypeQueryFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("index_type_query"))
        return input;
    return F.indexTypeQuery(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "primaryType" in input ? input.primaryType : input), _K4, _K6));
}
export function inferTypeFrom(input) {
    if (isNodeData(input))
        return input;
    return F.inferType({
        typeIdentifier: _resolveOneLeaf(input.typeIdentifier, "_type_identifier"),
        type: _resolveOne(input.type, _K4, _K5),
    });
}
export function instantiationExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.instantiationExpression({
        expression: _resolveOne(input.expression, _K7, _K9),
        typeArguments: _resolveOneBranch(input.typeArguments, "type_arguments") ?? F.typeArguments(),
    });
}
export function interfaceDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    return F.interfaceDeclaration({
        name: _resolveOneLeaf(input.name, "_type_identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        extendsTypeClause: _resolveOneBranch(input.extendsTypeClause, "extends_type_clause"),
        body: _resolveOneBranch(input.body, "object_type"),
    });
}
export function internalModuleFrom(input) {
    if (isNodeData(input))
        return input;
    return F.internalModule({
        name: _resolveOne(input.name, _super_import_identifier, _K33),
        body: _resolveOneBranch(input.body, "statement_block"),
    });
}
export function intersectionTypeFrom(input) {
    if (isNodeData(input))
        return input;
    return F.intersectionType({
        left: _resolveOne(input.left, _K4, _K5),
        right: _resolveOne(input.right, _K4, _K5),
    });
}
export function labeledStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.labeledStatement({
        label: _resolveOne(input.label, _super_property_identifier, _K2),
        body: _resolveOne(input.body, _K2, _K17),
    });
}
export function lexicalDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    const _ne_declarators = _resolveManyBranch(input.declarators, "variable_declarator");
    _assertNonEmpty(_ne_declarators, 'lexical_declaration.declarators');
    return F.lexicalDeclaration({
        kind: coerceKindEnumStorage(_resolveOneLeaf(input.kind, "_kind"), [["let", kindIdFromName("let")], ["const", kindIdFromName("const")]]),
        declarators: _ne_declarators,
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function literalTypeFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.LiteralType) {
        const data = input;
        const child = data._number;
        return F.literalType(child);
    }
    return F.literalType(input);
}
export function lookupTypeFrom(input) {
    if (isNodeData(input))
        return input;
    return F.lookupType({
        primaryType: _resolveOne(input.primaryType, _K4, _K6),
        indexType: _resolveOne(input.indexType, _K4, _K5),
    });
}
export function mappedTypeClauseFrom(input) {
    if (isNodeData(input))
        return input;
    return F.mappedTypeClause({
        name: _resolveOneLeaf(input.name, "_type_identifier"),
        type: _resolveOne(input.type, _K4, _K5),
        alias: _resolveOne(input.alias, _K4, _K5),
    });
}
export function memberExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.memberExpression({
        object: _resolveOne(input.object, _K34, _K9),
        optionalChain: _resolveBooleanKeyword(input.optionalChain),
        property: _resolveOne(input.property, _K35, _K2),
    });
}
export function metaPropertyFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.metaProperty(input);
}
export function methodDefinitionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.methodDefinition({
        accessibilityModifier: coerceKindEnumStorage(_resolveOneLeaf(input.accessibilityModifier, "_accessibility_modifier"), [["public", kindIdFromName("public")], ["private", kindIdFromName("private")], ["protected", kindIdFromName("protected")]]),
        staticMarker: _resolveBooleanKeyword(input.staticMarker),
        overrideModifier: _resolveBooleanKeyword(input.overrideModifier),
        readonlyMarker: _resolveBooleanKeyword(input.readonlyMarker),
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        accessorKind: coerceKindEnumStorage(_resolveOneLeaf(input.accessorKind, "_accessor_kind"), [["get", kindIdFromName("get")], ["set", kindIdFromName("set")], ["*", kindIdFromName("*")]]),
        name: _resolveOne(input.name, _K0, _K1),
        optionalMarker: _resolveBooleanKeyword(input.optionalMarker),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
        body: _resolveOneBranch(input.body, "statement_block") ?? F.statementBlock(),
    });
}
export function methodSignatureFrom(input) {
    if (isNodeData(input))
        return input;
    return F.methodSignature({
        accessibilityModifier: coerceKindEnumStorage(_resolveOneLeaf(input.accessibilityModifier, "_accessibility_modifier"), [["public", kindIdFromName("public")], ["private", kindIdFromName("private")], ["protected", kindIdFromName("protected")]]),
        staticMarker: _resolveBooleanKeyword(input.staticMarker),
        overrideModifier: _resolveBooleanKeyword(input.overrideModifier),
        readonlyMarker: _resolveBooleanKeyword(input.readonlyMarker),
        asyncMarker: _resolveBooleanKeyword(input.asyncMarker),
        accessorKind: coerceKindEnumStorage(_resolveOneLeaf(input.accessorKind, "_accessor_kind"), [["get", kindIdFromName("get")], ["set", kindIdFromName("set")], ["*", kindIdFromName("*")]]),
        name: _resolveOne(input.name, _K0, _K1),
        optionalMarker: _resolveBooleanKeyword(input.optionalMarker),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        parameters: _resolveOneBranch(input.parameters, "formal_parameters") ?? F.formalParameters(),
        returnType: _resolveOne(input.returnType, _K2, _K3),
    });
}
export function moduleFrom(input) {
    if (isNodeData(input))
        return input;
    return F.module({
        name: _resolveOne(input.name, _super_import_identifier, _K33),
        body: _resolveOneBranch(input.body, "statement_block"),
    });
}
export function namedImportsFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.NamedImports) {
        const data = input[0];
        const stored = data._import_specifier;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.namedImports(...children);
    }
    return F.namedImports(...input);
}
export function namespaceExportFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.NamespaceExport) {
        const data = input;
        const child = data._module_export_name;
        return F.namespaceExport(child);
    }
    return F.namespaceExport(input);
}
export function namespaceImportFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("namespace_import"))
        return input;
    return F.namespaceImport(_resolveOneLeaf((input !== null && typeof input === 'object' && !isNodeData(input) && "identifier" in input ? input.identifier : input), "identifier"));
}
export function nestedIdentifierFrom(input) {
    if (isNodeData(input))
        return input;
    return F.nestedIdentifier({
        object: _resolveOne(input.object, _super_import_identifier, _K27),
        property: _resolveOneLeaf(input.property, "identifier"),
    });
}
export function nestedTypeIdentifierFrom(input) {
    if (isNodeData(input))
        return input;
    return F.nestedTypeIdentifier({
        module: _resolveOne(input.module, _super_import_identifier, _K27),
        name: _resolveOneLeaf(input.name, "_type_identifier"),
    });
}
export function newExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.newExpression({
        constructor: _resolveOne(input.constructor, _K7, _K36),
        typeArguments: _resolveOneBranch(input.typeArguments, "type_arguments"),
        arguments: _resolveOneBranch(input.arguments, "arguments"),
    });
}
export function nonNullExpressionFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("non_null_expression"))
        return input;
    return F.nonNullExpression(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "expression" in input ? input.expression : input), _K7, _K9));
}
export function null_From(input) {
    if (isNodeData(input))
        return input;
    return F.null_();
}
export function numberFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.number(input);
}
export function objectFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.Object) {
        const data = input[0];
        const stored = data._pair;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.object(...children);
    }
    return F.object(...input);
}
export function objectAssignmentPatternFrom(input) {
    if (isNodeData(input))
        return input;
    return F.objectAssignmentPattern({
        left: _resolveOne(input.left, _super_property_identifier, _super_destructuring_pattern),
        right: _resolveOne(input.right, _K7, _K9),
    });
}
export function objectPatternFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.ObjectPattern) {
        const data = input[0];
        const stored = data._pair_pattern;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.objectPattern(...children);
    }
    return F.objectPattern(...input);
}
export function objectTypeFrom(input) {
    if (isNodeData(input))
        return input;
    const _ne_members = _resolveMany(input.members, _K37, _K38);
    _assertNonEmpty(_ne_members, 'object_type.members');
    return F.objectType({
        opening: coerceKindEnumStorage(_resolveOneLeaf(input.opening, "_object_type_opening"), [["{", kindIdFromName("{")], ["{|", kindIdFromName("{|")]]),
        members: _ne_members,
        closing: coerceKindEnumStorage(_resolveOneLeaf(input.closing, "_object_type_closing"), [["}", kindIdFromName("}")], ["|}", kindIdFromName("|}")]]),
    });
}
export function omittingTypeAnnotationFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("omitting_type_annotation"))
        return input;
    return F.omittingTypeAnnotation(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function optingTypeAnnotationFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("opting_type_annotation"))
        return input;
    return F.optingTypeAnnotation(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function optionalParameterFrom(input) {
    if (isNodeData(input))
        return input;
    return F.optionalParameter({
        decorator: _resolveManyBranch(input.decorator, "decorator"),
        accessibilityModifier: coerceKindEnumStorage(_resolveOneLeaf(input.accessibilityModifier, "accessibility_modifier"), [["public", kindIdFromName("public")], ["private", kindIdFromName("private")], ["protected", kindIdFromName("protected")]]),
        overrideModifier: _resolveBooleanKeyword(input.overrideModifier),
        readonlyMarker: _resolveBooleanKeyword(input.readonlyMarker),
        pattern: _resolveOne(input.pattern, _K39, _K12),
        type: _resolveOneBranch(input.type, "type_annotation"),
        value: _resolveOne(input.value, _K7, _K9),
    });
}
export function optionalTupleParameterFrom(input) {
    if (isNodeData(input))
        return input;
    return F.optionalTupleParameter({
        name: _resolveOneLeaf(input.name, "identifier"),
        type: _resolveOneBranch(input.type, "type_annotation"),
    });
}
export function optionalTypeFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("optional_type"))
        return input;
    return F.optionalType(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function overrideModifierFrom(input) {
    if (isNodeData(input))
        return input;
    return F.overrideModifier();
}
export function pairFrom(input) {
    if (isNodeData(input))
        return input;
    return F.pair({
        key: _resolveOne(input.key, _K0, _K1),
        value: _resolveOne(input.value, _K7, _K9),
    });
}
export function pairPatternFrom(input) {
    if (isNodeData(input))
        return input;
    return F.pairPattern({
        key: _resolveOne(input.key, _K0, _K1),
        value: _resolveOne(input.value, _super_identifier, _K40),
    });
}
export function parenthesizedExpressionSequenceFrom(input) {
    return F.parenthesizedExpressionSequence(input);
}
export function parenthesizedExpressionFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("parenthesized_expression"))
            return input;
    }
    return _applyFactory(F.parenthesizedExpression, input);
}
export function parenthesizedExpressionUFormTypedFrom(input) {
    return F.parenthesizedExpressionUFormTyped({
        parenthesizedExpressionTyped: _resolveOneBranch(input.parenthesizedExpressionTyped, "_parenthesized_expression_typed"),
    });
}
export function parenthesizedExpressionUFormSequenceFrom(input) {
    return F.parenthesizedExpressionUFormSequence({
        parenthesizedExpressionSequence: _resolveOneBranch(input.parenthesizedExpressionSequence, "_parenthesized_expression_sequence"),
    });
}
export function parenthesizedTypeFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("parenthesized_type"))
        return input;
    return F.parenthesizedType(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function predefinedTypeFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.predefinedType(input);
}
export function privatePropertyIdentifierFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.privatePropertyIdentifier(input);
}
export function programFrom(input) {
    if (input !== undefined && isNodeData(input))
        return input;
    return F.program({
        hashBangLine: _resolveOneLeaf(input?.hashBangLine, "hash_bang_line"),
        statements: _resolveMany(input?.statements, _K2, _K17),
    });
}
export function propertySignatureFrom(input) {
    if (isNodeData(input))
        return input;
    return F.propertySignature({
        accessibilityModifier: coerceKindEnumStorage(_resolveOneLeaf(input.accessibilityModifier, "_accessibility_modifier"), [["public", kindIdFromName("public")], ["private", kindIdFromName("private")], ["protected", kindIdFromName("protected")]]),
        staticMarker: _resolveBooleanKeyword(input.staticMarker),
        overrideModifier: _resolveBooleanKeyword(input.overrideModifier),
        readonlyMarker: _resolveBooleanKeyword(input.readonlyMarker),
        name: _resolveOne(input.name, _K0, _K1),
        optionalMarker: _resolveBooleanKeyword(input.optionalMarker),
        type: _resolveOneBranch(input.type, "type_annotation"),
    });
}
export function publicFieldDefinitionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.publicFieldDefinition({
        decorator: _resolveManyBranch(input.decorator, "decorator"),
        publicFieldDefinitionDeclareFirst: _resolveOne(input.publicFieldDefinitionDeclareFirst, _K2, _K41),
        publicFieldDefinitionStaticMods: _resolveOne(input.publicFieldDefinitionStaticMods, _K2, _K42),
        name: _resolveOne(input.name, _K0, _K1),
        optionalityMarker: coerceKindEnumStorage(_resolveOneLeaf(input.optionalityMarker, "_public_field_definition_optionality_marker"), [["?", kindIdFromName("?")], ["!", kindIdFromName("!")]]),
        type: _resolveOneBranch(input.type, "type_annotation"),
        value: _resolveOne(input.value, _K7, _K9),
    });
}
export function readonlyTypeFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("readonly_type"))
        return input;
    return F.readonlyType(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function regexFrom(input) {
    if (isNodeData(input))
        return input;
    return F.regex({
        pattern: _resolveOneLeaf(input.pattern, "regex_pattern"),
        flags: _resolveOneLeaf(input.flags, "regex_flags"),
    });
}
export function regexFlagsFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.regexFlags(input);
}
export function regexPatternFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.regexPattern(input);
}
export function requiredParameterFrom(input) {
    if (isNodeData(input))
        return input;
    return F.requiredParameter({
        decorator: _resolveManyBranch(input.decorator, "decorator"),
        accessibilityModifier: coerceKindEnumStorage(_resolveOneLeaf(input.accessibilityModifier, "accessibility_modifier"), [["public", kindIdFromName("public")], ["private", kindIdFromName("private")], ["protected", kindIdFromName("protected")]]),
        overrideModifier: _resolveBooleanKeyword(input.overrideModifier),
        readonlyMarker: _resolveBooleanKeyword(input.readonlyMarker),
        pattern: _resolveOne(input.pattern, _K39, _K12),
        type: _resolveOneBranch(input.type, "type_annotation"),
        value: _resolveOne(input.value, _K7, _K9),
    });
}
export function restPatternFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.RestPattern) {
        const data = input;
        const child = data._lhs_expression;
        return F.restPattern(child);
    }
    return F.restPattern(input);
}
export function restTypeFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("rest_type"))
        return input;
    return F.restType(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function returnStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.returnStatement({
        expressions: _resolveOne(input.expressions, _K7, _K19),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function satisfiesExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.satisfiesExpression({
        expression: _resolveOne(input.expression, _K7, _K9),
        typeAnnotation: _resolveOne(input.typeAnnotation, _K4, _K5),
    });
}
export function sequenceExpressionFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.SequenceExpression) {
        const data = input[0];
        const stored = data._expression;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.sequenceExpression(...children);
    }
    return F.sequenceExpression(...input);
}
export function spreadElementFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("spread_element"))
        return input;
    return F.spreadElement(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "expression" in input ? input.expression : input), _K7, _K9));
}
export function statementBlockFrom(input) {
    if (input !== undefined && isNodeData(input))
        return input;
    return F.statementBlock({
        statements: _resolveMany(input?.statements, _K2, _K17),
        automaticSemicolon: _resolveOneLeaf(input?.automaticSemicolon, "_automatic_semicolon"),
    });
}
export function stringFrom(input) {
    if (isNodeData(input))
        return input;
    return F.string({
        opening: coerceKindEnumStorage(_resolveOneLeaf(input.opening, "_string_opening"), [["\"", kindIdFromName("\"")], ["'", kindIdFromName("'")]]),
        contents: _resolveMany(input.contents, _K43, _K2),
        closing: coerceKindEnumStorage(_resolveOneLeaf(input.closing, "_string_opening"), [["\"", kindIdFromName("\"")], ["'", kindIdFromName("'")]]),
    });
}
export function subscriptExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.subscriptExpression({
        object: _resolveOne(input.object, _K7, _K9),
        optionalChain: _resolveBooleanKeyword(input.optionalChain),
        index: _resolveOne(input.index, _K7, _K19),
    });
}
export function superFrom(input) {
    if (isNodeData(input))
        return input;
    return F.super_();
}
export function switchBodyFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.SwitchBody) {
        const data = input[0];
        const stored = data._switch_case;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.switchBody(...children);
    }
    return F.switchBody(...input);
}
export function switchCaseFrom(input) {
    if (isNodeData(input))
        return input;
    return F.switchCase({
        value: _resolveOne(input.value, _K7, _K19),
        body: _resolveMany(input.body, _K2, _K17),
    });
}
export function switchDefaultFrom(input) {
    if (input !== undefined && isNodeData(input))
        return input;
    return F.switchDefault({
        body: _resolveMany(input?.body, _K2, _K17),
    });
}
export function switchStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.switchStatement({
        value: _resolveOneBranch(input.value, "parenthesized_expression"),
        body: _resolveOneBranch(input.body, "switch_body") ?? F.switchBody(),
    });
}
export function templateLiteralTypeFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.TemplateLiteralType) {
        const data = input[0];
        const stored = data._template_chars;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.templateLiteralType(...children);
    }
    return F.templateLiteralType(...input);
}
export function templateStringFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.TemplateString) {
        const data = input[0];
        const stored = data._template_chars;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.templateString(...children);
    }
    return F.templateString(...input);
}
export function templateSubstitutionFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.TemplateSubstitution) {
        const data = input;
        const child = data._expressions;
        return F.templateSubstitution(child);
    }
    return F.templateSubstitution(input);
}
export function templateTypeFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.TemplateType) {
        const data = input;
        const child = data._primary_type;
        return F.templateType(child);
    }
    return F.templateType(input);
}
export function ternaryExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.ternaryExpression({
        condition: _resolveOne(input.condition, _K7, _K9),
        consequence: _resolveOne(input.consequence, _K7, _K9),
        alternative: _resolveOne(input.alternative, _K7, _K9),
    });
}
export function thisFrom(input) {
    if (isNodeData(input))
        return input;
    return F.this_();
}
export function throwStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.throwStatement({
        expressions: _resolveOne(input.expressions, _K7, _K19),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function true_From(input) {
    if (isNodeData(input))
        return input;
    return F.true_();
}
export function tryStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.tryStatement({
        body: _resolveOneBranch(input.body, "statement_block") ?? F.statementBlock(),
        handler: _resolveOneBranch(input.handler, "catch_clause"),
        finalizer: _resolveOneBranch(input.finalizer, "finally_clause"),
    });
}
export function tupleParameterFrom(input) {
    if (isNodeData(input))
        return input;
    return F.tupleParameter({
        name: _resolveOne(input.name, _super_import_identifier, _K44),
        type: _resolveOneBranch(input.type, "type_annotation"),
    });
}
export function tupleTypeFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.TupleType) {
        const data = input[0];
        const stored = data._tuple_type_member;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.tupleType(...children);
    }
    return F.tupleType(...input);
}
export function typeAliasDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    return F.typeAliasDeclaration({
        name: _resolveOneLeaf(input.name, "_type_identifier"),
        typeParameters: _resolveOneBranch(input.typeParameters, "type_parameters"),
        value: _resolveOne(input.value, _K4, _K5),
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function typeAnnotationFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("type_annotation"))
        return input;
    return F.typeAnnotation(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "type" in input ? input.type : input), _K4, _K5));
}
export function typeArgumentsFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.TypeArguments) {
        const data = input[0];
        const stored = data._type;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.typeArguments(...children);
    }
    return F.typeArguments(...input);
}
export function typeAssertionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.typeAssertion({
        typeArguments: _resolveOneBranch(input.typeArguments, "type_arguments") ?? F.typeArguments(),
        expression: _resolveOne(input.expression, _K7, _K9),
    });
}
export function typeParameterFrom(input) {
    if (isNodeData(input))
        return input;
    return F.typeParameter({
        constMarker: _resolveBooleanKeyword(input.constMarker),
        name: _resolveOneLeaf(input.name, "_type_identifier"),
        constraint: _resolveOneBranch(input.constraint, "constraint"),
        value: _resolveOneBranch(input.value, "default_type"),
    });
}
export function typeParametersFrom(...input) {
    if (input.length === 1 && isNodeData(input[0]) && input[0].$type === TSKindId.TypeParameters) {
        const data = input[0];
        const stored = data._type_parameter;
        const children = stored === undefined ? [] : Array.isArray(stored) ? stored : [stored];
        return F.typeParameters(...children);
    }
    return F.typeParameters(...input);
}
export function typePredicateFrom(input) {
    if (isNodeData(input))
        return input;
    return F.typePredicate({
        name: _resolveOne(input.name, _K45, _K2),
        type: _resolveOne(input.type, _K4, _K5),
    });
}
export function typePredicateAnnotationFrom(input) {
    if (isNodeData(input) && input.$type === kindIdFromName("type_predicate_annotation"))
        return input;
    return F.typePredicateAnnotation(_resolveOneBranch((input !== null && typeof input === 'object' && !isNodeData(input) && "typePredicate" in input ? input.typePredicate : input), "type_predicate"));
}
export function typeQueryFrom(input) {
    if (isNodeData(input) && input.$type === TSKindId.TypeQuery) {
        const data = input;
        const child = data._type_query_subscript_expression;
        return F.typeQuery(child);
    }
    return F.typeQuery(input);
}
export function unaryExpressionFrom(input) {
    if (isNodeData(input))
        return input;
    return F.unaryExpression({
        operator: coerceKindEnumStorage(_resolveOneLeaf(input.operator, "_unary_expression_operator"), [["!", kindIdFromName("!")], ["~", kindIdFromName("~")], ["-", kindIdFromName("-")], ["+", kindIdFromName("+")], ["typeof", kindIdFromName("typeof")], ["void", kindIdFromName("void")], ["delete", kindIdFromName("delete")]]),
        argument: _resolveOne(input.argument, _K7, _K9),
    });
}
export function undefined_From(input) {
    if (isNodeData(input))
        return input;
    return F.undefined_();
}
export function unescapedDoubleStringFragmentFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.unescapedDoubleStringFragment(input);
}
export function unescapedSingleStringFragmentFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.unescapedSingleStringFragment(input);
}
export function unionTypeFrom(input) {
    if (isNodeData(input))
        return input;
    return F.unionType({
        left: _resolveOne(input.left, _K4, _K5),
        right: _resolveOne(input.right, _K4, _K5),
    });
}
export function updateExpressionFrom(input) {
    if (input !== undefined && isNodeData(input)) {
        if (input.$type === kindIdFromName("update_expression"))
            return input;
    }
    return _applyFactory(F.updateExpression, input);
}
export function updateExpressionUFormPostfixFrom(input) {
    return F.updateExpressionUFormPostfix({
        updateExpressionPostfix: _resolveOneBranch(input.updateExpressionPostfix, "_update_expression_postfix"),
    });
}
export function updateExpressionUFormPrefixFrom(input) {
    return F.updateExpressionUFormPrefix({
        updateExpressionPrefix: _resolveOneBranch(input.updateExpressionPrefix, "_update_expression_prefix"),
    });
}
export function variableDeclarationFrom(input) {
    if (isNodeData(input))
        return input;
    const _ne_declarators = _resolveManyBranch(input.declarators, "variable_declarator");
    _assertNonEmpty(_ne_declarators, 'variable_declaration.declarators');
    return F.variableDeclaration({
        declarators: _ne_declarators,
        semicolon: _resolveOneLeaf(input.semicolon, "_automatic_semicolon"),
    });
}
export function variableDeclaratorFrom(input) {
    if (isNodeData(input))
        return input;
    return F.variableDeclarator({
        name: _resolveOne(input.name, _super_import_identifier, _super_destructuring_pattern),
        type: _resolveOneBranch(input.type, "type_annotation"),
        value: _resolveOne(input.value, _K7, _K9),
    });
}
export function whileStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.whileStatement({
        condition: _resolveOneBranch(input.condition, "parenthesized_expression"),
        body: _resolveOne(input.body, _K2, _K17),
    });
}
export function withStatementFrom(input) {
    if (isNodeData(input))
        return input;
    return F.withStatement({
        object: _resolveOneBranch(input.object, "parenthesized_expression"),
        body: _resolveOne(input.body, _K2, _K17),
    });
}
export function yieldExpressionFrom(input) {
    if (input !== undefined && isNodeData(input) && input.$type === kindIdFromName("yield_expression"))
        return input;
    return F.yieldExpression(_resolveOne((input !== null && typeof input === 'object' && !isNodeData(input) && "expression" in input ? input.expression : input), _K7, _K9));
}
export function htmlCommentFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.htmlComment(input);
}
export function ororFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.oror(input);
}
export function jsxTextFrom(input) {
    if (typeof input !== 'string')
        return input;
    return F.jsxText(input);
}
//# sourceMappingURL=from.js.map
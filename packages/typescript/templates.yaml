# Auto-generated by @sittir/codegen — do not edit
language: typescript
extensions:
  - ts
  - tsx
expandoChar: null
metadata:
  grammarSha: ""
rules:
  abstract_class_declaration: $$$DECORATOR abstract class $NAME $TYPE_PARAMETERS $CLASS_HERITAGE $BODY
  abstract_method_signature: $ACCESSIBILITY_MODIFIER abstract $OVERRIDE_MODIFIER $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE
  adding_type_annotation: +?:$CHILDREN
  ambient_declaration:
    - declare $DECLARATION
    - declare global $DECLARATION
    - declare module.$DECLARATION:$TYPE_ANNOTATION $SEMICOLON
  arguments:
    template: ($$$CHILDREN)
    joinBy: ","
  array:
    template: "[$$$CHILDREN]"
    joinBy: ","
  array_pattern:
    template: "[$$$CHILDREN]"
    joinBy: ","
  array_type: $PRIMARY_TYPE[]
  arrow_function: $PARAMETER $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE=>$BODY
  as_expression:
    - $EXPRESSION as const
    - $EXPRESSION as $TYPE_ANNOTATION
  asserts: asserts $CHILDREN
  asserts_annotation: :$ASSERTS
  assignment_expression: $LEFT=$RIGHT
  assignment_pattern: $LEFT=$RIGHT
  augmented_assignment_expression: $LEFT $OPERATOR $RIGHT
  await_expression: await $EXPRESSION
  binary_expression: $LEFT $OPERATOR $RIGHT
  break_statement: break $LABEL
  call_expression:
    - $FUNCTION $TYPE_ARGUMENTS $ARGUMENTS
    - $FUNCTION $ARGUMENTS
    - $FUNCTION?.$TYPE_ARGUMENTS $ARGUMENTS
  call_signature: $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE
  catch_clause: catch $PARAMETER $TYPE $BODY
  class: $$$DECORATOR class $NAME $TYPE_PARAMETERS $CLASS_HERITAGE $BODY
  class_body:
    - "{$$$DECORATOR $$$CHILDREN}"
    - "{$$$CHILDREN}"
    - "{} $$$CHILDREN"
  class_declaration: $$$DECORATOR class $NAME $TYPE_PARAMETERS $CLASS_HERITAGE $BODY $AUTOMATIC_SEMICOLON
  class_heritage: $EXTENDS_CLAUSE $IMPLEMENTS_CLAUSE
  class_static_block: static $BODY
  computed_property_name: "[$EXPRESSION]"
  conditional_type: $LEFT extends $RIGHT?$CONSEQUENCE:$ALTERNATIVE
  constraint: extends $CHILDREN
  construct_signature: new $TYPE_PARAMETERS $PARAMETERS $TYPE
  constructor_type: new $TYPE_PARAMETERS $PARAMETERS=>$TYPE
  continue_statement: continue $LABEL
  decorator: "@$CHILDREN"
  default_type: =$CHILDREN
  do_statement: do $BODY while $CONDITION
  else_clause: else $STATEMENT
  enum_assignment: $NAME=$VALUE
  enum_body: "{$$$NAME $OPENING $$$MEMBERS}"
  enum_declaration: enum $NAME $BODY
  export_clause:
    template: "{$$$CHILDREN}"
    joinBy: ","
  export_specifier:
    template: $NAME $ALIAS_CLAUSE
    alias_clause: as $ALIAS
  export_statement:
    template:
      - export*from $SOURCE $$$CHILDREN
      - export $DECLARATION from $SOURCE
      - export $DECLARATION
      - $$$DECORATOR export $DECLARATION
      - $$$DECORATOR export default $DECLARATION
      - $$$DECORATOR export default $VALUE $DECLARATION
      - export type $DECLARATION $SOURCE_CLAUSE
      - export=$DECLARATION
      - export as namespace $DECLARATION
    source_clause: from $SOURCE
  expression_statement: $$$CHILDREN
  extends_clause: extends $$$VALUE $$$TYPE_ARGUMENTS
  extends_type_clause:
    template: extends $$$TYPE
    joinBy: ","
  finally_clause: finally $BODY
  flow_maybe_type: ?$PRIMARY_TYPE
  for_in_statement:
    template:
      - for($LEFT $OPERATOR $RIGHT)$BODY
      - for($KIND $LEFT $VALUE_CLAUSE $OPERATOR $RIGHT)$BODY
      - for($KIND $LEFT $OPERATOR $RIGHT)$BODY
    value_clause: = $VALUE
  for_statement: for($INITIALIZER;$$$CONDITION $INCREMENT)$BODY
  formal_parameters: ($$$CHILDREN)
  function_declaration: function $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE $BODY
  function_expression: function $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE $BODY
  function_signature: function $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE
  function_type: $TYPE_PARAMETERS $PARAMETERS=>$RETURN_TYPE
  generator_function: function*$NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE $BODY
  generator_function_declaration: function*$NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE $BODY
  generic_type: $NAME $TYPE_ARGUMENTS
  if_statement: if $CONDITION $CONSEQUENCE $ALTERNATIVE
  implements_clause:
    template: implements $$$CHILDREN
    joinBy: ","
  import_alias: import $NAME=$VALUE $SEMICOLON
  import_attribute: with $OBJECT
  import_clause: $DEFAULT_IMPORT
  import_require_clause: $IDENTIFIER=require($SOURCE)
  import_specifier:
    - $NAME
    - $NAME as $ALIAS
  import_statement:
    - import $IMPORT_CLAUSE $FROM_CLAUSE $IMPORT_ATTRIBUTE $SEMICOLON $SOURCE
    - import $IMPORT_CLAUSE $IMPORT_ATTRIBUTE $SEMICOLON
    - import $SOURCE $IMPORT_ATTRIBUTE $SEMICOLON
  index_signature:
    template:
      - $SIGN_CLAUSE[$NAME:$INDEX_TYPE]$TYPE
      - $SIGN_CLAUSE[$MAPPED_TYPE_CLAUSE]$TYPE
    sign_clause: $SIGN readonly
  index_type_query: keyof $PRIMARY_TYPE
  infer_type:
    template: infer $TYPE_IDENTIFIER $CONSTRAINT_CLAUSE
    constraint_clause: extends $CONSTRAINT
  instantiation_expression: $EXPRESSION $TYPE_ARGUMENTS $FUNCTION
  interface_body: $$$CHILDREN
  interface_declaration: interface $NAME $TYPE_PARAMETERS $EXTENDS_TYPE_CLAUSE $BODY
  internal_module: namespace $NAME $BODY
  intersection_type: $LEFT&$RIGHT
  labeled_statement: $LABEL:$BODY
  lexical_declaration: $KIND $$$DECLARATORS $$$CHILDREN $SEMICOLON
  literal_type: $CHILDREN
  lookup_type: $PRIMARY_TYPE[$INDEX_TYPE]
  mapped_type_clause:
    template: $NAME in $TYPE $ALIAS_CLAUSE
    alias_clause: as $ALIAS
  member_expression:
    - $OBJECT.$PROPERTY
    - $OBJECT $OPTIONAL_CHAIN $PROPERTY
  method_definition: $ACCESSIBILITY_MODIFIER $OVERRIDE_MODIFIER $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE $BODY
  method_signature: $ACCESSIBILITY_MODIFIER $OVERRIDE_MODIFIER $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE
  module: module $NAME $BODY
  named_imports:
    template: "{$$$CHILDREN}"
    joinBy: ","
  namespace_export: "*as $$$CHILDREN"
  namespace_import: "*as $IDENTIFIER"
  nested_identifier: $OBJECT.$PROPERTY
  nested_type_identifier: $MODULE.$NAME
  new_expression: new $CONSTRUCTOR $TYPE_ARGUMENTS $ARGUMENTS
  non_null_expression: $EXPRESSION!
  object:
    template: "{$$$CHILDREN}"
    joinBy: ","
  object_assignment_pattern: $LEFT=$RIGHT
  object_pattern:
    template: "{$$$CHILDREN}"
    joinBy: ","
  object_type: "{$OPENING $$$CHILDREN $$$MEMBERS} $CLOSING"
  omitting_type_annotation: -?:$CHILDREN
  opting_type_annotation: ?:$CHILDREN
  optional_parameter:
    template: $PARAMETER_NAME?$TYPE $VALUE_CLAUSE $$$DECORATOR $NAME $PATTERN $INITIALIZER
    value_clause: = $VALUE
  optional_type: $CHILDREN?
  pair: $KEY:$VALUE
  pair_pattern: $KEY:$VALUE
  parenthesized_expression:
    - ($CHILDREN $TYPE)
    - ($CHILDREN)
  parenthesized_type: ($CHILDREN)
  program: $HASH_BANG_LINE $$$STATEMENTS
  property_signature: $ACCESSIBILITY_MODIFIER $OVERRIDE_MODIFIER $NAME $TYPE
  public_field_definition:
    - $$$DECORATOR $ACCESSIBILITY_MODIFIER $OVERRIDE_MODIFIER $NAME $TYPE $INITIALIZER $VALUE
    - $$$DECORATOR $ACCESSIBILITY_MODIFIER $NAME $TYPE $INITIALIZER $VALUE
  readonly_type: readonly $CHILDREN
  regex: /$PATTERN/$FLAGS
  required_parameter:
    template: $PARAMETER_NAME $TYPE $VALUE_CLAUSE $$$DECORATOR $NAME $PATTERN $INITIALIZER
    value_clause: = $VALUE
  rest_pattern: ... $$$CHILDREN
  rest_type: ...$CHILDREN
  return_statement: return $$$CHILDREN
  satisfies_expression: $EXPRESSION satisfies $TYPE_ANNOTATION
  sequence_expression:
    template: $$$CHILDREN
    joinBy: ","
  spread_element: ...$EXPRESSION
  statement_block: "{$$$STATEMENTS}$AUTOMATIC_SEMICOLON"
  string:
    variants:
      tok_22: '"$$$CHILDREN"'
      tok_27: "'$$$CHILDREN'"
    detect:
      tok_22: '"'
      tok_27: "'"
  subscript_expression: $OBJECT $OPTIONAL_CHAIN[$INDEX]
  switch_body: "{$$$CHILDREN}"
  switch_case: case $VALUE:$$$BODY
  switch_default: default:$$$BODY
  switch_statement: switch $VALUE $BODY
  template_literal_type: "`$$$CHILDREN`"
  template_string: "`$$$CHILDREN`"
  template_substitution: ${} $$$CHILDREN
  template_type: ${ $CHILDREN}
  ternary_expression: $CONDITION?$CONSEQUENCE:$ALTERNATIVE
  throw_statement: throw $$$CHILDREN
  try_statement: try $BODY $HANDLER $FINALIZER
  tuple_type: "[$$$CHILDREN]"
  type_alias_declaration: type $NAME $TYPE_PARAMETERS=$VALUE
  type_annotation: :$CHILDREN
  type_arguments:
    template: <$$$CHILDREN>
    joinBy: ","
  type_assertion: $TYPE_ARGUMENTS $EXPRESSION
  type_parameter: $NAME $CONSTRAINT $VALUE
  type_parameters:
    template: <$$$CHILDREN>
    joinBy: ","
  type_predicate: $NAME is $TYPE
  type_predicate_annotation: :$TYPE_PREDICATE
  type_query: typeof $CHILDREN
  unary_expression: $OPERATOR $ARGUMENT
  union_type: $LEFT|$RIGHT
  update_expression: $ARGUMENT $OPERATOR
  variable_declaration: var $$$DECLARATORS $$$CHILDREN $SEMICOLON
  variable_declarator:
    template:
      - $NAME $TYPE $VALUE_CLAUSE
      - $NAME!$TYPE
    value_clause: = $VALUE
  while_statement: while $CONDITION $BODY
  with_statement: with $OBJECT $BODY
  yield_expression:
    - yield*$EXPRESSION
    - yield $EXPRESSION

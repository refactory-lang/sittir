/**
 * Test template for `{{NODE_KIND}}`.
 *
 * Generated by ir-codegen from the {{GRAMMAR}} grammar.
 * Do not edit manually — re-run ir-codegen to regenerate.
 */

import { describe, it, expect } from 'vitest';
import { {{FACTORY_NAME}} } from '../src/nodes/{{FILE_NAME}}.ts';
import { ir } from '../src/fluent.ts';
import { render } from '../src/render.ts';
import { validate } from '../src/validate.ts';

describe('{{NODE_KIND}}', () => {
	it('should create a valid {{NODE_KIND}} node', () => {
		const node = {{FACTORY_NAME}}({
			{{MINIMAL_CONFIG}}
		});
		expect(node.kind).toBe('{{NODE_KIND}}');
	});

	it('should render valid {{GRAMMAR}} source', () => {
		const node = {{FACTORY_NAME}}({
			{{MINIMAL_CONFIG}}
		});
		const source = render(node);
		const result = validate(source);
		expect(result.ok).toBe(true);
	});

	it('should render via fluent API', () => {
		const source = ir.{{SHORT_NAME}}({{MINIMAL_ARGS}}).render();
		const result = validate(source);
		expect(result.ok).toBe(true);
	});
});

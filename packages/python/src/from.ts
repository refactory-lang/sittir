// Auto-generated by @sittir/codegen — do not edit
// .from() API — shared resolvers, immutable ergonomic setters

import type { AliasedImport, AliasedImportConfig, ArgumentList, ArgumentListConfig, AsPattern, AsPatternConfig, AssertStatement, AssertStatementConfig, Assignment, AssignmentConfig, AssignmentV0, AssignmentV0Config, AssignmentV1, AssignmentV1Config, AssignmentV2, AssignmentV2Config, Attribute, AttributeConfig, AugmentedAssignment, AugmentedAssignmentConfig, Await, AwaitConfig, BinaryOperator, BinaryOperatorAmp, BinaryOperatorAmpConfig, BinaryOperatorAt, BinaryOperatorAtConfig, BinaryOperatorCaret, BinaryOperatorCaretConfig, BinaryOperatorConfig, BinaryOperatorMinus, BinaryOperatorMinusConfig, BinaryOperatorPercent, BinaryOperatorPercentConfig, BinaryOperatorPipe, BinaryOperatorPipeConfig, BinaryOperatorPlus, BinaryOperatorPlusConfig, BinaryOperatorSlash, BinaryOperatorSlashConfig, BinaryOperatorStar, BinaryOperatorStarConfig, BinaryOperatorTok_2a2a, BinaryOperatorTok_2a2aConfig, BinaryOperatorTok_2f2f, BinaryOperatorTok_2f2fConfig, BinaryOperatorTok_3c3c, BinaryOperatorTok_3c3cConfig, BinaryOperatorTok_3e3e, BinaryOperatorTok_3e3eConfig, Block, BlockConfig, BooleanOperator, BooleanOperatorAnd, BooleanOperatorAndConfig, BooleanOperatorConfig, BooleanOperatorOr, BooleanOperatorOrConfig, Call, CallConfig, CaseClause, CaseClauseConfig, CasePattern, CasePatternConfig, Chevron, ChevronConfig, ClassDefinition, ClassDefinitionConfig, ClassPattern, ClassPatternConfig, ComparisonOperator, ComparisonOperatorConfig, ComplexPattern, ComplexPatternConfig, CompoundStatement, ComprehensionClauses, ConcatenatedString, ConcatenatedStringConfig, ConditionalExpression, ConditionalExpressionConfig, ConstrainedType, ConstrainedTypeConfig, DecoratedDefinition, DecoratedDefinitionConfig, Decorator, DecoratorConfig, DefaultParameter, DefaultParameterConfig, DeleteStatement, DeleteStatementConfig, DictPattern, DictPatternConfig, Dictionary, DictionaryComprehension, DictionaryComprehensionConfig, DictionaryConfig, DictionarySplat, DictionarySplatConfig, DictionarySplatPattern, DictionarySplatPatternConfig, DottedName, DottedNameConfig, ElifClause, ElifClauseConfig, ElseClause, ElseClauseConfig, EscapeInterpolation, EscapeSequence, ExceptClause, ExceptClauseConfig, ExecStatement, ExecStatementConfig, Expression, ExpressionList, ExpressionListConfig, ExpressionStatement, ExpressionStatementConfig, ExpressionWithinForInClause, Expressions, FExpression, FinallyClause, FinallyClauseConfig, ForInClause, ForInClauseConfig, ForStatement, ForStatementConfig, FormatExpression, FormatExpressionConfig, FormatSpecifier, FormatSpecifierConfig, FunctionDefinition, FunctionDefinitionConfig, FutureImportStatement, FutureImportStatementConfig, FutureImportStatementParen, FutureImportStatementParenConfig, FutureImportStatementV0, FutureImportStatementV0Config, GeneratorExpression, GeneratorExpressionConfig, GenericType, GenericTypeConfig, GlobalStatement, GlobalStatementConfig, Identifier, IfClause, IfClauseConfig, IfStatement, IfStatementConfig, ImportFromStatement, ImportFromStatementConfig, ImportFromStatementParen, ImportFromStatementParenConfig, ImportFromStatementV0, ImportFromStatementV0Config, ImportFromStatementV1, ImportFromStatementV1Config, ImportStatement, ImportStatementConfig, Interpolation, InterpolationConfig, KeywordArgument, KeywordArgumentConfig, KeywordPattern, KeywordPatternConfig, Lambda, LambdaConfig, LambdaParameters, LambdaParametersConfig, LeftHandSide, List, ListComprehension, ListComprehensionConfig, ListConfig, ListPattern, ListPatternConfig, ListSplat, ListSplatConfig, ListSplatPattern, ListSplatPatternConfig, MatchStatement, MatchStatementConfig, MemberType, MemberTypeConfig, Module, ModuleConfig, NamedExpression, NamedExpressionConfig, NamedExpressionLhs, NonlocalStatement, NonlocalStatementConfig, NotOperator, NotOperatorConfig, Pair, PairConfig, Parameter, Parameters, ParametersConfig, ParenthesizedExpression, ParenthesizedExpressionConfig, ParenthesizedListSplat, ParenthesizedListSplatConfig, Pattern, PatternList, PatternListConfig, PrimaryExpression, PrintStatement, PrintStatementChevron, PrintStatementChevronConfig, PrintStatementConfig, PrintStatementV1, PrintStatementV1Config, RaiseStatement, RaiseStatementConfig, RelativeImport, RelativeImportConfig, ReturnStatement, ReturnStatementConfig, RightHandSide, Set, SetComprehension, SetComprehensionConfig, SetConfig, SimplePattern, SimpleStatement, Slice, SliceConfig, SplatPattern, SplatPatternConfig, SplatType, SplatTypeConfig, String, StringConfig, StringContent, StringContentConfig, Subscript, SubscriptConfig, Suite, TryStatement, TryStatementConfig, Tuple, TupleConfig, TuplePattern, TuplePatternConfig, Type, TypeAliasStatement, TypeAliasStatementConfig, TypeConfig, TypeParameter, TypeParameterConfig, TypedDefaultParameter, TypedDefaultParameterConfig, TypedParameter, TypedParameterConfig, UnaryOperator, UnaryOperatorConfig, UnionPattern, UnionPatternConfig, UnionType, UnionTypeConfig, WhileStatement, WhileStatementConfig, WithClause, WithClauseConfig, WithItem, WithItemConfig, WithStatement, WithStatementConfig, Yield, YieldConfig } from './types.js';
import type { KindMap, FromInputMap } from './types.js';
import type { RuntimeNodeOf } from '@sittir/types';
import { isNodeData, hasKind } from './utils.js';

import { aliased_import_, argument_list_, as_pattern_, assert_statement_, assignment_, assignment__v0_, assignment__v1_, assignment__v2_, attribute_, augmented_assignment_, await_, binary_operator_, binary_operator__amp_, binary_operator__at_, binary_operator__caret_, binary_operator__minus_, binary_operator__percent_, binary_operator__pipe_, binary_operator__plus_, binary_operator__slash_, binary_operator__star_, binary_operator__tok_2a2a_, binary_operator__tok_2f2f_, binary_operator__tok_3c3c_, binary_operator__tok_3e3e_, block_, boolean_operator_, boolean_operator__and_, boolean_operator__or_, break_statement_, call_, case_clause_, case_pattern_, chevron_, class_definition_, class_pattern_, comment_, comparison_operator_, complex_pattern_, concatenated_string_, conditional_expression_, constrained_type_, continue_statement_, decorated_definition_, decorator_, default_parameter_, delete_statement_, dict_pattern_, dictionary_, dictionary_comprehension_, dictionary_splat_, dictionary_splat_pattern_, dotted_name_, elif_clause_, ellipsis_, else_clause_, escape_interpolation_, escape_sequence_, except_clause_, exec_statement_, expression_list_, expression_statement_, false_, finally_clause_, float_, for_in_clause_, for_statement_, format_expression_, format_specifier_, function_definition_, future_import_statement_, future_import_statement__paren_, future_import_statement__v0_, generator_expression_, generic_type_, global_statement_, identifier_, if_clause_, if_statement_, import_from_statement_, import_from_statement__paren_, import_from_statement__v0_, import_from_statement__v1_, import_prefix_, import_statement_, integer_, interpolation_, keyword_argument_, keyword_pattern_, keyword_separator_, lambda_, lambda_parameters_, line_continuation_, list_, list_comprehension_, list_pattern_, list_splat_, list_splat_pattern_, match_statement_, member_type_, module_, named_expression_, none_, nonlocal_statement_, not_operator_, pair_, parameters_, parenthesized_expression_, parenthesized_list_splat_, pass_statement_, pattern_list_, positional_separator_, print_statement_, print_statement__chevron_, print_statement__v1_, raise_statement_, relative_import_, return_statement_, set_, set_comprehension_, slice_, splat_pattern_, splat_type_, string_, string_content_, string_end_, string_start_, subscript_, true_, try_statement_, tuple_, tuple_pattern_, type_, type_alias_statement_, type_conversion_, type_parameter_, typed_default_parameter_, typed_parameter_, unary_operator_, union_pattern_, union_type_, while_statement_, wildcard_import_, with_clause_, with_item_, with_statement_, yield_ } from './factories.js';
import type { AliasedImportFromInput, ArgumentListFromInput, AsPatternFromInput, AssertStatementFromInput, AssignmentFromInput, AttributeFromInput, AugmentedAssignmentFromInput, AwaitFromInput, BinaryOperatorFromInput, BlockFromInput, BooleanOperatorFromInput, CallFromInput, CaseClauseFromInput, CasePatternFromInput, ChevronFromInput, ClassDefinitionFromInput, ClassPatternFromInput, ComparisonOperatorFromInput, ComplexPatternFromInput, ConcatenatedStringFromInput, ConditionalExpressionFromInput, ConstrainedTypeFromInput, DecoratedDefinitionFromInput, DecoratorFromInput, DefaultParameterFromInput, DeleteStatementFromInput, DictPatternFromInput, DictionaryFromInput, DictionaryComprehensionFromInput, DictionarySplatFromInput, DictionarySplatPatternFromInput, DottedNameFromInput, ElifClauseFromInput, ElseClauseFromInput, ExceptClauseFromInput, ExecStatementFromInput, ExpressionListFromInput, ExpressionStatementFromInput, FinallyClauseFromInput, ForInClauseFromInput, ForStatementFromInput, FormatExpressionFromInput, FormatSpecifierFromInput, FunctionDefinitionFromInput, FutureImportStatementFromInput, GeneratorExpressionFromInput, GenericTypeFromInput, GlobalStatementFromInput, IfClauseFromInput, IfStatementFromInput, ImportFromStatementFromInput, ImportStatementFromInput, InterpolationFromInput, KeywordArgumentFromInput, KeywordPatternFromInput, LambdaFromInput, LambdaParametersFromInput, ListFromInput, ListComprehensionFromInput, ListPatternFromInput, ListSplatFromInput, ListSplatPatternFromInput, MatchStatementFromInput, MemberTypeFromInput, ModuleFromInput, NamedExpressionFromInput, NonlocalStatementFromInput, NotOperatorFromInput, PairFromInput, ParametersFromInput, ParenthesizedExpressionFromInput, ParenthesizedListSplatFromInput, PatternListFromInput, PrintStatementFromInput, RaiseStatementFromInput, RelativeImportFromInput, ReturnStatementFromInput, SetFromInput, SetComprehensionFromInput, SliceFromInput, SplatPatternFromInput, SplatTypeFromInput, StringFromInput, StringContentFromInput, SubscriptFromInput, TryStatementFromInput, TupleFromInput, TuplePatternFromInput, TypeFromInput, TypeAliasStatementFromInput, TypeParameterFromInput, TypedDefaultParameterFromInput, TypedParameterFromInput, UnaryOperatorFromInput, UnionPatternFromInput, UnionTypeFromInput, WhileStatementFromInput, WithClauseFromInput, WithItemFromInput, WithStatementFromInput, YieldFromInput, AssignmentV0FromInput, AssignmentV1FromInput, AssignmentV2FromInput, BinaryOperatorPlusFromInput, BinaryOperatorMinusFromInput, BinaryOperatorStarFromInput, BinaryOperatorAtFromInput, BinaryOperatorSlashFromInput, BinaryOperatorPercentFromInput, BinaryOperatorTok_2f2fFromInput, BinaryOperatorTok_2a2aFromInput, BinaryOperatorPipeFromInput, BinaryOperatorAmpFromInput, BinaryOperatorCaretFromInput, BinaryOperatorTok_3c3cFromInput, BinaryOperatorTok_3e3eFromInput, BooleanOperatorAndFromInput, BooleanOperatorOrFromInput, FutureImportStatementV0FromInput, FutureImportStatementParenFromInput, ImportFromStatementV0FromInput, ImportFromStatementV1FromInput, ImportFromStatementParenFromInput, PrintStatementChevronFromInput, PrintStatementV1FromInput } from './types.js';

/** Maps kind string to its typed .from() resolver. */
export type FromMap = {
  [K in keyof FromInputMap]: (input: KindMap[K] | FromInputMap[K]) => RuntimeNodeOf<KindMap[K]>;
};

/** @internal Map of kind string to .from() resolver function. */
export const _fromMap = {
  'aliased_import': aliasedImportFrom,
  'argument_list': argumentListFrom,
  'as_pattern': asPatternFrom,
  'assert_statement': assertStatementFrom,
  'assignment': assignmentFrom,
  'attribute': attributeFrom,
  'augmented_assignment': augmentedAssignmentFrom,
  'await': await_From,
  'binary_operator': binaryOperatorFrom,
  'block': blockFrom,
  'boolean_operator': booleanOperatorFrom,
  'call': callFrom,
  'case_clause': caseClauseFrom,
  'case_pattern': casePatternFrom,
  'chevron': chevronFrom,
  'class_definition': classDefinitionFrom,
  'class_pattern': classPatternFrom,
  'comparison_operator': comparisonOperatorFrom,
  'complex_pattern': complexPatternFrom,
  'concatenated_string': concatenatedStringFrom,
  'conditional_expression': conditionalExpressionFrom,
  'constrained_type': constrainedTypeFrom,
  'decorated_definition': decoratedDefinitionFrom,
  'decorator': decoratorFrom,
  'default_parameter': defaultParameterFrom,
  'delete_statement': deleteStatementFrom,
  'dict_pattern': dictPatternFrom,
  'dictionary': dictionaryFrom,
  'dictionary_comprehension': dictionaryComprehensionFrom,
  'dictionary_splat': dictionarySplatFrom,
  'dictionary_splat_pattern': dictionarySplatPatternFrom,
  'dotted_name': dottedNameFrom,
  'elif_clause': elifClauseFrom,
  'else_clause': elseClauseFrom,
  'except_clause': exceptClauseFrom,
  'exec_statement': execStatementFrom,
  'expression_list': expressionListFrom,
  'expression_statement': expressionStatementFrom,
  'finally_clause': finallyClauseFrom,
  'for_in_clause': forInClauseFrom,
  'for_statement': forStatementFrom,
  'format_expression': formatExpressionFrom,
  'format_specifier': formatSpecifierFrom,
  'function_definition': functionDefinitionFrom,
  'future_import_statement': futureImportStatementFrom,
  'generator_expression': generatorExpressionFrom,
  'generic_type': genericTypeFrom,
  'global_statement': globalStatementFrom,
  'if_clause': ifClauseFrom,
  'if_statement': ifStatementFrom,
  'import_from_statement': importFromStatementFrom,
  'import_statement': importStatementFrom,
  'interpolation': interpolationFrom,
  'keyword_argument': keywordArgumentFrom,
  'keyword_pattern': keywordPatternFrom,
  'lambda': lambdaFrom,
  'lambda_parameters': lambdaParametersFrom,
  'list': listFrom,
  'list_comprehension': listComprehensionFrom,
  'list_pattern': listPatternFrom,
  'list_splat': listSplatFrom,
  'list_splat_pattern': listSplatPatternFrom,
  'match_statement': matchStatementFrom,
  'member_type': memberTypeFrom,
  'module': moduleFrom,
  'named_expression': namedExpressionFrom,
  'nonlocal_statement': nonlocalStatementFrom,
  'not_operator': notOperatorFrom,
  'pair': pairFrom,
  'parameters': parametersFrom,
  'parenthesized_expression': parenthesizedExpressionFrom,
  'parenthesized_list_splat': parenthesizedListSplatFrom,
  'pattern_list': patternListFrom,
  'print_statement': printStatementFrom,
  'raise_statement': raiseStatementFrom,
  'relative_import': relativeImportFrom,
  'return_statement': returnStatementFrom,
  'set': setFrom,
  'set_comprehension': setComprehensionFrom,
  'slice': sliceFrom,
  'splat_pattern': splatPatternFrom,
  'splat_type': splatTypeFrom,
  'string': stringFrom,
  'string_content': stringContentFrom,
  'subscript': subscriptFrom,
  'try_statement': tryStatementFrom,
  'tuple': tupleFrom,
  'tuple_pattern': tuplePatternFrom,
  'type': type_From,
  'type_alias_statement': typeAliasStatementFrom,
  'type_parameter': typeParameterFrom,
  'typed_default_parameter': typedDefaultParameterFrom,
  'typed_parameter': typedParameterFrom,
  'unary_operator': unaryOperatorFrom,
  'union_pattern': unionPatternFrom,
  'union_type': unionTypeFrom,
  'while_statement': whileStatementFrom,
  'with_clause': withClauseFrom,
  'with_item': withItemFrom,
  'with_statement': withStatementFrom,
  'yield': yield_From,
};

export function _resolveByKind(kind: string, rest: unknown) {
  const fn = (_fromMap as Record<string, (input: any) => unknown>)[kind];
  if (fn) return fn(rest);
  throw new Error(`Unknown kind for .from(): '${kind}'`);
}

/** Leaf kind registry for string dispatch. */
const _leafRegistry: Record<string, { values?: string[]; pattern?: RegExp; factory: (text: string) => unknown }> = {
  'break_statement': { values: ["break"], factory: () => break_statement_() },
  'continue_statement': { values: ["continue"], factory: () => continue_statement_() },
  'import_prefix': { pattern: /^(?:\.)+$/, factory: import_prefix_ },
  'keyword_separator': { values: ["*"], factory: () => keyword_separator_() },
  'pass_statement': { values: ["pass"], factory: () => pass_statement_() },
  'positional_separator': { values: ["/"], factory: () => positional_separator_() },
  'wildcard_import': { values: ["*"], factory: () => wildcard_import_() },
  'comment': { pattern: /^#.*$/, factory: comment_ },
  'ellipsis': { values: ["..."], factory: () => ellipsis_() },
  'escape_interpolation': { factory: escape_interpolation_ },
  'escape_sequence': { pattern: /^\\(?:u[a-fA-F\d]{4}|U[a-fA-F\d]{8}|x[a-fA-F\d]{2}|\d{1,3}|\r?\n|['"abfrntv\\]|N\{[^}]+\})$/, factory: escape_sequence_ },
  'false': { values: ["False"], factory: () => false_() },
  'float': { pattern: /^(?:(?:[0-9]+_?)+\.(?:[0-9]+_?)+?[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+?\.(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+)[jJ]?$/, factory: float_ },
  'identifier': { pattern: /^[_\p{XID_Start}][_\p{XID_Continue}]*$/u, factory: identifier_ },
  'integer': { pattern: /^(?:(?:0x|0X)(?:_?[A-Fa-f0-9]+)+[Ll]?|(?:0o|0O)(?:_?[0-7]+)+[Ll]?|(?:0b|0B)(?:_?[0-1]+)+[Ll]?|(?:[0-9]+_?)+(?:[Ll]?|[jJ]?))$/, factory: integer_ },
  'line_continuation': { pattern: /^\\(?:\r?\n| )$/, factory: line_continuation_ },
  'none': { values: ["None"], factory: () => none_() },
  'string_end': { factory: string_end_ },
  'string_start': { factory: string_start_ },
  'true': { values: ["True"], factory: () => true_() },
  'type_conversion': { pattern: /^![a-z]$/, factory: type_conversion_ },
};

/**
 * Resolve a string to the best matching leaf factory from a set of accepted kinds.
 * Returns undefined if no leaf kind matches.
 */
function _resolveLeafString(v: string, kinds: readonly string[]): unknown {
  for (const kind of kinds) {
    const entry = _leafRegistry[kind];
    if (!entry) continue;
    if (entry.values && entry.values.includes(v)) return entry.factory(v);
    if (entry.pattern && entry.pattern.test(v)) return entry.factory(v);
  }
  for (const kind of kinds) {
    const entry = _leafRegistry[kind];
    if (entry && !entry.values && !entry.pattern) return entry.factory(v);
  }
  return undefined;
}

function _resolveCompoundStatement(v: unknown): CompoundStatement;
function _resolveCompoundStatement(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: class_definition, decorated_definition, for_statement, function_definition, if_statement, match_statement, try_statement, while_statement, with_statement. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSimpleStatement(v: unknown): SimpleStatement;
function _resolveSimpleStatement(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["break_statement","continue_statement","pass_statement"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: assert_statement, delete_statement, exec_statement, expression_statement, future_import_statement, global_statement, import_from_statement, import_statement, nonlocal_statement, print_statement, raise_statement, return_statement, type_alias_statement. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryExpression(v: unknown): PrimaryExpression;
function _resolvePrimaryExpression(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["ellipsis","false","float","identifier","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpression(v: unknown): Expression;
function _resolveExpression(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["ellipsis","false","float","identifier","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: as_pattern, attribute, await, binary_operator, boolean_operator, call, comparison_operator, concatenated_string, conditional_expression, dictionary, dictionary_comprehension, generator_expression, lambda, list, list_comprehension, list_splat, named_expression, not_operator, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveParameter(v: unknown): Parameter;
function _resolveParameter(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier","keyword_separator","positional_separator"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: default_parameter, dictionary_splat_pattern, list_splat_pattern, tuple_pattern, typed_default_parameter, typed_parameter. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePattern(v: unknown): Pattern;
function _resolvePattern(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute, list_pattern, list_splat_pattern, subscript, tuple_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSimplePattern(v: unknown): SimplePattern;
function _resolveSimplePattern(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["false","float","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: class_pattern, complex_pattern, concatenated_string, dict_pattern, dotted_name, list_pattern, splat_pattern, string, tuple_pattern, union_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses(v: unknown): ComprehensionClauses;
function _resolveComprehensionClauses(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('for_in_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('for_in_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSuite(v: unknown): Suite;
function _resolveSuite(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('block', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('block', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressions(v: unknown): Expressions;
function _resolveExpressions(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["ellipsis","false","float","identifier","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: as_pattern, attribute, await, binary_operator, boolean_operator, call, comparison_operator, concatenated_string, conditional_expression, dictionary, dictionary_comprehension, expression_list, generator_expression, lambda, list, list_comprehension, list_splat, named_expression, not_operator, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveLeftHandSide(v: unknown): LeftHandSide;
function _resolveLeftHandSide(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute, list_pattern, list_splat_pattern, pattern_list, subscript, tuple_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveRightHandSide(v: unknown): RightHandSide;
function _resolveRightHandSide(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["ellipsis","false","float","identifier","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: as_pattern, assignment, attribute, augmented_assignment, await, binary_operator, boolean_operator, call, comparison_operator, concatenated_string, conditional_expression, dictionary, dictionary_comprehension, expression_list, generator_expression, lambda, list, list_comprehension, list_splat, named_expression, not_operator, parenthesized_expression, pattern_list, set, set_comprehension, string, subscript, tuple, unary_operator, yield. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveFExpression(v: unknown): FExpression;
function _resolveFExpression(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["ellipsis","false","float","identifier","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: as_pattern, attribute, await, binary_operator, boolean_operator, call, comparison_operator, concatenated_string, conditional_expression, dictionary, dictionary_comprehension, expression_list, generator_expression, lambda, list, list_comprehension, list_splat, named_expression, not_operator, parenthesized_expression, pattern_list, set, set_comprehension, string, subscript, tuple, unary_operator, yield. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveNamedExpressionLhs(v: unknown): NamedExpressionLhs;
function _resolveNamedExpressionLhs(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSimplePattern2(v: unknown): KindMap['dotted_name'];
function _resolveSimplePattern2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('dotted_name', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('dotted_name', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1pqs0ui(v: unknown): KindMap['dictionary_splat'] | KindMap['keyword_argument'] | KindMap['list_splat'] | KindMap['parenthesized_expression'] | Expression;
function _r1pqs0ui(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: dictionary_splat, keyword_argument, list_splat, parenthesized_expression, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses2(v: unknown): string;
function _resolveComprehensionClauses2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1mkpyke(v: unknown): KindMap['identifier'] | KindMap['case_pattern'] | Expression;
function _r1mkpyke(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: case_pattern, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses3(v: unknown): Expression;
function _resolveComprehensionClauses3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveExpression(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveExpression(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveLeftHandSide2(v: unknown): KindMap['pattern_list'] | LeftHandSide | Pattern;
function _resolveLeftHandSide2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: pattern_list, attribute, list_pattern, list_splat_pattern, subscript, tuple_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveRightHandSide2(v: unknown): KindMap['assignment'] | KindMap['augmented_assignment'] | KindMap['expression_list'] | KindMap['pattern_list'] | KindMap['yield'] | RightHandSide | Expression;
function _resolveRightHandSide2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: assignment, augmented_assignment, expression_list, pattern_list, yield, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rtec52c(v: unknown): KindMap['type'];
function _rtec52c(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('type', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('type', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses4(v: unknown): PrimaryExpression;
function _resolveComprehensionClauses4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolvePrimaryExpression(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolvePrimaryExpression(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses5(v: unknown): '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=';
function _resolveComprehensionClauses5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['%=', '&=', '**=', '*=', '+=', '-=', '//=', '/=', '<<=', '>>=', '@=', '^=', '|='].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses6(v: unknown): '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|';
function _resolveComprehensionClauses6(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['%', '&', '*', '**', '+', '-', '/', '//', '<<', '>>', '@', '^', '|'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1ednr96(v: unknown): KindMap['case_clause'];
function _r1ednr96(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('case_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('case_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses7(v: unknown): CompoundStatement | SimpleStatement;
function _resolveComprehensionClauses7(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: class_definition, decorated_definition, for_statement, function_definition, if_statement, match_statement, try_statement, while_statement, with_statement, assert_statement, delete_statement, exec_statement, expression_statement, future_import_statement, global_statement, import_from_statement, import_statement, nonlocal_statement, print_statement, raise_statement, return_statement, type_alias_statement. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses8(v: unknown): 'and' | 'or';
function _resolveComprehensionClauses8(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['and', 'or'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1wv67cy(v: unknown): KindMap['argument_list'] | KindMap['generator_expression'];
function _r1wv67cy(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: argument_list, generator_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSuite2(v: unknown): KindMap['block'] | Suite;
function _resolveSuite2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: block. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r17d6grx(v: unknown): KindMap['if_clause'];
function _r17d6grx(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('if_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('if_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rflltaz(v: unknown): KindMap['case_pattern'];
function _rflltaz(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('case_pattern', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('case_pattern', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r17obc8i(v: unknown): KindMap['false'] | KindMap['float'] | KindMap['integer'] | KindMap['none'] | KindMap['true'] | KindMap['as_pattern'] | KindMap['class_pattern'] | KindMap['complex_pattern'] | KindMap['concatenated_string'] | KindMap['dict_pattern'] | KindMap['dotted_name'] | KindMap['keyword_pattern'] | KindMap['list_pattern'] | KindMap['splat_pattern'] | KindMap['string'] | KindMap['tuple_pattern'] | KindMap['union_pattern'] | SimplePattern;
function _r17obc8i(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["false","float","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: as_pattern, class_pattern, complex_pattern, concatenated_string, dict_pattern, dotted_name, keyword_pattern, list_pattern, splat_pattern, string, tuple_pattern, union_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r3e3k0(v: unknown): KindMap['argument_list'];
function _r3e3k0(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('argument_list', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('argument_list', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _reaq3s4(v: unknown): KindMap['type_parameter'];
function _reaq3s4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('type_parameter', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('type_parameter', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses9(v: unknown): '!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in';
function _resolveComprehensionClauses9(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['!=', '<', '<=', '<>', '==', '>', '>=', 'in', 'is', 'is not', 'not in'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSimplePattern3(v: unknown): KindMap['float'] | KindMap['integer'];
function _resolveSimplePattern3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["float","integer"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSimplePattern4(v: unknown): KindMap['string'];
function _resolveSimplePattern4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('string', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('string', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveCompoundStatement2(v: unknown): KindMap['class_definition'] | KindMap['function_definition'];
function _resolveCompoundStatement2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: class_definition, function_definition. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1fl8679(v: unknown): KindMap['decorator'];
function _r1fl8679(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('decorator', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('decorator', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePattern2(v: unknown): KindMap['identifier'] | KindMap['tuple_pattern'];
function _resolvePattern2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('tuple_pattern', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('tuple_pattern', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressions2(v: unknown): KindMap['expression_list'] | Expression;
function _resolveExpressions2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: expression_list, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSimplePattern5(v: unknown): KindMap['splat_pattern'];
function _resolveSimplePattern5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('splat_pattern', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('splat_pattern', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r34sltf(v: unknown): KindMap['dictionary_splat'] | KindMap['pair'];
function _r34sltf(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: dictionary_splat, pair. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rte8hta(v: unknown): KindMap['pair'];
function _rte8hta(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('pair', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('pair', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _ryqidua(v: unknown): KindMap['for_in_clause'] | KindMap['if_clause'] | ComprehensionClauses;
function _ryqidua(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: for_in_clause, if_clause. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePattern3(v: unknown): KindMap['identifier'] | KindMap['attribute'] | KindMap['subscript'];
function _resolvePattern3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute, subscript. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryExpression2(v: unknown): KindMap['identifier'] | KindMap['string'];
function _resolvePrimaryExpression2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveByKind('string', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('string', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveRightHandSide3(v: unknown): KindMap['assignment'] | KindMap['augmented_assignment'] | KindMap['yield'] | Expression;
function _resolveRightHandSide3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: assignment, augmented_assignment, yield, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses10(v: unknown): ExpressionWithinForInClause | Expression;
function _resolveComprehensionClauses10(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if ([','].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1totjtz(v: unknown): KindMap['else_clause'];
function _r1totjtz(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('else_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('else_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressions3(v: unknown): KindMap['expression_list'] | Expressions | Expression;
function _resolveExpressions3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: expression_list, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveFExpression2(v: unknown): KindMap['expression_list'] | KindMap['pattern_list'] | KindMap['yield'] | Expression;
function _resolveFExpression2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: expression_list, pattern_list, yield, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rt4o2h0(v: unknown): KindMap['format_specifier'];
function _rt4o2h0(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('format_specifier', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('format_specifier', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rd1leh5(v: unknown): KindMap['type_conversion'];
function _rd1leh5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["type_conversion"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r15bktga(v: unknown): KindMap['format_expression'];
function _r15bktga(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('format_expression', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('format_expression', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1tsfyeu(v: unknown): KindMap['parameters'];
function _r1tsfyeu(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('parameters', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('parameters', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1s79fxb(v: unknown): KindMap['aliased_import'] | KindMap['dotted_name'];
function _r1s79fxb(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: aliased_import, dotted_name. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rn6vv1r(v: unknown): KindMap['elif_clause'] | KindMap['else_clause'];
function _rn6vv1r(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: elif_clause, else_clause. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r2jsk94(v: unknown): KindMap['dotted_name'] | KindMap['relative_import'];
function _r2jsk94(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: dotted_name, relative_import. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r18yiena(v: unknown): KindMap['wildcard_import'];
function _r18yiena(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["wildcard_import"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveFExpression3(v: unknown): KindMap['expression_list'] | KindMap['pattern_list'] | KindMap['yield'] | FExpression | Expression;
function _resolveFExpression3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: expression_list, pattern_list, yield, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rfpc4ja(v: unknown): KindMap['false'] | KindMap['float'] | KindMap['identifier'] | KindMap['integer'] | KindMap['none'] | KindMap['true'] | KindMap['class_pattern'] | KindMap['complex_pattern'] | KindMap['concatenated_string'] | KindMap['dict_pattern'] | KindMap['dotted_name'] | KindMap['list_pattern'] | KindMap['splat_pattern'] | KindMap['string'] | KindMap['tuple_pattern'] | KindMap['union_pattern'];
function _rfpc4ja(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["false","float","identifier","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: class_pattern, complex_pattern, concatenated_string, dict_pattern, dotted_name, list_pattern, splat_pattern, string, tuple_pattern, union_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveSimplePattern6(v: unknown): KindMap['false'] | KindMap['float'] | KindMap['integer'] | KindMap['none'] | KindMap['true'] | KindMap['class_pattern'] | KindMap['complex_pattern'] | KindMap['concatenated_string'] | KindMap['dict_pattern'] | KindMap['dotted_name'] | KindMap['list_pattern'] | KindMap['splat_pattern'] | KindMap['string'] | KindMap['tuple_pattern'] | KindMap['union_pattern'] | SimplePattern;
function _resolveSimplePattern6(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["false","float","integer","none","true"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: class_pattern, complex_pattern, concatenated_string, dict_pattern, dotted_name, list_pattern, splat_pattern, string, tuple_pattern, union_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _ruhfgja(v: unknown): KindMap['lambda_parameters'];
function _ruhfgja(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('lambda_parameters', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('lambda_parameters', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses11(v: unknown): Parameter;
function _resolveComprehensionClauses11(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveParameter(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveParameter(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rr83r9g(v: unknown): KindMap['list_splat'] | KindMap['parenthesized_list_splat'] | KindMap['yield'] | Expression;
function _rr83r9g(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: list_splat, parenthesized_list_splat, yield, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rflltaz2(v: unknown): KindMap['case_pattern'] | Pattern;
function _rflltaz2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: case_pattern, attribute, list_pattern, list_splat_pattern, subscript, tuple_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePattern4(v: unknown): KindMap['identifier'] | KindMap['attribute'] | KindMap['subscript'] | Expression;
function _resolvePattern4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute, subscript, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveNamedExpressionLhs2(v: unknown): KindMap['identifier'] | NamedExpressionLhs;
function _resolveNamedExpressionLhs2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) return _resolveNamedExpressionLhs(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveNamedExpressionLhs(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveFExpression4(v: unknown): KindMap['list_splat'] | KindMap['parenthesized_expression'] | KindMap['yield'] | Expression;
function _resolveFExpression4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: list_splat, parenthesized_expression, yield, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePrimaryExpression3(v: unknown): KindMap['list_splat'] | KindMap['parenthesized_expression'];
function _resolvePrimaryExpression3(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: list_splat, parenthesized_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses12(v: unknown): Pattern;
function _resolveComprehensionClauses12(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolvePattern(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolvePattern(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rnbxrnb(v: unknown): KindMap['chevron'];
function _rnbxrnb(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('chevron', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('chevron', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rtjvpca(v: unknown): KindMap['import_prefix'];
function _rtjvpca(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["import_prefix"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r11rdi4m(v: unknown): KindMap['string_start'];
function _r11rdi4m(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["string_start"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r14l28kn(v: unknown): KindMap['interpolation'] | KindMap['string_content'];
function _r14l28kn(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: interpolation, string_content. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r8ytgv(v: unknown): KindMap['string_end'];
function _r8ytgv(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["string_end"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1qu323z(v: unknown): KindMap['escape_interpolation'] | KindMap['escape_sequence'];
function _r1qu323z(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["escape_interpolation","escape_sequence"]); if (leaf !== undefined) return leaf; }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1apoc2a(v: unknown): KindMap['slice'] | Expression;
function _r1apoc2a(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: slice, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r16jeonb(v: unknown): KindMap['except_clause'];
function _r16jeonb(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('except_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('except_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rqhziy5(v: unknown): KindMap['finally_clause'];
function _rqhziy5(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('finally_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('finally_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1f2r8fn(v: unknown): KindMap['constrained_type'] | KindMap['generic_type'] | KindMap['member_type'] | KindMap['splat_type'] | KindMap['union_type'] | Expression;
function _r1f2r8fn(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: constrained_type, generic_type, member_type, splat_type, union_type, as_pattern, boolean_operator, comparison_operator, conditional_expression, lambda, named_expression, not_operator, attribute, await, binary_operator, call, concatenated_string, dictionary, dictionary_comprehension, generator_expression, list, list_comprehension, list_splat, parenthesized_expression, set, set_comprehension, string, subscript, tuple, unary_operator. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveParameter2(v: unknown): KindMap['identifier'] | KindMap['dictionary_splat_pattern'] | KindMap['list_splat_pattern'];
function _resolveParameter2(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') { const leaf = _resolveLeafString(v, ["identifier"]); if (leaf !== undefined) return leaf; }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: dictionary_splat_pattern, list_splat_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveComprehensionClauses13(v: unknown): '+' | '-' | '~';
function _resolveComprehensionClauses13(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['+', '-', '~'].includes(v)) return { type: v, text: v };
    const leaf = _resolveLeafString(v, []); if (leaf !== undefined) return leaf;
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r7jahe4(v: unknown): KindMap['with_item'];
function _r7jahe4(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('with_item', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('with_item', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rxymsmy(v: unknown): KindMap['with_clause'];
function _rxymsmy(v: unknown) {
  if (v === undefined) return undefined;
  if (isNodeData(v)) return v;
  if (Array.isArray(v)) return _resolveByKind('with_clause', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('with_clause', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

/** Shared field resolver for hidden rule `_import_list` (used by 3 nodes). */
function _resolveImportListFields(
  obj: Record<string, unknown>,
): { name: (KindMap['aliased_import'] | KindMap['dotted_name'])[] } {
  return {
    name: obj.name !== undefined ? ((Array.isArray(obj.name) ? obj.name : [obj.name])).map(_r1s79fxb) : [],
  };
}

export function aliasedImportFrom(input: RuntimeNodeOf<AliasedImport> | AliasedImportFromInput) {
  if (isNodeData<'aliased_import'>(input)) {
    return aliased_import_({
      alias: input.fields?.alias,
      name: input.fields?.name,
    });
  }
  const obj = input as AliasedImportFromInput;
  return aliased_import_({
    alias: _resolveNamedExpressionLhs(obj.alias),
    name: _resolveSimplePattern2(obj.name),
  });
}

export function argumentListFrom(input: RuntimeNodeOf<ArgumentList>): ReturnType<typeof argument_list_>;
export function argumentListFrom(...children: ArgumentListFromInput[]): ReturnType<typeof argument_list_>;
export function argumentListFrom(...args: (RuntimeNodeOf<ArgumentList> | ArgumentListFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'argument_list') {
    return argument_list_(...(args[0].children ?? []) as (DictionarySplat | Expression | KeywordArgument | ListSplat | ParenthesizedExpression)[]);
  }
  return argument_list_(...args.map(v => _r1pqs0ui(v)) as (DictionarySplat | Expression | KeywordArgument | ListSplat | ParenthesizedExpression)[]);
}

export function asPatternFrom(input: RuntimeNodeOf<AsPattern> | AsPatternFromInput) {
  if (isNodeData<'as_pattern'>(input)) {
    return as_pattern_({
      alias: input.fields?.alias,
      expression: input.fields?.expression,
    });
  }
  const obj = input as AsPatternFromInput;
  return as_pattern_({
    alias: _resolveComprehensionClauses2(obj.alias),
    expression: _r1mkpyke(obj.expression),
  });
}

export function assertStatementFrom(input: RuntimeNodeOf<AssertStatement>): ReturnType<typeof assert_statement_>;
export function assertStatementFrom(...children: AssertStatementFromInput[]): ReturnType<typeof assert_statement_>;
export function assertStatementFrom(...args: (RuntimeNodeOf<AssertStatement> | AssertStatementFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'assert_statement') {
    return assert_statement_(...(args[0].children ?? []) as (Expression)[]);
  }
  return assert_statement_(...args.map(v => _resolveComprehensionClauses3(v)) as (Expression)[]);
}

export function assignmentFrom(input: RuntimeNodeOf<Assignment> | AssignmentFromInput) {
  if (isNodeData<'assignment'>(input)) {
    return assignment_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as AssignmentConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return assignment_(mapped as AssignmentConfig);
}

export function assignmentV0From(input: RuntimeNodeOf<AssignmentV0> | AssignmentV0FromInput) {
  if (isNodeData<'assignment'>(input)) {
    return assignment__v0_({
      left: input.fields?.left,
      right: input.fields?.right,
    } as AssignmentV0Config);
  }
  const obj = input as AssignmentV0FromInput;
  return assignment__v0_({
    left: _resolveLeftHandSide2(obj.left),
    right: _resolveRightHandSide2(obj.right),
  } as AssignmentV0Config);
}

export function assignmentV1From(input: RuntimeNodeOf<AssignmentV1> | AssignmentV1FromInput) {
  if (isNodeData<'assignment'>(input)) {
    return assignment__v1_({
      left: input.fields?.left,
      type: input.fields?.type,
    } as AssignmentV1Config);
  }
  const obj = input as AssignmentV1FromInput;
  return assignment__v1_({
    left: _resolveLeftHandSide2(obj.left),
    type: _rtec52c(obj.type),
  } as AssignmentV1Config);
}

export function assignmentV2From(input: RuntimeNodeOf<AssignmentV2> | AssignmentV2FromInput) {
  if (isNodeData<'assignment'>(input)) {
    return assignment__v2_({
      left: input.fields?.left,
      right: input.fields?.right,
      type: input.fields?.type,
    } as AssignmentV2Config);
  }
  const obj = input as AssignmentV2FromInput;
  return assignment__v2_({
    left: _resolveLeftHandSide2(obj.left),
    right: _resolveRightHandSide2(obj.right),
    type: _rtec52c(obj.type),
  } as AssignmentV2Config);
}

export function attributeFrom(input: RuntimeNodeOf<Attribute> | AttributeFromInput) {
  if (isNodeData<'attribute'>(input)) {
    return attribute_({
      attribute: input.fields?.attribute,
      object: input.fields?.object,
    });
  }
  const obj = input as AttributeFromInput;
  return attribute_({
    attribute: _resolveNamedExpressionLhs(obj.attribute),
    object: _resolveComprehensionClauses4(obj.object),
  });
}

export function augmentedAssignmentFrom(input: RuntimeNodeOf<AugmentedAssignment> | AugmentedAssignmentFromInput) {
  if (isNodeData<'augmented_assignment'>(input)) {
    return augmented_assignment_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    });
  }
  const obj = input as AugmentedAssignmentFromInput;
  return augmented_assignment_({
    left: _resolveLeftHandSide2(obj.left),
    operator: _resolveComprehensionClauses5(obj.operator),
    right: _resolveRightHandSide2(obj.right),
  });
}

export function await_From(input: RuntimeNodeOf<Await> | AwaitFromInput) {
  if (isNodeData<'await'>(input)) {
    return await_({
      primaryExpression: input.fields?.primary_expression,
    });
  }
  const obj = input as AwaitFromInput;
  return await_({
    primaryExpression: _resolveComprehensionClauses4(obj.primary_expression),
  });
}

export function binaryOperatorFrom(input: RuntimeNodeOf<BinaryOperator> | BinaryOperatorFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator_({
      ...Object.fromEntries(Object.entries(input.fields ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as BinaryOperatorConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return binary_operator_(mapped as BinaryOperatorConfig);
}

export function binaryOperatorPlusFrom(input: RuntimeNodeOf<BinaryOperatorPlus> | BinaryOperatorPlusFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__plus_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorPlusConfig);
  }
  const obj = input as BinaryOperatorPlusFromInput;
  return binary_operator__plus_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorPlusConfig);
}

export function binaryOperatorMinusFrom(input: RuntimeNodeOf<BinaryOperatorMinus> | BinaryOperatorMinusFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__minus_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorMinusConfig);
  }
  const obj = input as BinaryOperatorMinusFromInput;
  return binary_operator__minus_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorMinusConfig);
}

export function binaryOperatorStarFrom(input: RuntimeNodeOf<BinaryOperatorStar> | BinaryOperatorStarFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__star_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorStarConfig);
  }
  const obj = input as BinaryOperatorStarFromInput;
  return binary_operator__star_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorStarConfig);
}

export function binaryOperatorAtFrom(input: RuntimeNodeOf<BinaryOperatorAt> | BinaryOperatorAtFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__at_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorAtConfig);
  }
  const obj = input as BinaryOperatorAtFromInput;
  return binary_operator__at_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorAtConfig);
}

export function binaryOperatorSlashFrom(input: RuntimeNodeOf<BinaryOperatorSlash> | BinaryOperatorSlashFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__slash_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorSlashConfig);
  }
  const obj = input as BinaryOperatorSlashFromInput;
  return binary_operator__slash_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorSlashConfig);
}

export function binaryOperatorPercentFrom(input: RuntimeNodeOf<BinaryOperatorPercent> | BinaryOperatorPercentFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__percent_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorPercentConfig);
  }
  const obj = input as BinaryOperatorPercentFromInput;
  return binary_operator__percent_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorPercentConfig);
}

export function binaryOperatorTok_2f2fFrom(input: RuntimeNodeOf<BinaryOperatorTok_2f2f> | BinaryOperatorTok_2f2fFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__tok_2f2f_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorTok_2f2fConfig);
  }
  const obj = input as BinaryOperatorTok_2f2fFromInput;
  return binary_operator__tok_2f2f_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorTok_2f2fConfig);
}

export function binaryOperatorTok_2a2aFrom(input: RuntimeNodeOf<BinaryOperatorTok_2a2a> | BinaryOperatorTok_2a2aFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__tok_2a2a_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorTok_2a2aConfig);
  }
  const obj = input as BinaryOperatorTok_2a2aFromInput;
  return binary_operator__tok_2a2a_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorTok_2a2aConfig);
}

export function binaryOperatorPipeFrom(input: RuntimeNodeOf<BinaryOperatorPipe> | BinaryOperatorPipeFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__pipe_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorPipeConfig);
  }
  const obj = input as BinaryOperatorPipeFromInput;
  return binary_operator__pipe_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorPipeConfig);
}

export function binaryOperatorAmpFrom(input: RuntimeNodeOf<BinaryOperatorAmp> | BinaryOperatorAmpFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__amp_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorAmpConfig);
  }
  const obj = input as BinaryOperatorAmpFromInput;
  return binary_operator__amp_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorAmpConfig);
}

export function binaryOperatorCaretFrom(input: RuntimeNodeOf<BinaryOperatorCaret> | BinaryOperatorCaretFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__caret_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorCaretConfig);
  }
  const obj = input as BinaryOperatorCaretFromInput;
  return binary_operator__caret_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorCaretConfig);
}

export function binaryOperatorTok_3c3cFrom(input: RuntimeNodeOf<BinaryOperatorTok_3c3c> | BinaryOperatorTok_3c3cFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__tok_3c3c_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorTok_3c3cConfig);
  }
  const obj = input as BinaryOperatorTok_3c3cFromInput;
  return binary_operator__tok_3c3c_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorTok_3c3cConfig);
}

export function binaryOperatorTok_3e3eFrom(input: RuntimeNodeOf<BinaryOperatorTok_3e3e> | BinaryOperatorTok_3e3eFromInput) {
  if (isNodeData<'binary_operator'>(input)) {
    return binary_operator__tok_3e3e_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BinaryOperatorTok_3e3eConfig);
  }
  const obj = input as BinaryOperatorTok_3e3eFromInput;
  return binary_operator__tok_3e3e_({
    left: _resolveComprehensionClauses4(obj.left),
    operator: _resolveComprehensionClauses6(obj.operator),
    right: _resolveComprehensionClauses4(obj.right),
  } as BinaryOperatorTok_3e3eConfig);
}

export function blockFrom(input: RuntimeNodeOf<Block> | BlockFromInput) {
  if (isNodeData<'block'>(input)) {
    return block_({
      alternative: input.fields?.alternative,
      children: input.children,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as BlockFromInput;
  return block_({
    alternative: obj.alternative !== undefined ? ((Array.isArray(obj.alternative) ? obj.alternative : [obj.alternative])).map(_r1ednr96) : undefined,
    children: obj.children !== undefined ? ((Array.isArray(obj.children) ? obj.children : [obj.children])).map(_resolveComprehensionClauses7) : undefined,
  });
}

export function booleanOperatorFrom(input: RuntimeNodeOf<BooleanOperator> | BooleanOperatorFromInput) {
  if (isNodeData<'boolean_operator'>(input)) {
    return boolean_operator_({
      ...Object.fromEntries(Object.entries(input.fields ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as BooleanOperatorConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return boolean_operator_(mapped as BooleanOperatorConfig);
}

export function booleanOperatorAndFrom(input: RuntimeNodeOf<BooleanOperatorAnd> | BooleanOperatorAndFromInput) {
  if (isNodeData<'boolean_operator'>(input)) {
    return boolean_operator__and_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BooleanOperatorAndConfig);
  }
  const obj = input as BooleanOperatorAndFromInput;
  return boolean_operator__and_({
    left: _resolveComprehensionClauses3(obj.left),
    operator: _resolveComprehensionClauses8(obj.operator),
    right: _resolveComprehensionClauses3(obj.right),
  } as BooleanOperatorAndConfig);
}

export function booleanOperatorOrFrom(input: RuntimeNodeOf<BooleanOperatorOr> | BooleanOperatorOrFromInput) {
  if (isNodeData<'boolean_operator'>(input)) {
    return boolean_operator__or_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as BooleanOperatorOrConfig);
  }
  const obj = input as BooleanOperatorOrFromInput;
  return boolean_operator__or_({
    left: _resolveComprehensionClauses3(obj.left),
    operator: _resolveComprehensionClauses8(obj.operator),
    right: _resolveComprehensionClauses3(obj.right),
  } as BooleanOperatorOrConfig);
}

export function callFrom(input: RuntimeNodeOf<Call> | CallFromInput) {
  if (isNodeData<'call'>(input)) {
    return call_({
      arguments: input.fields?.arguments,
      function: input.fields?.function,
    });
  }
  const obj = input as CallFromInput;
  return call_({
    arguments: _r1wv67cy(obj.arguments),
    function: _resolveComprehensionClauses4(obj.function),
  });
}

export function caseClauseFrom(input: RuntimeNodeOf<CaseClause> | CaseClauseFromInput) {
  if (isNodeData<'case_clause'>(input)) {
    return case_clause_({
      consequence: input.fields?.consequence,
      guard: input.fields?.guard,
      children: input.children,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as CaseClauseFromInput;
  return case_clause_({
    consequence: _resolveSuite2(obj.consequence),
    guard: _r17d6grx(obj.guard),
    children: obj.children !== undefined ? ((Array.isArray(obj.children) ? obj.children : [obj.children])).map(_rflltaz) : [],
  });
}

export function casePatternFrom(input: RuntimeNodeOf<CasePattern>): ReturnType<typeof case_pattern_>;
export function casePatternFrom(child: CasePatternFromInput): ReturnType<typeof case_pattern_>;
export function casePatternFrom(arg?: RuntimeNodeOf<CasePattern> | CasePatternFromInput) {
  if (isNodeData(arg) && arg.type === 'case_pattern') {
    return case_pattern_(arg.children?.[0] as AsPattern | KeywordPattern | SimplePattern);
  }
  return case_pattern_(_r17obc8i(arg) as AsPattern | KeywordPattern | SimplePattern);
}

export function chevronFrom(input: RuntimeNodeOf<Chevron> | ChevronFromInput) {
  if (isNodeData<'chevron'>(input)) {
    return chevron_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as ChevronFromInput;
  return chevron_({
    expression: _resolveComprehensionClauses3(obj.expression),
  });
}

export function classDefinitionFrom(input: RuntimeNodeOf<ClassDefinition> | ClassDefinitionFromInput) {
  if (isNodeData<'class_definition'>(input)) {
    return class_definition_({
      body: input.fields?.body,
      name: input.fields?.name,
      superclasses: input.fields?.superclasses,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as ClassDefinitionFromInput;
  return class_definition_({
    body: _resolveSuite2(obj.body),
    name: _resolveNamedExpressionLhs(obj.name),
    superclasses: _r3e3k0(obj.superclasses),
    typeParameters: _reaq3s4(obj.type_parameters),
  });
}

export function classPatternFrom(input: RuntimeNodeOf<ClassPattern> | ClassPatternFromInput) {
  if (isNodeData<'class_pattern'>(input)) {
    return class_pattern_({
      dottedName: input.fields?.dotted_name,
      arguments: input.fields?.arguments,
    });
  }
  const obj = input as ClassPatternFromInput;
  return class_pattern_({
    dottedName: _resolveSimplePattern2(obj.dotted_name),
    arguments: obj.arguments !== undefined ? ((Array.isArray(obj.arguments) ? obj.arguments : [obj.arguments])).map(_rflltaz) : undefined,
  });
}

export function comparisonOperatorFrom(input: RuntimeNodeOf<ComparisonOperator> | ComparisonOperatorFromInput) {
  if (isNodeData<'comparison_operator'>(input)) {
    return comparison_operator_({
      operators: input.fields?.operators,
      left: input.fields?.left,
      comparators: input.fields?.comparators,
    });
  }
  const obj = input as ComparisonOperatorFromInput;
  return comparison_operator_({
    operators: obj.operators !== undefined ? ((Array.isArray(obj.operators) ? obj.operators : [obj.operators])).map(_resolveComprehensionClauses9) : [],
    left: _resolveComprehensionClauses4(obj.left),
    comparators: obj.comparators !== undefined ? ((Array.isArray(obj.comparators) ? obj.comparators : [obj.comparators])).map(_resolveComprehensionClauses4) : [],
  });
}

export function complexPatternFrom(input: RuntimeNodeOf<ComplexPattern> | ComplexPatternFromInput) {
  if (isNodeData<'complex_pattern'>(input)) {
    return complex_pattern_({
      real: input.fields?.real,
      imaginary: input.fields?.imaginary,
    });
  }
  const obj = input as ComplexPatternFromInput;
  return complex_pattern_({
    real: _resolveSimplePattern3(obj.real),
    imaginary: _resolveSimplePattern3(obj.imaginary),
  });
}

export function concatenatedStringFrom(input: RuntimeNodeOf<ConcatenatedString>): ReturnType<typeof concatenated_string_>;
export function concatenatedStringFrom(...children: ConcatenatedStringFromInput[]): ReturnType<typeof concatenated_string_>;
export function concatenatedStringFrom(...args: (RuntimeNodeOf<ConcatenatedString> | ConcatenatedStringFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'concatenated_string') {
    return concatenated_string_(...(args[0].children ?? []) as (String)[]);
  }
  return concatenated_string_(...args.map(v => _resolveSimplePattern4(v)) as (String)[]);
}

export function conditionalExpressionFrom(input: RuntimeNodeOf<ConditionalExpression> | ConditionalExpressionFromInput) {
  if (isNodeData<'conditional_expression'>(input)) {
    return conditional_expression_({
      body: input.fields?.body,
      condition: input.fields?.condition,
      alternative: input.fields?.alternative,
    });
  }
  const obj = input as ConditionalExpressionFromInput;
  return conditional_expression_({
    body: _resolveComprehensionClauses3(obj.body),
    condition: _resolveComprehensionClauses3(obj.condition),
    alternative: _resolveComprehensionClauses3(obj.alternative),
  });
}

export function constrainedTypeFrom(input: RuntimeNodeOf<ConstrainedType> | ConstrainedTypeFromInput) {
  if (isNodeData<'constrained_type'>(input)) {
    return constrained_type_({
      baseType: input.fields?.base_type,
      constraint: input.fields?.constraint,
    });
  }
  const obj = input as ConstrainedTypeFromInput;
  return constrained_type_({
    baseType: _rtec52c(obj.base_type),
    constraint: _rtec52c(obj.constraint),
  });
}

export function decoratedDefinitionFrom(input: RuntimeNodeOf<DecoratedDefinition> | DecoratedDefinitionFromInput) {
  if (isNodeData<'decorated_definition'>(input)) {
    return decorated_definition_({
      definition: input.fields?.definition,
      children: input.children,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as DecoratedDefinitionFromInput;
  return decorated_definition_({
    definition: _resolveCompoundStatement2(obj.definition),
    children: obj.children !== undefined ? ((Array.isArray(obj.children) ? obj.children : [obj.children])).map(_r1fl8679) : [],
  });
}

export function decoratorFrom(input: RuntimeNodeOf<Decorator> | DecoratorFromInput) {
  if (isNodeData<'decorator'>(input)) {
    return decorator_({
      expression: input.fields?.expression,
      newline: input.fields?.newline,
    });
  }
  const obj = input as DecoratorFromInput;
  return decorator_({
    expression: _resolveComprehensionClauses3(obj.expression),
    newline: _resolveComprehensionClauses2(obj.newline),
  });
}

export function defaultParameterFrom(input: RuntimeNodeOf<DefaultParameter> | DefaultParameterFromInput) {
  if (isNodeData<'default_parameter'>(input)) {
    return default_parameter_({
      name: input.fields?.name,
      value: input.fields?.value,
    });
  }
  const obj = input as DefaultParameterFromInput;
  return default_parameter_({
    name: _resolvePattern2(obj.name),
    value: _resolveComprehensionClauses3(obj.value),
  });
}

export function deleteStatementFrom(input: RuntimeNodeOf<DeleteStatement>): ReturnType<typeof delete_statement_>;
export function deleteStatementFrom(child: DeleteStatementFromInput): ReturnType<typeof delete_statement_>;
export function deleteStatementFrom(arg?: RuntimeNodeOf<DeleteStatement> | DeleteStatementFromInput) {
  if (isNodeData(arg) && arg.type === 'delete_statement') {
    return delete_statement_(arg.children?.[0] as Expression | ExpressionList);
  }
  return delete_statement_(_resolveExpressions2(arg) as Expression | ExpressionList);
}

export function dictPatternFrom(input: RuntimeNodeOf<DictPattern>): ReturnType<typeof dict_pattern_>;
export function dictPatternFrom(...children: DictPatternFromInput[]): ReturnType<typeof dict_pattern_>;
export function dictPatternFrom(...args: (RuntimeNodeOf<DictPattern> | DictPatternFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'dict_pattern') {
    return dict_pattern_(...(args[0].children ?? []) as (SplatPattern)[]);
  }
  return dict_pattern_(...args.map(v => _resolveSimplePattern5(v)) as (SplatPattern)[]);
}

export function dictionaryFrom(input: RuntimeNodeOf<Dictionary>): ReturnType<typeof dictionary_>;
export function dictionaryFrom(...children: DictionaryFromInput[]): ReturnType<typeof dictionary_>;
export function dictionaryFrom(...args: (RuntimeNodeOf<Dictionary> | DictionaryFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'dictionary') {
    return dictionary_(...(args[0].children ?? []) as (DictionarySplat | Pair)[]);
  }
  return dictionary_(...args.map(v => _r34sltf(v)) as (DictionarySplat | Pair)[]);
}

export function dictionaryComprehensionFrom(input: RuntimeNodeOf<DictionaryComprehension> | DictionaryComprehensionFromInput) {
  if (isNodeData<'dictionary_comprehension'>(input)) {
    return dictionary_comprehension_({
      body: input.fields?.body,
      children: input.children[0]!,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as DictionaryComprehensionFromInput;
  return dictionary_comprehension_({
    body: _rte8hta(obj.body),
    children: _ryqidua(obj.children),
  });
}

export function dictionarySplatFrom(input: RuntimeNodeOf<DictionarySplat> | DictionarySplatFromInput) {
  if (isNodeData<'dictionary_splat'>(input)) {
    return dictionary_splat_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as DictionarySplatFromInput;
  return dictionary_splat_({
    expression: _resolveComprehensionClauses3(obj.expression),
  });
}

export function dictionarySplatPatternFrom(input: RuntimeNodeOf<DictionarySplatPattern>): ReturnType<typeof dictionary_splat_pattern_>;
export function dictionarySplatPatternFrom(child: DictionarySplatPatternFromInput): ReturnType<typeof dictionary_splat_pattern_>;
export function dictionarySplatPatternFrom(arg?: RuntimeNodeOf<DictionarySplatPattern> | DictionarySplatPatternFromInput) {
  if (isNodeData(arg) && arg.type === 'dictionary_splat_pattern') {
    return dictionary_splat_pattern_(arg.children?.[0] as Attribute | Identifier | Subscript);
  }
  return dictionary_splat_pattern_(_resolvePattern3(arg) as Attribute | Identifier | Subscript);
}

export function dottedNameFrom(input: RuntimeNodeOf<DottedName>): ReturnType<typeof dotted_name_>;
export function dottedNameFrom(...children: DottedNameFromInput[]): ReturnType<typeof dotted_name_>;
export function dottedNameFrom(...args: (RuntimeNodeOf<DottedName> | DottedNameFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'dotted_name') {
    return dotted_name_(...(args[0].children ?? []) as (Identifier)[]);
  }
  return dotted_name_(...args.map(v => _resolveNamedExpressionLhs(v)) as (Identifier)[]);
}

export function elifClauseFrom(input: RuntimeNodeOf<ElifClause> | ElifClauseFromInput) {
  if (isNodeData<'elif_clause'>(input)) {
    return elif_clause_({
      condition: input.fields?.condition,
      consequence: input.fields?.consequence,
    });
  }
  const obj = input as ElifClauseFromInput;
  return elif_clause_({
    condition: _resolveComprehensionClauses3(obj.condition),
    consequence: _resolveSuite2(obj.consequence),
  });
}

export function elseClauseFrom(input: RuntimeNodeOf<ElseClause> | ElseClauseFromInput) {
  if (isNodeData<'else_clause'>(input)) {
    return else_clause_({
      body: input.fields?.body,
    });
  }
  const obj = input as ElseClauseFromInput;
  return else_clause_({
    body: _resolveSuite2(obj.body),
  });
}

export function exceptClauseFrom(input: RuntimeNodeOf<ExceptClause> | ExceptClauseFromInput) {
  if (isNodeData<'except_clause'>(input)) {
    return except_clause_({
      alias: input.fields?.alias,
      value: input.fields?.value,
      children: input.children[0]!,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as ExceptClauseFromInput;
  return except_clause_({
    alias: _resolveComprehensionClauses3(obj.alias),
    value: obj.value !== undefined ? ((Array.isArray(obj.value) ? obj.value : [obj.value])).map(_resolveComprehensionClauses3) : undefined,
    children: _resolveSuite2(obj.children),
  });
}

export function execStatementFrom(input: RuntimeNodeOf<ExecStatement> | ExecStatementFromInput) {
  if (isNodeData<'exec_statement'>(input)) {
    return exec_statement_({
      code: input.fields?.code,
      children: input.children,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as ExecStatementFromInput;
  return exec_statement_({
    code: _resolvePrimaryExpression2(obj.code),
    children: obj.children !== undefined ? ((Array.isArray(obj.children) ? obj.children : [obj.children])).map(_resolveComprehensionClauses3) : undefined,
  });
}

export function expressionListFrom(input: RuntimeNodeOf<ExpressionList>): ReturnType<typeof expression_list_>;
export function expressionListFrom(...children: ExpressionListFromInput[]): ReturnType<typeof expression_list_>;
export function expressionListFrom(...args: (RuntimeNodeOf<ExpressionList> | ExpressionListFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'expression_list') {
    return expression_list_(...(args[0].children ?? []) as (Expression)[]);
  }
  return expression_list_(...args.map(v => _resolveComprehensionClauses3(v)) as (Expression)[]);
}

export function expressionStatementFrom(input: RuntimeNodeOf<ExpressionStatement>): ReturnType<typeof expression_statement_>;
export function expressionStatementFrom(...children: ExpressionStatementFromInput[]): ReturnType<typeof expression_statement_>;
export function expressionStatementFrom(...args: (RuntimeNodeOf<ExpressionStatement> | ExpressionStatementFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'expression_statement') {
    return expression_statement_(...(args[0].children ?? []) as (Assignment | AugmentedAssignment | Expression | Yield)[]);
  }
  return expression_statement_(...args.map(v => _resolveRightHandSide3(v)) as (Assignment | AugmentedAssignment | Expression | Yield)[]);
}

export function finallyClauseFrom(input: RuntimeNodeOf<FinallyClause> | FinallyClauseFromInput) {
  if (isNodeData<'finally_clause'>(input)) {
    return finally_clause_({
      block: input.fields?.block,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as FinallyClauseFromInput;
  return finally_clause_({
    block: _resolveSuite(obj.block),
  });
}

export function forInClauseFrom(input: RuntimeNodeOf<ForInClause> | ForInClauseFromInput) {
  if (isNodeData<'for_in_clause'>(input)) {
    return for_in_clause_({
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as ForInClauseFromInput;
  return for_in_clause_({
    left: _resolveLeftHandSide2(obj.left),
    right: obj.right !== undefined ? ((Array.isArray(obj.right) ? obj.right : [obj.right])).map(_resolveComprehensionClauses10) : [],
  });
}

export function forStatementFrom(input: RuntimeNodeOf<ForStatement> | ForStatementFromInput) {
  if (isNodeData<'for_statement'>(input)) {
    return for_statement_({
      alternative: input.fields?.alternative,
      body: input.fields?.body,
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as ForStatementFromInput;
  return for_statement_({
    alternative: _r1totjtz(obj.alternative),
    body: _resolveSuite2(obj.body),
    left: _resolveLeftHandSide2(obj.left),
    right: _resolveExpressions3(obj.right),
  });
}

export function formatExpressionFrom(input: RuntimeNodeOf<FormatExpression> | FormatExpressionFromInput) {
  if (isNodeData<'format_expression'>(input)) {
    return format_expression_({
      expression: input.fields?.expression,
      formatSpecifier: input.fields?.format_specifier,
      typeConversion: input.fields?.type_conversion,
    });
  }
  const obj = input as FormatExpressionFromInput;
  return format_expression_({
    expression: _resolveFExpression2(obj.expression),
    formatSpecifier: _rt4o2h0(obj.format_specifier),
    typeConversion: _rd1leh5(obj.type_conversion),
  });
}

export function formatSpecifierFrom(input: RuntimeNodeOf<FormatSpecifier>): ReturnType<typeof format_specifier_>;
export function formatSpecifierFrom(...children: FormatSpecifierFromInput[]): ReturnType<typeof format_specifier_>;
export function formatSpecifierFrom(...args: (RuntimeNodeOf<FormatSpecifier> | FormatSpecifierFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'format_specifier') {
    return format_specifier_(...(args[0].children ?? []) as (FormatExpression)[]);
  }
  return format_specifier_(...args.map(v => _r15bktga(v)) as (FormatExpression)[]);
}

export function functionDefinitionFrom(input: RuntimeNodeOf<FunctionDefinition> | FunctionDefinitionFromInput) {
  if (isNodeData<'function_definition'>(input)) {
    return function_definition_({
      body: input.fields?.body,
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
    });
  }
  const obj = input as FunctionDefinitionFromInput;
  return function_definition_({
    body: _resolveSuite2(obj.body),
    name: _resolveNamedExpressionLhs(obj.name),
    parameters: _r1tsfyeu(obj.parameters),
    returnType: _rtec52c(obj.return_type),
    typeParameters: _reaq3s4(obj.type_parameters),
  });
}

export function futureImportStatementFrom(input: RuntimeNodeOf<FutureImportStatement> | FutureImportStatementFromInput) {
  if (isNodeData<'future_import_statement'>(input)) {
    return future_import_statement_({
      ...Object.fromEntries(Object.entries(input.fields ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as FutureImportStatementConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return future_import_statement_(mapped as FutureImportStatementConfig);
}

export function futureImportStatementV0From(input: RuntimeNodeOf<FutureImportStatementV0> | FutureImportStatementV0FromInput) {
  if (isNodeData<'future_import_statement'>(input)) {
    return future_import_statement__v0_({
      name: input.fields?.name,
    } as FutureImportStatementV0Config);
  }
  const obj = input as FutureImportStatementV0FromInput;
  return future_import_statement__v0_({
    name: obj.name !== undefined ? ((Array.isArray(obj.name) ? obj.name : [obj.name])).map(_r1s79fxb) : [],
  } as FutureImportStatementV0Config);
}

export function futureImportStatementParenFrom(input: RuntimeNodeOf<FutureImportStatementParen> | FutureImportStatementParenFromInput) {
  if (isNodeData<'future_import_statement'>(input)) {
    return future_import_statement__paren_({
      name: input.fields?.name,
    } as FutureImportStatementParenConfig);
  }
  const obj = input as FutureImportStatementParenFromInput;
  return future_import_statement__paren_({
    name: obj.name !== undefined ? ((Array.isArray(obj.name) ? obj.name : [obj.name])).map(_r1s79fxb) : [],
  } as FutureImportStatementParenConfig);
}

export function generatorExpressionFrom(input: RuntimeNodeOf<GeneratorExpression> | GeneratorExpressionFromInput) {
  if (isNodeData<'generator_expression'>(input)) {
    return generator_expression_({
      body: input.fields?.body,
      children: input.children[0]!,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as GeneratorExpressionFromInput;
  return generator_expression_({
    body: _resolveComprehensionClauses3(obj.body),
    children: _ryqidua(obj.children),
  });
}

export function genericTypeFrom(input: RuntimeNodeOf<GenericType> | GenericTypeFromInput) {
  if (isNodeData<'generic_type'>(input)) {
    return generic_type_({
      identifier: input.fields?.identifier,
      typeParameter: input.fields?.type_parameter,
    });
  }
  const obj = input as GenericTypeFromInput;
  return generic_type_({
    identifier: _resolveNamedExpressionLhs(obj.identifier),
    typeParameter: _reaq3s4(obj.type_parameter),
  });
}

export function globalStatementFrom(input: RuntimeNodeOf<GlobalStatement>): ReturnType<typeof global_statement_>;
export function globalStatementFrom(...children: GlobalStatementFromInput[]): ReturnType<typeof global_statement_>;
export function globalStatementFrom(...args: (RuntimeNodeOf<GlobalStatement> | GlobalStatementFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'global_statement') {
    return global_statement_(...(args[0].children ?? []) as (Identifier)[]);
  }
  return global_statement_(...args.map(v => _resolveNamedExpressionLhs(v)) as (Identifier)[]);
}

export function ifClauseFrom(input: RuntimeNodeOf<IfClause> | IfClauseFromInput) {
  if (isNodeData<'if_clause'>(input)) {
    return if_clause_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as IfClauseFromInput;
  return if_clause_({
    expression: _resolveComprehensionClauses3(obj.expression),
  });
}

export function ifStatementFrom(input: RuntimeNodeOf<IfStatement> | IfStatementFromInput) {
  if (isNodeData<'if_statement'>(input)) {
    return if_statement_({
      alternative: input.fields?.alternative,
      condition: input.fields?.condition,
      consequence: input.fields?.consequence,
    });
  }
  const obj = input as IfStatementFromInput;
  return if_statement_({
    alternative: obj.alternative !== undefined ? ((Array.isArray(obj.alternative) ? obj.alternative : [obj.alternative])).map(_rn6vv1r) : undefined,
    condition: _resolveComprehensionClauses3(obj.condition),
    consequence: _resolveSuite2(obj.consequence),
  });
}

export function importFromStatementFrom(input: RuntimeNodeOf<ImportFromStatement> | ImportFromStatementFromInput) {
  if (isNodeData<'import_from_statement'>(input)) {
    return import_from_statement_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as ImportFromStatementConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return import_from_statement_(mapped as ImportFromStatementConfig);
}

export function importFromStatementV0From(input: RuntimeNodeOf<ImportFromStatementV0> | ImportFromStatementV0FromInput) {
  if (isNodeData<'import_from_statement'>(input)) {
    return import_from_statement__v0_({
      moduleName: input.fields?.module_name,
      wildcardImport: input.fields?.wildcard_import,
    } as ImportFromStatementV0Config);
  }
  const obj = input as ImportFromStatementV0FromInput;
  return import_from_statement__v0_({
    moduleName: _r2jsk94(obj.module_name),
    wildcardImport: _r18yiena(obj.wildcard_import),
  } as ImportFromStatementV0Config);
}

export function importFromStatementV1From(input: RuntimeNodeOf<ImportFromStatementV1> | ImportFromStatementV1FromInput) {
  if (isNodeData<'import_from_statement'>(input)) {
    return import_from_statement__v1_({
      moduleName: input.fields?.module_name,
      name: input.fields?.name,
      wildcardImport: input.fields?.wildcard_import,
    } as ImportFromStatementV1Config);
  }
  const obj = input as ImportFromStatementV1FromInput;
  return import_from_statement__v1_({
    moduleName: _r2jsk94(obj.module_name),
    name: obj.name !== undefined ? ((Array.isArray(obj.name) ? obj.name : [obj.name])).map(_r1s79fxb) : undefined,
    wildcardImport: _r18yiena(obj.wildcard_import),
  } as ImportFromStatementV1Config);
}

export function importFromStatementParenFrom(input: RuntimeNodeOf<ImportFromStatementParen> | ImportFromStatementParenFromInput) {
  if (isNodeData<'import_from_statement'>(input)) {
    return import_from_statement__paren_({
      moduleName: input.fields?.module_name,
      name: input.fields?.name,
      wildcardImport: input.fields?.wildcard_import,
    } as ImportFromStatementParenConfig);
  }
  const obj = input as ImportFromStatementParenFromInput;
  return import_from_statement__paren_({
    moduleName: _r2jsk94(obj.module_name),
    name: obj.name !== undefined ? ((Array.isArray(obj.name) ? obj.name : [obj.name])).map(_r1s79fxb) : undefined,
    wildcardImport: _r18yiena(obj.wildcard_import),
  } as ImportFromStatementParenConfig);
}

export function importStatementFrom(input: RuntimeNodeOf<ImportStatement> | ImportStatementFromInput) {
  if (isNodeData<'import_statement'>(input)) {
    return import_statement_({
      name: input.fields?.name,
    });
  }
  const obj = input as ImportStatementFromInput;
  return import_statement_({
    ..._resolveImportListFields(obj),
  });
}

export function interpolationFrom(input: RuntimeNodeOf<Interpolation> | InterpolationFromInput) {
  if (isNodeData<'interpolation'>(input)) {
    return interpolation_({
      expression: input.fields?.expression,
      formatSpecifier: input.fields?.format_specifier,
      typeConversion: input.fields?.type_conversion,
    });
  }
  const obj = input as InterpolationFromInput;
  return interpolation_({
    expression: _resolveFExpression3(obj.expression),
    formatSpecifier: _rt4o2h0(obj.format_specifier),
    typeConversion: _rd1leh5(obj.type_conversion),
  });
}

export function keywordArgumentFrom(input: RuntimeNodeOf<KeywordArgument> | KeywordArgumentFromInput) {
  if (isNodeData<'keyword_argument'>(input)) {
    return keyword_argument_({
      name: input.fields?.name,
      value: input.fields?.value,
    });
  }
  const obj = input as KeywordArgumentFromInput;
  return keyword_argument_({
    name: _resolveNamedExpressionLhs(obj.name),
    value: _resolveComprehensionClauses3(obj.value),
  });
}

export function keywordPatternFrom(input: RuntimeNodeOf<KeywordPattern> | KeywordPatternFromInput) {
  if (isNodeData<'keyword_pattern'>(input)) {
    return keyword_pattern_({
      identifier: input.fields?.identifier,
      simplePattern: input.fields?.simple_pattern,
    });
  }
  const obj = input as KeywordPatternFromInput;
  return keyword_pattern_({
    identifier: _rfpc4ja(obj.identifier),
    simplePattern: _resolveSimplePattern6(obj.simple_pattern),
  });
}

export function lambdaFrom(input: RuntimeNodeOf<Lambda> | LambdaFromInput) {
  if (isNodeData<'lambda'>(input)) {
    return lambda_({
      body: input.fields?.body,
      parameters: input.fields?.parameters,
    });
  }
  const obj = input as LambdaFromInput;
  return lambda_({
    body: _resolveComprehensionClauses3(obj.body),
    parameters: _ruhfgja(obj.parameters),
  });
}

export function lambdaParametersFrom(input: RuntimeNodeOf<LambdaParameters>): ReturnType<typeof lambda_parameters_>;
export function lambdaParametersFrom(...children: LambdaParametersFromInput[]): ReturnType<typeof lambda_parameters_>;
export function lambdaParametersFrom(...args: (RuntimeNodeOf<LambdaParameters> | LambdaParametersFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'lambda_parameters') {
    return lambda_parameters_(...(args[0].children ?? []) as (Parameter)[]);
  }
  return lambda_parameters_(...args.map(v => _resolveComprehensionClauses11(v)) as (Parameter)[]);
}

export function listFrom(input: RuntimeNodeOf<List>): ReturnType<typeof list_>;
export function listFrom(...children: ListFromInput[]): ReturnType<typeof list_>;
export function listFrom(...args: (RuntimeNodeOf<List> | ListFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'list') {
    return list_(...(args[0].children ?? []) as (Expression | ListSplat | ParenthesizedListSplat | Yield)[]);
  }
  return list_(...args.map(v => _rr83r9g(v)) as (Expression | ListSplat | ParenthesizedListSplat | Yield)[]);
}

export function listComprehensionFrom(input: RuntimeNodeOf<ListComprehension> | ListComprehensionFromInput) {
  if (isNodeData<'list_comprehension'>(input)) {
    return list_comprehension_({
      body: input.fields?.body,
      children: input.children[0]!,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as ListComprehensionFromInput;
  return list_comprehension_({
    body: _resolveComprehensionClauses3(obj.body),
    children: _ryqidua(obj.children),
  });
}

export function listPatternFrom(input: RuntimeNodeOf<ListPattern>): ReturnType<typeof list_pattern_>;
export function listPatternFrom(...children: ListPatternFromInput[]): ReturnType<typeof list_pattern_>;
export function listPatternFrom(...args: (RuntimeNodeOf<ListPattern> | ListPatternFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'list_pattern') {
    return list_pattern_(...(args[0].children ?? []) as (CasePattern | Pattern)[]);
  }
  return list_pattern_(...args.map(v => _rflltaz2(v)) as (CasePattern | Pattern)[]);
}

export function listSplatFrom(input: RuntimeNodeOf<ListSplat> | ListSplatFromInput) {
  if (isNodeData<'list_splat'>(input)) {
    return list_splat_({
      expression: input.fields?.expression,
    });
  }
  const obj = input as ListSplatFromInput;
  return list_splat_({
    expression: _resolvePattern4(obj.expression),
  });
}

export function listSplatPatternFrom(input: RuntimeNodeOf<ListSplatPattern>): ReturnType<typeof list_splat_pattern_>;
export function listSplatPatternFrom(child: ListSplatPatternFromInput): ReturnType<typeof list_splat_pattern_>;
export function listSplatPatternFrom(arg?: RuntimeNodeOf<ListSplatPattern> | ListSplatPatternFromInput) {
  if (isNodeData(arg) && arg.type === 'list_splat_pattern') {
    return list_splat_pattern_(arg.children?.[0] as Attribute | Identifier | Subscript);
  }
  return list_splat_pattern_(_resolvePattern3(arg) as Attribute | Identifier | Subscript);
}

export function matchStatementFrom(input: RuntimeNodeOf<MatchStatement> | MatchStatementFromInput) {
  if (isNodeData<'match_statement'>(input)) {
    return match_statement_({
      body: input.fields?.body,
      subject: input.fields?.subject,
    });
  }
  const obj = input as MatchStatementFromInput;
  return match_statement_({
    body: _resolveSuite(obj.body),
    subject: obj.subject !== undefined ? ((Array.isArray(obj.subject) ? obj.subject : [obj.subject])).map(_resolveComprehensionClauses3) : [],
  });
}

export function memberTypeFrom(input: RuntimeNodeOf<MemberType> | MemberTypeFromInput) {
  if (isNodeData<'member_type'>(input)) {
    return member_type_({
      baseType: input.fields?.base_type,
      identifier: input.fields?.identifier,
    });
  }
  const obj = input as MemberTypeFromInput;
  return member_type_({
    baseType: _rtec52c(obj.base_type),
    identifier: _resolveNamedExpressionLhs(obj.identifier),
  });
}

export function moduleFrom(input: RuntimeNodeOf<Module>): ReturnType<typeof module_>;
export function moduleFrom(...children: ModuleFromInput[]): ReturnType<typeof module_>;
export function moduleFrom(...args: (RuntimeNodeOf<Module> | ModuleFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'module') {
    return module_(...(args[0].children ?? []) as (CompoundStatement | SimpleStatement)[]);
  }
  return module_(...args.map(v => _resolveComprehensionClauses7(v)) as (CompoundStatement | SimpleStatement)[]);
}

export function namedExpressionFrom(input: RuntimeNodeOf<NamedExpression> | NamedExpressionFromInput) {
  if (isNodeData<'named_expression'>(input)) {
    return named_expression_({
      name: input.fields?.name,
      value: input.fields?.value,
    });
  }
  const obj = input as NamedExpressionFromInput;
  return named_expression_({
    name: _resolveNamedExpressionLhs2(obj.name),
    value: _resolveComprehensionClauses3(obj.value),
  });
}

export function nonlocalStatementFrom(input: RuntimeNodeOf<NonlocalStatement>): ReturnType<typeof nonlocal_statement_>;
export function nonlocalStatementFrom(...children: NonlocalStatementFromInput[]): ReturnType<typeof nonlocal_statement_>;
export function nonlocalStatementFrom(...args: (RuntimeNodeOf<NonlocalStatement> | NonlocalStatementFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'nonlocal_statement') {
    return nonlocal_statement_(...(args[0].children ?? []) as (Identifier)[]);
  }
  return nonlocal_statement_(...args.map(v => _resolveNamedExpressionLhs(v)) as (Identifier)[]);
}

export function notOperatorFrom(input: RuntimeNodeOf<NotOperator> | NotOperatorFromInput) {
  if (isNodeData<'not_operator'>(input)) {
    return not_operator_({
      argument: input.fields?.argument,
    });
  }
  const obj = input as NotOperatorFromInput;
  return not_operator_({
    argument: _resolveComprehensionClauses3(obj.argument),
  });
}

export function pairFrom(input: RuntimeNodeOf<Pair> | PairFromInput) {
  if (isNodeData<'pair'>(input)) {
    return pair_({
      key: input.fields?.key,
      value: input.fields?.value,
    });
  }
  const obj = input as PairFromInput;
  return pair_({
    key: _resolveComprehensionClauses3(obj.key),
    value: _resolveComprehensionClauses3(obj.value),
  });
}

export function parametersFrom(input: RuntimeNodeOf<Parameters>): ReturnType<typeof parameters_>;
export function parametersFrom(...children: ParametersFromInput[]): ReturnType<typeof parameters_>;
export function parametersFrom(...args: (RuntimeNodeOf<Parameters> | ParametersFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'parameters') {
    return parameters_(...(args[0].children ?? []) as (Parameter)[]);
  }
  return parameters_(...args.map(v => _resolveComprehensionClauses11(v)) as (Parameter)[]);
}

export function parenthesizedExpressionFrom(input: RuntimeNodeOf<ParenthesizedExpression>): ReturnType<typeof parenthesized_expression_>;
export function parenthesizedExpressionFrom(child: ParenthesizedExpressionFromInput): ReturnType<typeof parenthesized_expression_>;
export function parenthesizedExpressionFrom(arg?: RuntimeNodeOf<ParenthesizedExpression> | ParenthesizedExpressionFromInput) {
  if (isNodeData(arg) && arg.type === 'parenthesized_expression') {
    return parenthesized_expression_(arg.children?.[0] as Expression | ListSplat | ParenthesizedExpression | Yield);
  }
  return parenthesized_expression_(_resolveFExpression4(arg) as Expression | ListSplat | ParenthesizedExpression | Yield);
}

export function parenthesizedListSplatFrom(input: RuntimeNodeOf<ParenthesizedListSplat>): ReturnType<typeof parenthesized_list_splat_>;
export function parenthesizedListSplatFrom(child: ParenthesizedListSplatFromInput): ReturnType<typeof parenthesized_list_splat_>;
export function parenthesizedListSplatFrom(arg?: RuntimeNodeOf<ParenthesizedListSplat> | ParenthesizedListSplatFromInput) {
  if (isNodeData(arg) && arg.type === 'parenthesized_list_splat') {
    return parenthesized_list_splat_(arg.children?.[0] as ListSplat | ParenthesizedExpression);
  }
  return parenthesized_list_splat_(_resolvePrimaryExpression3(arg) as ListSplat | ParenthesizedExpression);
}

export function patternListFrom(input: RuntimeNodeOf<PatternList>): ReturnType<typeof pattern_list_>;
export function patternListFrom(...children: PatternListFromInput[]): ReturnType<typeof pattern_list_>;
export function patternListFrom(...args: (RuntimeNodeOf<PatternList> | PatternListFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'pattern_list') {
    return pattern_list_(...(args[0].children ?? []) as (Pattern)[]);
  }
  return pattern_list_(...args.map(v => _resolveComprehensionClauses12(v)) as (Pattern)[]);
}

export function printStatementFrom(input: RuntimeNodeOf<PrintStatement> | PrintStatementFromInput) {
  if (isNodeData<'print_statement'>(input)) {
    return print_statement_({
      ...Object.fromEntries(Object.entries(('fields' in input ? input.fields : undefined) ?? {}).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v])),
    } as PrintStatementConfig);
  }
  const mapped: Record<string, unknown> = Object.fromEntries(Object.entries(input).map(([k, v]) => [k.replace(/_([a-z])/g, (_, c: string) => c.toUpperCase()), v]));
  return print_statement_(mapped as PrintStatementConfig);
}

export function printStatementChevronFrom(input: RuntimeNodeOf<PrintStatementChevron> | PrintStatementChevronFromInput) {
  if (isNodeData<'print_statement'>(input)) {
    return print_statement__chevron_({
      argument: input.fields?.argument,
      chevron: input.fields?.chevron,
    } as PrintStatementChevronConfig);
  }
  const obj = input as PrintStatementChevronFromInput;
  return print_statement__chevron_({
    argument: obj.argument !== undefined ? ((Array.isArray(obj.argument) ? obj.argument : [obj.argument])).map(_resolveComprehensionClauses3) : undefined,
    chevron: _rnbxrnb(obj.chevron),
  } as PrintStatementChevronConfig);
}

export function printStatementV1From(input: RuntimeNodeOf<PrintStatementV1> | PrintStatementV1FromInput) {
  if (isNodeData<'print_statement'>(input)) {
    return print_statement__v1_({
      argument: input.fields?.argument,
    } as PrintStatementV1Config);
  }
  const obj = input as PrintStatementV1FromInput;
  return print_statement__v1_({
    argument: obj.argument !== undefined ? ((Array.isArray(obj.argument) ? obj.argument : [obj.argument])).map(_resolveComprehensionClauses3) : undefined,
  } as PrintStatementV1Config);
}

export function raiseStatementFrom(input: RuntimeNodeOf<RaiseStatement> | RaiseStatementFromInput) {
  if (isNodeData<'raise_statement'>(input)) {
    return raise_statement_({
      cause: input.fields?.cause,
      children: input.children?.[0],
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as RaiseStatementFromInput;
  return raise_statement_({
    cause: _resolveComprehensionClauses3(obj.cause),
    children: _resolveExpressions3(obj.children),
  });
}

export function relativeImportFrom(input: RuntimeNodeOf<RelativeImport> | RelativeImportFromInput) {
  if (isNodeData<'relative_import'>(input)) {
    return relative_import_({
      importPrefix: input.fields?.import_prefix,
      dottedName: input.fields?.dotted_name,
    });
  }
  const obj = input as RelativeImportFromInput;
  return relative_import_({
    importPrefix: _rtjvpca(obj.import_prefix),
    dottedName: _resolveSimplePattern2(obj.dotted_name),
  });
}

export function returnStatementFrom(input: RuntimeNodeOf<ReturnStatement>): ReturnType<typeof return_statement_>;
export function returnStatementFrom(child?: ReturnStatementFromInput): ReturnType<typeof return_statement_>;
export function returnStatementFrom(arg?: RuntimeNodeOf<ReturnStatement> | ReturnStatementFromInput) {
  if (isNodeData(arg) && arg.type === 'return_statement') {
    return return_statement_(arg.children?.[0] as Expression | ExpressionList);
  }
  return return_statement_(_resolveExpressions2(arg) as Expression | ExpressionList);
}

export function setFrom(input: RuntimeNodeOf<Set>): ReturnType<typeof set_>;
export function setFrom(...children: SetFromInput[]): ReturnType<typeof set_>;
export function setFrom(...args: (RuntimeNodeOf<Set> | SetFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'set') {
    return set_(...(args[0].children ?? []) as (Expression | ListSplat | ParenthesizedListSplat | Yield)[]);
  }
  return set_(...args.map(v => _rr83r9g(v)) as (Expression | ListSplat | ParenthesizedListSplat | Yield)[]);
}

export function setComprehensionFrom(input: RuntimeNodeOf<SetComprehension> | SetComprehensionFromInput) {
  if (isNodeData<'set_comprehension'>(input)) {
    return set_comprehension_({
      body: input.fields?.body,
      children: input.children[0]!,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as SetComprehensionFromInput;
  return set_comprehension_({
    body: _resolveComprehensionClauses3(obj.body),
    children: _ryqidua(obj.children),
  });
}

export function sliceFrom(input: RuntimeNodeOf<Slice> | SliceFromInput) {
  if (isNodeData<'slice'>(input)) {
    return slice_({
      start: input.fields?.start,
      stop: input.fields?.stop,
      step: input.fields?.step,
    });
  }
  const obj = input as SliceFromInput;
  return slice_({
    start: _resolveComprehensionClauses3(obj.start),
    stop: _resolveComprehensionClauses3(obj.stop),
    step: _resolveComprehensionClauses3(obj.step),
  });
}

export function splatPatternFrom(input: RuntimeNodeOf<SplatPattern> | SplatPatternFromInput) {
  if (isNodeData<'splat_pattern'>(input)) {
    return splat_pattern_({
      identifier: input.fields?.identifier,
    });
  }
  const obj = input as SplatPatternFromInput;
  return splat_pattern_({
    identifier: _resolveNamedExpressionLhs(obj.identifier),
  });
}

export function splatTypeFrom(input: RuntimeNodeOf<SplatType> | SplatTypeFromInput) {
  if (isNodeData<'splat_type'>(input)) {
    return splat_type_({
      identifier: input.fields?.identifier,
    });
  }
  const obj = input as SplatTypeFromInput;
  return splat_type_({
    identifier: _resolveNamedExpressionLhs(obj.identifier),
  });
}

export function stringFrom(input: RuntimeNodeOf<String> | StringFromInput) {
  if (isNodeData<'string'>(input)) {
    return string_({
      stringStart: input.fields?.string_start,
      content: input.fields?.content,
      stringEnd: input.fields?.string_end,
    });
  }
  const obj = input as StringFromInput;
  return string_({
    stringStart: _r11rdi4m(obj.string_start),
    content: obj.content !== undefined ? ((Array.isArray(obj.content) ? obj.content : [obj.content])).map(_r14l28kn) : undefined,
    stringEnd: _r8ytgv(obj.string_end),
  });
}

export function stringContentFrom(input: RuntimeNodeOf<StringContent>): ReturnType<typeof string_content_>;
export function stringContentFrom(...children: StringContentFromInput[]): ReturnType<typeof string_content_>;
export function stringContentFrom(...args: (RuntimeNodeOf<StringContent> | StringContentFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'string_content') {
    return string_content_(...(args[0].children ?? []) as (EscapeInterpolation | EscapeSequence)[]);
  }
  return string_content_(...args.map(v => _r1qu323z(v)) as (EscapeInterpolation | EscapeSequence)[]);
}

export function subscriptFrom(input: RuntimeNodeOf<Subscript> | SubscriptFromInput) {
  if (isNodeData<'subscript'>(input)) {
    return subscript_({
      subscript: input.fields?.subscript,
      value: input.fields?.value,
    });
  }
  const obj = input as SubscriptFromInput;
  return subscript_({
    subscript: obj.subscript !== undefined ? ((Array.isArray(obj.subscript) ? obj.subscript : [obj.subscript])).map(_r1apoc2a) : [],
    value: _resolveComprehensionClauses4(obj.value),
  });
}

export function tryStatementFrom(input: RuntimeNodeOf<TryStatement> | TryStatementFromInput) {
  if (isNodeData<'try_statement'>(input)) {
    return try_statement_({
      body: input.fields?.body,
      exceptClauses: input.fields?.except_clauses,
      elseClause: input.fields?.else_clause,
      finallyClause: input.fields?.finally_clause,
    });
  }
  const obj = input as TryStatementFromInput;
  return try_statement_({
    body: _resolveSuite2(obj.body),
    exceptClauses: obj.except_clauses !== undefined ? ((Array.isArray(obj.except_clauses) ? obj.except_clauses : [obj.except_clauses])).map(_r16jeonb) : undefined,
    elseClause: _r1totjtz(obj.else_clause),
    finallyClause: _rqhziy5(obj.finally_clause),
  });
}

export function tupleFrom(input: RuntimeNodeOf<Tuple>): ReturnType<typeof tuple_>;
export function tupleFrom(...children: TupleFromInput[]): ReturnType<typeof tuple_>;
export function tupleFrom(...args: (RuntimeNodeOf<Tuple> | TupleFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'tuple') {
    return tuple_(...(args[0].children ?? []) as (Expression | ListSplat | ParenthesizedListSplat | Yield)[]);
  }
  return tuple_(...args.map(v => _rr83r9g(v)) as (Expression | ListSplat | ParenthesizedListSplat | Yield)[]);
}

export function tuplePatternFrom(input: RuntimeNodeOf<TuplePattern>): ReturnType<typeof tuple_pattern_>;
export function tuplePatternFrom(...children: TuplePatternFromInput[]): ReturnType<typeof tuple_pattern_>;
export function tuplePatternFrom(...args: (RuntimeNodeOf<TuplePattern> | TuplePatternFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'tuple_pattern') {
    return tuple_pattern_(...(args[0].children ?? []) as (CasePattern | Pattern)[]);
  }
  return tuple_pattern_(...args.map(v => _rflltaz2(v)) as (CasePattern | Pattern)[]);
}

export function type_From(input: RuntimeNodeOf<Type>): ReturnType<typeof type_>;
export function type_From(child: TypeFromInput): ReturnType<typeof type_>;
export function type_From(arg?: RuntimeNodeOf<Type> | TypeFromInput) {
  if (isNodeData(arg) && arg.type === 'type') {
    return type_(arg.children?.[0] as ConstrainedType | Expression | GenericType | MemberType | SplatType | UnionType);
  }
  return type_(_r1f2r8fn(arg) as ConstrainedType | Expression | GenericType | MemberType | SplatType | UnionType);
}

export function typeAliasStatementFrom(input: RuntimeNodeOf<TypeAliasStatement> | TypeAliasStatementFromInput) {
  if (isNodeData<'type_alias_statement'>(input)) {
    return type_alias_statement_({
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as TypeAliasStatementFromInput;
  return type_alias_statement_({
    left: _rtec52c(obj.left),
    right: _rtec52c(obj.right),
  });
}

export function typeParameterFrom(input: RuntimeNodeOf<TypeParameter>): ReturnType<typeof type_parameter_>;
export function typeParameterFrom(...children: TypeParameterFromInput[]): ReturnType<typeof type_parameter_>;
export function typeParameterFrom(...args: (RuntimeNodeOf<TypeParameter> | TypeParameterFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'type_parameter') {
    return type_parameter_(...(args[0].children ?? []) as (Type)[]);
  }
  return type_parameter_(...args.map(v => _rtec52c(v)) as (Type)[]);
}

export function typedDefaultParameterFrom(input: RuntimeNodeOf<TypedDefaultParameter> | TypedDefaultParameterFromInput) {
  if (isNodeData<'typed_default_parameter'>(input)) {
    return typed_default_parameter_({
      name: input.fields?.name,
      type: input.fields?.type,
      value: input.fields?.value,
    });
  }
  const obj = input as TypedDefaultParameterFromInput;
  return typed_default_parameter_({
    name: _resolveNamedExpressionLhs(obj.name),
    type: _rtec52c(obj.type),
    value: _resolveComprehensionClauses3(obj.value),
  });
}

export function typedParameterFrom(input: RuntimeNodeOf<TypedParameter> | TypedParameterFromInput) {
  if (isNodeData<'typed_parameter'>(input)) {
    return typed_parameter_({
      type: input.fields?.type,
      children: input.children[0]!,
    });
  }
  const obj = (Array.isArray(input) ? { children: input } : input) as TypedParameterFromInput;
  return typed_parameter_({
    type: _rtec52c(obj.type),
    children: _resolveParameter2(obj.children),
  });
}

export function unaryOperatorFrom(input: RuntimeNodeOf<UnaryOperator> | UnaryOperatorFromInput) {
  if (isNodeData<'unary_operator'>(input)) {
    return unary_operator_({
      argument: input.fields?.argument,
      operator: input.fields?.operator,
    });
  }
  const obj = input as UnaryOperatorFromInput;
  return unary_operator_({
    argument: _resolveComprehensionClauses4(obj.argument),
    operator: _resolveComprehensionClauses13(obj.operator),
  });
}

export function unionPatternFrom(input: RuntimeNodeOf<UnionPattern>): ReturnType<typeof union_pattern_>;
export function unionPatternFrom(...children: UnionPatternFromInput[]): ReturnType<typeof union_pattern_>;
export function unionPatternFrom(...args: (RuntimeNodeOf<UnionPattern> | UnionPatternFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'union_pattern') {
    return union_pattern_(...(args[0].children ?? []) as (SimplePattern)[]);
  }
  return union_pattern_(...args.map(v => _resolveSimplePattern(v)) as (SimplePattern)[]);
}

export function unionTypeFrom(input: RuntimeNodeOf<UnionType> | UnionTypeFromInput) {
  if (isNodeData<'union_type'>(input)) {
    return union_type_({
      left: input.fields?.left,
      right: input.fields?.right,
    });
  }
  const obj = input as UnionTypeFromInput;
  return union_type_({
    left: _rtec52c(obj.left),
    right: _rtec52c(obj.right),
  });
}

export function whileStatementFrom(input: RuntimeNodeOf<WhileStatement> | WhileStatementFromInput) {
  if (isNodeData<'while_statement'>(input)) {
    return while_statement_({
      alternative: input.fields?.alternative,
      body: input.fields?.body,
      condition: input.fields?.condition,
    });
  }
  const obj = input as WhileStatementFromInput;
  return while_statement_({
    alternative: _r1totjtz(obj.alternative),
    body: _resolveSuite2(obj.body),
    condition: _resolveComprehensionClauses3(obj.condition),
  });
}

export function withClauseFrom(input: RuntimeNodeOf<WithClause>): ReturnType<typeof with_clause_>;
export function withClauseFrom(...children: WithClauseFromInput[]): ReturnType<typeof with_clause_>;
export function withClauseFrom(...args: (RuntimeNodeOf<WithClause> | WithClauseFromInput)[]) {
  if (args.length === 1 && isNodeData(args[0]) && args[0].type === 'with_clause') {
    return with_clause_(...(args[0].children ?? []) as (WithItem)[]);
  }
  return with_clause_(...args.map(v => _r7jahe4(v)) as (WithItem)[]);
}

export function withItemFrom(input: RuntimeNodeOf<WithItem> | WithItemFromInput) {
  if (isNodeData<'with_item'>(input)) {
    return with_item_({
      value: input.fields?.value,
    });
  }
  const obj = input as WithItemFromInput;
  return with_item_({
    value: _resolveComprehensionClauses3(obj.value),
  });
}

export function withStatementFrom(input: RuntimeNodeOf<WithStatement> | WithStatementFromInput) {
  if (isNodeData<'with_statement'>(input)) {
    return with_statement_({
      body: input.fields?.body,
      withClause: input.fields?.with_clause,
    });
  }
  const obj = input as WithStatementFromInput;
  return with_statement_({
    body: _resolveSuite2(obj.body),
    withClause: _rxymsmy(obj.with_clause),
  });
}

export function yield_From(input: RuntimeNodeOf<Yield>): ReturnType<typeof yield_>;
export function yield_From(child?: YieldFromInput): ReturnType<typeof yield_>;
export function yield_From(arg?: RuntimeNodeOf<Yield> | YieldFromInput) {
  if (isNodeData(arg) && arg.type === 'yield') {
    return yield_(arg.children?.[0] as Expression | ExpressionList | Expressions);
  }
  return yield_(_resolveExpressions3(arg) as Expression | ExpressionList | Expressions);
}

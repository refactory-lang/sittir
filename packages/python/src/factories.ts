// Auto-generated by @sittir/codegen — do not edit

import type * as T from './types.js';
import type { ByteRange, FluentNode, NonEmptyArray } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const _leafRe_typeConversion = /^(?:![a-z])/u;
const _leafRe_identifier = /^(?:[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const RESERVED_KEYWORDS: ReadonlySet<string> = new Set([
  'False',
  'None',
  'True',
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'case',
  'class',
  'continue',
  'def',
  'del',
  'elif',
  'else',
  'exec',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'lambda',
  'match',
  'nonlocal',
  'not',
  'or',
  'pass',
  'print',
  'raise',
  'return',
  'try',
  'while',
  'with',
]);
const _wordRe = /^(?:[_\p{XID_Start}][_\p{XID_Continue}]*)/u;

export function module(...children: T.Statement[]) {
  return {
    $type: 'module' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importStatement(config: T.ImportStatement.Config) {
  const fields = {
    import: config?.import,
  };
  const children = config?.children ?? [];
  return {
    $type: 'import_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    import(value?: "import") { return _fs(config, importStatement, 'import', value, fields.import); },
    getChild() { return children[0]; },
    setChild(child: T.ImportList) { return importStatement({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importPrefix(text: string) {
  if (text.length === 0) throw new Error(`import_prefix: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`import_prefix: text '${text}' is a reserved keyword`);
  return {
    $type: 'import_prefix' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ImportPrefixTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function relativeImport(config: T.RelativeImport.Config) {
  const fields = {
    import_prefix: config?.importPrefix,
    dotted_name: config?.dottedName,
  };
  return {
    $type: 'relative_import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    importPrefix(value?: T.ImportPrefix) { return _fs(config, relativeImport, 'importPrefix', value, fields.import_prefix); },
    dottedName(value?: T.DottedName | undefined) { return _fs(config, relativeImport, 'dottedName', value, fields.dotted_name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RelativeImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function futureImportStatement(config: T.FutureImportStatement.Config) {
  const fields = {
    from: config?.from,
  };
  const children = config?.children ?? [];
  return {
    $type: 'future_import_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    from(value?: "from") { return _fs(config, futureImportStatement, 'from', value, fields.from); },
    getChild() { return children[0]; },
    setChild(child: T.ImportList) { return futureImportStatement({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importFromStatement(config: T.ImportFromStatement.Config) {
  const fields = {
    module_name: config?.moduleName,
    wildcard_import: config?.wildcardImport,
  };
  return {
    $type: 'import_from_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    moduleName(value?: T.RelativeImport | T.DottedName) { return _fs(config, importFromStatement, 'moduleName', value, fields.module_name); },
    wildcardImport(value?: T.WildcardImport | T.ImportList) { return _fs(config, importFromStatement, 'wildcardImport', value, fields.wildcard_import); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function aliasedImport(config: T.AliasedImport.Config) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    $type: 'aliased_import' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.DottedName) { return _fs(config, aliasedImport, 'name', value, fields.name); },
    alias(value?: T.Identifier) { return _fs(config, aliasedImport, 'alias', value, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AliasedImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function printStatement(config: T.PrintStatement.Config) {
  const fields = {
    argument: config?.argument,
  };
  const children = config?.children ?? [];
  return {
    $type: 'print_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    argument(...values: T.Expression[]) { return _fsm(config, printStatement, 'argument', values, fields.argument); },
    getChild() { return children[0]; },
    setChild(child: T.Chevron) { return printStatement({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function chevron(config: T.Chevron.Config) {
  const fields = {
    expression: config?.expression,
  };
  return {
    $type: 'chevron' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, chevron, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ChevronTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertStatement(config: T.AssertStatement.Config) {
  const fields = {
    assert: config?.assert,
  };
  const children = config?.children ?? [];
  return {
    $type: 'assert_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    assert(value?: "assert") { return _fs(config, assertStatement, 'assert', value, fields.assert); },
    getChildren() { return children; },
    setChildren(...items: T.Expression[]) {
      _assertNonEmpty(items, 'assert_statement.children');
      return assertStatement({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssertStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child?: (T.Expression | T.Assignment | T.AugmentedAssignment | T.Yield)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'expression_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedExpression(config: T.NamedExpression.Config) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    $type: 'named_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.NamedExpressionLhs) { return _fs(config, namedExpression, 'name', value, fields.name); },
    value(value?: T.Expression) { return _fs(config, namedExpression, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NamedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(config: T.ReturnStatement.Config) {
  const fields = {
    return: config?.return,
  };
  const children = config?.children ?? [];
  return {
    $type: 'return_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    return(value?: "return") { return _fs(config, returnStatement, 'return', value, fields.return); },
    getChild() { return children[0]; },
    setChild(child: T.Expressions) { return returnStatement({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function deleteStatement(config: T.DeleteStatement.Config) {
  const fields = {
    del: config?.del,
  };
  const children = config?.children ?? [];
  return {
    $type: 'delete_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    del(value?: "del") { return _fs(config, deleteStatement, 'del', value, fields.del); },
    getChild() { return children[0]; },
    setChild(child: T.Expressions) { return deleteStatement({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DeleteStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function raiseStatement(config: T.RaiseStatement.Config) {
  const fields = {
    raise: config?.raise,
    cause: config?.cause,
  };
  const children = config?.children ?? [];
  return {
    $type: 'raise_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    raise(value?: "raise") { return _fs(config, raiseStatement, 'raise', value, fields.raise); },
    cause(value?: T.Expression | undefined) { return _fs(config, raiseStatement, 'cause', value, fields.cause); },
    getChild() { return children[0]; },
    setChild(child: T.Expressions) { return raiseStatement({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RaiseStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function passStatement() {
  return {
    $type: 'pass_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'pass' as const,
    render: () => 'pass' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'pass' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'pass' as const },
    replace: (t: T.PassStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' as const }; },
  };
}

export function breakStatement() {
  return {
    $type: 'break_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'break' as const,
    render: () => 'break' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'break' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'break' as const },
    replace: (t: T.BreakStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' as const }; },
  };
}

export function continueStatement() {
  return {
    $type: 'continue_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'continue' as const,
    render: () => 'continue' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'continue' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'continue' as const },
    replace: (t: T.ContinueStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' as const }; },
  };
}

export function ifStatement(config: T.IfStatement.Config) {
  const fields = {
    if: config?.if,
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    $type: 'if_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    if(value?: "if") { return _fs(config, ifStatement, 'if', value, fields.if); },
    condition(value?: T.Expression) { return _fs(config, ifStatement, 'condition', value, fields.condition); },
    consequence(value?: T.Suite) { return _fs(config, ifStatement, 'consequence', value, fields.consequence); },
    alternative(...values: (T.ElifClause | T.ElseClause)[]) { return _fsm(config, ifStatement, 'alternative', values, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elifClause(config: T.ElifClause.Config) {
  const fields = {
    elif: config?.elif,
    condition: config?.condition,
    consequence: config?.consequence,
  };
  return {
    $type: 'elif_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    elif(value?: "elif") { return _fs(config, elifClause, 'elif', value, fields.elif); },
    condition(value?: T.Expression) { return _fs(config, elifClause, 'condition', value, fields.condition); },
    consequence(value?: T.Suite) { return _fs(config, elifClause, 'consequence', value, fields.consequence); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ElifClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: T.ElseClause.Config) {
  const fields = {
    else: config?.else,
    body: config?.body,
  };
  return {
    $type: 'else_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    else(value?: "else") { return _fs(config, elseClause, 'else', value, fields.else); },
    body(value?: T.Suite) { return _fs(config, elseClause, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchStatement(config: T.MatchStatement.Config) {
  const fields = {
    match: config?.match,
    subject: config?.subject,
    body: config?.body,
  };
  return {
    $type: 'match_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    match(value?: "match") { return _fs(config, matchStatement, 'match', value, fields.match); },
    subject(...values: NonEmptyArray<T.Expression>) { return _fsm(config, matchStatement, 'subject', values, fields.subject); },
    body(value?: T.Block) { return _fs(config, matchStatement, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function caseClause(config: T.CaseClause.Config) {
  const fields = {
    case: config?.case,
    guard: config?.guard,
    consequence: config?.consequence,
  };
  const children = config?.children ?? [];
  return {
    $type: 'case_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    case(value?: "case") { return _fs(config, caseClause, 'case', value, fields.case); },
    guard(value?: T.IfClause | undefined) { return _fs(config, caseClause, 'guard', value, fields.guard); },
    consequence(value?: T.Suite) { return _fs(config, caseClause, 'consequence', value, fields.consequence); },
    getChildren() { return children; },
    setChildren(...items: T.CasePattern[]) {
      _assertNonEmpty(items, 'case_clause.children');
      return caseClause({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CaseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: T.ForStatement.Config) {
  const fields = {
    async: config?.async,
    left: config?.left,
    right: config?.right,
    body: config?.body,
    alternative: config?.alternative,
  };
  return {
    $type: 'for_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: T.KwAsync | undefined) { return _fs(config, forStatement, 'async', value, fields.async); },
    left(value?: T.LeftHandSide) { return _fs(config, forStatement, 'left', value, fields.left); },
    right(value?: T.Expressions) { return _fs(config, forStatement, 'right', value, fields.right); },
    body(value?: T.Suite) { return _fs(config, forStatement, 'body', value, fields.body); },
    alternative(value?: T.ElseClause | undefined) { return _fs(config, forStatement, 'alternative', value, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: T.WhileStatement.Config) {
  const fields = {
    while: config?.while,
    condition: config?.condition,
    body: config?.body,
    alternative: config?.alternative,
  };
  return {
    $type: 'while_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    while(value?: "while") { return _fs(config, whileStatement, 'while', value, fields.while); },
    condition(value?: T.Expression) { return _fs(config, whileStatement, 'condition', value, fields.condition); },
    body(value?: T.Suite) { return _fs(config, whileStatement, 'body', value, fields.body); },
    alternative(value?: T.ElseClause | undefined) { return _fs(config, whileStatement, 'alternative', value, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: T.TryStatement.Config) {
  const fields = {
    body: config?.body,
    except_clauses: config?.exceptClauses,
    else_clause: config?.elseClause,
    finally_clause: config?.finallyClause,
  };
  return {
    $type: 'try_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(value?: T.Suite) { return _fs(config, tryStatement, 'body', value, fields.body); },
    exceptClauses(...values: T.ExceptClause[]) { return _fsm(config, tryStatement, 'exceptClauses', values, fields.except_clauses); },
    elseClause(value?: T.ElseClause | undefined) { return _fs(config, tryStatement, 'elseClause', value, fields.else_clause); },
    finallyClause(value?: T.FinallyClause | undefined) { return _fs(config, tryStatement, 'finallyClause', value, fields.finally_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exceptClause(config: T.ExceptClause.Config) {
  const fields = {
    except: config?.except,
    value: config?.value,
    alias: config?.alias,
  };
  const children = config?.children ?? [];
  return {
    $type: 'except_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    except(value?: "except") { return _fs(config, exceptClause, 'except', value, fields.except); },
    value(value?: T.Expression | undefined) { return _fs(config, exceptClause, 'value', value, fields.value); },
    alias(value?: T.Expression | undefined) { return _fs(config, exceptClause, 'alias', value, fields.alias); },
    getChild() { return children[0]; },
    setChild(child: T.Suite) { return exceptClause({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExceptClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: T.FinallyClause.Config) {
  const fields = {
    block: config?.block,
  };
  return {
    $type: 'finally_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    block(value?: T.Suite) { return _fs(config, finallyClause, 'block', value, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: T.WithStatement.Config) {
  const fields = {
    async: config?.async,
    with_clause: config?.withClause,
    body: config?.body,
  };
  return {
    $type: 'with_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: T.KwAsync | undefined) { return _fs(config, withStatement, 'async', value, fields.async); },
    withClause(value?: T.WithClause) { return _fs(config, withStatement, 'withClause', value, fields.with_clause); },
    body(value?: T.Suite) { return _fs(config, withStatement, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withClause(...children: T.WithItem[]) {
  _assertNonEmpty(children, 'with_clause.children');
  return {
    $type: 'with_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withItem(config: T.WithItem.Config) {
  const fields = {
    value: config?.value,
  };
  return {
    $type: 'with_item' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    value(value?: T.Expression) { return _fs(config, withItem, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WithItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDefinition(config: T.FunctionDefinition.Config) {
  const fields = {
    async: config?.async,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    body: config?.body,
  };
  return {
    $type: 'function_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: T.KwAsync | undefined) { return _fs(config, functionDefinition, 'async', value, fields.async); },
    name(value?: T.Identifier) { return _fs(config, functionDefinition, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameter | undefined) { return _fs(config, functionDefinition, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.Parameters) { return _fs(config, functionDefinition, 'parameters', value, fields.parameters); },
    returnType(value?: T.Type | undefined) { return _fs(config, functionDefinition, 'returnType', value, fields.return_type); },
    body(value?: T.Suite) { return _fs(config, functionDefinition, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(child?: T._Parameters) {
  const children = child != null ? [child] : [];
  return {
    $type: 'parameters' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaParameters(child: T._Parameters) {
  const children = [child];
  return {
    $type: 'lambda_parameters' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LambdaParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplat(config: T.ListSplat.Config) {
  const fields = {
    expression: config?.expression,
  };
  return {
    $type: 'list_splat' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, listSplat, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplat(config: T.DictionarySplat.Config) {
  const fields = {
    expression: config?.expression,
  };
  return {
    $type: 'dictionary_splat' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, dictionarySplat, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictionarySplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function globalStatement(config: T.GlobalStatement.Config) {
  const fields = {
    global: config?.global,
  };
  const children = config?.children ?? [];
  return {
    $type: 'global_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    global(value?: "global") { return _fs(config, globalStatement, 'global', value, fields.global); },
    getChildren() { return children; },
    setChildren(...items: T.Identifier[]) {
      _assertNonEmpty(items, 'global_statement.children');
      return globalStatement({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GlobalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonlocalStatement(config: T.NonlocalStatement.Config) {
  const fields = {
    nonlocal: config?.nonlocal,
  };
  const children = config?.children ?? [];
  return {
    $type: 'nonlocal_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    nonlocal(value?: "nonlocal") { return _fs(config, nonlocalStatement, 'nonlocal', value, fields.nonlocal); },
    getChildren() { return children; },
    setChildren(...items: T.Identifier[]) {
      _assertNonEmpty(items, 'nonlocal_statement.children');
      return nonlocalStatement({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NonlocalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function execStatement(config: T.ExecStatement.Config) {
  const fields = {
    exec: config?.exec,
    code: config?.code,
  };
  const children = config?.children ?? [];
  return {
    $type: 'exec_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    exec(value?: "exec") { return _fs(config, execStatement, 'exec', value, fields.exec); },
    code(value?: T.String | T.Identifier) { return _fs(config, execStatement, 'code', value, fields.code); },
    getChildren() { return children; },
    setChildren(...items: T.Expression[]) { return execStatement({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExecStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasStatement(config: T.TypeAliasStatement.Config) {
  const fields = {
    type: config?.type,
    left: config?.left,
    right: config?.right,
  };
  return {
    $type: 'type_alias_statement' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: "type") { return _fs(config, typeAliasStatement, 'type', value, fields.type); },
    left(value?: T.Type) { return _fs(config, typeAliasStatement, 'left', value, fields.left); },
    right(value?: T.Type) { return _fs(config, typeAliasStatement, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeAliasStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDefinition(config: T.ClassDefinition.Config) {
  const fields = {
    class: config?.class,
    name: config?.name,
    type_parameters: config?.typeParameters,
    superclasses: config?.superclasses,
    body: config?.body,
  };
  return {
    $type: 'class_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    class(value?: "class") { return _fs(config, classDefinition, 'class', value, fields.class); },
    name(value?: T.Identifier) { return _fs(config, classDefinition, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameter | undefined) { return _fs(config, classDefinition, 'typeParameters', value, fields.type_parameters); },
    superclasses(value?: T.ArgumentList | undefined) { return _fs(config, classDefinition, 'superclasses', value, fields.superclasses); },
    body(value?: T.Suite) { return _fs(config, classDefinition, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(...children: T.Type[]) {
  _assertNonEmpty(children, 'type_parameter.children');
  return {
    $type: 'type_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedListSplat(child?: (T.ParenthesizedExpression | T.ListSplat)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'parenthesized_list_splat' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function argumentList(...children: (T.Expression | T.ListSplat | T.DictionarySplat | T.ParenthesizedExpression | T.KeywordArgument)[]) {
  return {
    $type: 'argument_list' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArgumentListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratedDefinition(config: T.DecoratedDefinition.Config) {
  const fields = {
    definition: config?.definition,
  };
  const children = config?.children ?? [];
  return {
    $type: 'decorated_definition' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    definition(value?: T.ClassDefinition | T.FunctionDefinition) { return _fs(config, decoratedDefinition, 'definition', value, fields.definition); },
    getChildren() { return children; },
    setChildren(...items: T.Decorator[]) {
      _assertNonEmpty(items, 'decorated_definition.children');
      return decoratedDefinition({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratedDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(config: T.Decorator.Config) {
  const fields = {
    expression: config?.expression,
    newline: config?.newline,
  };
  return {
    $type: 'decorator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, decorator, 'expression', value, fields.expression); },
    newline(value?: string) { return _fs(config, decorator, 'newline', value, fields.newline); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(...children: T.Statement[]) {
  return {
    $type: 'block' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionList(config: T.ExpressionList.Config) {
  const fields = {
    expression: config?.expression,
  };
  const children = config?.children ?? [];
  return {
    $type: 'expression_list' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    expression(value?: T.Expression) { return _fs(config, expressionList, 'expression', value, fields.expression); },
    getChildren() { return children; },
    setChildren(...items: T.Expression[]) { return expressionList({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dottedName(...children: T.Identifier[]) {
  _assertNonEmpty(children, 'dotted_name.children');
  return {
    $type: 'dotted_name' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DottedNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function casePattern(child?: (T.AsPattern | T.KeywordPattern | T.SimplePattern)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'case_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionPattern(...children: T.SimplePattern[]) {
  return {
    $type: 'union_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictPattern(...children: (T.KeyValuePattern | T.SplatPattern)[]) {
  return {
    $type: 'dict_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordPattern(config: T.KeywordPattern.Config) {
  const fields = {
    identifier: config?.identifier,
    simple_pattern: config?.simplePattern,
  };
  return {
    $type: 'keyword_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    identifier(value?: T.Identifier) { return _fs(config, keywordPattern, 'identifier', value, fields.identifier); },
    simplePattern(value?: T.SimplePattern) { return _fs(config, keywordPattern, 'simplePattern', value, fields.simple_pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.KeywordPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatPattern(config: T.SplatPattern.Config) {
  const fields = {
    identifier: config?.identifier,
  };
  const children = config?.children ?? [];
  return {
    $type: 'splat_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    identifier(value?: "*" | "**") { return _fs(config, splatPattern, 'identifier', value, fields.identifier); },
    getChild() { return children[0]; },
    setChild(child: T.Identifier) { return splatPattern({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classPattern(config: T.ClassPattern.Config) {
  const fields = {
    dotted_name: config?.dottedName,
    arguments: config?.arguments,
  };
  return {
    $type: 'class_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    dottedName(value?: T.DottedName) { return _fs(config, classPattern, 'dottedName', value, fields.dotted_name); },
    arguments(...values: string[]) { return _fsm(config, classPattern, 'arguments', values, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function complexPattern(config: T.ComplexPattern.Config) {
  const fields = {
    real: config?.real,
    imaginary: config?.imaginary,
  };
  const children = config?.children ?? [];
  return {
    $type: 'complex_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    real(value?: string) { return _fs(config, complexPattern, 'real', value, fields.real); },
    imaginary(value?: T.Integer | T.Float) { return _fs(config, complexPattern, 'imaginary', value, fields.imaginary); },
    getChild() { return children[0]; },
    setChild(child: (T.Integer | T.Float)) { return complexPattern({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(child?: T.Patterns) {
  const children = child != null ? [child] : [];
  return {
    $type: 'tuple_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listPattern(child?: T.Patterns) {
  const children = child != null ? [child] : [];
  return {
    $type: 'list_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultParameter(config: T.DefaultParameter.Config) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    $type: 'default_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier | T.TuplePattern) { return _fs(config, defaultParameter, 'name', value, fields.name); },
    value(value?: T.Expression) { return _fs(config, defaultParameter, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedDefaultParameter(config: T.TypedDefaultParameter.Config) {
  const fields = {
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    $type: 'typed_default_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier) { return _fs(config, typedDefaultParameter, 'name', value, fields.name); },
    typeField(value?: T.Type) { return _fs(config, typedDefaultParameter, 'type', value, fields.type); },
    value(value?: T.Expression) { return _fs(config, typedDefaultParameter, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypedDefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplatPattern(child?: (T.Identifier | T.KeywordIdentifier | T.Subscript | T.Attribute)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'list_splat_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListSplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplatPattern(child?: (T.Identifier | T.KeywordIdentifier | T.Subscript | T.Attribute)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'dictionary_splat_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictionarySplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asPattern(config: T.AsPattern.Config) {
  const fields = {
    expression: config?.expression,
    alias: config?.alias,
  };
  return {
    $type: 'as_pattern' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, asPattern, 'expression', value, fields.expression); },
    alias(value?: T.AsPatternTarget) { return _fs(config, asPattern, 'alias', value, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function notOperator(config: T.NotOperator.Config) {
  const fields = {
    not: config?.not,
    argument: config?.argument,
  };
  return {
    $type: 'not_operator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    not(value?: T.KwNot) { return _fs(config, notOperator, 'not', value, fields.not); },
    argument(value?: T.Expression) { return _fs(config, notOperator, 'argument', value, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NotOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function booleanOperator(config: T.BooleanOperator.Config) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    $type: 'boolean_operator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Expression) { return _fs(config, booleanOperator, 'left', value, fields.left); },
    operator(value?: "and") { return _fs(config, booleanOperator, 'operator', value, fields.operator); },
    right(value?: T.Expression) { return _fs(config, booleanOperator, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryOperator(config: T.BinaryOperator.Config) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    $type: 'binary_operator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.PrimaryExpression) { return _fs(config, binaryOperator, 'left', value, fields.left); },
    operator(value?: "+") { return _fs(config, binaryOperator, 'operator', value, fields.operator); },
    right(value?: T.PrimaryExpression) { return _fs(config, binaryOperator, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryOperator(config: T.UnaryOperator.Config) {
  const fields = {
    operator: config?.operator,
    argument: config?.argument,
  };
  return {
    $type: 'unary_operator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    operator(value?: "+" | "-" | "~") { return _fs(config, unaryOperator, 'operator', value, fields.operator); },
    argument(value?: T.PrimaryExpression) { return _fs(config, unaryOperator, 'argument', value, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comparisonOperator(config: T.ComparisonOperator.Config) {
  const fields = {
    left: config?.left,
    operators: config?.operators,
  };
  return {
    $type: 'comparison_operator' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.PrimaryExpression) { return _fs(config, comparisonOperator, 'left', value, fields.left); },
    operators(...values: NonEmptyArray<T.NotIn | T.IsNot>) { return _fsm(config, comparisonOperator, 'operators', values, fields.operators); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambda(config: T.Lambda.Config) {
  const fields = {
    lambda: config?.lambda,
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    $type: 'lambda' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    lambda(value?: T.KwLambda) { return _fs(config, lambda, 'lambda', value, fields.lambda); },
    parameters(value?: T.LambdaParameters | undefined) { return _fs(config, lambda, 'parameters', value, fields.parameters); },
    body(value?: T.Expression) { return _fs(config, lambda, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LambdaTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaWithinForInClause(config: T.LambdaWithinForInClause.Config) {
  const fields = {
    lambda: config?.lambda,
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    $type: 'lambda_within_for_in_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    lambda(value?: T.KwLambda) { return _fs(config, lambdaWithinForInClause, 'lambda', value, fields.lambda); },
    parameters(value?: T.LambdaParameters | undefined) { return _fs(config, lambdaWithinForInClause, 'parameters', value, fields.parameters); },
    body(value?: T.ExpressionWithinForInClause) { return _fs(config, lambdaWithinForInClause, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LambdaWithinForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignment(config: T.AssignmentUFormEqConfig | T.AssignmentUFormTypeConfig | T.AssignmentUFormTypedConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'assignment_eq') return assignmentUFormEq(config as T.AssignmentUFormEqConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'assignment_type') return assignmentUFormType(config as T.AssignmentUFormTypeConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'assignment_typed') return assignmentUFormTyped(config as T.AssignmentUFormTypedConfig);
  if (config && (config as any).variant === 'eq') return assignmentUFormEq(config as T.AssignmentUFormEqConfig);
  if (config && (config as any).variant === 'type') return assignmentUFormType(config as T.AssignmentUFormTypeConfig);
  if (config && (config as any).variant === 'typed') return assignmentUFormTyped(config as T.AssignmentUFormTypedConfig);
  return assignmentUFormEq(config as T.AssignmentUFormEqConfig);
}
export function assignmentUFormEq(config: T.AssignmentUFormEqConfig) {
  const fields = {
    left: config?.left,
  };
  const children = config?.children ?? [];
  return {
    $type: 'assignment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: '_form_eq' as const,
    $fields: fields,
    $children: children,
    left(value?: T.LeftHandSide) { return _fs(config, assignmentUFormEq, 'left', value, fields.left); },
    getChild() { return children[0]; },
    setChild(child: T.AssignmentEq) { return assignmentUFormEq({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentUFormEqTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function assignmentUFormType(config: T.AssignmentUFormTypeConfig) {
  const fields = {
    left: config?.left,
  };
  const children = config?.children ?? [];
  return {
    $type: 'assignment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: '_form_type' as const,
    $fields: fields,
    $children: children,
    left(value?: T.LeftHandSide) { return _fs(config, assignmentUFormType, 'left', value, fields.left); },
    getChild() { return children[0]; },
    setChild(child: T.AssignmentType) { return assignmentUFormType({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentUFormTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function assignmentUFormTyped(config: T.AssignmentUFormTypedConfig) {
  const fields = {
    left: config?.left,
  };
  const children = config?.children ?? [];
  return {
    $type: 'assignment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $variant: '_form_typed' as const,
    $fields: fields,
    $children: children,
    left(value?: T.LeftHandSide) { return _fs(config, assignmentUFormTyped, 'left', value, fields.left); },
    getChild() { return children[0]; },
    setChild(child: T.AssignmentTyped) { return assignmentUFormTyped({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentUFormTypedTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignment(config: T.AugmentedAssignment.Config) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    $type: 'augmented_assignment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.LeftHandSide) { return _fs(config, augmentedAssignment, 'left', value, fields.left); },
    operator(value?: "+=" | "-=" | "*=" | "/=" | "@=" | "//=" | "%=" | "**=" | ">>=" | "<<=" | "&=" | "^=" | "|=") { return _fs(config, augmentedAssignment, 'operator', value, fields.operator); },
    right(value?: T.RightHandSide) { return _fs(config, augmentedAssignment, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function patternList(config: T.PatternList.Config) {
  const fields = {
    pattern: config?.pattern,
  };
  const children = config?.children ?? [];
  return {
    $type: 'pattern_list' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    pattern(value?: T.Pattern) { return _fs(config, patternList, 'pattern', value, fields.pattern); },
    getChildren() { return children; },
    setChildren(...items: T.Pattern[]) { return patternList({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yield_(config: T.Yield.Config) {
  const fields = {
    yield: config?.yield,
  };
  const children = config?.children ?? [];
  return {
    $type: 'yield' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    yield(value?: "yield") { return _fs(config, yield_, 'yield', value, fields.yield); },
    getChild() { return children[0]; },
    setChild(child: (T.Expression | T.Expressions)) { return yield_({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: T.Attribute.Config) {
  const fields = {
    object: config?.object,
    attribute: config?.attribute,
  };
  return {
    $type: 'attribute' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    object(value?: T.PrimaryExpression) { return _fs(config, attribute, 'object', value, fields.object); },
    attribute(value?: T.Identifier) { return _fs(config, attribute, 'attribute', value, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscript(config: T.Subscript.Config) {
  const fields = {
    value: config?.value,
    subscript: config?.subscript,
  };
  return {
    $type: 'subscript' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    value(value?: T.PrimaryExpression) { return _fs(config, subscript, 'value', value, fields.value); },
    subscript(...values: NonEmptyArray<T.Expression | T.Slice>) { return _fsm(config, subscript, 'subscript', values, fields.subscript); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SubscriptTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slice(config?: T.Slice.Config) {
  const fields = {
    start: config?.start,
    stop: config?.stop,
    step: config?.step,
  };
  return {
    $type: 'slice' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    start(value?: T.Expression | undefined) { return _fs(config, slice, 'start', value, fields.start); },
    stop(value?: T.Expression | undefined) { return _fs(config, slice, 'stop', value, fields.stop); },
    step(value?: T.Expression | undefined) { return _fs(config, slice, 'step', value, fields.step); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SliceTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function call(config: T.Call.Config) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    $type: 'call' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    function(value?: T.PrimaryExpression) { return _fs(config, call, 'function', value, fields.function); },
    arguments(value?: T.GeneratorExpression | T.ArgumentList) { return _fs(config, call, 'arguments', value, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedParameter(config: T.TypedParameter.Config) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    $type: 'typed_parameter' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    typeField(value?: T.Type) { return _fs(config, typedParameter, 'type', value, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (T.Identifier | T.ListSplatPattern | T.DictionarySplatPattern)) { return typedParameter({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypedParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function type(child?: (T.Expression | T.SplatType | T.GenericType | T.UnionType | T.ConstrainedType | T.MemberType)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatType(config: T.SplatType.Config) {
  const fields = {
    identifier: config?.identifier,
  };
  const children = config?.children ?? [];
  return {
    $type: 'splat_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    identifier(value?: "*" | "**") { return _fs(config, splatType, 'identifier', value, fields.identifier); },
    getChild() { return children[0]; },
    setChild(child: T.Identifier) { return splatType({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: T.GenericType.Config) {
  const fields = {
    identifier: config?.identifier,
    type_parameter: config?.typeParameter,
  };
  return {
    $type: 'generic_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    identifier(value?: T.Identifier) { return _fs(config, genericType, 'identifier', value, fields.identifier); },
    typeParameter(value?: T.TypeParameter) { return _fs(config, genericType, 'typeParameter', value, fields.type_parameter); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: T.UnionType.Config) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    $type: 'union_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    left(value?: T.Type) { return _fs(config, unionType, 'left', value, fields.left); },
    right(value?: T.Type) { return _fs(config, unionType, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constrainedType(config: T.ConstrainedType.Config) {
  const fields = {
    base_type: config?.baseType,
    constraint: config?.constraint,
  };
  return {
    $type: 'constrained_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    baseType(value?: T.Type) { return _fs(config, constrainedType, 'baseType', value, fields.base_type); },
    constraint(value?: T.Type) { return _fs(config, constrainedType, 'constraint', value, fields.constraint); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstrainedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberType(config: T.MemberType.Config) {
  const fields = {
    base_type: config?.baseType,
    identifier: config?.identifier,
  };
  return {
    $type: 'member_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    baseType(value?: T.Type) { return _fs(config, memberType, 'baseType', value, fields.base_type); },
    identifier(value?: T.Identifier) { return _fs(config, memberType, 'identifier', value, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MemberTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordArgument(config: T.KeywordArgument.Config) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    $type: 'keyword_argument' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    name(value?: T.Identifier | T.KeywordIdentifier) { return _fs(config, keywordArgument, 'name', value, fields.name); },
    value(value?: T.Expression) { return _fs(config, keywordArgument, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.KeywordArgumentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function list(child?: T.CollectionElements) {
  const children = child != null ? [child] : [];
  return {
    $type: 'list' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function set(child: T.CollectionElements) {
  const children = [child];
  return {
    $type: 'set' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuple(child?: T.CollectionElements) {
  const children = child != null ? [child] : [];
  return {
    $type: 'tuple' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionary(...children: (T.Pair | T.DictionarySplat)[]) {
  return {
    $type: 'dictionary' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictionaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: T.Pair.Config) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    $type: 'pair' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    key(value?: T.Expression) { return _fs(config, pair, 'key', value, fields.key); },
    value(value?: T.Expression) { return _fs(config, pair, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listComprehension(config: T.ListComprehension.Config) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    $type: 'list_comprehension' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    body(value?: T.Expression) { return _fs(config, listComprehension, 'body', value, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ComprehensionClauses) { return listComprehension({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionaryComprehension(config: T.DictionaryComprehension.Config) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    $type: 'dictionary_comprehension' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    body(value?: T.Pair) { return _fs(config, dictionaryComprehension, 'body', value, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ComprehensionClauses) { return dictionaryComprehension({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictionaryComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function setComprehension(config: T.SetComprehension.Config) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    $type: 'set_comprehension' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    body(value?: T.Expression) { return _fs(config, setComprehension, 'body', value, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ComprehensionClauses) { return setComprehension({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SetComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorExpression(config: T.GeneratorExpression.Config) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    $type: 'generator_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    body(value?: T.Expression) { return _fs(config, generatorExpression, 'body', value, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ComprehensionClauses) { return generatorExpression({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GeneratorExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child?: (T.Expression | T.Yield)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'parenthesized_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInClause(config: T.ForInClause.Config) {
  const fields = {
    async: config?.async,
    left: config?.left,
    right: config?.right,
  };
  return {
    $type: 'for_in_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    async(value?: T.KwAsync | undefined) { return _fs(config, forInClause, 'async', value, fields.async); },
    left(value?: T.LeftHandSide) { return _fs(config, forInClause, 'left', value, fields.left); },
    right(...values: NonEmptyArray<string>) { return _fsm(config, forInClause, 'right', values, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifClause(config: T.IfClause.Config) {
  const fields = {
    expression: config?.expression,
  };
  return {
    $type: 'if_clause' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.Expression) { return _fs(config, ifClause, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IfClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalExpression(config: T.ConditionalExpression.Config) {
  const fields = {
    body: config?.body,
    condition: config?.condition,
    alternative: config?.alternative,
  };
  return {
    $type: 'conditional_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    body(value?: T.Expression) { return _fs(config, conditionalExpression, 'body', value, fields.body); },
    condition(value?: T.Expression) { return _fs(config, conditionalExpression, 'condition', value, fields.condition); },
    alternative(value?: T.Expression) { return _fs(config, conditionalExpression, 'alternative', value, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConditionalExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function concatenatedString(config: T.ConcatenatedString.Config) {
  const fields = {
    string: config?.string,
  };
  const children = config?.children ?? [];
  return {
    $type: 'concatenated_string' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    $children: children,
    string(value?: T.String) { return _fs(config, concatenatedString, 'string', value, fields.string); },
    getChildren() { return children; },
    setChildren(...items: T.String[]) {
      _assertNonEmpty(items, 'concatenated_string.children');
      return concatenatedString({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConcatenatedStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(config: T.String.Config) {
  const fields = {
    string_start: config?.stringStart,
    content: config?.content,
    string_end: config?.stringEnd,
  };
  return {
    $type: 'string' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    stringStart(value?: T.StringStart) { return _fs(config, string, 'stringStart', value, fields.string_start); },
    content(...values: (T.Interpolation | T.StringContent)[]) { return _fsm(config, string, 'content', values, fields.content); },
    stringEnd(value?: T.StringEnd) { return _fs(config, string, 'stringEnd', value, fields.string_end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringContent(...children: (T.EscapeInterpolation | T.EscapeSequence | T.NotEscapeSequence | T._StringContent)[]) {
  return {
    $type: 'string_content' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringContentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interpolation(config: T.Interpolation.Config) {
  const fields = {
    expression: config?.expression,
    type_conversion: config?.typeConversion,
    format_specifier: config?.formatSpecifier,
  };
  return {
    $type: 'interpolation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.FExpression) { return _fs(config, interpolation, 'expression', value, fields.expression); },
    typeConversion(value?: T.TypeConversion | undefined) { return _fs(config, interpolation, 'typeConversion', value, fields.type_conversion); },
    formatSpecifier(value?: T.FormatSpecifier | undefined) { return _fs(config, interpolation, 'formatSpecifier', value, fields.format_specifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InterpolationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_sequence: text '${text}' is a reserved keyword`);
  return {
    $type: 'escape_sequence' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function formatSpecifier(...children: T.FormatExpression[]) {
  return {
    $type: 'format_specifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FormatSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeConversion(text: string) {
  if (text.length === 0) throw new Error(`type_conversion: text must be non-empty`); if (!_leafRe_typeConversion.test(text)) throw new Error(`type_conversion: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`type_conversion: text '${text}' is a reserved keyword`);
  return {
    $type: 'type_conversion' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.TypeConversionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer(text: string) {
  if (text.length === 0) throw new Error(`integer: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`integer: text '${text}' is a reserved keyword`);
  return {
    $type: 'integer' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IntegerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function float(text: string) {
  if (text.length === 0) throw new Error(`float: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`float: text '${text}' is a reserved keyword`);
  return {
    $type: 'float' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FloatTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`); if (!_leafRe_identifier.test(text)) throw new Error(`identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`identifier: text '${text}' is a reserved keyword`);
  return {
    $type: 'identifier' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    $type: 'true' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'True' as const,
    render: () => 'True' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'True' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'True' as const },
    replace: (t: T.TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' as const }; },
  };
}

export function false_() {
  return {
    $type: 'false' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'False' as const,
    render: () => 'False' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'False' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'False' as const },
    replace: (t: T.FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' as const }; },
  };
}

export function none() {
  return {
    $type: 'none' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: 'None' as const,
    render: () => 'None' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'None' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'None' as const },
    replace: (t: T.NoneTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' as const }; },
  };
}

export function await_(config: T.Await.Config) {
  const fields = {
    primary_expression: config?.primaryExpression,
  };
  return {
    $type: 'await' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    primaryExpression(value?: T.PrimaryExpression) { return _fs(config, await_, 'primaryExpression', value, fields.primary_expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AwaitTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comment(text: string) {
  if (text.length === 0) throw new Error(`comment: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`comment: text '${text}' is a reserved keyword`);
  return {
    $type: 'comment' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function lineContinuation(text: string) {
  if (text.length === 0) throw new Error(`line_continuation: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`line_continuation: text '${text}' is a reserved keyword`);
  return {
    $type: 'line_continuation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.LineContinuationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringStart(text: string) {
  if (text.length === 0) throw new Error(`string_start: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_start: text '${text}' is a reserved keyword`);
  return {
    $type: 'string_start' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringStartTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeInterpolation(text: string) {
  if (text.length === 0) throw new Error(`escape_interpolation: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_interpolation: text '${text}' is a reserved keyword`);
  return {
    $type: 'escape_interpolation' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeInterpolationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringEnd(text: string) {
  if (text.length === 0) throw new Error(`string_end: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_end: text '${text}' is a reserved keyword`);
  return {
    $type: 'string_end' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringEndTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeBracket(text: string) {
  if (text.length === 0) throw new Error(`]: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`]: text '${text}' is a reserved keyword`);
  return {
    $type: ']' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CloseBracketTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeParen(text: string) {
  if (text.length === 0) throw new Error(`): text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`): text '${text}' is a reserved keyword`);
  return {
    $type: ')' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CloseParenTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeBrace(text: string) {
  if (text.length === 0) throw new Error(`}: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`}: text '${text}' is a reserved keyword`);
  return {
    $type: '}' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CloseBraceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function except(text: string) {
  if (text.length === 0) throw new Error(`except: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`except: text '${text}' is a reserved keyword`);
  return {
    $type: 'except' as const,
    $source: 'factory' as const,
    $named: true as const,
    $text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ExceptTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function asPatternTarget(child?: (T.ComparisonOperator | T.NotOperator | T.BooleanOperator | T.Lambda | T.PrimaryExpression | T.ConditionalExpression | T.NamedExpression | T.AsPattern)) {
  const children = child != null ? [child] : [];
  return {
    $type: 'as_pattern_target' as const,
    $source: 'factory' as const,
    $named: true as const,
    $children: children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentEq(config: T.AssignmentEq.Config) {
  const fields = {
    right: config?.right,
  };
  return {
    $type: 'assignment_eq' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    right(value?: T.RightHandSide) { return _fs(config, assignmentEq, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentEqTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentType(config: T.AssignmentType.Config) {
  const fields = {
    type: config?.type,
  };
  return {
    $type: 'assignment_type' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, assignmentType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentTyped(config: T.AssignmentTyped.Config) {
  const fields = {
    type: config?.type,
    right: config?.right,
  };
  return {
    $type: 'assignment_typed' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    typeField(value?: T.Type) { return _fs(config, assignmentTyped, 'type', value, fields.type); },
    right(value?: T.RightHandSide) { return _fs(config, assignmentTyped, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentTypedTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formatExpression(config: T.FormatExpression.Config) {
  const fields = {
    expression: config?.expression,
    type_conversion: config?.typeConversion,
    format_specifier: config?.formatSpecifier,
  };
  return {
    $type: 'format_expression' as const,
    $source: 'factory' as const,
    $named: true as const,
    $fields: fields,
    expression(value?: T.FExpression) { return _fs(config, formatExpression, 'expression', value, fields.expression); },
    typeConversion(value?: T.TypeConversion | undefined) { return _fs(config, formatExpression, 'typeConversion', value, fields.type_conversion); },
    formatSpecifier(value?: T.FormatSpecifier | undefined) { return _fs(config, formatExpression, 'formatSpecifier', value, fields.format_specifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FormatExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export type FluentKindMap = {
  "module": FluentNode<"module", T.Module.Config>;
  "import_statement": FluentNode<"import_statement", T.ImportStatement.Config>;
  "import_prefix": T.ImportPrefix;
  "relative_import": FluentNode<"relative_import", T.RelativeImport.Config>;
  "future_import_statement": FluentNode<"future_import_statement", T.FutureImportStatement.Config>;
  "import_from_statement": FluentNode<"import_from_statement", T.ImportFromStatement.Config>;
  "aliased_import": FluentNode<"aliased_import", T.AliasedImport.Config>;
  "print_statement": FluentNode<"print_statement", T.PrintStatement.Config>;
  "chevron": FluentNode<"chevron", T.Chevron.Config>;
  "assert_statement": FluentNode<"assert_statement", T.AssertStatement.Config>;
  "expression_statement": FluentNode<"expression_statement", T.ExpressionStatement.Config>;
  "named_expression": FluentNode<"named_expression", T.NamedExpression.Config>;
  "return_statement": FluentNode<"return_statement", T.ReturnStatement.Config>;
  "delete_statement": FluentNode<"delete_statement", T.DeleteStatement.Config>;
  "raise_statement": FluentNode<"raise_statement", T.RaiseStatement.Config>;
  "pass_statement": T.PassStatement;
  "break_statement": T.BreakStatement;
  "continue_statement": T.ContinueStatement;
  "if_statement": FluentNode<"if_statement", T.IfStatement.Config>;
  "elif_clause": FluentNode<"elif_clause", T.ElifClause.Config>;
  "else_clause": FluentNode<"else_clause", T.ElseClause.Config>;
  "match_statement": FluentNode<"match_statement", T.MatchStatement.Config>;
  "case_clause": FluentNode<"case_clause", T.CaseClause.Config>;
  "for_statement": FluentNode<"for_statement", T.ForStatement.Config>;
  "while_statement": FluentNode<"while_statement", T.WhileStatement.Config>;
  "try_statement": FluentNode<"try_statement", T.TryStatement.Config>;
  "except_clause": FluentNode<"except_clause", T.ExceptClause.Config>;
  "finally_clause": FluentNode<"finally_clause", T.FinallyClause.Config>;
  "with_statement": FluentNode<"with_statement", T.WithStatement.Config>;
  "with_clause": FluentNode<"with_clause", T.WithClause.Config>;
  "with_item": FluentNode<"with_item", T.WithItem.Config>;
  "function_definition": FluentNode<"function_definition", T.FunctionDefinition.Config>;
  "parameters": FluentNode<"parameters", T.Parameters.Config>;
  "lambda_parameters": FluentNode<"lambda_parameters", T.LambdaParameters.Config>;
  "list_splat": FluentNode<"list_splat", T.ListSplat.Config>;
  "dictionary_splat": FluentNode<"dictionary_splat", T.DictionarySplat.Config>;
  "global_statement": FluentNode<"global_statement", T.GlobalStatement.Config>;
  "nonlocal_statement": FluentNode<"nonlocal_statement", T.NonlocalStatement.Config>;
  "exec_statement": FluentNode<"exec_statement", T.ExecStatement.Config>;
  "type_alias_statement": FluentNode<"type_alias_statement", T.TypeAliasStatement.Config>;
  "class_definition": FluentNode<"class_definition", T.ClassDefinition.Config>;
  "type_parameter": FluentNode<"type_parameter", T.TypeParameter.Config>;
  "parenthesized_list_splat": FluentNode<"parenthesized_list_splat", T.ParenthesizedListSplat.Config>;
  "argument_list": FluentNode<"argument_list", T.ArgumentList.Config>;
  "decorated_definition": FluentNode<"decorated_definition", T.DecoratedDefinition.Config>;
  "decorator": FluentNode<"decorator", T.Decorator.Config>;
  "block": FluentNode<"block", T.Block.Config>;
  "expression_list": FluentNode<"expression_list", T.ExpressionList.Config>;
  "dotted_name": FluentNode<"dotted_name", T.DottedName.Config>;
  "case_pattern": FluentNode<"case_pattern", T.CasePattern.Config>;
  "union_pattern": FluentNode<"union_pattern", T.UnionPattern.Config>;
  "dict_pattern": FluentNode<"dict_pattern", T.DictPattern.Config>;
  "keyword_pattern": FluentNode<"keyword_pattern", T.KeywordPattern.Config>;
  "splat_pattern": FluentNode<"splat_pattern", T.SplatPattern.Config>;
  "class_pattern": FluentNode<"class_pattern", T.ClassPattern.Config>;
  "complex_pattern": FluentNode<"complex_pattern", T.ComplexPattern.Config>;
  "tuple_pattern": FluentNode<"tuple_pattern", T.TuplePattern.Config>;
  "list_pattern": FluentNode<"list_pattern", T.ListPattern.Config>;
  "default_parameter": FluentNode<"default_parameter", T.DefaultParameter.Config>;
  "typed_default_parameter": FluentNode<"typed_default_parameter", T.TypedDefaultParameter.Config>;
  "list_splat_pattern": FluentNode<"list_splat_pattern", T.ListSplatPattern.Config>;
  "dictionary_splat_pattern": FluentNode<"dictionary_splat_pattern", T.DictionarySplatPattern.Config>;
  "as_pattern": FluentNode<"as_pattern", T.AsPattern.Config>;
  "not_operator": FluentNode<"not_operator", T.NotOperator.Config>;
  "boolean_operator": FluentNode<"boolean_operator", T.BooleanOperator.Config>;
  "binary_operator": FluentNode<"binary_operator", T.BinaryOperator.Config>;
  "unary_operator": FluentNode<"unary_operator", T.UnaryOperator.Config>;
  "comparison_operator": FluentNode<"comparison_operator", T.ComparisonOperator.Config>;
  "lambda": FluentNode<"lambda", T.Lambda.Config>;
  "lambda_within_for_in_clause": FluentNode<"lambda_within_for_in_clause", T.LambdaWithinForInClause.Config>;
  "assignment": FluentNode<"assignment", T.Assignment.Config>;
  "augmented_assignment": FluentNode<"augmented_assignment", T.AugmentedAssignment.Config>;
  "pattern_list": FluentNode<"pattern_list", T.PatternList.Config>;
  "yield": FluentNode<"yield", T.Yield.Config>;
  "attribute": FluentNode<"attribute", T.Attribute.Config>;
  "subscript": FluentNode<"subscript", T.Subscript.Config>;
  "slice": FluentNode<"slice", T.Slice.Config>;
  "call": FluentNode<"call", T.Call.Config>;
  "typed_parameter": FluentNode<"typed_parameter", T.TypedParameter.Config>;
  "type": FluentNode<"type", T.Type.Config>;
  "splat_type": FluentNode<"splat_type", T.SplatType.Config>;
  "generic_type": FluentNode<"generic_type", T.GenericType.Config>;
  "union_type": FluentNode<"union_type", T.UnionType.Config>;
  "constrained_type": FluentNode<"constrained_type", T.ConstrainedType.Config>;
  "member_type": FluentNode<"member_type", T.MemberType.Config>;
  "keyword_argument": FluentNode<"keyword_argument", T.KeywordArgument.Config>;
  "list": FluentNode<"list", T.List.Config>;
  "set": FluentNode<"set", T.Set.Config>;
  "tuple": FluentNode<"tuple", T.Tuple.Config>;
  "dictionary": FluentNode<"dictionary", T.Dictionary.Config>;
  "pair": FluentNode<"pair", T.Pair.Config>;
  "list_comprehension": FluentNode<"list_comprehension", T.ListComprehension.Config>;
  "dictionary_comprehension": FluentNode<"dictionary_comprehension", T.DictionaryComprehension.Config>;
  "set_comprehension": FluentNode<"set_comprehension", T.SetComprehension.Config>;
  "generator_expression": FluentNode<"generator_expression", T.GeneratorExpression.Config>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", T.ParenthesizedExpression.Config>;
  "for_in_clause": FluentNode<"for_in_clause", T.ForInClause.Config>;
  "if_clause": FluentNode<"if_clause", T.IfClause.Config>;
  "conditional_expression": FluentNode<"conditional_expression", T.ConditionalExpression.Config>;
  "concatenated_string": FluentNode<"concatenated_string", T.ConcatenatedString.Config>;
  "string": FluentNode<"string", T.String.Config>;
  "string_content": FluentNode<"string_content", T.StringContent.Config>;
  "interpolation": FluentNode<"interpolation", T.Interpolation.Config>;
  "escape_sequence": T.EscapeSequence;
  "format_specifier": FluentNode<"format_specifier", T.FormatSpecifier.Config>;
  "type_conversion": T.TypeConversion;
  "integer": T.Integer;
  "float": T.Float;
  "identifier": T.Identifier;
  "true": T.True;
  "false": T.False;
  "none": T.None;
  "await": FluentNode<"await", T.Await.Config>;
  "comment": T.Comment;
  "line_continuation": T.LineContinuation;
  "string_start": T.StringStart;
  "escape_interpolation": T.EscapeInterpolation;
  "string_end": T.StringEnd;
  "]": T.CloseBracket;
  ")": T.CloseParen;
  "}": T.CloseBrace;
  "except": T.Except;
  "as_pattern_target": FluentNode<"as_pattern_target", T.AsPatternTarget.Config>;
  "assignment_eq": FluentNode<"assignment_eq", T.AssignmentEq.Config>;
  "assignment_type": FluentNode<"assignment_type", T.AssignmentType.Config>;
  "assignment_typed": FluentNode<"assignment_typed", T.AssignmentTyped.Config>;
  "format_expression": FluentNode<"format_expression", T.FormatExpression.Config>;
};

export const _factoryMap = {
  "module": module,
  "import_statement": importStatement,
  "import_prefix": importPrefix,
  "relative_import": relativeImport,
  "future_import_statement": futureImportStatement,
  "import_from_statement": importFromStatement,
  "aliased_import": aliasedImport,
  "print_statement": printStatement,
  "chevron": chevron,
  "assert_statement": assertStatement,
  "expression_statement": expressionStatement,
  "named_expression": namedExpression,
  "return_statement": returnStatement,
  "delete_statement": deleteStatement,
  "raise_statement": raiseStatement,
  "pass_statement": passStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "if_statement": ifStatement,
  "elif_clause": elifClause,
  "else_clause": elseClause,
  "match_statement": matchStatement,
  "case_clause": caseClause,
  "for_statement": forStatement,
  "while_statement": whileStatement,
  "try_statement": tryStatement,
  "except_clause": exceptClause,
  "finally_clause": finallyClause,
  "with_statement": withStatement,
  "with_clause": withClause,
  "with_item": withItem,
  "function_definition": functionDefinition,
  "parameters": parameters,
  "lambda_parameters": lambdaParameters,
  "list_splat": listSplat,
  "dictionary_splat": dictionarySplat,
  "global_statement": globalStatement,
  "nonlocal_statement": nonlocalStatement,
  "exec_statement": execStatement,
  "type_alias_statement": typeAliasStatement,
  "class_definition": classDefinition,
  "type_parameter": typeParameter,
  "parenthesized_list_splat": parenthesizedListSplat,
  "argument_list": argumentList,
  "decorated_definition": decoratedDefinition,
  "decorator": decorator,
  "block": block,
  "expression_list": expressionList,
  "dotted_name": dottedName,
  "case_pattern": casePattern,
  "union_pattern": unionPattern,
  "dict_pattern": dictPattern,
  "keyword_pattern": keywordPattern,
  "splat_pattern": splatPattern,
  "class_pattern": classPattern,
  "complex_pattern": complexPattern,
  "tuple_pattern": tuplePattern,
  "list_pattern": listPattern,
  "default_parameter": defaultParameter,
  "typed_default_parameter": typedDefaultParameter,
  "list_splat_pattern": listSplatPattern,
  "dictionary_splat_pattern": dictionarySplatPattern,
  "as_pattern": asPattern,
  "not_operator": notOperator,
  "boolean_operator": booleanOperator,
  "binary_operator": binaryOperator,
  "unary_operator": unaryOperator,
  "comparison_operator": comparisonOperator,
  "lambda": lambda,
  "lambda_within_for_in_clause": lambdaWithinForInClause,
  "assignment": assignment,
  "augmented_assignment": augmentedAssignment,
  "pattern_list": patternList,
  "yield": yield_,
  "attribute": attribute,
  "subscript": subscript,
  "slice": slice,
  "call": call,
  "typed_parameter": typedParameter,
  "type": type,
  "splat_type": splatType,
  "generic_type": genericType,
  "union_type": unionType,
  "constrained_type": constrainedType,
  "member_type": memberType,
  "keyword_argument": keywordArgument,
  "list": list,
  "set": set,
  "tuple": tuple,
  "dictionary": dictionary,
  "pair": pair,
  "list_comprehension": listComprehension,
  "dictionary_comprehension": dictionaryComprehension,
  "set_comprehension": setComprehension,
  "generator_expression": generatorExpression,
  "parenthesized_expression": parenthesizedExpression,
  "for_in_clause": forInClause,
  "if_clause": ifClause,
  "conditional_expression": conditionalExpression,
  "concatenated_string": concatenatedString,
  "string": string,
  "string_content": stringContent,
  "interpolation": interpolation,
  "escape_sequence": escapeSequence,
  "format_specifier": formatSpecifier,
  "type_conversion": typeConversion,
  "integer": integer,
  "float": float,
  "identifier": identifier,
  "true": true_,
  "false": false_,
  "none": none,
  "await": await_,
  "comment": comment,
  "line_continuation": lineContinuation,
  "string_start": stringStart,
  "escape_interpolation": escapeInterpolation,
  "string_end": stringEnd,
  "]": closeBracket,
  ")": closeParen,
  "}": closeBrace,
  "except": except,
  "as_pattern_target": asPatternTarget,
  "assignment_eq": assignmentEq,
  "assignment_type": assignmentType,
  "assignment_typed": assignmentTyped,
  "format_expression": formatExpression,
} as const;
export type _FactoryMap = typeof _factoryMap;

export const _factoryShapes = {
  "module": "children",
  "import_statement": "config",
  "import_prefix": "text",
  "relative_import": "config",
  "future_import_statement": "config",
  "import_from_statement": "config",
  "aliased_import": "config",
  "print_statement": "config",
  "chevron": "config",
  "assert_statement": "config",
  "expression_statement": "children",
  "named_expression": "config",
  "return_statement": "config",
  "delete_statement": "config",
  "raise_statement": "config",
  "pass_statement": "text",
  "break_statement": "text",
  "continue_statement": "text",
  "if_statement": "config",
  "elif_clause": "config",
  "else_clause": "config",
  "match_statement": "config",
  "case_clause": "config",
  "for_statement": "config",
  "while_statement": "config",
  "try_statement": "config",
  "except_clause": "config",
  "finally_clause": "config",
  "with_statement": "config",
  "with_clause": "children",
  "with_item": "config",
  "function_definition": "config",
  "parameters": "children",
  "lambda_parameters": "children",
  "list_splat": "config",
  "dictionary_splat": "config",
  "global_statement": "config",
  "nonlocal_statement": "config",
  "exec_statement": "config",
  "type_alias_statement": "config",
  "class_definition": "config",
  "type_parameter": "children",
  "parenthesized_list_splat": "children",
  "argument_list": "children",
  "decorated_definition": "config",
  "decorator": "config",
  "block": "children",
  "expression_list": "config",
  "dotted_name": "children",
  "case_pattern": "children",
  "union_pattern": "children",
  "dict_pattern": "children",
  "keyword_pattern": "config",
  "splat_pattern": "config",
  "class_pattern": "config",
  "complex_pattern": "config",
  "tuple_pattern": "children",
  "list_pattern": "children",
  "default_parameter": "config",
  "typed_default_parameter": "config",
  "list_splat_pattern": "children",
  "dictionary_splat_pattern": "children",
  "as_pattern": "config",
  "not_operator": "config",
  "boolean_operator": "config",
  "binary_operator": "config",
  "unary_operator": "config",
  "comparison_operator": "config",
  "lambda": "config",
  "lambda_within_for_in_clause": "config",
  "assignment": "config",
  "augmented_assignment": "config",
  "pattern_list": "config",
  "yield": "config",
  "attribute": "config",
  "subscript": "config",
  "slice": "config",
  "call": "config",
  "typed_parameter": "config",
  "type": "children",
  "splat_type": "config",
  "generic_type": "config",
  "union_type": "config",
  "constrained_type": "config",
  "member_type": "config",
  "keyword_argument": "config",
  "list": "children",
  "set": "children",
  "tuple": "children",
  "dictionary": "children",
  "pair": "config",
  "list_comprehension": "config",
  "dictionary_comprehension": "config",
  "set_comprehension": "config",
  "generator_expression": "config",
  "parenthesized_expression": "children",
  "for_in_clause": "config",
  "if_clause": "config",
  "conditional_expression": "config",
  "concatenated_string": "config",
  "string": "config",
  "string_content": "children",
  "interpolation": "config",
  "escape_sequence": "text",
  "format_specifier": "children",
  "type_conversion": "text",
  "integer": "text",
  "float": "text",
  "identifier": "text",
  "true": "text",
  "false": "text",
  "none": "text",
  "await": "config",
  "comment": "text",
  "line_continuation": "text",
  "string_start": "text",
  "escape_interpolation": "text",
  "string_end": "text",
  "]": "text",
  ")": "text",
  "}": "text",
  "except": "text",
  "as_pattern_target": "children",
  "assignment_eq": "config",
  "assignment_type": "config",
  "assignment_typed": "config",
  "format_expression": "config",
} as const satisfies Record<string, 'config' | 'children' | 'text'>;
export type _FactoryShapes = typeof _factoryShapes;

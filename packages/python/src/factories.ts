// Auto-generated by @sittir/codegen — do not edit

import type { NodeData, Edit } from '@sittir/core';
import { render, toEdit } from '@sittir/core';
import { rules } from './rules.js';
import { joinBy } from './joinby.js';

function isNodeData(v: any): v is NodeData {
  return v !== null && typeof v === 'object' && 'type' in v;
}

function resolveInput(v: any): NodeData {
  if (typeof v === 'string') return { type: 'string', fields: {}, text: v };
  if (typeof v === 'number') return { type: 'number', fields: {}, text: String(v) };
  return v;
}

const RESERVED_KEYWORDS = new Set([
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'case',
  'class',
  'continue',
  'def',
  'del',
  'elif',
  'else',
  'except',
  'exec',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'match',
  'nonlocal',
  'not',
  'or',
  'pass',
  'print',
  'raise',
  'return',
  'try',
  'while',
  'with',
]);

export interface AliasedImportConfig {
  name: NodeData;
  alias: NodeData | string;
}

export type AliasedImportNode = NodeData<'aliased_import'> & {
  alias(value: NodeData | string): AliasedImportNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'aliased_import'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function aliasedImport(
  nameOrConfig: NodeData | AliasedImportConfig,
  config?: Partial<AliasedImportConfig>,
): AliasedImportNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveInput(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'aliased_import', fields };
  node.alias = (v: any) => { fields['alias'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ArgumentListConfig {
  children?: NodeData[];
}

export type ArgumentListNode = NodeData<'argument_list'> & {
  children(...value: NodeData[]): ArgumentListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'argument_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function argumentList(
  config?: ArgumentListConfig,
): ArgumentListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'argument_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface AsPatternConfig {
  alias?: NodeData;
  children?: NodeData[];
}

export type AsPatternNode = NodeData<'as_pattern'> & {
  alias(value: NodeData): AsPatternNode;
  children(...value: NodeData[]): AsPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'as_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function asPattern(
  config?: AsPatternConfig,
): AsPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'as_pattern', fields };
  node.alias = (v: any) => { fields['alias'] = resolveInput(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface AssertStatementConfig {
  children?: NodeData[];
}

export type AssertStatementNode = NodeData<'assert_statement'> & {
  children(...value: NodeData[]): AssertStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assert_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function assertStatement(
  config?: AssertStatementConfig,
): AssertStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'assert_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface AssignmentConfig {
  left: NodeData;
  right?: NodeData;
  type?: NodeData;
}

export type AssignmentNode = NodeData<'assignment'> & {
  right(value: NodeData): AssignmentNode;
  typeField(value: NodeData): AssignmentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assignment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function assignment(
  leftOrConfig: NodeData | AssignmentConfig,
  config?: Partial<AssignmentConfig>,
): AssignmentNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveInput(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'assignment', fields };
  node.right = (v: any) => { fields['right'] = resolveInput(v); return node; };
  node.typeField = (v: any) => { fields['type'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface AttributeConfig {
  object: NodeData;
  attribute: NodeData | string;
}

export type AttributeNode = NodeData<'attribute'> & {
  attribute(value: NodeData | string): AttributeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'attribute'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function attribute(
  objectOrConfig: NodeData | AttributeConfig,
  config?: Partial<AttributeConfig>,
): AttributeNode {
  const fields: any = isNodeData(objectOrConfig) || typeof objectOrConfig === 'string'
    ? { 'object': resolveInput(objectOrConfig), ...config }
    : objectOrConfig;
  const node: any = { type: 'attribute', fields };
  node.attribute = (v: any) => { fields['attribute'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface AugmentedAssignmentConfig {
  left: NodeData;
  operator: NodeData;
  right: NodeData;
}

export type AugmentedAssignmentNode = NodeData<'augmented_assignment'> & {
  operator(value: NodeData): AugmentedAssignmentNode;
  right(value: NodeData): AugmentedAssignmentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'augmented_assignment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function augmentedAssignment(
  leftOrConfig: NodeData | AugmentedAssignmentConfig,
  config?: Partial<AugmentedAssignmentConfig>,
): AugmentedAssignmentNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveInput(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'augmented_assignment', fields };
  node.operator = (v: any) => { fields['operator'] = resolveInput(v); return node; };
  node.right = (v: any) => { fields['right'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface AwaitConfig {
  children?: NodeData[];
}

export type AwaitNode = NodeData<'await'> & {
  children(...value: NodeData[]): AwaitNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'await'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function await_(
  config?: AwaitConfig,
): AwaitNode {
  const fields: any = config ?? {};
  const node: any = { type: 'await', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface BinaryOperatorConfig {
  left: NodeData;
  operator: NodeData;
  right: NodeData;
}

export type BinaryOperatorNode = NodeData<'binary_operator'> & {
  operator(value: NodeData): BinaryOperatorNode;
  right(value: NodeData): BinaryOperatorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'binary_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function binaryOperator(
  leftOrConfig: NodeData | BinaryOperatorConfig,
  config?: Partial<BinaryOperatorConfig>,
): BinaryOperatorNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveInput(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'binary_operator', fields };
  node.operator = (v: any) => { fields['operator'] = resolveInput(v); return node; };
  node.right = (v: any) => { fields['right'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface BlockConfig {
  alternative?: NodeData[];
  children?: NodeData[];
}

export type BlockNode = NodeData<'block'> & {
  alternative(value: NodeData[]): BlockNode;
  children(...value: NodeData[]): BlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function block(
  config?: BlockConfig,
): BlockNode {
  const fields: any = config ?? {};
  const node: any = { type: 'block', fields };
  node.alternative = (...v: any[]) => { fields['alternative'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface BooleanOperatorConfig {
  left: NodeData;
  operator: NodeData;
  right: NodeData;
}

export type BooleanOperatorNode = NodeData<'boolean_operator'> & {
  operator(value: NodeData): BooleanOperatorNode;
  right(value: NodeData): BooleanOperatorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'boolean_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function booleanOperator(
  leftOrConfig: NodeData | BooleanOperatorConfig,
  config?: Partial<BooleanOperatorConfig>,
): BooleanOperatorNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveInput(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'boolean_operator', fields };
  node.operator = (v: any) => { fields['operator'] = resolveInput(v); return node; };
  node.right = (v: any) => { fields['right'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface CallConfig {
  function: NodeData;
  arguments: NodeData;
}

export type CallNode = NodeData<'call'> & {
  arguments(value: NodeData): CallNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'call'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function call(
  functionOrConfig: NodeData | CallConfig,
  config?: Partial<CallConfig>,
): CallNode {
  const fields: any = isNodeData(functionOrConfig) || typeof functionOrConfig === 'string'
    ? { 'function': resolveInput(functionOrConfig), ...config }
    : functionOrConfig;
  const node: any = { type: 'call', fields };
  node.arguments = (v: any) => { fields['arguments'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface CaseClauseConfig {
  guard?: NodeData;
  consequence: NodeData;
  children?: NodeData[];
}

export type CaseClauseNode = NodeData<'case_clause'> & {
  guard(value: NodeData): CaseClauseNode;
  children(...value: NodeData[]): CaseClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'case_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function caseClause(
  consequenceOrConfig: NodeData | CaseClauseConfig,
  config?: Partial<CaseClauseConfig>,
): CaseClauseNode {
  const fields: any = isNodeData(consequenceOrConfig) || typeof consequenceOrConfig === 'string'
    ? { 'consequence': resolveInput(consequenceOrConfig), ...config }
    : consequenceOrConfig;
  const node: any = { type: 'case_clause', fields };
  node.guard = (v: any) => { fields['guard'] = resolveInput(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface CasePatternConfig {
  children?: NodeData[];
}

export type CasePatternNode = NodeData<'case_pattern'> & {
  children(...value: NodeData[]): CasePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'case_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function casePattern(
  config?: CasePatternConfig,
): CasePatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'case_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ChevronConfig {
  children?: NodeData[];
}

export type ChevronNode = NodeData<'chevron'> & {
  children(...value: NodeData[]): ChevronNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'chevron'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function chevron(
  config?: ChevronConfig,
): ChevronNode {
  const fields: any = config ?? {};
  const node: any = { type: 'chevron', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ClassDefinitionConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  superclasses?: NodeData;
  body: NodeData;
}

export type ClassDefinitionNode = NodeData<'class_definition'> & {
  typeParameters(value: NodeData): ClassDefinitionNode;
  superclasses(value: NodeData): ClassDefinitionNode;
  body(value: NodeData): ClassDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function classDefinition(
  nameOrConfig: NodeData | string | ClassDefinitionConfig,
  config?: Partial<ClassDefinitionConfig>,
): ClassDefinitionNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveInput(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'class_definition', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveInput(v); return node; };
  node.superclasses = (v: any) => { fields['superclasses'] = resolveInput(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ClassPatternConfig {
  children?: NodeData[];
}

export type ClassPatternNode = NodeData<'class_pattern'> & {
  children(...value: NodeData[]): ClassPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function classPattern(
  config?: ClassPatternConfig,
): ClassPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'class_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ComparisonOperatorConfig {
  operators: NodeData[];
  children?: NodeData[];
}

export type ComparisonOperatorNode = NodeData<'comparison_operator'> & {
  operators(value: NodeData[]): ComparisonOperatorNode;
  children(...value: NodeData[]): ComparisonOperatorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'comparison_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function comparisonOperator(
  config?: ComparisonOperatorConfig,
): ComparisonOperatorNode {
  const fields: any = config ?? {};
  const node: any = { type: 'comparison_operator', fields };
  node.operators = (...v: any[]) => { fields['operators'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ComplexPatternConfig {
  children?: NodeData[];
}

export type ComplexPatternNode = NodeData<'complex_pattern'> & {
  children(...value: NodeData[]): ComplexPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'complex_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function complexPattern(
  childrenOrConfig?: string | NodeData | NodeData[] | ComplexPatternConfig,
): ComplexPatternNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'integer', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'complex_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ConcatenatedStringConfig {
  children?: NodeData[];
}

export type ConcatenatedStringNode = NodeData<'concatenated_string'> & {
  children(...value: NodeData[]): ConcatenatedStringNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'concatenated_string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function concatenatedString(
  config?: ConcatenatedStringConfig,
): ConcatenatedStringNode {
  const fields: any = config ?? {};
  const node: any = { type: 'concatenated_string', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ConditionalExpressionConfig {
  children?: NodeData[];
}

export type ConditionalExpressionNode = NodeData<'conditional_expression'> & {
  children(...value: NodeData[]): ConditionalExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'conditional_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function conditionalExpression(
  config?: ConditionalExpressionConfig,
): ConditionalExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'conditional_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ConstrainedTypeConfig {
  children?: NodeData[];
}

export type ConstrainedTypeNode = NodeData<'constrained_type'> & {
  children(...value: NodeData[]): ConstrainedTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'constrained_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function constrainedType(
  config?: ConstrainedTypeConfig,
): ConstrainedTypeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'constrained_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DecoratedDefinitionConfig {
  definition: NodeData;
  children?: NodeData[];
}

export type DecoratedDefinitionNode = NodeData<'decorated_definition'> & {
  children(...value: NodeData[]): DecoratedDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'decorated_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function decoratedDefinition(
  definitionOrConfig: NodeData | DecoratedDefinitionConfig,
  config?: Partial<DecoratedDefinitionConfig>,
): DecoratedDefinitionNode {
  const fields: any = isNodeData(definitionOrConfig) || typeof definitionOrConfig === 'string'
    ? { 'definition': resolveInput(definitionOrConfig), ...config }
    : definitionOrConfig;
  const node: any = { type: 'decorated_definition', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DecoratorConfig {
  children?: NodeData[];
}

export type DecoratorNode = NodeData<'decorator'> & {
  children(...value: NodeData[]): DecoratorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'decorator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function decorator(
  config?: DecoratorConfig,
): DecoratorNode {
  const fields: any = config ?? {};
  const node: any = { type: 'decorator', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DefaultParameterConfig {
  name: NodeData;
  value: NodeData;
}

export type DefaultParameterNode = NodeData<'default_parameter'> & {
  value(value: NodeData): DefaultParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'default_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function defaultParameter(
  nameOrConfig: NodeData | DefaultParameterConfig,
  config?: Partial<DefaultParameterConfig>,
): DefaultParameterNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveInput(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'default_parameter', fields };
  node.value = (v: any) => { fields['value'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DeleteStatementConfig {
  children?: NodeData[];
}

export type DeleteStatementNode = NodeData<'delete_statement'> & {
  children(...value: NodeData[]): DeleteStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'delete_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function deleteStatement(
  config?: DeleteStatementConfig,
): DeleteStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'delete_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DictPatternConfig {
  key?: NodeData[];
  value?: NodeData[];
  children?: NodeData[];
}

export type DictPatternNode = NodeData<'dict_pattern'> & {
  key(value: NodeData[]): DictPatternNode;
  value(value: NodeData[]): DictPatternNode;
  children(...value: NodeData[]): DictPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dict_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictPattern(
  config?: DictPatternConfig,
): DictPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'dict_pattern', fields };
  node.key = (...v: any[]) => { fields['key'] = v; return node; };
  node.value = (...v: any[]) => { fields['value'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DictionaryConfig {
  children?: NodeData[];
}

export type DictionaryNode = NodeData<'dictionary'> & {
  children(...value: NodeData[]): DictionaryNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictionary(
  config?: DictionaryConfig,
): DictionaryNode {
  const fields: any = config ?? {};
  const node: any = { type: 'dictionary', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DictionaryComprehensionConfig {
  body: NodeData;
  children?: NodeData[];
}

export type DictionaryComprehensionNode = NodeData<'dictionary_comprehension'> & {
  children(...value: NodeData[]): DictionaryComprehensionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictionaryComprehension(
  bodyOrConfig: NodeData | DictionaryComprehensionConfig,
  config?: Partial<DictionaryComprehensionConfig>,
): DictionaryComprehensionNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'dictionary_comprehension', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DictionarySplatConfig {
  children?: NodeData[];
}

export type DictionarySplatNode = NodeData<'dictionary_splat'> & {
  children(...value: NodeData[]): DictionarySplatNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictionarySplat(
  config?: DictionarySplatConfig,
): DictionarySplatNode {
  const fields: any = config ?? {};
  const node: any = { type: 'dictionary_splat', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DictionarySplatPatternConfig {
  children?: NodeData[];
}

export type DictionarySplatPatternNode = NodeData<'dictionary_splat_pattern'> & {
  children(...value: NodeData[]): DictionarySplatPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictionarySplatPattern(
  config?: DictionarySplatPatternConfig,
): DictionarySplatPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'dictionary_splat_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface DottedNameConfig {
  children?: NodeData[];
}

export type DottedNameNode = NodeData<'dotted_name'> & {
  children(...value: NodeData[]): DottedNameNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dotted_name'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dottedName(
  childrenOrConfig?: string | NodeData | NodeData[] | DottedNameConfig,
): DottedNameNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'identifier', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'dotted_name', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ElifClauseConfig {
  condition: NodeData;
  consequence: NodeData;
}

export type ElifClauseNode = NodeData<'elif_clause'> & {
  consequence(value: NodeData): ElifClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'elif_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function elifClause(
  conditionOrConfig: NodeData | ElifClauseConfig,
  config?: Partial<ElifClauseConfig>,
): ElifClauseNode {
  const fields: any = isNodeData(conditionOrConfig) || typeof conditionOrConfig === 'string'
    ? { 'condition': resolveInput(conditionOrConfig), ...config }
    : conditionOrConfig;
  const node: any = { type: 'elif_clause', fields };
  node.consequence = (v: any) => { fields['consequence'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ElseClauseConfig {
  body: NodeData;
}

export type ElseClauseNode = NodeData<'else_clause'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'else_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function elseClause(
  bodyOrConfig: NodeData | ElseClauseConfig,
  config?: Partial<ElseClauseConfig>,
): ElseClauseNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'else_clause', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ExceptClauseConfig {
  value?: NodeData[];
  alias?: NodeData;
  children?: NodeData[];
}

export type ExceptClauseNode = NodeData<'except_clause'> & {
  value(value: NodeData[]): ExceptClauseNode;
  alias(value: NodeData): ExceptClauseNode;
  children(...value: NodeData[]): ExceptClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'except_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function exceptClause(
  config?: ExceptClauseConfig,
): ExceptClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'except_clause', fields };
  node.value = (...v: any[]) => { fields['value'] = v; return node; };
  node.alias = (v: any) => { fields['alias'] = resolveInput(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ExecStatementConfig {
  code: NodeData;
  children?: NodeData[];
}

export type ExecStatementNode = NodeData<'exec_statement'> & {
  children(...value: NodeData[]): ExecStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'exec_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function execStatement(
  codeOrConfig: NodeData | ExecStatementConfig,
  config?: Partial<ExecStatementConfig>,
): ExecStatementNode {
  const fields: any = isNodeData(codeOrConfig) || typeof codeOrConfig === 'string'
    ? { 'code': resolveInput(codeOrConfig), ...config }
    : codeOrConfig;
  const node: any = { type: 'exec_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ExpressionListConfig {
  children?: NodeData[];
}

export type ExpressionListNode = NodeData<'expression_list'> & {
  children(...value: NodeData[]): ExpressionListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function expressionList(
  config?: ExpressionListConfig,
): ExpressionListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'expression_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ExpressionStatementConfig {
  children?: NodeData[];
}

export type ExpressionStatementNode = NodeData<'expression_statement'> & {
  children(...value: NodeData[]): ExpressionStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function expressionStatement(
  config?: ExpressionStatementConfig,
): ExpressionStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'expression_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface FinallyClauseConfig {
  children?: NodeData[];
}

export type FinallyClauseNode = NodeData<'finally_clause'> & {
  children(...value: NodeData[]): FinallyClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'finally_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function finallyClause(
  config?: FinallyClauseConfig,
): FinallyClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'finally_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ForInClauseConfig {
  left: NodeData;
  right: NodeData[];
}

export type ForInClauseNode = NodeData<'for_in_clause'> & {
  right(value: NodeData[]): ForInClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_in_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function forInClause(
  leftOrConfig: NodeData | ForInClauseConfig,
  config?: Partial<ForInClauseConfig>,
): ForInClauseNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveInput(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'for_in_clause', fields };
  node.right = (...v: any[]) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ForStatementConfig {
  left: NodeData;
  right: NodeData;
  body: NodeData;
  alternative?: NodeData;
}

export type ForStatementNode = NodeData<'for_statement'> & {
  right(value: NodeData): ForStatementNode;
  body(value: NodeData): ForStatementNode;
  alternative(value: NodeData): ForStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function forStatement(
  leftOrConfig: NodeData | ForStatementConfig,
  config?: Partial<ForStatementConfig>,
): ForStatementNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveInput(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'for_statement', fields };
  node.right = (v: any) => { fields['right'] = resolveInput(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveInput(v); return node; };
  node.alternative = (v: any) => { fields['alternative'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface FormatExpressionConfig {
  expression: NodeData;
  format_specifier?: NodeData;
  type_conversion?: NodeData | string;
}

export type FormatExpressionNode = NodeData<'format_expression'> & {
  formatSpecifier(value: NodeData): FormatExpressionNode;
  typeConversion(value: NodeData | string): FormatExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'format_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function formatExpression(
  expressionOrConfig: NodeData | FormatExpressionConfig,
  config?: Partial<FormatExpressionConfig>,
): FormatExpressionNode {
  const fields: any = isNodeData(expressionOrConfig) || typeof expressionOrConfig === 'string'
    ? { 'expression': resolveInput(expressionOrConfig), ...config }
    : expressionOrConfig;
  const node: any = { type: 'format_expression', fields };
  node.formatSpecifier = (v: any) => { fields['format_specifier'] = resolveInput(v); return node; };
  node.typeConversion = (v: any) => { fields['type_conversion'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface FormatSpecifierConfig {
  children?: NodeData[];
}

export type FormatSpecifierNode = NodeData<'format_specifier'> & {
  children(...value: NodeData[]): FormatSpecifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'format_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function formatSpecifier(
  config?: FormatSpecifierConfig,
): FormatSpecifierNode {
  const fields: any = config ?? {};
  const node: any = { type: 'format_specifier', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface FunctionDefinitionConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  parameters: NodeData;
  return_type?: NodeData;
  body: NodeData;
}

export type FunctionDefinitionNode = NodeData<'function_definition'> & {
  typeParameters(value: NodeData): FunctionDefinitionNode;
  parameters(value: NodeData): FunctionDefinitionNode;
  returnType(value: NodeData): FunctionDefinitionNode;
  body(value: NodeData): FunctionDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionDefinition(
  nameOrConfig: NodeData | string | FunctionDefinitionConfig,
  config?: Partial<FunctionDefinitionConfig>,
): FunctionDefinitionNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveInput(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'function_definition', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveInput(v); return node; };
  node.parameters = (v: any) => { fields['parameters'] = resolveInput(v); return node; };
  node.returnType = (v: any) => { fields['return_type'] = resolveInput(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface FutureImportStatementConfig {
  name: NodeData[];
}

export type FutureImportStatementNode = NodeData<'future_import_statement'> & {
  name(value: NodeData[]): FutureImportStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'future_import_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function futureImportStatement(
  config?: FutureImportStatementConfig,
): FutureImportStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'future_import_statement', fields };
  node.name = (...v: any[]) => { fields['name'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface GeneratorExpressionConfig {
  body: NodeData;
  children?: NodeData[];
}

export type GeneratorExpressionNode = NodeData<'generator_expression'> & {
  children(...value: NodeData[]): GeneratorExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generator_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function generatorExpression(
  bodyOrConfig: NodeData | GeneratorExpressionConfig,
  config?: Partial<GeneratorExpressionConfig>,
): GeneratorExpressionNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'generator_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface GenericTypeConfig {
  children?: NodeData[];
}

export type GenericTypeNode = NodeData<'generic_type'> & {
  children(...value: NodeData[]): GenericTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function genericType(
  config?: GenericTypeConfig,
): GenericTypeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'generic_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface GlobalStatementConfig {
  children?: NodeData[];
}

export type GlobalStatementNode = NodeData<'global_statement'> & {
  children(...value: NodeData[]): GlobalStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'global_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function globalStatement(
  childrenOrConfig?: string | NodeData | NodeData[] | GlobalStatementConfig,
): GlobalStatementNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'identifier', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'global_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface IfClauseConfig {
  children?: NodeData[];
}

export type IfClauseNode = NodeData<'if_clause'> & {
  children(...value: NodeData[]): IfClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function ifClause(
  config?: IfClauseConfig,
): IfClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'if_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface IfStatementConfig {
  condition: NodeData;
  consequence: NodeData;
  alternative?: NodeData[];
}

export type IfStatementNode = NodeData<'if_statement'> & {
  consequence(value: NodeData): IfStatementNode;
  alternative(value: NodeData[]): IfStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function ifStatement(
  conditionOrConfig: NodeData | IfStatementConfig,
  config?: Partial<IfStatementConfig>,
): IfStatementNode {
  const fields: any = isNodeData(conditionOrConfig) || typeof conditionOrConfig === 'string'
    ? { 'condition': resolveInput(conditionOrConfig), ...config }
    : conditionOrConfig;
  const node: any = { type: 'if_statement', fields };
  node.consequence = (v: any) => { fields['consequence'] = resolveInput(v); return node; };
  node.alternative = (...v: any[]) => { fields['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ImportFromStatementConfig {
  module_name: NodeData;
  name?: NodeData[];
  children?: NodeData[];
}

export type ImportFromStatementNode = NodeData<'import_from_statement'> & {
  name(value: NodeData[]): ImportFromStatementNode;
  children(...value: NodeData[]): ImportFromStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_from_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importFromStatement(
  moduleNameOrConfig: NodeData | ImportFromStatementConfig,
  config?: Partial<ImportFromStatementConfig>,
): ImportFromStatementNode {
  const fields: any = isNodeData(moduleNameOrConfig) || typeof moduleNameOrConfig === 'string'
    ? { 'module_name': resolveInput(moduleNameOrConfig), ...config }
    : moduleNameOrConfig;
  const node: any = { type: 'import_from_statement', fields };
  node.name = (...v: any[]) => { fields['name'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ImportStatementConfig {
  name: NodeData[];
}

export type ImportStatementNode = NodeData<'import_statement'> & {
  name(value: NodeData[]): ImportStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importStatement(
  config?: ImportStatementConfig,
): ImportStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'import_statement', fields };
  node.name = (...v: any[]) => { fields['name'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface InterpolationConfig {
  expression: NodeData;
  type_conversion?: NodeData | string;
  format_specifier?: NodeData;
}

export type InterpolationNode = NodeData<'interpolation'> & {
  typeConversion(value: NodeData | string): InterpolationNode;
  formatSpecifier(value: NodeData): InterpolationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'interpolation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function interpolation(
  expressionOrConfig: NodeData | InterpolationConfig,
  config?: Partial<InterpolationConfig>,
): InterpolationNode {
  const fields: any = isNodeData(expressionOrConfig) || typeof expressionOrConfig === 'string'
    ? { 'expression': resolveInput(expressionOrConfig), ...config }
    : expressionOrConfig;
  const node: any = { type: 'interpolation', fields };
  node.typeConversion = (v: any) => { fields['type_conversion'] = resolveInput(v); return node; };
  node.formatSpecifier = (v: any) => { fields['format_specifier'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface KeywordArgumentConfig {
  name: NodeData;
  value: NodeData;
}

export type KeywordArgumentNode = NodeData<'keyword_argument'> & {
  value(value: NodeData): KeywordArgumentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'keyword_argument'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function keywordArgument(
  nameOrConfig: NodeData | KeywordArgumentConfig,
  config?: Partial<KeywordArgumentConfig>,
): KeywordArgumentNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveInput(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'keyword_argument', fields };
  node.value = (v: any) => { fields['value'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface KeywordPatternConfig {
  children?: NodeData[];
}

export type KeywordPatternNode = NodeData<'keyword_pattern'> & {
  children(...value: NodeData[]): KeywordPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'keyword_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function keywordPattern(
  config?: KeywordPatternConfig,
): KeywordPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'keyword_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface LambdaConfig {
  parameters?: NodeData;
  body: NodeData;
}

export type LambdaNode = NodeData<'lambda'> & {
  parameters(value: NodeData): LambdaNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lambda'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lambda(
  bodyOrConfig: NodeData | LambdaConfig,
  config?: Partial<LambdaConfig>,
): LambdaNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'lambda', fields };
  node.parameters = (v: any) => { fields['parameters'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface LambdaParametersConfig {
  children?: NodeData[];
}

export type LambdaParametersNode = NodeData<'lambda_parameters'> & {
  children(...value: NodeData[]): LambdaParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lambda_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lambdaParameters(
  config?: LambdaParametersConfig,
): LambdaParametersNode {
  const fields: any = config ?? {};
  const node: any = { type: 'lambda_parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ListConfig {
  children?: NodeData[];
}

export type ListNode = NodeData<'list'> & {
  children(...value: NodeData[]): ListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function list(
  config?: ListConfig,
): ListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ListComprehensionConfig {
  body: NodeData;
  children?: NodeData[];
}

export type ListComprehensionNode = NodeData<'list_comprehension'> & {
  children(...value: NodeData[]): ListComprehensionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function listComprehension(
  bodyOrConfig: NodeData | ListComprehensionConfig,
  config?: Partial<ListComprehensionConfig>,
): ListComprehensionNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'list_comprehension', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ListPatternConfig {
  children?: NodeData[];
}

export type ListPatternNode = NodeData<'list_pattern'> & {
  children(...value: NodeData[]): ListPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function listPattern(
  config?: ListPatternConfig,
): ListPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'list_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ListSplatConfig {
  children?: NodeData[];
}

export type ListSplatNode = NodeData<'list_splat'> & {
  children(...value: NodeData[]): ListSplatNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function listSplat(
  config?: ListSplatConfig,
): ListSplatNode {
  const fields: any = config ?? {};
  const node: any = { type: 'list_splat', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ListSplatPatternConfig {
  children?: NodeData[];
}

export type ListSplatPatternNode = NodeData<'list_splat_pattern'> & {
  children(...value: NodeData[]): ListSplatPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function listSplatPattern(
  config?: ListSplatPatternConfig,
): ListSplatPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'list_splat_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface MatchStatementConfig {
  subject: NodeData[];
  body: NodeData;
}

export type MatchStatementNode = NodeData<'match_statement'> & {
  subject(value: NodeData[]): MatchStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'match_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function matchStatement(
  bodyOrConfig: NodeData | MatchStatementConfig,
  config?: Partial<MatchStatementConfig>,
): MatchStatementNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'match_statement', fields };
  node.subject = (...v: any[]) => { fields['subject'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface MemberTypeConfig {
  children?: NodeData[];
}

export type MemberTypeNode = NodeData<'member_type'> & {
  children(...value: NodeData[]): MemberTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'member_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function memberType(
  config?: MemberTypeConfig,
): MemberTypeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'member_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ModuleConfig {
  children?: NodeData[];
}

export type ModuleNode = NodeData<'module'> & {
  children(...value: NodeData[]): ModuleNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'module'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function module(
  config?: ModuleConfig,
): ModuleNode {
  const fields: any = config ?? {};
  const node: any = { type: 'module', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface NamedExpressionConfig {
  name: NodeData;
  value: NodeData;
}

export type NamedExpressionNode = NodeData<'named_expression'> & {
  value(value: NodeData): NamedExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'named_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function namedExpression(
  nameOrConfig: NodeData | NamedExpressionConfig,
  config?: Partial<NamedExpressionConfig>,
): NamedExpressionNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveInput(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'named_expression', fields };
  node.value = (v: any) => { fields['value'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface NonlocalStatementConfig {
  children?: NodeData[];
}

export type NonlocalStatementNode = NodeData<'nonlocal_statement'> & {
  children(...value: NodeData[]): NonlocalStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'nonlocal_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function nonlocalStatement(
  childrenOrConfig?: string | NodeData | NodeData[] | NonlocalStatementConfig,
): NonlocalStatementNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'identifier', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'nonlocal_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface NotOperatorConfig {
  argument: NodeData;
}

export type NotOperatorNode = NodeData<'not_operator'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'not_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function notOperator(
  argumentOrConfig: NodeData | NotOperatorConfig,
  config?: Partial<NotOperatorConfig>,
): NotOperatorNode {
  const fields: any = isNodeData(argumentOrConfig) || typeof argumentOrConfig === 'string'
    ? { 'argument': resolveInput(argumentOrConfig), ...config }
    : argumentOrConfig;
  const node: any = { type: 'not_operator', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface PairConfig {
  key: NodeData;
  value: NodeData;
}

export type PairNode = NodeData<'pair'> & {
  value(value: NodeData): PairNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pair'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function pair(
  keyOrConfig: NodeData | PairConfig,
  config?: Partial<PairConfig>,
): PairNode {
  const fields: any = isNodeData(keyOrConfig) || typeof keyOrConfig === 'string'
    ? { 'key': resolveInput(keyOrConfig), ...config }
    : keyOrConfig;
  const node: any = { type: 'pair', fields };
  node.value = (v: any) => { fields['value'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ParametersConfig {
  children?: NodeData[];
}

export type ParametersNode = NodeData<'parameters'> & {
  children(...value: NodeData[]): ParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parameters(
  config?: ParametersConfig,
): ParametersNode {
  const fields: any = config ?? {};
  const node: any = { type: 'parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ParenthesizedExpressionConfig {
  children?: NodeData[];
}

export type ParenthesizedExpressionNode = NodeData<'parenthesized_expression'> & {
  children(...value: NodeData[]): ParenthesizedExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parenthesizedExpression(
  config?: ParenthesizedExpressionConfig,
): ParenthesizedExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'parenthesized_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ParenthesizedListSplatConfig {
  children?: NodeData[];
}

export type ParenthesizedListSplatNode = NodeData<'parenthesized_list_splat'> & {
  children(...value: NodeData[]): ParenthesizedListSplatNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_list_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parenthesizedListSplat(
  config?: ParenthesizedListSplatConfig,
): ParenthesizedListSplatNode {
  const fields: any = config ?? {};
  const node: any = { type: 'parenthesized_list_splat', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface PatternListConfig {
  children?: NodeData[];
}

export type PatternListNode = NodeData<'pattern_list'> & {
  children(...value: NodeData[]): PatternListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pattern_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function patternList(
  config?: PatternListConfig,
): PatternListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'pattern_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface PrintStatementConfig {
  argument?: NodeData[];
  children?: NodeData[];
}

export type PrintStatementNode = NodeData<'print_statement'> & {
  argument(value: NodeData[]): PrintStatementNode;
  children(...value: NodeData[]): PrintStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'print_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function printStatement(
  config?: PrintStatementConfig,
): PrintStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'print_statement', fields };
  node.argument = (...v: any[]) => { fields['argument'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface RaiseStatementConfig {
  cause?: NodeData;
  children?: NodeData[];
}

export type RaiseStatementNode = NodeData<'raise_statement'> & {
  cause(value: NodeData): RaiseStatementNode;
  children(...value: NodeData[]): RaiseStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'raise_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function raiseStatement(
  config?: RaiseStatementConfig,
): RaiseStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'raise_statement', fields };
  node.cause = (v: any) => { fields['cause'] = resolveInput(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface RelativeImportConfig {
  children?: NodeData[];
}

export type RelativeImportNode = NodeData<'relative_import'> & {
  children(...value: NodeData[]): RelativeImportNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'relative_import'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function relativeImport(
  config?: RelativeImportConfig,
): RelativeImportNode {
  const fields: any = config ?? {};
  const node: any = { type: 'relative_import', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface ReturnStatementConfig {
  children?: NodeData[];
}

export type ReturnStatementNode = NodeData<'return_statement'> & {
  children(...value: NodeData[]): ReturnStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'return_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function returnStatement(
  config?: ReturnStatementConfig,
): ReturnStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'return_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface SetConfig {
  children?: NodeData[];
}

export type SetNode = NodeData<'set'> & {
  children(...value: NodeData[]): SetNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'set'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function set(
  config?: SetConfig,
): SetNode {
  const fields: any = config ?? {};
  const node: any = { type: 'set', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface SetComprehensionConfig {
  body: NodeData;
  children?: NodeData[];
}

export type SetComprehensionNode = NodeData<'set_comprehension'> & {
  children(...value: NodeData[]): SetComprehensionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'set_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function setComprehension(
  bodyOrConfig: NodeData | SetComprehensionConfig,
  config?: Partial<SetComprehensionConfig>,
): SetComprehensionNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'set_comprehension', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface SliceConfig {
  children?: NodeData[];
}

export type SliceNode = NodeData<'slice'> & {
  children(...value: NodeData[]): SliceNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'slice'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function slice(
  config?: SliceConfig,
): SliceNode {
  const fields: any = config ?? {};
  const node: any = { type: 'slice', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface SplatPatternConfig {
  children?: NodeData[];
}

export type SplatPatternNode = NodeData<'splat_pattern'> & {
  children(...value: NodeData[]): SplatPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function splatPattern(
  childrenOrConfig?: string | NodeData | NodeData[] | SplatPatternConfig,
): SplatPatternNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'identifier', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'splat_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface SplatTypeConfig {
  children?: NodeData[];
}

export type SplatTypeNode = NodeData<'splat_type'> & {
  children(...value: NodeData[]): SplatTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'splat_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function splatType(
  childrenOrConfig?: string | NodeData | NodeData[] | SplatTypeConfig,
): SplatTypeNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'identifier', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'splat_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface StringConfig {
  children?: NodeData[];
}

export type StringNode = NodeData<'string'> & {
  children(...value: NodeData[]): StringNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function string(
  config?: StringConfig,
): StringNode {
  const fields: any = config ?? {};
  const node: any = { type: 'string', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface StringContentConfig {
  children?: NodeData[];
}

export type StringContentNode = NodeData<'string_content'> & {
  children(...value: NodeData[]): StringContentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string_content'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function stringContent(
  childrenOrConfig?: string | NodeData | NodeData[] | StringContentConfig,
): StringContentNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'escape_interpolation', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'string_content', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface SubscriptConfig {
  value: NodeData;
  subscript: NodeData[];
}

export type SubscriptNode = NodeData<'subscript'> & {
  subscript(value: NodeData[]): SubscriptNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'subscript'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function subscript(
  valueOrConfig: NodeData | SubscriptConfig,
  config?: Partial<SubscriptConfig>,
): SubscriptNode {
  const fields: any = isNodeData(valueOrConfig) || typeof valueOrConfig === 'string'
    ? { 'value': resolveInput(valueOrConfig), ...config }
    : valueOrConfig;
  const node: any = { type: 'subscript', fields };
  node.subscript = (...v: any[]) => { fields['subscript'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface TryStatementConfig {
  body: NodeData;
  children?: NodeData[];
}

export type TryStatementNode = NodeData<'try_statement'> & {
  children(...value: NodeData[]): TryStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'try_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tryStatement(
  bodyOrConfig: NodeData | TryStatementConfig,
  config?: Partial<TryStatementConfig>,
): TryStatementNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'try_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface TupleConfig {
  children?: NodeData[];
}

export type TupleNode = NodeData<'tuple'> & {
  children(...value: NodeData[]): TupleNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tuple(
  config?: TupleConfig,
): TupleNode {
  const fields: any = config ?? {};
  const node: any = { type: 'tuple', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface TuplePatternConfig {
  children?: NodeData[];
}

export type TuplePatternNode = NodeData<'tuple_pattern'> & {
  children(...value: NodeData[]): TuplePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tuplePattern(
  config?: TuplePatternConfig,
): TuplePatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'tuple_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface TypeConfig {
  children?: NodeData[];
}

export type TypeNode = NodeData<'type'> & {
  children(...value: NodeData[]): TypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function type_(
  config?: TypeConfig,
): TypeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface TypeAliasStatementConfig {
  left: NodeData;
  right: NodeData;
}

export type TypeAliasStatementNode = NodeData<'type_alias_statement'> & {
  right(value: NodeData): TypeAliasStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_alias_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeAliasStatement(
  leftOrConfig: NodeData | TypeAliasStatementConfig,
  config?: Partial<TypeAliasStatementConfig>,
): TypeAliasStatementNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveInput(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'type_alias_statement', fields };
  node.right = (v: any) => { fields['right'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface TypeParameterConfig {
  children?: NodeData[];
}

export type TypeParameterNode = NodeData<'type_parameter'> & {
  children(...value: NodeData[]): TypeParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeParameter(
  config?: TypeParameterConfig,
): TypeParameterNode {
  const fields: any = config ?? {};
  const node: any = { type: 'type_parameter', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface TypedDefaultParameterConfig {
  name: NodeData | string;
  type: NodeData;
  value: NodeData;
}

export type TypedDefaultParameterNode = NodeData<'typed_default_parameter'> & {
  typeField(value: NodeData): TypedDefaultParameterNode;
  value(value: NodeData): TypedDefaultParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'typed_default_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typedDefaultParameter(
  nameOrConfig: NodeData | string | TypedDefaultParameterConfig,
  config?: Partial<TypedDefaultParameterConfig>,
): TypedDefaultParameterNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveInput(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'typed_default_parameter', fields };
  node.typeField = (v: any) => { fields['type'] = resolveInput(v); return node; };
  node.value = (v: any) => { fields['value'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface TypedParameterConfig {
  type: NodeData;
  children?: NodeData[];
}

export type TypedParameterNode = NodeData<'typed_parameter'> & {
  children(...value: NodeData[]): TypedParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'typed_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typedParameter(
  typeOrConfig: NodeData | TypedParameterConfig,
  config?: Partial<TypedParameterConfig>,
): TypedParameterNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveInput(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'typed_parameter', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface UnaryOperatorConfig {
  operator: NodeData;
  argument: NodeData;
}

export type UnaryOperatorNode = NodeData<'unary_operator'> & {
  argument(value: NodeData): UnaryOperatorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'unary_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unaryOperator(
  operatorOrConfig: NodeData | UnaryOperatorConfig,
  config?: Partial<UnaryOperatorConfig>,
): UnaryOperatorNode {
  const fields: any = isNodeData(operatorOrConfig) || typeof operatorOrConfig === 'string'
    ? { 'operator': resolveInput(operatorOrConfig), ...config }
    : operatorOrConfig;
  const node: any = { type: 'unary_operator', fields };
  node.argument = (v: any) => { fields['argument'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface UnionPatternConfig {
  children?: NodeData[];
}

export type UnionPatternNode = NodeData<'union_pattern'> & {
  children(...value: NodeData[]): UnionPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unionPattern(
  config?: UnionPatternConfig,
): UnionPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'union_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface UnionTypeConfig {
  children?: NodeData[];
}

export type UnionTypeNode = NodeData<'union_type'> & {
  children(...value: NodeData[]): UnionTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unionType(
  config?: UnionTypeConfig,
): UnionTypeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'union_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface WhileStatementConfig {
  condition: NodeData;
  body: NodeData;
  alternative?: NodeData;
}

export type WhileStatementNode = NodeData<'while_statement'> & {
  body(value: NodeData): WhileStatementNode;
  alternative(value: NodeData): WhileStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'while_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function whileStatement(
  conditionOrConfig: NodeData | WhileStatementConfig,
  config?: Partial<WhileStatementConfig>,
): WhileStatementNode {
  const fields: any = isNodeData(conditionOrConfig) || typeof conditionOrConfig === 'string'
    ? { 'condition': resolveInput(conditionOrConfig), ...config }
    : conditionOrConfig;
  const node: any = { type: 'while_statement', fields };
  node.body = (v: any) => { fields['body'] = resolveInput(v); return node; };
  node.alternative = (v: any) => { fields['alternative'] = resolveInput(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface WithClauseConfig {
  children?: NodeData[];
}

export type WithClauseNode = NodeData<'with_clause'> & {
  children(...value: NodeData[]): WithClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function withClause(
  config?: WithClauseConfig,
): WithClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'with_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface WithItemConfig {
  value: NodeData;
}

export type WithItemNode = NodeData<'with_item'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function withItem(
  valueOrConfig: NodeData | WithItemConfig,
  config?: Partial<WithItemConfig>,
): WithItemNode {
  const fields: any = isNodeData(valueOrConfig) || typeof valueOrConfig === 'string'
    ? { 'value': resolveInput(valueOrConfig), ...config }
    : valueOrConfig;
  const node: any = { type: 'with_item', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface WithStatementConfig {
  body: NodeData;
  children?: NodeData[];
}

export type WithStatementNode = NodeData<'with_statement'> & {
  children(...value: NodeData[]): WithStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function withStatement(
  bodyOrConfig: NodeData | WithStatementConfig,
  config?: Partial<WithStatementConfig>,
): WithStatementNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveInput(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'with_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export interface YieldConfig {
  children?: NodeData[];
}

export type YieldNode = NodeData<'yield'> & {
  children(...value: NodeData[]): YieldNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'yield'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function yield_(
  config?: YieldConfig,
): YieldNode {
  const fields: any = config ?? {};
  const node: any = { type: 'yield', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => toEdit(node, rules, startOrRange, endPos, joinBy);
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, undefined, joinBy); };
  return node;
}

export function breakStatement(): NodeData<'break_statement'> & { render(): string; replace(target: { type: 'break_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'break_statement', fields: {}, text: 'break' };
  node.render = () => 'break';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: 'break' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'break' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' }; };
  return node;
}

export function continueStatement(): NodeData<'continue_statement'> & { render(): string; replace(target: { type: 'continue_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'continue_statement', fields: {}, text: 'continue' };
  node.render = () => 'continue';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: 'continue' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'continue' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' }; };
  return node;
}

export function importPrefix(text: string): NodeData<'import_prefix'> & { render(): string; replace(target: { type: 'import_prefix'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'import_prefix', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function keywordSeparator(): NodeData<'keyword_separator'> & { render(): string; replace(target: { type: 'keyword_separator'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'keyword_separator', fields: {}, text: '*' };
  node.render = () => '*';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: '*' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; };
  return node;
}

export function passStatement(): NodeData<'pass_statement'> & { render(): string; replace(target: { type: 'pass_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'pass_statement', fields: {}, text: 'pass' };
  node.render = () => 'pass';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: 'pass' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'pass' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' }; };
  return node;
}

export function positionalSeparator(): NodeData<'positional_separator'> & { render(): string; replace(target: { type: 'positional_separator'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'positional_separator', fields: {}, text: '/' };
  node.render = () => '/';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: '/' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '/' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '/' }; };
  return node;
}

export function wildcardImport(): NodeData<'wildcard_import'> & { render(): string; replace(target: { type: 'wildcard_import'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'wildcard_import', fields: {}, text: '*' };
  node.render = () => '*';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: '*' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; };
  return node;
}

export function comment(text: string): NodeData<'comment'> & { render(): string; replace(target: { type: 'comment'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'comment', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function ellipsis(): NodeData<'ellipsis'> & { render(): string; replace(target: { type: 'ellipsis'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'ellipsis', fields: {}, text: '...' };
  node.render = () => '...';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: '...' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '...' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '...' }; };
  return node;
}

export function escapeInterpolation(text: string): NodeData<'escape_interpolation'> & { render(): string; replace(target: { type: 'escape_interpolation'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'escape_interpolation', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function escapeSequence(text: `\\${string}`): NodeData<'escape_sequence'> & { render(): string; replace(target: { type: 'escape_sequence'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'escape_sequence', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function false_(): NodeData<'false'> & { render(): string; replace(target: { type: 'false'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'false', fields: {}, text: 'False' };
  node.render = () => 'False';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: 'False' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'False' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' }; };
  return node;
}

export function float(text: string): NodeData<'float'> & { render(): string; replace(target: { type: 'float'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'float', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function identifier(text: string): NodeData<'identifier'> & { render(): string; replace(target: { type: 'identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  const node: any = { type: 'identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function integer(text: string): NodeData<'integer'> & { render(): string; replace(target: { type: 'integer'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'integer', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function lineContinuation(text: string): NodeData<'line_continuation'> & { render(): string; replace(target: { type: 'line_continuation'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'line_continuation', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function none(): NodeData<'none'> & { render(): string; replace(target: { type: 'none'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'none', fields: {}, text: 'None' };
  node.render = () => 'None';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: 'None' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'None' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' }; };
  return node;
}

export function stringEnd(text: string): NodeData<'string_end'> & { render(): string; replace(target: { type: 'string_end'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'string_end', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function stringStart(text: string): NodeData<'string_start'> & { render(): string; replace(target: { type: 'string_start'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'string_start', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function true_(): NodeData<'true'> & { render(): string; replace(target: { type: 'true'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'true', fields: {}, text: 'True' };
  node.render = () => 'True';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: 'True' };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'True' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' }; };
  return node;
}

export function typeConversion(text: string): NodeData<'type_conversion'> & { render(): string; replace(target: { type: 'type_conversion'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'type_conversion', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return { startPos: startOrRange, endPos: endPos!, insertedText: text };
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

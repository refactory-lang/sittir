// Auto-generated by @sittir/codegen — do not edit

import type { AliasedImport, AliasedImportTree, ArgumentList, ArgumentListTree, AsPattern, AsPatternTarget, AsPatternTargetTree, AsPatternTree, AssertStatement, AssertStatementTree, Assignment, AssignmentColon, AssignmentColon2, AssignmentColon2Config, AssignmentColon2Tree, AssignmentColonConfig, AssignmentColonTree, AssignmentEq, AssignmentEqConfig, AssignmentEqTree, AssignmentTree, Attribute, AttributeTree, AugmentedAssignment, AugmentedAssignmentTree, Await, AwaitTree, BinaryOperator, BinaryOperatorTree, Block, BlockTree, BooleanOperator, BooleanOperatorTree, BreakStatement, BreakStatementTree, Call, CallTree, CaseClause, CaseClauseTree, CasePattern, CasePatternTree, Chevron, ChevronTree, ClassDefinition, ClassDefinitionTree, ClassPattern, ClassPatternTree, CloseBrace, CloseBraceTree, CloseBracket, CloseBracketTree, CloseParen, CloseParenTree, CollectionElements, CollectionElementsTree, Comment, CommentTree, ComparisonOperator, ComparisonOperatorTree, ComplexPattern, ComplexPatternTree, ComprehensionClauses, ComprehensionClausesTree, ConcatenatedString, ConcatenatedStringTree, ConditionalExpression, ConditionalExpressionTree, ConfigMap, ConstrainedType, ConstrainedTypeTree, ContinueStatement, ContinueStatementTree, DecoratedDefinition, DecoratedDefinitionTree, Decorator, DecoratorTree, Dedent, DedentTree, DefaultParameter, DefaultParameterTree, DeleteStatement, DeleteStatementTree, DictPattern, DictPatternTree, Dictionary, DictionaryComprehension, DictionaryComprehensionTree, DictionarySplat, DictionarySplatPattern, DictionarySplatPatternTree, DictionarySplatTree, DictionaryTree, DottedName, DottedNameTree, ElifClause, ElifClauseTree, ElseClause, ElseClauseTree, EscapeInterpolation, EscapeInterpolationTree, EscapeSequence, EscapeSequenceTree, Except, ExceptClause, ExceptClauseTree, ExceptTree, ExecStatement, ExecStatementTree, Expression, ExpressionList, ExpressionListTree, ExpressionStatement, ExpressionStatementTree, ExpressionTree, False, FalseTree, FinallyClause, FinallyClauseTree, Float, FloatTree, ForInClause, ForInClauseTree, ForStatement, ForStatementTree, FormatExpression, FormatExpressionTree, FormatSpecifier, FormatSpecifierTree, FunctionDefinition, FunctionDefinitionTree, FutureImportStatement, FutureImportStatementTree, GeneratorExpression, GeneratorExpressionTree, GenericType, GenericTypeTree, GlobalStatement, GlobalStatementTree, HiddenAsPattern, HiddenAsPatternTree, HiddenListPattern, HiddenListPatternTree, HiddenParameters, HiddenParametersTree, HiddenStringContent, HiddenStringContentTree, HiddenTuplePattern, HiddenTuplePatternTree, Identifier, IdentifierTree, IfClause, IfClauseTree, IfStatement, IfStatementTree, ImportFromStatement, ImportFromStatementTree, ImportList, ImportListTree, ImportPrefix, ImportPrefixTree, ImportStatement, ImportStatementTree, Indent, IndentTree, Integer, IntegerTree, Interpolation, InterpolationTree, IsNot, IsNotTree, KeywordArgument, KeywordArgumentTree, KeywordIdentifier, KeywordIdentifierTree, KeywordPattern, KeywordPatternTree, KindMap, Lambda, LambdaParameters, LambdaParametersTree, LambdaTree, LambdaWithinForInClause, LambdaWithinForInClauseTree, LineContinuation, LineContinuationTree, List, ListComprehension, ListComprehensionTree, ListPattern, ListPatternTree, ListSplat, ListSplatPattern, ListSplatPatternTree, ListSplatTree, ListTree, MatchBlock, MatchBlockTree, MatchStatement, MatchStatementTree, MemberType, MemberTypeTree, Module, ModuleTree, NamedExpression, NamedExpressionTree, Newline, NewlineTree, None, NoneTree, NonlocalStatement, NonlocalStatementTree, NotIn, NotInTree, NotOperator, NotOperatorTree, Pair, PairTree, Parameter, ParameterTree, Parameters, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionTree, ParenthesizedListSplat, ParenthesizedListSplatTree, PassStatement, PassStatementTree, Pattern, PatternList, PatternListTree, PatternTree, Patterns, PatternsTree, PrimaryExpression, PrimaryExpressionTree, PrintStatement, PrintStatementTree, RaiseStatement, RaiseStatementTree, RelativeImport, RelativeImportTree, ReturnStatement, ReturnStatementTree, Set, SetComprehension, SetComprehensionTree, SetTree, SimplePattern, SimplePatternTree, SimpleStatements, SimpleStatementsTree, Slice, SliceTree, SplatPattern, SplatPatternTree, SplatType, SplatTypeTree, String, StringContent, StringContentTree, StringEnd, StringEndTree, StringStart, StringStartTree, StringTree, Subscript, SubscriptTree, Suite, SuiteTree, True, TrueTree, TryStatement, TryStatementTree, Tuple, TuplePattern, TuplePatternTree, TupleTree, Type, TypeAliasStatement, TypeAliasStatementTree, TypeConversion, TypeConversionTree, TypeParameter, TypeParameterTree, TypeTree, TypedDefaultParameter, TypedDefaultParameterTree, TypedParameter, TypedParameterTree, UnaryOperator, UnaryOperatorTree, UnionPattern, UnionPatternTree, UnionType, UnionTypeTree, WhileStatement, WhileStatementTree, WithClause, WithClauseTree, WithItem, WithItemTree, WithStatement, WithStatementTree, Yield, YieldTree } from './types.js';
import type { Edit, ConfigOf, ChildOf, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  'False',
  'None',
  'True',
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'break_statement',
  'case',
  'class',
  'continue',
  'continue_statement',
  'def',
  'del',
  'elif',
  'else',
  'exec',
  'false',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'match',
  'none',
  'nonlocal',
  'not',
  'or',
  'pass',
  'pass_statement',
  'print',
  'raise',
  'return',
  'true',
  'try',
  'while',
  'with',
]);

export function module(..._children: ChildOf<Module>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Module>['children'];
  return {
    type: 'module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function simpleStatements(..._children: ChildOf<SimpleStatements>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<SimpleStatements>['children'];
  return {
    type: '_simple_statements' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SimpleStatementsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importStatement(child: ChildOf<ImportStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ImportStatement>['children'];
  return {
    type: 'import_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importPrefix(text: string) {
  return {
    type: 'import_prefix' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ImportPrefixTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function relativeImport(config: ConfigOf<RelativeImport>) {
  const fields = {
    import_prefix: config?.importPrefix,
    dotted_name: config?.dottedName,
  };
  return {
    type: 'relative_import' as const,
    named: true as const,
    fields,
    importPrefix(importPrefix_?: ConfigOf<RelativeImport>['importPrefix']) { return importPrefix_ !== undefined ? relativeImport({ ...(config ?? {}), importPrefix: importPrefix_ }) : fields.import_prefix; },
    dottedName(dottedName_?: ConfigOf<RelativeImport>['dottedName']) { return dottedName_ !== undefined ? relativeImport({ ...(config ?? {}), dottedName: dottedName_ }) : fields.dotted_name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RelativeImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function futureImportStatement(child: ChildOf<FutureImportStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<FutureImportStatement>['children'];
  return {
    type: 'future_import_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importFromStatement(config: ConfigOf<ImportFromStatement>) {
  const fields = {
    module_name: config?.moduleName,
    wildcard_import: config?.wildcardImport,
  };
  return {
    type: 'import_from_statement' as const,
    named: true as const,
    fields,
    moduleName(moduleName_?: ConfigOf<ImportFromStatement>['moduleName']) { return moduleName_ !== undefined ? importFromStatement({ ...(config ?? {}), moduleName: moduleName_ }) : fields.module_name; },
    wildcardImport(wildcardImport_?: ConfigOf<ImportFromStatement>['wildcardImport']) { return wildcardImport_ !== undefined ? importFromStatement({ ...(config ?? {}), wildcardImport: wildcardImport_ }) : fields.wildcard_import; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importList(config: ConfigOf<ImportList>) {
  const fields = {
    name: config?.name,
  };
  return {
    type: '_import_list' as const,
    named: true as const,
    fields,
    name(...name_: NonNullable<ConfigOf<ImportList>['name']>) { return name_.length ? importList({ ...(config ?? {}), name: name_ }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function aliasedImport(config: ConfigOf<AliasedImport>) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'aliased_import' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<AliasedImport>['name']) { return name_ !== undefined ? aliasedImport({ ...(config ?? {}), name: name_ }) : fields.name; },
    alias(alias_?: ConfigOf<AliasedImport>['alias']) { return alias_ !== undefined ? aliasedImport({ ...(config ?? {}), alias: alias_ }) : fields.alias; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AliasedImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function printStatement(config: ConfigOf<PrintStatement>) {
  const fields = {
    argument: config?.argument,
  };
  const children = config?.children ?? [];
  return {
    type: 'print_statement' as const,
    named: true as const,
    fields,
    children,
    argument(...argument_: NonNullable<ConfigOf<PrintStatement>['argument']>) { return argument_.length ? printStatement({ ...(config ?? {}), argument: argument_ }) : fields.argument; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<PrintStatement>) { return printStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function chevron(config: ConfigOf<Chevron>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'chevron' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<Chevron>['expression']) { return expression_ !== undefined ? chevron({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ChevronTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertStatement(..._children: ChildOf<AssertStatement>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<AssertStatement>['children'];
  return {
    type: 'assert_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(..._children: ChildOf<ExpressionStatement>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ExpressionStatement>['children'];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedExpression(config: ConfigOf<NamedExpression>) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'named_expression' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<NamedExpression>['name']) { return name_ !== undefined ? namedExpression({ ...(config ?? {}), name: name_ }) : fields.name; },
    value(value_?: ConfigOf<NamedExpression>['value']) { return value_ !== undefined ? namedExpression({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(child?: ChildOf<ReturnStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ReturnStatement>['children'];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function deleteStatement(child: ChildOf<DeleteStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<DeleteStatement>['children'];
  return {
    type: 'delete_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeleteStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function raiseStatement(config?: ConfigOf<RaiseStatement>) {
  const fields = {
    cause: config?.cause,
  };
  const children = config?.children ?? [];
  return {
    type: 'raise_statement' as const,
    named: true as const,
    fields,
    children,
    cause(cause_?: ConfigOf<RaiseStatement>['cause']) { return cause_ !== undefined ? raiseStatement({ ...(config ?? {}), cause: cause_ }) : fields.cause; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<RaiseStatement>) { return raiseStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RaiseStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function passStatement() {
  return {
    type: 'pass_statement' as const,
    named: true as const,
    text: 'pass' as const,
    render: () => 'pass' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'pass' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'pass' as const },
    replace: (t: PassStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' as const }; },
  };
}

export function breakStatement() {
  return {
    type: 'break_statement' as const,
    named: true as const,
    text: 'break' as const,
    render: () => 'break' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'break' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'break' as const },
    replace: (t: BreakStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' as const }; },
  };
}

export function continueStatement() {
  return {
    type: 'continue_statement' as const,
    named: true as const,
    text: 'continue' as const,
    render: () => 'continue' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'continue' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'continue' as const },
    replace: (t: ContinueStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' as const }; },
  };
}

export function ifStatement(config: ConfigOf<IfStatement>) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: ConfigOf<IfStatement>['condition']) { return condition_ !== undefined ? ifStatement({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    consequence(consequence_?: ConfigOf<IfStatement>['consequence']) { return consequence_ !== undefined ? ifStatement({ ...(config ?? {}), consequence: consequence_ }) : fields.consequence; },
    alternative(...alternative_: NonNullable<ConfigOf<IfStatement>['alternative']>) { return alternative_.length ? ifStatement({ ...(config ?? {}), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elifClause(config: ConfigOf<ElifClause>) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
  };
  return {
    type: 'elif_clause' as const,
    named: true as const,
    fields,
    condition(condition_?: ConfigOf<ElifClause>['condition']) { return condition_ !== undefined ? elifClause({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    consequence(consequence_?: ConfigOf<ElifClause>['consequence']) { return consequence_ !== undefined ? elifClause({ ...(config ?? {}), consequence: consequence_ }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElifClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: ConfigOf<ElseClause>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<ElseClause>['body']) { return body_ !== undefined ? elseClause({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchStatement(config: ConfigOf<MatchStatement>) {
  const fields = {
    subject: config?.subject,
    body: config?.body,
  };
  return {
    type: 'match_statement' as const,
    named: true as const,
    fields,
    subject(...subject_: NonNullable<ConfigOf<MatchStatement>['subject']>) { return subject_.length ? matchStatement({ ...(config ?? {}), subject: subject_ }) : fields.subject; },
    body(body_?: ConfigOf<MatchStatement>['body']) { return body_ !== undefined ? matchStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchBlock(config: ConfigOf<MatchBlock>) {
  const fields = {
    alternative: config?.alternative,
  };
  return {
    type: '_match_block' as const,
    named: true as const,
    fields,
    alternative(...alternative_: NonNullable<ConfigOf<MatchBlock>['alternative']>) { return alternative_.length ? matchBlock({ ...(config ?? {}), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function caseClause(config: ConfigOf<CaseClause>) {
  const fields = {
    guard: config?.guard,
    consequence: config?.consequence,
  };
  const children = config?.children ?? [];
  return {
    type: 'case_clause' as const,
    named: true as const,
    fields,
    children,
    guard(guard_?: ConfigOf<CaseClause>['guard']) { return guard_ !== undefined ? caseClause({ ...(config ?? {}), guard: guard_ }) : fields.guard; },
    consequence(consequence_?: ConfigOf<CaseClause>['consequence']) { return consequence_ !== undefined ? caseClause({ ...(config ?? {}), consequence: consequence_ }) : fields.consequence; },
    getChildren() { return children; },
    setChildren(...items: ChildOf<CaseClause>[]) { return caseClause({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CaseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: ConfigOf<ForStatement>) {
  const fields = {
    left: config?.left,
    right: config?.right,
    body: config?.body,
    alternative: config?.alternative,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<ForStatement>['left']) { return left_ !== undefined ? forStatement({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<ForStatement>['right']) { return right_ !== undefined ? forStatement({ ...(config ?? {}), right: right_ }) : fields.right; },
    body(body_?: ConfigOf<ForStatement>['body']) { return body_ !== undefined ? forStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    alternative(alternative_?: ConfigOf<ForStatement>['alternative']) { return alternative_ !== undefined ? forStatement({ ...(config ?? {}), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: ConfigOf<WhileStatement>) {
  const fields = {
    condition: config?.condition,
    body: config?.body,
    alternative: config?.alternative,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: ConfigOf<WhileStatement>['condition']) { return condition_ !== undefined ? whileStatement({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    body(body_?: ConfigOf<WhileStatement>['body']) { return body_ !== undefined ? whileStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    alternative(alternative_?: ConfigOf<WhileStatement>['alternative']) { return alternative_ !== undefined ? whileStatement({ ...(config ?? {}), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: ConfigOf<TryStatement>) {
  const fields = {
    body: config?.body,
    except_clauses: config?.exceptClauses,
    else_clause: config?.elseClause,
    finally_clause: config?.finallyClause,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<TryStatement>['body']) { return body_ !== undefined ? tryStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    exceptClauses(...exceptClauses_: NonNullable<ConfigOf<TryStatement>['exceptClauses']>) { return exceptClauses_.length ? tryStatement({ ...(config ?? {}), exceptClauses: exceptClauses_ }) : fields.except_clauses; },
    elseClause(elseClause_?: ConfigOf<TryStatement>['elseClause']) { return elseClause_ !== undefined ? tryStatement({ ...(config ?? {}), elseClause: elseClause_ }) : fields.else_clause; },
    finallyClause(finallyClause_?: ConfigOf<TryStatement>['finallyClause']) { return finallyClause_ !== undefined ? tryStatement({ ...(config ?? {}), finallyClause: finallyClause_ }) : fields.finally_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exceptClause(config: ConfigOf<ExceptClause>) {
  const fields = {
    value: config?.value,
    alias: config?.alias,
  };
  const children = config?.children ?? [];
  return {
    type: 'except_clause' as const,
    named: true as const,
    fields,
    children,
    value(...value_: NonNullable<ConfigOf<ExceptClause>['value']>) { return value_.length ? exceptClause({ ...(config ?? {}), value: value_ }) : fields.value; },
    alias(alias_?: ConfigOf<ExceptClause>['alias']) { return alias_ !== undefined ? exceptClause({ ...(config ?? {}), alias: alias_ }) : fields.alias; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ExceptClause>) { return exceptClause({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExceptClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: ConfigOf<FinallyClause>) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    block(block_?: ConfigOf<FinallyClause>['block']) { return block_ !== undefined ? finallyClause({ ...(config ?? {}), block: block_ }) : fields.block; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: ConfigOf<WithStatement>) {
  const fields = {
    with_clause: config?.withClause,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    children,
    withClause(withClause_?: ConfigOf<WithStatement>['withClause']) { return withClause_ !== undefined ? withStatement({ ...(config ?? {}), withClause: withClause_ }) : fields.with_clause; },
    body(body_?: ConfigOf<WithStatement>['body']) { return body_ !== undefined ? withStatement({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<WithStatement>) { return withStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withClause(..._children: ChildOf<WithClause>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<WithClause>['children'];
  return {
    type: 'with_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withItem(config: ConfigOf<WithItem>) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'with_item' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<WithItem>['value']) { return value_ !== undefined ? withItem({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDefinition(config: ConfigOf<FunctionDefinition>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    body: config?.body,
  };
  return {
    type: 'function_definition' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<FunctionDefinition>['name']) { return name_ !== undefined ? functionDefinition({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<FunctionDefinition>['typeParameters']) { return typeParameters_ !== undefined ? functionDefinition({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    parameters(parameters_?: ConfigOf<FunctionDefinition>['parameters']) { return parameters_ !== undefined ? functionDefinition({ ...(config ?? {}), parameters: parameters_ }) : fields.parameters; },
    returnType(returnType_?: ConfigOf<FunctionDefinition>['returnType']) { return returnType_ !== undefined ? functionDefinition({ ...(config ?? {}), returnType: returnType_ }) : fields.return_type; },
    body(body_?: ConfigOf<FunctionDefinition>['body']) { return body_ !== undefined ? functionDefinition({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(child?: ChildOf<Parameters>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Parameters>['children'];
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaParameters(child: ChildOf<LambdaParameters>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<LambdaParameters>['children'];
  return {
    type: 'lambda_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplat(config: ConfigOf<ListSplat>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'list_splat' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<ListSplat>['expression']) { return expression_ !== undefined ? listSplat({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplat(config: ConfigOf<DictionarySplat>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'dictionary_splat' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<DictionarySplat>['expression']) { return expression_ !== undefined ? dictionarySplat({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function globalStatement(..._children: ChildOf<GlobalStatement>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<GlobalStatement>['children'];
  return {
    type: 'global_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GlobalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonlocalStatement(..._children: ChildOf<NonlocalStatement>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<NonlocalStatement>['children'];
  return {
    type: 'nonlocal_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonlocalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function execStatement(config: ConfigOf<ExecStatement>) {
  const fields = {
    code: config?.code,
  };
  const children = config?.children ?? [];
  return {
    type: 'exec_statement' as const,
    named: true as const,
    fields,
    children,
    code(code_?: ConfigOf<ExecStatement>['code']) { return code_ !== undefined ? execStatement({ ...(config ?? {}), code: code_ }) : fields.code; },
    getChildren() { return children; },
    setChildren(...items: ChildOf<ExecStatement>[]) { return execStatement({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExecStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasStatement(config: ConfigOf<TypeAliasStatement>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'type_alias_statement' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<TypeAliasStatement>['left']) { return left_ !== undefined ? typeAliasStatement({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<TypeAliasStatement>['right']) { return right_ !== undefined ? typeAliasStatement({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDefinition(config: ConfigOf<ClassDefinition>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    superclasses: config?.superclasses,
    body: config?.body,
  };
  return {
    type: 'class_definition' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ClassDefinition>['name']) { return name_ !== undefined ? classDefinition({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeParameters(typeParameters_?: ConfigOf<ClassDefinition>['typeParameters']) { return typeParameters_ !== undefined ? classDefinition({ ...(config ?? {}), typeParameters: typeParameters_ }) : fields.type_parameters; },
    superclasses(superclasses_?: ConfigOf<ClassDefinition>['superclasses']) { return superclasses_ !== undefined ? classDefinition({ ...(config ?? {}), superclasses: superclasses_ }) : fields.superclasses; },
    body(body_?: ConfigOf<ClassDefinition>['body']) { return body_ !== undefined ? classDefinition({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(..._children: ChildOf<TypeParameter>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TypeParameter>['children'];
  return {
    type: 'type_parameter' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedListSplat(child: ChildOf<ParenthesizedListSplat>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ParenthesizedListSplat>['children'];
  return {
    type: 'parenthesized_list_splat' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function argumentList(..._children: ChildOf<ArgumentList>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ArgumentList>['children'];
  return {
    type: 'argument_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratedDefinition(config: ConfigOf<DecoratedDefinition>) {
  const fields = {
    definition: config?.definition,
  };
  const children = config?.children ?? [];
  return {
    type: 'decorated_definition' as const,
    named: true as const,
    fields,
    children,
    definition(definition_?: ConfigOf<DecoratedDefinition>['definition']) { return definition_ !== undefined ? decoratedDefinition({ ...(config ?? {}), definition: definition_ }) : fields.definition; },
    getChildren() { return children; },
    setChildren(...items: ChildOf<DecoratedDefinition>[]) { return decoratedDefinition({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratedDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(config: ConfigOf<Decorator>) {
  const fields = {
    expression: config?.expression,
    newline: config?.newline,
  };
  return {
    type: 'decorator' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<Decorator>['expression']) { return expression_ !== undefined ? decorator({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    newline(newline_?: ConfigOf<Decorator>['newline']) { return newline_ !== undefined ? decorator({ ...(config ?? {}), newline: newline_ }) : fields.newline; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function suite(child: ChildOf<Suite>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Suite>['children'];
  return {
    type: '_suite' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SuiteTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(..._children: ChildOf<Block>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Block>['children'];
  return {
    type: 'block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionList(..._children: ChildOf<ExpressionList>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ExpressionList>['children'];
  return {
    type: 'expression_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dottedName(..._children: ChildOf<DottedName>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<DottedName>['children'];
  return {
    type: 'dotted_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DottedNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function casePattern(child: ChildOf<CasePattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<CasePattern>['children'];
  return {
    type: 'case_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function simplePattern(child: ChildOf<SimplePattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<SimplePattern>['children'];
  return {
    type: '_simple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SimplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenAsPattern(child: ChildOf<HiddenAsPattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<HiddenAsPattern>['children'];
  return {
    type: '_as_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenAsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionPattern(..._children: ChildOf<UnionPattern>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<UnionPattern>['children'];
  return {
    type: 'union_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenListPattern(..._children: ChildOf<HiddenListPattern>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<HiddenListPattern>['children'];
  return {
    type: '_list_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenTuplePattern(..._children: ChildOf<HiddenTuplePattern>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<HiddenTuplePattern>['children'];
  return {
    type: '_tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenTuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictPattern(..._children: ChildOf<DictPattern>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<DictPattern>['children'];
  return {
    type: 'dict_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordPattern(config: ConfigOf<KeywordPattern>) {
  const fields = {
    identifier: config?.identifier,
    simple_pattern: config?.simplePattern,
  };
  return {
    type: 'keyword_pattern' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<KeywordPattern>['identifier']) { return identifier_ !== undefined ? keywordPattern({ ...(config ?? {}), identifier: identifier_ }) : fields.identifier; },
    simplePattern(simplePattern_?: ConfigOf<KeywordPattern>['simplePattern']) { return simplePattern_ !== undefined ? keywordPattern({ ...(config ?? {}), simplePattern: simplePattern_ }) : fields.simple_pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatPattern(config: ConfigOf<SplatPattern>) {
  const fields = {
    identifier: config?.identifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'splat_pattern' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: ConfigOf<SplatPattern>['identifier']) { return identifier_ !== undefined ? splatPattern({ ...(config ?? {}), identifier: identifier_ }) : fields.identifier; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<SplatPattern>) { return splatPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classPattern(config: ConfigOf<ClassPattern>) {
  const fields = {
    dotted_name: config?.dottedName,
    arguments: config?.arguments,
  };
  return {
    type: 'class_pattern' as const,
    named: true as const,
    fields,
    dottedName(dottedName_?: ConfigOf<ClassPattern>['dottedName']) { return dottedName_ !== undefined ? classPattern({ ...(config ?? {}), dottedName: dottedName_ }) : fields.dotted_name; },
    arguments(arguments_?: ConfigOf<ClassPattern>['arguments']) { return arguments_ !== undefined ? classPattern({ ...(config ?? {}), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function complexPattern(config: ConfigOf<ComplexPattern>) {
  const fields = {
    real: config?.real,
    imaginary: config?.imaginary,
  };
  const children = config?.children ?? [];
  return {
    type: 'complex_pattern' as const,
    named: true as const,
    fields,
    children,
    real(real_?: ConfigOf<ComplexPattern>['real']) { return real_ !== undefined ? complexPattern({ ...(config ?? {}), real: real_ }) : fields.real; },
    imaginary(imaginary_?: ConfigOf<ComplexPattern>['imaginary']) { return imaginary_ !== undefined ? complexPattern({ ...(config ?? {}), imaginary: imaginary_ }) : fields.imaginary; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ComplexPattern>) { return complexPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenParameters(..._children: ChildOf<HiddenParameters>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<HiddenParameters>['children'];
  return {
    type: '_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function patterns(..._children: ChildOf<Patterns>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Patterns>['children'];
  return {
    type: '_patterns' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameter(child: ChildOf<Parameter>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Parameter>['children'];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pattern(child: ChildOf<Pattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Pattern>['children'];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(child?: ChildOf<TuplePattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<TuplePattern>['children'];
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listPattern(child?: ChildOf<ListPattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ListPattern>['children'];
  return {
    type: 'list_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultParameter(config: ConfigOf<DefaultParameter>) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'default_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<DefaultParameter>['name']) { return name_ !== undefined ? defaultParameter({ ...(config ?? {}), name: name_ }) : fields.name; },
    value(value_?: ConfigOf<DefaultParameter>['value']) { return value_ !== undefined ? defaultParameter({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedDefaultParameter(config: ConfigOf<TypedDefaultParameter>) {
  const fields = {
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'typed_default_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TypedDefaultParameter>['name']) { return name_ !== undefined ? typedDefaultParameter({ ...(config ?? {}), name: name_ }) : fields.name; },
    typeField(type?: ConfigOf<TypedDefaultParameter>['type']) { return type !== undefined ? typedDefaultParameter({ ...(config ?? {}), type: type }) : fields.type; },
    value(value_?: ConfigOf<TypedDefaultParameter>['value']) { return value_ !== undefined ? typedDefaultParameter({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedDefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplatPattern(child: ChildOf<ListSplatPattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ListSplatPattern>['children'];
  return {
    type: 'list_splat_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplatPattern(child: ChildOf<DictionarySplatPattern>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<DictionarySplatPattern>['children'];
  return {
    type: 'dictionary_splat_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asPattern(config: ConfigOf<AsPattern>) {
  const fields = {
    expression: config?.expression,
    alias: config?.alias,
  };
  return {
    type: 'as_pattern' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<AsPattern>['expression']) { return expression_ !== undefined ? asPattern({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    alias(alias_?: ConfigOf<AsPattern>['alias']) { return alias_ !== undefined ? asPattern({ ...(config ?? {}), alias: alias_ }) : fields.alias; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expression(child: ChildOf<Expression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Expression>['children'];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function primaryExpression(child: ChildOf<PrimaryExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<PrimaryExpression>['children'];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function notOperator(config: ConfigOf<NotOperator>) {
  const fields = {
    argument: config?.argument,
  };
  return {
    type: 'not_operator' as const,
    named: true as const,
    fields,
    argument(argument_?: ConfigOf<NotOperator>['argument']) { return argument_ !== undefined ? notOperator({ ...(config ?? {}), argument: argument_ }) : fields.argument; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NotOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function booleanOperator(config: ConfigOf<BooleanOperator>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'boolean_operator' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<BooleanOperator>['left']) { return left_ !== undefined ? booleanOperator({ ...(config ?? {}), left: left_ }) : fields.left; },
    operator(operator_?: ConfigOf<BooleanOperator>['operator']) { return operator_ !== undefined ? booleanOperator({ ...(config ?? {}), operator: operator_ }) : fields.operator; },
    right(right_?: ConfigOf<BooleanOperator>['right']) { return right_ !== undefined ? booleanOperator({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryOperator(config: ConfigOf<BinaryOperator>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<BinaryOperator>['left']) { return left_ !== undefined ? binaryOperator({ ...(config ?? {}), left: left_ }) : fields.left; },
    operator(operator_?: ConfigOf<BinaryOperator>['operator']) { return operator_ !== undefined ? binaryOperator({ ...(config ?? {}), operator: operator_ }) : fields.operator; },
    right(right_?: ConfigOf<BinaryOperator>['right']) { return right_ !== undefined ? binaryOperator({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryOperator(config: ConfigOf<UnaryOperator>) {
  const fields = {
    operator: config?.operator,
    argument: config?.argument,
  };
  return {
    type: 'unary_operator' as const,
    named: true as const,
    fields,
    operator(operator_?: ConfigOf<UnaryOperator>['operator']) { return operator_ !== undefined ? unaryOperator({ ...(config ?? {}), operator: operator_ }) : fields.operator; },
    argument(argument_?: ConfigOf<UnaryOperator>['argument']) { return argument_ !== undefined ? unaryOperator({ ...(config ?? {}), argument: argument_ }) : fields.argument; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function notIn(text: string) {
  return {
    type: '_not_in' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: NotInTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function isNot(text: string) {
  return {
    type: '_is_not' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IsNotTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comparisonOperator(config: ConfigOf<ComparisonOperator>) {
  const fields = {
    left: config?.left,
    comparators: config?.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<ComparisonOperator>['left']) { return left_ !== undefined ? comparisonOperator({ ...(config ?? {}), left: left_ }) : fields.left; },
    comparators(...comparators_: NonNullable<ConfigOf<ComparisonOperator>['comparators']>) { return comparators_.length ? comparisonOperator({ ...(config ?? {}), comparators: comparators_ }) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambda(config: ConfigOf<Lambda>) {
  const fields = {
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    type: 'lambda' as const,
    named: true as const,
    fields,
    parameters(parameters_?: ConfigOf<Lambda>['parameters']) { return parameters_ !== undefined ? lambda({ ...(config ?? {}), parameters: parameters_ }) : fields.parameters; },
    body(body_?: ConfigOf<Lambda>['body']) { return body_ !== undefined ? lambda({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaWithinForInClause(config: ConfigOf<LambdaWithinForInClause>) {
  const fields = {
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    type: 'lambda_within_for_in_clause' as const,
    named: true as const,
    fields,
    parameters(parameters_?: ConfigOf<LambdaWithinForInClause>['parameters']) { return parameters_ !== undefined ? lambdaWithinForInClause({ ...(config ?? {}), parameters: parameters_ }) : fields.parameters; },
    body(body_?: ConfigOf<LambdaWithinForInClause>['body']) { return body_ !== undefined ? lambdaWithinForInClause({ ...(config ?? {}), body: body_ }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaWithinForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignment(config: ConfigOf<AssignmentEq> | ConfigOf<AssignmentColon> | ConfigOf<AssignmentColon2>) {
  if (config && 'type' in config && 'right' in config) return assignmentColon2(config as ConfigOf<AssignmentColon2>);
  if (config && 'right' in config) return assignmentEq(config as ConfigOf<AssignmentEq>);
  return assignmentColon(config as ConfigOf<AssignmentColon>);
}
export function assignmentEq(config: ConfigOf<AssignmentEq>) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    fields,
    right(right_?: ConfigOf<AssignmentEq>['right']) { return right_ !== undefined ? assignmentEq({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentEqTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function assignmentColon(config: ConfigOf<AssignmentColon>) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    fields,
    typeField(type?: ConfigOf<AssignmentColon>['type']) { return type !== undefined ? assignmentColon({ ...(config ?? {}), type: type }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentColonTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function assignmentColon2(config: ConfigOf<AssignmentColon2>) {
  const fields = {
    type: config?.type,
    right: config?.right,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    fields,
    typeField(type?: ConfigOf<AssignmentColon2>['type']) { return type !== undefined ? assignmentColon2({ ...(config ?? {}), type: type }) : fields.type; },
    right(right_?: ConfigOf<AssignmentColon2>['right']) { return right_ !== undefined ? assignmentColon2({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentColon2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignment(config: ConfigOf<AugmentedAssignment>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<AugmentedAssignment>['left']) { return left_ !== undefined ? augmentedAssignment({ ...(config ?? {}), left: left_ }) : fields.left; },
    operator(operator_?: ConfigOf<AugmentedAssignment>['operator']) { return operator_ !== undefined ? augmentedAssignment({ ...(config ?? {}), operator: operator_ }) : fields.operator; },
    right(right_?: ConfigOf<AugmentedAssignment>['right']) { return right_ !== undefined ? augmentedAssignment({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function patternList(..._children: ChildOf<PatternList>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<PatternList>['children'];
  return {
    type: 'pattern_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yield_(child: ChildOf<Yield>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Yield>['children'];
  return {
    type: 'yield' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: ConfigOf<Attribute>) {
  const fields = {
    object: config?.object,
    attribute: config?.attribute,
  };
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<Attribute>['object']) { return object_ !== undefined ? attribute({ ...(config ?? {}), object: object_ }) : fields.object; },
    attribute(attribute_?: ConfigOf<Attribute>['attribute']) { return attribute_ !== undefined ? attribute({ ...(config ?? {}), attribute: attribute_ }) : fields.attribute; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscript(config: ConfigOf<Subscript>) {
  const fields = {
    value: config?.value,
    subscript: config?.subscript,
  };
  return {
    type: 'subscript' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<Subscript>['value']) { return value_ !== undefined ? subscript({ ...(config ?? {}), value: value_ }) : fields.value; },
    subscript(...subscript_: NonNullable<ConfigOf<Subscript>['subscript']>) { return subscript_.length ? subscript({ ...(config ?? {}), subscript: subscript_ }) : fields.subscript; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slice(config?: ConfigOf<Slice>) {
  const fields = {
    start: config?.start,
    stop: config?.stop,
    step: config?.step,
  };
  return {
    type: 'slice' as const,
    named: true as const,
    fields,
    start(start_?: ConfigOf<Slice>['start']) { return start_ !== undefined ? slice({ ...(config ?? {}), start: start_ }) : fields.start; },
    stop(stop_?: ConfigOf<Slice>['stop']) { return stop_ !== undefined ? slice({ ...(config ?? {}), stop: stop_ }) : fields.stop; },
    step(step_?: ConfigOf<Slice>['step']) { return step_ !== undefined ? slice({ ...(config ?? {}), step: step_ }) : fields.step; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SliceTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function call(config: ConfigOf<Call>) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<Call>['function']) { return function_ !== undefined ? call({ ...(config ?? {}), function: function_ }) : fields.function; },
    arguments(arguments_?: ConfigOf<Call>['arguments']) { return arguments_ !== undefined ? call({ ...(config ?? {}), arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedParameter(config: ConfigOf<TypedParameter>) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'typed_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: ConfigOf<TypedParameter>['type']) { return type !== undefined ? typedParameter({ ...(config ?? {}), type: type }) : fields.type; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<TypedParameter>) { return typedParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function type(child: ChildOf<Type>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Type>['children'];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatType(config: ConfigOf<SplatType>) {
  const fields = {
    identifier: config?.identifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'splat_type' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: ConfigOf<SplatType>['identifier']) { return identifier_ !== undefined ? splatType({ ...(config ?? {}), identifier: identifier_ }) : fields.identifier; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<SplatType>) { return splatType({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: ConfigOf<GenericType>) {
  const fields = {
    identifier: config?.identifier,
    type_parameter: config?.typeParameter,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<GenericType>['identifier']) { return identifier_ !== undefined ? genericType({ ...(config ?? {}), identifier: identifier_ }) : fields.identifier; },
    typeParameter(typeParameter_?: ConfigOf<GenericType>['typeParameter']) { return typeParameter_ !== undefined ? genericType({ ...(config ?? {}), typeParameter: typeParameter_ }) : fields.type_parameter; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: ConfigOf<UnionType>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<UnionType>['left']) { return left_ !== undefined ? unionType({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<UnionType>['right']) { return right_ !== undefined ? unionType({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constrainedType(config: ConfigOf<ConstrainedType>) {
  const fields = {
    base_type: config?.baseType,
    constraint: config?.constraint,
  };
  return {
    type: 'constrained_type' as const,
    named: true as const,
    fields,
    baseType(baseType_?: ConfigOf<ConstrainedType>['baseType']) { return baseType_ !== undefined ? constrainedType({ ...(config ?? {}), baseType: baseType_ }) : fields.base_type; },
    constraint(constraint_?: ConfigOf<ConstrainedType>['constraint']) { return constraint_ !== undefined ? constrainedType({ ...(config ?? {}), constraint: constraint_ }) : fields.constraint; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstrainedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberType(config: ConfigOf<MemberType>) {
  const fields = {
    base_type: config?.baseType,
    identifier: config?.identifier,
  };
  return {
    type: 'member_type' as const,
    named: true as const,
    fields,
    baseType(baseType_?: ConfigOf<MemberType>['baseType']) { return baseType_ !== undefined ? memberType({ ...(config ?? {}), baseType: baseType_ }) : fields.base_type; },
    identifier(identifier_?: ConfigOf<MemberType>['identifier']) { return identifier_ !== undefined ? memberType({ ...(config ?? {}), identifier: identifier_ }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordArgument(config: ConfigOf<KeywordArgument>) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'keyword_argument' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<KeywordArgument>['name']) { return name_ !== undefined ? keywordArgument({ ...(config ?? {}), name: name_ }) : fields.name; },
    value(value_?: ConfigOf<KeywordArgument>['value']) { return value_ !== undefined ? keywordArgument({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordArgumentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function list(child?: ChildOf<List>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<List>['children'];
  return {
    type: 'list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function set(child: ChildOf<Set>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Set>['children'];
  return {
    type: 'set' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuple(child?: ChildOf<Tuple>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Tuple>['children'];
  return {
    type: 'tuple' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionary(..._children: ChildOf<Dictionary>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Dictionary>['children'];
  return {
    type: 'dictionary' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: ConfigOf<Pair>) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key_?: ConfigOf<Pair>['key']) { return key_ !== undefined ? pair({ ...(config ?? {}), key: key_ }) : fields.key; },
    value(value_?: ConfigOf<Pair>['value']) { return value_ !== undefined ? pair({ ...(config ?? {}), value: value_ }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listComprehension(config: ConfigOf<ListComprehension>) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'list_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<ListComprehension>['body']) { return body_ !== undefined ? listComprehension({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ListComprehension>) { return listComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionaryComprehension(config: ConfigOf<DictionaryComprehension>) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'dictionary_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<DictionaryComprehension>['body']) { return body_ !== undefined ? dictionaryComprehension({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<DictionaryComprehension>) { return dictionaryComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function setComprehension(config: ConfigOf<SetComprehension>) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'set_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<SetComprehension>['body']) { return body_ !== undefined ? setComprehension({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<SetComprehension>) { return setComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorExpression(config: ConfigOf<GeneratorExpression>) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_expression' as const,
    named: true as const,
    fields,
    children,
    body(body_?: ConfigOf<GeneratorExpression>['body']) { return body_ !== undefined ? generatorExpression({ ...(config ?? {}), body: body_ }) : fields.body; },
    getChild() { return children[0]; },
    setChild(child: ChildOf<GeneratorExpression>) { return generatorExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comprehensionClauses(..._children: ChildOf<ComprehensionClauses>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ComprehensionClauses>['children'];
  return {
    type: '_comprehension_clauses' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComprehensionClausesTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child: ChildOf<ParenthesizedExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ParenthesizedExpression>['children'];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function collectionElements(..._children: ChildOf<CollectionElements>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<CollectionElements>['children'];
  return {
    type: '_collection_elements' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CollectionElementsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInClause(config: ConfigOf<ForInClause>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'for_in_clause' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<ForInClause>['left']) { return left_ !== undefined ? forInClause({ ...(config ?? {}), left: left_ }) : fields.left; },
    right(right_?: ConfigOf<ForInClause>['right']) { return right_ !== undefined ? forInClause({ ...(config ?? {}), right: right_ }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifClause(config: ConfigOf<IfClause>) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'if_clause' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<IfClause>['expression']) { return expression_ !== undefined ? ifClause({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalExpression(config: ConfigOf<ConditionalExpression>) {
  const fields = {
    body: config?.body,
    condition: config?.condition,
    alternative: config?.alternative,
  };
  return {
    type: 'conditional_expression' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<ConditionalExpression>['body']) { return body_ !== undefined ? conditionalExpression({ ...(config ?? {}), body: body_ }) : fields.body; },
    condition(condition_?: ConfigOf<ConditionalExpression>['condition']) { return condition_ !== undefined ? conditionalExpression({ ...(config ?? {}), condition: condition_ }) : fields.condition; },
    alternative(alternative_?: ConfigOf<ConditionalExpression>['alternative']) { return alternative_ !== undefined ? conditionalExpression({ ...(config ?? {}), alternative: alternative_ }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function concatenatedString(..._children: ChildOf<ConcatenatedString>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ConcatenatedString>['children'];
  return {
    type: 'concatenated_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConcatenatedStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(config: ConfigOf<String>) {
  const fields = {
    string_start: config?.stringStart,
    content: config?.content,
    string_end: config?.stringEnd,
  };
  return {
    type: 'string' as const,
    named: true as const,
    fields,
    stringStart(stringStart_?: ConfigOf<String>['stringStart']) { return stringStart_ !== undefined ? string({ ...(config ?? {}), stringStart: stringStart_ }) : fields.string_start; },
    content(...content_: NonNullable<ConfigOf<String>['content']>) { return content_.length ? string({ ...(config ?? {}), content: content_ }) : fields.content; },
    stringEnd(stringEnd_?: ConfigOf<String>['stringEnd']) { return stringEnd_ !== undefined ? string({ ...(config ?? {}), stringEnd: stringEnd_ }) : fields.string_end; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringContent(..._children: ChildOf<StringContent>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<StringContent>['children'];
  return {
    type: 'string_content' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringContentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interpolation(config: ConfigOf<Interpolation>) {
  const fields = {
    expression: config?.expression,
    type_conversion: config?.typeConversion,
    format_specifier: config?.formatSpecifier,
  };
  return {
    type: 'interpolation' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<Interpolation>['expression']) { return expression_ !== undefined ? interpolation({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    typeConversion(typeConversion_?: ConfigOf<Interpolation>['typeConversion']) { return typeConversion_ !== undefined ? interpolation({ ...(config ?? {}), typeConversion: typeConversion_ }) : fields.type_conversion; },
    formatSpecifier(formatSpecifier_?: ConfigOf<Interpolation>['formatSpecifier']) { return formatSpecifier_ !== undefined ? interpolation({ ...(config ?? {}), formatSpecifier: formatSpecifier_ }) : fields.format_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterpolationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function escapeSequence(text: string) {
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function formatSpecifier(..._children: ChildOf<FormatSpecifier>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<FormatSpecifier>['children'];
  return {
    type: 'format_specifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeConversion(text: string) {
  return {
    type: 'type_conversion' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeConversionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer(text: string) {
  return {
    type: 'integer' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IntegerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function float(text: string) {
  return {
    type: 'float' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FloatTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function keywordIdentifier(text: string) {
  return {
    type: 'keyword_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: KeywordIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'True' as const,
    render: () => 'True' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'True' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'True' as const },
    replace: (t: TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' as const }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'False' as const,
    render: () => 'False' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'False' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'False' as const },
    replace: (t: FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' as const }; },
  };
}

export function none() {
  return {
    type: 'none' as const,
    named: true as const,
    text: 'None' as const,
    render: () => 'None' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'None' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'None' as const },
    replace: (t: NoneTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' as const }; },
  };
}

export function await_(config: ConfigOf<Await>) {
  const fields = {
    primary_expression: config?.primaryExpression,
  };
  return {
    type: 'await' as const,
    named: true as const,
    fields,
    primaryExpression(primaryExpression_?: ConfigOf<Await>['primaryExpression']) { return primaryExpression_ !== undefined ? await_({ ...(config ?? {}), primaryExpression: primaryExpression_ }) : fields.primary_expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comment(text: string) {
  return {
    type: 'comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function lineContinuation(text: string) {
  return {
    type: 'line_continuation' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: LineContinuationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function newline(text: string) {
  return {
    type: '_newline' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: NewlineTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function indent(text: string) {
  return {
    type: '_indent' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IndentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function dedent(text: string) {
  return {
    type: '_dedent' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: DedentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringStart(text: string) {
  return {
    type: 'string_start' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringStartTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function hiddenStringContent(text: string) {
  return {
    type: '_string_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HiddenStringContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeInterpolation(text: string) {
  return {
    type: 'escape_interpolation' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeInterpolationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringEnd(text: string) {
  return {
    type: 'string_end' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringEndTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeBracket(text: string) {
  return {
    type: ']' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CloseBracketTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeParen(text: string) {
  return {
    type: ')' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CloseParenTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeBrace(text: string) {
  return {
    type: '}' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CloseBraceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function except(text: string) {
  return {
    type: 'except' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ExceptTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function asPatternTarget(child: ChildOf<AsPatternTarget>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<AsPatternTarget>['children'];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formatExpression(config: ConfigOf<FormatExpression>) {
  const fields = {
    expression: config?.expression,
    type_conversion: config?.typeConversion,
    format_specifier: config?.formatSpecifier,
  };
  return {
    type: 'format_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: ConfigOf<FormatExpression>['expression']) { return expression_ !== undefined ? formatExpression({ ...(config ?? {}), expression: expression_ }) : fields.expression; },
    typeConversion(typeConversion_?: ConfigOf<FormatExpression>['typeConversion']) { return typeConversion_ !== undefined ? formatExpression({ ...(config ?? {}), typeConversion: typeConversion_ }) : fields.type_conversion; },
    formatSpecifier(formatSpecifier_?: ConfigOf<FormatExpression>['formatSpecifier']) { return formatSpecifier_ !== undefined ? formatExpression({ ...(config ?? {}), formatSpecifier: formatSpecifier_ }) : fields.format_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export type FluentKindMap = {
  "module": FluentNode<"module", ConfigOf<Module>>;
  "_simple_statements": FluentNode<"_simple_statements", ConfigOf<SimpleStatements>>;
  "import_statement": FluentNode<"import_statement", ConfigOf<ImportStatement>>;
  "import_prefix": ImportPrefix;
  "relative_import": FluentNode<"relative_import", ConfigOf<RelativeImport>>;
  "future_import_statement": FluentNode<"future_import_statement", ConfigOf<FutureImportStatement>>;
  "import_from_statement": FluentNode<"import_from_statement", ConfigOf<ImportFromStatement>>;
  "_import_list": FluentNode<"_import_list", ConfigOf<ImportList>>;
  "aliased_import": FluentNode<"aliased_import", ConfigOf<AliasedImport>>;
  "print_statement": FluentNode<"print_statement", ConfigOf<PrintStatement>>;
  "chevron": FluentNode<"chevron", ConfigOf<Chevron>>;
  "assert_statement": FluentNode<"assert_statement", ConfigOf<AssertStatement>>;
  "expression_statement": FluentNode<"expression_statement", ConfigOf<ExpressionStatement>>;
  "named_expression": FluentNode<"named_expression", ConfigOf<NamedExpression>>;
  "return_statement": FluentNode<"return_statement", ConfigOf<ReturnStatement>>;
  "delete_statement": FluentNode<"delete_statement", ConfigOf<DeleteStatement>>;
  "raise_statement": FluentNode<"raise_statement", ConfigOf<RaiseStatement>>;
  "pass_statement": PassStatement;
  "break_statement": BreakStatement;
  "continue_statement": ContinueStatement;
  "if_statement": FluentNode<"if_statement", ConfigOf<IfStatement>>;
  "elif_clause": FluentNode<"elif_clause", ConfigOf<ElifClause>>;
  "else_clause": FluentNode<"else_clause", ConfigOf<ElseClause>>;
  "match_statement": FluentNode<"match_statement", ConfigOf<MatchStatement>>;
  "_match_block": FluentNode<"_match_block", ConfigOf<MatchBlock>>;
  "case_clause": FluentNode<"case_clause", ConfigOf<CaseClause>>;
  "for_statement": FluentNode<"for_statement", ConfigOf<ForStatement>>;
  "while_statement": FluentNode<"while_statement", ConfigOf<WhileStatement>>;
  "try_statement": FluentNode<"try_statement", ConfigOf<TryStatement>>;
  "except_clause": FluentNode<"except_clause", ConfigOf<ExceptClause>>;
  "finally_clause": FluentNode<"finally_clause", ConfigOf<FinallyClause>>;
  "with_statement": FluentNode<"with_statement", ConfigOf<WithStatement>>;
  "with_clause": FluentNode<"with_clause", ConfigOf<WithClause>>;
  "with_item": FluentNode<"with_item", ConfigOf<WithItem>>;
  "function_definition": FluentNode<"function_definition", ConfigOf<FunctionDefinition>>;
  "parameters": FluentNode<"parameters", ConfigOf<Parameters>>;
  "lambda_parameters": FluentNode<"lambda_parameters", ConfigOf<LambdaParameters>>;
  "list_splat": FluentNode<"list_splat", ConfigOf<ListSplat>>;
  "dictionary_splat": FluentNode<"dictionary_splat", ConfigOf<DictionarySplat>>;
  "global_statement": FluentNode<"global_statement", ConfigOf<GlobalStatement>>;
  "nonlocal_statement": FluentNode<"nonlocal_statement", ConfigOf<NonlocalStatement>>;
  "exec_statement": FluentNode<"exec_statement", ConfigOf<ExecStatement>>;
  "type_alias_statement": FluentNode<"type_alias_statement", ConfigOf<TypeAliasStatement>>;
  "class_definition": FluentNode<"class_definition", ConfigOf<ClassDefinition>>;
  "type_parameter": FluentNode<"type_parameter", ConfigOf<TypeParameter>>;
  "parenthesized_list_splat": FluentNode<"parenthesized_list_splat", ConfigOf<ParenthesizedListSplat>>;
  "argument_list": FluentNode<"argument_list", ConfigOf<ArgumentList>>;
  "decorated_definition": FluentNode<"decorated_definition", ConfigOf<DecoratedDefinition>>;
  "decorator": FluentNode<"decorator", ConfigOf<Decorator>>;
  "_suite": FluentNode<"_suite", ConfigOf<Suite>>;
  "block": FluentNode<"block", ConfigOf<Block>>;
  "expression_list": FluentNode<"expression_list", ConfigOf<ExpressionList>>;
  "dotted_name": FluentNode<"dotted_name", ConfigOf<DottedName>>;
  "case_pattern": FluentNode<"case_pattern", ConfigOf<CasePattern>>;
  "_simple_pattern": FluentNode<"_simple_pattern", ConfigOf<SimplePattern>>;
  "_as_pattern": FluentNode<"_as_pattern", ConfigOf<HiddenAsPattern>>;
  "union_pattern": FluentNode<"union_pattern", ConfigOf<UnionPattern>>;
  "_list_pattern": FluentNode<"_list_pattern", ConfigOf<HiddenListPattern>>;
  "_tuple_pattern": FluentNode<"_tuple_pattern", ConfigOf<HiddenTuplePattern>>;
  "dict_pattern": FluentNode<"dict_pattern", ConfigOf<DictPattern>>;
  "keyword_pattern": FluentNode<"keyword_pattern", ConfigOf<KeywordPattern>>;
  "splat_pattern": FluentNode<"splat_pattern", ConfigOf<SplatPattern>>;
  "class_pattern": FluentNode<"class_pattern", ConfigOf<ClassPattern>>;
  "complex_pattern": FluentNode<"complex_pattern", ConfigOf<ComplexPattern>>;
  "_parameters": FluentNode<"_parameters", ConfigOf<HiddenParameters>>;
  "_patterns": FluentNode<"_patterns", ConfigOf<Patterns>>;
  "parameter": FluentNode<"parameter", ConfigOf<Parameter>>;
  "pattern": FluentNode<"pattern", ConfigOf<Pattern>>;
  "tuple_pattern": FluentNode<"tuple_pattern", ConfigOf<TuplePattern>>;
  "list_pattern": FluentNode<"list_pattern", ConfigOf<ListPattern>>;
  "default_parameter": FluentNode<"default_parameter", ConfigOf<DefaultParameter>>;
  "typed_default_parameter": FluentNode<"typed_default_parameter", ConfigOf<TypedDefaultParameter>>;
  "list_splat_pattern": FluentNode<"list_splat_pattern", ConfigOf<ListSplatPattern>>;
  "dictionary_splat_pattern": FluentNode<"dictionary_splat_pattern", ConfigOf<DictionarySplatPattern>>;
  "as_pattern": FluentNode<"as_pattern", ConfigOf<AsPattern>>;
  "expression": FluentNode<"expression", ConfigOf<Expression>>;
  "primary_expression": FluentNode<"primary_expression", ConfigOf<PrimaryExpression>>;
  "not_operator": FluentNode<"not_operator", ConfigOf<NotOperator>>;
  "boolean_operator": FluentNode<"boolean_operator", ConfigOf<BooleanOperator>>;
  "binary_operator": FluentNode<"binary_operator", ConfigOf<BinaryOperator>>;
  "unary_operator": FluentNode<"unary_operator", ConfigOf<UnaryOperator>>;
  "_not_in": NotIn;
  "_is_not": IsNot;
  "comparison_operator": FluentNode<"comparison_operator", ConfigOf<ComparisonOperator>>;
  "lambda": FluentNode<"lambda", ConfigOf<Lambda>>;
  "lambda_within_for_in_clause": FluentNode<"lambda_within_for_in_clause", ConfigOf<LambdaWithinForInClause>>;
  "assignment": FluentNode<"assignment", ConfigOf<Assignment>>;
  "augmented_assignment": FluentNode<"augmented_assignment", ConfigOf<AugmentedAssignment>>;
  "pattern_list": FluentNode<"pattern_list", ConfigOf<PatternList>>;
  "yield": FluentNode<"yield", ConfigOf<Yield>>;
  "attribute": FluentNode<"attribute", ConfigOf<Attribute>>;
  "subscript": FluentNode<"subscript", ConfigOf<Subscript>>;
  "slice": FluentNode<"slice", ConfigOf<Slice>>;
  "call": FluentNode<"call", ConfigOf<Call>>;
  "typed_parameter": FluentNode<"typed_parameter", ConfigOf<TypedParameter>>;
  "type": FluentNode<"type", ConfigOf<Type>>;
  "splat_type": FluentNode<"splat_type", ConfigOf<SplatType>>;
  "generic_type": FluentNode<"generic_type", ConfigOf<GenericType>>;
  "union_type": FluentNode<"union_type", ConfigOf<UnionType>>;
  "constrained_type": FluentNode<"constrained_type", ConfigOf<ConstrainedType>>;
  "member_type": FluentNode<"member_type", ConfigOf<MemberType>>;
  "keyword_argument": FluentNode<"keyword_argument", ConfigOf<KeywordArgument>>;
  "list": FluentNode<"list", ConfigOf<List>>;
  "set": FluentNode<"set", ConfigOf<Set>>;
  "tuple": FluentNode<"tuple", ConfigOf<Tuple>>;
  "dictionary": FluentNode<"dictionary", ConfigOf<Dictionary>>;
  "pair": FluentNode<"pair", ConfigOf<Pair>>;
  "list_comprehension": FluentNode<"list_comprehension", ConfigOf<ListComprehension>>;
  "dictionary_comprehension": FluentNode<"dictionary_comprehension", ConfigOf<DictionaryComprehension>>;
  "set_comprehension": FluentNode<"set_comprehension", ConfigOf<SetComprehension>>;
  "generator_expression": FluentNode<"generator_expression", ConfigOf<GeneratorExpression>>;
  "_comprehension_clauses": FluentNode<"_comprehension_clauses", ConfigOf<ComprehensionClauses>>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", ConfigOf<ParenthesizedExpression>>;
  "_collection_elements": FluentNode<"_collection_elements", ConfigOf<CollectionElements>>;
  "for_in_clause": FluentNode<"for_in_clause", ConfigOf<ForInClause>>;
  "if_clause": FluentNode<"if_clause", ConfigOf<IfClause>>;
  "conditional_expression": FluentNode<"conditional_expression", ConfigOf<ConditionalExpression>>;
  "concatenated_string": FluentNode<"concatenated_string", ConfigOf<ConcatenatedString>>;
  "string": FluentNode<"string", ConfigOf<String>>;
  "string_content": FluentNode<"string_content", ConfigOf<StringContent>>;
  "interpolation": FluentNode<"interpolation", ConfigOf<Interpolation>>;
  "escape_sequence": EscapeSequence;
  "format_specifier": FluentNode<"format_specifier", ConfigOf<FormatSpecifier>>;
  "type_conversion": TypeConversion;
  "integer": Integer;
  "float": Float;
  "identifier": Identifier;
  "keyword_identifier": KeywordIdentifier;
  "true": True;
  "false": False;
  "none": None;
  "await": FluentNode<"await", ConfigOf<Await>>;
  "comment": Comment;
  "line_continuation": LineContinuation;
  "_newline": Newline;
  "_indent": Indent;
  "_dedent": Dedent;
  "string_start": StringStart;
  "_string_content": HiddenStringContent;
  "escape_interpolation": EscapeInterpolation;
  "string_end": StringEnd;
  "]": CloseBracket;
  ")": CloseParen;
  "}": CloseBrace;
  "except": Except;
  "as_pattern_target": FluentNode<"as_pattern_target", ConfigOf<AsPatternTarget>>;
  "format_expression": FluentNode<"format_expression", ConfigOf<FormatExpression>>;
};

export const _factoryMap = {
  "module": module,
  "_simple_statements": simpleStatements,
  "import_statement": importStatement,
  "import_prefix": importPrefix,
  "relative_import": relativeImport,
  "future_import_statement": futureImportStatement,
  "import_from_statement": importFromStatement,
  "_import_list": importList,
  "aliased_import": aliasedImport,
  "print_statement": printStatement,
  "chevron": chevron,
  "assert_statement": assertStatement,
  "expression_statement": expressionStatement,
  "named_expression": namedExpression,
  "return_statement": returnStatement,
  "delete_statement": deleteStatement,
  "raise_statement": raiseStatement,
  "pass_statement": passStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "if_statement": ifStatement,
  "elif_clause": elifClause,
  "else_clause": elseClause,
  "match_statement": matchStatement,
  "_match_block": matchBlock,
  "case_clause": caseClause,
  "for_statement": forStatement,
  "while_statement": whileStatement,
  "try_statement": tryStatement,
  "except_clause": exceptClause,
  "finally_clause": finallyClause,
  "with_statement": withStatement,
  "with_clause": withClause,
  "with_item": withItem,
  "function_definition": functionDefinition,
  "parameters": parameters,
  "lambda_parameters": lambdaParameters,
  "list_splat": listSplat,
  "dictionary_splat": dictionarySplat,
  "global_statement": globalStatement,
  "nonlocal_statement": nonlocalStatement,
  "exec_statement": execStatement,
  "type_alias_statement": typeAliasStatement,
  "class_definition": classDefinition,
  "type_parameter": typeParameter,
  "parenthesized_list_splat": parenthesizedListSplat,
  "argument_list": argumentList,
  "decorated_definition": decoratedDefinition,
  "decorator": decorator,
  "_suite": suite,
  "block": block,
  "expression_list": expressionList,
  "dotted_name": dottedName,
  "case_pattern": casePattern,
  "_simple_pattern": simplePattern,
  "_as_pattern": hiddenAsPattern,
  "union_pattern": unionPattern,
  "_list_pattern": hiddenListPattern,
  "_tuple_pattern": hiddenTuplePattern,
  "dict_pattern": dictPattern,
  "keyword_pattern": keywordPattern,
  "splat_pattern": splatPattern,
  "class_pattern": classPattern,
  "complex_pattern": complexPattern,
  "_parameters": hiddenParameters,
  "_patterns": patterns,
  "parameter": parameter,
  "pattern": pattern,
  "tuple_pattern": tuplePattern,
  "list_pattern": listPattern,
  "default_parameter": defaultParameter,
  "typed_default_parameter": typedDefaultParameter,
  "list_splat_pattern": listSplatPattern,
  "dictionary_splat_pattern": dictionarySplatPattern,
  "as_pattern": asPattern,
  "expression": expression,
  "primary_expression": primaryExpression,
  "not_operator": notOperator,
  "boolean_operator": booleanOperator,
  "binary_operator": binaryOperator,
  "unary_operator": unaryOperator,
  "_not_in": notIn,
  "_is_not": isNot,
  "comparison_operator": comparisonOperator,
  "lambda": lambda,
  "lambda_within_for_in_clause": lambdaWithinForInClause,
  "assignment": assignment,
  "augmented_assignment": augmentedAssignment,
  "pattern_list": patternList,
  "yield": yield_,
  "attribute": attribute,
  "subscript": subscript,
  "slice": slice,
  "call": call,
  "typed_parameter": typedParameter,
  "type": type,
  "splat_type": splatType,
  "generic_type": genericType,
  "union_type": unionType,
  "constrained_type": constrainedType,
  "member_type": memberType,
  "keyword_argument": keywordArgument,
  "list": list,
  "set": set,
  "tuple": tuple,
  "dictionary": dictionary,
  "pair": pair,
  "list_comprehension": listComprehension,
  "dictionary_comprehension": dictionaryComprehension,
  "set_comprehension": setComprehension,
  "generator_expression": generatorExpression,
  "_comprehension_clauses": comprehensionClauses,
  "parenthesized_expression": parenthesizedExpression,
  "_collection_elements": collectionElements,
  "for_in_clause": forInClause,
  "if_clause": ifClause,
  "conditional_expression": conditionalExpression,
  "concatenated_string": concatenatedString,
  "string": string,
  "string_content": stringContent,
  "interpolation": interpolation,
  "escape_sequence": escapeSequence,
  "format_specifier": formatSpecifier,
  "type_conversion": typeConversion,
  "integer": integer,
  "float": float,
  "identifier": identifier,
  "keyword_identifier": keywordIdentifier,
  "true": true_,
  "false": false_,
  "none": none,
  "await": await_,
  "comment": comment,
  "line_continuation": lineContinuation,
  "_newline": newline,
  "_indent": indent,
  "_dedent": dedent,
  "string_start": stringStart,
  "_string_content": hiddenStringContent,
  "escape_interpolation": escapeInterpolation,
  "string_end": stringEnd,
  "]": closeBracket,
  ")": closeParen,
  "}": closeBrace,
  "except": except,
  "as_pattern_target": asPatternTarget,
  "format_expression": formatExpression,
} as const;
export type _FactoryMap = typeof _factoryMap;

// Auto-generated by @sittir/codegen — do not edit

import type { NodeData } from './types.js';
import type { AliasedImport, AliasedImportConfig, AliasedImportTree, ArgumentList, ArgumentListConfig, ArgumentListTree, AsPattern, AsPatternConfig, AsPatternTree, AssertStatement, AssertStatementConfig, AssertStatementTree, Assignment, AssignmentConfig, AssignmentTree, Attribute, AttributeConfig, AttributeTree, AugmentedAssignment, AugmentedAssignmentConfig, AugmentedAssignmentTree, Await, AwaitConfig, AwaitTree, BinaryOperator, BinaryOperatorConfig, BinaryOperatorTree, Block, BlockConfig, BlockTree, BooleanOperator, BooleanOperatorConfig, BooleanOperatorTree, BreakStatement, BreakStatementTree, Call, CallConfig, CallTree, CaseClause, CaseClauseConfig, CaseClauseTree, CasePattern, CasePatternConfig, CasePatternTree, Chevron, ChevronConfig, ChevronTree, ClassDefinition, ClassDefinitionConfig, ClassDefinitionTree, ClassPattern, ClassPatternConfig, ClassPatternTree, Comment, CommentTree, ComparisonOperator, ComparisonOperatorConfig, ComparisonOperatorTree, ComplexPattern, ComplexPatternConfig, ComplexPatternTree, CompoundStatement, ConcatenatedString, ConcatenatedStringConfig, ConcatenatedStringTree, ConditionalExpression, ConditionalExpressionConfig, ConditionalExpressionTree, ConstrainedType, ConstrainedTypeConfig, ConstrainedTypeTree, ContinueStatement, ContinueStatementTree, DecoratedDefinition, DecoratedDefinitionConfig, DecoratedDefinitionTree, Decorator, DecoratorConfig, DecoratorTree, DefaultParameter, DefaultParameterConfig, DefaultParameterTree, DeleteStatement, DeleteStatementConfig, DeleteStatementTree, DictPattern, DictPatternConfig, DictPatternTree, Dictionary, DictionaryComprehension, DictionaryComprehensionConfig, DictionaryComprehensionTree, DictionaryConfig, DictionarySplat, DictionarySplatConfig, DictionarySplatPattern, DictionarySplatPatternConfig, DictionarySplatPatternTree, DictionarySplatTree, DictionaryTree, DottedName, DottedNameConfig, DottedNameTree, ElifClause, ElifClauseConfig, ElifClauseTree, Ellipsis, EllipsisTree, ElseClause, ElseClauseConfig, ElseClauseTree, EscapeInterpolation, EscapeInterpolationTree, EscapeSequence, EscapeSequenceTree, ExceptClause, ExceptClauseConfig, ExceptClauseTree, ExecStatement, ExecStatementConfig, ExecStatementTree, Expression, ExpressionList, ExpressionListConfig, ExpressionListTree, ExpressionStatement, ExpressionStatementConfig, ExpressionStatementTree, False, FalseTree, FinallyClause, FinallyClauseConfig, FinallyClauseTree, Float, FloatTree, ForInClause, ForInClauseConfig, ForInClauseTree, ForStatement, ForStatementConfig, ForStatementTree, FormatExpression, FormatExpressionConfig, FormatExpressionTree, FormatSpecifier, FormatSpecifierConfig, FormatSpecifierTree, FunctionDefinition, FunctionDefinitionConfig, FunctionDefinitionTree, FutureImportStatement, FutureImportStatementConfig, FutureImportStatementTree, GeneratorExpression, GeneratorExpressionConfig, GeneratorExpressionTree, GenericType, GenericTypeConfig, GenericTypeTree, GlobalStatement, GlobalStatementConfig, GlobalStatementTree, Identifier, IdentifierTree, IfClause, IfClauseConfig, IfClauseTree, IfStatement, IfStatementConfig, IfStatementTree, ImportFromStatement, ImportFromStatementConfig, ImportFromStatementTree, ImportPrefix, ImportPrefixTree, ImportStatement, ImportStatementConfig, ImportStatementTree, Integer, IntegerTree, Interpolation, InterpolationConfig, InterpolationTree, KeywordArgument, KeywordArgumentConfig, KeywordArgumentTree, KeywordPattern, KeywordPatternConfig, KeywordPatternTree, KeywordSeparator, KeywordSeparatorTree, Lambda, LambdaConfig, LambdaParameters, LambdaParametersConfig, LambdaParametersTree, LambdaTree, LineContinuation, LineContinuationTree, List, ListComprehension, ListComprehensionConfig, ListComprehensionTree, ListConfig, ListPattern, ListPatternConfig, ListPatternTree, ListSplat, ListSplatConfig, ListSplatPattern, ListSplatPatternConfig, ListSplatPatternTree, ListSplatTree, ListTree, MatchStatement, MatchStatementConfig, MatchStatementTree, MemberType, MemberTypeConfig, MemberTypeTree, Module, ModuleConfig, ModuleTree, NamedExpression, NamedExpressionConfig, NamedExpressionTree, None, NoneTree, NonlocalStatement, NonlocalStatementConfig, NonlocalStatementTree, NotOperator, NotOperatorConfig, NotOperatorTree, Pair, PairConfig, PairTree, Parameter, Parameters, ParametersConfig, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionConfig, ParenthesizedExpressionTree, ParenthesizedListSplat, ParenthesizedListSplatConfig, ParenthesizedListSplatTree, PassStatement, PassStatementTree, Pattern, PatternList, PatternListConfig, PatternListTree, PositionalSeparator, PositionalSeparatorTree, PrimaryExpression, PrintStatement, PrintStatementConfig, PrintStatementTree, RaiseStatement, RaiseStatementConfig, RaiseStatementTree, RelativeImport, RelativeImportConfig, RelativeImportTree, ReturnStatement, ReturnStatementConfig, ReturnStatementTree, Set, SetComprehension, SetComprehensionConfig, SetComprehensionTree, SetConfig, SetTree, SimpleStatement, Slice, SliceConfig, SliceTree, SplatPattern, SplatPatternConfig, SplatPatternTree, SplatType, SplatTypeConfig, SplatTypeTree, String, StringConfig, StringContent, StringContentConfig, StringContentTree, StringEnd, StringEndTree, StringStart, StringStartTree, StringTree, Subscript, SubscriptConfig, SubscriptTree, True, TrueTree, TryStatement, TryStatementConfig, TryStatementTree, Tuple, TupleConfig, TuplePattern, TuplePatternConfig, TuplePatternTree, TupleTree, Type, TypeAliasStatement, TypeAliasStatementConfig, TypeAliasStatementTree, TypeConfig, TypeConversion, TypeConversionTree, TypeParameter, TypeParameterConfig, TypeParameterTree, TypeTree, TypedDefaultParameter, TypedDefaultParameterConfig, TypedDefaultParameterTree, TypedParameter, TypedParameterConfig, TypedParameterTree, UnaryOperator, UnaryOperatorConfig, UnaryOperatorTree, UnionPattern, UnionPatternConfig, UnionPatternTree, UnionType, UnionTypeConfig, UnionTypeTree, WhileStatement, WhileStatementConfig, WhileStatementTree, WildcardImport, WildcardImportTree, WithClause, WithClauseConfig, WithClauseTree, WithItem, WithItemConfig, WithItemTree, WithStatement, WithStatementConfig, WithStatementTree, Yield, YieldConfig, YieldTree } from './types.js';
import type { Edit, AnyNodeData } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { rules } from './rules.js';
import { joinBy } from './joinby.js';

const { render, toEdit } = createRenderer(rules, joinBy);

const RESERVED_KEYWORDS = new Set([
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'case',
  'class',
  'continue',
  'def',
  'del',
  'elif',
  'else',
  'except',
  'exec',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'match',
  'nonlocal',
  'not',
  'or',
  'pass',
  'print',
  'raise',
  'return',
  'try',
  'while',
  'with',
]);

export function aliased_import_(
  config: AliasedImportConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  return {
    type: 'aliased_import' as const,
    fields,
    alias(v?: Identifier): any { return v !== undefined ? aliased_import_({ ...config, alias: v }) : fields.alias; },
    name(v?: DottedName): any { return v !== undefined ? aliased_import_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AliasedImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function argument_list_(
  config?: ArgumentListConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'argument_list' as const,
    fields,
    children,
    children0(v?: DictionarySplat | Expression | KeywordArgument | ListSplat | ParenthesizedExpression): any { return v !== undefined ? argument_list_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (DictionarySplat | Expression | KeywordArgument | ListSplat | ParenthesizedExpression)[]): any { return v.length ? argument_list_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function as_pattern_(
  config: AsPatternConfig,
) {
  const fields = {
    alias: config.alias,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'as_pattern' as const,
    fields,
    children,
    alias(v?: string): any { return v !== undefined ? as_pattern_({ ...config, alias: v }) : fields.alias; },
    child(v?: CasePattern | Expression | Identifier): any { return v !== undefined ? as_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assert_statement_(
  config: AssertStatementConfig,
) {
  const fields = {
  };
  const children = [config.expression1, ...(config.expression2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'assert_statement' as const,
    fields,
    children,
    expression1(v?: Expression): any { return v !== undefined ? assert_statement_({ ...config, expression1: v }) : config?.expression1; },
    expression2(...v: (Expression)[]): any { return v.length ? assert_statement_({ ...config, expression2: v }) : config?.expression2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_(
  config: AssignmentConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
    type: config.type,
  };
  return {
    type: 'assignment' as const,
    fields,
    left(v?: Pattern | PatternList): any { return v !== undefined ? assignment_({ ...config, left: v }) : fields.left; },
    right(v?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | Yield): any { return v !== undefined ? assignment_({ ...config, right: v }) : fields.right; },
    typeField(v?: Type): any { return v !== undefined ? assignment_({ ...config, type: v }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function attribute_(
  config: AttributeConfig,
) {
  const fields = {
    attribute: config.attribute,
    object: config.object,
  };
  return {
    type: 'attribute' as const,
    fields,
    attribute(v?: Identifier): any { return v !== undefined ? attribute_({ ...config, attribute: v }) : fields.attribute; },
    object(v?: PrimaryExpression): any { return v !== undefined ? attribute_({ ...config, object: v }) : fields.object; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function augmented_assignment_(
  config: AugmentedAssignmentConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    fields,
    left(v?: Pattern | PatternList): any { return v !== undefined ? augmented_assignment_({ ...config, left: v }) : fields.left; },
    operator(v?: '+=' | '-=' | '*=' | '/=' | '@=' | '//=' | '%=' | '**=' | '>>=' | '<<=' | '&=' | '^=' | '|='): any { return v !== undefined ? augmented_assignment_({ ...config, operator: v }) : fields.operator; },
    right(v?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | Yield): any { return v !== undefined ? augmented_assignment_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function await_(
  config: AwaitConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'await' as const,
    fields,
    children,
    child(v?: PrimaryExpression): any { return v !== undefined ? await_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function binary_operator_(
  config: BinaryOperatorConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    fields,
    left(v?: PrimaryExpression): any { return v !== undefined ? binary_operator_({ ...config, left: v }) : fields.left; },
    operator(v?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|'): any { return v !== undefined ? binary_operator_({ ...config, operator: v }) : fields.operator; },
    right(v?: PrimaryExpression): any { return v !== undefined ? binary_operator_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function block_(
  config?: BlockConfig,
) {
  const fields = {
    alternative: config?.alternative,
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'block' as const,
    fields,
    children,
    alternative(...v: (CaseClause)[]): any { return v.length ? block_({ ...config, alternative: v }) : fields.alternative; },
    getChildren(): any { return children; },
    setChildren(...v: (CompoundStatement | SimpleStatement)[]): any { return block_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function boolean_operator_(
  config: BooleanOperatorConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'boolean_operator' as const,
    fields,
    left(v?: Expression): any { return v !== undefined ? boolean_operator_({ ...config, left: v }) : fields.left; },
    operator(v?: 'and' | 'or'): any { return v !== undefined ? boolean_operator_({ ...config, operator: v }) : fields.operator; },
    right(v?: Expression): any { return v !== undefined ? boolean_operator_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call_(
  config: CallConfig,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
  };
  return {
    type: 'call' as const,
    fields,
    arguments(v?: ArgumentList | GeneratorExpression): any { return v !== undefined ? call_({ ...config, arguments: v }) : fields.arguments; },
    function(v?: PrimaryExpression): any { return v !== undefined ? call_({ ...config, function: v }) : fields.function; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function case_clause_(
  config: CaseClauseConfig,
) {
  const fields = {
    consequence: config.consequence,
    guard: config.guard,
  };
  const children = [config.casePattern1, ...(config.casePattern2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'case_clause' as const,
    fields,
    children,
    consequence(v?: Block): any { return v !== undefined ? case_clause_({ ...config, consequence: v }) : fields.consequence; },
    guard(v?: IfClause): any { return v !== undefined ? case_clause_({ ...config, guard: v }) : fields.guard; },
    casePattern1(v?: CasePattern): any { return v !== undefined ? case_clause_({ ...config, casePattern1: v }) : config?.casePattern1; },
    casePattern2(...v: (CasePattern)[]): any { return v.length ? case_clause_({ ...config, casePattern2: v }) : config?.casePattern2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CaseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function case_pattern_(
  config?: CasePatternConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'case_pattern' as const,
    fields,
    children,
    child(v?: AsPattern | ClassPattern | ComplexPattern | ConcatenatedString | DictPattern | DottedName | False | Float | Integer | KeywordPattern | ListPattern | None | SplatPattern | String | True | TuplePattern | UnionPattern): any { return v !== undefined ? case_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function chevron_(
  config: ChevronConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'chevron' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? chevron_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ChevronTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_definition_(
  config: ClassDefinitionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    superclasses: config.superclasses,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'class_definition' as const,
    fields,
    body(v?: Block): any { return v !== undefined ? class_definition_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? class_definition_({ ...config, name: v }) : fields.name; },
    superclasses(v?: ArgumentList): any { return v !== undefined ? class_definition_({ ...config, superclasses: v }) : fields.superclasses; },
    typeParameters(v?: TypeParameter): any { return v !== undefined ? class_definition_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_pattern_(
  config: ClassPatternConfig,
) {
  const fields = {
  };
  const children = [config.dottedName, config.casePattern1, ...(config.casePattern2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'class_pattern' as const,
    fields,
    children,
    dottedName(v?: DottedName): any { return v !== undefined ? class_pattern_({ ...config, dottedName: v }) : config?.dottedName; },
    casePattern1(v?: CasePattern): any { return v !== undefined ? class_pattern_({ ...config, casePattern1: v }) : config?.casePattern1; },
    casePattern2(...v: (CasePattern)[]): any { return v.length ? class_pattern_({ ...config, casePattern2: v }) : config?.casePattern2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function comparison_operator_(
  config: ComparisonOperatorConfig,
) {
  const fields = {
    operators: config.operators,
  };
  const children = [config.primaryExpression1, ...(config.primaryExpression2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'comparison_operator' as const,
    fields,
    children,
    operators(...v: ('<' | '<=' | '==' | '!=' | '>=' | '>' | '<>' | 'in' | 'is' | 'is not' | 'not in')[]): any { return v.length ? comparison_operator_({ ...config, operators: v }) : fields.operators; },
    primaryExpression1(v?: PrimaryExpression): any { return v !== undefined ? comparison_operator_({ ...config, primaryExpression1: v }) : config?.primaryExpression1; },
    primaryExpression2(...v: (PrimaryExpression)[]): any { return v.length ? comparison_operator_({ ...config, primaryExpression2: v }) : config?.primaryExpression2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function complex_pattern_(
  config: ComplexPatternConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'complex_pattern' as const,
    fields,
    children,
    children0(v?: Float | Integer): any { return v !== undefined ? complex_pattern_({ ...config, children0: v }) : config?.children0; },
    children1(v?: Float | Integer): any { return v !== undefined ? complex_pattern_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function concatenated_string_(
  config: ConcatenatedStringConfig,
) {
  const fields = {
  };
  const children = [config.string1, ...(config.string2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'concatenated_string' as const,
    fields,
    children,
    string1(v?: String): any { return v !== undefined ? concatenated_string_({ ...config, string1: v }) : config?.string1; },
    string2(...v: (String)[]): any { return v.length ? concatenated_string_({ ...config, string2: v }) : config?.string2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConcatenatedStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function conditional_expression_(
  config: ConditionalExpressionConfig,
) {
  const fields = {
  };
  const children = [config.expression1, config.expression2, config.expression3].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'conditional_expression' as const,
    fields,
    children,
    expression1(v?: Expression): any { return v !== undefined ? conditional_expression_({ ...config, expression1: v }) : config?.expression1; },
    expression2(v?: Expression): any { return v !== undefined ? conditional_expression_({ ...config, expression2: v }) : config?.expression2; },
    expression3(v?: Expression): any { return v !== undefined ? conditional_expression_({ ...config, expression3: v }) : config?.expression3; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constrained_type_(
  config: ConstrainedTypeConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'constrained_type' as const,
    fields,
    children,
    children0(v?: Type): any { return v !== undefined ? constrained_type_({ ...config, children0: v }) : config?.children0; },
    children1(v?: Type): any { return v !== undefined ? constrained_type_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstrainedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decorated_definition_(
  config: DecoratedDefinitionConfig,
) {
  const fields = {
    definition: config.definition,
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'decorated_definition' as const,
    fields,
    children,
    definition(v?: ClassDefinition | FunctionDefinition): any { return v !== undefined ? decorated_definition_({ ...config, definition: v }) : fields.definition; },
    getChildren(): any { return children; },
    setChildren(...v: (Decorator)[]): any { return decorated_definition_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratedDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decorator_(
  config: DecoratorConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'decorator' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? decorator_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function default_parameter_(
  config: DefaultParameterConfig,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'default_parameter' as const,
    fields,
    name(v?: Identifier | TuplePattern): any { return v !== undefined ? default_parameter_({ ...config, name: v }) : fields.name; },
    value(v?: Expression): any { return v !== undefined ? default_parameter_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function delete_statement_(
  config: DeleteStatementConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'delete_statement' as const,
    fields,
    children,
    child(v?: Expression | ExpressionList): any { return v !== undefined ? delete_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeleteStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dict_pattern_(
  config?: DictPatternConfig,
) {
  const fields = {
  };
  const children = [config?.splatPattern1, ...(config?.splatPattern2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'dict_pattern' as const,
    fields,
    children,
    splatPattern1(v?: SplatPattern): any { return v !== undefined ? dict_pattern_({ ...config, splatPattern1: v }) : config?.splatPattern1; },
    splatPattern2(...v: (SplatPattern)[]): any { return v.length ? dict_pattern_({ ...config, splatPattern2: v }) : config?.splatPattern2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary_(
  config?: DictionaryConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'dictionary' as const,
    fields,
    children,
    children0(v?: DictionarySplat | Pair): any { return v !== undefined ? dictionary_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (DictionarySplat | Pair)[]): any { return v.length ? dictionary_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary_comprehension_(
  config: DictionaryComprehensionConfig,
) {
  const fields = {
    body: config.body,
  };
  const children = [config.forInClause, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'dictionary_comprehension' as const,
    fields,
    children,
    body(v?: Pair): any { return v !== undefined ? dictionary_comprehension_({ ...config, body: v }) : fields.body; },
    forInClause(v?: ForInClause): any { return v !== undefined ? dictionary_comprehension_({ ...config, forInClause: v }) : config?.forInClause; },
    children1(...v: (ForInClause | IfClause)[]): any { return v.length ? dictionary_comprehension_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary_splat_(
  config: DictionarySplatConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'dictionary_splat' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? dictionary_splat_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary_splat_pattern_(
  config: DictionarySplatPatternConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'dictionary_splat_pattern' as const,
    fields,
    children,
    child(v?: Attribute | Identifier | Subscript): any { return v !== undefined ? dictionary_splat_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dotted_name_(
  config: DottedNameConfig,
) {
  const fields = {
  };
  const children = [config.identifier1, ...(config.identifier2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'dotted_name' as const,
    fields,
    children,
    identifier1(v?: Identifier): any { return v !== undefined ? dotted_name_({ ...config, identifier1: v }) : config?.identifier1; },
    identifier2(...v: (Identifier)[]): any { return v.length ? dotted_name_({ ...config, identifier2: v }) : config?.identifier2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DottedNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function elif_clause_(
  config: ElifClauseConfig,
) {
  const fields = {
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'elif_clause' as const,
    fields,
    condition(v?: Expression): any { return v !== undefined ? elif_clause_({ ...config, condition: v }) : fields.condition; },
    consequence(v?: Block): any { return v !== undefined ? elif_clause_({ ...config, consequence: v }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElifClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function else_clause_(
  config: ElseClauseConfig,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'else_clause' as const,
    fields,
    body(v?: Block): any { return v !== undefined ? else_clause_({ ...config, body: v }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function except_clause_(
  config: ExceptClauseConfig,
) {
  const fields = {
    alias: config.alias,
    value: config.value,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'except_clause' as const,
    fields,
    children,
    alias(v?: Expression): any { return v !== undefined ? except_clause_({ ...config, alias: v }) : fields.alias; },
    value(...v: (Expression)[]): any { return v.length ? except_clause_({ ...config, value: v }) : fields.value; },
    child(v?: Block): any { return v !== undefined ? except_clause_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExceptClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function exec_statement_(
  config: ExecStatementConfig,
) {
  const fields = {
    code: config.code,
  };
  const children = [config.expression1, ...(config.expression2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'exec_statement' as const,
    fields,
    children,
    code(v?: Identifier | String): any { return v !== undefined ? exec_statement_({ ...config, code: v }) : fields.code; },
    expression1(v?: Expression): any { return v !== undefined ? exec_statement_({ ...config, expression1: v }) : config?.expression1; },
    expression2(...v: (Expression)[]): any { return v.length ? exec_statement_({ ...config, expression2: v }) : config?.expression2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExecStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expression_list_(
  config: ExpressionListConfig,
) {
  const fields = {
  };
  const children = [config.expression1, ...(config.expression2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'expression_list' as const,
    fields,
    children,
    expression1(v?: Expression): any { return v !== undefined ? expression_list_({ ...config, expression1: v }) : config?.expression1; },
    expression2(...v: (Expression)[]): any { return v.length ? expression_list_({ ...config, expression2: v }) : config?.expression2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expression_statement_(
  config: ExpressionStatementConfig,
) {
  const fields = {
  };
  const children = [config.children0, ...(config.expression ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'expression_statement' as const,
    fields,
    children,
    children0(v?: Assignment | AugmentedAssignment | Expression | Yield): any { return v !== undefined ? expression_statement_({ ...config, children0: v }) : config?.children0; },
    expression(...v: (Expression)[]): any { return v.length ? expression_statement_({ ...config, expression: v }) : config?.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function finally_clause_(
  config: FinallyClauseConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'finally_clause' as const,
    fields,
    children,
    child(v?: Block): any { return v !== undefined ? finally_clause_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_in_clause_(
  config: ForInClauseConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'for_in_clause' as const,
    fields,
    left(v?: Pattern | PatternList): any { return v !== undefined ? for_in_clause_({ ...config, left: v }) : fields.left; },
    right(...v: (Expression)[]): any { return v.length ? for_in_clause_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_statement_(
  config: ForStatementConfig,
) {
  const fields = {
    alternative: config.alternative,
    body: config.body,
    left: config.left,
    right: config.right,
  };
  return {
    type: 'for_statement' as const,
    fields,
    alternative(v?: ElseClause): any { return v !== undefined ? for_statement_({ ...config, alternative: v }) : fields.alternative; },
    body(v?: Block): any { return v !== undefined ? for_statement_({ ...config, body: v }) : fields.body; },
    left(v?: Pattern | PatternList): any { return v !== undefined ? for_statement_({ ...config, left: v }) : fields.left; },
    right(v?: Expression | ExpressionList): any { return v !== undefined ? for_statement_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function format_expression_(
  config: FormatExpressionConfig,
) {
  const fields = {
    expression: config.expression,
    format_specifier: config.formatSpecifier,
    type_conversion: config.typeConversion,
  };
  return {
    type: 'format_expression' as const,
    fields,
    expression(v?: Expression | ExpressionList | PatternList | Yield): any { return v !== undefined ? format_expression_({ ...config, expression: v }) : fields.expression; },
    formatSpecifier(v?: FormatSpecifier): any { return v !== undefined ? format_expression_({ ...config, formatSpecifier: v }) : fields.format_specifier; },
    typeConversion(v?: TypeConversion): any { return v !== undefined ? format_expression_({ ...config, typeConversion: v }) : fields.type_conversion; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function format_specifier_(
  config?: FormatSpecifierConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'format_specifier' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (FormatExpression)[]): any { return format_specifier_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_definition_(
  config: FunctionDefinitionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_definition' as const,
    fields,
    body(v?: Block): any { return v !== undefined ? function_definition_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? function_definition_({ ...config, name: v }) : fields.name; },
    parameters(v?: Parameters): any { return v !== undefined ? function_definition_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: Type): any { return v !== undefined ? function_definition_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameter): any { return v !== undefined ? function_definition_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function future_import_statement_(
  config: FutureImportStatementConfig,
) {
  const fields = {
    name: config.name,
  };
  return {
    type: 'future_import_statement' as const,
    fields,
    name(...v: (AliasedImport | DottedName)[]): any { return v.length ? future_import_statement_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generator_expression_(
  config: GeneratorExpressionConfig,
) {
  const fields = {
    body: config.body,
  };
  const children = [config.forInClause, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'generator_expression' as const,
    fields,
    children,
    body(v?: Expression): any { return v !== undefined ? generator_expression_({ ...config, body: v }) : fields.body; },
    forInClause(v?: ForInClause): any { return v !== undefined ? generator_expression_({ ...config, forInClause: v }) : config?.forInClause; },
    children1(...v: (ForInClause | IfClause)[]): any { return v.length ? generator_expression_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_(
  config: GenericTypeConfig,
) {
  const fields = {
  };
  const children = [config.identifier, config.typeParameter].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'generic_type' as const,
    fields,
    children,
    identifier(v?: Identifier): any { return v !== undefined ? generic_type_({ ...config, identifier: v }) : config?.identifier; },
    typeParameter(v?: TypeParameter): any { return v !== undefined ? generic_type_({ ...config, typeParameter: v }) : config?.typeParameter; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function global_statement_(
  config: GlobalStatementConfig,
) {
  const fields = {
  };
  const children = [config.identifier1, ...(config.identifier2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'global_statement' as const,
    fields,
    children,
    identifier1(v?: Identifier): any { return v !== undefined ? global_statement_({ ...config, identifier1: v }) : config?.identifier1; },
    identifier2(...v: (Identifier)[]): any { return v.length ? global_statement_({ ...config, identifier2: v }) : config?.identifier2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GlobalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_clause_(
  config: IfClauseConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'if_clause' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? if_clause_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_statement_(
  config: IfStatementConfig,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'if_statement' as const,
    fields,
    alternative(...v: (ElifClause | ElseClause)[]): any { return v.length ? if_statement_({ ...config, alternative: v }) : fields.alternative; },
    condition(v?: Expression): any { return v !== undefined ? if_statement_({ ...config, condition: v }) : fields.condition; },
    consequence(v?: Block): any { return v !== undefined ? if_statement_({ ...config, consequence: v }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_from_statement_(
  config: ImportFromStatementConfig,
) {
  const fields = {
    module_name: config.moduleName,
    name: config.name,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'import_from_statement' as const,
    fields,
    children,
    moduleName(v?: DottedName | RelativeImport): any { return v !== undefined ? import_from_statement_({ ...config, moduleName: v }) : fields.module_name; },
    name(...v: (AliasedImport | DottedName)[]): any { return v.length ? import_from_statement_({ ...config, name: v }) : fields.name; },
    child(v?: WildcardImport): any { return v !== undefined ? import_from_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_statement_(
  config: ImportStatementConfig,
) {
  const fields = {
    name: config.name,
  };
  return {
    type: 'import_statement' as const,
    fields,
    name(...v: (AliasedImport | DottedName)[]): any { return v.length ? import_statement_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interpolation_(
  config: InterpolationConfig,
) {
  const fields = {
    expression: config.expression,
    format_specifier: config.formatSpecifier,
    type_conversion: config.typeConversion,
  };
  return {
    type: 'interpolation' as const,
    fields,
    expression(v?: Expression | ExpressionList | PatternList | Yield): any { return v !== undefined ? interpolation_({ ...config, expression: v }) : fields.expression; },
    formatSpecifier(v?: FormatSpecifier): any { return v !== undefined ? interpolation_({ ...config, formatSpecifier: v }) : fields.format_specifier; },
    typeConversion(v?: TypeConversion): any { return v !== undefined ? interpolation_({ ...config, typeConversion: v }) : fields.type_conversion; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterpolationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function keyword_argument_(
  config: KeywordArgumentConfig,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'keyword_argument' as const,
    fields,
    name(v?: Identifier): any { return v !== undefined ? keyword_argument_({ ...config, name: v }) : fields.name; },
    value(v?: Expression): any { return v !== undefined ? keyword_argument_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordArgumentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function keyword_pattern_(
  config: KeywordPatternConfig,
) {
  const fields = {
  };
  const children = [config.identifier, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'keyword_pattern' as const,
    fields,
    children,
    identifier(v?: Identifier): any { return v !== undefined ? keyword_pattern_({ ...config, identifier: v }) : config?.identifier; },
    children1(v?: ClassPattern | ComplexPattern | ConcatenatedString | DictPattern | DottedName | False | Float | Integer | ListPattern | None | SplatPattern | String | True | TuplePattern | UnionPattern): any { return v !== undefined ? keyword_pattern_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lambda_(
  config: LambdaConfig,
) {
  const fields = {
    body: config.body,
    parameters: config.parameters,
  };
  return {
    type: 'lambda' as const,
    fields,
    body(v?: Expression): any { return v !== undefined ? lambda_({ ...config, body: v }) : fields.body; },
    parameters(v?: LambdaParameters): any { return v !== undefined ? lambda_({ ...config, parameters: v }) : fields.parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lambda_parameters_(
  config: LambdaParametersConfig,
) {
  const fields = {
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'lambda_parameters' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Parameter)[]): any { return lambda_parameters_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_(
  config?: ListConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'list' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]): any { return list_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_comprehension_(
  config: ListComprehensionConfig,
) {
  const fields = {
    body: config.body,
  };
  const children = [config.forInClause, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'list_comprehension' as const,
    fields,
    children,
    body(v?: Expression): any { return v !== undefined ? list_comprehension_({ ...config, body: v }) : fields.body; },
    forInClause(v?: ForInClause): any { return v !== undefined ? list_comprehension_({ ...config, forInClause: v }) : config?.forInClause; },
    children1(...v: (ForInClause | IfClause)[]): any { return v.length ? list_comprehension_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_pattern_(
  config?: ListPatternConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'list_pattern' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (CasePattern | Pattern)[]): any { return list_pattern_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_splat_(
  config: ListSplatConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'list_splat' as const,
    fields,
    children,
    child(v?: Attribute | Expression | Identifier | Subscript): any { return v !== undefined ? list_splat_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_splat_pattern_(
  config: ListSplatPatternConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'list_splat_pattern' as const,
    fields,
    children,
    child(v?: Attribute | Identifier | Subscript): any { return v !== undefined ? list_splat_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_statement_(
  config: MatchStatementConfig,
) {
  const fields = {
    body: config.body,
    subject: config.subject,
  };
  return {
    type: 'match_statement' as const,
    fields,
    body(v?: Block): any { return v !== undefined ? match_statement_({ ...config, body: v }) : fields.body; },
    subject(...v: (Expression)[]): any { return v.length ? match_statement_({ ...config, subject: v }) : fields.subject; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function member_type_(
  config: MemberTypeConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.identifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'member_type' as const,
    fields,
    children,
    children0(v?: Type): any { return v !== undefined ? member_type_({ ...config, children0: v }) : config?.children0; },
    identifier(v?: Identifier): any { return v !== undefined ? member_type_({ ...config, identifier: v }) : config?.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function module_(
  config?: ModuleConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'module' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (CompoundStatement | SimpleStatement)[]): any { return module_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function named_expression_(
  config: NamedExpressionConfig,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'named_expression' as const,
    fields,
    name(v?: Identifier): any { return v !== undefined ? named_expression_({ ...config, name: v }) : fields.name; },
    value(v?: Expression): any { return v !== undefined ? named_expression_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nonlocal_statement_(
  config: NonlocalStatementConfig,
) {
  const fields = {
  };
  const children = [config.identifier1, ...(config.identifier2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'nonlocal_statement' as const,
    fields,
    children,
    identifier1(v?: Identifier): any { return v !== undefined ? nonlocal_statement_({ ...config, identifier1: v }) : config?.identifier1; },
    identifier2(...v: (Identifier)[]): any { return v.length ? nonlocal_statement_({ ...config, identifier2: v }) : config?.identifier2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonlocalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function not_operator_(
  config: NotOperatorConfig,
) {
  const fields = {
    argument: config.argument,
  };
  return {
    type: 'not_operator' as const,
    fields,
    argument(v?: Expression): any { return v !== undefined ? not_operator_({ ...config, argument: v }) : fields.argument; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NotOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair_(
  config: PairConfig,
) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    type: 'pair' as const,
    fields,
    key(v?: Expression): any { return v !== undefined ? pair_({ ...config, key: v }) : fields.key; },
    value(v?: Expression): any { return v !== undefined ? pair_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parameters_(
  config?: ParametersConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'parameters' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Parameter)[]): any { return parameters_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_expression_(
  config: ParenthesizedExpressionConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'parenthesized_expression' as const,
    fields,
    children,
    child(v?: Expression | ListSplat | ParenthesizedExpression | Yield): any { return v !== undefined ? parenthesized_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_list_splat_(
  config: ParenthesizedListSplatConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'parenthesized_list_splat' as const,
    fields,
    children,
    child(v?: ListSplat | ParenthesizedExpression): any { return v !== undefined ? parenthesized_list_splat_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pattern_list_(
  config: PatternListConfig,
) {
  const fields = {
  };
  const children = [config.pattern1, ...(config.pattern2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'pattern_list' as const,
    fields,
    children,
    pattern1(v?: Pattern): any { return v !== undefined ? pattern_list_({ ...config, pattern1: v }) : config?.pattern1; },
    pattern2(...v: (Pattern)[]): any { return v.length ? pattern_list_({ ...config, pattern2: v }) : config?.pattern2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function print_statement_(
  config?: PrintStatementConfig,
) {
  const fields = {
    argument: config?.argument,
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'print_statement' as const,
    fields,
    children,
    argument(...v: (Expression)[]): any { return v.length ? print_statement_({ ...config, argument: v }) : fields.argument; },
    child(v?: Chevron): any { return v !== undefined ? print_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function raise_statement_(
  config?: RaiseStatementConfig,
) {
  const fields = {
    cause: config?.cause,
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'raise_statement' as const,
    fields,
    children,
    cause(v?: Expression): any { return v !== undefined ? raise_statement_({ ...config, cause: v }) : fields.cause; },
    child(v?: Expression | ExpressionList): any { return v !== undefined ? raise_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RaiseStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function relative_import_(
  config: RelativeImportConfig,
) {
  const fields = {
  };
  const children = [config.importPrefix, config.dottedName].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'relative_import' as const,
    fields,
    children,
    importPrefix(v?: ImportPrefix): any { return v !== undefined ? relative_import_({ ...config, importPrefix: v }) : config?.importPrefix; },
    dottedName(v?: DottedName): any { return v !== undefined ? relative_import_({ ...config, dottedName: v }) : config?.dottedName; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RelativeImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function return_statement_(
  config?: ReturnStatementConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'return_statement' as const,
    fields,
    children,
    child(v?: Expression | ExpressionList): any { return v !== undefined ? return_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function set_(
  config: SetConfig,
) {
  const fields = {
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'set' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]): any { return set_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function set_comprehension_(
  config: SetComprehensionConfig,
) {
  const fields = {
    body: config.body,
  };
  const children = [config.forInClause, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'set_comprehension' as const,
    fields,
    children,
    body(v?: Expression): any { return v !== undefined ? set_comprehension_({ ...config, body: v }) : fields.body; },
    forInClause(v?: ForInClause): any { return v !== undefined ? set_comprehension_({ ...config, forInClause: v }) : config?.forInClause; },
    children1(...v: (ForInClause | IfClause)[]): any { return v.length ? set_comprehension_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function slice_(
  config?: SliceConfig,
) {
  const fields = {
  };
  const children = [config?.expression1, config?.expression2, config?.expression3].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'slice' as const,
    fields,
    children,
    expression1(v?: Expression): any { return v !== undefined ? slice_({ ...config, expression1: v }) : config?.expression1; },
    expression2(v?: Expression): any { return v !== undefined ? slice_({ ...config, expression2: v }) : config?.expression2; },
    expression3(v?: Expression): any { return v !== undefined ? slice_({ ...config, expression3: v }) : config?.expression3; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SliceTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function splat_pattern_(
  config?: SplatPatternConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'splat_pattern' as const,
    fields,
    children,
    child(v?: Identifier): any { return v !== undefined ? splat_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function splat_type_(
  config: SplatTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'splat_type' as const,
    fields,
    children,
    child(v?: Identifier): any { return v !== undefined ? splat_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string_(
  config: StringConfig,
) {
  const fields = {
  };
  const children = [config.stringStart, ...(config.children1 ?? []), config.stringEnd].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'string' as const,
    fields,
    children,
    stringStart(v?: StringStart): any { return v !== undefined ? string_({ ...config, stringStart: v }) : config?.stringStart; },
    children1(...v: (Interpolation | StringContent)[]): any { return v.length ? string_({ ...config, children1: v }) : config?.children1; },
    stringEnd(v?: StringEnd): any { return v !== undefined ? string_({ ...config, stringEnd: v }) : config?.stringEnd; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string_content_(
  config?: StringContentConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'string_content' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (EscapeInterpolation | EscapeSequence)[]): any { return string_content_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringContentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function subscript_(
  config: SubscriptConfig,
) {
  const fields = {
    subscript: config.subscript,
    value: config.value,
  };
  return {
    type: 'subscript' as const,
    fields,
    subscript(...v: (Expression | Slice)[]): any { return v.length ? subscript_({ ...config, subscript: v }) : fields.subscript; },
    value(v?: PrimaryExpression): any { return v !== undefined ? subscript_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_statement_(
  config: TryStatementConfig,
) {
  const fields = {
    body: config.body,
  };
  const children = [...(config.exceptClause ?? []), config.elseClause, config.finallyClause].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'try_statement' as const,
    fields,
    children,
    body(v?: Block): any { return v !== undefined ? try_statement_({ ...config, body: v }) : fields.body; },
    exceptClause(...v: (ExceptClause)[]): any { return v.length ? try_statement_({ ...config, exceptClause: v }) : config?.exceptClause; },
    elseClause(v?: ElseClause): any { return v !== undefined ? try_statement_({ ...config, elseClause: v }) : config?.elseClause; },
    finallyClause(v?: FinallyClause): any { return v !== undefined ? try_statement_({ ...config, finallyClause: v }) : config?.finallyClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_(
  config?: TupleConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'tuple' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]): any { return tuple_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_pattern_(
  config?: TuplePatternConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'tuple_pattern' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (CasePattern | Pattern)[]): any { return tuple_pattern_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_(
  config: TypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type' as const,
    fields,
    children,
    child(v?: ConstrainedType | Expression | GenericType | MemberType | SplatType | UnionType): any { return v !== undefined ? type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_alias_statement_(
  config: TypeAliasStatementConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'type_alias_statement' as const,
    fields,
    left(v?: Type): any { return v !== undefined ? type_alias_statement_({ ...config, left: v }) : fields.left; },
    right(v?: Type): any { return v !== undefined ? type_alias_statement_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameter_(
  config: TypeParameterConfig,
) {
  const fields = {
  };
  const children = [config.children0, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_parameter' as const,
    fields,
    children,
    children0(v?: Type): any { return v !== undefined ? type_parameter_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (Type)[]): any { return v.length ? type_parameter_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function typed_default_parameter_(
  config: TypedDefaultParameterConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  return {
    type: 'typed_default_parameter' as const,
    fields,
    name(v?: Identifier): any { return v !== undefined ? typed_default_parameter_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? typed_default_parameter_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? typed_default_parameter_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedDefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function typed_parameter_(
  config: TypedParameterConfig,
) {
  const fields = {
    type: config.type,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'typed_parameter' as const,
    fields,
    children,
    typeField(v?: Type): any { return v !== undefined ? typed_parameter_({ ...config, type: v }) : fields.type; },
    child(v?: DictionarySplatPattern | Identifier | ListSplatPattern): any { return v !== undefined ? typed_parameter_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unary_operator_(
  config: UnaryOperatorConfig,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'unary_operator' as const,
    fields,
    argument(v?: PrimaryExpression): any { return v !== undefined ? unary_operator_({ ...config, argument: v }) : fields.argument; },
    operator(v?: '+' | '-' | '~'): any { return v !== undefined ? unary_operator_({ ...config, operator: v }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function union_pattern_(
  config?: UnionPatternConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'union_pattern' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (ClassPattern | ComplexPattern | ConcatenatedString | DictPattern | DottedName | False | Float | Integer | ListPattern | None | SplatPattern | String | True | TuplePattern | UnionPattern)[]): any { return union_pattern_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function union_type_(
  config: UnionTypeConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'union_type' as const,
    fields,
    children,
    children0(v?: Type): any { return v !== undefined ? union_type_({ ...config, children0: v }) : config?.children0; },
    children1(v?: Type): any { return v !== undefined ? union_type_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function while_statement_(
  config: WhileStatementConfig,
) {
  const fields = {
    alternative: config.alternative,
    body: config.body,
    condition: config.condition,
  };
  return {
    type: 'while_statement' as const,
    fields,
    alternative(v?: ElseClause): any { return v !== undefined ? while_statement_({ ...config, alternative: v }) : fields.alternative; },
    body(v?: Block): any { return v !== undefined ? while_statement_({ ...config, body: v }) : fields.body; },
    condition(v?: Expression): any { return v !== undefined ? while_statement_({ ...config, condition: v }) : fields.condition; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function with_clause_(
  config: WithClauseConfig,
) {
  const fields = {
  };
  const children = [config.withItem1, ...(config.withItem2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'with_clause' as const,
    fields,
    children,
    withItem1(v?: WithItem): any { return v !== undefined ? with_clause_({ ...config, withItem1: v }) : config?.withItem1; },
    withItem2(...v: (WithItem)[]): any { return v.length ? with_clause_({ ...config, withItem2: v }) : config?.withItem2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function with_item_(
  config: WithItemConfig,
) {
  const fields = {
    value: config.value,
  };
  return {
    type: 'with_item' as const,
    fields,
    value(v?: Expression): any { return v !== undefined ? with_item_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function with_statement_(
  config: WithStatementConfig,
) {
  const fields = {
    body: config.body,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'with_statement' as const,
    fields,
    children,
    body(v?: Block): any { return v !== undefined ? with_statement_({ ...config, body: v }) : fields.body; },
    child(v?: WithClause): any { return v !== undefined ? with_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function yield_(
  config?: YieldConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'yield' as const,
    fields,
    children,
    child(v?: Expression | ExpressionList): any { return v !== undefined ? yield_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function break_statement_() {
  return {
    type: 'break_statement' as const,
    text: 'break',
    render: () => 'break',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'break' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'break' };
    },
    replace: (target: BreakStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' }; },
  };
}

export function continue_statement_() {
  return {
    type: 'continue_statement' as const,
    text: 'continue',
    render: () => 'continue',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'continue' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'continue' };
    },
    replace: (target: ContinueStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' }; },
  };
}

export function import_prefix_(text: string) {
  if (!/^(?:\.)+$/.test(text)) throw new Error(`Invalid import_prefix: '${text}' does not match grammar pattern`);
  return {
    type: 'import_prefix' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ImportPrefixTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function keyword_separator_() {
  return {
    type: 'keyword_separator' as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: KeywordSeparatorTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function pass_statement_() {
  return {
    type: 'pass_statement' as const,
    text: 'pass',
    render: () => 'pass',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'pass' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'pass' };
    },
    replace: (target: PassStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' }; },
  };
}

export function positional_separator_() {
  return {
    type: 'positional_separator' as const,
    text: '/',
    render: () => '/',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '/' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '/' };
    },
    replace: (target: PositionalSeparatorTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '/' }; },
  };
}

export function wildcard_import_() {
  return {
    type: 'wildcard_import' as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: WildcardImportTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function comment_(text: string) {
  if (!/^#.*$/.test(text)) throw new Error(`Invalid comment: '${text}' does not match grammar pattern`);
  return {
    type: 'comment' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: CommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function ellipsis_() {
  return {
    type: 'ellipsis' as const,
    text: '...',
    render: () => '...',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '...' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '...' };
    },
    replace: (target: EllipsisTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '...' }; },
  };
}

export function escape_interpolation_(text: string) {
  return {
    type: 'escape_interpolation' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeInterpolationTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escape_sequence_(text: `\\${string}`) {
  if (!/^\\(?:u[a-fA-F\d]{4}|U[a-fA-F\d]{8}|x[a-fA-F\d]{2}|\d{1,3}|\r?\n|['"abfrntv\\]|N\{[^}]+\})$/.test(text)) throw new Error(`Invalid escape_sequence: '${text}' does not match grammar pattern`);
  return {
    type: 'escape_sequence' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeSequenceTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    text: 'False',
    render: () => 'False',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'False' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'False' };
    },
    replace: (target: FalseTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' }; },
  };
}

export function float_(text: string) {
  if (!/^(?:(?:[0-9]+_?)+\.(?:[0-9]+_?)+?[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+?\.(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+)[jJ]?$/.test(text)) throw new Error(`Invalid float: '${text}' does not match grammar pattern`);
  return {
    type: 'float' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FloatTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  if (!/^[_\p{XID_Start}][_\p{XID_Continue}]*$/u.test(text)) throw new Error(`Invalid identifier: '${text}' does not match grammar pattern`);
  return {
    type: 'identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer_(text: string) {
  if (!/^(?:(?:0x|0X)(?:_?[A-Fa-f0-9]+)+[Ll]?|(?:0o|0O)(?:_?[0-7]+)+[Ll]?|(?:0b|0B)(?:_?[0-1]+)+[Ll]?|(?:[0-9]+_?)+(?:[Ll]?|[jJ]?))$/.test(text)) throw new Error(`Invalid integer: '${text}' does not match grammar pattern`);
  return {
    type: 'integer' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IntegerTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function line_continuation_(text: string) {
  if (!/^\\(?:\r?\n|\0)$/.test(text)) throw new Error(`Invalid line_continuation: '${text}' does not match grammar pattern`);
  return {
    type: 'line_continuation' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: LineContinuationTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function none_() {
  return {
    type: 'none' as const,
    text: 'None',
    render: () => 'None',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'None' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'None' };
    },
    replace: (target: NoneTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' }; },
  };
}

export function string_end_(text: string) {
  return {
    type: 'string_end' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringEndTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function string_start_(text: string) {
  return {
    type: 'string_start' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringStartTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    text: 'True',
    render: () => 'True',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'True' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'True' };
    },
    replace: (target: TrueTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' }; },
  };
}

export function type_conversion_(text: string) {
  if (!/^![a-z]$/.test(text)) throw new Error(`Invalid type_conversion: '${text}' does not match grammar pattern`);
  return {
    type: 'type_conversion' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: TypeConversionTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}


// Auto-generated by @sittir/codegen — do not edit

import type { NodeData } from './types.js';
import type { AliasedImport, AliasedImportTree, ArgumentList, ArgumentListTree, AsPattern, AsPatternTree, AssertStatement, AssertStatementTree, Assignment, AssignmentTree, AssignmentV0, AssignmentV0Config, AssignmentV1, AssignmentV1Config, AssignmentV2, AssignmentV2Config, Attribute, AttributeTree, AugmentedAssignment, AugmentedAssignmentTok_253d, AugmentedAssignmentTok_253dConfig, AugmentedAssignmentTok_263d, AugmentedAssignmentTok_263dConfig, AugmentedAssignmentTok_2a2a3d, AugmentedAssignmentTok_2a2a3dConfig, AugmentedAssignmentTok_2a3d, AugmentedAssignmentTok_2a3dConfig, AugmentedAssignmentTok_2b3d, AugmentedAssignmentTok_2b3dConfig, AugmentedAssignmentTok_2d3d, AugmentedAssignmentTok_2d3dConfig, AugmentedAssignmentTok_2f2f3d, AugmentedAssignmentTok_2f2f3dConfig, AugmentedAssignmentTok_2f3d, AugmentedAssignmentTok_2f3dConfig, AugmentedAssignmentTok_3c3c3d, AugmentedAssignmentTok_3c3c3dConfig, AugmentedAssignmentTok_3e3e3d, AugmentedAssignmentTok_3e3e3dConfig, AugmentedAssignmentTok_403d, AugmentedAssignmentTok_403dConfig, AugmentedAssignmentTok_5e3d, AugmentedAssignmentTok_5e3dConfig, AugmentedAssignmentTok_7c3d, AugmentedAssignmentTok_7c3dConfig, AugmentedAssignmentTree, Await, AwaitTree, BinaryOperator, BinaryOperatorAmp, BinaryOperatorAmpConfig, BinaryOperatorAt, BinaryOperatorAtConfig, BinaryOperatorCaret, BinaryOperatorCaretConfig, BinaryOperatorMinus, BinaryOperatorMinusConfig, BinaryOperatorPercent, BinaryOperatorPercentConfig, BinaryOperatorPipe, BinaryOperatorPipeConfig, BinaryOperatorPlus, BinaryOperatorPlusConfig, BinaryOperatorSlash, BinaryOperatorSlashConfig, BinaryOperatorStar, BinaryOperatorStarConfig, BinaryOperatorTok_2a2a, BinaryOperatorTok_2a2aConfig, BinaryOperatorTok_2f2f, BinaryOperatorTok_2f2fConfig, BinaryOperatorTok_3c3c, BinaryOperatorTok_3c3cConfig, BinaryOperatorTok_3e3e, BinaryOperatorTok_3e3eConfig, BinaryOperatorTree, Block, BlockTree, BooleanOperator, BooleanOperatorAnd, BooleanOperatorAndConfig, BooleanOperatorOr, BooleanOperatorOrConfig, BooleanOperatorTree, BreakStatement, BreakStatementTree, Call, CallTree, CaseClause, CaseClauseTree, CasePattern, CasePatternTree, Chevron, ChevronTree, ClassDefinition, ClassDefinitionTree, ClassPattern, ClassPatternTree, Comment, CommentTree, ComparisonOperator, ComparisonOperatorAngle, ComparisonOperatorAngleConfig, ComparisonOperatorCloseAngle, ComparisonOperatorCloseAngleConfig, ComparisonOperatorIn, ComparisonOperatorInConfig, ComparisonOperatorIs, ComparisonOperatorIsConfig, ComparisonOperatorTok_213d, ComparisonOperatorTok_213dConfig, ComparisonOperatorTok_3c3d, ComparisonOperatorTok_3c3dConfig, ComparisonOperatorTok_3c3e, ComparisonOperatorTok_3c3eConfig, ComparisonOperatorTok_3d3d, ComparisonOperatorTok_3d3dConfig, ComparisonOperatorTok_3e3d, ComparisonOperatorTok_3e3dConfig, ComparisonOperatorTree, ComparisonOperatorV8, ComparisonOperatorV8Config, ComplexPattern, ComplexPatternPlus, ComplexPatternPlusConfig, ComplexPatternTree, ComplexPatternV1, ComplexPatternV1Config, CompoundStatement, ComprehensionClauses, ConcatenatedString, ConcatenatedStringTree, ConditionalExpression, ConditionalExpressionTree, ConstrainedType, ConstrainedTypeTree, ContinueStatement, ContinueStatementTree, DecoratedDefinition, DecoratedDefinitionTree, Decorator, DecoratorTree, DefaultParameter, DefaultParameterTree, DeleteStatement, DeleteStatementTree, DictPattern, DictPatternTree, Dictionary, DictionaryComprehension, DictionaryComprehensionTree, DictionarySplat, DictionarySplatPattern, DictionarySplatPatternTree, DictionarySplatTree, DictionaryTree, DottedName, DottedNameTree, ElifClause, ElifClauseTree, Ellipsis, EllipsisTree, ElseClause, ElseClauseTree, EscapeInterpolation, EscapeInterpolationTree, EscapeSequence, EscapeSequenceTree, ExceptClause, ExceptClauseTree, ExecStatement, ExecStatementTree, Expression, ExpressionList, ExpressionListTree, ExpressionListV0, ExpressionListV0Config, ExpressionListV1, ExpressionListV1Config, ExpressionStatement, ExpressionStatementComma, ExpressionStatementCommaConfig, ExpressionStatementTree, ExpressionStatementV0, ExpressionStatementV0Config, ExpressionWithinForInClause, Expressions, FExpression, False, FalseTree, FinallyClause, FinallyClauseTree, Float, FloatTree, ForInClause, ForInClauseTree, ForStatement, ForStatementTree, FormatExpression, FormatExpressionTree, FormatSpecifier, FormatSpecifierTree, FunctionDefinition, FunctionDefinitionTree, FutureImportStatement, FutureImportStatementParen, FutureImportStatementParenConfig, FutureImportStatementTree, FutureImportStatementV0, FutureImportStatementV0Config, GeneratorExpression, GeneratorExpressionTree, GenericType, GenericTypeTree, GlobalStatement, GlobalStatementTree, Identifier, IdentifierTree, IfClause, IfClauseTree, IfStatement, IfStatementTree, ImportFromStatement, ImportFromStatementParen, ImportFromStatementParenConfig, ImportFromStatementTree, ImportFromStatementV0, ImportFromStatementV0Config, ImportFromStatementV1, ImportFromStatementV1Config, ImportPrefix, ImportPrefixTree, ImportStatement, ImportStatementTree, Integer, IntegerTree, Interpolation, InterpolationTree, KeywordArgument, KeywordArgumentTree, KeywordPattern, KeywordPatternTree, KeywordSeparator, KeywordSeparatorTree, Lambda, LambdaParameters, LambdaParametersTree, LambdaTree, LeftHandSide, LineContinuation, LineContinuationTree, List, ListComprehension, ListComprehensionTree, ListPattern, ListPatternTree, ListSplat, ListSplatPattern, ListSplatPatternTree, ListSplatTree, ListTree, MatchStatement, MatchStatementTree, MemberType, MemberTypeTree, Module, ModuleTree, NamedExpression, NamedExpressionLhs, NamedExpressionTree, None, NoneTree, NonlocalStatement, NonlocalStatementTree, NotOperator, NotOperatorTree, Pair, PairTree, Parameter, Parameters, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionTree, ParenthesizedListSplat, ParenthesizedListSplatTree, PassStatement, PassStatementTree, Pattern, PatternList, PatternListTree, PatternListV0, PatternListV0Config, PatternListV1, PatternListV1Config, PositionalSeparator, PositionalSeparatorTree, PrimaryExpression, PrintStatement, PrintStatementChevron, PrintStatementChevronConfig, PrintStatementTree, PrintStatementV1, PrintStatementV1Config, RaiseStatement, RaiseStatementTree, RelativeImport, RelativeImportTree, ReturnStatement, ReturnStatementTree, RightHandSide, Set, SetComprehension, SetComprehensionTree, SetTree, SimplePattern, SimpleStatement, Slice, SliceTree, SplatPattern, SplatPatternTree, SplatPatternV0, SplatPatternV0Config, SplatPatternV1, SplatPatternV1Config, SplatPatternV2, SplatPatternV2Config, SplatPatternV3, SplatPatternV3Config, SplatType, SplatTypeStar, SplatTypeStarConfig, SplatTypeTok_2a2a, SplatTypeTok_2a2aConfig, SplatTypeTree, String, StringContent, StringContentTree, StringEnd, StringEndTree, StringStart, StringStartTree, StringTree, Subscript, SubscriptTree, Suite, True, TrueTree, TryStatement, TryStatementTree, Tuple, TuplePattern, TuplePatternTree, TupleTree, Type, TypeAliasStatement, TypeAliasStatementTree, TypeConversion, TypeConversionTree, TypeParameter, TypeParameterTree, TypeTree, TypedDefaultParameter, TypedDefaultParameterTree, TypedParameter, TypedParameterTree, UnaryOperator, UnaryOperatorMinus, UnaryOperatorMinusConfig, UnaryOperatorPlus, UnaryOperatorPlusConfig, UnaryOperatorTilde, UnaryOperatorTildeConfig, UnaryOperatorTree, UnionPattern, UnionPatternTree, UnionType, UnionTypeTree, WhileStatement, WhileStatementTree, WildcardImport, WildcardImportTree, WithClause, WithClauseParen, WithClauseParenConfig, WithClauseTree, WithClauseV0, WithClauseV0Config, WithItem, WithItemTree, WithStatement, WithStatementTree, Yield, YieldFrom, YieldFromConfig, YieldTree, YieldV1, YieldV1Config } from './types.js';
import type { Edit, AnyNodeData, ConfigOf } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'case',
  'class',
  'continue',
  'def',
  'del',
  'elif',
  'else',
  'except',
  'exec',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'match',
  'nonlocal',
  'not',
  'or',
  'pass',
  'print',
  'raise',
  'return',
  'try',
  'while',
  'with',
]);

export function aliased_import_(
  config: ConfigOf<AliasedImport>,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  return {
    type: 'aliased_import' as const,
    named: true as const,
    fields,
    alias(alias?: Identifier) { return alias !== undefined ? aliased_import_({ ...config, alias: alias }) : fields.alias; },
    name(name?: DottedName) { return name !== undefined ? aliased_import_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AliasedImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function argument_list_(
  ...children: (DictionarySplat | Expression | KeywordArgument | ListSplat | ParenthesizedExpression)[]
) {
  return {
    type: 'argument_list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (DictionarySplat | Expression | KeywordArgument | ListSplat | ParenthesizedExpression)[]) { return argument_list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function as_pattern_(
  config: ConfigOf<AsPattern>,
) {
  const fields = {
    alias: config.alias,
    expression: config.expression,
  };
  return {
    type: 'as_pattern' as const,
    named: true as const,
    fields,
    alias(alias?: string) { return alias !== undefined ? as_pattern_({ ...config, alias: alias }) : fields.alias; },
    expression(expression?: CasePattern | Expression | Identifier) { return expression !== undefined ? as_pattern_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assert_statement_(
  ...children: (Expression)[]
) {
  return {
    type: 'assert_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression)[]) { return assert_statement_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_(
  config: ConfigOf<Assignment>,
) {
  return assignment__v2_(config as AssignmentV2Config);
}

/** Variant factory: `assignment` — v0 form. */
export function assignment__v0_(
  config: AssignmentV0Config,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? assignment__v0_({ ...config, left: left } as AssignmentV0Config) : fields.left; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? assignment__v0_({ ...config, right: right } as AssignmentV0Config) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `assignment` — v1 form. */
export function assignment__v1_(
  config: AssignmentV1Config,
) {
  const fields = {
    left: config.left,
    type: config.type,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? assignment__v1_({ ...config, left: left } as AssignmentV1Config) : fields.left; },
    typeField(type_?: Type) { return type_ !== undefined ? assignment__v1_({ ...config, type: type_ } as AssignmentV1Config) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `assignment` — v2 form. */
export function assignment__v2_(
  config: AssignmentV2Config,
) {
  const fields = {
    left: config.left,
    right: config.right,
    type: config.type,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: 'v2' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? assignment__v2_({ ...config, left: left } as AssignmentV2Config) : fields.left; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? assignment__v2_({ ...config, right: right } as AssignmentV2Config) : fields.right; },
    typeField(type_?: Type) { return type_ !== undefined ? assignment__v2_({ ...config, type: type_ } as AssignmentV2Config) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute_(
  config: ConfigOf<Attribute>,
) {
  const fields = {
    attribute: config.attribute,
    object: config.object,
  };
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    attribute(attribute?: Identifier) { return attribute !== undefined ? attribute_({ ...config, attribute: attribute }) : fields.attribute; },
    object(object?: PrimaryExpression) { return object !== undefined ? attribute_({ ...config, object: object }) : fields.object; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function augmented_assignment_(
  config: ConfigOf<AugmentedAssignment>,
) {
  return augmented_assignment__tok_2b3d_(config as AugmentedAssignmentTok_2b3dConfig);
}

/** Variant factory: `augmented_assignment` — tok_2b3d form. */
export function augmented_assignment__tok_2b3d_(
  config: AugmentedAssignmentTok_2b3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_2b3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_2b3d_({ ...config, left: left } as AugmentedAssignmentTok_2b3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_2b3d_({ ...config, operator: operator } as AugmentedAssignmentTok_2b3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_2b3d_({ ...config, right: right } as AugmentedAssignmentTok_2b3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_2d3d form. */
export function augmented_assignment__tok_2d3d_(
  config: AugmentedAssignmentTok_2d3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_2d3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_2d3d_({ ...config, left: left } as AugmentedAssignmentTok_2d3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_2d3d_({ ...config, operator: operator } as AugmentedAssignmentTok_2d3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_2d3d_({ ...config, right: right } as AugmentedAssignmentTok_2d3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_2a3d form. */
export function augmented_assignment__tok_2a3d_(
  config: AugmentedAssignmentTok_2a3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_2a3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_2a3d_({ ...config, left: left } as AugmentedAssignmentTok_2a3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_2a3d_({ ...config, operator: operator } as AugmentedAssignmentTok_2a3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_2a3d_({ ...config, right: right } as AugmentedAssignmentTok_2a3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_2f3d form. */
export function augmented_assignment__tok_2f3d_(
  config: AugmentedAssignmentTok_2f3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_2f3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_2f3d_({ ...config, left: left } as AugmentedAssignmentTok_2f3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_2f3d_({ ...config, operator: operator } as AugmentedAssignmentTok_2f3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_2f3d_({ ...config, right: right } as AugmentedAssignmentTok_2f3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_403d form. */
export function augmented_assignment__tok_403d_(
  config: AugmentedAssignmentTok_403dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_403d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_403d_({ ...config, left: left } as AugmentedAssignmentTok_403dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_403d_({ ...config, operator: operator } as AugmentedAssignmentTok_403dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_403d_({ ...config, right: right } as AugmentedAssignmentTok_403dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_2f2f3d form. */
export function augmented_assignment__tok_2f2f3d_(
  config: AugmentedAssignmentTok_2f2f3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_2f2f3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_2f2f3d_({ ...config, left: left } as AugmentedAssignmentTok_2f2f3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_2f2f3d_({ ...config, operator: operator } as AugmentedAssignmentTok_2f2f3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_2f2f3d_({ ...config, right: right } as AugmentedAssignmentTok_2f2f3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_253d form. */
export function augmented_assignment__tok_253d_(
  config: AugmentedAssignmentTok_253dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_253d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_253d_({ ...config, left: left } as AugmentedAssignmentTok_253dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_253d_({ ...config, operator: operator } as AugmentedAssignmentTok_253dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_253d_({ ...config, right: right } as AugmentedAssignmentTok_253dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_2a2a3d form. */
export function augmented_assignment__tok_2a2a3d_(
  config: AugmentedAssignmentTok_2a2a3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_2a2a3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_2a2a3d_({ ...config, left: left } as AugmentedAssignmentTok_2a2a3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_2a2a3d_({ ...config, operator: operator } as AugmentedAssignmentTok_2a2a3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_2a2a3d_({ ...config, right: right } as AugmentedAssignmentTok_2a2a3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_3e3e3d form. */
export function augmented_assignment__tok_3e3e3d_(
  config: AugmentedAssignmentTok_3e3e3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_3e3e3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_3e3e3d_({ ...config, left: left } as AugmentedAssignmentTok_3e3e3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_3e3e3d_({ ...config, operator: operator } as AugmentedAssignmentTok_3e3e3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_3e3e3d_({ ...config, right: right } as AugmentedAssignmentTok_3e3e3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_3c3c3d form. */
export function augmented_assignment__tok_3c3c3d_(
  config: AugmentedAssignmentTok_3c3c3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_3c3c3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_3c3c3d_({ ...config, left: left } as AugmentedAssignmentTok_3c3c3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_3c3c3d_({ ...config, operator: operator } as AugmentedAssignmentTok_3c3c3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_3c3c3d_({ ...config, right: right } as AugmentedAssignmentTok_3c3c3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_263d form. */
export function augmented_assignment__tok_263d_(
  config: AugmentedAssignmentTok_263dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_263d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_263d_({ ...config, left: left } as AugmentedAssignmentTok_263dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_263d_({ ...config, operator: operator } as AugmentedAssignmentTok_263dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_263d_({ ...config, right: right } as AugmentedAssignmentTok_263dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_5e3d form. */
export function augmented_assignment__tok_5e3d_(
  config: AugmentedAssignmentTok_5e3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_5e3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_5e3d_({ ...config, left: left } as AugmentedAssignmentTok_5e3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_5e3d_({ ...config, operator: operator } as AugmentedAssignmentTok_5e3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_5e3d_({ ...config, right: right } as AugmentedAssignmentTok_5e3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `augmented_assignment` — tok_7c3d form. */
export function augmented_assignment__tok_7c3d_(
  config: AugmentedAssignmentTok_7c3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    variant: 'tok_7c3d' as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? augmented_assignment__tok_7c3d_({ ...config, left: left } as AugmentedAssignmentTok_7c3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '**=' | '*=' | '+=' | '-=' | '//=' | '/=' | '<<=' | '>>=' | '@=' | '^=' | '|=') { return operator !== undefined ? augmented_assignment__tok_7c3d_({ ...config, operator: operator } as AugmentedAssignmentTok_7c3dConfig) : fields.operator; },
    right(right?: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | RightHandSide | Yield) { return right !== undefined ? augmented_assignment__tok_7c3d_({ ...config, right: right } as AugmentedAssignmentTok_7c3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function await_(
  config: ConfigOf<Await>,
) {
  const fields = {
    primary_expression: config.primaryExpression,
  };
  return {
    type: 'await' as const,
    named: true as const,
    fields,
    primaryExpression(primaryExpression?: PrimaryExpression) { return primaryExpression !== undefined ? await_({ ...config, primaryExpression: primaryExpression }) : fields.primary_expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function binary_operator_(
  config: ConfigOf<BinaryOperator>,
) {
  return binary_operator__plus_(config as BinaryOperatorPlusConfig);
}

/** Variant factory: `binary_operator` — plus form. */
export function binary_operator__plus_(
  config: BinaryOperatorPlusConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'plus' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__plus_({ ...config, left: left } as BinaryOperatorPlusConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__plus_({ ...config, operator: operator } as BinaryOperatorPlusConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__plus_({ ...config, right: right } as BinaryOperatorPlusConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — minus form. */
export function binary_operator__minus_(
  config: BinaryOperatorMinusConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'minus' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__minus_({ ...config, left: left } as BinaryOperatorMinusConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__minus_({ ...config, operator: operator } as BinaryOperatorMinusConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__minus_({ ...config, right: right } as BinaryOperatorMinusConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — star form. */
export function binary_operator__star_(
  config: BinaryOperatorStarConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'star' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__star_({ ...config, left: left } as BinaryOperatorStarConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__star_({ ...config, operator: operator } as BinaryOperatorStarConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__star_({ ...config, right: right } as BinaryOperatorStarConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — at form. */
export function binary_operator__at_(
  config: BinaryOperatorAtConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'at' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__at_({ ...config, left: left } as BinaryOperatorAtConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__at_({ ...config, operator: operator } as BinaryOperatorAtConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__at_({ ...config, right: right } as BinaryOperatorAtConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — slash form. */
export function binary_operator__slash_(
  config: BinaryOperatorSlashConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'slash' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__slash_({ ...config, left: left } as BinaryOperatorSlashConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__slash_({ ...config, operator: operator } as BinaryOperatorSlashConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__slash_({ ...config, right: right } as BinaryOperatorSlashConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — percent form. */
export function binary_operator__percent_(
  config: BinaryOperatorPercentConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'percent' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__percent_({ ...config, left: left } as BinaryOperatorPercentConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__percent_({ ...config, operator: operator } as BinaryOperatorPercentConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__percent_({ ...config, right: right } as BinaryOperatorPercentConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — tok_2f2f form. */
export function binary_operator__tok_2f2f_(
  config: BinaryOperatorTok_2f2fConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'tok_2f2f' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__tok_2f2f_({ ...config, left: left } as BinaryOperatorTok_2f2fConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__tok_2f2f_({ ...config, operator: operator } as BinaryOperatorTok_2f2fConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__tok_2f2f_({ ...config, right: right } as BinaryOperatorTok_2f2fConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — tok_2a2a form. */
export function binary_operator__tok_2a2a_(
  config: BinaryOperatorTok_2a2aConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'tok_2a2a' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__tok_2a2a_({ ...config, left: left } as BinaryOperatorTok_2a2aConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__tok_2a2a_({ ...config, operator: operator } as BinaryOperatorTok_2a2aConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__tok_2a2a_({ ...config, right: right } as BinaryOperatorTok_2a2aConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — pipe form. */
export function binary_operator__pipe_(
  config: BinaryOperatorPipeConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'pipe' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__pipe_({ ...config, left: left } as BinaryOperatorPipeConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__pipe_({ ...config, operator: operator } as BinaryOperatorPipeConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__pipe_({ ...config, right: right } as BinaryOperatorPipeConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — amp form. */
export function binary_operator__amp_(
  config: BinaryOperatorAmpConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'amp' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__amp_({ ...config, left: left } as BinaryOperatorAmpConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__amp_({ ...config, operator: operator } as BinaryOperatorAmpConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__amp_({ ...config, right: right } as BinaryOperatorAmpConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — caret form. */
export function binary_operator__caret_(
  config: BinaryOperatorCaretConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'caret' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__caret_({ ...config, left: left } as BinaryOperatorCaretConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__caret_({ ...config, operator: operator } as BinaryOperatorCaretConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__caret_({ ...config, right: right } as BinaryOperatorCaretConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — tok_3c3c form. */
export function binary_operator__tok_3c3c_(
  config: BinaryOperatorTok_3c3cConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'tok_3c3c' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__tok_3c3c_({ ...config, left: left } as BinaryOperatorTok_3c3cConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__tok_3c3c_({ ...config, operator: operator } as BinaryOperatorTok_3c3cConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__tok_3c3c_({ ...config, right: right } as BinaryOperatorTok_3c3cConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_operator` — tok_3e3e form. */
export function binary_operator__tok_3e3e_(
  config: BinaryOperatorTok_3e3eConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    variant: 'tok_3e3e' as const,
    fields,
    left(left?: PrimaryExpression) { return left !== undefined ? binary_operator__tok_3e3e_({ ...config, left: left } as BinaryOperatorTok_3e3eConfig) : fields.left; },
    operator(operator?: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') { return operator !== undefined ? binary_operator__tok_3e3e_({ ...config, operator: operator } as BinaryOperatorTok_3e3eConfig) : fields.operator; },
    right(right?: PrimaryExpression) { return right !== undefined ? binary_operator__tok_3e3e_({ ...config, right: right } as BinaryOperatorTok_3e3eConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block_(
  config?: ConfigOf<Block>,
) {
  const fields = {
    alternative: config?.alternative,
  };
  const children = config?.children ?? [];
  return {
    type: 'block' as const,
    named: true as const,
    fields,
    children,
    alternative(...alternative: (CaseClause)[]) { return alternative.length ? block_({ ...config, alternative: alternative }) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...children: (CompoundStatement | SimpleStatement)[]) { return block_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function boolean_operator_(
  config: ConfigOf<BooleanOperator>,
) {
  return boolean_operator__and_(config as BooleanOperatorAndConfig);
}

/** Variant factory: `boolean_operator` — and form. */
export function boolean_operator__and_(
  config: BooleanOperatorAndConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'boolean_operator' as const,
    named: true as const,
    variant: 'and' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? boolean_operator__and_({ ...config, left: left } as BooleanOperatorAndConfig) : fields.left; },
    operator(operator?: 'and' | 'or') { return operator !== undefined ? boolean_operator__and_({ ...config, operator: operator } as BooleanOperatorAndConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? boolean_operator__and_({ ...config, right: right } as BooleanOperatorAndConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `boolean_operator` — or form. */
export function boolean_operator__or_(
  config: BooleanOperatorOrConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'boolean_operator' as const,
    named: true as const,
    variant: 'or' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? boolean_operator__or_({ ...config, left: left } as BooleanOperatorOrConfig) : fields.left; },
    operator(operator?: 'and' | 'or') { return operator !== undefined ? boolean_operator__or_({ ...config, operator: operator } as BooleanOperatorOrConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? boolean_operator__or_({ ...config, right: right } as BooleanOperatorOrConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function call_(
  config: ConfigOf<Call>,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
  };
  return {
    type: 'call' as const,
    named: true as const,
    fields,
    arguments(arguments_?: ArgumentList | GeneratorExpression) { return arguments_ !== undefined ? call_({ ...config, arguments: arguments_ }) : fields.arguments; },
    function(function_?: PrimaryExpression) { return function_ !== undefined ? call_({ ...config, function: function_ }) : fields.function; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function case_clause_(
  config: ConfigOf<CaseClause>,
) {
  const fields = {
    consequence: config.consequence,
    guard: config.guard,
  };
  const children = config.children ?? [];
  return {
    type: 'case_clause' as const,
    named: true as const,
    fields,
    children,
    consequence(consequence?: Block) { return consequence !== undefined ? case_clause_({ ...config, consequence: consequence }) : fields.consequence; },
    guard(guard?: IfClause) { return guard !== undefined ? case_clause_({ ...config, guard: guard }) : fields.guard; },
    getChildren() { return children; },
    setChildren(...children: (CasePattern)[]) { return case_clause_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CaseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function case_pattern_(
  child: AsPattern | KeywordPattern | SimplePattern,
) {
  const children = child ? [child] : [];
  return {
    type: 'case_pattern' as const,
    named: true as const,
    children,
    child(c?: AsPattern | KeywordPattern | SimplePattern) { return c !== undefined ? case_pattern_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function chevron_(
  config: ConfigOf<Chevron>,
) {
  const fields = {
    expression: config.expression,
  };
  return {
    type: 'chevron' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? chevron_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ChevronTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_definition_(
  config: ConfigOf<ClassDefinition>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    superclasses: config.superclasses,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'class_definition' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? class_definition_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? class_definition_({ ...config, name: name }) : fields.name; },
    superclasses(superclasses?: ArgumentList) { return superclasses !== undefined ? class_definition_({ ...config, superclasses: superclasses }) : fields.superclasses; },
    typeParameters(typeParameters?: TypeParameter) { return typeParameters !== undefined ? class_definition_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function class_pattern_(
  config: ConfigOf<ClassPattern>,
) {
  const fields = {
    dotted_name: config.dottedName,
    arguments: config.arguments,
  };
  return {
    type: 'class_pattern' as const,
    named: true as const,
    fields,
    dottedName(dottedName?: DottedName) { return dottedName !== undefined ? class_pattern_({ ...config, dottedName: dottedName }) : fields.dotted_name; },
    arguments(...arguments_: (CasePattern)[]) { return arguments_.length ? class_pattern_({ ...config, arguments: arguments_ }) : fields.arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function comparison_operator_(
  config: ConfigOf<ComparisonOperator>,
) {
  return comparison_operator__angle_(config as ComparisonOperatorAngleConfig);
}

/** Variant factory: `comparison_operator` — angle form. */
export function comparison_operator__angle_(
  config: ComparisonOperatorAngleConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'angle' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__angle_({ ...config, operators: operators } as ComparisonOperatorAngleConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__angle_({ ...config, left: left } as ComparisonOperatorAngleConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__angle_({ ...config, comparators: comparators } as ComparisonOperatorAngleConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — tok_3c3d form. */
export function comparison_operator__tok_3c3d_(
  config: ComparisonOperatorTok_3c3dConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'tok_3c3d' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__tok_3c3d_({ ...config, operators: operators } as ComparisonOperatorTok_3c3dConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__tok_3c3d_({ ...config, left: left } as ComparisonOperatorTok_3c3dConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__tok_3c3d_({ ...config, comparators: comparators } as ComparisonOperatorTok_3c3dConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — tok_3d3d form. */
export function comparison_operator__tok_3d3d_(
  config: ComparisonOperatorTok_3d3dConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'tok_3d3d' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__tok_3d3d_({ ...config, operators: operators } as ComparisonOperatorTok_3d3dConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__tok_3d3d_({ ...config, left: left } as ComparisonOperatorTok_3d3dConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__tok_3d3d_({ ...config, comparators: comparators } as ComparisonOperatorTok_3d3dConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — tok_213d form. */
export function comparison_operator__tok_213d_(
  config: ComparisonOperatorTok_213dConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'tok_213d' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__tok_213d_({ ...config, operators: operators } as ComparisonOperatorTok_213dConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__tok_213d_({ ...config, left: left } as ComparisonOperatorTok_213dConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__tok_213d_({ ...config, comparators: comparators } as ComparisonOperatorTok_213dConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — tok_3e3d form. */
export function comparison_operator__tok_3e3d_(
  config: ComparisonOperatorTok_3e3dConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'tok_3e3d' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__tok_3e3d_({ ...config, operators: operators } as ComparisonOperatorTok_3e3dConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__tok_3e3d_({ ...config, left: left } as ComparisonOperatorTok_3e3dConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__tok_3e3d_({ ...config, comparators: comparators } as ComparisonOperatorTok_3e3dConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — close_angle form. */
export function comparison_operator__close_angle_(
  config: ComparisonOperatorCloseAngleConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'close_angle' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__close_angle_({ ...config, operators: operators } as ComparisonOperatorCloseAngleConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__close_angle_({ ...config, left: left } as ComparisonOperatorCloseAngleConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__close_angle_({ ...config, comparators: comparators } as ComparisonOperatorCloseAngleConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — tok_3c3e form. */
export function comparison_operator__tok_3c3e_(
  config: ComparisonOperatorTok_3c3eConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'tok_3c3e' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__tok_3c3e_({ ...config, operators: operators } as ComparisonOperatorTok_3c3eConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__tok_3c3e_({ ...config, left: left } as ComparisonOperatorTok_3c3eConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__tok_3c3e_({ ...config, comparators: comparators } as ComparisonOperatorTok_3c3eConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — in form. */
export function comparison_operator__in_(
  config: ComparisonOperatorInConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'in' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__in_({ ...config, operators: operators } as ComparisonOperatorInConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__in_({ ...config, left: left } as ComparisonOperatorInConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__in_({ ...config, comparators: comparators } as ComparisonOperatorInConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — v8 form. */
export function comparison_operator__v8_(
  config: ComparisonOperatorV8Config,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'v8' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__v8_({ ...config, operators: operators } as ComparisonOperatorV8Config) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__v8_({ ...config, left: left } as ComparisonOperatorV8Config) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__v8_({ ...config, comparators: comparators } as ComparisonOperatorV8Config) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `comparison_operator` — is form. */
export function comparison_operator__is_(
  config: ComparisonOperatorIsConfig,
) {
  const fields = {
    operators: config.operators,
    left: config.left,
    comparators: config.comparators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    variant: 'is' as const,
    fields,
    operators(...operators: ('!=' | '<' | '<=' | '<>' | '==' | '>' | '>=' | 'in' | 'is' | 'is not' | 'not in')[]) { return operators.length ? comparison_operator__is_({ ...config, operators: operators } as ComparisonOperatorIsConfig) : fields.operators; },
    left(left?: PrimaryExpression) { return left !== undefined ? comparison_operator__is_({ ...config, left: left } as ComparisonOperatorIsConfig) : fields.left; },
    comparators(...comparators: (PrimaryExpression)[]) { return comparators.length ? comparison_operator__is_({ ...config, comparators: comparators } as ComparisonOperatorIsConfig) : fields.comparators; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function complex_pattern_(
  config: ConfigOf<ComplexPattern>,
) {
  return complex_pattern__plus_(config as ComplexPatternPlusConfig);
}

/** Variant factory: `complex_pattern` — plus form. */
export function complex_pattern__plus_(
  config: ComplexPatternPlusConfig,
) {
  const fields = {
    real: config.real,
    imaginary: config.imaginary,
  };
  return {
    type: 'complex_pattern' as const,
    named: true as const,
    variant: 'plus' as const,
    fields,
    real(real?: Float | Integer) { return real !== undefined ? complex_pattern__plus_({ ...config, real: real } as ComplexPatternPlusConfig) : fields.real; },
    imaginary(imaginary?: Float | Integer) { return imaginary !== undefined ? complex_pattern__plus_({ ...config, imaginary: imaginary } as ComplexPatternPlusConfig) : fields.imaginary; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `complex_pattern` — v1 form. */
export function complex_pattern__v1_(
  config: ComplexPatternV1Config,
) {
  const fields = {
    real: config.real,
    imaginary: config.imaginary,
  };
  return {
    type: 'complex_pattern' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    real(real?: Float | Integer) { return real !== undefined ? complex_pattern__v1_({ ...config, real: real } as ComplexPatternV1Config) : fields.real; },
    imaginary(imaginary?: Float | Integer) { return imaginary !== undefined ? complex_pattern__v1_({ ...config, imaginary: imaginary } as ComplexPatternV1Config) : fields.imaginary; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function concatenated_string_(
  ...children: (String)[]
) {
  return {
    type: 'concatenated_string' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (String)[]) { return concatenated_string_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConcatenatedStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function conditional_expression_(
  config: ConfigOf<ConditionalExpression>,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
    alternative: config.alternative,
  };
  return {
    type: 'conditional_expression' as const,
    named: true as const,
    fields,
    body(body?: Expression) { return body !== undefined ? conditional_expression_({ ...config, body: body }) : fields.body; },
    condition(condition?: Expression) { return condition !== undefined ? conditional_expression_({ ...config, condition: condition }) : fields.condition; },
    alternative(alternative?: Expression) { return alternative !== undefined ? conditional_expression_({ ...config, alternative: alternative }) : fields.alternative; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constrained_type_(
  config: ConfigOf<ConstrainedType>,
) {
  const fields = {
    base_type: config.baseType,
    constraint: config.constraint,
  };
  return {
    type: 'constrained_type' as const,
    named: true as const,
    fields,
    baseType(baseType?: Type) { return baseType !== undefined ? constrained_type_({ ...config, baseType: baseType }) : fields.base_type; },
    constraint(constraint?: Type) { return constraint !== undefined ? constrained_type_({ ...config, constraint: constraint }) : fields.constraint; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstrainedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decorated_definition_(
  config: ConfigOf<DecoratedDefinition>,
) {
  const fields = {
    definition: config.definition,
  };
  const children = config.children ?? [];
  return {
    type: 'decorated_definition' as const,
    named: true as const,
    fields,
    children,
    definition(definition?: ClassDefinition | FunctionDefinition) { return definition !== undefined ? decorated_definition_({ ...config, definition: definition }) : fields.definition; },
    getChildren() { return children; },
    setChildren(...children: (Decorator)[]) { return decorated_definition_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratedDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decorator_(
  config: ConfigOf<Decorator>,
) {
  const fields = {
    expression: config.expression,
    newline: config.newline,
  };
  return {
    type: 'decorator' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? decorator_({ ...config, expression: expression }) : fields.expression; },
    newline(newline?: string) { return newline !== undefined ? decorator_({ ...config, newline: newline }) : fields.newline; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function default_parameter_(
  config: ConfigOf<DefaultParameter>,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'default_parameter' as const,
    named: true as const,
    fields,
    name(name?: Identifier | TuplePattern) { return name !== undefined ? default_parameter_({ ...config, name: name }) : fields.name; },
    value(value?: Expression) { return value !== undefined ? default_parameter_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function delete_statement_(
  child: Expression | ExpressionList,
) {
  const children = child ? [child] : [];
  return {
    type: 'delete_statement' as const,
    named: true as const,
    children,
    child(c?: Expression | ExpressionList) { return c !== undefined ? delete_statement_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeleteStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dict_pattern_(
  ...children: (SplatPattern)[]
) {
  return {
    type: 'dict_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (SplatPattern)[]) { return dict_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary_(
  ...children: (DictionarySplat | Pair)[]
) {
  return {
    type: 'dictionary' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (DictionarySplat | Pair)[]) { return dictionary_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary_comprehension_(
  config: ConfigOf<DictionaryComprehension>,
) {
  const fields = {
    body: config.body,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'dictionary_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body?: Pair) { return body !== undefined ? dictionary_comprehension_({ ...config, body: body }) : fields.body; },
    child(child?: ForInClause | IfClause) { return child !== undefined ? dictionary_comprehension_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary_splat_(
  config: ConfigOf<DictionarySplat>,
) {
  const fields = {
    expression: config.expression,
  };
  return {
    type: 'dictionary_splat' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? dictionary_splat_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary_splat_pattern_(
  child: Attribute | Identifier | Subscript,
) {
  const children = child ? [child] : [];
  return {
    type: 'dictionary_splat_pattern' as const,
    named: true as const,
    children,
    child(c?: Attribute | Identifier | Subscript) { return c !== undefined ? dictionary_splat_pattern_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dotted_name_(
  ...children: (Identifier)[]
) {
  return {
    type: 'dotted_name' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Identifier)[]) { return dotted_name_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DottedNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function elif_clause_(
  config: ConfigOf<ElifClause>,
) {
  const fields = {
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'elif_clause' as const,
    named: true as const,
    fields,
    condition(condition?: Expression) { return condition !== undefined ? elif_clause_({ ...config, condition: condition }) : fields.condition; },
    consequence(consequence?: Block) { return consequence !== undefined ? elif_clause_({ ...config, consequence: consequence }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElifClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function else_clause_(
  config: ConfigOf<ElseClause>,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? else_clause_({ ...config, body: body }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function except_clause_(
  config: ConfigOf<ExceptClause>,
) {
  const fields = {
    alias: config.alias,
    value: config.value,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'except_clause' as const,
    named: true as const,
    fields,
    children,
    alias(alias?: Expression) { return alias !== undefined ? except_clause_({ ...config, alias: alias }) : fields.alias; },
    value(...value: (Expression)[]) { return value.length ? except_clause_({ ...config, value: value }) : fields.value; },
    child(child?: Block) { return child !== undefined ? except_clause_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExceptClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function exec_statement_(
  config: ConfigOf<ExecStatement>,
) {
  const fields = {
    code: config.code,
  };
  const children = config.children ?? [];
  return {
    type: 'exec_statement' as const,
    named: true as const,
    fields,
    children,
    code(code?: Identifier | String) { return code !== undefined ? exec_statement_({ ...config, code: code }) : fields.code; },
    getChildren() { return children; },
    setChildren(...children: (Expression)[]) { return exec_statement_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExecStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expression_list_(
  ...children: (Expression)[]
) {
  return {
    type: 'expression_list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression)[]) { return expression_list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `expression_list` — v0 form. */
export function expression_list__v0_(
  config?: ExpressionListV0Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'expression_list' as const,
    named: true as const,
    variant: 'v0' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression)[]) { return expression_list__v0_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `expression_list` — v1 form. */
export function expression_list__v1_(
  config?: ExpressionListV1Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'expression_list' as const,
    named: true as const,
    variant: 'v1' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression)[]) { return expression_list__v1_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expression_statement_(
  ...children: (Assignment | AugmentedAssignment | Expression | Yield)[]
) {
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Assignment | AugmentedAssignment | Expression | Yield)[]) { return expression_statement_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `expression_statement` — v0 form. */
export function expression_statement__v0_(
  config?: ExpressionStatementV0Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    variant: 'v0' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Assignment | AugmentedAssignment | Expression | Yield)[]) { return expression_statement__v0_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `expression_statement` — comma form. */
export function expression_statement__comma_(
  config?: ExpressionStatementCommaConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    variant: 'comma' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Assignment | AugmentedAssignment | Expression | Yield)[]) { return expression_statement__comma_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finally_clause_(
  config: ConfigOf<FinallyClause>,
) {
  const fields = {
    block: config.block,
  };
  const children: unknown[] = [];
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    children,
    block(block?: Block) { return block !== undefined ? finally_clause_({ ...config, block: block }) : fields.block; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_in_clause_(
  config: ConfigOf<ForInClause>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'for_in_clause' as const,
    named: true as const,
    fields,
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? for_in_clause_({ ...config, left: left }) : fields.left; },
    right(...right: (Expression | ExpressionWithinForInClause)[]) { return right.length ? for_in_clause_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_statement_(
  config: ConfigOf<ForStatement>,
) {
  const fields = {
    alternative: config.alternative,
    body: config.body,
    left: config.left,
    right: config.right,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    alternative(alternative?: ElseClause) { return alternative !== undefined ? for_statement_({ ...config, alternative: alternative }) : fields.alternative; },
    body(body?: Block) { return body !== undefined ? for_statement_({ ...config, body: body }) : fields.body; },
    left(left?: LeftHandSide | Pattern | PatternList) { return left !== undefined ? for_statement_({ ...config, left: left }) : fields.left; },
    right(right?: Expression | ExpressionList | Expressions) { return right !== undefined ? for_statement_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function format_expression_(
  config: ConfigOf<FormatExpression>,
) {
  const fields = {
    expression: config.expression,
    format_specifier: config.formatSpecifier,
    type_conversion: config.typeConversion,
  };
  return {
    type: 'format_expression' as const,
    named: true as const,
    fields,
    expression(expression?: Expression | ExpressionList | PatternList | Yield) { return expression !== undefined ? format_expression_({ ...config, expression: expression }) : fields.expression; },
    formatSpecifier(formatSpecifier?: FormatSpecifier) { return formatSpecifier !== undefined ? format_expression_({ ...config, formatSpecifier: formatSpecifier }) : fields.format_specifier; },
    typeConversion(typeConversion?: TypeConversion) { return typeConversion !== undefined ? format_expression_({ ...config, typeConversion: typeConversion }) : fields.type_conversion; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function format_specifier_(
  ...children: (FormatExpression)[]
) {
  return {
    type: 'format_specifier' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (FormatExpression)[]) { return format_specifier_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_definition_(
  config: ConfigOf<FunctionDefinition>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'function_definition' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? function_definition_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? function_definition_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: Parameters) { return parameters !== undefined ? function_definition_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: Type) { return returnType !== undefined ? function_definition_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameter) { return typeParameters !== undefined ? function_definition_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function future_import_statement_(
  config: ConfigOf<FutureImportStatement>,
) {
  return future_import_statement__v0_(config as FutureImportStatementV0Config);
}

/** Variant factory: `future_import_statement` — v0 form. */
export function future_import_statement__v0_(
  config: FutureImportStatementV0Config,
) {
  const fields = {
    name: config.name,
  };
  return {
    type: 'future_import_statement' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    name(...name: (AliasedImport | DottedName)[]) { return name.length ? future_import_statement__v0_({ ...config, name: name } as FutureImportStatementV0Config) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `future_import_statement` — paren form. */
export function future_import_statement__paren_(
  config: FutureImportStatementParenConfig,
) {
  const fields = {
    name: config.name,
  };
  return {
    type: 'future_import_statement' as const,
    named: true as const,
    variant: 'paren' as const,
    fields,
    name(...name: (AliasedImport | DottedName)[]) { return name.length ? future_import_statement__paren_({ ...config, name: name } as FutureImportStatementParenConfig) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generator_expression_(
  config: ConfigOf<GeneratorExpression>,
) {
  const fields = {
    body: config.body,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'generator_expression' as const,
    named: true as const,
    fields,
    children,
    body(body?: Expression) { return body !== undefined ? generator_expression_({ ...config, body: body }) : fields.body; },
    child(child?: ForInClause | IfClause) { return child !== undefined ? generator_expression_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_(
  config: ConfigOf<GenericType>,
) {
  const fields = {
    identifier: config.identifier,
    type_parameter: config.typeParameter,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? generic_type_({ ...config, identifier: identifier }) : fields.identifier; },
    typeParameter(typeParameter?: TypeParameter) { return typeParameter !== undefined ? generic_type_({ ...config, typeParameter: typeParameter }) : fields.type_parameter; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function global_statement_(
  ...children: (Identifier)[]
) {
  return {
    type: 'global_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Identifier)[]) { return global_statement_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GlobalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_clause_(
  config: ConfigOf<IfClause>,
) {
  const fields = {
    expression: config.expression,
  };
  return {
    type: 'if_clause' as const,
    named: true as const,
    fields,
    expression(expression?: Expression) { return expression !== undefined ? if_clause_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_statement_(
  config: ConfigOf<IfStatement>,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    alternative(...alternative: (ElifClause | ElseClause)[]) { return alternative.length ? if_statement_({ ...config, alternative: alternative }) : fields.alternative; },
    condition(condition?: Expression) { return condition !== undefined ? if_statement_({ ...config, condition: condition }) : fields.condition; },
    consequence(consequence?: Block) { return consequence !== undefined ? if_statement_({ ...config, consequence: consequence }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function import_from_statement_(
  config: ConfigOf<ImportFromStatement>,
) {
  return import_from_statement__v1_(config as ImportFromStatementV1Config);
}

/** Variant factory: `import_from_statement` — v0 form. */
export function import_from_statement__v0_(
  config: ImportFromStatementV0Config,
) {
  const fields = {
    module_name: config.moduleName,
    wildcard_import: config.wildcardImport,
  };
  return {
    type: 'import_from_statement' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    moduleName(moduleName?: DottedName | RelativeImport) { return moduleName !== undefined ? import_from_statement__v0_({ ...config, moduleName: moduleName } as ImportFromStatementV0Config) : fields.module_name; },
    wildcardImport(wildcardImport?: WildcardImport) { return wildcardImport !== undefined ? import_from_statement__v0_({ ...config, wildcardImport: wildcardImport } as ImportFromStatementV0Config) : fields.wildcard_import; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `import_from_statement` — v1 form. */
export function import_from_statement__v1_(
  config: ImportFromStatementV1Config,
) {
  const fields = {
    module_name: config.moduleName,
    name: config.name,
    wildcard_import: config.wildcardImport,
  };
  return {
    type: 'import_from_statement' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    moduleName(moduleName?: DottedName | RelativeImport) { return moduleName !== undefined ? import_from_statement__v1_({ ...config, moduleName: moduleName } as ImportFromStatementV1Config) : fields.module_name; },
    name(...name: (AliasedImport | DottedName)[]) { return name.length ? import_from_statement__v1_({ ...config, name: name } as ImportFromStatementV1Config) : fields.name; },
    wildcardImport(wildcardImport?: WildcardImport) { return wildcardImport !== undefined ? import_from_statement__v1_({ ...config, wildcardImport: wildcardImport } as ImportFromStatementV1Config) : fields.wildcard_import; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `import_from_statement` — paren form. */
export function import_from_statement__paren_(
  config: ImportFromStatementParenConfig,
) {
  const fields = {
    module_name: config.moduleName,
    name: config.name,
    wildcard_import: config.wildcardImport,
  };
  return {
    type: 'import_from_statement' as const,
    named: true as const,
    variant: 'paren' as const,
    fields,
    moduleName(moduleName?: DottedName | RelativeImport) { return moduleName !== undefined ? import_from_statement__paren_({ ...config, moduleName: moduleName } as ImportFromStatementParenConfig) : fields.module_name; },
    name(...name: (AliasedImport | DottedName)[]) { return name.length ? import_from_statement__paren_({ ...config, name: name } as ImportFromStatementParenConfig) : fields.name; },
    wildcardImport(wildcardImport?: WildcardImport) { return wildcardImport !== undefined ? import_from_statement__paren_({ ...config, wildcardImport: wildcardImport } as ImportFromStatementParenConfig) : fields.wildcard_import; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function import_statement_(
  config: ConfigOf<ImportStatement>,
) {
  const fields = {
    name: config.name,
  };
  return {
    type: 'import_statement' as const,
    named: true as const,
    fields,
    name(...name: (AliasedImport | DottedName)[]) { return name.length ? import_statement_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interpolation_(
  config: ConfigOf<Interpolation>,
) {
  const fields = {
    expression: config.expression,
    format_specifier: config.formatSpecifier,
    type_conversion: config.typeConversion,
  };
  return {
    type: 'interpolation' as const,
    named: true as const,
    fields,
    expression(expression?: Expression | ExpressionList | FExpression | PatternList | Yield) { return expression !== undefined ? interpolation_({ ...config, expression: expression }) : fields.expression; },
    formatSpecifier(formatSpecifier?: FormatSpecifier) { return formatSpecifier !== undefined ? interpolation_({ ...config, formatSpecifier: formatSpecifier }) : fields.format_specifier; },
    typeConversion(typeConversion?: TypeConversion) { return typeConversion !== undefined ? interpolation_({ ...config, typeConversion: typeConversion }) : fields.type_conversion; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterpolationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function keyword_argument_(
  config: ConfigOf<KeywordArgument>,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'keyword_argument' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? keyword_argument_({ ...config, name: name }) : fields.name; },
    value(value?: Expression) { return value !== undefined ? keyword_argument_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordArgumentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function keyword_pattern_(
  config: ConfigOf<KeywordPattern>,
) {
  const fields = {
    identifier: config.identifier,
    simple_pattern: config.simplePattern,
  };
  return {
    type: 'keyword_pattern' as const,
    named: true as const,
    fields,
    identifier(identifier?: Identifier | SimplePattern) { return identifier !== undefined ? keyword_pattern_({ ...config, identifier: identifier }) : fields.identifier; },
    simplePattern(simplePattern?: SimplePattern) { return simplePattern !== undefined ? keyword_pattern_({ ...config, simplePattern: simplePattern }) : fields.simple_pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lambda_(
  config: ConfigOf<Lambda>,
) {
  const fields = {
    body: config.body,
    parameters: config.parameters,
  };
  return {
    type: 'lambda' as const,
    named: true as const,
    fields,
    body(body?: Expression) { return body !== undefined ? lambda_({ ...config, body: body }) : fields.body; },
    parameters(parameters?: LambdaParameters) { return parameters !== undefined ? lambda_({ ...config, parameters: parameters }) : fields.parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lambda_parameters_(
  ...children: (Parameter)[]
) {
  return {
    type: 'lambda_parameters' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Parameter)[]) { return lambda_parameters_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_(
  ...children: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]
) {
  return {
    type: 'list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]) { return list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_comprehension_(
  config: ConfigOf<ListComprehension>,
) {
  const fields = {
    body: config.body,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'list_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body?: Expression) { return body !== undefined ? list_comprehension_({ ...config, body: body }) : fields.body; },
    child(child?: ForInClause | IfClause) { return child !== undefined ? list_comprehension_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_pattern_(
  ...children: (CasePattern | Pattern)[]
) {
  return {
    type: 'list_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (CasePattern | Pattern)[]) { return list_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_splat_(
  config: ConfigOf<ListSplat>,
) {
  const fields = {
    expression: config.expression,
  };
  return {
    type: 'list_splat' as const,
    named: true as const,
    fields,
    expression(expression?: Attribute | Expression | Identifier | Subscript) { return expression !== undefined ? list_splat_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list_splat_pattern_(
  child: Attribute | Identifier | Subscript,
) {
  const children = child ? [child] : [];
  return {
    type: 'list_splat_pattern' as const,
    named: true as const,
    children,
    child(c?: Attribute | Identifier | Subscript) { return c !== undefined ? list_splat_pattern_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_statement_(
  config: ConfigOf<MatchStatement>,
) {
  const fields = {
    body: config.body,
    subject: config.subject,
  };
  return {
    type: 'match_statement' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? match_statement_({ ...config, body: body }) : fields.body; },
    subject(...subject: (Expression)[]) { return subject.length ? match_statement_({ ...config, subject: subject }) : fields.subject; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function member_type_(
  config: ConfigOf<MemberType>,
) {
  const fields = {
    base_type: config.baseType,
    identifier: config.identifier,
  };
  return {
    type: 'member_type' as const,
    named: true as const,
    fields,
    baseType(baseType?: Type) { return baseType !== undefined ? member_type_({ ...config, baseType: baseType }) : fields.base_type; },
    identifier(identifier?: Identifier) { return identifier !== undefined ? member_type_({ ...config, identifier: identifier }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function module_(
  ...children: (CompoundStatement | SimpleStatement)[]
) {
  return {
    type: 'module' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (CompoundStatement | SimpleStatement)[]) { return module_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function named_expression_(
  config: ConfigOf<NamedExpression>,
) {
  const fields = {
    name: config.name,
    value: config.value,
  };
  return {
    type: 'named_expression' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? named_expression_({ ...config, name: name }) : fields.name; },
    value(value?: Expression) { return value !== undefined ? named_expression_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nonlocal_statement_(
  ...children: (Identifier)[]
) {
  return {
    type: 'nonlocal_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Identifier)[]) { return nonlocal_statement_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonlocalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function not_operator_(
  config: ConfigOf<NotOperator>,
) {
  const fields = {
    argument: config.argument,
  };
  return {
    type: 'not_operator' as const,
    named: true as const,
    fields,
    argument(argument?: Expression) { return argument !== undefined ? not_operator_({ ...config, argument: argument }) : fields.argument; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NotOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair_(
  config: ConfigOf<Pair>,
) {
  const fields = {
    key: config.key,
    value: config.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key?: Expression) { return key !== undefined ? pair_({ ...config, key: key }) : fields.key; },
    value(value?: Expression) { return value !== undefined ? pair_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parameters_(
  ...children: (Parameter)[]
) {
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Parameter)[]) { return parameters_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_expression_(
  child: Expression | ListSplat | ParenthesizedExpression | Yield,
) {
  const children = child ? [child] : [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    child(c?: Expression | ListSplat | ParenthesizedExpression | Yield) { return c !== undefined ? parenthesized_expression_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_list_splat_(
  child: ListSplat | ParenthesizedExpression,
) {
  const children = child ? [child] : [];
  return {
    type: 'parenthesized_list_splat' as const,
    named: true as const,
    children,
    child(c?: ListSplat | ParenthesizedExpression) { return c !== undefined ? parenthesized_list_splat_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pattern_list_(
  ...children: (Pattern)[]
) {
  return {
    type: 'pattern_list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Pattern)[]) { return pattern_list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `pattern_list` — v0 form. */
export function pattern_list__v0_(
  config?: PatternListV0Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'pattern_list' as const,
    named: true as const,
    variant: 'v0' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Pattern)[]) { return pattern_list__v0_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `pattern_list` — v1 form. */
export function pattern_list__v1_(
  config?: PatternListV1Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'pattern_list' as const,
    named: true as const,
    variant: 'v1' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Pattern)[]) { return pattern_list__v1_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function print_statement_(
  config?: ConfigOf<PrintStatement>,
) {
  if (config && 'chevron' in config && config.chevron !== undefined) return print_statement__chevron_(config as PrintStatementChevronConfig);
  return print_statement__v1_(config as PrintStatementV1Config);
}

/** Variant factory: `print_statement` — chevron form. */
export function print_statement__chevron_(
  config?: PrintStatementChevronConfig,
) {
  const fields = {
    argument: config?.argument,
    chevron: config?.chevron,
  };
  return {
    type: 'print_statement' as const,
    named: true as const,
    variant: 'chevron' as const,
    fields,
    argument(...argument: (Expression)[]) { return argument.length ? print_statement__chevron_({ ...config, argument: argument } as PrintStatementChevronConfig) : fields.argument; },
    chevron(chevron?: Chevron) { return chevron !== undefined ? print_statement__chevron_({ ...config, chevron: chevron } as PrintStatementChevronConfig) : fields.chevron; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `print_statement` — v1 form. */
export function print_statement__v1_(
  config?: PrintStatementV1Config,
) {
  const fields = {
    argument: config?.argument,
  };
  return {
    type: 'print_statement' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    argument(...argument: (Expression)[]) { return argument.length ? print_statement__v1_({ ...config, argument: argument } as PrintStatementV1Config) : fields.argument; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function raise_statement_(
  config?: ConfigOf<RaiseStatement>,
) {
  const fields = {
    cause: config?.cause,
  };
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'raise_statement' as const,
    named: true as const,
    fields,
    children,
    cause(cause?: Expression) { return cause !== undefined ? raise_statement_({ ...config, cause: cause }) : fields.cause; },
    child(child?: Expression | ExpressionList | Expressions) { return child !== undefined ? raise_statement_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RaiseStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function relative_import_(
  config: ConfigOf<RelativeImport>,
) {
  const fields = {
    import_prefix: config.importPrefix,
    dotted_name: config.dottedName,
  };
  return {
    type: 'relative_import' as const,
    named: true as const,
    fields,
    importPrefix(importPrefix?: ImportPrefix) { return importPrefix !== undefined ? relative_import_({ ...config, importPrefix: importPrefix }) : fields.import_prefix; },
    dottedName(dottedName?: DottedName) { return dottedName !== undefined ? relative_import_({ ...config, dottedName: dottedName }) : fields.dotted_name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RelativeImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function return_statement_(
  child?: Expression | ExpressionList,
) {
  const children = child ? [child] : [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    child(c?: Expression | ExpressionList) { return c !== undefined ? return_statement_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function set_(
  ...children: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]
) {
  return {
    type: 'set' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]) { return set_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function set_comprehension_(
  config: ConfigOf<SetComprehension>,
) {
  const fields = {
    body: config.body,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'set_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body?: Expression) { return body !== undefined ? set_comprehension_({ ...config, body: body }) : fields.body; },
    child(child?: ForInClause | IfClause) { return child !== undefined ? set_comprehension_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function slice_(
  config?: ConfigOf<Slice>,
) {
  const fields = {
    start: config?.start,
    stop: config?.stop,
    step: config?.step,
  };
  return {
    type: 'slice' as const,
    named: true as const,
    fields,
    start(start?: Expression) { return start !== undefined ? slice_({ ...config, start: start }) : fields.start; },
    stop(stop?: Expression) { return stop !== undefined ? slice_({ ...config, stop: stop }) : fields.stop; },
    step(step?: Expression) { return step !== undefined ? slice_({ ...config, step: step }) : fields.step; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SliceTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function splat_pattern_(
  config?: ConfigOf<SplatPattern>,
) {
  return splat_pattern__v0_(config as SplatPatternV0Config);
}

/** Variant factory: `splat_pattern` — v0 form. */
export function splat_pattern__v0_(
  config?: SplatPatternV0Config,
) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'splat_pattern' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? splat_pattern__v0_({ ...config, identifier: identifier } as SplatPatternV0Config) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `splat_pattern` — v1 form. */
export function splat_pattern__v1_(
  config?: SplatPatternV1Config,
) {
  return {
    type: 'splat_pattern' as const,
    named: true as const,
    variant: 'v1' as const,
    fields: {},
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `splat_pattern` — v2 form. */
export function splat_pattern__v2_(
  config?: SplatPatternV2Config,
) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'splat_pattern' as const,
    named: true as const,
    variant: 'v2' as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? splat_pattern__v2_({ ...config, identifier: identifier } as SplatPatternV2Config) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `splat_pattern` — v3 form. */
export function splat_pattern__v3_(
  config?: SplatPatternV3Config,
) {
  return {
    type: 'splat_pattern' as const,
    named: true as const,
    variant: 'v3' as const,
    fields: {},
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splat_type_(
  config: ConfigOf<SplatType>,
) {
  return splat_type__star_(config as SplatTypeStarConfig);
}

/** Variant factory: `splat_type` — star form. */
export function splat_type__star_(
  config: SplatTypeStarConfig,
) {
  const fields = {
    identifier: config.identifier,
  };
  return {
    type: 'splat_type' as const,
    named: true as const,
    variant: 'star' as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? splat_type__star_({ ...config, identifier: identifier } as SplatTypeStarConfig) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `splat_type` — tok_2a2a form. */
export function splat_type__tok_2a2a_(
  config: SplatTypeTok_2a2aConfig,
) {
  const fields = {
    identifier: config.identifier,
  };
  return {
    type: 'splat_type' as const,
    named: true as const,
    variant: 'tok_2a2a' as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? splat_type__tok_2a2a_({ ...config, identifier: identifier } as SplatTypeTok_2a2aConfig) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string_(
  config: ConfigOf<String>,
) {
  const fields = {
    string_start: config.stringStart,
    content: config.content,
    string_end: config.stringEnd,
  };
  return {
    type: 'string' as const,
    named: true as const,
    fields,
    stringStart(stringStart?: StringStart) { return stringStart !== undefined ? string_({ ...config, stringStart: stringStart }) : fields.string_start; },
    content(...content: (Interpolation | StringContent)[]) { return content.length ? string_({ ...config, content: content }) : fields.content; },
    stringEnd(stringEnd?: StringEnd) { return stringEnd !== undefined ? string_({ ...config, stringEnd: stringEnd }) : fields.string_end; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string_content_(
  ...children: (EscapeInterpolation | EscapeSequence)[]
) {
  return {
    type: 'string_content' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (EscapeInterpolation | EscapeSequence)[]) { return string_content_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringContentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function subscript_(
  config: ConfigOf<Subscript>,
) {
  const fields = {
    subscript: config.subscript,
    value: config.value,
  };
  return {
    type: 'subscript' as const,
    named: true as const,
    fields,
    subscript(...subscript: (Expression | Slice)[]) { return subscript.length ? subscript_({ ...config, subscript: subscript }) : fields.subscript; },
    value(value?: PrimaryExpression) { return value !== undefined ? subscript_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_statement_(
  config: ConfigOf<TryStatement>,
) {
  const fields = {
    body: config.body,
    except_clauses: config.exceptClauses,
    else_clause: config.elseClause,
    finally_clause: config.finallyClause,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? try_statement_({ ...config, body: body }) : fields.body; },
    exceptClauses(...exceptClauses: (ExceptClause)[]) { return exceptClauses.length ? try_statement_({ ...config, exceptClauses: exceptClauses }) : fields.except_clauses; },
    elseClause(elseClause?: ElseClause) { return elseClause !== undefined ? try_statement_({ ...config, elseClause: elseClause }) : fields.else_clause; },
    finallyClause(finallyClause?: FinallyClause) { return finallyClause !== undefined ? try_statement_({ ...config, finallyClause: finallyClause }) : fields.finally_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_(
  ...children: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]
) {
  return {
    type: 'tuple' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]) { return tuple_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_pattern_(
  ...children: (CasePattern | Pattern)[]
) {
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (CasePattern | Pattern)[]) { return tuple_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_(
  child: ConstrainedType | Expression | GenericType | MemberType | SplatType | UnionType,
) {
  const children = child ? [child] : [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    child(c?: ConstrainedType | Expression | GenericType | MemberType | SplatType | UnionType) { return c !== undefined ? type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_alias_statement_(
  config: ConfigOf<TypeAliasStatement>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'type_alias_statement' as const,
    named: true as const,
    fields,
    left(left?: Type) { return left !== undefined ? type_alias_statement_({ ...config, left: left }) : fields.left; },
    right(right?: Type) { return right !== undefined ? type_alias_statement_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameter_(
  ...children: (Type)[]
) {
  return {
    type: 'type_parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Type)[]) { return type_parameter_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function typed_default_parameter_(
  config: ConfigOf<TypedDefaultParameter>,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  return {
    type: 'typed_default_parameter' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? typed_default_parameter_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? typed_default_parameter_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? typed_default_parameter_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedDefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function typed_parameter_(
  config: ConfigOf<TypedParameter>,
) {
  const fields = {
    type: config.type,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'typed_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type_?: Type) { return type_ !== undefined ? typed_parameter_({ ...config, type: type_ }) : fields.type; },
    child(child?: DictionarySplatPattern | Identifier | ListSplatPattern) { return child !== undefined ? typed_parameter_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unary_operator_(
  config: ConfigOf<UnaryOperator>,
) {
  return unary_operator__plus_(config as UnaryOperatorPlusConfig);
}

/** Variant factory: `unary_operator` — plus form. */
export function unary_operator__plus_(
  config: UnaryOperatorPlusConfig,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'unary_operator' as const,
    named: true as const,
    variant: 'plus' as const,
    fields,
    argument(argument?: PrimaryExpression) { return argument !== undefined ? unary_operator__plus_({ ...config, argument: argument } as UnaryOperatorPlusConfig) : fields.argument; },
    operator(operator?: '+' | '-' | '~') { return operator !== undefined ? unary_operator__plus_({ ...config, operator: operator } as UnaryOperatorPlusConfig) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `unary_operator` — minus form. */
export function unary_operator__minus_(
  config: UnaryOperatorMinusConfig,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'unary_operator' as const,
    named: true as const,
    variant: 'minus' as const,
    fields,
    argument(argument?: PrimaryExpression) { return argument !== undefined ? unary_operator__minus_({ ...config, argument: argument } as UnaryOperatorMinusConfig) : fields.argument; },
    operator(operator?: '+' | '-' | '~') { return operator !== undefined ? unary_operator__minus_({ ...config, operator: operator } as UnaryOperatorMinusConfig) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `unary_operator` — tilde form. */
export function unary_operator__tilde_(
  config: UnaryOperatorTildeConfig,
) {
  const fields = {
    argument: config.argument,
    operator: config.operator,
  };
  return {
    type: 'unary_operator' as const,
    named: true as const,
    variant: 'tilde' as const,
    fields,
    argument(argument?: PrimaryExpression) { return argument !== undefined ? unary_operator__tilde_({ ...config, argument: argument } as UnaryOperatorTildeConfig) : fields.argument; },
    operator(operator?: '+' | '-' | '~') { return operator !== undefined ? unary_operator__tilde_({ ...config, operator: operator } as UnaryOperatorTildeConfig) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function union_pattern_(
  ...children: (SimplePattern)[]
) {
  return {
    type: 'union_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (SimplePattern)[]) { return union_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function union_type_(
  config: ConfigOf<UnionType>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left?: Type) { return left !== undefined ? union_type_({ ...config, left: left }) : fields.left; },
    right(right?: Type) { return right !== undefined ? union_type_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function while_statement_(
  config: ConfigOf<WhileStatement>,
) {
  const fields = {
    alternative: config.alternative,
    body: config.body,
    condition: config.condition,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    alternative(alternative?: ElseClause) { return alternative !== undefined ? while_statement_({ ...config, alternative: alternative }) : fields.alternative; },
    body(body?: Block) { return body !== undefined ? while_statement_({ ...config, body: body }) : fields.body; },
    condition(condition?: Expression) { return condition !== undefined ? while_statement_({ ...config, condition: condition }) : fields.condition; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function with_clause_(
  ...children: (WithItem)[]
) {
  return {
    type: 'with_clause' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (WithItem)[]) { return with_clause_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `with_clause` — v0 form. */
export function with_clause__v0_(
  config?: WithClauseV0Config,
) {
  const children = config?.children ?? [];
  return {
    type: 'with_clause' as const,
    named: true as const,
    variant: 'v0' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (WithItem)[]) { return with_clause__v0_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `with_clause` — paren form. */
export function with_clause__paren_(
  config?: WithClauseParenConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'with_clause' as const,
    named: true as const,
    variant: 'paren' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (WithItem)[]) { return with_clause__paren_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function with_item_(
  config: ConfigOf<WithItem>,
) {
  const fields = {
    value: config.value,
  };
  return {
    type: 'with_item' as const,
    named: true as const,
    fields,
    value(value?: Expression) { return value !== undefined ? with_item_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function with_statement_(
  config: ConfigOf<WithStatement>,
) {
  const fields = {
    body: config.body,
    with_clause: config.withClause,
  };
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? with_statement_({ ...config, body: body }) : fields.body; },
    withClause(withClause?: WithClause) { return withClause !== undefined ? with_statement_({ ...config, withClause: withClause }) : fields.with_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function yield_(
  child?: Expression | ExpressionList | Expressions,
) {
  const children = child ? [child] : [];
  return {
    type: 'yield' as const,
    named: true as const,
    children,
    child(c?: Expression | ExpressionList | Expressions) { return c !== undefined ? yield_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `yield` — from form. */
export function yield__from_(
  config?: YieldFromConfig,
) {
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'yield' as const,
    named: true as const,
    variant: 'from' as const,
    children,
    child(child?: Expression | ExpressionList | Expressions) { return child !== undefined ? yield__from_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `yield` — v1 form. */
export function yield__v1_(
  config?: YieldV1Config,
) {
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'yield' as const,
    named: true as const,
    variant: 'v1' as const,
    children,
    child(child?: Expression | ExpressionList | Expressions) { return child !== undefined ? yield__v1_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function break_statement_() {
  return {
    type: 'break_statement' as const,
    named: true as const,
    text: 'break',
    render: () => 'break',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'break' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'break' };
    },
    replace: (target: BreakStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' }; },
  };
}

export function continue_statement_() {
  return {
    type: 'continue_statement' as const,
    named: true as const,
    text: 'continue',
    render: () => 'continue',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'continue' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'continue' };
    },
    replace: (target: ContinueStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' }; },
  };
}

export function import_prefix_(text: string) {
  if (!/^(?:\.)+$/.test(text)) throw new Error(`Invalid import_prefix: '${text}' does not match grammar pattern`);
  return {
    type: 'import_prefix' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ImportPrefixTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function keyword_separator_() {
  return {
    type: 'keyword_separator' as const,
    named: true as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: KeywordSeparatorTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function pass_statement_() {
  return {
    type: 'pass_statement' as const,
    named: true as const,
    text: 'pass',
    render: () => 'pass',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'pass' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'pass' };
    },
    replace: (target: PassStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' }; },
  };
}

export function positional_separator_() {
  return {
    type: 'positional_separator' as const,
    named: true as const,
    text: '/',
    render: () => '/',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '/' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '/' };
    },
    replace: (target: PositionalSeparatorTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '/' }; },
  };
}

export function wildcard_import_() {
  return {
    type: 'wildcard_import' as const,
    named: true as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: WildcardImportTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function comment_(text: string) {
  if (!/^#.*$/.test(text)) throw new Error(`Invalid comment: '${text}' does not match grammar pattern`);
  return {
    type: 'comment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: CommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function ellipsis_() {
  return {
    type: 'ellipsis' as const,
    named: true as const,
    text: '...',
    render: () => '...',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '...' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '...' };
    },
    replace: (target: EllipsisTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '...' }; },
  };
}

export function escape_interpolation_(text: string) {
  return {
    type: 'escape_interpolation' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeInterpolationTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escape_sequence_(text: string) {
  if (!/^\\(?:u[a-fA-F\d]{4}|U[a-fA-F\d]{8}|x[a-fA-F\d]{2}|\d{1,3}|\r?\n|['"abfrntv\\]|N\{[^}]+\})$/.test(text)) throw new Error(`Invalid escape_sequence: '${text}' does not match grammar pattern`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeSequenceTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'False',
    render: () => 'False',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'False' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'False' };
    },
    replace: (target: FalseTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' }; },
  };
}

export function float_(text: string) {
  if (!/^(?:(?:[0-9]+_?)+\.(?:[0-9]+_?)+?[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+?\.(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+)[jJ]?$/.test(text)) throw new Error(`Invalid float: '${text}' does not match grammar pattern`);
  return {
    type: 'float' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FloatTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  if (!/^[_\p{XID_Start}][_\p{XID_Continue}]*$/u.test(text)) throw new Error(`Invalid identifier: '${text}' does not match grammar pattern`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer_(text: string) {
  if (!/^(?:(?:0x|0X)(?:_?[A-Fa-f0-9]+)+[Ll]?|(?:0o|0O)(?:_?[0-7]+)+[Ll]?|(?:0b|0B)(?:_?[0-1]+)+[Ll]?|(?:[0-9]+_?)+(?:[Ll]?|[jJ]?))$/.test(text)) throw new Error(`Invalid integer: '${text}' does not match grammar pattern`);
  return {
    type: 'integer' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IntegerTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function line_continuation_(text: string) {
  if (!/^\\(?:\r?\n|\0)$/.test(text)) throw new Error(`Invalid line_continuation: '${text}' does not match grammar pattern`);
  return {
    type: 'line_continuation' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: LineContinuationTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function none_() {
  return {
    type: 'none' as const,
    named: true as const,
    text: 'None',
    render: () => 'None',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'None' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'None' };
    },
    replace: (target: NoneTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' }; },
  };
}

export function string_end_(text: string) {
  return {
    type: 'string_end' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringEndTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function string_start_(text: string) {
  return {
    type: 'string_start' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringStartTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'True',
    render: () => 'True',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'True' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'True' };
    },
    replace: (target: TrueTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' }; },
  };
}

export function type_conversion_(text: string) {
  if (!/^![a-z]$/.test(text)) throw new Error(`Invalid type_conversion: '${text}' does not match grammar pattern`);
  return {
    type: 'type_conversion' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: TypeConversionTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}


/** @internal Map of kind string to factory function. */
export const _factoryMap: Record<string, (config?: any) => unknown> = {
  'aliased_import': aliased_import_,
  'argument_list': argument_list_,
  'as_pattern': as_pattern_,
  'assert_statement': assert_statement_,
  'assignment': assignment_,
  'attribute': attribute_,
  'augmented_assignment': augmented_assignment_,
  'await': await_,
  'binary_operator': binary_operator_,
  'block': block_,
  'boolean_operator': boolean_operator_,
  'call': call_,
  'case_clause': case_clause_,
  'case_pattern': case_pattern_,
  'chevron': chevron_,
  'class_definition': class_definition_,
  'class_pattern': class_pattern_,
  'comparison_operator': comparison_operator_,
  'complex_pattern': complex_pattern_,
  'concatenated_string': concatenated_string_,
  'conditional_expression': conditional_expression_,
  'constrained_type': constrained_type_,
  'decorated_definition': decorated_definition_,
  'decorator': decorator_,
  'default_parameter': default_parameter_,
  'delete_statement': delete_statement_,
  'dict_pattern': dict_pattern_,
  'dictionary': dictionary_,
  'dictionary_comprehension': dictionary_comprehension_,
  'dictionary_splat': dictionary_splat_,
  'dictionary_splat_pattern': dictionary_splat_pattern_,
  'dotted_name': dotted_name_,
  'elif_clause': elif_clause_,
  'else_clause': else_clause_,
  'except_clause': except_clause_,
  'exec_statement': exec_statement_,
  'expression_list': expression_list_,
  'expression_statement': expression_statement_,
  'finally_clause': finally_clause_,
  'for_in_clause': for_in_clause_,
  'for_statement': for_statement_,
  'format_expression': format_expression_,
  'format_specifier': format_specifier_,
  'function_definition': function_definition_,
  'future_import_statement': future_import_statement_,
  'generator_expression': generator_expression_,
  'generic_type': generic_type_,
  'global_statement': global_statement_,
  'if_clause': if_clause_,
  'if_statement': if_statement_,
  'import_from_statement': import_from_statement_,
  'import_statement': import_statement_,
  'interpolation': interpolation_,
  'keyword_argument': keyword_argument_,
  'keyword_pattern': keyword_pattern_,
  'lambda': lambda_,
  'lambda_parameters': lambda_parameters_,
  'list': list_,
  'list_comprehension': list_comprehension_,
  'list_pattern': list_pattern_,
  'list_splat': list_splat_,
  'list_splat_pattern': list_splat_pattern_,
  'match_statement': match_statement_,
  'member_type': member_type_,
  'module': module_,
  'named_expression': named_expression_,
  'nonlocal_statement': nonlocal_statement_,
  'not_operator': not_operator_,
  'pair': pair_,
  'parameters': parameters_,
  'parenthesized_expression': parenthesized_expression_,
  'parenthesized_list_splat': parenthesized_list_splat_,
  'pattern_list': pattern_list_,
  'print_statement': print_statement_,
  'raise_statement': raise_statement_,
  'relative_import': relative_import_,
  'return_statement': return_statement_,
  'set': set_,
  'set_comprehension': set_comprehension_,
  'slice': slice_,
  'splat_pattern': splat_pattern_,
  'splat_type': splat_type_,
  'string': string_,
  'string_content': string_content_,
  'subscript': subscript_,
  'try_statement': try_statement_,
  'tuple': tuple_,
  'tuple_pattern': tuple_pattern_,
  'type': type_,
  'type_alias_statement': type_alias_statement_,
  'type_parameter': type_parameter_,
  'typed_default_parameter': typed_default_parameter_,
  'typed_parameter': typed_parameter_,
  'unary_operator': unary_operator_,
  'union_pattern': union_pattern_,
  'union_type': union_type_,
  'while_statement': while_statement_,
  'with_clause': with_clause_,
  'with_item': with_item_,
  'with_statement': with_statement_,
  'yield': yield_,
};

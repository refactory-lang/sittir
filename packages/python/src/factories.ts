// Auto-generated by @sittir/codegen — do not edit

import type * as T from './types.js';
import type { Edit, ByteRange, NonEmptyArray, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const RESERVED_KEYWORDS: ReadonlySet<string> = new Set([
  'False',
  'None',
  'True',
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'case',
  'class',
  'continue',
  'def',
  'del',
  'elif',
  'else',
  'exec',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'match',
  'nonlocal',
  'not',
  'or',
  'pass',
  'print',
  'raise',
  'return',
  'try',
  'while',
  'with',
]);

const _leafRe_typeConversion = /^(?:![a-z])/u;
const _leafRe_identifier = /^(?:[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _wordRe = /^(?:[_\p{XID_Start}][_\p{XID_Continue}]*)/u;

export function module(...children: T.Statement[]) {
  return {
    type: 'module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importStatement(config: T.ImportStatementConfig) {
  const fields = {
    import: config?.import,
  };
  const children = config?.children ?? [];
  return {
    type: 'import_statement' as const,
    named: true as const,
    fields,
    children,
    import(import_?: "import") { return _fs(config, importStatement, 'import', import_, fields.import); },
    getChild() { return children[0]; },
    setChild(child: T.ImportList) { return importStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importPrefix(text: string) {
  if (text.length === 0) throw new Error(`import_prefix: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`import_prefix: text '${text}' is a reserved keyword`);
  return {
    type: 'import_prefix' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ImportPrefixTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function relativeImport(config: T.RelativeImportConfig) {
  const fields = {
    import_prefix: config?.importPrefix,
    dotted_name: config?.dottedName,
  };
  return {
    type: 'relative_import' as const,
    named: true as const,
    fields,
    importPrefix(importPrefix_?: T.ImportPrefix) { return _fs(config, relativeImport, 'importPrefix', importPrefix_, fields.import_prefix); },
    dottedName(dottedName_?: T.DottedName | undefined) { return _fs(config, relativeImport, 'dottedName', dottedName_, fields.dotted_name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RelativeImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function futureImportStatement(config: T.FutureImportStatementConfig) {
  const fields = {
    from: config?.from,
  };
  const children = config?.children ?? [];
  return {
    type: 'future_import_statement' as const,
    named: true as const,
    fields,
    children,
    from(from_?: "from") { return _fs(config, futureImportStatement, 'from', from_, fields.from); },
    getChild() { return children[0]; },
    setChild(child: T.ImportList) { return futureImportStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importFromStatement(config: T.ImportFromStatementConfig) {
  const fields = {
    from: config?.from,
    module_name: config?.moduleName,
    wildcard_import: config?.wildcardImport,
  };
  return {
    type: 'import_from_statement' as const,
    named: true as const,
    fields,
    from(from_?: "from") { return _fs(config, importFromStatement, 'from', from_, fields.from); },
    moduleName(moduleName_?: T.RelativeImport | T.DottedName) { return _fs(config, importFromStatement, 'moduleName', moduleName_, fields.module_name); },
    wildcardImport(wildcardImport_?: T.WildcardImport | T.ImportList) { return _fs(config, importFromStatement, 'wildcardImport', wildcardImport_, fields.wildcard_import); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function aliasedImport(config: T.AliasedImportConfig) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'aliased_import' as const,
    named: true as const,
    fields,
    name(name_?: T.DottedName) { return _fs(config, aliasedImport, 'name', name_, fields.name); },
    alias(alias_?: T.Identifier) { return _fs(config, aliasedImport, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AliasedImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function printStatement(config: T.PrintStatementConfig) {
  const fields = {
    argument: config?.argument,
  };
  const children = config?.children ?? [];
  return {
    type: 'print_statement' as const,
    named: true as const,
    fields,
    children,
    argument(...argument_: T.Expression[]) { return _fsm(config, printStatement, 'argument', argument_, fields.argument); },
    getChild() { return children[0]; },
    setChild(child: T.Chevron) { return printStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function chevron(config: T.ChevronConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'chevron' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, chevron, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ChevronTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertStatement(config: T.AssertStatementConfig) {
  const fields = {
    assert: config?.assert,
  };
  const children = config?.children ?? [];
  return {
    type: 'assert_statement' as const,
    named: true as const,
    fields,
    children,
    assert(assert_?: "assert") { return _fs(config, assertStatement, 'assert', assert_, fields.assert); },
    getChildren() { return children; },
    setChildren(...items: T.Expression[]) {
      _assertNonEmpty(items, 'assert_statement.children');
      return assertStatement({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssertStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child?: (T.Expression | T.Assignment | T.AugmentedAssignment | T.Yield)) {
  const children = child != null ? [child] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedExpression(config: T.NamedExpressionConfig) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'named_expression' as const,
    named: true as const,
    fields,
    name(name_?: T.NamedExpressionLhs) { return _fs(config, namedExpression, 'name', name_, fields.name); },
    value(value_?: T.Expression) { return _fs(config, namedExpression, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NamedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(config: T.ReturnStatementConfig) {
  const fields = {
    return: config?.return,
  };
  const children = config?.children ?? [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    fields,
    children,
    return(return_?: "return") { return _fs(config, returnStatement, 'return', return_, fields.return); },
    getChild() { return children[0]; },
    setChild(child: T.Expressions) { return returnStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function deleteStatement(config: T.DeleteStatementConfig) {
  const fields = {
    del: config?.del,
  };
  const children = config?.children ?? [];
  return {
    type: 'delete_statement' as const,
    named: true as const,
    fields,
    children,
    del(del_?: "del") { return _fs(config, deleteStatement, 'del', del_, fields.del); },
    getChild() { return children[0]; },
    setChild(child: T.Expressions) { return deleteStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DeleteStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function raiseStatement(config: T.RaiseStatementConfig) {
  const fields = {
    raise: config?.raise,
    cause: config?.cause,
  };
  const children = config?.children ?? [];
  return {
    type: 'raise_statement' as const,
    named: true as const,
    fields,
    children,
    raise(raise_?: "raise") { return _fs(config, raiseStatement, 'raise', raise_, fields.raise); },
    cause(cause_?: T.Expression | undefined) { return _fs(config, raiseStatement, 'cause', cause_, fields.cause); },
    getChild() { return children[0]; },
    setChild(child: T.Expressions) { return raiseStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RaiseStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function passStatement() {
  return {
    type: 'pass_statement' as const,
    named: true as const,
    text: 'pass' as const,
    render: () => 'pass' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'pass' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'pass' as const },
    replace: (t: T.PassStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' as const }; },
  };
}

export function breakStatement() {
  return {
    type: 'break_statement' as const,
    named: true as const,
    text: 'break' as const,
    render: () => 'break' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'break' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'break' as const },
    replace: (t: T.BreakStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' as const }; },
  };
}

export function continueStatement() {
  return {
    type: 'continue_statement' as const,
    named: true as const,
    text: 'continue' as const,
    render: () => 'continue' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'continue' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'continue' as const },
    replace: (t: T.ContinueStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' as const }; },
  };
}

export function ifStatement(config: T.IfStatementConfig) {
  const fields = {
    if: config?.if,
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    if(if_?: "if") { return _fs(config, ifStatement, 'if', if_, fields.if); },
    condition(condition_?: T.Expression) { return _fs(config, ifStatement, 'condition', condition_, fields.condition); },
    consequence(consequence_?: T.Suite) { return _fs(config, ifStatement, 'consequence', consequence_, fields.consequence); },
    alternative(...alternative_: (T.ElifClause | T.ElseClause)[]) { return _fsm(config, ifStatement, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elifClause(config: T.ElifClauseConfig) {
  const fields = {
    elif: config?.elif,
    condition: config?.condition,
    consequence: config?.consequence,
  };
  return {
    type: 'elif_clause' as const,
    named: true as const,
    fields,
    elif(elif_?: "elif") { return _fs(config, elifClause, 'elif', elif_, fields.elif); },
    condition(condition_?: T.Expression) { return _fs(config, elifClause, 'condition', condition_, fields.condition); },
    consequence(consequence_?: T.Suite) { return _fs(config, elifClause, 'consequence', consequence_, fields.consequence); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ElifClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: T.ElseClauseConfig) {
  const fields = {
    else: config?.else,
    body: config?.body,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    else(else_?: "else") { return _fs(config, elseClause, 'else', else_, fields.else); },
    body(body_?: T.Suite) { return _fs(config, elseClause, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchStatement(config: T.MatchStatementConfig) {
  const fields = {
    match: config?.match,
    subject: config?.subject,
    body: config?.body,
  };
  return {
    type: 'match_statement' as const,
    named: true as const,
    fields,
    match(match_?: "match") { return _fs(config, matchStatement, 'match', match_, fields.match); },
    subject(...subject_: NonEmptyArray<T.Expression>) { return _fsm(config, matchStatement, 'subject', subject_, fields.subject); },
    body(body_?: T.Block) { return _fs(config, matchStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function caseClause(config: T.CaseClauseConfig) {
  const fields = {
    case: config?.case,
    guard: config?.guard,
    consequence: config?.consequence,
  };
  const children = config?.children ?? [];
  return {
    type: 'case_clause' as const,
    named: true as const,
    fields,
    children,
    case(case_?: "case") { return _fs(config, caseClause, 'case', case_, fields.case); },
    guard(guard_?: T.IfClause | undefined) { return _fs(config, caseClause, 'guard', guard_, fields.guard); },
    consequence(consequence_?: T.Suite) { return _fs(config, caseClause, 'consequence', consequence_, fields.consequence); },
    getChildren() { return children; },
    setChildren(...items: T.CasePattern[]) {
      _assertNonEmpty(items, 'case_clause.children');
      return caseClause({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CaseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: T.ForStatementConfig) {
  const fields = {
    async: config?.async,
    left: config?.left,
    right: config?.right,
    body: config?.body,
    alternative: config?.alternative,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    async(async_?: "async" | undefined) { return _fs(config, forStatement, 'async', async_, fields.async); },
    left(left_?: T.LeftHandSide) { return _fs(config, forStatement, 'left', left_, fields.left); },
    right(right_?: T.Expressions) { return _fs(config, forStatement, 'right', right_, fields.right); },
    body(body_?: T.Suite) { return _fs(config, forStatement, 'body', body_, fields.body); },
    alternative(alternative_?: T.ElseClause | undefined) { return _fs(config, forStatement, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: T.WhileStatementConfig) {
  const fields = {
    while: config?.while,
    condition: config?.condition,
    body: config?.body,
    alternative: config?.alternative,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    while(while_?: "while") { return _fs(config, whileStatement, 'while', while_, fields.while); },
    condition(condition_?: T.Expression) { return _fs(config, whileStatement, 'condition', condition_, fields.condition); },
    body(body_?: T.Suite) { return _fs(config, whileStatement, 'body', body_, fields.body); },
    alternative(alternative_?: T.ElseClause | undefined) { return _fs(config, whileStatement, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: T.TryStatementConfig) {
  const fields = {
    try: config?.try,
    body: config?.body,
    except_clauses: config?.exceptClauses,
    else_clause: config?.elseClause,
    finally_clause: config?.finallyClause,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    try(try_?: "try") { return _fs(config, tryStatement, 'try', try_, fields.try); },
    body(body_?: T.Suite) { return _fs(config, tryStatement, 'body', body_, fields.body); },
    exceptClauses(...exceptClauses_: T.ExceptClause[]) { return _fsm(config, tryStatement, 'exceptClauses', exceptClauses_, fields.except_clauses); },
    elseClause(elseClause_?: T.ElseClause | undefined) { return _fs(config, tryStatement, 'elseClause', elseClause_, fields.else_clause); },
    finallyClause(finallyClause_?: T.FinallyClause | undefined) { return _fs(config, tryStatement, 'finallyClause', finallyClause_, fields.finally_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exceptClause(config: T.ExceptClauseConfig) {
  const fields = {
    except: config?.except,
    value: config?.value,
    alias: config?.alias,
  };
  const children = config?.children ?? [];
  return {
    type: 'except_clause' as const,
    named: true as const,
    fields,
    children,
    except(except_?: "except") { return _fs(config, exceptClause, 'except', except_, fields.except); },
    value(value_?: T.Expression | undefined) { return _fs(config, exceptClause, 'value', value_, fields.value); },
    alias(alias_?: T.Expression | undefined) { return _fs(config, exceptClause, 'alias', alias_, fields.alias); },
    getChild() { return children[0]; },
    setChild(child: T.Suite) { return exceptClause({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExceptClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: T.FinallyClauseConfig) {
  const fields = {
    finally: config?.finally,
    block: config?.block,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    finally(finally_?: "finally") { return _fs(config, finallyClause, 'finally', finally_, fields.finally); },
    block(block_?: T.Suite) { return _fs(config, finallyClause, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: T.WithStatementConfig) {
  const fields = {
    async: config?.async,
    with_clause: config?.withClause,
    body: config?.body,
  };
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    async(async_?: "async" | undefined) { return _fs(config, withStatement, 'async', async_, fields.async); },
    withClause(withClause_?: T.WithClause) { return _fs(config, withStatement, 'withClause', withClause_, fields.with_clause); },
    body(body_?: T.Suite) { return _fs(config, withStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withClause(...children: T.WithItem[]) {
  _assertNonEmpty(children, 'with_clause.children');
  return {
    type: 'with_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withItem(config: T.WithItemConfig) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'with_item' as const,
    named: true as const,
    fields,
    value(value_?: T.Expression) { return _fs(config, withItem, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WithItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDefinition(config: T.FunctionDefinitionConfig) {
  const fields = {
    async: config?.async,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    body: config?.body,
  };
  return {
    type: 'function_definition' as const,
    named: true as const,
    fields,
    async(async_?: "async" | undefined) { return _fs(config, functionDefinition, 'async', async_, fields.async); },
    name(name_?: T.Identifier) { return _fs(config, functionDefinition, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameter | undefined) { return _fs(config, functionDefinition, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: T.Parameters) { return _fs(config, functionDefinition, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: T.Type | undefined) { return _fs(config, functionDefinition, 'returnType', returnType_, fields.return_type); },
    body(body_?: T.Suite) { return _fs(config, functionDefinition, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(child?: T._Parameters) {
  const children = child != null ? [child] : [];
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaParameters(child: T._Parameters) {
  const children = [child];
  return {
    type: 'lambda_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LambdaParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplat(config: T.ListSplatConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'list_splat' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, listSplat, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplat(config: T.DictionarySplatConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'dictionary_splat' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, dictionarySplat, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictionarySplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function globalStatement(config: T.GlobalStatementConfig) {
  const fields = {
    global: config?.global,
  };
  const children = config?.children ?? [];
  return {
    type: 'global_statement' as const,
    named: true as const,
    fields,
    children,
    global(global_?: "global") { return _fs(config, globalStatement, 'global', global_, fields.global); },
    getChildren() { return children; },
    setChildren(...items: T.Identifier[]) {
      _assertNonEmpty(items, 'global_statement.children');
      return globalStatement({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GlobalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonlocalStatement(config: T.NonlocalStatementConfig) {
  const fields = {
    nonlocal: config?.nonlocal,
  };
  const children = config?.children ?? [];
  return {
    type: 'nonlocal_statement' as const,
    named: true as const,
    fields,
    children,
    nonlocal(nonlocal_?: "nonlocal") { return _fs(config, nonlocalStatement, 'nonlocal', nonlocal_, fields.nonlocal); },
    getChildren() { return children; },
    setChildren(...items: T.Identifier[]) {
      _assertNonEmpty(items, 'nonlocal_statement.children');
      return nonlocalStatement({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NonlocalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function execStatement(config: T.ExecStatementConfig) {
  const fields = {
    exec: config?.exec,
    code: config?.code,
  };
  const children = config?.children ?? [];
  return {
    type: 'exec_statement' as const,
    named: true as const,
    fields,
    children,
    exec(exec_?: "exec") { return _fs(config, execStatement, 'exec', exec_, fields.exec); },
    code(code_?: T.String | T.Identifier) { return _fs(config, execStatement, 'code', code_, fields.code); },
    getChildren() { return children; },
    setChildren(...items: T.Expression[]) { return execStatement({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExecStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasStatement(config: T.TypeAliasStatementConfig) {
  const fields = {
    type: config?.type,
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'type_alias_statement' as const,
    named: true as const,
    fields,
    typeField(type?: "type") { return _fs(config, typeAliasStatement, 'type', type, fields.type); },
    left(left_?: T.Type) { return _fs(config, typeAliasStatement, 'left', left_, fields.left); },
    right(right_?: T.Type) { return _fs(config, typeAliasStatement, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeAliasStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDefinition(config: T.ClassDefinitionConfig) {
  const fields = {
    class: config?.class,
    name: config?.name,
    type_parameters: config?.typeParameters,
    superclasses: config?.superclasses,
    body: config?.body,
  };
  return {
    type: 'class_definition' as const,
    named: true as const,
    fields,
    class(class_?: "class") { return _fs(config, classDefinition, 'class', class_, fields.class); },
    name(name_?: T.Identifier) { return _fs(config, classDefinition, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameter | undefined) { return _fs(config, classDefinition, 'typeParameters', typeParameters_, fields.type_parameters); },
    superclasses(superclasses_?: T.ArgumentList | undefined) { return _fs(config, classDefinition, 'superclasses', superclasses_, fields.superclasses); },
    body(body_?: T.Suite) { return _fs(config, classDefinition, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(...children: T.Type[]) {
  _assertNonEmpty(children, 'type_parameter.children');
  return {
    type: 'type_parameter' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedListSplat(child?: (T.ParenthesizedExpression | T.ListSplat)) {
  const children = child != null ? [child] : [];
  return {
    type: 'parenthesized_list_splat' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function argumentList(...children: (T.Expression | T.ListSplat | T.DictionarySplat | T.ParenthesizedExpression | T.KeywordArgument)[]) {
  return {
    type: 'argument_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArgumentListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratedDefinition(config: T.DecoratedDefinitionConfig) {
  const fields = {
    definition: config?.definition,
  };
  const children = config?.children ?? [];
  return {
    type: 'decorated_definition' as const,
    named: true as const,
    fields,
    children,
    definition(definition_?: T.ClassDefinition | T.FunctionDefinition) { return _fs(config, decoratedDefinition, 'definition', definition_, fields.definition); },
    getChildren() { return children; },
    setChildren(...items: T.Decorator[]) {
      _assertNonEmpty(items, 'decorated_definition.children');
      return decoratedDefinition({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratedDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(config: T.DecoratorConfig) {
  const fields = {
    expression: config?.expression,
    newline: config?.newline,
  };
  return {
    type: 'decorator' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, decorator, 'expression', expression_, fields.expression); },
    newline(newline_?: string) { return _fs(config, decorator, 'newline', newline_, fields.newline); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(...children: T.Statement[]) {
  return {
    type: 'block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionList(config: T.ExpressionListConfig) {
  const fields = {
    expression: config?.expression,
  };
  const children = config?.children ?? [];
  return {
    type: 'expression_list' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: T.Expression) { return _fs(config, expressionList, 'expression', expression_, fields.expression); },
    getChildren() { return children; },
    setChildren(...items: T.Expression[]) { return expressionList({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dottedName(...children: T.Identifier[]) {
  _assertNonEmpty(children, 'dotted_name.children');
  return {
    type: 'dotted_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DottedNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function casePattern(child?: (T.AsPattern | T.KeywordPattern | T.SimplePattern)) {
  const children = child != null ? [child] : [];
  return {
    type: 'case_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionPattern(...children: T.SimplePattern[]) {
  return {
    type: 'union_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictPattern(...children: (T.KeyValuePattern | T.SplatPattern)[]) {
  return {
    type: 'dict_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordPattern(config: T.KeywordPatternConfig) {
  const fields = {
    identifier: config?.identifier,
    simple_pattern: config?.simplePattern,
  };
  return {
    type: 'keyword_pattern' as const,
    named: true as const,
    fields,
    identifier(identifier_?: T.Identifier) { return _fs(config, keywordPattern, 'identifier', identifier_, fields.identifier); },
    simplePattern(simplePattern_?: T.SimplePattern) { return _fs(config, keywordPattern, 'simplePattern', simplePattern_, fields.simple_pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.KeywordPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatPattern(config: T.SplatPatternConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'splat_pattern' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: "*" | "**") { return _fs(config, splatPattern, 'identifier', identifier_, fields.identifier); },
    getChild() { return children[0]; },
    setChild(child: T.Identifier) { return splatPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classPattern(config: T.ClassPatternConfig) {
  const fields = {
    dotted_name: config?.dottedName,
    arguments: config?.arguments,
  };
  return {
    type: 'class_pattern' as const,
    named: true as const,
    fields,
    dottedName(dottedName_?: T.DottedName) { return _fs(config, classPattern, 'dottedName', dottedName_, fields.dotted_name); },
    arguments(...arguments_: string[]) { return _fsm(config, classPattern, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClassPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function complexPattern(config: T.ComplexPatternConfig) {
  const fields = {
    real: config?.real,
    imaginary: config?.imaginary,
  };
  const children = config?.children ?? [];
  return {
    type: 'complex_pattern' as const,
    named: true as const,
    fields,
    children,
    real(real_?: string) { return _fs(config, complexPattern, 'real', real_, fields.real); },
    imaginary(imaginary_?: T.Integer | T.Float) { return _fs(config, complexPattern, 'imaginary', imaginary_, fields.imaginary); },
    getChild() { return children[0]; },
    setChild(child: (T.Integer | T.Float)) { return complexPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(child?: T.Patterns) {
  const children = child != null ? [child] : [];
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listPattern(child?: T.Patterns) {
  const children = child != null ? [child] : [];
  return {
    type: 'list_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultParameter(config: T.DefaultParameterConfig) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'default_parameter' as const,
    named: true as const,
    fields,
    name(name_?: T.Identifier | T.TuplePattern) { return _fs(config, defaultParameter, 'name', name_, fields.name); },
    value(value_?: T.Expression) { return _fs(config, defaultParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedDefaultParameter(config: T.TypedDefaultParameterConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'typed_default_parameter' as const,
    named: true as const,
    fields,
    name(name_?: T.Identifier) { return _fs(config, typedDefaultParameter, 'name', name_, fields.name); },
    typeField(type?: T.Type) { return _fs(config, typedDefaultParameter, 'type', type, fields.type); },
    value(value_?: T.Expression) { return _fs(config, typedDefaultParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypedDefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplatPattern(child?: (T.Identifier | T.KeywordIdentifier | T.Subscript | T.Attribute)) {
  const children = child != null ? [child] : [];
  return {
    type: 'list_splat_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListSplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplatPattern(child?: (T.Identifier | T.KeywordIdentifier | T.Subscript | T.Attribute)) {
  const children = child != null ? [child] : [];
  return {
    type: 'dictionary_splat_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictionarySplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asPattern(config: T.AsPatternConfig) {
  const fields = {
    expression: config?.expression,
    alias: config?.alias,
  };
  return {
    type: 'as_pattern' as const,
    named: true as const,
    fields,
    expression(expression_?: T.Expression) { return _fs(config, asPattern, 'expression', expression_, fields.expression); },
    alias(alias_?: T.AsPatternTarget) { return _fs(config, asPattern, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function notOperator(config: T.NotOperatorConfig) {
  const fields = {
    not: config?.not,
    argument: config?.argument,
  };
  return {
    type: 'not_operator' as const,
    named: true as const,
    fields,
    not(not_?: "not") { return _fs(config, notOperator, 'not', not_, fields.not); },
    argument(argument_?: T.Expression) { return _fs(config, notOperator, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NotOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function booleanOperator(config: T.BooleanOperatorConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'boolean_operator' as const,
    named: true as const,
    fields,
    left(left_?: T.Expression) { return _fs(config, booleanOperator, 'left', left_, fields.left); },
    operator(operator_?: "and") { return _fs(config, booleanOperator, 'operator', operator_, fields.operator); },
    right(right_?: T.Expression) { return _fs(config, booleanOperator, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryOperator(config: T.BinaryOperatorConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    fields,
    left(left_?: T.PrimaryExpression) { return _fs(config, binaryOperator, 'left', left_, fields.left); },
    operator(operator_?: "+") { return _fs(config, binaryOperator, 'operator', operator_, fields.operator); },
    right(right_?: T.PrimaryExpression) { return _fs(config, binaryOperator, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryOperator(config: T.UnaryOperatorConfig) {
  const fields = {
    operator: config?.operator,
    argument: config?.argument,
  };
  return {
    type: 'unary_operator' as const,
    named: true as const,
    fields,
    operator(operator_?: "+" | "-" | "~") { return _fs(config, unaryOperator, 'operator', operator_, fields.operator); },
    argument(argument_?: T.PrimaryExpression) { return _fs(config, unaryOperator, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comparisonOperator(config: T.ComparisonOperatorConfig) {
  const fields = {
    left: config?.left,
    operators: config?.operators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    fields,
    left(left_?: T.PrimaryExpression) { return _fs(config, comparisonOperator, 'left', left_, fields.left); },
    operators(...operators_: NonEmptyArray<T.NotIn | T.IsNot>) { return _fsm(config, comparisonOperator, 'operators', operators_, fields.operators); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambda(config: T.LambdaConfig) {
  const fields = {
    lambda: config?.lambda,
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    type: 'lambda' as const,
    named: true as const,
    fields,
    lambda(lambda_?: "lambda") { return _fs(config, lambda, 'lambda', lambda_, fields.lambda); },
    parameters(parameters_?: T.LambdaParameters | undefined) { return _fs(config, lambda, 'parameters', parameters_, fields.parameters); },
    body(body_?: T.Expression) { return _fs(config, lambda, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LambdaTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaWithinForInClause(config: T.LambdaWithinForInClauseConfig) {
  const fields = {
    lambda: config?.lambda,
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    type: 'lambda_within_for_in_clause' as const,
    named: true as const,
    fields,
    lambda(lambda_?: "lambda") { return _fs(config, lambdaWithinForInClause, 'lambda', lambda_, fields.lambda); },
    parameters(parameters_?: T.LambdaParameters | undefined) { return _fs(config, lambdaWithinForInClause, 'parameters', parameters_, fields.parameters); },
    body(body_?: T.ExpressionWithinForInClause) { return _fs(config, lambdaWithinForInClause, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LambdaWithinForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignment(config: T.AssignmentUFormEqConfig | T.AssignmentUFormTypeConfig | T.AssignmentUFormTypedConfig) {
  if (config && 'left' in config) return assignmentUFormEq(config as T.AssignmentUFormEqConfig);
  return assignmentUFormTyped(config as T.AssignmentUFormTypedConfig);
}
export function assignmentUFormEq(config: T.AssignmentUFormEqConfig) {
  const fields = {
    left: config?.left,
  };
  const children = config?.children ?? [];
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: '_form_eq' as const,
    fields,
    children,
    left(left_?: T.LeftHandSide) { return _fs(config, assignmentUFormEq, 'left', left_, fields.left); },
    getChild() { return children[0]; },
    setChild(child: T.AssignmentEq) { return assignmentUFormEq({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentUFormEqTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function assignmentUFormType(config: T.AssignmentUFormTypeConfig) {
  const fields = {
    left: config?.left,
  };
  const children = config?.children ?? [];
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: '_form_type' as const,
    fields,
    children,
    left(left_?: T.LeftHandSide) { return _fs(config, assignmentUFormType, 'left', left_, fields.left); },
    getChild() { return children[0]; },
    setChild(child: T.AssignmentType) { return assignmentUFormType({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentUFormTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function assignmentUFormTyped(config: T.AssignmentUFormTypedConfig) {
  const fields = {
    left: config?.left,
  };
  const children = config?.children ?? [];
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: '_form_typed' as const,
    fields,
    children,
    left(left_?: T.LeftHandSide) { return _fs(config, assignmentUFormTyped, 'left', left_, fields.left); },
    getChild() { return children[0]; },
    setChild(child: T.AssignmentTyped) { return assignmentUFormTyped({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentUFormTypedTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignment(config: T.AugmentedAssignmentConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    fields,
    left(left_?: T.LeftHandSide) { return _fs(config, augmentedAssignment, 'left', left_, fields.left); },
    operator(operator_?: "+=" | "-=" | "*=" | "/=" | "@=" | "//=" | "%=" | "**=" | ">>=" | "<<=" | "&=" | "^=" | "|=") { return _fs(config, augmentedAssignment, 'operator', operator_, fields.operator); },
    right(right_?: T.RightHandSide) { return _fs(config, augmentedAssignment, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function patternList(config: T.PatternListConfig) {
  const fields = {
    pattern: config?.pattern,
  };
  const children = config?.children ?? [];
  return {
    type: 'pattern_list' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: T.Pattern) { return _fs(config, patternList, 'pattern', pattern_, fields.pattern); },
    getChildren() { return children; },
    setChildren(...items: T.Pattern[]) { return patternList({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yield_(config: T.YieldConfig) {
  const fields = {
    yield: config?.yield,
  };
  const children = config?.children ?? [];
  return {
    type: 'yield' as const,
    named: true as const,
    fields,
    children,
    yield(yield__?: "yield") { return _fs(config, yield_, 'yield', yield__, fields.yield); },
    getChild() { return children[0]; },
    setChild(child: (T.Expression | T.Expressions)) { return yield_({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: T.AttributeConfig) {
  const fields = {
    object: config?.object,
    attribute: config?.attribute,
  };
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    object(object_?: T.PrimaryExpression) { return _fs(config, attribute, 'object', object_, fields.object); },
    attribute(attribute_?: T.Identifier) { return _fs(config, attribute, 'attribute', attribute_, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscript(config: T.SubscriptConfig) {
  const fields = {
    value: config?.value,
    subscript: config?.subscript,
  };
  return {
    type: 'subscript' as const,
    named: true as const,
    fields,
    value(value_?: T.PrimaryExpression) { return _fs(config, subscript, 'value', value_, fields.value); },
    subscript(...subscript_: NonEmptyArray<T.Expression | T.Slice>) { return _fsm(config, subscript, 'subscript', subscript_, fields.subscript); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SubscriptTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slice(config?: T.SliceConfig) {
  const fields = {
    start: config?.start,
    stop: config?.stop,
    step: config?.step,
  };
  return {
    type: 'slice' as const,
    named: true as const,
    fields,
    start(start_?: T.Expression | undefined) { return _fs(config, slice, 'start', start_, fields.start); },
    stop(stop_?: T.Expression | undefined) { return _fs(config, slice, 'stop', stop_, fields.stop); },
    step(step_?: T.Expression | undefined) { return _fs(config, slice, 'step', step_, fields.step); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SliceTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function call(config: T.CallConfig) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call' as const,
    named: true as const,
    fields,
    function(function_?: T.PrimaryExpression) { return _fs(config, call, 'function', function_, fields.function); },
    arguments(arguments_?: T.GeneratorExpression | T.ArgumentList) { return _fs(config, call, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedParameter(config: T.TypedParameterConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'typed_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: T.Type) { return _fs(config, typedParameter, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (T.Identifier | T.ListSplatPattern | T.DictionarySplatPattern)) { return typedParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypedParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function type(child?: (T.Expression | T.SplatType | T.GenericType | T.UnionType | T.ConstrainedType | T.MemberType)) {
  const children = child != null ? [child] : [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatType(config: T.SplatTypeConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'splat_type' as const,
    named: true as const,
    fields,
    identifier(identifier_?: "*" | "**") { return _fs(config, splatType, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: T.GenericTypeConfig) {
  const fields = {
    identifier: config?.identifier,
    type_parameter: config?.typeParameter,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    identifier(identifier_?: T.Identifier) { return _fs(config, genericType, 'identifier', identifier_, fields.identifier); },
    typeParameter(typeParameter_?: T.TypeParameter) { return _fs(config, genericType, 'typeParameter', typeParameter_, fields.type_parameter); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: T.UnionTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left_?: T.Type) { return _fs(config, unionType, 'left', left_, fields.left); },
    right(right_?: T.Type) { return _fs(config, unionType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constrainedType(config: T.ConstrainedTypeConfig) {
  const fields = {
    base_type: config?.baseType,
    constraint: config?.constraint,
  };
  return {
    type: 'constrained_type' as const,
    named: true as const,
    fields,
    baseType(baseType_?: T.Type) { return _fs(config, constrainedType, 'baseType', baseType_, fields.base_type); },
    constraint(constraint_?: T.Type) { return _fs(config, constrainedType, 'constraint', constraint_, fields.constraint); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstrainedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberType(config: T.MemberTypeConfig) {
  const fields = {
    base_type: config?.baseType,
    identifier: config?.identifier,
  };
  return {
    type: 'member_type' as const,
    named: true as const,
    fields,
    baseType(baseType_?: T.Type) { return _fs(config, memberType, 'baseType', baseType_, fields.base_type); },
    identifier(identifier_?: T.Identifier) { return _fs(config, memberType, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MemberTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordArgument(config: T.KeywordArgumentConfig) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'keyword_argument' as const,
    named: true as const,
    fields,
    name(name_?: T.Identifier | T.KeywordIdentifier) { return _fs(config, keywordArgument, 'name', name_, fields.name); },
    value(value_?: T.Expression) { return _fs(config, keywordArgument, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.KeywordArgumentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function list(child?: T.CollectionElements) {
  const children = child != null ? [child] : [];
  return {
    type: 'list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function set(child: T.CollectionElements) {
  const children = [child];
  return {
    type: 'set' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuple(child?: T.CollectionElements) {
  const children = child != null ? [child] : [];
  return {
    type: 'tuple' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionary(...children: (T.Pair | T.DictionarySplat)[]) {
  return {
    type: 'dictionary' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictionaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: T.PairConfig) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key_?: T.Expression) { return _fs(config, pair, 'key', key_, fields.key); },
    value(value_?: T.Expression) { return _fs(config, pair, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listComprehension(config: T.ListComprehensionConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'list_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: T.Expression) { return _fs(config, listComprehension, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ComprehensionClauses) { return listComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ListComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionaryComprehension(config: T.DictionaryComprehensionConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'dictionary_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: T.Pair) { return _fs(config, dictionaryComprehension, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ComprehensionClauses) { return dictionaryComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DictionaryComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function setComprehension(config: T.SetComprehensionConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'set_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: T.Expression) { return _fs(config, setComprehension, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ComprehensionClauses) { return setComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SetComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorExpression(config: T.GeneratorExpressionConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_expression' as const,
    named: true as const,
    fields,
    children,
    body(body_?: T.Expression) { return _fs(config, generatorExpression, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.ComprehensionClauses) { return generatorExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GeneratorExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child?: (T.Expression | T.Yield)) {
  const children = child != null ? [child] : [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInClause(config: T.ForInClauseConfig) {
  const fields = {
    async: config?.async,
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'for_in_clause' as const,
    named: true as const,
    fields,
    async(async_?: "async" | undefined) { return _fs(config, forInClause, 'async', async_, fields.async); },
    left(left_?: T.LeftHandSide) { return _fs(config, forInClause, 'left', left_, fields.left); },
    right(...right_: NonEmptyArray<string>) { return _fsm(config, forInClause, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifClause(config: T.IfClauseConfig) {
  const fields = {
    if: config?.if,
    expression: config?.expression,
  };
  return {
    type: 'if_clause' as const,
    named: true as const,
    fields,
    if(if_?: "if") { return _fs(config, ifClause, 'if', if_, fields.if); },
    expression(expression_?: T.Expression) { return _fs(config, ifClause, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IfClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalExpression(config: T.ConditionalExpressionConfig) {
  const fields = {
    body: config?.body,
    condition: config?.condition,
    alternative: config?.alternative,
  };
  return {
    type: 'conditional_expression' as const,
    named: true as const,
    fields,
    body(body_?: T.Expression) { return _fs(config, conditionalExpression, 'body', body_, fields.body); },
    condition(condition_?: T.Expression) { return _fs(config, conditionalExpression, 'condition', condition_, fields.condition); },
    alternative(alternative_?: T.Expression) { return _fs(config, conditionalExpression, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConditionalExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function concatenatedString(config: T.ConcatenatedStringConfig) {
  const fields = {
    string: config?.string,
  };
  const children = config?.children ?? [];
  return {
    type: 'concatenated_string' as const,
    named: true as const,
    fields,
    children,
    string(string_?: T.String) { return _fs(config, concatenatedString, 'string', string_, fields.string); },
    getChildren() { return children; },
    setChildren(...items: T.String[]) {
      _assertNonEmpty(items, 'concatenated_string.children');
      return concatenatedString({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConcatenatedStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(config: T.StringConfig) {
  const fields = {
    string_start: config?.stringStart,
    content: config?.content,
    string_end: config?.stringEnd,
  };
  return {
    type: 'string' as const,
    named: true as const,
    fields,
    stringStart(stringStart_?: T.StringStart) { return _fs(config, string, 'stringStart', stringStart_, fields.string_start); },
    content(...content_: (T.Interpolation | T.StringContent)[]) { return _fsm(config, string, 'content', content_, fields.content); },
    stringEnd(stringEnd_?: T.StringEnd) { return _fs(config, string, 'stringEnd', stringEnd_, fields.string_end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringContent(...children: (T.EscapeInterpolation | T.EscapeSequence | T.NotEscapeSequence | T._StringContent)[]) {
  return {
    type: 'string_content' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringContentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interpolation(config: T.InterpolationConfig) {
  const fields = {
    expression: config?.expression,
    type_conversion: config?.typeConversion,
    format_specifier: config?.formatSpecifier,
  };
  return {
    type: 'interpolation' as const,
    named: true as const,
    fields,
    expression(expression_?: T.FExpression) { return _fs(config, interpolation, 'expression', expression_, fields.expression); },
    typeConversion(typeConversion_?: T.TypeConversion | undefined) { return _fs(config, interpolation, 'typeConversion', typeConversion_, fields.type_conversion); },
    formatSpecifier(formatSpecifier_?: T.FormatSpecifier | undefined) { return _fs(config, interpolation, 'formatSpecifier', formatSpecifier_, fields.format_specifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InterpolationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_sequence: text '${text}' is a reserved keyword`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function formatSpecifier(...children: T.FormatExpression[]) {
  return {
    type: 'format_specifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FormatSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeConversion(text: string) {
  if (text.length === 0) throw new Error(`type_conversion: text must be non-empty`); if (!_leafRe_typeConversion.test(text)) throw new Error(`type_conversion: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`type_conversion: text '${text}' is a reserved keyword`);
  return {
    type: 'type_conversion' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.TypeConversionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer(text: string) {
  if (text.length === 0) throw new Error(`integer: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`integer: text '${text}' is a reserved keyword`);
  return {
    type: 'integer' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IntegerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function float(text: string) {
  if (text.length === 0) throw new Error(`float: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`float: text '${text}' is a reserved keyword`);
  return {
    type: 'float' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FloatTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`); if (!_leafRe_identifier.test(text)) throw new Error(`identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'True' as const,
    render: () => 'True' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'True' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'True' as const },
    replace: (t: T.TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' as const }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'False' as const,
    render: () => 'False' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'False' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'False' as const },
    replace: (t: T.FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' as const }; },
  };
}

export function none() {
  return {
    type: 'none' as const,
    named: true as const,
    text: 'None' as const,
    render: () => 'None' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'None' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'None' as const },
    replace: (t: T.NoneTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' as const }; },
  };
}

export function await_(config: T.AwaitConfig) {
  const fields = {
    await: config?.await,
    primary_expression: config?.primaryExpression,
  };
  return {
    type: 'await' as const,
    named: true as const,
    fields,
    await(await__?: "await") { return _fs(config, await_, 'await', await__, fields.await); },
    primaryExpression(primaryExpression_?: T.PrimaryExpression) { return _fs(config, await_, 'primaryExpression', primaryExpression_, fields.primary_expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AwaitTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comment(text: string) {
  if (text.length === 0) throw new Error(`comment: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`comment: text '${text}' is a reserved keyword`);
  return {
    type: 'comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function lineContinuation(text: string) {
  if (text.length === 0) throw new Error(`line_continuation: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`line_continuation: text '${text}' is a reserved keyword`);
  return {
    type: 'line_continuation' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.LineContinuationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringStart(text: string) {
  if (text.length === 0) throw new Error(`string_start: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_start: text '${text}' is a reserved keyword`);
  return {
    type: 'string_start' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringStartTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeInterpolation(text: string) {
  if (text.length === 0) throw new Error(`escape_interpolation: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_interpolation: text '${text}' is a reserved keyword`);
  return {
    type: 'escape_interpolation' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeInterpolationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringEnd(text: string) {
  if (text.length === 0) throw new Error(`string_end: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_end: text '${text}' is a reserved keyword`);
  return {
    type: 'string_end' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringEndTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeBracket(text: string) {
  if (text.length === 0) throw new Error(`]: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`]: text '${text}' is a reserved keyword`);
  return {
    type: ']' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CloseBracketTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeParen(text: string) {
  if (text.length === 0) throw new Error(`): text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`): text '${text}' is a reserved keyword`);
  return {
    type: ')' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CloseParenTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeBrace(text: string) {
  if (text.length === 0) throw new Error(`}: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`}: text '${text}' is a reserved keyword`);
  return {
    type: '}' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CloseBraceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function except(text: string) {
  if (text.length === 0) throw new Error(`except: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`except: text '${text}' is a reserved keyword`);
  return {
    type: 'except' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ExceptTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function asPatternTarget(child?: (T.ComparisonOperator | T.NotOperator | T.BooleanOperator | T.Lambda | T.PrimaryExpression | T.ConditionalExpression | T.NamedExpression | T.AsPattern)) {
  const children = child != null ? [child] : [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentEq(config: T.AssignmentEqConfig) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'assignment_eq' as const,
    named: true as const,
    fields,
    right(right_?: T.RightHandSide) { return _fs(config, assignmentEq, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentEqTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentType(config: T.AssignmentTypeConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'assignment_type' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, assignmentType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentTyped(config: T.AssignmentTypedConfig) {
  const fields = {
    type: config?.type,
    right: config?.right,
  };
  return {
    type: 'assignment_typed' as const,
    named: true as const,
    fields,
    typeField(type?: T.Type) { return _fs(config, assignmentTyped, 'type', type, fields.type); },
    right(right_?: T.RightHandSide) { return _fs(config, assignmentTyped, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentTypedTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formatExpression(config: T.FormatExpressionConfig) {
  const fields = {
    expression: config?.expression,
    type_conversion: config?.typeConversion,
    format_specifier: config?.formatSpecifier,
  };
  return {
    type: 'format_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: T.FExpression) { return _fs(config, formatExpression, 'expression', expression_, fields.expression); },
    typeConversion(typeConversion_?: T.TypeConversion | undefined) { return _fs(config, formatExpression, 'typeConversion', typeConversion_, fields.type_conversion); },
    formatSpecifier(formatSpecifier_?: T.FormatSpecifier | undefined) { return _fs(config, formatExpression, 'formatSpecifier', formatSpecifier_, fields.format_specifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FormatExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export type FluentKindMap = {
  "module": FluentNode<"module", T.ModuleConfig>;
  "import_statement": FluentNode<"import_statement", T.ImportStatementConfig>;
  "import_prefix": T.ImportPrefix;
  "relative_import": FluentNode<"relative_import", T.RelativeImportConfig>;
  "future_import_statement": FluentNode<"future_import_statement", T.FutureImportStatementConfig>;
  "import_from_statement": FluentNode<"import_from_statement", T.ImportFromStatementConfig>;
  "aliased_import": FluentNode<"aliased_import", T.AliasedImportConfig>;
  "print_statement": FluentNode<"print_statement", T.PrintStatementConfig>;
  "chevron": FluentNode<"chevron", T.ChevronConfig>;
  "assert_statement": FluentNode<"assert_statement", T.AssertStatementConfig>;
  "expression_statement": FluentNode<"expression_statement", T.ExpressionStatementConfig>;
  "named_expression": FluentNode<"named_expression", T.NamedExpressionConfig>;
  "return_statement": FluentNode<"return_statement", T.ReturnStatementConfig>;
  "delete_statement": FluentNode<"delete_statement", T.DeleteStatementConfig>;
  "raise_statement": FluentNode<"raise_statement", T.RaiseStatementConfig>;
  "pass_statement": T.PassStatement;
  "break_statement": T.BreakStatement;
  "continue_statement": T.ContinueStatement;
  "if_statement": FluentNode<"if_statement", T.IfStatementConfig>;
  "elif_clause": FluentNode<"elif_clause", T.ElifClauseConfig>;
  "else_clause": FluentNode<"else_clause", T.ElseClauseConfig>;
  "match_statement": FluentNode<"match_statement", T.MatchStatementConfig>;
  "case_clause": FluentNode<"case_clause", T.CaseClauseConfig>;
  "for_statement": FluentNode<"for_statement", T.ForStatementConfig>;
  "while_statement": FluentNode<"while_statement", T.WhileStatementConfig>;
  "try_statement": FluentNode<"try_statement", T.TryStatementConfig>;
  "except_clause": FluentNode<"except_clause", T.ExceptClauseConfig>;
  "finally_clause": FluentNode<"finally_clause", T.FinallyClauseConfig>;
  "with_statement": FluentNode<"with_statement", T.WithStatementConfig>;
  "with_clause": FluentNode<"with_clause", T.WithClauseConfig>;
  "with_item": FluentNode<"with_item", T.WithItemConfig>;
  "function_definition": FluentNode<"function_definition", T.FunctionDefinitionConfig>;
  "parameters": FluentNode<"parameters", T.ParametersConfig>;
  "lambda_parameters": FluentNode<"lambda_parameters", T.LambdaParametersConfig>;
  "list_splat": FluentNode<"list_splat", T.ListSplatConfig>;
  "dictionary_splat": FluentNode<"dictionary_splat", T.DictionarySplatConfig>;
  "global_statement": FluentNode<"global_statement", T.GlobalStatementConfig>;
  "nonlocal_statement": FluentNode<"nonlocal_statement", T.NonlocalStatementConfig>;
  "exec_statement": FluentNode<"exec_statement", T.ExecStatementConfig>;
  "type_alias_statement": FluentNode<"type_alias_statement", T.TypeAliasStatementConfig>;
  "class_definition": FluentNode<"class_definition", T.ClassDefinitionConfig>;
  "type_parameter": FluentNode<"type_parameter", T.TypeParameterConfig>;
  "parenthesized_list_splat": FluentNode<"parenthesized_list_splat", T.ParenthesizedListSplatConfig>;
  "argument_list": FluentNode<"argument_list", T.ArgumentListConfig>;
  "decorated_definition": FluentNode<"decorated_definition", T.DecoratedDefinitionConfig>;
  "decorator": FluentNode<"decorator", T.DecoratorConfig>;
  "block": FluentNode<"block", T.BlockConfig>;
  "expression_list": FluentNode<"expression_list", T.ExpressionListConfig>;
  "dotted_name": FluentNode<"dotted_name", T.DottedNameConfig>;
  "case_pattern": FluentNode<"case_pattern", T.CasePatternConfig>;
  "union_pattern": FluentNode<"union_pattern", T.UnionPatternConfig>;
  "dict_pattern": FluentNode<"dict_pattern", T.DictPatternConfig>;
  "keyword_pattern": FluentNode<"keyword_pattern", T.KeywordPatternConfig>;
  "splat_pattern": FluentNode<"splat_pattern", T.SplatPatternConfig>;
  "class_pattern": FluentNode<"class_pattern", T.ClassPatternConfig>;
  "complex_pattern": FluentNode<"complex_pattern", T.ComplexPatternConfig>;
  "tuple_pattern": FluentNode<"tuple_pattern", T.TuplePatternConfig>;
  "list_pattern": FluentNode<"list_pattern", T.ListPatternConfig>;
  "default_parameter": FluentNode<"default_parameter", T.DefaultParameterConfig>;
  "typed_default_parameter": FluentNode<"typed_default_parameter", T.TypedDefaultParameterConfig>;
  "list_splat_pattern": FluentNode<"list_splat_pattern", T.ListSplatPatternConfig>;
  "dictionary_splat_pattern": FluentNode<"dictionary_splat_pattern", T.DictionarySplatPatternConfig>;
  "as_pattern": FluentNode<"as_pattern", T.AsPatternConfig>;
  "not_operator": FluentNode<"not_operator", T.NotOperatorConfig>;
  "boolean_operator": FluentNode<"boolean_operator", T.BooleanOperatorConfig>;
  "binary_operator": FluentNode<"binary_operator", T.BinaryOperatorConfig>;
  "unary_operator": FluentNode<"unary_operator", T.UnaryOperatorConfig>;
  "comparison_operator": FluentNode<"comparison_operator", T.ComparisonOperatorConfig>;
  "lambda": FluentNode<"lambda", T.LambdaConfig>;
  "lambda_within_for_in_clause": FluentNode<"lambda_within_for_in_clause", T.LambdaWithinForInClauseConfig>;
  "assignment": FluentNode<"assignment", T.AssignmentConfig>;
  "augmented_assignment": FluentNode<"augmented_assignment", T.AugmentedAssignmentConfig>;
  "pattern_list": FluentNode<"pattern_list", T.PatternListConfig>;
  "yield": FluentNode<"yield", T.YieldConfig>;
  "attribute": FluentNode<"attribute", T.AttributeConfig>;
  "subscript": FluentNode<"subscript", T.SubscriptConfig>;
  "slice": FluentNode<"slice", T.SliceConfig>;
  "call": FluentNode<"call", T.CallConfig>;
  "typed_parameter": FluentNode<"typed_parameter", T.TypedParameterConfig>;
  "type": FluentNode<"type", T.TypeConfig>;
  "splat_type": FluentNode<"splat_type", T.SplatTypeConfig>;
  "generic_type": FluentNode<"generic_type", T.GenericTypeConfig>;
  "union_type": FluentNode<"union_type", T.UnionTypeConfig>;
  "constrained_type": FluentNode<"constrained_type", T.ConstrainedTypeConfig>;
  "member_type": FluentNode<"member_type", T.MemberTypeConfig>;
  "keyword_argument": FluentNode<"keyword_argument", T.KeywordArgumentConfig>;
  "list": FluentNode<"list", T.ListConfig>;
  "set": FluentNode<"set", T.SetConfig>;
  "tuple": FluentNode<"tuple", T.TupleConfig>;
  "dictionary": FluentNode<"dictionary", T.DictionaryConfig>;
  "pair": FluentNode<"pair", T.PairConfig>;
  "list_comprehension": FluentNode<"list_comprehension", T.ListComprehensionConfig>;
  "dictionary_comprehension": FluentNode<"dictionary_comprehension", T.DictionaryComprehensionConfig>;
  "set_comprehension": FluentNode<"set_comprehension", T.SetComprehensionConfig>;
  "generator_expression": FluentNode<"generator_expression", T.GeneratorExpressionConfig>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", T.ParenthesizedExpressionConfig>;
  "for_in_clause": FluentNode<"for_in_clause", T.ForInClauseConfig>;
  "if_clause": FluentNode<"if_clause", T.IfClauseConfig>;
  "conditional_expression": FluentNode<"conditional_expression", T.ConditionalExpressionConfig>;
  "concatenated_string": FluentNode<"concatenated_string", T.ConcatenatedStringConfig>;
  "string": FluentNode<"string", T.StringConfig>;
  "string_content": FluentNode<"string_content", T.StringContentConfig>;
  "interpolation": FluentNode<"interpolation", T.InterpolationConfig>;
  "escape_sequence": T.EscapeSequence;
  "format_specifier": FluentNode<"format_specifier", T.FormatSpecifierConfig>;
  "type_conversion": T.TypeConversion;
  "integer": T.Integer;
  "float": T.Float;
  "identifier": T.Identifier;
  "true": T.True;
  "false": T.False;
  "none": T.None;
  "await": FluentNode<"await", T.AwaitConfig>;
  "comment": T.Comment;
  "line_continuation": T.LineContinuation;
  "string_start": T.StringStart;
  "escape_interpolation": T.EscapeInterpolation;
  "string_end": T.StringEnd;
  "]": T.CloseBracket;
  ")": T.CloseParen;
  "}": T.CloseBrace;
  "except": T.Except;
  "as_pattern_target": FluentNode<"as_pattern_target", T.AsPatternTargetConfig>;
  "assignment_eq": FluentNode<"assignment_eq", T.AssignmentEqConfig>;
  "assignment_type": FluentNode<"assignment_type", T.AssignmentTypeConfig>;
  "assignment_typed": FluentNode<"assignment_typed", T.AssignmentTypedConfig>;
  "format_expression": FluentNode<"format_expression", T.FormatExpressionConfig>;
};

export const _factoryMap = {
  "module": module,
  "import_statement": importStatement,
  "import_prefix": importPrefix,
  "relative_import": relativeImport,
  "future_import_statement": futureImportStatement,
  "import_from_statement": importFromStatement,
  "aliased_import": aliasedImport,
  "print_statement": printStatement,
  "chevron": chevron,
  "assert_statement": assertStatement,
  "expression_statement": expressionStatement,
  "named_expression": namedExpression,
  "return_statement": returnStatement,
  "delete_statement": deleteStatement,
  "raise_statement": raiseStatement,
  "pass_statement": passStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "if_statement": ifStatement,
  "elif_clause": elifClause,
  "else_clause": elseClause,
  "match_statement": matchStatement,
  "case_clause": caseClause,
  "for_statement": forStatement,
  "while_statement": whileStatement,
  "try_statement": tryStatement,
  "except_clause": exceptClause,
  "finally_clause": finallyClause,
  "with_statement": withStatement,
  "with_clause": withClause,
  "with_item": withItem,
  "function_definition": functionDefinition,
  "parameters": parameters,
  "lambda_parameters": lambdaParameters,
  "list_splat": listSplat,
  "dictionary_splat": dictionarySplat,
  "global_statement": globalStatement,
  "nonlocal_statement": nonlocalStatement,
  "exec_statement": execStatement,
  "type_alias_statement": typeAliasStatement,
  "class_definition": classDefinition,
  "type_parameter": typeParameter,
  "parenthesized_list_splat": parenthesizedListSplat,
  "argument_list": argumentList,
  "decorated_definition": decoratedDefinition,
  "decorator": decorator,
  "block": block,
  "expression_list": expressionList,
  "dotted_name": dottedName,
  "case_pattern": casePattern,
  "union_pattern": unionPattern,
  "dict_pattern": dictPattern,
  "keyword_pattern": keywordPattern,
  "splat_pattern": splatPattern,
  "class_pattern": classPattern,
  "complex_pattern": complexPattern,
  "tuple_pattern": tuplePattern,
  "list_pattern": listPattern,
  "default_parameter": defaultParameter,
  "typed_default_parameter": typedDefaultParameter,
  "list_splat_pattern": listSplatPattern,
  "dictionary_splat_pattern": dictionarySplatPattern,
  "as_pattern": asPattern,
  "not_operator": notOperator,
  "boolean_operator": booleanOperator,
  "binary_operator": binaryOperator,
  "unary_operator": unaryOperator,
  "comparison_operator": comparisonOperator,
  "lambda": lambda,
  "lambda_within_for_in_clause": lambdaWithinForInClause,
  "assignment": assignment,
  "augmented_assignment": augmentedAssignment,
  "pattern_list": patternList,
  "yield": yield_,
  "attribute": attribute,
  "subscript": subscript,
  "slice": slice,
  "call": call,
  "typed_parameter": typedParameter,
  "type": type,
  "splat_type": splatType,
  "generic_type": genericType,
  "union_type": unionType,
  "constrained_type": constrainedType,
  "member_type": memberType,
  "keyword_argument": keywordArgument,
  "list": list,
  "set": set,
  "tuple": tuple,
  "dictionary": dictionary,
  "pair": pair,
  "list_comprehension": listComprehension,
  "dictionary_comprehension": dictionaryComprehension,
  "set_comprehension": setComprehension,
  "generator_expression": generatorExpression,
  "parenthesized_expression": parenthesizedExpression,
  "for_in_clause": forInClause,
  "if_clause": ifClause,
  "conditional_expression": conditionalExpression,
  "concatenated_string": concatenatedString,
  "string": string,
  "string_content": stringContent,
  "interpolation": interpolation,
  "escape_sequence": escapeSequence,
  "format_specifier": formatSpecifier,
  "type_conversion": typeConversion,
  "integer": integer,
  "float": float,
  "identifier": identifier,
  "true": true_,
  "false": false_,
  "none": none,
  "await": await_,
  "comment": comment,
  "line_continuation": lineContinuation,
  "string_start": stringStart,
  "escape_interpolation": escapeInterpolation,
  "string_end": stringEnd,
  "]": closeBracket,
  ")": closeParen,
  "}": closeBrace,
  "except": except,
  "as_pattern_target": asPatternTarget,
  "assignment_eq": assignmentEq,
  "assignment_type": assignmentType,
  "assignment_typed": assignmentTyped,
  "format_expression": formatExpression,
} as const;
export type _FactoryMap = typeof _factoryMap;

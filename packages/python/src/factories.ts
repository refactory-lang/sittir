// Auto-generated by @sittir/codegen — do not edit

import type {
  AliasedImport,
  AliasedImportConfig,
  AliasedImportTree,
  ArgumentList,
  ArgumentListConfig,
  ArgumentListTree,
  AsPattern,
  AsPatternConfig,
  AsPatternTarget,
  AsPatternTargetConfig,
  AsPatternTargetTree,
  AsPatternTree,
  AssertStatement,
  AssertStatementConfig,
  AssertStatementTree,
  Assignment,
  AssignmentColon,
  AssignmentColon2,
  AssignmentColon2Config,
  AssignmentColon2Tree,
  AssignmentColonConfig,
  AssignmentColonTree,
  AssignmentConfig,
  AssignmentEq,
  AssignmentEqConfig,
  AssignmentEqTree,
  AssignmentTree,
  Attribute,
  AttributeConfig,
  AttributeTree,
  AugmentedAssignment,
  AugmentedAssignmentConfig,
  AugmentedAssignmentTree,
  Await,
  AwaitConfig,
  AwaitTree,
  BinaryOperator,
  BinaryOperatorConfig,
  BinaryOperatorTree,
  Block,
  BlockConfig,
  BlockTree,
  BooleanOperator,
  BooleanOperatorConfig,
  BooleanOperatorTree,
  BreakStatement,
  BreakStatementTree,
  Call,
  CallConfig,
  CallTree,
  CaseClause,
  CaseClauseConfig,
  CaseClauseTree,
  CasePattern,
  CasePatternConfig,
  CasePatternTree,
  Chevron,
  ChevronConfig,
  ChevronTree,
  ClassDefinition,
  ClassDefinitionConfig,
  ClassDefinitionTree,
  ClassPattern,
  ClassPatternConfig,
  ClassPatternTree,
  CloseBrace,
  CloseBraceTree,
  CloseBracket,
  CloseBracketTree,
  CloseParen,
  CloseParenTree,
  CollectionElements,
  Comment,
  CommentTree,
  ComparisonOperator,
  ComparisonOperatorConfig,
  ComparisonOperatorTree,
  ComplexPattern,
  ComplexPatternConfig,
  ComplexPatternTree,
  ComprehensionClauses,
  ConcatenatedString,
  ConcatenatedStringConfig,
  ConcatenatedStringTree,
  ConditionalExpression,
  ConditionalExpressionConfig,
  ConditionalExpressionTree,
  ConfigMap,
  ConstrainedType,
  ConstrainedTypeConfig,
  ConstrainedTypeTree,
  ContinueStatement,
  ContinueStatementTree,
  DecoratedDefinition,
  DecoratedDefinitionConfig,
  DecoratedDefinitionTree,
  Decorator,
  DecoratorConfig,
  DecoratorTree,
  DefaultParameter,
  DefaultParameterConfig,
  DefaultParameterTree,
  DeleteStatement,
  DeleteStatementConfig,
  DeleteStatementTree,
  DictPattern,
  DictPatternConfig,
  DictPatternTree,
  Dictionary,
  DictionaryComprehension,
  DictionaryComprehensionConfig,
  DictionaryComprehensionTree,
  DictionaryConfig,
  DictionarySplat,
  DictionarySplatConfig,
  DictionarySplatPattern,
  DictionarySplatPatternConfig,
  DictionarySplatPatternTree,
  DictionarySplatTree,
  DictionaryTree,
  DottedName,
  DottedNameConfig,
  DottedNameTree,
  ElifClause,
  ElifClauseConfig,
  ElifClauseTree,
  ElseClause,
  ElseClauseConfig,
  ElseClauseTree,
  EscapeInterpolation,
  EscapeInterpolationTree,
  EscapeSequence,
  EscapeSequenceTree,
  Except,
  ExceptClause,
  ExceptClauseConfig,
  ExceptClauseTree,
  ExceptTree,
  ExecStatement,
  ExecStatementConfig,
  ExecStatementTree,
  Expression,
  ExpressionList,
  ExpressionListConfig,
  ExpressionListTree,
  ExpressionStatement,
  ExpressionStatementConfig,
  ExpressionStatementTree,
  ExpressionWithinForInClause,
  Expressions,
  FExpression,
  False,
  FalseTree,
  FinallyClause,
  FinallyClauseConfig,
  FinallyClauseTree,
  Float,
  FloatTree,
  ForInClause,
  ForInClauseConfig,
  ForInClauseTree,
  ForStatement,
  ForStatementConfig,
  ForStatementTree,
  FormatExpression,
  FormatExpressionConfig,
  FormatExpressionTree,
  FormatSpecifier,
  FormatSpecifierConfig,
  FormatSpecifierTree,
  FunctionDefinition,
  FunctionDefinitionConfig,
  FunctionDefinitionTree,
  FutureImportStatement,
  FutureImportStatementConfig,
  FutureImportStatementTree,
  GeneratorExpression,
  GeneratorExpressionConfig,
  GeneratorExpressionTree,
  GenericType,
  GenericTypeConfig,
  GenericTypeTree,
  GlobalStatement,
  GlobalStatementConfig,
  GlobalStatementTree,
  Identifier,
  IdentifierTree,
  IfClause,
  IfClauseConfig,
  IfClauseTree,
  IfStatement,
  IfStatementConfig,
  IfStatementTree,
  ImportFromStatement,
  ImportFromStatementConfig,
  ImportFromStatementTree,
  ImportList,
  ImportPrefix,
  ImportPrefixTree,
  ImportStatement,
  ImportStatementConfig,
  ImportStatementTree,
  Integer,
  IntegerTree,
  Interpolation,
  InterpolationConfig,
  InterpolationTree,
  IsNot,
  KeyValuePattern,
  KeywordArgument,
  KeywordArgumentConfig,
  KeywordArgumentTree,
  KeywordIdentifier,
  KeywordIdentifierTree,
  KeywordPattern,
  KeywordPatternConfig,
  KeywordPatternTree,
  KindMap,
  Lambda,
  LambdaConfig,
  LambdaParameters,
  LambdaParametersConfig,
  LambdaParametersTree,
  LambdaTree,
  LambdaWithinForInClause,
  LambdaWithinForInClauseConfig,
  LambdaWithinForInClauseTree,
  LeftHandSide,
  LineContinuation,
  LineContinuationTree,
  List,
  ListComprehension,
  ListComprehensionConfig,
  ListComprehensionTree,
  ListConfig,
  ListPattern,
  ListPatternConfig,
  ListPatternTree,
  ListSplat,
  ListSplatConfig,
  ListSplatPattern,
  ListSplatPatternConfig,
  ListSplatPatternTree,
  ListSplatTree,
  ListTree,
  MatchStatement,
  MatchStatementConfig,
  MatchStatementTree,
  MemberType,
  MemberTypeConfig,
  MemberTypeTree,
  Module,
  ModuleConfig,
  ModuleTree,
  NamedExpression,
  NamedExpressionConfig,
  NamedExpressionLhs,
  NamedExpressionTree,
  None,
  NoneTree,
  NonlocalStatement,
  NonlocalStatementConfig,
  NonlocalStatementTree,
  NotEscapeSequence,
  NotIn,
  NotOperator,
  NotOperatorConfig,
  NotOperatorTree,
  Pair,
  PairConfig,
  PairTree,
  Parameters,
  ParametersConfig,
  ParametersTree,
  ParenthesizedExpression,
  ParenthesizedExpressionConfig,
  ParenthesizedExpressionTree,
  ParenthesizedListSplat,
  ParenthesizedListSplatConfig,
  ParenthesizedListSplatTree,
  PassStatement,
  PassStatementTree,
  Pattern,
  PatternList,
  PatternListConfig,
  PatternListTree,
  Patterns,
  PrimaryExpression,
  PrintStatement,
  PrintStatementConfig,
  PrintStatementTree,
  RaiseStatement,
  RaiseStatementConfig,
  RaiseStatementTree,
  RelativeImport,
  RelativeImportConfig,
  RelativeImportTree,
  ReturnStatement,
  ReturnStatementConfig,
  ReturnStatementTree,
  RightHandSide,
  Set,
  SetComprehension,
  SetComprehensionConfig,
  SetComprehensionTree,
  SetConfig,
  SetTree,
  SimplePattern,
  Slice,
  SliceConfig,
  SliceTree,
  SplatPattern,
  SplatPatternConfig,
  SplatPatternTree,
  SplatType,
  SplatTypeConfig,
  SplatTypeTree,
  Statement,
  String,
  StringConfig,
  StringContent,
  StringContentConfig,
  StringContentTree,
  StringEnd,
  StringEndTree,
  StringStart,
  StringStartTree,
  StringTree,
  Subscript,
  SubscriptConfig,
  SubscriptTree,
  Suite,
  True,
  TrueTree,
  TryStatement,
  TryStatementConfig,
  TryStatementTree,
  Tuple,
  TupleConfig,
  TuplePattern,
  TuplePatternConfig,
  TuplePatternTree,
  TupleTree,
  Type,
  TypeAliasStatement,
  TypeAliasStatementConfig,
  TypeAliasStatementTree,
  TypeConfig,
  TypeConversion,
  TypeConversionTree,
  TypeParameter,
  TypeParameterConfig,
  TypeParameterTree,
  TypeTree,
  TypedDefaultParameter,
  TypedDefaultParameterConfig,
  TypedDefaultParameterTree,
  TypedParameter,
  TypedParameterConfig,
  TypedParameterTree,
  UnaryOperator,
  UnaryOperatorConfig,
  UnaryOperatorTree,
  UnionPattern,
  UnionPatternConfig,
  UnionPatternTree,
  UnionType,
  UnionTypeConfig,
  UnionTypeTree,
  WhileStatement,
  WhileStatementConfig,
  WhileStatementTree,
  WildcardImport,
  WithClause,
  WithClauseConfig,
  WithClauseTree,
  WithItem,
  WithItemConfig,
  WithItemTree,
  WithStatement,
  WithStatementConfig,
  WithStatementTree,
  Yield,
  YieldConfig,
  YieldTree,
  _Parameters,
  _StringContent,
} from './types.js';
import type { Edit, ByteRange, NonEmptyArray, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const RESERVED_KEYWORDS: ReadonlySet<string> = new Set([
  'False',
  'None',
  'True',
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'case',
  'class',
  'continue',
  'def',
  'del',
  'elif',
  'else',
  'exec',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'match',
  'nonlocal',
  'not',
  'or',
  'pass',
  'print',
  'raise',
  'return',
  'try',
  'while',
  'with',
]);

const _leafRe_typeConversion = /^(?:![a-z])/u;
const _leafRe_identifier = /^(?:[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _wordRe = /^(?:[_\p{XID_Start}][_\p{XID_Continue}]*)/u;

export function module(...children: Statement[]) {
  return {
    type: 'module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importStatement(child: ImportList) {
  const children = [child];
  return {
    type: 'import_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importPrefix(text: string) {
  if (text.length === 0) throw new Error(`import_prefix: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`import_prefix: text '${text}' is a reserved keyword`);
  return {
    type: 'import_prefix' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ImportPrefixTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function relativeImport(config: RelativeImportConfig) {
  const fields = {
    import_prefix: config?.importPrefix,
    dotted_name: config?.dottedName,
  };
  return {
    type: 'relative_import' as const,
    named: true as const,
    fields,
    importPrefix(importPrefix_?: ImportPrefix) { return _fs(config, relativeImport, 'importPrefix', importPrefix_, fields.import_prefix); },
    dottedName(dottedName_?: DottedName | undefined) { return _fs(config, relativeImport, 'dottedName', dottedName_, fields.dotted_name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RelativeImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function futureImportStatement(child: ImportList) {
  const children = [child];
  return {
    type: 'future_import_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importFromStatement(config: ImportFromStatementConfig) {
  const fields = {
    module_name: config?.moduleName,
    wildcard_import: config?.wildcardImport,
  };
  return {
    type: 'import_from_statement' as const,
    named: true as const,
    fields,
    moduleName(moduleName_?: RelativeImport | DottedName) { return _fs(config, importFromStatement, 'moduleName', moduleName_, fields.module_name); },
    wildcardImport(wildcardImport_?: WildcardImport | ImportList) { return _fs(config, importFromStatement, 'wildcardImport', wildcardImport_, fields.wildcard_import); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function aliasedImport(config: AliasedImportConfig) {
  const fields = {
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'aliased_import' as const,
    named: true as const,
    fields,
    name(name_?: DottedName) { return _fs(config, aliasedImport, 'name', name_, fields.name); },
    alias(alias_?: Identifier) { return _fs(config, aliasedImport, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AliasedImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function printStatement(config: PrintStatementConfig) {
  const fields = {
    argument: config?.argument,
  };
  const children = config?.children ?? [];
  return {
    type: 'print_statement' as const,
    named: true as const,
    fields,
    children,
    argument(...argument_: Expression[]) { return _fsm(config, printStatement, 'argument', argument_, fields.argument); },
    getChild() { return children[0]; },
    setChild(child: Chevron) { return printStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function chevron(config: ChevronConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'chevron' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, chevron, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ChevronTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertStatement(...children: Expression[]) {
  _assertNonEmpty(children, 'assert_statement.children');
  return {
    type: 'assert_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(...children: (Expression | Assignment | AugmentedAssignment | Yield)[]) {
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedExpression(config: NamedExpressionConfig) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'named_expression' as const,
    named: true as const,
    fields,
    name(name_?: NamedExpressionLhs) { return _fs(config, namedExpression, 'name', name_, fields.name); },
    value(value_?: Expression) { return _fs(config, namedExpression, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(child?: Expressions) {
  const children = child != null ? [child] : [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function deleteStatement(child: Expressions) {
  const children = [child];
  return {
    type: 'delete_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeleteStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function raiseStatement(config?: RaiseStatementConfig) {
  const fields = {
    cause: config?.cause,
  };
  const children = config?.children ?? [];
  return {
    type: 'raise_statement' as const,
    named: true as const,
    fields,
    children,
    cause(cause_?: Expression | undefined) { return _fs(config, raiseStatement, 'cause', cause_, fields.cause); },
    getChild() { return children[0]; },
    setChild(child: Expressions) { return raiseStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RaiseStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function passStatement() {
  return {
    type: 'pass_statement' as const,
    named: true as const,
    text: 'pass' as const,
    render: () => 'pass' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'pass' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'pass' as const },
    replace: (t: PassStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' as const }; },
  };
}

export function breakStatement() {
  return {
    type: 'break_statement' as const,
    named: true as const,
    text: 'break' as const,
    render: () => 'break' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'break' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'break' as const },
    replace: (t: BreakStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' as const }; },
  };
}

export function continueStatement() {
  return {
    type: 'continue_statement' as const,
    named: true as const,
    text: 'continue' as const,
    render: () => 'continue' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'continue' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'continue' as const },
    replace: (t: ContinueStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' as const }; },
  };
}

export function ifStatement(config: IfStatementConfig) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: Expression) { return _fs(config, ifStatement, 'condition', condition_, fields.condition); },
    consequence(consequence_?: Suite) { return _fs(config, ifStatement, 'consequence', consequence_, fields.consequence); },
    alternative(...alternative_: (ElifClause | ElseClause)[]) { return _fsm(config, ifStatement, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elifClause(config: ElifClauseConfig) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
  };
  return {
    type: 'elif_clause' as const,
    named: true as const,
    fields,
    condition(condition_?: Expression) { return _fs(config, elifClause, 'condition', condition_, fields.condition); },
    consequence(consequence_?: Suite) { return _fs(config, elifClause, 'consequence', consequence_, fields.consequence); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElifClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: ElseClauseConfig) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    body(body_?: Suite) { return _fs(config, elseClause, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchStatement(config: MatchStatementConfig) {
  const fields = {
    subject: config?.subject,
    body: config?.body,
  };
  return {
    type: 'match_statement' as const,
    named: true as const,
    fields,
    subject(...subject_: NonEmptyArray<Expression>) { return _fsm(config, matchStatement, 'subject', subject_, fields.subject); },
    body(body_?: CaseClause) { return _fs(config, matchStatement, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function caseClause(config: CaseClauseConfig) {
  const fields = {
    guard: config?.guard,
    consequence: config?.consequence,
  };
  const children = config?.children ?? [];
  return {
    type: 'case_clause' as const,
    named: true as const,
    fields,
    children,
    guard(guard_?: IfClause | undefined) { return _fs(config, caseClause, 'guard', guard_, fields.guard); },
    consequence(consequence_?: Suite) { return _fs(config, caseClause, 'consequence', consequence_, fields.consequence); },
    getChildren() { return children; },
    setChildren(...items: CasePattern[]) {
      _assertNonEmpty(items, 'case_clause.children');
      return caseClause({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CaseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: ForStatementConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
    body: config?.body,
    alternative: config?.alternative,
  };
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    left(left_?: LeftHandSide) { return _fs(config, forStatement, 'left', left_, fields.left); },
    right(right_?: Expressions) { return _fs(config, forStatement, 'right', right_, fields.right); },
    body(body_?: Suite) { return _fs(config, forStatement, 'body', body_, fields.body); },
    alternative(alternative_?: ElseClause | undefined) { return _fs(config, forStatement, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: WhileStatementConfig) {
  const fields = {
    condition: config?.condition,
    body: config?.body,
    alternative: config?.alternative,
  };
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    condition(condition_?: Expression) { return _fs(config, whileStatement, 'condition', condition_, fields.condition); },
    body(body_?: Suite) { return _fs(config, whileStatement, 'body', body_, fields.body); },
    alternative(alternative_?: ElseClause | undefined) { return _fs(config, whileStatement, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: TryStatementConfig) {
  const fields = {
    body: config?.body,
    except_clauses: config?.exceptClauses,
    else_clause: config?.elseClause,
    finally_clause: config?.finallyClause,
  };
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    body(body_?: Suite) { return _fs(config, tryStatement, 'body', body_, fields.body); },
    exceptClauses(...exceptClauses_: ExceptClause[]) { return _fsm(config, tryStatement, 'exceptClauses', exceptClauses_, fields.except_clauses); },
    elseClause(elseClause_?: ElseClause | undefined) { return _fs(config, tryStatement, 'elseClause', elseClause_, fields.else_clause); },
    finallyClause(finallyClause_?: FinallyClause | undefined) { return _fs(config, tryStatement, 'finallyClause', finallyClause_, fields.finally_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exceptClause(config: ExceptClauseConfig) {
  const fields = {
    value: config?.value,
    alias: config?.alias,
  };
  const children = config?.children ?? [];
  return {
    type: 'except_clause' as const,
    named: true as const,
    fields,
    children,
    value(...value_: Expression[]) { return _fsm(config, exceptClause, 'value', value_, fields.value); },
    alias(alias_?: Expression | undefined) { return _fs(config, exceptClause, 'alias', alias_, fields.alias); },
    getChild() { return children[0]; },
    setChild(child: Suite) { return exceptClause({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExceptClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: FinallyClauseConfig) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    block(block_?: Suite) { return _fs(config, finallyClause, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: WithStatementConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    children,
    body(body_?: Suite) { return _fs(config, withStatement, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: WithClause) { return withStatement({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withClause(...children: WithItem[]) {
  _assertNonEmpty(children, 'with_clause.children');
  return {
    type: 'with_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withItem(config: WithItemConfig) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'with_item' as const,
    named: true as const,
    fields,
    value(value_?: Expression) { return _fs(config, withItem, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDefinition(config: FunctionDefinitionConfig) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    body: config?.body,
  };
  return {
    type: 'function_definition' as const,
    named: true as const,
    fields,
    name(name_?: Identifier) { return _fs(config, functionDefinition, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameter | undefined) { return _fs(config, functionDefinition, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: Parameters) { return _fs(config, functionDefinition, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: Type | undefined) { return _fs(config, functionDefinition, 'returnType', returnType_, fields.return_type); },
    body(body_?: Suite) { return _fs(config, functionDefinition, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(child?: _Parameters) {
  const children = child != null ? [child] : [];
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaParameters(child: _Parameters) {
  const children = [child];
  return {
    type: 'lambda_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplat(config: ListSplatConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'list_splat' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, listSplat, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplat(config: DictionarySplatConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'dictionary_splat' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, dictionarySplat, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function globalStatement(...children: Identifier[]) {
  _assertNonEmpty(children, 'global_statement.children');
  return {
    type: 'global_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GlobalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonlocalStatement(...children: Identifier[]) {
  _assertNonEmpty(children, 'nonlocal_statement.children');
  return {
    type: 'nonlocal_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonlocalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function execStatement(config: ExecStatementConfig) {
  const fields = {
    code: config?.code,
  };
  const children = config?.children ?? [];
  return {
    type: 'exec_statement' as const,
    named: true as const,
    fields,
    children,
    code(code_?: String | Identifier) { return _fs(config, execStatement, 'code', code_, fields.code); },
    getChildren() { return children; },
    setChildren(...items: Expression[]) { return execStatement({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExecStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasStatement(config: TypeAliasStatementConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'type_alias_statement' as const,
    named: true as const,
    fields,
    left(left_?: Type) { return _fs(config, typeAliasStatement, 'left', left_, fields.left); },
    right(right_?: Type) { return _fs(config, typeAliasStatement, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDefinition(config: ClassDefinitionConfig) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    superclasses: config?.superclasses,
    body: config?.body,
  };
  return {
    type: 'class_definition' as const,
    named: true as const,
    fields,
    name(name_?: Identifier) { return _fs(config, classDefinition, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameter | undefined) { return _fs(config, classDefinition, 'typeParameters', typeParameters_, fields.type_parameters); },
    superclasses(superclasses_?: ArgumentList | undefined) { return _fs(config, classDefinition, 'superclasses', superclasses_, fields.superclasses); },
    body(body_?: Suite) { return _fs(config, classDefinition, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(...children: Type[]) {
  _assertNonEmpty(children, 'type_parameter.children');
  return {
    type: 'type_parameter' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedListSplat(child: (ParenthesizedListSplat | ListSplat)) {
  const children = [child];
  return {
    type: 'parenthesized_list_splat' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function argumentList(...children: (Expression | ListSplat | DictionarySplat | ParenthesizedListSplat | KeywordArgument)[]) {
  return {
    type: 'argument_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratedDefinition(config: DecoratedDefinitionConfig) {
  const fields = {
    definition: config?.definition,
  };
  const children = config?.children ?? [];
  return {
    type: 'decorated_definition' as const,
    named: true as const,
    fields,
    children,
    definition(definition_?: ClassDefinition | FunctionDefinition) { return _fs(config, decoratedDefinition, 'definition', definition_, fields.definition); },
    getChildren() { return children; },
    setChildren(...items: Decorator[]) {
      _assertNonEmpty(items, 'decorated_definition.children');
      return decoratedDefinition({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratedDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(config: DecoratorConfig) {
  const fields = {
    expression: config?.expression,
    newline: config?.newline,
  };
  return {
    type: 'decorator' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, decorator, 'expression', expression_, fields.expression); },
    newline(newline_?: string) { return _fs(config, decorator, 'newline', newline_, fields.newline); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(...children: Statement[]) {
  return {
    type: 'block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionList(...children: Expression[]) {
  return {
    type: 'expression_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dottedName(...children: Identifier[]) {
  _assertNonEmpty(children, 'dotted_name.children');
  return {
    type: 'dotted_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DottedNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function casePattern(child: (CasePattern | Identifier | KeywordPattern | SimplePattern)) {
  const children = [child];
  return {
    type: 'case_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionPattern(...children: SimplePattern[]) {
  return {
    type: 'union_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictPattern(...children: (KeyValuePattern | SplatPattern)[]) {
  return {
    type: 'dict_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordPattern(config: KeywordPatternConfig) {
  const fields = {
    identifier: config?.identifier,
    simple_pattern: config?.simplePattern,
  };
  return {
    type: 'keyword_pattern' as const,
    named: true as const,
    fields,
    identifier(identifier_?: Identifier) { return _fs(config, keywordPattern, 'identifier', identifier_, fields.identifier); },
    simplePattern(simplePattern_?: SimplePattern) { return _fs(config, keywordPattern, 'simplePattern', simplePattern_, fields.simple_pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatPattern(config: SplatPatternConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'splat_pattern' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: "*" | "**") { return _fs(config, splatPattern, 'identifier', identifier_, fields.identifier); },
    getChild() { return children[0]; },
    setChild(child: Identifier) { return splatPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classPattern(config: ClassPatternConfig) {
  const fields = {
    dotted_name: config?.dottedName,
    arguments: config?.arguments,
  };
  return {
    type: 'class_pattern' as const,
    named: true as const,
    fields,
    dottedName(dottedName_?: DottedName) { return _fs(config, classPattern, 'dottedName', dottedName_, fields.dotted_name); },
    arguments(...arguments_: string[]) { return _fsm(config, classPattern, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function complexPattern(config: ComplexPatternConfig) {
  const fields = {
    real: config?.real,
    imaginary: config?.imaginary,
  };
  const children = config?.children ?? [];
  return {
    type: 'complex_pattern' as const,
    named: true as const,
    fields,
    children,
    real(real_?: string) { return _fs(config, complexPattern, 'real', real_, fields.real); },
    imaginary(imaginary_?: Integer | Float) { return _fs(config, complexPattern, 'imaginary', imaginary_, fields.imaginary); },
    getChild() { return children[0]; },
    setChild(child: (Integer | Float)) { return complexPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(child?: Patterns) {
  const children = child != null ? [child] : [];
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listPattern(child?: Patterns) {
  const children = child != null ? [child] : [];
  return {
    type: 'list_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultParameter(config: DefaultParameterConfig) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'default_parameter' as const,
    named: true as const,
    fields,
    name(name_?: Identifier | TuplePattern) { return _fs(config, defaultParameter, 'name', name_, fields.name); },
    value(value_?: Expression) { return _fs(config, defaultParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedDefaultParameter(config: TypedDefaultParameterConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'typed_default_parameter' as const,
    named: true as const,
    fields,
    name(name_?: Identifier) { return _fs(config, typedDefaultParameter, 'name', name_, fields.name); },
    typeField(type?: Type) { return _fs(config, typedDefaultParameter, 'type', type, fields.type); },
    value(value_?: Expression) { return _fs(config, typedDefaultParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedDefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplatPattern(child: (Identifier | KeywordIdentifier | Subscript | Attribute)) {
  const children = [child];
  return {
    type: 'list_splat_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplatPattern(child: (Identifier | KeywordIdentifier | Subscript | Attribute)) {
  const children = [child];
  return {
    type: 'dictionary_splat_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asPattern(config: AsPatternConfig) {
  const fields = {
    expression: config?.expression,
    alias: config?.alias,
  };
  return {
    type: 'as_pattern' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, asPattern, 'expression', expression_, fields.expression); },
    alias(alias_?: Expression) { return _fs(config, asPattern, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function notOperator(config: NotOperatorConfig) {
  const fields = {
    argument: config?.argument,
  };
  return {
    type: 'not_operator' as const,
    named: true as const,
    fields,
    argument(argument_?: Expression) { return _fs(config, notOperator, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NotOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function booleanOperator(config: BooleanOperatorConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'boolean_operator' as const,
    named: true as const,
    fields,
    left(left_?: Expression) { return _fs(config, booleanOperator, 'left', left_, fields.left); },
    operator(operator_?: "and" | "or") { return _fs(config, booleanOperator, 'operator', operator_, fields.operator); },
    right(right_?: Expression) { return _fs(config, booleanOperator, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryOperator(config: BinaryOperatorConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_operator' as const,
    named: true as const,
    fields,
    left(left_?: PrimaryExpression) { return _fs(config, binaryOperator, 'left', left_, fields.left); },
    operator(operator_?: "+" | "-" | "*" | "@" | "/" | "%" | "//" | "**" | "|" | "&" | "^" | "<<" | ">>") { return _fs(config, binaryOperator, 'operator', operator_, fields.operator); },
    right(right_?: PrimaryExpression) { return _fs(config, binaryOperator, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryOperator(config: UnaryOperatorConfig) {
  const fields = {
    operator: config?.operator,
    argument: config?.argument,
  };
  return {
    type: 'unary_operator' as const,
    named: true as const,
    fields,
    operator(operator_?: "+" | "-" | "~") { return _fs(config, unaryOperator, 'operator', operator_, fields.operator); },
    argument(argument_?: PrimaryExpression) { return _fs(config, unaryOperator, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comparisonOperator(config: ComparisonOperatorConfig) {
  const fields = {
    left: config?.left,
    operators: config?.operators,
  };
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    fields,
    left(left_?: PrimaryExpression) { return _fs(config, comparisonOperator, 'left', left_, fields.left); },
    operators(...operators_: NonEmptyArray<NotIn | IsNot>) { return _fsm(config, comparisonOperator, 'operators', operators_, fields.operators); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambda(config: LambdaConfig) {
  const fields = {
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    type: 'lambda' as const,
    named: true as const,
    fields,
    parameters(parameters_?: LambdaParameters | undefined) { return _fs(config, lambda, 'parameters', parameters_, fields.parameters); },
    body(body_?: Expression) { return _fs(config, lambda, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaWithinForInClause(config: LambdaWithinForInClauseConfig) {
  const fields = {
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    type: 'lambda_within_for_in_clause' as const,
    named: true as const,
    fields,
    parameters(parameters_?: LambdaParameters | undefined) { return _fs(config, lambdaWithinForInClause, 'parameters', parameters_, fields.parameters); },
    body(body_?: ExpressionWithinForInClause) { return _fs(config, lambdaWithinForInClause, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaWithinForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignment(config: AssignmentEqConfig | AssignmentColonConfig | AssignmentColon2Config) {
  if (config && 'left' in config && 'type' in config && 'right' in config) return assignmentColon2(config as AssignmentColon2Config);
  if (config && 'left' in config && 'right' in config) return assignmentEq(config as AssignmentEqConfig);
  return assignmentColon(config as AssignmentColonConfig);
}
export function assignmentEq(config: AssignmentEqConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: 'eq' as const,
    fields,
    left(left_?: LeftHandSide) { return _fs(config, assignmentEq, 'left', left_, fields.left); },
    right(right_?: RightHandSide) { return _fs(config, assignmentEq, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentEqTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function assignmentColon(config: AssignmentColonConfig) {
  const fields = {
    left: config?.left,
    type: config?.type,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: 'colon' as const,
    fields,
    left(left_?: LeftHandSide) { return _fs(config, assignmentColon, 'left', left_, fields.left); },
    typeField(type?: Type) { return _fs(config, assignmentColon, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentColonTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function assignmentColon2(config: AssignmentColon2Config) {
  const fields = {
    left: config?.left,
    type: config?.type,
    right: config?.right,
  };
  return {
    type: 'assignment' as const,
    named: true as const,
    variant: 'colon2' as const,
    fields,
    left(left_?: LeftHandSide) { return _fs(config, assignmentColon2, 'left', left_, fields.left); },
    typeField(type?: Type) { return _fs(config, assignmentColon2, 'type', type, fields.type); },
    right(right_?: RightHandSide) { return _fs(config, assignmentColon2, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentColon2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignment(config: AugmentedAssignmentConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    fields,
    left(left_?: LeftHandSide) { return _fs(config, augmentedAssignment, 'left', left_, fields.left); },
    operator(operator_?: "+=" | "-=" | "*=" | "/=" | "@=" | "//=" | "%=" | "**=" | ">>=" | "<<=" | "&=" | "^=" | "|=") { return _fs(config, augmentedAssignment, 'operator', operator_, fields.operator); },
    right(right_?: RightHandSide) { return _fs(config, augmentedAssignment, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function patternList(...children: Pattern[]) {
  return {
    type: 'pattern_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yield_(child: (Expression | Expressions)) {
  const children = [child];
  return {
    type: 'yield' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: AttributeConfig) {
  const fields = {
    object: config?.object,
    attribute: config?.attribute,
  };
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    object(object_?: PrimaryExpression) { return _fs(config, attribute, 'object', object_, fields.object); },
    attribute(attribute_?: Identifier) { return _fs(config, attribute, 'attribute', attribute_, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscript(config: SubscriptConfig) {
  const fields = {
    value: config?.value,
    subscript: config?.subscript,
  };
  return {
    type: 'subscript' as const,
    named: true as const,
    fields,
    value(value_?: PrimaryExpression) { return _fs(config, subscript, 'value', value_, fields.value); },
    subscript(...subscript_: NonEmptyArray<Expression | Slice>) { return _fsm(config, subscript, 'subscript', subscript_, fields.subscript); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slice(config?: SliceConfig) {
  const fields = {
    start: config?.start,
    stop: config?.stop,
    step: config?.step,
  };
  return {
    type: 'slice' as const,
    named: true as const,
    fields,
    start(start_?: Expression | undefined) { return _fs(config, slice, 'start', start_, fields.start); },
    stop(stop_?: Expression | undefined) { return _fs(config, slice, 'stop', stop_, fields.stop); },
    step(step_?: Expression | undefined) { return _fs(config, slice, 'step', step_, fields.step); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SliceTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function call(config: CallConfig) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call' as const,
    named: true as const,
    fields,
    function(function_?: PrimaryExpression) { return _fs(config, call, 'function', function_, fields.function); },
    arguments(arguments_?: GeneratorExpression | ArgumentList) { return _fs(config, call, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedParameter(config: TypedParameterConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'typed_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: Type) { return _fs(config, typedParameter, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: (Identifier | ListSplatPattern | DictionarySplatPattern)) { return typedParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function type(child: (Expression | SplatType | GenericType | UnionType | ConstrainedType | MemberType)) {
  const children = [child];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatType(config: SplatTypeConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'splat_type' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: "*" | "**") { return _fs(config, splatType, 'identifier', identifier_, fields.identifier); },
    getChild() { return children[0]; },
    setChild(child: Identifier) { return splatType({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: GenericTypeConfig) {
  const fields = {
    identifier: config?.identifier,
    type_parameter: config?.typeParameter,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    identifier(identifier_?: Identifier) { return _fs(config, genericType, 'identifier', identifier_, fields.identifier); },
    typeParameter(typeParameter_?: TypeParameter) { return _fs(config, genericType, 'typeParameter', typeParameter_, fields.type_parameter); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: UnionTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    left(left_?: Type) { return _fs(config, unionType, 'left', left_, fields.left); },
    right(right_?: Type) { return _fs(config, unionType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constrainedType(config: ConstrainedTypeConfig) {
  const fields = {
    base_type: config?.baseType,
    constraint: config?.constraint,
  };
  return {
    type: 'constrained_type' as const,
    named: true as const,
    fields,
    baseType(baseType_?: Type) { return _fs(config, constrainedType, 'baseType', baseType_, fields.base_type); },
    constraint(constraint_?: Type) { return _fs(config, constrainedType, 'constraint', constraint_, fields.constraint); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstrainedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberType(config: MemberTypeConfig) {
  const fields = {
    base_type: config?.baseType,
    identifier: config?.identifier,
  };
  return {
    type: 'member_type' as const,
    named: true as const,
    fields,
    baseType(baseType_?: Type) { return _fs(config, memberType, 'baseType', baseType_, fields.base_type); },
    identifier(identifier_?: Identifier) { return _fs(config, memberType, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordArgument(config: KeywordArgumentConfig) {
  const fields = {
    name: config?.name,
    value: config?.value,
  };
  return {
    type: 'keyword_argument' as const,
    named: true as const,
    fields,
    name(name_?: Identifier | KeywordIdentifier) { return _fs(config, keywordArgument, 'name', name_, fields.name); },
    value(value_?: Expression) { return _fs(config, keywordArgument, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordArgumentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function list(child?: CollectionElements) {
  const children = child != null ? [child] : [];
  return {
    type: 'list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function set(child: CollectionElements) {
  const children = [child];
  return {
    type: 'set' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuple(child?: CollectionElements) {
  const children = child != null ? [child] : [];
  return {
    type: 'tuple' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionary(...children: (Pair | DictionarySplat)[]) {
  return {
    type: 'dictionary' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: PairConfig) {
  const fields = {
    key: config?.key,
    value: config?.value,
  };
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    key(key_?: Expression) { return _fs(config, pair, 'key', key_, fields.key); },
    value(value_?: Expression) { return _fs(config, pair, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listComprehension(config: ListComprehensionConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'list_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: Expression) { return _fs(config, listComprehension, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ComprehensionClauses) { return listComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionaryComprehension(config: DictionaryComprehensionConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'dictionary_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: Pair) { return _fs(config, dictionaryComprehension, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ComprehensionClauses) { return dictionaryComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function setComprehension(config: SetComprehensionConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'set_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: Expression) { return _fs(config, setComprehension, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ComprehensionClauses) { return setComprehension({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorExpression(config: GeneratorExpressionConfig) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'generator_expression' as const,
    named: true as const,
    fields,
    children,
    body(body_?: Expression) { return _fs(config, generatorExpression, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ComprehensionClauses) { return generatorExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child: (Expression | Yield)) {
  const children = [child];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInClause(config: ForInClauseConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'for_in_clause' as const,
    named: true as const,
    fields,
    left(left_?: LeftHandSide) { return _fs(config, forInClause, 'left', left_, fields.left); },
    right(...right_: NonEmptyArray<string>) { return _fsm(config, forInClause, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifClause(config: IfClauseConfig) {
  const fields = {
    expression: config?.expression,
  };
  return {
    type: 'if_clause' as const,
    named: true as const,
    fields,
    expression(expression_?: Expression) { return _fs(config, ifClause, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalExpression(config: ConditionalExpressionConfig) {
  const fields = {
    body: config?.body,
    condition: config?.condition,
    alternative: config?.alternative,
  };
  return {
    type: 'conditional_expression' as const,
    named: true as const,
    fields,
    body(body_?: Expression) { return _fs(config, conditionalExpression, 'body', body_, fields.body); },
    condition(condition_?: Expression) { return _fs(config, conditionalExpression, 'condition', condition_, fields.condition); },
    alternative(alternative_?: Expression) { return _fs(config, conditionalExpression, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function concatenatedString(...children: String[]) {
  return {
    type: 'concatenated_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConcatenatedStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(config: StringConfig) {
  const fields = {
    string_start: config?.stringStart,
    content: config?.content,
    string_end: config?.stringEnd,
  };
  return {
    type: 'string' as const,
    named: true as const,
    fields,
    stringStart(stringStart_?: StringStart) { return _fs(config, string, 'stringStart', stringStart_, fields.string_start); },
    content(...content_: (Interpolation | StringContent)[]) { return _fsm(config, string, 'content', content_, fields.content); },
    stringEnd(stringEnd_?: StringEnd) { return _fs(config, string, 'stringEnd', stringEnd_, fields.string_end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringContent(...children: (EscapeInterpolation | EscapeSequence | NotEscapeSequence | _StringContent)[]) {
  _assertNonEmpty(children, 'string_content.children');
  return {
    type: 'string_content' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringContentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interpolation(config: InterpolationConfig) {
  const fields = {
    expression: config?.expression,
    type_conversion: config?.typeConversion,
    format_specifier: config?.formatSpecifier,
  };
  return {
    type: 'interpolation' as const,
    named: true as const,
    fields,
    expression(expression_?: FExpression) { return _fs(config, interpolation, 'expression', expression_, fields.expression); },
    typeConversion(typeConversion_?: TypeConversion | undefined) { return _fs(config, interpolation, 'typeConversion', typeConversion_, fields.type_conversion); },
    formatSpecifier(formatSpecifier_?: FormatSpecifier | undefined) { return _fs(config, interpolation, 'formatSpecifier', formatSpecifier_, fields.format_specifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterpolationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_sequence: text '${text}' is a reserved keyword`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function formatSpecifier(...children: Interpolation[]) {
  return {
    type: 'format_specifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeConversion(text: string) {
  if (text.length === 0) throw new Error(`type_conversion: text must be non-empty`); if (!_leafRe_typeConversion.test(text)) throw new Error(`type_conversion: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`type_conversion: text '${text}' is a reserved keyword`);
  return {
    type: 'type_conversion' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeConversionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer(text: string) {
  if (text.length === 0) throw new Error(`integer: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`integer: text '${text}' is a reserved keyword`);
  return {
    type: 'integer' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IntegerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function float(text: string) {
  if (text.length === 0) throw new Error(`float: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`float: text '${text}' is a reserved keyword`);
  return {
    type: 'float' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FloatTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`); if (!_leafRe_identifier.test(text)) throw new Error(`identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function keywordIdentifier(text: string) {
  if (text.length === 0) throw new Error(`keyword_identifier: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`keyword_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'keyword_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: KeywordIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'True' as const,
    render: () => 'True' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'True' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'True' as const },
    replace: (t: TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' as const }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'False' as const,
    render: () => 'False' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'False' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'False' as const },
    replace: (t: FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' as const }; },
  };
}

export function none() {
  return {
    type: 'none' as const,
    named: true as const,
    text: 'None' as const,
    render: () => 'None' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'None' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'None' as const },
    replace: (t: NoneTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' as const }; },
  };
}

export function await_(config: AwaitConfig) {
  const fields = {
    primary_expression: config?.primaryExpression,
  };
  return {
    type: 'await' as const,
    named: true as const,
    fields,
    primaryExpression(primaryExpression_?: PrimaryExpression) { return _fs(config, await_, 'primaryExpression', primaryExpression_, fields.primary_expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comment(text: string) {
  if (text.length === 0) throw new Error(`comment: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`comment: text '${text}' is a reserved keyword`);
  return {
    type: 'comment' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function lineContinuation(text: string) {
  if (text.length === 0) throw new Error(`line_continuation: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`line_continuation: text '${text}' is a reserved keyword`);
  return {
    type: 'line_continuation' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: LineContinuationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringStart(text: string) {
  if (text.length === 0) throw new Error(`string_start: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_start: text '${text}' is a reserved keyword`);
  return {
    type: 'string_start' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringStartTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeInterpolation(text: string) {
  if (text.length === 0) throw new Error(`escape_interpolation: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_interpolation: text '${text}' is a reserved keyword`);
  return {
    type: 'escape_interpolation' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeInterpolationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringEnd(text: string) {
  if (text.length === 0) throw new Error(`string_end: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_end: text '${text}' is a reserved keyword`);
  return {
    type: 'string_end' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringEndTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeBracket(text: string) {
  if (text.length === 0) throw new Error(`]: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`]: text '${text}' is a reserved keyword`);
  return {
    type: ']' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CloseBracketTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeParen(text: string) {
  if (text.length === 0) throw new Error(`): text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`): text '${text}' is a reserved keyword`);
  return {
    type: ')' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CloseParenTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function closeBrace(text: string) {
  if (text.length === 0) throw new Error(`}: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`}: text '${text}' is a reserved keyword`);
  return {
    type: '}' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CloseBraceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function except(text: string) {
  if (text.length === 0) throw new Error(`except: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`except: text '${text}' is a reserved keyword`);
  return {
    type: 'except' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ExceptTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function asPatternTarget(child: (ComparisonOperator | NotOperator | BooleanOperator | Lambda | PrimaryExpression | ConditionalExpression | NamedExpression | AsPattern)) {
  const children = [child];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formatExpression(config: FormatExpressionConfig) {
  const fields = {
    expression: config?.expression,
    type_conversion: config?.typeConversion,
    format_specifier: config?.formatSpecifier,
  };
  return {
    type: 'format_expression' as const,
    named: true as const,
    fields,
    expression(expression_?: FExpression) { return _fs(config, formatExpression, 'expression', expression_, fields.expression); },
    typeConversion(typeConversion_?: TypeConversion | undefined) { return _fs(config, formatExpression, 'typeConversion', typeConversion_, fields.type_conversion); },
    formatSpecifier(formatSpecifier_?: FormatSpecifier | undefined) { return _fs(config, formatExpression, 'formatSpecifier', formatSpecifier_, fields.format_specifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export type FluentKindMap = {
  "module": FluentNode<"module", ModuleConfig>;
  "import_statement": FluentNode<"import_statement", ImportStatementConfig>;
  "import_prefix": ImportPrefix;
  "relative_import": FluentNode<"relative_import", RelativeImportConfig>;
  "future_import_statement": FluentNode<"future_import_statement", FutureImportStatementConfig>;
  "import_from_statement": FluentNode<"import_from_statement", ImportFromStatementConfig>;
  "aliased_import": FluentNode<"aliased_import", AliasedImportConfig>;
  "print_statement": FluentNode<"print_statement", PrintStatementConfig>;
  "chevron": FluentNode<"chevron", ChevronConfig>;
  "assert_statement": FluentNode<"assert_statement", AssertStatementConfig>;
  "expression_statement": FluentNode<"expression_statement", ExpressionStatementConfig>;
  "named_expression": FluentNode<"named_expression", NamedExpressionConfig>;
  "return_statement": FluentNode<"return_statement", ReturnStatementConfig>;
  "delete_statement": FluentNode<"delete_statement", DeleteStatementConfig>;
  "raise_statement": FluentNode<"raise_statement", RaiseStatementConfig>;
  "pass_statement": PassStatement;
  "break_statement": BreakStatement;
  "continue_statement": ContinueStatement;
  "if_statement": FluentNode<"if_statement", IfStatementConfig>;
  "elif_clause": FluentNode<"elif_clause", ElifClauseConfig>;
  "else_clause": FluentNode<"else_clause", ElseClauseConfig>;
  "match_statement": FluentNode<"match_statement", MatchStatementConfig>;
  "case_clause": FluentNode<"case_clause", CaseClauseConfig>;
  "for_statement": FluentNode<"for_statement", ForStatementConfig>;
  "while_statement": FluentNode<"while_statement", WhileStatementConfig>;
  "try_statement": FluentNode<"try_statement", TryStatementConfig>;
  "except_clause": FluentNode<"except_clause", ExceptClauseConfig>;
  "finally_clause": FluentNode<"finally_clause", FinallyClauseConfig>;
  "with_statement": FluentNode<"with_statement", WithStatementConfig>;
  "with_clause": FluentNode<"with_clause", WithClauseConfig>;
  "with_item": FluentNode<"with_item", WithItemConfig>;
  "function_definition": FluentNode<"function_definition", FunctionDefinitionConfig>;
  "parameters": FluentNode<"parameters", ParametersConfig>;
  "lambda_parameters": FluentNode<"lambda_parameters", LambdaParametersConfig>;
  "list_splat": FluentNode<"list_splat", ListSplatConfig>;
  "dictionary_splat": FluentNode<"dictionary_splat", DictionarySplatConfig>;
  "global_statement": FluentNode<"global_statement", GlobalStatementConfig>;
  "nonlocal_statement": FluentNode<"nonlocal_statement", NonlocalStatementConfig>;
  "exec_statement": FluentNode<"exec_statement", ExecStatementConfig>;
  "type_alias_statement": FluentNode<"type_alias_statement", TypeAliasStatementConfig>;
  "class_definition": FluentNode<"class_definition", ClassDefinitionConfig>;
  "type_parameter": FluentNode<"type_parameter", TypeParameterConfig>;
  "parenthesized_list_splat": FluentNode<"parenthesized_list_splat", ParenthesizedListSplatConfig>;
  "argument_list": FluentNode<"argument_list", ArgumentListConfig>;
  "decorated_definition": FluentNode<"decorated_definition", DecoratedDefinitionConfig>;
  "decorator": FluentNode<"decorator", DecoratorConfig>;
  "block": FluentNode<"block", BlockConfig>;
  "expression_list": FluentNode<"expression_list", ExpressionListConfig>;
  "dotted_name": FluentNode<"dotted_name", DottedNameConfig>;
  "case_pattern": FluentNode<"case_pattern", CasePatternConfig>;
  "union_pattern": FluentNode<"union_pattern", UnionPatternConfig>;
  "dict_pattern": FluentNode<"dict_pattern", DictPatternConfig>;
  "keyword_pattern": FluentNode<"keyword_pattern", KeywordPatternConfig>;
  "splat_pattern": FluentNode<"splat_pattern", SplatPatternConfig>;
  "class_pattern": FluentNode<"class_pattern", ClassPatternConfig>;
  "complex_pattern": FluentNode<"complex_pattern", ComplexPatternConfig>;
  "tuple_pattern": FluentNode<"tuple_pattern", TuplePatternConfig>;
  "list_pattern": FluentNode<"list_pattern", ListPatternConfig>;
  "default_parameter": FluentNode<"default_parameter", DefaultParameterConfig>;
  "typed_default_parameter": FluentNode<"typed_default_parameter", TypedDefaultParameterConfig>;
  "list_splat_pattern": FluentNode<"list_splat_pattern", ListSplatPatternConfig>;
  "dictionary_splat_pattern": FluentNode<"dictionary_splat_pattern", DictionarySplatPatternConfig>;
  "as_pattern": FluentNode<"as_pattern", AsPatternConfig>;
  "not_operator": FluentNode<"not_operator", NotOperatorConfig>;
  "boolean_operator": FluentNode<"boolean_operator", BooleanOperatorConfig>;
  "binary_operator": FluentNode<"binary_operator", BinaryOperatorConfig>;
  "unary_operator": FluentNode<"unary_operator", UnaryOperatorConfig>;
  "comparison_operator": FluentNode<"comparison_operator", ComparisonOperatorConfig>;
  "lambda": FluentNode<"lambda", LambdaConfig>;
  "lambda_within_for_in_clause": FluentNode<"lambda_within_for_in_clause", LambdaWithinForInClauseConfig>;
  "assignment": FluentNode<"assignment", AssignmentConfig>;
  "augmented_assignment": FluentNode<"augmented_assignment", AugmentedAssignmentConfig>;
  "pattern_list": FluentNode<"pattern_list", PatternListConfig>;
  "yield": FluentNode<"yield", YieldConfig>;
  "attribute": FluentNode<"attribute", AttributeConfig>;
  "subscript": FluentNode<"subscript", SubscriptConfig>;
  "slice": FluentNode<"slice", SliceConfig>;
  "call": FluentNode<"call", CallConfig>;
  "typed_parameter": FluentNode<"typed_parameter", TypedParameterConfig>;
  "type": FluentNode<"type", TypeConfig>;
  "splat_type": FluentNode<"splat_type", SplatTypeConfig>;
  "generic_type": FluentNode<"generic_type", GenericTypeConfig>;
  "union_type": FluentNode<"union_type", UnionTypeConfig>;
  "constrained_type": FluentNode<"constrained_type", ConstrainedTypeConfig>;
  "member_type": FluentNode<"member_type", MemberTypeConfig>;
  "keyword_argument": FluentNode<"keyword_argument", KeywordArgumentConfig>;
  "list": FluentNode<"list", ListConfig>;
  "set": FluentNode<"set", SetConfig>;
  "tuple": FluentNode<"tuple", TupleConfig>;
  "dictionary": FluentNode<"dictionary", DictionaryConfig>;
  "pair": FluentNode<"pair", PairConfig>;
  "list_comprehension": FluentNode<"list_comprehension", ListComprehensionConfig>;
  "dictionary_comprehension": FluentNode<"dictionary_comprehension", DictionaryComprehensionConfig>;
  "set_comprehension": FluentNode<"set_comprehension", SetComprehensionConfig>;
  "generator_expression": FluentNode<"generator_expression", GeneratorExpressionConfig>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", ParenthesizedExpressionConfig>;
  "for_in_clause": FluentNode<"for_in_clause", ForInClauseConfig>;
  "if_clause": FluentNode<"if_clause", IfClauseConfig>;
  "conditional_expression": FluentNode<"conditional_expression", ConditionalExpressionConfig>;
  "concatenated_string": FluentNode<"concatenated_string", ConcatenatedStringConfig>;
  "string": FluentNode<"string", StringConfig>;
  "string_content": FluentNode<"string_content", StringContentConfig>;
  "interpolation": FluentNode<"interpolation", InterpolationConfig>;
  "escape_sequence": EscapeSequence;
  "format_specifier": FluentNode<"format_specifier", FormatSpecifierConfig>;
  "type_conversion": TypeConversion;
  "integer": Integer;
  "float": Float;
  "identifier": Identifier;
  "keyword_identifier": KeywordIdentifier;
  "true": True;
  "false": False;
  "none": None;
  "await": FluentNode<"await", AwaitConfig>;
  "comment": Comment;
  "line_continuation": LineContinuation;
  "string_start": StringStart;
  "escape_interpolation": EscapeInterpolation;
  "string_end": StringEnd;
  "]": CloseBracket;
  ")": CloseParen;
  "}": CloseBrace;
  "except": Except;
  "as_pattern_target": FluentNode<"as_pattern_target", AsPatternTargetConfig>;
  "format_expression": FluentNode<"format_expression", FormatExpressionConfig>;
};

export const _factoryMap = {
  "module": module,
  "import_statement": importStatement,
  "import_prefix": importPrefix,
  "relative_import": relativeImport,
  "future_import_statement": futureImportStatement,
  "import_from_statement": importFromStatement,
  "aliased_import": aliasedImport,
  "print_statement": printStatement,
  "chevron": chevron,
  "assert_statement": assertStatement,
  "expression_statement": expressionStatement,
  "named_expression": namedExpression,
  "return_statement": returnStatement,
  "delete_statement": deleteStatement,
  "raise_statement": raiseStatement,
  "pass_statement": passStatement,
  "break_statement": breakStatement,
  "continue_statement": continueStatement,
  "if_statement": ifStatement,
  "elif_clause": elifClause,
  "else_clause": elseClause,
  "match_statement": matchStatement,
  "case_clause": caseClause,
  "for_statement": forStatement,
  "while_statement": whileStatement,
  "try_statement": tryStatement,
  "except_clause": exceptClause,
  "finally_clause": finallyClause,
  "with_statement": withStatement,
  "with_clause": withClause,
  "with_item": withItem,
  "function_definition": functionDefinition,
  "parameters": parameters,
  "lambda_parameters": lambdaParameters,
  "list_splat": listSplat,
  "dictionary_splat": dictionarySplat,
  "global_statement": globalStatement,
  "nonlocal_statement": nonlocalStatement,
  "exec_statement": execStatement,
  "type_alias_statement": typeAliasStatement,
  "class_definition": classDefinition,
  "type_parameter": typeParameter,
  "parenthesized_list_splat": parenthesizedListSplat,
  "argument_list": argumentList,
  "decorated_definition": decoratedDefinition,
  "decorator": decorator,
  "block": block,
  "expression_list": expressionList,
  "dotted_name": dottedName,
  "case_pattern": casePattern,
  "union_pattern": unionPattern,
  "dict_pattern": dictPattern,
  "keyword_pattern": keywordPattern,
  "splat_pattern": splatPattern,
  "class_pattern": classPattern,
  "complex_pattern": complexPattern,
  "tuple_pattern": tuplePattern,
  "list_pattern": listPattern,
  "default_parameter": defaultParameter,
  "typed_default_parameter": typedDefaultParameter,
  "list_splat_pattern": listSplatPattern,
  "dictionary_splat_pattern": dictionarySplatPattern,
  "as_pattern": asPattern,
  "not_operator": notOperator,
  "boolean_operator": booleanOperator,
  "binary_operator": binaryOperator,
  "unary_operator": unaryOperator,
  "comparison_operator": comparisonOperator,
  "lambda": lambda,
  "lambda_within_for_in_clause": lambdaWithinForInClause,
  "assignment": assignment,
  "augmented_assignment": augmentedAssignment,
  "pattern_list": patternList,
  "yield": yield_,
  "attribute": attribute,
  "subscript": subscript,
  "slice": slice,
  "call": call,
  "typed_parameter": typedParameter,
  "type": type,
  "splat_type": splatType,
  "generic_type": genericType,
  "union_type": unionType,
  "constrained_type": constrainedType,
  "member_type": memberType,
  "keyword_argument": keywordArgument,
  "list": list,
  "set": set,
  "tuple": tuple,
  "dictionary": dictionary,
  "pair": pair,
  "list_comprehension": listComprehension,
  "dictionary_comprehension": dictionaryComprehension,
  "set_comprehension": setComprehension,
  "generator_expression": generatorExpression,
  "parenthesized_expression": parenthesizedExpression,
  "for_in_clause": forInClause,
  "if_clause": ifClause,
  "conditional_expression": conditionalExpression,
  "concatenated_string": concatenatedString,
  "string": string,
  "string_content": stringContent,
  "interpolation": interpolation,
  "escape_sequence": escapeSequence,
  "format_specifier": formatSpecifier,
  "type_conversion": typeConversion,
  "integer": integer,
  "float": float,
  "identifier": identifier,
  "keyword_identifier": keywordIdentifier,
  "true": true_,
  "false": false_,
  "none": none,
  "await": await_,
  "comment": comment,
  "line_continuation": lineContinuation,
  "string_start": stringStart,
  "escape_interpolation": escapeInterpolation,
  "string_end": stringEnd,
  "]": closeBracket,
  ")": closeParen,
  "}": closeBrace,
  "except": except,
  "as_pattern_target": asPatternTarget,
  "format_expression": formatExpression,
} as const;
export type _FactoryMap = typeof _factoryMap;

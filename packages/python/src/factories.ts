// Auto-generated by @sittir/codegen — do not edit

import type { NodeData } from './types.js';
import type { AliasedImport, AliasedImportConfig, AliasedImportTree, ArgumentList, ArgumentListConfig, ArgumentListTree, AsPattern, AsPatternConfig, AsPatternTree, AssertStatement, AssertStatementConfig, AssertStatementTree, Assignment, AssignmentConfig, AssignmentTree, Attribute, AttributeConfig, AttributeTree, AugmentedAssignment, AugmentedAssignmentConfig, AugmentedAssignmentTree, Await, AwaitConfig, AwaitTree, BinaryOperator, BinaryOperatorConfig, BinaryOperatorTree, Block, BlockConfig, BlockTree, BooleanOperator, BooleanOperatorConfig, BooleanOperatorTree, BreakStatement, BreakStatementTree, Call, CallConfig, CallTree, CaseClause, CaseClauseConfig, CaseClauseTree, CasePattern, CasePatternConfig, CasePatternTree, Chevron, ChevronConfig, ChevronTree, ClassDefinition, ClassDefinitionConfig, ClassDefinitionTree, ClassPattern, ClassPatternConfig, ClassPatternTree, Comment, CommentTree, ComparisonOperator, ComparisonOperatorConfig, ComparisonOperatorTree, ComplexPattern, ComplexPatternConfig, ComplexPatternTree, CompoundStatement, ConcatenatedString, ConcatenatedStringConfig, ConcatenatedStringTree, ConditionalExpression, ConditionalExpressionConfig, ConditionalExpressionTree, ConstrainedType, ConstrainedTypeConfig, ConstrainedTypeTree, ContinueStatement, ContinueStatementTree, DecoratedDefinition, DecoratedDefinitionConfig, DecoratedDefinitionTree, Decorator, DecoratorConfig, DecoratorTree, DefaultParameter, DefaultParameterConfig, DefaultParameterTree, DeleteStatement, DeleteStatementConfig, DeleteStatementTree, DictPattern, DictPatternConfig, DictPatternTree, Dictionary, DictionaryComprehension, DictionaryComprehensionConfig, DictionaryComprehensionTree, DictionaryConfig, DictionarySplat, DictionarySplatConfig, DictionarySplatPattern, DictionarySplatPatternConfig, DictionarySplatPatternTree, DictionarySplatTree, DictionaryTree, DottedName, DottedNameConfig, DottedNameTree, ElifClause, ElifClauseConfig, ElifClauseTree, Ellipsis, EllipsisTree, ElseClause, ElseClauseConfig, ElseClauseTree, EscapeInterpolation, EscapeInterpolationTree, EscapeSequence, EscapeSequenceTree, ExceptClause, ExceptClauseConfig, ExceptClauseTree, ExecStatement, ExecStatementConfig, ExecStatementTree, Expression, ExpressionList, ExpressionListConfig, ExpressionListTree, ExpressionStatement, ExpressionStatementConfig, ExpressionStatementTree, False, FalseTree, FinallyClause, FinallyClauseConfig, FinallyClauseTree, Float, FloatTree, ForInClause, ForInClauseConfig, ForInClauseTree, ForStatement, ForStatementConfig, ForStatementTree, FormatExpression, FormatExpressionConfig, FormatExpressionTree, FormatSpecifier, FormatSpecifierConfig, FormatSpecifierTree, FunctionDefinition, FunctionDefinitionConfig, FunctionDefinitionTree, FutureImportStatement, FutureImportStatementConfig, FutureImportStatementTree, GeneratorExpression, GeneratorExpressionConfig, GeneratorExpressionTree, GenericType, GenericTypeConfig, GenericTypeTree, GlobalStatement, GlobalStatementConfig, GlobalStatementTree, Identifier, IdentifierTree, IfClause, IfClauseConfig, IfClauseTree, IfStatement, IfStatementConfig, IfStatementTree, ImportFromStatement, ImportFromStatementConfig, ImportFromStatementTree, ImportPrefix, ImportPrefixTree, ImportStatement, ImportStatementConfig, ImportStatementTree, Integer, IntegerTree, Interpolation, InterpolationConfig, InterpolationTree, KeywordArgument, KeywordArgumentConfig, KeywordArgumentTree, KeywordPattern, KeywordPatternConfig, KeywordPatternTree, KeywordSeparator, KeywordSeparatorTree, Lambda, LambdaConfig, LambdaParameters, LambdaParametersConfig, LambdaParametersTree, LambdaTree, LineContinuation, LineContinuationTree, List, ListComprehension, ListComprehensionConfig, ListComprehensionTree, ListConfig, ListPattern, ListPatternConfig, ListPatternTree, ListSplat, ListSplatConfig, ListSplatPattern, ListSplatPatternConfig, ListSplatPatternTree, ListSplatTree, ListTree, MatchStatement, MatchStatementConfig, MatchStatementTree, MemberType, MemberTypeConfig, MemberTypeTree, Module, ModuleConfig, ModuleTree, NamedExpression, NamedExpressionConfig, NamedExpressionTree, None, NoneTree, NonlocalStatement, NonlocalStatementConfig, NonlocalStatementTree, NotOperator, NotOperatorConfig, NotOperatorTree, Pair, PairConfig, PairTree, Parameter, Parameters, ParametersConfig, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionConfig, ParenthesizedExpressionTree, ParenthesizedListSplat, ParenthesizedListSplatConfig, ParenthesizedListSplatTree, PassStatement, PassStatementTree, Pattern, PatternList, PatternListConfig, PatternListTree, PositionalSeparator, PositionalSeparatorTree, PrimaryExpression, PrintStatement, PrintStatementConfig, PrintStatementTree, RaiseStatement, RaiseStatementConfig, RaiseStatementTree, RelativeImport, RelativeImportConfig, RelativeImportTree, ReturnStatement, ReturnStatementConfig, ReturnStatementTree, Set, SetComprehension, SetComprehensionConfig, SetComprehensionTree, SetConfig, SetTree, SimpleStatement, Slice, SliceConfig, SliceTree, SplatPattern, SplatPatternConfig, SplatPatternTree, SplatType, SplatTypeConfig, SplatTypeTree, String, StringConfig, StringContent, StringContentConfig, StringContentTree, StringEnd, StringEndTree, StringStart, StringStartTree, StringTree, Subscript, SubscriptConfig, SubscriptTree, True, TrueTree, TryStatement, TryStatementConfig, TryStatementTree, Tuple, TupleConfig, TuplePattern, TuplePatternConfig, TuplePatternTree, TupleTree, Type, TypeAliasStatement, TypeAliasStatementConfig, TypeAliasStatementTree, TypeConfig, TypeConversion, TypeConversionTree, TypeParameter, TypeParameterConfig, TypeParameterTree, TypeTree, TypedDefaultParameter, TypedDefaultParameterConfig, TypedDefaultParameterTree, TypedParameter, TypedParameterConfig, TypedParameterTree, UnaryOperator, UnaryOperatorConfig, UnaryOperatorTree, UnionPattern, UnionPatternConfig, UnionPatternTree, UnionType, UnionTypeConfig, UnionTypeTree, WhileStatement, WhileStatementConfig, WhileStatementTree, WildcardImport, WildcardImportTree, WithClause, WithClauseConfig, WithClauseTree, WithItem, WithItemConfig, WithItemTree, WithStatement, WithStatementConfig, WithStatementTree, Yield, YieldConfig, YieldTree } from './types.js';
import type { Edit, AnyNodeData } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { rules } from './rules.js';
import { joinBy } from './joinby.js';

const { render, toEdit } = createRenderer(rules, joinBy);

const RESERVED_KEYWORDS = new Set([
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'case',
  'class',
  'continue',
  'def',
  'del',
  'elif',
  'else',
  'except',
  'exec',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'match',
  'nonlocal',
  'not',
  'or',
  'pass',
  'print',
  'raise',
  'return',
  'try',
  'while',
  'with',
]);

export function aliasedImport(
  config: AliasedImportConfig,
) {
  return {
    type: 'aliased_import' as const,
    alias: (v: Identifier) => aliasedImport({ ...config, 'alias': v }),
    name: (v: DottedName) => aliasedImport({ ...config, 'name': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AliasedImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function argumentList(
  config?: ArgumentListConfig,
) {
  return {
    type: 'argument_list' as const,
    children0: (v: DictionarySplat | Expression | KeywordArgument | ListSplat | ParenthesizedExpression) => argumentList({ ...config, children0: v }),
    children1: (...v: (DictionarySplat | Expression | KeywordArgument | ListSplat | ParenthesizedExpression)[]) => argumentList({ ...config, children1: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function asPattern(
  config: AsPatternConfig,
) {
  return {
    type: 'as_pattern' as const,
    alias: (v: string) => asPattern({ ...config, 'alias': v }),
    children: (v: CasePattern | Expression | Identifier) => asPattern({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assertStatement(
  config: AssertStatementConfig,
) {
  return {
    type: 'assert_statement' as const,
    expression1: (v: Expression) => assertStatement({ ...config, expression1: v }),
    expression2: (...v: (Expression)[]) => assertStatement({ ...config, expression2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment(
  config: AssignmentConfig,
) {
  return {
    type: 'assignment' as const,
    left: (v: Pattern | PatternList) => assignment({ ...config, 'left': v }),
    right: (v: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | Yield) => assignment({ ...config, 'right': v }),
    typeField: (v: Type) => assignment({ ...config, 'type': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function attribute(
  config: AttributeConfig,
) {
  return {
    type: 'attribute' as const,
    attribute: (v: Identifier) => attribute({ ...config, 'attribute': v }),
    object: (v: PrimaryExpression) => attribute({ ...config, 'object': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function augmentedAssignment(
  config: AugmentedAssignmentConfig,
) {
  return {
    type: 'augmented_assignment' as const,
    left: (v: Pattern | PatternList) => augmentedAssignment({ ...config, 'left': v }),
    operator: (v: '+=' | '-=' | '*=' | '/=' | '@=' | '//=' | '%=' | '**=' | '>>=' | '<<=' | '&=' | '^=' | '|=') => augmentedAssignment({ ...config, 'operator': { type: v, text: v } as const }),
    right: (v: Assignment | AugmentedAssignment | Expression | ExpressionList | PatternList | Yield) => augmentedAssignment({ ...config, 'right': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function await_(
  config: AwaitConfig,
) {
  return {
    type: 'await' as const,
    children: (v: PrimaryExpression) => await_({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function binaryOperator(
  config: BinaryOperatorConfig,
) {
  return {
    type: 'binary_operator' as const,
    left: (v: PrimaryExpression) => binaryOperator({ ...config, 'left': v }),
    operator: (v: '%' | '&' | '*' | '**' | '+' | '-' | '/' | '//' | '<<' | '>>' | '@' | '^' | '|') => binaryOperator({ ...config, 'operator': { type: v, text: v } as const }),
    right: (v: PrimaryExpression) => binaryOperator({ ...config, 'right': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function block(
  config?: BlockConfig,
) {
  return {
    type: 'block' as const,
    alternative: (...v: (CaseClause)[]) => block({ ...config, 'alternative': v }),
    children: (...v: (CompoundStatement | SimpleStatement)[]) => block({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function booleanOperator(
  config: BooleanOperatorConfig,
) {
  return {
    type: 'boolean_operator' as const,
    left: (v: Expression) => booleanOperator({ ...config, 'left': v }),
    operator: (v: 'and' | 'or') => booleanOperator({ ...config, 'operator': { type: v, text: v } as const }),
    right: (v: Expression) => booleanOperator({ ...config, 'right': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call(
  config: CallConfig,
) {
  return {
    type: 'call' as const,
    arguments: (v: ArgumentList | GeneratorExpression) => call({ ...config, 'arguments': v }),
    function: (v: PrimaryExpression) => call({ ...config, 'function': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function caseClause(
  config: CaseClauseConfig,
) {
  return {
    type: 'case_clause' as const,
    consequence: (v: Block) => caseClause({ ...config, 'consequence': v }),
    guard: (v: IfClause) => caseClause({ ...config, 'guard': v }),
    casePattern1: (v: CasePattern) => caseClause({ ...config, casePattern1: v }),
    casePattern2: (...v: (CasePattern)[]) => caseClause({ ...config, casePattern2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CaseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function casePattern(
  config?: CasePatternConfig,
) {
  return {
    type: 'case_pattern' as const,
    children: (v: AsPattern | ClassPattern | ComplexPattern | ConcatenatedString | DictPattern | DottedName | False | Float | Integer | KeywordPattern | ListPattern | None | SplatPattern | String | True | TuplePattern | UnionPattern) => casePattern({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function chevron(
  config: ChevronConfig,
) {
  return {
    type: 'chevron' as const,
    children: (v: Expression) => chevron({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ChevronTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function classDefinition(
  config: ClassDefinitionConfig,
) {
  return {
    type: 'class_definition' as const,
    body: (v: Block) => classDefinition({ ...config, 'body': v }),
    name: (v: Identifier) => classDefinition({ ...config, 'name': v }),
    superclasses: (v: ArgumentList) => classDefinition({ ...config, 'superclasses': v }),
    typeParameters: (v: TypeParameter) => classDefinition({ ...config, 'type_parameters': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function classPattern(
  config: ClassPatternConfig,
) {
  return {
    type: 'class_pattern' as const,
    dottedName: (v: DottedName) => classPattern({ ...config, dottedName: v }),
    casePattern1: (v: CasePattern) => classPattern({ ...config, casePattern1: v }),
    casePattern2: (...v: (CasePattern)[]) => classPattern({ ...config, casePattern2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function comparisonOperator(
  config: ComparisonOperatorConfig,
) {
  return {
    type: 'comparison_operator' as const,
    operators: (...v: ('<' | '<=' | '==' | '!=' | '>=' | '>' | '<>' | 'in' | 'is' | 'is not' | 'not in')[]) => comparisonOperator({ ...config, 'operators': v.map(t => ({ type: t, text: t }) as const) }),
    primaryExpression1: (v: PrimaryExpression) => comparisonOperator({ ...config, primaryExpression1: v }),
    primaryExpression2: (...v: (PrimaryExpression)[]) => comparisonOperator({ ...config, primaryExpression2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function complexPattern(
  config: ComplexPatternConfig,
) {
  return {
    type: 'complex_pattern' as const,
    children0: (v: Float | Integer) => complexPattern({ ...config, children0: v }),
    children1: (v: Float | Integer) => complexPattern({ ...config, children1: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function concatenatedString(
  config: ConcatenatedStringConfig,
) {
  return {
    type: 'concatenated_string' as const,
    string1: (v: String) => concatenatedString({ ...config, string1: v }),
    string2: (...v: (String)[]) => concatenatedString({ ...config, string2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConcatenatedStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function conditionalExpression(
  config: ConditionalExpressionConfig,
) {
  return {
    type: 'conditional_expression' as const,
    expression1: (v: Expression) => conditionalExpression({ ...config, expression1: v }),
    expression2: (v: Expression) => conditionalExpression({ ...config, expression2: v }),
    expression3: (v: Expression) => conditionalExpression({ ...config, expression3: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function constrainedType(
  config: ConstrainedTypeConfig,
) {
  return {
    type: 'constrained_type' as const,
    type1: (v: Type) => constrainedType({ ...config, type1: v }),
    type2: (v: Type) => constrainedType({ ...config, type2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstrainedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decoratedDefinition(
  config: DecoratedDefinitionConfig,
) {
  return {
    type: 'decorated_definition' as const,
    definition: (v: ClassDefinition | FunctionDefinition) => decoratedDefinition({ ...config, 'definition': v }),
    children: (...v: (Decorator)[]) => decoratedDefinition({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratedDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function decorator(
  config: DecoratorConfig,
) {
  return {
    type: 'decorator' as const,
    children: (v: Expression) => decorator({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function defaultParameter(
  config: DefaultParameterConfig,
) {
  return {
    type: 'default_parameter' as const,
    name: (v: Identifier | TuplePattern) => defaultParameter({ ...config, 'name': v }),
    value: (v: Expression) => defaultParameter({ ...config, 'value': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function deleteStatement(
  config: DeleteStatementConfig,
) {
  return {
    type: 'delete_statement' as const,
    children: (v: Expression | ExpressionList) => deleteStatement({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeleteStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictPattern(
  config?: DictPatternConfig,
) {
  return {
    type: 'dict_pattern' as const,
    splatPattern1: (v: SplatPattern) => dictPattern({ ...config, splatPattern1: v }),
    splatPattern2: (...v: (SplatPattern)[]) => dictPattern({ ...config, splatPattern2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionary(
  config?: DictionaryConfig,
) {
  return {
    type: 'dictionary' as const,
    children0: (v: DictionarySplat | Pair) => dictionary({ ...config, children0: v }),
    children1: (...v: (DictionarySplat | Pair)[]) => dictionary({ ...config, children1: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionaryComprehension(
  config: DictionaryComprehensionConfig,
) {
  return {
    type: 'dictionary_comprehension' as const,
    body: (v: Pair) => dictionaryComprehension({ ...config, 'body': v }),
    forInClause: (v: ForInClause) => dictionaryComprehension({ ...config, forInClause: v }),
    children1: (...v: (ForInClause | IfClause)[]) => dictionaryComprehension({ ...config, children1: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionarySplat(
  config: DictionarySplatConfig,
) {
  return {
    type: 'dictionary_splat' as const,
    children: (v: Expression) => dictionarySplat({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dictionarySplatPattern(
  config: DictionarySplatPatternConfig,
) {
  return {
    type: 'dictionary_splat_pattern' as const,
    children: (v: Attribute | Identifier | Subscript) => dictionarySplatPattern({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dottedName(
  config: DottedNameConfig,
) {
  return {
    type: 'dotted_name' as const,
    identifier1: (v: Identifier) => dottedName({ ...config, identifier1: v }),
    identifier2: (...v: (Identifier)[]) => dottedName({ ...config, identifier2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DottedNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function elifClause(
  config: ElifClauseConfig,
) {
  return {
    type: 'elif_clause' as const,
    condition: (v: Expression) => elifClause({ ...config, 'condition': v }),
    consequence: (v: Block) => elifClause({ ...config, 'consequence': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElifClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function elseClause(
  config: ElseClauseConfig,
) {
  return {
    type: 'else_clause' as const,
    body: (v: Block) => elseClause({ ...config, 'body': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function exceptClause(
  config: ExceptClauseConfig,
) {
  return {
    type: 'except_clause' as const,
    alias: (v: Expression) => exceptClause({ ...config, 'alias': v }),
    value: (...v: (Expression)[]) => exceptClause({ ...config, 'value': v }),
    children: (v: Block) => exceptClause({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExceptClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function execStatement(
  config: ExecStatementConfig,
) {
  return {
    type: 'exec_statement' as const,
    code: (v: Identifier | String) => execStatement({ ...config, 'code': v }),
    expression1: (v: Expression) => execStatement({ ...config, expression1: v }),
    expression2: (...v: (Expression)[]) => execStatement({ ...config, expression2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExecStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expressionList(
  config: ExpressionListConfig,
) {
  return {
    type: 'expression_list' as const,
    expression1: (v: Expression) => expressionList({ ...config, expression1: v }),
    expression2: (...v: (Expression)[]) => expressionList({ ...config, expression2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expressionStatement(
  config: ExpressionStatementConfig,
) {
  return {
    type: 'expression_statement' as const,
    children0: (v: Assignment | AugmentedAssignment | Expression | Yield) => expressionStatement({ ...config, children0: v }),
    expression: (...v: (Expression)[]) => expressionStatement({ ...config, expression: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function finallyClause(
  config: FinallyClauseConfig,
) {
  return {
    type: 'finally_clause' as const,
    children: (v: Block) => finallyClause({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function forInClause(
  config: ForInClauseConfig,
) {
  return {
    type: 'for_in_clause' as const,
    left: (v: Pattern | PatternList) => forInClause({ ...config, 'left': v }),
    right: (...v: (Expression)[]) => forInClause({ ...config, 'right': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function forStatement(
  config: ForStatementConfig,
) {
  return {
    type: 'for_statement' as const,
    alternative: (v: ElseClause) => forStatement({ ...config, 'alternative': v }),
    body: (v: Block) => forStatement({ ...config, 'body': v }),
    left: (v: Pattern | PatternList) => forStatement({ ...config, 'left': v }),
    right: (v: Expression | ExpressionList) => forStatement({ ...config, 'right': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function formatExpression(
  config: FormatExpressionConfig,
) {
  return {
    type: 'format_expression' as const,
    expression: (v: Expression | ExpressionList | PatternList | Yield) => formatExpression({ ...config, 'expression': v }),
    formatSpecifier: (v: FormatSpecifier) => formatExpression({ ...config, 'format_specifier': v }),
    typeConversion: (v: TypeConversion) => formatExpression({ ...config, 'type_conversion': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function formatSpecifier(
  config?: FormatSpecifierConfig,
) {
  return {
    type: 'format_specifier' as const,
    children: (...v: (FormatExpression)[]) => formatSpecifier({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function functionDefinition(
  config: FunctionDefinitionConfig,
) {
  return {
    type: 'function_definition' as const,
    body: (v: Block) => functionDefinition({ ...config, 'body': v }),
    name: (v: Identifier) => functionDefinition({ ...config, 'name': v }),
    parameters: (v: Parameters) => functionDefinition({ ...config, 'parameters': v }),
    returnType: (v: Type) => functionDefinition({ ...config, 'return_type': v }),
    typeParameters: (v: TypeParameter) => functionDefinition({ ...config, 'type_parameters': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function futureImportStatement(
  config: FutureImportStatementConfig,
) {
  return {
    type: 'future_import_statement' as const,
    name: (...v: (AliasedImport | DottedName)[]) => futureImportStatement({ ...config, 'name': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generatorExpression(
  config: GeneratorExpressionConfig,
) {
  return {
    type: 'generator_expression' as const,
    body: (v: Expression) => generatorExpression({ ...config, 'body': v }),
    forInClause: (v: ForInClause) => generatorExpression({ ...config, forInClause: v }),
    children1: (...v: (ForInClause | IfClause)[]) => generatorExpression({ ...config, children1: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function genericType(
  config: GenericTypeConfig,
) {
  return {
    type: 'generic_type' as const,
    identifier: (v: Identifier) => genericType({ ...config, identifier: v }),
    typeParameter: (v: TypeParameter) => genericType({ ...config, typeParameter: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function globalStatement(
  config: GlobalStatementConfig,
) {
  return {
    type: 'global_statement' as const,
    identifier1: (v: Identifier) => globalStatement({ ...config, identifier1: v }),
    identifier2: (...v: (Identifier)[]) => globalStatement({ ...config, identifier2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GlobalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ifClause(
  config: IfClauseConfig,
) {
  return {
    type: 'if_clause' as const,
    children: (v: Expression) => ifClause({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ifStatement(
  config: IfStatementConfig,
) {
  return {
    type: 'if_statement' as const,
    alternative: (...v: (ElifClause | ElseClause)[]) => ifStatement({ ...config, 'alternative': v }),
    condition: (v: Expression) => ifStatement({ ...config, 'condition': v }),
    consequence: (v: Block) => ifStatement({ ...config, 'consequence': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function importFromStatement(
  config: ImportFromStatementConfig,
) {
  return {
    type: 'import_from_statement' as const,
    moduleName: (v: DottedName | RelativeImport) => importFromStatement({ ...config, 'module_name': v }),
    name: (...v: (AliasedImport | DottedName)[]) => importFromStatement({ ...config, 'name': v }),
    children: (v: WildcardImport) => importFromStatement({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function importStatement(
  config: ImportStatementConfig,
) {
  return {
    type: 'import_statement' as const,
    name: (...v: (AliasedImport | DottedName)[]) => importStatement({ ...config, 'name': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function interpolation(
  config: InterpolationConfig,
) {
  return {
    type: 'interpolation' as const,
    expression: (v: Expression | ExpressionList | PatternList | Yield) => interpolation({ ...config, 'expression': v }),
    formatSpecifier: (v: FormatSpecifier) => interpolation({ ...config, 'format_specifier': v }),
    typeConversion: (v: TypeConversion) => interpolation({ ...config, 'type_conversion': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterpolationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function keywordArgument(
  config: KeywordArgumentConfig,
) {
  return {
    type: 'keyword_argument' as const,
    name: (v: Identifier) => keywordArgument({ ...config, 'name': v }),
    value: (v: Expression) => keywordArgument({ ...config, 'value': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordArgumentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function keywordPattern(
  config: KeywordPatternConfig,
) {
  return {
    type: 'keyword_pattern' as const,
    identifier: (v: Identifier) => keywordPattern({ ...config, identifier: v }),
    children1: (v: ClassPattern | ComplexPattern | ConcatenatedString | DictPattern | DottedName | False | Float | Integer | ListPattern | None | SplatPattern | String | True | TuplePattern | UnionPattern) => keywordPattern({ ...config, children1: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lambda(
  config: LambdaConfig,
) {
  return {
    type: 'lambda' as const,
    body: (v: Expression) => lambda({ ...config, 'body': v }),
    parameters: (v: LambdaParameters) => lambda({ ...config, 'parameters': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lambdaParameters(
  config: LambdaParametersConfig,
) {
  return {
    type: 'lambda_parameters' as const,
    children: (...v: (Parameter)[]) => lambdaParameters({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function list(
  config?: ListConfig,
) {
  return {
    type: 'list' as const,
    children: (...v: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]) => list({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function listComprehension(
  config: ListComprehensionConfig,
) {
  return {
    type: 'list_comprehension' as const,
    body: (v: Expression) => listComprehension({ ...config, 'body': v }),
    forInClause: (v: ForInClause) => listComprehension({ ...config, forInClause: v }),
    children1: (...v: (ForInClause | IfClause)[]) => listComprehension({ ...config, children1: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function listPattern(
  config?: ListPatternConfig,
) {
  return {
    type: 'list_pattern' as const,
    children: (...v: (CasePattern | Pattern)[]) => listPattern({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function listSplat(
  config: ListSplatConfig,
) {
  return {
    type: 'list_splat' as const,
    children: (v: Attribute | Expression | Identifier | Subscript) => listSplat({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function listSplatPattern(
  config: ListSplatPatternConfig,
) {
  return {
    type: 'list_splat_pattern' as const,
    children: (v: Attribute | Identifier | Subscript) => listSplatPattern({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function matchStatement(
  config: MatchStatementConfig,
) {
  return {
    type: 'match_statement' as const,
    body: (v: Block) => matchStatement({ ...config, 'body': v }),
    subject: (...v: (Expression)[]) => matchStatement({ ...config, 'subject': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function memberType(
  config: MemberTypeConfig,
) {
  return {
    type: 'member_type' as const,
    type: (v: Type) => memberType({ ...config, type: v }),
    identifier: (v: Identifier) => memberType({ ...config, identifier: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function module(
  config?: ModuleConfig,
) {
  return {
    type: 'module' as const,
    children: (...v: (CompoundStatement | SimpleStatement)[]) => module({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function namedExpression(
  config: NamedExpressionConfig,
) {
  return {
    type: 'named_expression' as const,
    name: (v: Identifier) => namedExpression({ ...config, 'name': v }),
    value: (v: Expression) => namedExpression({ ...config, 'value': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function nonlocalStatement(
  config: NonlocalStatementConfig,
) {
  return {
    type: 'nonlocal_statement' as const,
    identifier1: (v: Identifier) => nonlocalStatement({ ...config, identifier1: v }),
    identifier2: (...v: (Identifier)[]) => nonlocalStatement({ ...config, identifier2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonlocalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function notOperator(
  config: NotOperatorConfig,
) {
  return {
    type: 'not_operator' as const,
    argument: (v: Expression) => notOperator({ ...config, 'argument': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NotOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pair(
  config: PairConfig,
) {
  return {
    type: 'pair' as const,
    key: (v: Expression) => pair({ ...config, 'key': v }),
    value: (v: Expression) => pair({ ...config, 'value': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parameters(
  config?: ParametersConfig,
) {
  return {
    type: 'parameters' as const,
    children: (...v: (Parameter)[]) => parameters({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesizedExpression(
  config: ParenthesizedExpressionConfig,
) {
  return {
    type: 'parenthesized_expression' as const,
    children: (v: Expression | ListSplat | ParenthesizedExpression | Yield) => parenthesizedExpression({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesizedListSplat(
  config: ParenthesizedListSplatConfig,
) {
  return {
    type: 'parenthesized_list_splat' as const,
    children: (v: ListSplat | ParenthesizedExpression) => parenthesizedListSplat({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function patternList(
  config: PatternListConfig,
) {
  return {
    type: 'pattern_list' as const,
    pattern1: (v: Pattern) => patternList({ ...config, pattern1: v }),
    pattern2: (...v: (Pattern)[]) => patternList({ ...config, pattern2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function printStatement(
  config?: PrintStatementConfig,
) {
  return {
    type: 'print_statement' as const,
    argument: (...v: (Expression)[]) => printStatement({ ...config, 'argument': v }),
    children: (v: Chevron) => printStatement({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function raiseStatement(
  config?: RaiseStatementConfig,
) {
  return {
    type: 'raise_statement' as const,
    cause: (v: Expression) => raiseStatement({ ...config, 'cause': v }),
    children: (v: Expression | ExpressionList) => raiseStatement({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RaiseStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function relativeImport(
  config: RelativeImportConfig,
) {
  return {
    type: 'relative_import' as const,
    importPrefix: (v: ImportPrefix) => relativeImport({ ...config, importPrefix: v }),
    dottedName: (v: DottedName) => relativeImport({ ...config, dottedName: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RelativeImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function returnStatement(
  config?: ReturnStatementConfig,
) {
  return {
    type: 'return_statement' as const,
    children: (v: Expression | ExpressionList) => returnStatement({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function set(
  config: SetConfig,
) {
  return {
    type: 'set' as const,
    children: (...v: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]) => set({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function setComprehension(
  config: SetComprehensionConfig,
) {
  return {
    type: 'set_comprehension' as const,
    body: (v: Expression) => setComprehension({ ...config, 'body': v }),
    forInClause: (v: ForInClause) => setComprehension({ ...config, forInClause: v }),
    children1: (...v: (ForInClause | IfClause)[]) => setComprehension({ ...config, children1: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function slice(
  config?: SliceConfig,
) {
  return {
    type: 'slice' as const,
    expression1: (v: Expression) => slice({ ...config, expression1: v }),
    expression2: (v: Expression) => slice({ ...config, expression2: v }),
    expression3: (v: Expression) => slice({ ...config, expression3: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SliceTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function splatPattern(
  config?: SplatPatternConfig,
) {
  return {
    type: 'splat_pattern' as const,
    children: (v: Identifier) => splatPattern({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function splatType(
  config: SplatTypeConfig,
) {
  return {
    type: 'splat_type' as const,
    children: (v: Identifier) => splatType({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string(
  config: StringConfig,
) {
  return {
    type: 'string' as const,
    stringStart: (v: StringStart) => string({ ...config, stringStart: v }),
    children1: (...v: (Interpolation | StringContent)[]) => string({ ...config, children1: v }),
    stringEnd: (v: StringEnd) => string({ ...config, stringEnd: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function stringContent(
  config?: StringContentConfig,
) {
  return {
    type: 'string_content' as const,
    children: (...v: (EscapeInterpolation | EscapeSequence)[]) => stringContent({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringContentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function subscript(
  config: SubscriptConfig,
) {
  return {
    type: 'subscript' as const,
    subscript: (...v: (Expression | Slice)[]) => subscript({ ...config, 'subscript': v }),
    value: (v: PrimaryExpression) => subscript({ ...config, 'value': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tryStatement(
  config: TryStatementConfig,
) {
  return {
    type: 'try_statement' as const,
    body: (v: Block) => tryStatement({ ...config, 'body': v }),
    exceptClause: (...v: (ExceptClause)[]) => tryStatement({ ...config, exceptClause: v }),
    elseClause: (v: ElseClause) => tryStatement({ ...config, elseClause: v }),
    finallyClause: (v: FinallyClause) => tryStatement({ ...config, finallyClause: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple(
  config?: TupleConfig,
) {
  return {
    type: 'tuple' as const,
    children: (...v: (Expression | ListSplat | ParenthesizedListSplat | Yield)[]) => tuple({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuplePattern(
  config?: TuplePatternConfig,
) {
  return {
    type: 'tuple_pattern' as const,
    children: (...v: (CasePattern | Pattern)[]) => tuplePattern({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_(
  config: TypeConfig,
) {
  return {
    type: 'type' as const,
    children: (v: ConstrainedType | Expression | GenericType | MemberType | SplatType | UnionType) => type_({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function typeAliasStatement(
  config: TypeAliasStatementConfig,
) {
  return {
    type: 'type_alias_statement' as const,
    left: (v: Type) => typeAliasStatement({ ...config, 'left': v }),
    right: (v: Type) => typeAliasStatement({ ...config, 'right': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function typeParameter(
  config: TypeParameterConfig,
) {
  return {
    type: 'type_parameter' as const,
    type1: (v: Type) => typeParameter({ ...config, type1: v }),
    type2: (...v: (Type)[]) => typeParameter({ ...config, type2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function typedDefaultParameter(
  config: TypedDefaultParameterConfig,
) {
  return {
    type: 'typed_default_parameter' as const,
    name: (v: Identifier) => typedDefaultParameter({ ...config, 'name': v }),
    typeField: (v: Type) => typedDefaultParameter({ ...config, 'type': v }),
    value: (v: Expression) => typedDefaultParameter({ ...config, 'value': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedDefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function typedParameter(
  config: TypedParameterConfig,
) {
  return {
    type: 'typed_parameter' as const,
    typeField: (v: Type) => typedParameter({ ...config, 'type': v }),
    children: (v: DictionarySplatPattern | Identifier | ListSplatPattern) => typedParameter({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unaryOperator(
  config: UnaryOperatorConfig,
) {
  return {
    type: 'unary_operator' as const,
    argument: (v: PrimaryExpression) => unaryOperator({ ...config, 'argument': v }),
    operator: (v: '+' | '-' | '~') => unaryOperator({ ...config, 'operator': { type: v, text: v } as const }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unionPattern(
  config?: UnionPatternConfig,
) {
  return {
    type: 'union_pattern' as const,
    children: (...v: (ClassPattern | ComplexPattern | ConcatenatedString | DictPattern | DottedName | False | Float | Integer | ListPattern | None | SplatPattern | String | True | TuplePattern | UnionPattern)[]) => unionPattern({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unionType(
  config: UnionTypeConfig,
) {
  return {
    type: 'union_type' as const,
    type1: (v: Type) => unionType({ ...config, type1: v }),
    type2: (v: Type) => unionType({ ...config, type2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function whileStatement(
  config: WhileStatementConfig,
) {
  return {
    type: 'while_statement' as const,
    alternative: (v: ElseClause) => whileStatement({ ...config, 'alternative': v }),
    body: (v: Block) => whileStatement({ ...config, 'body': v }),
    condition: (v: Expression) => whileStatement({ ...config, 'condition': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function withClause(
  config: WithClauseConfig,
) {
  return {
    type: 'with_clause' as const,
    withItem1: (v: WithItem) => withClause({ ...config, withItem1: v }),
    withItem2: (...v: (WithItem)[]) => withClause({ ...config, withItem2: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function withItem(
  config: WithItemConfig,
) {
  return {
    type: 'with_item' as const,
    value: (v: Expression) => withItem({ ...config, 'value': v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function withStatement(
  config: WithStatementConfig,
) {
  return {
    type: 'with_statement' as const,
    body: (v: Block) => withStatement({ ...config, 'body': v }),
    children: (v: WithClause) => withStatement({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function yield_(
  config?: YieldConfig,
) {
  return {
    type: 'yield' as const,
    children: (v: Expression | ExpressionList) => yield_({ ...config, children: v }),
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function breakStatement() {
  return {
    type: 'break_statement' as const,
    text: 'break',
    render: () => 'break',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'break' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'break' };
    },
    replace: (target: BreakStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' }; },
  };
}

export function continueStatement() {
  return {
    type: 'continue_statement' as const,
    text: 'continue',
    render: () => 'continue',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'continue' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'continue' };
    },
    replace: (target: ContinueStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' }; },
  };
}

export function importPrefix(text: string) {
  if (!/^(?:\.)+$/.test(text)) throw new Error(`Invalid import_prefix: '${text}' does not match grammar pattern`);
  return {
    type: 'import_prefix' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ImportPrefixTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function keywordSeparator() {
  return {
    type: 'keyword_separator' as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: KeywordSeparatorTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function passStatement() {
  return {
    type: 'pass_statement' as const,
    text: 'pass',
    render: () => 'pass',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'pass' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'pass' };
    },
    replace: (target: PassStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' }; },
  };
}

export function positionalSeparator() {
  return {
    type: 'positional_separator' as const,
    text: '/',
    render: () => '/',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '/' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '/' };
    },
    replace: (target: PositionalSeparatorTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '/' }; },
  };
}

export function wildcardImport() {
  return {
    type: 'wildcard_import' as const,
    text: '*',
    render: () => '*',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '*' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
    },
    replace: (target: WildcardImportTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; },
  };
}

export function comment(text: string) {
  if (!/^#.*$/.test(text)) throw new Error(`Invalid comment: '${text}' does not match grammar pattern`);
  return {
    type: 'comment' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: CommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function ellipsis() {
  return {
    type: 'ellipsis' as const,
    text: '...',
    render: () => '...',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '...' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '...' };
    },
    replace: (target: EllipsisTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '...' }; },
  };
}

export function escapeInterpolation(text: string) {
  return {
    type: 'escape_interpolation' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeInterpolationTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: `\\${string}`) {
  if (!/^\\(?:u[a-fA-F\d]{4}|U[a-fA-F\d]{8}|x[a-fA-F\d]{2}|\d{1,3}|\r?\n|['"abfrntv\\]|N\{[^}]+\})$/.test(text)) throw new Error(`Invalid escape_sequence: '${text}' does not match grammar pattern`);
  return {
    type: 'escape_sequence' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeSequenceTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    text: 'False',
    render: () => 'False',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'False' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'False' };
    },
    replace: (target: FalseTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' }; },
  };
}

export function float(text: string) {
  if (!/^(?:(?:[0-9]+_?)+\.(?:[0-9]+_?)+?[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+?\.(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+)[jJ]?$/.test(text)) throw new Error(`Invalid float: '${text}' does not match grammar pattern`);
  return {
    type: 'float' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FloatTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  if (!/^[_\p{XID_Start}][_\p{XID_Continue}]*$/u.test(text)) throw new Error(`Invalid identifier: '${text}' does not match grammar pattern`);
  return {
    type: 'identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer(text: string) {
  if (!/^(?:(?:0x|0X)(?:_?[A-Fa-f0-9]+)+[Ll]?|(?:0o|0O)(?:_?[0-7]+)+[Ll]?|(?:0b|0B)(?:_?[0-1]+)+[Ll]?|(?:[0-9]+_?)+(?:[Ll]?|[jJ]?))$/.test(text)) throw new Error(`Invalid integer: '${text}' does not match grammar pattern`);
  return {
    type: 'integer' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IntegerTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function lineContinuation(text: string) {
  if (!/^\\(?:\r?\n|\0)$/.test(text)) throw new Error(`Invalid line_continuation: '${text}' does not match grammar pattern`);
  return {
    type: 'line_continuation' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: LineContinuationTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function none() {
  return {
    type: 'none' as const,
    text: 'None',
    render: () => 'None',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'None' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'None' };
    },
    replace: (target: NoneTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' }; },
  };
}

export function stringEnd(text: string) {
  return {
    type: 'string_end' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringEndTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringStart(text: string) {
  return {
    type: 'string_start' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringStartTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    text: 'True',
    render: () => 'True',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'True' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'True' };
    },
    replace: (target: TrueTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' }; },
  };
}

export function typeConversion(text: string) {
  if (!/^![a-z]$/.test(text)) throw new Error(`Invalid type_conversion: '${text}' does not match grammar pattern`);
  return {
    type: 'type_conversion' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: TypeConversionTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}


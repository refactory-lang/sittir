// Auto-generated by @sittir/codegen — do not edit

import type { NodeData, Edit, AssignableNode } from '@sittir/core';
import { render, toEdit } from '@sittir/core';
import { isNodeData, type FromValue } from './utils.js';
import { rules } from './rules.js';
import { joinBy } from './joinby.js';

const RESERVED_KEYWORDS = new Set([
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'case',
  'class',
  'continue',
  'def',
  'del',
  'elif',
  'else',
  'except',
  'exec',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'match',
  'nonlocal',
  'not',
  'or',
  'pass',
  'print',
  'raise',
  'return',
  'try',
  'while',
  'with',
]);

export interface AliasedImportConfig {
  name: NodeData<'dotted_name'>;
  alias: NodeData<'identifier'>;
}

export interface AliasedImportFromInput {
  name: NodeData<'dotted_name'> | DottedNameFromInput | FromValue[];
  alias: NodeData<'identifier'> | string;
}

export type AliasedImportNode = NodeData<'aliased_import'> & {
  alias(value: NodeData<'identifier'>): AliasedImportNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'aliased_import'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AliasedImportFromNode = NodeData<'aliased_import'> & {
  alias(value: NodeData<'identifier'> | string): AliasedImportFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'aliased_import'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function aliasedImport(
  nameOrConfig: NodeData<'dotted_name'> | AliasedImportConfig,
  config?: Partial<AliasedImportConfig>,
): AliasedImportNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'aliased_import', fields };
  node.alias = (v: any) => { fields['alias'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

aliasedImport.assign = function(target: AssignableNode<'aliased_import'>): AliasedImportNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'aliased_import'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.alias = (v: any) => { overrides['alias'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArgumentListConfig {
  children?: NodeData[];
}

export interface ArgumentListFromInput {
  children?: (NodeData<'expression' | 'list_splat' | 'dictionary_splat' | 'parenthesized_expression' | 'keyword_argument'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'list_splat' } & ListSplatFromInput) | ({ kind: 'dictionary_splat' } & DictionarySplatFromInput) | ({ kind: 'parenthesized_expression' } & ParenthesizedExpressionFromInput) | ({ kind: 'keyword_argument' } & KeywordArgumentFromInput))[];
}

export type ArgumentListNode = NodeData<'argument_list'> & {
  children(...value: NodeData[]): ArgumentListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'argument_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ArgumentListFromNode = NodeData<'argument_list'> & {
  children(...value: FromValue[]): ArgumentListFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'argument_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function argumentList(
  childrenOrConfig?: NodeData<'expression' | 'list_splat' | 'dictionary_splat' | 'parenthesized_expression' | 'keyword_argument'> | NodeData<'expression' | 'list_splat' | 'dictionary_splat' | 'parenthesized_expression' | 'keyword_argument'>[] | ArgumentListConfig,
): ArgumentListNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'argument_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

argumentList.assign = function(target: AssignableNode<'argument_list'>): ArgumentListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'argument_list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AsPatternConfig {
  alias?: NodeData<'as_pattern_target'>;
  children?: NodeData[];
}

export interface AsPatternFromInput {
  alias?: NodeData<'as_pattern_target'> | AsPatternTargetFromInput | FromValue[];
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type AsPatternNode = NodeData<'as_pattern'> & {
  alias(value: NodeData<'as_pattern_target'>): AsPatternNode;
  children(...value: NodeData[]): AsPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'as_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AsPatternFromNode = NodeData<'as_pattern'> & {
  alias(value: NodeData<'as_pattern_target'> | AsPatternTargetFromInput | FromValue[]): AsPatternFromNode;
  children(...value: FromValue[]): AsPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'as_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function asPattern(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | AsPatternConfig,
): AsPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'as_pattern', fields };
  node.alias = (v: any) => { fields['alias'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

asPattern.assign = function(target: AssignableNode<'as_pattern'>): AsPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'as_pattern'; }, get fields() { return getFields(); } };
  node.alias = (v: any) => { overrides['alias'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AssertStatementConfig {
  children?: NodeData[];
}

export interface AssertStatementFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type AssertStatementNode = NodeData<'assert_statement'> & {
  children(...value: NodeData[]): AssertStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assert_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AssertStatementFromNode = NodeData<'assert_statement'> & {
  children(...value: FromValue[]): AssertStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assert_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function assertStatement(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | AssertStatementConfig,
): AssertStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'assert_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

assertStatement.assign = function(target: AssignableNode<'assert_statement'>): AssertStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'assert_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AssignmentConfig {
  left: NodeData<'pattern' | 'pattern_list'>;
  right?: NodeData<'expression' | 'expression_list' | 'assignment' | 'augmented_assignment' | 'pattern_list' | 'yield'>;
  type?: NodeData<'type'>;
}

export interface AssignmentFromInput {
  left: NodeData<'pattern' | 'pattern_list'> | ({ kind: 'pattern' } & PatternFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | FromValue[];
  right?: NodeData<'expression' | 'expression_list' | 'assignment' | 'augmented_assignment' | 'pattern_list' | 'yield'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput) | ({ kind: 'assignment' } & AssignmentFromInput) | ({ kind: 'augmented_assignment' } & AugmentedAssignmentFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | ({ kind: 'yield' } & YieldFromInput) | FromValue[];
  type?: NodeData<'type'> | TypeFromInput | FromValue[];
}

export type AssignmentNode = NodeData<'assignment'> & {
  right(value: NodeData<'expression' | 'expression_list' | 'assignment' | 'augmented_assignment' | 'pattern_list' | 'yield'>): AssignmentNode;
  typeField(value: NodeData<'type'>): AssignmentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assignment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AssignmentFromNode = NodeData<'assignment'> & {
  right(value: NodeData<'expression' | 'expression_list' | 'assignment' | 'augmented_assignment' | 'pattern_list' | 'yield'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput) | ({ kind: 'assignment' } & AssignmentFromInput) | ({ kind: 'augmented_assignment' } & AugmentedAssignmentFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | ({ kind: 'yield' } & YieldFromInput) | FromValue[]): AssignmentFromNode;
  typeField(value: NodeData<'type'> | TypeFromInput | FromValue[]): AssignmentFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assignment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function assignment(
  leftOrConfig: NodeData<'pattern' | 'pattern_list'> | AssignmentConfig,
  config?: Partial<AssignmentConfig>,
): AssignmentNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'assignment', fields };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

assignment.assign = function(target: AssignableNode<'assignment'>): AssignmentNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'assignment'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AttributeConfig {
  object: NodeData<'primary_expression'>;
  attribute: NodeData<'identifier'>;
}

export interface AttributeFromInput {
  object: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[];
  attribute: NodeData<'identifier'> | string;
}

export type AttributeNode = NodeData<'attribute'> & {
  attribute(value: NodeData<'identifier'>): AttributeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'attribute'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AttributeFromNode = NodeData<'attribute'> & {
  attribute(value: NodeData<'identifier'> | string): AttributeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'attribute'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function attribute(
  objectOrConfig: NodeData<'primary_expression'> | AttributeConfig,
  config?: Partial<AttributeConfig>,
): AttributeNode {
  const fields: any = isNodeData(objectOrConfig)
    ? { 'object': objectOrConfig, ...config }
    : objectOrConfig;
  const node: any = { type: 'attribute', fields };
  node.attribute = (v: any) => { fields['attribute'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

attribute.assign = function(target: AssignableNode<'attribute'>): AttributeNode {
  const overrides: any = {};
  const fieldList: string[] = ['object', 'attribute'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'attribute'; }, get fields() { return getFields(); } };
  node.object = (v: any) => { overrides['object'] = v; return node; };
  node.attribute = (v: any) => { overrides['attribute'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AugmentedAssignmentConfig {
  left: NodeData<'pattern' | 'pattern_list'>;
  operator: NodeData<string>;
  right: NodeData<'expression' | 'expression_list' | 'assignment' | 'augmented_assignment' | 'pattern_list' | 'yield'>;
}

export interface AugmentedAssignmentFromInput {
  left: NodeData<'pattern' | 'pattern_list'> | ({ kind: 'pattern' } & PatternFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | FromValue[];
  operator: NodeData | '+=' | '-=' | '*=' | '/=' | '@=' | '//=' | '%=' | '**=' | '>>=' | '<<=' | '&=' | '^=' | '|=';
  right: NodeData<'expression' | 'expression_list' | 'assignment' | 'augmented_assignment' | 'pattern_list' | 'yield'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput) | ({ kind: 'assignment' } & AssignmentFromInput) | ({ kind: 'augmented_assignment' } & AugmentedAssignmentFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | ({ kind: 'yield' } & YieldFromInput) | FromValue[];
}

export type AugmentedAssignmentNode = NodeData<'augmented_assignment'> & {
  operator(value: NodeData<string>): AugmentedAssignmentNode;
  right(value: NodeData<'expression' | 'expression_list' | 'assignment' | 'augmented_assignment' | 'pattern_list' | 'yield'>): AugmentedAssignmentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'augmented_assignment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AugmentedAssignmentFromNode = NodeData<'augmented_assignment'> & {
  operator(value: NodeData | '+=' | '-=' | '*=' | '/=' | '@=' | '//=' | '%=' | '**=' | '>>=' | '<<=' | '&=' | '^=' | '|='): AugmentedAssignmentFromNode;
  right(value: NodeData<'expression' | 'expression_list' | 'assignment' | 'augmented_assignment' | 'pattern_list' | 'yield'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput) | ({ kind: 'assignment' } & AssignmentFromInput) | ({ kind: 'augmented_assignment' } & AugmentedAssignmentFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | ({ kind: 'yield' } & YieldFromInput) | FromValue[]): AugmentedAssignmentFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'augmented_assignment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function augmentedAssignment(
  leftOrConfig: NodeData<'pattern' | 'pattern_list'> | AugmentedAssignmentConfig,
  config?: Partial<AugmentedAssignmentConfig>,
): AugmentedAssignmentNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'augmented_assignment', fields };
  node.operator = (v: any) => { fields['operator'] = v; return node; };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

augmentedAssignment.assign = function(target: AssignableNode<'augmented_assignment'>): AugmentedAssignmentNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'operator', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'augmented_assignment'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AwaitConfig {
  children?: NodeData[];
}

export interface AwaitFromInput {
  children?: (NodeData<'primary_expression'> | PrimaryExpressionFromInput)[];
}

export type AwaitNode = NodeData<'await'> & {
  children(...value: NodeData[]): AwaitNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'await'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type AwaitFromNode = NodeData<'await'> & {
  children(...value: FromValue[]): AwaitFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'await'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function await_(
  childrenOrConfig?: NodeData<'primary_expression'> | NodeData<'primary_expression'>[] | AwaitConfig,
): AwaitNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'await', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

await_.assign = function(target: AssignableNode<'await'>): AwaitNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'await'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BinaryOperatorConfig {
  left: NodeData<'primary_expression'>;
  operator: NodeData<string>;
  right: NodeData<'primary_expression'>;
}

export interface BinaryOperatorFromInput {
  left: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[];
  operator: NodeData | '+' | '-' | '*' | '@' | '/' | '%' | '//' | '**' | '|' | '&' | '^' | '<<' | '>>';
  right: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[];
}

export type BinaryOperatorNode = NodeData<'binary_operator'> & {
  operator(value: NodeData<string>): BinaryOperatorNode;
  right(value: NodeData<'primary_expression'>): BinaryOperatorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'binary_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type BinaryOperatorFromNode = NodeData<'binary_operator'> & {
  operator(value: NodeData | '+' | '-' | '*' | '@' | '/' | '%' | '//' | '**' | '|' | '&' | '^' | '<<' | '>>'): BinaryOperatorFromNode;
  right(value: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[]): BinaryOperatorFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'binary_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function binaryOperator(
  leftOrConfig: NodeData<'primary_expression'> | BinaryOperatorConfig,
  config?: Partial<BinaryOperatorConfig>,
): BinaryOperatorNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'binary_operator', fields };
  node.operator = (v: any) => { fields['operator'] = v; return node; };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

binaryOperator.assign = function(target: AssignableNode<'binary_operator'>): BinaryOperatorNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'operator', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'binary_operator'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BlockConfig {
  alternative?: NodeData<'case_clause'>[];
  children?: NodeData[];
}

export interface BlockFromInput {
  alternative?: (NodeData<'case_clause'> | CaseClauseFromInput)[] | NodeData<'case_clause'> | CaseClauseFromInput;
  children?: (NodeData<'future_import_statement' | 'import_statement' | 'import_from_statement' | 'print_statement' | 'assert_statement' | 'expression_statement' | 'return_statement' | 'delete_statement' | 'raise_statement' | 'pass_statement' | 'break_statement' | 'continue_statement' | 'global_statement' | 'nonlocal_statement' | 'exec_statement' | 'type_alias_statement' | 'if_statement' | 'for_statement' | 'while_statement' | 'try_statement' | 'with_statement' | 'function_definition' | 'class_definition' | 'decorated_definition' | 'match_statement'> | string | ({ kind: 'future_import_statement' } & FutureImportStatementFromInput) | ({ kind: 'import_statement' } & ImportStatementFromInput) | ({ kind: 'import_from_statement' } & ImportFromStatementFromInput) | ({ kind: 'print_statement' } & PrintStatementFromInput) | ({ kind: 'assert_statement' } & AssertStatementFromInput) | ({ kind: 'expression_statement' } & ExpressionStatementFromInput) | ({ kind: 'return_statement' } & ReturnStatementFromInput) | ({ kind: 'delete_statement' } & DeleteStatementFromInput) | ({ kind: 'raise_statement' } & RaiseStatementFromInput) | ({ kind: 'global_statement' } & GlobalStatementFromInput) | ({ kind: 'nonlocal_statement' } & NonlocalStatementFromInput) | ({ kind: 'exec_statement' } & ExecStatementFromInput) | ({ kind: 'type_alias_statement' } & TypeAliasStatementFromInput) | ({ kind: 'if_statement' } & IfStatementFromInput) | ({ kind: 'for_statement' } & ForStatementFromInput) | ({ kind: 'while_statement' } & WhileStatementFromInput) | ({ kind: 'try_statement' } & TryStatementFromInput) | ({ kind: 'with_statement' } & WithStatementFromInput) | ({ kind: 'function_definition' } & FunctionDefinitionFromInput) | ({ kind: 'class_definition' } & ClassDefinitionFromInput) | ({ kind: 'decorated_definition' } & DecoratedDefinitionFromInput) | ({ kind: 'match_statement' } & MatchStatementFromInput))[];
}

export type BlockNode = NodeData<'block'> & {
  alternative(value: NodeData<'case_clause'>[]): BlockNode;
  children(...value: NodeData[]): BlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type BlockFromNode = NodeData<'block'> & {
  alternative(value: (NodeData<'case_clause'> | CaseClauseFromInput)[] | NodeData<'case_clause'> | CaseClauseFromInput): BlockFromNode;
  children(...value: FromValue[]): BlockFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function block(
  childrenOrConfig?: NodeData<'future_import_statement' | 'import_statement' | 'import_from_statement' | 'print_statement' | 'assert_statement' | 'expression_statement' | 'return_statement' | 'delete_statement' | 'raise_statement' | 'pass_statement' | 'break_statement' | 'continue_statement' | 'global_statement' | 'nonlocal_statement' | 'exec_statement' | 'type_alias_statement' | 'if_statement' | 'for_statement' | 'while_statement' | 'try_statement' | 'with_statement' | 'function_definition' | 'class_definition' | 'decorated_definition' | 'match_statement'> | NodeData<'future_import_statement' | 'import_statement' | 'import_from_statement' | 'print_statement' | 'assert_statement' | 'expression_statement' | 'return_statement' | 'delete_statement' | 'raise_statement' | 'pass_statement' | 'break_statement' | 'continue_statement' | 'global_statement' | 'nonlocal_statement' | 'exec_statement' | 'type_alias_statement' | 'if_statement' | 'for_statement' | 'while_statement' | 'try_statement' | 'with_statement' | 'function_definition' | 'class_definition' | 'decorated_definition' | 'match_statement'>[] | BlockConfig,
): BlockNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'block', fields };
  node.alternative = (...v: any[]) => { fields['alternative'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

block.assign = function(target: AssignableNode<'block'>): BlockNode {
  const overrides: any = {};
  const fieldList: string[] = ['alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'block'; }, get fields() { return getFields(); } };
  node.alternative = (...v: any[]) => { overrides['alternative'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BooleanOperatorConfig {
  left: NodeData<'expression'>;
  operator: NodeData<string>;
  right: NodeData<'expression'>;
}

export interface BooleanOperatorFromInput {
  left: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  operator: NodeData | 'and' | 'or';
  right: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type BooleanOperatorNode = NodeData<'boolean_operator'> & {
  operator(value: NodeData<string>): BooleanOperatorNode;
  right(value: NodeData<'expression'>): BooleanOperatorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'boolean_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type BooleanOperatorFromNode = NodeData<'boolean_operator'> & {
  operator(value: NodeData | 'and' | 'or'): BooleanOperatorFromNode;
  right(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): BooleanOperatorFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'boolean_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function booleanOperator(
  leftOrConfig: NodeData<'expression'> | BooleanOperatorConfig,
  config?: Partial<BooleanOperatorConfig>,
): BooleanOperatorNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'boolean_operator', fields };
  node.operator = (v: any) => { fields['operator'] = v; return node; };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

booleanOperator.assign = function(target: AssignableNode<'boolean_operator'>): BooleanOperatorNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'operator', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'boolean_operator'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CallConfig {
  function: NodeData<'primary_expression'>;
  arguments: NodeData<'generator_expression' | 'argument_list'>;
}

export interface CallFromInput {
  function: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[];
  arguments: NodeData<'generator_expression' | 'argument_list'> | ({ kind: 'generator_expression' } & GeneratorExpressionFromInput) | ({ kind: 'argument_list' } & ArgumentListFromInput) | FromValue[];
}

export type CallNode = NodeData<'call'> & {
  arguments(value: NodeData<'generator_expression' | 'argument_list'>): CallNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'call'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type CallFromNode = NodeData<'call'> & {
  arguments(value: NodeData<'generator_expression' | 'argument_list'> | ({ kind: 'generator_expression' } & GeneratorExpressionFromInput) | ({ kind: 'argument_list' } & ArgumentListFromInput) | FromValue[]): CallFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'call'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function call(
  functionOrConfig: NodeData<'primary_expression'> | CallConfig,
  config?: Partial<CallConfig>,
): CallNode {
  const fields: any = isNodeData(functionOrConfig)
    ? { 'function': functionOrConfig, ...config }
    : functionOrConfig;
  const node: any = { type: 'call', fields };
  node.arguments = (v: any) => { fields['arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

call.assign = function(target: AssignableNode<'call'>): CallNode {
  const overrides: any = {};
  const fieldList: string[] = ['function', 'arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'call'; }, get fields() { return getFields(); } };
  node.function = (v: any) => { overrides['function'] = v; return node; };
  node.arguments = (v: any) => { overrides['arguments'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CaseClauseConfig {
  guard?: NodeData<'if_clause'>;
  consequence: NodeData<'block'>;
  children?: NodeData[];
}

export interface CaseClauseFromInput {
  guard?: NodeData<'if_clause'> | IfClauseFromInput | FromValue[];
  consequence: NodeData<'block'> | BlockFromInput | FromValue[];
  children?: (NodeData<'case_pattern'> | CasePatternFromInput)[];
}

export type CaseClauseNode = NodeData<'case_clause'> & {
  guard(value: NodeData<'if_clause'>): CaseClauseNode;
  children(...value: NodeData[]): CaseClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'case_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type CaseClauseFromNode = NodeData<'case_clause'> & {
  guard(value: NodeData<'if_clause'> | IfClauseFromInput | FromValue[]): CaseClauseFromNode;
  children(...value: FromValue[]): CaseClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'case_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function caseClause(
  consequenceOrConfig: NodeData<'block'> | CaseClauseConfig,
  config?: Partial<CaseClauseConfig>,
): CaseClauseNode {
  const fields: any = isNodeData(consequenceOrConfig)
    ? { 'consequence': consequenceOrConfig, ...config }
    : consequenceOrConfig;
  const node: any = { type: 'case_clause', fields };
  node.guard = (v: any) => { fields['guard'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

caseClause.assign = function(target: AssignableNode<'case_clause'>): CaseClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['guard', 'consequence'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'case_clause'; }, get fields() { return getFields(); } };
  node.guard = (v: any) => { overrides['guard'] = v; return node; };
  node.consequence = (v: any) => { overrides['consequence'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CasePatternConfig {
  children?: NodeData[];
}

export interface CasePatternFromInput {
  children?: (NodeData<'as_pattern' | 'keyword_pattern' | 'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | string | ({ kind: 'as_pattern' } & AsPatternFromInput) | ({ kind: 'keyword_pattern' } & KeywordPatternFromInput) | ({ kind: 'class_pattern' } & ClassPatternFromInput) | ({ kind: 'splat_pattern' } & SplatPatternFromInput) | ({ kind: 'union_pattern' } & UnionPatternFromInput) | ({ kind: 'list_pattern' } & ListPatternFromInput) | ({ kind: 'tuple_pattern' } & TuplePatternFromInput) | ({ kind: 'dict_pattern' } & DictPatternFromInput) | ({ kind: 'string' } & StringFromInput) | ({ kind: 'concatenated_string' } & ConcatenatedStringFromInput) | ({ kind: 'complex_pattern' } & ComplexPatternFromInput) | ({ kind: 'dotted_name' } & DottedNameFromInput))[];
}

export type CasePatternNode = NodeData<'case_pattern'> & {
  children(...value: NodeData[]): CasePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'case_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type CasePatternFromNode = NodeData<'case_pattern'> & {
  children(...value: FromValue[]): CasePatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'case_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function casePattern(
  childrenOrConfig?: NodeData<'as_pattern' | 'keyword_pattern' | 'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | NodeData<'as_pattern' | 'keyword_pattern' | 'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'>[] | CasePatternConfig,
): CasePatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'case_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

casePattern.assign = function(target: AssignableNode<'case_pattern'>): CasePatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'case_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ChevronConfig {
  children?: NodeData[];
}

export interface ChevronFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type ChevronNode = NodeData<'chevron'> & {
  children(...value: NodeData[]): ChevronNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'chevron'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ChevronFromNode = NodeData<'chevron'> & {
  children(...value: FromValue[]): ChevronFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'chevron'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function chevron(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | ChevronConfig,
): ChevronNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'chevron', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

chevron.assign = function(target: AssignableNode<'chevron'>): ChevronNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'chevron'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClassDefinitionConfig {
  name: NodeData<'identifier'>;
  type_parameters?: NodeData<'type_parameter'>;
  superclasses?: NodeData<'argument_list'>;
  body: NodeData<'block'>;
}

export interface ClassDefinitionFromInput {
  name: NodeData<'identifier'> | string;
  type_parameters?: NodeData<'type_parameter'> | TypeParameterFromInput | FromValue[];
  superclasses?: NodeData<'argument_list'> | ArgumentListFromInput | FromValue[];
  body: NodeData<'block'> | BlockFromInput | FromValue[];
}

export type ClassDefinitionNode = NodeData<'class_definition'> & {
  typeParameters(value: NodeData<'type_parameter'>): ClassDefinitionNode;
  superclasses(value: NodeData<'argument_list'>): ClassDefinitionNode;
  body(value: NodeData<'block'>): ClassDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ClassDefinitionFromNode = NodeData<'class_definition'> & {
  typeParameters(value: NodeData<'type_parameter'> | TypeParameterFromInput | FromValue[]): ClassDefinitionFromNode;
  superclasses(value: NodeData<'argument_list'> | ArgumentListFromInput | FromValue[]): ClassDefinitionFromNode;
  body(value: NodeData<'block'> | BlockFromInput | FromValue[]): ClassDefinitionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function classDefinition(
  nameOrConfig: NodeData<'identifier'> | ClassDefinitionConfig,
  config?: Partial<ClassDefinitionConfig>,
): ClassDefinitionNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'class_definition', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.superclasses = (v: any) => { fields['superclasses'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

classDefinition.assign = function(target: AssignableNode<'class_definition'>): ClassDefinitionNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'superclasses', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'class_definition'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.superclasses = (v: any) => { overrides['superclasses'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClassPatternConfig {
  children?: NodeData[];
}

export interface ClassPatternFromInput {
  children?: (NodeData<'dotted_name' | 'case_pattern'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'case_pattern' } & CasePatternFromInput))[];
}

export type ClassPatternNode = NodeData<'class_pattern'> & {
  children(...value: NodeData[]): ClassPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ClassPatternFromNode = NodeData<'class_pattern'> & {
  children(...value: FromValue[]): ClassPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'class_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function classPattern(
  childrenOrConfig?: NodeData<'dotted_name' | 'case_pattern'> | NodeData<'dotted_name' | 'case_pattern'>[] | ClassPatternConfig,
): ClassPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'class_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

classPattern.assign = function(target: AssignableNode<'class_pattern'>): ClassPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'class_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ComparisonOperatorConfig {
  operators: NodeData<string>[];
  children?: NodeData[];
}

export interface ComparisonOperatorFromInput {
  operators: (NodeData | '<' | '<=' | '==' | '!=' | '>=' | '>' | '<>' | 'in' | 'is')[] | NodeData | '<' | '<=' | '==' | '!=' | '>=' | '>' | '<>' | 'in' | 'is';
  children?: (NodeData<'primary_expression'> | PrimaryExpressionFromInput)[];
}

export type ComparisonOperatorNode = NodeData<'comparison_operator'> & {
  operators(value: NodeData<string>[]): ComparisonOperatorNode;
  children(...value: NodeData[]): ComparisonOperatorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'comparison_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ComparisonOperatorFromNode = NodeData<'comparison_operator'> & {
  operators(value: (NodeData | '<' | '<=' | '==' | '!=' | '>=' | '>' | '<>' | 'in' | 'is')[] | NodeData | '<' | '<=' | '==' | '!=' | '>=' | '>' | '<>' | 'in' | 'is'): ComparisonOperatorFromNode;
  children(...value: FromValue[]): ComparisonOperatorFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'comparison_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function comparisonOperator(
  childrenOrConfig?: NodeData<'primary_expression'> | NodeData<'primary_expression'>[] | ComparisonOperatorConfig,
): ComparisonOperatorNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'comparison_operator', fields };
  node.operators = (...v: any[]) => { fields['operators'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

comparisonOperator.assign = function(target: AssignableNode<'comparison_operator'>): ComparisonOperatorNode {
  const overrides: any = {};
  const fieldList: string[] = ['operators'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'comparison_operator'; }, get fields() { return getFields(); } };
  node.operators = (...v: any[]) => { overrides['operators'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ComplexPatternConfig {
  children?: NodeData[];
}

export interface ComplexPatternFromInput {
  children?: (NodeData<'integer' | 'float'> | string)[];
}

export type ComplexPatternNode = NodeData<'complex_pattern'> & {
  children(...value: NodeData[]): ComplexPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'complex_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ComplexPatternFromNode = NodeData<'complex_pattern'> & {
  children(...value: FromValue[]): ComplexPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'complex_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function complexPattern(
  childrenOrConfig?: NodeData<'integer' | 'float'> | NodeData<'integer' | 'float'>[] | ComplexPatternConfig,
): ComplexPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'complex_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

complexPattern.assign = function(target: AssignableNode<'complex_pattern'>): ComplexPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'complex_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConcatenatedStringConfig {
  children?: NodeData[];
}

export interface ConcatenatedStringFromInput {
  children?: (NodeData<'string'> | StringFromInput)[];
}

export type ConcatenatedStringNode = NodeData<'concatenated_string'> & {
  children(...value: NodeData[]): ConcatenatedStringNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'concatenated_string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ConcatenatedStringFromNode = NodeData<'concatenated_string'> & {
  children(...value: FromValue[]): ConcatenatedStringFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'concatenated_string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function concatenatedString(
  childrenOrConfig?: NodeData<'string'> | NodeData<'string'>[] | ConcatenatedStringConfig,
): ConcatenatedStringNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'concatenated_string', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

concatenatedString.assign = function(target: AssignableNode<'concatenated_string'>): ConcatenatedStringNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'concatenated_string'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConditionalExpressionConfig {
  children?: NodeData[];
}

export interface ConditionalExpressionFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type ConditionalExpressionNode = NodeData<'conditional_expression'> & {
  children(...value: NodeData[]): ConditionalExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'conditional_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ConditionalExpressionFromNode = NodeData<'conditional_expression'> & {
  children(...value: FromValue[]): ConditionalExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'conditional_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function conditionalExpression(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | ConditionalExpressionConfig,
): ConditionalExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'conditional_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

conditionalExpression.assign = function(target: AssignableNode<'conditional_expression'>): ConditionalExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'conditional_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConstrainedTypeConfig {
  children?: NodeData[];
}

export interface ConstrainedTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type ConstrainedTypeNode = NodeData<'constrained_type'> & {
  children(...value: NodeData[]): ConstrainedTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'constrained_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ConstrainedTypeFromNode = NodeData<'constrained_type'> & {
  children(...value: FromValue[]): ConstrainedTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'constrained_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function constrainedType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | ConstrainedTypeConfig,
): ConstrainedTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'constrained_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

constrainedType.assign = function(target: AssignableNode<'constrained_type'>): ConstrainedTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'constrained_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DecoratedDefinitionConfig {
  definition: NodeData<'class_definition' | 'function_definition'>;
  children?: NodeData[];
}

export interface DecoratedDefinitionFromInput {
  definition: NodeData<'class_definition' | 'function_definition'> | ({ kind: 'class_definition' } & ClassDefinitionFromInput) | ({ kind: 'function_definition' } & FunctionDefinitionFromInput) | FromValue[];
  children?: (NodeData<'decorator'> | DecoratorFromInput)[];
}

export type DecoratedDefinitionNode = NodeData<'decorated_definition'> & {
  children(...value: NodeData[]): DecoratedDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'decorated_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DecoratedDefinitionFromNode = NodeData<'decorated_definition'> & {
  children(...value: FromValue[]): DecoratedDefinitionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'decorated_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function decoratedDefinition(
  definitionOrConfig: NodeData<'class_definition' | 'function_definition'> | DecoratedDefinitionConfig,
  config?: Partial<DecoratedDefinitionConfig>,
): DecoratedDefinitionNode {
  const fields: any = isNodeData(definitionOrConfig)
    ? { 'definition': definitionOrConfig, ...config }
    : definitionOrConfig;
  const node: any = { type: 'decorated_definition', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

decoratedDefinition.assign = function(target: AssignableNode<'decorated_definition'>): DecoratedDefinitionNode {
  const overrides: any = {};
  const fieldList: string[] = ['definition'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'decorated_definition'; }, get fields() { return getFields(); } };
  node.definition = (v: any) => { overrides['definition'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DecoratorConfig {
  children?: NodeData[];
}

export interface DecoratorFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type DecoratorNode = NodeData<'decorator'> & {
  children(...value: NodeData[]): DecoratorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'decorator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DecoratorFromNode = NodeData<'decorator'> & {
  children(...value: FromValue[]): DecoratorFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'decorator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function decorator(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | DecoratorConfig,
): DecoratorNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'decorator', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

decorator.assign = function(target: AssignableNode<'decorator'>): DecoratorNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'decorator'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DefaultParameterConfig {
  name: NodeData<'identifier' | 'tuple_pattern'>;
  value: NodeData<'expression'>;
}

export interface DefaultParameterFromInput {
  name: NodeData<'identifier' | 'tuple_pattern'> | string | TuplePatternFromInput | FromValue[];
  value: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type DefaultParameterNode = NodeData<'default_parameter'> & {
  value(value: NodeData<'expression'>): DefaultParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'default_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DefaultParameterFromNode = NodeData<'default_parameter'> & {
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): DefaultParameterFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'default_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function defaultParameter(
  nameOrConfig: NodeData<'identifier' | 'tuple_pattern'> | DefaultParameterConfig,
  config?: Partial<DefaultParameterConfig>,
): DefaultParameterNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'default_parameter', fields };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

defaultParameter.assign = function(target: AssignableNode<'default_parameter'>): DefaultParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'default_parameter'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DeleteStatementConfig {
  children?: NodeData[];
}

export interface DeleteStatementFromInput {
  children?: (NodeData<'expression' | 'expression_list'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput))[];
}

export type DeleteStatementNode = NodeData<'delete_statement'> & {
  children(...value: NodeData[]): DeleteStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'delete_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DeleteStatementFromNode = NodeData<'delete_statement'> & {
  children(...value: FromValue[]): DeleteStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'delete_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function deleteStatement(
  childrenOrConfig?: NodeData<'expression' | 'expression_list'> | NodeData<'expression' | 'expression_list'>[] | DeleteStatementConfig,
): DeleteStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'delete_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

deleteStatement.assign = function(target: AssignableNode<'delete_statement'>): DeleteStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'delete_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DictPatternConfig {
  key?: NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'>[];
  value?: NodeData<'case_pattern'>[];
  children?: NodeData[];
}

export interface DictPatternFromInput {
  key?: (NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | string | ({ kind: 'class_pattern' } & ClassPatternFromInput) | ({ kind: 'splat_pattern' } & SplatPatternFromInput) | ({ kind: 'union_pattern' } & UnionPatternFromInput) | ({ kind: 'list_pattern' } & ListPatternFromInput) | ({ kind: 'tuple_pattern' } & TuplePatternFromInput) | ({ kind: 'dict_pattern' } & DictPatternFromInput) | ({ kind: 'string' } & StringFromInput) | ({ kind: 'concatenated_string' } & ConcatenatedStringFromInput) | ({ kind: 'complex_pattern' } & ComplexPatternFromInput) | ({ kind: 'dotted_name' } & DottedNameFromInput))[] | NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | string | ({ kind: 'class_pattern' } & ClassPatternFromInput) | ({ kind: 'splat_pattern' } & SplatPatternFromInput) | ({ kind: 'union_pattern' } & UnionPatternFromInput) | ({ kind: 'list_pattern' } & ListPatternFromInput) | ({ kind: 'tuple_pattern' } & TuplePatternFromInput) | ({ kind: 'dict_pattern' } & DictPatternFromInput) | ({ kind: 'string' } & StringFromInput) | ({ kind: 'concatenated_string' } & ConcatenatedStringFromInput) | ({ kind: 'complex_pattern' } & ComplexPatternFromInput) | ({ kind: 'dotted_name' } & DottedNameFromInput);
  value?: (NodeData<'case_pattern'> | CasePatternFromInput)[] | NodeData<'case_pattern'> | CasePatternFromInput;
  children?: (NodeData<'splat_pattern'> | SplatPatternFromInput)[];
}

export type DictPatternNode = NodeData<'dict_pattern'> & {
  key(value: NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'>[]): DictPatternNode;
  value(value: NodeData<'case_pattern'>[]): DictPatternNode;
  children(...value: NodeData[]): DictPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dict_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DictPatternFromNode = NodeData<'dict_pattern'> & {
  key(value: (NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | string | ({ kind: 'class_pattern' } & ClassPatternFromInput) | ({ kind: 'splat_pattern' } & SplatPatternFromInput) | ({ kind: 'union_pattern' } & UnionPatternFromInput) | ({ kind: 'list_pattern' } & ListPatternFromInput) | ({ kind: 'tuple_pattern' } & TuplePatternFromInput) | ({ kind: 'dict_pattern' } & DictPatternFromInput) | ({ kind: 'string' } & StringFromInput) | ({ kind: 'concatenated_string' } & ConcatenatedStringFromInput) | ({ kind: 'complex_pattern' } & ComplexPatternFromInput) | ({ kind: 'dotted_name' } & DottedNameFromInput))[] | NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | string | ({ kind: 'class_pattern' } & ClassPatternFromInput) | ({ kind: 'splat_pattern' } & SplatPatternFromInput) | ({ kind: 'union_pattern' } & UnionPatternFromInput) | ({ kind: 'list_pattern' } & ListPatternFromInput) | ({ kind: 'tuple_pattern' } & TuplePatternFromInput) | ({ kind: 'dict_pattern' } & DictPatternFromInput) | ({ kind: 'string' } & StringFromInput) | ({ kind: 'concatenated_string' } & ConcatenatedStringFromInput) | ({ kind: 'complex_pattern' } & ComplexPatternFromInput) | ({ kind: 'dotted_name' } & DottedNameFromInput)): DictPatternFromNode;
  value(value: (NodeData<'case_pattern'> | CasePatternFromInput)[] | NodeData<'case_pattern'> | CasePatternFromInput): DictPatternFromNode;
  children(...value: FromValue[]): DictPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dict_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictPattern(
  config?: DictPatternConfig,
): DictPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'dict_pattern', fields };
  node.key = (...v: any[]) => { fields['key'] = v; return node; };
  node.value = (...v: any[]) => { fields['value'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

dictPattern.assign = function(target: AssignableNode<'dict_pattern'>): DictPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['key', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'dict_pattern'; }, get fields() { return getFields(); } };
  node.key = (...v: any[]) => { overrides['key'] = v; return node; };
  node.value = (...v: any[]) => { overrides['value'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DictionaryConfig {
  children?: NodeData[];
}

export interface DictionaryFromInput {
  children?: (NodeData<'pair' | 'dictionary_splat'> | ({ kind: 'pair' } & PairFromInput) | ({ kind: 'dictionary_splat' } & DictionarySplatFromInput))[];
}

export type DictionaryNode = NodeData<'dictionary'> & {
  children(...value: NodeData[]): DictionaryNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DictionaryFromNode = NodeData<'dictionary'> & {
  children(...value: FromValue[]): DictionaryFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictionary(
  childrenOrConfig?: NodeData<'pair' | 'dictionary_splat'> | NodeData<'pair' | 'dictionary_splat'>[] | DictionaryConfig,
): DictionaryNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'dictionary', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

dictionary.assign = function(target: AssignableNode<'dictionary'>): DictionaryNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'dictionary'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DictionaryComprehensionConfig {
  body: NodeData<'pair'>;
  children?: NodeData[];
}

export interface DictionaryComprehensionFromInput {
  body: NodeData<'pair'> | PairFromInput | FromValue[];
  children?: (NodeData<'for_in_clause' | 'if_clause'> | ({ kind: 'for_in_clause' } & ForInClauseFromInput) | ({ kind: 'if_clause' } & IfClauseFromInput))[];
}

export type DictionaryComprehensionNode = NodeData<'dictionary_comprehension'> & {
  children(...value: NodeData[]): DictionaryComprehensionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DictionaryComprehensionFromNode = NodeData<'dictionary_comprehension'> & {
  children(...value: FromValue[]): DictionaryComprehensionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictionaryComprehension(
  bodyOrConfig: NodeData<'pair'> | DictionaryComprehensionConfig,
  config?: Partial<DictionaryComprehensionConfig>,
): DictionaryComprehensionNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'dictionary_comprehension', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

dictionaryComprehension.assign = function(target: AssignableNode<'dictionary_comprehension'>): DictionaryComprehensionNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'dictionary_comprehension'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DictionarySplatConfig {
  children?: NodeData[];
}

export interface DictionarySplatFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type DictionarySplatNode = NodeData<'dictionary_splat'> & {
  children(...value: NodeData[]): DictionarySplatNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DictionarySplatFromNode = NodeData<'dictionary_splat'> & {
  children(...value: FromValue[]): DictionarySplatFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictionarySplat(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | DictionarySplatConfig,
): DictionarySplatNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'dictionary_splat', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

dictionarySplat.assign = function(target: AssignableNode<'dictionary_splat'>): DictionarySplatNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'dictionary_splat'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DictionarySplatPatternConfig {
  children?: NodeData[];
}

export interface DictionarySplatPatternFromInput {
  children?: (NodeData<'identifier' | 'keyword_identifier' | 'subscript' | 'attribute'> | string | ({ kind: 'keyword_identifier' } & KeywordIdentifierFromInput) | ({ kind: 'subscript' } & SubscriptFromInput) | ({ kind: 'attribute' } & AttributeFromInput))[];
}

export type DictionarySplatPatternNode = NodeData<'dictionary_splat_pattern'> & {
  children(...value: NodeData[]): DictionarySplatPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DictionarySplatPatternFromNode = NodeData<'dictionary_splat_pattern'> & {
  children(...value: FromValue[]): DictionarySplatPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dictionary_splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dictionarySplatPattern(
  childrenOrConfig?: NodeData<'identifier' | 'keyword_identifier' | 'subscript' | 'attribute'> | NodeData<'identifier' | 'keyword_identifier' | 'subscript' | 'attribute'>[] | DictionarySplatPatternConfig,
): DictionarySplatPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'dictionary_splat_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

dictionarySplatPattern.assign = function(target: AssignableNode<'dictionary_splat_pattern'>): DictionarySplatPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'dictionary_splat_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DottedNameConfig {
  children?: NodeData[];
}

export interface DottedNameFromInput {
  children?: (NodeData<'identifier'> | string)[];
}

export type DottedNameNode = NodeData<'dotted_name'> & {
  children(...value: NodeData[]): DottedNameNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dotted_name'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type DottedNameFromNode = NodeData<'dotted_name'> & {
  children(...value: FromValue[]): DottedNameFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dotted_name'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dottedName(
  childrenOrConfig?: NodeData<'identifier'> | NodeData<'identifier'>[] | DottedNameConfig,
): DottedNameNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'dotted_name', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

dottedName.assign = function(target: AssignableNode<'dotted_name'>): DottedNameNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'dotted_name'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ElifClauseConfig {
  condition: NodeData<'expression'>;
  consequence: NodeData<'block'>;
}

export interface ElifClauseFromInput {
  condition: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  consequence: NodeData<'block'> | BlockFromInput | FromValue[];
}

export type ElifClauseNode = NodeData<'elif_clause'> & {
  consequence(value: NodeData<'block'>): ElifClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'elif_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ElifClauseFromNode = NodeData<'elif_clause'> & {
  consequence(value: NodeData<'block'> | BlockFromInput | FromValue[]): ElifClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'elif_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function elifClause(
  conditionOrConfig: NodeData<'expression'> | ElifClauseConfig,
  config?: Partial<ElifClauseConfig>,
): ElifClauseNode {
  const fields: any = isNodeData(conditionOrConfig)
    ? { 'condition': conditionOrConfig, ...config }
    : conditionOrConfig;
  const node: any = { type: 'elif_clause', fields };
  node.consequence = (v: any) => { fields['consequence'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

elifClause.assign = function(target: AssignableNode<'elif_clause'>): ElifClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition', 'consequence'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'elif_clause'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = v; return node; };
  node.consequence = (v: any) => { overrides['consequence'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ElseClauseConfig {
  body: NodeData<'block'>;
}

export interface ElseClauseFromInput {
  body: NodeData<'block'> | BlockFromInput | FromValue[];
}

export type ElseClauseNode = NodeData<'else_clause'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'else_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ElseClauseFromNode = NodeData<'else_clause'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'else_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function elseClause(
  bodyOrConfig: NodeData<'block'> | ElseClauseConfig,
  config?: Partial<ElseClauseConfig>,
): ElseClauseNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'else_clause', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

elseClause.assign = function(target: AssignableNode<'else_clause'>): ElseClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'else_clause'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExceptClauseConfig {
  value?: NodeData<'expression'>[];
  alias?: NodeData<'expression'>;
  children?: NodeData[];
}

export interface ExceptClauseFromInput {
  value?: (NodeData<'expression'> | ExpressionFromInput)[] | NodeData<'expression'> | ExpressionFromInput;
  alias?: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  children?: (NodeData<'block'> | BlockFromInput)[];
}

export type ExceptClauseNode = NodeData<'except_clause'> & {
  value(value: NodeData<'expression'>[]): ExceptClauseNode;
  alias(value: NodeData<'expression'>): ExceptClauseNode;
  children(...value: NodeData[]): ExceptClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'except_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExceptClauseFromNode = NodeData<'except_clause'> & {
  value(value: (NodeData<'expression'> | ExpressionFromInput)[] | NodeData<'expression'> | ExpressionFromInput): ExceptClauseFromNode;
  alias(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): ExceptClauseFromNode;
  children(...value: FromValue[]): ExceptClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'except_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function exceptClause(
  config?: ExceptClauseConfig,
): ExceptClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'except_clause', fields };
  node.value = (...v: any[]) => { fields['value'] = v; return node; };
  node.alias = (v: any) => { fields['alias'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

exceptClause.assign = function(target: AssignableNode<'except_clause'>): ExceptClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'except_clause'; }, get fields() { return getFields(); } };
  node.value = (...v: any[]) => { overrides['value'] = v; return node; };
  node.alias = (v: any) => { overrides['alias'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExecStatementConfig {
  code: NodeData<'string' | 'identifier'>;
  children?: NodeData[];
}

export interface ExecStatementFromInput {
  code: NodeData<'string' | 'identifier'> | string | StringFromInput | FromValue[];
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type ExecStatementNode = NodeData<'exec_statement'> & {
  children(...value: NodeData[]): ExecStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'exec_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExecStatementFromNode = NodeData<'exec_statement'> & {
  children(...value: FromValue[]): ExecStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'exec_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function execStatement(
  codeOrConfig: NodeData<'string' | 'identifier'> | ExecStatementConfig,
  config?: Partial<ExecStatementConfig>,
): ExecStatementNode {
  const fields: any = isNodeData(codeOrConfig)
    ? { 'code': codeOrConfig, ...config }
    : codeOrConfig;
  const node: any = { type: 'exec_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

execStatement.assign = function(target: AssignableNode<'exec_statement'>): ExecStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['code'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'exec_statement'; }, get fields() { return getFields(); } };
  node.code = (v: any) => { overrides['code'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExpressionListConfig {
  children?: NodeData[];
}

export interface ExpressionListFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type ExpressionListNode = NodeData<'expression_list'> & {
  children(...value: NodeData[]): ExpressionListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExpressionListFromNode = NodeData<'expression_list'> & {
  children(...value: FromValue[]): ExpressionListFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function expressionList(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | ExpressionListConfig,
): ExpressionListNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'expression_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

expressionList.assign = function(target: AssignableNode<'expression_list'>): ExpressionListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'expression_list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExpressionStatementConfig {
  children?: NodeData[];
}

export interface ExpressionStatementFromInput {
  children?: (NodeData<'expression' | 'assignment' | 'augmented_assignment' | 'yield'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'assignment' } & AssignmentFromInput) | ({ kind: 'augmented_assignment' } & AugmentedAssignmentFromInput) | ({ kind: 'yield' } & YieldFromInput))[];
}

export type ExpressionStatementNode = NodeData<'expression_statement'> & {
  children(...value: NodeData[]): ExpressionStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ExpressionStatementFromNode = NodeData<'expression_statement'> & {
  children(...value: FromValue[]): ExpressionStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function expressionStatement(
  childrenOrConfig?: NodeData<'expression' | 'assignment' | 'augmented_assignment' | 'yield'> | NodeData<'expression' | 'assignment' | 'augmented_assignment' | 'yield'>[] | ExpressionStatementConfig,
): ExpressionStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'expression_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

expressionStatement.assign = function(target: AssignableNode<'expression_statement'>): ExpressionStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'expression_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FinallyClauseConfig {
  children?: NodeData[];
}

export interface FinallyClauseFromInput {
  children?: (NodeData<'block'> | BlockFromInput)[];
}

export type FinallyClauseNode = NodeData<'finally_clause'> & {
  children(...value: NodeData[]): FinallyClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'finally_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FinallyClauseFromNode = NodeData<'finally_clause'> & {
  children(...value: FromValue[]): FinallyClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'finally_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function finallyClause(
  childrenOrConfig?: NodeData<'block'> | NodeData<'block'>[] | FinallyClauseConfig,
): FinallyClauseNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'finally_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

finallyClause.assign = function(target: AssignableNode<'finally_clause'>): FinallyClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'finally_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ForInClauseConfig {
  left: NodeData<'pattern' | 'pattern_list'>;
  right: NodeData<'expression' | 'lambda'>[];
}

export interface ForInClauseFromInput {
  left: NodeData<'pattern' | 'pattern_list'> | ({ kind: 'pattern' } & PatternFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | FromValue[];
  right: (NodeData<'expression' | 'lambda'> | ',' | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'lambda' } & LambdaFromInput))[] | NodeData<'expression' | 'lambda'> | ',' | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'lambda' } & LambdaFromInput);
}

export type ForInClauseNode = NodeData<'for_in_clause'> & {
  right(value: NodeData<'expression' | 'lambda'>[]): ForInClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_in_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ForInClauseFromNode = NodeData<'for_in_clause'> & {
  right(value: (NodeData<'expression' | 'lambda'> | ',' | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'lambda' } & LambdaFromInput))[] | NodeData<'expression' | 'lambda'> | ',' | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'lambda' } & LambdaFromInput)): ForInClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_in_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function forInClause(
  leftOrConfig: NodeData<'pattern' | 'pattern_list'> | ForInClauseConfig,
  config?: Partial<ForInClauseConfig>,
): ForInClauseNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'for_in_clause', fields };
  node.right = (...v: any[]) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

forInClause.assign = function(target: AssignableNode<'for_in_clause'>): ForInClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'for_in_clause'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.right = (...v: any[]) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ForStatementConfig {
  left: NodeData<'pattern' | 'pattern_list'>;
  right: NodeData<'expression' | 'expression_list'>;
  body: NodeData<'block'>;
  alternative?: NodeData<'else_clause'>;
}

export interface ForStatementFromInput {
  left: NodeData<'pattern' | 'pattern_list'> | ({ kind: 'pattern' } & PatternFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | FromValue[];
  right: NodeData<'expression' | 'expression_list'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput) | FromValue[];
  body: NodeData<'block'> | BlockFromInput | FromValue[];
  alternative?: NodeData<'else_clause'> | ElseClauseFromInput | FromValue[];
}

export type ForStatementNode = NodeData<'for_statement'> & {
  right(value: NodeData<'expression' | 'expression_list'>): ForStatementNode;
  body(value: NodeData<'block'>): ForStatementNode;
  alternative(value: NodeData<'else_clause'>): ForStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ForStatementFromNode = NodeData<'for_statement'> & {
  right(value: NodeData<'expression' | 'expression_list'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput) | FromValue[]): ForStatementFromNode;
  body(value: NodeData<'block'> | BlockFromInput | FromValue[]): ForStatementFromNode;
  alternative(value: NodeData<'else_clause'> | ElseClauseFromInput | FromValue[]): ForStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function forStatement(
  leftOrConfig: NodeData<'pattern' | 'pattern_list'> | ForStatementConfig,
  config?: Partial<ForStatementConfig>,
): ForStatementNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'for_statement', fields };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.alternative = (v: any) => { fields['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

forStatement.assign = function(target: AssignableNode<'for_statement'>): ForStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right', 'body', 'alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'for_statement'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.alternative = (v: any) => { overrides['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FormatExpressionConfig {
  expression: NodeData<'expression' | 'expression_list' | 'pattern_list' | 'yield'>;
  format_specifier?: NodeData<'format_specifier'>;
  type_conversion?: NodeData<'type_conversion'>;
}

export interface FormatExpressionFromInput {
  expression: NodeData<'expression' | 'expression_list' | 'pattern_list' | 'yield'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | ({ kind: 'yield' } & YieldFromInput) | FromValue[];
  format_specifier?: NodeData<'format_specifier'> | FormatSpecifierFromInput | FromValue[];
  type_conversion?: NodeData<'type_conversion'> | string;
}

export type FormatExpressionNode = NodeData<'format_expression'> & {
  formatSpecifier(value: NodeData<'format_specifier'>): FormatExpressionNode;
  typeConversion(value: NodeData<'type_conversion'>): FormatExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'format_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FormatExpressionFromNode = NodeData<'format_expression'> & {
  formatSpecifier(value: NodeData<'format_specifier'> | FormatSpecifierFromInput | FromValue[]): FormatExpressionFromNode;
  typeConversion(value: NodeData<'type_conversion'> | string): FormatExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'format_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function formatExpression(
  expressionOrConfig: NodeData<'expression' | 'expression_list' | 'pattern_list' | 'yield'> | FormatExpressionConfig,
  config?: Partial<FormatExpressionConfig>,
): FormatExpressionNode {
  const fields: any = isNodeData(expressionOrConfig)
    ? { 'expression': expressionOrConfig, ...config }
    : expressionOrConfig;
  const node: any = { type: 'format_expression', fields };
  node.formatSpecifier = (v: any) => { fields['format_specifier'] = v; return node; };
  node.typeConversion = (v: any) => { fields['type_conversion'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

formatExpression.assign = function(target: AssignableNode<'format_expression'>): FormatExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['expression', 'format_specifier', 'type_conversion'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'format_expression'; }, get fields() { return getFields(); } };
  node.expression = (v: any) => { overrides['expression'] = v; return node; };
  node.formatSpecifier = (v: any) => { overrides['format_specifier'] = v; return node; };
  node.typeConversion = (v: any) => { overrides['type_conversion'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FormatSpecifierConfig {
  children?: NodeData[];
}

export interface FormatSpecifierFromInput {
  children?: (NodeData<'format_expression'> | FormatExpressionFromInput)[];
}

export type FormatSpecifierNode = NodeData<'format_specifier'> & {
  children(...value: NodeData[]): FormatSpecifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'format_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FormatSpecifierFromNode = NodeData<'format_specifier'> & {
  children(...value: FromValue[]): FormatSpecifierFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'format_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function formatSpecifier(
  childrenOrConfig?: NodeData<'format_expression'> | NodeData<'format_expression'>[] | FormatSpecifierConfig,
): FormatSpecifierNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'format_specifier', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

formatSpecifier.assign = function(target: AssignableNode<'format_specifier'>): FormatSpecifierNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'format_specifier'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionDefinitionConfig {
  name: NodeData<'identifier'>;
  type_parameters?: NodeData<'type_parameter'>;
  parameters: NodeData<'parameters'>;
  return_type?: NodeData<'type'>;
  body: NodeData<'block'>;
}

export interface FunctionDefinitionFromInput {
  name: NodeData<'identifier'> | string;
  type_parameters?: NodeData<'type_parameter'> | TypeParameterFromInput | FromValue[];
  parameters: NodeData<'parameters'> | ParametersFromInput | FromValue[];
  return_type?: NodeData<'type'> | TypeFromInput | FromValue[];
  body: NodeData<'block'> | BlockFromInput | FromValue[];
}

export type FunctionDefinitionNode = NodeData<'function_definition'> & {
  typeParameters(value: NodeData<'type_parameter'>): FunctionDefinitionNode;
  parameters(value: NodeData<'parameters'>): FunctionDefinitionNode;
  returnType(value: NodeData<'type'>): FunctionDefinitionNode;
  body(value: NodeData<'block'>): FunctionDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FunctionDefinitionFromNode = NodeData<'function_definition'> & {
  typeParameters(value: NodeData<'type_parameter'> | TypeParameterFromInput | FromValue[]): FunctionDefinitionFromNode;
  parameters(value: NodeData<'parameters'> | ParametersFromInput | FromValue[]): FunctionDefinitionFromNode;
  returnType(value: NodeData<'type'> | TypeFromInput | FromValue[]): FunctionDefinitionFromNode;
  body(value: NodeData<'block'> | BlockFromInput | FromValue[]): FunctionDefinitionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionDefinition(
  nameOrConfig: NodeData<'identifier'> | FunctionDefinitionConfig,
  config?: Partial<FunctionDefinitionConfig>,
): FunctionDefinitionNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'function_definition', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.returnType = (v: any) => { fields['return_type'] = v; return node; };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionDefinition.assign = function(target: AssignableNode<'function_definition'>): FunctionDefinitionNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'function_definition'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = v; return node; };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.returnType = (v: any) => { overrides['return_type'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FutureImportStatementConfig {
  name: NodeData<'dotted_name' | 'aliased_import'>[];
}

export interface FutureImportStatementFromInput {
  name: (NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput))[] | NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput);
}

export type FutureImportStatementNode = NodeData<'future_import_statement'> & {
  name(value: NodeData<'dotted_name' | 'aliased_import'>[]): FutureImportStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'future_import_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type FutureImportStatementFromNode = NodeData<'future_import_statement'> & {
  name(value: (NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput))[] | NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput)): FutureImportStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'future_import_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function futureImportStatement(
  config?: FutureImportStatementConfig,
): FutureImportStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'future_import_statement', fields };
  node.name = (...v: any[]) => { fields['name'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

futureImportStatement.assign = function(target: AssignableNode<'future_import_statement'>): FutureImportStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'future_import_statement'; }, get fields() { return getFields(); } };
  node.name = (...v: any[]) => { overrides['name'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GeneratorExpressionConfig {
  body: NodeData<'expression'>;
  children?: NodeData[];
}

export interface GeneratorExpressionFromInput {
  body: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  children?: (NodeData<'for_in_clause' | 'if_clause'> | ({ kind: 'for_in_clause' } & ForInClauseFromInput) | ({ kind: 'if_clause' } & IfClauseFromInput))[];
}

export type GeneratorExpressionNode = NodeData<'generator_expression'> & {
  children(...value: NodeData[]): GeneratorExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generator_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type GeneratorExpressionFromNode = NodeData<'generator_expression'> & {
  children(...value: FromValue[]): GeneratorExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generator_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function generatorExpression(
  bodyOrConfig: NodeData<'expression'> | GeneratorExpressionConfig,
  config?: Partial<GeneratorExpressionConfig>,
): GeneratorExpressionNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'generator_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

generatorExpression.assign = function(target: AssignableNode<'generator_expression'>): GeneratorExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'generator_expression'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GenericTypeConfig {
  children?: NodeData[];
}

export interface GenericTypeFromInput {
  children?: (NodeData<'identifier' | 'type_parameter'> | string | TypeParameterFromInput)[];
}

export type GenericTypeNode = NodeData<'generic_type'> & {
  children(...value: NodeData[]): GenericTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type GenericTypeFromNode = NodeData<'generic_type'> & {
  children(...value: FromValue[]): GenericTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function genericType(
  childrenOrConfig?: NodeData<'identifier' | 'type_parameter'> | NodeData<'identifier' | 'type_parameter'>[] | GenericTypeConfig,
): GenericTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'generic_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

genericType.assign = function(target: AssignableNode<'generic_type'>): GenericTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'generic_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GlobalStatementConfig {
  children?: NodeData[];
}

export interface GlobalStatementFromInput {
  children?: (NodeData<'identifier'> | string)[];
}

export type GlobalStatementNode = NodeData<'global_statement'> & {
  children(...value: NodeData[]): GlobalStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'global_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type GlobalStatementFromNode = NodeData<'global_statement'> & {
  children(...value: FromValue[]): GlobalStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'global_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function globalStatement(
  childrenOrConfig?: NodeData<'identifier'> | NodeData<'identifier'>[] | GlobalStatementConfig,
): GlobalStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'global_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

globalStatement.assign = function(target: AssignableNode<'global_statement'>): GlobalStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'global_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface IfClauseConfig {
  children?: NodeData[];
}

export interface IfClauseFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type IfClauseNode = NodeData<'if_clause'> & {
  children(...value: NodeData[]): IfClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type IfClauseFromNode = NodeData<'if_clause'> & {
  children(...value: FromValue[]): IfClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function ifClause(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | IfClauseConfig,
): IfClauseNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'if_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

ifClause.assign = function(target: AssignableNode<'if_clause'>): IfClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'if_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface IfStatementConfig {
  condition: NodeData<'expression'>;
  consequence: NodeData<'block'>;
  alternative?: NodeData<'elif_clause' | 'else_clause'>[];
}

export interface IfStatementFromInput {
  condition: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  consequence: NodeData<'block'> | BlockFromInput | FromValue[];
  alternative?: (NodeData<'elif_clause' | 'else_clause'> | ({ kind: 'elif_clause' } & ElifClauseFromInput) | ({ kind: 'else_clause' } & ElseClauseFromInput))[] | NodeData<'elif_clause' | 'else_clause'> | ({ kind: 'elif_clause' } & ElifClauseFromInput) | ({ kind: 'else_clause' } & ElseClauseFromInput);
}

export type IfStatementNode = NodeData<'if_statement'> & {
  consequence(value: NodeData<'block'>): IfStatementNode;
  alternative(value: NodeData<'elif_clause' | 'else_clause'>[]): IfStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type IfStatementFromNode = NodeData<'if_statement'> & {
  consequence(value: NodeData<'block'> | BlockFromInput | FromValue[]): IfStatementFromNode;
  alternative(value: (NodeData<'elif_clause' | 'else_clause'> | ({ kind: 'elif_clause' } & ElifClauseFromInput) | ({ kind: 'else_clause' } & ElseClauseFromInput))[] | NodeData<'elif_clause' | 'else_clause'> | ({ kind: 'elif_clause' } & ElifClauseFromInput) | ({ kind: 'else_clause' } & ElseClauseFromInput)): IfStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function ifStatement(
  conditionOrConfig: NodeData<'expression'> | IfStatementConfig,
  config?: Partial<IfStatementConfig>,
): IfStatementNode {
  const fields: any = isNodeData(conditionOrConfig)
    ? { 'condition': conditionOrConfig, ...config }
    : conditionOrConfig;
  const node: any = { type: 'if_statement', fields };
  node.consequence = (v: any) => { fields['consequence'] = v; return node; };
  node.alternative = (...v: any[]) => { fields['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

ifStatement.assign = function(target: AssignableNode<'if_statement'>): IfStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition', 'consequence', 'alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'if_statement'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = v; return node; };
  node.consequence = (v: any) => { overrides['consequence'] = v; return node; };
  node.alternative = (...v: any[]) => { overrides['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImportFromStatementConfig {
  module_name: NodeData<'relative_import' | 'dotted_name'>;
  name?: NodeData<'dotted_name' | 'aliased_import'>[];
  children?: NodeData[];
}

export interface ImportFromStatementFromInput {
  module_name: NodeData<'relative_import' | 'dotted_name'> | ({ kind: 'relative_import' } & RelativeImportFromInput) | ({ kind: 'dotted_name' } & DottedNameFromInput) | FromValue[];
  name?: (NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput))[] | NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput);
  children?: (NodeData<'wildcard_import'> | string)[];
}

export type ImportFromStatementNode = NodeData<'import_from_statement'> & {
  name(value: NodeData<'dotted_name' | 'aliased_import'>[]): ImportFromStatementNode;
  children(...value: NodeData[]): ImportFromStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_from_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImportFromStatementFromNode = NodeData<'import_from_statement'> & {
  name(value: (NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput))[] | NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput)): ImportFromStatementFromNode;
  children(...value: FromValue[]): ImportFromStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_from_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importFromStatement(
  moduleNameOrConfig: NodeData<'relative_import' | 'dotted_name'> | ImportFromStatementConfig,
  config?: Partial<ImportFromStatementConfig>,
): ImportFromStatementNode {
  const fields: any = isNodeData(moduleNameOrConfig)
    ? { 'module_name': moduleNameOrConfig, ...config }
    : moduleNameOrConfig;
  const node: any = { type: 'import_from_statement', fields };
  node.name = (...v: any[]) => { fields['name'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

importFromStatement.assign = function(target: AssignableNode<'import_from_statement'>): ImportFromStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['module_name', 'name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'import_from_statement'; }, get fields() { return getFields(); } };
  node.moduleName = (v: any) => { overrides['module_name'] = v; return node; };
  node.name = (...v: any[]) => { overrides['name'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImportStatementConfig {
  name: NodeData<'dotted_name' | 'aliased_import'>[];
}

export interface ImportStatementFromInput {
  name: (NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput))[] | NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput);
}

export type ImportStatementNode = NodeData<'import_statement'> & {
  name(value: NodeData<'dotted_name' | 'aliased_import'>[]): ImportStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ImportStatementFromNode = NodeData<'import_statement'> & {
  name(value: (NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput))[] | NodeData<'dotted_name' | 'aliased_import'> | ({ kind: 'dotted_name' } & DottedNameFromInput) | ({ kind: 'aliased_import' } & AliasedImportFromInput)): ImportStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'import_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function importStatement(
  config?: ImportStatementConfig,
): ImportStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'import_statement', fields };
  node.name = (...v: any[]) => { fields['name'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

importStatement.assign = function(target: AssignableNode<'import_statement'>): ImportStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'import_statement'; }, get fields() { return getFields(); } };
  node.name = (...v: any[]) => { overrides['name'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface InterpolationConfig {
  expression: NodeData<'expression' | 'expression_list' | 'pattern_list' | 'yield'>;
  type_conversion?: NodeData<'type_conversion'>;
  format_specifier?: NodeData<'format_specifier'>;
}

export interface InterpolationFromInput {
  expression: NodeData<'expression' | 'expression_list' | 'pattern_list' | 'yield'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput) | ({ kind: 'pattern_list' } & PatternListFromInput) | ({ kind: 'yield' } & YieldFromInput) | FromValue[];
  type_conversion?: NodeData<'type_conversion'> | string;
  format_specifier?: NodeData<'format_specifier'> | FormatSpecifierFromInput | FromValue[];
}

export type InterpolationNode = NodeData<'interpolation'> & {
  typeConversion(value: NodeData<'type_conversion'>): InterpolationNode;
  formatSpecifier(value: NodeData<'format_specifier'>): InterpolationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'interpolation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type InterpolationFromNode = NodeData<'interpolation'> & {
  typeConversion(value: NodeData<'type_conversion'> | string): InterpolationFromNode;
  formatSpecifier(value: NodeData<'format_specifier'> | FormatSpecifierFromInput | FromValue[]): InterpolationFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'interpolation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function interpolation(
  expressionOrConfig: NodeData<'expression' | 'expression_list' | 'pattern_list' | 'yield'> | InterpolationConfig,
  config?: Partial<InterpolationConfig>,
): InterpolationNode {
  const fields: any = isNodeData(expressionOrConfig)
    ? { 'expression': expressionOrConfig, ...config }
    : expressionOrConfig;
  const node: any = { type: 'interpolation', fields };
  node.typeConversion = (v: any) => { fields['type_conversion'] = v; return node; };
  node.formatSpecifier = (v: any) => { fields['format_specifier'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

interpolation.assign = function(target: AssignableNode<'interpolation'>): InterpolationNode {
  const overrides: any = {};
  const fieldList: string[] = ['expression', 'type_conversion', 'format_specifier'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'interpolation'; }, get fields() { return getFields(); } };
  node.expression = (v: any) => { overrides['expression'] = v; return node; };
  node.typeConversion = (v: any) => { overrides['type_conversion'] = v; return node; };
  node.formatSpecifier = (v: any) => { overrides['format_specifier'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface KeywordArgumentConfig {
  name: NodeData<'identifier' | 'keyword_identifier'>;
  value: NodeData<'expression'>;
}

export interface KeywordArgumentFromInput {
  name: NodeData<'identifier' | 'keyword_identifier'> | string | KeywordIdentifierFromInput | FromValue[];
  value: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type KeywordArgumentNode = NodeData<'keyword_argument'> & {
  value(value: NodeData<'expression'>): KeywordArgumentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'keyword_argument'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type KeywordArgumentFromNode = NodeData<'keyword_argument'> & {
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): KeywordArgumentFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'keyword_argument'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function keywordArgument(
  nameOrConfig: NodeData<'identifier' | 'keyword_identifier'> | KeywordArgumentConfig,
  config?: Partial<KeywordArgumentConfig>,
): KeywordArgumentNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'keyword_argument', fields };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

keywordArgument.assign = function(target: AssignableNode<'keyword_argument'>): KeywordArgumentNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'keyword_argument'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface KeywordPatternConfig {
  children?: NodeData[];
}

export interface KeywordPatternFromInput {
  children?: (NodeData<'identifier' | 'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | string | ({ kind: 'class_pattern' } & ClassPatternFromInput) | ({ kind: 'splat_pattern' } & SplatPatternFromInput) | ({ kind: 'union_pattern' } & UnionPatternFromInput) | ({ kind: 'list_pattern' } & ListPatternFromInput) | ({ kind: 'tuple_pattern' } & TuplePatternFromInput) | ({ kind: 'dict_pattern' } & DictPatternFromInput) | ({ kind: 'string' } & StringFromInput) | ({ kind: 'concatenated_string' } & ConcatenatedStringFromInput) | ({ kind: 'complex_pattern' } & ComplexPatternFromInput) | ({ kind: 'dotted_name' } & DottedNameFromInput))[];
}

export type KeywordPatternNode = NodeData<'keyword_pattern'> & {
  children(...value: NodeData[]): KeywordPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'keyword_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type KeywordPatternFromNode = NodeData<'keyword_pattern'> & {
  children(...value: FromValue[]): KeywordPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'keyword_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function keywordPattern(
  childrenOrConfig?: NodeData<'identifier' | 'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | NodeData<'identifier' | 'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'>[] | KeywordPatternConfig,
): KeywordPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'keyword_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

keywordPattern.assign = function(target: AssignableNode<'keyword_pattern'>): KeywordPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'keyword_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LambdaConfig {
  parameters?: NodeData<'lambda_parameters'>;
  body: NodeData<'expression'>;
}

export interface LambdaFromInput {
  parameters?: NodeData<'lambda_parameters'> | LambdaParametersFromInput | FromValue[];
  body: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type LambdaNode = NodeData<'lambda'> & {
  parameters(value: NodeData<'lambda_parameters'>): LambdaNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lambda'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type LambdaFromNode = NodeData<'lambda'> & {
  parameters(value: NodeData<'lambda_parameters'> | LambdaParametersFromInput | FromValue[]): LambdaFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lambda'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lambda(
  bodyOrConfig: NodeData<'expression'> | LambdaConfig,
  config?: Partial<LambdaConfig>,
): LambdaNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'lambda', fields };
  node.parameters = (v: any) => { fields['parameters'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

lambda.assign = function(target: AssignableNode<'lambda'>): LambdaNode {
  const overrides: any = {};
  const fieldList: string[] = ['parameters', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'lambda'; }, get fields() { return getFields(); } };
  node.parameters = (v: any) => { overrides['parameters'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LambdaParametersConfig {
  children?: NodeData[];
}

export interface LambdaParametersFromInput {
  children?: (NodeData<'parameter'> | ParameterFromInput)[];
}

export type LambdaParametersNode = NodeData<'lambda_parameters'> & {
  children(...value: NodeData[]): LambdaParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lambda_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type LambdaParametersFromNode = NodeData<'lambda_parameters'> & {
  children(...value: FromValue[]): LambdaParametersFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lambda_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lambdaParameters(
  childrenOrConfig?: NodeData<'parameter'> | NodeData<'parameter'>[] | LambdaParametersConfig,
): LambdaParametersNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'lambda_parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

lambdaParameters.assign = function(target: AssignableNode<'lambda_parameters'>): LambdaParametersNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'lambda_parameters'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ListConfig {
  children?: NodeData[];
}

export interface ListFromInput {
  children?: (NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'yield' } & YieldFromInput) | ({ kind: 'list_splat' } & ListSplatFromInput) | ({ kind: 'parenthesized_list_splat' } & ParenthesizedListSplatFromInput))[];
}

export type ListNode = NodeData<'list'> & {
  children(...value: NodeData[]): ListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ListFromNode = NodeData<'list'> & {
  children(...value: FromValue[]): ListFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function list(
  childrenOrConfig?: NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'> | NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'>[] | ListConfig,
): ListNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

list.assign = function(target: AssignableNode<'list'>): ListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ListComprehensionConfig {
  body: NodeData<'expression'>;
  children?: NodeData[];
}

export interface ListComprehensionFromInput {
  body: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  children?: (NodeData<'for_in_clause' | 'if_clause'> | ({ kind: 'for_in_clause' } & ForInClauseFromInput) | ({ kind: 'if_clause' } & IfClauseFromInput))[];
}

export type ListComprehensionNode = NodeData<'list_comprehension'> & {
  children(...value: NodeData[]): ListComprehensionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ListComprehensionFromNode = NodeData<'list_comprehension'> & {
  children(...value: FromValue[]): ListComprehensionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function listComprehension(
  bodyOrConfig: NodeData<'expression'> | ListComprehensionConfig,
  config?: Partial<ListComprehensionConfig>,
): ListComprehensionNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'list_comprehension', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

listComprehension.assign = function(target: AssignableNode<'list_comprehension'>): ListComprehensionNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'list_comprehension'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ListPatternConfig {
  children?: NodeData[];
}

export interface ListPatternFromInput {
  children?: (NodeData<'pattern'> | PatternFromInput)[];
}

export type ListPatternNode = NodeData<'list_pattern'> & {
  children(...value: NodeData[]): ListPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ListPatternFromNode = NodeData<'list_pattern'> & {
  children(...value: FromValue[]): ListPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function listPattern(
  childrenOrConfig?: NodeData<'pattern'> | NodeData<'pattern'>[] | ListPatternConfig,
): ListPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'list_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

listPattern.assign = function(target: AssignableNode<'list_pattern'>): ListPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'list_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ListSplatConfig {
  children?: NodeData[];
}

export interface ListSplatFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type ListSplatNode = NodeData<'list_splat'> & {
  children(...value: NodeData[]): ListSplatNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ListSplatFromNode = NodeData<'list_splat'> & {
  children(...value: FromValue[]): ListSplatFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function listSplat(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | ListSplatConfig,
): ListSplatNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'list_splat', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

listSplat.assign = function(target: AssignableNode<'list_splat'>): ListSplatNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'list_splat'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ListSplatPatternConfig {
  children?: NodeData[];
}

export interface ListSplatPatternFromInput {
  children?: (NodeData<'identifier' | 'keyword_identifier' | 'subscript' | 'attribute'> | string | ({ kind: 'keyword_identifier' } & KeywordIdentifierFromInput) | ({ kind: 'subscript' } & SubscriptFromInput) | ({ kind: 'attribute' } & AttributeFromInput))[];
}

export type ListSplatPatternNode = NodeData<'list_splat_pattern'> & {
  children(...value: NodeData[]): ListSplatPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ListSplatPatternFromNode = NodeData<'list_splat_pattern'> & {
  children(...value: FromValue[]): ListSplatPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'list_splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function listSplatPattern(
  childrenOrConfig?: NodeData<'identifier' | 'keyword_identifier' | 'subscript' | 'attribute'> | NodeData<'identifier' | 'keyword_identifier' | 'subscript' | 'attribute'>[] | ListSplatPatternConfig,
): ListSplatPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'list_splat_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

listSplatPattern.assign = function(target: AssignableNode<'list_splat_pattern'>): ListSplatPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'list_splat_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MatchStatementConfig {
  subject: NodeData<'expression'>[];
  body: NodeData<'block'>;
}

export interface MatchStatementFromInput {
  subject: (NodeData<'expression'> | ExpressionFromInput)[] | NodeData<'expression'> | ExpressionFromInput;
  body: NodeData<'block'> | BlockFromInput | FromValue[];
}

export type MatchStatementNode = NodeData<'match_statement'> & {
  subject(value: NodeData<'expression'>[]): MatchStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'match_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type MatchStatementFromNode = NodeData<'match_statement'> & {
  subject(value: (NodeData<'expression'> | ExpressionFromInput)[] | NodeData<'expression'> | ExpressionFromInput): MatchStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'match_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function matchStatement(
  bodyOrConfig: NodeData<'block'> | MatchStatementConfig,
  config?: Partial<MatchStatementConfig>,
): MatchStatementNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'match_statement', fields };
  node.subject = (...v: any[]) => { fields['subject'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

matchStatement.assign = function(target: AssignableNode<'match_statement'>): MatchStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['subject', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'match_statement'; }, get fields() { return getFields(); } };
  node.subject = (...v: any[]) => { overrides['subject'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MemberTypeConfig {
  children?: NodeData[];
}

export interface MemberTypeFromInput {
  children?: (NodeData<'type' | 'identifier'> | string | TypeFromInput)[];
}

export type MemberTypeNode = NodeData<'member_type'> & {
  children(...value: NodeData[]): MemberTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'member_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type MemberTypeFromNode = NodeData<'member_type'> & {
  children(...value: FromValue[]): MemberTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'member_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function memberType(
  childrenOrConfig?: NodeData<'type' | 'identifier'> | NodeData<'type' | 'identifier'>[] | MemberTypeConfig,
): MemberTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'member_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

memberType.assign = function(target: AssignableNode<'member_type'>): MemberTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'member_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ModuleConfig {
  children?: NodeData[];
}

export interface ModuleFromInput {
  children?: (NodeData<'future_import_statement' | 'import_statement' | 'import_from_statement' | 'print_statement' | 'assert_statement' | 'expression_statement' | 'return_statement' | 'delete_statement' | 'raise_statement' | 'pass_statement' | 'break_statement' | 'continue_statement' | 'global_statement' | 'nonlocal_statement' | 'exec_statement' | 'type_alias_statement' | 'if_statement' | 'for_statement' | 'while_statement' | 'try_statement' | 'with_statement' | 'function_definition' | 'class_definition' | 'decorated_definition' | 'match_statement'> | string | ({ kind: 'future_import_statement' } & FutureImportStatementFromInput) | ({ kind: 'import_statement' } & ImportStatementFromInput) | ({ kind: 'import_from_statement' } & ImportFromStatementFromInput) | ({ kind: 'print_statement' } & PrintStatementFromInput) | ({ kind: 'assert_statement' } & AssertStatementFromInput) | ({ kind: 'expression_statement' } & ExpressionStatementFromInput) | ({ kind: 'return_statement' } & ReturnStatementFromInput) | ({ kind: 'delete_statement' } & DeleteStatementFromInput) | ({ kind: 'raise_statement' } & RaiseStatementFromInput) | ({ kind: 'global_statement' } & GlobalStatementFromInput) | ({ kind: 'nonlocal_statement' } & NonlocalStatementFromInput) | ({ kind: 'exec_statement' } & ExecStatementFromInput) | ({ kind: 'type_alias_statement' } & TypeAliasStatementFromInput) | ({ kind: 'if_statement' } & IfStatementFromInput) | ({ kind: 'for_statement' } & ForStatementFromInput) | ({ kind: 'while_statement' } & WhileStatementFromInput) | ({ kind: 'try_statement' } & TryStatementFromInput) | ({ kind: 'with_statement' } & WithStatementFromInput) | ({ kind: 'function_definition' } & FunctionDefinitionFromInput) | ({ kind: 'class_definition' } & ClassDefinitionFromInput) | ({ kind: 'decorated_definition' } & DecoratedDefinitionFromInput) | ({ kind: 'match_statement' } & MatchStatementFromInput))[];
}

export type ModuleNode = NodeData<'module'> & {
  children(...value: NodeData[]): ModuleNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'module'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ModuleFromNode = NodeData<'module'> & {
  children(...value: FromValue[]): ModuleFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'module'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function module(
  childrenOrConfig?: NodeData<'future_import_statement' | 'import_statement' | 'import_from_statement' | 'print_statement' | 'assert_statement' | 'expression_statement' | 'return_statement' | 'delete_statement' | 'raise_statement' | 'pass_statement' | 'break_statement' | 'continue_statement' | 'global_statement' | 'nonlocal_statement' | 'exec_statement' | 'type_alias_statement' | 'if_statement' | 'for_statement' | 'while_statement' | 'try_statement' | 'with_statement' | 'function_definition' | 'class_definition' | 'decorated_definition' | 'match_statement'> | NodeData<'future_import_statement' | 'import_statement' | 'import_from_statement' | 'print_statement' | 'assert_statement' | 'expression_statement' | 'return_statement' | 'delete_statement' | 'raise_statement' | 'pass_statement' | 'break_statement' | 'continue_statement' | 'global_statement' | 'nonlocal_statement' | 'exec_statement' | 'type_alias_statement' | 'if_statement' | 'for_statement' | 'while_statement' | 'try_statement' | 'with_statement' | 'function_definition' | 'class_definition' | 'decorated_definition' | 'match_statement'>[] | ModuleConfig,
): ModuleNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'module', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

module.assign = function(target: AssignableNode<'module'>): ModuleNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'module'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NamedExpressionConfig {
  name: NodeData<'identifier' | 'keyword_identifier'>;
  value: NodeData<'expression'>;
}

export interface NamedExpressionFromInput {
  name: NodeData<'identifier' | 'keyword_identifier'> | string | KeywordIdentifierFromInput | FromValue[];
  value: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type NamedExpressionNode = NodeData<'named_expression'> & {
  value(value: NodeData<'expression'>): NamedExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'named_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NamedExpressionFromNode = NodeData<'named_expression'> & {
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): NamedExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'named_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function namedExpression(
  nameOrConfig: NodeData<'identifier' | 'keyword_identifier'> | NamedExpressionConfig,
  config?: Partial<NamedExpressionConfig>,
): NamedExpressionNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'named_expression', fields };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

namedExpression.assign = function(target: AssignableNode<'named_expression'>): NamedExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'named_expression'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NonlocalStatementConfig {
  children?: NodeData[];
}

export interface NonlocalStatementFromInput {
  children?: (NodeData<'identifier'> | string)[];
}

export type NonlocalStatementNode = NodeData<'nonlocal_statement'> & {
  children(...value: NodeData[]): NonlocalStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'nonlocal_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NonlocalStatementFromNode = NodeData<'nonlocal_statement'> & {
  children(...value: FromValue[]): NonlocalStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'nonlocal_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function nonlocalStatement(
  childrenOrConfig?: NodeData<'identifier'> | NodeData<'identifier'>[] | NonlocalStatementConfig,
): NonlocalStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'nonlocal_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

nonlocalStatement.assign = function(target: AssignableNode<'nonlocal_statement'>): NonlocalStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'nonlocal_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NotOperatorConfig {
  argument: NodeData<'expression'>;
}

export interface NotOperatorFromInput {
  argument: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type NotOperatorNode = NodeData<'not_operator'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'not_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type NotOperatorFromNode = NodeData<'not_operator'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'not_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function notOperator(
  argumentOrConfig: NodeData<'expression'> | NotOperatorConfig,
  config?: Partial<NotOperatorConfig>,
): NotOperatorNode {
  const fields: any = isNodeData(argumentOrConfig)
    ? { 'argument': argumentOrConfig, ...config }
    : argumentOrConfig;
  const node: any = { type: 'not_operator', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

notOperator.assign = function(target: AssignableNode<'not_operator'>): NotOperatorNode {
  const overrides: any = {};
  const fieldList: string[] = ['argument'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'not_operator'; }, get fields() { return getFields(); } };
  node.argument = (v: any) => { overrides['argument'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface PairConfig {
  key: NodeData<'expression'>;
  value: NodeData<'expression'>;
}

export interface PairFromInput {
  key: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  value: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type PairNode = NodeData<'pair'> & {
  value(value: NodeData<'expression'>): PairNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pair'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type PairFromNode = NodeData<'pair'> & {
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): PairFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pair'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function pair(
  keyOrConfig: NodeData<'expression'> | PairConfig,
  config?: Partial<PairConfig>,
): PairNode {
  const fields: any = isNodeData(keyOrConfig)
    ? { 'key': keyOrConfig, ...config }
    : keyOrConfig;
  const node: any = { type: 'pair', fields };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

pair.assign = function(target: AssignableNode<'pair'>): PairNode {
  const overrides: any = {};
  const fieldList: string[] = ['key', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'pair'; }, get fields() { return getFields(); } };
  node.key = (v: any) => { overrides['key'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ParametersConfig {
  children?: NodeData[];
}

export interface ParametersFromInput {
  children?: (NodeData<'parameter'> | ParameterFromInput)[];
}

export type ParametersNode = NodeData<'parameters'> & {
  children(...value: NodeData[]): ParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ParametersFromNode = NodeData<'parameters'> & {
  children(...value: FromValue[]): ParametersFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parameters(
  childrenOrConfig?: NodeData<'parameter'> | NodeData<'parameter'>[] | ParametersConfig,
): ParametersNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

parameters.assign = function(target: AssignableNode<'parameters'>): ParametersNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'parameters'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ParenthesizedExpressionConfig {
  children?: NodeData[];
}

export interface ParenthesizedExpressionFromInput {
  children?: (NodeData<'expression' | 'yield'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'yield' } & YieldFromInput))[];
}

export type ParenthesizedExpressionNode = NodeData<'parenthesized_expression'> & {
  children(...value: NodeData[]): ParenthesizedExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ParenthesizedExpressionFromNode = NodeData<'parenthesized_expression'> & {
  children(...value: FromValue[]): ParenthesizedExpressionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parenthesizedExpression(
  childrenOrConfig?: NodeData<'expression' | 'yield'> | NodeData<'expression' | 'yield'>[] | ParenthesizedExpressionConfig,
): ParenthesizedExpressionNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'parenthesized_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

parenthesizedExpression.assign = function(target: AssignableNode<'parenthesized_expression'>): ParenthesizedExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'parenthesized_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ParenthesizedListSplatConfig {
  children?: NodeData[];
}

export interface ParenthesizedListSplatFromInput {
  children?: (NodeData<'parenthesized_expression' | 'list_splat'> | ({ kind: 'parenthesized_expression' } & ParenthesizedExpressionFromInput) | ({ kind: 'list_splat' } & ListSplatFromInput))[];
}

export type ParenthesizedListSplatNode = NodeData<'parenthesized_list_splat'> & {
  children(...value: NodeData[]): ParenthesizedListSplatNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_list_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ParenthesizedListSplatFromNode = NodeData<'parenthesized_list_splat'> & {
  children(...value: FromValue[]): ParenthesizedListSplatFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_list_splat'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parenthesizedListSplat(
  childrenOrConfig?: NodeData<'parenthesized_expression' | 'list_splat'> | NodeData<'parenthesized_expression' | 'list_splat'>[] | ParenthesizedListSplatConfig,
): ParenthesizedListSplatNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'parenthesized_list_splat', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

parenthesizedListSplat.assign = function(target: AssignableNode<'parenthesized_list_splat'>): ParenthesizedListSplatNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'parenthesized_list_splat'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface PatternListConfig {
  children?: NodeData[];
}

export interface PatternListFromInput {
  children?: (NodeData<'pattern'> | PatternFromInput)[];
}

export type PatternListNode = NodeData<'pattern_list'> & {
  children(...value: NodeData[]): PatternListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pattern_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type PatternListFromNode = NodeData<'pattern_list'> & {
  children(...value: FromValue[]): PatternListFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pattern_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function patternList(
  childrenOrConfig?: NodeData<'pattern'> | NodeData<'pattern'>[] | PatternListConfig,
): PatternListNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'pattern_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

patternList.assign = function(target: AssignableNode<'pattern_list'>): PatternListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'pattern_list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface PrintStatementConfig {
  argument?: NodeData<'expression'>[];
  children?: NodeData[];
}

export interface PrintStatementFromInput {
  argument?: (NodeData<'expression'> | ExpressionFromInput)[] | NodeData<'expression'> | ExpressionFromInput;
  children?: (NodeData<'chevron'> | ChevronFromInput)[];
}

export type PrintStatementNode = NodeData<'print_statement'> & {
  argument(value: NodeData<'expression'>[]): PrintStatementNode;
  children(...value: NodeData[]): PrintStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'print_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type PrintStatementFromNode = NodeData<'print_statement'> & {
  argument(value: (NodeData<'expression'> | ExpressionFromInput)[] | NodeData<'expression'> | ExpressionFromInput): PrintStatementFromNode;
  children(...value: FromValue[]): PrintStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'print_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function printStatement(
  childrenOrConfig?: NodeData<'chevron'> | NodeData<'chevron'>[] | PrintStatementConfig,
): PrintStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'print_statement', fields };
  node.argument = (...v: any[]) => { fields['argument'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

printStatement.assign = function(target: AssignableNode<'print_statement'>): PrintStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['argument'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'print_statement'; }, get fields() { return getFields(); } };
  node.argument = (...v: any[]) => { overrides['argument'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RaiseStatementConfig {
  cause?: NodeData<'expression'>;
  children?: NodeData[];
}

export interface RaiseStatementFromInput {
  cause?: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  children?: (NodeData<'expression' | 'expression_list'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput))[];
}

export type RaiseStatementNode = NodeData<'raise_statement'> & {
  cause(value: NodeData<'expression'>): RaiseStatementNode;
  children(...value: NodeData[]): RaiseStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'raise_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type RaiseStatementFromNode = NodeData<'raise_statement'> & {
  cause(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): RaiseStatementFromNode;
  children(...value: FromValue[]): RaiseStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'raise_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function raiseStatement(
  childrenOrConfig?: NodeData<'expression' | 'expression_list'> | NodeData<'expression' | 'expression_list'>[] | RaiseStatementConfig,
): RaiseStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'raise_statement', fields };
  node.cause = (v: any) => { fields['cause'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

raiseStatement.assign = function(target: AssignableNode<'raise_statement'>): RaiseStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['cause'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'raise_statement'; }, get fields() { return getFields(); } };
  node.cause = (v: any) => { overrides['cause'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RelativeImportConfig {
  children?: NodeData[];
}

export interface RelativeImportFromInput {
  children?: (NodeData<'import_prefix' | 'dotted_name'> | string | DottedNameFromInput)[];
}

export type RelativeImportNode = NodeData<'relative_import'> & {
  children(...value: NodeData[]): RelativeImportNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'relative_import'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type RelativeImportFromNode = NodeData<'relative_import'> & {
  children(...value: FromValue[]): RelativeImportFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'relative_import'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function relativeImport(
  childrenOrConfig?: NodeData<'import_prefix' | 'dotted_name'> | NodeData<'import_prefix' | 'dotted_name'>[] | RelativeImportConfig,
): RelativeImportNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'relative_import', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

relativeImport.assign = function(target: AssignableNode<'relative_import'>): RelativeImportNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'relative_import'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ReturnStatementConfig {
  children?: NodeData[];
}

export interface ReturnStatementFromInput {
  children?: (NodeData<'expression' | 'expression_list'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput))[];
}

export type ReturnStatementNode = NodeData<'return_statement'> & {
  children(...value: NodeData[]): ReturnStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'return_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type ReturnStatementFromNode = NodeData<'return_statement'> & {
  children(...value: FromValue[]): ReturnStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'return_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function returnStatement(
  childrenOrConfig?: NodeData<'expression' | 'expression_list'> | NodeData<'expression' | 'expression_list'>[] | ReturnStatementConfig,
): ReturnStatementNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'return_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

returnStatement.assign = function(target: AssignableNode<'return_statement'>): ReturnStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'return_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SetConfig {
  children?: NodeData[];
}

export interface SetFromInput {
  children?: (NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'yield' } & YieldFromInput) | ({ kind: 'list_splat' } & ListSplatFromInput) | ({ kind: 'parenthesized_list_splat' } & ParenthesizedListSplatFromInput))[];
}

export type SetNode = NodeData<'set'> & {
  children(...value: NodeData[]): SetNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'set'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SetFromNode = NodeData<'set'> & {
  children(...value: FromValue[]): SetFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'set'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function set(
  childrenOrConfig?: NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'> | NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'>[] | SetConfig,
): SetNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'set', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

set.assign = function(target: AssignableNode<'set'>): SetNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'set'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SetComprehensionConfig {
  body: NodeData<'expression'>;
  children?: NodeData[];
}

export interface SetComprehensionFromInput {
  body: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  children?: (NodeData<'for_in_clause' | 'if_clause'> | ({ kind: 'for_in_clause' } & ForInClauseFromInput) | ({ kind: 'if_clause' } & IfClauseFromInput))[];
}

export type SetComprehensionNode = NodeData<'set_comprehension'> & {
  children(...value: NodeData[]): SetComprehensionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'set_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SetComprehensionFromNode = NodeData<'set_comprehension'> & {
  children(...value: FromValue[]): SetComprehensionFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'set_comprehension'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function setComprehension(
  bodyOrConfig: NodeData<'expression'> | SetComprehensionConfig,
  config?: Partial<SetComprehensionConfig>,
): SetComprehensionNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'set_comprehension', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

setComprehension.assign = function(target: AssignableNode<'set_comprehension'>): SetComprehensionNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'set_comprehension'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SliceConfig {
  children?: NodeData[];
}

export interface SliceFromInput {
  children?: (NodeData<'expression'> | ExpressionFromInput)[];
}

export type SliceNode = NodeData<'slice'> & {
  children(...value: NodeData[]): SliceNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'slice'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SliceFromNode = NodeData<'slice'> & {
  children(...value: FromValue[]): SliceFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'slice'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function slice(
  childrenOrConfig?: NodeData<'expression'> | NodeData<'expression'>[] | SliceConfig,
): SliceNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'slice', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

slice.assign = function(target: AssignableNode<'slice'>): SliceNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'slice'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SplatPatternConfig {
  children?: NodeData[];
}

export interface SplatPatternFromInput {
  children?: (NodeData<'identifier'> | string)[];
}

export type SplatPatternNode = NodeData<'splat_pattern'> & {
  children(...value: NodeData[]): SplatPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SplatPatternFromNode = NodeData<'splat_pattern'> & {
  children(...value: FromValue[]): SplatPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'splat_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function splatPattern(
  childrenOrConfig?: NodeData<'identifier'> | NodeData<'identifier'>[] | SplatPatternConfig,
): SplatPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'splat_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

splatPattern.assign = function(target: AssignableNode<'splat_pattern'>): SplatPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'splat_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SplatTypeConfig {
  children?: NodeData[];
}

export interface SplatTypeFromInput {
  children?: (NodeData<'identifier'> | string)[];
}

export type SplatTypeNode = NodeData<'splat_type'> & {
  children(...value: NodeData[]): SplatTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'splat_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SplatTypeFromNode = NodeData<'splat_type'> & {
  children(...value: FromValue[]): SplatTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'splat_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function splatType(
  childrenOrConfig?: NodeData<'identifier'> | NodeData<'identifier'>[] | SplatTypeConfig,
): SplatTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'splat_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

splatType.assign = function(target: AssignableNode<'splat_type'>): SplatTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'splat_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StringConfig {
  children?: NodeData[];
}

export interface StringFromInput {
  children?: (NodeData<'string_start' | 'interpolation' | 'string_content' | 'string_end'> | string | ({ kind: 'interpolation' } & InterpolationFromInput) | ({ kind: 'string_content' } & StringContentFromInput))[];
}

export type StringNode = NodeData<'string'> & {
  children(...value: NodeData[]): StringNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type StringFromNode = NodeData<'string'> & {
  children(...value: FromValue[]): StringFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function string(
  childrenOrConfig?: NodeData<'string_start' | 'interpolation' | 'string_content' | 'string_end'> | NodeData<'string_start' | 'interpolation' | 'string_content' | 'string_end'>[] | StringConfig,
): StringNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'string', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

string.assign = function(target: AssignableNode<'string'>): StringNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'string'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StringContentConfig {
  children?: NodeData[];
}

export interface StringContentFromInput {
  children?: (NodeData<'escape_interpolation' | 'escape_sequence'> | string)[];
}

export type StringContentNode = NodeData<'string_content'> & {
  children(...value: NodeData[]): StringContentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string_content'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type StringContentFromNode = NodeData<'string_content'> & {
  children(...value: FromValue[]): StringContentFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string_content'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function stringContent(
  childrenOrConfig?: NodeData<'escape_interpolation' | 'escape_sequence'> | NodeData<'escape_interpolation' | 'escape_sequence'>[] | StringContentConfig,
): StringContentNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'string_content', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

stringContent.assign = function(target: AssignableNode<'string_content'>): StringContentNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'string_content'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SubscriptConfig {
  value: NodeData<'primary_expression'>;
  subscript: NodeData<'expression' | 'slice'>[];
}

export interface SubscriptFromInput {
  value: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[];
  subscript: (NodeData<'expression' | 'slice'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'slice' } & SliceFromInput))[] | NodeData<'expression' | 'slice'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'slice' } & SliceFromInput);
}

export type SubscriptNode = NodeData<'subscript'> & {
  subscript(value: NodeData<'expression' | 'slice'>[]): SubscriptNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'subscript'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type SubscriptFromNode = NodeData<'subscript'> & {
  subscript(value: (NodeData<'expression' | 'slice'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'slice' } & SliceFromInput))[] | NodeData<'expression' | 'slice'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'slice' } & SliceFromInput)): SubscriptFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'subscript'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function subscript(
  valueOrConfig: NodeData<'primary_expression'> | SubscriptConfig,
  config?: Partial<SubscriptConfig>,
): SubscriptNode {
  const fields: any = isNodeData(valueOrConfig)
    ? { 'value': valueOrConfig, ...config }
    : valueOrConfig;
  const node: any = { type: 'subscript', fields };
  node.subscript = (...v: any[]) => { fields['subscript'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

subscript.assign = function(target: AssignableNode<'subscript'>): SubscriptNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'subscript'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'subscript'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.subscript = (...v: any[]) => { overrides['subscript'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TryStatementConfig {
  body: NodeData<'block'>;
  children?: NodeData[];
}

export interface TryStatementFromInput {
  body: NodeData<'block'> | BlockFromInput | FromValue[];
  children?: (NodeData<'except_clause' | 'else_clause' | 'finally_clause'> | ({ kind: 'except_clause' } & ExceptClauseFromInput) | ({ kind: 'else_clause' } & ElseClauseFromInput) | ({ kind: 'finally_clause' } & FinallyClauseFromInput))[];
}

export type TryStatementNode = NodeData<'try_statement'> & {
  children(...value: NodeData[]): TryStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'try_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TryStatementFromNode = NodeData<'try_statement'> & {
  children(...value: FromValue[]): TryStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'try_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tryStatement(
  bodyOrConfig: NodeData<'block'> | TryStatementConfig,
  config?: Partial<TryStatementConfig>,
): TryStatementNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'try_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tryStatement.assign = function(target: AssignableNode<'try_statement'>): TryStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'try_statement'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TupleConfig {
  children?: NodeData[];
}

export interface TupleFromInput {
  children?: (NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'yield' } & YieldFromInput) | ({ kind: 'list_splat' } & ListSplatFromInput) | ({ kind: 'parenthesized_list_splat' } & ParenthesizedListSplatFromInput))[];
}

export type TupleNode = NodeData<'tuple'> & {
  children(...value: NodeData[]): TupleNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TupleFromNode = NodeData<'tuple'> & {
  children(...value: FromValue[]): TupleFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tuple(
  childrenOrConfig?: NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'> | NodeData<'expression' | 'yield' | 'list_splat' | 'parenthesized_list_splat'>[] | TupleConfig,
): TupleNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'tuple', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tuple.assign = function(target: AssignableNode<'tuple'>): TupleNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'tuple'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TuplePatternConfig {
  children?: NodeData[];
}

export interface TuplePatternFromInput {
  children?: (NodeData<'pattern'> | PatternFromInput)[];
}

export type TuplePatternNode = NodeData<'tuple_pattern'> & {
  children(...value: NodeData[]): TuplePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TuplePatternFromNode = NodeData<'tuple_pattern'> & {
  children(...value: FromValue[]): TuplePatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tuplePattern(
  childrenOrConfig?: NodeData<'pattern'> | NodeData<'pattern'>[] | TuplePatternConfig,
): TuplePatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'tuple_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tuplePattern.assign = function(target: AssignableNode<'tuple_pattern'>): TuplePatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'tuple_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeConfig {
  children?: NodeData[];
}

export interface TypeFromInput {
  children?: (NodeData<'expression' | 'splat_type' | 'generic_type' | 'union_type' | 'constrained_type' | 'member_type'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'splat_type' } & SplatTypeFromInput) | ({ kind: 'generic_type' } & GenericTypeFromInput) | ({ kind: 'union_type' } & UnionTypeFromInput) | ({ kind: 'constrained_type' } & ConstrainedTypeFromInput) | ({ kind: 'member_type' } & MemberTypeFromInput))[];
}

export type TypeNode = NodeData<'type'> & {
  children(...value: NodeData[]): TypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeFromNode = NodeData<'type'> & {
  children(...value: FromValue[]): TypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function type_(
  childrenOrConfig?: NodeData<'expression' | 'splat_type' | 'generic_type' | 'union_type' | 'constrained_type' | 'member_type'> | NodeData<'expression' | 'splat_type' | 'generic_type' | 'union_type' | 'constrained_type' | 'member_type'>[] | TypeConfig,
): TypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

type_.assign = function(target: AssignableNode<'type'>): TypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeAliasStatementConfig {
  left: NodeData<'type'>;
  right: NodeData<'type'>;
}

export interface TypeAliasStatementFromInput {
  left: NodeData<'type'> | TypeFromInput | FromValue[];
  right: NodeData<'type'> | TypeFromInput | FromValue[];
}

export type TypeAliasStatementNode = NodeData<'type_alias_statement'> & {
  right(value: NodeData<'type'>): TypeAliasStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_alias_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeAliasStatementFromNode = NodeData<'type_alias_statement'> & {
  right(value: NodeData<'type'> | TypeFromInput | FromValue[]): TypeAliasStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_alias_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeAliasStatement(
  leftOrConfig: NodeData<'type'> | TypeAliasStatementConfig,
  config?: Partial<TypeAliasStatementConfig>,
): TypeAliasStatementNode {
  const fields: any = isNodeData(leftOrConfig)
    ? { 'left': leftOrConfig, ...config }
    : leftOrConfig;
  const node: any = { type: 'type_alias_statement', fields };
  node.right = (v: any) => { fields['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeAliasStatement.assign = function(target: AssignableNode<'type_alias_statement'>): TypeAliasStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'type_alias_statement'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = v; return node; };
  node.right = (v: any) => { overrides['right'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeParameterConfig {
  children?: NodeData[];
}

export interface TypeParameterFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type TypeParameterNode = NodeData<'type_parameter'> & {
  children(...value: NodeData[]): TypeParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypeParameterFromNode = NodeData<'type_parameter'> & {
  children(...value: FromValue[]): TypeParameterFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeParameter(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | TypeParameterConfig,
): TypeParameterNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'type_parameter', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeParameter.assign = function(target: AssignableNode<'type_parameter'>): TypeParameterNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_parameter'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypedDefaultParameterConfig {
  name: NodeData<'identifier'>;
  type: NodeData<'type'>;
  value: NodeData<'expression'>;
}

export interface TypedDefaultParameterFromInput {
  name: NodeData<'identifier'> | string;
  type: NodeData<'type'> | TypeFromInput | FromValue[];
  value: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type TypedDefaultParameterNode = NodeData<'typed_default_parameter'> & {
  typeField(value: NodeData<'type'>): TypedDefaultParameterNode;
  value(value: NodeData<'expression'>): TypedDefaultParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'typed_default_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypedDefaultParameterFromNode = NodeData<'typed_default_parameter'> & {
  typeField(value: NodeData<'type'> | TypeFromInput | FromValue[]): TypedDefaultParameterFromNode;
  value(value: NodeData<'expression'> | ExpressionFromInput | FromValue[]): TypedDefaultParameterFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'typed_default_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typedDefaultParameter(
  nameOrConfig: NodeData<'identifier'> | TypedDefaultParameterConfig,
  config?: Partial<TypedDefaultParameterConfig>,
): TypedDefaultParameterNode {
  const fields: any = isNodeData(nameOrConfig)
    ? { 'name': nameOrConfig, ...config }
    : nameOrConfig;
  const node: any = { type: 'typed_default_parameter', fields };
  node.typeField = (v: any) => { fields['type'] = v; return node; };
  node.value = (v: any) => { fields['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typedDefaultParameter.assign = function(target: AssignableNode<'typed_default_parameter'>): TypedDefaultParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'typed_default_parameter'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = v; return node; };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypedParameterConfig {
  type: NodeData<'type'>;
  children?: NodeData[];
}

export interface TypedParameterFromInput {
  type: NodeData<'type'> | TypeFromInput | FromValue[];
  children?: (NodeData<'identifier' | 'list_splat_pattern' | 'dictionary_splat_pattern'> | string | ({ kind: 'list_splat_pattern' } & ListSplatPatternFromInput) | ({ kind: 'dictionary_splat_pattern' } & DictionarySplatPatternFromInput))[];
}

export type TypedParameterNode = NodeData<'typed_parameter'> & {
  children(...value: NodeData[]): TypedParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'typed_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type TypedParameterFromNode = NodeData<'typed_parameter'> & {
  children(...value: FromValue[]): TypedParameterFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'typed_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typedParameter(
  typeOrConfig: NodeData<'type'> | TypedParameterConfig,
  config?: Partial<TypedParameterConfig>,
): TypedParameterNode {
  const fields: any = isNodeData(typeOrConfig)
    ? { 'type': typeOrConfig, ...config }
    : typeOrConfig;
  const node: any = { type: 'typed_parameter', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typedParameter.assign = function(target: AssignableNode<'typed_parameter'>): TypedParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'typed_parameter'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UnaryOperatorConfig {
  operator: NodeData<string>;
  argument: NodeData<'primary_expression'>;
}

export interface UnaryOperatorFromInput {
  operator: NodeData | '+' | '-' | '~';
  argument: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[];
}

export type UnaryOperatorNode = NodeData<'unary_operator'> & {
  argument(value: NodeData<'primary_expression'>): UnaryOperatorNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'unary_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type UnaryOperatorFromNode = NodeData<'unary_operator'> & {
  argument(value: NodeData<'primary_expression'> | PrimaryExpressionFromInput | FromValue[]): UnaryOperatorFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'unary_operator'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unaryOperator(
  operatorOrConfig: NodeData<string> | UnaryOperatorConfig,
  config?: Partial<UnaryOperatorConfig>,
): UnaryOperatorNode {
  const fields: any = isNodeData(operatorOrConfig)
    ? { 'operator': operatorOrConfig, ...config }
    : operatorOrConfig;
  const node: any = { type: 'unary_operator', fields };
  node.argument = (v: any) => { fields['argument'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

unaryOperator.assign = function(target: AssignableNode<'unary_operator'>): UnaryOperatorNode {
  const overrides: any = {};
  const fieldList: string[] = ['operator', 'argument'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'unary_operator'; }, get fields() { return getFields(); } };
  node.operator = (v: any) => { overrides['operator'] = v; return node; };
  node.argument = (v: any) => { overrides['argument'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UnionPatternConfig {
  children?: NodeData[];
}

export interface UnionPatternFromInput {
  children?: (NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | string | ({ kind: 'class_pattern' } & ClassPatternFromInput) | ({ kind: 'splat_pattern' } & SplatPatternFromInput) | ({ kind: 'union_pattern' } & UnionPatternFromInput) | ({ kind: 'list_pattern' } & ListPatternFromInput) | ({ kind: 'tuple_pattern' } & TuplePatternFromInput) | ({ kind: 'dict_pattern' } & DictPatternFromInput) | ({ kind: 'string' } & StringFromInput) | ({ kind: 'concatenated_string' } & ConcatenatedStringFromInput) | ({ kind: 'complex_pattern' } & ComplexPatternFromInput) | ({ kind: 'dotted_name' } & DottedNameFromInput))[];
}

export type UnionPatternNode = NodeData<'union_pattern'> & {
  children(...value: NodeData[]): UnionPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type UnionPatternFromNode = NodeData<'union_pattern'> & {
  children(...value: FromValue[]): UnionPatternFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unionPattern(
  childrenOrConfig?: NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'> | NodeData<'class_pattern' | 'splat_pattern' | 'union_pattern' | 'list_pattern' | 'tuple_pattern' | 'dict_pattern' | 'string' | 'concatenated_string' | 'true' | 'false' | 'none' | 'integer' | 'float' | 'complex_pattern' | 'dotted_name'>[] | UnionPatternConfig,
): UnionPatternNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'union_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

unionPattern.assign = function(target: AssignableNode<'union_pattern'>): UnionPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'union_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UnionTypeConfig {
  children?: NodeData[];
}

export interface UnionTypeFromInput {
  children?: (NodeData<'type'> | TypeFromInput)[];
}

export type UnionTypeNode = NodeData<'union_type'> & {
  children(...value: NodeData[]): UnionTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type UnionTypeFromNode = NodeData<'union_type'> & {
  children(...value: FromValue[]): UnionTypeFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unionType(
  childrenOrConfig?: NodeData<'type'> | NodeData<'type'>[] | UnionTypeConfig,
): UnionTypeNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'union_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

unionType.assign = function(target: AssignableNode<'union_type'>): UnionTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'union_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WhileStatementConfig {
  condition: NodeData<'expression'>;
  body: NodeData<'block'>;
  alternative?: NodeData<'else_clause'>;
}

export interface WhileStatementFromInput {
  condition: NodeData<'expression'> | ExpressionFromInput | FromValue[];
  body: NodeData<'block'> | BlockFromInput | FromValue[];
  alternative?: NodeData<'else_clause'> | ElseClauseFromInput | FromValue[];
}

export type WhileStatementNode = NodeData<'while_statement'> & {
  body(value: NodeData<'block'>): WhileStatementNode;
  alternative(value: NodeData<'else_clause'>): WhileStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'while_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type WhileStatementFromNode = NodeData<'while_statement'> & {
  body(value: NodeData<'block'> | BlockFromInput | FromValue[]): WhileStatementFromNode;
  alternative(value: NodeData<'else_clause'> | ElseClauseFromInput | FromValue[]): WhileStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'while_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function whileStatement(
  conditionOrConfig: NodeData<'expression'> | WhileStatementConfig,
  config?: Partial<WhileStatementConfig>,
): WhileStatementNode {
  const fields: any = isNodeData(conditionOrConfig)
    ? { 'condition': conditionOrConfig, ...config }
    : conditionOrConfig;
  const node: any = { type: 'while_statement', fields };
  node.body = (v: any) => { fields['body'] = v; return node; };
  node.alternative = (v: any) => { fields['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

whileStatement.assign = function(target: AssignableNode<'while_statement'>): WhileStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition', 'body', 'alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'while_statement'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = v; return node; };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.alternative = (v: any) => { overrides['alternative'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WithClauseConfig {
  children?: NodeData[];
}

export interface WithClauseFromInput {
  children?: (NodeData<'with_item'> | WithItemFromInput)[];
}

export type WithClauseNode = NodeData<'with_clause'> & {
  children(...value: NodeData[]): WithClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type WithClauseFromNode = NodeData<'with_clause'> & {
  children(...value: FromValue[]): WithClauseFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function withClause(
  childrenOrConfig?: NodeData<'with_item'> | NodeData<'with_item'>[] | WithClauseConfig,
): WithClauseNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'with_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

withClause.assign = function(target: AssignableNode<'with_clause'>): WithClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'with_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WithItemConfig {
  value: NodeData<'expression'>;
}

export interface WithItemFromInput {
  value: NodeData<'expression'> | ExpressionFromInput | FromValue[];
}

export type WithItemNode = NodeData<'with_item'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type WithItemFromNode = NodeData<'with_item'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function withItem(
  valueOrConfig: NodeData<'expression'> | WithItemConfig,
  config?: Partial<WithItemConfig>,
): WithItemNode {
  const fields: any = isNodeData(valueOrConfig)
    ? { 'value': valueOrConfig, ...config }
    : valueOrConfig;
  const node: any = { type: 'with_item', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

withItem.assign = function(target: AssignableNode<'with_item'>): WithItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'with_item'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WithStatementConfig {
  body: NodeData<'block'>;
  children?: NodeData[];
}

export interface WithStatementFromInput {
  body: NodeData<'block'> | BlockFromInput | FromValue[];
  children?: (NodeData<'with_clause'> | WithClauseFromInput)[];
}

export type WithStatementNode = NodeData<'with_statement'> & {
  children(...value: NodeData[]): WithStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type WithStatementFromNode = NodeData<'with_statement'> & {
  children(...value: FromValue[]): WithStatementFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'with_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function withStatement(
  bodyOrConfig: NodeData<'block'> | WithStatementConfig,
  config?: Partial<WithStatementConfig>,
): WithStatementNode {
  const fields: any = isNodeData(bodyOrConfig)
    ? { 'body': bodyOrConfig, ...config }
    : bodyOrConfig;
  const node: any = { type: 'with_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

withStatement.assign = function(target: AssignableNode<'with_statement'>): WithStatementNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'with_statement'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface YieldConfig {
  children?: NodeData[];
}

export interface YieldFromInput {
  children?: (NodeData<'expression' | 'expression_list'> | ({ kind: 'expression' } & ExpressionFromInput) | ({ kind: 'expression_list' } & ExpressionListFromInput))[];
}

export type YieldNode = NodeData<'yield'> & {
  children(...value: NodeData[]): YieldNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'yield'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export type YieldFromNode = NodeData<'yield'> & {
  children(...value: FromValue[]): YieldFromNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'yield'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function yield_(
  childrenOrConfig?: NodeData<'expression' | 'expression_list'> | NodeData<'expression' | 'expression_list'>[] | YieldConfig,
): YieldNode {
  let fields: any;
  if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'yield', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

yield_.assign = function(target: AssignableNode<'yield'>): YieldNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'yield'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export function breakStatement(): NodeData<'break_statement'> & { render(): string; replace(target: { type: 'break_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'break_statement', fields: {}, text: 'break' };
  node.render = () => 'break';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'break' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'break' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' }; };
  return node;
}

export function continueStatement(): NodeData<'continue_statement'> & { render(): string; replace(target: { type: 'continue_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'continue_statement', fields: {}, text: 'continue' };
  node.render = () => 'continue';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'continue' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'continue' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' }; };
  return node;
}

export function importPrefix(text: string): NodeData<'import_prefix'> & { render(): string; replace(target: { type: 'import_prefix'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^(?:\.)+$/.test(text)) throw new Error(`Invalid import_prefix: '${text}' does not match grammar pattern`);
  const node: any = { type: 'import_prefix', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function keywordSeparator(): NodeData<'keyword_separator'> & { render(): string; replace(target: { type: 'keyword_separator'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'keyword_separator', fields: {}, text: '*' };
  node.render = () => '*';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '*' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; };
  return node;
}

export function passStatement(): NodeData<'pass_statement'> & { render(): string; replace(target: { type: 'pass_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'pass_statement', fields: {}, text: 'pass' };
  node.render = () => 'pass';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'pass' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'pass' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' }; };
  return node;
}

export function positionalSeparator(): NodeData<'positional_separator'> & { render(): string; replace(target: { type: 'positional_separator'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'positional_separator', fields: {}, text: '/' };
  node.render = () => '/';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '/' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '/' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '/' }; };
  return node;
}

export function wildcardImport(): NodeData<'wildcard_import'> & { render(): string; replace(target: { type: 'wildcard_import'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'wildcard_import', fields: {}, text: '*' };
  node.render = () => '*';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '*' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '*' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '*' }; };
  return node;
}

export function comment(text: string): NodeData<'comment'> & { render(): string; replace(target: { type: 'comment'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^#.*$/.test(text)) throw new Error(`Invalid comment: '${text}' does not match grammar pattern`);
  const node: any = { type: 'comment', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function ellipsis(): NodeData<'ellipsis'> & { render(): string; replace(target: { type: 'ellipsis'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'ellipsis', fields: {}, text: '...' };
  node.render = () => '...';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '...' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '...' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '...' }; };
  return node;
}

export function escapeInterpolation(text: string): NodeData<'escape_interpolation'> & { render(): string; replace(target: { type: 'escape_interpolation'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'escape_interpolation', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function escapeSequence(text: `\\${string}`): NodeData<'escape_sequence'> & { render(): string; replace(target: { type: 'escape_sequence'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^\\(?:u[a-fA-F\d]{4}|U[a-fA-F\d]{8}|x[a-fA-F\d]{2}|\d{1,3}|\r?\n|['"abfrntv\\]|N\{[^}]+\})$/.test(text)) throw new Error(`Invalid escape_sequence: '${text}' does not match grammar pattern`);
  const node: any = { type: 'escape_sequence', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function false_(): NodeData<'false'> & { render(): string; replace(target: { type: 'false'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'false', fields: {}, text: 'False' };
  node.render = () => 'False';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'False' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'False' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' }; };
  return node;
}

export function float(text: string): NodeData<'float'> & { render(): string; replace(target: { type: 'float'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^(?:(?:[0-9]+_?)+\.(?:[0-9]+_?)+?[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+?\.(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+?|(?:[0-9]+_?)+[eE][\+-]?(?:[0-9]+_?)+)[jJ]?$/.test(text)) throw new Error(`Invalid float: '${text}' does not match grammar pattern`);
  const node: any = { type: 'float', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function identifier(text: string): NodeData<'identifier'> & { render(): string; replace(target: { type: 'identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  if (!/^[_\p{XID_Start}][_\p{XID_Continue}]*$/u.test(text)) throw new Error(`Invalid identifier: '${text}' does not match grammar pattern`);
  const node: any = { type: 'identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function integer(text: string): NodeData<'integer'> & { render(): string; replace(target: { type: 'integer'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^(?:(?:0x|0X)(?:_?[A-Fa-f0-9]+)+[Ll]?|(?:0o|0O)(?:_?[0-7]+)+[Ll]?|(?:0b|0B)(?:_?[0-1]+)+[Ll]?|(?:[0-9]+_?)+(?:[Ll]?|[jJ]?))$/.test(text)) throw new Error(`Invalid integer: '${text}' does not match grammar pattern`);
  const node: any = { type: 'integer', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function lineContinuation(text: string): NodeData<'line_continuation'> & { render(): string; replace(target: { type: 'line_continuation'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^\\(?:?
| )$/.test(text)) throw new Error(`Invalid line_continuation: '${text}' does not match grammar pattern`);
  const node: any = { type: 'line_continuation', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function none(): NodeData<'none'> & { render(): string; replace(target: { type: 'none'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'none', fields: {}, text: 'None' };
  node.render = () => 'None';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'None' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'None' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' }; };
  return node;
}

export function stringEnd(text: string): NodeData<'string_end'> & { render(): string; replace(target: { type: 'string_end'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'string_end', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function stringStart(text: string): NodeData<'string_start'> & { render(): string; replace(target: { type: 'string_start'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'string_start', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function true_(): NodeData<'true'> & { render(): string; replace(target: { type: 'true'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'true', fields: {}, text: 'True' };
  node.render = () => 'True';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'True' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'True' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' }; };
  return node;
}

export function typeConversion(text: string): NodeData<'type_conversion'> & { render(): string; replace(target: { type: 'type_conversion'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^![a-z]$/.test(text)) throw new Error(`Invalid type_conversion: '${text}' does not match grammar pattern`);
  const node: any = { type: 'type_conversion', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

/**
 * Create an in-place editor for a parsed tree node.
 * Hydrates a factory from the target node's fields,
 * then allows fluent modification. .toEdit() uses the target's range.
 *
 * Usage: edit(matchedNode).returnType(ir.primitiveType("i64")).toEdit()
 */
export function edit<K extends string>(target: AssignableNode<K>): any {
  const factories: Record<string, { assign: (t: any) => any }> = {
    'aliased_import': aliasedImport,
    'argument_list': argumentList,
    'as_pattern': asPattern,
    'assert_statement': assertStatement,
    'assignment': assignment,
    'attribute': attribute,
    'augmented_assignment': augmentedAssignment,
    'await': await_,
    'binary_operator': binaryOperator,
    'block': block,
    'boolean_operator': booleanOperator,
    'call': call,
    'case_clause': caseClause,
    'case_pattern': casePattern,
    'chevron': chevron,
    'class_definition': classDefinition,
    'class_pattern': classPattern,
    'comparison_operator': comparisonOperator,
    'complex_pattern': complexPattern,
    'concatenated_string': concatenatedString,
    'conditional_expression': conditionalExpression,
    'constrained_type': constrainedType,
    'decorated_definition': decoratedDefinition,
    'decorator': decorator,
    'default_parameter': defaultParameter,
    'delete_statement': deleteStatement,
    'dict_pattern': dictPattern,
    'dictionary': dictionary,
    'dictionary_comprehension': dictionaryComprehension,
    'dictionary_splat': dictionarySplat,
    'dictionary_splat_pattern': dictionarySplatPattern,
    'dotted_name': dottedName,
    'elif_clause': elifClause,
    'else_clause': elseClause,
    'except_clause': exceptClause,
    'exec_statement': execStatement,
    'expression_list': expressionList,
    'expression_statement': expressionStatement,
    'finally_clause': finallyClause,
    'for_in_clause': forInClause,
    'for_statement': forStatement,
    'format_expression': formatExpression,
    'format_specifier': formatSpecifier,
    'function_definition': functionDefinition,
    'future_import_statement': futureImportStatement,
    'generator_expression': generatorExpression,
    'generic_type': genericType,
    'global_statement': globalStatement,
    'if_clause': ifClause,
    'if_statement': ifStatement,
    'import_from_statement': importFromStatement,
    'import_statement': importStatement,
    'interpolation': interpolation,
    'keyword_argument': keywordArgument,
    'keyword_pattern': keywordPattern,
    'lambda': lambda,
    'lambda_parameters': lambdaParameters,
    'list': list,
    'list_comprehension': listComprehension,
    'list_pattern': listPattern,
    'list_splat': listSplat,
    'list_splat_pattern': listSplatPattern,
    'match_statement': matchStatement,
    'member_type': memberType,
    'module': module,
    'named_expression': namedExpression,
    'nonlocal_statement': nonlocalStatement,
    'not_operator': notOperator,
    'pair': pair,
    'parameters': parameters,
    'parenthesized_expression': parenthesizedExpression,
    'parenthesized_list_splat': parenthesizedListSplat,
    'pattern_list': patternList,
    'print_statement': printStatement,
    'raise_statement': raiseStatement,
    'relative_import': relativeImport,
    'return_statement': returnStatement,
    'set': set,
    'set_comprehension': setComprehension,
    'slice': slice,
    'splat_pattern': splatPattern,
    'splat_type': splatType,
    'string': string,
    'string_content': stringContent,
    'subscript': subscript,
    'try_statement': tryStatement,
    'tuple': tuple,
    'tuple_pattern': tuplePattern,
    'type': type_,
    'type_alias_statement': typeAliasStatement,
    'type_parameter': typeParameter,
    'typed_default_parameter': typedDefaultParameter,
    'typed_parameter': typedParameter,
    'unary_operator': unaryOperator,
    'union_pattern': unionPattern,
    'union_type': unionType,
    'while_statement': whileStatement,
    'with_clause': withClause,
    'with_item': withItem,
    'with_statement': withStatement,
    'yield': yield_,
  };
  const factory = factories[target.type];
  if (!factory) throw new Error(`No factory for kind '${target.type}'`);
  return factory.assign(target);
}

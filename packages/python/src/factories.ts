// Auto-generated by @sittir/codegen — do not edit

import type { AliasedImport, AliasedImportTree, ArgumentList, ArgumentListTree, AsPattern, AsPatternTarget, AsPatternTargetAsPattern, AsPatternTargetAsPatternConfig, AsPatternTargetAsPatternTree, AsPatternTargetBooleanOperator, AsPatternTargetBooleanOperatorConfig, AsPatternTargetBooleanOperatorTree, AsPatternTargetComparisonOperator, AsPatternTargetComparisonOperatorConfig, AsPatternTargetComparisonOperatorTree, AsPatternTargetConditionalExpression, AsPatternTargetConditionalExpressionConfig, AsPatternTargetConditionalExpressionTree, AsPatternTargetLambda, AsPatternTargetLambdaConfig, AsPatternTargetLambdaTree, AsPatternTargetNamedExpression, AsPatternTargetNamedExpressionConfig, AsPatternTargetNamedExpressionTree, AsPatternTargetNotOperator, AsPatternTargetNotOperatorConfig, AsPatternTargetNotOperatorTree, AsPatternTargetPrimaryExpression, AsPatternTargetPrimaryExpressionConfig, AsPatternTargetPrimaryExpressionTree, AsPatternTargetTree, AsPatternTree, AssertStatement, AssertStatementTree, Assignment, AssignmentTree, Attribute, AttributeTree, AugmentedAssignment, AugmentedAssignmentTree, Await, AwaitTree, BinaryOperator, BinaryOperatorTree, Block, BlockTree, BooleanOperator, BooleanOperatorTree, BreakStatement, BreakStatementTree, Call, CallTree, CaseClause, CaseClauseTree, CasePattern, CasePatternKeywordPattern, CasePatternKeywordPatternConfig, CasePatternKeywordPatternTree, CasePatternTree, CasePatternUAsPattern, CasePatternUAsPatternConfig, CasePatternUAsPatternTree, CasePatternUSimplePattern, CasePatternUSimplePatternConfig, CasePatternUSimplePatternTree, Chevron, ChevronTree, ClassDefinition, ClassDefinitionTree, ClassPattern, ClassPatternTree, Comment, CommentTree, ComparisonOperator, ComparisonOperatorTree, ComplexPattern, ComplexPatternTree, ConcatenatedString, ConcatenatedStringTree, ConditionalExpression, ConditionalExpressionTree, ConstrainedType, ConstrainedTypeTree, ContinueStatement, ContinueStatementTree, DecoratedDefinition, DecoratedDefinitionTree, Decorator, DecoratorTree, DefaultParameter, DefaultParameterTree, DeleteStatement, DeleteStatementTree, DictPattern, DictPatternTree, Dictionary, DictionaryComprehension, DictionaryComprehensionTree, DictionarySplat, DictionarySplatPattern, DictionarySplatPatternTree, DictionarySplatTree, DictionaryTree, DottedName, DottedNameTree, ElifClause, ElifClauseTree, ElseClause, ElseClauseTree, EscapeSequence, EscapeSequenceTree, ExceptClause, ExceptClauseTree, ExecStatement, ExecStatementTree, Expression, ExpressionAsPattern, ExpressionAsPatternConfig, ExpressionAsPatternTree, ExpressionBooleanOperator, ExpressionBooleanOperatorConfig, ExpressionBooleanOperatorTree, ExpressionComparisonOperator, ExpressionComparisonOperatorConfig, ExpressionComparisonOperatorTree, ExpressionConditionalExpression, ExpressionConditionalExpressionConfig, ExpressionConditionalExpressionTree, ExpressionLambda, ExpressionLambdaConfig, ExpressionLambdaTree, ExpressionList, ExpressionListTree, ExpressionNamedExpression, ExpressionNamedExpressionConfig, ExpressionNamedExpressionTree, ExpressionNotOperator, ExpressionNotOperatorConfig, ExpressionNotOperatorTree, ExpressionPrimaryExpression, ExpressionPrimaryExpressionConfig, ExpressionPrimaryExpressionTree, ExpressionStatement, ExpressionStatementAssignment, ExpressionStatementAssignmentConfig, ExpressionStatementAssignmentTree, ExpressionStatementAugmentedAssignment, ExpressionStatementAugmentedAssignmentConfig, ExpressionStatementAugmentedAssignmentTree, ExpressionStatementExpression, ExpressionStatementExpression2, ExpressionStatementExpression2Config, ExpressionStatementExpression2Tree, ExpressionStatementExpressionConfig, ExpressionStatementExpressionTree, ExpressionStatementTree, ExpressionStatementYield, ExpressionStatementYieldConfig, ExpressionStatementYieldTree, ExpressionTree, False, FalseTree, FinallyClause, FinallyClauseTree, Float, FloatTree, ForInClause, ForInClauseTree, ForStatement, ForStatementTree, FormatExpression, FormatExpressionTree, FormatSpecifier, FormatSpecifierTree, FunctionDefinition, FunctionDefinitionTree, FutureImportStatement, FutureImportStatementTree, GeneratorExpression, GeneratorExpressionTree, GenericType, GenericTypeTree, GlobalStatement, GlobalStatementTree, HiddenAsPattern, HiddenAsPatternTree, HiddenCollectionElements, HiddenCollectionElementsTree, HiddenComprehensionClauses, HiddenComprehensionClausesTree, HiddenImportList, HiddenImportListTree, HiddenIsNot, HiddenIsNotTree, HiddenListPattern, HiddenListPatternTree, HiddenNotIn, HiddenNotInTree, HiddenParameters, HiddenParametersTree, HiddenPatterns, HiddenPatternsTree, HiddenSimpleStatements, HiddenSimpleStatementsTree, HiddenTuplePattern, HiddenTuplePatternTree, Identifier, IdentifierTree, IfClause, IfClauseTree, IfStatement, IfStatementTree, ImportFromStatement, ImportFromStatementTree, ImportPrefix, ImportPrefixTree, ImportStatement, ImportStatementTree, Integer, IntegerTree, Interpolation, InterpolationTree, KeywordArgument, KeywordArgumentTree, KeywordIdentifier, KeywordIdentifierTree, KeywordPattern, KeywordPatternTree, Lambda, LambdaParameters, LambdaParametersTree, LambdaTree, LambdaWithinForInClause, LambdaWithinForInClauseTree, LineContinuation, LineContinuationTree, List, ListComprehension, ListComprehensionTree, ListPattern, ListPatternTree, ListSplat, ListSplatPattern, ListSplatPatternTree, ListSplatTree, ListTree, MatchStatement, MatchStatementTree, MemberType, MemberTypeTree, Module, ModuleTree, NamedExpression, NamedExpressionTree, None, NoneTree, NonlocalStatement, NonlocalStatementTree, NotOperator, NotOperatorTree, Pair, PairTree, Parameter, ParameterDefaultParameter, ParameterDefaultParameterConfig, ParameterDefaultParameterTree, ParameterDictionarySplatPattern, ParameterDictionarySplatPatternConfig, ParameterDictionarySplatPatternTree, ParameterIdentifier, ParameterIdentifierConfig, ParameterIdentifierTree, ParameterKeywordSeparator, ParameterKeywordSeparatorConfig, ParameterKeywordSeparatorTree, ParameterListSplatPattern, ParameterListSplatPatternConfig, ParameterListSplatPatternTree, ParameterPositionalSeparator, ParameterPositionalSeparatorConfig, ParameterPositionalSeparatorTree, ParameterTree, ParameterTuplePattern, ParameterTuplePatternConfig, ParameterTuplePatternTree, ParameterTypedDefaultParameter, ParameterTypedDefaultParameterConfig, ParameterTypedDefaultParameterTree, ParameterTypedParameter, ParameterTypedParameterConfig, ParameterTypedParameterTree, Parameters, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionTree, ParenthesizedListSplat, ParenthesizedListSplatTree, PassStatement, PassStatementTree, Pattern, PatternAttribute, PatternAttributeConfig, PatternAttributeTree, PatternIdentifier, PatternIdentifierConfig, PatternIdentifierTree, PatternKeywordIdentifier, PatternKeywordIdentifierConfig, PatternKeywordIdentifierTree, PatternList, PatternListPattern, PatternListPatternConfig, PatternListPatternTree, PatternListSplatPattern, PatternListSplatPatternConfig, PatternListSplatPatternTree, PatternListTree, PatternSubscript, PatternSubscriptConfig, PatternSubscriptTree, PatternTree, PatternTuplePattern, PatternTuplePatternConfig, PatternTuplePatternTree, PrimaryExpression, PrimaryExpressionAttribute, PrimaryExpressionAttributeConfig, PrimaryExpressionAttributeTree, PrimaryExpressionAwait, PrimaryExpressionAwaitConfig, PrimaryExpressionAwaitTree, PrimaryExpressionBinaryOperator, PrimaryExpressionBinaryOperatorConfig, PrimaryExpressionBinaryOperatorTree, PrimaryExpressionCall, PrimaryExpressionCallConfig, PrimaryExpressionCallTree, PrimaryExpressionConcatenatedString, PrimaryExpressionConcatenatedStringConfig, PrimaryExpressionConcatenatedStringTree, PrimaryExpressionDictionary, PrimaryExpressionDictionaryComprehension, PrimaryExpressionDictionaryComprehensionConfig, PrimaryExpressionDictionaryComprehensionTree, PrimaryExpressionDictionaryConfig, PrimaryExpressionDictionaryTree, PrimaryExpressionEllipsis, PrimaryExpressionEllipsisConfig, PrimaryExpressionEllipsisTree, PrimaryExpressionFalse, PrimaryExpressionFalseConfig, PrimaryExpressionFalseTree, PrimaryExpressionFloat, PrimaryExpressionFloatConfig, PrimaryExpressionFloatTree, PrimaryExpressionGeneratorExpression, PrimaryExpressionGeneratorExpressionConfig, PrimaryExpressionGeneratorExpressionTree, PrimaryExpressionIdentifier, PrimaryExpressionIdentifierConfig, PrimaryExpressionIdentifierTree, PrimaryExpressionInteger, PrimaryExpressionIntegerConfig, PrimaryExpressionIntegerTree, PrimaryExpressionKeywordIdentifier, PrimaryExpressionKeywordIdentifierConfig, PrimaryExpressionKeywordIdentifierTree, PrimaryExpressionList, PrimaryExpressionListComprehension, PrimaryExpressionListComprehensionConfig, PrimaryExpressionListComprehensionTree, PrimaryExpressionListConfig, PrimaryExpressionListSplatPattern, PrimaryExpressionListSplatPatternConfig, PrimaryExpressionListSplatPatternTree, PrimaryExpressionListTree, PrimaryExpressionNone, PrimaryExpressionNoneConfig, PrimaryExpressionNoneTree, PrimaryExpressionParenthesizedExpression, PrimaryExpressionParenthesizedExpressionConfig, PrimaryExpressionParenthesizedExpressionTree, PrimaryExpressionSet, PrimaryExpressionSetComprehension, PrimaryExpressionSetComprehensionConfig, PrimaryExpressionSetComprehensionTree, PrimaryExpressionSetConfig, PrimaryExpressionSetTree, PrimaryExpressionString, PrimaryExpressionStringConfig, PrimaryExpressionStringTree, PrimaryExpressionSubscript, PrimaryExpressionSubscriptConfig, PrimaryExpressionSubscriptTree, PrimaryExpressionTree, PrimaryExpressionTrue, PrimaryExpressionTrueConfig, PrimaryExpressionTrueTree, PrimaryExpressionTuple, PrimaryExpressionTupleConfig, PrimaryExpressionTupleTree, PrimaryExpressionUnaryOperator, PrimaryExpressionUnaryOperatorConfig, PrimaryExpressionUnaryOperatorTree, PrintStatement, PrintStatementTree, RaiseStatement, RaiseStatementTree, RelativeImport, RelativeImportTree, ReturnStatement, ReturnStatementTree, Set, SetComprehension, SetComprehensionTree, SetTree, Slice, SliceTree, SplatPattern, SplatPatternTree, SplatType, SplatTypeTree, String, StringContent, StringContentTree, StringTree, Subscript, SubscriptTree, True, TrueTree, TryStatement, TryStatementTree, Tuple, TuplePattern, TuplePatternTree, TupleTree, Type, TypeAliasStatement, TypeAliasStatementTree, TypeConstrainedType, TypeConstrainedTypeConfig, TypeConstrainedTypeTree, TypeConversion, TypeConversionTree, TypeExpression, TypeExpressionConfig, TypeExpressionTree, TypeGenericType, TypeGenericTypeConfig, TypeGenericTypeTree, TypeMemberType, TypeMemberTypeConfig, TypeMemberTypeTree, TypeParameter, TypeParameterTree, TypeSplatType, TypeSplatTypeConfig, TypeSplatTypeTree, TypeTree, TypeUnionType, TypeUnionTypeConfig, TypeUnionTypeTree, TypedDefaultParameter, TypedDefaultParameterTree, TypedParameter, TypedParameterTree, UnaryOperator, UnaryOperatorTree, UnionPattern, UnionPatternTree, UnionType, UnionTypeTree, WhileStatement, WhileStatementTree, WithClause, WithClauseParen, WithClauseParenConfig, WithClauseParenTree, WithClauseTree, WithClauseWithItem, WithClauseWithItemConfig, WithClauseWithItemTree, WithItem, WithItemTree, WithStatement, WithStatementTree, Yield, YieldTree } from './types.js';
import type { Edit, AnyNodeData, ConfigOf } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  '0B',
  '0O',
  '0X',
  '0b',
  '0o',
  '0x',
  'False',
  'None',
  'True',
  '_',
  '__future__',
  'and',
  'as',
  'assert',
  'async',
  'break',
  'break_statement',
  'case',
  'class',
  'continue',
  'continue_statement',
  'def',
  'del',
  'elif',
  'else',
  'except',
  'exec',
  'false',
  'finally',
  'for',
  'from',
  'global',
  'if',
  'import',
  'in',
  'is',
  'match',
  'none',
  'nonlocal',
  'not',
  'or',
  'pass',
  'pass_statement',
  'print',
  'raise',
  'return',
  'true',
  'try',
  'while',
  'with',
]);

export function module_(...children: any[]) {
  return {
    type: 'module' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenSimpleStatements(...children: any[]) {
  return {
    type: '_simple_statements' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenSimpleStatementsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'import_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importPrefix(text: string) {
  return {
    type: 'import_prefix' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ImportPrefixTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function relativeImport(config: ConfigOf<RelativeImport>) {
  const fields = {
    import_prefix: (config as any)?.importPrefix,
    dotted_name: (config as any)?.dottedName,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'relative_import' as const,
    named: true as const,
    fields,
    children,
    importPrefix(importPrefix_?: any) { return importPrefix_ !== undefined ? relativeImport({ ...(config as any), importPrefix: importPrefix_ } as any) : fields.import_prefix; },
    dottedName(dottedName_?: any) { return dottedName_ !== undefined ? relativeImport({ ...(config as any), dottedName: dottedName_ } as any) : fields.dotted_name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return relativeImport({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RelativeImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function futureImportStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'future_import_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FutureImportStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function importFromStatement(config: ConfigOf<ImportFromStatement>) {
  const fields = {
    wildcard_import: (config as any)?.wildcardImport,
    module_name: (config as any)?.moduleName,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'import_from_statement' as const,
    named: true as const,
    fields,
    children,
    wildcardImport(wildcardImport_?: any) { return wildcardImport_ !== undefined ? importFromStatement({ ...(config as any), wildcardImport: wildcardImport_ } as any) : fields.wildcard_import; },
    moduleName(moduleName_?: any) { return moduleName_ !== undefined ? importFromStatement({ ...(config as any), moduleName: moduleName_ } as any) : fields.module_name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return importFromStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImportFromStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenImportList(config: ConfigOf<HiddenImportList>) {
  const fields = {
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: '_import_list' as const,
    named: true as const,
    fields,
    children,
    name(...name_: any[]) { return name_.length ? hiddenImportList({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return hiddenImportList({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenImportListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function aliasedImport(config: ConfigOf<AliasedImport>) {
  const fields = {
    name: (config as any)?.name,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'aliased_import' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? aliasedImport({ ...(config as any), name: name_ } as any) : fields.name; },
    alias(alias_?: any) { return alias_ !== undefined ? aliasedImport({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return aliasedImport({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AliasedImportTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function printStatement(config: ConfigOf<PrintStatement>) {
  const fields = {
    chevron: (config as any)?.chevron,
    argument: (config as any)?.argument,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'print_statement' as const,
    named: true as const,
    fields,
    children,
    chevron(chevron_?: any) { return chevron_ !== undefined ? printStatement({ ...(config as any), chevron: chevron_ } as any) : fields.chevron; },
    argument(...argument_: any[]) { return argument_.length ? printStatement({ ...(config as any), argument: argument_ } as any) : fields.argument; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return printStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrintStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function chevron(config: ConfigOf<Chevron>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'chevron' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? chevron({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return chevron({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ChevronTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assertStatement(...children: any[]) {
  return {
    type: 'assert_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssertStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}






export function expressionStatement(config?: ConfigOf<ExpressionStatement>) {
  return expressionStatementYield(config as any);
}
export function expressionStatementExpression(config?: ConfigOf<ExpressionStatementExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionStatementExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionStatementExpression2(config?: ConfigOf<ExpressionStatementExpression2>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionStatementExpression2({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionStatementAssignment(config?: ConfigOf<ExpressionStatementAssignment>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionStatementAssignment({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionStatementAugmentedAssignment(config?: ConfigOf<ExpressionStatementAugmentedAssignment>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionStatementAugmentedAssignment({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionStatementYield(config?: ConfigOf<ExpressionStatementYield>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionStatementYield({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function namedExpression(config: ConfigOf<NamedExpression>) {
  const fields = {
    name: (config as any)?.name,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'named_expression' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? namedExpression({ ...(config as any), name: name_ } as any) : fields.name; },
    value(value_?: any) { return value_ !== undefined ? namedExpression({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return namedExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NamedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'return_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function deleteStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'delete_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeleteStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function raiseStatement(config?: ConfigOf<RaiseStatement>) {
  const fields = {
    cause: (config as any)?.cause,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'raise_statement' as const,
    named: true as const,
    fields,
    children,
    cause(cause_?: any) { return cause_ !== undefined ? raiseStatement({ ...(config as any), cause: cause_ } as any) : fields.cause; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return raiseStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RaiseStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function passStatement() {
  return {
    type: 'pass_statement' as const,
    named: true as const,
    text: 'pass',
    render: () => 'pass',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'pass' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'pass' },
    replace: (t: PassStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'pass' }; },
  };
}

export function breakStatement() {
  return {
    type: 'break_statement' as const,
    named: true as const,
    text: 'break',
    render: () => 'break',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'break' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'break' },
    replace: (t: BreakStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'break' }; },
  };
}

export function continueStatement() {
  return {
    type: 'continue_statement' as const,
    named: true as const,
    text: 'continue',
    render: () => 'continue',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'continue' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'continue' },
    replace: (t: ContinueStatementTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'continue' }; },
  };
}

export function ifStatement(config: ConfigOf<IfStatement>) {
  const fields = {
    condition: (config as any)?.condition,
    consequence: (config as any)?.consequence,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'if_statement' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: any) { return condition_ !== undefined ? ifStatement({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    consequence(consequence_?: any) { return consequence_ !== undefined ? ifStatement({ ...(config as any), consequence: consequence_ } as any) : fields.consequence; },
    alternative(...alternative_: any[]) { return alternative_.length ? ifStatement({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return ifStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elifClause(config: ConfigOf<ElifClause>) {
  const fields = {
    condition: (config as any)?.condition,
    consequence: (config as any)?.consequence,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'elif_clause' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: any) { return condition_ !== undefined ? elifClause({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    consequence(consequence_?: any) { return consequence_ !== undefined ? elifClause({ ...(config as any), consequence: consequence_ } as any) : fields.consequence; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return elifClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElifClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: ConfigOf<ElseClause>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? elseClause({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return elseClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchStatement(config: ConfigOf<MatchStatement>) {
  const fields = {
    subject: (config as any)?.subject,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'match_statement' as const,
    named: true as const,
    fields,
    children,
    subject(...subject_: any[]) { return subject_.length ? matchStatement({ ...(config as any), subject: subject_ } as any) : fields.subject; },
    body(body_?: any) { return body_ !== undefined ? matchStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return matchStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function caseClause(config: ConfigOf<CaseClause>) {
  const fields = {
    guard: (config as any)?.guard,
    consequence: (config as any)?.consequence,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'case_clause' as const,
    named: true as const,
    fields,
    children,
    guard(guard_?: any) { return guard_ !== undefined ? caseClause({ ...(config as any), guard: guard_ } as any) : fields.guard; },
    consequence(consequence_?: any) { return consequence_ !== undefined ? caseClause({ ...(config as any), consequence: consequence_ } as any) : fields.consequence; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return caseClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CaseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forStatement(config: ConfigOf<ForStatement>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
    body: (config as any)?.body,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'for_statement' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? forStatement({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? forStatement({ ...(config as any), right: right_ } as any) : fields.right; },
    body(body_?: any) { return body_ !== undefined ? forStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    alternative(alternative_?: any) { return alternative_ !== undefined ? forStatement({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return forStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileStatement(config: ConfigOf<WhileStatement>) {
  const fields = {
    condition: (config as any)?.condition,
    body: (config as any)?.body,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'while_statement' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: any) { return condition_ !== undefined ? whileStatement({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    body(body_?: any) { return body_ !== undefined ? whileStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    alternative(alternative_?: any) { return alternative_ !== undefined ? whileStatement({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return whileStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryStatement(config: ConfigOf<TryStatement>) {
  const fields = {
    except_clauses: (config as any)?.exceptClauses,
    else_clause: (config as any)?.elseClause,
    finally_clause: (config as any)?.finallyClause,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'try_statement' as const,
    named: true as const,
    fields,
    children,
    exceptClauses(exceptClauses_?: any) { return exceptClauses_ !== undefined ? tryStatement({ ...(config as any), exceptClauses: exceptClauses_ } as any) : fields.except_clauses; },
    elseClause(elseClause_?: any) { return elseClause_ !== undefined ? tryStatement({ ...(config as any), elseClause: elseClause_ } as any) : fields.else_clause; },
    finallyClause(finallyClause_?: any) { return finallyClause_ !== undefined ? tryStatement({ ...(config as any), finallyClause: finallyClause_ } as any) : fields.finally_clause; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return tryStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function exceptClause(config?: ConfigOf<ExceptClause>) {
  const fields = {
    value: (config as any)?.value,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'except_clause' as const,
    named: true as const,
    fields,
    children,
    value(...value_: any[]) { return value_.length ? exceptClause({ ...(config as any), value: value_ } as any) : fields.value; },
    alias(alias_?: any) { return alias_ !== undefined ? exceptClause({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return exceptClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExceptClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function finallyClause(config: ConfigOf<FinallyClause>) {
  const fields = {
    block: (config as any)?.block,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'finally_clause' as const,
    named: true as const,
    fields,
    children,
    block(block_?: any) { return block_ !== undefined ? finallyClause({ ...(config as any), block: block_ } as any) : fields.block; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return finallyClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FinallyClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withStatement(config: ConfigOf<WithStatement>) {
  const fields = {
    with_clause: (config as any)?.withClause,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'with_statement' as const,
    named: true as const,
    fields,
    children,
    withClause(withClause_?: any) { return withClause_ !== undefined ? withStatement({ ...(config as any), withClause: withClause_ } as any) : fields.with_clause; },
    body(body_?: any) { return body_ !== undefined ? withStatement({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return withStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function withClause(config?: ConfigOf<WithClause>) {
  return withClauseParen(config as any);
}
export function withClauseWithItem(config?: ConfigOf<WithClauseWithItem>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'with_clause' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return withClauseWithItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function withClauseParen(config?: ConfigOf<WithClauseParen>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'with_clause' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return withClauseParen({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function withItem(config: ConfigOf<WithItem>) {
  const fields = {
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'with_item' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? withItem({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return withItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WithItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionDefinition(config: ConfigOf<FunctionDefinition>) {
  const fields = {
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    parameters: (config as any)?.parameters,
    return_type: (config as any)?.returnType,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_definition' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? functionDefinition({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? functionDefinition({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    parameters(parameters_?: any) { return parameters_ !== undefined ? functionDefinition({ ...(config as any), parameters: parameters_ } as any) : fields.parameters; },
    returnType(returnType_?: any) { return returnType_ !== undefined ? functionDefinition({ ...(config as any), returnType: returnType_ } as any) : fields.return_type; },
    body(body_?: any) { return body_ !== undefined ? functionDefinition({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaParameters(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'lambda_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplat(config: ConfigOf<ListSplat>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'list_splat' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? listSplat({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return listSplat({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplat(config: ConfigOf<DictionarySplat>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'dictionary_splat' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? dictionarySplat({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return dictionarySplat({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function globalStatement(...children: any[]) {
  return {
    type: 'global_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GlobalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonlocalStatement(...children: any[]) {
  return {
    type: 'nonlocal_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonlocalStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function execStatement(config: ConfigOf<ExecStatement>) {
  const fields = {
    code: (config as any)?.code,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'exec_statement' as const,
    named: true as const,
    fields,
    children,
    code(code_?: any) { return code_ !== undefined ? execStatement({ ...(config as any), code: code_ } as any) : fields.code; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return execStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExecStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeAliasStatement(config: ConfigOf<TypeAliasStatement>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_alias_statement' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? typeAliasStatement({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? typeAliasStatement({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeAliasStatement({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeAliasStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classDefinition(config: ConfigOf<ClassDefinition>) {
  const fields = {
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    superclasses: (config as any)?.superclasses,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_definition' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? classDefinition({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? classDefinition({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    superclasses(superclasses_?: any) { return superclasses_ !== undefined ? classDefinition({ ...(config as any), superclasses: superclasses_ } as any) : fields.superclasses; },
    body(body_?: any) { return body_ !== undefined ? classDefinition({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return classDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(...children: any[]) {
  return {
    type: 'type_parameter' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedListSplat(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'parenthesized_list_splat' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedListSplatTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function argumentList(...children: any[]) {
  return {
    type: 'argument_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decoratedDefinition(config: ConfigOf<DecoratedDefinition>) {
  const fields = {
    definition: (config as any)?.definition,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'decorated_definition' as const,
    named: true as const,
    fields,
    children,
    definition(definition_?: any) { return definition_ !== undefined ? decoratedDefinition({ ...(config as any), definition: definition_ } as any) : fields.definition; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return decoratedDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratedDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function decorator(config: ConfigOf<Decorator>) {
  const fields = {
    expression: (config as any)?.expression,
    newline: (config as any)?.newline,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'decorator' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? decorator({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    newline(newline_?: any) { return newline_ !== undefined ? decorator({ ...(config as any), newline: newline_ } as any) : fields.newline; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return decorator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DecoratorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(...children: any[]) {
  return {
    type: 'block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionList(...children: any[]) {
  return {
    type: 'expression_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dottedName(...children: any[]) {
  return {
    type: 'dotted_name' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DottedNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}




export function casePattern(config?: ConfigOf<CasePattern>) {
  return casePatternUSimplePattern(config as any);
}
export function casePatternUAsPattern(config?: ConfigOf<CasePatternUAsPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'case_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return casePatternUAsPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function casePatternKeywordPattern(config?: ConfigOf<CasePatternKeywordPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'case_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return casePatternKeywordPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function casePatternUSimplePattern(config?: ConfigOf<CasePatternUSimplePattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'case_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return casePatternUSimplePattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CasePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenAsPattern(child?: any) {
  const children = child ? [child] : [];
  return {
    type: '_as_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenAsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionPattern(...children: any[]) {
  return {
    type: 'union_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenListPattern(...children: any[]) {
  return {
    type: '_list_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenTuplePattern(...children: any[]) {
  return {
    type: '_tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenTuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictPattern(...children: any[]) {
  return {
    type: 'dict_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordPattern(config: ConfigOf<KeywordPattern>) {
  const fields = {
    identifier: (config as any)?.identifier,
    simple_pattern: (config as any)?.simplePattern,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'keyword_pattern' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? keywordPattern({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    simplePattern(simplePattern_?: any) { return simplePattern_ !== undefined ? keywordPattern({ ...(config as any), simplePattern: simplePattern_ } as any) : fields.simple_pattern; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return keywordPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatPattern(config: ConfigOf<SplatPattern>) {
  const fields = {
    identifier: (config as any)?.identifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'splat_pattern' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? splatPattern({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return splatPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function classPattern(config: ConfigOf<ClassPattern>) {
  const fields = {
    dotted_name: (config as any)?.dottedName,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'class_pattern' as const,
    named: true as const,
    fields,
    children,
    dottedName(dottedName_?: any) { return dottedName_ !== undefined ? classPattern({ ...(config as any), dottedName: dottedName_ } as any) : fields.dotted_name; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? classPattern({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return classPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClassPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function complexPattern(config: ConfigOf<ComplexPattern>) {
  const fields = {
    real: (config as any)?.real,
    imaginary: (config as any)?.imaginary,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'complex_pattern' as const,
    named: true as const,
    fields,
    children,
    real(real_?: any) { return real_ !== undefined ? complexPattern({ ...(config as any), real: real_ } as any) : fields.real; },
    imaginary(imaginary_?: any) { return imaginary_ !== undefined ? complexPattern({ ...(config as any), imaginary: imaginary_ } as any) : fields.imaginary; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return complexPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComplexPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenParameters(...children: any[]) {
  return {
    type: '_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenPatterns(...children: any[]) {
  return {
    type: '_patterns' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenPatternsTree) { const r = target.range(); return toEdit(this, r); },
  };
}










export function parameter(config?: ConfigOf<Parameter>) {
  return parameterDictionarySplatPattern(config as any);
}
export function parameterIdentifier(config?: ConfigOf<ParameterIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parameterTypedParameter(config?: ConfigOf<ParameterTypedParameter>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterTypedParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parameterDefaultParameter(config?: ConfigOf<ParameterDefaultParameter>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterDefaultParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parameterTypedDefaultParameter(config?: ConfigOf<ParameterTypedDefaultParameter>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterTypedDefaultParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parameterListSplatPattern(config?: ConfigOf<ParameterListSplatPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterListSplatPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parameterTuplePattern(config?: ConfigOf<ParameterTuplePattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterTuplePattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parameterKeywordSeparator(config?: ConfigOf<ParameterKeywordSeparator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterKeywordSeparator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parameterPositionalSeparator(config?: ConfigOf<ParameterPositionalSeparator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterPositionalSeparator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function parameterDictionarySplatPattern(config?: ConfigOf<ParameterDictionarySplatPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameterDictionarySplatPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}








export function pattern(config?: ConfigOf<Pattern>) {
  return patternListPattern(config as any);
}
export function patternIdentifier(config?: ConfigOf<PatternIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function patternKeywordIdentifier(config?: ConfigOf<PatternKeywordIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternKeywordIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function patternSubscript(config?: ConfigOf<PatternSubscript>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternSubscript({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function patternAttribute(config?: ConfigOf<PatternAttribute>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternAttribute({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function patternListSplatPattern(config?: ConfigOf<PatternListSplatPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternListSplatPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function patternTuplePattern(config?: ConfigOf<PatternTuplePattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternTuplePattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function patternListPattern(config?: ConfigOf<PatternListPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return patternListPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listPattern(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'list_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function defaultParameter(config: ConfigOf<DefaultParameter>) {
  const fields = {
    name: (config as any)?.name,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'default_parameter' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? defaultParameter({ ...(config as any), name: name_ } as any) : fields.name; },
    value(value_?: any) { return value_ !== undefined ? defaultParameter({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return defaultParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedDefaultParameter(config: ConfigOf<TypedDefaultParameter>) {
  const fields = {
    name: (config as any)?.name,
    type: (config as any)?.type,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'typed_default_parameter' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? typedDefaultParameter({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? typedDefaultParameter({ ...(config as any), type: type } as any) : fields.type; },
    value(value_?: any) { return value_ !== undefined ? typedDefaultParameter({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typedDefaultParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedDefaultParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listSplatPattern(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'list_splat_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListSplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionarySplatPattern(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'dictionary_splat_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionarySplatPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asPattern(config: ConfigOf<AsPattern>) {
  const fields = {
    expression: (config as any)?.expression,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? asPattern({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    alias(alias_?: any) { return alias_ !== undefined ? asPattern({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}









export function expression(config?: ConfigOf<Expression>) {
  return expressionAsPattern(config as any);
}
export function expressionComparisonOperator(config?: ConfigOf<ExpressionComparisonOperator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionComparisonOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionNotOperator(config?: ConfigOf<ExpressionNotOperator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionNotOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionBooleanOperator(config?: ConfigOf<ExpressionBooleanOperator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionBooleanOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionLambda(config?: ConfigOf<ExpressionLambda>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionLambda({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionPrimaryExpression(config?: ConfigOf<ExpressionPrimaryExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionPrimaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionConditionalExpression(config?: ConfigOf<ExpressionConditionalExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionConditionalExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionNamedExpression(config?: ConfigOf<ExpressionNamedExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionNamedExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function expressionAsPattern(config?: ConfigOf<ExpressionAsPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return expressionAsPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}



























export function primaryExpression(config?: ConfigOf<PrimaryExpression>) {
  return primaryExpressionListSplatPattern(config as any);
}
export function primaryExpressionAwait(config?: ConfigOf<PrimaryExpressionAwait>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionAwait({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionBinaryOperator(config?: ConfigOf<PrimaryExpressionBinaryOperator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionBinaryOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionIdentifier(config?: ConfigOf<PrimaryExpressionIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionKeywordIdentifier(config?: ConfigOf<PrimaryExpressionKeywordIdentifier>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionKeywordIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionString(config?: ConfigOf<PrimaryExpressionString>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionString({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionConcatenatedString(config?: ConfigOf<PrimaryExpressionConcatenatedString>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionConcatenatedString({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionInteger(config?: ConfigOf<PrimaryExpressionInteger>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionInteger({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionFloat(config?: ConfigOf<PrimaryExpressionFloat>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionFloat({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionTrue(config?: ConfigOf<PrimaryExpressionTrue>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionTrue({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionFalse(config?: ConfigOf<PrimaryExpressionFalse>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionFalse({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionNone(config?: ConfigOf<PrimaryExpressionNone>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionNone({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionUnaryOperator(config?: ConfigOf<PrimaryExpressionUnaryOperator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionUnaryOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionAttribute(config?: ConfigOf<PrimaryExpressionAttribute>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionAttribute({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionSubscript(config?: ConfigOf<PrimaryExpressionSubscript>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionSubscript({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionCall(config?: ConfigOf<PrimaryExpressionCall>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionCall({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionList(config?: ConfigOf<PrimaryExpressionList>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionList({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionListComprehension(config?: ConfigOf<PrimaryExpressionListComprehension>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionListComprehension({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionDictionary(config?: ConfigOf<PrimaryExpressionDictionary>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionDictionary({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionDictionaryComprehension(config?: ConfigOf<PrimaryExpressionDictionaryComprehension>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionDictionaryComprehension({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionSet(config?: ConfigOf<PrimaryExpressionSet>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionSet({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionSetComprehension(config?: ConfigOf<PrimaryExpressionSetComprehension>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionSetComprehension({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionTuple(config?: ConfigOf<PrimaryExpressionTuple>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionTuple({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionParenthesizedExpression(config?: ConfigOf<PrimaryExpressionParenthesizedExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionParenthesizedExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionGeneratorExpression(config?: ConfigOf<PrimaryExpressionGeneratorExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionGeneratorExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionEllipsis(config?: ConfigOf<PrimaryExpressionEllipsis>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionEllipsis({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function primaryExpressionListSplatPattern(config?: ConfigOf<PrimaryExpressionListSplatPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'primary_expression' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return primaryExpressionListSplatPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PrimaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function notOperator(config: ConfigOf<NotOperator>) {
  const fields = {
    argument: (config as any)?.argument,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'not_operator' as const,
    named: true as const,
    fields,
    children,
    argument(argument_?: any) { return argument_ !== undefined ? notOperator({ ...(config as any), argument: argument_ } as any) : fields.argument; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return notOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NotOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function booleanOperator(config: ConfigOf<BooleanOperator>) {
  const fields = {
    left: (config as any)?.left,
    operator: (config as any)?.operator,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'boolean_operator' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? booleanOperator({ ...(config as any), left: left_ } as any) : fields.left; },
    operator(operator_?: any) { return operator_ !== undefined ? booleanOperator({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    right(right_?: any) { return right_ !== undefined ? booleanOperator({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return booleanOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BooleanOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryOperator(config: ConfigOf<BinaryOperator>) {
  const fields = {
    left: (config as any)?.left,
    operator: (config as any)?.operator,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'binary_operator' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? binaryOperator({ ...(config as any), left: left_ } as any) : fields.left; },
    operator(operator_?: any) { return operator_ !== undefined ? binaryOperator({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    right(right_?: any) { return right_ !== undefined ? binaryOperator({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return binaryOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryOperator(config: ConfigOf<UnaryOperator>) {
  const fields = {
    operator: (config as any)?.operator,
    argument: (config as any)?.argument,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'unary_operator' as const,
    named: true as const,
    fields,
    children,
    operator(operator_?: any) { return operator_ !== undefined ? unaryOperator({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    argument(argument_?: any) { return argument_ !== undefined ? unaryOperator({ ...(config as any), argument: argument_ } as any) : fields.argument; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return unaryOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenNotIn(text: string) {
  return {
    type: '_not_in' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HiddenNotInTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function hiddenIsNot(text: string) {
  return {
    type: '_is_not' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: HiddenIsNotTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comparisonOperator(config: ConfigOf<ComparisonOperator>) {
  const fields = {
    left: (config as any)?.left,
    comparators: (config as any)?.comparators,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'comparison_operator' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? comparisonOperator({ ...(config as any), left: left_ } as any) : fields.left; },
    comparators(comparators_?: any) { return comparators_ !== undefined ? comparisonOperator({ ...(config as any), comparators: comparators_ } as any) : fields.comparators; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return comparisonOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ComparisonOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambda(config: ConfigOf<Lambda>) {
  const fields = {
    parameters: (config as any)?.parameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'lambda' as const,
    named: true as const,
    fields,
    children,
    parameters(parameters_?: any) { return parameters_ !== undefined ? lambda({ ...(config as any), parameters: parameters_ } as any) : fields.parameters; },
    body(body_?: any) { return body_ !== undefined ? lambda({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return lambda({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lambdaWithinForInClause(config: ConfigOf<LambdaWithinForInClause>) {
  const fields = {
    parameters: (config as any)?.parameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'lambda_within_for_in_clause' as const,
    named: true as const,
    fields,
    children,
    parameters(parameters_?: any) { return parameters_ !== undefined ? lambdaWithinForInClause({ ...(config as any), parameters: parameters_ } as any) : fields.parameters; },
    body(body_?: any) { return body_ !== undefined ? lambdaWithinForInClause({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return lambdaWithinForInClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LambdaWithinForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignment(config: ConfigOf<Assignment>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'assignment' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? assignment({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? assignment({ ...(config as any), right: right_ } as any) : fields.right; },
    typeField(type?: any) { return type !== undefined ? assignment({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return assignment({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function augmentedAssignment(config: ConfigOf<AugmentedAssignment>) {
  const fields = {
    left: (config as any)?.left,
    operator: (config as any)?.operator,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'augmented_assignment' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? augmentedAssignment({ ...(config as any), left: left_ } as any) : fields.left; },
    operator(operator_?: any) { return operator_ !== undefined ? augmentedAssignment({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    right(right_?: any) { return right_ !== undefined ? augmentedAssignment({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return augmentedAssignment({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AugmentedAssignmentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function patternList(...children: any[]) {
  return {
    type: 'pattern_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PatternListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yield_(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'yield' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: ConfigOf<Attribute>) {
  const fields = {
    object: (config as any)?.object,
    attribute: (config as any)?.attribute,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    children,
    object(object_?: any) { return object_ !== undefined ? attribute({ ...(config as any), object: object_ } as any) : fields.object; },
    attribute(attribute_?: any) { return attribute_ !== undefined ? attribute({ ...(config as any), attribute: attribute_ } as any) : fields.attribute; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return attribute({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function subscript(config: ConfigOf<Subscript>) {
  const fields = {
    value: (config as any)?.value,
    subscript: (config as any)?.subscript,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'subscript' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? subscript({ ...(config as any), value: value_ } as any) : fields.value; },
    subscript(...subscript_: any[]) { return subscript_.length ? subscript({ ...(config as any), subscript: subscript_ } as any) : fields.subscript; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return subscript({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SubscriptTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slice(config: ConfigOf<Slice>) {
  const fields = {
    start: (config as any)?.start,
    stop: (config as any)?.stop,
    step: (config as any)?.step,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'slice' as const,
    named: true as const,
    fields,
    children,
    start(start_?: any) { return start_ !== undefined ? slice({ ...(config as any), start: start_ } as any) : fields.start; },
    stop(stop_?: any) { return stop_ !== undefined ? slice({ ...(config as any), stop: stop_ } as any) : fields.stop; },
    step(step_?: any) { return step_ !== undefined ? slice({ ...(config as any), step: step_ } as any) : fields.step; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return slice({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SliceTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function call(config: ConfigOf<Call>) {
  const fields = {
    function: (config as any)?.function,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'call' as const,
    named: true as const,
    fields,
    children,
    function(function_?: any) { return function_ !== undefined ? call({ ...(config as any), function: function_ } as any) : fields.function; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? call({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return call({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typedParameter(config: ConfigOf<TypedParameter>) {
  const fields = {
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'typed_parameter' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: any) { return type !== undefined ? typedParameter({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typedParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypedParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}







export function type_(config?: ConfigOf<Type>) {
  return typeMemberType(config as any);
}
export function typeExpression(config?: ConfigOf<TypeExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeSplatType(config?: ConfigOf<TypeSplatType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeSplatType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeGenericType(config?: ConfigOf<TypeGenericType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeGenericType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeUnionType(config?: ConfigOf<TypeUnionType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeUnionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeConstrainedType(config?: ConfigOf<TypeConstrainedType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeConstrainedType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function typeMemberType(config?: ConfigOf<TypeMemberType>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeMemberType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function splatType(config: ConfigOf<SplatType>) {
  const fields = {
    identifier: (config as any)?.identifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'splat_type' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? splatType({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return splatType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SplatTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: ConfigOf<GenericType>) {
  const fields = {
    identifier: (config as any)?.identifier,
    type_parameter: (config as any)?.typeParameter,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? genericType({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    typeParameter(typeParameter_?: any) { return typeParameter_ !== undefined ? genericType({ ...(config as any), typeParameter: typeParameter_ } as any) : fields.type_parameter; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return genericType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionType(config: ConfigOf<UnionType>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'union_type' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? unionType({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? unionType({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return unionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constrainedType(config: ConfigOf<ConstrainedType>) {
  const fields = {
    base_type: (config as any)?.baseType,
    constraint: (config as any)?.constraint,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'constrained_type' as const,
    named: true as const,
    fields,
    children,
    baseType(baseType_?: any) { return baseType_ !== undefined ? constrainedType({ ...(config as any), baseType: baseType_ } as any) : fields.base_type; },
    constraint(constraint_?: any) { return constraint_ !== undefined ? constrainedType({ ...(config as any), constraint: constraint_ } as any) : fields.constraint; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return constrainedType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstrainedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function memberType(config: ConfigOf<MemberType>) {
  const fields = {
    base_type: (config as any)?.baseType,
    identifier: (config as any)?.identifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'member_type' as const,
    named: true as const,
    fields,
    children,
    baseType(baseType_?: any) { return baseType_ !== undefined ? memberType({ ...(config as any), baseType: baseType_ } as any) : fields.base_type; },
    identifier(identifier_?: any) { return identifier_ !== undefined ? memberType({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return memberType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MemberTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function keywordArgument(config: ConfigOf<KeywordArgument>) {
  const fields = {
    name: (config as any)?.name,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'keyword_argument' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? keywordArgument({ ...(config as any), name: name_ } as any) : fields.name; },
    value(value_?: any) { return value_ !== undefined ? keywordArgument({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return keywordArgument({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: KeywordArgumentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function list(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function set(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'set' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuple(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'tuple' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionary(...children: any[]) {
  return {
    type: 'dictionary' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pair(config: ConfigOf<Pair>) {
  const fields = {
    key: (config as any)?.key,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'pair' as const,
    named: true as const,
    fields,
    children,
    key(key_?: any) { return key_ !== undefined ? pair({ ...(config as any), key: key_ } as any) : fields.key; },
    value(value_?: any) { return value_ !== undefined ? pair({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return pair({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PairTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function listComprehension(config: ConfigOf<ListComprehension>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'list_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? listComprehension({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return listComprehension({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ListComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dictionaryComprehension(config: ConfigOf<DictionaryComprehension>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'dictionary_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? dictionaryComprehension({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return dictionaryComprehension({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DictionaryComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function setComprehension(config: ConfigOf<SetComprehension>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'set_comprehension' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? setComprehension({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return setComprehension({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SetComprehensionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function generatorExpression(config: ConfigOf<GeneratorExpression>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generator_expression' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? generatorExpression({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return generatorExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GeneratorExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenComprehensionClauses(...children: any[]) {
  return {
    type: '_comprehension_clauses' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenComprehensionClausesTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenCollectionElements(...children: any[]) {
  return {
    type: '_collection_elements' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenCollectionElementsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forInClause(config: ConfigOf<ForInClause>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'for_in_clause' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? forInClause({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? forInClause({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return forInClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForInClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifClause(config: ConfigOf<IfClause>) {
  const fields = {
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'if_clause' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? ifClause({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return ifClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function conditionalExpression(config: ConfigOf<ConditionalExpression>) {
  const fields = {
    body: (config as any)?.body,
    condition: (config as any)?.condition,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'conditional_expression' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? conditionalExpression({ ...(config as any), body: body_ } as any) : fields.body; },
    condition(condition_?: any) { return condition_ !== undefined ? conditionalExpression({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    alternative(alternative_?: any) { return alternative_ !== undefined ? conditionalExpression({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return conditionalExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConditionalExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function concatenatedString(...children: any[]) {
  return {
    type: 'concatenated_string' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConcatenatedStringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function string(config: ConfigOf<String>) {
  const fields = {
    string_start: (config as any)?.stringStart,
    content: (config as any)?.content,
    string_end: (config as any)?.stringEnd,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'string' as const,
    named: true as const,
    fields,
    children,
    stringStart(stringStart_?: any) { return stringStart_ !== undefined ? string({ ...(config as any), stringStart: stringStart_ } as any) : fields.string_start; },
    content(content_?: any) { return content_ !== undefined ? string({ ...(config as any), content: content_ } as any) : fields.content; },
    stringEnd(stringEnd_?: any) { return stringEnd_ !== undefined ? string({ ...(config as any), stringEnd: stringEnd_ } as any) : fields.string_end; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return string({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringContent(...children: any[]) {
  return {
    type: 'string_content' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringContentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function interpolation(config: ConfigOf<Interpolation>) {
  const fields = {
    expression: (config as any)?.expression,
    type_conversion: (config as any)?.typeConversion,
    format_specifier: (config as any)?.formatSpecifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'interpolation' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? interpolation({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    typeConversion(typeConversion_?: any) { return typeConversion_ !== undefined ? interpolation({ ...(config as any), typeConversion: typeConversion_ } as any) : fields.type_conversion; },
    formatSpecifier(formatSpecifier_?: any) { return formatSpecifier_ !== undefined ? interpolation({ ...(config as any), formatSpecifier: formatSpecifier_ } as any) : fields.format_specifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return interpolation({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InterpolationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function escapeSequence(text: string) {
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function formatSpecifier(...children: any[]) {
  return {
    type: 'format_specifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatSpecifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeConversion(text: string) {
  return {
    type: 'type_conversion' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeConversionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer(text: string) {
  return {
    type: 'integer' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IntegerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function float(text: string) {
  return {
    type: 'float' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FloatTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier(text: string) {
  return {
    type: 'identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function keywordIdentifier(text: string) {
  return {
    type: 'keyword_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: KeywordIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function true_() {
  return {
    type: 'true' as const,
    named: true as const,
    text: 'True',
    render: () => 'True',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'True' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'True' },
    replace: (t: TrueTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'True' }; },
  };
}

export function false_() {
  return {
    type: 'false' as const,
    named: true as const,
    text: 'False',
    render: () => 'False',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'False' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'False' },
    replace: (t: FalseTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'False' }; },
  };
}

export function none() {
  return {
    type: 'none' as const,
    named: true as const,
    text: 'None',
    render: () => 'None',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'None' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'None' },
    replace: (t: NoneTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'None' }; },
  };
}

export function await_(config: ConfigOf<Await>) {
  const fields = {
    primary_expression: (config as any)?.primaryExpression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'await' as const,
    named: true as const,
    fields,
    children,
    primaryExpression(primaryExpression_?: any) { return primaryExpression_ !== undefined ? await_({ ...(config as any), primaryExpression: primaryExpression_ } as any) : fields.primary_expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return await_({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function comment(text: string) {
  return {
    type: 'comment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CommentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function lineContinuation(text: string) {
  return {
    type: 'line_continuation' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: LineContinuationTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}









export function asPatternTarget(config?: ConfigOf<AsPatternTarget>) {
  return asPatternTargetAsPattern(config as any);
}
export function asPatternTargetComparisonOperator(config?: ConfigOf<AsPatternTargetComparisonOperator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPatternTargetComparisonOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function asPatternTargetNotOperator(config?: ConfigOf<AsPatternTargetNotOperator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPatternTargetNotOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function asPatternTargetBooleanOperator(config?: ConfigOf<AsPatternTargetBooleanOperator>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPatternTargetBooleanOperator({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function asPatternTargetLambda(config?: ConfigOf<AsPatternTargetLambda>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPatternTargetLambda({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function asPatternTargetPrimaryExpression(config?: ConfigOf<AsPatternTargetPrimaryExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPatternTargetPrimaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function asPatternTargetConditionalExpression(config?: ConfigOf<AsPatternTargetConditionalExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPatternTargetConditionalExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function asPatternTargetNamedExpression(config?: ConfigOf<AsPatternTargetNamedExpression>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPatternTargetNamedExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function asPatternTargetAsPattern(config?: ConfigOf<AsPatternTargetAsPattern>) {
  const children = (config as any)?.children ?? [];
  return {
    type: 'as_pattern_target' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...items: any[]) { return asPatternTargetAsPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsPatternTargetTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function formatExpression(config: ConfigOf<FormatExpression>) {
  const fields = {
    expression: (config as any)?.expression,
    type_conversion: (config as any)?.typeConversion,
    format_specifier: (config as any)?.formatSpecifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'format_expression' as const,
    named: true as const,
    fields,
    children,
    expression(expression_?: any) { return expression_ !== undefined ? formatExpression({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    typeConversion(typeConversion_?: any) { return typeConversion_ !== undefined ? formatExpression({ ...(config as any), typeConversion: typeConversion_ } as any) : fields.type_conversion; },
    formatSpecifier(formatSpecifier_?: any) { return formatSpecifier_ !== undefined ? formatExpression({ ...(config as any), formatSpecifier: formatSpecifier_ } as any) : fields.format_specifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return formatExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FormatExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export const _factoryMap: Record<string, (config?: any) => unknown> = {
  'module': module_,
  '_simple_statements': hiddenSimpleStatements,
  'import_statement': importStatement,
  'import_prefix': importPrefix,
  'relative_import': relativeImport,
  'future_import_statement': futureImportStatement,
  'import_from_statement': importFromStatement,
  '_import_list': hiddenImportList,
  'aliased_import': aliasedImport,
  'print_statement': printStatement,
  'chevron': chevron,
  'assert_statement': assertStatement,
  'expression_statement_expression': expressionStatementExpression,
  'expression_statement_expression2': expressionStatementExpression2,
  'expression_statement_assignment': expressionStatementAssignment,
  'expression_statement_augmented_assignment': expressionStatementAugmentedAssignment,
  'expression_statement_yield': expressionStatementYield,
  'expression_statement': expressionStatement,
  'named_expression': namedExpression,
  'return_statement': returnStatement,
  'delete_statement': deleteStatement,
  'raise_statement': raiseStatement,
  'pass_statement': passStatement,
  'break_statement': breakStatement,
  'continue_statement': continueStatement,
  'if_statement': ifStatement,
  'elif_clause': elifClause,
  'else_clause': elseClause,
  'match_statement': matchStatement,
  'case_clause': caseClause,
  'for_statement': forStatement,
  'while_statement': whileStatement,
  'try_statement': tryStatement,
  'except_clause': exceptClause,
  'finally_clause': finallyClause,
  'with_statement': withStatement,
  'with_clause_with_item': withClauseWithItem,
  'with_clause_paren': withClauseParen,
  'with_clause': withClause,
  'with_item': withItem,
  'function_definition': functionDefinition,
  'parameters': parameters,
  'lambda_parameters': lambdaParameters,
  'list_splat': listSplat,
  'dictionary_splat': dictionarySplat,
  'global_statement': globalStatement,
  'nonlocal_statement': nonlocalStatement,
  'exec_statement': execStatement,
  'type_alias_statement': typeAliasStatement,
  'class_definition': classDefinition,
  'type_parameter': typeParameter,
  'parenthesized_list_splat': parenthesizedListSplat,
  'argument_list': argumentList,
  'decorated_definition': decoratedDefinition,
  'decorator': decorator,
  'block': block,
  'expression_list': expressionList,
  'dotted_name': dottedName,
  'case_pattern__as_pattern': casePatternUAsPattern,
  'case_pattern_keyword_pattern': casePatternKeywordPattern,
  'case_pattern__simple_pattern': casePatternUSimplePattern,
  'case_pattern': casePattern,
  '_as_pattern': hiddenAsPattern,
  'union_pattern': unionPattern,
  '_list_pattern': hiddenListPattern,
  '_tuple_pattern': hiddenTuplePattern,
  'dict_pattern': dictPattern,
  'keyword_pattern': keywordPattern,
  'splat_pattern': splatPattern,
  'class_pattern': classPattern,
  'complex_pattern': complexPattern,
  '_parameters': hiddenParameters,
  '_patterns': hiddenPatterns,
  'parameter_identifier': parameterIdentifier,
  'parameter_typed_parameter': parameterTypedParameter,
  'parameter_default_parameter': parameterDefaultParameter,
  'parameter_typed_default_parameter': parameterTypedDefaultParameter,
  'parameter_list_splat_pattern': parameterListSplatPattern,
  'parameter_tuple_pattern': parameterTuplePattern,
  'parameter_keyword_separator': parameterKeywordSeparator,
  'parameter_positional_separator': parameterPositionalSeparator,
  'parameter_dictionary_splat_pattern': parameterDictionarySplatPattern,
  'parameter': parameter,
  'pattern_identifier': patternIdentifier,
  'pattern_keyword_identifier': patternKeywordIdentifier,
  'pattern_subscript': patternSubscript,
  'pattern_attribute': patternAttribute,
  'pattern_list_splat_pattern': patternListSplatPattern,
  'pattern_tuple_pattern': patternTuplePattern,
  'pattern_list_pattern': patternListPattern,
  'pattern': pattern,
  'tuple_pattern': tuplePattern,
  'list_pattern': listPattern,
  'default_parameter': defaultParameter,
  'typed_default_parameter': typedDefaultParameter,
  'list_splat_pattern': listSplatPattern,
  'dictionary_splat_pattern': dictionarySplatPattern,
  'as_pattern': asPattern,
  'expression_comparison_operator': expressionComparisonOperator,
  'expression_not_operator': expressionNotOperator,
  'expression_boolean_operator': expressionBooleanOperator,
  'expression_lambda': expressionLambda,
  'expression_primary_expression': expressionPrimaryExpression,
  'expression_conditional_expression': expressionConditionalExpression,
  'expression_named_expression': expressionNamedExpression,
  'expression_as_pattern': expressionAsPattern,
  'expression': expression,
  'primary_expression_await': primaryExpressionAwait,
  'primary_expression_binary_operator': primaryExpressionBinaryOperator,
  'primary_expression_identifier': primaryExpressionIdentifier,
  'primary_expression_keyword_identifier': primaryExpressionKeywordIdentifier,
  'primary_expression_string': primaryExpressionString,
  'primary_expression_concatenated_string': primaryExpressionConcatenatedString,
  'primary_expression_integer': primaryExpressionInteger,
  'primary_expression_float': primaryExpressionFloat,
  'primary_expression_true': primaryExpressionTrue,
  'primary_expression_false': primaryExpressionFalse,
  'primary_expression_none': primaryExpressionNone,
  'primary_expression_unary_operator': primaryExpressionUnaryOperator,
  'primary_expression_attribute': primaryExpressionAttribute,
  'primary_expression_subscript': primaryExpressionSubscript,
  'primary_expression_call': primaryExpressionCall,
  'primary_expression_list': primaryExpressionList,
  'primary_expression_list_comprehension': primaryExpressionListComprehension,
  'primary_expression_dictionary': primaryExpressionDictionary,
  'primary_expression_dictionary_comprehension': primaryExpressionDictionaryComprehension,
  'primary_expression_set': primaryExpressionSet,
  'primary_expression_set_comprehension': primaryExpressionSetComprehension,
  'primary_expression_tuple': primaryExpressionTuple,
  'primary_expression_parenthesized_expression': primaryExpressionParenthesizedExpression,
  'primary_expression_generator_expression': primaryExpressionGeneratorExpression,
  'primary_expression_ellipsis': primaryExpressionEllipsis,
  'primary_expression_list_splat_pattern': primaryExpressionListSplatPattern,
  'primary_expression': primaryExpression,
  'not_operator': notOperator,
  'boolean_operator': booleanOperator,
  'binary_operator': binaryOperator,
  'unary_operator': unaryOperator,
  '_not_in': hiddenNotIn,
  '_is_not': hiddenIsNot,
  'comparison_operator': comparisonOperator,
  'lambda': lambda,
  'lambda_within_for_in_clause': lambdaWithinForInClause,
  'assignment': assignment,
  'augmented_assignment': augmentedAssignment,
  'pattern_list': patternList,
  'yield': yield_,
  'attribute': attribute,
  'subscript': subscript,
  'slice': slice,
  'call': call,
  'typed_parameter': typedParameter,
  'type_expression': typeExpression,
  'type_splat_type': typeSplatType,
  'type_generic_type': typeGenericType,
  'type_union_type': typeUnionType,
  'type_constrained_type': typeConstrainedType,
  'type_member_type': typeMemberType,
  'type': type_,
  'splat_type': splatType,
  'generic_type': genericType,
  'union_type': unionType,
  'constrained_type': constrainedType,
  'member_type': memberType,
  'keyword_argument': keywordArgument,
  'list': list,
  'set': set,
  'tuple': tuple,
  'dictionary': dictionary,
  'pair': pair,
  'list_comprehension': listComprehension,
  'dictionary_comprehension': dictionaryComprehension,
  'set_comprehension': setComprehension,
  'generator_expression': generatorExpression,
  '_comprehension_clauses': hiddenComprehensionClauses,
  'parenthesized_expression': parenthesizedExpression,
  '_collection_elements': hiddenCollectionElements,
  'for_in_clause': forInClause,
  'if_clause': ifClause,
  'conditional_expression': conditionalExpression,
  'concatenated_string': concatenatedString,
  'string': string,
  'string_content': stringContent,
  'interpolation': interpolation,
  'escape_sequence': escapeSequence,
  'format_specifier': formatSpecifier,
  'type_conversion': typeConversion,
  'integer': integer,
  'float': float,
  'identifier': identifier,
  'keyword_identifier': keywordIdentifier,
  'true': true_,
  'false': false_,
  'none': none,
  'await': await_,
  'comment': comment,
  'line_continuation': lineContinuation,
  'as_pattern_target_comparison_operator': asPatternTargetComparisonOperator,
  'as_pattern_target_not_operator': asPatternTargetNotOperator,
  'as_pattern_target_boolean_operator': asPatternTargetBooleanOperator,
  'as_pattern_target_lambda': asPatternTargetLambda,
  'as_pattern_target_primary_expression': asPatternTargetPrimaryExpression,
  'as_pattern_target_conditional_expression': asPatternTargetConditionalExpression,
  'as_pattern_target_named_expression': asPatternTargetNamedExpression,
  'as_pattern_target_as_pattern': asPatternTargetAsPattern,
  'as_pattern_target': asPatternTarget,
  'format_expression': formatExpression,
};

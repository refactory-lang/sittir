// Auto-generated by ir-codegen — do not edit

import { LeafBuilder } from '@sittir/types';
import { aliased_import } from './nodes/aliased-import.js';
import { argument_list } from './nodes/argument-list.js';
import { as_pattern } from './nodes/as-pattern.js';
import { assert_statement } from './nodes/assert-statement.js';
import { assignment } from './nodes/assignment.js';
import { attribute } from './nodes/attribute.js';
import { augmented_assignment } from './nodes/augmented-assignment.js';
import { binary_operator } from './nodes/binary-operator.js';
import { block } from './nodes/block.js';
import { boolean_operator } from './nodes/boolean-operator.js';
import { call } from './nodes/call.js';
import { case_clause } from './nodes/case-clause.js';
import { case_pattern } from './nodes/case-pattern.js';
import { chevron } from './nodes/chevron.js';
import { class_definition } from './nodes/class-definition.js';
import { class_pattern } from './nodes/class-pattern.js';
import { comparison_operator } from './nodes/comparison-operator.js';
import { complex_pattern } from './nodes/complex-pattern.js';
import { concatenated_string } from './nodes/concatenated-string.js';
import { conditional_expression } from './nodes/conditional-expression.js';
import { constrained_type } from './nodes/constrained-type.js';
import { decorated_definition } from './nodes/decorated-definition.js';
import { decorator } from './nodes/decorator.js';
import { default_parameter } from './nodes/default-parameter.js';
import { delete_statement } from './nodes/delete-statement.js';
import { dict_pattern } from './nodes/dict-pattern.js';
import { dictionary } from './nodes/dictionary.js';
import { dictionary_comprehension } from './nodes/dictionary-comprehension.js';
import { dictionary_splat } from './nodes/dictionary-splat.js';
import { dictionary_splat_pattern } from './nodes/dictionary-splat-pattern.js';
import { dotted_name } from './nodes/dotted-name.js';
import { elif_clause } from './nodes/elif-clause.js';
import { else_clause } from './nodes/else-clause.js';
import { except_clause } from './nodes/except-clause.js';
import { exec_statement } from './nodes/exec-statement.js';
import { expression_list } from './nodes/expression-list.js';
import { expression_statement } from './nodes/expression-statement.js';
import { finally_clause } from './nodes/finally-clause.js';
import { for_in_clause } from './nodes/for-in-clause.js';
import { for_statement } from './nodes/for-statement.js';
import { format_expression } from './nodes/format-expression.js';
import { format_specifier } from './nodes/format-specifier.js';
import { function_definition } from './nodes/function-definition.js';
import { future_import_statement } from './nodes/future-import-statement.js';
import { generator_expression } from './nodes/generator-expression.js';
import { generic_type } from './nodes/generic-type.js';
import { global_statement } from './nodes/global-statement.js';
import { if_clause } from './nodes/if-clause.js';
import { if_statement } from './nodes/if-statement.js';
import { import_from_statement } from './nodes/import-from-statement.js';
import { import_statement } from './nodes/import-statement.js';
import { interpolation } from './nodes/interpolation.js';
import { keyword_argument } from './nodes/keyword-argument.js';
import { keyword_pattern } from './nodes/keyword-pattern.js';
import { lambda_parameters } from './nodes/lambda-parameters.js';
import { list } from './nodes/list.js';
import { list_comprehension } from './nodes/list-comprehension.js';
import { list_pattern } from './nodes/list-pattern.js';
import { list_splat } from './nodes/list-splat.js';
import { list_splat_pattern } from './nodes/list-splat-pattern.js';
import { match_statement } from './nodes/match-statement.js';
import { member_type } from './nodes/member-type.js';
import { module } from './nodes/module.js';
import { named_expression } from './nodes/named-expression.js';
import { nonlocal_statement } from './nodes/nonlocal-statement.js';
import { not_operator } from './nodes/not-operator.js';
import { pair } from './nodes/pair.js';
import { parameters } from './nodes/parameters.js';
import { parenthesized_expression } from './nodes/parenthesized-expression.js';
import { parenthesized_list_splat } from './nodes/parenthesized-list-splat.js';
import { pattern_list } from './nodes/pattern-list.js';
import { print_statement } from './nodes/print-statement.js';
import { raise_statement } from './nodes/raise-statement.js';
import { relative_import } from './nodes/relative-import.js';
import { return_statement } from './nodes/return-statement.js';
import { set } from './nodes/set.js';
import { set_comprehension } from './nodes/set-comprehension.js';
import { slice } from './nodes/slice.js';
import { splat_pattern } from './nodes/splat-pattern.js';
import { splat_type } from './nodes/splat-type.js';
import { string } from './nodes/string.js';
import { string_content } from './nodes/string-content.js';
import { subscript } from './nodes/subscript.js';
import { try_statement } from './nodes/try-statement.js';
import { tuple } from './nodes/tuple.js';
import { tuple_pattern } from './nodes/tuple-pattern.js';
import { type_alias_statement } from './nodes/type-alias-statement.js';
import { type_parameter } from './nodes/type-parameter.js';
import { typed_default_parameter } from './nodes/typed-default-parameter.js';
import { typed_parameter } from './nodes/typed-parameter.js';
import { unary_operator } from './nodes/unary-operator.js';
import { union_pattern } from './nodes/union-pattern.js';
import { union_type } from './nodes/union-type.js';
import { while_statement } from './nodes/while-statement.js';
import { with_clause } from './nodes/with-clause.js';
import { with_ } from './nodes/with.js';
import { with_statement } from './nodes/with-statement.js';

export const ir = {
  aliasedImport: aliased_import,
  argumentList: argument_list,
  asPattern: as_pattern,
  assertStatement: assert_statement,
  assignment,
  attribute,
  augmentedAssignment: augmented_assignment,
  binaryOperator: binary_operator,
  block,
  booleanOperator: boolean_operator,
  call,
  caseClause: case_clause,
  casePattern: case_pattern,
  chevron,
  classDefinition: class_definition,
  classPattern: class_pattern,
  comparisonOperator: comparison_operator,
  complexPattern: complex_pattern,
  concatenatedString: concatenated_string,
  conditionalExpression: conditional_expression,
  constrainedType: constrained_type,
  decoratedDefinition: decorated_definition,
  decorator,
  defaultParameter: default_parameter,
  deleteStatement: delete_statement,
  dictPattern: dict_pattern,
  dictionary,
  dictionaryComprehension: dictionary_comprehension,
  dictionarySplat: dictionary_splat,
  dictionarySplatPattern: dictionary_splat_pattern,
  dottedName: dotted_name,
  elifClause: elif_clause,
  elseClause: else_clause,
  exceptClause: except_clause,
  execStatement: exec_statement,
  expressionList: expression_list,
  expressionStatement: expression_statement,
  finallyClause: finally_clause,
  forInClause: for_in_clause,
  forStatement: for_statement,
  formatExpression: format_expression,
  formatSpecifier: format_specifier,
  functionDefinition: function_definition,
  futureImportStatement: future_import_statement,
  generatorExpression: generator_expression,
  genericType: generic_type,
  globalStatement: global_statement,
  ifClause: if_clause,
  ifStatement: if_statement,
  importFromStatement: import_from_statement,
  importStatement: import_statement,
  interpolation,
  keywordArgument: keyword_argument,
  keywordPattern: keyword_pattern,
  lambdaParameters: lambda_parameters,
  list,
  listComprehension: list_comprehension,
  listPattern: list_pattern,
  listSplat: list_splat,
  listSplatPattern: list_splat_pattern,
  matchStatement: match_statement,
  memberType: member_type,
  module,
  namedExpression: named_expression,
  nonlocalStatement: nonlocal_statement,
  notOperator: not_operator,
  pair,
  parameters,
  parenthesizedExpression: parenthesized_expression,
  parenthesizedListSplat: parenthesized_list_splat,
  patternList: pattern_list,
  printStatement: print_statement,
  raiseStatement: raise_statement,
  relativeImport: relative_import,
  returnStatement: return_statement,
  set,
  setComprehension: set_comprehension,
  slice,
  splatPattern: splat_pattern,
  splatType: splat_type,
  string,
  stringContent: string_content,
  subscript,
  tryStatement: try_statement,
  tuple,
  tuplePattern: tuple_pattern,
  typeAliasStatement: type_alias_statement,
  typeParameter: type_parameter,
  typedDefaultParameter: typed_default_parameter,
  typedParameter: typed_parameter,
  unaryOperator: unary_operator,
  unionPattern: union_pattern,
  unionType: union_type,
  whileStatement: while_statement,
  withClause: with_clause,
  with: with_,
  withStatement: with_statement,

  // Leaf node builders
  breakStatement: (text: string) => new LeafBuilder('break_statement', text),
  continueStatement: (text: string) => new LeafBuilder('continue_statement', text),
  importPrefix: (text: string) => new LeafBuilder('import_prefix', text),
  keywordSeparator: (text: string) => new LeafBuilder('keyword_separator', text),
  passStatement: (text: string) => new LeafBuilder('pass_statement', text),
  positionalSeparator: (text: string) => new LeafBuilder('positional_separator', text),
  wildcardImport: (text: string) => new LeafBuilder('wildcard_import', text),
  comment: (text: string) => new LeafBuilder('comment', text),
  ellipsis: (text: string) => new LeafBuilder('ellipsis', text),
  escapeInterpolation: (text: string) => new LeafBuilder('escape_interpolation', text),
  escapeSequence: (text: string) => new LeafBuilder('escape_sequence', text),
  false: (text: string) => new LeafBuilder('false', text),
  float: (text: string) => new LeafBuilder('float', text),
  identifier: (text: string) => new LeafBuilder('identifier', text),
  integer: (text: string) => new LeafBuilder('integer', text),
  lineContinuation: (text: string) => new LeafBuilder('line_continuation', text),
  none: (text: string) => new LeafBuilder('none', text),
  stringEnd: (text: string) => new LeafBuilder('string_end', text),
  stringStart: (text: string) => new LeafBuilder('string_start', text),
  true: (text: string) => new LeafBuilder('true', text),
  typeConversion: (text: string) => new LeafBuilder('type_conversion', text),

  // Namespaced access via supertypes
  compoundStatement: {
    classDefinition: class_definition,
    decoratedDefinition: decorated_definition,
    forStatement: for_statement,
    functionDefinition: function_definition,
    ifStatement: if_statement,
    matchStatement: match_statement,
    tryStatement: try_statement,
    whileStatement: while_statement,
    withStatement: with_statement,
  },
  simpleStatement: {
    assertStatement: assert_statement,
    deleteStatement: delete_statement,
    execStatement: exec_statement,
    expressionStatement: expression_statement,
    futureImportStatement: future_import_statement,
    globalStatement: global_statement,
    importFromStatement: import_from_statement,
    importStatement: import_statement,
    nonlocalStatement: nonlocal_statement,
    printStatement: print_statement,
    raiseStatement: raise_statement,
    returnStatement: return_statement,
    typeAliasStatement: type_alias_statement,
  },
  expression: {
    asPattern: as_pattern,
    booleanOperator: boolean_operator,
    comparisonOperator: comparison_operator,
    conditional: conditional_expression,
    named: named_expression,
    notOperator: not_operator,
  },
  parameter: {
    default: default_parameter,
    dictionarySplatPattern: dictionary_splat_pattern,
    listSplatPattern: list_splat_pattern,
    tuplePattern: tuple_pattern,
    typedDefault: typed_default_parameter,
    typed: typed_parameter,
  },
  pattern: {
    attribute: attribute,
    list: list_pattern,
    listSplat: list_splat_pattern,
    subscript: subscript,
    tuple: tuple_pattern,
  },
  primaryExpression: {
    attribute: attribute,
    binaryOperator: binary_operator,
    call: call,
    concatenatedString: concatenated_string,
    dictionary: dictionary,
    dictionaryComprehension: dictionary_comprehension,
    generatorExpression: generator_expression,
    list: list,
    listComprehension: list_comprehension,
    listSplat: list_splat,
    parenthesizedExpression: parenthesized_expression,
    set: set,
    setComprehension: set_comprehension,
    string: string,
    subscript: subscript,
    tuple: tuple,
    unaryOperator: unary_operator,
  },
} as const;

export type PythonIr = typeof ir;

import type { Builder, Edit, FieldKinds, FieldName, NodeTransform, NodeType } from '@sittir/types';
import type { PythonGrammar } from './types.js';

/** Tree-sitter CST node interface, parameterized by grammar node kind. */
export interface CSTNode<K extends keyof PythonGrammar = keyof PythonGrammar> {
  type: K;
  text: string;
  isNamed: boolean;
  startIndex: number;
  endIndex: number;
  namedChildren: CSTNode<keyof PythonGrammar>[];
  childForFieldName<F extends FieldName<PythonGrammar, K & string>>(name: F): CSTNode<FieldKinds<PythonGrammar, K & string, F> & keyof PythonGrammar> | null;
  childForFieldName(name: string): CSTNode<keyof PythonGrammar> | null;
  childrenForFieldName<F extends FieldName<PythonGrammar, K & string>>(name: F): CSTNode<FieldKinds<PythonGrammar, K & string, F> & keyof PythonGrammar>[];
  childrenForFieldName(name: string): CSTNode<keyof PythonGrammar>[];
}

/**
 * Hydrate a tree-sitter CST node into a builder tree.
 * Walks the CST recursively — no parsing needed.
 */
export function fromCST<K extends keyof PythonGrammar>(node: CSTNode<K>): Builder<NodeType<PythonGrammar, K>> {
  // Leaf / unknown nodes → LeafBuilder
  const builder = resolveBuilder(node);
  if (!builder) return new LeafBuilder(node.type, node.text) as any;
  return builder as any;
}

function resolveBuilder(node: CSTNode): Builder | null {
  switch (node.type) {
    case 'aliased_import': {
      const n = node as CSTNode<'aliased_import'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.aliasedImport(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const aliasChild = n.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild) as any);
      return b;
    }
    case 'argument_list': {
      const n = node as CSTNode<'argument_list'>;
      const b = ir.argumentList();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'as_pattern': {
      const n = node as CSTNode<'as_pattern'>;
      const b = ir.asPattern(...n.namedChildren.map(c => fromCST(c) as any));
      const aliasChild = n.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild) as any);
      return b;
    }
    case 'assert_statement': {
      const n = node as CSTNode<'assert_statement'>;
      const b = ir.assertStatement(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'assignment': {
      const n = node as CSTNode<'assignment'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.assignment(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      return b;
    }
    case 'attribute': {
      const n = node as CSTNode<'attribute'>;
      const ctorChild = n.childForFieldName('object');
      const b = ir.attribute(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('object', '') as any);
      const attributeChild = n.childForFieldName('attribute');
      if (attributeChild) b.attribute(fromCST(attributeChild) as any);
      return b;
    }
    case 'augmented_assignment': {
      const n = node as CSTNode<'augmented_assignment'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.augmentedAssignment(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'binary_operator': {
      const n = node as CSTNode<'binary_operator'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.binaryOperator(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'block': {
      const n = node as CSTNode<'block'>;
      const b = ir.block();
      const alternativeChildren = n.childrenForFieldName('alternative');
      if (alternativeChildren.length > 0) b.alternative(...alternativeChildren.map(c => fromCST(c) as any));
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['alternative'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'boolean_operator': {
      const n = node as CSTNode<'boolean_operator'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.booleanOperator(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'call': {
      const n = node as CSTNode<'call'>;
      const ctorChild = n.childForFieldName('function');
      const b = ir.call(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('function', '') as any);
      const argumentsChild = n.childForFieldName('arguments');
      if (argumentsChild) b.arguments(fromCST(argumentsChild) as any);
      return b;
    }
    case 'case_clause': {
      const n = node as CSTNode<'case_clause'>;
      const ctorChild = n.childForFieldName('consequence');
      const b = ir.caseClause(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('consequence', '') as any);
      const guardChild = n.childForFieldName('guard');
      if (guardChild) b.guard(fromCST(guardChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['consequence', 'guard'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'case_pattern': {
      const n = node as CSTNode<'case_pattern'>;
      const b = ir.casePattern();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'chevron': {
      const n = node as CSTNode<'chevron'>;
      const firstChild = n.namedChildren[0];
      const b = ir.chevron(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'class_definition': {
      const n = node as CSTNode<'class_definition'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.classDefinition(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      const superclassesChild = n.childForFieldName('superclasses');
      if (superclassesChild) b.superclasses(fromCST(superclassesChild) as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      return b;
    }
    case 'class_pattern': {
      const n = node as CSTNode<'class_pattern'>;
      const b = ir.classPattern(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'comparison_operator': {
      const n = node as CSTNode<'comparison_operator'>;
      const ctorChildren = n.childrenForFieldName('operators');
      const b = ir.comparisonOperator(...ctorChildren.map(c => fromCST(c) as any));
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['operators'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'complex_pattern': {
      const n = node as CSTNode<'complex_pattern'>;
      const b = ir.complexPattern(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'concatenated_string': {
      const n = node as CSTNode<'concatenated_string'>;
      const b = ir.concatenatedString(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'conditional_expression': {
      const n = node as CSTNode<'conditional_expression'>;
      const b = ir.conditionalExpression(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'constrained_type': {
      const n = node as CSTNode<'constrained_type'>;
      const b = ir.constrainedType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'decorated_definition': {
      const n = node as CSTNode<'decorated_definition'>;
      const ctorChild = n.childForFieldName('definition');
      const b = ir.decoratedDefinition(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('definition', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['definition'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'decorator': {
      const n = node as CSTNode<'decorator'>;
      const firstChild = n.namedChildren[0];
      const b = ir.decorator(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'default_parameter': {
      const n = node as CSTNode<'default_parameter'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.defaultParameter(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'delete_statement': {
      const n = node as CSTNode<'delete_statement'>;
      const firstChild = n.namedChildren[0];
      const b = ir.deleteStatement(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'dict_pattern': {
      const n = node as CSTNode<'dict_pattern'>;
      const b = ir.dictPattern();
      const keyChildren = n.childrenForFieldName('key');
      if (keyChildren.length > 0) b.key(...keyChildren.map(c => fromCST(c) as any));
      const valueChildren = n.childrenForFieldName('value');
      if (valueChildren.length > 0) b.value(...valueChildren.map(c => fromCST(c) as any));
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['key', 'value'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'dictionary': {
      const n = node as CSTNode<'dictionary'>;
      const b = ir.dictionary();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'dictionary_comprehension': {
      const n = node as CSTNode<'dictionary_comprehension'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.dictionaryComprehension(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'dictionary_splat': {
      const n = node as CSTNode<'dictionary_splat'>;
      const firstChild = n.namedChildren[0];
      const b = ir.dictionarySplat(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'dictionary_splat_pattern': {
      const n = node as CSTNode<'dictionary_splat_pattern'>;
      const firstChild = n.namedChildren[0];
      const b = ir.dictionarySplatPattern(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'dotted_name': {
      const n = node as CSTNode<'dotted_name'>;
      const b = ir.dottedName(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'elif_clause': {
      const n = node as CSTNode<'elif_clause'>;
      const ctorChild = n.childForFieldName('condition');
      const b = ir.elifClause(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('condition', '') as any);
      const consequenceChild = n.childForFieldName('consequence');
      if (consequenceChild) b.consequence(fromCST(consequenceChild) as any);
      return b;
    }
    case 'else_clause': {
      const n = node as CSTNode<'else_clause'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.elseClause(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      return b;
    }
    case 'except_clause': {
      const n = node as CSTNode<'except_clause'>;
      const firstChild = n.namedChildren[0];
      const b = ir.exceptClause(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      const aliasChild = n.childForFieldName('alias');
      if (aliasChild) b.alias(fromCST(aliasChild) as any);
      const valueChildren = n.childrenForFieldName('value');
      if (valueChildren.length > 0) b.value(...valueChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'exec_statement': {
      const n = node as CSTNode<'exec_statement'>;
      const ctorChild = n.childForFieldName('code');
      const b = ir.execStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('code', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['code'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'expression_list': {
      const n = node as CSTNode<'expression_list'>;
      const b = ir.expressionList(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'expression_statement': {
      const n = node as CSTNode<'expression_statement'>;
      const b = ir.expressionStatement(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'finally_clause': {
      const n = node as CSTNode<'finally_clause'>;
      const firstChild = n.namedChildren[0];
      const b = ir.finallyClause(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'for_in_clause': {
      const n = node as CSTNode<'for_in_clause'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.forInClause(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const rightChildren = n.childrenForFieldName('right');
      if (rightChildren.length > 0) b.right(...rightChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'for_statement': {
      const n = node as CSTNode<'for_statement'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.forStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const alternativeChild = n.childForFieldName('alternative');
      if (alternativeChild) b.alternative(fromCST(alternativeChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'format_expression': {
      const n = node as CSTNode<'format_expression'>;
      const ctorChild = n.childForFieldName('expression');
      const b = ir.formatExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('expression', '') as any);
      const formatSpecifierChild = n.childForFieldName('format_specifier');
      if (formatSpecifierChild) b.formatSpecifier(fromCST(formatSpecifierChild) as any);
      const typeConversionChild = n.childForFieldName('type_conversion');
      if (typeConversionChild) b.typeConversion(fromCST(typeConversionChild) as any);
      return b;
    }
    case 'format_specifier': {
      const n = node as CSTNode<'format_specifier'>;
      const b = ir.formatSpecifier();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'function_definition': {
      const n = node as CSTNode<'function_definition'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.functionDefinition(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      const parametersChild = n.childForFieldName('parameters');
      if (parametersChild) b.parameters(fromCST(parametersChild) as any);
      const returnTypeChild = n.childForFieldName('return_type');
      if (returnTypeChild) b.returnType(fromCST(returnTypeChild) as any);
      const typeParametersChild = n.childForFieldName('type_parameters');
      if (typeParametersChild) b.typeParameters(fromCST(typeParametersChild) as any);
      return b;
    }
    case 'future_import_statement': {
      const n = node as CSTNode<'future_import_statement'>;
      const ctorChildren = n.childrenForFieldName('name');
      const b = ir.futureImportStatement(...ctorChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'generator_expression': {
      const n = node as CSTNode<'generator_expression'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.generatorExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'generic_type': {
      const n = node as CSTNode<'generic_type'>;
      const b = ir.genericType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'global_statement': {
      const n = node as CSTNode<'global_statement'>;
      const b = ir.globalStatement(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'if_clause': {
      const n = node as CSTNode<'if_clause'>;
      const firstChild = n.namedChildren[0];
      const b = ir.ifClause(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'if_statement': {
      const n = node as CSTNode<'if_statement'>;
      const ctorChild = n.childForFieldName('condition');
      const b = ir.ifStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('condition', '') as any);
      const alternativeChildren = n.childrenForFieldName('alternative');
      if (alternativeChildren.length > 0) b.alternative(...alternativeChildren.map(c => fromCST(c) as any));
      const consequenceChild = n.childForFieldName('consequence');
      if (consequenceChild) b.consequence(fromCST(consequenceChild) as any);
      return b;
    }
    case 'import_from_statement': {
      const n = node as CSTNode<'import_from_statement'>;
      const ctorChild = n.childForFieldName('module_name');
      const b = ir.importFromStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('module_name', '') as any);
      const nameChildren = n.childrenForFieldName('name');
      if (nameChildren.length > 0) b.name(...nameChildren.map(c => fromCST(c) as any));
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['module_name', 'name'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'import_statement': {
      const n = node as CSTNode<'import_statement'>;
      const ctorChildren = n.childrenForFieldName('name');
      const b = ir.importStatement(...ctorChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'interpolation': {
      const n = node as CSTNode<'interpolation'>;
      const ctorChild = n.childForFieldName('expression');
      const b = ir.interpolation(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('expression', '') as any);
      const formatSpecifierChild = n.childForFieldName('format_specifier');
      if (formatSpecifierChild) b.formatSpecifier(fromCST(formatSpecifierChild) as any);
      const typeConversionChild = n.childForFieldName('type_conversion');
      if (typeConversionChild) b.typeConversion(fromCST(typeConversionChild) as any);
      return b;
    }
    case 'keyword_argument': {
      const n = node as CSTNode<'keyword_argument'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.keywordArgument(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'keyword_pattern': {
      const n = node as CSTNode<'keyword_pattern'>;
      const b = ir.keywordPattern(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'lambda_parameters': {
      const n = node as CSTNode<'lambda_parameters'>;
      const b = ir.lambdaParameters(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'list': {
      const n = node as CSTNode<'list'>;
      const b = ir.list();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'list_comprehension': {
      const n = node as CSTNode<'list_comprehension'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.listComprehension(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'list_pattern': {
      const n = node as CSTNode<'list_pattern'>;
      const b = ir.listPattern();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'list_splat': {
      const n = node as CSTNode<'list_splat'>;
      const firstChild = n.namedChildren[0];
      const b = ir.listSplat(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'list_splat_pattern': {
      const n = node as CSTNode<'list_splat_pattern'>;
      const firstChild = n.namedChildren[0];
      const b = ir.listSplatPattern(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'match_statement': {
      const n = node as CSTNode<'match_statement'>;
      const ctorChildren = n.childrenForFieldName('subject');
      const b = ir.matchStatement(...ctorChildren.map(c => fromCST(c) as any));
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'member_type': {
      const n = node as CSTNode<'member_type'>;
      const b = ir.memberType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'module': {
      const n = node as CSTNode<'module'>;
      const b = ir.module();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'named_expression': {
      const n = node as CSTNode<'named_expression'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.namedExpression(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'nonlocal_statement': {
      const n = node as CSTNode<'nonlocal_statement'>;
      const b = ir.nonlocalStatement(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'not_operator': {
      const n = node as CSTNode<'not_operator'>;
      const ctorChild = n.childForFieldName('argument');
      const b = ir.notOperator(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('argument', '') as any);
      return b;
    }
    case 'pair': {
      const n = node as CSTNode<'pair'>;
      const ctorChild = n.childForFieldName('key');
      const b = ir.pair(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('key', '') as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'parameters': {
      const n = node as CSTNode<'parameters'>;
      const b = ir.parameters();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'parenthesized_expression': {
      const n = node as CSTNode<'parenthesized_expression'>;
      const firstChild = n.namedChildren[0];
      const b = ir.parenthesizedExpression(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'parenthesized_list_splat': {
      const n = node as CSTNode<'parenthesized_list_splat'>;
      const firstChild = n.namedChildren[0];
      const b = ir.parenthesizedListSplat(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'pattern_list': {
      const n = node as CSTNode<'pattern_list'>;
      const b = ir.patternList(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'print_statement': {
      const n = node as CSTNode<'print_statement'>;
      const b = ir.printStatement();
      const argumentChildren = n.childrenForFieldName('argument');
      if (argumentChildren.length > 0) b.argument(...argumentChildren.map(c => fromCST(c) as any));
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['argument'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'raise_statement': {
      const n = node as CSTNode<'raise_statement'>;
      const b = ir.raiseStatement();
      const causeChild = n.childForFieldName('cause');
      if (causeChild) b.cause(fromCST(causeChild) as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['cause'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'relative_import': {
      const n = node as CSTNode<'relative_import'>;
      const b = ir.relativeImport(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'return_statement': {
      const n = node as CSTNode<'return_statement'>;
      const b = ir.returnStatement();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'set': {
      const n = node as CSTNode<'set'>;
      const b = ir.set(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'set_comprehension': {
      const n = node as CSTNode<'set_comprehension'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.setComprehension(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'slice': {
      const n = node as CSTNode<'slice'>;
      const b = ir.slice();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'splat_pattern': {
      const n = node as CSTNode<'splat_pattern'>;
      const b = ir.splatPattern();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'splat_type': {
      const n = node as CSTNode<'splat_type'>;
      const firstChild = n.namedChildren[0];
      const b = ir.splatType(firstChild ? fromCST(firstChild) as any : new LeafBuilder('unknown', '') as any);
      return b;
    }
    case 'string': {
      const n = node as CSTNode<'string'>;
      const b = ir.string(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'string_content': {
      const n = node as CSTNode<'string_content'>;
      const b = ir.stringContent();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'subscript': {
      const n = node as CSTNode<'subscript'>;
      const ctorChild = n.childForFieldName('value');
      const b = ir.subscript(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('value', '') as any);
      const subscriptChildren = n.childrenForFieldName('subscript');
      if (subscriptChildren.length > 0) b.subscript(...subscriptChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'try_statement': {
      const n = node as CSTNode<'try_statement'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.tryStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'tuple': {
      const n = node as CSTNode<'tuple'>;
      const b = ir.tuple();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'tuple_pattern': {
      const n = node as CSTNode<'tuple_pattern'>;
      const b = ir.tuplePattern();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'type_alias_statement': {
      const n = node as CSTNode<'type_alias_statement'>;
      const ctorChild = n.childForFieldName('left');
      const b = ir.typeAliasStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('left', '') as any);
      const rightChild = n.childForFieldName('right');
      if (rightChild) b.right(fromCST(rightChild) as any);
      return b;
    }
    case 'type_parameter': {
      const n = node as CSTNode<'type_parameter'>;
      const b = ir.typeParameter(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'typed_default_parameter': {
      const n = node as CSTNode<'typed_default_parameter'>;
      const ctorChild = n.childForFieldName('name');
      const b = ir.typedDefaultParameter(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('name', '') as any);
      const typeChild = n.childForFieldName('type');
      if (typeChild) b.type(fromCST(typeChild) as any);
      const valueChild = n.childForFieldName('value');
      if (valueChild) b.value(fromCST(valueChild) as any);
      return b;
    }
    case 'typed_parameter': {
      const n = node as CSTNode<'typed_parameter'>;
      const ctorChild = n.childForFieldName('type');
      const b = ir.typedParameter(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('type', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['type'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'unary_operator': {
      const n = node as CSTNode<'unary_operator'>;
      const ctorChild = n.childForFieldName('argument');
      const b = ir.unaryOperator(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('argument', '') as any);
      const operatorChild = n.childForFieldName('operator');
      if (operatorChild) b.operator(fromCST(operatorChild) as any);
      return b;
    }
    case 'union_pattern': {
      const n = node as CSTNode<'union_pattern'>;
      const b = ir.unionPattern();
      const remainingChildren = n.namedChildren.filter(c => {
        return true;
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'union_type': {
      const n = node as CSTNode<'union_type'>;
      const b = ir.unionType(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'while_statement': {
      const n = node as CSTNode<'while_statement'>;
      const ctorChild = n.childForFieldName('condition');
      const b = ir.whileStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('condition', '') as any);
      const alternativeChild = n.childForFieldName('alternative');
      if (alternativeChild) b.alternative(fromCST(alternativeChild) as any);
      const bodyChild = n.childForFieldName('body');
      if (bodyChild) b.body(fromCST(bodyChild) as any);
      return b;
    }
    case 'with_clause': {
      const n = node as CSTNode<'with_clause'>;
      const b = ir.withClause(...n.namedChildren.map(c => fromCST(c) as any));
      return b;
    }
    case 'with_item': {
      const n = node as CSTNode<'with_item'>;
      const ctorChild = n.childForFieldName('value');
      const b = ir.with(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('value', '') as any);
      return b;
    }
    case 'with_statement': {
      const n = node as CSTNode<'with_statement'>;
      const ctorChild = n.childForFieldName('body');
      const b = ir.withStatement(ctorChild ? fromCST(ctorChild) as any : new LeafBuilder('body', '') as any);
      const remainingChildren = n.namedChildren.filter(c => {
        const fieldNames = ['body'];
        return !fieldNames.some(fn => n.childForFieldName(fn) === c);
      });
      if (remainingChildren.length > 0) b.children(...remainingChildren.map(c => fromCST(c) as any));
      return b;
    }
    default: return null;
  }
}

/**
 * Create a codemod-compatible Edit from a tree-sitter node.
 * Hydrates the node into a builder, passes it to the transform,
 * then renders the result and wraps it with byte positions.
 */
export function edit<K extends keyof PythonGrammar>(
  node: CSTNode<K>,
  transform: NodeTransform<NodeType<PythonGrammar, K>>,
): Edit {
  const builder = fromCST(node);
  const result = transform(builder);
  return {
    startPos: node.startIndex,
    endPos: node.endIndex,
    insertedText: result.renderImpl(),
  };
}

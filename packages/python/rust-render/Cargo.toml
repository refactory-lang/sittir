# @generated scaffold — spec 012 T005 placeholder. T027/T028/T031 will
# overwrite `src/` with codegen output. This Cargo.toml is hand-written
# until T031 emits the real manifest.
[package]
name = "sittir-python-render"
version = "0.0.0"
edition = "2021"
rust-version = "1.82"
license = "MIT"
repository = "https://github.com/refactory-lang/sittir"
description = "Generated render dispatch for the Python grammar — codegen output from packages/python/node-model.json5 and packages/python/templates/*.jinja."

[dependencies]
sittir-core = { path = "../../../rust/crates/sittir-core" }
askama = { workspace = true }
serde = { workspace = true }

// Auto-generated by @sittir/codegen — do not edit
import { describe, it, expect } from 'vitest';
import { ir } from '../src/ir.js';

describe('module', () => {
  it('factory produces correct type', () => {
    const node = ir.module();
    expect(node.type).toBe('module');
  });
});

describe('_simple_statements', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenSimpleStatements();
    expect(node.type).toBe('_simple_statements');
  });
});

describe('import_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.importStatement();
    expect(node.type).toBe('import_statement');
  });
});

describe('import_prefix', () => {
  it('factory produces correct type', () => {
    const node = ir.importPrefix('test');
    expect(node.type).toBe('import_prefix');
    expect(node.text).toBe('test');
  });
});

describe('relative_import', () => {
  it('factory produces correct type', () => {
    const node = ir.relativeImport({ importPrefix: { type: 'import_prefix', text: 'test' } as any, dottedName: { type: 'dotted_name', text: 'test' } as any });
    expect(node.type).toBe('relative_import');
  });
  it('render produces non-empty string', () => {
    const node = ir.relativeImport({ importPrefix: { type: 'import_prefix', text: 'test' } as any, dottedName: { type: 'dotted_name', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('future_import_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.futureImportStatement();
    expect(node.type).toBe('future_import_statement');
  });
});

describe('import_from_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.importFromStatement({ wildcardImport: 'test' as any, moduleName: { type: 'relative_import', text: 'test' } as any });
    expect(node.type).toBe('import_from_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.importFromStatement({ wildcardImport: 'test' as any, moduleName: { type: 'relative_import', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('_import_list', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenImportList({ name: [] });
    expect(node.type).toBe('_import_list');
  });
  it('render produces non-empty string', () => {
    const node = ir.hiddenImportList({ name: [] });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('aliased_import', () => {
  it('factory produces correct type', () => {
    const node = ir.aliasedImport({ name: { type: 'dotted_name', text: 'test' } as any, alias: { type: 'identifier', text: 'test' } as any });
    expect(node.type).toBe('aliased_import');
  });
  it('render produces non-empty string', () => {
    const node = ir.aliasedImport({ name: { type: 'dotted_name', text: 'test' } as any, alias: { type: 'identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('print_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.printStatement({ chevron: 'test' as any, argument: [] });
    expect(node.type).toBe('print_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.printStatement({ chevron: 'test' as any, argument: [] });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('chevron', () => {
  it('factory produces correct type', () => {
    const node = ir.chevron({ expression: 'test' as any });
    expect(node.type).toBe('chevron');
  });
  it('render produces non-empty string', () => {
    const node = ir.chevron({ expression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('assert_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.assertStatement();
    expect(node.type).toBe('assert_statement');
  });
});

describe('expression_statement', () => {
  it('expression form produces correct type', () => {
    const node = ir.expressionStatement.expression({});
    expect(node.type).toBe('expression_statement');
  });
  it('expression2 form produces correct type', () => {
    const node = ir.expressionStatement.expression2({});
    expect(node.type).toBe('expression_statement');
  });
  it('assignment form produces correct type', () => {
    const node = ir.expressionStatement.assignment({});
    expect(node.type).toBe('expression_statement');
  });
  it('augmented_assignment form produces correct type', () => {
    const node = ir.expressionStatement.augmented_assignment({});
    expect(node.type).toBe('expression_statement');
  });
  it('yield form produces correct type', () => {
    const node = ir.expressionStatement.yield({});
    expect(node.type).toBe('expression_statement');
  });
});

describe('named_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.namedExpression({ name: { type: '_named_expression_lhs', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('named_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.namedExpression({ name: { type: '_named_expression_lhs', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('return_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.returnStatement();
    expect(node.type).toBe('return_statement');
  });
});

describe('delete_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.deleteStatement();
    expect(node.type).toBe('delete_statement');
  });
});

describe('raise_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.raiseStatement({});
    expect(node.type).toBe('raise_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.raiseStatement({});
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('pass_statement', () => {
  it('factory produces keyword', () => {
    const node = ir.passStatement();
    expect(node.type).toBe('pass_statement');
    expect(node.text).toBe('pass');
  });
});

describe('break_statement', () => {
  it('factory produces keyword', () => {
    const node = ir.breakStatement();
    expect(node.type).toBe('break_statement');
    expect(node.text).toBe('break');
  });
});

describe('continue_statement', () => {
  it('factory produces keyword', () => {
    const node = ir.continueStatement();
    expect(node.type).toBe('continue_statement');
    expect(node.text).toBe('continue');
  });
});

describe('if_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.ifStatement({ condition: { type: 'expression', text: 'test' } as any, consequence: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('if_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.ifStatement({ condition: { type: 'expression', text: 'test' } as any, consequence: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('elif_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.elifClause({ condition: { type: 'expression', text: 'test' } as any, consequence: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('elif_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.elifClause({ condition: { type: 'expression', text: 'test' } as any, consequence: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('else_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.elseClause({ body: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('else_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.elseClause({ body: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('match_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.matchStatement({ subject: [], body: { type: '_match_block', text: 'test' } as any });
    expect(node.type).toBe('match_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.matchStatement({ subject: [], body: { type: '_match_block', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('case_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.caseClause({ consequence: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('case_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.caseClause({ consequence: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('for_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.forStatement({ left: { type: '_left_hand_side', text: 'test' } as any, right: { type: '_expressions', text: 'test' } as any, body: { type: '_suite', text: 'test' } as any, alternative: { type: 'else_clause', text: 'test' } as any });
    expect(node.type).toBe('for_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.forStatement({ left: { type: '_left_hand_side', text: 'test' } as any, right: { type: '_expressions', text: 'test' } as any, body: { type: '_suite', text: 'test' } as any, alternative: { type: 'else_clause', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('while_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.whileStatement({ condition: { type: 'expression', text: 'test' } as any, body: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('while_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.whileStatement({ condition: { type: 'expression', text: 'test' } as any, body: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('try_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.tryStatement({ exceptClauses: 'test' as any, elseClause: 'test' as any, finallyClause: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('try_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.tryStatement({ exceptClauses: 'test' as any, elseClause: 'test' as any, finallyClause: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('except_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.exceptClause({});
    expect(node.type).toBe('except_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.exceptClause({});
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('finally_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.finallyClause({ block: 'test' as any });
    expect(node.type).toBe('finally_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.finallyClause({ block: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('with_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.withStatement({ withClause: 'test' as any, body: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('with_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.withStatement({ withClause: 'test' as any, body: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('with_clause', () => {
  it('with_item form produces correct type', () => {
    const node = ir.withClause.with_item({});
    expect(node.type).toBe('with_clause');
  });
  it('paren form produces correct type', () => {
    const node = ir.withClause.paren({});
    expect(node.type).toBe('with_clause');
  });
});

describe('with_item', () => {
  it('factory produces correct type', () => {
    const node = ir.withItem({ value: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('with_item');
  });
  it('render produces non-empty string', () => {
    const node = ir.withItem({ value: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('function_definition', () => {
  it('factory produces correct type', () => {
    const node = ir.functionDefinition({ name: { type: 'identifier', text: 'test' } as any, typeParameters: { type: 'type_parameter', text: 'test' } as any, parameters: { type: 'parameters', text: 'test' } as any, body: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('function_definition');
  });
  it('render produces non-empty string', () => {
    const node = ir.functionDefinition({ name: { type: 'identifier', text: 'test' } as any, typeParameters: { type: 'type_parameter', text: 'test' } as any, parameters: { type: 'parameters', text: 'test' } as any, body: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('parameters', () => {
  it('factory produces correct type', () => {
    const node = ir.parameters();
    expect(node.type).toBe('parameters');
  });
});

describe('lambda_parameters', () => {
  it('factory produces correct type', () => {
    const node = ir.lambdaParameters();
    expect(node.type).toBe('lambda_parameters');
  });
});

describe('list_splat', () => {
  it('factory produces correct type', () => {
    const node = ir.listSplat({ expression: 'test' as any });
    expect(node.type).toBe('list_splat');
  });
  it('render produces non-empty string', () => {
    const node = ir.listSplat({ expression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('dictionary_splat', () => {
  it('factory produces correct type', () => {
    const node = ir.dictionarySplat({ expression: 'test' as any });
    expect(node.type).toBe('dictionary_splat');
  });
  it('render produces non-empty string', () => {
    const node = ir.dictionarySplat({ expression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('global_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.globalStatement();
    expect(node.type).toBe('global_statement');
  });
});

describe('nonlocal_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.nonlocalStatement();
    expect(node.type).toBe('nonlocal_statement');
  });
});

describe('exec_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.execStatement({ code: { type: 'string', text: 'test' } as any });
    expect(node.type).toBe('exec_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.execStatement({ code: { type: 'string', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('type_alias_statement', () => {
  it('factory produces correct type', () => {
    const node = ir.typeAliasStatement({ left: { type: 'type', text: 'test' } as any, right: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('type_alias_statement');
  });
  it('render produces non-empty string', () => {
    const node = ir.typeAliasStatement({ left: { type: 'type', text: 'test' } as any, right: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('class_definition', () => {
  it('factory produces correct type', () => {
    const node = ir.classDefinition({ name: { type: 'identifier', text: 'test' } as any, typeParameters: { type: 'type_parameter', text: 'test' } as any, superclasses: { type: 'argument_list', text: 'test' } as any, body: { type: '_suite', text: 'test' } as any });
    expect(node.type).toBe('class_definition');
  });
  it('render produces non-empty string', () => {
    const node = ir.classDefinition({ name: { type: 'identifier', text: 'test' } as any, typeParameters: { type: 'type_parameter', text: 'test' } as any, superclasses: { type: 'argument_list', text: 'test' } as any, body: { type: '_suite', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('type_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.typeParameter();
    expect(node.type).toBe('type_parameter');
  });
});

describe('parenthesized_list_splat', () => {
  it('factory produces correct type', () => {
    const node = ir.parenthesizedListSplat();
    expect(node.type).toBe('parenthesized_list_splat');
  });
});

describe('argument_list', () => {
  it('factory produces correct type', () => {
    const node = ir.argumentList();
    expect(node.type).toBe('argument_list');
  });
});

describe('decorated_definition', () => {
  it('factory produces correct type', () => {
    const node = ir.decoratedDefinition({ definition: { type: 'class_definition', text: 'test' } as any });
    expect(node.type).toBe('decorated_definition');
  });
  it('render produces non-empty string', () => {
    const node = ir.decoratedDefinition({ definition: { type: 'class_definition', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('decorator', () => {
  it('factory produces correct type', () => {
    const node = ir.decorator({ expression: 'test' as any, newline: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('decorator');
  });
  it('render produces non-empty string', () => {
    const node = ir.decorator({ expression: 'test' as any, newline: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('block', () => {
  it('factory produces correct type', () => {
    const node = ir.block();
    expect(node.type).toBe('block');
  });
});

describe('expression_list', () => {
  it('factory produces correct type', () => {
    const node = ir.expressionList();
    expect(node.type).toBe('expression_list');
  });
});

describe('dotted_name', () => {
  it('factory produces correct type', () => {
    const node = ir.dottedName();
    expect(node.type).toBe('dotted_name');
  });
});

describe('case_pattern', () => {
  it('_as_pattern form produces correct type', () => {
    const node = ir.casePattern._as_pattern({});
    expect(node.type).toBe('case_pattern');
  });
  it('keyword_pattern form produces correct type', () => {
    const node = ir.casePattern.keyword_pattern({});
    expect(node.type).toBe('case_pattern');
  });
  it('_simple_pattern form produces correct type', () => {
    const node = ir.casePattern._simple_pattern({});
    expect(node.type).toBe('case_pattern');
  });
});

describe('_as_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenAsPattern();
    expect(node.type).toBe('_as_pattern');
  });
});

describe('union_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.unionPattern();
    expect(node.type).toBe('union_pattern');
  });
});

describe('_list_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenListPattern();
    expect(node.type).toBe('_list_pattern');
  });
});

describe('_tuple_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenTuplePattern();
    expect(node.type).toBe('_tuple_pattern');
  });
});

describe('dict_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.dictPattern();
    expect(node.type).toBe('dict_pattern');
  });
});

describe('keyword_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.keywordPattern({ identifier: { type: 'identifier', text: 'test' } as any, simplePattern: 'test' as any });
    expect(node.type).toBe('keyword_pattern');
  });
  it('render produces non-empty string', () => {
    const node = ir.keywordPattern({ identifier: { type: 'identifier', text: 'test' } as any, simplePattern: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('splat_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.splatPattern({ identifier: { type: '*', text: 'test' } as any });
    expect(node.type).toBe('splat_pattern');
  });
  it('render produces non-empty string', () => {
    const node = ir.splatPattern({ identifier: { type: '*', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('class_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.classPattern({ dottedName: { type: 'dotted_name', text: 'test' } as any, arguments: 'test' as any });
    expect(node.type).toBe('class_pattern');
  });
  it('render produces non-empty string', () => {
    const node = ir.classPattern({ dottedName: { type: 'dotted_name', text: 'test' } as any, arguments: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('complex_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.complexPattern({ real: 'test' as any, imaginary: { type: 'integer', text: 'test' } as any });
    expect(node.type).toBe('complex_pattern');
  });
  it('render produces non-empty string', () => {
    const node = ir.complexPattern({ real: 'test' as any, imaginary: { type: 'integer', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('_parameters', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenParameters();
    expect(node.type).toBe('_parameters');
  });
});

describe('_patterns', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenPatterns();
    expect(node.type).toBe('_patterns');
  });
});

describe('parameter', () => {
  it('identifier form produces correct type', () => {
    const node = ir.parameter.identifier({});
    expect(node.type).toBe('parameter');
  });
  it('typed_parameter form produces correct type', () => {
    const node = ir.parameter.typed_parameter({});
    expect(node.type).toBe('parameter');
  });
  it('default_parameter form produces correct type', () => {
    const node = ir.parameter.default_parameter({});
    expect(node.type).toBe('parameter');
  });
  it('typed_default_parameter form produces correct type', () => {
    const node = ir.parameter.typed_default_parameter({});
    expect(node.type).toBe('parameter');
  });
  it('list_splat_pattern form produces correct type', () => {
    const node = ir.parameter.list_splat_pattern({});
    expect(node.type).toBe('parameter');
  });
  it('tuple_pattern form produces correct type', () => {
    const node = ir.parameter.tuple_pattern({});
    expect(node.type).toBe('parameter');
  });
  it('keyword_separator form produces correct type', () => {
    const node = ir.parameter.keyword_separator({});
    expect(node.type).toBe('parameter');
  });
  it('positional_separator form produces correct type', () => {
    const node = ir.parameter.positional_separator({});
    expect(node.type).toBe('parameter');
  });
  it('dictionary_splat_pattern form produces correct type', () => {
    const node = ir.parameter.dictionary_splat_pattern({});
    expect(node.type).toBe('parameter');
  });
});

describe('pattern', () => {
  it('identifier form produces correct type', () => {
    const node = ir.pattern.identifier({});
    expect(node.type).toBe('pattern');
  });
  it('keyword_identifier form produces correct type', () => {
    const node = ir.pattern.keyword_identifier({});
    expect(node.type).toBe('pattern');
  });
  it('subscript form produces correct type', () => {
    const node = ir.pattern.subscript({});
    expect(node.type).toBe('pattern');
  });
  it('attribute form produces correct type', () => {
    const node = ir.pattern.attribute({});
    expect(node.type).toBe('pattern');
  });
  it('list_splat_pattern form produces correct type', () => {
    const node = ir.pattern.list_splat_pattern({});
    expect(node.type).toBe('pattern');
  });
  it('tuple_pattern form produces correct type', () => {
    const node = ir.pattern.tuple_pattern({});
    expect(node.type).toBe('pattern');
  });
  it('list_pattern form produces correct type', () => {
    const node = ir.pattern.list_pattern({});
    expect(node.type).toBe('pattern');
  });
});

describe('tuple_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.tuplePattern();
    expect(node.type).toBe('tuple_pattern');
  });
});

describe('list_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.listPattern();
    expect(node.type).toBe('list_pattern');
  });
});

describe('default_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.defaultParameter({ name: { type: 'identifier', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('default_parameter');
  });
  it('render produces non-empty string', () => {
    const node = ir.defaultParameter({ name: { type: 'identifier', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('typed_default_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.typedDefaultParameter({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('typed_default_parameter');
  });
  it('render produces non-empty string', () => {
    const node = ir.typedDefaultParameter({ name: { type: 'identifier', text: 'test' } as any, type: { type: 'type', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('list_splat_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.listSplatPattern();
    expect(node.type).toBe('list_splat_pattern');
  });
});

describe('dictionary_splat_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.dictionarySplatPattern();
    expect(node.type).toBe('dictionary_splat_pattern');
  });
});

describe('as_pattern', () => {
  it('factory produces correct type', () => {
    const node = ir.asPattern({ expression: { type: 'expression', text: 'test' } as any, alias: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('as_pattern');
  });
  it('render produces non-empty string', () => {
    const node = ir.asPattern({ expression: { type: 'expression', text: 'test' } as any, alias: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('expression', () => {
  it('comparison_operator form produces correct type', () => {
    const node = ir.expression.comparison_operator({});
    expect(node.type).toBe('expression');
  });
  it('not_operator form produces correct type', () => {
    const node = ir.expression.not_operator({});
    expect(node.type).toBe('expression');
  });
  it('boolean_operator form produces correct type', () => {
    const node = ir.expression.boolean_operator({});
    expect(node.type).toBe('expression');
  });
  it('lambda form produces correct type', () => {
    const node = ir.expression.lambda({});
    expect(node.type).toBe('expression');
  });
  it('primary_expression form produces correct type', () => {
    const node = ir.expression.primary_expression({});
    expect(node.type).toBe('expression');
  });
  it('conditional_expression form produces correct type', () => {
    const node = ir.expression.conditional_expression({});
    expect(node.type).toBe('expression');
  });
  it('named_expression form produces correct type', () => {
    const node = ir.expression.named_expression({});
    expect(node.type).toBe('expression');
  });
  it('as_pattern form produces correct type', () => {
    const node = ir.expression.as_pattern({});
    expect(node.type).toBe('expression');
  });
});

describe('primary_expression', () => {
  it('await form produces correct type', () => {
    const node = ir.primaryExpression.await({});
    expect(node.type).toBe('primary_expression');
  });
  it('binary_operator form produces correct type', () => {
    const node = ir.primaryExpression.binary_operator({});
    expect(node.type).toBe('primary_expression');
  });
  it('identifier form produces correct type', () => {
    const node = ir.primaryExpression.identifier({});
    expect(node.type).toBe('primary_expression');
  });
  it('keyword_identifier form produces correct type', () => {
    const node = ir.primaryExpression.keyword_identifier({});
    expect(node.type).toBe('primary_expression');
  });
  it('string form produces correct type', () => {
    const node = ir.primaryExpression.string({});
    expect(node.type).toBe('primary_expression');
  });
  it('concatenated_string form produces correct type', () => {
    const node = ir.primaryExpression.concatenated_string({});
    expect(node.type).toBe('primary_expression');
  });
  it('integer form produces correct type', () => {
    const node = ir.primaryExpression.integer({});
    expect(node.type).toBe('primary_expression');
  });
  it('float form produces correct type', () => {
    const node = ir.primaryExpression.float({});
    expect(node.type).toBe('primary_expression');
  });
  it('true form produces correct type', () => {
    const node = ir.primaryExpression.true({});
    expect(node.type).toBe('primary_expression');
  });
  it('false form produces correct type', () => {
    const node = ir.primaryExpression.false({});
    expect(node.type).toBe('primary_expression');
  });
  it('none form produces correct type', () => {
    const node = ir.primaryExpression.none({});
    expect(node.type).toBe('primary_expression');
  });
  it('unary_operator form produces correct type', () => {
    const node = ir.primaryExpression.unary_operator({});
    expect(node.type).toBe('primary_expression');
  });
  it('attribute form produces correct type', () => {
    const node = ir.primaryExpression.attribute({});
    expect(node.type).toBe('primary_expression');
  });
  it('subscript form produces correct type', () => {
    const node = ir.primaryExpression.subscript({});
    expect(node.type).toBe('primary_expression');
  });
  it('call form produces correct type', () => {
    const node = ir.primaryExpression.call({});
    expect(node.type).toBe('primary_expression');
  });
  it('list form produces correct type', () => {
    const node = ir.primaryExpression.list({});
    expect(node.type).toBe('primary_expression');
  });
  it('list_comprehension form produces correct type', () => {
    const node = ir.primaryExpression.list_comprehension({});
    expect(node.type).toBe('primary_expression');
  });
  it('dictionary form produces correct type', () => {
    const node = ir.primaryExpression.dictionary({});
    expect(node.type).toBe('primary_expression');
  });
  it('dictionary_comprehension form produces correct type', () => {
    const node = ir.primaryExpression.dictionary_comprehension({});
    expect(node.type).toBe('primary_expression');
  });
  it('set form produces correct type', () => {
    const node = ir.primaryExpression.set({});
    expect(node.type).toBe('primary_expression');
  });
  it('set_comprehension form produces correct type', () => {
    const node = ir.primaryExpression.set_comprehension({});
    expect(node.type).toBe('primary_expression');
  });
  it('tuple form produces correct type', () => {
    const node = ir.primaryExpression.tuple({});
    expect(node.type).toBe('primary_expression');
  });
  it('parenthesized_expression form produces correct type', () => {
    const node = ir.primaryExpression.parenthesized_expression({});
    expect(node.type).toBe('primary_expression');
  });
  it('generator_expression form produces correct type', () => {
    const node = ir.primaryExpression.generator_expression({});
    expect(node.type).toBe('primary_expression');
  });
  it('ellipsis form produces correct type', () => {
    const node = ir.primaryExpression.ellipsis({});
    expect(node.type).toBe('primary_expression');
  });
  it('list_splat_pattern form produces correct type', () => {
    const node = ir.primaryExpression.list_splat_pattern({});
    expect(node.type).toBe('primary_expression');
  });
});

describe('not_operator', () => {
  it('factory produces correct type', () => {
    const node = ir.notOperator({ argument: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('not_operator');
  });
  it('render produces non-empty string', () => {
    const node = ir.notOperator({ argument: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('boolean_operator', () => {
  it('factory produces correct type', () => {
    const node = ir.booleanOperator({ left: { type: 'expression', text: 'test' } as any, operator: 'test' as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('boolean_operator');
  });
  it('render produces non-empty string', () => {
    const node = ir.booleanOperator({ left: { type: 'expression', text: 'test' } as any, operator: 'test' as any, right: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('binary_operator', () => {
  it('factory produces correct type', () => {
    const node = ir.binaryOperator({ left: { type: 'primary_expression', text: 'test' } as any, operator: 'test' as any, right: { type: 'primary_expression', text: 'test' } as any });
    expect(node.type).toBe('binary_operator');
  });
  it('render produces non-empty string', () => {
    const node = ir.binaryOperator({ left: { type: 'primary_expression', text: 'test' } as any, operator: 'test' as any, right: { type: 'primary_expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('unary_operator', () => {
  it('factory produces correct type', () => {
    const node = ir.unaryOperator({ operator: { type: '+', text: 'test' } as any, argument: { type: 'primary_expression', text: 'test' } as any });
    expect(node.type).toBe('unary_operator');
  });
  it('render produces non-empty string', () => {
    const node = ir.unaryOperator({ operator: { type: '+', text: 'test' } as any, argument: { type: 'primary_expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('_not_in', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenNotIn('test');
    expect(node.type).toBe('_not_in');
    expect(node.text).toBe('test');
  });
});

describe('_is_not', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenIsNot('test');
    expect(node.type).toBe('_is_not');
    expect(node.text).toBe('test');
  });
});

describe('comparison_operator', () => {
  it('factory produces correct type', () => {
    const node = ir.comparisonOperator({ left: { type: 'primary_expression', text: 'test' } as any, comparators: 'test' as any });
    expect(node.type).toBe('comparison_operator');
  });
  it('render produces non-empty string', () => {
    const node = ir.comparisonOperator({ left: { type: 'primary_expression', text: 'test' } as any, comparators: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('lambda', () => {
  it('factory produces correct type', () => {
    const node = ir.lambda({ parameters: { type: 'lambda_parameters', text: 'test' } as any, body: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('lambda');
  });
  it('render produces non-empty string', () => {
    const node = ir.lambda({ parameters: { type: 'lambda_parameters', text: 'test' } as any, body: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('lambda_within_for_in_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.lambdaWithinForInClause({ parameters: { type: 'lambda_parameters', text: 'test' } as any, body: { type: '_expression_within_for_in_clause', text: 'test' } as any });
    expect(node.type).toBe('lambda_within_for_in_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.lambdaWithinForInClause({ parameters: { type: 'lambda_parameters', text: 'test' } as any, body: { type: '_expression_within_for_in_clause', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('assignment', () => {
  it('factory produces correct type', () => {
    const node = ir.assignment({ left: { type: '_left_hand_side', text: 'test' } as any, right: { type: '_right_hand_side', text: 'test' } as any, type: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('assignment');
  });
  it('render produces non-empty string', () => {
    const node = ir.assignment({ left: { type: '_left_hand_side', text: 'test' } as any, right: { type: '_right_hand_side', text: 'test' } as any, type: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('augmented_assignment', () => {
  it('factory produces correct type', () => {
    const node = ir.augmentedAssignment({ left: { type: '_left_hand_side', text: 'test' } as any, operator: { type: '+=', text: 'test' } as any, right: { type: '_right_hand_side', text: 'test' } as any });
    expect(node.type).toBe('augmented_assignment');
  });
  it('render produces non-empty string', () => {
    const node = ir.augmentedAssignment({ left: { type: '_left_hand_side', text: 'test' } as any, operator: { type: '+=', text: 'test' } as any, right: { type: '_right_hand_side', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('pattern_list', () => {
  it('factory produces correct type', () => {
    const node = ir.patternList();
    expect(node.type).toBe('pattern_list');
  });
});

describe('yield', () => {
  it('factory produces correct type', () => {
    const node = ir.yield_();
    expect(node.type).toBe('yield');
  });
});

describe('attribute', () => {
  it('factory produces correct type', () => {
    const node = ir.attribute({ object: { type: 'primary_expression', text: 'test' } as any, attribute: { type: 'identifier', text: 'test' } as any });
    expect(node.type).toBe('attribute');
  });
  it('render produces non-empty string', () => {
    const node = ir.attribute({ object: { type: 'primary_expression', text: 'test' } as any, attribute: { type: 'identifier', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('subscript', () => {
  it('factory produces correct type', () => {
    const node = ir.subscript({ value: { type: 'primary_expression', text: 'test' } as any, subscript: [] });
    expect(node.type).toBe('subscript');
  });
  it('render produces non-empty string', () => {
    const node = ir.subscript({ value: { type: 'primary_expression', text: 'test' } as any, subscript: [] });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('slice', () => {
  it('factory produces correct type', () => {
    const node = ir.slice({ start: { type: 'expression', text: 'test' } as any, stop: 'test' as any, step: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('slice');
  });
  it('render produces non-empty string', () => {
    const node = ir.slice({ start: { type: 'expression', text: 'test' } as any, stop: 'test' as any, step: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('call', () => {
  it('factory produces correct type', () => {
    const node = ir.call({ function: { type: 'primary_expression', text: 'test' } as any, arguments: { type: 'generator_expression', text: 'test' } as any });
    expect(node.type).toBe('call');
  });
  it('render produces non-empty string', () => {
    const node = ir.call({ function: { type: 'primary_expression', text: 'test' } as any, arguments: { type: 'generator_expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('typed_parameter', () => {
  it('factory produces correct type', () => {
    const node = ir.typedParameter({ type: { type: 'type', text: 'test' } as any });
    expect(node.type).toBe('typed_parameter');
  });
  it('render produces non-empty string', () => {
    const node = ir.typedParameter({ type: { type: 'type', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('type', () => {
  it('expression form produces correct type', () => {
    const node = ir.type.expression({});
    expect(node.type).toBe('type');
  });
  it('splat_type form produces correct type', () => {
    const node = ir.type.splat_type({});
    expect(node.type).toBe('type');
  });
  it('generic_type form produces correct type', () => {
    const node = ir.type.generic_type({});
    expect(node.type).toBe('type');
  });
  it('union_type form produces correct type', () => {
    const node = ir.type.union_type({});
    expect(node.type).toBe('type');
  });
  it('constrained_type form produces correct type', () => {
    const node = ir.type.constrained_type({});
    expect(node.type).toBe('type');
  });
  it('member_type form produces correct type', () => {
    const node = ir.type.member_type({});
    expect(node.type).toBe('type');
  });
});

describe('splat_type', () => {
  it('factory produces correct type', () => {
    const node = ir.splatType({ identifier: { type: '*', text: 'test' } as any });
    expect(node.type).toBe('splat_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.splatType({ identifier: { type: '*', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('generic_type', () => {
  it('factory produces correct type', () => {
    const node = ir.genericType({ identifier: { type: 'identifier', text: 'test' } as any, typeParameter: { type: 'type_parameter', text: 'test' } as any });
    expect(node.type).toBe('generic_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.genericType({ identifier: { type: 'identifier', text: 'test' } as any, typeParameter: { type: 'type_parameter', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('union_type', () => {
  it('factory produces correct type', () => {
    const node = ir.unionType({ left: { type: 'type', text: 'test' } as any, right: 'test' as any });
    expect(node.type).toBe('union_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.unionType({ left: { type: 'type', text: 'test' } as any, right: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('constrained_type', () => {
  it('factory produces correct type', () => {
    const node = ir.constrainedType({ baseType: { type: 'type', text: 'test' } as any, constraint: 'test' as any });
    expect(node.type).toBe('constrained_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.constrainedType({ baseType: { type: 'type', text: 'test' } as any, constraint: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('member_type', () => {
  it('factory produces correct type', () => {
    const node = ir.memberType({ baseType: { type: 'type', text: 'test' } as any, identifier: 'test' as any });
    expect(node.type).toBe('member_type');
  });
  it('render produces non-empty string', () => {
    const node = ir.memberType({ baseType: { type: 'type', text: 'test' } as any, identifier: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('keyword_argument', () => {
  it('factory produces correct type', () => {
    const node = ir.keywordArgument({ name: { type: 'identifier', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('keyword_argument');
  });
  it('render produces non-empty string', () => {
    const node = ir.keywordArgument({ name: { type: 'identifier', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('list', () => {
  it('factory produces correct type', () => {
    const node = ir.list();
    expect(node.type).toBe('list');
  });
});

describe('set', () => {
  it('factory produces correct type', () => {
    const node = ir.set();
    expect(node.type).toBe('set');
  });
});

describe('tuple', () => {
  it('factory produces correct type', () => {
    const node = ir.tuple();
    expect(node.type).toBe('tuple');
  });
});

describe('dictionary', () => {
  it('factory produces correct type', () => {
    const node = ir.dictionary();
    expect(node.type).toBe('dictionary');
  });
});

describe('pair', () => {
  it('factory produces correct type', () => {
    const node = ir.pair({ key: { type: 'expression', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('pair');
  });
  it('render produces non-empty string', () => {
    const node = ir.pair({ key: { type: 'expression', text: 'test' } as any, value: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('list_comprehension', () => {
  it('factory produces correct type', () => {
    const node = ir.listComprehension({ body: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('list_comprehension');
  });
  it('render produces non-empty string', () => {
    const node = ir.listComprehension({ body: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('dictionary_comprehension', () => {
  it('factory produces correct type', () => {
    const node = ir.dictionaryComprehension({ body: { type: 'pair', text: 'test' } as any });
    expect(node.type).toBe('dictionary_comprehension');
  });
  it('render produces non-empty string', () => {
    const node = ir.dictionaryComprehension({ body: { type: 'pair', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('set_comprehension', () => {
  it('factory produces correct type', () => {
    const node = ir.setComprehension({ body: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('set_comprehension');
  });
  it('render produces non-empty string', () => {
    const node = ir.setComprehension({ body: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('generator_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.generatorExpression({ body: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('generator_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.generatorExpression({ body: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('_comprehension_clauses', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenComprehensionClauses();
    expect(node.type).toBe('_comprehension_clauses');
  });
});

describe('parenthesized_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.parenthesizedExpression();
    expect(node.type).toBe('parenthesized_expression');
  });
});

describe('_collection_elements', () => {
  it('factory produces correct type', () => {
    const node = ir.hiddenCollectionElements();
    expect(node.type).toBe('_collection_elements');
  });
});

describe('for_in_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.forInClause({ left: { type: '_left_hand_side', text: 'test' } as any, right: 'test' as any });
    expect(node.type).toBe('for_in_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.forInClause({ left: { type: '_left_hand_side', text: 'test' } as any, right: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('if_clause', () => {
  it('factory produces correct type', () => {
    const node = ir.ifClause({ expression: 'test' as any });
    expect(node.type).toBe('if_clause');
  });
  it('render produces non-empty string', () => {
    const node = ir.ifClause({ expression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('conditional_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.conditionalExpression({ body: { type: 'expression', text: 'test' } as any, condition: 'test' as any, alternative: { type: 'expression', text: 'test' } as any });
    expect(node.type).toBe('conditional_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.conditionalExpression({ body: { type: 'expression', text: 'test' } as any, condition: 'test' as any, alternative: { type: 'expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('concatenated_string', () => {
  it('factory produces correct type', () => {
    const node = ir.concatenatedString();
    expect(node.type).toBe('concatenated_string');
  });
});

describe('string', () => {
  it('factory produces correct type', () => {
    const node = ir.string({ stringStart: { type: 'string_start', text: 'test' } as any, content: 'test' as any, stringEnd: { type: 'string_end', text: 'test' } as any });
    expect(node.type).toBe('string');
  });
  it('render produces non-empty string', () => {
    const node = ir.string({ stringStart: { type: 'string_start', text: 'test' } as any, content: 'test' as any, stringEnd: { type: 'string_end', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('string_content', () => {
  it('factory produces correct type', () => {
    const node = ir.stringContent();
    expect(node.type).toBe('string_content');
  });
});

describe('interpolation', () => {
  it('factory produces correct type', () => {
    const node = ir.interpolation({ expression: { type: '_f_expression', text: 'test' } as any });
    expect(node.type).toBe('interpolation');
  });
  it('render produces non-empty string', () => {
    const node = ir.interpolation({ expression: { type: '_f_expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('escape_sequence', () => {
  it('factory produces correct type', () => {
    const node = ir.escapeSequence('test');
    expect(node.type).toBe('escape_sequence');
    expect(node.text).toBe('test');
  });
});

describe('format_specifier', () => {
  it('factory produces correct type', () => {
    const node = ir.formatSpecifier();
    expect(node.type).toBe('format_specifier');
  });
});

describe('type_conversion', () => {
  it('factory produces correct type', () => {
    const node = ir.typeConversion('test');
    expect(node.type).toBe('type_conversion');
    expect(node.text).toBe('test');
  });
});

describe('integer', () => {
  it('factory produces correct type', () => {
    const node = ir.integer('test');
    expect(node.type).toBe('integer');
    expect(node.text).toBe('test');
  });
});

describe('float', () => {
  it('factory produces correct type', () => {
    const node = ir.float('test');
    expect(node.type).toBe('float');
    expect(node.text).toBe('test');
  });
});

describe('identifier', () => {
  it('factory produces correct type', () => {
    const node = ir.identifier('test');
    expect(node.type).toBe('identifier');
    expect(node.text).toBe('test');
  });
});

describe('keyword_identifier', () => {
  it('factory produces correct type', () => {
    const node = ir.keywordIdentifier('test');
    expect(node.type).toBe('keyword_identifier');
    expect(node.text).toBe('test');
  });
});

describe('true', () => {
  it('factory produces keyword', () => {
    const node = ir.true_();
    expect(node.type).toBe('true');
    expect(node.text).toBe('True');
  });
});

describe('false', () => {
  it('factory produces keyword', () => {
    const node = ir.false_();
    expect(node.type).toBe('false');
    expect(node.text).toBe('False');
  });
});

describe('none', () => {
  it('factory produces keyword', () => {
    const node = ir.none();
    expect(node.type).toBe('none');
    expect(node.text).toBe('None');
  });
});

describe('await', () => {
  it('factory produces correct type', () => {
    const node = ir.await_({ primaryExpression: 'test' as any });
    expect(node.type).toBe('await');
  });
  it('render produces non-empty string', () => {
    const node = ir.await_({ primaryExpression: 'test' as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

describe('comment', () => {
  it('factory produces correct type', () => {
    const node = ir.comment('test');
    expect(node.type).toBe('comment');
    expect(node.text).toBe('test');
  });
});

describe('line_continuation', () => {
  it('factory produces correct type', () => {
    const node = ir.lineContinuation('test');
    expect(node.type).toBe('line_continuation');
    expect(node.text).toBe('test');
  });
});

describe('as_pattern_target', () => {
  it('comparison_operator form produces correct type', () => {
    const node = ir.asPatternTarget.comparison_operator({});
    expect(node.type).toBe('as_pattern_target');
  });
  it('not_operator form produces correct type', () => {
    const node = ir.asPatternTarget.not_operator({});
    expect(node.type).toBe('as_pattern_target');
  });
  it('boolean_operator form produces correct type', () => {
    const node = ir.asPatternTarget.boolean_operator({});
    expect(node.type).toBe('as_pattern_target');
  });
  it('lambda form produces correct type', () => {
    const node = ir.asPatternTarget.lambda({});
    expect(node.type).toBe('as_pattern_target');
  });
  it('primary_expression form produces correct type', () => {
    const node = ir.asPatternTarget.primary_expression({});
    expect(node.type).toBe('as_pattern_target');
  });
  it('conditional_expression form produces correct type', () => {
    const node = ir.asPatternTarget.conditional_expression({});
    expect(node.type).toBe('as_pattern_target');
  });
  it('named_expression form produces correct type', () => {
    const node = ir.asPatternTarget.named_expression({});
    expect(node.type).toBe('as_pattern_target');
  });
  it('as_pattern form produces correct type', () => {
    const node = ir.asPatternTarget.as_pattern({});
    expect(node.type).toBe('as_pattern_target');
  });
});

describe('format_expression', () => {
  it('factory produces correct type', () => {
    const node = ir.formatExpression({ expression: { type: '_f_expression', text: 'test' } as any });
    expect(node.type).toBe('format_expression');
  });
  it('render produces non-empty string', () => {
    const node = ir.formatExpression({ expression: { type: '_f_expression', text: 'test' } as any });
    expect(node.render().length).toBeGreaterThan(0);
  });
});

# Auto-generated by @sittir/codegen — do not edit
language: rust
extensions:
  - rs
expandoChar: null
metadata:
  grammarSha: ""
rules:
  abstract_type:
    template: impl $TYPE_PARAMETERS_CLAUSE $TRAIT $$$CHILDREN
    type_parameters_clause: for
  arguments:
    template: ($$$CHILDREN)
    joinBy: ","
  array_expression:
    template: "[$$$ATTRIBUTE_ITEM $$$CHILDREN ; $LENGTH] $$$NEEDS_NAME_0 $$$NEEDS_NAME_1"
    joinBy: ","
  array_type:
    template: "[$ELEMENT $LENGTH_CLAUSE]"
    length_clause: ; $LENGTH
  assignment_expression: $LEFT = $RIGHT
  associated_type: type $NAME $TYPE_PARAMETERS $BOUNDS $CHILDREN ;
  async_block: async $CHILDREN
  attribute: $CHILDREN $VALUE $ARGUMENTS
  attribute_item: "#[$CHILDREN]"
  await_expression: $CHILDREN.await
  base_field_initializer: .. $CHILDREN
  binary_expression: $LEFT $OPERATOR $RIGHT
  block:
    template: $LABEL_CLAUSE{$$$STATEMENT $$$CHILDREN} $LABEL $$$STATEMENT $EXPRESSION
    label_clause: ":"
  block_comment: /* $OUTER $DOC */ $INNER
  bounded_type: $$$LIFETIME + $$$CHILDREN $NEEDS_NAME_0 $NEEDS_NAME_1
  bracketed_type: <$CHILDREN>
  break_expression: break $LABEL $$$CHILDREN $LABEL $EXPRESSION
  call_expression: $FUNCTION $ARGUMENTS
  captured_pattern: $IDENTIFIER @ $$$CHILDREN $IDENTIFIER $PATTERN
  closure_expression:
    template: $PARAMETERS $RETURN_TYPE_CLAUSE $BODY
    return_type_clause: -> $RETURN_TYPE
  closure_parameters:
    template: "| $$$CHILDREN |"
    joinBy: ","
  compound_assignment_expr: $LEFT $OPERATOR $RIGHT
  const_block: const $BODY
  const_item:
    template: "$CHILDREN const $NAME : $TYPE $VALUE_CLAUSE ;"
    value_clause: = $VALUE
  const_parameter:
    template: "const $NAME : $TYPE $VALUE_CLAUSE"
    value_clause: = $VALUE
  continue_expression: continue $CHILDREN
  declaration_list: "{$$$CHILDREN}"
  dynamic_type: dyn $TRAIT
  else_clause: else $CHILDREN
  enum_item: $VISIBILITY_MODIFIER enum $NAME $TYPE_PARAMETERS $WHERE_CLAUSE $BODY $VISIBILITY_MODIFIER $WHERE_CLAUSE $$$CHILDREN
  enum_variant:
    template: $CHILDREN $NAME $BODY $VALUE_CLAUSE
    value_clause: = $VALUE
  enum_variant_list:
    template: "{$$$CHILDREN}"
    joinBy: ","
  expression_statement: $CHILDREN ;
  extern_crate_declaration:
    template: $VISIBILITY_MODIFIER extern $CRATE $NAME $ALIAS_CLAUSE ; $VISIBILITY_MODIFIER $CRATE $$$CHILDREN
    alias_clause: as $ALIAS
  extern_modifier: extern $CHILDREN
  field_declaration: "$CHILDREN $NAME : $TYPE"
  field_declaration_list:
    template: "{$$$CHILDREN}"
    joinBy: ","
  field_expression: $VALUE.$FIELD
  field_initializer: "$$$CHILDREN $FIELD : $VALUE"
  field_initializer_list:
    template: "{$$$CHILDREN}"
    joinBy: ","
  field_pattern: "$CHILDREN $NAME : $PATTERN"
  for_expression:
    template: $LABEL_CLAUSE for $PATTERN in $VALUE $BODY $$$CHILDREN
    label_clause: ":"
  for_lifetimes:
    template: for<$$$CHILDREN ,>
    joinBy: ","
  foreign_mod_item: $VISIBILITY_MODIFIER $EXTERN_MODIFIER $BODY $VISIBILITY_MODIFIER $EXTERN_MODIFIER $$$CHILDREN
  function_item:
    template: $VISIBILITY_MODIFIER $FUNCTION_MODIFIERS fn $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE_CLAUSE $WHERE_CLAUSE $BODY $VISIBILITY_MODIFIER $FUNCTION_MODIFIERS $WHERE_CLAUSE $$$CHILDREN
    return_type_clause: -> $RETURN_TYPE
  function_modifiers: $$$CHILDREN
  function_signature_item:
    template: $VISIBILITY_MODIFIER $FUNCTION_MODIFIERS fn $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE_CLAUSE $WHERE_CLAUSE ; $VISIBILITY_MODIFIER $FUNCTION_MODIFIERS $WHERE_CLAUSE $$$CHILDREN
    return_type_clause: -> $RETURN_TYPE
  function_type:
    template: $FOR_LIFETIMES $TRAIT $PARAMETERS $RETURN_TYPE_CLAUSE $FOR_LIFETIMES $FUNCTION_MODIFIERS $$$CHILDREN
    return_type_clause: -> $RETURN_TYPE
  gen_block: gen $CHILDREN
  generic_function: $FUNCTION::$TYPE_ARGUMENTS
  generic_pattern: $CHILDREN::$TYPE_ARGUMENTS
  generic_type: $TYPE $TYPE_ARGUMENTS
  generic_type_with_turbofish: $TYPE::$TYPE_ARGUMENTS
  higher_ranked_trait_bound: for $TYPE_PARAMETERS $TYPE
  if_expression: if $CONDITION $CONSEQUENCE $ALTERNATIVE
  impl_item: impl $TYPE_PARAMETERS $TRAIT $TYPE $CHILDREN $BODY
  index_expression: $$$CHILDREN[] $NEEDS_NAME_0 $NEEDS_NAME_1
  inner_attribute_item: "# ![$CHILDREN]"
  label: "' $CHILDREN"
  let_chain: $$$CHILDREN
  let_condition: let $PATTERN = $VALUE
  let_declaration:
    template: let $CHILDREN $PATTERN $TYPE_CLAUSE $VALUE_CLAUSE $ALTERNATIVE_CLAUSE ;
    type_clause: ": $TYPE"
    value_clause: = $VALUE
    alternative_clause: else $ALTERNATIVE
  lifetime: "' $CHILDREN"
  lifetime_parameter: $NAME $BOUNDS
  line_comment: // $OUTER $DOC $INNER
  loop_expression:
    template: $LABEL_CLAUSE loop $BODY $$$CHILDREN
    label_clause: ":"
  macro_definition:
    template: macro_rules! $NAME($$$MACRO_RULE ;) ; $$$NEEDS_NAME_0 $NEEDS_NAME_1 $$$CHILDREN
    joinBy: ;
  macro_invocation: $MACRO ! $CHILDREN $$$CHILDREN
  macro_rule: $LEFT => $RIGHT
  match_arm: $$$CHILDREN $PATTERN => $VALUE ,
  match_block: "{$$$CHILDREN}"
  match_expression: match $VALUE $BODY
  match_pattern:
    template: $CHILDREN $CONDITION_CLAUSE
    condition_clause: if $CONDITION
  mod_item: $CHILDREN mod $NAME $BODY
  mut_pattern: $MUTABLE_SPECIFIER $$$CHILDREN $MUTABLE_SPECIFIER $PATTERN
  negative_literal: "- $CHILDREN"
  or_pattern: $$$CHILDREN | $NEEDS_NAME_0 $NEEDS_NAME_1
  ordered_field_declaration_list:
    template: ($$$ATTRIBUTE_ITEM $$$VISIBILITY_MODIFIER $$$TYPE) $$$NEEDS_NAME_0 $NEEDS_NAME_1 $$$NEEDS_NAME_2 $$$CHILDREN
    joinBy: ","
  parameter: "$CHILDREN $PATTERN : $TYPE"
  parameters:
    template: ($$$CHILDREN)
    joinBy: ","
  parenthesized_expression: ($CHILDREN)
  pointer_type: "* $CHILDREN $TYPE"
  qualified_type: $TYPE as $ALIAS
  range_expression: $$$CHILDREN .. $NEEDS_NAME_0 $NEEDS_NAME_1
  range_pattern: $LEFT ... $RIGHT
  raw_string_literal: $$$CHILDREN $STRING_CONTENT $RAW_STRING_LITERAL_START $STRING_CONTENT $RAW_STRING_LITERAL_END
  ref_pattern: ref $CHILDREN
  reference_expression: "& raw $CHILDREN $VALUE"
  reference_pattern: "& $MUTABLE_SPECIFIER $$$CHILDREN $MUTABLE_SPECIFIER $PATTERN"
  reference_type: "& $LIFETIME $MUTABLE_SPECIFIER $TYPE $LIFETIME $MUTABLE_SPECIFIER $$$CHILDREN"
  removed_trait_bound: "? $CHILDREN"
  return_expression: return $CHILDREN
  scoped_identifier: $PATH::$NAME
  scoped_type_identifier: $PATH::$NAME
  scoped_use_list: $PATH::$LIST
  self_parameter: $LIFETIME $MUTABLE_SPECIFIER $SELF $LIFETIME $MUTABLE_SPECIFIER $SELF $$$CHILDREN
  shorthand_field_initializer: $$$ATTRIBUTE_ITEM $IDENTIFIER $$$ATTRIBUTE_ITEM $IDENTIFIER $$$CHILDREN
  slice_pattern:
    template: "[$$$CHILDREN]"
    joinBy: ","
  source_file: $SHEBANG $$$STATEMENT $SHEBANG $$$STATEMENT $$$CHILDREN
  static_item:
    template: "$VISIBILITY_MODIFIER static $MUTABLE_SPECIFIER $NAME : $TYPE $VALUE_CLAUSE ; $VISIBILITY_MODIFIER $MUTABLE_SPECIFIER $$$CHILDREN"
    value_clause: = $VALUE
  string_literal: '" $$$CHILDREN "'
  struct_expression: $NAME $BODY
  struct_item: $VISIBILITY_MODIFIER struct $NAME $TYPE_PARAMETERS $WHERE_CLAUSE $BODY $VISIBILITY_MODIFIER $WHERE_CLAUSE $$$CHILDREN
  struct_pattern:
    template: $TYPE{$$$CHILDREN}
    joinBy: ","
  token_binding_pattern: "$NAME : $TYPE"
  token_repetition: $($$$CHILDREN) +
  token_repetition_pattern: $($$$CHILDREN) +
  token_tree: ($$$CHILDREN)
  token_tree_pattern: ($$$CHILDREN)
  trait_bounds: ": $$$CHILDREN +"
  trait_item: $VISIBILITY_MODIFIER trait $NAME $TYPE_PARAMETERS $BOUNDS $WHERE_CLAUSE $BODY $VISIBILITY_MODIFIER $WHERE_CLAUSE $$$CHILDREN
  try_block: try $CHILDREN
  try_expression: $CHILDREN ?
  tuple_expression:
    template: ($$$ATTRIBUTE_ITEM $$$CHILDREN , ,) $$$ATTRIBUTE_ITEM $NEEDS_NAME_1 $$$NEEDS_NAME_2 $NEEDS_NAME_3
    joinBy: ","
  tuple_pattern:
    template: ($$$CHILDREN)
    joinBy: ","
  tuple_struct_pattern:
    template: $TYPE($$$CHILDREN)
    joinBy: ","
  tuple_type:
    template: ($$$CHILDREN ,)
    joinBy: ","
  type_arguments:
    template: <$$$CHILDREN ,>
    joinBy: ","
  type_binding: $NAME $TYPE_ARGUMENTS = $TYPE
  type_cast_expression: $VALUE as $TYPE
  type_item: $VISIBILITY_MODIFIER type $NAME $TYPE_PARAMETERS $$$WHERE_CLAUSE = $TYPE ; $VISIBILITY_MODIFIER $NEEDS_NAME_1 $NEEDS_NAME_2 $$$CHILDREN
  type_parameter:
    template: $NAME $BOUNDS $DEFAULT_TYPE_CLAUSE
    default_type_clause: = $DEFAULT_TYPE
  type_parameters:
    template: <$$$CHILDREN ,>
    joinBy: ","
  unary_expression: "- $CHILDREN"
  union_item: $VISIBILITY_MODIFIER union $NAME $TYPE_PARAMETERS $WHERE_CLAUSE $BODY $VISIBILITY_MODIFIER $WHERE_CLAUSE $$$CHILDREN
  unsafe_block: unsafe $CHILDREN
  use_as_clause: $PATH as $ALIAS
  use_bounds:
    template: use<$$$CHILDREN>
    joinBy: ","
  use_declaration: $CHILDREN use $ARGUMENT ;
  use_list: "{$$$CHILDREN}"
  use_wildcard: $$$CHILDREN *
  variadic_parameter:
    template: $CHILDREN $PATTERN_CLAUSE ...
    pattern_clause: "$PATTERN :"
  visibility_modifier: $CHILDREN
  where_clause:
    template: where $$$CHILDREN
    joinBy: ","
  where_predicate: $LEFT $BOUNDS
  while_expression:
    template: $LABEL_CLAUSE while $CONDITION $BODY $$$CHILDREN
    label_clause: ":"
  yield_expression: yield $CHILDREN

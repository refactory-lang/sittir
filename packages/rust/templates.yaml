# Auto-generated by @sittir/codegen — do not edit
grammar: rust
grammarSha: ""
rules:
  source_file:
    template: $SHEBANG $$$STATEMENTS
    joinByField:
      statements: "\n"
  expression_statement: $$$CHILDREN;
  macro_definition:
    template: macro_rules!$NAME $RULES
    joinBy: ";"
  macro_rule: $LEFT=>$RIGHT
  token_tree_pattern: ($$$CHILDREN)
  token_binding_pattern: "$NAME:$TYPE"
  token_repetition_pattern: $($$$CHILDREN)+
  token_tree: ($$$CHILDREN)
  token_repetition: $($$$CHILDREN)+
  attribute_item: "#[$ATTRIBUTE]"
  inner_attribute_item: "#![$ATTRIBUTE]"
  attribute: $$$CHILDREN=$VALUE$ARGUMENTS
  mod_item: $VISIBILITY_MODIFIER mod $NAME;$BODY
  foreign_mod_item: $VISIBILITY_MODIFIER $EXTERN_MODIFIER;$BODY
  declaration_list: "{$$$CHILDREN}"
  struct_item: $VISIBILITY_MODIFIER struct $NAME $TYPE_PARAMETERS $$$CHILDREN $BODY
  union_item: $VISIBILITY_MODIFIER union $NAME $TYPE_PARAMETERS $WHERE_CLAUSE $BODY
  enum_item: $VISIBILITY_MODIFIER enum $NAME $TYPE_PARAMETERS $WHERE_CLAUSE $BODY
  enum_variant_list:
    template: "{$$$CHILDREN}"
    joinBy: ","
  enum_variant:
    template: $VISIBILITY_MODIFIER $NAME $BODY $VALUE_CLAUSE
    value_clause: =$VALUE
  field_declaration_list:
    template: "{$$$CHILDREN}"
    joinBy: ","
  field_declaration: "$VISIBILITY_MODIFIER $NAME:$TYPE"
  ordered_field_declaration_list:
    template: ($$$CHILDREN $$$TYPE)
    joinBy: ","
  extern_crate_declaration:
    template: $VISIBILITY_MODIFIER extern $CRATE $NAME $ALIAS_CLAUSE;
    alias_clause: as $ALIAS
  const_item:
    template: "$VISIBILITY_MODIFIER const $NAME:$TYPE $VALUE_CLAUSE;"
    value_clause: =$VALUE
  static_item:
    template: "$VISIBILITY_MODIFIER static $MUTABLE_SPECIFIER $$$CHILDREN $NAME:$TYPE $VALUE_CLAUSE;"
    value_clause: =$VALUE
  type_item: $VISIBILITY_MODIFIER type $NAME $TYPE_PARAMETERS $WHERE_CLAUSE=$TYPE $TRAILING_WHERE_CLAUSE;
  function_item:
    template: $VISIBILITY_MODIFIER $FUNCTION_MODIFIERS fn $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE_CLAUSE $WHERE_CLAUSE $BODY
    return_type_clause: "->$RETURN_TYPE"
  function_signature_item:
    template: $VISIBILITY_MODIFIER $FUNCTION_MODIFIERS fn $NAME $TYPE_PARAMETERS $PARAMETERS $RETURN_TYPE_CLAUSE $WHERE_CLAUSE;
    return_type_clause: "->$RETURN_TYPE"
  function_modifiers: $$$ASYNC$$$DEFAULT$$$CONST$$$UNSAFE$$$CHILDREN
  where_clause:
    template: $WHERE $$$CHILDREN
    joinBy: ","
  where_predicate: $LEFT $BOUNDS
  impl_item:
    template: $UNSAFE impl $TYPE_PARAMETERS $TRAIT_CLAUSE $TYPE $$$CHILDREN $BODY
    trait_clause: $TRAIT for
  trait_item:
    template: $VISIBILITY_MODIFIER $UNSAFE_CLAUSE trait $NAME $TYPE_PARAMETERS $BOUNDS $$$CHILDREN $BODY
    unsafe_clause: $UNSAFE
  associated_type: type $NAME $TYPE_PARAMETERS $BOUNDS $WHERE_CLAUSE;
  trait_bounds:
    template: ":$$$CHILDREN"
    joinBy: +
  higher_ranked_trait_bound: $FOR $TYPE_PARAMETERS $TYPE
  removed_trait_bound: "?$$$CHILDREN"
  type_parameters:
    template: <$$$CHILDREN>
    joinBy: ","
  const_parameter:
    template: "$CONST $NAME:$TYPE $VALUE_CLAUSE"
    value_clause: =$VALUE
  type_parameter:
    template: $NAME $BOUNDS $DEFAULT_TYPE_CLAUSE
    default_type_clause: =$DEFAULT_TYPE
  lifetime_parameter: $NAME $BOUNDS
  let_declaration:
    template: let $MUTABLE_SPECIFIER $PATTERN $TYPE_CLAUSE $VALUE_CLAUSE $ALTERNATIVE_CLAUSE;
    type_clause: ":$TYPE"
    value_clause: =$VALUE
    alternative_clause: else $ALTERNATIVE
  use_declaration: $VISIBILITY_MODIFIER use $ARGUMENT;
  scoped_use_list: "$PATH::$LIST"
  use_list:
    template: "{$$$CHILDREN}"
    joinBy: ","
  use_as_clause: $PATH as $ALIAS
  use_wildcard:
    template: $PATH_CLAUSE*
    path_clause: "$PATH::"
  parameters:
    template: ($$$CHILDREN)
    joinBy: ","
  self_parameter: $LIFETIME $MUTABLE_SPECIFIER $SELF $$$CHILDREN
  variadic_parameter:
    template: $MUTABLE_SPECIFIER $PATTERN_CLAUSE...
    pattern_clause: "$PATTERN:"
  parameter: "$MUTABLE_SPECIFIER $PATTERN:$TYPE"
  extern_modifier: extern $STRING_LITERAL
  visibility_modifier:
    variants:
      form0: $$$CHILDREN
      form1: $PUB($$$CHILDREN$IN)
  bracketed_type: <$$$CHILDREN>
  qualified_type: $TYPE as $ALIAS
  lifetime: "'$IDENTIFIER"
  array_type:
    template: "[$ELEMENT $LENGTH_CLAUSE]"
    length_clause: ;$LENGTH
  for_lifetimes:
    template: $FOR<$$$CHILDREN>
    joinBy: ","
  function_type:
    template: $FOR_LIFETIMES $TRAIT$$$CHILDREN $PARAMETERS $RETURN_TYPE_CLAUSE
    return_type_clause: "->$RETURN_TYPE"
  tuple_type:
    template: ($$$CHILDREN)
    joinBy: ","
  generic_function: "$FUNCTION::$TYPE_ARGUMENTS"
  generic_type: $TYPE $TYPE_ARGUMENTS
  generic_type_with_turbofish: "$TYPE::$TYPE_ARGUMENTS"
  bounded_type: $LEFT+$RIGHT
  use_bounds:
    template: $USE<$$$CHILDREN>
    joinBy: ","
  type_arguments:
    template: <$$$CHILDREN>
    joinBy: ","
  type_binding: $NAME $TYPE_ARGUMENTS=$TYPE
  reference_type: "&$LIFETIME $MUTABLE_SPECIFIER $TYPE"
  pointer_type: "*$MUTABLE_SPECIFIER $TYPE"
  abstract_type:
    template: impl $TYPE_PARAMETERS_CLAUSE $TRAIT
    type_parameters_clause: for$TYPE_PARAMETERS
  dynamic_type: $DYN $TRAIT
  macro_invocation: $MACRO!$TOKEN_TREE
  delim_token_tree: ($$$CHILDREN)
  scoped_identifier: "$PATH::$NAME"
  scoped_type_identifier_in_expression_position: "$PATH::$NAME"
  scoped_type_identifier: "$PATH::$NAME"
  range_expression:
    variants:
      binary: $$$CHILDREN
      postfix: $$$CHILDREN
      prefix: $$$CHILDREN
      bare: $$$CHILDREN
  unary_expression: $OPERATOR $OPERAND
  try_expression: $VALUE?
  reference_expression: "&$MUTABLE_SPECIFIER $VALUE"
  binary_expression: $LEFT $OPERATOR $RIGHT
  assignment_expression: $LEFT=$RIGHT
  compound_assignment_expr: $LEFT $OPERATOR $RIGHT
  type_cast_expression: $VALUE as $TYPE
  return_expression: return $$$CHILDREN
  yield_expression: yield $$$CHILDREN
  call_expression: $FUNCTION $ARGUMENTS
  arguments:
    template: ($$$CHILDREN)
    joinBy: ","
  array_expression:
    template: "[$$$ATTRIBUTES $ELEMENTS]"
    joinBy: ","
    joinByField:
      attributes: "\n"
  parenthesized_expression: ($$$CHILDREN)
  tuple_expression:
    template: ($$$ATTRIBUTES $FIRST,$$$REST $TRAILING)
    joinBy: ","
    joinByField:
      attributes: "\n"
      rest: ","
  struct_expression: $NAME $BODY
  field_initializer_list:
    template: "{$$$CHILDREN}"
    joinBy: ","
  shorthand_field_initializer:
    template: $$$ATTRIBUTES $IDENTIFIER
    joinByField:
      attributes: "\n"
  field_initializer: "$$$CHILDREN $FIELD:$VALUE"
  base_field_initializer: ..$$$CHILDREN
  if_expression: $IF $CONDITION $CONSEQUENCE $ALTERNATIVE
  let_condition: $LET $PATTERN=$VALUE
  else_clause: $ELSE $$$CHILDREN
  match_expression: $MATCH $VALUE $BODY
  match_block: "{$$$CHILDREN}"
  match_arm: $$$CHILDREN $PATTERN=>$VALUE,
  last_match_arm: $$$CHILDREN $PATTERN=>$VALUE
  match_pattern:
    template: $$$CHILDREN $CONDITION_CLAUSE
    condition_clause: if $CONDITION
  while_expression:
    template: $LABEL_CLAUSE while $CONDITION $BODY
    label_clause: "$LABEL:"
  loop_expression:
    template: $LABEL_CLAUSE loop $BODY
    label_clause: "$LABEL:"
  for_expression:
    template: $LABEL_CLAUSE for $PATTERN in $VALUE $BODY
    label_clause: "$LABEL:"
  const_block: $CONST $BODY
  closure_expression:
    variants:
      block: $STATIC $ASYNC $MOVE $PARAMETERS $$$CHILDREN
      expr: $STATIC $ASYNC $MOVE $PARAMETERS $$$CHILDREN
  closure_parameters:
    template: "|$$$CHILDREN|"
    joinBy: ","
  label: "'$IDENTIFIER"
  break_expression: break $LABEL $EXPRESSION
  continue_expression: continue $LABEL
  index_expression: "$OBJECT[$INDEX]"
  await_expression: $$$CHILDREN.await
  field_expression: $VALUE.$FIELD
  unsafe_block: unsafe $BLOCK
  async_block:
    template: async $MOVE_CLAUSE $BLOCK
    move_clause: $MOVE
  gen_block:
    template: gen $MOVE_CLAUSE $BLOCK
    move_clause: $MOVE
  try_block: try $BLOCK
  block:
    template: "$LABEL_CLAUSE{$$$CHILDREN}"
    label_clause: "$LABEL:"
  generic_pattern: "$$$CHILDREN::$TYPE_ARGUMENTS"
  tuple_pattern:
    template: ($$$CHILDREN)
    joinBy: ","
  slice_pattern:
    template: "[$$$CHILDREN]"
    joinBy: ","
  tuple_struct_pattern:
    template: $TYPE($$$CHILDREN)
    joinBy: ","
  struct_pattern:
    template: "$TYPE{$$$CHILDREN}"
    joinBy: ","
  field_pattern:
    variants:
      shorthand: $REF_CLAUSE $MUTABLE_SPECIFIER $$$CHILDREN
      named: $REF_CLAUSE $MUTABLE_SPECIFIER $$$CHILDREN
    ref_clause: $REF
  mut_pattern: $MUTABLE_SPECIFIER $PATTERN
  range_pattern:
    variants:
      left: $$$CHILDREN
      prefix: $$$CHILDREN
  ref_pattern: $REF $$$CHILDREN
  captured_pattern: $IDENTIFIER@$PATTERN
  reference_pattern: "&$MUTABLE_SPECIFIER $PATTERN"
  or_pattern:
    variants:
      binary: $$$CHILDREN
      prefix: $$$CHILDREN
  negative_literal: "-$VALUE"
  string_literal: "\"$$$CHILDREN\""
  raw_string_literal: $RAW_STRING_LITERAL_START $STRING_CONTENT $RAW_STRING_LITERAL_END
  comment: $$$CHILDREN
  line_comment: //$OUTER$INNER$DOC
  block_comment: /*$OUTER$INNER $DOC*/
  range_expression_binary: $START $OPERATOR $END
  range_expression_postfix: $START $OPERATOR
  range_expression_prefix: $OPERATOR $END
  range_expression_bare: $OPERATOR
  let_chain:
    template: $$$CHILDREN&&
    joinByField:
      children: "&&"
  closure_expression_block: "->$RETURN_TYPE $BODY"
  closure_expression_expr: $BODY
  field_pattern_shorthand: $NAME
  field_pattern_named: "$NAME:$PATTERN"
  range_pattern_left: $LEFT...$RIGHT
  range_pattern_prefix: ..=$RIGHT
  or_pattern_binary: $LEFT|$RIGHT
  or_pattern_prefix: "|$RIGHT"
// Auto-generated by @sittir/codegen — do not edit

import type { AbstractType, AbstractTypeTree, Arguments, ArgumentsTree, ArrayExpression, ArrayExpressionTree, ArrayType, ArrayTypeTree, AssignmentExpression, AssignmentExpressionTree, AssociatedType, AssociatedTypeTree, AsyncBlock, AsyncBlockTree, Attribute, AttributeItem, AttributeItemTree, AttributeTree, AwaitExpression, AwaitExpressionTree, BaseFieldInitializer, BaseFieldInitializerTree, BinaryExpression, BinaryExpressionTree, Block, BlockComment, BlockCommentTree, BlockTree, BoundedType, BoundedTypeTree, BracketedType, BracketedTypeTree, BreakExpression, BreakExpressionTree, CallExpression, CallExpressionTree, CapturedPattern, CapturedPatternTree, CharLiteral, CharLiteralTree, ClosureExpression, ClosureExpressionBody, ClosureExpressionBody2, ClosureExpressionBody2Config, ClosureExpressionBody2Tree, ClosureExpressionBodyConfig, ClosureExpressionBodyTree, ClosureExpressionTree, ClosureParameters, ClosureParametersTree, Comment, CommentTree, CompoundAssignmentExpr, CompoundAssignmentExprTree, ConstBlock, ConstBlockTree, ConstItem, ConstItemTree, ConstParameter, ConstParameterTree, ContinueExpression, ContinueExpressionTree, Crate, CrateTree, DeclarationList, DeclarationListTree, DelimTokenTree, DelimTokenTreeTree, DynamicType, DynamicTypeTree, ElseClause, ElseClauseTree, EnumItem, EnumItemTree, EnumVariant, EnumVariantList, EnumVariantListTree, EnumVariantTree, EscapeSequence, EscapeSequenceTree, ExpressionStatement, ExpressionStatementTree, ExternCrateDeclaration, ExternCrateDeclarationTree, ExternModifier, ExternModifierTree, FieldDeclaration, FieldDeclarationList, FieldDeclarationListTree, FieldDeclarationTree, FieldExpression, FieldExpressionTree, FieldIdentifier, FieldIdentifierTree, FieldInitializer, FieldInitializerList, FieldInitializerListTree, FieldInitializerTree, FieldPattern, FieldPatternColon, FieldPatternColonConfig, FieldPatternColonTree, FieldPatternName, FieldPatternNameConfig, FieldPatternNameTree, FieldPatternTree, ForExpression, ForExpressionTree, ForLifetimes, ForLifetimesTree, ForeignModItem, ForeignModItemTree, FunctionItem, FunctionItemTree, FunctionModifiers, FunctionModifiersTree, FunctionSignatureItem, FunctionSignatureItemTree, FunctionType, FunctionTypeTree, GenBlock, GenBlockTree, GenericFunction, GenericFunctionTree, GenericPattern, GenericPatternTree, GenericType, GenericTypeTree, GenericTypeWithTurbofish, GenericTypeWithTurbofishTree, HiddenFieldIdentifier, HiddenFieldIdentifierTree, HiddenTypeIdentifier, HiddenTypeIdentifierTree, HigherRankedTraitBound, HigherRankedTraitBoundTree, Identifier, IdentifierTree, IfExpression, IfExpressionTree, ImplItem, ImplItemTree, IndexExpression, IndexExpressionTree, InnerAttributeItem, InnerAttributeItemTree, InnerDocCommentMarker, InnerDocCommentMarkerTree, IntegerLiteral, IntegerLiteralTree, Label, LabelTree, LastMatchArm, LastMatchArmTree, LetChain, LetChainTree, LetCondition, LetConditionTree, LetDeclaration, LetDeclarationTree, Lifetime, LifetimeParameter, LifetimeParameterTree, LifetimeTree, LineComment, LineCommentTree, LoopExpression, LoopExpressionTree, MacroDefinition, MacroDefinitionTree, MacroInvocation, MacroInvocationTree, MacroRule, MacroRuleTree, MatchArm, MatchArmTree, MatchBlock, MatchBlockTree, MatchExpression, MatchExpressionTree, MatchPattern, MatchPatternTree, Metavariable, MetavariableTree, ModItem, ModItemTree, MutPattern, MutPatternTree, MutableSpecifier, MutableSpecifierTree, NegativeLiteral, NegativeLiteralTree, OrPattern, OrPatternPipe, OrPatternPipe2, OrPatternPipe2Config, OrPatternPipe2Tree, OrPatternPipeConfig, OrPatternPipeTree, OrPatternTree, OrderedFieldDeclarationList, OrderedFieldDeclarationListTree, OuterDocCommentMarker, OuterDocCommentMarkerTree, Parameter, ParameterTree, Parameters, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionTree, PointerType, PointerTypeTree, QualifiedType, QualifiedTypeTree, RangeExpression, RangeExpressionTree, RangePattern, RangePatternLeft, RangePatternLeftConfig, RangePatternLeftTree, RangePatternRight, RangePatternRightConfig, RangePatternRightTree, RangePatternTree, RawStringLiteral, RawStringLiteralTree, RefPattern, RefPatternTree, ReferenceExpression, ReferenceExpressionTree, ReferencePattern, ReferencePatternTree, ReferenceType, ReferenceTypeTree, RemovedTraitBound, RemovedTraitBoundTree, ReturnExpression, ReturnExpressionTree, ScopedIdentifier, ScopedIdentifierTree, ScopedTypeIdentifier, ScopedTypeIdentifierInExpressionPosition, ScopedTypeIdentifierInExpressionPositionTree, ScopedTypeIdentifierTree, ScopedUseList, ScopedUseListTree, Self, SelfParameter, SelfParameterTree, SelfTree, Shebang, ShebangTree, ShorthandFieldIdentifier, ShorthandFieldIdentifierTree, ShorthandFieldInitializer, ShorthandFieldInitializerTree, SlicePattern, SlicePatternTree, SourceFile, SourceFileTree, StaticItem, StaticItemTree, StringLiteral, StringLiteralTree, StructExpression, StructExpressionTree, StructItem, StructItemTree, StructPattern, StructPatternTree, Super, SuperTree, TokenBindingPattern, TokenBindingPatternTree, TokenRepetition, TokenRepetitionPattern, TokenRepetitionPatternTree, TokenRepetitionTree, TokenTree, TokenTreePattern, TokenTreePatternTree, TokenTreeTree, TraitBounds, TraitBoundsTree, TraitItem, TraitItemTree, TryBlock, TryBlockTree, TryExpression, TryExpressionTree, TupleExpression, TupleExpressionTree, TuplePattern, TuplePatternTree, TupleStructPattern, TupleStructPatternTree, TupleType, TupleTypeTree, TypeArguments, TypeArgumentsTree, TypeBinding, TypeBindingTree, TypeCastExpression, TypeCastExpressionTree, TypeIdentifier, TypeIdentifierTree, TypeItem, TypeItemTree, TypeParameter, TypeParameterTree, TypeParameters, TypeParametersTree, UnaryExpression, UnaryExpressionTree, UnionItem, UnionItemTree, UnitExpression, UnitExpressionTree, UnitType, UnitTypeTree, UnsafeBlock, UnsafeBlockTree, UseAsClause, UseAsClauseTree, UseBounds, UseBoundsTree, UseDeclaration, UseDeclarationTree, UseList, UseListTree, UseWildcard, UseWildcardTree, VariadicParameter, VariadicParameterTree, VisibilityModifier, VisibilityModifierTree, WhereClause, WhereClauseTree, WherePredicate, WherePredicateTree, WhileExpression, WhileExpressionTree, YieldExpression, YieldExpressionTree } from './types.js';
import type { Edit, AnyNodeData, ConfigOf } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  '_',
  'as',
  'async',
  'await',
  'bool',
  'break',
  'char',
  'const',
  'continue',
  'crate',
  'default',
  'dyn',
  'else',
  'enum',
  'expr',
  'expr_2021',
  'extern',
  'f32',
  'f64',
  'false',
  'fn',
  'for',
  'gen',
  'i128',
  'i16',
  'i32',
  'i64',
  'i8',
  'ident',
  'if',
  'impl',
  'in',
  'isize',
  'item',
  'let',
  'literal',
  'loop',
  'match',
  'meta',
  'mod',
  'move',
  'mut',
  'mutable_specifier',
  'pat',
  'pat_param',
  'path',
  'pub',
  'raw',
  'ref',
  'return',
  'self',
  'static',
  'stmt',
  'str',
  'struct',
  'super',
  'trait',
  'true',
  'try',
  'tt',
  'ty',
  'type',
  'u128',
  'u16',
  'u32',
  'u64',
  'u8',
  'union',
  'unsafe',
  'use',
  'usize',
  'vis',
  'where',
  'while',
  'yield',
]);

export function sourceFile(config: ConfigOf<SourceFile>) {
  const fields = {
    shebang: (config as any)?.shebang,
    statements: (config as any)?.statements,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'source_file' as const,
    named: true as const,
    fields,
    children,
    shebang(shebang_?: any) { return shebang_ !== undefined ? sourceFile({ ...(config as any), shebang: shebang_ } as any) : fields.shebang; },
    statements(statements_?: any) { return statements_ !== undefined ? sourceFile({ ...(config as any), statements: statements_ } as any) : fields.statements; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return sourceFile({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SourceFileTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroDefinition(config: ConfigOf<MacroDefinition>) {
  const fields = {
    rules: (config as any)?.rules,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'macro_definition' as const,
    named: true as const,
    fields,
    children,
    rules(rules_?: any) { return rules_ !== undefined ? macroDefinition({ ...(config as any), rules: rules_ } as any) : fields.rules; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return macroDefinition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroRule(config: ConfigOf<MacroRule>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'macro_rule' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? macroRule({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? macroRule({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return macroRule({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroRuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenTreePattern(...children: any[]) {
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenBindingPattern(config: ConfigOf<TokenBindingPattern>) {
  const fields = {
    name: (config as any)?.name,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'token_binding_pattern' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? tokenBindingPattern({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? tokenBindingPattern({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return tokenBindingPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenBindingPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetitionPattern(...children: any[]) {
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenTree(...children: any[]) {
  return {
    type: 'token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetition(...children: any[]) {
  return {
    type: 'token_repetition' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attributeItem(config: ConfigOf<AttributeItem>) {
  const fields = {
    attribute: (config as any)?.attribute,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'attribute_item' as const,
    named: true as const,
    fields,
    children,
    attribute(attribute_?: any) { return attribute_ !== undefined ? attributeItem({ ...(config as any), attribute: attribute_ } as any) : fields.attribute; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return attributeItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function innerAttributeItem(config: ConfigOf<InnerAttributeItem>) {
  const fields = {
    attribute: (config as any)?.attribute,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'inner_attribute_item' as const,
    named: true as const,
    fields,
    children,
    attribute(attribute_?: any) { return attribute_ !== undefined ? innerAttributeItem({ ...(config as any), attribute: attribute_ } as any) : fields.attribute; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return innerAttributeItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InnerAttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config?: ConfigOf<Attribute>) {
  const fields = {
    value: (config as any)?.value,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? attribute({ ...(config as any), value: value_ } as any) : fields.value; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? attribute({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return attribute({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function modItem(config: ConfigOf<ModItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    name: (config as any)?.name,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'mod_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? modItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    name(name_?: any) { return name_ !== undefined ? modItem({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? modItem({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return modItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function foreignModItem(config: ConfigOf<ForeignModItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    extern_modifier: (config as any)?.externModifier,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? foreignModItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    externModifier(externModifier_?: any) { return externModifier_ !== undefined ? foreignModItem({ ...(config as any), externModifier: externModifier_ } as any) : fields.extern_modifier; },
    body(body_?: any) { return body_ !== undefined ? foreignModItem({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return foreignModItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForeignModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declarationList(...children: any[]) {
  return {
    type: 'declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structItem(config: ConfigOf<StructItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    where_clause: (config as any)?.whereClause,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? structItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? structItem({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    name(name_?: any) { return name_ !== undefined ? structItem({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? structItem({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    body(body_?: any) { return body_ !== undefined ? structItem({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return structItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionItem(config: ConfigOf<UnionItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    where_clause: (config as any)?.whereClause,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'union_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? unionItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? unionItem({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    name(name_?: any) { return name_ !== undefined ? unionItem({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? unionItem({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    body(body_?: any) { return body_ !== undefined ? unionItem({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return unionItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumItem(config: ConfigOf<EnumItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    where_clause: (config as any)?.whereClause,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'enum_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? enumItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? enumItem({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    name(name_?: any) { return name_ !== undefined ? enumItem({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? enumItem({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    body(body_?: any) { return body_ !== undefined ? enumItem({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return enumItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariantList(...children: any[]) {
  return {
    type: 'enum_variant_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariant(config: ConfigOf<EnumVariant>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    name: (config as any)?.name,
    body: (config as any)?.body,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'enum_variant' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? enumVariant({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    name(name_?: any) { return name_ !== undefined ? enumVariant({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? enumVariant({ ...(config as any), body: body_ } as any) : fields.body; },
    value(value_?: any) { return value_ !== undefined ? enumVariant({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return enumVariant({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclarationList(...children: any[]) {
  return {
    type: 'field_declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclaration(config: ConfigOf<FieldDeclaration>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    name: (config as any)?.name,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'field_declaration' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? fieldDeclaration({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    name(name_?: any) { return name_ !== undefined ? fieldDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? fieldDeclaration({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return fieldDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orderedFieldDeclarationList(config: ConfigOf<OrderedFieldDeclarationList>) {
  const fields = {
    attributes: (config as any)?.attributes,
    visibility_modifier: (config as any)?.visibilityModifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'ordered_field_declaration_list' as const,
    named: true as const,
    fields,
    children,
    attributes(attributes_?: any) { return attributes_ !== undefined ? orderedFieldDeclarationList({ ...(config as any), attributes: attributes_ } as any) : fields.attributes; },
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? orderedFieldDeclarationList({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return orderedFieldDeclarationList({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrderedFieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externCrateDeclaration(config: ConfigOf<ExternCrateDeclaration>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    crate: (config as any)?.crate,
    name: (config as any)?.name,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'extern_crate_declaration' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? externCrateDeclaration({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    crate(crate_?: any) { return crate_ !== undefined ? externCrateDeclaration({ ...(config as any), crate: crate_ } as any) : fields.crate; },
    name(name_?: any) { return name_ !== undefined ? externCrateDeclaration({ ...(config as any), name: name_ } as any) : fields.name; },
    alias(alias_?: any) { return alias_ !== undefined ? externCrateDeclaration({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return externCrateDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternCrateDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constItem(config: ConfigOf<ConstItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    name: (config as any)?.name,
    type: (config as any)?.type,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'const_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? constItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    name(name_?: any) { return name_ !== undefined ? constItem({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? constItem({ ...(config as any), type: type } as any) : fields.type; },
    value(value_?: any) { return value_ !== undefined ? constItem({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return constItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function staticItem(config: ConfigOf<StaticItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    mutable_specifier: (config as any)?.mutableSpecifier,
    name: (config as any)?.name,
    type: (config as any)?.type,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'static_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? staticItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? staticItem({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    name(name_?: any) { return name_ !== undefined ? staticItem({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? staticItem({ ...(config as any), type: type } as any) : fields.type; },
    value(value_?: any) { return value_ !== undefined ? staticItem({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return staticItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StaticItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeItem(config: ConfigOf<TypeItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    where_clause: (config as any)?.whereClause,
    trailing_where_clause: (config as any)?.trailingWhereClause,
    type_parameters: (config as any)?.typeParameters,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? typeItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? typeItem({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    trailingWhereClause(trailingWhereClause_?: any) { return trailingWhereClause_ !== undefined ? typeItem({ ...(config as any), trailingWhereClause: trailingWhereClause_ } as any) : fields.trailing_where_clause; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? typeItem({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    typeField(type?: any) { return type !== undefined ? typeItem({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionItem(config: ConfigOf<FunctionItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    function_modifiers: (config as any)?.functionModifiers,
    where_clause: (config as any)?.whereClause,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    parameters: (config as any)?.parameters,
    return_type: (config as any)?.returnType,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? functionItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    functionModifiers(functionModifiers_?: any) { return functionModifiers_ !== undefined ? functionItem({ ...(config as any), functionModifiers: functionModifiers_ } as any) : fields.function_modifiers; },
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? functionItem({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    name(name_?: any) { return name_ !== undefined ? functionItem({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? functionItem({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    parameters(parameters_?: any) { return parameters_ !== undefined ? functionItem({ ...(config as any), parameters: parameters_ } as any) : fields.parameters; },
    returnType(returnType_?: any) { return returnType_ !== undefined ? functionItem({ ...(config as any), returnType: returnType_ } as any) : fields.return_type; },
    body(body_?: any) { return body_ !== undefined ? functionItem({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignatureItem(config: ConfigOf<FunctionSignatureItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    function_modifiers: (config as any)?.functionModifiers,
    where_clause: (config as any)?.whereClause,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    parameters: (config as any)?.parameters,
    return_type: (config as any)?.returnType,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_signature_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? functionSignatureItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    functionModifiers(functionModifiers_?: any) { return functionModifiers_ !== undefined ? functionSignatureItem({ ...(config as any), functionModifiers: functionModifiers_ } as any) : fields.function_modifiers; },
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? functionSignatureItem({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    name(name_?: any) { return name_ !== undefined ? functionSignatureItem({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? functionSignatureItem({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    parameters(parameters_?: any) { return parameters_ !== undefined ? functionSignatureItem({ ...(config as any), parameters: parameters_ } as any) : fields.parameters; },
    returnType(returnType_?: any) { return returnType_ !== undefined ? functionSignatureItem({ ...(config as any), returnType: returnType_ } as any) : fields.return_type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionSignatureItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionModifiers(...children: any[]) {
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whereClause(...children: any[]) {
  return {
    type: 'where_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhereClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function wherePredicate(config: ConfigOf<WherePredicate>) {
  const fields = {
    left: (config as any)?.left,
    bounds: (config as any)?.bounds,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'where_predicate' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? wherePredicate({ ...(config as any), left: left_ } as any) : fields.left; },
    bounds(bounds_?: any) { return bounds_ !== undefined ? wherePredicate({ ...(config as any), bounds: bounds_ } as any) : fields.bounds; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return wherePredicate({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implItem(config: ConfigOf<ImplItem>) {
  const fields = {
    where_clause: (config as any)?.whereClause,
    type_parameters: (config as any)?.typeParameters,
    trait: (config as any)?.trait,
    type: (config as any)?.type,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'impl_item' as const,
    named: true as const,
    fields,
    children,
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? implItem({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? implItem({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    trait(trait_?: any) { return trait_ !== undefined ? implItem({ ...(config as any), trait: trait_ } as any) : fields.trait; },
    typeField(type?: any) { return type !== undefined ? implItem({ ...(config as any), type: type } as any) : fields.type; },
    body(body_?: any) { return body_ !== undefined ? implItem({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return implItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitItem(config: ConfigOf<TraitItem>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    where_clause: (config as any)?.whereClause,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    bounds: (config as any)?.bounds,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'trait_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? traitItem({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? traitItem({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    name(name_?: any) { return name_ !== undefined ? traitItem({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? traitItem({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    bounds(bounds_?: any) { return bounds_ !== undefined ? traitItem({ ...(config as any), bounds: bounds_ } as any) : fields.bounds; },
    body(body_?: any) { return body_ !== undefined ? traitItem({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return traitItem({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function associatedType(config: ConfigOf<AssociatedType>) {
  const fields = {
    where_clause: (config as any)?.whereClause,
    name: (config as any)?.name,
    type_parameters: (config as any)?.typeParameters,
    bounds: (config as any)?.bounds,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'associated_type' as const,
    named: true as const,
    fields,
    children,
    whereClause(whereClause_?: any) { return whereClause_ !== undefined ? associatedType({ ...(config as any), whereClause: whereClause_ } as any) : fields.where_clause; },
    name(name_?: any) { return name_ !== undefined ? associatedType({ ...(config as any), name: name_ } as any) : fields.name; },
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? associatedType({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    bounds(bounds_?: any) { return bounds_ !== undefined ? associatedType({ ...(config as any), bounds: bounds_ } as any) : fields.bounds; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return associatedType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssociatedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitBounds(...children: any[]) {
  return {
    type: 'trait_bounds' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function higherRankedTraitBound(config: ConfigOf<HigherRankedTraitBound>) {
  const fields = {
    type_parameters: (config as any)?.typeParameters,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'higher_ranked_trait_bound' as const,
    named: true as const,
    fields,
    children,
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? higherRankedTraitBound({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    typeField(type?: any) { return type !== undefined ? higherRankedTraitBound({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return higherRankedTraitBound({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HigherRankedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function removedTraitBound(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'removed_trait_bound' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RemovedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: any[]) {
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constParameter(config: ConfigOf<ConstParameter>) {
  const fields = {
    name: (config as any)?.name,
    type: (config as any)?.type,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'const_parameter' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? constParameter({ ...(config as any), name: name_ } as any) : fields.name; },
    typeField(type?: any) { return type !== undefined ? constParameter({ ...(config as any), type: type } as any) : fields.type; },
    value(value_?: any) { return value_ !== undefined ? constParameter({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return constParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: ConfigOf<TypeParameter>) {
  const fields = {
    name: (config as any)?.name,
    bounds: (config as any)?.bounds,
    default_type: (config as any)?.defaultType,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? typeParameter({ ...(config as any), name: name_ } as any) : fields.name; },
    bounds(bounds_?: any) { return bounds_ !== undefined ? typeParameter({ ...(config as any), bounds: bounds_ } as any) : fields.bounds; },
    defaultType(defaultType_?: any) { return defaultType_ !== undefined ? typeParameter({ ...(config as any), defaultType: defaultType_ } as any) : fields.default_type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetimeParameter(config: ConfigOf<LifetimeParameter>) {
  const fields = {
    name: (config as any)?.name,
    bounds: (config as any)?.bounds,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'lifetime_parameter' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? lifetimeParameter({ ...(config as any), name: name_ } as any) : fields.name; },
    bounds(bounds_?: any) { return bounds_ !== undefined ? lifetimeParameter({ ...(config as any), bounds: bounds_ } as any) : fields.bounds; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return lifetimeParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letDeclaration(config: ConfigOf<LetDeclaration>) {
  const fields = {
    mutable_specifier: (config as any)?.mutableSpecifier,
    pattern: (config as any)?.pattern,
    type: (config as any)?.type,
    value: (config as any)?.value,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'let_declaration' as const,
    named: true as const,
    fields,
    children,
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? letDeclaration({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    pattern(pattern_?: any) { return pattern_ !== undefined ? letDeclaration({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    typeField(type?: any) { return type !== undefined ? letDeclaration({ ...(config as any), type: type } as any) : fields.type; },
    value(value_?: any) { return value_ !== undefined ? letDeclaration({ ...(config as any), value: value_ } as any) : fields.value; },
    alternative(alternative_?: any) { return alternative_ !== undefined ? letDeclaration({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return letDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useDeclaration(config: ConfigOf<UseDeclaration>) {
  const fields = {
    visibility_modifier: (config as any)?.visibilityModifier,
    argument: (config as any)?.argument,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'use_declaration' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: any) { return visibilityModifier_ !== undefined ? useDeclaration({ ...(config as any), visibilityModifier: visibilityModifier_ } as any) : fields.visibility_modifier; },
    argument(argument_?: any) { return argument_ !== undefined ? useDeclaration({ ...(config as any), argument: argument_ } as any) : fields.argument; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return useDeclaration({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedUseList(config: ConfigOf<ScopedUseList>) {
  const fields = {
    path: (config as any)?.path,
    list: (config as any)?.list,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'scoped_use_list' as const,
    named: true as const,
    fields,
    children,
    path(path_?: any) { return path_ !== undefined ? scopedUseList({ ...(config as any), path: path_ } as any) : fields.path; },
    list(list_?: any) { return list_ !== undefined ? scopedUseList({ ...(config as any), list: list_ } as any) : fields.list; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return scopedUseList({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedUseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useList(...children: any[]) {
  return {
    type: 'use_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useAsClause(config: ConfigOf<UseAsClause>) {
  const fields = {
    path: (config as any)?.path,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'use_as_clause' as const,
    named: true as const,
    fields,
    children,
    path(path_?: any) { return path_ !== undefined ? useAsClause({ ...(config as any), path: path_ } as any) : fields.path; },
    alias(alias_?: any) { return alias_ !== undefined ? useAsClause({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return useAsClause({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseAsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useWildcard(config: ConfigOf<UseWildcard>) {
  const fields = {
    path: (config as any)?.path,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'use_wildcard' as const,
    named: true as const,
    fields,
    children,
    path(path_?: any) { return path_ !== undefined ? useWildcard({ ...(config as any), path: path_ } as any) : fields.path; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return useWildcard({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseWildcardTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(...children: any[]) {
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function selfParameter(config: ConfigOf<SelfParameter>) {
  const fields = {
    lifetime: (config as any)?.lifetime,
    mutable_specifier: (config as any)?.mutableSpecifier,
    self: (config as any)?.self,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'self_parameter' as const,
    named: true as const,
    fields,
    children,
    lifetime(lifetime_?: any) { return lifetime_ !== undefined ? selfParameter({ ...(config as any), lifetime: lifetime_ } as any) : fields.lifetime; },
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? selfParameter({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    self(self_?: any) { return self_ !== undefined ? selfParameter({ ...(config as any), self: self_ } as any) : fields.self; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return selfParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SelfParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variadicParameter(config: ConfigOf<VariadicParameter>) {
  const fields = {
    mutable_specifier: (config as any)?.mutableSpecifier,
    pattern: (config as any)?.pattern,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'variadic_parameter' as const,
    named: true as const,
    fields,
    children,
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? variadicParameter({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    pattern(pattern_?: any) { return pattern_ !== undefined ? variadicParameter({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return variadicParameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariadicParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameter(config: ConfigOf<Parameter>) {
  const fields = {
    mutable_specifier: (config as any)?.mutableSpecifier,
    pattern: (config as any)?.pattern,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'parameter' as const,
    named: true as const,
    fields,
    children,
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? parameter({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    pattern(pattern_?: any) { return pattern_ !== undefined ? parameter({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    typeField(type?: any) { return type !== undefined ? parameter({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return parameter({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externModifier(config: ConfigOf<ExternModifier>) {
  const fields = {
    string_literal: (config as any)?.stringLiteral,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'extern_modifier' as const,
    named: true as const,
    fields,
    children,
    stringLiteral(stringLiteral_?: any) { return stringLiteral_ !== undefined ? externModifier({ ...(config as any), stringLiteral: stringLiteral_ } as any) : fields.string_literal; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return externModifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function visibilityModifier(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VisibilityModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function bracketedType(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'bracketed_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BracketedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function qualifiedType(config: ConfigOf<QualifiedType>) {
  const fields = {
    type: (config as any)?.type,
    alias: (config as any)?.alias,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'qualified_type' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: any) { return type !== undefined ? qualifiedType({ ...(config as any), type: type } as any) : fields.type; },
    alias(alias_?: any) { return alias_ !== undefined ? qualifiedType({ ...(config as any), alias: alias_ } as any) : fields.alias; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return qualifiedType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: QualifiedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetime(config: ConfigOf<Lifetime>) {
  const fields = {
    identifier: (config as any)?.identifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'lifetime' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? lifetime({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return lifetime({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: ConfigOf<ArrayType>) {
  const fields = {
    element: (config as any)?.element,
    length: (config as any)?.length,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    children,
    element(element_?: any) { return element_ !== undefined ? arrayType({ ...(config as any), element: element_ } as any) : fields.element; },
    length(length_?: any) { return length_ !== undefined ? arrayType({ ...(config as any), length: length_ } as any) : fields.length; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return arrayType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forLifetimes(...children: any[]) {
  return {
    type: 'for_lifetimes' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForLifetimesTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: ConfigOf<FunctionType>) {
  const fields = {
    for_lifetimes: (config as any)?.forLifetimes,
    function_modifiers: (config as any)?.functionModifiers,
    return_type: (config as any)?.returnType,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    children,
    forLifetimes(forLifetimes_?: any) { return forLifetimes_ !== undefined ? functionType({ ...(config as any), forLifetimes: forLifetimes_ } as any) : fields.for_lifetimes; },
    functionModifiers(functionModifiers_?: any) { return functionModifiers_ !== undefined ? functionType({ ...(config as any), functionModifiers: functionModifiers_ } as any) : fields.function_modifiers; },
    returnType(returnType_?: any) { return returnType_ !== undefined ? functionType({ ...(config as any), returnType: returnType_ } as any) : fields.return_type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return functionType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: any[]) {
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitType(text: string) {
  return {
    type: 'unit_type' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnitTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function genericFunction(config: ConfigOf<GenericFunction>) {
  const fields = {
    function: (config as any)?.function,
    type_arguments: (config as any)?.typeArguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generic_function' as const,
    named: true as const,
    fields,
    children,
    function(function_?: any) { return function_ !== undefined ? genericFunction({ ...(config as any), function: function_ } as any) : fields.function; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? genericFunction({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return genericFunction({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: ConfigOf<GenericType>) {
  const fields = {
    type: (config as any)?.type,
    type_arguments: (config as any)?.typeArguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: any) { return type !== undefined ? genericType({ ...(config as any), type: type } as any) : fields.type; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? genericType({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return genericType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericTypeWithTurbofish(config: ConfigOf<GenericTypeWithTurbofish>) {
  const fields = {
    type: (config as any)?.type,
    type_arguments: (config as any)?.typeArguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generic_type_with_turbofish' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: any) { return type !== undefined ? genericTypeWithTurbofish({ ...(config as any), type: type } as any) : fields.type; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? genericTypeWithTurbofish({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return genericTypeWithTurbofish({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeWithTurbofishTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function boundedType(config: ConfigOf<BoundedType>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'bounded_type' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? boundedType({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? boundedType({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return boundedType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BoundedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useBounds(...children: any[]) {
  return {
    type: 'use_bounds' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeArguments(...children: any[]) {
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeBinding(config: ConfigOf<TypeBinding>) {
  const fields = {
    name: (config as any)?.name,
    type_arguments: (config as any)?.typeArguments,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_binding' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? typeBinding({ ...(config as any), name: name_ } as any) : fields.name; },
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? typeBinding({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    typeField(type?: any) { return type !== undefined ? typeBinding({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeBinding({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeBindingTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceType(config: ConfigOf<ReferenceType>) {
  const fields = {
    lifetime: (config as any)?.lifetime,
    mutable_specifier: (config as any)?.mutableSpecifier,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'reference_type' as const,
    named: true as const,
    fields,
    children,
    lifetime(lifetime_?: any) { return lifetime_ !== undefined ? referenceType({ ...(config as any), lifetime: lifetime_ } as any) : fields.lifetime; },
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? referenceType({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    typeField(type?: any) { return type !== undefined ? referenceType({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return referenceType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pointerType(config: ConfigOf<PointerType>) {
  const fields = {
    mutable_specifier: (config as any)?.mutableSpecifier,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'pointer_type' as const,
    named: true as const,
    fields,
    children,
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? pointerType({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    typeField(type?: any) { return type !== undefined ? pointerType({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return pointerType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PointerTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractType(config: ConfigOf<AbstractType>) {
  const fields = {
    type_parameters: (config as any)?.typeParameters,
    trait: (config as any)?.trait,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'abstract_type' as const,
    named: true as const,
    fields,
    children,
    typeParameters(typeParameters_?: any) { return typeParameters_ !== undefined ? abstractType({ ...(config as any), typeParameters: typeParameters_ } as any) : fields.type_parameters; },
    trait(trait_?: any) { return trait_ !== undefined ? abstractType({ ...(config as any), trait: trait_ } as any) : fields.trait; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return abstractType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dynamicType(config: ConfigOf<DynamicType>) {
  const fields = {
    trait: (config as any)?.trait,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'dynamic_type' as const,
    named: true as const,
    fields,
    children,
    trait(trait_?: any) { return trait_ !== undefined ? dynamicType({ ...(config as any), trait: trait_ } as any) : fields.trait; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return dynamicType({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DynamicTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutableSpecifier() {
  return {
    type: 'mutable_specifier' as const,
    named: true as const,
    text: 'mut',
    render: () => 'mut',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'mut' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'mut' },
    replace: (t: MutableSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'mut' }; },
  };
}

export function macroInvocation(config: ConfigOf<MacroInvocation>) {
  const fields = {
    token_tree: (config as any)?.tokenTree,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'macro_invocation' as const,
    named: true as const,
    fields,
    children,
    tokenTree(tokenTree_?: any) { return tokenTree_ !== undefined ? macroInvocation({ ...(config as any), tokenTree: tokenTree_ } as any) : fields.token_tree; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return macroInvocation({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroInvocationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function delimTokenTree(...children: any[]) {
  return {
    type: 'delim_token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DelimTokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedIdentifier(config: ConfigOf<ScopedIdentifier>) {
  const fields = {
    path: (config as any)?.path,
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'scoped_identifier' as const,
    named: true as const,
    fields,
    children,
    path(path_?: any) { return path_ !== undefined ? scopedIdentifier({ ...(config as any), path: path_ } as any) : fields.path; },
    name(name_?: any) { return name_ !== undefined ? scopedIdentifier({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return scopedIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifierInExpressionPosition(config: ConfigOf<ScopedTypeIdentifierInExpressionPosition>) {
  const fields = {
    path: (config as any)?.path,
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'scoped_type_identifier_in_expression_position' as const,
    named: true as const,
    fields,
    children,
    path(path_?: any) { return path_ !== undefined ? scopedTypeIdentifierInExpressionPosition({ ...(config as any), path: path_ } as any) : fields.path; },
    name(name_?: any) { return name_ !== undefined ? scopedTypeIdentifierInExpressionPosition({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return scopedTypeIdentifierInExpressionPosition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedTypeIdentifierInExpressionPositionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifier(config: ConfigOf<ScopedTypeIdentifier>) {
  const fields = {
    path: (config as any)?.path,
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'scoped_type_identifier' as const,
    named: true as const,
    fields,
    children,
    path(path_?: any) { return path_ !== undefined ? scopedTypeIdentifier({ ...(config as any), path: path_ } as any) : fields.path; },
    name(name_?: any) { return name_ !== undefined ? scopedTypeIdentifier({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return scopedTypeIdentifier({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpression(config: ConfigOf<RangeExpression>) {
  const fields = {
    start: (config as any)?.start,
    end: (config as any)?.end,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    fields,
    children,
    start(start_?: any) { return start_ !== undefined ? rangeExpression({ ...(config as any), start: start_ } as any) : fields.start; },
    end(end_?: any) { return end_ !== undefined ? rangeExpression({ ...(config as any), end: end_ } as any) : fields.end; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return rangeExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: ConfigOf<UnaryExpression>) {
  const fields = {
    operand: (config as any)?.operand,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    children,
    operand(operand_?: any) { return operand_ !== undefined ? unaryExpression({ ...(config as any), operand: operand_ } as any) : fields.operand; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return unaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryExpression(config: ConfigOf<TryExpression>) {
  const fields = {
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'try_expression' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? tryExpression({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return tryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceExpression(config: ConfigOf<ReferenceExpression>) {
  const fields = {
    mutable_specifier: (config as any)?.mutableSpecifier,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'reference_expression' as const,
    named: true as const,
    fields,
    children,
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? referenceExpression({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    value(value_?: any) { return value_ !== undefined ? referenceExpression({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return referenceExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: ConfigOf<BinaryExpression>) {
  const fields = {
    left: (config as any)?.left,
    operator: (config as any)?.operator,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? binaryExpression({ ...(config as any), left: left_ } as any) : fields.left; },
    operator(operator_?: any) { return operator_ !== undefined ? binaryExpression({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    right(right_?: any) { return right_ !== undefined ? binaryExpression({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return binaryExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: ConfigOf<AssignmentExpression>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? assignmentExpression({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? assignmentExpression({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return assignmentExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function compoundAssignmentExpr(config: ConfigOf<CompoundAssignmentExpr>) {
  const fields = {
    left: (config as any)?.left,
    operator: (config as any)?.operator,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? compoundAssignmentExpr({ ...(config as any), left: left_ } as any) : fields.left; },
    operator(operator_?: any) { return operator_ !== undefined ? compoundAssignmentExpr({ ...(config as any), operator: operator_ } as any) : fields.operator; },
    right(right_?: any) { return right_ !== undefined ? compoundAssignmentExpr({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return compoundAssignmentExpr({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeCastExpression(config: ConfigOf<TypeCastExpression>) {
  const fields = {
    value: (config as any)?.value,
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'type_cast_expression' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? typeCastExpression({ ...(config as any), value: value_ } as any) : fields.value; },
    typeField(type?: any) { return type !== undefined ? typeCastExpression({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return typeCastExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeCastExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnExpression(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'return_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'yield_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: ConfigOf<CallExpression>) {
  const fields = {
    function: (config as any)?.function,
    arguments: (config as any)?.arguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    children,
    function(function_?: any) { return function_ !== undefined ? callExpression({ ...(config as any), function: function_ } as any) : fields.function; },
    arguments(arguments_?: any) { return arguments_ !== undefined ? callExpression({ ...(config as any), arguments: arguments_ } as any) : fields.arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return callExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arguments_(...children: any[]) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayExpression(config: ConfigOf<ArrayExpression>) {
  const fields = {
    attributes: (config as any)?.attributes,
    elements: (config as any)?.elements,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'array_expression' as const,
    named: true as const,
    fields,
    children,
    attributes(attributes_?: any) { return attributes_ !== undefined ? arrayExpression({ ...(config as any), attributes: attributes_ } as any) : fields.attributes; },
    elements(elements_?: any) { return elements_ !== undefined ? arrayExpression({ ...(config as any), elements: elements_ } as any) : fields.elements; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return arrayExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleExpression(config: ConfigOf<TupleExpression>) {
  const fields = {
    attributes: (config as any)?.attributes,
    first: (config as any)?.first,
    rest: (config as any)?.rest,
    trailing: (config as any)?.trailing,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'tuple_expression' as const,
    named: true as const,
    fields,
    children,
    attributes(attributes_?: any) { return attributes_ !== undefined ? tupleExpression({ ...(config as any), attributes: attributes_ } as any) : fields.attributes; },
    first(first_?: any) { return first_ !== undefined ? tupleExpression({ ...(config as any), first: first_ } as any) : fields.first; },
    rest(rest_?: any) { return rest_ !== undefined ? tupleExpression({ ...(config as any), rest: rest_ } as any) : fields.rest; },
    trailing(trailing_?: any) { return trailing_ !== undefined ? tupleExpression({ ...(config as any), trailing: trailing_ } as any) : fields.trailing; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return tupleExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitExpression(text: string) {
  return {
    type: 'unit_expression' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnitExpressionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function structExpression(config: ConfigOf<StructExpression>) {
  const fields = {
    name: (config as any)?.name,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'struct_expression' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? structExpression({ ...(config as any), name: name_ } as any) : fields.name; },
    body(body_?: any) { return body_ !== undefined ? structExpression({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return structExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializerList(...children: any[]) {
  return {
    type: 'field_initializer_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandFieldInitializer(config: ConfigOf<ShorthandFieldInitializer>) {
  const fields = {
    attributes: (config as any)?.attributes,
    identifier: (config as any)?.identifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'shorthand_field_initializer' as const,
    named: true as const,
    fields,
    children,
    attributes(attributes_?: any) { return attributes_ !== undefined ? shorthandFieldInitializer({ ...(config as any), attributes: attributes_ } as any) : fields.attributes; },
    identifier(identifier_?: any) { return identifier_ !== undefined ? shorthandFieldInitializer({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return shorthandFieldInitializer({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializer(config: ConfigOf<FieldInitializer>) {
  const fields = {
    field: (config as any)?.field,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'field_initializer' as const,
    named: true as const,
    fields,
    children,
    field(field_?: any) { return field_ !== undefined ? fieldInitializer({ ...(config as any), field: field_ } as any) : fields.field; },
    value(value_?: any) { return value_ !== undefined ? fieldInitializer({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return fieldInitializer({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function baseFieldInitializer(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'base_field_initializer' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BaseFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifExpression(config: ConfigOf<IfExpression>) {
  const fields = {
    condition: (config as any)?.condition,
    consequence: (config as any)?.consequence,
    alternative: (config as any)?.alternative,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'if_expression' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: any) { return condition_ !== undefined ? ifExpression({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    consequence(consequence_?: any) { return consequence_ !== undefined ? ifExpression({ ...(config as any), consequence: consequence_ } as any) : fields.consequence; },
    alternative(alternative_?: any) { return alternative_ !== undefined ? ifExpression({ ...(config as any), alternative: alternative_ } as any) : fields.alternative; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return ifExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letCondition(config: ConfigOf<LetCondition>) {
  const fields = {
    pattern: (config as any)?.pattern,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'let_condition' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: any) { return pattern_ !== undefined ? letCondition({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    value(value_?: any) { return value_ !== undefined ? letCondition({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return letCondition({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetConditionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'else_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchExpression(config: ConfigOf<MatchExpression>) {
  const fields = {
    value: (config as any)?.value,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'match_expression' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? matchExpression({ ...(config as any), value: value_ } as any) : fields.value; },
    body(body_?: any) { return body_ !== undefined ? matchExpression({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return matchExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchBlock(...children: any[]) {
  return {
    type: 'match_block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchArm(config: ConfigOf<MatchArm>) {
  const fields = {
    pattern: (config as any)?.pattern,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: any) { return pattern_ !== undefined ? matchArm({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    value(value_?: any) { return value_ !== undefined ? matchArm({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return matchArm({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lastMatchArm(config: ConfigOf<LastMatchArm>) {
  const fields = {
    pattern: (config as any)?.pattern,
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'last_match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: any) { return pattern_ !== undefined ? lastMatchArm({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    value(value_?: any) { return value_ !== undefined ? lastMatchArm({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return lastMatchArm({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LastMatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchPattern(config?: ConfigOf<MatchPattern>) {
  const fields = {
    condition: (config as any)?.condition,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'match_pattern' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: any) { return condition_ !== undefined ? matchPattern({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return matchPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileExpression(config: ConfigOf<WhileExpression>) {
  const fields = {
    label: (config as any)?.label,
    condition: (config as any)?.condition,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'while_expression' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? whileExpression({ ...(config as any), label: label_ } as any) : fields.label; },
    condition(condition_?: any) { return condition_ !== undefined ? whileExpression({ ...(config as any), condition: condition_ } as any) : fields.condition; },
    body(body_?: any) { return body_ !== undefined ? whileExpression({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return whileExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function loopExpression(config: ConfigOf<LoopExpression>) {
  const fields = {
    label: (config as any)?.label,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'loop_expression' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? loopExpression({ ...(config as any), label: label_ } as any) : fields.label; },
    body(body_?: any) { return body_ !== undefined ? loopExpression({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return loopExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LoopExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forExpression(config: ConfigOf<ForExpression>) {
  const fields = {
    label: (config as any)?.label,
    pattern: (config as any)?.pattern,
    value: (config as any)?.value,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'for_expression' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? forExpression({ ...(config as any), label: label_ } as any) : fields.label; },
    pattern(pattern_?: any) { return pattern_ !== undefined ? forExpression({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    value(value_?: any) { return value_ !== undefined ? forExpression({ ...(config as any), value: value_ } as any) : fields.value; },
    body(body_?: any) { return body_ !== undefined ? forExpression({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return forExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constBlock(config: ConfigOf<ConstBlock>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'const_block' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? constBlock({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return constBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function closureExpression(config?: ConfigOf<ClosureExpression>) {
  if (config && 'returnType' in config) return closureExpressionBody(config as any);
  return closureExpressionBody2(config as any);
}
export function closureExpressionBody(config: ConfigOf<ClosureExpressionBody>) {
  const fields = {
    return_type: (config as any)?.returnType,
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'closure_expression' as const,
    named: true as const,
    fields,
    children,
    returnType(returnType_?: any) { return returnType_ !== undefined ? closureExpressionBody({ ...(config as any), returnType: returnType_ } as any) : fields.return_type; },
    body(body_?: any) { return body_ !== undefined ? closureExpressionBody({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return closureExpressionBody({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function closureExpressionBody2(config: ConfigOf<ClosureExpressionBody2>) {
  const fields = {
    body: (config as any)?.body,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'closure_expression' as const,
    named: true as const,
    fields,
    children,
    body(body_?: any) { return body_ !== undefined ? closureExpressionBody2({ ...(config as any), body: body_ } as any) : fields.body; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return closureExpressionBody2({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureParameters(...children: any[]) {
  return {
    type: 'closure_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function label(config: ConfigOf<Label>) {
  const fields = {
    identifier: (config as any)?.identifier,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'label' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? label({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return label({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabelTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakExpression(config: ConfigOf<BreakExpression>) {
  const fields = {
    label: (config as any)?.label,
    expression: (config as any)?.expression,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'break_expression' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? breakExpression({ ...(config as any), label: label_ } as any) : fields.label; },
    expression(expression_?: any) { return expression_ !== undefined ? breakExpression({ ...(config as any), expression: expression_ } as any) : fields.expression; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return breakExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueExpression(config: ConfigOf<ContinueExpression>) {
  const fields = {
    label: (config as any)?.label,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'continue_expression' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? continueExpression({ ...(config as any), label: label_ } as any) : fields.label; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return continueExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexExpression(config: ConfigOf<IndexExpression>) {
  const fields = {
    object: (config as any)?.object,
    index: (config as any)?.index,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'index_expression' as const,
    named: true as const,
    fields,
    children,
    object(object_?: any) { return object_ !== undefined ? indexExpression({ ...(config as any), object: object_ } as any) : fields.object; },
    index(index_?: any) { return index_ !== undefined ? indexExpression({ ...(config as any), index: index_ } as any) : fields.index; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return indexExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'await_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldExpression(config: ConfigOf<FieldExpression>) {
  const fields = {
    value: (config as any)?.value,
    field: (config as any)?.field,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'field_expression' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? fieldExpression({ ...(config as any), value: value_ } as any) : fields.value; },
    field(field_?: any) { return field_ !== undefined ? fieldExpression({ ...(config as any), field: field_ } as any) : fields.field; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return fieldExpression({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unsafeBlock(config: ConfigOf<UnsafeBlock>) {
  const fields = {
    block: (config as any)?.block,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'unsafe_block' as const,
    named: true as const,
    fields,
    children,
    block(block_?: any) { return block_ !== undefined ? unsafeBlock({ ...(config as any), block: block_ } as any) : fields.block; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return unsafeBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnsafeBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asyncBlock(config: ConfigOf<AsyncBlock>) {
  const fields = {
    block: (config as any)?.block,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'async_block' as const,
    named: true as const,
    fields,
    children,
    block(block_?: any) { return block_ !== undefined ? asyncBlock({ ...(config as any), block: block_ } as any) : fields.block; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return asyncBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsyncBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genBlock(config: ConfigOf<GenBlock>) {
  const fields = {
    block: (config as any)?.block,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'gen_block' as const,
    named: true as const,
    fields,
    children,
    block(block_?: any) { return block_ !== undefined ? genBlock({ ...(config as any), block: block_ } as any) : fields.block; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return genBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryBlock(config: ConfigOf<TryBlock>) {
  const fields = {
    block: (config as any)?.block,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'try_block' as const,
    named: true as const,
    fields,
    children,
    block(block_?: any) { return block_ !== undefined ? tryBlock({ ...(config as any), block: block_ } as any) : fields.block; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return tryBlock({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(config: ConfigOf<Block>) {
  const fields = {
    label: (config as any)?.label,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'block' as const,
    named: true as const,
    fields,
    children,
    label(label_?: any) { return label_ !== undefined ? block({ ...(config as any), label: label_ } as any) : fields.label; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return block({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericPattern(config: ConfigOf<GenericPattern>) {
  const fields = {
    type_arguments: (config as any)?.typeArguments,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'generic_pattern' as const,
    named: true as const,
    fields,
    children,
    typeArguments(typeArguments_?: any) { return typeArguments_ !== undefined ? genericPattern({ ...(config as any), typeArguments: typeArguments_ } as any) : fields.type_arguments; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return genericPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(...children: any[]) {
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slicePattern(...children: any[]) {
  return {
    type: 'slice_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SlicePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleStructPattern(config: ConfigOf<TupleStructPattern>) {
  const fields = {
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'tuple_struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: any) { return type !== undefined ? tupleStructPattern({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return tupleStructPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleStructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structPattern(config: ConfigOf<StructPattern>) {
  const fields = {
    type: (config as any)?.type,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: any) { return type !== undefined ? structPattern({ ...(config as any), type: type } as any) : fields.type; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return structPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function fieldPattern(config?: ConfigOf<FieldPattern>) {
  if (config && 'pattern' in config) return fieldPatternColon(config as any);
  return fieldPatternName(config as any);
}
export function fieldPatternName(config: ConfigOf<FieldPatternName>) {
  const fields = {
    name: (config as any)?.name,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'field_pattern' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? fieldPatternName({ ...(config as any), name: name_ } as any) : fields.name; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return fieldPatternName({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function fieldPatternColon(config: ConfigOf<FieldPatternColon>) {
  const fields = {
    name: (config as any)?.name,
    pattern: (config as any)?.pattern,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'field_pattern' as const,
    named: true as const,
    fields,
    children,
    name(name_?: any) { return name_ !== undefined ? fieldPatternColon({ ...(config as any), name: name_ } as any) : fields.name; },
    pattern(pattern_?: any) { return pattern_ !== undefined ? fieldPatternColon({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return fieldPatternColon({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutPattern(config: ConfigOf<MutPattern>) {
  const fields = {
    mutable_specifier: (config as any)?.mutableSpecifier,
    pattern: (config as any)?.pattern,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'mut_pattern' as const,
    named: true as const,
    fields,
    children,
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? mutPattern({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    pattern(pattern_?: any) { return pattern_ !== undefined ? mutPattern({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return mutPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MutPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function rangePattern(config?: ConfigOf<RangePattern>) {
  if (config && 'left' in config) return rangePatternLeft(config as any);
  return rangePatternRight(config as any);
}
export function rangePatternLeft(config: ConfigOf<RangePatternLeft>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'range_pattern' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? rangePatternLeft({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? rangePatternLeft({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return rangePatternLeft({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangePatternRight(config: ConfigOf<RangePatternRight>) {
  const fields = {
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'range_pattern' as const,
    named: true as const,
    fields,
    children,
    right(right_?: any) { return right_ !== undefined ? rangePatternRight({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return rangePatternRight({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function refPattern(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'ref_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RefPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function capturedPattern(config: ConfigOf<CapturedPattern>) {
  const fields = {
    identifier: (config as any)?.identifier,
    pattern: (config as any)?.pattern,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'captured_pattern' as const,
    named: true as const,
    fields,
    children,
    identifier(identifier_?: any) { return identifier_ !== undefined ? capturedPattern({ ...(config as any), identifier: identifier_ } as any) : fields.identifier; },
    pattern(pattern_?: any) { return pattern_ !== undefined ? capturedPattern({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return capturedPattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CapturedPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referencePattern(config: ConfigOf<ReferencePattern>) {
  const fields = {
    mutable_specifier: (config as any)?.mutableSpecifier,
    pattern: (config as any)?.pattern,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'reference_pattern' as const,
    named: true as const,
    fields,
    children,
    mutableSpecifier(mutableSpecifier_?: any) { return mutableSpecifier_ !== undefined ? referencePattern({ ...(config as any), mutableSpecifier: mutableSpecifier_ } as any) : fields.mutable_specifier; },
    pattern(pattern_?: any) { return pattern_ !== undefined ? referencePattern({ ...(config as any), pattern: pattern_ } as any) : fields.pattern; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return referencePattern({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferencePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}



export function orPattern(config?: ConfigOf<OrPattern>) {
  if (config && 'right' in config) return orPatternPipe(config as any);
  return orPatternPipe2(config as any);
}
export function orPatternPipe(config: ConfigOf<OrPatternPipe>) {
  const fields = {
    left: (config as any)?.left,
    right: (config as any)?.right,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'or_pattern' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? orPatternPipe({ ...(config as any), left: left_ } as any) : fields.left; },
    right(right_?: any) { return right_ !== undefined ? orPatternPipe({ ...(config as any), right: right_ } as any) : fields.right; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return orPatternPipe({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function orPatternPipe2(config: ConfigOf<OrPatternPipe2>) {
  const fields = {
    left: (config as any)?.left,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'or_pattern' as const,
    named: true as const,
    fields,
    children,
    left(left_?: any) { return left_ !== undefined ? orPatternPipe2({ ...(config as any), left: left_ } as any) : fields.left; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return orPatternPipe2({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function negativeLiteral(config: ConfigOf<NegativeLiteral>) {
  const fields = {
    value: (config as any)?.value,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'negative_literal' as const,
    named: true as const,
    fields,
    children,
    value(value_?: any) { return value_ !== undefined ? negativeLiteral({ ...(config as any), value: value_ } as any) : fields.value; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return negativeLiteral({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NegativeLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function integerLiteral(text: string) {
  return {
    type: 'integer_literal' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IntegerLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringLiteral(...children: any[]) {
  return {
    type: 'string_literal' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rawStringLiteral(config: ConfigOf<RawStringLiteral>) {
  const fields = {
    raw_string_literal_start: (config as any)?.rawStringLiteralStart,
    string_content: (config as any)?.stringContent,
    raw_string_literal_end: (config as any)?.rawStringLiteralEnd,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'raw_string_literal' as const,
    named: true as const,
    fields,
    children,
    rawStringLiteralStart(rawStringLiteralStart_?: any) { return rawStringLiteralStart_ !== undefined ? rawStringLiteral({ ...(config as any), rawStringLiteralStart: rawStringLiteralStart_ } as any) : fields.raw_string_literal_start; },
    stringContent(stringContent_?: any) { return stringContent_ !== undefined ? rawStringLiteral({ ...(config as any), stringContent: stringContent_ } as any) : fields.string_content; },
    rawStringLiteralEnd(rawStringLiteralEnd_?: any) { return rawStringLiteralEnd_ !== undefined ? rawStringLiteral({ ...(config as any), rawStringLiteralEnd: rawStringLiteralEnd_ } as any) : fields.raw_string_literal_end; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return rawStringLiteral({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RawStringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function charLiteral(text: string) {
  return {
    type: 'char_literal' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CharLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'comment' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lineComment(config: ConfigOf<LineComment>) {
  const fields = {
    doc: (config as any)?.doc,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'line_comment' as const,
    named: true as const,
    fields,
    children,
    doc(doc_?: any) { return doc_ !== undefined ? lineComment({ ...(config as any), doc: doc_ } as any) : fields.doc; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return lineComment({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LineCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function blockComment(config?: ConfigOf<BlockComment>) {
  const fields = {
    doc: (config as any)?.doc,
  };
  const children = (config as any)?.children ?? [];
  return {
    type: 'block_comment' as const,
    named: true as const,
    fields,
    children,
    doc(doc_?: any) { return doc_ !== undefined ? blockComment({ ...(config as any), doc: doc_ } as any) : fields.doc; },
    getChildren() { return children; },
    setChildren(...items: any[]) { return blockComment({ ...(config as any), children: items } as any); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function identifier(text: string) {
  return {
    type: 'identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shebang(text: string) {
  return {
    type: 'shebang' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ShebangTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function hiddenTypeIdentifier(child?: any) {
  const children = child ? [child] : [];
  return {
    type: '_type_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenFieldIdentifier(child?: any) {
  const children = child ? [child] : [];
  return {
    type: '_field_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenFieldIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function self() {
  return {
    type: 'self' as const,
    named: true as const,
    text: 'self',
    render: () => 'self',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'self' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'self' },
    replace: (t: SelfTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'self' }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super',
    render: () => 'super',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' },
    replace: (t: SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; },
  };
}

export function crate() {
  return {
    type: 'crate' as const,
    named: true as const,
    text: 'crate',
    render: () => 'crate',
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'crate' } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'crate' },
    replace: (t: CrateTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'crate' }; },
  };
}

export function metavariable(text: string) {
  return {
    type: 'metavariable' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: MetavariableTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function letChain(child?: any) {
  const children = child ? [child] : [];
  return {
    type: 'let_chain' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandFieldIdentifier(text: string) {
  return {
    type: 'shorthand_field_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ShorthandFieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function outerDocCommentMarker(text: string) {
  return {
    type: 'outer_doc_comment_marker' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: OuterDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function innerDocCommentMarker(text: string) {
  return {
    type: 'inner_doc_comment_marker' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: InnerDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeIdentifier(text: string) {
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function fieldIdentifier(text: string) {
  return {
    type: 'field_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export const _factoryMap: Record<string, (config?: any) => unknown> = {
  'source_file': sourceFile,
  'expression_statement': expressionStatement,
  'macro_definition': macroDefinition,
  'macro_rule': macroRule,
  'token_tree_pattern': tokenTreePattern,
  'token_binding_pattern': tokenBindingPattern,
  'token_repetition_pattern': tokenRepetitionPattern,
  'token_tree': tokenTree,
  'token_repetition': tokenRepetition,
  'attribute_item': attributeItem,
  'inner_attribute_item': innerAttributeItem,
  'attribute': attribute,
  'mod_item': modItem,
  'foreign_mod_item': foreignModItem,
  'declaration_list': declarationList,
  'struct_item': structItem,
  'union_item': unionItem,
  'enum_item': enumItem,
  'enum_variant_list': enumVariantList,
  'enum_variant': enumVariant,
  'field_declaration_list': fieldDeclarationList,
  'field_declaration': fieldDeclaration,
  'ordered_field_declaration_list': orderedFieldDeclarationList,
  'extern_crate_declaration': externCrateDeclaration,
  'const_item': constItem,
  'static_item': staticItem,
  'type_item': typeItem,
  'function_item': functionItem,
  'function_signature_item': functionSignatureItem,
  'function_modifiers': functionModifiers,
  'where_clause': whereClause,
  'where_predicate': wherePredicate,
  'impl_item': implItem,
  'trait_item': traitItem,
  'associated_type': associatedType,
  'trait_bounds': traitBounds,
  'higher_ranked_trait_bound': higherRankedTraitBound,
  'removed_trait_bound': removedTraitBound,
  'type_parameters': typeParameters,
  'const_parameter': constParameter,
  'type_parameter': typeParameter,
  'lifetime_parameter': lifetimeParameter,
  'let_declaration': letDeclaration,
  'use_declaration': useDeclaration,
  'scoped_use_list': scopedUseList,
  'use_list': useList,
  'use_as_clause': useAsClause,
  'use_wildcard': useWildcard,
  'parameters': parameters,
  'self_parameter': selfParameter,
  'variadic_parameter': variadicParameter,
  'parameter': parameter,
  'extern_modifier': externModifier,
  'visibility_modifier': visibilityModifier,
  'bracketed_type': bracketedType,
  'qualified_type': qualifiedType,
  'lifetime': lifetime,
  'array_type': arrayType,
  'for_lifetimes': forLifetimes,
  'function_type': functionType,
  'tuple_type': tupleType,
  'unit_type': unitType,
  'generic_function': genericFunction,
  'generic_type': genericType,
  'generic_type_with_turbofish': genericTypeWithTurbofish,
  'bounded_type': boundedType,
  'use_bounds': useBounds,
  'type_arguments': typeArguments,
  'type_binding': typeBinding,
  'reference_type': referenceType,
  'pointer_type': pointerType,
  'abstract_type': abstractType,
  'dynamic_type': dynamicType,
  'mutable_specifier': mutableSpecifier,
  'macro_invocation': macroInvocation,
  'delim_token_tree': delimTokenTree,
  'scoped_identifier': scopedIdentifier,
  'scoped_type_identifier_in_expression_position': scopedTypeIdentifierInExpressionPosition,
  'scoped_type_identifier': scopedTypeIdentifier,
  'range_expression': rangeExpression,
  'unary_expression': unaryExpression,
  'try_expression': tryExpression,
  'reference_expression': referenceExpression,
  'binary_expression': binaryExpression,
  'assignment_expression': assignmentExpression,
  'compound_assignment_expr': compoundAssignmentExpr,
  'type_cast_expression': typeCastExpression,
  'return_expression': returnExpression,
  'yield_expression': yieldExpression,
  'call_expression': callExpression,
  'arguments': arguments_,
  'array_expression': arrayExpression,
  'parenthesized_expression': parenthesizedExpression,
  'tuple_expression': tupleExpression,
  'unit_expression': unitExpression,
  'struct_expression': structExpression,
  'field_initializer_list': fieldInitializerList,
  'shorthand_field_initializer': shorthandFieldInitializer,
  'field_initializer': fieldInitializer,
  'base_field_initializer': baseFieldInitializer,
  'if_expression': ifExpression,
  'let_condition': letCondition,
  'else_clause': elseClause,
  'match_expression': matchExpression,
  'match_block': matchBlock,
  'match_arm': matchArm,
  'last_match_arm': lastMatchArm,
  'match_pattern': matchPattern,
  'while_expression': whileExpression,
  'loop_expression': loopExpression,
  'for_expression': forExpression,
  'const_block': constBlock,
  'closure_expression_body': closureExpressionBody,
  'closure_expression_body2': closureExpressionBody2,
  'closure_expression': closureExpression,
  'closure_parameters': closureParameters,
  'label': label,
  'break_expression': breakExpression,
  'continue_expression': continueExpression,
  'index_expression': indexExpression,
  'await_expression': awaitExpression,
  'field_expression': fieldExpression,
  'unsafe_block': unsafeBlock,
  'async_block': asyncBlock,
  'gen_block': genBlock,
  'try_block': tryBlock,
  'block': block,
  'generic_pattern': genericPattern,
  'tuple_pattern': tuplePattern,
  'slice_pattern': slicePattern,
  'tuple_struct_pattern': tupleStructPattern,
  'struct_pattern': structPattern,
  'field_pattern_name': fieldPatternName,
  'field_pattern_colon': fieldPatternColon,
  'field_pattern': fieldPattern,
  'mut_pattern': mutPattern,
  'range_pattern_left': rangePatternLeft,
  'range_pattern_right': rangePatternRight,
  'range_pattern': rangePattern,
  'ref_pattern': refPattern,
  'captured_pattern': capturedPattern,
  'reference_pattern': referencePattern,
  'or_pattern_pipe': orPatternPipe,
  'or_pattern_pipe2': orPatternPipe2,
  'or_pattern': orPattern,
  'negative_literal': negativeLiteral,
  'integer_literal': integerLiteral,
  'string_literal': stringLiteral,
  'raw_string_literal': rawStringLiteral,
  'char_literal': charLiteral,
  'escape_sequence': escapeSequence,
  'comment': comment,
  'line_comment': lineComment,
  'block_comment': blockComment,
  'identifier': identifier,
  'shebang': shebang,
  '_type_identifier': hiddenTypeIdentifier,
  '_field_identifier': hiddenFieldIdentifier,
  'self': self,
  'super': super_,
  'crate': crate,
  'metavariable': metavariable,
  'let_chain': letChain,
  'shorthand_field_identifier': shorthandFieldIdentifier,
  'outer_doc_comment_marker': outerDocCommentMarker,
  'inner_doc_comment_marker': innerDocCommentMarker,
  'type_identifier': typeIdentifier,
  'field_identifier': fieldIdentifier,
};

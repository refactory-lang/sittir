// Auto-generated by @sittir/codegen — do not edit

import type * as T from './types.js';
import type { Edit, ByteRange, NonEmptyArray, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const RESERVED_KEYWORDS: ReadonlySet<string> = new Set([
  '_',
  'as',
  'async',
  'await',
  'break',
  'const',
  'continue',
  'crate',
  'default',
  'dyn',
  'else',
  'enum',
  'extern',
  'fn',
  'for',
  'gen',
  'if',
  'impl',
  'in',
  'let',
  'loop',
  'match',
  'mod',
  'move',
  'mut',
  'pub',
  'raw',
  'ref',
  'return',
  'self',
  'static',
  'struct',
  'super',
  'trait',
  'try',
  'type',
  'union',
  'unsafe',
  'use',
  'where',
  'while',
  'yield',
]);

const _leafRe_identifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_shebang = /^(?:#![\r\f\t\v ]*([^\[\n].*)?\n)/u;
const _leafRe_metavariable = /^(?:\$[a-zA-Z_]\w*)/u;
const _leafRe_typeIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_fieldIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_shorthandFieldIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _wordRe = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;

export function sourceFile(config: T.SourceFileConfig) {
  const fields = {
    shebang: config?.shebang,
    statements: config?.statements,
  };
  return {
    type: 'source_file' as const,
    named: true as const,
    fields,
    shebang(shebang_?: T.Shebang | undefined) { return _fs(config, sourceFile, 'shebang', shebang_, fields.shebang); },
    statements(...statements_: T.Statement[]) { return _fsm(config, sourceFile, 'statements', statements_, fields.statements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SourceFileTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child?: (T.Expression | T.ExpressionEndingWithBlock)) {
  const children = child != null ? [child] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroDefinition(config: T.MacroDefinitionConfig) {
  const fields = {
    name: config?.name,
    rules: config?.rules,
  };
  return {
    type: 'macro_definition' as const,
    named: true as const,
    fields,
    name(name_?: T.Identifier | T.ReservedIdentifier) { return _fs(config, macroDefinition, 'name', name_, fields.name); },
    rules(rules_?: T.MacroRule) { return _fs(config, macroDefinition, 'rules', rules_, fields.rules); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroRule(config: T.MacroRuleConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'macro_rule' as const,
    named: true as const,
    fields,
    left(left_?: T.TokenTreePattern) { return _fs(config, macroRule, 'left', left_, fields.left); },
    right(right_?: T.TokenTree) { return _fs(config, macroRule, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MacroRuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenTreePattern(...children: T.TokenPattern[]) {
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenBindingPattern(config: T.TokenBindingPatternConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'token_binding_pattern' as const,
    named: true as const,
    fields,
    name(name_?: T.Metavariable) { return _fs(config, tokenBindingPattern, 'name', name_, fields.name); },
    typeField(type?: T.FragmentSpecifier) { return _fs(config, tokenBindingPattern, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenBindingPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetitionPattern(...children: T.TokenPattern[]) {
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fragmentSpecifier(text: 'block' | 'expr' | 'expr_2021' | 'ident' | 'item' | 'lifetime' | 'literal' | 'meta' | 'pat' | 'pat_param' | 'path' | 'stmt' | 'tt' | 'ty' | 'vis') {
  return {
    type: 'fragment_specifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FragmentSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function tokenTree(...children: T.Tokens[]) {
  return {
    type: 'token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetition(...children: T.Tokens[]) {
  return {
    type: 'token_repetition' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attributeItem(config: T.AttributeItemConfig) {
  const fields = {
    attribute: config?.attribute,
  };
  return {
    type: 'attribute_item' as const,
    named: true as const,
    fields,
    attribute(attribute_?: T.Attribute) { return _fs(config, attributeItem, 'attribute', attribute_, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function innerAttributeItem(config: T.InnerAttributeItemConfig) {
  const fields = {
    attribute: config?.attribute,
  };
  return {
    type: 'inner_attribute_item' as const,
    named: true as const,
    fields,
    attribute(attribute_?: T.Attribute) { return _fs(config, innerAttributeItem, 'attribute', attribute_, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InnerAttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: T.AttributeConfig) {
  const fields = {
    value: config?.value,
    arguments: config?.arguments,
  };
  const children = config?.children ?? [];
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    children,
    value(value_?: T.Expression | undefined) { return _fs(config, attribute, 'value', value_, fields.value); },
    arguments(arguments_?: T.TokenTree | undefined) { return _fs(config, attribute, 'arguments', arguments_, fields.arguments); },
    getChild() { return children[0]; },
    setChild(child: T.Path) { return attribute({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function modItem(config: T.ModItemSemiConfig | T.ModItemBodyConfig) {
  if (config && 'visibilityModifier' in config && 'name' in config && 'body' in config) return modItemBody(config as T.ModItemBodyConfig);
  return modItemSemi(config as T.ModItemSemiConfig);
}
export function modItemSemi(config: T.ModItemSemiConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
  };
  return {
    type: 'mod_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, modItemSemi, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T.Identifier) { return _fs(config, modItemSemi, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModItemSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function modItemBody(config: T.ModItemBodyConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'mod_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, modItemBody, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T.Identifier) { return _fs(config, modItemBody, 'name', name_, fields.name); },
    body(body_?: T.DeclarationList) { return _fs(config, modItemBody, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModItemBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function foreignModItem(config: T.ForeignModItemSemiConfig | T.ForeignModItemBodyConfig) {
  if (config && 'visibilityModifier' in config && 'externModifier' in config && 'body' in config) return foreignModItemBody(config as T.ForeignModItemBodyConfig);
  return foreignModItemSemi(config as T.ForeignModItemSemiConfig);
}
export function foreignModItemSemi(config: T.ForeignModItemSemiConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    extern_modifier: config?.externModifier,
  };
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, foreignModItemSemi, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    externModifier(externModifier_?: T.ExternModifier) { return _fs(config, foreignModItemSemi, 'externModifier', externModifier_, fields.extern_modifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForeignModItemSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function foreignModItemBody(config: T.ForeignModItemBodyConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    extern_modifier: config?.externModifier,
    body: config?.body,
  };
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, foreignModItemBody, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    externModifier(externModifier_?: T.ExternModifier) { return _fs(config, foreignModItemBody, 'externModifier', externModifier_, fields.extern_modifier); },
    body(body_?: T.DeclarationList) { return _fs(config, foreignModItemBody, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForeignModItemBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declarationList(...children: T.DeclarationStatement[]) {
  return {
    type: 'declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structItem(config: T.StructItemBodyConfig | T.StructItemSemiConfig | T.StructItemSemi2Config) {
  if (config && 'visibilityModifier' in config && 'name' in config && 'typeParameters' in config && 'body' in config) return structItemBody(config as T.StructItemBodyConfig);
  return structItemSemi2(config as T.StructItemSemi2Config);
}
export function structItemBody(config: T.StructItemBodyConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, structItemBody, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T._TypeIdentifier) { return _fs(config, structItemBody, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, structItemBody, 'typeParameters', typeParameters_, fields.type_parameters); },
    body(body_?: T.FieldDeclarationList) { return _fs(config, structItemBody, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return structItemBody({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructItemBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function structItemSemi(config: T.StructItemSemiConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, structItemSemi, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T._TypeIdentifier) { return _fs(config, structItemSemi, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, structItemSemi, 'typeParameters', typeParameters_, fields.type_parameters); },
    body(body_?: T.OrderedFieldDeclarationList) { return _fs(config, structItemSemi, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return structItemSemi({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructItemSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function structItemSemi2(config: T.StructItemSemi2Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
  };
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'semi2' as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, structItemSemi2, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T._TypeIdentifier) { return _fs(config, structItemSemi2, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, structItemSemi2, 'typeParameters', typeParameters_, fields.type_parameters); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructItemSemi2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionItem(config: T.UnionItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'union_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, unionItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T._TypeIdentifier) { return _fs(config, unionItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, unionItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: T.WhereClause | undefined) { return _fs(config, unionItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: T.FieldDeclarationList) { return _fs(config, unionItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumItem(config: T.EnumItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'enum_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, enumItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T._TypeIdentifier) { return _fs(config, enumItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, enumItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: T.WhereClause | undefined) { return _fs(config, enumItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: T.EnumVariantList) { return _fs(config, enumItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariantList(...children: (T.AttributeItem | T.EnumVariant)[]) {
  return {
    type: 'enum_variant_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumVariantListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariant(config: T.EnumVariantConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    body: config?.body,
    value: config?.value,
  };
  return {
    type: 'enum_variant' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, enumVariant, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T.Identifier) { return _fs(config, enumVariant, 'name', name_, fields.name); },
    body(body_?: T.FieldDeclarationList | T.OrderedFieldDeclarationList | undefined) { return _fs(config, enumVariant, 'body', body_, fields.body); },
    value(value_?: T.Expression | undefined) { return _fs(config, enumVariant, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumVariantTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclarationList(...children: (T.AttributeItem | T.FieldDeclaration)[]) {
  return {
    type: 'field_declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclaration(config: T.FieldDeclarationConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'field_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, fieldDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T._FieldIdentifier) { return _fs(config, fieldDeclaration, 'name', name_, fields.name); },
    typeField(type?: T._Type) { return _fs(config, fieldDeclaration, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orderedFieldDeclarationList(config: T.OrderedFieldDeclarationListConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'ordered_field_declaration_list' as const,
    named: true as const,
    fields,
    typeField(...type: T._Type[]) { return _fsm(config, orderedFieldDeclarationList, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OrderedFieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externCrateDeclaration(config: T.ExternCrateDeclarationConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    crate: config?.crate,
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'extern_crate_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, externCrateDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    crate(crate_?: T.Crate) { return _fs(config, externCrateDeclaration, 'crate', crate_, fields.crate); },
    name(name_?: T.Identifier) { return _fs(config, externCrateDeclaration, 'name', name_, fields.name); },
    alias(alias_?: T.Identifier | undefined) { return _fs(config, externCrateDeclaration, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExternCrateDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constItem(config: T.ConstItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'const_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, constItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T.Identifier) { return _fs(config, constItem, 'name', name_, fields.name); },
    typeField(type?: T._Type) { return _fs(config, constItem, 'type', type, fields.type); },
    value(value_?: T.Expression | undefined) { return _fs(config, constItem, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function staticItem(config: T.StaticItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    mutable_specifier: config?.mutableSpecifier,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'static_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, staticItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    mutableSpecifier(mutableSpecifier_?: "ref" | undefined) { return _fs(config, staticItem, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    name(name_?: T.Identifier) { return _fs(config, staticItem, 'name', name_, fields.name); },
    typeField(type?: T._Type) { return _fs(config, staticItem, 'type', type, fields.type); },
    value(value_?: T.Expression | undefined) { return _fs(config, staticItem, 'value', value_, fields.value); },
    getChild() { return children[0]; },
    setChild(child: T.MutableSpecifier) { return staticItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StaticItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeItem(config: T.TypeItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    type: config?.type,
    trailing_where_clause: config?.trailingWhereClause,
  };
  return {
    type: 'type_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, typeItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: T._TypeIdentifier) { return _fs(config, typeItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, typeItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: T.WhereClause | undefined) { return _fs(config, typeItem, 'whereClause', whereClause_, fields.where_clause); },
    typeField(type?: T._Type) { return _fs(config, typeItem, 'type', type, fields.type); },
    trailingWhereClause(trailingWhereClause_?: T.WhereClause | undefined) { return _fs(config, typeItem, 'trailingWhereClause', trailingWhereClause_, fields.trailing_where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionItem(config: T.FunctionItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    function_modifiers: config?.functionModifiers,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'function_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, functionItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    functionModifiers(functionModifiers_?: T.FunctionModifiers | undefined) { return _fs(config, functionItem, 'functionModifiers', functionModifiers_, fields.function_modifiers); },
    name(name_?: T.Identifier | T.Metavariable) { return _fs(config, functionItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, functionItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: T.Parameters) { return _fs(config, functionItem, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: T._Type | undefined) { return _fs(config, functionItem, 'returnType', returnType_, fields.return_type); },
    whereClause(whereClause_?: T.WhereClause | undefined) { return _fs(config, functionItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: T.Block) { return _fs(config, functionItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignatureItem(config: T.FunctionSignatureItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    function_modifiers: config?.functionModifiers,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    where_clause: config?.whereClause,
  };
  return {
    type: 'function_signature_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, functionSignatureItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    functionModifiers(functionModifiers_?: T.FunctionModifiers | undefined) { return _fs(config, functionSignatureItem, 'functionModifiers', functionModifiers_, fields.function_modifiers); },
    name(name_?: T.Identifier | T.Metavariable) { return _fs(config, functionSignatureItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, functionSignatureItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: T.Parameters) { return _fs(config, functionSignatureItem, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: T._Type | undefined) { return _fs(config, functionSignatureItem, 'returnType', returnType_, fields.return_type); },
    whereClause(whereClause_?: T.WhereClause | undefined) { return _fs(config, functionSignatureItem, 'whereClause', whereClause_, fields.where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionSignatureItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionModifiers(config?: T.FunctionModifiersConfig) {
  const fields = {
    async: config?.async,
    default: config?.default,
    const: config?.const,
    unsafe: config?.unsafe,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    fields,
    children,
    async(...async_: "async"[]) { return _fsm(config, functionModifiers, 'async', async_, fields.async); },
    default(...default_: "default"[]) { return _fsm(config, functionModifiers, 'default', default_, fields.default); },
    const(...const_: "const"[]) { return _fsm(config, functionModifiers, 'const', const_, fields.const); },
    unsafe(...unsafe_: "unsafe"[]) { return _fsm(config, functionModifiers, 'unsafe', unsafe_, fields.unsafe); },
    getChildren() { return children; },
    setChildren(...items: T.ExternModifier[]) { return functionModifiers({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whereClause(config: T.WhereClauseConfig) {
  const fields = {
    where: config?.where,
  };
  const children = config?.children ?? [];
  return {
    type: 'where_clause' as const,
    named: true as const,
    fields,
    children,
    where(where_?: "where") { return _fs(config, whereClause, 'where', where_, fields.where); },
    getChildren() { return children; },
    setChildren(...items: T.WherePredicate[]) { return whereClause({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WhereClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function wherePredicate(config: T.WherePredicateConfig) {
  const fields = {
    left: config?.left,
    bounds: config?.bounds,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    fields,
    left(left_?: T.Lifetime | T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericType | T.ReferenceType | T.PointerType | T.TupleType | T.ArrayType | T.HigherRankedTraitBound | T.PrimitiveType) { return _fs(config, wherePredicate, 'left', left_, fields.left); },
    bounds(bounds_?: T.TraitBounds) { return _fs(config, wherePredicate, 'bounds', bounds_, fields.bounds); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implItem(config: T.ImplItemBodyConfig | T.ImplItemSemiConfig) {
  if (config && 'unsafe' in config && 'typeParameters' in config && 'trait' in config && 'type' in config && 'body' in config) return implItemBody(config as T.ImplItemBodyConfig);
  return implItemSemi(config as T.ImplItemSemiConfig);
}
export function implItemBody(config: T.ImplItemBodyConfig) {
  const fields = {
    unsafe: config?.unsafe,
    type_parameters: config?.typeParameters,
    trait: config?.trait,
    type: config?.type,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'impl_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    children,
    unsafe(unsafe_?: "unsafe" | undefined) { return _fs(config, implItemBody, 'unsafe', unsafe_, fields.unsafe); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, implItemBody, 'typeParameters', typeParameters_, fields.type_parameters); },
    trait(trait_?: T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericType | undefined) { return _fs(config, implItemBody, 'trait', trait_, fields.trait); },
    typeField(type?: T._Type) { return _fs(config, implItemBody, 'type', type, fields.type); },
    body(body_?: T.DeclarationList) { return _fs(config, implItemBody, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return implItemBody({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImplItemBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function implItemSemi(config: T.ImplItemSemiConfig) {
  const fields = {
    unsafe: config?.unsafe,
    type_parameters: config?.typeParameters,
    trait: config?.trait,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'impl_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    children,
    unsafe(unsafe_?: "unsafe" | undefined) { return _fs(config, implItemSemi, 'unsafe', unsafe_, fields.unsafe); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, implItemSemi, 'typeParameters', typeParameters_, fields.type_parameters); },
    trait(trait_?: T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericType | undefined) { return _fs(config, implItemSemi, 'trait', trait_, fields.trait); },
    typeField(type?: T._Type) { return _fs(config, implItemSemi, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return implItemSemi({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImplItemSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitItem(config: T.TraitItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    unsafe: config?.unsafe,
    name: config?.name,
    type_parameters: config?.typeParameters,
    bounds: config?.bounds,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'trait_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, traitItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    unsafe(unsafe_?: "unsafe" | undefined) { return _fs(config, traitItem, 'unsafe', unsafe_, fields.unsafe); },
    name(name_?: T._TypeIdentifier) { return _fs(config, traitItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, traitItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    bounds(bounds_?: T.TraitBounds | undefined) { return _fs(config, traitItem, 'bounds', bounds_, fields.bounds); },
    body(body_?: T.DeclarationList) { return _fs(config, traitItem, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return traitItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TraitItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function associatedType(config: T.AssociatedTypeConfig) {
  const fields = {
    type: config?.type,
    name: config?.name,
    type_parameters: config?.typeParameters,
    bounds: config?.bounds,
    where_clause: config?.whereClause,
  };
  return {
    type: 'associated_type' as const,
    named: true as const,
    fields,
    typeField(type?: "type") { return _fs(config, associatedType, 'type', type, fields.type); },
    name(name_?: T._TypeIdentifier) { return _fs(config, associatedType, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, associatedType, 'typeParameters', typeParameters_, fields.type_parameters); },
    bounds(bounds_?: T.TraitBounds | undefined) { return _fs(config, associatedType, 'bounds', bounds_, fields.bounds); },
    whereClause(whereClause_?: T.WhereClause | undefined) { return _fs(config, associatedType, 'whereClause', whereClause_, fields.where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssociatedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitBounds(...children: (T._Type | T.Lifetime | T.HigherRankedTraitBound)[]) {
  return {
    type: 'trait_bounds' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TraitBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function higherRankedTraitBound(config: T.HigherRankedTraitBoundConfig) {
  const fields = {
    for: config?.for,
    type_parameters: config?.typeParameters,
    type: config?.type,
  };
  return {
    type: 'higher_ranked_trait_bound' as const,
    named: true as const,
    fields,
    for(for_?: "for") { return _fs(config, higherRankedTraitBound, 'for', for_, fields.for); },
    typeParameters(typeParameters_?: T.TypeParameters) { return _fs(config, higherRankedTraitBound, 'typeParameters', typeParameters_, fields.type_parameters); },
    typeField(type?: T._Type) { return _fs(config, higherRankedTraitBound, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.HigherRankedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function removedTraitBound(child: T._Type) {
  const children = [child];
  return {
    type: 'removed_trait_bound' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RemovedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: (T.AttributeItem | T.Metavariable | T.TypeParameter | T.LifetimeParameter | T.ConstParameter)[]) {
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constParameter(config: T.ConstParameterConfig) {
  const fields = {
    const: config?.const,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'const_parameter' as const,
    named: true as const,
    fields,
    const(const_?: "const") { return _fs(config, constParameter, 'const', const_, fields.const); },
    name(name_?: T.Identifier) { return _fs(config, constParameter, 'name', name_, fields.name); },
    typeField(type?: T._Type) { return _fs(config, constParameter, 'type', type, fields.type); },
    value(value_?: T.Block | T.Identifier | T.Literal | T.NegativeLiteral | undefined) { return _fs(config, constParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: T.TypeParameterConfig) {
  const fields = {
    name: config?.name,
    bounds: config?.bounds,
    default_type: config?.defaultType,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    name(name_?: T._TypeIdentifier) { return _fs(config, typeParameter, 'name', name_, fields.name); },
    bounds(bounds_?: T.TraitBounds | undefined) { return _fs(config, typeParameter, 'bounds', bounds_, fields.bounds); },
    defaultType(defaultType_?: T._Type | undefined) { return _fs(config, typeParameter, 'defaultType', defaultType_, fields.default_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetimeParameter(config: T.LifetimeParameterConfig) {
  const fields = {
    name: config?.name,
    bounds: config?.bounds,
  };
  return {
    type: 'lifetime_parameter' as const,
    named: true as const,
    fields,
    name(name_?: T.Lifetime) { return _fs(config, lifetimeParameter, 'name', name_, fields.name); },
    bounds(bounds_?: T.TraitBounds | undefined) { return _fs(config, lifetimeParameter, 'bounds', bounds_, fields.bounds); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LifetimeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letDeclaration(config: T.LetDeclarationConfig) {
  const fields = {
    let: config?.let,
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
    type: config?.type,
    value: config?.value,
    alternative: config?.alternative,
  };
  return {
    type: 'let_declaration' as const,
    named: true as const,
    fields,
    let(let_?: "let") { return _fs(config, letDeclaration, 'let', let_, fields.let); },
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier | undefined) { return _fs(config, letDeclaration, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: T.Pattern) { return _fs(config, letDeclaration, 'pattern', pattern_, fields.pattern); },
    typeField(type?: T._Type | undefined) { return _fs(config, letDeclaration, 'type', type, fields.type); },
    value(value_?: T.Expression | undefined) { return _fs(config, letDeclaration, 'value', value_, fields.value); },
    alternative(alternative_?: T.Block | undefined) { return _fs(config, letDeclaration, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LetDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useDeclaration(config: T.UseDeclarationConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    argument: config?.argument,
  };
  return {
    type: 'use_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: T.VisibilityModifier | undefined) { return _fs(config, useDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    argument(argument_?: T.UseClause) { return _fs(config, useDeclaration, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedUseList(config: T.ScopedUseListConfig) {
  const fields = {
    path: config?.path,
    list: config?.list,
  };
  return {
    type: 'scoped_use_list' as const,
    named: true as const,
    fields,
    path(path_?: T.Path | undefined) { return _fs(config, scopedUseList, 'path', path_, fields.path); },
    list(list_?: T.UseList) { return _fs(config, scopedUseList, 'list', list_, fields.list); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ScopedUseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useList(...children: T.UseClause[]) {
  return {
    type: 'use_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useAsClause(config: T.UseAsClauseConfig) {
  const fields = {
    path: config?.path,
    alias: config?.alias,
  };
  return {
    type: 'use_as_clause' as const,
    named: true as const,
    fields,
    path(path_?: T.Path) { return _fs(config, useAsClause, 'path', path_, fields.path); },
    alias(alias_?: T.Identifier) { return _fs(config, useAsClause, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseAsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useWildcard(config?: T.UseWildcardConfig) {
  const fields = {
    path: config?.path,
  };
  return {
    type: 'use_wildcard' as const,
    named: true as const,
    fields,
    path(path_?: T.Path | undefined) { return _fs(config, useWildcard, 'path', path_, fields.path); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseWildcardTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(...children: (T.AttributeItem | T.Parameter | T.SelfParameter | T.VariadicParameter | T._Type)[]) {
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function selfParameter(config: T.SelfParameterConfig) {
  const fields = {
    lifetime: config?.lifetime,
    mutable_specifier: config?.mutableSpecifier,
    self: config?.self,
  };
  return {
    type: 'self_parameter' as const,
    named: true as const,
    fields,
    lifetime(lifetime_?: string) { return _fs(config, selfParameter, 'lifetime', lifetime_, fields.lifetime); },
    mutableSpecifier(mutableSpecifier_?: T.Lifetime | undefined) { return _fs(config, selfParameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    self(self_?: T.MutableSpecifier | T.Self | undefined) { return _fs(config, selfParameter, 'self', self_, fields.self); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SelfParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variadicParameter(config?: T.VariadicParameterConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'variadic_parameter' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier | undefined) { return _fs(config, variadicParameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: T.Pattern | undefined) { return _fs(config, variadicParameter, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VariadicParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameter(config: T.ParameterConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
    type: config?.type,
  };
  return {
    type: 'parameter' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier | undefined) { return _fs(config, parameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: T.Pattern | T.Self) { return _fs(config, parameter, 'pattern', pattern_, fields.pattern); },
    typeField(type?: T._Type) { return _fs(config, parameter, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externModifier(config: T.ExternModifierConfig) {
  const fields = {
    extern: config?.extern,
    string_literal: config?.stringLiteral,
  };
  return {
    type: 'extern_modifier' as const,
    named: true as const,
    fields,
    extern(extern_?: "extern") { return _fs(config, externModifier, 'extern', extern_, fields.extern); },
    stringLiteral(stringLiteral_?: T.StringLiteral | undefined) { return _fs(config, externModifier, 'stringLiteral', stringLiteral_, fields.string_literal); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExternModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function visibilityModifier(config: T.VisibilityModifierCrateConfig | T.VisibilityModifierPubConfig) {
  if (config && 'pub' in config && 'in' in config) return visibilityModifierPub(config as T.VisibilityModifierPubConfig);
  return visibilityModifierCrate(config as T.VisibilityModifierCrateConfig);
}
export function visibilityModifierCrate(config: T.VisibilityModifierCrateConfig) {
  const children = config?.children ?? [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    variant: 'crate' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.Crate) { return visibilityModifierCrate({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VisibilityModifierCrateTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function visibilityModifierPub(config: T.VisibilityModifierPubConfig) {
  const fields = {
    pub: config?.pub,
    in: config?.in,
  };
  const children = config?.children ?? [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    variant: 'pub' as const,
    fields,
    children,
    pub(pub_?: "pub") { return _fs(config, visibilityModifierPub, 'pub', pub_, fields.pub); },
    in(in_?: "in" | undefined) { return _fs(config, visibilityModifierPub, 'in', in_, fields.in); },
    getChild() { return children[0]; },
    setChild(child: (T.Self | T.Super | T.Crate | T.Path)) { return visibilityModifierPub({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VisibilityModifierPubTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function bracketedType(child?: (T._Type | T.QualifiedType)) {
  const children = child != null ? [child] : [];
  return {
    type: 'bracketed_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BracketedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function qualifiedType(config: T.QualifiedTypeConfig) {
  const fields = {
    type: config?.type,
    alias: config?.alias,
  };
  return {
    type: 'qualified_type' as const,
    named: true as const,
    fields,
    typeField(type?: T._Type) { return _fs(config, qualifiedType, 'type', type, fields.type); },
    alias(alias_?: T._Type) { return _fs(config, qualifiedType, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.QualifiedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetime(config: T.LifetimeConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'lifetime' as const,
    named: true as const,
    fields,
    identifier(identifier_?: T.Identifier) { return _fs(config, lifetime, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LifetimeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: T.ArrayTypeConfig) {
  const fields = {
    element: config?.element,
    length: config?.length,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    element(element_?: T._Type) { return _fs(config, arrayType, 'element', element_, fields.element); },
    length(length_?: T.Expression | undefined) { return _fs(config, arrayType, 'length', length_, fields.length); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forLifetimes(config: T.ForLifetimesConfig) {
  const fields = {
    for: config?.for,
  };
  const children = config?.children ?? [];
  return {
    type: 'for_lifetimes' as const,
    named: true as const,
    fields,
    children,
    for(for_?: "for") { return _fs(config, forLifetimes, 'for', for_, fields.for); },
    getChildren() { return children; },
    setChildren(...items: T.Lifetime[]) {
      _assertNonEmpty(items, 'for_lifetimes.children');
      return forLifetimes({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForLifetimesTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: T.FunctionTypeConfig) {
  const fields = {
    for_lifetimes: config?.forLifetimes,
    trait: config?.trait,
    parameters: config?.parameters,
    return_type: config?.returnType,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    forLifetimes(forLifetimes_?: T.ForLifetimes | undefined) { return _fs(config, functionType, 'forLifetimes', forLifetimes_, fields.for_lifetimes); },
    trait(trait_?: T._TypeIdentifier | T.ScopedTypeIdentifier | undefined) { return _fs(config, functionType, 'trait', trait_, fields.trait); },
    parameters(parameters_?: T.Parameters) { return _fs(config, functionType, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: T._Type | undefined) { return _fs(config, functionType, 'returnType', returnType_, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: T._Type[]) {
  _assertNonEmpty(children, 'tuple_type.children');
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitType(text: string) {
  if (text.length === 0) throw new Error(`unit_type: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`unit_type: text '${text}' is a reserved keyword`);
  return {
    type: 'unit_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnitTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function genericFunction(config: T.GenericFunctionConfig) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_function' as const,
    named: true as const,
    fields,
    function(function_?: T.Identifier | T.ScopedIdentifier | T.FieldExpression) { return _fs(config, genericFunction, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: T.TypeArguments) { return _fs(config, genericFunction, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: T.GenericTypeConfig) {
  const fields = {
    type: config?.type,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    typeField(type?: T._TypeIdentifier | T.ReservedIdentifier | T.ScopedTypeIdentifier) { return _fs(config, genericType, 'type', type, fields.type); },
    typeArguments(typeArguments_?: T.TypeArguments) { return _fs(config, genericType, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericTypeWithTurbofish(config: T.GenericTypeWithTurbofishConfig) {
  const fields = {
    type: config?.type,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type_with_turbofish' as const,
    named: true as const,
    fields,
    typeField(type?: T._TypeIdentifier | T.ScopedIdentifier) { return _fs(config, genericTypeWithTurbofish, 'type', type, fields.type); },
    typeArguments(typeArguments_?: T.TypeArguments) { return _fs(config, genericTypeWithTurbofish, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericTypeWithTurbofishTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function boundedType(config: T.BoundedTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'bounded_type' as const,
    named: true as const,
    fields,
    left(left_?: T.Lifetime | T._Type | T.UseBounds) { return _fs(config, boundedType, 'left', left_, fields.left); },
    right(right_?: T.Lifetime | T._Type | T.UseBounds) { return _fs(config, boundedType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BoundedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useBounds(config: T.UseBoundsConfig) {
  const fields = {
    use: config?.use,
  };
  const children = config?.children ?? [];
  return {
    type: 'use_bounds' as const,
    named: true as const,
    fields,
    children,
    use(use_?: "use") { return _fs(config, useBounds, 'use', use_, fields.use); },
    getChildren() { return children; },
    setChildren(...items: (T.Lifetime | T._TypeIdentifier)[]) { return useBounds({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeArguments(...children: (T._Type | T.TypeBinding | T.Lifetime | T.Literal | T.Block | T.TraitBounds)[]) {
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeBinding(config: T.TypeBindingConfig) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
    type: config?.type,
  };
  return {
    type: 'type_binding' as const,
    named: true as const,
    fields,
    name(name_?: T._TypeIdentifier) { return _fs(config, typeBinding, 'name', name_, fields.name); },
    typeArguments(typeArguments_?: T.TypeArguments | undefined) { return _fs(config, typeBinding, 'typeArguments', typeArguments_, fields.type_arguments); },
    typeField(type?: T._Type) { return _fs(config, typeBinding, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeBindingTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceType(config: T.ReferenceTypeConfig) {
  const fields = {
    lifetime: config?.lifetime,
    mutable_specifier: config?.mutableSpecifier,
    type: config?.type,
  };
  return {
    type: 'reference_type' as const,
    named: true as const,
    fields,
    lifetime(lifetime_?: T.Lifetime | undefined) { return _fs(config, referenceType, 'lifetime', lifetime_, fields.lifetime); },
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier | undefined) { return _fs(config, referenceType, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    typeField(type?: T._Type) { return _fs(config, referenceType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReferenceTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pointerType(config: T.PointerTypeConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    type: config?.type,
  };
  return {
    type: 'pointer_type' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier) { return _fs(config, pointerType, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    typeField(type?: T._Type) { return _fs(config, pointerType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PointerTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractType(config: T.AbstractTypeConfig) {
  const fields = {
    impl: config?.impl,
    type_parameters: config?.typeParameters,
    trait: config?.trait,
  };
  return {
    type: 'abstract_type' as const,
    named: true as const,
    fields,
    impl(impl_?: "impl") { return _fs(config, abstractType, 'impl', impl_, fields.impl); },
    typeParameters(typeParameters_?: T.TypeParameters | undefined) { return _fs(config, abstractType, 'typeParameters', typeParameters_, fields.type_parameters); },
    trait(trait_?: T._TypeIdentifier | T.ScopedTypeIdentifier | T.RemovedTraitBound | T.GenericType | T.FunctionType | T.TupleType | T.BoundedType) { return _fs(config, abstractType, 'trait', trait_, fields.trait); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AbstractTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dynamicType(config: T.DynamicTypeConfig) {
  const fields = {
    dyn: config?.dyn,
    trait: config?.trait,
  };
  return {
    type: 'dynamic_type' as const,
    named: true as const,
    fields,
    dyn(dyn_?: "dyn") { return _fs(config, dynamicType, 'dyn', dyn_, fields.dyn); },
    trait(trait_?: T.HigherRankedTraitBound | T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericType | T.FunctionType | T.TupleType) { return _fs(config, dynamicType, 'trait', trait_, fields.trait); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DynamicTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutableSpecifier() {
  return {
    type: 'mutable_specifier' as const,
    named: true as const,
    text: 'mut' as const,
    render: () => 'mut' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'mut' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'mut' as const },
    replace: (t: T.MutableSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'mut' as const }; },
  };
}

export function macroInvocation(config: T.MacroInvocationConfig) {
  const fields = {
    macro: config?.macro,
    token_tree: config?.tokenTree,
  };
  return {
    type: 'macro_invocation' as const,
    named: true as const,
    fields,
    macro(macro_?: T.ScopedIdentifier | T.Identifier | T.ReservedIdentifier) { return _fs(config, macroInvocation, 'macro', macro_, fields.macro); },
    tokenTree(tokenTree_?: T.TokenTree) { return _fs(config, macroInvocation, 'tokenTree', tokenTree_, fields.token_tree); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MacroInvocationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function delimTokenTree(...children: T.DelimTokens[]) {
  return {
    type: 'delim_token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DelimTokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedIdentifier(config: T.ScopedIdentifierConfig) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_identifier' as const,
    named: true as const,
    fields,
    path(path_?: T.Path | T.BracketedType | T.GenericType | undefined) { return _fs(config, scopedIdentifier, 'path', path_, fields.path); },
    name(name_?: T.Identifier | T.Super) { return _fs(config, scopedIdentifier, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ScopedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifierInExpressionPosition(config: T.ScopedTypeIdentifierInExpressionPositionConfig) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_type_identifier_in_expression_position' as const,
    named: true as const,
    fields,
    path(path_?: T.Path | T.GenericType | undefined) { return _fs(config, scopedTypeIdentifierInExpressionPosition, 'path', path_, fields.path); },
    name(name_?: T._TypeIdentifier) { return _fs(config, scopedTypeIdentifierInExpressionPosition, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ScopedTypeIdentifierInExpressionPositionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifier(config: T.ScopedTypeIdentifierConfig) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_type_identifier' as const,
    named: true as const,
    fields,
    path(path_?: T.Path | T.GenericType | T.BracketedType | undefined) { return _fs(config, scopedTypeIdentifier, 'path', path_, fields.path); },
    name(name_?: T._TypeIdentifier) { return _fs(config, scopedTypeIdentifier, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ScopedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpression(config: T.RangeExpressionBinaryConfig | T.RangeExpressionPostfixConfig | T.RangeExpressionPrefixConfig | T.RangeExpressionBareConfig) {
  return rangeExpressionBare(config as T.RangeExpressionBareConfig);
}
export function rangeExpressionBinary(config: T.RangeExpressionBinaryConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'binary' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangeExpressionBinary) { return rangeExpressionBinary({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionBinaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionPostfix(config: T.RangeExpressionPostfixConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'postfix' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangeExpressionPostfix) { return rangeExpressionPostfix({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionPostfixTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionPrefix(config: T.RangeExpressionPrefixConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'prefix' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangeExpressionPrefix) { return rangeExpressionPrefix({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionBare(config: T.RangeExpressionBareConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'bare' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangeExpressionBare) { return rangeExpressionBare({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionBareTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: T.UnaryExpressionConfig) {
  const fields = {
    operator: config?.operator,
    operand: config?.operand,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    operator(operator_?: "-" | "*" | "!") { return _fs(config, unaryExpression, 'operator', operator_, fields.operator); },
    operand(operand_?: T.Expression) { return _fs(config, unaryExpression, 'operand', operand_, fields.operand); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryExpression(config: T.TryExpressionConfig) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'try_expression' as const,
    named: true as const,
    fields,
    value(value_?: T.Expression) { return _fs(config, tryExpression, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceExpression(config: T.ReferenceExpressionConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    value: config?.value,
  };
  return {
    type: 'reference_expression' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier) { return _fs(config, referenceExpression, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    value(value_?: T.Expression) { return _fs(config, referenceExpression, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: T.BinaryExpressionConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(left_?: T.Expression) { return _fs(config, binaryExpression, 'left', left_, fields.left); },
    operator(operator_?: "&&") { return _fs(config, binaryExpression, 'operator', operator_, fields.operator); },
    right(right_?: T.Expression) { return _fs(config, binaryExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: T.AssignmentExpressionConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: T.Expression) { return _fs(config, assignmentExpression, 'left', left_, fields.left); },
    right(right_?: T.Expression) { return _fs(config, assignmentExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function compoundAssignmentExpr(config: T.CompoundAssignmentExprConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    fields,
    left(left_?: T.Expression) { return _fs(config, compoundAssignmentExpr, 'left', left_, fields.left); },
    operator(operator_?: "+=" | "-=" | "*=" | "/=" | "%=" | "&=" | "|=" | "^=" | "<<=" | ">>=") { return _fs(config, compoundAssignmentExpr, 'operator', operator_, fields.operator); },
    right(right_?: T.Expression) { return _fs(config, compoundAssignmentExpr, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeCastExpression(config: T.TypeCastExpressionConfig) {
  const fields = {
    value: config?.value,
    type: config?.type,
  };
  return {
    type: 'type_cast_expression' as const,
    named: true as const,
    fields,
    value(value_?: T.Expression) { return _fs(config, typeCastExpression, 'value', value_, fields.value); },
    typeField(type?: T._Type) { return _fs(config, typeCastExpression, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeCastExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnExpression(child?: T.Expression) {
  const children = child != null ? [child] : [];
  return {
    type: 'return_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReturnExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(child?: T.Expression) {
  const children = child != null ? [child] : [];
  return {
    type: 'yield_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: T.CallExpressionConfig) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: T.ExpressionExceptRange) { return _fs(config, callExpression, 'function', function_, fields.function); },
    arguments(arguments_?: T.Arguments) { return _fs(config, callExpression, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arguments_(...children: (T.AttributeItem | T.Expression)[]) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayExpression(config: T.ArrayExpressionConfig) {
  const fields = {
    attributes: config?.attributes,
    elements: config?.elements,
  };
  return {
    type: 'array_expression' as const,
    named: true as const,
    fields,
    attributes(...attributes_: T.AttributeItem[]) { return _fsm(config, arrayExpression, 'attributes', attributes_, fields.attributes); },
    elements(elements_?: T.Expression | T.AttributeItem) { return _fs(config, arrayExpression, 'elements', elements_, fields.elements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child: T.Expression) {
  const children = [child];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleExpression(config: T.TupleExpressionConfig) {
  const fields = {
    attributes: config?.attributes,
    first: config?.first,
    rest: config?.rest,
    trailing: config?.trailing,
  };
  return {
    type: 'tuple_expression' as const,
    named: true as const,
    fields,
    attributes(...attributes_: T.AttributeItem[]) { return _fsm(config, tupleExpression, 'attributes', attributes_, fields.attributes); },
    first(first_?: T.Expression) { return _fs(config, tupleExpression, 'first', first_, fields.first); },
    rest(...rest_: T.Expression[]) { return _fsm(config, tupleExpression, 'rest', rest_, fields.rest); },
    trailing(trailing_?: T.Expression | undefined) { return _fs(config, tupleExpression, 'trailing', trailing_, fields.trailing); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitExpression(text: string) {
  if (text.length === 0) throw new Error(`unit_expression: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`unit_expression: text '${text}' is a reserved keyword`);
  return {
    type: 'unit_expression' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnitExpressionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function structExpression(config: T.StructExpressionConfig) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'struct_expression' as const,
    named: true as const,
    fields,
    name(name_?: T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericTypeWithTurbofish) { return _fs(config, structExpression, 'name', name_, fields.name); },
    body(body_?: T.FieldInitializerList) { return _fs(config, structExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializerList(...children: (T.ShorthandFieldInitializer | T.FieldInitializer | T.BaseFieldInitializer)[]) {
  return {
    type: 'field_initializer_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldInitializerListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandFieldInitializer(config: T.ShorthandFieldInitializerConfig) {
  const fields = {
    attributes: config?.attributes,
    identifier: config?.identifier,
  };
  return {
    type: 'shorthand_field_initializer' as const,
    named: true as const,
    fields,
    attributes(...attributes_: T.AttributeItem[]) { return _fsm(config, shorthandFieldInitializer, 'attributes', attributes_, fields.attributes); },
    identifier(identifier_?: T.Identifier) { return _fs(config, shorthandFieldInitializer, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ShorthandFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializer(config: T.FieldInitializerConfig) {
  const fields = {
    field: config?.field,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_initializer' as const,
    named: true as const,
    fields,
    children,
    field(field_?: T._FieldIdentifier | T.IntegerLiteral) { return _fs(config, fieldInitializer, 'field', field_, fields.field); },
    value(value_?: T.Expression) { return _fs(config, fieldInitializer, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: T.AttributeItem[]) { return fieldInitializer({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function baseFieldInitializer(child: T.Expression) {
  const children = [child];
  return {
    type: 'base_field_initializer' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BaseFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifExpression(config: T.IfExpressionConfig) {
  const fields = {
    if: config?.if,
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_expression' as const,
    named: true as const,
    fields,
    if(if_?: "if") { return _fs(config, ifExpression, 'if', if_, fields.if); },
    condition(condition_?: T.Condition) { return _fs(config, ifExpression, 'condition', condition_, fields.condition); },
    consequence(consequence_?: T.Block) { return _fs(config, ifExpression, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: T.ElseClause | undefined) { return _fs(config, ifExpression, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IfExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letCondition(config: T.LetConditionConfig) {
  const fields = {
    let: config?.let,
    pattern: config?.pattern,
    value: config?.value,
  };
  return {
    type: 'let_condition' as const,
    named: true as const,
    fields,
    let(let_?: "let") { return _fs(config, letCondition, 'let', let_, fields.let); },
    pattern(pattern_?: T.Pattern) { return _fs(config, letCondition, 'pattern', pattern_, fields.pattern); },
    value(value_?: T.Expression) { return _fs(config, letCondition, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LetConditionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: T.ElseClauseConfig) {
  const fields = {
    else: config?.else,
  };
  const children = config?.children ?? [];
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    children,
    else(else_?: "else") { return _fs(config, elseClause, 'else', else_, fields.else); },
    getChild() { return children[0]; },
    setChild(child: (T.Block | T.IfExpression)) { return elseClause({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchExpression(config: T.MatchExpressionConfig) {
  const fields = {
    match: config?.match,
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'match_expression' as const,
    named: true as const,
    fields,
    match(match_?: "match") { return _fs(config, matchExpression, 'match', match_, fields.match); },
    value(value_?: T.Expression) { return _fs(config, matchExpression, 'value', value_, fields.value); },
    body(body_?: T.MatchBlock) { return _fs(config, matchExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchBlock(...children: T.MatchArm[]) {
  return {
    type: 'match_block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchArm(config: T.MatchArmConfig) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: T.MatchPattern) { return _fs(config, matchArm, 'pattern', pattern_, fields.pattern); },
    value(value_?: T.Expression) { return _fs(config, matchArm, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: (T.AttributeItem | T.InnerAttributeItem)[]) { return matchArm({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lastMatchArm(config: T.LastMatchArmConfig) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'last_match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: T.MatchPattern) { return _fs(config, lastMatchArm, 'pattern', pattern_, fields.pattern); },
    value(value_?: T.Expression) { return _fs(config, lastMatchArm, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: (T.AttributeItem | T.InnerAttributeItem)[]) { return lastMatchArm({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LastMatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchPattern(config: T.MatchPatternConfig) {
  const fields = {
    condition: config?.condition,
  };
  const children = config?.children ?? [];
  return {
    type: 'match_pattern' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: T.Condition | undefined) { return _fs(config, matchPattern, 'condition', condition_, fields.condition); },
    getChild() { return children[0]; },
    setChild(child: T.Pattern) { return matchPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileExpression(config: T.WhileExpressionConfig) {
  const fields = {
    label: config?.label,
    condition: config?.condition,
    body: config?.body,
  };
  return {
    type: 'while_expression' as const,
    named: true as const,
    fields,
    label(label_?: T.Label | undefined) { return _fs(config, whileExpression, 'label', label_, fields.label); },
    condition(condition_?: T.Condition) { return _fs(config, whileExpression, 'condition', condition_, fields.condition); },
    body(body_?: T.Block) { return _fs(config, whileExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WhileExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function loopExpression(config: T.LoopExpressionConfig) {
  const fields = {
    label: config?.label,
    body: config?.body,
  };
  return {
    type: 'loop_expression' as const,
    named: true as const,
    fields,
    label(label_?: T.Label | undefined) { return _fs(config, loopExpression, 'label', label_, fields.label); },
    body(body_?: T.Block) { return _fs(config, loopExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LoopExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forExpression(config: T.ForExpressionConfig) {
  const fields = {
    label: config?.label,
    pattern: config?.pattern,
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'for_expression' as const,
    named: true as const,
    fields,
    label(label_?: T.Label | undefined) { return _fs(config, forExpression, 'label', label_, fields.label); },
    pattern(pattern_?: T.Pattern) { return _fs(config, forExpression, 'pattern', pattern_, fields.pattern); },
    value(value_?: T.Expression) { return _fs(config, forExpression, 'value', value_, fields.value); },
    body(body_?: T.Block) { return _fs(config, forExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constBlock(config: T.ConstBlockConfig) {
  const fields = {
    const: config?.const,
    body: config?.body,
  };
  return {
    type: 'const_block' as const,
    named: true as const,
    fields,
    const(const_?: "const") { return _fs(config, constBlock, 'const', const_, fields.const); },
    body(body_?: T.Block) { return _fs(config, constBlock, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureExpression(config: T.ClosureExpressionBlockConfig | T.ClosureExpressionExprConfig) {
  if (config && 'static' in config && 'async' in config && 'move' in config && 'parameters' in config) return closureExpressionBlock(config as T.ClosureExpressionBlockConfig);
  return closureExpressionExpr(config as T.ClosureExpressionExprConfig);
}
export function closureExpressionBlock(config: T.ClosureExpressionBlockConfig) {
  const fields = {
    static: config?.static,
    async: config?.async,
    move: config?.move,
    parameters: config?.parameters,
  };
  const children = config?.children ?? [];
  return {
    type: 'closure_expression' as const,
    named: true as const,
    variant: 'block' as const,
    fields,
    children,
    static(static_?: "static" | undefined) { return _fs(config, closureExpressionBlock, 'static', static_, fields.static); },
    async(async_?: "async" | undefined) { return _fs(config, closureExpressionBlock, 'async', async_, fields.async); },
    move(move_?: "move" | undefined) { return _fs(config, closureExpressionBlock, 'move', move_, fields.move); },
    parameters(parameters_?: T.ClosureParameters) { return _fs(config, closureExpressionBlock, 'parameters', parameters_, fields.parameters); },
    getChild() { return children[0]; },
    setChild(child: T.ClosureExpressionBlock) { return closureExpressionBlock({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClosureExpressionBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function closureExpressionExpr(config: T.ClosureExpressionExprConfig) {
  const fields = {
    static: config?.static,
    async: config?.async,
    move: config?.move,
    parameters: config?.parameters,
  };
  const children = config?.children ?? [];
  return {
    type: 'closure_expression' as const,
    named: true as const,
    variant: 'expr' as const,
    fields,
    children,
    static(static_?: "static" | undefined) { return _fs(config, closureExpressionExpr, 'static', static_, fields.static); },
    async(async_?: "async" | undefined) { return _fs(config, closureExpressionExpr, 'async', async_, fields.async); },
    move(move_?: "move" | undefined) { return _fs(config, closureExpressionExpr, 'move', move_, fields.move); },
    parameters(parameters_?: T.ClosureParameters) { return _fs(config, closureExpressionExpr, 'parameters', parameters_, fields.parameters); },
    getChild() { return children[0]; },
    setChild(child: T.ClosureExpressionExpr) { return closureExpressionExpr({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClosureExpressionExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureParameters(...children: (T.Pattern | T.Parameter)[]) {
  return {
    type: 'closure_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClosureParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function label(config: T.LabelConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'label' as const,
    named: true as const,
    fields,
    identifier(identifier_?: T.Identifier) { return _fs(config, label, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LabelTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakExpression(config: T.BreakExpressionConfig) {
  const fields = {
    break: config?.break,
    label: config?.label,
    expression: config?.expression,
  };
  return {
    type: 'break_expression' as const,
    named: true as const,
    fields,
    break(break_?: "break") { return _fs(config, breakExpression, 'break', break_, fields.break); },
    label(label_?: T.Label | undefined) { return _fs(config, breakExpression, 'label', label_, fields.label); },
    expression(expression_?: T.Expression | undefined) { return _fs(config, breakExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BreakExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueExpression(config: T.ContinueExpressionConfig) {
  const fields = {
    continue: config?.continue,
    label: config?.label,
  };
  return {
    type: 'continue_expression' as const,
    named: true as const,
    fields,
    continue(continue_?: "continue") { return _fs(config, continueExpression, 'continue', continue_, fields.continue); },
    label(label_?: T.Label | undefined) { return _fs(config, continueExpression, 'label', label_, fields.label); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ContinueExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexExpression(config: T.IndexExpressionConfig) {
  const fields = {
    object: config?.object,
    index: config?.index,
  };
  return {
    type: 'index_expression' as const,
    named: true as const,
    fields,
    object(object_?: T.Expression) { return _fs(config, indexExpression, 'object', object_, fields.object); },
    index(index_?: T.Expression) { return _fs(config, indexExpression, 'index', index_, fields.index); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IndexExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(child: T.Expression) {
  const children = [child];
  return {
    type: 'await_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldExpression(config: T.FieldExpressionConfig) {
  const fields = {
    value: config?.value,
    field: config?.field,
  };
  return {
    type: 'field_expression' as const,
    named: true as const,
    fields,
    value(value_?: T.Expression) { return _fs(config, fieldExpression, 'value', value_, fields.value); },
    field(field_?: T._FieldIdentifier | T.IntegerLiteral) { return _fs(config, fieldExpression, 'field', field_, fields.field); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unsafeBlock(config: T.UnsafeBlockConfig) {
  const fields = {
    unsafe: config?.unsafe,
    block: config?.block,
  };
  return {
    type: 'unsafe_block' as const,
    named: true as const,
    fields,
    unsafe(unsafe_?: "unsafe") { return _fs(config, unsafeBlock, 'unsafe', unsafe_, fields.unsafe); },
    block(block_?: T.Block) { return _fs(config, unsafeBlock, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnsafeBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asyncBlock(config: T.AsyncBlockConfig) {
  const fields = {
    async: config?.async,
    move: config?.move,
    block: config?.block,
  };
  return {
    type: 'async_block' as const,
    named: true as const,
    fields,
    async(async_?: "async") { return _fs(config, asyncBlock, 'async', async_, fields.async); },
    move(move_?: "move" | undefined) { return _fs(config, asyncBlock, 'move', move_, fields.move); },
    block(block_?: T.Block) { return _fs(config, asyncBlock, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AsyncBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genBlock(config: T.GenBlockConfig) {
  const fields = {
    gen: config?.gen,
    move: config?.move,
    block: config?.block,
  };
  return {
    type: 'gen_block' as const,
    named: true as const,
    fields,
    gen(gen_?: "gen") { return _fs(config, genBlock, 'gen', gen_, fields.gen); },
    move(move_?: "move" | undefined) { return _fs(config, genBlock, 'move', move_, fields.move); },
    block(block_?: T.Block) { return _fs(config, genBlock, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryBlock(config: T.TryBlockConfig) {
  const fields = {
    try: config?.try,
    block: config?.block,
  };
  return {
    type: 'try_block' as const,
    named: true as const,
    fields,
    try(try_?: "try") { return _fs(config, tryBlock, 'try', try_, fields.try); },
    block(block_?: T.Block) { return _fs(config, tryBlock, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TryBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(config: T.BlockConfig) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'block' as const,
    named: true as const,
    fields,
    children,
    label(label_?: T.Label | undefined) { return _fs(config, block, 'label', label_, fields.label); },
    getChildren() { return children; },
    setChildren(...items: (T.Statement | T.Expression)[]) { return block({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericPattern(config: T.GenericPatternConfig) {
  const fields = {
    type_arguments: config?.typeArguments,
  };
  const children = config?.children ?? [];
  return {
    type: 'generic_pattern' as const,
    named: true as const,
    fields,
    children,
    typeArguments(typeArguments_?: T.TypeArguments) { return _fs(config, genericPattern, 'typeArguments', typeArguments_, fields.type_arguments); },
    getChild() { return children[0]; },
    setChild(child: (T.Identifier | T.ScopedIdentifier)) { return genericPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(...children: (T.Pattern | T.ClosureExpression)[]) {
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slicePattern(...children: T.Pattern[]) {
  return {
    type: 'slice_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SlicePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleStructPattern(config: T.TupleStructPatternConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'tuple_struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: T.Identifier | T.ScopedIdentifier | T.GenericType) { return _fs(config, tupleStructPattern, 'type', type, fields.type); },
    getChildren() { return children; },
    setChildren(...items: T.Pattern[]) { return tupleStructPattern({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleStructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structPattern(config: T.StructPatternConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: T._TypeIdentifier | T.ScopedTypeIdentifier) { return _fs(config, structPattern, 'type', type, fields.type); },
    getChildren() { return children; },
    setChildren(...items: (T.FieldPattern | T.RemainingFieldPattern)[]) { return structPattern({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldPattern(config: T.FieldPatternShorthandConfig | T.FieldPatternNamedConfig) {
  if (config && 'ref' in config && 'mutableSpecifier' in config) return fieldPatternShorthand(config as T.FieldPatternShorthandConfig);
  return fieldPatternNamed(config as T.FieldPatternNamedConfig);
}
export function fieldPatternShorthand(config: T.FieldPatternShorthandConfig) {
  const fields = {
    ref: config?.ref,
    mutable_specifier: config?.mutableSpecifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_pattern' as const,
    named: true as const,
    variant: 'shorthand' as const,
    fields,
    children,
    ref(ref_?: "ref" | undefined) { return _fs(config, fieldPatternShorthand, 'ref', ref_, fields.ref); },
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier | undefined) { return _fs(config, fieldPatternShorthand, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    getChild() { return children[0]; },
    setChild(child: T.FieldPatternShorthand) { return fieldPatternShorthand({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldPatternShorthandTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function fieldPatternNamed(config: T.FieldPatternNamedConfig) {
  const fields = {
    ref: config?.ref,
    mutable_specifier: config?.mutableSpecifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_pattern' as const,
    named: true as const,
    variant: 'named' as const,
    fields,
    children,
    ref(ref_?: "ref" | undefined) { return _fs(config, fieldPatternNamed, 'ref', ref_, fields.ref); },
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier | undefined) { return _fs(config, fieldPatternNamed, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    getChild() { return children[0]; },
    setChild(child: T.FieldPatternNamed) { return fieldPatternNamed({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldPatternNamedTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutPattern(config: T.MutPatternConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'mut_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier) { return _fs(config, mutPattern, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: T.Pattern) { return _fs(config, mutPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MutPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangePattern(config: T.RangePatternLeftConfig | T.RangePatternPrefixConfig) {
  return rangePatternPrefix(config as T.RangePatternPrefixConfig);
}
export function rangePatternLeft(config: T.RangePatternLeftConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'left' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangePatternLeft) { return rangePatternLeft({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangePatternLeftTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangePatternPrefix(config: T.RangePatternPrefixConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'prefix' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangePatternPrefix) { return rangePatternPrefix({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangePatternPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function refPattern(config: T.RefPatternConfig) {
  const fields = {
    ref: config?.ref,
  };
  const children = config?.children ?? [];
  return {
    type: 'ref_pattern' as const,
    named: true as const,
    fields,
    children,
    ref(ref_?: "ref") { return _fs(config, refPattern, 'ref', ref_, fields.ref); },
    getChild() { return children[0]; },
    setChild(child: T.Pattern) { return refPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RefPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function capturedPattern(config: T.CapturedPatternConfig) {
  const fields = {
    identifier: config?.identifier,
    pattern: config?.pattern,
  };
  return {
    type: 'captured_pattern' as const,
    named: true as const,
    fields,
    identifier(identifier_?: T.Identifier) { return _fs(config, capturedPattern, 'identifier', identifier_, fields.identifier); },
    pattern(pattern_?: T.Pattern) { return _fs(config, capturedPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CapturedPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referencePattern(config: T.ReferencePatternConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'reference_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: T.MutableSpecifier | undefined) { return _fs(config, referencePattern, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: T.Pattern) { return _fs(config, referencePattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReferencePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orPattern(config: T.OrPatternBinaryConfig | T.OrPatternPrefixConfig) {
  return orPatternPrefix(config as T.OrPatternPrefixConfig);
}
export function orPatternBinary(config: T.OrPatternBinaryConfig) {
  const children = config?.children ?? [];
  return {
    type: 'or_pattern' as const,
    named: true as const,
    variant: 'binary' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.OrPatternBinary) { return orPatternBinary({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OrPatternBinaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function orPatternPrefix(config: T.OrPatternPrefixConfig) {
  const children = config?.children ?? [];
  return {
    type: 'or_pattern' as const,
    named: true as const,
    variant: 'prefix' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.OrPatternPrefix) { return orPatternPrefix({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OrPatternPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function negativeLiteral(config: T.NegativeLiteralConfig) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'negative_literal' as const,
    named: true as const,
    fields,
    value(value_?: T.IntegerLiteral | T.FloatLiteral) { return _fs(config, negativeLiteral, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NegativeLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function integerLiteral(text: string) {
  if (text.length === 0) throw new Error(`integer_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`integer_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'integer_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IntegerLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringLiteral(...children: (T.EscapeSequence | T.StringContent)[]) {
  return {
    type: 'string_literal' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rawStringLiteral(config: T.RawStringLiteralConfig) {
  const fields = {
    raw_string_literal_start: config?.rawStringLiteralStart,
    string_content: config?.stringContent,
    raw_string_literal_end: config?.rawStringLiteralEnd,
  };
  return {
    type: 'raw_string_literal' as const,
    named: true as const,
    fields,
    rawStringLiteralStart(rawStringLiteralStart_?: string) { return _fs(config, rawStringLiteral, 'rawStringLiteralStart', rawStringLiteralStart_, fields.raw_string_literal_start); },
    stringContent(stringContent_?: T.StringContent) { return _fs(config, rawStringLiteral, 'stringContent', stringContent_, fields.string_content); },
    rawStringLiteralEnd(rawStringLiteralEnd_?: string) { return _fs(config, rawStringLiteral, 'rawStringLiteralEnd', rawStringLiteralEnd_, fields.raw_string_literal_end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RawStringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function charLiteral(text: string) {
  if (text.length === 0) throw new Error(`char_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`char_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'char_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CharLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_sequence: text '${text}' is a reserved keyword`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function booleanLiteral(text: 'true' | 'false') {
  return {
    type: 'boolean_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.BooleanLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(child?: (T.LineComment | T.BlockComment)) {
  const children = child != null ? [child] : [];
  return {
    type: 'comment' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lineComment(config?: T.LineCommentConfig) {
  const fields = {
    outer: config?.outer,
    inner: config?.inner,
    doc: config?.doc,
  };
  return {
    type: 'line_comment' as const,
    named: true as const,
    fields,
    outer(outer_?: T.OuterDocCommentMarker | undefined) { return _fs(config, lineComment, 'outer', outer_, fields.outer); },
    inner(inner_?: T.InnerDocCommentMarker | undefined) { return _fs(config, lineComment, 'inner', inner_, fields.inner); },
    doc(doc_?: T.DocComment | undefined) { return _fs(config, lineComment, 'doc', doc_, fields.doc); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LineCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function blockComment(config?: T.BlockCommentConfig) {
  const fields = {
    outer: config?.outer,
    inner: config?.inner,
    doc: config?.doc,
  };
  return {
    type: 'block_comment' as const,
    named: true as const,
    fields,
    outer(outer_?: T.OuterDocCommentMarker | undefined) { return _fs(config, blockComment, 'outer', outer_, fields.outer); },
    inner(inner_?: T.InnerDocCommentMarker | undefined) { return _fs(config, blockComment, 'inner', inner_, fields.inner); },
    doc(doc_?: T.DocComment | undefined) { return _fs(config, blockComment, 'doc', doc_, fields.doc); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BlockCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`); if (!_leafRe_identifier.test(text)) throw new Error(`identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shebang(text: string) {
  if (text.length === 0) throw new Error(`shebang: text must be non-empty`); if (!_leafRe_shebang.test(text)) throw new Error(`shebang: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`shebang: text '${text}' is a reserved keyword`);
  return {
    type: 'shebang' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ShebangTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function self() {
  return {
    type: 'self' as const,
    named: true as const,
    text: 'self' as const,
    render: () => 'self' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'self' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'self' as const },
    replace: (t: T.SelfTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'self' as const }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: T.SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function crate() {
  return {
    type: 'crate' as const,
    named: true as const,
    text: 'crate' as const,
    render: () => 'crate' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'crate' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'crate' as const },
    replace: (t: T.CrateTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'crate' as const }; },
  };
}

export function metavariable(text: string) {
  if (text.length === 0) throw new Error(`metavariable: text must be non-empty`); if (!_leafRe_metavariable.test(text)) throw new Error(`metavariable: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`metavariable: text '${text}' is a reserved keyword`);
  return {
    type: 'metavariable' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.MetavariableTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringContent(text: string) {
  if (text.length === 0) throw new Error(`string_content: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_content: text '${text}' is a reserved keyword`);
  return {
    type: 'string_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function rawStringLiteralContent(text: string) {
  if (text.length === 0) throw new Error(`raw_string_literal_content: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`raw_string_literal_content: text '${text}' is a reserved keyword`);
  return {
    type: 'raw_string_literal_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.RawStringLiteralContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function floatLiteral(text: string) {
  if (text.length === 0) throw new Error(`float_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`float_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'float_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FloatLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function primitiveType(text: 'u8' | 'i8' | 'u16' | 'i16' | 'u32' | 'i32' | 'u64' | 'i64' | 'u128' | 'i128' | 'isize' | 'usize' | 'f32' | 'f64' | 'bool' | 'str' | 'char') {
  return {
    type: 'primitive_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PrimitiveTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function letChain(child?: (T._LetChain | T.LetCondition | T.Expression)) {
  const children = child != null ? [child] : [];
  return {
    type: 'let_chain' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LetChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function outerDocCommentMarker(text: string) {
  if (text.length === 0) throw new Error(`outer_doc_comment_marker: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`outer_doc_comment_marker: text '${text}' is a reserved keyword`);
  return {
    type: 'outer_doc_comment_marker' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.OuterDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function innerDocCommentMarker(text: string) {
  if (text.length === 0) throw new Error(`inner_doc_comment_marker: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`inner_doc_comment_marker: text '${text}' is a reserved keyword`);
  return {
    type: 'inner_doc_comment_marker' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.InnerDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeIdentifier(text: string) {
  if (text.length === 0) throw new Error(`type_identifier: text must be non-empty`); if (!_leafRe_typeIdentifier.test(text)) throw new Error(`type_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`type_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function fieldIdentifier(text: string) {
  if (text.length === 0) throw new Error(`field_identifier: text must be non-empty`); if (!_leafRe_fieldIdentifier.test(text)) throw new Error(`field_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`field_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'field_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthandFieldIdentifier(text: string) {
  if (text.length === 0) throw new Error(`shorthand_field_identifier: text must be non-empty`); if (!_leafRe_shorthandFieldIdentifier.test(text)) throw new Error(`shorthand_field_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`shorthand_field_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'shorthand_field_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ShorthandFieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "source_file": FluentNode<"source_file", T.SourceFileConfig>;
  "expression_statement": FluentNode<"expression_statement", T.ExpressionStatementConfig>;
  "macro_definition": FluentNode<"macro_definition", T.MacroDefinitionConfig>;
  "macro_rule": FluentNode<"macro_rule", T.MacroRuleConfig>;
  "token_tree_pattern": FluentNode<"token_tree_pattern", T.TokenTreePatternConfig>;
  "token_binding_pattern": FluentNode<"token_binding_pattern", T.TokenBindingPatternConfig>;
  "token_repetition_pattern": FluentNode<"token_repetition_pattern", T.TokenRepetitionPatternConfig>;
  "fragment_specifier": T.FragmentSpecifier;
  "token_tree": FluentNode<"token_tree", T.TokenTreeConfig>;
  "token_repetition": FluentNode<"token_repetition", T.TokenRepetitionConfig>;
  "attribute_item": FluentNode<"attribute_item", T.AttributeItemConfig>;
  "inner_attribute_item": FluentNode<"inner_attribute_item", T.InnerAttributeItemConfig>;
  "attribute": FluentNode<"attribute", T.AttributeConfig>;
  "mod_item": FluentNode<"mod_item", T.ModItemConfig>;
  "foreign_mod_item": FluentNode<"foreign_mod_item", T.ForeignModItemConfig>;
  "declaration_list": FluentNode<"declaration_list", T.DeclarationListConfig>;
  "struct_item": FluentNode<"struct_item", T.StructItemConfig>;
  "union_item": FluentNode<"union_item", T.UnionItemConfig>;
  "enum_item": FluentNode<"enum_item", T.EnumItemConfig>;
  "enum_variant_list": FluentNode<"enum_variant_list", T.EnumVariantListConfig>;
  "enum_variant": FluentNode<"enum_variant", T.EnumVariantConfig>;
  "field_declaration_list": FluentNode<"field_declaration_list", T.FieldDeclarationListConfig>;
  "field_declaration": FluentNode<"field_declaration", T.FieldDeclarationConfig>;
  "ordered_field_declaration_list": FluentNode<"ordered_field_declaration_list", T.OrderedFieldDeclarationListConfig>;
  "extern_crate_declaration": FluentNode<"extern_crate_declaration", T.ExternCrateDeclarationConfig>;
  "const_item": FluentNode<"const_item", T.ConstItemConfig>;
  "static_item": FluentNode<"static_item", T.StaticItemConfig>;
  "type_item": FluentNode<"type_item", T.TypeItemConfig>;
  "function_item": FluentNode<"function_item", T.FunctionItemConfig>;
  "function_signature_item": FluentNode<"function_signature_item", T.FunctionSignatureItemConfig>;
  "function_modifiers": FluentNode<"function_modifiers", T.FunctionModifiersConfig>;
  "where_clause": FluentNode<"where_clause", T.WhereClauseConfig>;
  "where_predicate": FluentNode<"where_predicate", T.WherePredicateConfig>;
  "impl_item": FluentNode<"impl_item", T.ImplItemConfig>;
  "trait_item": FluentNode<"trait_item", T.TraitItemConfig>;
  "associated_type": FluentNode<"associated_type", T.AssociatedTypeConfig>;
  "trait_bounds": FluentNode<"trait_bounds", T.TraitBoundsConfig>;
  "higher_ranked_trait_bound": FluentNode<"higher_ranked_trait_bound", T.HigherRankedTraitBoundConfig>;
  "removed_trait_bound": FluentNode<"removed_trait_bound", T.RemovedTraitBoundConfig>;
  "type_parameters": FluentNode<"type_parameters", T.TypeParametersConfig>;
  "const_parameter": FluentNode<"const_parameter", T.ConstParameterConfig>;
  "type_parameter": FluentNode<"type_parameter", T.TypeParameterConfig>;
  "lifetime_parameter": FluentNode<"lifetime_parameter", T.LifetimeParameterConfig>;
  "let_declaration": FluentNode<"let_declaration", T.LetDeclarationConfig>;
  "use_declaration": FluentNode<"use_declaration", T.UseDeclarationConfig>;
  "scoped_use_list": FluentNode<"scoped_use_list", T.ScopedUseListConfig>;
  "use_list": FluentNode<"use_list", T.UseListConfig>;
  "use_as_clause": FluentNode<"use_as_clause", T.UseAsClauseConfig>;
  "use_wildcard": FluentNode<"use_wildcard", T.UseWildcardConfig>;
  "parameters": FluentNode<"parameters", T.ParametersConfig>;
  "self_parameter": FluentNode<"self_parameter", T.SelfParameterConfig>;
  "variadic_parameter": FluentNode<"variadic_parameter", T.VariadicParameterConfig>;
  "parameter": FluentNode<"parameter", T.ParameterConfig>;
  "extern_modifier": FluentNode<"extern_modifier", T.ExternModifierConfig>;
  "visibility_modifier": FluentNode<"visibility_modifier", T.VisibilityModifierConfig>;
  "bracketed_type": FluentNode<"bracketed_type", T.BracketedTypeConfig>;
  "qualified_type": FluentNode<"qualified_type", T.QualifiedTypeConfig>;
  "lifetime": FluentNode<"lifetime", T.LifetimeConfig>;
  "array_type": FluentNode<"array_type", T.ArrayTypeConfig>;
  "for_lifetimes": FluentNode<"for_lifetimes", T.ForLifetimesConfig>;
  "function_type": FluentNode<"function_type", T.FunctionTypeConfig>;
  "tuple_type": FluentNode<"tuple_type", T.TupleTypeConfig>;
  "unit_type": T.UnitType;
  "generic_function": FluentNode<"generic_function", T.GenericFunctionConfig>;
  "generic_type": FluentNode<"generic_type", T.GenericTypeConfig>;
  "generic_type_with_turbofish": FluentNode<"generic_type_with_turbofish", T.GenericTypeWithTurbofishConfig>;
  "bounded_type": FluentNode<"bounded_type", T.BoundedTypeConfig>;
  "use_bounds": FluentNode<"use_bounds", T.UseBoundsConfig>;
  "type_arguments": FluentNode<"type_arguments", T.TypeArgumentsConfig>;
  "type_binding": FluentNode<"type_binding", T.TypeBindingConfig>;
  "reference_type": FluentNode<"reference_type", T.ReferenceTypeConfig>;
  "pointer_type": FluentNode<"pointer_type", T.PointerTypeConfig>;
  "abstract_type": FluentNode<"abstract_type", T.AbstractTypeConfig>;
  "dynamic_type": FluentNode<"dynamic_type", T.DynamicTypeConfig>;
  "mutable_specifier": T.MutableSpecifier;
  "macro_invocation": FluentNode<"macro_invocation", T.MacroInvocationConfig>;
  "delim_token_tree": FluentNode<"delim_token_tree", T.DelimTokenTreeConfig>;
  "scoped_identifier": FluentNode<"scoped_identifier", T.ScopedIdentifierConfig>;
  "scoped_type_identifier_in_expression_position": FluentNode<"scoped_type_identifier_in_expression_position", T.ScopedTypeIdentifierInExpressionPositionConfig>;
  "scoped_type_identifier": FluentNode<"scoped_type_identifier", T.ScopedTypeIdentifierConfig>;
  "range_expression": FluentNode<"range_expression", T.RangeExpressionConfig>;
  "unary_expression": FluentNode<"unary_expression", T.UnaryExpressionConfig>;
  "try_expression": FluentNode<"try_expression", T.TryExpressionConfig>;
  "reference_expression": FluentNode<"reference_expression", T.ReferenceExpressionConfig>;
  "binary_expression": FluentNode<"binary_expression", T.BinaryExpressionConfig>;
  "assignment_expression": FluentNode<"assignment_expression", T.AssignmentExpressionConfig>;
  "compound_assignment_expr": FluentNode<"compound_assignment_expr", T.CompoundAssignmentExprConfig>;
  "type_cast_expression": FluentNode<"type_cast_expression", T.TypeCastExpressionConfig>;
  "return_expression": FluentNode<"return_expression", T.ReturnExpressionConfig>;
  "yield_expression": FluentNode<"yield_expression", T.YieldExpressionConfig>;
  "call_expression": FluentNode<"call_expression", T.CallExpressionConfig>;
  "arguments": FluentNode<"arguments", T.ArgumentsConfig>;
  "array_expression": FluentNode<"array_expression", T.ArrayExpressionConfig>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", T.ParenthesizedExpressionConfig>;
  "tuple_expression": FluentNode<"tuple_expression", T.TupleExpressionConfig>;
  "unit_expression": T.UnitExpression;
  "struct_expression": FluentNode<"struct_expression", T.StructExpressionConfig>;
  "field_initializer_list": FluentNode<"field_initializer_list", T.FieldInitializerListConfig>;
  "shorthand_field_initializer": FluentNode<"shorthand_field_initializer", T.ShorthandFieldInitializerConfig>;
  "field_initializer": FluentNode<"field_initializer", T.FieldInitializerConfig>;
  "base_field_initializer": FluentNode<"base_field_initializer", T.BaseFieldInitializerConfig>;
  "if_expression": FluentNode<"if_expression", T.IfExpressionConfig>;
  "let_condition": FluentNode<"let_condition", T.LetConditionConfig>;
  "else_clause": FluentNode<"else_clause", T.ElseClauseConfig>;
  "match_expression": FluentNode<"match_expression", T.MatchExpressionConfig>;
  "match_block": FluentNode<"match_block", T.MatchBlockConfig>;
  "match_arm": FluentNode<"match_arm", T.MatchArmConfig>;
  "last_match_arm": FluentNode<"last_match_arm", T.LastMatchArmConfig>;
  "match_pattern": FluentNode<"match_pattern", T.MatchPatternConfig>;
  "while_expression": FluentNode<"while_expression", T.WhileExpressionConfig>;
  "loop_expression": FluentNode<"loop_expression", T.LoopExpressionConfig>;
  "for_expression": FluentNode<"for_expression", T.ForExpressionConfig>;
  "const_block": FluentNode<"const_block", T.ConstBlockConfig>;
  "closure_expression": FluentNode<"closure_expression", T.ClosureExpressionConfig>;
  "closure_parameters": FluentNode<"closure_parameters", T.ClosureParametersConfig>;
  "label": FluentNode<"label", T.LabelConfig>;
  "break_expression": FluentNode<"break_expression", T.BreakExpressionConfig>;
  "continue_expression": FluentNode<"continue_expression", T.ContinueExpressionConfig>;
  "index_expression": FluentNode<"index_expression", T.IndexExpressionConfig>;
  "await_expression": FluentNode<"await_expression", T.AwaitExpressionConfig>;
  "field_expression": FluentNode<"field_expression", T.FieldExpressionConfig>;
  "unsafe_block": FluentNode<"unsafe_block", T.UnsafeBlockConfig>;
  "async_block": FluentNode<"async_block", T.AsyncBlockConfig>;
  "gen_block": FluentNode<"gen_block", T.GenBlockConfig>;
  "try_block": FluentNode<"try_block", T.TryBlockConfig>;
  "block": FluentNode<"block", T.BlockConfig>;
  "generic_pattern": FluentNode<"generic_pattern", T.GenericPatternConfig>;
  "tuple_pattern": FluentNode<"tuple_pattern", T.TuplePatternConfig>;
  "slice_pattern": FluentNode<"slice_pattern", T.SlicePatternConfig>;
  "tuple_struct_pattern": FluentNode<"tuple_struct_pattern", T.TupleStructPatternConfig>;
  "struct_pattern": FluentNode<"struct_pattern", T.StructPatternConfig>;
  "field_pattern": FluentNode<"field_pattern", T.FieldPatternConfig>;
  "mut_pattern": FluentNode<"mut_pattern", T.MutPatternConfig>;
  "range_pattern": FluentNode<"range_pattern", T.RangePatternConfig>;
  "ref_pattern": FluentNode<"ref_pattern", T.RefPatternConfig>;
  "captured_pattern": FluentNode<"captured_pattern", T.CapturedPatternConfig>;
  "reference_pattern": FluentNode<"reference_pattern", T.ReferencePatternConfig>;
  "or_pattern": FluentNode<"or_pattern", T.OrPatternConfig>;
  "negative_literal": FluentNode<"negative_literal", T.NegativeLiteralConfig>;
  "integer_literal": T.IntegerLiteral;
  "string_literal": FluentNode<"string_literal", T.StringLiteralConfig>;
  "raw_string_literal": FluentNode<"raw_string_literal", T.RawStringLiteralConfig>;
  "char_literal": T.CharLiteral;
  "escape_sequence": T.EscapeSequence;
  "boolean_literal": T.BooleanLiteral;
  "comment": FluentNode<"comment", T.CommentConfig>;
  "line_comment": FluentNode<"line_comment", T.LineCommentConfig>;
  "block_comment": FluentNode<"block_comment", T.BlockCommentConfig>;
  "identifier": T.Identifier;
  "shebang": T.Shebang;
  "self": T.Self;
  "super": T.Super;
  "crate": T.Crate;
  "metavariable": T.Metavariable;
  "string_content": T.StringContent;
  "raw_string_literal_content": T.RawStringLiteralContent;
  "float_literal": T.FloatLiteral;
  "primitive_type": T.PrimitiveType;
  "let_chain": FluentNode<"let_chain", T.LetChainConfig>;
  "outer_doc_comment_marker": T.OuterDocCommentMarker;
  "inner_doc_comment_marker": T.InnerDocCommentMarker;
  "type_identifier": T.TypeIdentifier;
  "field_identifier": T.FieldIdentifier;
  "shorthand_field_identifier": T.ShorthandFieldIdentifier;
};

export const _factoryMap = {
  "source_file": sourceFile,
  "expression_statement": expressionStatement,
  "macro_definition": macroDefinition,
  "macro_rule": macroRule,
  "token_tree_pattern": tokenTreePattern,
  "token_binding_pattern": tokenBindingPattern,
  "token_repetition_pattern": tokenRepetitionPattern,
  "fragment_specifier": fragmentSpecifier,
  "token_tree": tokenTree,
  "token_repetition": tokenRepetition,
  "attribute_item": attributeItem,
  "inner_attribute_item": innerAttributeItem,
  "attribute": attribute,
  "mod_item": modItem,
  "foreign_mod_item": foreignModItem,
  "declaration_list": declarationList,
  "struct_item": structItem,
  "union_item": unionItem,
  "enum_item": enumItem,
  "enum_variant_list": enumVariantList,
  "enum_variant": enumVariant,
  "field_declaration_list": fieldDeclarationList,
  "field_declaration": fieldDeclaration,
  "ordered_field_declaration_list": orderedFieldDeclarationList,
  "extern_crate_declaration": externCrateDeclaration,
  "const_item": constItem,
  "static_item": staticItem,
  "type_item": typeItem,
  "function_item": functionItem,
  "function_signature_item": functionSignatureItem,
  "function_modifiers": functionModifiers,
  "where_clause": whereClause,
  "where_predicate": wherePredicate,
  "impl_item": implItem,
  "trait_item": traitItem,
  "associated_type": associatedType,
  "trait_bounds": traitBounds,
  "higher_ranked_trait_bound": higherRankedTraitBound,
  "removed_trait_bound": removedTraitBound,
  "type_parameters": typeParameters,
  "const_parameter": constParameter,
  "type_parameter": typeParameter,
  "lifetime_parameter": lifetimeParameter,
  "let_declaration": letDeclaration,
  "use_declaration": useDeclaration,
  "scoped_use_list": scopedUseList,
  "use_list": useList,
  "use_as_clause": useAsClause,
  "use_wildcard": useWildcard,
  "parameters": parameters,
  "self_parameter": selfParameter,
  "variadic_parameter": variadicParameter,
  "parameter": parameter,
  "extern_modifier": externModifier,
  "visibility_modifier": visibilityModifier,
  "bracketed_type": bracketedType,
  "qualified_type": qualifiedType,
  "lifetime": lifetime,
  "array_type": arrayType,
  "for_lifetimes": forLifetimes,
  "function_type": functionType,
  "tuple_type": tupleType,
  "unit_type": unitType,
  "generic_function": genericFunction,
  "generic_type": genericType,
  "generic_type_with_turbofish": genericTypeWithTurbofish,
  "bounded_type": boundedType,
  "use_bounds": useBounds,
  "type_arguments": typeArguments,
  "type_binding": typeBinding,
  "reference_type": referenceType,
  "pointer_type": pointerType,
  "abstract_type": abstractType,
  "dynamic_type": dynamicType,
  "mutable_specifier": mutableSpecifier,
  "macro_invocation": macroInvocation,
  "delim_token_tree": delimTokenTree,
  "scoped_identifier": scopedIdentifier,
  "scoped_type_identifier_in_expression_position": scopedTypeIdentifierInExpressionPosition,
  "scoped_type_identifier": scopedTypeIdentifier,
  "range_expression": rangeExpression,
  "unary_expression": unaryExpression,
  "try_expression": tryExpression,
  "reference_expression": referenceExpression,
  "binary_expression": binaryExpression,
  "assignment_expression": assignmentExpression,
  "compound_assignment_expr": compoundAssignmentExpr,
  "type_cast_expression": typeCastExpression,
  "return_expression": returnExpression,
  "yield_expression": yieldExpression,
  "call_expression": callExpression,
  "arguments": arguments_,
  "array_expression": arrayExpression,
  "parenthesized_expression": parenthesizedExpression,
  "tuple_expression": tupleExpression,
  "unit_expression": unitExpression,
  "struct_expression": structExpression,
  "field_initializer_list": fieldInitializerList,
  "shorthand_field_initializer": shorthandFieldInitializer,
  "field_initializer": fieldInitializer,
  "base_field_initializer": baseFieldInitializer,
  "if_expression": ifExpression,
  "let_condition": letCondition,
  "else_clause": elseClause,
  "match_expression": matchExpression,
  "match_block": matchBlock,
  "match_arm": matchArm,
  "last_match_arm": lastMatchArm,
  "match_pattern": matchPattern,
  "while_expression": whileExpression,
  "loop_expression": loopExpression,
  "for_expression": forExpression,
  "const_block": constBlock,
  "closure_expression": closureExpression,
  "closure_parameters": closureParameters,
  "label": label,
  "break_expression": breakExpression,
  "continue_expression": continueExpression,
  "index_expression": indexExpression,
  "await_expression": awaitExpression,
  "field_expression": fieldExpression,
  "unsafe_block": unsafeBlock,
  "async_block": asyncBlock,
  "gen_block": genBlock,
  "try_block": tryBlock,
  "block": block,
  "generic_pattern": genericPattern,
  "tuple_pattern": tuplePattern,
  "slice_pattern": slicePattern,
  "tuple_struct_pattern": tupleStructPattern,
  "struct_pattern": structPattern,
  "field_pattern": fieldPattern,
  "mut_pattern": mutPattern,
  "range_pattern": rangePattern,
  "ref_pattern": refPattern,
  "captured_pattern": capturedPattern,
  "reference_pattern": referencePattern,
  "or_pattern": orPattern,
  "negative_literal": negativeLiteral,
  "integer_literal": integerLiteral,
  "string_literal": stringLiteral,
  "raw_string_literal": rawStringLiteral,
  "char_literal": charLiteral,
  "escape_sequence": escapeSequence,
  "boolean_literal": booleanLiteral,
  "comment": comment,
  "line_comment": lineComment,
  "block_comment": blockComment,
  "identifier": identifier,
  "shebang": shebang,
  "self": self,
  "super": super_,
  "crate": crate,
  "metavariable": metavariable,
  "string_content": stringContent,
  "raw_string_literal_content": rawStringLiteralContent,
  "float_literal": floatLiteral,
  "primitive_type": primitiveType,
  "let_chain": letChain,
  "outer_doc_comment_marker": outerDocCommentMarker,
  "inner_doc_comment_marker": innerDocCommentMarker,
  "type_identifier": typeIdentifier,
  "field_identifier": fieldIdentifier,
  "shorthand_field_identifier": shorthandFieldIdentifier,
} as const;
export type _FactoryMap = typeof _factoryMap;

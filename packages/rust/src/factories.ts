// Auto-generated by @sittir/codegen — do not edit

import type { NodeData } from './types.js';
import type { AbstractType, AbstractTypeTree, Arguments, ArgumentsTree, ArrayExpression, ArrayExpressionComma, ArrayExpressionCommaConfig, ArrayExpressionSemi, ArrayExpressionSemiConfig, ArrayExpressionTree, ArrayType, ArrayTypeTree, AssignmentExpression, AssignmentExpressionTree, AssociatedType, AssociatedTypeTree, AsyncBlock, AsyncBlockTree, Attribute, AttributeItem, AttributeItemTree, AttributeTree, AwaitExpression, AwaitExpressionTree, BaseFieldInitializer, BaseFieldInitializerTree, BinaryExpression, BinaryExpressionAmp, BinaryExpressionAmpConfig, BinaryExpressionAngle, BinaryExpressionAngleConfig, BinaryExpressionCaret, BinaryExpressionCaretConfig, BinaryExpressionCloseAngle, BinaryExpressionCloseAngleConfig, BinaryExpressionMinus, BinaryExpressionMinusConfig, BinaryExpressionPercent, BinaryExpressionPercentConfig, BinaryExpressionPipe, BinaryExpressionPipeConfig, BinaryExpressionPlus, BinaryExpressionPlusConfig, BinaryExpressionSlash, BinaryExpressionSlashConfig, BinaryExpressionStar, BinaryExpressionStarConfig, BinaryExpressionTok_213d, BinaryExpressionTok_213dConfig, BinaryExpressionTok_2626, BinaryExpressionTok_2626Config, BinaryExpressionTok_3c3c, BinaryExpressionTok_3c3cConfig, BinaryExpressionTok_3c3d, BinaryExpressionTok_3c3dConfig, BinaryExpressionTok_3d3d, BinaryExpressionTok_3d3dConfig, BinaryExpressionTok_3e3d, BinaryExpressionTok_3e3dConfig, BinaryExpressionTok_3e3e, BinaryExpressionTok_3e3eConfig, BinaryExpressionTok_7c7c, BinaryExpressionTok_7c7cConfig, BinaryExpressionTree, Block, BlockComment, BlockCommentTree, BlockTree, BooleanLiteral, BooleanLiteralTree, BoundedType, BoundedTypeTree, BracketedType, BracketedTypeTree, BreakExpression, BreakExpressionTree, CallExpression, CallExpressionTree, CapturedPattern, CapturedPatternTree, CharLiteral, CharLiteralTree, ClosureExpression, ClosureExpressionTree, ClosureParameters, ClosureParametersTree, CompoundAssignmentExpr, CompoundAssignmentExprTok_253d, CompoundAssignmentExprTok_253dConfig, CompoundAssignmentExprTok_263d, CompoundAssignmentExprTok_263dConfig, CompoundAssignmentExprTok_2a3d, CompoundAssignmentExprTok_2a3dConfig, CompoundAssignmentExprTok_2b3d, CompoundAssignmentExprTok_2b3dConfig, CompoundAssignmentExprTok_2d3d, CompoundAssignmentExprTok_2d3dConfig, CompoundAssignmentExprTok_2f3d, CompoundAssignmentExprTok_2f3dConfig, CompoundAssignmentExprTok_3c3c3d, CompoundAssignmentExprTok_3c3c3dConfig, CompoundAssignmentExprTok_3e3e3d, CompoundAssignmentExprTok_3e3e3dConfig, CompoundAssignmentExprTok_5e3d, CompoundAssignmentExprTok_5e3dConfig, CompoundAssignmentExprTok_7c3d, CompoundAssignmentExprTok_7c3dConfig, CompoundAssignmentExprTree, Condition, ConstBlock, ConstBlockTree, ConstItem, ConstItemTree, ConstParameter, ConstParameterTree, ContinueExpression, ContinueExpressionTree, Crate, CrateTree, DeclarationList, DeclarationListTree, DeclarationStatement, DocComment, DocCommentTree, DynamicType, DynamicTypeTree, ElseClause, ElseClauseTree, EmptyStatement, EmptyStatementTree, EnumItem, EnumItemTree, EnumVariant, EnumVariantList, EnumVariantListTree, EnumVariantTree, EscapeSequence, EscapeSequenceTree, Expression, ExpressionEndingWithBlock, ExpressionExceptRange, ExpressionStatement, ExpressionStatementSemi, ExpressionStatementSemiConfig, ExpressionStatementTree, ExpressionStatementV1, ExpressionStatementV1Config, ExternCrateDeclaration, ExternCrateDeclarationTree, ExternModifier, ExternModifierTree, FieldDeclaration, FieldDeclarationList, FieldDeclarationListTree, FieldDeclarationTree, FieldExpression, FieldExpressionTree, FieldIdentifier, FieldIdentifierTree, FieldInitializer, FieldInitializerList, FieldInitializerListTree, FieldInitializerTree, FieldPattern, FieldPatternColon, FieldPatternColonConfig, FieldPatternTree, FieldPatternV0, FieldPatternV0Config, FloatLiteral, FloatLiteralTree, ForExpression, ForExpressionTree, ForLifetimes, ForLifetimesTree, ForeignModItem, ForeignModItemBody, ForeignModItemBodyConfig, ForeignModItemSemi, ForeignModItemSemiConfig, ForeignModItemTree, FragmentSpecifier, FragmentSpecifierTree, FunctionItem, FunctionItemTree, FunctionModifiers, FunctionModifiersAsync, FunctionModifiersAsyncConfig, FunctionModifiersConst, FunctionModifiersConstConfig, FunctionModifiersDefault, FunctionModifiersDefaultConfig, FunctionModifiersTree, FunctionModifiersUnsafe, FunctionModifiersUnsafeConfig, FunctionModifiersV4, FunctionModifiersV4Config, FunctionSignatureItem, FunctionSignatureItemTree, FunctionType, FunctionTypeFn, FunctionTypeFnConfig, FunctionTypeTrait, FunctionTypeTraitConfig, FunctionTypeTree, GenBlock, GenBlockTree, GenericFunction, GenericFunctionTree, GenericPattern, GenericPatternTree, GenericType, GenericTypeTree, GenericTypeWithTurbofish, GenericTypeWithTurbofishTree, HigherRankedTraitBound, HigherRankedTraitBoundTree, Identifier, IdentifierTree, IfExpression, IfExpressionTree, ImplItem, ImplItemBody, ImplItemBodyConfig, ImplItemSemi, ImplItemSemiConfig, ImplItemTree, IndexExpression, IndexExpressionTree, InnerAttributeItem, InnerAttributeItemTree, InnerDocCommentMarker, InnerDocCommentMarkerTree, IntegerLiteral, IntegerLiteralTree, Label, LabelTree, LetChain, LetChainTree, LetCondition, LetConditionTree, LetDeclaration, LetDeclarationTree, Lifetime, LifetimeParameter, LifetimeParameterTree, LifetimeTree, LineComment, LineCommentOuter, LineCommentOuterConfig, LineCommentTree, LineCommentV0, LineCommentV0Config, Literal, LiteralPattern, LoopExpression, LoopExpressionTree, MacroDefinition, MacroDefinitionBrace, MacroDefinitionBraceConfig, MacroDefinitionBracket, MacroDefinitionBracketConfig, MacroDefinitionParen, MacroDefinitionParenConfig, MacroDefinitionTree, MacroInvocation, MacroInvocationTree, MacroRule, MacroRuleTree, MatchArm, MatchArmTree, MatchBlock, MatchBlockTree, MatchExpression, MatchExpressionTree, MatchPattern, MatchPatternTree, Metavariable, MetavariableTree, ModItem, ModItemBody, ModItemBodyConfig, ModItemSemi, ModItemSemiConfig, ModItemTree, MutPattern, MutPatternTree, MutableSpecifier, MutableSpecifierTree, NegativeLiteral, NegativeLiteralTree, NeverType, NeverTypeTree, OrPattern, OrPatternRight, OrPatternRightConfig, OrPatternTree, OrPatternV1, OrPatternV1Config, OrderedFieldDeclarationList, OrderedFieldDeclarationListTree, OuterDocCommentMarker, OuterDocCommentMarkerTree, Parameter, ParameterTree, Parameters, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionTree, Path, Pattern, PointerType, PointerTypeConst, PointerTypeConstConfig, PointerTypeMutableSpecifier, PointerTypeMutableSpecifierConfig, PointerTypeTree, PrimitiveType, PrimitiveTypeTree, QualifiedType, QualifiedTypeTree, RangeExpression, RangeExpressionEllipsis, RangeExpressionEllipsisConfig, RangeExpressionTok_2e2e3d, RangeExpressionTok_2e2e3dConfig, RangeExpressionTree, RangeExpressionV0, RangeExpressionV0Config, RangeExpressionV3, RangeExpressionV3Config, RangeExpressionV4, RangeExpressionV4Config, RangePattern, RangePatternEllipsis, RangePatternEllipsisConfig, RangePatternTree, RangePatternV1, RangePatternV1Config, RangePatternV2, RangePatternV2Config, RangePatternV3, RangePatternV3Config, RangePatternV4, RangePatternV4Config, RangePatternV5, RangePatternV5Config, RawStringLiteral, RawStringLiteralTree, RefPattern, RefPatternTree, ReferenceExpression, ReferenceExpressionConst, ReferenceExpressionConstConfig, ReferenceExpressionTree, ReferenceExpressionV1, ReferenceExpressionV1Config, ReferenceExpressionV2, ReferenceExpressionV2Config, ReferencePattern, ReferencePatternTree, ReferenceType, ReferenceTypeTree, RemainingFieldPattern, RemainingFieldPatternTree, RemovedTraitBound, RemovedTraitBoundTree, ReservedIdentifier, ReturnExpression, ReturnExpressionTree, ScopedIdentifier, ScopedIdentifierTree, ScopedTypeIdentifier, ScopedTypeIdentifierTree, ScopedUseList, ScopedUseListTree, Self, SelfParameter, SelfParameterTree, SelfTree, Shebang, ShebangTree, ShorthandFieldIdentifier, ShorthandFieldIdentifierTree, ShorthandFieldInitializer, ShorthandFieldInitializerTree, SlicePattern, SlicePatternTree, SourceFile, SourceFileTree, Statement, StaticItem, StaticItemTree, StringContent, StringContentTree, StringLiteral, StringLiteralTree, StructExpression, StructExpressionTree, StructItem, StructItemTree, StructItemV0, StructItemV0Config, StructItemV1, StructItemV1Config, StructItemV2, StructItemV2Config, StructPattern, StructPatternTree, Super, SuperTree, TokenBindingPattern, TokenBindingPatternTree, TokenRepetition, TokenRepetitionPattern, TokenRepetitionPatternPlus, TokenRepetitionPatternPlusConfig, TokenRepetitionPatternQuestion, TokenRepetitionPatternQuestionConfig, TokenRepetitionPatternStar, TokenRepetitionPatternStarConfig, TokenRepetitionPatternTree, TokenRepetitionPlus, TokenRepetitionPlusConfig, TokenRepetitionQuestion, TokenRepetitionQuestionConfig, TokenRepetitionStar, TokenRepetitionStarConfig, TokenRepetitionTree, TokenTree, TokenTreeBrace, TokenTreeBraceConfig, TokenTreeBracket, TokenTreeBracketConfig, TokenTreeParen, TokenTreeParenConfig, TokenTreePattern, TokenTreePatternBrace, TokenTreePatternBraceConfig, TokenTreePatternBracket, TokenTreePatternBracketConfig, TokenTreePatternParen, TokenTreePatternParenConfig, TokenTreePatternTree, TokenTreeTree, TraitBounds, TraitBoundsTree, TraitItem, TraitItemTree, TryBlock, TryBlockTree, TryExpression, TryExpressionTree, TupleExpression, TupleExpressionTree, TuplePattern, TuplePatternTree, TupleStructPattern, TupleStructPatternTree, TupleType, TupleTypeTree, Type, TypeArguments, TypeArgumentsTree, TypeBinding, TypeBindingTree, TypeCastExpression, TypeCastExpressionTree, TypeIdentifier, TypeIdentifierTree, TypeItem, TypeItemTree, TypeParameter, TypeParameterTree, TypeParameters, TypeParametersTree, UnaryExpression, UnaryExpressionBang, UnaryExpressionBangConfig, UnaryExpressionMinus, UnaryExpressionMinusConfig, UnaryExpressionStar, UnaryExpressionStarConfig, UnaryExpressionTree, UnionItem, UnionItemTree, UnitExpression, UnitExpressionTree, UnitType, UnitTypeTree, UnsafeBlock, UnsafeBlockTree, UseAsClause, UseAsClauseTree, UseBounds, UseBoundsTree, UseClause, UseDeclaration, UseDeclarationTree, UseList, UseListTree, UseWildcard, UseWildcardTree, VariadicParameter, VariadicParameterTree, VisibilityModifier, VisibilityModifierPub, VisibilityModifierPubConfig, VisibilityModifierTree, VisibilityModifierV0, VisibilityModifierV0Config, WhereClause, WhereClauseTree, WherePredicate, WherePredicateBool, WherePredicateBoolConfig, WherePredicateChar, WherePredicateCharConfig, WherePredicateF32, WherePredicateF32Config, WherePredicateF64, WherePredicateF64Config, WherePredicateI128, WherePredicateI128Config, WherePredicateI16, WherePredicateI16Config, WherePredicateI32, WherePredicateI32Config, WherePredicateI64, WherePredicateI64Config, WherePredicateI8, WherePredicateI8Config, WherePredicateIsize, WherePredicateIsizeConfig, WherePredicateStr, WherePredicateStrConfig, WherePredicateTree, WherePredicateU128, WherePredicateU128Config, WherePredicateU16, WherePredicateU16Config, WherePredicateU32, WherePredicateU32Config, WherePredicateU64, WherePredicateU64Config, WherePredicateU8, WherePredicateU8Config, WherePredicateUsize, WherePredicateUsizeConfig, WherePredicateV0, WherePredicateV0Config, WhileExpression, WhileExpressionTree, YieldExpression, YieldExpressionTree } from './types.js';
import type { Edit, AnyNodeData, ConfigOf } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

const RESERVED_KEYWORDS = new Set([
  '_',
  'as',
  'async',
  'await',
  'break',
  'const',
  'continue',
  'default',
  'dyn',
  'else',
  'enum',
  'expr',
  'extern',
  'false',
  'fn',
  'for',
  'gen',
  'ident',
  'if',
  'impl',
  'in',
  'item',
  'let',
  'literal',
  'loop',
  'match',
  'meta',
  'mod',
  'move',
  'pat',
  'pat_param',
  'path',
  'pub',
  'raw',
  'ref',
  'return',
  'static',
  'stmt',
  'struct',
  'trait',
  'true',
  'try',
  'tt',
  'ty',
  'type',
  'union',
  'unsafe',
  'use',
  'vis',
  'where',
  'while',
  'yield',
]);

export function abstract_type_(
  config: ConfigOf<AbstractType>,
) {
  const fields = {
    trait: config.trait,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'abstract_type' as const,
    named: true as const,
    fields,
    trait(trait?: BoundedType | FunctionType | GenericType | RemovedTraitBound | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return trait !== undefined ? abstract_type_({ ...config, trait: trait }) : fields.trait; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? abstract_type_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function arguments_(
  ...children: (AttributeItem | Expression)[]
) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (AttributeItem | Expression)[]) { return arguments_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_expression_(
  config?: ConfigOf<ArrayExpression>,
) {
  if (config && 'length' in config && config.length !== undefined) return array_expression__semi_(config as ArrayExpressionSemiConfig);
  return array_expression__comma_(config as ArrayExpressionCommaConfig);
}

/** Variant factory: `array_expression` — semi form. */
export function array_expression__semi_(
  config?: ArrayExpressionSemiConfig,
) {
  const fields = {
    length: config?.length,
    attributes: config?.attributes,
    elements: config?.elements,
  };
  return {
    type: 'array_expression' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    length(length?: Expression) { return length !== undefined ? array_expression__semi_({ ...config, length: length } as ArrayExpressionSemiConfig) : fields.length; },
    attributes(...attributes: (AttributeItem)[]) { return attributes.length ? array_expression__semi_({ ...config, attributes: attributes } as ArrayExpressionSemiConfig) : fields.attributes; },
    elements(...elements: (AttributeItem | Expression)[]) { return elements.length ? array_expression__semi_({ ...config, elements: elements } as ArrayExpressionSemiConfig) : fields.elements; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `array_expression` — comma form. */
export function array_expression__comma_(
  config?: ArrayExpressionCommaConfig,
) {
  const fields = {
    attributes: config?.attributes,
    elements: config?.elements,
  };
  return {
    type: 'array_expression' as const,
    named: true as const,
    variant: 'comma' as const,
    fields,
    attributes(...attributes: (AttributeItem)[]) { return attributes.length ? array_expression__comma_({ ...config, attributes: attributes } as ArrayExpressionCommaConfig) : fields.attributes; },
    elements(...elements: (AttributeItem | Expression)[]) { return elements.length ? array_expression__comma_({ ...config, elements: elements } as ArrayExpressionCommaConfig) : fields.elements; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function array_type_(
  config: ConfigOf<ArrayType>,
) {
  const fields = {
    element: config.element,
    length: config.length,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    element(element?: Type) { return element !== undefined ? array_type_({ ...config, element: element }) : fields.element; },
    length(length?: Expression) { return length !== undefined ? array_type_({ ...config, length: length }) : fields.length; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_expression_(
  config: ConfigOf<AssignmentExpression>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left?: Expression) { return left !== undefined ? assignment_expression_({ ...config, left: left }) : fields.left; },
    right(right?: Expression) { return right !== undefined ? assignment_expression_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function associated_type_(
  config: ConfigOf<AssociatedType>,
) {
  const fields = {
    bounds: config.bounds,
    name: config.name,
    type_parameters: config.typeParameters,
    where_clause: config.whereClause,
  };
  return {
    type: 'associated_type' as const,
    named: true as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? associated_type_({ ...config, bounds: bounds }) : fields.bounds; },
    name(name?: TypeIdentifier) { return name !== undefined ? associated_type_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? associated_type_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? associated_type_({ ...config, whereClause: whereClause }) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssociatedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function async_block_(
  config: ConfigOf<AsyncBlock>,
) {
  const fields = {
    block: config.block,
  };
  return {
    type: 'async_block' as const,
    named: true as const,
    fields,
    block(block?: Block) { return block !== undefined ? async_block_({ ...config, block: block }) : fields.block; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsyncBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function attribute_(
  config: ConfigOf<Attribute>,
) {
  const fields = {
    arguments: config.arguments,
    value: config.value,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    children,
    arguments(arguments_?: TokenTree) { return arguments_ !== undefined ? attribute_({ ...config, arguments: arguments_ }) : fields.arguments; },
    value(value?: Expression) { return value !== undefined ? attribute_({ ...config, value: value }) : fields.value; },
    child(child?: Path) { return child !== undefined ? attribute_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function attribute_item_(
  config: ConfigOf<AttributeItem>,
) {
  const fields = {
    attribute: config.attribute,
  };
  return {
    type: 'attribute_item' as const,
    named: true as const,
    fields,
    attribute(attribute?: Attribute) { return attribute !== undefined ? attribute_item_({ ...config, attribute: attribute }) : fields.attribute; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function await_expression_(
  child: Expression,
) {
  const children = child ? [child] : [];
  return {
    type: 'await_expression' as const,
    named: true as const,
    children,
    child(c?: Expression) { return c !== undefined ? await_expression_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function base_field_initializer_(
  child: Expression,
) {
  const children = child ? [child] : [];
  return {
    type: 'base_field_initializer' as const,
    named: true as const,
    children,
    child(c?: Expression) { return c !== undefined ? base_field_initializer_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BaseFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function binary_expression_(
  config: ConfigOf<BinaryExpression>,
) {
  return binary_expression__percent_(config as BinaryExpressionPercentConfig);
}

/** Variant factory: `binary_expression` — tok_2626 form. */
export function binary_expression__tok_2626_(
  config: BinaryExpressionTok_2626Config,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_2626' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__tok_2626_({ ...config, left: left } as BinaryExpressionTok_2626Config) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__tok_2626_({ ...config, operator: operator } as BinaryExpressionTok_2626Config) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_2626_({ ...config, right: right } as BinaryExpressionTok_2626Config) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_7c7c form. */
export function binary_expression__tok_7c7c_(
  config: BinaryExpressionTok_7c7cConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_7c7c' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__tok_7c7c_({ ...config, left: left } as BinaryExpressionTok_7c7cConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__tok_7c7c_({ ...config, operator: operator } as BinaryExpressionTok_7c7cConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_7c7c_({ ...config, right: right } as BinaryExpressionTok_7c7cConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — amp form. */
export function binary_expression__amp_(
  config: BinaryExpressionAmpConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'amp' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__amp_({ ...config, left: left } as BinaryExpressionAmpConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__amp_({ ...config, operator: operator } as BinaryExpressionAmpConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__amp_({ ...config, right: right } as BinaryExpressionAmpConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — pipe form. */
export function binary_expression__pipe_(
  config: BinaryExpressionPipeConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'pipe' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__pipe_({ ...config, left: left } as BinaryExpressionPipeConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__pipe_({ ...config, operator: operator } as BinaryExpressionPipeConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__pipe_({ ...config, right: right } as BinaryExpressionPipeConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — caret form. */
export function binary_expression__caret_(
  config: BinaryExpressionCaretConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'caret' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__caret_({ ...config, left: left } as BinaryExpressionCaretConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__caret_({ ...config, operator: operator } as BinaryExpressionCaretConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__caret_({ ...config, right: right } as BinaryExpressionCaretConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3d3d form. */
export function binary_expression__tok_3d3d_(
  config: BinaryExpressionTok_3d3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3d3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__tok_3d3d_({ ...config, left: left } as BinaryExpressionTok_3d3dConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3d3d_({ ...config, operator: operator } as BinaryExpressionTok_3d3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3d3d_({ ...config, right: right } as BinaryExpressionTok_3d3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_213d form. */
export function binary_expression__tok_213d_(
  config: BinaryExpressionTok_213dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_213d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__tok_213d_({ ...config, left: left } as BinaryExpressionTok_213dConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__tok_213d_({ ...config, operator: operator } as BinaryExpressionTok_213dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_213d_({ ...config, right: right } as BinaryExpressionTok_213dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — angle form. */
export function binary_expression__angle_(
  config: BinaryExpressionAngleConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'angle' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__angle_({ ...config, left: left } as BinaryExpressionAngleConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__angle_({ ...config, operator: operator } as BinaryExpressionAngleConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__angle_({ ...config, right: right } as BinaryExpressionAngleConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3c3d form. */
export function binary_expression__tok_3c3d_(
  config: BinaryExpressionTok_3c3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3c3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__tok_3c3d_({ ...config, left: left } as BinaryExpressionTok_3c3dConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3c3d_({ ...config, operator: operator } as BinaryExpressionTok_3c3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3c3d_({ ...config, right: right } as BinaryExpressionTok_3c3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — close_angle form. */
export function binary_expression__close_angle_(
  config: BinaryExpressionCloseAngleConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'close_angle' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__close_angle_({ ...config, left: left } as BinaryExpressionCloseAngleConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__close_angle_({ ...config, operator: operator } as BinaryExpressionCloseAngleConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__close_angle_({ ...config, right: right } as BinaryExpressionCloseAngleConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3e3d form. */
export function binary_expression__tok_3e3d_(
  config: BinaryExpressionTok_3e3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3e3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__tok_3e3d_({ ...config, left: left } as BinaryExpressionTok_3e3dConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3e3d_({ ...config, operator: operator } as BinaryExpressionTok_3e3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3e3d_({ ...config, right: right } as BinaryExpressionTok_3e3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3c3c form. */
export function binary_expression__tok_3c3c_(
  config: BinaryExpressionTok_3c3cConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3c3c' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__tok_3c3c_({ ...config, left: left } as BinaryExpressionTok_3c3cConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3c3c_({ ...config, operator: operator } as BinaryExpressionTok_3c3cConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3c3c_({ ...config, right: right } as BinaryExpressionTok_3c3cConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — tok_3e3e form. */
export function binary_expression__tok_3e3e_(
  config: BinaryExpressionTok_3e3eConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'tok_3e3e' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__tok_3e3e_({ ...config, left: left } as BinaryExpressionTok_3e3eConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__tok_3e3e_({ ...config, operator: operator } as BinaryExpressionTok_3e3eConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__tok_3e3e_({ ...config, right: right } as BinaryExpressionTok_3e3eConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — plus form. */
export function binary_expression__plus_(
  config: BinaryExpressionPlusConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'plus' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__plus_({ ...config, left: left } as BinaryExpressionPlusConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__plus_({ ...config, operator: operator } as BinaryExpressionPlusConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__plus_({ ...config, right: right } as BinaryExpressionPlusConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — minus form. */
export function binary_expression__minus_(
  config: BinaryExpressionMinusConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'minus' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__minus_({ ...config, left: left } as BinaryExpressionMinusConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__minus_({ ...config, operator: operator } as BinaryExpressionMinusConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__minus_({ ...config, right: right } as BinaryExpressionMinusConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — star form. */
export function binary_expression__star_(
  config: BinaryExpressionStarConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'star' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__star_({ ...config, left: left } as BinaryExpressionStarConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__star_({ ...config, operator: operator } as BinaryExpressionStarConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__star_({ ...config, right: right } as BinaryExpressionStarConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — slash form. */
export function binary_expression__slash_(
  config: BinaryExpressionSlashConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'slash' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__slash_({ ...config, left: left } as BinaryExpressionSlashConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__slash_({ ...config, operator: operator } as BinaryExpressionSlashConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__slash_({ ...config, right: right } as BinaryExpressionSlashConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `binary_expression` — percent form. */
export function binary_expression__percent_(
  config: BinaryExpressionPercentConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    variant: 'percent' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? binary_expression__percent_({ ...config, left: left } as BinaryExpressionPercentConfig) : fields.left; },
    operator(operator?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||') { return operator !== undefined ? binary_expression__percent_({ ...config, operator: operator } as BinaryExpressionPercentConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? binary_expression__percent_({ ...config, right: right } as BinaryExpressionPercentConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block_(
  config?: ConfigOf<Block>,
) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'block' as const,
    named: true as const,
    fields,
    children,
    label(label?: Label) { return label !== undefined ? block_({ ...config, label: label }) : fields.label; },
    getChildren() { return children; },
    setChildren(...children: (DeclarationStatement | Expression | ExpressionStatement | Statement)[]) { return block_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function block_comment_(
  config?: ConfigOf<BlockComment>,
) {
  const fields = {
    doc: config?.doc,
    inner: config?.inner,
    outer: config?.outer,
  };
  return {
    type: 'block_comment' as const,
    named: true as const,
    fields,
    doc(doc?: DocComment) { return doc !== undefined ? block_comment_({ ...config, doc: doc }) : fields.doc; },
    inner(inner?: InnerDocCommentMarker) { return inner !== undefined ? block_comment_({ ...config, inner: inner }) : fields.inner; },
    outer(outer?: OuterDocCommentMarker) { return outer !== undefined ? block_comment_({ ...config, outer: outer }) : fields.outer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function bounded_type_(
  config: ConfigOf<BoundedType>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'bounded_type' as const,
    named: true as const,
    fields,
    left(left?: Lifetime | Type | UseBounds) { return left !== undefined ? bounded_type_({ ...config, left: left }) : fields.left; },
    right(right?: Lifetime | Type | UseBounds) { return right !== undefined ? bounded_type_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BoundedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function bracketed_type_(
  child: QualifiedType | Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'bracketed_type' as const,
    named: true as const,
    children,
    child(c?: QualifiedType | Type) { return c !== undefined ? bracketed_type_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BracketedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function break_expression_(
  config?: ConfigOf<BreakExpression>,
) {
  const fields = {
    label: config?.label,
    expression: config?.expression,
  };
  return {
    type: 'break_expression' as const,
    named: true as const,
    fields,
    label(label?: Label) { return label !== undefined ? break_expression_({ ...config, label: label }) : fields.label; },
    expression(expression?: Expression) { return expression !== undefined ? break_expression_({ ...config, expression: expression }) : fields.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call_expression_(
  config: ConfigOf<CallExpression>,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    arguments(arguments_?: Arguments) { return arguments_ !== undefined ? call_expression_({ ...config, arguments: arguments_ }) : fields.arguments; },
    function(function_?: ArrayExpression | AssignmentExpression | AwaitExpression | BinaryExpression | BreakExpression | CallExpression | ClosureExpression | CompoundAssignmentExpr | ContinueExpression | ExpressionEndingWithBlock | ExpressionExceptRange | FieldExpression | GenericFunction | Identifier | IndexExpression | Literal | MacroInvocation | Metavariable | ParenthesizedExpression | ReferenceExpression | ReturnExpression | ScopedIdentifier | Self | StructExpression | TryExpression | TupleExpression | TypeCastExpression | UnaryExpression | UnitExpression | YieldExpression) { return function_ !== undefined ? call_expression_({ ...config, function: function_ }) : fields.function; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function captured_pattern_(
  config: ConfigOf<CapturedPattern>,
) {
  const fields = {
    identifier: config.identifier,
    pattern: config.pattern,
  };
  return {
    type: 'captured_pattern' as const,
    named: true as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? captured_pattern_({ ...config, identifier: identifier }) : fields.identifier; },
    pattern(pattern?: Pattern) { return pattern !== undefined ? captured_pattern_({ ...config, pattern: pattern }) : fields.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CapturedPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function closure_expression_(
  config: ConfigOf<ClosureExpression>,
) {
  const fields = {
    body: config.body,
    parameters: config.parameters,
    return_type: config.returnType,
    async: config.async,
    move: config.move,
    static: config.static,
  };
  return {
    type: 'closure_expression' as const,
    named: true as const,
    fields,
    body(body?: Block | Expression) { return body !== undefined ? closure_expression_({ ...config, body: body }) : fields.body; },
    parameters(parameters?: ClosureParameters) { return parameters !== undefined ? closure_expression_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: Type) { return returnType !== undefined ? closure_expression_({ ...config, returnType: returnType }) : fields.return_type; },
    async(async?: 'async') { return async !== undefined ? closure_expression_({ ...config, async: async }) : fields.async; },
    move(move?: 'move') { return move !== undefined ? closure_expression_({ ...config, move: move }) : fields.move; },
    static(static_?: 'static') { return static_ !== undefined ? closure_expression_({ ...config, static: static_ }) : fields.static; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function closure_parameters_(
  ...children: (Parameter | Pattern)[]
) {
  return {
    type: 'closure_parameters' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Parameter | Pattern)[]) { return closure_parameters_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function compound_assignment_expr_(
  config: ConfigOf<CompoundAssignmentExpr>,
) {
  return compound_assignment_expr__tok_3e3e3d_(config as CompoundAssignmentExprTok_3e3e3dConfig);
}

/** Variant factory: `compound_assignment_expr` — tok_2b3d form. */
export function compound_assignment_expr__tok_2b3d_(
  config: CompoundAssignmentExprTok_2b3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_2b3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_2b3d_({ ...config, left: left } as CompoundAssignmentExprTok_2b3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_2b3d_({ ...config, operator: operator } as CompoundAssignmentExprTok_2b3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_2b3d_({ ...config, right: right } as CompoundAssignmentExprTok_2b3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_2d3d form. */
export function compound_assignment_expr__tok_2d3d_(
  config: CompoundAssignmentExprTok_2d3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_2d3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_2d3d_({ ...config, left: left } as CompoundAssignmentExprTok_2d3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_2d3d_({ ...config, operator: operator } as CompoundAssignmentExprTok_2d3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_2d3d_({ ...config, right: right } as CompoundAssignmentExprTok_2d3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_2a3d form. */
export function compound_assignment_expr__tok_2a3d_(
  config: CompoundAssignmentExprTok_2a3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_2a3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_2a3d_({ ...config, left: left } as CompoundAssignmentExprTok_2a3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_2a3d_({ ...config, operator: operator } as CompoundAssignmentExprTok_2a3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_2a3d_({ ...config, right: right } as CompoundAssignmentExprTok_2a3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_2f3d form. */
export function compound_assignment_expr__tok_2f3d_(
  config: CompoundAssignmentExprTok_2f3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_2f3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_2f3d_({ ...config, left: left } as CompoundAssignmentExprTok_2f3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_2f3d_({ ...config, operator: operator } as CompoundAssignmentExprTok_2f3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_2f3d_({ ...config, right: right } as CompoundAssignmentExprTok_2f3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_253d form. */
export function compound_assignment_expr__tok_253d_(
  config: CompoundAssignmentExprTok_253dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_253d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_253d_({ ...config, left: left } as CompoundAssignmentExprTok_253dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_253d_({ ...config, operator: operator } as CompoundAssignmentExprTok_253dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_253d_({ ...config, right: right } as CompoundAssignmentExprTok_253dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_263d form. */
export function compound_assignment_expr__tok_263d_(
  config: CompoundAssignmentExprTok_263dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_263d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_263d_({ ...config, left: left } as CompoundAssignmentExprTok_263dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_263d_({ ...config, operator: operator } as CompoundAssignmentExprTok_263dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_263d_({ ...config, right: right } as CompoundAssignmentExprTok_263dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_7c3d form. */
export function compound_assignment_expr__tok_7c3d_(
  config: CompoundAssignmentExprTok_7c3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_7c3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_7c3d_({ ...config, left: left } as CompoundAssignmentExprTok_7c3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_7c3d_({ ...config, operator: operator } as CompoundAssignmentExprTok_7c3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_7c3d_({ ...config, right: right } as CompoundAssignmentExprTok_7c3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_5e3d form. */
export function compound_assignment_expr__tok_5e3d_(
  config: CompoundAssignmentExprTok_5e3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_5e3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_5e3d_({ ...config, left: left } as CompoundAssignmentExprTok_5e3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_5e3d_({ ...config, operator: operator } as CompoundAssignmentExprTok_5e3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_5e3d_({ ...config, right: right } as CompoundAssignmentExprTok_5e3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_3c3c3d form. */
export function compound_assignment_expr__tok_3c3c3d_(
  config: CompoundAssignmentExprTok_3c3c3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_3c3c3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_3c3c3d_({ ...config, left: left } as CompoundAssignmentExprTok_3c3c3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_3c3c3d_({ ...config, operator: operator } as CompoundAssignmentExprTok_3c3c3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_3c3c3d_({ ...config, right: right } as CompoundAssignmentExprTok_3c3c3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `compound_assignment_expr` — tok_3e3e3d form. */
export function compound_assignment_expr__tok_3e3e3d_(
  config: CompoundAssignmentExprTok_3e3e3dConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    variant: 'tok_3e3e3d' as const,
    fields,
    left(left?: Expression) { return left !== undefined ? compound_assignment_expr__tok_3e3e3d_({ ...config, left: left } as CompoundAssignmentExprTok_3e3e3dConfig) : fields.left; },
    operator(operator?: '%=' | '&=' | '*=' | '+=' | '-=' | '/=' | '<<=' | '>>=' | '^=' | '|=') { return operator !== undefined ? compound_assignment_expr__tok_3e3e3d_({ ...config, operator: operator } as CompoundAssignmentExprTok_3e3e3dConfig) : fields.operator; },
    right(right?: Expression) { return right !== undefined ? compound_assignment_expr__tok_3e3e3d_({ ...config, right: right } as CompoundAssignmentExprTok_3e3e3dConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function const_block_(
  config: ConfigOf<ConstBlock>,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'const_block' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? const_block_({ ...config, body: body }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function const_item_(
  config: ConfigOf<ConstItem>,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
    visibility_modifier: config.visibilityModifier,
  };
  return {
    type: 'const_item' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? const_item_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? const_item_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? const_item_({ ...config, value: value }) : fields.value; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? const_item_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function const_parameter_(
  config: ConfigOf<ConstParameter>,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  return {
    type: 'const_parameter' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? const_parameter_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? const_parameter_({ ...config, type: type_ }) : fields.type; },
    value(value?: Block | Identifier | Literal | NegativeLiteral) { return value !== undefined ? const_parameter_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function continue_expression_(
  config?: ConfigOf<ContinueExpression>,
) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'continue_expression' as const,
    named: true as const,
    fields,
    label(label?: Label) { return label !== undefined ? continue_expression_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function declaration_list_(
  ...children: (DeclarationStatement)[]
) {
  return {
    type: 'declaration_list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (DeclarationStatement)[]) { return declaration_list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dynamic_type_(
  config: ConfigOf<DynamicType>,
) {
  const fields = {
    trait: config.trait,
  };
  return {
    type: 'dynamic_type' as const,
    named: true as const,
    fields,
    trait(trait?: FunctionType | GenericType | HigherRankedTraitBound | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return trait !== undefined ? dynamic_type_({ ...config, trait: trait }) : fields.trait; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DynamicTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function else_clause_(
  child: Block | IfExpression,
) {
  const children = child ? [child] : [];
  return {
    type: 'else_clause' as const,
    named: true as const,
    children,
    child(c?: Block | IfExpression) { return c !== undefined ? else_clause_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_item_(
  config: ConfigOf<EnumItem>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
    where_clause: config.whereClause,
  };
  return {
    type: 'enum_item' as const,
    named: true as const,
    fields,
    body(body?: EnumVariantList) { return body !== undefined ? enum_item_({ ...config, body: body }) : fields.body; },
    name(name?: TypeIdentifier) { return name !== undefined ? enum_item_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? enum_item_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? enum_item_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? enum_item_({ ...config, whereClause: whereClause }) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_variant_(
  config: ConfigOf<EnumVariant>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    value: config.value,
    visibility_modifier: config.visibilityModifier,
  };
  return {
    type: 'enum_variant' as const,
    named: true as const,
    fields,
    body(body?: FieldDeclarationList | OrderedFieldDeclarationList) { return body !== undefined ? enum_variant_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? enum_variant_({ ...config, name: name }) : fields.name; },
    value(value?: Expression) { return value !== undefined ? enum_variant_({ ...config, value: value }) : fields.value; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? enum_variant_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_variant_list_(
  ...children: (AttributeItem | EnumVariant)[]
) {
  return {
    type: 'enum_variant_list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (AttributeItem | EnumVariant)[]) { return enum_variant_list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expression_statement_(
  child: Expression | ExpressionEndingWithBlock,
) {
  const children = child ? [child] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    child(c?: Expression | ExpressionEndingWithBlock) { return c !== undefined ? expression_statement_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `expression_statement` — semi form. */
export function expression_statement__semi_(
  config?: ExpressionStatementSemiConfig,
) {
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    variant: 'semi' as const,
    children,
    child(child?: Expression | ExpressionEndingWithBlock) { return child !== undefined ? expression_statement__semi_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `expression_statement` — v1 form. */
export function expression_statement__v1_(
  config?: ExpressionStatementV1Config,
) {
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    variant: 'v1' as const,
    children,
    child(child?: Expression | ExpressionEndingWithBlock) { return child !== undefined ? expression_statement__v1_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function extern_crate_declaration_(
  config: ConfigOf<ExternCrateDeclaration>,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
    visibility_modifier: config.visibilityModifier,
    crate: config.crate,
  };
  return {
    type: 'extern_crate_declaration' as const,
    named: true as const,
    fields,
    alias(alias?: Identifier) { return alias !== undefined ? extern_crate_declaration_({ ...config, alias: alias }) : fields.alias; },
    name(name?: Identifier) { return name !== undefined ? extern_crate_declaration_({ ...config, name: name }) : fields.name; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? extern_crate_declaration_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    crate(crate?: Crate) { return crate !== undefined ? extern_crate_declaration_({ ...config, crate: crate }) : fields.crate; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternCrateDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extern_modifier_(
  config?: ConfigOf<ExternModifier>,
) {
  const fields = {
    string_literal: config?.stringLiteral,
  };
  return {
    type: 'extern_modifier' as const,
    named: true as const,
    fields,
    stringLiteral(stringLiteral?: StringLiteral) { return stringLiteral !== undefined ? extern_modifier_({ ...config, stringLiteral: stringLiteral }) : fields.string_literal; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_declaration_(
  config: ConfigOf<FieldDeclaration>,
) {
  const fields = {
    name: config.name,
    type: config.type,
    visibility_modifier: config.visibilityModifier,
  };
  return {
    type: 'field_declaration' as const,
    named: true as const,
    fields,
    name(name?: FieldIdentifier) { return name !== undefined ? field_declaration_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? field_declaration_({ ...config, type: type_ }) : fields.type; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? field_declaration_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_declaration_list_(
  ...children: (AttributeItem | FieldDeclaration)[]
) {
  return {
    type: 'field_declaration_list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (AttributeItem | FieldDeclaration)[]) { return field_declaration_list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_expression_(
  config: ConfigOf<FieldExpression>,
) {
  const fields = {
    field: config.field,
    value: config.value,
  };
  return {
    type: 'field_expression' as const,
    named: true as const,
    fields,
    field(field?: FieldIdentifier | IntegerLiteral) { return field !== undefined ? field_expression_({ ...config, field: field }) : fields.field; },
    value(value?: Expression) { return value !== undefined ? field_expression_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_initializer_(
  config: ConfigOf<FieldInitializer>,
) {
  const fields = {
    field: config.field,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    type: 'field_initializer' as const,
    named: true as const,
    fields,
    children,
    field(field?: FieldIdentifier | IntegerLiteral) { return field !== undefined ? field_initializer_({ ...config, field: field }) : fields.field; },
    value(value?: Expression) { return value !== undefined ? field_initializer_({ ...config, value: value }) : fields.value; },
    getChildren() { return children; },
    setChildren(...children: (AttributeItem)[]) { return field_initializer_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_initializer_list_(
  ...children: (BaseFieldInitializer | FieldInitializer | ShorthandFieldInitializer)[]
) {
  return {
    type: 'field_initializer_list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (BaseFieldInitializer | FieldInitializer | ShorthandFieldInitializer)[]) { return field_initializer_list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_pattern_(
  config: ConfigOf<FieldPattern>,
) {
  if ('pattern' in config && config.pattern !== undefined) return field_pattern__colon_(config as FieldPatternColonConfig);
  return field_pattern__v0_(config as FieldPatternV0Config);
}

/** Variant factory: `field_pattern` — v0 form. */
export function field_pattern__v0_(
  config: FieldPatternV0Config,
) {
  const fields = {
    name: config.name,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'field_pattern' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    name(name?: FieldIdentifier | ShorthandFieldIdentifier) { return name !== undefined ? field_pattern__v0_({ ...config, name: name } as FieldPatternV0Config) : fields.name; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? field_pattern__v0_({ ...config, mutableSpecifier: mutableSpecifier } as FieldPatternV0Config) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `field_pattern` — colon form. */
export function field_pattern__colon_(
  config: FieldPatternColonConfig,
) {
  const fields = {
    name: config.name,
    pattern: config.pattern,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'field_pattern' as const,
    named: true as const,
    variant: 'colon' as const,
    fields,
    name(name?: FieldIdentifier | ShorthandFieldIdentifier) { return name !== undefined ? field_pattern__colon_({ ...config, name: name } as FieldPatternColonConfig) : fields.name; },
    pattern(pattern?: Pattern) { return pattern !== undefined ? field_pattern__colon_({ ...config, pattern: pattern } as FieldPatternColonConfig) : fields.pattern; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? field_pattern__colon_({ ...config, mutableSpecifier: mutableSpecifier } as FieldPatternColonConfig) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function for_expression_(
  config: ConfigOf<ForExpression>,
) {
  const fields = {
    body: config.body,
    pattern: config.pattern,
    value: config.value,
    label: config.label,
  };
  return {
    type: 'for_expression' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? for_expression_({ ...config, body: body }) : fields.body; },
    pattern(pattern?: Pattern) { return pattern !== undefined ? for_expression_({ ...config, pattern: pattern }) : fields.pattern; },
    value(value?: Expression) { return value !== undefined ? for_expression_({ ...config, value: value }) : fields.value; },
    label(label?: Label) { return label !== undefined ? for_expression_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_lifetimes_(
  ...children: (Lifetime)[]
) {
  return {
    type: 'for_lifetimes' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Lifetime)[]) { return for_lifetimes_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForLifetimesTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function foreign_mod_item_(
  config: ConfigOf<ForeignModItem>,
) {
  if ('body' in config && config.body !== undefined) return foreign_mod_item__body_(config as ForeignModItemBodyConfig);
  return foreign_mod_item__semi_(config as ForeignModItemSemiConfig);
}

/** Variant factory: `foreign_mod_item` — semi form. */
export function foreign_mod_item__semi_(
  config: ForeignModItemSemiConfig,
) {
  const fields = {
    visibility_modifier: config.visibilityModifier,
    extern_modifier: config.externModifier,
  };
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? foreign_mod_item__semi_({ ...config, visibilityModifier: visibilityModifier } as ForeignModItemSemiConfig) : fields.visibility_modifier; },
    externModifier(externModifier?: ExternModifier) { return externModifier !== undefined ? foreign_mod_item__semi_({ ...config, externModifier: externModifier } as ForeignModItemSemiConfig) : fields.extern_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForeignModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `foreign_mod_item` — body form. */
export function foreign_mod_item__body_(
  config: ForeignModItemBodyConfig,
) {
  const fields = {
    body: config.body,
    visibility_modifier: config.visibilityModifier,
    extern_modifier: config.externModifier,
  };
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    body(body?: DeclarationList) { return body !== undefined ? foreign_mod_item__body_({ ...config, body: body } as ForeignModItemBodyConfig) : fields.body; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? foreign_mod_item__body_({ ...config, visibilityModifier: visibilityModifier } as ForeignModItemBodyConfig) : fields.visibility_modifier; },
    externModifier(externModifier?: ExternModifier) { return externModifier !== undefined ? foreign_mod_item__body_({ ...config, externModifier: externModifier } as ForeignModItemBodyConfig) : fields.extern_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForeignModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function function_item_(
  config: ConfigOf<FunctionItem>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
    function_modifiers: config.functionModifiers,
    where_clause: config.whereClause,
  };
  return {
    type: 'function_item' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? function_item_({ ...config, body: body }) : fields.body; },
    name(name?: Identifier | Metavariable) { return name !== undefined ? function_item_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: Parameters) { return parameters !== undefined ? function_item_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: Type) { return returnType !== undefined ? function_item_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_item_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? function_item_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    functionModifiers(functionModifiers?: FunctionModifiers) { return functionModifiers !== undefined ? function_item_({ ...config, functionModifiers: functionModifiers }) : fields.function_modifiers; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? function_item_({ ...config, whereClause: whereClause }) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_modifiers_(
  config?: ConfigOf<FunctionModifiers>,
) {
  return function_modifiers__v4_(config as FunctionModifiersV4Config);
}

/** Variant factory: `function_modifiers` — async form. */
export function function_modifiers__async_(
  config?: FunctionModifiersAsyncConfig,
) {
  const fields = {
    async: config?.async,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    variant: 'async' as const,
    fields,
    children,
    async(async?: 'async') { return async !== undefined ? function_modifiers__async_({ ...config, async: async } as FunctionModifiersAsyncConfig) : fields.async; },
    getChildren() { return children; },
    setChildren(...children: (ExternModifier)[]) { return function_modifiers__async_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `function_modifiers` — default form. */
export function function_modifiers__default_(
  config?: FunctionModifiersDefaultConfig,
) {
  const fields = {
    async: config?.async,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    variant: 'default' as const,
    fields,
    children,
    async(async?: 'async') { return async !== undefined ? function_modifiers__default_({ ...config, async: async } as FunctionModifiersDefaultConfig) : fields.async; },
    getChildren() { return children; },
    setChildren(...children: (ExternModifier)[]) { return function_modifiers__default_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `function_modifiers` — const form. */
export function function_modifiers__const_(
  config?: FunctionModifiersConstConfig,
) {
  const fields = {
    async: config?.async,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    variant: 'const' as const,
    fields,
    children,
    async(async?: 'async') { return async !== undefined ? function_modifiers__const_({ ...config, async: async } as FunctionModifiersConstConfig) : fields.async; },
    getChildren() { return children; },
    setChildren(...children: (ExternModifier)[]) { return function_modifiers__const_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `function_modifiers` — unsafe form. */
export function function_modifiers__unsafe_(
  config?: FunctionModifiersUnsafeConfig,
) {
  const fields = {
    async: config?.async,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    variant: 'unsafe' as const,
    fields,
    children,
    async(async?: 'async') { return async !== undefined ? function_modifiers__unsafe_({ ...config, async: async } as FunctionModifiersUnsafeConfig) : fields.async; },
    getChildren() { return children; },
    setChildren(...children: (ExternModifier)[]) { return function_modifiers__unsafe_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `function_modifiers` — v4 form. */
export function function_modifiers__v4_(
  config?: FunctionModifiersV4Config,
) {
  const fields = {
    async: config?.async,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    variant: 'v4' as const,
    fields,
    children,
    async(async?: 'async') { return async !== undefined ? function_modifiers__v4_({ ...config, async: async } as FunctionModifiersV4Config) : fields.async; },
    getChildren() { return children; },
    setChildren(...children: (ExternModifier)[]) { return function_modifiers__v4_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function function_signature_item_(
  config: ConfigOf<FunctionSignatureItem>,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
    function_modifiers: config.functionModifiers,
    where_clause: config.whereClause,
  };
  return {
    type: 'function_signature_item' as const,
    named: true as const,
    fields,
    name(name?: Identifier | Metavariable) { return name !== undefined ? function_signature_item_({ ...config, name: name }) : fields.name; },
    parameters(parameters?: Parameters) { return parameters !== undefined ? function_signature_item_({ ...config, parameters: parameters }) : fields.parameters; },
    returnType(returnType?: Type) { return returnType !== undefined ? function_signature_item_({ ...config, returnType: returnType }) : fields.return_type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? function_signature_item_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? function_signature_item_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    functionModifiers(functionModifiers?: FunctionModifiers) { return functionModifiers !== undefined ? function_signature_item_({ ...config, functionModifiers: functionModifiers }) : fields.function_modifiers; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? function_signature_item_({ ...config, whereClause: whereClause }) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_type_(
  config: ConfigOf<FunctionType>,
) {
  if ('trait' in config && config.trait !== undefined) return function_type__trait_(config as FunctionTypeTraitConfig);
  return function_type__fn_(config as FunctionTypeFnConfig);
}

/** Variant factory: `function_type` — trait form. */
export function function_type__trait_(
  config: FunctionTypeTraitConfig,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    trait: config.trait,
    for_lifetimes: config.forLifetimes,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    variant: 'trait' as const,
    fields,
    parameters(parameters?: Parameters) { return parameters !== undefined ? function_type__trait_({ ...config, parameters: parameters } as FunctionTypeTraitConfig) : fields.parameters; },
    returnType(returnType?: Type) { return returnType !== undefined ? function_type__trait_({ ...config, returnType: returnType } as FunctionTypeTraitConfig) : fields.return_type; },
    trait(trait?: ScopedTypeIdentifier | TypeIdentifier) { return trait !== undefined ? function_type__trait_({ ...config, trait: trait } as FunctionTypeTraitConfig) : fields.trait; },
    forLifetimes(forLifetimes?: ForLifetimes) { return forLifetimes !== undefined ? function_type__trait_({ ...config, forLifetimes: forLifetimes } as FunctionTypeTraitConfig) : fields.for_lifetimes; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `function_type` — fn form. */
export function function_type__fn_(
  config: FunctionTypeFnConfig,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    for_lifetimes: config.forLifetimes,
    function_modifiers: config.functionModifiers,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    variant: 'fn' as const,
    fields,
    parameters(parameters?: Parameters) { return parameters !== undefined ? function_type__fn_({ ...config, parameters: parameters } as FunctionTypeFnConfig) : fields.parameters; },
    returnType(returnType?: Type) { return returnType !== undefined ? function_type__fn_({ ...config, returnType: returnType } as FunctionTypeFnConfig) : fields.return_type; },
    forLifetimes(forLifetimes?: ForLifetimes) { return forLifetimes !== undefined ? function_type__fn_({ ...config, forLifetimes: forLifetimes } as FunctionTypeFnConfig) : fields.for_lifetimes; },
    functionModifiers(functionModifiers?: FunctionModifiers) { return functionModifiers !== undefined ? function_type__fn_({ ...config, functionModifiers: functionModifiers } as FunctionTypeFnConfig) : fields.function_modifiers; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function gen_block_(
  config: ConfigOf<GenBlock>,
) {
  const fields = {
    block: config.block,
  };
  return {
    type: 'gen_block' as const,
    named: true as const,
    fields,
    block(block?: Block) { return block !== undefined ? gen_block_({ ...config, block: block }) : fields.block; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_function_(
  config: ConfigOf<GenericFunction>,
) {
  const fields = {
    function: config.function,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_function' as const,
    named: true as const,
    fields,
    function(function_?: FieldExpression | Identifier | ScopedIdentifier) { return function_ !== undefined ? generic_function_({ ...config, function: function_ }) : fields.function; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? generic_function_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_pattern_(
  config: ConfigOf<GenericPattern>,
) {
  const fields = {
    type_arguments: config.typeArguments,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'generic_pattern' as const,
    named: true as const,
    fields,
    children,
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? generic_pattern_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    child(child?: Identifier | ScopedIdentifier) { return child !== undefined ? generic_pattern_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_(
  config: ConfigOf<GenericType>,
) {
  const fields = {
    type: config.type,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    typeField(type_?: Identifier | ScopedIdentifier | ScopedTypeIdentifier | TypeIdentifier) { return type_ !== undefined ? generic_type_({ ...config, type: type_ }) : fields.type; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? generic_type_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_with_turbofish_(
  config: ConfigOf<GenericTypeWithTurbofish>,
) {
  const fields = {
    type: config.type,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_type_with_turbofish' as const,
    named: true as const,
    fields,
    typeField(type_?: ScopedIdentifier | TypeIdentifier) { return type_ !== undefined ? generic_type_with_turbofish_({ ...config, type: type_ }) : fields.type; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? generic_type_with_turbofish_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeWithTurbofishTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function higher_ranked_trait_bound_(
  config: ConfigOf<HigherRankedTraitBound>,
) {
  const fields = {
    type: config.type,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'higher_ranked_trait_bound' as const,
    named: true as const,
    fields,
    typeField(type_?: Type) { return type_ !== undefined ? higher_ranked_trait_bound_({ ...config, type: type_ }) : fields.type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? higher_ranked_trait_bound_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HigherRankedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_expression_(
  config: ConfigOf<IfExpression>,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'if_expression' as const,
    named: true as const,
    fields,
    alternative(alternative?: ElseClause) { return alternative !== undefined ? if_expression_({ ...config, alternative: alternative }) : fields.alternative; },
    condition(condition?: Condition | Expression | LetChain | LetCondition) { return condition !== undefined ? if_expression_({ ...config, condition: condition }) : fields.condition; },
    consequence(consequence?: Block) { return consequence !== undefined ? if_expression_({ ...config, consequence: consequence }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function impl_item_(
  config: ConfigOf<ImplItem>,
) {
  if ('body' in config && config.body !== undefined) return impl_item__body_(config as ImplItemBodyConfig);
  return impl_item__semi_(config as ImplItemSemiConfig);
}

/** Variant factory: `impl_item` — body form. */
export function impl_item__body_(
  config: ImplItemBodyConfig,
) {
  const fields = {
    body: config.body,
    trait: config.trait,
    type: config.type,
    type_parameters: config.typeParameters,
    where_clause: config.whereClause,
  };
  return {
    type: 'impl_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    body(body?: DeclarationList) { return body !== undefined ? impl_item__body_({ ...config, body: body } as ImplItemBodyConfig) : fields.body; },
    trait(trait?: GenericType | ScopedTypeIdentifier | TypeIdentifier) { return trait !== undefined ? impl_item__body_({ ...config, trait: trait } as ImplItemBodyConfig) : fields.trait; },
    typeField(type_?: Type) { return type_ !== undefined ? impl_item__body_({ ...config, type: type_ } as ImplItemBodyConfig) : fields.type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? impl_item__body_({ ...config, typeParameters: typeParameters } as ImplItemBodyConfig) : fields.type_parameters; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? impl_item__body_({ ...config, whereClause: whereClause } as ImplItemBodyConfig) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `impl_item` — semi form. */
export function impl_item__semi_(
  config: ImplItemSemiConfig,
) {
  const fields = {
    trait: config.trait,
    type: config.type,
    type_parameters: config.typeParameters,
    where_clause: config.whereClause,
  };
  return {
    type: 'impl_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    trait(trait?: GenericType | ScopedTypeIdentifier | TypeIdentifier) { return trait !== undefined ? impl_item__semi_({ ...config, trait: trait } as ImplItemSemiConfig) : fields.trait; },
    typeField(type_?: Type) { return type_ !== undefined ? impl_item__semi_({ ...config, type: type_ } as ImplItemSemiConfig) : fields.type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? impl_item__semi_({ ...config, typeParameters: typeParameters } as ImplItemSemiConfig) : fields.type_parameters; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? impl_item__semi_({ ...config, whereClause: whereClause } as ImplItemSemiConfig) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function index_expression_(
  config: ConfigOf<IndexExpression>,
) {
  const fields = {
    object: config.object,
    index: config.index,
  };
  return {
    type: 'index_expression' as const,
    named: true as const,
    fields,
    object(object?: Expression) { return object !== undefined ? index_expression_({ ...config, object: object }) : fields.object; },
    index(index?: Expression) { return index !== undefined ? index_expression_({ ...config, index: index }) : fields.index; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function inner_attribute_item_(
  config: ConfigOf<InnerAttributeItem>,
) {
  const fields = {
    attribute: config.attribute,
  };
  return {
    type: 'inner_attribute_item' as const,
    named: true as const,
    fields,
    attribute(attribute?: Attribute) { return attribute !== undefined ? inner_attribute_item_({ ...config, attribute: attribute }) : fields.attribute; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InnerAttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function label_(
  config: ConfigOf<Label>,
) {
  const fields = {
    identifier: config.identifier,
  };
  return {
    type: 'label' as const,
    named: true as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? label_({ ...config, identifier: identifier }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabelTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function let_chain_(
  ...children: (Expression | LetCondition)[]
) {
  return {
    type: 'let_chain' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Expression | LetCondition)[]) { return let_chain_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function let_condition_(
  config: ConfigOf<LetCondition>,
) {
  const fields = {
    pattern: config.pattern,
    value: config.value,
  };
  return {
    type: 'let_condition' as const,
    named: true as const,
    fields,
    pattern(pattern?: Pattern) { return pattern !== undefined ? let_condition_({ ...config, pattern: pattern }) : fields.pattern; },
    value(value?: Expression) { return value !== undefined ? let_condition_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetConditionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function let_declaration_(
  config: ConfigOf<LetDeclaration>,
) {
  const fields = {
    alternative: config.alternative,
    pattern: config.pattern,
    type: config.type,
    value: config.value,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'let_declaration' as const,
    named: true as const,
    fields,
    alternative(alternative?: Block) { return alternative !== undefined ? let_declaration_({ ...config, alternative: alternative }) : fields.alternative; },
    pattern(pattern?: Pattern) { return pattern !== undefined ? let_declaration_({ ...config, pattern: pattern }) : fields.pattern; },
    typeField(type_?: Type) { return type_ !== undefined ? let_declaration_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? let_declaration_({ ...config, value: value }) : fields.value; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? let_declaration_({ ...config, mutableSpecifier: mutableSpecifier }) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lifetime_(
  config: ConfigOf<Lifetime>,
) {
  const fields = {
    identifier: config.identifier,
  };
  return {
    type: 'lifetime' as const,
    named: true as const,
    fields,
    identifier(identifier?: Identifier) { return identifier !== undefined ? lifetime_({ ...config, identifier: identifier }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lifetime_parameter_(
  config: ConfigOf<LifetimeParameter>,
) {
  const fields = {
    bounds: config.bounds,
    name: config.name,
  };
  return {
    type: 'lifetime_parameter' as const,
    named: true as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? lifetime_parameter_({ ...config, bounds: bounds }) : fields.bounds; },
    name(name?: Lifetime) { return name !== undefined ? lifetime_parameter_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function line_comment_(
  config?: ConfigOf<LineComment>,
) {
  if (config && 'outer' in config && config.outer !== undefined) return line_comment__outer_(config as LineCommentOuterConfig);
  return line_comment__v0_(config as LineCommentV0Config);
}

/** Variant factory: `line_comment` — v0 form. */
export function line_comment__v0_(
  config?: LineCommentV0Config,
) {
  return {
    type: 'line_comment' as const,
    named: true as const,
    variant: 'v0' as const,
    fields: {},
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LineCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `line_comment` — outer form. */
export function line_comment__outer_(
  config?: LineCommentOuterConfig,
) {
  const fields = {
    doc: config?.doc,
    inner: config?.inner,
    outer: config?.outer,
  };
  return {
    type: 'line_comment' as const,
    named: true as const,
    variant: 'outer' as const,
    fields,
    doc(doc?: DocComment) { return doc !== undefined ? line_comment__outer_({ ...config, doc: doc } as LineCommentOuterConfig) : fields.doc; },
    inner(inner?: InnerDocCommentMarker) { return inner !== undefined ? line_comment__outer_({ ...config, inner: inner } as LineCommentOuterConfig) : fields.inner; },
    outer(outer?: OuterDocCommentMarker) { return outer !== undefined ? line_comment__outer_({ ...config, outer: outer } as LineCommentOuterConfig) : fields.outer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LineCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function loop_expression_(
  config: ConfigOf<LoopExpression>,
) {
  const fields = {
    body: config.body,
    label: config.label,
  };
  return {
    type: 'loop_expression' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? loop_expression_({ ...config, body: body }) : fields.body; },
    label(label?: Label) { return label !== undefined ? loop_expression_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LoopExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function macro_definition_(
  config: ConfigOf<MacroDefinition>,
) {
  return macro_definition__brace_(config as MacroDefinitionBraceConfig);
}

/** Variant factory: `macro_definition` — paren form. */
export function macro_definition__paren_(
  config: MacroDefinitionParenConfig,
) {
  const fields = {
    name: config.name,
    rules: config.rules,
  };
  return {
    type: 'macro_definition' as const,
    named: true as const,
    variant: 'paren' as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? macro_definition__paren_({ ...config, name: name } as MacroDefinitionParenConfig) : fields.name; },
    rules(...rules: (MacroRule)[]) { return rules.length ? macro_definition__paren_({ ...config, rules: rules } as MacroDefinitionParenConfig) : fields.rules; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `macro_definition` — bracket form. */
export function macro_definition__bracket_(
  config: MacroDefinitionBracketConfig,
) {
  const fields = {
    name: config.name,
    rules: config.rules,
  };
  return {
    type: 'macro_definition' as const,
    named: true as const,
    variant: 'bracket' as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? macro_definition__bracket_({ ...config, name: name } as MacroDefinitionBracketConfig) : fields.name; },
    rules(...rules: (MacroRule)[]) { return rules.length ? macro_definition__bracket_({ ...config, rules: rules } as MacroDefinitionBracketConfig) : fields.rules; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `macro_definition` — brace form. */
export function macro_definition__brace_(
  config: MacroDefinitionBraceConfig,
) {
  const fields = {
    name: config.name,
    rules: config.rules,
  };
  return {
    type: 'macro_definition' as const,
    named: true as const,
    variant: 'brace' as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? macro_definition__brace_({ ...config, name: name } as MacroDefinitionBraceConfig) : fields.name; },
    rules(...rules: (MacroRule)[]) { return rules.length ? macro_definition__brace_({ ...config, rules: rules } as MacroDefinitionBraceConfig) : fields.rules; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macro_invocation_(
  config: ConfigOf<MacroInvocation>,
) {
  const fields = {
    macro: config.macro,
    token_tree: config.tokenTree,
  };
  return {
    type: 'macro_invocation' as const,
    named: true as const,
    fields,
    macro(macro?: Identifier | ScopedIdentifier) { return macro !== undefined ? macro_invocation_({ ...config, macro: macro }) : fields.macro; },
    tokenTree(tokenTree?: TokenTree) { return tokenTree !== undefined ? macro_invocation_({ ...config, tokenTree: tokenTree }) : fields.token_tree; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroInvocationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function macro_rule_(
  config: ConfigOf<MacroRule>,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'macro_rule' as const,
    named: true as const,
    fields,
    left(left?: TokenTreePattern) { return left !== undefined ? macro_rule_({ ...config, left: left }) : fields.left; },
    right(right?: TokenTree) { return right !== undefined ? macro_rule_({ ...config, right: right }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroRuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_arm_(
  config: ConfigOf<MatchArm>,
) {
  const fields = {
    pattern: config.pattern,
    value: config.value,
  };
  const children = config.children ?? [];
  return {
    type: 'match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern?: MatchPattern) { return pattern !== undefined ? match_arm_({ ...config, pattern: pattern }) : fields.pattern; },
    value(value?: Expression | ExpressionEndingWithBlock) { return value !== undefined ? match_arm_({ ...config, value: value }) : fields.value; },
    getChildren() { return children; },
    setChildren(...children: (AttributeItem | InnerAttributeItem)[]) { return match_arm_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_block_(
  ...children: (MatchArm)[]
) {
  return {
    type: 'match_block' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (MatchArm)[]) { return match_block_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_expression_(
  config: ConfigOf<MatchExpression>,
) {
  const fields = {
    body: config.body,
    value: config.value,
  };
  return {
    type: 'match_expression' as const,
    named: true as const,
    fields,
    body(body?: MatchBlock) { return body !== undefined ? match_expression_({ ...config, body: body }) : fields.body; },
    value(value?: Expression) { return value !== undefined ? match_expression_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_pattern_(
  config: ConfigOf<MatchPattern>,
) {
  const fields = {
    condition: config.condition,
  };
  const children = config.children ? [config.children] : [];
  return {
    type: 'match_pattern' as const,
    named: true as const,
    fields,
    children,
    condition(condition?: Condition | Expression | LetChain | LetCondition) { return condition !== undefined ? match_pattern_({ ...config, condition: condition }) : fields.condition; },
    child(child?: Pattern) { return child !== undefined ? match_pattern_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function mod_item_(
  config: ConfigOf<ModItem>,
) {
  if ('body' in config && config.body !== undefined) return mod_item__body_(config as ModItemBodyConfig);
  return mod_item__semi_(config as ModItemSemiConfig);
}

/** Variant factory: `mod_item` — semi form. */
export function mod_item__semi_(
  config: ModItemSemiConfig,
) {
  const fields = {
    name: config.name,
    visibility_modifier: config.visibilityModifier,
  };
  return {
    type: 'mod_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? mod_item__semi_({ ...config, name: name } as ModItemSemiConfig) : fields.name; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? mod_item__semi_({ ...config, visibilityModifier: visibilityModifier } as ModItemSemiConfig) : fields.visibility_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `mod_item` — body form. */
export function mod_item__body_(
  config: ModItemBodyConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    visibility_modifier: config.visibilityModifier,
  };
  return {
    type: 'mod_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    body(body?: DeclarationList) { return body !== undefined ? mod_item__body_({ ...config, body: body } as ModItemBodyConfig) : fields.body; },
    name(name?: Identifier) { return name !== undefined ? mod_item__body_({ ...config, name: name } as ModItemBodyConfig) : fields.name; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? mod_item__body_({ ...config, visibilityModifier: visibilityModifier } as ModItemBodyConfig) : fields.visibility_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mut_pattern_(
  config: ConfigOf<MutPattern>,
) {
  const fields = {
    mutable_specifier: config.mutableSpecifier,
    pattern: config.pattern,
  };
  return {
    type: 'mut_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? mut_pattern_({ ...config, mutableSpecifier: mutableSpecifier }) : fields.mutable_specifier; },
    pattern(pattern?: Pattern) { return pattern !== undefined ? mut_pattern_({ ...config, pattern: pattern }) : fields.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MutPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function negative_literal_(
  config: ConfigOf<NegativeLiteral>,
) {
  const fields = {
    operator: config.operator,
    value: config.value,
  };
  return {
    type: 'negative_literal' as const,
    named: true as const,
    fields,
    operator(operator?: '-') { return operator !== undefined ? negative_literal_({ ...config, operator: operator }) : fields.operator; },
    value(value?: FloatLiteral | IntegerLiteral) { return value !== undefined ? negative_literal_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NegativeLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function or_pattern_(
  config: ConfigOf<OrPattern>,
) {
  if ('right' in config && config.right !== undefined) return or_pattern__right_(config as OrPatternRightConfig);
  return or_pattern__v1_(config as OrPatternV1Config);
}

/** Variant factory: `or_pattern` — right form. */
export function or_pattern__right_(
  config: OrPatternRightConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'or_pattern' as const,
    named: true as const,
    variant: 'right' as const,
    fields,
    left(left?: Pattern) { return left !== undefined ? or_pattern__right_({ ...config, left: left } as OrPatternRightConfig) : fields.left; },
    right(right?: Pattern) { return right !== undefined ? or_pattern__right_({ ...config, right: right } as OrPatternRightConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `or_pattern` — v1 form. */
export function or_pattern__v1_(
  config: OrPatternV1Config,
) {
  const fields = {
    left: config.left,
  };
  return {
    type: 'or_pattern' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    left(left?: Pattern) { return left !== undefined ? or_pattern__v1_({ ...config, left: left } as OrPatternV1Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ordered_field_declaration_list_(
  config?: ConfigOf<OrderedFieldDeclarationList>,
) {
  const fields = {
    type: config?.type,
    attributes: config?.attributes,
    visibility_modifier: config?.visibilityModifier,
    declarations: config?.declarations,
  };
  return {
    type: 'ordered_field_declaration_list' as const,
    named: true as const,
    fields,
    typeField(...type_: (Type)[]) { return type_.length ? ordered_field_declaration_list_({ ...config, type: type_ }) : fields.type; },
    attributes(...attributes: (AttributeItem)[]) { return attributes.length ? ordered_field_declaration_list_({ ...config, attributes: attributes }) : fields.attributes; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? ordered_field_declaration_list_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    declarations(...declarations: (AttributeItem | VisibilityModifier)[]) { return declarations.length ? ordered_field_declaration_list_({ ...config, declarations: declarations }) : fields.declarations; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrderedFieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parameter_(
  config: ConfigOf<Parameter>,
) {
  const fields = {
    pattern: config.pattern,
    type: config.type,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'parameter' as const,
    named: true as const,
    fields,
    pattern(pattern?: Pattern | Self) { return pattern !== undefined ? parameter_({ ...config, pattern: pattern }) : fields.pattern; },
    typeField(type_?: Type) { return type_ !== undefined ? parameter_({ ...config, type: type_ }) : fields.type; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? parameter_({ ...config, mutableSpecifier: mutableSpecifier }) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parameters_(
  ...children: (AttributeItem | Parameter | SelfParameter | Type | VariadicParameter)[]
) {
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (AttributeItem | Parameter | SelfParameter | Type | VariadicParameter)[]) { return parameters_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_expression_(
  child: Expression,
) {
  const children = child ? [child] : [];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    child(c?: Expression) { return c !== undefined ? parenthesized_expression_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pointer_type_(
  config: ConfigOf<PointerType>,
) {
  if ('mutableSpecifier' in config && config.mutableSpecifier !== undefined) return pointer_type__mutable_specifier_(config as PointerTypeMutableSpecifierConfig);
  return pointer_type__const_(config as PointerTypeConstConfig);
}

/** Variant factory: `pointer_type` — const form. */
export function pointer_type__const_(
  config: PointerTypeConstConfig,
) {
  const fields = {
    type: config.type,
  };
  return {
    type: 'pointer_type' as const,
    named: true as const,
    variant: 'const' as const,
    fields,
    typeField(type_?: Type) { return type_ !== undefined ? pointer_type__const_({ ...config, type: type_ } as PointerTypeConstConfig) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PointerTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `pointer_type` — mutable_specifier form. */
export function pointer_type__mutable_specifier_(
  config: PointerTypeMutableSpecifierConfig,
) {
  const fields = {
    type: config.type,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'pointer_type' as const,
    named: true as const,
    variant: 'mutable_specifier' as const,
    fields,
    typeField(type_?: Type) { return type_ !== undefined ? pointer_type__mutable_specifier_({ ...config, type: type_ } as PointerTypeMutableSpecifierConfig) : fields.type; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? pointer_type__mutable_specifier_({ ...config, mutableSpecifier: mutableSpecifier } as PointerTypeMutableSpecifierConfig) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PointerTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function qualified_type_(
  config: ConfigOf<QualifiedType>,
) {
  const fields = {
    alias: config.alias,
    type: config.type,
  };
  return {
    type: 'qualified_type' as const,
    named: true as const,
    fields,
    alias(alias?: Type) { return alias !== undefined ? qualified_type_({ ...config, alias: alias }) : fields.alias; },
    typeField(type_?: Type) { return type_ !== undefined ? qualified_type_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: QualifiedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function range_expression_(
  config: ConfigOf<RangeExpression>,
) {
  if ('start' in config && config.start !== undefined) return range_expression__v0_(config as RangeExpressionV0Config);
  else if ('start' in config && config.start !== undefined) return range_expression__ellipsis_(config as RangeExpressionEllipsisConfig);
  else if ('start' in config && config.start !== undefined) return range_expression__tok_2e2e3d_(config as RangeExpressionTok_2e2e3dConfig);
  else if ('start' in config && config.start !== undefined) return range_expression__v3_(config as RangeExpressionV3Config);
  return range_expression__v4_(config as RangeExpressionV4Config);
}

/** Variant factory: `range_expression` — v0 form. */
export function range_expression__v0_(
  config: RangeExpressionV0Config,
) {
  const fields = {
    start: config.start,
    operator: config.operator,
    end: config.end,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    start(start?: Expression) { return start !== undefined ? range_expression__v0_({ ...config, start: start } as RangeExpressionV0Config) : fields.start; },
    operator(operator?: '..' | '..=' | '...') { return operator !== undefined ? range_expression__v0_({ ...config, operator: operator } as RangeExpressionV0Config) : fields.operator; },
    end(end?: Expression) { return end !== undefined ? range_expression__v0_({ ...config, end: end } as RangeExpressionV0Config) : fields.end; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_expression` — ellipsis form. */
export function range_expression__ellipsis_(
  config: RangeExpressionEllipsisConfig,
) {
  const fields = {
    start: config.start,
    operator: config.operator,
    end: config.end,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'ellipsis' as const,
    fields,
    start(start?: Expression) { return start !== undefined ? range_expression__ellipsis_({ ...config, start: start } as RangeExpressionEllipsisConfig) : fields.start; },
    operator(operator?: '..' | '..=' | '...') { return operator !== undefined ? range_expression__ellipsis_({ ...config, operator: operator } as RangeExpressionEllipsisConfig) : fields.operator; },
    end(end?: Expression) { return end !== undefined ? range_expression__ellipsis_({ ...config, end: end } as RangeExpressionEllipsisConfig) : fields.end; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_expression` — tok_2e2e3d form. */
export function range_expression__tok_2e2e3d_(
  config: RangeExpressionTok_2e2e3dConfig,
) {
  const fields = {
    start: config.start,
    operator: config.operator,
    end: config.end,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'tok_2e2e3d' as const,
    fields,
    start(start?: Expression) { return start !== undefined ? range_expression__tok_2e2e3d_({ ...config, start: start } as RangeExpressionTok_2e2e3dConfig) : fields.start; },
    operator(operator?: '..' | '..=' | '...') { return operator !== undefined ? range_expression__tok_2e2e3d_({ ...config, operator: operator } as RangeExpressionTok_2e2e3dConfig) : fields.operator; },
    end(end?: Expression) { return end !== undefined ? range_expression__tok_2e2e3d_({ ...config, end: end } as RangeExpressionTok_2e2e3dConfig) : fields.end; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_expression` — v3 form. */
export function range_expression__v3_(
  config: RangeExpressionV3Config,
) {
  const fields = {
    start: config.start,
    operator: config.operator,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'v3' as const,
    fields,
    start(start?: Expression) { return start !== undefined ? range_expression__v3_({ ...config, start: start } as RangeExpressionV3Config) : fields.start; },
    operator(operator?: '..' | '..=' | '...') { return operator !== undefined ? range_expression__v3_({ ...config, operator: operator } as RangeExpressionV3Config) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_expression` — v4 form. */
export function range_expression__v4_(
  config: RangeExpressionV4Config,
) {
  const fields = {
    operator: config.operator,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'v4' as const,
    fields,
    operator(operator?: '..' | '..=' | '...') { return operator !== undefined ? range_expression__v4_({ ...config, operator: operator } as RangeExpressionV4Config) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function range_pattern_(
  config?: ConfigOf<RangePattern>,
) {
  if (config && 'left' in config && config.left !== undefined) return range_pattern__ellipsis_(config as RangePatternEllipsisConfig);
  else if (config && 'left' in config && config.left !== undefined) return range_pattern__v1_(config as RangePatternV1Config);
  else if (config && 'left' in config && config.left !== undefined) return range_pattern__v2_(config as RangePatternV2Config);
  else if (config && 'left' in config && config.left !== undefined) return range_pattern__v3_(config as RangePatternV3Config);
  return range_pattern__v5_(config as RangePatternV5Config);
}

/** Variant factory: `range_pattern` — ellipsis form. */
export function range_pattern__ellipsis_(
  config?: RangePatternEllipsisConfig,
) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'ellipsis' as const,
    fields,
    left(left?: LiteralPattern | Path) { return left !== undefined ? range_pattern__ellipsis_({ ...config, left: left } as RangePatternEllipsisConfig) : fields.left; },
    right(right?: LiteralPattern | Path) { return right !== undefined ? range_pattern__ellipsis_({ ...config, right: right } as RangePatternEllipsisConfig) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_pattern` — v1 form. */
export function range_pattern__v1_(
  config?: RangePatternV1Config,
) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    left(left?: LiteralPattern | Path) { return left !== undefined ? range_pattern__v1_({ ...config, left: left } as RangePatternV1Config) : fields.left; },
    right(right?: LiteralPattern | Path) { return right !== undefined ? range_pattern__v1_({ ...config, right: right } as RangePatternV1Config) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_pattern` — v2 form. */
export function range_pattern__v2_(
  config?: RangePatternV2Config,
) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'v2' as const,
    fields,
    left(left?: LiteralPattern | Path) { return left !== undefined ? range_pattern__v2_({ ...config, left: left } as RangePatternV2Config) : fields.left; },
    right(right?: LiteralPattern | Path) { return right !== undefined ? range_pattern__v2_({ ...config, right: right } as RangePatternV2Config) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_pattern` — v3 form. */
export function range_pattern__v3_(
  config?: RangePatternV3Config,
) {
  const fields = {
    left: config?.left,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'v3' as const,
    fields,
    left(left?: LiteralPattern | Path) { return left !== undefined ? range_pattern__v3_({ ...config, left: left } as RangePatternV3Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_pattern` — v4 form. */
export function range_pattern__v4_(
  config?: RangePatternV4Config,
) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'v4' as const,
    fields,
    right(right?: LiteralPattern | Path) { return right !== undefined ? range_pattern__v4_({ ...config, right: right } as RangePatternV4Config) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `range_pattern` — v5 form. */
export function range_pattern__v5_(
  config?: RangePatternV5Config,
) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'v5' as const,
    fields,
    right(right?: LiteralPattern | Path) { return right !== undefined ? range_pattern__v5_({ ...config, right: right } as RangePatternV5Config) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function raw_string_literal_(
  config: ConfigOf<RawStringLiteral>,
) {
  const fields = {
    raw_string_literal_start: config.rawStringLiteralStart,
    string_content: config.stringContent,
    raw_string_literal_end: config.rawStringLiteralEnd,
  };
  return {
    type: 'raw_string_literal' as const,
    named: true as const,
    fields,
    rawStringLiteralStart(rawStringLiteralStart?: string) { return rawStringLiteralStart !== undefined ? raw_string_literal_({ ...config, rawStringLiteralStart: rawStringLiteralStart }) : fields.raw_string_literal_start; },
    stringContent(stringContent?: StringContent) { return stringContent !== undefined ? raw_string_literal_({ ...config, stringContent: stringContent }) : fields.string_content; },
    rawStringLiteralEnd(rawStringLiteralEnd?: string) { return rawStringLiteralEnd !== undefined ? raw_string_literal_({ ...config, rawStringLiteralEnd: rawStringLiteralEnd }) : fields.raw_string_literal_end; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RawStringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ref_pattern_(
  child: Pattern,
) {
  const children = child ? [child] : [];
  return {
    type: 'ref_pattern' as const,
    named: true as const,
    children,
    child(c?: Pattern) { return c !== undefined ? ref_pattern_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RefPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function reference_expression_(
  config: ConfigOf<ReferenceExpression>,
) {
  if ('mutableSpecifier' in config && config.mutableSpecifier !== undefined) return reference_expression__v1_(config as ReferenceExpressionV1Config);
  else if ('mutableSpecifier' in config && config.mutableSpecifier !== undefined) return reference_expression__v2_(config as ReferenceExpressionV2Config);
  return reference_expression__const_(config as ReferenceExpressionConstConfig);
}

/** Variant factory: `reference_expression` — const form. */
export function reference_expression__const_(
  config: ReferenceExpressionConstConfig,
) {
  const fields = {
    value: config.value,
  };
  return {
    type: 'reference_expression' as const,
    named: true as const,
    variant: 'const' as const,
    fields,
    value(value?: Expression) { return value !== undefined ? reference_expression__const_({ ...config, value: value } as ReferenceExpressionConstConfig) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `reference_expression` — v1 form. */
export function reference_expression__v1_(
  config: ReferenceExpressionV1Config,
) {
  const fields = {
    value: config.value,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'reference_expression' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    value(value?: Expression) { return value !== undefined ? reference_expression__v1_({ ...config, value: value } as ReferenceExpressionV1Config) : fields.value; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? reference_expression__v1_({ ...config, mutableSpecifier: mutableSpecifier } as ReferenceExpressionV1Config) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `reference_expression` — v2 form. */
export function reference_expression__v2_(
  config: ReferenceExpressionV2Config,
) {
  const fields = {
    value: config.value,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'reference_expression' as const,
    named: true as const,
    variant: 'v2' as const,
    fields,
    value(value?: Expression) { return value !== undefined ? reference_expression__v2_({ ...config, value: value } as ReferenceExpressionV2Config) : fields.value; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? reference_expression__v2_({ ...config, mutableSpecifier: mutableSpecifier } as ReferenceExpressionV2Config) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function reference_pattern_(
  config: ConfigOf<ReferencePattern>,
) {
  const fields = {
    mutable_specifier: config.mutableSpecifier,
    pattern: config.pattern,
  };
  return {
    type: 'reference_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? reference_pattern_({ ...config, mutableSpecifier: mutableSpecifier }) : fields.mutable_specifier; },
    pattern(pattern?: Pattern) { return pattern !== undefined ? reference_pattern_({ ...config, pattern: pattern }) : fields.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferencePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function reference_type_(
  config: ConfigOf<ReferenceType>,
) {
  const fields = {
    type: config.type,
    lifetime: config.lifetime,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'reference_type' as const,
    named: true as const,
    fields,
    typeField(type_?: Type) { return type_ !== undefined ? reference_type_({ ...config, type: type_ }) : fields.type; },
    lifetime(lifetime?: Lifetime) { return lifetime !== undefined ? reference_type_({ ...config, lifetime: lifetime }) : fields.lifetime; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? reference_type_({ ...config, mutableSpecifier: mutableSpecifier }) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function removed_trait_bound_(
  child: Type,
) {
  const children = child ? [child] : [];
  return {
    type: 'removed_trait_bound' as const,
    named: true as const,
    children,
    child(c?: Type) { return c !== undefined ? removed_trait_bound_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RemovedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function return_expression_(
  child?: Expression,
) {
  const children = child ? [child] : [];
  return {
    type: 'return_expression' as const,
    named: true as const,
    children,
    child(c?: Expression) { return c !== undefined ? return_expression_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function scoped_identifier_(
  config: ConfigOf<ScopedIdentifier>,
) {
  const fields = {
    name: config.name,
    path: config.path,
  };
  return {
    type: 'scoped_identifier' as const,
    named: true as const,
    fields,
    name(name?: Identifier | Super) { return name !== undefined ? scoped_identifier_({ ...config, name: name }) : fields.name; },
    path(path?: BracketedType | GenericType | Path) { return path !== undefined ? scoped_identifier_({ ...config, path: path }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function scoped_type_identifier_(
  config: ConfigOf<ScopedTypeIdentifier>,
) {
  const fields = {
    name: config.name,
    path: config.path,
  };
  return {
    type: 'scoped_type_identifier' as const,
    named: true as const,
    fields,
    name(name?: TypeIdentifier) { return name !== undefined ? scoped_type_identifier_({ ...config, name: name }) : fields.name; },
    path(path?: BracketedType | GenericType | Path) { return path !== undefined ? scoped_type_identifier_({ ...config, path: path }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function scoped_use_list_(
  config: ConfigOf<ScopedUseList>,
) {
  const fields = {
    list: config.list,
    path: config.path,
  };
  return {
    type: 'scoped_use_list' as const,
    named: true as const,
    fields,
    list(list?: UseList) { return list !== undefined ? scoped_use_list_({ ...config, list: list }) : fields.list; },
    path(path?: Path) { return path !== undefined ? scoped_use_list_({ ...config, path: path }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedUseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function self_parameter_(
  config: ConfigOf<SelfParameter>,
) {
  const fields = {
    lifetime: config.lifetime,
    mutable_specifier: config.mutableSpecifier,
    self: config.self,
  };
  return {
    type: 'self_parameter' as const,
    named: true as const,
    fields,
    lifetime(lifetime?: Lifetime) { return lifetime !== undefined ? self_parameter_({ ...config, lifetime: lifetime }) : fields.lifetime; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? self_parameter_({ ...config, mutableSpecifier: mutableSpecifier }) : fields.mutable_specifier; },
    self(self?: Self) { return self !== undefined ? self_parameter_({ ...config, self: self }) : fields.self; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SelfParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function shorthand_field_initializer_(
  config: ConfigOf<ShorthandFieldInitializer>,
) {
  const fields = {
    attributes: config.attributes,
    identifier: config.identifier,
  };
  return {
    type: 'shorthand_field_initializer' as const,
    named: true as const,
    fields,
    attributes(...attributes: (AttributeItem)[]) { return attributes.length ? shorthand_field_initializer_({ ...config, attributes: attributes }) : fields.attributes; },
    identifier(identifier?: Identifier) { return identifier !== undefined ? shorthand_field_initializer_({ ...config, identifier: identifier }) : fields.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function slice_pattern_(
  ...children: (Pattern)[]
) {
  return {
    type: 'slice_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Pattern)[]) { return slice_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SlicePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function source_file_(
  config?: ConfigOf<SourceFile>,
) {
  const fields = {
    shebang: config?.shebang,
    statements: config?.statements,
  };
  return {
    type: 'source_file' as const,
    named: true as const,
    fields,
    shebang(shebang?: Shebang) { return shebang !== undefined ? source_file_({ ...config, shebang: shebang }) : fields.shebang; },
    statements(...statements: (Statement)[]) { return statements.length ? source_file_({ ...config, statements: statements }) : fields.statements; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SourceFileTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function static_item_(
  config: ConfigOf<StaticItem>,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
    visibility_modifier: config.visibilityModifier,
    mutable_specifier: config.mutableSpecifier,
  };
  return {
    type: 'static_item' as const,
    named: true as const,
    fields,
    name(name?: Identifier) { return name !== undefined ? static_item_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? static_item_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? static_item_({ ...config, value: value }) : fields.value; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? static_item_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? static_item_({ ...config, mutableSpecifier: mutableSpecifier }) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StaticItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string_literal_(
  ...children: (EscapeSequence | StringContent)[]
) {
  return {
    type: 'string_literal' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (EscapeSequence | StringContent)[]) { return string_literal_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function struct_expression_(
  config: ConfigOf<StructExpression>,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'struct_expression' as const,
    named: true as const,
    fields,
    body(body?: FieldInitializerList) { return body !== undefined ? struct_expression_({ ...config, body: body }) : fields.body; },
    name(name?: GenericTypeWithTurbofish | ScopedTypeIdentifier | TypeIdentifier) { return name !== undefined ? struct_expression_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function struct_item_(
  config: ConfigOf<StructItem>,
) {
  if ('whereClause' in config && config.whereClause !== undefined) return struct_item__v0_(config as StructItemV0Config);
  else if ('body' in config && config.body !== undefined) return struct_item__v1_(config as StructItemV1Config);
  return struct_item__v2_(config as StructItemV2Config);
}

/** Variant factory: `struct_item` — v0 form. */
export function struct_item__v0_(
  config: StructItemV0Config,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
    where_clause: config.whereClause,
  };
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    body(body?: FieldDeclarationList | OrderedFieldDeclarationList) { return body !== undefined ? struct_item__v0_({ ...config, body: body } as StructItemV0Config) : fields.body; },
    name(name?: TypeIdentifier) { return name !== undefined ? struct_item__v0_({ ...config, name: name } as StructItemV0Config) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? struct_item__v0_({ ...config, typeParameters: typeParameters } as StructItemV0Config) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? struct_item__v0_({ ...config, visibilityModifier: visibilityModifier } as StructItemV0Config) : fields.visibility_modifier; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? struct_item__v0_({ ...config, whereClause: whereClause } as StructItemV0Config) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `struct_item` — v1 form. */
export function struct_item__v1_(
  config: StructItemV1Config,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
    where_clause: config.whereClause,
  };
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'v1' as const,
    fields,
    body(body?: FieldDeclarationList | OrderedFieldDeclarationList) { return body !== undefined ? struct_item__v1_({ ...config, body: body } as StructItemV1Config) : fields.body; },
    name(name?: TypeIdentifier) { return name !== undefined ? struct_item__v1_({ ...config, name: name } as StructItemV1Config) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? struct_item__v1_({ ...config, typeParameters: typeParameters } as StructItemV1Config) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? struct_item__v1_({ ...config, visibilityModifier: visibilityModifier } as StructItemV1Config) : fields.visibility_modifier; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? struct_item__v1_({ ...config, whereClause: whereClause } as StructItemV1Config) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `struct_item` — v2 form. */
export function struct_item__v2_(
  config: StructItemV2Config,
) {
  const fields = {
    name: config.name,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
  };
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'v2' as const,
    fields,
    name(name?: TypeIdentifier) { return name !== undefined ? struct_item__v2_({ ...config, name: name } as StructItemV2Config) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? struct_item__v2_({ ...config, typeParameters: typeParameters } as StructItemV2Config) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? struct_item__v2_({ ...config, visibilityModifier: visibilityModifier } as StructItemV2Config) : fields.visibility_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function struct_pattern_(
  config: ConfigOf<StructPattern>,
) {
  const fields = {
    type: config.type,
  };
  const children = config.children ?? [];
  return {
    type: 'struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type_?: ScopedTypeIdentifier | TypeIdentifier) { return type_ !== undefined ? struct_pattern_({ ...config, type: type_ }) : fields.type; },
    getChildren() { return children; },
    setChildren(...children: (FieldPattern | RemainingFieldPattern)[]) { return struct_pattern_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function token_binding_pattern_(
  config: ConfigOf<TokenBindingPattern>,
) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    type: 'token_binding_pattern' as const,
    named: true as const,
    fields,
    name(name?: Metavariable) { return name !== undefined ? token_binding_pattern_({ ...config, name: name }) : fields.name; },
    typeField(type_?: FragmentSpecifier) { return type_ !== undefined ? token_binding_pattern_({ ...config, type: type_ }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenBindingPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function token_repetition_(
  ...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]
) {
  return {
    type: 'token_repetition' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]) { return token_repetition_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `token_repetition` — plus form. */
export function token_repetition__plus_(
  config?: TokenRepetitionPlusConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_repetition' as const,
    named: true as const,
    variant: 'plus' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]) { return token_repetition__plus_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `token_repetition` — star form. */
export function token_repetition__star_(
  config?: TokenRepetitionStarConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_repetition' as const,
    named: true as const,
    variant: 'star' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]) { return token_repetition__star_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `token_repetition` — question form. */
export function token_repetition__question_(
  config?: TokenRepetitionQuestionConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_repetition' as const,
    named: true as const,
    variant: 'question' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]) { return token_repetition__question_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function token_repetition_pattern_(
  ...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]
) {
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]) { return token_repetition_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `token_repetition_pattern` — plus form. */
export function token_repetition_pattern__plus_(
  config?: TokenRepetitionPatternPlusConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    variant: 'plus' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]) { return token_repetition_pattern__plus_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `token_repetition_pattern` — star form. */
export function token_repetition_pattern__star_(
  config?: TokenRepetitionPatternStarConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    variant: 'star' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]) { return token_repetition_pattern__star_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `token_repetition_pattern` — question form. */
export function token_repetition_pattern__question_(
  config?: TokenRepetitionPatternQuestionConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    variant: 'question' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]) { return token_repetition_pattern__question_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function token_tree_(
  ...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]
) {
  return {
    type: 'token_tree' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]) { return token_tree_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `token_tree` — paren form. */
export function token_tree__paren_(
  config?: TokenTreeParenConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_tree' as const,
    named: true as const,
    variant: 'paren' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]) { return token_tree__paren_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `token_tree` — bracket form. */
export function token_tree__bracket_(
  config?: TokenTreeBracketConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_tree' as const,
    named: true as const,
    variant: 'bracket' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]) { return token_tree__bracket_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `token_tree` — brace form. */
export function token_tree__brace_(
  config?: TokenTreeBraceConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_tree' as const,
    named: true as const,
    variant: 'brace' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]) { return token_tree__brace_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function token_tree_pattern_(
  ...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]
) {
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]) { return token_tree_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


/** Variant factory: `token_tree_pattern` — paren form. */
export function token_tree_pattern__paren_(
  config?: TokenTreePatternParenConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    variant: 'paren' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]) { return token_tree_pattern__paren_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `token_tree_pattern` — bracket form. */
export function token_tree_pattern__bracket_(
  config?: TokenTreePatternBracketConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    variant: 'bracket' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]) { return token_tree_pattern__bracket_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `token_tree_pattern` — brace form. */
export function token_tree_pattern__brace_(
  config?: TokenTreePatternBraceConfig,
) {
  const children = config?.children ?? [];
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    variant: 'brace' as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]) { return token_tree_pattern__brace_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function trait_bounds_(
  ...children: (HigherRankedTraitBound | Lifetime | Type)[]
) {
  return {
    type: 'trait_bounds' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (HigherRankedTraitBound | Lifetime | Type)[]) { return trait_bounds_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function trait_item_(
  config: ConfigOf<TraitItem>,
) {
  const fields = {
    body: config.body,
    bounds: config.bounds,
    name: config.name,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
    where_clause: config.whereClause,
  };
  return {
    type: 'trait_item' as const,
    named: true as const,
    fields,
    body(body?: DeclarationList) { return body !== undefined ? trait_item_({ ...config, body: body }) : fields.body; },
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? trait_item_({ ...config, bounds: bounds }) : fields.bounds; },
    name(name?: TypeIdentifier) { return name !== undefined ? trait_item_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? trait_item_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? trait_item_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? trait_item_({ ...config, whereClause: whereClause }) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_block_(
  config: ConfigOf<TryBlock>,
) {
  const fields = {
    block: config.block,
  };
  return {
    type: 'try_block' as const,
    named: true as const,
    fields,
    block(block?: Block) { return block !== undefined ? try_block_({ ...config, block: block }) : fields.block; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_expression_(
  config: ConfigOf<TryExpression>,
) {
  const fields = {
    value: config.value,
    operator: config.operator,
  };
  return {
    type: 'try_expression' as const,
    named: true as const,
    fields,
    value(value?: Expression) { return value !== undefined ? try_expression_({ ...config, value: value }) : fields.value; },
    operator(operator?: '?') { return operator !== undefined ? try_expression_({ ...config, operator: operator }) : fields.operator; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_expression_(
  config: ConfigOf<TupleExpression>,
) {
  const fields = {
    attributes: config.attributes,
    first: config.first,
    rest: config.rest,
    trailing: config.trailing,
  };
  return {
    type: 'tuple_expression' as const,
    named: true as const,
    fields,
    attributes(...attributes: (AttributeItem)[]) { return attributes.length ? tuple_expression_({ ...config, attributes: attributes }) : fields.attributes; },
    first(first?: Expression) { return first !== undefined ? tuple_expression_({ ...config, first: first }) : fields.first; },
    rest(...rest: (Expression)[]) { return rest.length ? tuple_expression_({ ...config, rest: rest }) : fields.rest; },
    trailing(trailing?: Expression) { return trailing !== undefined ? tuple_expression_({ ...config, trailing: trailing }) : fields.trailing; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_pattern_(
  ...children: (ClosureExpression | Pattern)[]
) {
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (ClosureExpression | Pattern)[]) { return tuple_pattern_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_struct_pattern_(
  config: ConfigOf<TupleStructPattern>,
) {
  const fields = {
    type: config.type,
  };
  const children = config.children ?? [];
  return {
    type: 'tuple_struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type_?: GenericType | Identifier | ScopedIdentifier) { return type_ !== undefined ? tuple_struct_pattern_({ ...config, type: type_ }) : fields.type; },
    getChildren() { return children; },
    setChildren(...children: (Pattern)[]) { return tuple_struct_pattern_({ ...config, children }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleStructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_type_(
  ...children: (Type)[]
) {
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Type)[]) { return tuple_type_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_arguments_(
  ...children: (Block | Lifetime | Literal | TraitBounds | Type | TypeBinding)[]
) {
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Block | Lifetime | Literal | TraitBounds | Type | TypeBinding)[]) { return type_arguments_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_binding_(
  config: ConfigOf<TypeBinding>,
) {
  const fields = {
    name: config.name,
    type: config.type,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'type_binding' as const,
    named: true as const,
    fields,
    name(name?: TypeIdentifier) { return name !== undefined ? type_binding_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? type_binding_({ ...config, type: type_ }) : fields.type; },
    typeArguments(typeArguments?: TypeArguments) { return typeArguments !== undefined ? type_binding_({ ...config, typeArguments: typeArguments }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeBindingTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_cast_expression_(
  config: ConfigOf<TypeCastExpression>,
) {
  const fields = {
    type: config.type,
    value: config.value,
  };
  return {
    type: 'type_cast_expression' as const,
    named: true as const,
    fields,
    typeField(type_?: Type) { return type_ !== undefined ? type_cast_expression_({ ...config, type: type_ }) : fields.type; },
    value(value?: Expression) { return value !== undefined ? type_cast_expression_({ ...config, value: value }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeCastExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_item_(
  config: ConfigOf<TypeItem>,
) {
  const fields = {
    name: config.name,
    type: config.type,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
    where_clause: config.whereClause,
    trailing_where_clause: config.trailingWhereClause,
  };
  return {
    type: 'type_item' as const,
    named: true as const,
    fields,
    name(name?: TypeIdentifier) { return name !== undefined ? type_item_({ ...config, name: name }) : fields.name; },
    typeField(type_?: Type) { return type_ !== undefined ? type_item_({ ...config, type: type_ }) : fields.type; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? type_item_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? type_item_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? type_item_({ ...config, whereClause: whereClause }) : fields.where_clause; },
    trailingWhereClause(trailingWhereClause?: WhereClause) { return trailingWhereClause !== undefined ? type_item_({ ...config, trailingWhereClause: trailingWhereClause }) : fields.trailing_where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameter_(
  config: ConfigOf<TypeParameter>,
) {
  const fields = {
    bounds: config.bounds,
    default_type: config.defaultType,
    name: config.name,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? type_parameter_({ ...config, bounds: bounds }) : fields.bounds; },
    defaultType(defaultType?: Type) { return defaultType !== undefined ? type_parameter_({ ...config, defaultType: defaultType }) : fields.default_type; },
    name(name?: TypeIdentifier) { return name !== undefined ? type_parameter_({ ...config, name: name }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameters_(
  ...children: (AttributeItem | ConstParameter | LifetimeParameter | Metavariable | TypeParameter)[]
) {
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (AttributeItem | ConstParameter | LifetimeParameter | Metavariable | TypeParameter)[]) { return type_parameters_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unary_expression_(
  config: ConfigOf<UnaryExpression>,
) {
  return unary_expression__bang_(config as UnaryExpressionBangConfig);
}

/** Variant factory: `unary_expression` — minus form. */
export function unary_expression__minus_(
  config: UnaryExpressionMinusConfig,
) {
  const fields = {
    operator: config.operator,
    operand: config.operand,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    variant: 'minus' as const,
    fields,
    operator(operator?: '-' | '*' | '!') { return operator !== undefined ? unary_expression__minus_({ ...config, operator: operator } as UnaryExpressionMinusConfig) : fields.operator; },
    operand(operand?: Expression) { return operand !== undefined ? unary_expression__minus_({ ...config, operand: operand } as UnaryExpressionMinusConfig) : fields.operand; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `unary_expression` — star form. */
export function unary_expression__star_(
  config: UnaryExpressionStarConfig,
) {
  const fields = {
    operator: config.operator,
    operand: config.operand,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    variant: 'star' as const,
    fields,
    operator(operator?: '-' | '*' | '!') { return operator !== undefined ? unary_expression__star_({ ...config, operator: operator } as UnaryExpressionStarConfig) : fields.operator; },
    operand(operand?: Expression) { return operand !== undefined ? unary_expression__star_({ ...config, operand: operand } as UnaryExpressionStarConfig) : fields.operand; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `unary_expression` — bang form. */
export function unary_expression__bang_(
  config: UnaryExpressionBangConfig,
) {
  const fields = {
    operator: config.operator,
    operand: config.operand,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    variant: 'bang' as const,
    fields,
    operator(operator?: '-' | '*' | '!') { return operator !== undefined ? unary_expression__bang_({ ...config, operator: operator } as UnaryExpressionBangConfig) : fields.operator; },
    operand(operand?: Expression) { return operand !== undefined ? unary_expression__bang_({ ...config, operand: operand } as UnaryExpressionBangConfig) : fields.operand; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function union_item_(
  config: ConfigOf<UnionItem>,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
    visibility_modifier: config.visibilityModifier,
    where_clause: config.whereClause,
  };
  return {
    type: 'union_item' as const,
    named: true as const,
    fields,
    body(body?: FieldDeclarationList) { return body !== undefined ? union_item_({ ...config, body: body }) : fields.body; },
    name(name?: TypeIdentifier) { return name !== undefined ? union_item_({ ...config, name: name }) : fields.name; },
    typeParameters(typeParameters?: TypeParameters) { return typeParameters !== undefined ? union_item_({ ...config, typeParameters: typeParameters }) : fields.type_parameters; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? union_item_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    whereClause(whereClause?: WhereClause) { return whereClause !== undefined ? union_item_({ ...config, whereClause: whereClause }) : fields.where_clause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unsafe_block_(
  config: ConfigOf<UnsafeBlock>,
) {
  const fields = {
    block: config.block,
  };
  return {
    type: 'unsafe_block' as const,
    named: true as const,
    fields,
    block(block?: Block) { return block !== undefined ? unsafe_block_({ ...config, block: block }) : fields.block; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnsafeBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_as_clause_(
  config: ConfigOf<UseAsClause>,
) {
  const fields = {
    alias: config.alias,
    path: config.path,
  };
  return {
    type: 'use_as_clause' as const,
    named: true as const,
    fields,
    alias(alias?: Identifier) { return alias !== undefined ? use_as_clause_({ ...config, alias: alias }) : fields.alias; },
    path(path?: Path) { return path !== undefined ? use_as_clause_({ ...config, path: path }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseAsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_bounds_(
  ...children: (Lifetime | TypeIdentifier)[]
) {
  return {
    type: 'use_bounds' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (Lifetime | TypeIdentifier)[]) { return use_bounds_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_declaration_(
  config: ConfigOf<UseDeclaration>,
) {
  const fields = {
    argument: config.argument,
    visibility_modifier: config.visibilityModifier,
  };
  return {
    type: 'use_declaration' as const,
    named: true as const,
    fields,
    argument(argument?: UseClause) { return argument !== undefined ? use_declaration_({ ...config, argument: argument }) : fields.argument; },
    visibilityModifier(visibilityModifier?: VisibilityModifier) { return visibilityModifier !== undefined ? use_declaration_({ ...config, visibilityModifier: visibilityModifier }) : fields.visibility_modifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_list_(
  ...children: (UseClause)[]
) {
  return {
    type: 'use_list' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (UseClause)[]) { return use_list_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_wildcard_(
  config?: ConfigOf<UseWildcard>,
) {
  const fields = {
    path: config?.path,
  };
  const children: unknown[] = [];
  return {
    type: 'use_wildcard' as const,
    named: true as const,
    fields,
    children,
    path(path?: Path) { return path !== undefined ? use_wildcard_({ ...config, path: path }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseWildcardTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function variadic_parameter_(
  config?: ConfigOf<VariadicParameter>,
) {
  const fields = {
    pattern: config?.pattern,
    mutable_specifier: config?.mutableSpecifier,
  };
  return {
    type: 'variadic_parameter' as const,
    named: true as const,
    fields,
    pattern(pattern?: Pattern) { return pattern !== undefined ? variadic_parameter_({ ...config, pattern: pattern }) : fields.pattern; },
    mutableSpecifier(mutableSpecifier?: MutableSpecifier) { return mutableSpecifier !== undefined ? variadic_parameter_({ ...config, mutableSpecifier: mutableSpecifier }) : fields.mutable_specifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariadicParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function visibility_modifier_(
  config?: ConfigOf<VisibilityModifier>,
) {
  if (config && 'pub' in config && config.pub !== undefined) return visibility_modifier__pub_(config as VisibilityModifierPubConfig);
  return visibility_modifier__v0_(config as VisibilityModifierV0Config);
}

/** Variant factory: `visibility_modifier` — v0 form. */
export function visibility_modifier__v0_(
  config?: VisibilityModifierV0Config,
) {
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    variant: 'v0' as const,
    fields: {},
    children,
    child(child?: Path) { return child !== undefined ? visibility_modifier__v0_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VisibilityModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `visibility_modifier` — pub form. */
export function visibility_modifier__pub_(
  config?: VisibilityModifierPubConfig,
) {
  const fields = {
    pub: config?.pub,
    in: config?.in,
  };
  const children = config?.children ? [config?.children] : [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    variant: 'pub' as const,
    fields,
    children,
    pub(pub?: 'pub') { return pub !== undefined ? visibility_modifier__pub_({ ...config, pub: pub } as VisibilityModifierPubConfig) : fields.pub; },
    in(in_?: 'in') { return in_ !== undefined ? visibility_modifier__pub_({ ...config, in: in_ } as VisibilityModifierPubConfig) : fields.in; },
    child(child?: Path) { return child !== undefined ? visibility_modifier__pub_({ ...config, children: child }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VisibilityModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function where_clause_(
  ...children: (WherePredicate)[]
) {
  return {
    type: 'where_clause' as const,
    named: true as const,
    children,
    getChildren() { return children; },
    setChildren(...children: (WherePredicate)[]) { return where_clause_(...children); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhereClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function where_predicate_(
  config: ConfigOf<WherePredicate>,
) {
  return where_predicate__char_(config as WherePredicateCharConfig);
}

/** Variant factory: `where_predicate` — v0 form. */
export function where_predicate__v0_(
  config: WherePredicateV0Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'v0' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__v0_({ ...config, bounds: bounds } as WherePredicateV0Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__v0_({ ...config, left: left } as WherePredicateV0Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — u8 form. */
export function where_predicate__u8_(
  config: WherePredicateU8Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'u8' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__u8_({ ...config, bounds: bounds } as WherePredicateU8Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__u8_({ ...config, left: left } as WherePredicateU8Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — i8 form. */
export function where_predicate__i8_(
  config: WherePredicateI8Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'i8' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__i8_({ ...config, bounds: bounds } as WherePredicateI8Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__i8_({ ...config, left: left } as WherePredicateI8Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — u16 form. */
export function where_predicate__u16_(
  config: WherePredicateU16Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'u16' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__u16_({ ...config, bounds: bounds } as WherePredicateU16Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__u16_({ ...config, left: left } as WherePredicateU16Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — i16 form. */
export function where_predicate__i16_(
  config: WherePredicateI16Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'i16' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__i16_({ ...config, bounds: bounds } as WherePredicateI16Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__i16_({ ...config, left: left } as WherePredicateI16Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — u32 form. */
export function where_predicate__u32_(
  config: WherePredicateU32Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'u32' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__u32_({ ...config, bounds: bounds } as WherePredicateU32Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__u32_({ ...config, left: left } as WherePredicateU32Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — i32 form. */
export function where_predicate__i32_(
  config: WherePredicateI32Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'i32' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__i32_({ ...config, bounds: bounds } as WherePredicateI32Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__i32_({ ...config, left: left } as WherePredicateI32Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — u64 form. */
export function where_predicate__u64_(
  config: WherePredicateU64Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'u64' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__u64_({ ...config, bounds: bounds } as WherePredicateU64Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__u64_({ ...config, left: left } as WherePredicateU64Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — i64 form. */
export function where_predicate__i64_(
  config: WherePredicateI64Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'i64' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__i64_({ ...config, bounds: bounds } as WherePredicateI64Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__i64_({ ...config, left: left } as WherePredicateI64Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — u128 form. */
export function where_predicate__u128_(
  config: WherePredicateU128Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'u128' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__u128_({ ...config, bounds: bounds } as WherePredicateU128Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__u128_({ ...config, left: left } as WherePredicateU128Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — i128 form. */
export function where_predicate__i128_(
  config: WherePredicateI128Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'i128' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__i128_({ ...config, bounds: bounds } as WherePredicateI128Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__i128_({ ...config, left: left } as WherePredicateI128Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — isize form. */
export function where_predicate__isize_(
  config: WherePredicateIsizeConfig,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'isize' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__isize_({ ...config, bounds: bounds } as WherePredicateIsizeConfig) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__isize_({ ...config, left: left } as WherePredicateIsizeConfig) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — usize form. */
export function where_predicate__usize_(
  config: WherePredicateUsizeConfig,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'usize' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__usize_({ ...config, bounds: bounds } as WherePredicateUsizeConfig) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__usize_({ ...config, left: left } as WherePredicateUsizeConfig) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — f32 form. */
export function where_predicate__f32_(
  config: WherePredicateF32Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'f32' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__f32_({ ...config, bounds: bounds } as WherePredicateF32Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__f32_({ ...config, left: left } as WherePredicateF32Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — f64 form. */
export function where_predicate__f64_(
  config: WherePredicateF64Config,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'f64' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__f64_({ ...config, bounds: bounds } as WherePredicateF64Config) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__f64_({ ...config, left: left } as WherePredicateF64Config) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — bool form. */
export function where_predicate__bool_(
  config: WherePredicateBoolConfig,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'bool' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__bool_({ ...config, bounds: bounds } as WherePredicateBoolConfig) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__bool_({ ...config, left: left } as WherePredicateBoolConfig) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — str form. */
export function where_predicate__str_(
  config: WherePredicateStrConfig,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'str' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__str_({ ...config, bounds: bounds } as WherePredicateStrConfig) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__str_({ ...config, left: left } as WherePredicateStrConfig) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

/** Variant factory: `where_predicate` — char form. */
export function where_predicate__char_(
  config: WherePredicateCharConfig,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    variant: 'char' as const,
    fields,
    bounds(bounds?: TraitBounds) { return bounds !== undefined ? where_predicate__char_({ ...config, bounds: bounds } as WherePredicateCharConfig) : fields.bounds; },
    left(left?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier) { return left !== undefined ? where_predicate__char_({ ...config, left: left } as WherePredicateCharConfig) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function while_expression_(
  config: ConfigOf<WhileExpression>,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
    label: config.label,
  };
  return {
    type: 'while_expression' as const,
    named: true as const,
    fields,
    body(body?: Block) { return body !== undefined ? while_expression_({ ...config, body: body }) : fields.body; },
    condition(condition?: Condition | Expression | LetChain | LetCondition) { return condition !== undefined ? while_expression_({ ...config, condition: condition }) : fields.condition; },
    label(label?: Label) { return label !== undefined ? while_expression_({ ...config, label: label }) : fields.label; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function yield_expression_(
  child?: Expression,
) {
  const children = child ? [child] : [];
  return {
    type: 'yield_expression' as const,
    named: true as const,
    children,
    child(c?: Expression) { return c !== undefined ? yield_expression_(c) : children[0]; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function boolean_literal_(text: string) {
  return {
    type: 'boolean_literal' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: BooleanLiteralTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function empty_statement_() {
  return {
    type: 'empty_statement' as const,
    named: true as const,
    text: ';',
    render: () => ';',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: ';' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: ';' };
    },
    replace: (target: EmptyStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: ';' }; },
  };
}

export function fragment_specifier_(text: string) {
  return {
    type: 'fragment_specifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FragmentSpecifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function inner_doc_comment_marker_(text: string) {
  return {
    type: 'inner_doc_comment_marker' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: InnerDocCommentMarkerTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function never_type_() {
  return {
    type: 'never_type' as const,
    named: true as const,
    text: '!',
    render: () => '!',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '!' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '!' };
    },
    replace: (target: NeverTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '!' }; },
  };
}

export function outer_doc_comment_marker_(text: string) {
  return {
    type: 'outer_doc_comment_marker' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: OuterDocCommentMarkerTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function remaining_field_pattern_() {
  return {
    type: 'remaining_field_pattern' as const,
    named: true as const,
    text: '..',
    render: () => '..',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '..' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '..' };
    },
    replace: (target: RemainingFieldPatternTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '..' }; },
  };
}

export function unit_expression_() {
  return {
    type: 'unit_expression' as const,
    named: true as const,
    text: '()',
    render: () => '()',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '()' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '()' };
    },
    replace: (target: UnitExpressionTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '()' }; },
  };
}

export function unit_type_() {
  return {
    type: 'unit_type' as const,
    named: true as const,
    text: '()',
    render: () => '()',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '()' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '()' };
    },
    replace: (target: UnitTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '()' }; },
  };
}

export function char_literal_(text: string) {
  if (!/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(text)) throw new Error(`Invalid char_literal: '${text}' does not match grammar pattern`);
  return {
    type: 'char_literal' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: CharLiteralTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function crate_() {
  return {
    type: 'crate' as const,
    named: true as const,
    text: 'crate',
    render: () => 'crate',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'crate' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'crate' };
    },
    replace: (target: CrateTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'crate' }; },
  };
}

export function doc_comment_(text: string) {
  return {
    type: 'doc_comment' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: DocCommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escape_sequence_(text: string) {
  if (!/^\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})$/.test(text)) throw new Error(`Invalid escape_sequence: '${text}' does not match grammar pattern`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeSequenceTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function field_identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid field_identifier`);
  return {
    type: 'field_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FieldIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function float_literal_(text: string) {
  return {
    type: 'float_literal' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FloatLiteralTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  if (!/^(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*$/u.test(text)) throw new Error(`Invalid identifier: '${text}' does not match grammar pattern`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer_literal_(text: string) {
  if (!/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(text)) throw new Error(`Invalid integer_literal: '${text}' does not match grammar pattern`);
  return {
    type: 'integer_literal' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IntegerLiteralTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function metavariable_(text: string) {
  if (!/^\$[a-zA-Z_]\w*$/.test(text)) throw new Error(`Invalid metavariable: '${text}' does not match grammar pattern`);
  return {
    type: 'metavariable' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: MetavariableTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function mutable_specifier_() {
  return {
    type: 'mutable_specifier' as const,
    named: true as const,
    text: 'mut',
    render: () => 'mut',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'mut' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'mut' };
    },
    replace: (target: MutableSpecifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'mut' }; },
  };
}

export function primitive_type_(text: string) {
  return {
    type: 'primitive_type' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PrimitiveTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function self_() {
  return {
    type: 'self' as const,
    named: true as const,
    text: 'self',
    render: () => 'self',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'self' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'self' };
    },
    replace: (target: SelfTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'self' }; },
  };
}

export function shebang_(text: string) {
  if (!/^#![\r\f\t\v ]*([^\[\n].*)?\n$/.test(text)) throw new Error(`Invalid shebang: '${text}' does not match grammar pattern`);
  return {
    type: 'shebang' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShebangTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthand_field_identifier_(text: string) {
  return {
    type: 'shorthand_field_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShorthandFieldIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function string_content_(text: string) {
  return {
    type: 'string_content' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringContentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super',
    render: () => 'super',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'super' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'super' };
    },
    replace: (target: SuperTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; },
  };
}

export function type_identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid type_identifier`);
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: TypeIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}


/** @internal Map of kind string to factory function. */
export const _factoryMap: Record<string, (config?: any) => unknown> = {
  'abstract_type': abstract_type_,
  'arguments': arguments_,
  'array_expression': array_expression_,
  'array_type': array_type_,
  'assignment_expression': assignment_expression_,
  'associated_type': associated_type_,
  'async_block': async_block_,
  'attribute': attribute_,
  'attribute_item': attribute_item_,
  'await_expression': await_expression_,
  'base_field_initializer': base_field_initializer_,
  'binary_expression': binary_expression_,
  'block': block_,
  'block_comment': block_comment_,
  'bounded_type': bounded_type_,
  'bracketed_type': bracketed_type_,
  'break_expression': break_expression_,
  'call_expression': call_expression_,
  'captured_pattern': captured_pattern_,
  'closure_expression': closure_expression_,
  'closure_parameters': closure_parameters_,
  'compound_assignment_expr': compound_assignment_expr_,
  'const_block': const_block_,
  'const_item': const_item_,
  'const_parameter': const_parameter_,
  'continue_expression': continue_expression_,
  'declaration_list': declaration_list_,
  'dynamic_type': dynamic_type_,
  'else_clause': else_clause_,
  'enum_item': enum_item_,
  'enum_variant': enum_variant_,
  'enum_variant_list': enum_variant_list_,
  'expression_statement': expression_statement_,
  'extern_crate_declaration': extern_crate_declaration_,
  'extern_modifier': extern_modifier_,
  'field_declaration': field_declaration_,
  'field_declaration_list': field_declaration_list_,
  'field_expression': field_expression_,
  'field_initializer': field_initializer_,
  'field_initializer_list': field_initializer_list_,
  'field_pattern': field_pattern_,
  'for_expression': for_expression_,
  'for_lifetimes': for_lifetimes_,
  'foreign_mod_item': foreign_mod_item_,
  'function_item': function_item_,
  'function_modifiers': function_modifiers_,
  'function_signature_item': function_signature_item_,
  'function_type': function_type_,
  'gen_block': gen_block_,
  'generic_function': generic_function_,
  'generic_pattern': generic_pattern_,
  'generic_type': generic_type_,
  'generic_type_with_turbofish': generic_type_with_turbofish_,
  'higher_ranked_trait_bound': higher_ranked_trait_bound_,
  'if_expression': if_expression_,
  'impl_item': impl_item_,
  'index_expression': index_expression_,
  'inner_attribute_item': inner_attribute_item_,
  'label': label_,
  'let_chain': let_chain_,
  'let_condition': let_condition_,
  'let_declaration': let_declaration_,
  'lifetime': lifetime_,
  'lifetime_parameter': lifetime_parameter_,
  'line_comment': line_comment_,
  'loop_expression': loop_expression_,
  'macro_definition': macro_definition_,
  'macro_invocation': macro_invocation_,
  'macro_rule': macro_rule_,
  'match_arm': match_arm_,
  'match_block': match_block_,
  'match_expression': match_expression_,
  'match_pattern': match_pattern_,
  'mod_item': mod_item_,
  'mut_pattern': mut_pattern_,
  'negative_literal': negative_literal_,
  'or_pattern': or_pattern_,
  'ordered_field_declaration_list': ordered_field_declaration_list_,
  'parameter': parameter_,
  'parameters': parameters_,
  'parenthesized_expression': parenthesized_expression_,
  'pointer_type': pointer_type_,
  'qualified_type': qualified_type_,
  'range_expression': range_expression_,
  'range_pattern': range_pattern_,
  'raw_string_literal': raw_string_literal_,
  'ref_pattern': ref_pattern_,
  'reference_expression': reference_expression_,
  'reference_pattern': reference_pattern_,
  'reference_type': reference_type_,
  'removed_trait_bound': removed_trait_bound_,
  'return_expression': return_expression_,
  'scoped_identifier': scoped_identifier_,
  'scoped_type_identifier': scoped_type_identifier_,
  'scoped_use_list': scoped_use_list_,
  'self_parameter': self_parameter_,
  'shorthand_field_initializer': shorthand_field_initializer_,
  'slice_pattern': slice_pattern_,
  'source_file': source_file_,
  'static_item': static_item_,
  'string_literal': string_literal_,
  'struct_expression': struct_expression_,
  'struct_item': struct_item_,
  'struct_pattern': struct_pattern_,
  'token_binding_pattern': token_binding_pattern_,
  'token_repetition': token_repetition_,
  'token_repetition_pattern': token_repetition_pattern_,
  'token_tree': token_tree_,
  'token_tree_pattern': token_tree_pattern_,
  'trait_bounds': trait_bounds_,
  'trait_item': trait_item_,
  'try_block': try_block_,
  'try_expression': try_expression_,
  'tuple_expression': tuple_expression_,
  'tuple_pattern': tuple_pattern_,
  'tuple_struct_pattern': tuple_struct_pattern_,
  'tuple_type': tuple_type_,
  'type_arguments': type_arguments_,
  'type_binding': type_binding_,
  'type_cast_expression': type_cast_expression_,
  'type_item': type_item_,
  'type_parameter': type_parameter_,
  'type_parameters': type_parameters_,
  'unary_expression': unary_expression_,
  'union_item': union_item_,
  'unsafe_block': unsafe_block_,
  'use_as_clause': use_as_clause_,
  'use_bounds': use_bounds_,
  'use_declaration': use_declaration_,
  'use_list': use_list_,
  'use_wildcard': use_wildcard_,
  'variadic_parameter': variadic_parameter_,
  'visibility_modifier': visibility_modifier_,
  'where_clause': where_clause_,
  'where_predicate': where_predicate_,
  'while_expression': while_expression_,
  'yield_expression': yield_expression_,
};

// Auto-generated by @sittir/codegen — do not edit

import type { NodeData } from './types.js';
import type { AbstractType, AbstractTypeConfig, AbstractTypeTree, Arguments, ArgumentsConfig, ArgumentsTree, ArrayExpression, ArrayExpressionConfig, ArrayExpressionTree, ArrayType, ArrayTypeConfig, ArrayTypeTree, AssignmentExpression, AssignmentExpressionConfig, AssignmentExpressionTree, AssociatedType, AssociatedTypeConfig, AssociatedTypeTree, AsyncBlock, AsyncBlockConfig, AsyncBlockTree, Attribute, AttributeConfig, AttributeItem, AttributeItemConfig, AttributeItemTree, AttributeTree, AwaitExpression, AwaitExpressionConfig, AwaitExpressionTree, BaseFieldInitializer, BaseFieldInitializerConfig, BaseFieldInitializerTree, BinaryExpression, BinaryExpressionConfig, BinaryExpressionTree, Block, BlockComment, BlockCommentConfig, BlockCommentTree, BlockConfig, BlockTree, BooleanLiteral, BooleanLiteralTree, BoundedType, BoundedTypeConfig, BoundedTypeTree, BracketedType, BracketedTypeConfig, BracketedTypeTree, BreakExpression, BreakExpressionConfig, BreakExpressionTree, CallExpression, CallExpressionConfig, CallExpressionTree, CapturedPattern, CapturedPatternConfig, CapturedPatternTree, CharLiteral, CharLiteralTree, ClosureExpression, ClosureExpressionConfig, ClosureExpressionTree, ClosureParameters, ClosureParametersConfig, ClosureParametersTree, CompoundAssignmentExpr, CompoundAssignmentExprConfig, CompoundAssignmentExprTree, ConstBlock, ConstBlockConfig, ConstBlockTree, ConstItem, ConstItemConfig, ConstItemTree, ConstParameter, ConstParameterConfig, ConstParameterTree, ContinueExpression, ContinueExpressionConfig, ContinueExpressionTree, Crate, CrateTree, DeclarationList, DeclarationListConfig, DeclarationListTree, DeclarationStatement, DocComment, DocCommentTree, DynamicType, DynamicTypeConfig, DynamicTypeTree, ElseClause, ElseClauseConfig, ElseClauseTree, EmptyStatement, EmptyStatementTree, EnumItem, EnumItemConfig, EnumItemTree, EnumVariant, EnumVariantConfig, EnumVariantList, EnumVariantListConfig, EnumVariantListTree, EnumVariantTree, EscapeSequence, EscapeSequenceTree, Expression, ExpressionStatement, ExpressionStatementConfig, ExpressionStatementTree, ExternCrateDeclaration, ExternCrateDeclarationConfig, ExternCrateDeclarationTree, ExternModifier, ExternModifierConfig, ExternModifierTree, FieldDeclaration, FieldDeclarationConfig, FieldDeclarationList, FieldDeclarationListConfig, FieldDeclarationListTree, FieldDeclarationTree, FieldExpression, FieldExpressionConfig, FieldExpressionTree, FieldIdentifier, FieldIdentifierTree, FieldInitializer, FieldInitializerConfig, FieldInitializerList, FieldInitializerListConfig, FieldInitializerListTree, FieldInitializerTree, FieldPattern, FieldPatternConfig, FieldPatternTree, FloatLiteral, FloatLiteralTree, ForExpression, ForExpressionConfig, ForExpressionTree, ForLifetimes, ForLifetimesConfig, ForLifetimesTree, ForeignModItem, ForeignModItemConfig, ForeignModItemTree, FragmentSpecifier, FragmentSpecifierTree, FunctionItem, FunctionItemConfig, FunctionItemTree, FunctionModifiers, FunctionModifiersConfig, FunctionModifiersTree, FunctionSignatureItem, FunctionSignatureItemConfig, FunctionSignatureItemTree, FunctionType, FunctionTypeConfig, FunctionTypeTree, GenBlock, GenBlockConfig, GenBlockTree, GenericFunction, GenericFunctionConfig, GenericFunctionTree, GenericPattern, GenericPatternConfig, GenericPatternTree, GenericType, GenericTypeConfig, GenericTypeTree, GenericTypeWithTurbofish, GenericTypeWithTurbofishConfig, GenericTypeWithTurbofishTree, HigherRankedTraitBound, HigherRankedTraitBoundConfig, HigherRankedTraitBoundTree, Identifier, IdentifierTree, IfExpression, IfExpressionConfig, IfExpressionTree, ImplItem, ImplItemConfig, ImplItemTree, IndexExpression, IndexExpressionConfig, IndexExpressionTree, InnerAttributeItem, InnerAttributeItemConfig, InnerAttributeItemTree, InnerDocCommentMarker, InnerDocCommentMarkerTree, IntegerLiteral, IntegerLiteralTree, Label, LabelConfig, LabelTree, LetChain, LetChainConfig, LetChainTree, LetCondition, LetConditionConfig, LetConditionTree, LetDeclaration, LetDeclarationConfig, LetDeclarationTree, Lifetime, LifetimeConfig, LifetimeParameter, LifetimeParameterConfig, LifetimeParameterTree, LifetimeTree, LineComment, LineCommentConfig, LineCommentTree, Literal, LiteralPattern, LoopExpression, LoopExpressionConfig, LoopExpressionTree, MacroDefinition, MacroDefinitionConfig, MacroDefinitionTree, MacroInvocation, MacroInvocationConfig, MacroInvocationTree, MacroRule, MacroRuleConfig, MacroRuleTree, MatchArm, MatchArmConfig, MatchArmTree, MatchBlock, MatchBlockConfig, MatchBlockTree, MatchExpression, MatchExpressionConfig, MatchExpressionTree, MatchPattern, MatchPatternConfig, MatchPatternTree, Metavariable, MetavariableTree, ModItem, ModItemConfig, ModItemTree, MutPattern, MutPatternConfig, MutPatternTree, MutableSpecifier, MutableSpecifierTree, NegativeLiteral, NegativeLiteralConfig, NegativeLiteralTree, NeverType, NeverTypeTree, OrPattern, OrPatternConfig, OrPatternTree, OrderedFieldDeclarationList, OrderedFieldDeclarationListConfig, OrderedFieldDeclarationListTree, OuterDocCommentMarker, OuterDocCommentMarkerTree, Parameter, ParameterConfig, ParameterTree, Parameters, ParametersConfig, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionConfig, ParenthesizedExpressionTree, Pattern, PointerType, PointerTypeConfig, PointerTypeTree, PrimitiveType, PrimitiveTypeTree, QualifiedType, QualifiedTypeConfig, QualifiedTypeTree, RangeExpression, RangeExpressionConfig, RangeExpressionTree, RangePattern, RangePatternConfig, RangePatternTree, RawStringLiteral, RawStringLiteralConfig, RawStringLiteralTree, RefPattern, RefPatternConfig, RefPatternTree, ReferenceExpression, ReferenceExpressionConfig, ReferenceExpressionTree, ReferencePattern, ReferencePatternConfig, ReferencePatternTree, ReferenceType, ReferenceTypeConfig, ReferenceTypeTree, RemainingFieldPattern, RemainingFieldPatternTree, RemovedTraitBound, RemovedTraitBoundConfig, RemovedTraitBoundTree, ReturnExpression, ReturnExpressionConfig, ReturnExpressionTree, ScopedIdentifier, ScopedIdentifierConfig, ScopedIdentifierTree, ScopedTypeIdentifier, ScopedTypeIdentifierConfig, ScopedTypeIdentifierTree, ScopedUseList, ScopedUseListConfig, ScopedUseListTree, Self, SelfParameter, SelfParameterConfig, SelfParameterTree, SelfTree, Shebang, ShebangTree, ShorthandFieldIdentifier, ShorthandFieldIdentifierTree, ShorthandFieldInitializer, ShorthandFieldInitializerConfig, ShorthandFieldInitializerTree, SlicePattern, SlicePatternConfig, SlicePatternTree, SourceFile, SourceFileConfig, SourceFileTree, StaticItem, StaticItemConfig, StaticItemTree, StringContent, StringContentTree, StringLiteral, StringLiteralConfig, StringLiteralTree, StructExpression, StructExpressionConfig, StructExpressionTree, StructItem, StructItemConfig, StructItemTree, StructPattern, StructPatternConfig, StructPatternTree, Super, SuperTree, TokenBindingPattern, TokenBindingPatternConfig, TokenBindingPatternTree, TokenRepetition, TokenRepetitionConfig, TokenRepetitionPattern, TokenRepetitionPatternConfig, TokenRepetitionPatternTree, TokenRepetitionTree, TokenTree, TokenTreeConfig, TokenTreePattern, TokenTreePatternConfig, TokenTreePatternTree, TokenTreeTree, TraitBounds, TraitBoundsConfig, TraitBoundsTree, TraitItem, TraitItemConfig, TraitItemTree, TryBlock, TryBlockConfig, TryBlockTree, TryExpression, TryExpressionConfig, TryExpressionTree, TupleExpression, TupleExpressionConfig, TupleExpressionTree, TuplePattern, TuplePatternConfig, TuplePatternTree, TupleStructPattern, TupleStructPatternConfig, TupleStructPatternTree, TupleType, TupleTypeConfig, TupleTypeTree, Type, TypeArguments, TypeArgumentsConfig, TypeArgumentsTree, TypeBinding, TypeBindingConfig, TypeBindingTree, TypeCastExpression, TypeCastExpressionConfig, TypeCastExpressionTree, TypeIdentifier, TypeIdentifierTree, TypeItem, TypeItemConfig, TypeItemTree, TypeParameter, TypeParameterConfig, TypeParameterTree, TypeParameters, TypeParametersConfig, TypeParametersTree, UnaryExpression, UnaryExpressionConfig, UnaryExpressionTree, UnionItem, UnionItemConfig, UnionItemTree, UnitExpression, UnitExpressionTree, UnitType, UnitTypeTree, UnsafeBlock, UnsafeBlockConfig, UnsafeBlockTree, UseAsClause, UseAsClauseConfig, UseAsClauseTree, UseBounds, UseBoundsConfig, UseBoundsTree, UseDeclaration, UseDeclarationConfig, UseDeclarationTree, UseList, UseListConfig, UseListTree, UseWildcard, UseWildcardConfig, UseWildcardTree, VariadicParameter, VariadicParameterConfig, VariadicParameterTree, VisibilityModifier, VisibilityModifierConfig, VisibilityModifierTree, WhereClause, WhereClauseConfig, WhereClauseTree, WherePredicate, WherePredicateConfig, WherePredicateTree, WhileExpression, WhileExpressionConfig, WhileExpressionTree, YieldExpression, YieldExpressionConfig, YieldExpressionTree } from './types.js';
import type { Edit, AnyNodeData } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { rules } from './rules.js';
import { joinBy } from './joinby.js';

const { render, toEdit } = createRenderer(rules, joinBy);

const RESERVED_KEYWORDS = new Set([
  '_',
  'as',
  'async',
  'await',
  'break',
  'const',
  'continue',
  'default',
  'dyn',
  'else',
  'enum',
  'expr',
  'extern',
  'false',
  'fn',
  'for',
  'gen',
  'ident',
  'if',
  'impl',
  'in',
  'item',
  'let',
  'literal',
  'loop',
  'match',
  'meta',
  'mod',
  'move',
  'pat',
  'pat_param',
  'path',
  'pub',
  'raw',
  'ref',
  'return',
  'static',
  'stmt',
  'struct',
  'trait',
  'true',
  'try',
  'tt',
  'ty',
  'type',
  'union',
  'unsafe',
  'use',
  'vis',
  'where',
  'while',
  'yield',
]);

export function abstract_type_(
  config: AbstractTypeConfig,
) {
  const fields = {
    trait: config.trait,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'abstract_type' as const,
    fields,
    children,
    trait(v?: BoundedType | FunctionType | GenericType | RemovedTraitBound | ScopedTypeIdentifier | TupleType | TypeIdentifier): any { return v !== undefined ? abstract_type_({ ...config, trait: v }) : fields.trait; },
    child(v?: TypeParameters): any { return v !== undefined ? abstract_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function arguments_(
  config?: ArgumentsConfig,
) {
  const fields = {
  };
  const children = [...(config?.attributeItem ?? []), config?.expression, ...(config?.children2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'arguments' as const,
    fields,
    children,
    attributeItem(...v: (AttributeItem)[]): any { return v.length ? arguments_({ ...config, attributeItem: v }) : config?.attributeItem; },
    expression(v?: Expression): any { return v !== undefined ? arguments_({ ...config, expression: v }) : config?.expression; },
    children2(...v: (AttributeItem | Expression)[]): any { return v.length ? arguments_({ ...config, children2: v }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_expression_(
  config?: ArrayExpressionConfig,
) {
  const fields = {
    length: config?.length,
  };
  const children = [...(config?.attributeItem ?? []), ...(config?.children1 ?? []), config?.expression, ...(config?.children3 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'array_expression' as const,
    fields,
    children,
    length(v?: Expression): any { return v !== undefined ? array_expression_({ ...config, length: v }) : fields.length; },
    attributeItem(...v: (AttributeItem)[]): any { return v.length ? array_expression_({ ...config, attributeItem: v }) : config?.attributeItem; },
    children1(...v: (AttributeItem | Expression)[]): any { return v.length ? array_expression_({ ...config, children1: v }) : config?.children1; },
    expression(v?: Expression): any { return v !== undefined ? array_expression_({ ...config, expression: v }) : config?.expression; },
    children3(...v: (AttributeItem | Expression)[]): any { return v.length ? array_expression_({ ...config, children3: v }) : config?.children3; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function array_type_(
  config: ArrayTypeConfig,
) {
  const fields = {
    element: config.element,
    length: config.length,
  };
  return {
    type: 'array_type' as const,
    fields,
    element(v?: Type): any { return v !== undefined ? array_type_({ ...config, element: v }) : fields.element; },
    length(v?: Expression): any { return v !== undefined ? array_type_({ ...config, length: v }) : fields.length; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function assignment_expression_(
  config: AssignmentExpressionConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'assignment_expression' as const,
    fields,
    left(v?: Expression): any { return v !== undefined ? assignment_expression_({ ...config, left: v }) : fields.left; },
    right(v?: Expression): any { return v !== undefined ? assignment_expression_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function associated_type_(
  config: AssociatedTypeConfig,
) {
  const fields = {
    bounds: config.bounds,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'associated_type' as const,
    fields,
    children,
    bounds(v?: TraitBounds): any { return v !== undefined ? associated_type_({ ...config, bounds: v }) : fields.bounds; },
    name(v?: TypeIdentifier): any { return v !== undefined ? associated_type_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? associated_type_({ ...config, typeParameters: v }) : fields.type_parameters; },
    child(v?: WhereClause): any { return v !== undefined ? associated_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssociatedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function async_block_(
  config: AsyncBlockConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'async_block' as const,
    fields,
    children,
    child(v?: Block): any { return v !== undefined ? async_block_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsyncBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function attribute_(
  config: AttributeConfig,
) {
  const fields = {
    arguments: config.arguments,
    value: config.value,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'attribute' as const,
    fields,
    children,
    arguments(v?: TokenTree): any { return v !== undefined ? attribute_({ ...config, arguments: v }) : fields.arguments; },
    value(v?: Expression): any { return v !== undefined ? attribute_({ ...config, value: v }) : fields.value; },
    child(v?: Crate | Identifier | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? attribute_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function attribute_item_(
  config: AttributeItemConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'attribute_item' as const,
    fields,
    children,
    child(v?: Attribute): any { return v !== undefined ? attribute_item_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function await_expression_(
  config: AwaitExpressionConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'await_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? await_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function base_field_initializer_(
  config: BaseFieldInitializerConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'base_field_initializer' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? base_field_initializer_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BaseFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function binary_expression_(
  config: BinaryExpressionConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'binary_expression' as const,
    fields,
    left(v?: Expression): any { return v !== undefined ? binary_expression_({ ...config, left: v }) : fields.left; },
    operator(v?: '!=' | '%' | '&' | '&&' | '*' | '+' | '-' | '/' | '<' | '<<' | '<=' | '==' | '>' | '>=' | '>>' | '^' | '|' | '||'): any { return v !== undefined ? binary_expression_({ ...config, operator: v }) : fields.operator; },
    right(v?: Expression): any { return v !== undefined ? binary_expression_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function block_(
  config?: BlockConfig,
) {
  const fields = {
  };
  const children = [config?.label, ...(config?.children1 ?? []), config?.expression].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'block' as const,
    fields,
    children,
    label(v?: Label): any { return v !== undefined ? block_({ ...config, label: v }) : config?.label; },
    children1(...v: (DeclarationStatement | ExpressionStatement)[]): any { return v.length ? block_({ ...config, children1: v }) : config?.children1; },
    expression(v?: Expression): any { return v !== undefined ? block_({ ...config, expression: v }) : config?.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function block_comment_(
  config?: BlockCommentConfig,
) {
  const fields = {
    doc: config?.doc,
    inner: config?.inner,
    outer: config?.outer,
  };
  return {
    type: 'block_comment' as const,
    fields,
    doc(v?: DocComment): any { return v !== undefined ? block_comment_({ ...config, doc: v }) : fields.doc; },
    inner(v?: InnerDocCommentMarker): any { return v !== undefined ? block_comment_({ ...config, inner: v }) : fields.inner; },
    outer(v?: OuterDocCommentMarker): any { return v !== undefined ? block_comment_({ ...config, outer: v }) : fields.outer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function bounded_type_(
  config: BoundedTypeConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.children1].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'bounded_type' as const,
    fields,
    children,
    children0(v?: Lifetime | Type | UseBounds): any { return v !== undefined ? bounded_type_({ ...config, children0: v }) : config?.children0; },
    children1(v?: Lifetime | Type | UseBounds): any { return v !== undefined ? bounded_type_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BoundedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function bracketed_type_(
  config: BracketedTypeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'bracketed_type' as const,
    fields,
    children,
    child(v?: QualifiedType | Type): any { return v !== undefined ? bracketed_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BracketedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function break_expression_(
  config?: BreakExpressionConfig,
) {
  const fields = {
  };
  const children = [config?.label, config?.expression].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'break_expression' as const,
    fields,
    children,
    label(v?: Label): any { return v !== undefined ? break_expression_({ ...config, label: v }) : config?.label; },
    expression(v?: Expression): any { return v !== undefined ? break_expression_({ ...config, expression: v }) : config?.expression; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function call_expression_(
  config: CallExpressionConfig,
) {
  const fields = {
    arguments: config.arguments,
    function: config.function,
  };
  return {
    type: 'call_expression' as const,
    fields,
    arguments(v?: Arguments): any { return v !== undefined ? call_expression_({ ...config, arguments: v }) : fields.arguments; },
    function(v?: ArrayExpression | AssignmentExpression | AsyncBlock | AwaitExpression | BinaryExpression | Block | BreakExpression | CallExpression | ClosureExpression | CompoundAssignmentExpr | ConstBlock | ContinueExpression | FieldExpression | ForExpression | GenBlock | GenericFunction | Identifier | IfExpression | IndexExpression | Literal | LoopExpression | MacroInvocation | MatchExpression | Metavariable | ParenthesizedExpression | ReferenceExpression | ReturnExpression | ScopedIdentifier | Self | StructExpression | TryBlock | TryExpression | TupleExpression | TypeCastExpression | UnaryExpression | UnitExpression | UnsafeBlock | WhileExpression | YieldExpression): any { return v !== undefined ? call_expression_({ ...config, function: v }) : fields.function; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function captured_pattern_(
  config: CapturedPatternConfig,
) {
  const fields = {
  };
  const children = [config.identifier, config.pattern].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'captured_pattern' as const,
    fields,
    children,
    identifier(v?: Identifier): any { return v !== undefined ? captured_pattern_({ ...config, identifier: v }) : config?.identifier; },
    pattern(v?: Pattern): any { return v !== undefined ? captured_pattern_({ ...config, pattern: v }) : config?.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CapturedPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function closure_expression_(
  config: ClosureExpressionConfig,
) {
  const fields = {
    body: config.body,
    parameters: config.parameters,
    return_type: config.returnType,
  };
  return {
    type: 'closure_expression' as const,
    fields,
    body(v?: Block | Expression): any { return v !== undefined ? closure_expression_({ ...config, body: v }) : fields.body; },
    parameters(v?: ClosureParameters): any { return v !== undefined ? closure_expression_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: Type): any { return v !== undefined ? closure_expression_({ ...config, returnType: v }) : fields.return_type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function closure_parameters_(
  config?: ClosureParametersConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'closure_parameters' as const,
    fields,
    children,
    children0(v?: Parameter | Pattern): any { return v !== undefined ? closure_parameters_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (Parameter | Pattern)[]): any { return v.length ? closure_parameters_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function compound_assignment_expr_(
  config: CompoundAssignmentExprConfig,
) {
  const fields = {
    left: config.left,
    operator: config.operator,
    right: config.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    fields,
    left(v?: Expression): any { return v !== undefined ? compound_assignment_expr_({ ...config, left: v }) : fields.left; },
    operator(v?: '+=' | '-=' | '*=' | '/=' | '%=' | '&=' | '|=' | '^=' | '<<=' | '>>='): any { return v !== undefined ? compound_assignment_expr_({ ...config, operator: v }) : fields.operator; },
    right(v?: Expression): any { return v !== undefined ? compound_assignment_expr_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function const_block_(
  config: ConstBlockConfig,
) {
  const fields = {
    body: config.body,
  };
  return {
    type: 'const_block' as const,
    fields,
    body(v?: Block): any { return v !== undefined ? const_block_({ ...config, body: v }) : fields.body; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function const_item_(
  config: ConstItemConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'const_item' as const,
    fields,
    children,
    name(v?: Identifier): any { return v !== undefined ? const_item_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? const_item_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? const_item_({ ...config, value: v }) : fields.value; },
    child(v?: VisibilityModifier): any { return v !== undefined ? const_item_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function const_parameter_(
  config: ConstParameterConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  return {
    type: 'const_parameter' as const,
    fields,
    name(v?: Identifier): any { return v !== undefined ? const_parameter_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? const_parameter_({ ...config, type: v }) : fields.type; },
    value(v?: Block | Identifier | Literal | NegativeLiteral): any { return v !== undefined ? const_parameter_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function continue_expression_(
  config?: ContinueExpressionConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'continue_expression' as const,
    fields,
    children,
    child(v?: Label): any { return v !== undefined ? continue_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function declaration_list_(
  config?: DeclarationListConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'declaration_list' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (DeclarationStatement)[]): any { return declaration_list_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function dynamic_type_(
  config: DynamicTypeConfig,
) {
  const fields = {
    trait: config.trait,
  };
  return {
    type: 'dynamic_type' as const,
    fields,
    trait(v?: FunctionType | GenericType | HigherRankedTraitBound | ScopedTypeIdentifier | TupleType | TypeIdentifier): any { return v !== undefined ? dynamic_type_({ ...config, trait: v }) : fields.trait; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DynamicTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function else_clause_(
  config: ElseClauseConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'else_clause' as const,
    fields,
    children,
    child(v?: Block | IfExpression): any { return v !== undefined ? else_clause_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_item_(
  config: EnumItemConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.visibilityModifier, config.whereClause].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'enum_item' as const,
    fields,
    children,
    body(v?: EnumVariantList): any { return v !== undefined ? enum_item_({ ...config, body: v }) : fields.body; },
    name(v?: TypeIdentifier): any { return v !== undefined ? enum_item_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? enum_item_({ ...config, typeParameters: v }) : fields.type_parameters; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? enum_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    whereClause(v?: WhereClause): any { return v !== undefined ? enum_item_({ ...config, whereClause: v }) : config?.whereClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_variant_(
  config: EnumVariantConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    value: config.value,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'enum_variant' as const,
    fields,
    children,
    body(v?: FieldDeclarationList | OrderedFieldDeclarationList): any { return v !== undefined ? enum_variant_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? enum_variant_({ ...config, name: v }) : fields.name; },
    value(v?: Expression): any { return v !== undefined ? enum_variant_({ ...config, value: v }) : fields.value; },
    child(v?: VisibilityModifier): any { return v !== undefined ? enum_variant_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function enum_variant_list_(
  config?: EnumVariantListConfig,
) {
  const fields = {
  };
  const children = [...(config?.attributeItem ?? []), config?.enumVariant, ...(config?.children2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'enum_variant_list' as const,
    fields,
    children,
    attributeItem(...v: (AttributeItem)[]): any { return v.length ? enum_variant_list_({ ...config, attributeItem: v }) : config?.attributeItem; },
    enumVariant(v?: EnumVariant): any { return v !== undefined ? enum_variant_list_({ ...config, enumVariant: v }) : config?.enumVariant; },
    children2(...v: (AttributeItem | EnumVariant)[]): any { return v.length ? enum_variant_list_({ ...config, children2: v }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function expression_statement_(
  config: ExpressionStatementConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'expression_statement' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? expression_statement_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extern_crate_declaration_(
  config: ExternCrateDeclarationConfig,
) {
  const fields = {
    alias: config.alias,
    name: config.name,
  };
  const children = [config.visibilityModifier, config.crate].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'extern_crate_declaration' as const,
    fields,
    children,
    alias(v?: Identifier): any { return v !== undefined ? extern_crate_declaration_({ ...config, alias: v }) : fields.alias; },
    name(v?: Identifier): any { return v !== undefined ? extern_crate_declaration_({ ...config, name: v }) : fields.name; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? extern_crate_declaration_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    crate(v?: Crate): any { return v !== undefined ? extern_crate_declaration_({ ...config, crate: v }) : config?.crate; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternCrateDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function extern_modifier_(
  config?: ExternModifierConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'extern_modifier' as const,
    fields,
    children,
    child(v?: StringLiteral): any { return v !== undefined ? extern_modifier_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_declaration_(
  config: FieldDeclarationConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'field_declaration' as const,
    fields,
    children,
    name(v?: FieldIdentifier): any { return v !== undefined ? field_declaration_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? field_declaration_({ ...config, type: v }) : fields.type; },
    child(v?: VisibilityModifier): any { return v !== undefined ? field_declaration_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_declaration_list_(
  config?: FieldDeclarationListConfig,
) {
  const fields = {
  };
  const children = [...(config?.attributeItem ?? []), config?.fieldDeclaration, ...(config?.children2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'field_declaration_list' as const,
    fields,
    children,
    attributeItem(...v: (AttributeItem)[]): any { return v.length ? field_declaration_list_({ ...config, attributeItem: v }) : config?.attributeItem; },
    fieldDeclaration(v?: FieldDeclaration): any { return v !== undefined ? field_declaration_list_({ ...config, fieldDeclaration: v }) : config?.fieldDeclaration; },
    children2(...v: (AttributeItem | FieldDeclaration)[]): any { return v.length ? field_declaration_list_({ ...config, children2: v }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_expression_(
  config: FieldExpressionConfig,
) {
  const fields = {
    field: config.field,
    value: config.value,
  };
  return {
    type: 'field_expression' as const,
    fields,
    field(v?: FieldIdentifier | IntegerLiteral): any { return v !== undefined ? field_expression_({ ...config, field: v }) : fields.field; },
    value(v?: Expression): any { return v !== undefined ? field_expression_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_initializer_(
  config: FieldInitializerConfig,
) {
  const fields = {
    field: config.field,
    value: config.value,
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'field_initializer' as const,
    fields,
    children,
    field(v?: FieldIdentifier | IntegerLiteral): any { return v !== undefined ? field_initializer_({ ...config, field: v }) : fields.field; },
    value(v?: Expression): any { return v !== undefined ? field_initializer_({ ...config, value: v }) : fields.value; },
    getChildren(): any { return children; },
    setChildren(...v: (AttributeItem)[]): any { return field_initializer_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_initializer_list_(
  config?: FieldInitializerListConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'field_initializer_list' as const,
    fields,
    children,
    children0(v?: BaseFieldInitializer | FieldInitializer | ShorthandFieldInitializer): any { return v !== undefined ? field_initializer_list_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (BaseFieldInitializer | FieldInitializer | ShorthandFieldInitializer)[]): any { return v.length ? field_initializer_list_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function field_pattern_(
  config: FieldPatternConfig,
) {
  const fields = {
    name: config.name,
    pattern: config.pattern,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'field_pattern' as const,
    fields,
    children,
    name(v?: FieldIdentifier | ShorthandFieldIdentifier): any { return v !== undefined ? field_pattern_({ ...config, name: v }) : fields.name; },
    pattern(v?: Pattern): any { return v !== undefined ? field_pattern_({ ...config, pattern: v }) : fields.pattern; },
    child(v?: MutableSpecifier): any { return v !== undefined ? field_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_expression_(
  config: ForExpressionConfig,
) {
  const fields = {
    body: config.body,
    pattern: config.pattern,
    value: config.value,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'for_expression' as const,
    fields,
    children,
    body(v?: Block): any { return v !== undefined ? for_expression_({ ...config, body: v }) : fields.body; },
    pattern(v?: Pattern): any { return v !== undefined ? for_expression_({ ...config, pattern: v }) : fields.pattern; },
    value(v?: Expression): any { return v !== undefined ? for_expression_({ ...config, value: v }) : fields.value; },
    child(v?: Label): any { return v !== undefined ? for_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function for_lifetimes_(
  config: ForLifetimesConfig,
) {
  const fields = {
  };
  const children = [config.lifetime1, ...(config.lifetime2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'for_lifetimes' as const,
    fields,
    children,
    lifetime1(v?: Lifetime): any { return v !== undefined ? for_lifetimes_({ ...config, lifetime1: v }) : config?.lifetime1; },
    lifetime2(...v: (Lifetime)[]): any { return v.length ? for_lifetimes_({ ...config, lifetime2: v }) : config?.lifetime2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForLifetimesTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function foreign_mod_item_(
  config: ForeignModItemConfig,
) {
  const fields = {
    body: config.body,
  };
  const children = [config.visibilityModifier, config.externModifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'foreign_mod_item' as const,
    fields,
    children,
    body(v?: DeclarationList): any { return v !== undefined ? foreign_mod_item_({ ...config, body: v }) : fields.body; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? foreign_mod_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    externModifier(v?: ExternModifier): any { return v !== undefined ? foreign_mod_item_({ ...config, externModifier: v }) : config?.externModifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForeignModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_item_(
  config: FunctionItemConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  const children = [config.visibilityModifier, config.functionModifiers, config.whereClause].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'function_item' as const,
    fields,
    children,
    body(v?: Block): any { return v !== undefined ? function_item_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier | Metavariable): any { return v !== undefined ? function_item_({ ...config, name: v }) : fields.name; },
    parameters(v?: Parameters): any { return v !== undefined ? function_item_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: Type): any { return v !== undefined ? function_item_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? function_item_({ ...config, typeParameters: v }) : fields.type_parameters; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? function_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    functionModifiers(v?: FunctionModifiers): any { return v !== undefined ? function_item_({ ...config, functionModifiers: v }) : config?.functionModifiers; },
    whereClause(v?: WhereClause): any { return v !== undefined ? function_item_({ ...config, whereClause: v }) : config?.whereClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_modifiers_(
  config?: FunctionModifiersConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'function_modifiers' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (ExternModifier)[]): any { return function_modifiers_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_signature_item_(
  config: FunctionSignatureItemConfig,
) {
  const fields = {
    name: config.name,
    parameters: config.parameters,
    return_type: config.returnType,
    type_parameters: config.typeParameters,
  };
  const children = [config.visibilityModifier, config.functionModifiers, config.whereClause].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'function_signature_item' as const,
    fields,
    children,
    name(v?: Identifier | Metavariable): any { return v !== undefined ? function_signature_item_({ ...config, name: v }) : fields.name; },
    parameters(v?: Parameters): any { return v !== undefined ? function_signature_item_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: Type): any { return v !== undefined ? function_signature_item_({ ...config, returnType: v }) : fields.return_type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? function_signature_item_({ ...config, typeParameters: v }) : fields.type_parameters; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? function_signature_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    functionModifiers(v?: FunctionModifiers): any { return v !== undefined ? function_signature_item_({ ...config, functionModifiers: v }) : config?.functionModifiers; },
    whereClause(v?: WhereClause): any { return v !== undefined ? function_signature_item_({ ...config, whereClause: v }) : config?.whereClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function function_type_(
  config: FunctionTypeConfig,
) {
  const fields = {
    parameters: config.parameters,
    return_type: config.returnType,
    trait: config.trait,
  };
  const children = [config.forLifetimes, config.functionModifiers].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'function_type' as const,
    fields,
    children,
    parameters(v?: Parameters): any { return v !== undefined ? function_type_({ ...config, parameters: v }) : fields.parameters; },
    returnType(v?: Type): any { return v !== undefined ? function_type_({ ...config, returnType: v }) : fields.return_type; },
    trait(v?: ScopedTypeIdentifier | TypeIdentifier): any { return v !== undefined ? function_type_({ ...config, trait: v }) : fields.trait; },
    forLifetimes(v?: ForLifetimes): any { return v !== undefined ? function_type_({ ...config, forLifetimes: v }) : config?.forLifetimes; },
    functionModifiers(v?: FunctionModifiers): any { return v !== undefined ? function_type_({ ...config, functionModifiers: v }) : config?.functionModifiers; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function gen_block_(
  config: GenBlockConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'gen_block' as const,
    fields,
    children,
    child(v?: Block): any { return v !== undefined ? gen_block_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_function_(
  config: GenericFunctionConfig,
) {
  const fields = {
    function: config.function,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_function' as const,
    fields,
    function(v?: FieldExpression | Identifier | ScopedIdentifier): any { return v !== undefined ? generic_function_({ ...config, function: v }) : fields.function; },
    typeArguments(v?: TypeArguments): any { return v !== undefined ? generic_function_({ ...config, typeArguments: v }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_pattern_(
  config: GenericPatternConfig,
) {
  const fields = {
    type_arguments: config.typeArguments,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'generic_pattern' as const,
    fields,
    children,
    typeArguments(v?: TypeArguments): any { return v !== undefined ? generic_pattern_({ ...config, typeArguments: v }) : fields.type_arguments; },
    child(v?: Identifier | ScopedIdentifier): any { return v !== undefined ? generic_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_(
  config: GenericTypeConfig,
) {
  const fields = {
    type: config.type,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    fields,
    typeField(v?: Identifier | ScopedIdentifier | ScopedTypeIdentifier | TypeIdentifier): any { return v !== undefined ? generic_type_({ ...config, type: v }) : fields.type; },
    typeArguments(v?: TypeArguments): any { return v !== undefined ? generic_type_({ ...config, typeArguments: v }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function generic_type_with_turbofish_(
  config: GenericTypeWithTurbofishConfig,
) {
  const fields = {
    type: config.type,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'generic_type_with_turbofish' as const,
    fields,
    typeField(v?: ScopedIdentifier | TypeIdentifier): any { return v !== undefined ? generic_type_with_turbofish_({ ...config, type: v }) : fields.type; },
    typeArguments(v?: TypeArguments): any { return v !== undefined ? generic_type_with_turbofish_({ ...config, typeArguments: v }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeWithTurbofishTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function higher_ranked_trait_bound_(
  config: HigherRankedTraitBoundConfig,
) {
  const fields = {
    type: config.type,
    type_parameters: config.typeParameters,
  };
  return {
    type: 'higher_ranked_trait_bound' as const,
    fields,
    typeField(v?: Type): any { return v !== undefined ? higher_ranked_trait_bound_({ ...config, type: v }) : fields.type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? higher_ranked_trait_bound_({ ...config, typeParameters: v }) : fields.type_parameters; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HigherRankedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function if_expression_(
  config: IfExpressionConfig,
) {
  const fields = {
    alternative: config.alternative,
    condition: config.condition,
    consequence: config.consequence,
  };
  return {
    type: 'if_expression' as const,
    fields,
    alternative(v?: ElseClause): any { return v !== undefined ? if_expression_({ ...config, alternative: v }) : fields.alternative; },
    condition(v?: Expression | LetChain | LetCondition): any { return v !== undefined ? if_expression_({ ...config, condition: v }) : fields.condition; },
    consequence(v?: Block): any { return v !== undefined ? if_expression_({ ...config, consequence: v }) : fields.consequence; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function impl_item_(
  config: ImplItemConfig,
) {
  const fields = {
    body: config.body,
    trait: config.trait,
    type: config.type,
    type_parameters: config.typeParameters,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'impl_item' as const,
    fields,
    children,
    body(v?: DeclarationList): any { return v !== undefined ? impl_item_({ ...config, body: v }) : fields.body; },
    trait(v?: GenericType | ScopedTypeIdentifier | TypeIdentifier): any { return v !== undefined ? impl_item_({ ...config, trait: v }) : fields.trait; },
    typeField(v?: Type): any { return v !== undefined ? impl_item_({ ...config, type: v }) : fields.type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? impl_item_({ ...config, typeParameters: v }) : fields.type_parameters; },
    child(v?: WhereClause): any { return v !== undefined ? impl_item_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function index_expression_(
  config: IndexExpressionConfig,
) {
  const fields = {
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'index_expression' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Expression)[]): any { return index_expression_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function inner_attribute_item_(
  config: InnerAttributeItemConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'inner_attribute_item' as const,
    fields,
    children,
    child(v?: Attribute): any { return v !== undefined ? inner_attribute_item_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InnerAttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function label_(
  config: LabelConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'label' as const,
    fields,
    children,
    child(v?: Identifier): any { return v !== undefined ? label_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabelTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function let_chain_(
  config: LetChainConfig,
) {
  const fields = {
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'let_chain' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Expression | LetCondition)[]): any { return let_chain_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function let_condition_(
  config: LetConditionConfig,
) {
  const fields = {
    pattern: config.pattern,
    value: config.value,
  };
  return {
    type: 'let_condition' as const,
    fields,
    pattern(v?: Pattern): any { return v !== undefined ? let_condition_({ ...config, pattern: v }) : fields.pattern; },
    value(v?: Expression): any { return v !== undefined ? let_condition_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetConditionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function let_declaration_(
  config: LetDeclarationConfig,
) {
  const fields = {
    alternative: config.alternative,
    pattern: config.pattern,
    type: config.type,
    value: config.value,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'let_declaration' as const,
    fields,
    children,
    alternative(v?: Block): any { return v !== undefined ? let_declaration_({ ...config, alternative: v }) : fields.alternative; },
    pattern(v?: Pattern): any { return v !== undefined ? let_declaration_({ ...config, pattern: v }) : fields.pattern; },
    typeField(v?: Type): any { return v !== undefined ? let_declaration_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? let_declaration_({ ...config, value: v }) : fields.value; },
    child(v?: MutableSpecifier): any { return v !== undefined ? let_declaration_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lifetime_(
  config: LifetimeConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'lifetime' as const,
    fields,
    children,
    child(v?: Identifier): any { return v !== undefined ? lifetime_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function lifetime_parameter_(
  config: LifetimeParameterConfig,
) {
  const fields = {
    bounds: config.bounds,
    name: config.name,
  };
  return {
    type: 'lifetime_parameter' as const,
    fields,
    bounds(v?: TraitBounds): any { return v !== undefined ? lifetime_parameter_({ ...config, bounds: v }) : fields.bounds; },
    name(v?: Lifetime): any { return v !== undefined ? lifetime_parameter_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function line_comment_(
  config?: LineCommentConfig,
) {
  const fields = {
    doc: config?.doc,
    inner: config?.inner,
    outer: config?.outer,
  };
  return {
    type: 'line_comment' as const,
    fields,
    doc(v?: DocComment): any { return v !== undefined ? line_comment_({ ...config, doc: v }) : fields.doc; },
    inner(v?: InnerDocCommentMarker): any { return v !== undefined ? line_comment_({ ...config, inner: v }) : fields.inner; },
    outer(v?: OuterDocCommentMarker): any { return v !== undefined ? line_comment_({ ...config, outer: v }) : fields.outer; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LineCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function loop_expression_(
  config: LoopExpressionConfig,
) {
  const fields = {
    body: config.body,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'loop_expression' as const,
    fields,
    children,
    body(v?: Block): any { return v !== undefined ? loop_expression_({ ...config, body: v }) : fields.body; },
    child(v?: Label): any { return v !== undefined ? loop_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LoopExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function macro_definition_(
  config: MacroDefinitionConfig,
) {
  const fields = {
    name: config.name,
  };
  const children = [...(config.macroRule1 ?? []), config.macroRule2].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'macro_definition' as const,
    fields,
    children,
    name(v?: Identifier): any { return v !== undefined ? macro_definition_({ ...config, name: v }) : fields.name; },
    macroRule1(...v: (MacroRule)[]): any { return v.length ? macro_definition_({ ...config, macroRule1: v }) : config?.macroRule1; },
    macroRule2(v?: MacroRule): any { return v !== undefined ? macro_definition_({ ...config, macroRule2: v }) : config?.macroRule2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function macro_invocation_(
  config: MacroInvocationConfig,
) {
  const fields = {
    macro: config.macro,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'macro_invocation' as const,
    fields,
    children,
    macro(v?: Identifier | ScopedIdentifier): any { return v !== undefined ? macro_invocation_({ ...config, macro: v }) : fields.macro; },
    child(v?: TokenTree): any { return v !== undefined ? macro_invocation_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroInvocationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function macro_rule_(
  config: MacroRuleConfig,
) {
  const fields = {
    left: config.left,
    right: config.right,
  };
  return {
    type: 'macro_rule' as const,
    fields,
    left(v?: TokenTreePattern): any { return v !== undefined ? macro_rule_({ ...config, left: v }) : fields.left; },
    right(v?: TokenTree): any { return v !== undefined ? macro_rule_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroRuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_arm_(
  config: MatchArmConfig,
) {
  const fields = {
    pattern: config.pattern,
    value: config.value,
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'match_arm' as const,
    fields,
    children,
    pattern(v?: MatchPattern): any { return v !== undefined ? match_arm_({ ...config, pattern: v }) : fields.pattern; },
    value(v?: Expression): any { return v !== undefined ? match_arm_({ ...config, value: v }) : fields.value; },
    getChildren(): any { return children; },
    setChildren(...v: (AttributeItem | InnerAttributeItem)[]): any { return match_arm_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_block_(
  config?: MatchBlockConfig,
) {
  const fields = {
  };
  const children = [...(config?.matchArm1 ?? []), config?.matchArm2].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'match_block' as const,
    fields,
    children,
    matchArm1(...v: (MatchArm)[]): any { return v.length ? match_block_({ ...config, matchArm1: v }) : config?.matchArm1; },
    matchArm2(v?: MatchArm): any { return v !== undefined ? match_block_({ ...config, matchArm2: v }) : config?.matchArm2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_expression_(
  config: MatchExpressionConfig,
) {
  const fields = {
    body: config.body,
    value: config.value,
  };
  return {
    type: 'match_expression' as const,
    fields,
    body(v?: MatchBlock): any { return v !== undefined ? match_expression_({ ...config, body: v }) : fields.body; },
    value(v?: Expression): any { return v !== undefined ? match_expression_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function match_pattern_(
  config: MatchPatternConfig,
) {
  const fields = {
    condition: config.condition,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'match_pattern' as const,
    fields,
    children,
    condition(v?: Expression | LetChain | LetCondition): any { return v !== undefined ? match_pattern_({ ...config, condition: v }) : fields.condition; },
    child(v?: Pattern): any { return v !== undefined ? match_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function mod_item_(
  config: ModItemConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'mod_item' as const,
    fields,
    children,
    body(v?: DeclarationList): any { return v !== undefined ? mod_item_({ ...config, body: v }) : fields.body; },
    name(v?: Identifier): any { return v !== undefined ? mod_item_({ ...config, name: v }) : fields.name; },
    child(v?: VisibilityModifier): any { return v !== undefined ? mod_item_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function mut_pattern_(
  config: MutPatternConfig,
) {
  const fields = {
  };
  const children = [config.mutableSpecifier, config.pattern].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'mut_pattern' as const,
    fields,
    children,
    mutableSpecifier(v?: MutableSpecifier): any { return v !== undefined ? mut_pattern_({ ...config, mutableSpecifier: v }) : config?.mutableSpecifier; },
    pattern(v?: Pattern): any { return v !== undefined ? mut_pattern_({ ...config, pattern: v }) : config?.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MutPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function negative_literal_(
  config: NegativeLiteralConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'negative_literal' as const,
    fields,
    children,
    child(v?: FloatLiteral | IntegerLiteral): any { return v !== undefined ? negative_literal_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NegativeLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function or_pattern_(
  config: OrPatternConfig,
) {
  const fields = {
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'or_pattern' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Pattern)[]): any { return or_pattern_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ordered_field_declaration_list_(
  config?: OrderedFieldDeclarationListConfig,
) {
  const fields = {
    type: config?.type,
  };
  const children = [...(config?.attributeItem ?? []), config?.visibilityModifier, ...(config?.children2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'ordered_field_declaration_list' as const,
    fields,
    children,
    typeField(...v: (Type)[]): any { return v.length ? ordered_field_declaration_list_({ ...config, type: v }) : fields.type; },
    attributeItem(...v: (AttributeItem)[]): any { return v.length ? ordered_field_declaration_list_({ ...config, attributeItem: v }) : config?.attributeItem; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? ordered_field_declaration_list_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    children2(...v: (AttributeItem | VisibilityModifier)[]): any { return v.length ? ordered_field_declaration_list_({ ...config, children2: v }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrderedFieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parameter_(
  config: ParameterConfig,
) {
  const fields = {
    pattern: config.pattern,
    type: config.type,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'parameter' as const,
    fields,
    children,
    pattern(v?: Pattern | Self): any { return v !== undefined ? parameter_({ ...config, pattern: v }) : fields.pattern; },
    typeField(v?: Type): any { return v !== undefined ? parameter_({ ...config, type: v }) : fields.type; },
    child(v?: MutableSpecifier): any { return v !== undefined ? parameter_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parameters_(
  config?: ParametersConfig,
) {
  const fields = {
  };
  const children = [config?.attributeItem, config?.children1, ...(config?.children2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'parameters' as const,
    fields,
    children,
    attributeItem(v?: AttributeItem): any { return v !== undefined ? parameters_({ ...config, attributeItem: v }) : config?.attributeItem; },
    children1(v?: Parameter | SelfParameter | Type | VariadicParameter): any { return v !== undefined ? parameters_({ ...config, children1: v }) : config?.children1; },
    children2(...v: (AttributeItem | Parameter | SelfParameter | Type | VariadicParameter)[]): any { return v.length ? parameters_({ ...config, children2: v }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function parenthesized_expression_(
  config: ParenthesizedExpressionConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'parenthesized_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? parenthesized_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function pointer_type_(
  config: PointerTypeConfig,
) {
  const fields = {
    type: config.type,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'pointer_type' as const,
    fields,
    children,
    typeField(v?: Type): any { return v !== undefined ? pointer_type_({ ...config, type: v }) : fields.type; },
    child(v?: MutableSpecifier): any { return v !== undefined ? pointer_type_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PointerTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function qualified_type_(
  config: QualifiedTypeConfig,
) {
  const fields = {
    alias: config.alias,
    type: config.type,
  };
  return {
    type: 'qualified_type' as const,
    fields,
    alias(v?: Type): any { return v !== undefined ? qualified_type_({ ...config, alias: v }) : fields.alias; },
    typeField(v?: Type): any { return v !== undefined ? qualified_type_({ ...config, type: v }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: QualifiedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function range_expression_(
  config?: RangeExpressionConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'range_expression' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Expression)[]): any { return range_expression_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function range_pattern_(
  config?: RangePatternConfig,
) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    fields,
    left(v?: Crate | Identifier | LiteralPattern | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? range_pattern_({ ...config, left: v }) : fields.left; },
    right(v?: Crate | Identifier | LiteralPattern | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? range_pattern_({ ...config, right: v }) : fields.right; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function raw_string_literal_(
  config: RawStringLiteralConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'raw_string_literal' as const,
    fields,
    children,
    child(v?: StringContent): any { return v !== undefined ? raw_string_literal_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RawStringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function ref_pattern_(
  config: RefPatternConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'ref_pattern' as const,
    fields,
    children,
    child(v?: Pattern): any { return v !== undefined ? ref_pattern_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RefPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function reference_expression_(
  config: ReferenceExpressionConfig,
) {
  const fields = {
    value: config.value,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'reference_expression' as const,
    fields,
    children,
    value(v?: Expression): any { return v !== undefined ? reference_expression_({ ...config, value: v }) : fields.value; },
    child(v?: MutableSpecifier): any { return v !== undefined ? reference_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function reference_pattern_(
  config: ReferencePatternConfig,
) {
  const fields = {
  };
  const children = [config.mutableSpecifier, config.pattern].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'reference_pattern' as const,
    fields,
    children,
    mutableSpecifier(v?: MutableSpecifier): any { return v !== undefined ? reference_pattern_({ ...config, mutableSpecifier: v }) : config?.mutableSpecifier; },
    pattern(v?: Pattern): any { return v !== undefined ? reference_pattern_({ ...config, pattern: v }) : config?.pattern; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferencePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function reference_type_(
  config: ReferenceTypeConfig,
) {
  const fields = {
    type: config.type,
  };
  const children = [config.lifetime, config.mutableSpecifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'reference_type' as const,
    fields,
    children,
    typeField(v?: Type): any { return v !== undefined ? reference_type_({ ...config, type: v }) : fields.type; },
    lifetime(v?: Lifetime): any { return v !== undefined ? reference_type_({ ...config, lifetime: v }) : config?.lifetime; },
    mutableSpecifier(v?: MutableSpecifier): any { return v !== undefined ? reference_type_({ ...config, mutableSpecifier: v }) : config?.mutableSpecifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function removed_trait_bound_(
  config: RemovedTraitBoundConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'removed_trait_bound' as const,
    fields,
    children,
    child(v?: Type): any { return v !== undefined ? removed_trait_bound_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RemovedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function return_expression_(
  config?: ReturnExpressionConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'return_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? return_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function scoped_identifier_(
  config: ScopedIdentifierConfig,
) {
  const fields = {
    name: config.name,
    path: config.path,
  };
  return {
    type: 'scoped_identifier' as const,
    fields,
    name(v?: Identifier | Super): any { return v !== undefined ? scoped_identifier_({ ...config, name: v }) : fields.name; },
    path(v?: BracketedType | Crate | GenericType | Identifier | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? scoped_identifier_({ ...config, path: v }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function scoped_type_identifier_(
  config: ScopedTypeIdentifierConfig,
) {
  const fields = {
    name: config.name,
    path: config.path,
  };
  return {
    type: 'scoped_type_identifier' as const,
    fields,
    name(v?: TypeIdentifier): any { return v !== undefined ? scoped_type_identifier_({ ...config, name: v }) : fields.name; },
    path(v?: BracketedType | Crate | GenericType | Identifier | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? scoped_type_identifier_({ ...config, path: v }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function scoped_use_list_(
  config: ScopedUseListConfig,
) {
  const fields = {
    list: config.list,
    path: config.path,
  };
  return {
    type: 'scoped_use_list' as const,
    fields,
    list(v?: UseList): any { return v !== undefined ? scoped_use_list_({ ...config, list: v }) : fields.list; },
    path(v?: Crate | Identifier | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? scoped_use_list_({ ...config, path: v }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedUseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function self_parameter_(
  config: SelfParameterConfig,
) {
  const fields = {
  };
  const children = [config.lifetime, config.mutableSpecifier, config.self].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'self_parameter' as const,
    fields,
    children,
    lifetime(v?: Lifetime): any { return v !== undefined ? self_parameter_({ ...config, lifetime: v }) : config?.lifetime; },
    mutableSpecifier(v?: MutableSpecifier): any { return v !== undefined ? self_parameter_({ ...config, mutableSpecifier: v }) : config?.mutableSpecifier; },
    self(v?: Self): any { return v !== undefined ? self_parameter_({ ...config, self: v }) : config?.self; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SelfParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function shorthand_field_initializer_(
  config: ShorthandFieldInitializerConfig,
) {
  const fields = {
  };
  const children = [...(config.attributeItem ?? []), config.identifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'shorthand_field_initializer' as const,
    fields,
    children,
    attributeItem(...v: (AttributeItem)[]): any { return v.length ? shorthand_field_initializer_({ ...config, attributeItem: v }) : config?.attributeItem; },
    identifier(v?: Identifier): any { return v !== undefined ? shorthand_field_initializer_({ ...config, identifier: v }) : config?.identifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function slice_pattern_(
  config?: SlicePatternConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'slice_pattern' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Pattern)[]): any { return slice_pattern_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SlicePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function source_file_(
  config?: SourceFileConfig,
) {
  const fields = {
  };
  const children = [config?.shebang, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'source_file' as const,
    fields,
    children,
    shebang(v?: Shebang): any { return v !== undefined ? source_file_({ ...config, shebang: v }) : config?.shebang; },
    children1(...v: (DeclarationStatement | ExpressionStatement)[]): any { return v.length ? source_file_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SourceFileTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function static_item_(
  config: StaticItemConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    value: config.value,
  };
  const children = [config.visibilityModifier, config.mutableSpecifier].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'static_item' as const,
    fields,
    children,
    name(v?: Identifier): any { return v !== undefined ? static_item_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? static_item_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? static_item_({ ...config, value: v }) : fields.value; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? static_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    mutableSpecifier(v?: MutableSpecifier): any { return v !== undefined ? static_item_({ ...config, mutableSpecifier: v }) : config?.mutableSpecifier; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StaticItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function string_literal_(
  config?: StringLiteralConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'string_literal' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (EscapeSequence | StringContent)[]): any { return string_literal_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function struct_expression_(
  config: StructExpressionConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
  };
  return {
    type: 'struct_expression' as const,
    fields,
    body(v?: FieldInitializerList): any { return v !== undefined ? struct_expression_({ ...config, body: v }) : fields.body; },
    name(v?: GenericTypeWithTurbofish | ScopedTypeIdentifier | TypeIdentifier): any { return v !== undefined ? struct_expression_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function struct_item_(
  config: StructItemConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.visibilityModifier, config.whereClause].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'struct_item' as const,
    fields,
    children,
    body(v?: FieldDeclarationList | OrderedFieldDeclarationList): any { return v !== undefined ? struct_item_({ ...config, body: v }) : fields.body; },
    name(v?: TypeIdentifier): any { return v !== undefined ? struct_item_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? struct_item_({ ...config, typeParameters: v }) : fields.type_parameters; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? struct_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    whereClause(v?: WhereClause): any { return v !== undefined ? struct_item_({ ...config, whereClause: v }) : config?.whereClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function struct_pattern_(
  config: StructPatternConfig,
) {
  const fields = {
    type: config.type,
  };
  const children = [config.children0, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'struct_pattern' as const,
    fields,
    children,
    typeField(v?: ScopedTypeIdentifier | TypeIdentifier): any { return v !== undefined ? struct_pattern_({ ...config, type: v }) : fields.type; },
    children0(v?: FieldPattern | RemainingFieldPattern): any { return v !== undefined ? struct_pattern_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (FieldPattern | RemainingFieldPattern)[]): any { return v.length ? struct_pattern_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function token_binding_pattern_(
  config: TokenBindingPatternConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
  };
  return {
    type: 'token_binding_pattern' as const,
    fields,
    name(v?: Metavariable): any { return v !== undefined ? token_binding_pattern_({ ...config, name: v }) : fields.name; },
    typeField(v?: FragmentSpecifier): any { return v !== undefined ? token_binding_pattern_({ ...config, type: v }) : fields.type; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenBindingPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function token_repetition_(
  config?: TokenRepetitionConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'token_repetition' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]): any { return token_repetition_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function token_repetition_pattern_(
  config?: TokenRepetitionPatternConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'token_repetition_pattern' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]): any { return token_repetition_pattern_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function token_tree_(
  config?: TokenTreeConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'token_tree' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenRepetition | TokenTree)[]): any { return token_tree_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function token_tree_pattern_(
  config?: TokenTreePatternConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'token_tree_pattern' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Crate | Identifier | Literal | Metavariable | MutableSpecifier | PrimitiveType | Self | Super | TokenBindingPattern | TokenRepetitionPattern | TokenTreePattern)[]): any { return token_tree_pattern_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function trait_bounds_(
  config: TraitBoundsConfig,
) {
  const fields = {
  };
  const children = [config.children0, ...(config.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'trait_bounds' as const,
    fields,
    children,
    children0(v?: HigherRankedTraitBound | Lifetime | Type): any { return v !== undefined ? trait_bounds_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (HigherRankedTraitBound | Lifetime | Type)[]): any { return v.length ? trait_bounds_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function trait_item_(
  config: TraitItemConfig,
) {
  const fields = {
    body: config.body,
    bounds: config.bounds,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.visibilityModifier, config.whereClause].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'trait_item' as const,
    fields,
    children,
    body(v?: DeclarationList): any { return v !== undefined ? trait_item_({ ...config, body: v }) : fields.body; },
    bounds(v?: TraitBounds): any { return v !== undefined ? trait_item_({ ...config, bounds: v }) : fields.bounds; },
    name(v?: TypeIdentifier): any { return v !== undefined ? trait_item_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? trait_item_({ ...config, typeParameters: v }) : fields.type_parameters; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? trait_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    whereClause(v?: WhereClause): any { return v !== undefined ? trait_item_({ ...config, whereClause: v }) : config?.whereClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_block_(
  config: TryBlockConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'try_block' as const,
    fields,
    children,
    child(v?: Block): any { return v !== undefined ? try_block_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function try_expression_(
  config: TryExpressionConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'try_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? try_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_expression_(
  config: TupleExpressionConfig,
) {
  const fields = {
  };
  const children = [...(config.attributeItem ?? []), config.expression1, ...(config.expression2 ?? []), config.expression3].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'tuple_expression' as const,
    fields,
    children,
    attributeItem(...v: (AttributeItem)[]): any { return v.length ? tuple_expression_({ ...config, attributeItem: v }) : config?.attributeItem; },
    expression1(v?: Expression): any { return v !== undefined ? tuple_expression_({ ...config, expression1: v }) : config?.expression1; },
    expression2(...v: (Expression)[]): any { return v.length ? tuple_expression_({ ...config, expression2: v }) : config?.expression2; },
    expression3(v?: Expression): any { return v !== undefined ? tuple_expression_({ ...config, expression3: v }) : config?.expression3; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_pattern_(
  config?: TuplePatternConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'tuple_pattern' as const,
    fields,
    children,
    children0(v?: ClosureExpression | Pattern): any { return v !== undefined ? tuple_pattern_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (ClosureExpression | Pattern)[]): any { return v.length ? tuple_pattern_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_struct_pattern_(
  config: TupleStructPatternConfig,
) {
  const fields = {
    type: config.type,
  };
  const children = [config.pattern1, ...(config.pattern2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'tuple_struct_pattern' as const,
    fields,
    children,
    typeField(v?: GenericType | Identifier | ScopedIdentifier): any { return v !== undefined ? tuple_struct_pattern_({ ...config, type: v }) : fields.type; },
    pattern1(v?: Pattern): any { return v !== undefined ? tuple_struct_pattern_({ ...config, pattern1: v }) : config?.pattern1; },
    pattern2(...v: (Pattern)[]): any { return v.length ? tuple_struct_pattern_({ ...config, pattern2: v }) : config?.pattern2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleStructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function tuple_type_(
  config: TupleTypeConfig,
) {
  const fields = {
  };
  const children = [...(config.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'tuple_type' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Type)[]): any { return tuple_type_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_arguments_(
  config: TypeArgumentsConfig,
) {
  const fields = {
  };
  const children = [config.children0, config.traitBounds, ...(config.children2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_arguments' as const,
    fields,
    children,
    children0(v?: Block | Lifetime | Literal | Type | TypeBinding): any { return v !== undefined ? type_arguments_({ ...config, children0: v }) : config?.children0; },
    traitBounds(v?: TraitBounds): any { return v !== undefined ? type_arguments_({ ...config, traitBounds: v }) : config?.traitBounds; },
    children2(...v: (Block | Lifetime | Literal | TraitBounds | Type | TypeBinding)[]): any { return v.length ? type_arguments_({ ...config, children2: v }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_binding_(
  config: TypeBindingConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    type_arguments: config.typeArguments,
  };
  return {
    type: 'type_binding' as const,
    fields,
    name(v?: TypeIdentifier): any { return v !== undefined ? type_binding_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? type_binding_({ ...config, type: v }) : fields.type; },
    typeArguments(v?: TypeArguments): any { return v !== undefined ? type_binding_({ ...config, typeArguments: v }) : fields.type_arguments; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeBindingTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_cast_expression_(
  config: TypeCastExpressionConfig,
) {
  const fields = {
    type: config.type,
    value: config.value,
  };
  return {
    type: 'type_cast_expression' as const,
    fields,
    typeField(v?: Type): any { return v !== undefined ? type_cast_expression_({ ...config, type: v }) : fields.type; },
    value(v?: Expression): any { return v !== undefined ? type_cast_expression_({ ...config, value: v }) : fields.value; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeCastExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_item_(
  config: TypeItemConfig,
) {
  const fields = {
    name: config.name,
    type: config.type,
    type_parameters: config.typeParameters,
  };
  const children = [config.visibilityModifier, config.whereClause1, config.whereClause2].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_item' as const,
    fields,
    children,
    name(v?: TypeIdentifier): any { return v !== undefined ? type_item_({ ...config, name: v }) : fields.name; },
    typeField(v?: Type): any { return v !== undefined ? type_item_({ ...config, type: v }) : fields.type; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? type_item_({ ...config, typeParameters: v }) : fields.type_parameters; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? type_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    whereClause1(v?: WhereClause): any { return v !== undefined ? type_item_({ ...config, whereClause1: v }) : config?.whereClause1; },
    whereClause2(v?: WhereClause): any { return v !== undefined ? type_item_({ ...config, whereClause2: v }) : config?.whereClause2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameter_(
  config: TypeParameterConfig,
) {
  const fields = {
    bounds: config.bounds,
    default_type: config.defaultType,
    name: config.name,
  };
  return {
    type: 'type_parameter' as const,
    fields,
    bounds(v?: TraitBounds): any { return v !== undefined ? type_parameter_({ ...config, bounds: v }) : fields.bounds; },
    defaultType(v?: Type): any { return v !== undefined ? type_parameter_({ ...config, defaultType: v }) : fields.default_type; },
    name(v?: TypeIdentifier): any { return v !== undefined ? type_parameter_({ ...config, name: v }) : fields.name; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function type_parameters_(
  config: TypeParametersConfig,
) {
  const fields = {
  };
  const children = [...(config.attributeItem ?? []), config.children1, ...(config.children2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'type_parameters' as const,
    fields,
    children,
    attributeItem(...v: (AttributeItem)[]): any { return v.length ? type_parameters_({ ...config, attributeItem: v }) : config?.attributeItem; },
    children1(v?: ConstParameter | LifetimeParameter | Metavariable | TypeParameter): any { return v !== undefined ? type_parameters_({ ...config, children1: v }) : config?.children1; },
    children2(...v: (AttributeItem | ConstParameter | LifetimeParameter | Metavariable | TypeParameter)[]): any { return v.length ? type_parameters_({ ...config, children2: v }) : config?.children2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unary_expression_(
  config: UnaryExpressionConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'unary_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? unary_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function union_item_(
  config: UnionItemConfig,
) {
  const fields = {
    body: config.body,
    name: config.name,
    type_parameters: config.typeParameters,
  };
  const children = [config.visibilityModifier, config.whereClause].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'union_item' as const,
    fields,
    children,
    body(v?: FieldDeclarationList): any { return v !== undefined ? union_item_({ ...config, body: v }) : fields.body; },
    name(v?: TypeIdentifier): any { return v !== undefined ? union_item_({ ...config, name: v }) : fields.name; },
    typeParameters(v?: TypeParameters): any { return v !== undefined ? union_item_({ ...config, typeParameters: v }) : fields.type_parameters; },
    visibilityModifier(v?: VisibilityModifier): any { return v !== undefined ? union_item_({ ...config, visibilityModifier: v }) : config?.visibilityModifier; },
    whereClause(v?: WhereClause): any { return v !== undefined ? union_item_({ ...config, whereClause: v }) : config?.whereClause; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function unsafe_block_(
  config: UnsafeBlockConfig,
) {
  const fields = {
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'unsafe_block' as const,
    fields,
    children,
    child(v?: Block): any { return v !== undefined ? unsafe_block_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnsafeBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_as_clause_(
  config: UseAsClauseConfig,
) {
  const fields = {
    alias: config.alias,
    path: config.path,
  };
  return {
    type: 'use_as_clause' as const,
    fields,
    alias(v?: Identifier): any { return v !== undefined ? use_as_clause_({ ...config, alias: v }) : fields.alias; },
    path(v?: Crate | Identifier | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? use_as_clause_({ ...config, path: v }) : fields.path; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseAsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_bounds_(
  config?: UseBoundsConfig,
) {
  const fields = {
  };
  const children = [config?.children0, ...(config?.children1 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'use_bounds' as const,
    fields,
    children,
    children0(v?: Lifetime | TypeIdentifier): any { return v !== undefined ? use_bounds_({ ...config, children0: v }) : config?.children0; },
    children1(...v: (Lifetime | TypeIdentifier)[]): any { return v.length ? use_bounds_({ ...config, children1: v }) : config?.children1; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_declaration_(
  config: UseDeclarationConfig,
) {
  const fields = {
    argument: config.argument,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'use_declaration' as const,
    fields,
    children,
    argument(v?: Crate | Identifier | Metavariable | ScopedIdentifier | ScopedUseList | Self | Super | UseAsClause | UseList | UseWildcard): any { return v !== undefined ? use_declaration_({ ...config, argument: v }) : fields.argument; },
    child(v?: VisibilityModifier): any { return v !== undefined ? use_declaration_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_list_(
  config?: UseListConfig,
) {
  const fields = {
  };
  const children = [...(config?.children ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'use_list' as const,
    fields,
    children,
    getChildren(): any { return children; },
    setChildren(...v: (Crate | Identifier | Metavariable | ScopedIdentifier | ScopedUseList | Self | Super | UseAsClause | UseList | UseWildcard)[]): any { return use_list_({ ...config, children: v }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function use_wildcard_(
  config?: UseWildcardConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'use_wildcard' as const,
    fields,
    children,
    child(v?: Crate | Identifier | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? use_wildcard_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseWildcardTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function variadic_parameter_(
  config?: VariadicParameterConfig,
) {
  const fields = {
    pattern: config?.pattern,
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'variadic_parameter' as const,
    fields,
    children,
    pattern(v?: Pattern): any { return v !== undefined ? variadic_parameter_({ ...config, pattern: v }) : fields.pattern; },
    child(v?: MutableSpecifier): any { return v !== undefined ? variadic_parameter_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariadicParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function visibility_modifier_(
  config?: VisibilityModifierConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'visibility_modifier' as const,
    fields,
    children,
    child(v?: Crate | Identifier | Metavariable | ScopedIdentifier | Self | Super): any { return v !== undefined ? visibility_modifier_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VisibilityModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function where_clause_(
  config?: WhereClauseConfig,
) {
  const fields = {
  };
  const children = [config?.wherePredicate1, ...(config?.wherePredicate2 ?? [])].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'where_clause' as const,
    fields,
    children,
    wherePredicate1(v?: WherePredicate): any { return v !== undefined ? where_clause_({ ...config, wherePredicate1: v }) : config?.wherePredicate1; },
    wherePredicate2(...v: (WherePredicate)[]): any { return v.length ? where_clause_({ ...config, wherePredicate2: v }) : config?.wherePredicate2; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhereClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function where_predicate_(
  config: WherePredicateConfig,
) {
  const fields = {
    bounds: config.bounds,
    left: config.left,
  };
  return {
    type: 'where_predicate' as const,
    fields,
    bounds(v?: TraitBounds): any { return v !== undefined ? where_predicate_({ ...config, bounds: v }) : fields.bounds; },
    left(v?: ArrayType | GenericType | HigherRankedTraitBound | Lifetime | PointerType | PrimitiveType | ReferenceType | ScopedTypeIdentifier | TupleType | TypeIdentifier): any { return v !== undefined ? where_predicate_({ ...config, left: v }) : fields.left; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function while_expression_(
  config: WhileExpressionConfig,
) {
  const fields = {
    body: config.body,
    condition: config.condition,
  };
  const children = [config.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'while_expression' as const,
    fields,
    children,
    body(v?: Block): any { return v !== undefined ? while_expression_({ ...config, body: v }) : fields.body; },
    condition(v?: Expression | LetChain | LetCondition): any { return v !== undefined ? while_expression_({ ...config, condition: v }) : fields.condition; },
    child(v?: Label): any { return v !== undefined ? while_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function yield_expression_(
  config?: YieldExpressionConfig,
) {
  const fields = {
  };
  const children = [config?.children].filter(Boolean) as unknown as AnyNodeData[];
  return {
    type: 'yield_expression' as const,
    fields,
    children,
    child(v?: Expression): any { return v !== undefined ? yield_expression_({ ...config, children: v }) : config?.children; },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}


export function boolean_literal_(text: 'false' | 'true') {
  return {
    type: 'boolean_literal' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: BooleanLiteralTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function empty_statement_() {
  return {
    type: 'empty_statement' as const,
    text: ';',
    render: () => ';',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: ';' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: ';' };
    },
    replace: (target: EmptyStatementTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: ';' }; },
  };
}

export function fragment_specifier_(text: 'block' | 'expr' | 'expr_2021' | 'ident' | 'item' | 'lifetime' | 'literal' | 'meta' | 'pat' | 'pat_param' | 'path' | 'stmt' | 'tt' | 'ty' | 'vis') {
  return {
    type: 'fragment_specifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FragmentSpecifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function inner_doc_comment_marker_(text: string) {
  return {
    type: 'inner_doc_comment_marker' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: InnerDocCommentMarkerTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function never_type_() {
  return {
    type: 'never_type' as const,
    text: '!',
    render: () => '!',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '!' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '!' };
    },
    replace: (target: NeverTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '!' }; },
  };
}

export function outer_doc_comment_marker_(text: string) {
  return {
    type: 'outer_doc_comment_marker' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: OuterDocCommentMarkerTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function remaining_field_pattern_() {
  return {
    type: 'remaining_field_pattern' as const,
    text: '..',
    render: () => '..',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '..' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '..' };
    },
    replace: (target: RemainingFieldPatternTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '..' }; },
  };
}

export function unit_expression_() {
  return {
    type: 'unit_expression' as const,
    text: '()',
    render: () => '()',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '()' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '()' };
    },
    replace: (target: UnitExpressionTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '()' }; },
  };
}

export function unit_type_() {
  return {
    type: 'unit_type' as const,
    text: '()',
    render: () => '()',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: '()' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '()' };
    },
    replace: (target: UnitTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '()' }; },
  };
}

export function char_literal_(text: string) {
  if (!/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(text)) throw new Error(`Invalid char_literal: '${text}' does not match grammar pattern`);
  return {
    type: 'char_literal' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: CharLiteralTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function crate_() {
  return {
    type: 'crate' as const,
    text: 'crate',
    render: () => 'crate',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'crate' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'crate' };
    },
    replace: (target: CrateTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'crate' }; },
  };
}

export function doc_comment_(text: string) {
  return {
    type: 'doc_comment' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: DocCommentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escape_sequence_(text: `\\${string}`) {
  if (!/^\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})$/.test(text)) throw new Error(`Invalid escape_sequence: '${text}' does not match grammar pattern`);
  return {
    type: 'escape_sequence' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: EscapeSequenceTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function field_identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid field_identifier`);
  return {
    type: 'field_identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FieldIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function float_literal_(text: string) {
  return {
    type: 'float_literal' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: FloatLiteralTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  if (!/^(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*$/u.test(text)) throw new Error(`Invalid identifier: '${text}' does not match grammar pattern`);
  return {
    type: 'identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function integer_literal_(text: string) {
  if (!/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(text)) throw new Error(`Invalid integer_literal: '${text}' does not match grammar pattern`);
  return {
    type: 'integer_literal' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: IntegerLiteralTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function metavariable_(text: string) {
  if (!/^\$[a-zA-Z_]\w*$/.test(text)) throw new Error(`Invalid metavariable: '${text}' does not match grammar pattern`);
  return {
    type: 'metavariable' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: MetavariableTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function mutable_specifier_() {
  return {
    type: 'mutable_specifier' as const,
    text: 'mut',
    render: () => 'mut',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'mut' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'mut' };
    },
    replace: (target: MutableSpecifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'mut' }; },
  };
}

export function primitive_type_(text: 'bool' | 'char' | 'f32' | 'f64' | 'i128' | 'i16' | 'i32' | 'i64' | 'i8' | 'isize' | 'str' | 'u128' | 'u16' | 'u32' | 'u64' | 'u8' | 'usize') {
  return {
    type: 'primitive_type' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: PrimitiveTypeTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function self_() {
  return {
    type: 'self' as const,
    text: 'self',
    render: () => 'self',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'self' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'self' };
    },
    replace: (target: SelfTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'self' }; },
  };
}

export function shebang_(text: string) {
  if (!/^#![\r\f\t\v ]*([^\[\n].*)?\n$/.test(text)) throw new Error(`Invalid shebang: '${text}' does not match grammar pattern`);
  return {
    type: 'shebang' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShebangTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthand_field_identifier_(text: string) {
  return {
    type: 'shorthand_field_identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: ShorthandFieldIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function string_content_(text: string) {
  return {
    type: 'string_content' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: StringContentTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    text: 'super',
    render: () => 'super',
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: 'super' };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'super' };
    },
    replace: (target: SuperTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; },
  };
}

export function type_identifier_(text: string) {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid type_identifier`);
  return {
    type: 'type_identifier' as const,
    text,
    render: () => text,
    toEdit: (startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) => {
      if (typeof startOrRange === 'number') {
        if (endPos === undefined) throw new Error('endPos required when startPos is a number');
        return { startPos: startOrRange, endPos, insertedText: text };
      }
      return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
    },
    replace: (target: TypeIdentifierTree) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}


// Auto-generated by @sittir/codegen — do not edit

import type * as T from './types.js';
import type { ByteRange, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const _leafRe_identifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_shebang = /^(?:#![\r\f\t\v ]*([^[\n].*)?\n)/u;
const _leafRe_metavariable = /^(?:\$[a-zA-Z_]\w*)/u;
const _leafRe_typeIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_fieldIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_shorthandFieldIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const RESERVED_KEYWORDS: ReadonlySet<string> = new Set([
  '_',
  'as',
  'async',
  'await',
  'break',
  'const',
  'continue',
  'crate',
  'default',
  'dyn',
  'else',
  'enum',
  'extern',
  'fn',
  'for',
  'gen',
  'if',
  'impl',
  'in',
  'let',
  'loop',
  'match',
  'mod',
  'move',
  'mut',
  'pub',
  'raw',
  'ref',
  'return',
  'self',
  'static',
  'struct',
  'super',
  'trait',
  'try',
  'type',
  'union',
  'unsafe',
  'use',
  'where',
  'while',
  'yield',
]);
const _wordRe = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;

export function sourceFile(config: T.SourceFile.Config) {
  const fields = {
    shebang: config?.shebang,
    statements: config?.statements,
  };
  return {
    type: 'source_file' as const,
    named: true as const,
    fields,
    shebang(value?: T.Shebang | undefined) { return _fs(config, sourceFile, 'shebang', value, fields.shebang); },
    statements(...values: T.Statement[]) { return _fsm(config, sourceFile, 'statements', values, fields.statements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SourceFileTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child?: (T.Expression | T.ExpressionEndingWithBlock)) {
  const children = child != null ? [child] : [];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroDefinition(config: T.MacroDefinition.Config) {
  const fields = {
    name: config?.name,
    rules: config?.rules,
  };
  return {
    type: 'macro_definition' as const,
    named: true as const,
    fields,
    name(value?: T.Identifier | T.ReservedIdentifier) { return _fs(config, macroDefinition, 'name', value, fields.name); },
    rules(value?: T.MacroRule) { return _fs(config, macroDefinition, 'rules', value, fields.rules); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroRule(config: T.MacroRule.Config) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'macro_rule' as const,
    named: true as const,
    fields,
    left(value?: T.TokenTreePattern) { return _fs(config, macroRule, 'left', value, fields.left); },
    right(value?: T.TokenTree) { return _fs(config, macroRule, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MacroRuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenTreePattern(...children: T.TokenPattern[]) {
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenBindingPattern(config: T.TokenBindingPattern.Config) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'token_binding_pattern' as const,
    named: true as const,
    fields,
    name(value?: T.Metavariable) { return _fs(config, tokenBindingPattern, 'name', value, fields.name); },
    typeField(value?: T.FragmentSpecifier) { return _fs(config, tokenBindingPattern, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenBindingPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetitionPattern(...children: T.TokenPattern[]) {
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fragmentSpecifier(text: 'block' | 'expr' | 'expr_2021' | 'ident' | 'item' | 'lifetime' | 'literal' | 'meta' | 'pat' | 'pat_param' | 'path' | 'stmt' | 'tt' | 'ty' | 'vis') {
  return {
    type: 'fragment_specifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FragmentSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function tokenTree(...children: T.Tokens[]) {
  return {
    type: 'token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetition(...children: T.Tokens[]) {
  return {
    type: 'token_repetition' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attributeItem(config: T.AttributeItem.Config) {
  const fields = {
    attribute: config?.attribute,
  };
  return {
    type: 'attribute_item' as const,
    named: true as const,
    fields,
    attribute(value?: T.Attribute) { return _fs(config, attributeItem, 'attribute', value, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function innerAttributeItem(config: T.InnerAttributeItem.Config) {
  const fields = {
    attribute: config?.attribute,
  };
  return {
    type: 'inner_attribute_item' as const,
    named: true as const,
    fields,
    attribute(value?: T.Attribute) { return _fs(config, innerAttributeItem, 'attribute', value, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.InnerAttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: T.Attribute.Config) {
  const fields = {
    value: config?.value,
    arguments: config?.arguments,
  };
  const children = config?.children ?? [];
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    children,
    value(value?: T.Expression | undefined) { return _fs(config, attribute, 'value', value, fields.value); },
    arguments(value?: T.TokenTree | undefined) { return _fs(config, attribute, 'arguments', value, fields.arguments); },
    getChild() { return children[0]; },
    setChild(child: T.Path) { return attribute({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function modItem(config: T.ModItemUFormExternalConfig | T.ModItemUFormInlineConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'mod_item_external') return modItemUFormExternal(config as T.ModItemUFormExternalConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'mod_item_inline') return modItemUFormInline(config as T.ModItemUFormInlineConfig);
  if (config && (config as any).variant === 'external') return modItemUFormExternal(config as T.ModItemUFormExternalConfig);
  if (config && (config as any).variant === 'inline') return modItemUFormInline(config as T.ModItemUFormInlineConfig);
  return modItemUFormExternal(config as T.ModItemUFormExternalConfig);
}
export function modItemUFormExternal(config: T.ModItemUFormExternalConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'mod_item' as const,
    named: true as const,
    variant: '_form_external' as const,
    fields,
    children,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, modItemUFormExternal, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T.Identifier) { return _fs(config, modItemUFormExternal, 'name', value, fields.name); },
    getChild() { return children[0]; },
    setChild(child: T.ModItemExternal) { return modItemUFormExternal({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModItemUFormExternalTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function modItemUFormInline(config: T.ModItemUFormInlineConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
  };
  const children = config?.children ?? [];
  return {
    type: 'mod_item' as const,
    named: true as const,
    variant: '_form_inline' as const,
    fields,
    children,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, modItemUFormInline, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T.Identifier) { return _fs(config, modItemUFormInline, 'name', value, fields.name); },
    getChild() { return children[0]; },
    setChild(child: T.ModItemInline) { return modItemUFormInline({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModItemUFormInlineTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function foreignModItem(config: T.ForeignModItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    extern_modifier: config?.externModifier,
    body: config?.body,
  };
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, foreignModItem, 'visibilityModifier', value, fields.visibility_modifier); },
    externModifier(value?: T.ExternModifier) { return _fs(config, foreignModItem, 'externModifier', value, fields.extern_modifier); },
    body(value?: T.DeclarationList | undefined) { return _fs(config, foreignModItem, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForeignModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declarationList(...children: T.DeclarationStatement[]) {
  return {
    type: 'declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structItem(config: T.StructItemUFormBraceConfig | T.StructItemUFormTupleConfig | T.StructItemUFormUnitConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'struct_item_brace') return structItemUFormBrace(config as T.StructItemUFormBraceConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'struct_item_tuple') return structItemUFormTuple(config as T.StructItemUFormTupleConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'struct_item_unit') return structItemUFormUnit(config as T.StructItemUFormUnitConfig);
  if (config && (config as any).variant === 'brace') return structItemUFormBrace(config as T.StructItemUFormBraceConfig);
  if (config && (config as any).variant === 'tuple') return structItemUFormTuple(config as T.StructItemUFormTupleConfig);
  if (config && (config as any).variant === 'unit') return structItemUFormUnit(config as T.StructItemUFormUnitConfig);
  return structItemUFormBrace(config as T.StructItemUFormBraceConfig);
}
export function structItemUFormBrace(config: T.StructItemUFormBraceConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: '_form_brace' as const,
    fields,
    children,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, structItemUFormBrace, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T._TypeIdentifier) { return _fs(config, structItemUFormBrace, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, structItemUFormBrace, 'typeParameters', value, fields.type_parameters); },
    getChild() { return children[0]; },
    setChild(child: T.StructItemBrace) { return structItemUFormBrace({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructItemUFormBraceTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function structItemUFormTuple(config: T.StructItemUFormTupleConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: '_form_tuple' as const,
    fields,
    children,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, structItemUFormTuple, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T._TypeIdentifier) { return _fs(config, structItemUFormTuple, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, structItemUFormTuple, 'typeParameters', value, fields.type_parameters); },
    getChild() { return children[0]; },
    setChild(child: T.StructItemTuple) { return structItemUFormTuple({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructItemUFormTupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function structItemUFormUnit(config: T.StructItemUFormUnitConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: '_form_unit' as const,
    fields,
    children,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, structItemUFormUnit, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T._TypeIdentifier) { return _fs(config, structItemUFormUnit, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, structItemUFormUnit, 'typeParameters', value, fields.type_parameters); },
    getChild() { return children[0]; },
    setChild(child: T.StructItemUnit) { return structItemUFormUnit({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructItemUFormUnitTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionItem(config: T.UnionItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'union_item' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, unionItem, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T._TypeIdentifier) { return _fs(config, unionItem, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, unionItem, 'typeParameters', value, fields.type_parameters); },
    whereClause(value?: T.WhereClause | undefined) { return _fs(config, unionItem, 'whereClause', value, fields.where_clause); },
    body(value?: T.FieldDeclarationList) { return _fs(config, unionItem, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumItem(config: T.EnumItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'enum_item' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, enumItem, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T._TypeIdentifier) { return _fs(config, enumItem, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, enumItem, 'typeParameters', value, fields.type_parameters); },
    whereClause(value?: T.WhereClause | undefined) { return _fs(config, enumItem, 'whereClause', value, fields.where_clause); },
    body(value?: T.EnumVariantList) { return _fs(config, enumItem, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariantList(...children: (T.AttributeItem | T.EnumVariant)[]) {
  return {
    type: 'enum_variant_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumVariantListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariant(config: T.EnumVariant.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    body: config?.body,
    value: config?.value,
  };
  return {
    type: 'enum_variant' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, enumVariant, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T.Identifier) { return _fs(config, enumVariant, 'name', value, fields.name); },
    body(value?: T.FieldDeclarationList | T.OrderedFieldDeclarationList | undefined) { return _fs(config, enumVariant, 'body', value, fields.body); },
    value(value?: T.Expression | undefined) { return _fs(config, enumVariant, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.EnumVariantTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclarationList(...children: (T.AttributeItem | T.FieldDeclaration)[]) {
  return {
    type: 'field_declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclaration(config: T.FieldDeclaration.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'field_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, fieldDeclaration, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T._FieldIdentifier) { return _fs(config, fieldDeclaration, 'name', value, fields.name); },
    typeField(value?: T._Type) { return _fs(config, fieldDeclaration, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orderedFieldDeclarationList(config: T.OrderedFieldDeclarationList.Config) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'ordered_field_declaration_list' as const,
    named: true as const,
    fields,
    typeField(...values: T._Type[]) { return _fsm(config, orderedFieldDeclarationList, 'type', values, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OrderedFieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externCrateDeclaration(config: T.ExternCrateDeclaration.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    crate: config?.crate,
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'extern_crate_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, externCrateDeclaration, 'visibilityModifier', value, fields.visibility_modifier); },
    crate(value?: T.Crate) { return _fs(config, externCrateDeclaration, 'crate', value, fields.crate); },
    name(value?: T.Identifier) { return _fs(config, externCrateDeclaration, 'name', value, fields.name); },
    alias(value?: T.Identifier | undefined) { return _fs(config, externCrateDeclaration, 'alias', value, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExternCrateDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constItem(config: T.ConstItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'const_item' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, constItem, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T.Identifier) { return _fs(config, constItem, 'name', value, fields.name); },
    typeField(value?: T._Type) { return _fs(config, constItem, 'type', value, fields.type); },
    value(value?: T.Expression | undefined) { return _fs(config, constItem, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function staticItem(config: T.StaticItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    mutable_specifier: config?.mutableSpecifier,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'static_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, staticItem, 'visibilityModifier', value, fields.visibility_modifier); },
    mutableSpecifier(value?: "ref" | undefined) { return _fs(config, staticItem, 'mutableSpecifier', value, fields.mutable_specifier); },
    name(value?: T.Identifier) { return _fs(config, staticItem, 'name', value, fields.name); },
    typeField(value?: T._Type) { return _fs(config, staticItem, 'type', value, fields.type); },
    value(value?: T.Expression | undefined) { return _fs(config, staticItem, 'value', value, fields.value); },
    getChild() { return children[0]; },
    setChild(child: T.MutableSpecifier) { return staticItem({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StaticItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeItem(config: T.TypeItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    type: config?.type,
    trailing_where_clause: config?.trailingWhereClause,
  };
  return {
    type: 'type_item' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, typeItem, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T._TypeIdentifier) { return _fs(config, typeItem, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, typeItem, 'typeParameters', value, fields.type_parameters); },
    whereClause(value?: T.WhereClause | undefined) { return _fs(config, typeItem, 'whereClause', value, fields.where_clause); },
    typeField(value?: T._Type) { return _fs(config, typeItem, 'type', value, fields.type); },
    trailingWhereClause(value?: T.WhereClause | undefined) { return _fs(config, typeItem, 'trailingWhereClause', value, fields.trailing_where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionItem(config: T.FunctionItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    function_modifiers: config?.functionModifiers,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'function_item' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, functionItem, 'visibilityModifier', value, fields.visibility_modifier); },
    functionModifiers(value?: T.FunctionModifiers | undefined) { return _fs(config, functionItem, 'functionModifiers', value, fields.function_modifiers); },
    name(value?: T.Identifier | T.Metavariable) { return _fs(config, functionItem, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionItem, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.Parameters) { return _fs(config, functionItem, 'parameters', value, fields.parameters); },
    returnType(value?: T._Type | undefined) { return _fs(config, functionItem, 'returnType', value, fields.return_type); },
    whereClause(value?: T.WhereClause | undefined) { return _fs(config, functionItem, 'whereClause', value, fields.where_clause); },
    body(value?: T.Block) { return _fs(config, functionItem, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignatureItem(config: T.FunctionSignatureItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    function_modifiers: config?.functionModifiers,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    where_clause: config?.whereClause,
  };
  return {
    type: 'function_signature_item' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, functionSignatureItem, 'visibilityModifier', value, fields.visibility_modifier); },
    functionModifiers(value?: T.FunctionModifiers | undefined) { return _fs(config, functionSignatureItem, 'functionModifiers', value, fields.function_modifiers); },
    name(value?: T.Identifier | T.Metavariable) { return _fs(config, functionSignatureItem, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, functionSignatureItem, 'typeParameters', value, fields.type_parameters); },
    parameters(value?: T.Parameters) { return _fs(config, functionSignatureItem, 'parameters', value, fields.parameters); },
    returnType(value?: T._Type | undefined) { return _fs(config, functionSignatureItem, 'returnType', value, fields.return_type); },
    whereClause(value?: T.WhereClause | undefined) { return _fs(config, functionSignatureItem, 'whereClause', value, fields.where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionSignatureItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionModifiers(config?: T.FunctionModifiers.Config) {
  const fields = {
    async: config?.async,
    default: config?.default,
    const: config?.const,
    unsafe: config?.unsafe,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    fields,
    children,
    async(...values: T.KwAsync[]) { return _fsm(config, functionModifiers, 'async', values, fields.async); },
    default(...values: T.KwDefault[]) { return _fsm(config, functionModifiers, 'default', values, fields.default); },
    const(...values: T.KwConst[]) { return _fsm(config, functionModifiers, 'const', values, fields.const); },
    unsafe(...values: T.KwUnsafe[]) { return _fsm(config, functionModifiers, 'unsafe', values, fields.unsafe); },
    getChildren() { return children; },
    setChildren(...items: T.ExternModifier[]) { return functionModifiers({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whereClause(config: T.WhereClause.Config) {
  const fields = {
    where: config?.where,
  };
  const children = config?.children ?? [];
  return {
    type: 'where_clause' as const,
    named: true as const,
    fields,
    children,
    where(value?: "where") { return _fs(config, whereClause, 'where', value, fields.where); },
    getChildren() { return children; },
    setChildren(...items: T.WherePredicate[]) { return whereClause({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WhereClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function wherePredicate(config: T.WherePredicate.Config) {
  const fields = {
    left: config?.left,
    bounds: config?.bounds,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    fields,
    left(value?: T.Lifetime | T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericType | T.ReferenceType | T.PointerType | T.TupleType | T.ArrayType | T.HigherRankedTraitBound | T.PrimitiveType) { return _fs(config, wherePredicate, 'left', value, fields.left); },
    bounds(value?: T.TraitBounds) { return _fs(config, wherePredicate, 'bounds', value, fields.bounds); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implItem(config: T.ImplItem.Config) {
  const fields = {
    unsafe: config?.unsafe,
    type_parameters: config?.typeParameters,
    trait: config?.trait,
    type: config?.type,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'impl_item' as const,
    named: true as const,
    fields,
    children,
    unsafe(value?: T.KwUnsafe | undefined) { return _fs(config, implItem, 'unsafe', value, fields.unsafe); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, implItem, 'typeParameters', value, fields.type_parameters); },
    trait(value?: T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericType | undefined) { return _fs(config, implItem, 'trait', value, fields.trait); },
    typeField(value?: T._Type) { return _fs(config, implItem, 'type', value, fields.type); },
    body(value?: T.DeclarationList | undefined) { return _fs(config, implItem, 'body', value, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return implItem({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ImplItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitItem(config: T.TraitItem.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    bounds: config?.bounds,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'trait_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, traitItem, 'visibilityModifier', value, fields.visibility_modifier); },
    name(value?: T._TypeIdentifier) { return _fs(config, traitItem, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, traitItem, 'typeParameters', value, fields.type_parameters); },
    bounds(value?: T.TraitBounds | undefined) { return _fs(config, traitItem, 'bounds', value, fields.bounds); },
    body(value?: T.DeclarationList) { return _fs(config, traitItem, 'body', value, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return traitItem({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TraitItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function associatedType(config: T.AssociatedType.Config) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    bounds: config?.bounds,
    where_clause: config?.whereClause,
  };
  return {
    type: 'associated_type' as const,
    named: true as const,
    fields,
    name(value?: T._TypeIdentifier) { return _fs(config, associatedType, 'name', value, fields.name); },
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, associatedType, 'typeParameters', value, fields.type_parameters); },
    bounds(value?: T.TraitBounds | undefined) { return _fs(config, associatedType, 'bounds', value, fields.bounds); },
    whereClause(value?: T.WhereClause | undefined) { return _fs(config, associatedType, 'whereClause', value, fields.where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssociatedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitBounds(...children: (T._Type | T.Lifetime | T.HigherRankedTraitBound)[]) {
  return {
    type: 'trait_bounds' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TraitBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function higherRankedTraitBound(config: T.HigherRankedTraitBound.Config) {
  const fields = {
    for: config?.for,
    type_parameters: config?.typeParameters,
    type: config?.type,
  };
  return {
    type: 'higher_ranked_trait_bound' as const,
    named: true as const,
    fields,
    for(value?: T.KwFor) { return _fs(config, higherRankedTraitBound, 'for', value, fields.for); },
    typeParameters(value?: T.TypeParameters) { return _fs(config, higherRankedTraitBound, 'typeParameters', value, fields.type_parameters); },
    typeField(value?: T._Type) { return _fs(config, higherRankedTraitBound, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.HigherRankedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function removedTraitBound(child: T._Type) {
  const children = [child];
  return {
    type: 'removed_trait_bound' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RemovedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: (T.AttributeItem | T.Metavariable | T.TypeParameter | T.LifetimeParameter | T.ConstParameter)[]) {
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constParameter(config: T.ConstParameter.Config) {
  const fields = {
    const: config?.const,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'const_parameter' as const,
    named: true as const,
    fields,
    const(value?: T.KwConst) { return _fs(config, constParameter, 'const', value, fields.const); },
    name(value?: T.Identifier) { return _fs(config, constParameter, 'name', value, fields.name); },
    typeField(value?: T._Type) { return _fs(config, constParameter, 'type', value, fields.type); },
    value(value?: T.Block | T.Identifier | T.Literal | T.NegativeLiteral | undefined) { return _fs(config, constParameter, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: T.TypeParameter.Config) {
  const fields = {
    name: config?.name,
    bounds: config?.bounds,
    default_type: config?.defaultType,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    name(value?: T._TypeIdentifier) { return _fs(config, typeParameter, 'name', value, fields.name); },
    bounds(value?: T.TraitBounds | undefined) { return _fs(config, typeParameter, 'bounds', value, fields.bounds); },
    defaultType(value?: T._Type | undefined) { return _fs(config, typeParameter, 'defaultType', value, fields.default_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetimeParameter(config: T.LifetimeParameter.Config) {
  const fields = {
    name: config?.name,
    bounds: config?.bounds,
  };
  return {
    type: 'lifetime_parameter' as const,
    named: true as const,
    fields,
    name(value?: T.Lifetime) { return _fs(config, lifetimeParameter, 'name', value, fields.name); },
    bounds(value?: T.TraitBounds | undefined) { return _fs(config, lifetimeParameter, 'bounds', value, fields.bounds); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LifetimeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letDeclaration(config: T.LetDeclaration.Config) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
    type: config?.type,
    value: config?.value,
    alternative: config?.alternative,
  };
  return {
    type: 'let_declaration' as const,
    named: true as const,
    fields,
    mutableSpecifier(value?: T.MutableSpecifier | undefined) { return _fs(config, letDeclaration, 'mutableSpecifier', value, fields.mutable_specifier); },
    pattern(value?: T.Pattern) { return _fs(config, letDeclaration, 'pattern', value, fields.pattern); },
    typeField(value?: T._Type | undefined) { return _fs(config, letDeclaration, 'type', value, fields.type); },
    value(value?: T.Expression | undefined) { return _fs(config, letDeclaration, 'value', value, fields.value); },
    alternative(value?: T.Block | undefined) { return _fs(config, letDeclaration, 'alternative', value, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LetDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useDeclaration(config: T.UseDeclaration.Config) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    argument: config?.argument,
  };
  return {
    type: 'use_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(value?: T.VisibilityModifier | undefined) { return _fs(config, useDeclaration, 'visibilityModifier', value, fields.visibility_modifier); },
    argument(value?: T.UseClause) { return _fs(config, useDeclaration, 'argument', value, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedUseList(config: T.ScopedUseList.Config) {
  const fields = {
    path: config?.path,
    list: config?.list,
  };
  return {
    type: 'scoped_use_list' as const,
    named: true as const,
    fields,
    path(value?: T.Path | undefined) { return _fs(config, scopedUseList, 'path', value, fields.path); },
    list(value?: T.UseList) { return _fs(config, scopedUseList, 'list', value, fields.list); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ScopedUseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useList(...children: T.UseClause[]) {
  return {
    type: 'use_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useAsClause(config: T.UseAsClause.Config) {
  const fields = {
    path: config?.path,
    alias: config?.alias,
  };
  return {
    type: 'use_as_clause' as const,
    named: true as const,
    fields,
    path(value?: T.Path) { return _fs(config, useAsClause, 'path', value, fields.path); },
    alias(value?: T.Identifier) { return _fs(config, useAsClause, 'alias', value, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseAsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useWildcard(config?: T.UseWildcard.Config) {
  const fields = {
    path: config?.path,
  };
  return {
    type: 'use_wildcard' as const,
    named: true as const,
    fields,
    path(value?: T.Path | undefined) { return _fs(config, useWildcard, 'path', value, fields.path); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseWildcardTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(...children: (T.AttributeItem | T.Parameter | T.SelfParameter | T.VariadicParameter | T._Type)[]) {
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function selfParameter(config: T.SelfParameter.Config) {
  const fields = {
    lifetime: config?.lifetime,
    mutable_specifier: config?.mutableSpecifier,
    self: config?.self,
  };
  const children = config?.children ?? [];
  return {
    type: 'self_parameter' as const,
    named: true as const,
    fields,
    children,
    lifetime(value?: string) { return _fs(config, selfParameter, 'lifetime', value, fields.lifetime); },
    mutableSpecifier(value?: T.Lifetime | undefined) { return _fs(config, selfParameter, 'mutableSpecifier', value, fields.mutable_specifier); },
    self(value?: T.MutableSpecifier | undefined) { return _fs(config, selfParameter, 'self', value, fields.self); },
    getChild() { return children[0]; },
    setChild(child: T.Self) { return selfParameter({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SelfParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variadicParameter(config?: T.VariadicParameter.Config) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'variadic_parameter' as const,
    named: true as const,
    fields,
    mutableSpecifier(value?: T.MutableSpecifier | undefined) { return _fs(config, variadicParameter, 'mutableSpecifier', value, fields.mutable_specifier); },
    pattern(value?: T.Pattern | undefined) { return _fs(config, variadicParameter, 'pattern', value, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VariadicParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameter(config: T.Parameter.Config) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
    type: config?.type,
  };
  return {
    type: 'parameter' as const,
    named: true as const,
    fields,
    mutableSpecifier(value?: T.MutableSpecifier | undefined) { return _fs(config, parameter, 'mutableSpecifier', value, fields.mutable_specifier); },
    pattern(value?: T.Pattern | T.Self) { return _fs(config, parameter, 'pattern', value, fields.pattern); },
    typeField(value?: T._Type) { return _fs(config, parameter, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externModifier(config?: T.ExternModifier.Config) {
  const fields = {
    string_literal: config?.stringLiteral,
  };
  return {
    type: 'extern_modifier' as const,
    named: true as const,
    fields,
    stringLiteral(value?: T.StringLiteral | undefined) { return _fs(config, externModifier, 'stringLiteral', value, fields.string_literal); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ExternModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function visibilityModifier(config: T.VisibilityModifierForm0Config | T.VisibilityModifierForm1Config) {
  if (config && 'pub' in config && 'in' in config) return visibilityModifierForm1(config as T.VisibilityModifierForm1Config);
  return visibilityModifierForm0(config as T.VisibilityModifierForm0Config);
}
export function visibilityModifierForm0(config: T.VisibilityModifierForm0Config) {
  const children = config?.children ?? [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    variant: 'form0' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.Crate) { return visibilityModifierForm0({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VisibilityModifierForm0Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function visibilityModifierForm1(config: T.VisibilityModifierForm1Config) {
  const fields = {
    pub: config?.pub,
    in: config?.in,
  };
  const children = config?.children ?? [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    variant: 'form1' as const,
    fields,
    children,
    pub(value?: "pub") { return _fs(config, visibilityModifierForm1, 'pub', value, fields.pub); },
    in(value?: "in" | undefined) { return _fs(config, visibilityModifierForm1, 'in', value, fields.in); },
    getChild() { return children[0]; },
    setChild(child: (T.Self | T.Super | T.Crate | T.Path)) { return visibilityModifierForm1({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.VisibilityModifierForm1Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function bracketedType(child?: (T._Type | T.QualifiedType)) {
  const children = child != null ? [child] : [];
  return {
    type: 'bracketed_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BracketedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function qualifiedType(config: T.QualifiedType.Config) {
  const fields = {
    type: config?.type,
    alias: config?.alias,
  };
  return {
    type: 'qualified_type' as const,
    named: true as const,
    fields,
    typeField(value?: T._Type) { return _fs(config, qualifiedType, 'type', value, fields.type); },
    alias(value?: T._Type) { return _fs(config, qualifiedType, 'alias', value, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.QualifiedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetime(config: T.Lifetime.Config) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'lifetime' as const,
    named: true as const,
    fields,
    identifier(value?: T.Identifier) { return _fs(config, lifetime, 'identifier', value, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LifetimeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: T.ArrayType.Config) {
  const fields = {
    element: config?.element,
    length: config?.length,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    element(value?: T._Type) { return _fs(config, arrayType, 'element', value, fields.element); },
    length(value?: T.Expression | undefined) { return _fs(config, arrayType, 'length', value, fields.length); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forLifetimes(config: T.ForLifetimes.Config) {
  const fields = {
    for: config?.for,
  };
  const children = config?.children ?? [];
  return {
    type: 'for_lifetimes' as const,
    named: true as const,
    fields,
    children,
    for(value?: T.KwFor) { return _fs(config, forLifetimes, 'for', value, fields.for); },
    getChildren() { return children; },
    setChildren(...items: T.Lifetime[]) {
      _assertNonEmpty(items, 'for_lifetimes.children');
      return forLifetimes({ ...config, children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForLifetimesTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: T.FunctionType.Config) {
  const fields = {
    for_lifetimes: config?.forLifetimes,
    trait: config?.trait,
    parameters: config?.parameters,
    return_type: config?.returnType,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    forLifetimes(value?: T.ForLifetimes | undefined) { return _fs(config, functionType, 'forLifetimes', value, fields.for_lifetimes); },
    trait(value?: T._TypeIdentifier | T.ScopedTypeIdentifier | undefined) { return _fs(config, functionType, 'trait', value, fields.trait); },
    parameters(value?: T.Parameters) { return _fs(config, functionType, 'parameters', value, fields.parameters); },
    returnType(value?: T._Type | undefined) { return _fs(config, functionType, 'returnType', value, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: T._Type[]) {
  _assertNonEmpty(children, 'tuple_type.children');
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitType(text: string) {
  if (text.length === 0) throw new Error(`unit_type: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`unit_type: text '${text}' is a reserved keyword`);
  return {
    type: 'unit_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnitTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function genericFunction(config: T.GenericFunction.Config) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_function' as const,
    named: true as const,
    fields,
    function(value?: T.Identifier | T.ScopedIdentifier | T.FieldExpression) { return _fs(config, genericFunction, 'function', value, fields.function); },
    typeArguments(value?: T.TypeArguments) { return _fs(config, genericFunction, 'typeArguments', value, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: T.GenericType.Config) {
  const fields = {
    type: config?.type,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    typeField(value?: T._TypeIdentifier | T.ReservedIdentifier | T.ScopedTypeIdentifier) { return _fs(config, genericType, 'type', value, fields.type); },
    typeArguments(value?: T.TypeArguments) { return _fs(config, genericType, 'typeArguments', value, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericTypeWithTurbofish(config: T.GenericTypeWithTurbofish.Config) {
  const fields = {
    type: config?.type,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type_with_turbofish' as const,
    named: true as const,
    fields,
    typeField(value?: T._TypeIdentifier | T.ScopedIdentifier) { return _fs(config, genericTypeWithTurbofish, 'type', value, fields.type); },
    typeArguments(value?: T.TypeArguments) { return _fs(config, genericTypeWithTurbofish, 'typeArguments', value, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericTypeWithTurbofishTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function boundedType(config: T.BoundedType.Config) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'bounded_type' as const,
    named: true as const,
    fields,
    left(value?: T.Lifetime | T._Type | T.UseBounds) { return _fs(config, boundedType, 'left', value, fields.left); },
    right(value?: T.Lifetime | T._Type | T.UseBounds) { return _fs(config, boundedType, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BoundedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useBounds(config: T.UseBounds.Config) {
  const fields = {
    use: config?.use,
  };
  const children = config?.children ?? [];
  return {
    type: 'use_bounds' as const,
    named: true as const,
    fields,
    children,
    use(value?: "use") { return _fs(config, useBounds, 'use', value, fields.use); },
    getChildren() { return children; },
    setChildren(...items: (T.Lifetime | T._TypeIdentifier)[]) { return useBounds({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UseBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeArguments(...children: (T._Type | T.TypeBinding | T.Lifetime | T.Literal | T.Block | T.TraitBounds)[]) {
  return {
    type: 'type_arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeBinding(config: T.TypeBinding.Config) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
    type: config?.type,
  };
  return {
    type: 'type_binding' as const,
    named: true as const,
    fields,
    name(value?: T._TypeIdentifier) { return _fs(config, typeBinding, 'name', value, fields.name); },
    typeArguments(value?: T.TypeArguments | undefined) { return _fs(config, typeBinding, 'typeArguments', value, fields.type_arguments); },
    typeField(value?: T._Type) { return _fs(config, typeBinding, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeBindingTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceType(config: T.ReferenceType.Config) {
  const fields = {
    lifetime: config?.lifetime,
    mutable_specifier: config?.mutableSpecifier,
    type: config?.type,
  };
  return {
    type: 'reference_type' as const,
    named: true as const,
    fields,
    lifetime(value?: T.Lifetime | undefined) { return _fs(config, referenceType, 'lifetime', value, fields.lifetime); },
    mutableSpecifier(value?: T.MutableSpecifier | undefined) { return _fs(config, referenceType, 'mutableSpecifier', value, fields.mutable_specifier); },
    typeField(value?: T._Type) { return _fs(config, referenceType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReferenceTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pointerType(config: T.PointerType.Config) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    type: config?.type,
  };
  return {
    type: 'pointer_type' as const,
    named: true as const,
    fields,
    mutableSpecifier(value?: T.MutableSpecifier) { return _fs(config, pointerType, 'mutableSpecifier', value, fields.mutable_specifier); },
    typeField(value?: T._Type) { return _fs(config, pointerType, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.PointerTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractType(config: T.AbstractType.Config) {
  const fields = {
    type_parameters: config?.typeParameters,
    trait: config?.trait,
  };
  return {
    type: 'abstract_type' as const,
    named: true as const,
    fields,
    typeParameters(value?: T.TypeParameters | undefined) { return _fs(config, abstractType, 'typeParameters', value, fields.type_parameters); },
    trait(value?: T._TypeIdentifier | T.ScopedTypeIdentifier | T.RemovedTraitBound | T.GenericType | T.FunctionType | T.TupleType | T.BoundedType) { return _fs(config, abstractType, 'trait', value, fields.trait); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AbstractTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dynamicType(config: T.DynamicType.Config) {
  const fields = {
    dyn: config?.dyn,
    trait: config?.trait,
  };
  return {
    type: 'dynamic_type' as const,
    named: true as const,
    fields,
    dyn(value?: "dyn") { return _fs(config, dynamicType, 'dyn', value, fields.dyn); },
    trait(value?: T.HigherRankedTraitBound | T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericType | T.FunctionType | T.TupleType) { return _fs(config, dynamicType, 'trait', value, fields.trait); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DynamicTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutableSpecifier() {
  return {
    type: 'mutable_specifier' as const,
    named: true as const,
    text: 'mut' as const,
    render: () => 'mut' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'mut' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'mut' as const },
    replace: (t: T.MutableSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'mut' as const }; },
  };
}

export function macroInvocation(config: T.MacroInvocation.Config) {
  const fields = {
    macro: config?.macro,
    token_tree: config?.tokenTree,
  };
  return {
    type: 'macro_invocation' as const,
    named: true as const,
    fields,
    macro(value?: T.ScopedIdentifier | T.Identifier | T.ReservedIdentifier) { return _fs(config, macroInvocation, 'macro', value, fields.macro); },
    tokenTree(value?: T.TokenTree) { return _fs(config, macroInvocation, 'tokenTree', value, fields.token_tree); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MacroInvocationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function delimTokenTree(...children: T.DelimTokens[]) {
  return {
    type: 'delim_token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.DelimTokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedIdentifier(config: T.ScopedIdentifier.Config) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_identifier' as const,
    named: true as const,
    fields,
    path(value?: T.Path | T.BracketedType | T.GenericType | undefined) { return _fs(config, scopedIdentifier, 'path', value, fields.path); },
    name(value?: T.Identifier | T.Super) { return _fs(config, scopedIdentifier, 'name', value, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ScopedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifierInExpressionPosition(config: T.ScopedTypeIdentifierInExpressionPosition.Config) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_type_identifier_in_expression_position' as const,
    named: true as const,
    fields,
    path(value?: T.Path | T.GenericType | undefined) { return _fs(config, scopedTypeIdentifierInExpressionPosition, 'path', value, fields.path); },
    name(value?: T._TypeIdentifier) { return _fs(config, scopedTypeIdentifierInExpressionPosition, 'name', value, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ScopedTypeIdentifierInExpressionPositionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifier(config: T.ScopedTypeIdentifier.Config) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_type_identifier' as const,
    named: true as const,
    fields,
    path(value?: T.Path | T.GenericType | T.BracketedType | undefined) { return _fs(config, scopedTypeIdentifier, 'path', value, fields.path); },
    name(value?: T._TypeIdentifier) { return _fs(config, scopedTypeIdentifier, 'name', value, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ScopedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpression(config: T.RangeExpressionUFormBinaryConfig | T.RangeExpressionUFormPostfixConfig | T.RangeExpressionUFormPrefixConfig | T.RangeExpressionUFormBareConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'range_expression_binary') return rangeExpressionUFormBinary(config as T.RangeExpressionUFormBinaryConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'range_expression_postfix') return rangeExpressionUFormPostfix(config as T.RangeExpressionUFormPostfixConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'range_expression_prefix') return rangeExpressionUFormPrefix(config as T.RangeExpressionUFormPrefixConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'range_expression_bare') return rangeExpressionUFormBare(config as T.RangeExpressionUFormBareConfig);
  if (config && (config as any).variant === 'binary') return rangeExpressionUFormBinary(config as T.RangeExpressionUFormBinaryConfig);
  if (config && (config as any).variant === 'postfix') return rangeExpressionUFormPostfix(config as T.RangeExpressionUFormPostfixConfig);
  if (config && (config as any).variant === 'prefix') return rangeExpressionUFormPrefix(config as T.RangeExpressionUFormPrefixConfig);
  if (config && (config as any).variant === 'bare') return rangeExpressionUFormBare(config as T.RangeExpressionUFormBareConfig);
  return rangeExpressionUFormBinary(config as T.RangeExpressionUFormBinaryConfig);
}
export function rangeExpressionUFormBinary(config: T.RangeExpressionUFormBinaryConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: '_form_binary' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangeExpressionBinary) { return rangeExpressionUFormBinary({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionUFormBinaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionUFormPostfix(config: T.RangeExpressionUFormPostfixConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: '_form_postfix' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangeExpressionPostfix) { return rangeExpressionUFormPostfix({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionUFormPostfixTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionUFormPrefix(config: T.RangeExpressionUFormPrefixConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: '_form_prefix' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangeExpressionPrefix) { return rangeExpressionUFormPrefix({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionUFormPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionUFormBare(config: T.RangeExpressionUFormBareConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: '_form_bare' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangeExpressionBare) { return rangeExpressionUFormBare({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionUFormBareTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: T.UnaryExpression.Config) {
  const fields = {
    operator: config?.operator,
    operand: config?.operand,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    operator(value?: "-" | "*" | "!") { return _fs(config, unaryExpression, 'operator', value, fields.operator); },
    operand(value?: T.Expression) { return _fs(config, unaryExpression, 'operand', value, fields.operand); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryExpression(config: T.TryExpression.Config) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'try_expression' as const,
    named: true as const,
    fields,
    value(value?: T.Expression) { return _fs(config, tryExpression, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceExpression(config: T.ReferenceExpression.Config) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    value: config?.value,
  };
  return {
    type: 'reference_expression' as const,
    named: true as const,
    fields,
    mutableSpecifier(value?: T.MutableSpecifier) { return _fs(config, referenceExpression, 'mutableSpecifier', value, fields.mutable_specifier); },
    value(value?: T.Expression) { return _fs(config, referenceExpression, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: T.BinaryExpression.Config) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(value?: T.Expression) { return _fs(config, binaryExpression, 'left', value, fields.left); },
    operator(value?: "&&") { return _fs(config, binaryExpression, 'operator', value, fields.operator); },
    right(value?: T.Expression) { return _fs(config, binaryExpression, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: T.AssignmentExpression.Config) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(value?: T.Expression) { return _fs(config, assignmentExpression, 'left', value, fields.left); },
    right(value?: T.Expression) { return _fs(config, assignmentExpression, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function compoundAssignmentExpr(config: T.CompoundAssignmentExpr.Config) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    fields,
    left(value?: T.Expression) { return _fs(config, compoundAssignmentExpr, 'left', value, fields.left); },
    operator(value?: "+=" | "-=" | "*=" | "/=" | "%=" | "&=" | "|=" | "^=" | "<<=" | ">>=") { return _fs(config, compoundAssignmentExpr, 'operator', value, fields.operator); },
    right(value?: T.Expression) { return _fs(config, compoundAssignmentExpr, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeCastExpression(config: T.TypeCastExpression.Config) {
  const fields = {
    value: config?.value,
    type: config?.type,
  };
  return {
    type: 'type_cast_expression' as const,
    named: true as const,
    fields,
    value(value?: T.Expression) { return _fs(config, typeCastExpression, 'value', value, fields.value); },
    typeField(value?: T._Type) { return _fs(config, typeCastExpression, 'type', value, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TypeCastExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnExpression(child?: T.Expression) {
  const children = child != null ? [child] : [];
  return {
    type: 'return_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReturnExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(child?: T.Expression) {
  const children = child != null ? [child] : [];
  return {
    type: 'yield_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: T.CallExpression.Config) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(value?: T.ExpressionExceptRange) { return _fs(config, callExpression, 'function', value, fields.function); },
    arguments(value?: T.Arguments) { return _fs(config, callExpression, 'arguments', value, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arguments_(...children: (T.AttributeItem | T.Expression)[]) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayExpression(config: T.ArrayExpressionUFormSemiConfig | T.ArrayExpressionUFormListConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'array_expression_semi') return arrayExpressionUFormSemi(config as T.ArrayExpressionUFormSemiConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'array_expression_list') return arrayExpressionUFormList(config as T.ArrayExpressionUFormListConfig);
  if (config && (config as any).variant === 'semi') return arrayExpressionUFormSemi(config as T.ArrayExpressionUFormSemiConfig);
  if (config && (config as any).variant === 'list') return arrayExpressionUFormList(config as T.ArrayExpressionUFormListConfig);
  return arrayExpressionUFormSemi(config as T.ArrayExpressionUFormSemiConfig);
}
export function arrayExpressionUFormSemi(config: T.ArrayExpressionUFormSemiConfig) {
  const children = config?.children ?? [];
  return {
    type: 'array_expression' as const,
    named: true as const,
    variant: '_form_semi' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.ArrayExpressionSemi) { return arrayExpressionUFormSemi({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayExpressionUFormSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function arrayExpressionUFormList(config: T.ArrayExpressionUFormListConfig) {
  const children = config?.children ?? [];
  return {
    type: 'array_expression' as const,
    named: true as const,
    variant: '_form_list' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.ArrayExpressionList) { return arrayExpressionUFormList({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayExpressionUFormListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child: T.Expression) {
  const children = [child];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleExpression(config: T.TupleExpression.Config) {
  const fields = {
    attributes: config?.attributes,
    elements: config?.elements,
  };
  return {
    type: 'tuple_expression' as const,
    named: true as const,
    fields,
    attributes(...values: T.AttributeItem[]) { return _fsm(config, tupleExpression, 'attributes', values, fields.attributes); },
    elements(...values: T.Expression[]) { return _fsm(config, tupleExpression, 'elements', values, fields.elements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitExpression(text: string) {
  if (text.length === 0) throw new Error(`unit_expression: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`unit_expression: text '${text}' is a reserved keyword`);
  return {
    type: 'unit_expression' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.UnitExpressionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function structExpression(config: T.StructExpression.Config) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'struct_expression' as const,
    named: true as const,
    fields,
    name(value?: T._TypeIdentifier | T.ScopedTypeIdentifier | T.GenericTypeWithTurbofish) { return _fs(config, structExpression, 'name', value, fields.name); },
    body(value?: T.FieldInitializerList) { return _fs(config, structExpression, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializerList(...children: (T.ShorthandFieldInitializer | T.FieldInitializer | T.BaseFieldInitializer)[]) {
  return {
    type: 'field_initializer_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldInitializerListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandFieldInitializer(config: T.ShorthandFieldInitializer.Config) {
  const fields = {
    attributes: config?.attributes,
    identifier: config?.identifier,
  };
  return {
    type: 'shorthand_field_initializer' as const,
    named: true as const,
    fields,
    attributes(...values: T.AttributeItem[]) { return _fsm(config, shorthandFieldInitializer, 'attributes', values, fields.attributes); },
    identifier(value?: T.Identifier) { return _fs(config, shorthandFieldInitializer, 'identifier', value, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ShorthandFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializer(config: T.FieldInitializer.Config) {
  const fields = {
    field: config?.field,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_initializer' as const,
    named: true as const,
    fields,
    children,
    field(value?: T._FieldIdentifier | T.IntegerLiteral) { return _fs(config, fieldInitializer, 'field', value, fields.field); },
    value(value?: T.Expression) { return _fs(config, fieldInitializer, 'value', value, fields.value); },
    getChildren() { return children; },
    setChildren(...items: T.AttributeItem[]) { return fieldInitializer({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function baseFieldInitializer(child: T.Expression) {
  const children = [child];
  return {
    type: 'base_field_initializer' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BaseFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifExpression(config: T.IfExpression.Config) {
  const fields = {
    if: config?.if,
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_expression' as const,
    named: true as const,
    fields,
    if(value?: "if") { return _fs(config, ifExpression, 'if', value, fields.if); },
    condition(value?: T.Condition) { return _fs(config, ifExpression, 'condition', value, fields.condition); },
    consequence(value?: T.Block) { return _fs(config, ifExpression, 'consequence', value, fields.consequence); },
    alternative(value?: T.ElseClause | undefined) { return _fs(config, ifExpression, 'alternative', value, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IfExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letCondition(config: T.LetCondition.Config) {
  const fields = {
    let: config?.let,
    pattern: config?.pattern,
    value: config?.value,
  };
  return {
    type: 'let_condition' as const,
    named: true as const,
    fields,
    let(value?: "let") { return _fs(config, letCondition, 'let', value, fields.let); },
    pattern(value?: T.Pattern) { return _fs(config, letCondition, 'pattern', value, fields.pattern); },
    value(value?: T.Expression) { return _fs(config, letCondition, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LetConditionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(config: T.ElseClause.Config) {
  const fields = {
    else: config?.else,
  };
  const children = config?.children ?? [];
  return {
    type: 'else_clause' as const,
    named: true as const,
    fields,
    children,
    else(value?: "else") { return _fs(config, elseClause, 'else', value, fields.else); },
    getChild() { return children[0]; },
    setChild(child: (T.Block | T.IfExpression)) { return elseClause({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchExpression(config: T.MatchExpression.Config) {
  const fields = {
    match: config?.match,
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'match_expression' as const,
    named: true as const,
    fields,
    match(value?: "match") { return _fs(config, matchExpression, 'match', value, fields.match); },
    value(value?: T.Expression) { return _fs(config, matchExpression, 'value', value, fields.value); },
    body(value?: T.MatchBlock) { return _fs(config, matchExpression, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchBlock(...children: T.MatchArm[]) {
  return {
    type: 'match_block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchArm(config: T.MatchArm.Config) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(value?: T.MatchPattern) { return _fs(config, matchArm, 'pattern', value, fields.pattern); },
    value(value?: T.Expression) { return _fs(config, matchArm, 'value', value, fields.value); },
    getChildren() { return children; },
    setChildren(...items: (T.AttributeItem | T.InnerAttributeItem)[]) { return matchArm({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lastMatchArm(config: T.LastMatchArm.Config) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'last_match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(value?: T.MatchPattern) { return _fs(config, lastMatchArm, 'pattern', value, fields.pattern); },
    value(value?: T.Expression) { return _fs(config, lastMatchArm, 'value', value, fields.value); },
    getChildren() { return children; },
    setChildren(...items: (T.AttributeItem | T.InnerAttributeItem)[]) { return lastMatchArm({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LastMatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchPattern(config: T.MatchPattern.Config) {
  const fields = {
    condition: config?.condition,
  };
  const children = config?.children ?? [];
  return {
    type: 'match_pattern' as const,
    named: true as const,
    fields,
    children,
    condition(value?: T.Condition | undefined) { return _fs(config, matchPattern, 'condition', value, fields.condition); },
    getChild() { return children[0]; },
    setChild(child: T.Pattern) { return matchPattern({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MatchPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileExpression(config: T.WhileExpression.Config) {
  const fields = {
    label: config?.label,
    condition: config?.condition,
    body: config?.body,
  };
  return {
    type: 'while_expression' as const,
    named: true as const,
    fields,
    label(value?: T.Label | undefined) { return _fs(config, whileExpression, 'label', value, fields.label); },
    condition(value?: T.Condition) { return _fs(config, whileExpression, 'condition', value, fields.condition); },
    body(value?: T.Block) { return _fs(config, whileExpression, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.WhileExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function loopExpression(config: T.LoopExpression.Config) {
  const fields = {
    label: config?.label,
    body: config?.body,
  };
  return {
    type: 'loop_expression' as const,
    named: true as const,
    fields,
    label(value?: T.Label | undefined) { return _fs(config, loopExpression, 'label', value, fields.label); },
    body(value?: T.Block) { return _fs(config, loopExpression, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LoopExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forExpression(config: T.ForExpression.Config) {
  const fields = {
    label: config?.label,
    pattern: config?.pattern,
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'for_expression' as const,
    named: true as const,
    fields,
    label(value?: T.Label | undefined) { return _fs(config, forExpression, 'label', value, fields.label); },
    pattern(value?: T.Pattern) { return _fs(config, forExpression, 'pattern', value, fields.pattern); },
    value(value?: T.Expression) { return _fs(config, forExpression, 'value', value, fields.value); },
    body(value?: T.Block) { return _fs(config, forExpression, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ForExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constBlock(config: T.ConstBlock.Config) {
  const fields = {
    const: config?.const,
    body: config?.body,
  };
  return {
    type: 'const_block' as const,
    named: true as const,
    fields,
    const(value?: T.KwConst) { return _fs(config, constBlock, 'const', value, fields.const); },
    body(value?: T.Block) { return _fs(config, constBlock, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ConstBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureExpression(config: T.ClosureExpressionUFormBlockConfig | T.ClosureExpressionUFormExprConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'closure_expression_block') return closureExpressionUFormBlock(config as T.ClosureExpressionUFormBlockConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'closure_expression_expr') return closureExpressionUFormExpr(config as T.ClosureExpressionUFormExprConfig);
  if (config && (config as any).variant === 'block') return closureExpressionUFormBlock(config as T.ClosureExpressionUFormBlockConfig);
  if (config && (config as any).variant === 'expr') return closureExpressionUFormExpr(config as T.ClosureExpressionUFormExprConfig);
  return closureExpressionUFormBlock(config as T.ClosureExpressionUFormBlockConfig);
}
export function closureExpressionUFormBlock(config: T.ClosureExpressionUFormBlockConfig) {
  const fields = {
    static: config?.static,
    async: config?.async,
    move: config?.move,
    parameters: config?.parameters,
  };
  const children = config?.children ?? [];
  return {
    type: 'closure_expression' as const,
    named: true as const,
    variant: '_form_block' as const,
    fields,
    children,
    static(value?: "static" | undefined) { return _fs(config, closureExpressionUFormBlock, 'static', value, fields.static); },
    async(value?: "async" | undefined) { return _fs(config, closureExpressionUFormBlock, 'async', value, fields.async); },
    move(value?: "move" | undefined) { return _fs(config, closureExpressionUFormBlock, 'move', value, fields.move); },
    parameters(value?: T.ClosureParameters) { return _fs(config, closureExpressionUFormBlock, 'parameters', value, fields.parameters); },
    getChild() { return children[0]; },
    setChild(child: T.ClosureExpressionBlock) { return closureExpressionUFormBlock({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClosureExpressionUFormBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function closureExpressionUFormExpr(config: T.ClosureExpressionUFormExprConfig) {
  const fields = {
    static: config?.static,
    async: config?.async,
    move: config?.move,
    parameters: config?.parameters,
  };
  const children = config?.children ?? [];
  return {
    type: 'closure_expression' as const,
    named: true as const,
    variant: '_form_expr' as const,
    fields,
    children,
    static(value?: "static" | undefined) { return _fs(config, closureExpressionUFormExpr, 'static', value, fields.static); },
    async(value?: "async" | undefined) { return _fs(config, closureExpressionUFormExpr, 'async', value, fields.async); },
    move(value?: "move" | undefined) { return _fs(config, closureExpressionUFormExpr, 'move', value, fields.move); },
    parameters(value?: T.ClosureParameters) { return _fs(config, closureExpressionUFormExpr, 'parameters', value, fields.parameters); },
    getChild() { return children[0]; },
    setChild(child: T.ClosureExpressionExpr) { return closureExpressionUFormExpr({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClosureExpressionUFormExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureParameters(...children: (T.Pattern | T.Parameter)[]) {
  return {
    type: 'closure_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClosureParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function label(config: T.Label.Config) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'label' as const,
    named: true as const,
    fields,
    identifier(value?: T.Identifier) { return _fs(config, label, 'identifier', value, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LabelTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakExpression(config?: T.BreakExpression.Config) {
  const fields = {
    label: config?.label,
    expression: config?.expression,
  };
  return {
    type: 'break_expression' as const,
    named: true as const,
    fields,
    label(value?: T.Label | undefined) { return _fs(config, breakExpression, 'label', value, fields.label); },
    expression(value?: T.Expression | undefined) { return _fs(config, breakExpression, 'expression', value, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BreakExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueExpression(config?: T.ContinueExpression.Config) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'continue_expression' as const,
    named: true as const,
    fields,
    label(value?: T.Label | undefined) { return _fs(config, continueExpression, 'label', value, fields.label); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ContinueExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexExpression(config: T.IndexExpression.Config) {
  const fields = {
    object: config?.object,
    index: config?.index,
  };
  return {
    type: 'index_expression' as const,
    named: true as const,
    fields,
    object(value?: T.Expression) { return _fs(config, indexExpression, 'object', value, fields.object); },
    index(value?: T.Expression) { return _fs(config, indexExpression, 'index', value, fields.index); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.IndexExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(child: T.Expression) {
  const children = [child];
  return {
    type: 'await_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldExpression(config: T.FieldExpression.Config) {
  const fields = {
    value: config?.value,
    field: config?.field,
  };
  return {
    type: 'field_expression' as const,
    named: true as const,
    fields,
    value(value?: T.Expression) { return _fs(config, fieldExpression, 'value', value, fields.value); },
    field(value?: T._FieldIdentifier | T.IntegerLiteral) { return _fs(config, fieldExpression, 'field', value, fields.field); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unsafeBlock(config: T.UnsafeBlock.Config) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'unsafe_block' as const,
    named: true as const,
    fields,
    block(value?: T.Block) { return _fs(config, unsafeBlock, 'block', value, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.UnsafeBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asyncBlock(config: T.AsyncBlock.Config) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'async_block' as const,
    named: true as const,
    fields,
    block(value?: T.Block) { return _fs(config, asyncBlock, 'block', value, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.AsyncBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genBlock(config: T.GenBlock.Config) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'gen_block' as const,
    named: true as const,
    fields,
    block(value?: T.Block) { return _fs(config, genBlock, 'block', value, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryBlock(config: T.TryBlock.Config) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'try_block' as const,
    named: true as const,
    fields,
    block(value?: T.Block) { return _fs(config, tryBlock, 'block', value, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TryBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(config: T.Block.Config) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'block' as const,
    named: true as const,
    fields,
    children,
    label(value?: T.Label | undefined) { return _fs(config, block, 'label', value, fields.label); },
    getChildren() { return children; },
    setChildren(...items: (T.Statement | T.Expression)[]) { return block({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericPattern(config: T.GenericPattern.Config) {
  const fields = {
    type_arguments: config?.typeArguments,
  };
  const children = config?.children ?? [];
  return {
    type: 'generic_pattern' as const,
    named: true as const,
    fields,
    children,
    typeArguments(value?: T.TypeArguments) { return _fs(config, genericPattern, 'typeArguments', value, fields.type_arguments); },
    getChild() { return children[0]; },
    setChild(child: (T.Identifier | T.ScopedIdentifier)) { return genericPattern({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.GenericPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(...children: (T.Pattern | T.ClosureExpression)[]) {
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slicePattern(...children: T.Pattern[]) {
  return {
    type: 'slice_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.SlicePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleStructPattern(config: T.TupleStructPattern.Config) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'tuple_struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(value?: T.Identifier | T.ScopedIdentifier | T.GenericType) { return _fs(config, tupleStructPattern, 'type', value, fields.type); },
    getChildren() { return children; },
    setChildren(...items: T.Pattern[]) { return tupleStructPattern({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.TupleStructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structPattern(config: T.StructPattern.Config) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(value?: T._TypeIdentifier | T.ScopedTypeIdentifier) { return _fs(config, structPattern, 'type', value, fields.type); },
    getChildren() { return children; },
    setChildren(...items: (T.FieldPattern | T.RemainingFieldPattern)[]) { return structPattern({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldPattern(config: T.FieldPatternUFormShorthandConfig | T.FieldPatternUFormNamedConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'field_pattern_shorthand') return fieldPatternUFormShorthand(config as T.FieldPatternUFormShorthandConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'field_pattern_named') return fieldPatternUFormNamed(config as T.FieldPatternUFormNamedConfig);
  if (config && (config as any).variant === 'shorthand') return fieldPatternUFormShorthand(config as T.FieldPatternUFormShorthandConfig);
  if (config && (config as any).variant === 'named') return fieldPatternUFormNamed(config as T.FieldPatternUFormNamedConfig);
  return fieldPatternUFormShorthand(config as T.FieldPatternUFormShorthandConfig);
}
export function fieldPatternUFormShorthand(config: T.FieldPatternUFormShorthandConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_pattern' as const,
    named: true as const,
    variant: '_form_shorthand' as const,
    fields,
    children,
    mutableSpecifier(value?: T.MutableSpecifier | undefined) { return _fs(config, fieldPatternUFormShorthand, 'mutableSpecifier', value, fields.mutable_specifier); },
    getChild() { return children[0]; },
    setChild(child: T.FieldPatternShorthand) { return fieldPatternUFormShorthand({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldPatternUFormShorthandTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function fieldPatternUFormNamed(config: T.FieldPatternUFormNamedConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_pattern' as const,
    named: true as const,
    variant: '_form_named' as const,
    fields,
    children,
    mutableSpecifier(value?: T.MutableSpecifier | undefined) { return _fs(config, fieldPatternUFormNamed, 'mutableSpecifier', value, fields.mutable_specifier); },
    getChild() { return children[0]; },
    setChild(child: T.FieldPatternNamed) { return fieldPatternUFormNamed({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldPatternUFormNamedTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutPattern(config: T.MutPattern.Config) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'mut_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(value?: T.MutableSpecifier) { return _fs(config, mutPattern, 'mutableSpecifier', value, fields.mutable_specifier); },
    pattern(value?: T.Pattern) { return _fs(config, mutPattern, 'pattern', value, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.MutPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangePattern(config: T.RangePatternUFormLeftConfig | T.RangePatternUFormPrefixConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'range_pattern_left') return rangePatternUFormLeft(config as T.RangePatternUFormLeftConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'range_pattern_prefix') return rangePatternUFormPrefix(config as T.RangePatternUFormPrefixConfig);
  if (config && (config as any).variant === 'left') return rangePatternUFormLeft(config as T.RangePatternUFormLeftConfig);
  if (config && (config as any).variant === 'prefix') return rangePatternUFormPrefix(config as T.RangePatternUFormPrefixConfig);
  return rangePatternUFormLeft(config as T.RangePatternUFormLeftConfig);
}
export function rangePatternUFormLeft(config: T.RangePatternUFormLeftConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: '_form_left' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangePatternLeft) { return rangePatternUFormLeft({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangePatternUFormLeftTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangePatternUFormPrefix(config: T.RangePatternUFormPrefixConfig) {
  const children = config?.children ?? [];
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: '_form_prefix' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.RangePatternPrefix) { return rangePatternUFormPrefix({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangePatternUFormPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function refPattern(config: T.RefPattern.Config) {
  const fields = {
    ref: config?.ref,
  };
  const children = config?.children ?? [];
  return {
    type: 'ref_pattern' as const,
    named: true as const,
    fields,
    children,
    ref(value?: "ref") { return _fs(config, refPattern, 'ref', value, fields.ref); },
    getChild() { return children[0]; },
    setChild(child: T.Pattern) { return refPattern({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RefPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function capturedPattern(config: T.CapturedPattern.Config) {
  const fields = {
    identifier: config?.identifier,
    pattern: config?.pattern,
  };
  return {
    type: 'captured_pattern' as const,
    named: true as const,
    fields,
    identifier(value?: T.Identifier) { return _fs(config, capturedPattern, 'identifier', value, fields.identifier); },
    pattern(value?: T.Pattern) { return _fs(config, capturedPattern, 'pattern', value, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CapturedPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referencePattern(config: T.ReferencePattern.Config) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'reference_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(value?: T.MutableSpecifier | undefined) { return _fs(config, referencePattern, 'mutableSpecifier', value, fields.mutable_specifier); },
    pattern(value?: T.Pattern) { return _fs(config, referencePattern, 'pattern', value, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ReferencePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orPattern(config: T.OrPatternUFormBinaryConfig | T.OrPatternUFormPrefixConfig) {
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'or_pattern_binary') return orPatternUFormBinary(config as T.OrPatternUFormBinaryConfig);
  if (config && Array.isArray((config as any).children) && (config as any).children[0]?.type === 'or_pattern_prefix') return orPatternUFormPrefix(config as T.OrPatternUFormPrefixConfig);
  if (config && (config as any).variant === 'binary') return orPatternUFormBinary(config as T.OrPatternUFormBinaryConfig);
  if (config && (config as any).variant === 'prefix') return orPatternUFormPrefix(config as T.OrPatternUFormPrefixConfig);
  return orPatternUFormBinary(config as T.OrPatternUFormBinaryConfig);
}
export function orPatternUFormBinary(config: T.OrPatternUFormBinaryConfig) {
  const children = config?.children ?? [];
  return {
    type: 'or_pattern' as const,
    named: true as const,
    variant: '_form_binary' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.OrPatternBinary) { return orPatternUFormBinary({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OrPatternUFormBinaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function orPatternUFormPrefix(config: T.OrPatternUFormPrefixConfig) {
  const children = config?.children ?? [];
  return {
    type: 'or_pattern' as const,
    named: true as const,
    variant: '_form_prefix' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: T.OrPatternPrefix) { return orPatternUFormPrefix({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OrPatternUFormPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function negativeLiteral(config: T.NegativeLiteral.Config) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'negative_literal' as const,
    named: true as const,
    fields,
    value(value?: T.IntegerLiteral | T.FloatLiteral) { return _fs(config, negativeLiteral, 'value', value, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.NegativeLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function integerLiteral(text: string) {
  if (text.length === 0) throw new Error(`integer_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`integer_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'integer_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IntegerLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringLiteral(...children: (T.EscapeSequence | T.StringContent)[]) {
  return {
    type: 'string_literal' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rawStringLiteral(config: T.RawStringLiteral.Config) {
  const fields = {
    raw_string_literal_start: config?.rawStringLiteralStart,
    string_content: config?.stringContent,
    raw_string_literal_end: config?.rawStringLiteralEnd,
  };
  return {
    type: 'raw_string_literal' as const,
    named: true as const,
    fields,
    rawStringLiteralStart(value?: string) { return _fs(config, rawStringLiteral, 'rawStringLiteralStart', value, fields.raw_string_literal_start); },
    stringContent(value?: T.StringContent) { return _fs(config, rawStringLiteral, 'stringContent', value, fields.string_content); },
    rawStringLiteralEnd(value?: string) { return _fs(config, rawStringLiteral, 'rawStringLiteralEnd', value, fields.raw_string_literal_end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RawStringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function charLiteral(text: string) {
  if (text.length === 0) throw new Error(`char_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`char_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'char_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.CharLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_sequence: text '${text}' is a reserved keyword`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function booleanLiteral(text: 'true' | 'false') {
  return {
    type: 'boolean_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.BooleanLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(child?: (T.LineComment | T.BlockComment)) {
  const children = child != null ? [child] : [];
  return {
    type: 'comment' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.CommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lineComment(config?: T.LineComment.Config) {
  const fields = {
    outer: config?.outer,
    inner: config?.inner,
    doc: config?.doc,
  };
  return {
    type: 'line_comment' as const,
    named: true as const,
    fields,
    outer(value?: T.OuterDocCommentMarker | undefined) { return _fs(config, lineComment, 'outer', value, fields.outer); },
    inner(value?: T.InnerDocCommentMarker | undefined) { return _fs(config, lineComment, 'inner', value, fields.inner); },
    doc(value?: T.DocComment | undefined) { return _fs(config, lineComment, 'doc', value, fields.doc); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LineCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function blockComment(config?: T.BlockComment.Config) {
  const fields = {
    outer: config?.outer,
    inner: config?.inner,
    doc: config?.doc,
  };
  return {
    type: 'block_comment' as const,
    named: true as const,
    fields,
    outer(value?: T.OuterDocCommentMarker | undefined) { return _fs(config, blockComment, 'outer', value, fields.outer); },
    inner(value?: T.InnerDocCommentMarker | undefined) { return _fs(config, blockComment, 'inner', value, fields.inner); },
    doc(value?: T.DocComment | undefined) { return _fs(config, blockComment, 'doc', value, fields.doc); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.BlockCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`); if (!_leafRe_identifier.test(text)) throw new Error(`identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shebang(text: string) {
  if (text.length === 0) throw new Error(`shebang: text must be non-empty`); if (!_leafRe_shebang.test(text)) throw new Error(`shebang: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`shebang: text '${text}' is a reserved keyword`);
  return {
    type: 'shebang' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ShebangTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function self() {
  return {
    type: 'self' as const,
    named: true as const,
    text: 'self' as const,
    render: () => 'self' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'self' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'self' as const },
    replace: (t: T.SelfTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'self' as const }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: T.SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function crate() {
  return {
    type: 'crate' as const,
    named: true as const,
    text: 'crate' as const,
    render: () => 'crate' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'crate' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'crate' as const },
    replace: (t: T.CrateTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'crate' as const }; },
  };
}

export function metavariable(text: string) {
  if (text.length === 0) throw new Error(`metavariable: text must be non-empty`); if (!_leafRe_metavariable.test(text)) throw new Error(`metavariable: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`metavariable: text '${text}' is a reserved keyword`);
  return {
    type: 'metavariable' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.MetavariableTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function arrayExpressionSemi(config: T.ArrayExpressionSemi.Config) {
  const fields = {
    attributes: config?.attributes,
    elements: config?.elements,
    length: config?.length,
  };
  return {
    type: 'array_expression_semi' as const,
    named: true as const,
    fields,
    attributes(...values: T.AttributeItem[]) { return _fsm(config, arrayExpressionSemi, 'attributes', values, fields.attributes); },
    elements(value?: T.Expression) { return _fs(config, arrayExpressionSemi, 'elements', value, fields.elements); },
    length(value?: T.Expression) { return _fs(config, arrayExpressionSemi, 'length', value, fields.length); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayExpressionSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayExpressionList(config: T.ArrayExpressionList.Config) {
  const fields = {
    attributes: config?.attributes,
    elements: config?.elements,
  };
  const children = config?.children ?? [];
  return {
    type: 'array_expression_list' as const,
    named: true as const,
    fields,
    children,
    attributes(...values: T.AttributeItem[]) { return _fsm(config, arrayExpressionList, 'attributes', values, fields.attributes); },
    elements(...values: T.Expression[]) { return _fsm(config, arrayExpressionList, 'elements', values, fields.elements); },
    getChildren() { return children; },
    setChildren(...items: T.AttributeItem[]) { return arrayExpressionList({ ...config, children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ArrayExpressionListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function stringContent(text: string) {
  if (text.length === 0) throw new Error(`string_content: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_content: text '${text}' is a reserved keyword`);
  return {
    type: 'string_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.StringContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function rawStringLiteralContent(text: string) {
  if (text.length === 0) throw new Error(`raw_string_literal_content: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`raw_string_literal_content: text '${text}' is a reserved keyword`);
  return {
    type: 'raw_string_literal_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.RawStringLiteralContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function floatLiteral(text: string) {
  if (text.length === 0) throw new Error(`float_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`float_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'float_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FloatLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function primitiveType(text: 'u8' | 'i8' | 'u16' | 'i16' | 'u32' | 'i32' | 'u64' | 'i64' | 'u128' | 'i128' | 'isize' | 'usize' | 'f32' | 'f64' | 'bool' | 'str' | 'char') {
  return {
    type: 'primitive_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.PrimitiveTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function modItemInline(config: T.ModItemInline.Config) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'mod_item_inline' as const,
    named: true as const,
    fields,
    body(value?: T.DeclarationList) { return _fs(config, modItemInline, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ModItemInlineTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structItemBrace(config: T.StructItemBrace.Config) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item_brace' as const,
    named: true as const,
    fields,
    children,
    body(value?: T.FieldDeclarationList) { return _fs(config, structItemBrace, 'body', value, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return structItemBrace({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructItemBraceTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structItemTuple(config: T.StructItemTuple.Config) {
  const fields = {
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item_tuple' as const,
    named: true as const,
    fields,
    children,
    body(value?: T.OrderedFieldDeclarationList) { return _fs(config, structItemTuple, 'body', value, fields.body); },
    getChild() { return children[0]; },
    setChild(child: T.WhereClause) { return structItemTuple({ ...config, children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.StructItemTupleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpressionBinary(config: T.RangeExpressionBinary.Config) {
  const fields = {
    start: config?.start,
    operator: config?.operator,
    end: config?.end,
  };
  return {
    type: 'range_expression_binary' as const,
    named: true as const,
    fields,
    start(value?: T.Expression) { return _fs(config, rangeExpressionBinary, 'start', value, fields.start); },
    operator(value?: ".." | "..." | "..=") { return _fs(config, rangeExpressionBinary, 'operator', value, fields.operator); },
    end(value?: T.Expression) { return _fs(config, rangeExpressionBinary, 'end', value, fields.end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionBinaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpressionPostfix(config: T.RangeExpressionPostfix.Config) {
  const fields = {
    start: config?.start,
    operator: config?.operator,
  };
  return {
    type: 'range_expression_postfix' as const,
    named: true as const,
    fields,
    start(value?: T.Expression) { return _fs(config, rangeExpressionPostfix, 'start', value, fields.start); },
    operator(value?: T.KwOperator) { return _fs(config, rangeExpressionPostfix, 'operator', value, fields.operator); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionPostfixTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpressionPrefix(config: T.RangeExpressionPrefix.Config) {
  const fields = {
    operator: config?.operator,
    end: config?.end,
  };
  return {
    type: 'range_expression_prefix' as const,
    named: true as const,
    fields,
    operator(value?: T.KwOperator) { return _fs(config, rangeExpressionPrefix, 'operator', value, fields.operator); },
    end(value?: T.Expression) { return _fs(config, rangeExpressionPrefix, 'end', value, fields.end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpressionBare(config: T.RangeExpressionBare.Config) {
  const fields = {
    operator: config?.operator,
  };
  return {
    type: 'range_expression_bare' as const,
    named: true as const,
    fields,
    operator(value?: T.KwOperator) { return _fs(config, rangeExpressionBare, 'operator', value, fields.operator); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangeExpressionBareTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letChain(child?: (T._LetChain | T.LetCondition | T.Expression)) {
  const children = child != null ? [child] : [];
  return {
    type: 'let_chain' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.LetChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureExpressionBlock(config: T.ClosureExpressionBlock.Config) {
  const fields = {
    return_type: config?.returnType,
    body: config?.body,
  };
  return {
    type: 'closure_expression_block' as const,
    named: true as const,
    fields,
    returnType(value?: T._Type | undefined) { return _fs(config, closureExpressionBlock, 'returnType', value, fields.return_type); },
    body(value?: T.Block) { return _fs(config, closureExpressionBlock, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClosureExpressionBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureExpressionExpr(config: T.ClosureExpressionExpr.Config) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'closure_expression_expr' as const,
    named: true as const,
    fields,
    body(value?: T.Expression) { return _fs(config, closureExpressionExpr, 'body', value, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.ClosureExpressionExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldPatternShorthand(config: T.FieldPatternShorthand.Config) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'field_pattern_shorthand' as const,
    named: true as const,
    fields,
    name(value?: string) { return _fs(config, fieldPatternShorthand, 'name', value, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldPatternShorthandTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldPatternNamed(config: T.FieldPatternNamed.Config) {
  const fields = {
    name: config?.name,
    pattern: config?.pattern,
  };
  return {
    type: 'field_pattern_named' as const,
    named: true as const,
    fields,
    name(value?: T._FieldIdentifier) { return _fs(config, fieldPatternNamed, 'name', value, fields.name); },
    pattern(value?: T.Pattern) { return _fs(config, fieldPatternNamed, 'pattern', value, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.FieldPatternNamedTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangePatternLeft(config: T.RangePatternLeft.Config) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'range_pattern_left' as const,
    named: true as const,
    fields,
    left(value?: T.LiteralPattern | T.Path) { return _fs(config, rangePatternLeft, 'left', value, fields.left); },
    right(value?: T.LiteralPattern | T.Path | undefined) { return _fs(config, rangePatternLeft, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangePatternLeftTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangePatternPrefix(config: T.RangePatternPrefix.Config) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'range_pattern_prefix' as const,
    named: true as const,
    fields,
    right(value?: T.LiteralPattern | T.Path) { return _fs(config, rangePatternPrefix, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.RangePatternPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orPatternBinary(config: T.OrPatternBinary.Config) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'or_pattern_binary' as const,
    named: true as const,
    fields,
    left(value?: T.Pattern) { return _fs(config, orPatternBinary, 'left', value, fields.left); },
    right(value?: T.Pattern) { return _fs(config, orPatternBinary, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OrPatternBinaryTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orPatternPrefix(config: T.OrPatternPrefix.Config) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'or_pattern_prefix' as const,
    named: true as const,
    fields,
    right(value?: T.Pattern) { return _fs(config, orPatternPrefix, 'right', value, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: T.OrPatternPrefixTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function outerDocCommentMarker(text: string) {
  if (text.length === 0) throw new Error(`outer_doc_comment_marker: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`outer_doc_comment_marker: text '${text}' is a reserved keyword`);
  return {
    type: 'outer_doc_comment_marker' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.OuterDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function innerDocCommentMarker(text: string) {
  if (text.length === 0) throw new Error(`inner_doc_comment_marker: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`inner_doc_comment_marker: text '${text}' is a reserved keyword`);
  return {
    type: 'inner_doc_comment_marker' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.InnerDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeIdentifier(text: string) {
  if (text.length === 0) throw new Error(`type_identifier: text must be non-empty`); if (!_leafRe_typeIdentifier.test(text)) throw new Error(`type_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`type_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function fieldIdentifier(text: string) {
  if (text.length === 0) throw new Error(`field_identifier: text must be non-empty`); if (!_leafRe_fieldIdentifier.test(text)) throw new Error(`field_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`field_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'field_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.FieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shorthandFieldIdentifier(text: string) {
  if (text.length === 0) throw new Error(`shorthand_field_identifier: text must be non-empty`); if (!_leafRe_shorthandFieldIdentifier.test(text)) throw new Error(`shorthand_field_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`shorthand_field_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'shorthand_field_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: T.ShorthandFieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "source_file": FluentNode<"source_file", T.SourceFile.Config>;
  "expression_statement": FluentNode<"expression_statement", T.ExpressionStatement.Config>;
  "macro_definition": FluentNode<"macro_definition", T.MacroDefinition.Config>;
  "macro_rule": FluentNode<"macro_rule", T.MacroRule.Config>;
  "token_tree_pattern": FluentNode<"token_tree_pattern", T.TokenTreePattern.Config>;
  "token_binding_pattern": FluentNode<"token_binding_pattern", T.TokenBindingPattern.Config>;
  "token_repetition_pattern": FluentNode<"token_repetition_pattern", T.TokenRepetitionPattern.Config>;
  "fragment_specifier": T.FragmentSpecifier;
  "token_tree": FluentNode<"token_tree", T.TokenTree.Config>;
  "token_repetition": FluentNode<"token_repetition", T.TokenRepetition.Config>;
  "attribute_item": FluentNode<"attribute_item", T.AttributeItem.Config>;
  "inner_attribute_item": FluentNode<"inner_attribute_item", T.InnerAttributeItem.Config>;
  "attribute": FluentNode<"attribute", T.Attribute.Config>;
  "mod_item": FluentNode<"mod_item", T.ModItem.Config>;
  "foreign_mod_item": FluentNode<"foreign_mod_item", T.ForeignModItem.Config>;
  "declaration_list": FluentNode<"declaration_list", T.DeclarationList.Config>;
  "struct_item": FluentNode<"struct_item", T.StructItem.Config>;
  "union_item": FluentNode<"union_item", T.UnionItem.Config>;
  "enum_item": FluentNode<"enum_item", T.EnumItem.Config>;
  "enum_variant_list": FluentNode<"enum_variant_list", T.EnumVariantList.Config>;
  "enum_variant": FluentNode<"enum_variant", T.EnumVariant.Config>;
  "field_declaration_list": FluentNode<"field_declaration_list", T.FieldDeclarationList.Config>;
  "field_declaration": FluentNode<"field_declaration", T.FieldDeclaration.Config>;
  "ordered_field_declaration_list": FluentNode<"ordered_field_declaration_list", T.OrderedFieldDeclarationList.Config>;
  "extern_crate_declaration": FluentNode<"extern_crate_declaration", T.ExternCrateDeclaration.Config>;
  "const_item": FluentNode<"const_item", T.ConstItem.Config>;
  "static_item": FluentNode<"static_item", T.StaticItem.Config>;
  "type_item": FluentNode<"type_item", T.TypeItem.Config>;
  "function_item": FluentNode<"function_item", T.FunctionItem.Config>;
  "function_signature_item": FluentNode<"function_signature_item", T.FunctionSignatureItem.Config>;
  "function_modifiers": FluentNode<"function_modifiers", T.FunctionModifiers.Config>;
  "where_clause": FluentNode<"where_clause", T.WhereClause.Config>;
  "where_predicate": FluentNode<"where_predicate", T.WherePredicate.Config>;
  "impl_item": FluentNode<"impl_item", T.ImplItem.Config>;
  "trait_item": FluentNode<"trait_item", T.TraitItem.Config>;
  "associated_type": FluentNode<"associated_type", T.AssociatedType.Config>;
  "trait_bounds": FluentNode<"trait_bounds", T.TraitBounds.Config>;
  "higher_ranked_trait_bound": FluentNode<"higher_ranked_trait_bound", T.HigherRankedTraitBound.Config>;
  "removed_trait_bound": FluentNode<"removed_trait_bound", T.RemovedTraitBound.Config>;
  "type_parameters": FluentNode<"type_parameters", T.TypeParameters.Config>;
  "const_parameter": FluentNode<"const_parameter", T.ConstParameter.Config>;
  "type_parameter": FluentNode<"type_parameter", T.TypeParameter.Config>;
  "lifetime_parameter": FluentNode<"lifetime_parameter", T.LifetimeParameter.Config>;
  "let_declaration": FluentNode<"let_declaration", T.LetDeclaration.Config>;
  "use_declaration": FluentNode<"use_declaration", T.UseDeclaration.Config>;
  "scoped_use_list": FluentNode<"scoped_use_list", T.ScopedUseList.Config>;
  "use_list": FluentNode<"use_list", T.UseList.Config>;
  "use_as_clause": FluentNode<"use_as_clause", T.UseAsClause.Config>;
  "use_wildcard": FluentNode<"use_wildcard", T.UseWildcard.Config>;
  "parameters": FluentNode<"parameters", T.Parameters.Config>;
  "self_parameter": FluentNode<"self_parameter", T.SelfParameter.Config>;
  "variadic_parameter": FluentNode<"variadic_parameter", T.VariadicParameter.Config>;
  "parameter": FluentNode<"parameter", T.Parameter.Config>;
  "extern_modifier": FluentNode<"extern_modifier", T.ExternModifier.Config>;
  "visibility_modifier": FluentNode<"visibility_modifier", T.VisibilityModifier.Config>;
  "bracketed_type": FluentNode<"bracketed_type", T.BracketedType.Config>;
  "qualified_type": FluentNode<"qualified_type", T.QualifiedType.Config>;
  "lifetime": FluentNode<"lifetime", T.Lifetime.Config>;
  "array_type": FluentNode<"array_type", T.ArrayType.Config>;
  "for_lifetimes": FluentNode<"for_lifetimes", T.ForLifetimes.Config>;
  "function_type": FluentNode<"function_type", T.FunctionType.Config>;
  "tuple_type": FluentNode<"tuple_type", T.TupleType.Config>;
  "unit_type": T.UnitType;
  "generic_function": FluentNode<"generic_function", T.GenericFunction.Config>;
  "generic_type": FluentNode<"generic_type", T.GenericType.Config>;
  "generic_type_with_turbofish": FluentNode<"generic_type_with_turbofish", T.GenericTypeWithTurbofish.Config>;
  "bounded_type": FluentNode<"bounded_type", T.BoundedType.Config>;
  "use_bounds": FluentNode<"use_bounds", T.UseBounds.Config>;
  "type_arguments": FluentNode<"type_arguments", T.TypeArguments.Config>;
  "type_binding": FluentNode<"type_binding", T.TypeBinding.Config>;
  "reference_type": FluentNode<"reference_type", T.ReferenceType.Config>;
  "pointer_type": FluentNode<"pointer_type", T.PointerType.Config>;
  "abstract_type": FluentNode<"abstract_type", T.AbstractType.Config>;
  "dynamic_type": FluentNode<"dynamic_type", T.DynamicType.Config>;
  "mutable_specifier": T.MutableSpecifier;
  "macro_invocation": FluentNode<"macro_invocation", T.MacroInvocation.Config>;
  "delim_token_tree": FluentNode<"delim_token_tree", T.DelimTokenTree.Config>;
  "scoped_identifier": FluentNode<"scoped_identifier", T.ScopedIdentifier.Config>;
  "scoped_type_identifier_in_expression_position": FluentNode<"scoped_type_identifier_in_expression_position", T.ScopedTypeIdentifierInExpressionPosition.Config>;
  "scoped_type_identifier": FluentNode<"scoped_type_identifier", T.ScopedTypeIdentifier.Config>;
  "range_expression": FluentNode<"range_expression", T.RangeExpression.Config>;
  "unary_expression": FluentNode<"unary_expression", T.UnaryExpression.Config>;
  "try_expression": FluentNode<"try_expression", T.TryExpression.Config>;
  "reference_expression": FluentNode<"reference_expression", T.ReferenceExpression.Config>;
  "binary_expression": FluentNode<"binary_expression", T.BinaryExpression.Config>;
  "assignment_expression": FluentNode<"assignment_expression", T.AssignmentExpression.Config>;
  "compound_assignment_expr": FluentNode<"compound_assignment_expr", T.CompoundAssignmentExpr.Config>;
  "type_cast_expression": FluentNode<"type_cast_expression", T.TypeCastExpression.Config>;
  "return_expression": FluentNode<"return_expression", T.ReturnExpression.Config>;
  "yield_expression": FluentNode<"yield_expression", T.YieldExpression.Config>;
  "call_expression": FluentNode<"call_expression", T.CallExpression.Config>;
  "arguments": FluentNode<"arguments", T.Arguments.Config>;
  "array_expression": FluentNode<"array_expression", T.ArrayExpression.Config>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", T.ParenthesizedExpression.Config>;
  "tuple_expression": FluentNode<"tuple_expression", T.TupleExpression.Config>;
  "unit_expression": T.UnitExpression;
  "struct_expression": FluentNode<"struct_expression", T.StructExpression.Config>;
  "field_initializer_list": FluentNode<"field_initializer_list", T.FieldInitializerList.Config>;
  "shorthand_field_initializer": FluentNode<"shorthand_field_initializer", T.ShorthandFieldInitializer.Config>;
  "field_initializer": FluentNode<"field_initializer", T.FieldInitializer.Config>;
  "base_field_initializer": FluentNode<"base_field_initializer", T.BaseFieldInitializer.Config>;
  "if_expression": FluentNode<"if_expression", T.IfExpression.Config>;
  "let_condition": FluentNode<"let_condition", T.LetCondition.Config>;
  "else_clause": FluentNode<"else_clause", T.ElseClause.Config>;
  "match_expression": FluentNode<"match_expression", T.MatchExpression.Config>;
  "match_block": FluentNode<"match_block", T.MatchBlock.Config>;
  "match_arm": FluentNode<"match_arm", T.MatchArm.Config>;
  "last_match_arm": FluentNode<"last_match_arm", T.LastMatchArm.Config>;
  "match_pattern": FluentNode<"match_pattern", T.MatchPattern.Config>;
  "while_expression": FluentNode<"while_expression", T.WhileExpression.Config>;
  "loop_expression": FluentNode<"loop_expression", T.LoopExpression.Config>;
  "for_expression": FluentNode<"for_expression", T.ForExpression.Config>;
  "const_block": FluentNode<"const_block", T.ConstBlock.Config>;
  "closure_expression": FluentNode<"closure_expression", T.ClosureExpression.Config>;
  "closure_parameters": FluentNode<"closure_parameters", T.ClosureParameters.Config>;
  "label": FluentNode<"label", T.Label.Config>;
  "break_expression": FluentNode<"break_expression", T.BreakExpression.Config>;
  "continue_expression": FluentNode<"continue_expression", T.ContinueExpression.Config>;
  "index_expression": FluentNode<"index_expression", T.IndexExpression.Config>;
  "await_expression": FluentNode<"await_expression", T.AwaitExpression.Config>;
  "field_expression": FluentNode<"field_expression", T.FieldExpression.Config>;
  "unsafe_block": FluentNode<"unsafe_block", T.UnsafeBlock.Config>;
  "async_block": FluentNode<"async_block", T.AsyncBlock.Config>;
  "gen_block": FluentNode<"gen_block", T.GenBlock.Config>;
  "try_block": FluentNode<"try_block", T.TryBlock.Config>;
  "block": FluentNode<"block", T.Block.Config>;
  "generic_pattern": FluentNode<"generic_pattern", T.GenericPattern.Config>;
  "tuple_pattern": FluentNode<"tuple_pattern", T.TuplePattern.Config>;
  "slice_pattern": FluentNode<"slice_pattern", T.SlicePattern.Config>;
  "tuple_struct_pattern": FluentNode<"tuple_struct_pattern", T.TupleStructPattern.Config>;
  "struct_pattern": FluentNode<"struct_pattern", T.StructPattern.Config>;
  "field_pattern": FluentNode<"field_pattern", T.FieldPattern.Config>;
  "mut_pattern": FluentNode<"mut_pattern", T.MutPattern.Config>;
  "range_pattern": FluentNode<"range_pattern", T.RangePattern.Config>;
  "ref_pattern": FluentNode<"ref_pattern", T.RefPattern.Config>;
  "captured_pattern": FluentNode<"captured_pattern", T.CapturedPattern.Config>;
  "reference_pattern": FluentNode<"reference_pattern", T.ReferencePattern.Config>;
  "or_pattern": FluentNode<"or_pattern", T.OrPattern.Config>;
  "negative_literal": FluentNode<"negative_literal", T.NegativeLiteral.Config>;
  "integer_literal": T.IntegerLiteral;
  "string_literal": FluentNode<"string_literal", T.StringLiteral.Config>;
  "raw_string_literal": FluentNode<"raw_string_literal", T.RawStringLiteral.Config>;
  "char_literal": T.CharLiteral;
  "escape_sequence": T.EscapeSequence;
  "boolean_literal": T.BooleanLiteral;
  "comment": FluentNode<"comment", T.Comment.Config>;
  "line_comment": FluentNode<"line_comment", T.LineComment.Config>;
  "block_comment": FluentNode<"block_comment", T.BlockComment.Config>;
  "identifier": T.Identifier;
  "shebang": T.Shebang;
  "self": T.Self;
  "super": T.Super;
  "crate": T.Crate;
  "metavariable": T.Metavariable;
  "array_expression_semi": FluentNode<"array_expression_semi", T.ArrayExpressionSemi.Config>;
  "array_expression_list": FluentNode<"array_expression_list", T.ArrayExpressionList.Config>;
  "string_content": T.StringContent;
  "raw_string_literal_content": T.RawStringLiteralContent;
  "float_literal": T.FloatLiteral;
  "primitive_type": T.PrimitiveType;
  "mod_item_inline": FluentNode<"mod_item_inline", T.ModItemInline.Config>;
  "struct_item_brace": FluentNode<"struct_item_brace", T.StructItemBrace.Config>;
  "struct_item_tuple": FluentNode<"struct_item_tuple", T.StructItemTuple.Config>;
  "range_expression_binary": FluentNode<"range_expression_binary", T.RangeExpressionBinary.Config>;
  "range_expression_postfix": FluentNode<"range_expression_postfix", T.RangeExpressionPostfix.Config>;
  "range_expression_prefix": FluentNode<"range_expression_prefix", T.RangeExpressionPrefix.Config>;
  "range_expression_bare": FluentNode<"range_expression_bare", T.RangeExpressionBare.Config>;
  "let_chain": FluentNode<"let_chain", T.LetChain.Config>;
  "closure_expression_block": FluentNode<"closure_expression_block", T.ClosureExpressionBlock.Config>;
  "closure_expression_expr": FluentNode<"closure_expression_expr", T.ClosureExpressionExpr.Config>;
  "field_pattern_shorthand": FluentNode<"field_pattern_shorthand", T.FieldPatternShorthand.Config>;
  "field_pattern_named": FluentNode<"field_pattern_named", T.FieldPatternNamed.Config>;
  "range_pattern_left": FluentNode<"range_pattern_left", T.RangePatternLeft.Config>;
  "range_pattern_prefix": FluentNode<"range_pattern_prefix", T.RangePatternPrefix.Config>;
  "or_pattern_binary": FluentNode<"or_pattern_binary", T.OrPatternBinary.Config>;
  "or_pattern_prefix": FluentNode<"or_pattern_prefix", T.OrPatternPrefix.Config>;
  "outer_doc_comment_marker": T.OuterDocCommentMarker;
  "inner_doc_comment_marker": T.InnerDocCommentMarker;
  "type_identifier": T.TypeIdentifier;
  "field_identifier": T.FieldIdentifier;
  "shorthand_field_identifier": T.ShorthandFieldIdentifier;
};

export const _factoryMap = {
  "source_file": sourceFile,
  "expression_statement": expressionStatement,
  "macro_definition": macroDefinition,
  "macro_rule": macroRule,
  "token_tree_pattern": tokenTreePattern,
  "token_binding_pattern": tokenBindingPattern,
  "token_repetition_pattern": tokenRepetitionPattern,
  "fragment_specifier": fragmentSpecifier,
  "token_tree": tokenTree,
  "token_repetition": tokenRepetition,
  "attribute_item": attributeItem,
  "inner_attribute_item": innerAttributeItem,
  "attribute": attribute,
  "mod_item": modItem,
  "foreign_mod_item": foreignModItem,
  "declaration_list": declarationList,
  "struct_item": structItem,
  "union_item": unionItem,
  "enum_item": enumItem,
  "enum_variant_list": enumVariantList,
  "enum_variant": enumVariant,
  "field_declaration_list": fieldDeclarationList,
  "field_declaration": fieldDeclaration,
  "ordered_field_declaration_list": orderedFieldDeclarationList,
  "extern_crate_declaration": externCrateDeclaration,
  "const_item": constItem,
  "static_item": staticItem,
  "type_item": typeItem,
  "function_item": functionItem,
  "function_signature_item": functionSignatureItem,
  "function_modifiers": functionModifiers,
  "where_clause": whereClause,
  "where_predicate": wherePredicate,
  "impl_item": implItem,
  "trait_item": traitItem,
  "associated_type": associatedType,
  "trait_bounds": traitBounds,
  "higher_ranked_trait_bound": higherRankedTraitBound,
  "removed_trait_bound": removedTraitBound,
  "type_parameters": typeParameters,
  "const_parameter": constParameter,
  "type_parameter": typeParameter,
  "lifetime_parameter": lifetimeParameter,
  "let_declaration": letDeclaration,
  "use_declaration": useDeclaration,
  "scoped_use_list": scopedUseList,
  "use_list": useList,
  "use_as_clause": useAsClause,
  "use_wildcard": useWildcard,
  "parameters": parameters,
  "self_parameter": selfParameter,
  "variadic_parameter": variadicParameter,
  "parameter": parameter,
  "extern_modifier": externModifier,
  "visibility_modifier": visibilityModifier,
  "bracketed_type": bracketedType,
  "qualified_type": qualifiedType,
  "lifetime": lifetime,
  "array_type": arrayType,
  "for_lifetimes": forLifetimes,
  "function_type": functionType,
  "tuple_type": tupleType,
  "unit_type": unitType,
  "generic_function": genericFunction,
  "generic_type": genericType,
  "generic_type_with_turbofish": genericTypeWithTurbofish,
  "bounded_type": boundedType,
  "use_bounds": useBounds,
  "type_arguments": typeArguments,
  "type_binding": typeBinding,
  "reference_type": referenceType,
  "pointer_type": pointerType,
  "abstract_type": abstractType,
  "dynamic_type": dynamicType,
  "mutable_specifier": mutableSpecifier,
  "macro_invocation": macroInvocation,
  "delim_token_tree": delimTokenTree,
  "scoped_identifier": scopedIdentifier,
  "scoped_type_identifier_in_expression_position": scopedTypeIdentifierInExpressionPosition,
  "scoped_type_identifier": scopedTypeIdentifier,
  "range_expression": rangeExpression,
  "unary_expression": unaryExpression,
  "try_expression": tryExpression,
  "reference_expression": referenceExpression,
  "binary_expression": binaryExpression,
  "assignment_expression": assignmentExpression,
  "compound_assignment_expr": compoundAssignmentExpr,
  "type_cast_expression": typeCastExpression,
  "return_expression": returnExpression,
  "yield_expression": yieldExpression,
  "call_expression": callExpression,
  "arguments": arguments_,
  "array_expression": arrayExpression,
  "parenthesized_expression": parenthesizedExpression,
  "tuple_expression": tupleExpression,
  "unit_expression": unitExpression,
  "struct_expression": structExpression,
  "field_initializer_list": fieldInitializerList,
  "shorthand_field_initializer": shorthandFieldInitializer,
  "field_initializer": fieldInitializer,
  "base_field_initializer": baseFieldInitializer,
  "if_expression": ifExpression,
  "let_condition": letCondition,
  "else_clause": elseClause,
  "match_expression": matchExpression,
  "match_block": matchBlock,
  "match_arm": matchArm,
  "last_match_arm": lastMatchArm,
  "match_pattern": matchPattern,
  "while_expression": whileExpression,
  "loop_expression": loopExpression,
  "for_expression": forExpression,
  "const_block": constBlock,
  "closure_expression": closureExpression,
  "closure_parameters": closureParameters,
  "label": label,
  "break_expression": breakExpression,
  "continue_expression": continueExpression,
  "index_expression": indexExpression,
  "await_expression": awaitExpression,
  "field_expression": fieldExpression,
  "unsafe_block": unsafeBlock,
  "async_block": asyncBlock,
  "gen_block": genBlock,
  "try_block": tryBlock,
  "block": block,
  "generic_pattern": genericPattern,
  "tuple_pattern": tuplePattern,
  "slice_pattern": slicePattern,
  "tuple_struct_pattern": tupleStructPattern,
  "struct_pattern": structPattern,
  "field_pattern": fieldPattern,
  "mut_pattern": mutPattern,
  "range_pattern": rangePattern,
  "ref_pattern": refPattern,
  "captured_pattern": capturedPattern,
  "reference_pattern": referencePattern,
  "or_pattern": orPattern,
  "negative_literal": negativeLiteral,
  "integer_literal": integerLiteral,
  "string_literal": stringLiteral,
  "raw_string_literal": rawStringLiteral,
  "char_literal": charLiteral,
  "escape_sequence": escapeSequence,
  "boolean_literal": booleanLiteral,
  "comment": comment,
  "line_comment": lineComment,
  "block_comment": blockComment,
  "identifier": identifier,
  "shebang": shebang,
  "self": self,
  "super": super_,
  "crate": crate,
  "metavariable": metavariable,
  "array_expression_semi": arrayExpressionSemi,
  "array_expression_list": arrayExpressionList,
  "string_content": stringContent,
  "raw_string_literal_content": rawStringLiteralContent,
  "float_literal": floatLiteral,
  "primitive_type": primitiveType,
  "mod_item_inline": modItemInline,
  "struct_item_brace": structItemBrace,
  "struct_item_tuple": structItemTuple,
  "range_expression_binary": rangeExpressionBinary,
  "range_expression_postfix": rangeExpressionPostfix,
  "range_expression_prefix": rangeExpressionPrefix,
  "range_expression_bare": rangeExpressionBare,
  "let_chain": letChain,
  "closure_expression_block": closureExpressionBlock,
  "closure_expression_expr": closureExpressionExpr,
  "field_pattern_shorthand": fieldPatternShorthand,
  "field_pattern_named": fieldPatternNamed,
  "range_pattern_left": rangePatternLeft,
  "range_pattern_prefix": rangePatternPrefix,
  "or_pattern_binary": orPatternBinary,
  "or_pattern_prefix": orPatternPrefix,
  "outer_doc_comment_marker": outerDocCommentMarker,
  "inner_doc_comment_marker": innerDocCommentMarker,
  "type_identifier": typeIdentifier,
  "field_identifier": fieldIdentifier,
  "shorthand_field_identifier": shorthandFieldIdentifier,
} as const;
export type _FactoryMap = typeof _factoryMap;

export const _factoryShapes = {
  "source_file": "config",
  "expression_statement": "children",
  "macro_definition": "config",
  "macro_rule": "config",
  "token_tree_pattern": "children",
  "token_binding_pattern": "config",
  "token_repetition_pattern": "children",
  "fragment_specifier": "text",
  "token_tree": "children",
  "token_repetition": "children",
  "attribute_item": "config",
  "inner_attribute_item": "config",
  "attribute": "config",
  "mod_item": "config",
  "foreign_mod_item": "config",
  "declaration_list": "children",
  "struct_item": "config",
  "union_item": "config",
  "enum_item": "config",
  "enum_variant_list": "children",
  "enum_variant": "config",
  "field_declaration_list": "children",
  "field_declaration": "config",
  "ordered_field_declaration_list": "config",
  "extern_crate_declaration": "config",
  "const_item": "config",
  "static_item": "config",
  "type_item": "config",
  "function_item": "config",
  "function_signature_item": "config",
  "function_modifiers": "config",
  "where_clause": "config",
  "where_predicate": "config",
  "impl_item": "config",
  "trait_item": "config",
  "associated_type": "config",
  "trait_bounds": "children",
  "higher_ranked_trait_bound": "config",
  "removed_trait_bound": "children",
  "type_parameters": "children",
  "const_parameter": "config",
  "type_parameter": "config",
  "lifetime_parameter": "config",
  "let_declaration": "config",
  "use_declaration": "config",
  "scoped_use_list": "config",
  "use_list": "children",
  "use_as_clause": "config",
  "use_wildcard": "config",
  "parameters": "children",
  "self_parameter": "config",
  "variadic_parameter": "config",
  "parameter": "config",
  "extern_modifier": "config",
  "visibility_modifier": "config",
  "bracketed_type": "children",
  "qualified_type": "config",
  "lifetime": "config",
  "array_type": "config",
  "for_lifetimes": "config",
  "function_type": "config",
  "tuple_type": "children",
  "unit_type": "text",
  "generic_function": "config",
  "generic_type": "config",
  "generic_type_with_turbofish": "config",
  "bounded_type": "config",
  "use_bounds": "config",
  "type_arguments": "children",
  "type_binding": "config",
  "reference_type": "config",
  "pointer_type": "config",
  "abstract_type": "config",
  "dynamic_type": "config",
  "mutable_specifier": "text",
  "macro_invocation": "config",
  "delim_token_tree": "children",
  "scoped_identifier": "config",
  "scoped_type_identifier_in_expression_position": "config",
  "scoped_type_identifier": "config",
  "range_expression": "config",
  "unary_expression": "config",
  "try_expression": "config",
  "reference_expression": "config",
  "binary_expression": "config",
  "assignment_expression": "config",
  "compound_assignment_expr": "config",
  "type_cast_expression": "config",
  "return_expression": "children",
  "yield_expression": "children",
  "call_expression": "config",
  "arguments": "children",
  "array_expression": "config",
  "parenthesized_expression": "children",
  "tuple_expression": "config",
  "unit_expression": "text",
  "struct_expression": "config",
  "field_initializer_list": "children",
  "shorthand_field_initializer": "config",
  "field_initializer": "config",
  "base_field_initializer": "children",
  "if_expression": "config",
  "let_condition": "config",
  "else_clause": "config",
  "match_expression": "config",
  "match_block": "children",
  "match_arm": "config",
  "last_match_arm": "config",
  "match_pattern": "config",
  "while_expression": "config",
  "loop_expression": "config",
  "for_expression": "config",
  "const_block": "config",
  "closure_expression": "config",
  "closure_parameters": "children",
  "label": "config",
  "break_expression": "config",
  "continue_expression": "config",
  "index_expression": "config",
  "await_expression": "children",
  "field_expression": "config",
  "unsafe_block": "config",
  "async_block": "config",
  "gen_block": "config",
  "try_block": "config",
  "block": "config",
  "generic_pattern": "config",
  "tuple_pattern": "children",
  "slice_pattern": "children",
  "tuple_struct_pattern": "config",
  "struct_pattern": "config",
  "field_pattern": "config",
  "mut_pattern": "config",
  "range_pattern": "config",
  "ref_pattern": "config",
  "captured_pattern": "config",
  "reference_pattern": "config",
  "or_pattern": "config",
  "negative_literal": "config",
  "integer_literal": "text",
  "string_literal": "children",
  "raw_string_literal": "config",
  "char_literal": "text",
  "escape_sequence": "text",
  "boolean_literal": "text",
  "comment": "children",
  "line_comment": "config",
  "block_comment": "config",
  "identifier": "text",
  "shebang": "text",
  "self": "text",
  "super": "text",
  "crate": "text",
  "metavariable": "text",
  "array_expression_semi": "config",
  "array_expression_list": "config",
  "string_content": "text",
  "raw_string_literal_content": "text",
  "float_literal": "text",
  "primitive_type": "text",
  "mod_item_inline": "config",
  "struct_item_brace": "config",
  "struct_item_tuple": "config",
  "range_expression_binary": "config",
  "range_expression_postfix": "config",
  "range_expression_prefix": "config",
  "range_expression_bare": "config",
  "let_chain": "children",
  "closure_expression_block": "config",
  "closure_expression_expr": "config",
  "field_pattern_shorthand": "config",
  "field_pattern_named": "config",
  "range_pattern_left": "config",
  "range_pattern_prefix": "config",
  "or_pattern_binary": "config",
  "or_pattern_prefix": "config",
  "outer_doc_comment_marker": "text",
  "inner_doc_comment_marker": "text",
  "type_identifier": "text",
  "field_identifier": "text",
  "shorthand_field_identifier": "text",
} as const satisfies Record<string, 'config' | 'children' | 'text'>;
export type _FactoryShapes = typeof _factoryShapes;

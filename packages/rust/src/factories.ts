// Auto-generated by @sittir/codegen — do not edit

import type { NodeData, Edit, ReplaceTarget, AssignableNode, FromFieldInfo, FromContext } from '@sittir/core';
import { render, toEdit, bindRange, resolveFromInput } from '@sittir/core';
import { rules } from './rules.js';
import { joinBy } from './joinby.js';

// .from() input types — kind constrained to valid grammar kinds
type FromKind = 'abstract_type' | 'arguments' | 'array_expression' | 'array_type' | 'assignment_expression' | 'associated_type' | 'async_block' | 'attribute' | 'attribute_item' | 'await_expression' | 'base_field_initializer' | 'binary_expression' | 'block' | 'block_comment' | 'bounded_type' | 'bracketed_type' | 'break_expression' | 'call_expression' | 'captured_pattern' | 'closure_expression' | 'closure_parameters' | 'compound_assignment_expr' | 'const_block' | 'const_item' | 'const_parameter' | 'continue_expression' | 'declaration_list' | 'dynamic_type' | 'else_clause' | 'enum_item' | 'enum_variant' | 'enum_variant_list' | 'expression_statement' | 'extern_crate_declaration' | 'extern_modifier' | 'field_declaration' | 'field_declaration_list' | 'field_expression' | 'field_initializer' | 'field_initializer_list' | 'field_pattern' | 'for_expression' | 'for_lifetimes' | 'foreign_mod_item' | 'function_item' | 'function_modifiers' | 'function_signature_item' | 'function_type' | 'gen_block' | 'generic_function' | 'generic_pattern' | 'generic_type' | 'generic_type_with_turbofish' | 'higher_ranked_trait_bound' | 'if_expression' | 'impl_item' | 'index_expression' | 'inner_attribute_item' | 'label' | 'let_chain' | 'let_condition' | 'let_declaration' | 'lifetime' | 'lifetime_parameter' | 'line_comment' | 'loop_expression' | 'macro_definition' | 'macro_invocation' | 'macro_rule' | 'match_arm' | 'match_block' | 'match_expression' | 'match_pattern' | 'mod_item' | 'mut_pattern' | 'negative_literal' | 'or_pattern' | 'ordered_field_declaration_list' | 'parameter' | 'parameters' | 'parenthesized_expression' | 'pointer_type' | 'qualified_type' | 'range_expression' | 'range_pattern' | 'raw_string_literal' | 'ref_pattern' | 'reference_expression' | 'reference_pattern' | 'reference_type' | 'removed_trait_bound' | 'return_expression' | 'scoped_identifier' | 'scoped_type_identifier' | 'scoped_use_list' | 'self_parameter' | 'shorthand_field_initializer' | 'slice_pattern' | 'source_file' | 'static_item' | 'string_literal' | 'struct_expression' | 'struct_item' | 'struct_pattern' | 'token_binding_pattern' | 'token_repetition' | 'token_repetition_pattern' | 'token_tree' | 'token_tree_pattern' | 'trait_bounds' | 'trait_item' | 'try_block' | 'try_expression' | 'tuple_expression' | 'tuple_pattern' | 'tuple_struct_pattern' | 'tuple_type' | 'type_arguments' | 'type_binding' | 'type_cast_expression' | 'type_item' | 'type_parameter' | 'type_parameters' | 'unary_expression' | 'union_item' | 'unsafe_block' | 'use_as_clause' | 'use_bounds' | 'use_declaration' | 'use_list' | 'use_wildcard' | 'variadic_parameter' | 'visibility_modifier' | 'where_clause' | 'where_predicate' | 'while_expression' | 'yield_expression' | 'boolean_literal' | 'empty_statement' | 'fragment_specifier' | 'inner_doc_comment_marker' | 'never_type' | 'outer_doc_comment_marker' | 'remaining_field_pattern' | 'unit_expression' | 'unit_type' | 'char_literal' | 'crate' | 'doc_comment' | 'escape_sequence' | 'field_identifier' | 'float_literal' | 'identifier' | 'integer_literal' | 'metavariable' | 'mutable_specifier' | 'primitive_type' | 'self' | 'shebang' | 'shorthand_field_identifier' | 'string_content' | 'super' | 'type_identifier';
interface FromObject { kind?: FromKind; [key: string]: FromValue | undefined; }
type FromValue = string | number | boolean | NodeData | FromValue[] | FromObject;

function isNodeData(v: any): v is NodeData {
  return v !== null && typeof v === 'object' && 'type' in v && 'fields' in v;
}

function resolveInput(v: any): NodeData {
  if (typeof v === 'string') return { type: 'string', fields: {}, text: v };
  if (typeof v === 'number') return { type: 'number', fields: {}, text: String(v) };
  return v;
}

const LEAF_PATTERNS: Record<string, RegExp> = {
  'char_literal': /^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/,
  'escape_sequence': /^\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})$/,
  'identifier': /^(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*$/u,
  'integer_literal': /^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/,
  'metavariable': /^\$[a-zA-Z_]\w*$/,
  'shebang': /^#![\r\f\t\v ]*([^\[\n].*)?\n$/,
};

function validateNodeText(node: NodeData): void {
  if (node.text === undefined) return;
  const pattern = LEAF_PATTERNS[node.type];
  if (pattern && !pattern.test(node.text)) {
    throw new Error(`Invalid ${node.type}: '${node.text}' does not match grammar pattern`);
  }
}

function resolveAndValidate(v: any): NodeData {
  const node = resolveInput(v);
  validateNodeText(node);
  return node;
}

const RESERVED_KEYWORDS = new Set([
  '_',
  'as',
  'async',
  'await',
  'break',
  'const',
  'continue',
  'default',
  'dyn',
  'else',
  'enum',
  'expr',
  'extern',
  'false',
  'fn',
  'for',
  'gen',
  'ident',
  'if',
  'impl',
  'in',
  'item',
  'let',
  'literal',
  'loop',
  'match',
  'meta',
  'mod',
  'move',
  'pat',
  'pat_param',
  'path',
  'pub',
  'raw',
  'ref',
  'return',
  'static',
  'stmt',
  'struct',
  'trait',
  'true',
  'try',
  'tt',
  'ty',
  'type',
  'union',
  'unsafe',
  'use',
  'vis',
  'where',
  'while',
  'yield',
]);

export interface AbstractTypeConfig {
  trait: NodeData;
  children?: NodeData[];
}

export interface AbstractTypeFromInput {
  trait: FromValue;
  children?: FromValue | FromValue[];
}

export type AbstractTypeNode = NodeData<'abstract_type'> & {
  children(...value: NodeData[]): AbstractTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'abstract_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function abstractType(
  traitOrConfig: NodeData | AbstractTypeConfig,
  config?: Partial<AbstractTypeConfig>,
): AbstractTypeNode {
  const fields: any = isNodeData(traitOrConfig) || typeof traitOrConfig === 'string'
    ? { 'trait': resolveAndValidate(traitOrConfig), ...config }
    : traitOrConfig;
  const node: any = { type: 'abstract_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

abstractType.assign = function(target: AssignableNode<'abstract_type'>): AbstractTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['trait'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'abstract_type'; }, get fields() { return getFields(); } };
  node.trait = (v: any) => { overrides['trait'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArgumentsConfig {
  children?: NodeData[];
}

export interface ArgumentsFromInput {
  children?: FromValue | FromValue[];
}

export type ArgumentsNode = NodeData<'arguments'> & {
  children(...value: NodeData[]): ArgumentsNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'arguments'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function arguments_(
  config?: ArgumentsConfig,
): ArgumentsNode {
  const fields: any = config ?? {};
  const node: any = { type: 'arguments', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

arguments_.assign = function(target: AssignableNode<'arguments'>): ArgumentsNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'arguments'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArrayExpressionConfig {
  length?: NodeData;
  children?: NodeData[];
}

export interface ArrayExpressionFromInput {
  length?: FromValue;
  children?: FromValue | FromValue[];
}

export type ArrayExpressionNode = NodeData<'array_expression'> & {
  length(value: NodeData): ArrayExpressionNode;
  children(...value: NodeData[]): ArrayExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'array_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function arrayExpression(
  config?: ArrayExpressionConfig,
): ArrayExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'array_expression', fields };
  node.length = (v: any) => { fields['length'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

arrayExpression.assign = function(target: AssignableNode<'array_expression'>): ArrayExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['length'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'array_expression'; }, get fields() { return getFields(); } };
  node.length = (v: any) => { overrides['length'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ArrayTypeConfig {
  element: NodeData;
  length?: NodeData;
}

export interface ArrayTypeFromInput {
  element: FromValue;
  length?: FromValue;
}

export type ArrayTypeNode = NodeData<'array_type'> & {
  length(value: NodeData): ArrayTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'array_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function arrayType(
  elementOrConfig: NodeData | ArrayTypeConfig,
  config?: Partial<ArrayTypeConfig>,
): ArrayTypeNode {
  const fields: any = isNodeData(elementOrConfig) || typeof elementOrConfig === 'string'
    ? { 'element': resolveAndValidate(elementOrConfig), ...config }
    : elementOrConfig;
  const node: any = { type: 'array_type', fields };
  node.length = (v: any) => { fields['length'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

arrayType.assign = function(target: AssignableNode<'array_type'>): ArrayTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['element', 'length'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'array_type'; }, get fields() { return getFields(); } };
  node.element = (v: any) => { overrides['element'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.length = (v: any) => { overrides['length'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AssignmentExpressionConfig {
  left: NodeData;
  right: NodeData;
}

export interface AssignmentExpressionFromInput {
  left: FromValue;
  right: FromValue;
}

export type AssignmentExpressionNode = NodeData<'assignment_expression'> & {
  right(value: NodeData): AssignmentExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'assignment_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function assignmentExpression(
  leftOrConfig: NodeData | AssignmentExpressionConfig,
  config?: Partial<AssignmentExpressionConfig>,
): AssignmentExpressionNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveAndValidate(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'assignment_expression', fields };
  node.right = (v: any) => { fields['right'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

assignmentExpression.assign = function(target: AssignableNode<'assignment_expression'>): AssignmentExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'assignment_expression'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.right = (v: any) => { overrides['right'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AssociatedTypeConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  bounds?: NodeData;
  children?: NodeData[];
}

export interface AssociatedTypeFromInput {
  name: NodeData | string;
  type_parameters?: FromValue;
  bounds?: FromValue;
  children?: FromValue | FromValue[];
}

export type AssociatedTypeNode = NodeData<'associated_type'> & {
  typeParameters(value: NodeData): AssociatedTypeNode;
  bounds(value: NodeData): AssociatedTypeNode;
  children(...value: NodeData[]): AssociatedTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'associated_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function associatedType(
  nameOrConfig: NodeData | string | AssociatedTypeConfig,
  config?: Partial<AssociatedTypeConfig>,
): AssociatedTypeNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'associated_type', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.bounds = (v: any) => { fields['bounds'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

associatedType.assign = function(target: AssignableNode<'associated_type'>): AssociatedTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'bounds'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'associated_type'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.bounds = (v: any) => { overrides['bounds'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AsyncBlockConfig {
  children?: NodeData[];
}

export interface AsyncBlockFromInput {
  children?: FromValue | FromValue[];
}

export type AsyncBlockNode = NodeData<'async_block'> & {
  children(...value: NodeData[]): AsyncBlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'async_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function asyncBlock(
  config?: AsyncBlockConfig,
): AsyncBlockNode {
  const fields: any = config ?? {};
  const node: any = { type: 'async_block', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

asyncBlock.assign = function(target: AssignableNode<'async_block'>): AsyncBlockNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'async_block'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AttributeConfig {
  value?: NodeData;
  arguments?: NodeData;
  children?: NodeData[];
}

export interface AttributeFromInput {
  value?: FromValue;
  arguments?: FromValue;
  children?: FromValue | FromValue[];
}

export type AttributeNode = NodeData<'attribute'> & {
  value(value: NodeData): AttributeNode;
  arguments(value: NodeData): AttributeNode;
  children(...value: NodeData[]): AttributeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'attribute'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function attribute(
  config?: AttributeConfig,
): AttributeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'attribute', fields };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.arguments = (v: any) => { fields['arguments'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

attribute.assign = function(target: AssignableNode<'attribute'>): AttributeNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'attribute'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.arguments = (v: any) => { overrides['arguments'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AttributeItemConfig {
  children?: NodeData[];
}

export interface AttributeItemFromInput {
  children?: FromValue | FromValue[];
}

export type AttributeItemNode = NodeData<'attribute_item'> & {
  children(...value: NodeData[]): AttributeItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'attribute_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function attributeItem(
  config?: AttributeItemConfig,
): AttributeItemNode {
  const fields: any = config ?? {};
  const node: any = { type: 'attribute_item', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

attributeItem.assign = function(target: AssignableNode<'attribute_item'>): AttributeItemNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'attribute_item'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface AwaitExpressionConfig {
  children?: NodeData[];
}

export interface AwaitExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type AwaitExpressionNode = NodeData<'await_expression'> & {
  children(...value: NodeData[]): AwaitExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'await_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function awaitExpression(
  config?: AwaitExpressionConfig,
): AwaitExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'await_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

awaitExpression.assign = function(target: AssignableNode<'await_expression'>): AwaitExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'await_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BaseFieldInitializerConfig {
  children?: NodeData[];
}

export interface BaseFieldInitializerFromInput {
  children?: FromValue | FromValue[];
}

export type BaseFieldInitializerNode = NodeData<'base_field_initializer'> & {
  children(...value: NodeData[]): BaseFieldInitializerNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'base_field_initializer'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function baseFieldInitializer(
  config?: BaseFieldInitializerConfig,
): BaseFieldInitializerNode {
  const fields: any = config ?? {};
  const node: any = { type: 'base_field_initializer', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

baseFieldInitializer.assign = function(target: AssignableNode<'base_field_initializer'>): BaseFieldInitializerNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'base_field_initializer'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BinaryExpressionConfig {
  left: NodeData;
  operator: NodeData;
  right: NodeData;
}

export interface BinaryExpressionFromInput {
  left: FromValue;
  operator: NodeData | '&&' | '||' | '&' | '|' | '^' | '==' | '!=' | '<' | '<=' | '>' | '>=' | '<<' | '>>' | '+' | '-' | '*' | '/' | '%';
  right: FromValue;
}

export type BinaryExpressionNode = NodeData<'binary_expression'> & {
  operator(value: NodeData): BinaryExpressionNode;
  right(value: NodeData): BinaryExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'binary_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function binaryExpression(
  leftOrConfig: NodeData | BinaryExpressionConfig,
  config?: Partial<BinaryExpressionConfig>,
): BinaryExpressionNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveAndValidate(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'binary_expression', fields };
  node.operator = (v: any) => { fields['operator'] = resolveAndValidate(v); return node; };
  node.right = (v: any) => { fields['right'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

binaryExpression.assign = function(target: AssignableNode<'binary_expression'>): BinaryExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'operator', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'binary_expression'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.operator = (v: any) => { overrides['operator'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.right = (v: any) => { overrides['right'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BlockConfig {
  children?: NodeData[];
}

export interface BlockFromInput {
  children?: FromValue | FromValue[];
}

export type BlockNode = NodeData<'block'> & {
  children(...value: NodeData[]): BlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function block(
  config?: BlockConfig,
): BlockNode {
  const fields: any = config ?? {};
  const node: any = { type: 'block', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

block.assign = function(target: AssignableNode<'block'>): BlockNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'block'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BlockCommentConfig {
  outer?: NodeData | string;
  inner?: NodeData | string;
  doc?: NodeData | string;
}

export interface BlockCommentFromInput {
  outer?: NodeData | string;
  inner?: NodeData | string;
  doc?: NodeData | string;
}

export type BlockCommentNode = NodeData<'block_comment'> & {
  outer(value: NodeData | string): BlockCommentNode;
  inner(value: NodeData | string): BlockCommentNode;
  doc(value: NodeData | string): BlockCommentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'block_comment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function blockComment(
  config?: BlockCommentConfig,
): BlockCommentNode {
  const fields: any = config ?? {};
  const node: any = { type: 'block_comment', fields };
  node.outer = (v: any) => { fields['outer'] = resolveAndValidate(v); return node; };
  node.inner = (v: any) => { fields['inner'] = resolveAndValidate(v); return node; };
  node.doc = (v: any) => { fields['doc'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

blockComment.assign = function(target: AssignableNode<'block_comment'>): BlockCommentNode {
  const overrides: any = {};
  const fieldList: string[] = ['outer', 'inner', 'doc'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'block_comment'; }, get fields() { return getFields(); } };
  node.outer = (v: any) => { overrides['outer'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.inner = (v: any) => { overrides['inner'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.doc = (v: any) => { overrides['doc'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BoundedTypeConfig {
  children?: NodeData[];
}

export interface BoundedTypeFromInput {
  children?: FromValue | FromValue[];
}

export type BoundedTypeNode = NodeData<'bounded_type'> & {
  children(...value: NodeData[]): BoundedTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'bounded_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function boundedType(
  config?: BoundedTypeConfig,
): BoundedTypeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'bounded_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

boundedType.assign = function(target: AssignableNode<'bounded_type'>): BoundedTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'bounded_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BracketedTypeConfig {
  children?: NodeData[];
}

export interface BracketedTypeFromInput {
  children?: FromValue | FromValue[];
}

export type BracketedTypeNode = NodeData<'bracketed_type'> & {
  children(...value: NodeData[]): BracketedTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'bracketed_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function bracketedType(
  config?: BracketedTypeConfig,
): BracketedTypeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'bracketed_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

bracketedType.assign = function(target: AssignableNode<'bracketed_type'>): BracketedTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'bracketed_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface BreakExpressionConfig {
  children?: NodeData[];
}

export interface BreakExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type BreakExpressionNode = NodeData<'break_expression'> & {
  children(...value: NodeData[]): BreakExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'break_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function breakExpression(
  config?: BreakExpressionConfig,
): BreakExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'break_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

breakExpression.assign = function(target: AssignableNode<'break_expression'>): BreakExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'break_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CallExpressionConfig {
  function: NodeData;
  arguments: NodeData;
}

export interface CallExpressionFromInput {
  function: FromValue;
  arguments: FromValue;
}

export type CallExpressionNode = NodeData<'call_expression'> & {
  arguments(value: NodeData): CallExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'call_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function callExpression(
  functionOrConfig: NodeData | CallExpressionConfig,
  config?: Partial<CallExpressionConfig>,
): CallExpressionNode {
  const fields: any = isNodeData(functionOrConfig) || typeof functionOrConfig === 'string'
    ? { 'function': resolveAndValidate(functionOrConfig), ...config }
    : functionOrConfig;
  const node: any = { type: 'call_expression', fields };
  node.arguments = (v: any) => { fields['arguments'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

callExpression.assign = function(target: AssignableNode<'call_expression'>): CallExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['function', 'arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'call_expression'; }, get fields() { return getFields(); } };
  node.function = (v: any) => { overrides['function'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.arguments = (v: any) => { overrides['arguments'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CapturedPatternConfig {
  children?: NodeData[];
}

export interface CapturedPatternFromInput {
  children?: FromValue | FromValue[];
}

export type CapturedPatternNode = NodeData<'captured_pattern'> & {
  children(...value: NodeData[]): CapturedPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'captured_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function capturedPattern(
  config?: CapturedPatternConfig,
): CapturedPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'captured_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

capturedPattern.assign = function(target: AssignableNode<'captured_pattern'>): CapturedPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'captured_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClosureExpressionConfig {
  parameters: NodeData;
  return_type?: NodeData;
  body: NodeData;
}

export interface ClosureExpressionFromInput {
  parameters: FromValue;
  return_type?: FromValue;
  body: FromValue;
}

export type ClosureExpressionNode = NodeData<'closure_expression'> & {
  returnType(value: NodeData): ClosureExpressionNode;
  body(value: NodeData): ClosureExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'closure_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function closureExpression(
  parametersOrConfig: NodeData | ClosureExpressionConfig,
  config?: Partial<ClosureExpressionConfig>,
): ClosureExpressionNode {
  const fields: any = isNodeData(parametersOrConfig) || typeof parametersOrConfig === 'string'
    ? { 'parameters': resolveAndValidate(parametersOrConfig), ...config }
    : parametersOrConfig;
  const node: any = { type: 'closure_expression', fields };
  node.returnType = (v: any) => { fields['return_type'] = resolveAndValidate(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

closureExpression.assign = function(target: AssignableNode<'closure_expression'>): ClosureExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'closure_expression'; }, get fields() { return getFields(); } };
  node.parameters = (v: any) => { overrides['parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.returnType = (v: any) => { overrides['return_type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ClosureParametersConfig {
  children?: NodeData[];
}

export interface ClosureParametersFromInput {
  children?: FromValue | FromValue[];
}

export type ClosureParametersNode = NodeData<'closure_parameters'> & {
  children(...value: NodeData[]): ClosureParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'closure_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function closureParameters(
  config?: ClosureParametersConfig,
): ClosureParametersNode {
  const fields: any = config ?? {};
  const node: any = { type: 'closure_parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

closureParameters.assign = function(target: AssignableNode<'closure_parameters'>): ClosureParametersNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'closure_parameters'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface CompoundAssignmentExprConfig {
  left: NodeData;
  operator: NodeData;
  right: NodeData;
}

export interface CompoundAssignmentExprFromInput {
  left: FromValue;
  operator: NodeData | '+=' | '-=' | '*=' | '/=' | '%=' | '&=' | '|=' | '^=' | '<<=' | '>>=';
  right: FromValue;
}

export type CompoundAssignmentExprNode = NodeData<'compound_assignment_expr'> & {
  operator(value: NodeData): CompoundAssignmentExprNode;
  right(value: NodeData): CompoundAssignmentExprNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'compound_assignment_expr'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function compoundAssignmentExpr(
  leftOrConfig: NodeData | CompoundAssignmentExprConfig,
  config?: Partial<CompoundAssignmentExprConfig>,
): CompoundAssignmentExprNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveAndValidate(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'compound_assignment_expr', fields };
  node.operator = (v: any) => { fields['operator'] = resolveAndValidate(v); return node; };
  node.right = (v: any) => { fields['right'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

compoundAssignmentExpr.assign = function(target: AssignableNode<'compound_assignment_expr'>): CompoundAssignmentExprNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'operator', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'compound_assignment_expr'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.operator = (v: any) => { overrides['operator'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.right = (v: any) => { overrides['right'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConstBlockConfig {
  body: NodeData;
}

export interface ConstBlockFromInput {
  body: FromValue;
}

export type ConstBlockNode = NodeData<'const_block'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'const_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function constBlock(
  bodyOrConfig: NodeData | ConstBlockConfig,
  config?: Partial<ConstBlockConfig>,
): ConstBlockNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveAndValidate(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'const_block', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

constBlock.assign = function(target: AssignableNode<'const_block'>): ConstBlockNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'const_block'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConstItemConfig {
  name: NodeData | string;
  type: NodeData;
  value?: NodeData;
  children?: NodeData[];
}

export interface ConstItemFromInput {
  name: NodeData | string;
  type: FromValue;
  value?: FromValue;
  children?: FromValue | FromValue[];
}

export type ConstItemNode = NodeData<'const_item'> & {
  typeField(value: NodeData): ConstItemNode;
  value(value: NodeData): ConstItemNode;
  children(...value: NodeData[]): ConstItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'const_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function constItem(
  nameOrConfig: NodeData | string | ConstItemConfig,
  config?: Partial<ConstItemConfig>,
): ConstItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'const_item', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

constItem.assign = function(target: AssignableNode<'const_item'>): ConstItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'const_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ConstParameterConfig {
  name: NodeData | string;
  type: NodeData;
  value?: NodeData;
}

export interface ConstParameterFromInput {
  name: NodeData | string;
  type: FromValue;
  value?: FromValue;
}

export type ConstParameterNode = NodeData<'const_parameter'> & {
  typeField(value: NodeData): ConstParameterNode;
  value(value: NodeData): ConstParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'const_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function constParameter(
  nameOrConfig: NodeData | string | ConstParameterConfig,
  config?: Partial<ConstParameterConfig>,
): ConstParameterNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'const_parameter', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

constParameter.assign = function(target: AssignableNode<'const_parameter'>): ConstParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'const_parameter'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ContinueExpressionConfig {
  children?: NodeData[];
}

export interface ContinueExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type ContinueExpressionNode = NodeData<'continue_expression'> & {
  children(...value: NodeData[]): ContinueExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'continue_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function continueExpression(
  config?: ContinueExpressionConfig,
): ContinueExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'continue_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

continueExpression.assign = function(target: AssignableNode<'continue_expression'>): ContinueExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'continue_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DeclarationListConfig {
  children?: NodeData[];
}

export interface DeclarationListFromInput {
  children?: FromValue | FromValue[];
}

export type DeclarationListNode = NodeData<'declaration_list'> & {
  children(...value: NodeData[]): DeclarationListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'declaration_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function declarationList(
  config?: DeclarationListConfig,
): DeclarationListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'declaration_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

declarationList.assign = function(target: AssignableNode<'declaration_list'>): DeclarationListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'declaration_list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface DynamicTypeConfig {
  trait: NodeData;
}

export interface DynamicTypeFromInput {
  trait: FromValue;
}

export type DynamicTypeNode = NodeData<'dynamic_type'> & {
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'dynamic_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function dynamicType(
  traitOrConfig: NodeData | DynamicTypeConfig,
  config?: Partial<DynamicTypeConfig>,
): DynamicTypeNode {
  const fields: any = isNodeData(traitOrConfig) || typeof traitOrConfig === 'string'
    ? { 'trait': resolveAndValidate(traitOrConfig), ...config }
    : traitOrConfig;
  const node: any = { type: 'dynamic_type', fields };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

dynamicType.assign = function(target: AssignableNode<'dynamic_type'>): DynamicTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['trait'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'dynamic_type'; }, get fields() { return getFields(); } };
  node.trait = (v: any) => { overrides['trait'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ElseClauseConfig {
  children?: NodeData[];
}

export interface ElseClauseFromInput {
  children?: FromValue | FromValue[];
}

export type ElseClauseNode = NodeData<'else_clause'> & {
  children(...value: NodeData[]): ElseClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'else_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function elseClause(
  config?: ElseClauseConfig,
): ElseClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'else_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

elseClause.assign = function(target: AssignableNode<'else_clause'>): ElseClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'else_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface EnumItemConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  body: NodeData;
  children?: NodeData[];
}

export interface EnumItemFromInput {
  name: NodeData | string;
  type_parameters?: FromValue;
  body: FromValue;
  children?: FromValue | FromValue[];
}

export type EnumItemNode = NodeData<'enum_item'> & {
  typeParameters(value: NodeData): EnumItemNode;
  body(value: NodeData): EnumItemNode;
  children(...value: NodeData[]): EnumItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function enumItem(
  nameOrConfig: NodeData | string | EnumItemConfig,
  config?: Partial<EnumItemConfig>,
): EnumItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'enum_item', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

enumItem.assign = function(target: AssignableNode<'enum_item'>): EnumItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'enum_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface EnumVariantConfig {
  name: NodeData | string;
  body?: NodeData;
  value?: NodeData;
  children?: NodeData[];
}

export interface EnumVariantFromInput {
  name: NodeData | string;
  body?: FromValue;
  value?: FromValue;
  children?: FromValue | FromValue[];
}

export type EnumVariantNode = NodeData<'enum_variant'> & {
  body(value: NodeData): EnumVariantNode;
  value(value: NodeData): EnumVariantNode;
  children(...value: NodeData[]): EnumVariantNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_variant'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function enumVariant(
  nameOrConfig: NodeData | string | EnumVariantConfig,
  config?: Partial<EnumVariantConfig>,
): EnumVariantNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'enum_variant', fields };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

enumVariant.assign = function(target: AssignableNode<'enum_variant'>): EnumVariantNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'body', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'enum_variant'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface EnumVariantListConfig {
  children?: NodeData[];
}

export interface EnumVariantListFromInput {
  children?: FromValue | FromValue[];
}

export type EnumVariantListNode = NodeData<'enum_variant_list'> & {
  children(...value: NodeData[]): EnumVariantListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'enum_variant_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function enumVariantList(
  config?: EnumVariantListConfig,
): EnumVariantListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'enum_variant_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

enumVariantList.assign = function(target: AssignableNode<'enum_variant_list'>): EnumVariantListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'enum_variant_list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExpressionStatementConfig {
  children?: NodeData[];
}

export interface ExpressionStatementFromInput {
  children?: FromValue | FromValue[];
}

export type ExpressionStatementNode = NodeData<'expression_statement'> & {
  children(...value: NodeData[]): ExpressionStatementNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'expression_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function expressionStatement(
  config?: ExpressionStatementConfig,
): ExpressionStatementNode {
  const fields: any = config ?? {};
  const node: any = { type: 'expression_statement', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

expressionStatement.assign = function(target: AssignableNode<'expression_statement'>): ExpressionStatementNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'expression_statement'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExternCrateDeclarationConfig {
  name: NodeData | string;
  alias?: NodeData | string;
  children?: NodeData[];
}

export interface ExternCrateDeclarationFromInput {
  name: NodeData | string;
  alias?: NodeData | string;
  children?: FromValue | FromValue[];
}

export type ExternCrateDeclarationNode = NodeData<'extern_crate_declaration'> & {
  alias(value: NodeData | string): ExternCrateDeclarationNode;
  children(...value: NodeData[]): ExternCrateDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'extern_crate_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function externCrateDeclaration(
  nameOrConfig: NodeData | string | ExternCrateDeclarationConfig,
  config?: Partial<ExternCrateDeclarationConfig>,
): ExternCrateDeclarationNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'extern_crate_declaration', fields };
  node.alias = (v: any) => { fields['alias'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

externCrateDeclaration.assign = function(target: AssignableNode<'extern_crate_declaration'>): ExternCrateDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'extern_crate_declaration'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.alias = (v: any) => { overrides['alias'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ExternModifierConfig {
  children?: NodeData[];
}

export interface ExternModifierFromInput {
  children?: FromValue | FromValue[];
}

export type ExternModifierNode = NodeData<'extern_modifier'> & {
  children(...value: NodeData[]): ExternModifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'extern_modifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function externModifier(
  config?: ExternModifierConfig,
): ExternModifierNode {
  const fields: any = config ?? {};
  const node: any = { type: 'extern_modifier', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

externModifier.assign = function(target: AssignableNode<'extern_modifier'>): ExternModifierNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'extern_modifier'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FieldDeclarationConfig {
  name: NodeData | string;
  type: NodeData;
  children?: NodeData[];
}

export interface FieldDeclarationFromInput {
  name: NodeData | string;
  type: FromValue;
  children?: FromValue | FromValue[];
}

export type FieldDeclarationNode = NodeData<'field_declaration'> & {
  typeField(value: NodeData): FieldDeclarationNode;
  children(...value: NodeData[]): FieldDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'field_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function fieldDeclaration(
  nameOrConfig: NodeData | string | FieldDeclarationConfig,
  config?: Partial<FieldDeclarationConfig>,
): FieldDeclarationNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'field_declaration', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

fieldDeclaration.assign = function(target: AssignableNode<'field_declaration'>): FieldDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'field_declaration'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FieldDeclarationListConfig {
  children?: NodeData[];
}

export interface FieldDeclarationListFromInput {
  children?: FromValue | FromValue[];
}

export type FieldDeclarationListNode = NodeData<'field_declaration_list'> & {
  children(...value: NodeData[]): FieldDeclarationListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'field_declaration_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function fieldDeclarationList(
  config?: FieldDeclarationListConfig,
): FieldDeclarationListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'field_declaration_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

fieldDeclarationList.assign = function(target: AssignableNode<'field_declaration_list'>): FieldDeclarationListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'field_declaration_list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FieldExpressionConfig {
  value: NodeData;
  field: NodeData | string;
}

export interface FieldExpressionFromInput {
  value: FromValue;
  field: NodeData | string | number;
}

export type FieldExpressionNode = NodeData<'field_expression'> & {
  field(value: NodeData | string): FieldExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'field_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function fieldExpression(
  valueOrConfig: NodeData | FieldExpressionConfig,
  config?: Partial<FieldExpressionConfig>,
): FieldExpressionNode {
  const fields: any = isNodeData(valueOrConfig) || typeof valueOrConfig === 'string'
    ? { 'value': resolveAndValidate(valueOrConfig), ...config }
    : valueOrConfig;
  const node: any = { type: 'field_expression', fields };
  node.field = (v: any) => { fields['field'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

fieldExpression.assign = function(target: AssignableNode<'field_expression'>): FieldExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'field'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'field_expression'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.field = (v: any) => { overrides['field'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FieldInitializerConfig {
  field: NodeData | string;
  value: NodeData;
  children?: NodeData[];
}

export interface FieldInitializerFromInput {
  field: NodeData | string | number;
  value: FromValue;
  children?: FromValue | FromValue[];
}

export type FieldInitializerNode = NodeData<'field_initializer'> & {
  value(value: NodeData): FieldInitializerNode;
  children(...value: NodeData[]): FieldInitializerNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'field_initializer'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function fieldInitializer(
  fieldOrConfig: NodeData | string | FieldInitializerConfig,
  config?: Partial<FieldInitializerConfig>,
): FieldInitializerNode {
  const fields: any = isNodeData(fieldOrConfig) || typeof fieldOrConfig === 'string'
    ? { 'field': resolveAndValidate(fieldOrConfig), ...config }
    : fieldOrConfig;
  const node: any = { type: 'field_initializer', fields };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

fieldInitializer.assign = function(target: AssignableNode<'field_initializer'>): FieldInitializerNode {
  const overrides: any = {};
  const fieldList: string[] = ['field', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'field_initializer'; }, get fields() { return getFields(); } };
  node.field = (v: any) => { overrides['field'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FieldInitializerListConfig {
  children?: NodeData[];
}

export interface FieldInitializerListFromInput {
  children?: FromValue | FromValue[];
}

export type FieldInitializerListNode = NodeData<'field_initializer_list'> & {
  children(...value: NodeData[]): FieldInitializerListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'field_initializer_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function fieldInitializerList(
  config?: FieldInitializerListConfig,
): FieldInitializerListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'field_initializer_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

fieldInitializerList.assign = function(target: AssignableNode<'field_initializer_list'>): FieldInitializerListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'field_initializer_list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FieldPatternConfig {
  name: NodeData | string;
  pattern?: NodeData;
  children?: NodeData[];
}

export interface FieldPatternFromInput {
  name: NodeData | string;
  pattern?: FromValue;
  children?: FromValue | FromValue[];
}

export type FieldPatternNode = NodeData<'field_pattern'> & {
  pattern(value: NodeData): FieldPatternNode;
  children(...value: NodeData[]): FieldPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'field_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function fieldPattern(
  nameOrConfig: NodeData | string | FieldPatternConfig,
  config?: Partial<FieldPatternConfig>,
): FieldPatternNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'field_pattern', fields };
  node.pattern = (v: any) => { fields['pattern'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

fieldPattern.assign = function(target: AssignableNode<'field_pattern'>): FieldPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'pattern'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'field_pattern'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.pattern = (v: any) => { overrides['pattern'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ForExpressionConfig {
  pattern: NodeData;
  value: NodeData;
  body: NodeData;
  children?: NodeData[];
}

export interface ForExpressionFromInput {
  pattern: FromValue;
  value: FromValue;
  body: FromValue;
  children?: FromValue | FromValue[];
}

export type ForExpressionNode = NodeData<'for_expression'> & {
  value(value: NodeData): ForExpressionNode;
  body(value: NodeData): ForExpressionNode;
  children(...value: NodeData[]): ForExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function forExpression(
  patternOrConfig: NodeData | ForExpressionConfig,
  config?: Partial<ForExpressionConfig>,
): ForExpressionNode {
  const fields: any = isNodeData(patternOrConfig) || typeof patternOrConfig === 'string'
    ? { 'pattern': resolveAndValidate(patternOrConfig), ...config }
    : patternOrConfig;
  const node: any = { type: 'for_expression', fields };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

forExpression.assign = function(target: AssignableNode<'for_expression'>): ForExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['pattern', 'value', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'for_expression'; }, get fields() { return getFields(); } };
  node.pattern = (v: any) => { overrides['pattern'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ForLifetimesConfig {
  children?: NodeData[];
}

export interface ForLifetimesFromInput {
  children?: FromValue | FromValue[];
}

export type ForLifetimesNode = NodeData<'for_lifetimes'> & {
  children(...value: NodeData[]): ForLifetimesNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'for_lifetimes'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function forLifetimes(
  config?: ForLifetimesConfig,
): ForLifetimesNode {
  const fields: any = config ?? {};
  const node: any = { type: 'for_lifetimes', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

forLifetimes.assign = function(target: AssignableNode<'for_lifetimes'>): ForLifetimesNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'for_lifetimes'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ForeignModItemConfig {
  body?: NodeData;
  children?: NodeData[];
}

export interface ForeignModItemFromInput {
  body?: FromValue;
  children?: FromValue | FromValue[];
}

export type ForeignModItemNode = NodeData<'foreign_mod_item'> & {
  body(value: NodeData): ForeignModItemNode;
  children(...value: NodeData[]): ForeignModItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'foreign_mod_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function foreignModItem(
  config?: ForeignModItemConfig,
): ForeignModItemNode {
  const fields: any = config ?? {};
  const node: any = { type: 'foreign_mod_item', fields };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

foreignModItem.assign = function(target: AssignableNode<'foreign_mod_item'>): ForeignModItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'foreign_mod_item'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionItemConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  parameters: NodeData;
  return_type?: NodeData;
  body: NodeData;
  children?: NodeData[];
}

export interface FunctionItemFromInput {
  name: NodeData | string;
  type_parameters?: FromValue;
  parameters: FromValue;
  return_type?: FromValue;
  body: FromValue;
  children?: FromValue | FromValue[];
}

export type FunctionItemNode = NodeData<'function_item'> & {
  typeParameters(value: NodeData): FunctionItemNode;
  parameters(value: NodeData): FunctionItemNode;
  returnType(value: NodeData): FunctionItemNode;
  body(value: NodeData): FunctionItemNode;
  children(...value: NodeData[]): FunctionItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionItem(
  nameOrConfig: NodeData | string | FunctionItemConfig,
  config?: Partial<FunctionItemConfig>,
): FunctionItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'function_item', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.parameters = (v: any) => { fields['parameters'] = resolveAndValidate(v); return node; };
  node.returnType = (v: any) => { fields['return_type'] = resolveAndValidate(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionItem.assign = function(target: AssignableNode<'function_item'>): FunctionItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'function_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.parameters = (v: any) => { overrides['parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.returnType = (v: any) => { overrides['return_type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionModifiersConfig {
  children?: NodeData[];
}

export interface FunctionModifiersFromInput {
  children?: FromValue | FromValue[];
}

export type FunctionModifiersNode = NodeData<'function_modifiers'> & {
  children(...value: NodeData[]): FunctionModifiersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_modifiers'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionModifiers(
  config?: FunctionModifiersConfig,
): FunctionModifiersNode {
  const fields: any = config ?? {};
  const node: any = { type: 'function_modifiers', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionModifiers.assign = function(target: AssignableNode<'function_modifiers'>): FunctionModifiersNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'function_modifiers'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionSignatureItemConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  parameters: NodeData;
  return_type?: NodeData;
  children?: NodeData[];
}

export interface FunctionSignatureItemFromInput {
  name: NodeData | string;
  type_parameters?: FromValue;
  parameters: FromValue;
  return_type?: FromValue;
  children?: FromValue | FromValue[];
}

export type FunctionSignatureItemNode = NodeData<'function_signature_item'> & {
  typeParameters(value: NodeData): FunctionSignatureItemNode;
  parameters(value: NodeData): FunctionSignatureItemNode;
  returnType(value: NodeData): FunctionSignatureItemNode;
  children(...value: NodeData[]): FunctionSignatureItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_signature_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionSignatureItem(
  nameOrConfig: NodeData | string | FunctionSignatureItemConfig,
  config?: Partial<FunctionSignatureItemConfig>,
): FunctionSignatureItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'function_signature_item', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.parameters = (v: any) => { fields['parameters'] = resolveAndValidate(v); return node; };
  node.returnType = (v: any) => { fields['return_type'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionSignatureItem.assign = function(target: AssignableNode<'function_signature_item'>): FunctionSignatureItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'parameters', 'return_type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'function_signature_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.parameters = (v: any) => { overrides['parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.returnType = (v: any) => { overrides['return_type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface FunctionTypeConfig {
  trait?: NodeData;
  parameters: NodeData;
  return_type?: NodeData;
  children?: NodeData[];
}

export interface FunctionTypeFromInput {
  trait?: FromValue;
  parameters: FromValue;
  return_type?: FromValue;
  children?: FromValue | FromValue[];
}

export type FunctionTypeNode = NodeData<'function_type'> & {
  trait(value: NodeData): FunctionTypeNode;
  returnType(value: NodeData): FunctionTypeNode;
  children(...value: NodeData[]): FunctionTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'function_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function functionType(
  parametersOrConfig: NodeData | FunctionTypeConfig,
  config?: Partial<FunctionTypeConfig>,
): FunctionTypeNode {
  const fields: any = isNodeData(parametersOrConfig) || typeof parametersOrConfig === 'string'
    ? { 'parameters': resolveAndValidate(parametersOrConfig), ...config }
    : parametersOrConfig;
  const node: any = { type: 'function_type', fields };
  node.trait = (v: any) => { fields['trait'] = resolveAndValidate(v); return node; };
  node.returnType = (v: any) => { fields['return_type'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

functionType.assign = function(target: AssignableNode<'function_type'>): FunctionTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['trait', 'parameters', 'return_type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'function_type'; }, get fields() { return getFields(); } };
  node.trait = (v: any) => { overrides['trait'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.parameters = (v: any) => { overrides['parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.returnType = (v: any) => { overrides['return_type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GenBlockConfig {
  children?: NodeData[];
}

export interface GenBlockFromInput {
  children?: FromValue | FromValue[];
}

export type GenBlockNode = NodeData<'gen_block'> & {
  children(...value: NodeData[]): GenBlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'gen_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function genBlock(
  config?: GenBlockConfig,
): GenBlockNode {
  const fields: any = config ?? {};
  const node: any = { type: 'gen_block', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

genBlock.assign = function(target: AssignableNode<'gen_block'>): GenBlockNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'gen_block'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GenericFunctionConfig {
  function: NodeData;
  type_arguments: NodeData;
}

export interface GenericFunctionFromInput {
  function: FromValue;
  type_arguments: FromValue;
}

export type GenericFunctionNode = NodeData<'generic_function'> & {
  typeArguments(value: NodeData): GenericFunctionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_function'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function genericFunction(
  functionOrConfig: NodeData | GenericFunctionConfig,
  config?: Partial<GenericFunctionConfig>,
): GenericFunctionNode {
  const fields: any = isNodeData(functionOrConfig) || typeof functionOrConfig === 'string'
    ? { 'function': resolveAndValidate(functionOrConfig), ...config }
    : functionOrConfig;
  const node: any = { type: 'generic_function', fields };
  node.typeArguments = (v: any) => { fields['type_arguments'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

genericFunction.assign = function(target: AssignableNode<'generic_function'>): GenericFunctionNode {
  const overrides: any = {};
  const fieldList: string[] = ['function', 'type_arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'generic_function'; }, get fields() { return getFields(); } };
  node.function = (v: any) => { overrides['function'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeArguments = (v: any) => { overrides['type_arguments'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GenericPatternConfig {
  type_arguments: NodeData;
  children?: NodeData[];
}

export interface GenericPatternFromInput {
  type_arguments: FromValue;
  children?: FromValue | FromValue[];
}

export type GenericPatternNode = NodeData<'generic_pattern'> & {
  children(...value: NodeData[]): GenericPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function genericPattern(
  typeArgumentsOrConfig: NodeData | GenericPatternConfig,
  config?: Partial<GenericPatternConfig>,
): GenericPatternNode {
  const fields: any = isNodeData(typeArgumentsOrConfig) || typeof typeArgumentsOrConfig === 'string'
    ? { 'type_arguments': resolveAndValidate(typeArgumentsOrConfig), ...config }
    : typeArgumentsOrConfig;
  const node: any = { type: 'generic_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

genericPattern.assign = function(target: AssignableNode<'generic_pattern'>): GenericPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['type_arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'generic_pattern'; }, get fields() { return getFields(); } };
  node.typeArguments = (v: any) => { overrides['type_arguments'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GenericTypeConfig {
  type: NodeData;
  type_arguments: NodeData;
}

export interface GenericTypeFromInput {
  type: FromValue;
  type_arguments: FromValue;
}

export type GenericTypeNode = NodeData<'generic_type'> & {
  typeArguments(value: NodeData): GenericTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function genericType(
  typeOrConfig: NodeData | GenericTypeConfig,
  config?: Partial<GenericTypeConfig>,
): GenericTypeNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveAndValidate(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'generic_type', fields };
  node.typeArguments = (v: any) => { fields['type_arguments'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

genericType.assign = function(target: AssignableNode<'generic_type'>): GenericTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['type', 'type_arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'generic_type'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeArguments = (v: any) => { overrides['type_arguments'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface GenericTypeWithTurbofishConfig {
  type: NodeData;
  type_arguments: NodeData;
}

export interface GenericTypeWithTurbofishFromInput {
  type: FromValue;
  type_arguments: FromValue;
}

export type GenericTypeWithTurbofishNode = NodeData<'generic_type_with_turbofish'> & {
  typeArguments(value: NodeData): GenericTypeWithTurbofishNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'generic_type_with_turbofish'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function genericTypeWithTurbofish(
  typeOrConfig: NodeData | GenericTypeWithTurbofishConfig,
  config?: Partial<GenericTypeWithTurbofishConfig>,
): GenericTypeWithTurbofishNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveAndValidate(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'generic_type_with_turbofish', fields };
  node.typeArguments = (v: any) => { fields['type_arguments'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

genericTypeWithTurbofish.assign = function(target: AssignableNode<'generic_type_with_turbofish'>): GenericTypeWithTurbofishNode {
  const overrides: any = {};
  const fieldList: string[] = ['type', 'type_arguments'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'generic_type_with_turbofish'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeArguments = (v: any) => { overrides['type_arguments'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface HigherRankedTraitBoundConfig {
  type_parameters: NodeData;
  type: NodeData;
}

export interface HigherRankedTraitBoundFromInput {
  type_parameters: FromValue;
  type: FromValue;
}

export type HigherRankedTraitBoundNode = NodeData<'higher_ranked_trait_bound'> & {
  typeField(value: NodeData): HigherRankedTraitBoundNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'higher_ranked_trait_bound'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function higherRankedTraitBound(
  typeParametersOrConfig: NodeData | HigherRankedTraitBoundConfig,
  config?: Partial<HigherRankedTraitBoundConfig>,
): HigherRankedTraitBoundNode {
  const fields: any = isNodeData(typeParametersOrConfig) || typeof typeParametersOrConfig === 'string'
    ? { 'type_parameters': resolveAndValidate(typeParametersOrConfig), ...config }
    : typeParametersOrConfig;
  const node: any = { type: 'higher_ranked_trait_bound', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

higherRankedTraitBound.assign = function(target: AssignableNode<'higher_ranked_trait_bound'>): HigherRankedTraitBoundNode {
  const overrides: any = {};
  const fieldList: string[] = ['type_parameters', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'higher_ranked_trait_bound'; }, get fields() { return getFields(); } };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface IfExpressionConfig {
  condition: NodeData;
  consequence: NodeData;
  alternative?: NodeData;
}

export interface IfExpressionFromInput {
  condition: FromValue;
  consequence: FromValue;
  alternative?: FromValue;
}

export type IfExpressionNode = NodeData<'if_expression'> & {
  consequence(value: NodeData): IfExpressionNode;
  alternative(value: NodeData): IfExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'if_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function ifExpression(
  conditionOrConfig: NodeData | IfExpressionConfig,
  config?: Partial<IfExpressionConfig>,
): IfExpressionNode {
  const fields: any = isNodeData(conditionOrConfig) || typeof conditionOrConfig === 'string'
    ? { 'condition': resolveAndValidate(conditionOrConfig), ...config }
    : conditionOrConfig;
  const node: any = { type: 'if_expression', fields };
  node.consequence = (v: any) => { fields['consequence'] = resolveAndValidate(v); return node; };
  node.alternative = (v: any) => { fields['alternative'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

ifExpression.assign = function(target: AssignableNode<'if_expression'>): IfExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition', 'consequence', 'alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'if_expression'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.consequence = (v: any) => { overrides['consequence'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.alternative = (v: any) => { overrides['alternative'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ImplItemConfig {
  type_parameters?: NodeData;
  trait?: NodeData;
  type: NodeData;
  body?: NodeData;
  children?: NodeData[];
}

export interface ImplItemFromInput {
  type_parameters?: FromValue;
  trait?: FromValue;
  type: FromValue;
  body?: FromValue;
  children?: FromValue | FromValue[];
}

export type ImplItemNode = NodeData<'impl_item'> & {
  typeParameters(value: NodeData): ImplItemNode;
  trait(value: NodeData): ImplItemNode;
  body(value: NodeData): ImplItemNode;
  children(...value: NodeData[]): ImplItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'impl_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function implItem(
  typeOrConfig: NodeData | ImplItemConfig,
  config?: Partial<ImplItemConfig>,
): ImplItemNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveAndValidate(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'impl_item', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.trait = (v: any) => { fields['trait'] = resolveAndValidate(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

implItem.assign = function(target: AssignableNode<'impl_item'>): ImplItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['type_parameters', 'trait', 'type', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'impl_item'; }, get fields() { return getFields(); } };
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.trait = (v: any) => { overrides['trait'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface IndexExpressionConfig {
  children?: NodeData[];
}

export interface IndexExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type IndexExpressionNode = NodeData<'index_expression'> & {
  children(...value: NodeData[]): IndexExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'index_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function indexExpression(
  config?: IndexExpressionConfig,
): IndexExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'index_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

indexExpression.assign = function(target: AssignableNode<'index_expression'>): IndexExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'index_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface InnerAttributeItemConfig {
  children?: NodeData[];
}

export interface InnerAttributeItemFromInput {
  children?: FromValue | FromValue[];
}

export type InnerAttributeItemNode = NodeData<'inner_attribute_item'> & {
  children(...value: NodeData[]): InnerAttributeItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'inner_attribute_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function innerAttributeItem(
  config?: InnerAttributeItemConfig,
): InnerAttributeItemNode {
  const fields: any = config ?? {};
  const node: any = { type: 'inner_attribute_item', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

innerAttributeItem.assign = function(target: AssignableNode<'inner_attribute_item'>): InnerAttributeItemNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'inner_attribute_item'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LabelConfig {
  children?: NodeData[];
}

export interface LabelFromInput {
  children?: FromValue | FromValue[];
}

export type LabelNode = NodeData<'label'> & {
  children(...value: NodeData[]): LabelNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'label'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function label(
  childrenOrConfig?: string | NodeData | NodeData[] | LabelConfig,
): LabelNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'identifier', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'label', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

label.assign = function(target: AssignableNode<'label'>): LabelNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'label'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LetChainConfig {
  children?: NodeData[];
}

export interface LetChainFromInput {
  children?: FromValue | FromValue[];
}

export type LetChainNode = NodeData<'let_chain'> & {
  children(...value: NodeData[]): LetChainNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'let_chain'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function letChain(
  config?: LetChainConfig,
): LetChainNode {
  const fields: any = config ?? {};
  const node: any = { type: 'let_chain', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

letChain.assign = function(target: AssignableNode<'let_chain'>): LetChainNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'let_chain'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LetConditionConfig {
  pattern: NodeData;
  value: NodeData;
}

export interface LetConditionFromInput {
  pattern: FromValue;
  value: FromValue;
}

export type LetConditionNode = NodeData<'let_condition'> & {
  value(value: NodeData): LetConditionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'let_condition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function letCondition(
  patternOrConfig: NodeData | LetConditionConfig,
  config?: Partial<LetConditionConfig>,
): LetConditionNode {
  const fields: any = isNodeData(patternOrConfig) || typeof patternOrConfig === 'string'
    ? { 'pattern': resolveAndValidate(patternOrConfig), ...config }
    : patternOrConfig;
  const node: any = { type: 'let_condition', fields };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

letCondition.assign = function(target: AssignableNode<'let_condition'>): LetConditionNode {
  const overrides: any = {};
  const fieldList: string[] = ['pattern', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'let_condition'; }, get fields() { return getFields(); } };
  node.pattern = (v: any) => { overrides['pattern'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LetDeclarationConfig {
  pattern: NodeData;
  type?: NodeData;
  value?: NodeData;
  alternative?: NodeData;
  children?: NodeData[];
}

export interface LetDeclarationFromInput {
  pattern: FromValue;
  type?: FromValue;
  value?: FromValue;
  alternative?: FromValue;
  children?: FromValue | FromValue[];
}

export type LetDeclarationNode = NodeData<'let_declaration'> & {
  typeField(value: NodeData): LetDeclarationNode;
  value(value: NodeData): LetDeclarationNode;
  alternative(value: NodeData): LetDeclarationNode;
  children(...value: NodeData[]): LetDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'let_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function letDeclaration(
  patternOrConfig: NodeData | LetDeclarationConfig,
  config?: Partial<LetDeclarationConfig>,
): LetDeclarationNode {
  const fields: any = isNodeData(patternOrConfig) || typeof patternOrConfig === 'string'
    ? { 'pattern': resolveAndValidate(patternOrConfig), ...config }
    : patternOrConfig;
  const node: any = { type: 'let_declaration', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.alternative = (v: any) => { fields['alternative'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

letDeclaration.assign = function(target: AssignableNode<'let_declaration'>): LetDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['pattern', 'type', 'value', 'alternative'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'let_declaration'; }, get fields() { return getFields(); } };
  node.pattern = (v: any) => { overrides['pattern'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.alternative = (v: any) => { overrides['alternative'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LifetimeConfig {
  children?: NodeData[];
}

export interface LifetimeFromInput {
  children?: FromValue | FromValue[];
}

export type LifetimeNode = NodeData<'lifetime'> & {
  children(...value: NodeData[]): LifetimeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lifetime'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lifetime(
  childrenOrConfig?: string | NodeData | NodeData[] | LifetimeConfig,
): LifetimeNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'identifier', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'lifetime', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

lifetime.assign = function(target: AssignableNode<'lifetime'>): LifetimeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'lifetime'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LifetimeParameterConfig {
  name: NodeData;
  bounds?: NodeData;
}

export interface LifetimeParameterFromInput {
  name: FromValue;
  bounds?: FromValue;
}

export type LifetimeParameterNode = NodeData<'lifetime_parameter'> & {
  bounds(value: NodeData): LifetimeParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'lifetime_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lifetimeParameter(
  nameOrConfig: NodeData | LifetimeParameterConfig,
  config?: Partial<LifetimeParameterConfig>,
): LifetimeParameterNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'lifetime_parameter', fields };
  node.bounds = (v: any) => { fields['bounds'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

lifetimeParameter.assign = function(target: AssignableNode<'lifetime_parameter'>): LifetimeParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'bounds'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'lifetime_parameter'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.bounds = (v: any) => { overrides['bounds'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LineCommentConfig {
  outer?: NodeData | string;
  inner?: NodeData | string;
  doc?: NodeData | string;
}

export interface LineCommentFromInput {
  outer?: NodeData | string;
  inner?: NodeData | string;
  doc?: NodeData | string;
}

export type LineCommentNode = NodeData<'line_comment'> & {
  outer(value: NodeData | string): LineCommentNode;
  inner(value: NodeData | string): LineCommentNode;
  doc(value: NodeData | string): LineCommentNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'line_comment'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function lineComment(
  config?: LineCommentConfig,
): LineCommentNode {
  const fields: any = config ?? {};
  const node: any = { type: 'line_comment', fields };
  node.outer = (v: any) => { fields['outer'] = resolveAndValidate(v); return node; };
  node.inner = (v: any) => { fields['inner'] = resolveAndValidate(v); return node; };
  node.doc = (v: any) => { fields['doc'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

lineComment.assign = function(target: AssignableNode<'line_comment'>): LineCommentNode {
  const overrides: any = {};
  const fieldList: string[] = ['outer', 'inner', 'doc'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'line_comment'; }, get fields() { return getFields(); } };
  node.outer = (v: any) => { overrides['outer'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.inner = (v: any) => { overrides['inner'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.doc = (v: any) => { overrides['doc'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface LoopExpressionConfig {
  body: NodeData;
  children?: NodeData[];
}

export interface LoopExpressionFromInput {
  body: FromValue;
  children?: FromValue | FromValue[];
}

export type LoopExpressionNode = NodeData<'loop_expression'> & {
  children(...value: NodeData[]): LoopExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'loop_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function loopExpression(
  bodyOrConfig: NodeData | LoopExpressionConfig,
  config?: Partial<LoopExpressionConfig>,
): LoopExpressionNode {
  const fields: any = isNodeData(bodyOrConfig) || typeof bodyOrConfig === 'string'
    ? { 'body': resolveAndValidate(bodyOrConfig), ...config }
    : bodyOrConfig;
  const node: any = { type: 'loop_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

loopExpression.assign = function(target: AssignableNode<'loop_expression'>): LoopExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'loop_expression'; }, get fields() { return getFields(); } };
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MacroDefinitionConfig {
  name: NodeData | string;
  children?: NodeData[];
}

export interface MacroDefinitionFromInput {
  name: NodeData | string;
  children?: FromValue | FromValue[];
}

export type MacroDefinitionNode = NodeData<'macro_definition'> & {
  children(...value: NodeData[]): MacroDefinitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'macro_definition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function macroDefinition(
  nameOrConfig: NodeData | string | MacroDefinitionConfig,
  config?: Partial<MacroDefinitionConfig>,
): MacroDefinitionNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'macro_definition', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

macroDefinition.assign = function(target: AssignableNode<'macro_definition'>): MacroDefinitionNode {
  const overrides: any = {};
  const fieldList: string[] = ['name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'macro_definition'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MacroInvocationConfig {
  macro: NodeData;
  children?: NodeData[];
}

export interface MacroInvocationFromInput {
  macro: FromValue;
  children?: FromValue | FromValue[];
}

export type MacroInvocationNode = NodeData<'macro_invocation'> & {
  children(...value: NodeData[]): MacroInvocationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'macro_invocation'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function macroInvocation(
  macroOrConfig: NodeData | MacroInvocationConfig,
  config?: Partial<MacroInvocationConfig>,
): MacroInvocationNode {
  const fields: any = isNodeData(macroOrConfig) || typeof macroOrConfig === 'string'
    ? { 'macro': resolveAndValidate(macroOrConfig), ...config }
    : macroOrConfig;
  const node: any = { type: 'macro_invocation', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

macroInvocation.assign = function(target: AssignableNode<'macro_invocation'>): MacroInvocationNode {
  const overrides: any = {};
  const fieldList: string[] = ['macro'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'macro_invocation'; }, get fields() { return getFields(); } };
  node.macro = (v: any) => { overrides['macro'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MacroRuleConfig {
  left: NodeData;
  right: NodeData;
}

export interface MacroRuleFromInput {
  left: FromValue;
  right: FromValue;
}

export type MacroRuleNode = NodeData<'macro_rule'> & {
  right(value: NodeData): MacroRuleNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'macro_rule'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function macroRule(
  leftOrConfig: NodeData | MacroRuleConfig,
  config?: Partial<MacroRuleConfig>,
): MacroRuleNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveAndValidate(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'macro_rule', fields };
  node.right = (v: any) => { fields['right'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

macroRule.assign = function(target: AssignableNode<'macro_rule'>): MacroRuleNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'macro_rule'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.right = (v: any) => { overrides['right'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MatchArmConfig {
  pattern: NodeData;
  value: NodeData;
  children?: NodeData[];
}

export interface MatchArmFromInput {
  pattern: FromValue;
  value: FromValue;
  children?: FromValue | FromValue[];
}

export type MatchArmNode = NodeData<'match_arm'> & {
  value(value: NodeData): MatchArmNode;
  children(...value: NodeData[]): MatchArmNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'match_arm'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function matchArm(
  patternOrConfig: NodeData | MatchArmConfig,
  config?: Partial<MatchArmConfig>,
): MatchArmNode {
  const fields: any = isNodeData(patternOrConfig) || typeof patternOrConfig === 'string'
    ? { 'pattern': resolveAndValidate(patternOrConfig), ...config }
    : patternOrConfig;
  const node: any = { type: 'match_arm', fields };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

matchArm.assign = function(target: AssignableNode<'match_arm'>): MatchArmNode {
  const overrides: any = {};
  const fieldList: string[] = ['pattern', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'match_arm'; }, get fields() { return getFields(); } };
  node.pattern = (v: any) => { overrides['pattern'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MatchBlockConfig {
  children?: NodeData[];
}

export interface MatchBlockFromInput {
  children?: FromValue | FromValue[];
}

export type MatchBlockNode = NodeData<'match_block'> & {
  children(...value: NodeData[]): MatchBlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'match_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function matchBlock(
  config?: MatchBlockConfig,
): MatchBlockNode {
  const fields: any = config ?? {};
  const node: any = { type: 'match_block', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

matchBlock.assign = function(target: AssignableNode<'match_block'>): MatchBlockNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'match_block'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MatchExpressionConfig {
  value: NodeData;
  body: NodeData;
}

export interface MatchExpressionFromInput {
  value: FromValue;
  body: FromValue;
}

export type MatchExpressionNode = NodeData<'match_expression'> & {
  body(value: NodeData): MatchExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'match_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function matchExpression(
  valueOrConfig: NodeData | MatchExpressionConfig,
  config?: Partial<MatchExpressionConfig>,
): MatchExpressionNode {
  const fields: any = isNodeData(valueOrConfig) || typeof valueOrConfig === 'string'
    ? { 'value': resolveAndValidate(valueOrConfig), ...config }
    : valueOrConfig;
  const node: any = { type: 'match_expression', fields };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

matchExpression.assign = function(target: AssignableNode<'match_expression'>): MatchExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'match_expression'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MatchPatternConfig {
  condition?: NodeData;
  children?: NodeData[];
}

export interface MatchPatternFromInput {
  condition?: FromValue;
  children?: FromValue | FromValue[];
}

export type MatchPatternNode = NodeData<'match_pattern'> & {
  condition(value: NodeData): MatchPatternNode;
  children(...value: NodeData[]): MatchPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'match_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function matchPattern(
  config?: MatchPatternConfig,
): MatchPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'match_pattern', fields };
  node.condition = (v: any) => { fields['condition'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

matchPattern.assign = function(target: AssignableNode<'match_pattern'>): MatchPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'match_pattern'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ModItemConfig {
  name: NodeData | string;
  body?: NodeData;
  children?: NodeData[];
}

export interface ModItemFromInput {
  name: NodeData | string;
  body?: FromValue;
  children?: FromValue | FromValue[];
}

export type ModItemNode = NodeData<'mod_item'> & {
  body(value: NodeData): ModItemNode;
  children(...value: NodeData[]): ModItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'mod_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function modItem(
  nameOrConfig: NodeData | string | ModItemConfig,
  config?: Partial<ModItemConfig>,
): ModItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'mod_item', fields };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

modItem.assign = function(target: AssignableNode<'mod_item'>): ModItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'mod_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface MutPatternConfig {
  children?: NodeData[];
}

export interface MutPatternFromInput {
  children?: FromValue | FromValue[];
}

export type MutPatternNode = NodeData<'mut_pattern'> & {
  children(...value: NodeData[]): MutPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'mut_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function mutPattern(
  config?: MutPatternConfig,
): MutPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'mut_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

mutPattern.assign = function(target: AssignableNode<'mut_pattern'>): MutPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'mut_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface NegativeLiteralConfig {
  children?: NodeData[];
}

export interface NegativeLiteralFromInput {
  children?: FromValue | FromValue[];
}

export type NegativeLiteralNode = NodeData<'negative_literal'> & {
  children(...value: NodeData[]): NegativeLiteralNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'negative_literal'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function negativeLiteral(
  childrenOrConfig?: string | NodeData | NodeData[] | NegativeLiteralConfig,
): NegativeLiteralNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'integer_literal', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'negative_literal', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

negativeLiteral.assign = function(target: AssignableNode<'negative_literal'>): NegativeLiteralNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'negative_literal'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface OrPatternConfig {
  children?: NodeData[];
}

export interface OrPatternFromInput {
  children?: FromValue | FromValue[];
}

export type OrPatternNode = NodeData<'or_pattern'> & {
  children(...value: NodeData[]): OrPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'or_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function orPattern(
  config?: OrPatternConfig,
): OrPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'or_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

orPattern.assign = function(target: AssignableNode<'or_pattern'>): OrPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'or_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface OrderedFieldDeclarationListConfig {
  type?: NodeData[];
  children?: NodeData[];
}

export interface OrderedFieldDeclarationListFromInput {
  type?: FromValue[];
  children?: FromValue | FromValue[];
}

export type OrderedFieldDeclarationListNode = NodeData<'ordered_field_declaration_list'> & {
  typeField(value: NodeData[]): OrderedFieldDeclarationListNode;
  children(...value: NodeData[]): OrderedFieldDeclarationListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'ordered_field_declaration_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function orderedFieldDeclarationList(
  config?: OrderedFieldDeclarationListConfig,
): OrderedFieldDeclarationListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'ordered_field_declaration_list', fields };
  node.typeField = (...v: any[]) => { fields['type'] = v; return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

orderedFieldDeclarationList.assign = function(target: AssignableNode<'ordered_field_declaration_list'>): OrderedFieldDeclarationListNode {
  const overrides: any = {};
  const fieldList: string[] = ['type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'ordered_field_declaration_list'; }, get fields() { return getFields(); } };
  node.typeField = (...v: any[]) => { overrides['type'] = v; return node; };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ParameterConfig {
  pattern: NodeData;
  type: NodeData;
  children?: NodeData[];
}

export interface ParameterFromInput {
  pattern: FromValue;
  type: FromValue;
  children?: FromValue | FromValue[];
}

export type ParameterNode = NodeData<'parameter'> & {
  typeField(value: NodeData): ParameterNode;
  children(...value: NodeData[]): ParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parameter(
  patternOrConfig: NodeData | ParameterConfig,
  config?: Partial<ParameterConfig>,
): ParameterNode {
  const fields: any = isNodeData(patternOrConfig) || typeof patternOrConfig === 'string'
    ? { 'pattern': resolveAndValidate(patternOrConfig), ...config }
    : patternOrConfig;
  const node: any = { type: 'parameter', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

parameter.assign = function(target: AssignableNode<'parameter'>): ParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['pattern', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'parameter'; }, get fields() { return getFields(); } };
  node.pattern = (v: any) => { overrides['pattern'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ParametersConfig {
  children?: NodeData[];
}

export interface ParametersFromInput {
  children?: FromValue | FromValue[];
}

export type ParametersNode = NodeData<'parameters'> & {
  children(...value: NodeData[]): ParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parameters(
  config?: ParametersConfig,
): ParametersNode {
  const fields: any = config ?? {};
  const node: any = { type: 'parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

parameters.assign = function(target: AssignableNode<'parameters'>): ParametersNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'parameters'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ParenthesizedExpressionConfig {
  children?: NodeData[];
}

export interface ParenthesizedExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type ParenthesizedExpressionNode = NodeData<'parenthesized_expression'> & {
  children(...value: NodeData[]): ParenthesizedExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'parenthesized_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function parenthesizedExpression(
  config?: ParenthesizedExpressionConfig,
): ParenthesizedExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'parenthesized_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

parenthesizedExpression.assign = function(target: AssignableNode<'parenthesized_expression'>): ParenthesizedExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'parenthesized_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface PointerTypeConfig {
  type: NodeData;
  children?: NodeData[];
}

export interface PointerTypeFromInput {
  type: FromValue;
  children?: FromValue | FromValue[];
}

export type PointerTypeNode = NodeData<'pointer_type'> & {
  children(...value: NodeData[]): PointerTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'pointer_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function pointerType(
  typeOrConfig: NodeData | PointerTypeConfig,
  config?: Partial<PointerTypeConfig>,
): PointerTypeNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveAndValidate(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'pointer_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

pointerType.assign = function(target: AssignableNode<'pointer_type'>): PointerTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'pointer_type'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface QualifiedTypeConfig {
  type: NodeData;
  alias: NodeData;
}

export interface QualifiedTypeFromInput {
  type: FromValue;
  alias: FromValue;
}

export type QualifiedTypeNode = NodeData<'qualified_type'> & {
  alias(value: NodeData): QualifiedTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'qualified_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function qualifiedType(
  typeOrConfig: NodeData | QualifiedTypeConfig,
  config?: Partial<QualifiedTypeConfig>,
): QualifiedTypeNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveAndValidate(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'qualified_type', fields };
  node.alias = (v: any) => { fields['alias'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

qualifiedType.assign = function(target: AssignableNode<'qualified_type'>): QualifiedTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['type', 'alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'qualified_type'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.alias = (v: any) => { overrides['alias'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RangeExpressionConfig {
  children?: NodeData[];
}

export interface RangeExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type RangeExpressionNode = NodeData<'range_expression'> & {
  children(...value: NodeData[]): RangeExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'range_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function rangeExpression(
  config?: RangeExpressionConfig,
): RangeExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'range_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

rangeExpression.assign = function(target: AssignableNode<'range_expression'>): RangeExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'range_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RangePatternConfig {
  left?: NodeData;
  right?: NodeData;
}

export interface RangePatternFromInput {
  left?: FromValue;
  right?: FromValue;
}

export type RangePatternNode = NodeData<'range_pattern'> & {
  left(value: NodeData): RangePatternNode;
  right(value: NodeData): RangePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'range_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function rangePattern(
  config?: RangePatternConfig,
): RangePatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'range_pattern', fields };
  node.left = (v: any) => { fields['left'] = resolveAndValidate(v); return node; };
  node.right = (v: any) => { fields['right'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

rangePattern.assign = function(target: AssignableNode<'range_pattern'>): RangePatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'right'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'range_pattern'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.right = (v: any) => { overrides['right'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RawStringLiteralConfig {
  children?: NodeData[];
}

export interface RawStringLiteralFromInput {
  children?: FromValue | FromValue[];
}

export type RawStringLiteralNode = NodeData<'raw_string_literal'> & {
  children(...value: NodeData[]): RawStringLiteralNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'raw_string_literal'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function rawStringLiteral(
  childrenOrConfig?: string | NodeData | NodeData[] | RawStringLiteralConfig,
): RawStringLiteralNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'string_content', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'raw_string_literal', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

rawStringLiteral.assign = function(target: AssignableNode<'raw_string_literal'>): RawStringLiteralNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'raw_string_literal'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RefPatternConfig {
  children?: NodeData[];
}

export interface RefPatternFromInput {
  children?: FromValue | FromValue[];
}

export type RefPatternNode = NodeData<'ref_pattern'> & {
  children(...value: NodeData[]): RefPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'ref_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function refPattern(
  config?: RefPatternConfig,
): RefPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'ref_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

refPattern.assign = function(target: AssignableNode<'ref_pattern'>): RefPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'ref_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ReferenceExpressionConfig {
  value: NodeData;
  children?: NodeData[];
}

export interface ReferenceExpressionFromInput {
  value: FromValue;
  children?: FromValue | FromValue[];
}

export type ReferenceExpressionNode = NodeData<'reference_expression'> & {
  children(...value: NodeData[]): ReferenceExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'reference_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function referenceExpression(
  valueOrConfig: NodeData | ReferenceExpressionConfig,
  config?: Partial<ReferenceExpressionConfig>,
): ReferenceExpressionNode {
  const fields: any = isNodeData(valueOrConfig) || typeof valueOrConfig === 'string'
    ? { 'value': resolveAndValidate(valueOrConfig), ...config }
    : valueOrConfig;
  const node: any = { type: 'reference_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

referenceExpression.assign = function(target: AssignableNode<'reference_expression'>): ReferenceExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'reference_expression'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ReferencePatternConfig {
  children?: NodeData[];
}

export interface ReferencePatternFromInput {
  children?: FromValue | FromValue[];
}

export type ReferencePatternNode = NodeData<'reference_pattern'> & {
  children(...value: NodeData[]): ReferencePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'reference_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function referencePattern(
  config?: ReferencePatternConfig,
): ReferencePatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'reference_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

referencePattern.assign = function(target: AssignableNode<'reference_pattern'>): ReferencePatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'reference_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ReferenceTypeConfig {
  type: NodeData;
  children?: NodeData[];
}

export interface ReferenceTypeFromInput {
  type: FromValue;
  children?: FromValue | FromValue[];
}

export type ReferenceTypeNode = NodeData<'reference_type'> & {
  children(...value: NodeData[]): ReferenceTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'reference_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function referenceType(
  typeOrConfig: NodeData | ReferenceTypeConfig,
  config?: Partial<ReferenceTypeConfig>,
): ReferenceTypeNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveAndValidate(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'reference_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

referenceType.assign = function(target: AssignableNode<'reference_type'>): ReferenceTypeNode {
  const overrides: any = {};
  const fieldList: string[] = ['type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'reference_type'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface RemovedTraitBoundConfig {
  children?: NodeData[];
}

export interface RemovedTraitBoundFromInput {
  children?: FromValue | FromValue[];
}

export type RemovedTraitBoundNode = NodeData<'removed_trait_bound'> & {
  children(...value: NodeData[]): RemovedTraitBoundNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'removed_trait_bound'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function removedTraitBound(
  config?: RemovedTraitBoundConfig,
): RemovedTraitBoundNode {
  const fields: any = config ?? {};
  const node: any = { type: 'removed_trait_bound', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

removedTraitBound.assign = function(target: AssignableNode<'removed_trait_bound'>): RemovedTraitBoundNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'removed_trait_bound'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ReturnExpressionConfig {
  children?: NodeData[];
}

export interface ReturnExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type ReturnExpressionNode = NodeData<'return_expression'> & {
  children(...value: NodeData[]): ReturnExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'return_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function returnExpression(
  config?: ReturnExpressionConfig,
): ReturnExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'return_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

returnExpression.assign = function(target: AssignableNode<'return_expression'>): ReturnExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'return_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ScopedIdentifierConfig {
  path?: NodeData;
  name: NodeData | string;
}

export interface ScopedIdentifierFromInput {
  path?: FromValue;
  name: NodeData | string;
}

export type ScopedIdentifierNode = NodeData<'scoped_identifier'> & {
  path(value: NodeData): ScopedIdentifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'scoped_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function scopedIdentifier(
  nameOrConfig: NodeData | string | ScopedIdentifierConfig,
  config?: Partial<ScopedIdentifierConfig>,
): ScopedIdentifierNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'scoped_identifier', fields };
  node.path = (v: any) => { fields['path'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

scopedIdentifier.assign = function(target: AssignableNode<'scoped_identifier'>): ScopedIdentifierNode {
  const overrides: any = {};
  const fieldList: string[] = ['path', 'name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'scoped_identifier'; }, get fields() { return getFields(); } };
  node.path = (v: any) => { overrides['path'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ScopedTypeIdentifierConfig {
  path?: NodeData;
  name: NodeData | string;
}

export interface ScopedTypeIdentifierFromInput {
  path?: FromValue;
  name: NodeData | string;
}

export type ScopedTypeIdentifierNode = NodeData<'scoped_type_identifier'> & {
  path(value: NodeData): ScopedTypeIdentifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'scoped_type_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function scopedTypeIdentifier(
  nameOrConfig: NodeData | string | ScopedTypeIdentifierConfig,
  config?: Partial<ScopedTypeIdentifierConfig>,
): ScopedTypeIdentifierNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'scoped_type_identifier', fields };
  node.path = (v: any) => { fields['path'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

scopedTypeIdentifier.assign = function(target: AssignableNode<'scoped_type_identifier'>): ScopedTypeIdentifierNode {
  const overrides: any = {};
  const fieldList: string[] = ['path', 'name'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'scoped_type_identifier'; }, get fields() { return getFields(); } };
  node.path = (v: any) => { overrides['path'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ScopedUseListConfig {
  path?: NodeData;
  list: NodeData;
}

export interface ScopedUseListFromInput {
  path?: FromValue;
  list: FromValue;
}

export type ScopedUseListNode = NodeData<'scoped_use_list'> & {
  path(value: NodeData): ScopedUseListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'scoped_use_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function scopedUseList(
  listOrConfig: NodeData | ScopedUseListConfig,
  config?: Partial<ScopedUseListConfig>,
): ScopedUseListNode {
  const fields: any = isNodeData(listOrConfig) || typeof listOrConfig === 'string'
    ? { 'list': resolveAndValidate(listOrConfig), ...config }
    : listOrConfig;
  const node: any = { type: 'scoped_use_list', fields };
  node.path = (v: any) => { fields['path'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

scopedUseList.assign = function(target: AssignableNode<'scoped_use_list'>): ScopedUseListNode {
  const overrides: any = {};
  const fieldList: string[] = ['path', 'list'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'scoped_use_list'; }, get fields() { return getFields(); } };
  node.path = (v: any) => { overrides['path'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.list = (v: any) => { overrides['list'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SelfParameterConfig {
  children?: NodeData[];
}

export interface SelfParameterFromInput {
  children?: FromValue | FromValue[];
}

export type SelfParameterNode = NodeData<'self_parameter'> & {
  children(...value: NodeData[]): SelfParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'self_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function selfParameter(
  config?: SelfParameterConfig,
): SelfParameterNode {
  const fields: any = config ?? {};
  const node: any = { type: 'self_parameter', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

selfParameter.assign = function(target: AssignableNode<'self_parameter'>): SelfParameterNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'self_parameter'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface ShorthandFieldInitializerConfig {
  children?: NodeData[];
}

export interface ShorthandFieldInitializerFromInput {
  children?: FromValue | FromValue[];
}

export type ShorthandFieldInitializerNode = NodeData<'shorthand_field_initializer'> & {
  children(...value: NodeData[]): ShorthandFieldInitializerNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'shorthand_field_initializer'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function shorthandFieldInitializer(
  config?: ShorthandFieldInitializerConfig,
): ShorthandFieldInitializerNode {
  const fields: any = config ?? {};
  const node: any = { type: 'shorthand_field_initializer', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

shorthandFieldInitializer.assign = function(target: AssignableNode<'shorthand_field_initializer'>): ShorthandFieldInitializerNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'shorthand_field_initializer'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SlicePatternConfig {
  children?: NodeData[];
}

export interface SlicePatternFromInput {
  children?: FromValue | FromValue[];
}

export type SlicePatternNode = NodeData<'slice_pattern'> & {
  children(...value: NodeData[]): SlicePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'slice_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function slicePattern(
  config?: SlicePatternConfig,
): SlicePatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'slice_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

slicePattern.assign = function(target: AssignableNode<'slice_pattern'>): SlicePatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'slice_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface SourceFileConfig {
  children?: NodeData[];
}

export interface SourceFileFromInput {
  children?: FromValue | FromValue[];
}

export type SourceFileNode = NodeData<'source_file'> & {
  children(...value: NodeData[]): SourceFileNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'source_file'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function sourceFile(
  config?: SourceFileConfig,
): SourceFileNode {
  const fields: any = config ?? {};
  const node: any = { type: 'source_file', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

sourceFile.assign = function(target: AssignableNode<'source_file'>): SourceFileNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'source_file'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StaticItemConfig {
  name: NodeData | string;
  type: NodeData;
  value?: NodeData;
  children?: NodeData[];
}

export interface StaticItemFromInput {
  name: NodeData | string;
  type: FromValue;
  value?: FromValue;
  children?: FromValue | FromValue[];
}

export type StaticItemNode = NodeData<'static_item'> & {
  typeField(value: NodeData): StaticItemNode;
  value(value: NodeData): StaticItemNode;
  children(...value: NodeData[]): StaticItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'static_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function staticItem(
  nameOrConfig: NodeData | string | StaticItemConfig,
  config?: Partial<StaticItemConfig>,
): StaticItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'static_item', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.value = (v: any) => { fields['value'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

staticItem.assign = function(target: AssignableNode<'static_item'>): StaticItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type', 'value'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'static_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StringLiteralConfig {
  children?: NodeData[];
}

export interface StringLiteralFromInput {
  children?: FromValue | FromValue[];
}

export type StringLiteralNode = NodeData<'string_literal'> & {
  children(...value: NodeData[]): StringLiteralNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'string_literal'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function stringLiteral(
  childrenOrConfig?: string | NodeData | NodeData[] | StringLiteralConfig,
): StringLiteralNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'string_content', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'string_literal', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

stringLiteral.assign = function(target: AssignableNode<'string_literal'>): StringLiteralNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'string_literal'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StructExpressionConfig {
  name: NodeData;
  body: NodeData;
}

export interface StructExpressionFromInput {
  name: FromValue;
  body: FromValue;
}

export type StructExpressionNode = NodeData<'struct_expression'> & {
  body(value: NodeData): StructExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'struct_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function structExpression(
  nameOrConfig: NodeData | StructExpressionConfig,
  config?: Partial<StructExpressionConfig>,
): StructExpressionNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'struct_expression', fields };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

structExpression.assign = function(target: AssignableNode<'struct_expression'>): StructExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'struct_expression'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StructItemConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  body?: NodeData;
  children?: NodeData[];
}

export interface StructItemFromInput {
  name: NodeData | string;
  type_parameters?: FromValue;
  body?: FromValue;
  children?: FromValue | FromValue[];
}

export type StructItemNode = NodeData<'struct_item'> & {
  typeParameters(value: NodeData): StructItemNode;
  body(value: NodeData): StructItemNode;
  children(...value: NodeData[]): StructItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'struct_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function structItem(
  nameOrConfig: NodeData | string | StructItemConfig,
  config?: Partial<StructItemConfig>,
): StructItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'struct_item', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

structItem.assign = function(target: AssignableNode<'struct_item'>): StructItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'struct_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface StructPatternConfig {
  type: NodeData;
  children?: NodeData[];
}

export interface StructPatternFromInput {
  type: FromValue;
  children?: FromValue | FromValue[];
}

export type StructPatternNode = NodeData<'struct_pattern'> & {
  children(...value: NodeData[]): StructPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'struct_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function structPattern(
  typeOrConfig: NodeData | StructPatternConfig,
  config?: Partial<StructPatternConfig>,
): StructPatternNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveAndValidate(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'struct_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

structPattern.assign = function(target: AssignableNode<'struct_pattern'>): StructPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'struct_pattern'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TokenBindingPatternConfig {
  name: NodeData | string;
  type: NodeData | string;
}

export interface TokenBindingPatternFromInput {
  name: NodeData | string;
  type: NodeData | string;
}

export type TokenBindingPatternNode = NodeData<'token_binding_pattern'> & {
  typeField(value: NodeData | string): TokenBindingPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'token_binding_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tokenBindingPattern(
  nameOrConfig: NodeData | string | TokenBindingPatternConfig,
  config?: Partial<TokenBindingPatternConfig>,
): TokenBindingPatternNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'token_binding_pattern', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tokenBindingPattern.assign = function(target: AssignableNode<'token_binding_pattern'>): TokenBindingPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'token_binding_pattern'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TokenRepetitionConfig {
  children?: NodeData[];
}

export interface TokenRepetitionFromInput {
  children?: FromValue | FromValue[];
}

export type TokenRepetitionNode = NodeData<'token_repetition'> & {
  children(...value: NodeData[]): TokenRepetitionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'token_repetition'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tokenRepetition(
  config?: TokenRepetitionConfig,
): TokenRepetitionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'token_repetition', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tokenRepetition.assign = function(target: AssignableNode<'token_repetition'>): TokenRepetitionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'token_repetition'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TokenRepetitionPatternConfig {
  children?: NodeData[];
}

export interface TokenRepetitionPatternFromInput {
  children?: FromValue | FromValue[];
}

export type TokenRepetitionPatternNode = NodeData<'token_repetition_pattern'> & {
  children(...value: NodeData[]): TokenRepetitionPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'token_repetition_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tokenRepetitionPattern(
  config?: TokenRepetitionPatternConfig,
): TokenRepetitionPatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'token_repetition_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tokenRepetitionPattern.assign = function(target: AssignableNode<'token_repetition_pattern'>): TokenRepetitionPatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'token_repetition_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TokenTreeConfig {
  children?: NodeData[];
}

export interface TokenTreeFromInput {
  children?: FromValue | FromValue[];
}

export type TokenTreeNode = NodeData<'token_tree'> & {
  children(...value: NodeData[]): TokenTreeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'token_tree'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tokenTree(
  config?: TokenTreeConfig,
): TokenTreeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'token_tree', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tokenTree.assign = function(target: AssignableNode<'token_tree'>): TokenTreeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'token_tree'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TokenTreePatternConfig {
  children?: NodeData[];
}

export interface TokenTreePatternFromInput {
  children?: FromValue | FromValue[];
}

export type TokenTreePatternNode = NodeData<'token_tree_pattern'> & {
  children(...value: NodeData[]): TokenTreePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'token_tree_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tokenTreePattern(
  config?: TokenTreePatternConfig,
): TokenTreePatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'token_tree_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tokenTreePattern.assign = function(target: AssignableNode<'token_tree_pattern'>): TokenTreePatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'token_tree_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TraitBoundsConfig {
  children?: NodeData[];
}

export interface TraitBoundsFromInput {
  children?: FromValue | FromValue[];
}

export type TraitBoundsNode = NodeData<'trait_bounds'> & {
  children(...value: NodeData[]): TraitBoundsNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'trait_bounds'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function traitBounds(
  config?: TraitBoundsConfig,
): TraitBoundsNode {
  const fields: any = config ?? {};
  const node: any = { type: 'trait_bounds', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

traitBounds.assign = function(target: AssignableNode<'trait_bounds'>): TraitBoundsNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'trait_bounds'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TraitItemConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  bounds?: NodeData;
  body: NodeData;
  children?: NodeData[];
}

export interface TraitItemFromInput {
  name: NodeData | string;
  type_parameters?: FromValue;
  bounds?: FromValue;
  body: FromValue;
  children?: FromValue | FromValue[];
}

export type TraitItemNode = NodeData<'trait_item'> & {
  typeParameters(value: NodeData): TraitItemNode;
  bounds(value: NodeData): TraitItemNode;
  body(value: NodeData): TraitItemNode;
  children(...value: NodeData[]): TraitItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'trait_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function traitItem(
  nameOrConfig: NodeData | string | TraitItemConfig,
  config?: Partial<TraitItemConfig>,
): TraitItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'trait_item', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.bounds = (v: any) => { fields['bounds'] = resolveAndValidate(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

traitItem.assign = function(target: AssignableNode<'trait_item'>): TraitItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'bounds', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'trait_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.bounds = (v: any) => { overrides['bounds'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TryBlockConfig {
  children?: NodeData[];
}

export interface TryBlockFromInput {
  children?: FromValue | FromValue[];
}

export type TryBlockNode = NodeData<'try_block'> & {
  children(...value: NodeData[]): TryBlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'try_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tryBlock(
  config?: TryBlockConfig,
): TryBlockNode {
  const fields: any = config ?? {};
  const node: any = { type: 'try_block', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tryBlock.assign = function(target: AssignableNode<'try_block'>): TryBlockNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'try_block'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TryExpressionConfig {
  children?: NodeData[];
}

export interface TryExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type TryExpressionNode = NodeData<'try_expression'> & {
  children(...value: NodeData[]): TryExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'try_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tryExpression(
  config?: TryExpressionConfig,
): TryExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'try_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tryExpression.assign = function(target: AssignableNode<'try_expression'>): TryExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'try_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TupleExpressionConfig {
  children?: NodeData[];
}

export interface TupleExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type TupleExpressionNode = NodeData<'tuple_expression'> & {
  children(...value: NodeData[]): TupleExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tupleExpression(
  config?: TupleExpressionConfig,
): TupleExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'tuple_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tupleExpression.assign = function(target: AssignableNode<'tuple_expression'>): TupleExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'tuple_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TuplePatternConfig {
  children?: NodeData[];
}

export interface TuplePatternFromInput {
  children?: FromValue | FromValue[];
}

export type TuplePatternNode = NodeData<'tuple_pattern'> & {
  children(...value: NodeData[]): TuplePatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tuplePattern(
  config?: TuplePatternConfig,
): TuplePatternNode {
  const fields: any = config ?? {};
  const node: any = { type: 'tuple_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tuplePattern.assign = function(target: AssignableNode<'tuple_pattern'>): TuplePatternNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'tuple_pattern'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TupleStructPatternConfig {
  type: NodeData;
  children?: NodeData[];
}

export interface TupleStructPatternFromInput {
  type: FromValue;
  children?: FromValue | FromValue[];
}

export type TupleStructPatternNode = NodeData<'tuple_struct_pattern'> & {
  children(...value: NodeData[]): TupleStructPatternNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_struct_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tupleStructPattern(
  typeOrConfig: NodeData | TupleStructPatternConfig,
  config?: Partial<TupleStructPatternConfig>,
): TupleStructPatternNode {
  const fields: any = isNodeData(typeOrConfig) || typeof typeOrConfig === 'string'
    ? { 'type': resolveAndValidate(typeOrConfig), ...config }
    : typeOrConfig;
  const node: any = { type: 'tuple_struct_pattern', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tupleStructPattern.assign = function(target: AssignableNode<'tuple_struct_pattern'>): TupleStructPatternNode {
  const overrides: any = {};
  const fieldList: string[] = ['type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'tuple_struct_pattern'; }, get fields() { return getFields(); } };
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TupleTypeConfig {
  children?: NodeData[];
}

export interface TupleTypeFromInput {
  children?: FromValue | FromValue[];
}

export type TupleTypeNode = NodeData<'tuple_type'> & {
  children(...value: NodeData[]): TupleTypeNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'tuple_type'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function tupleType(
  config?: TupleTypeConfig,
): TupleTypeNode {
  const fields: any = config ?? {};
  const node: any = { type: 'tuple_type', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

tupleType.assign = function(target: AssignableNode<'tuple_type'>): TupleTypeNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'tuple_type'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeArgumentsConfig {
  children?: NodeData[];
}

export interface TypeArgumentsFromInput {
  children?: FromValue | FromValue[];
}

export type TypeArgumentsNode = NodeData<'type_arguments'> & {
  children(...value: NodeData[]): TypeArgumentsNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_arguments'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeArguments(
  config?: TypeArgumentsConfig,
): TypeArgumentsNode {
  const fields: any = config ?? {};
  const node: any = { type: 'type_arguments', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeArguments.assign = function(target: AssignableNode<'type_arguments'>): TypeArgumentsNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_arguments'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeBindingConfig {
  name: NodeData | string;
  type_arguments?: NodeData;
  type: NodeData;
}

export interface TypeBindingFromInput {
  name: NodeData | string;
  type_arguments?: FromValue;
  type: FromValue;
}

export type TypeBindingNode = NodeData<'type_binding'> & {
  typeArguments(value: NodeData): TypeBindingNode;
  typeField(value: NodeData): TypeBindingNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_binding'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeBinding(
  nameOrConfig: NodeData | string | TypeBindingConfig,
  config?: Partial<TypeBindingConfig>,
): TypeBindingNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'type_binding', fields };
  node.typeArguments = (v: any) => { fields['type_arguments'] = resolveAndValidate(v); return node; };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeBinding.assign = function(target: AssignableNode<'type_binding'>): TypeBindingNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_arguments', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'type_binding'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeArguments = (v: any) => { overrides['type_arguments'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeCastExpressionConfig {
  value: NodeData;
  type: NodeData;
}

export interface TypeCastExpressionFromInput {
  value: FromValue;
  type: FromValue;
}

export type TypeCastExpressionNode = NodeData<'type_cast_expression'> & {
  typeField(value: NodeData): TypeCastExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_cast_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeCastExpression(
  valueOrConfig: NodeData | TypeCastExpressionConfig,
  config?: Partial<TypeCastExpressionConfig>,
): TypeCastExpressionNode {
  const fields: any = isNodeData(valueOrConfig) || typeof valueOrConfig === 'string'
    ? { 'value': resolveAndValidate(valueOrConfig), ...config }
    : valueOrConfig;
  const node: any = { type: 'type_cast_expression', fields };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeCastExpression.assign = function(target: AssignableNode<'type_cast_expression'>): TypeCastExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['value', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'type_cast_expression'; }, get fields() { return getFields(); } };
  node.value = (v: any) => { overrides['value'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeItemConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  type: NodeData;
  children?: NodeData[];
}

export interface TypeItemFromInput {
  name: NodeData | string;
  type_parameters?: FromValue;
  type: FromValue;
  children?: FromValue | FromValue[];
}

export type TypeItemNode = NodeData<'type_item'> & {
  typeParameters(value: NodeData): TypeItemNode;
  typeField(value: NodeData): TypeItemNode;
  children(...value: NodeData[]): TypeItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeItem(
  nameOrConfig: NodeData | string | TypeItemConfig,
  config?: Partial<TypeItemConfig>,
): TypeItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'type_item', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.typeField = (v: any) => { fields['type'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeItem.assign = function(target: AssignableNode<'type_item'>): TypeItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeField = (v: any) => { overrides['type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeParameterConfig {
  name: NodeData | string;
  bounds?: NodeData;
  default_type?: NodeData;
}

export interface TypeParameterFromInput {
  name: NodeData | string;
  bounds?: FromValue;
  default_type?: FromValue;
}

export type TypeParameterNode = NodeData<'type_parameter'> & {
  bounds(value: NodeData): TypeParameterNode;
  defaultType(value: NodeData): TypeParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeParameter(
  nameOrConfig: NodeData | string | TypeParameterConfig,
  config?: Partial<TypeParameterConfig>,
): TypeParameterNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'type_parameter', fields };
  node.bounds = (v: any) => { fields['bounds'] = resolveAndValidate(v); return node; };
  node.defaultType = (v: any) => { fields['default_type'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeParameter.assign = function(target: AssignableNode<'type_parameter'>): TypeParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'bounds', 'default_type'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'type_parameter'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.bounds = (v: any) => { overrides['bounds'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.defaultType = (v: any) => { overrides['default_type'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface TypeParametersConfig {
  children?: NodeData[];
}

export interface TypeParametersFromInput {
  children?: FromValue | FromValue[];
}

export type TypeParametersNode = NodeData<'type_parameters'> & {
  children(...value: NodeData[]): TypeParametersNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'type_parameters'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function typeParameters(
  config?: TypeParametersConfig,
): TypeParametersNode {
  const fields: any = config ?? {};
  const node: any = { type: 'type_parameters', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

typeParameters.assign = function(target: AssignableNode<'type_parameters'>): TypeParametersNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'type_parameters'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UnaryExpressionConfig {
  children?: NodeData[];
}

export interface UnaryExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type UnaryExpressionNode = NodeData<'unary_expression'> & {
  children(...value: NodeData[]): UnaryExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'unary_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unaryExpression(
  config?: UnaryExpressionConfig,
): UnaryExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'unary_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

unaryExpression.assign = function(target: AssignableNode<'unary_expression'>): UnaryExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'unary_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UnionItemConfig {
  name: NodeData | string;
  type_parameters?: NodeData;
  body: NodeData;
  children?: NodeData[];
}

export interface UnionItemFromInput {
  name: NodeData | string;
  type_parameters?: FromValue;
  body: FromValue;
  children?: FromValue | FromValue[];
}

export type UnionItemNode = NodeData<'union_item'> & {
  typeParameters(value: NodeData): UnionItemNode;
  body(value: NodeData): UnionItemNode;
  children(...value: NodeData[]): UnionItemNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'union_item'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unionItem(
  nameOrConfig: NodeData | string | UnionItemConfig,
  config?: Partial<UnionItemConfig>,
): UnionItemNode {
  const fields: any = isNodeData(nameOrConfig) || typeof nameOrConfig === 'string'
    ? { 'name': resolveAndValidate(nameOrConfig), ...config }
    : nameOrConfig;
  const node: any = { type: 'union_item', fields };
  node.typeParameters = (v: any) => { fields['type_parameters'] = resolveAndValidate(v); return node; };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

unionItem.assign = function(target: AssignableNode<'union_item'>): UnionItemNode {
  const overrides: any = {};
  const fieldList: string[] = ['name', 'type_parameters', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'union_item'; }, get fields() { return getFields(); } };
  node.name = (v: any) => { overrides['name'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.typeParameters = (v: any) => { overrides['type_parameters'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UnsafeBlockConfig {
  children?: NodeData[];
}

export interface UnsafeBlockFromInput {
  children?: FromValue | FromValue[];
}

export type UnsafeBlockNode = NodeData<'unsafe_block'> & {
  children(...value: NodeData[]): UnsafeBlockNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'unsafe_block'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function unsafeBlock(
  config?: UnsafeBlockConfig,
): UnsafeBlockNode {
  const fields: any = config ?? {};
  const node: any = { type: 'unsafe_block', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

unsafeBlock.assign = function(target: AssignableNode<'unsafe_block'>): UnsafeBlockNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'unsafe_block'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UseAsClauseConfig {
  path: NodeData;
  alias: NodeData | string;
}

export interface UseAsClauseFromInput {
  path: FromValue;
  alias: NodeData | string;
}

export type UseAsClauseNode = NodeData<'use_as_clause'> & {
  alias(value: NodeData | string): UseAsClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'use_as_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function useAsClause(
  pathOrConfig: NodeData | UseAsClauseConfig,
  config?: Partial<UseAsClauseConfig>,
): UseAsClauseNode {
  const fields: any = isNodeData(pathOrConfig) || typeof pathOrConfig === 'string'
    ? { 'path': resolveAndValidate(pathOrConfig), ...config }
    : pathOrConfig;
  const node: any = { type: 'use_as_clause', fields };
  node.alias = (v: any) => { fields['alias'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

useAsClause.assign = function(target: AssignableNode<'use_as_clause'>): UseAsClauseNode {
  const overrides: any = {};
  const fieldList: string[] = ['path', 'alias'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'use_as_clause'; }, get fields() { return getFields(); } };
  node.path = (v: any) => { overrides['path'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.alias = (v: any) => { overrides['alias'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UseBoundsConfig {
  children?: NodeData[];
}

export interface UseBoundsFromInput {
  children?: FromValue | FromValue[];
}

export type UseBoundsNode = NodeData<'use_bounds'> & {
  children(...value: NodeData[]): UseBoundsNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'use_bounds'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function useBounds(
  config?: UseBoundsConfig,
): UseBoundsNode {
  const fields: any = config ?? {};
  const node: any = { type: 'use_bounds', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

useBounds.assign = function(target: AssignableNode<'use_bounds'>): UseBoundsNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'use_bounds'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UseDeclarationConfig {
  argument: NodeData;
  children?: NodeData[];
}

export interface UseDeclarationFromInput {
  argument: FromValue;
  children?: FromValue | FromValue[];
}

export type UseDeclarationNode = NodeData<'use_declaration'> & {
  children(...value: NodeData[]): UseDeclarationNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'use_declaration'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function useDeclaration(
  argumentOrConfig: NodeData | UseDeclarationConfig,
  config?: Partial<UseDeclarationConfig>,
): UseDeclarationNode {
  const fields: any = isNodeData(argumentOrConfig) || typeof argumentOrConfig === 'string'
    ? { 'argument': resolveAndValidate(argumentOrConfig), ...config }
    : argumentOrConfig;
  const node: any = { type: 'use_declaration', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

useDeclaration.assign = function(target: AssignableNode<'use_declaration'>): UseDeclarationNode {
  const overrides: any = {};
  const fieldList: string[] = ['argument'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'use_declaration'; }, get fields() { return getFields(); } };
  node.argument = (v: any) => { overrides['argument'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UseListConfig {
  children?: NodeData[];
}

export interface UseListFromInput {
  children?: FromValue | FromValue[];
}

export type UseListNode = NodeData<'use_list'> & {
  children(...value: NodeData[]): UseListNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'use_list'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function useList(
  config?: UseListConfig,
): UseListNode {
  const fields: any = config ?? {};
  const node: any = { type: 'use_list', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

useList.assign = function(target: AssignableNode<'use_list'>): UseListNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'use_list'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface UseWildcardConfig {
  children?: NodeData[];
}

export interface UseWildcardFromInput {
  children?: FromValue | FromValue[];
}

export type UseWildcardNode = NodeData<'use_wildcard'> & {
  children(...value: NodeData[]): UseWildcardNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'use_wildcard'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function useWildcard(
  config?: UseWildcardConfig,
): UseWildcardNode {
  const fields: any = config ?? {};
  const node: any = { type: 'use_wildcard', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

useWildcard.assign = function(target: AssignableNode<'use_wildcard'>): UseWildcardNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'use_wildcard'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface VariadicParameterConfig {
  pattern?: NodeData;
  children?: NodeData[];
}

export interface VariadicParameterFromInput {
  pattern?: FromValue;
  children?: FromValue | FromValue[];
}

export type VariadicParameterNode = NodeData<'variadic_parameter'> & {
  pattern(value: NodeData): VariadicParameterNode;
  children(...value: NodeData[]): VariadicParameterNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'variadic_parameter'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function variadicParameter(
  childrenOrConfig?: string | NodeData | NodeData[] | VariadicParameterConfig,
): VariadicParameterNode {
  let fields: any;
  if (typeof childrenOrConfig === 'string') {
    fields = { children: [{ type: 'mutable_specifier', fields: {}, text: childrenOrConfig }] };
  } else if (Array.isArray(childrenOrConfig)) {
    fields = { children: childrenOrConfig };
  } else if (childrenOrConfig && isNodeData(childrenOrConfig)) {
    fields = { children: [childrenOrConfig] };
  } else {
    fields = childrenOrConfig ?? {};
  }
  const node: any = { type: 'variadic_parameter', fields };
  node.pattern = (v: any) => { fields['pattern'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

variadicParameter.assign = function(target: AssignableNode<'variadic_parameter'>): VariadicParameterNode {
  const overrides: any = {};
  const fieldList: string[] = ['pattern'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'variadic_parameter'; }, get fields() { return getFields(); } };
  node.pattern = (v: any) => { overrides['pattern'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface VisibilityModifierConfig {
  children?: NodeData[];
}

export interface VisibilityModifierFromInput {
  children?: FromValue | FromValue[];
}

export type VisibilityModifierNode = NodeData<'visibility_modifier'> & {
  children(...value: NodeData[]): VisibilityModifierNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'visibility_modifier'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function visibilityModifier(
  config?: VisibilityModifierConfig,
): VisibilityModifierNode {
  const fields: any = config ?? {};
  const node: any = { type: 'visibility_modifier', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

visibilityModifier.assign = function(target: AssignableNode<'visibility_modifier'>): VisibilityModifierNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'visibility_modifier'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WhereClauseConfig {
  children?: NodeData[];
}

export interface WhereClauseFromInput {
  children?: FromValue | FromValue[];
}

export type WhereClauseNode = NodeData<'where_clause'> & {
  children(...value: NodeData[]): WhereClauseNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'where_clause'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function whereClause(
  config?: WhereClauseConfig,
): WhereClauseNode {
  const fields: any = config ?? {};
  const node: any = { type: 'where_clause', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

whereClause.assign = function(target: AssignableNode<'where_clause'>): WhereClauseNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'where_clause'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WherePredicateConfig {
  left: NodeData;
  bounds: NodeData;
}

export interface WherePredicateFromInput {
  left: FromValue;
  bounds: FromValue;
}

export type WherePredicateNode = NodeData<'where_predicate'> & {
  bounds(value: NodeData): WherePredicateNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'where_predicate'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function wherePredicate(
  leftOrConfig: NodeData | WherePredicateConfig,
  config?: Partial<WherePredicateConfig>,
): WherePredicateNode {
  const fields: any = isNodeData(leftOrConfig) || typeof leftOrConfig === 'string'
    ? { 'left': resolveAndValidate(leftOrConfig), ...config }
    : leftOrConfig;
  const node: any = { type: 'where_predicate', fields };
  node.bounds = (v: any) => { fields['bounds'] = resolveAndValidate(v); return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

wherePredicate.assign = function(target: AssignableNode<'where_predicate'>): WherePredicateNode {
  const overrides: any = {};
  const fieldList: string[] = ['left', 'bounds'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    return merged;
  };
  const node: any = { get type() { return 'where_predicate'; }, get fields() { return getFields(); } };
  node.left = (v: any) => { overrides['left'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.bounds = (v: any) => { overrides['bounds'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface WhileExpressionConfig {
  condition: NodeData;
  body: NodeData;
  children?: NodeData[];
}

export interface WhileExpressionFromInput {
  condition: FromValue;
  body: FromValue;
  children?: FromValue | FromValue[];
}

export type WhileExpressionNode = NodeData<'while_expression'> & {
  body(value: NodeData): WhileExpressionNode;
  children(...value: NodeData[]): WhileExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'while_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function whileExpression(
  conditionOrConfig: NodeData | WhileExpressionConfig,
  config?: Partial<WhileExpressionConfig>,
): WhileExpressionNode {
  const fields: any = isNodeData(conditionOrConfig) || typeof conditionOrConfig === 'string'
    ? { 'condition': resolveAndValidate(conditionOrConfig), ...config }
    : conditionOrConfig;
  const node: any = { type: 'while_expression', fields };
  node.body = (v: any) => { fields['body'] = resolveAndValidate(v); return node; };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

whileExpression.assign = function(target: AssignableNode<'while_expression'>): WhileExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = ['condition', 'body'];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'while_expression'; }, get fields() { return getFields(); } };
  node.condition = (v: any) => { overrides['condition'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.body = (v: any) => { overrides['body'] = resolveAndValidate(v); return node; };  // validate user-provided overrides
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export interface YieldExpressionConfig {
  children?: NodeData[];
}

export interface YieldExpressionFromInput {
  children?: FromValue | FromValue[];
}

export type YieldExpressionNode = NodeData<'yield_expression'> & {
  children(...value: NodeData[]): YieldExpressionNode;
  render(): string;
  toEdit(start: number, end: number): Edit;
  toEdit(range: { start: { index: number }, end: { index: number } }): Edit;
  replace(target: { type: 'yield_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit;
};

export function yieldExpression(
  config?: YieldExpressionConfig,
): YieldExpressionNode {
  const fields: any = config ?? {};
  const node: any = { type: 'yield_expression', fields };
  node.children = (...v: any[]) => { fields.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') return toEdit(node, rules, startOrRange, endPos!, joinBy);
    return toEdit(node, rules, startOrRange, joinBy);
  };
  node.replace = (target: any) => { const r = target.range(); return toEdit(node, rules, r, joinBy); };
  return node;
}

yieldExpression.assign = function(target: AssignableNode<'yield_expression'>): YieldExpressionNode {
  const overrides: any = {};
  const fieldList: string[] = [];
  const getFields = () => {
    const merged: any = {};
    for (const f of fieldList) {
      if (f in overrides) { merged[f] = overrides[f]; continue; }
      const child = target.field(f);
      if (child) merged[f] = { type: child.type, fields: {}, text: child.text() };
    }
    if ('children' in overrides) { merged.children = overrides.children; }
    else { merged.children = target.children().map(c => ({ type: c.type, fields: {}, text: c.text() })); }
    return merged;
  };
  const node: any = { get type() { return 'yield_expression'; }, get fields() { return getFields(); } };
  node.children = (...v: any[]) => { overrides.children = v; return node; };
  node.render = () => render(node, rules, joinBy);
  node.toEdit = () => {
    const r = target.range();
    return { startPos: r.start.index, endPos: r.end.index, insertedText: node.render() };
  };
  node.replace = node.toEdit;
  return node;
} as any;

export function booleanLiteral(text: 'false' | 'true'): NodeData<'boolean_literal'> & { render(): string; replace(target: { type: 'boolean_literal'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'boolean_literal', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function emptyStatement(): NodeData<'empty_statement'> & { render(): string; replace(target: { type: 'empty_statement'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'empty_statement', fields: {}, text: ';' };
  node.render = () => ';';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: ';' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: ';' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: ';' }; };
  return node;
}

export function fragmentSpecifier(text: 'block' | 'expr' | 'expr_2021' | 'ident' | 'item' | 'lifetime' | 'literal' | 'meta' | 'pat' | 'pat_param' | 'path' | 'stmt' | 'tt' | 'ty' | 'vis'): NodeData<'fragment_specifier'> & { render(): string; replace(target: { type: 'fragment_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'fragment_specifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function innerDocCommentMarker(text: string): NodeData<'inner_doc_comment_marker'> & { render(): string; replace(target: { type: 'inner_doc_comment_marker'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'inner_doc_comment_marker', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function neverType(): NodeData<'never_type'> & { render(): string; replace(target: { type: 'never_type'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'never_type', fields: {}, text: '!' };
  node.render = () => '!';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '!' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '!' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '!' }; };
  return node;
}

export function outerDocCommentMarker(text: string): NodeData<'outer_doc_comment_marker'> & { render(): string; replace(target: { type: 'outer_doc_comment_marker'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'outer_doc_comment_marker', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function remainingFieldPattern(): NodeData<'remaining_field_pattern'> & { render(): string; replace(target: { type: 'remaining_field_pattern'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'remaining_field_pattern', fields: {}, text: '..' };
  node.render = () => '..';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '..' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '..' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '..' }; };
  return node;
}

export function unitExpression(): NodeData<'unit_expression'> & { render(): string; replace(target: { type: 'unit_expression'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'unit_expression', fields: {}, text: '()' };
  node.render = () => '()';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '()' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '()' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '()' }; };
  return node;
}

export function unitType(): NodeData<'unit_type'> & { render(): string; replace(target: { type: 'unit_type'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'unit_type', fields: {}, text: '()' };
  node.render = () => '()';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: '()' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: '()' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: '()' }; };
  return node;
}

export function charLiteral(text: string): NodeData<'char_literal'> & { render(): string; replace(target: { type: 'char_literal'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(text)) throw new Error(`Invalid char_literal: '${text}' does not match grammar pattern`);
  const node: any = { type: 'char_literal', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function crate(): NodeData<'crate'> & { render(): string; replace(target: { type: 'crate'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'crate', fields: {}, text: 'crate' };
  node.render = () => 'crate';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'crate' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'crate' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'crate' }; };
  return node;
}

export function docComment(text: string): NodeData<'doc_comment'> & { render(): string; replace(target: { type: 'doc_comment'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'doc_comment', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function escapeSequence(text: `\\${string}`): NodeData<'escape_sequence'> & { render(): string; replace(target: { type: 'escape_sequence'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})$/.test(text)) throw new Error(`Invalid escape_sequence: '${text}' does not match grammar pattern`);
  const node: any = { type: 'escape_sequence', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function fieldIdentifier(text: string): NodeData<'field_identifier'> & { render(): string; replace(target: { type: 'field_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid field_identifier`);
  const node: any = { type: 'field_identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function floatLiteral(text: string): NodeData<'float_literal'> & { render(): string; replace(target: { type: 'float_literal'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'float_literal', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function identifier(text: string): NodeData<'identifier'> & { render(): string; replace(target: { type: 'identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid identifier`);
  if (!/^(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*$/u.test(text)) throw new Error(`Invalid identifier: '${text}' does not match grammar pattern`);
  const node: any = { type: 'identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function integerLiteral(text: string): NodeData<'integer_literal'> & { render(): string; replace(target: { type: 'integer_literal'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(text)) throw new Error(`Invalid integer_literal: '${text}' does not match grammar pattern`);
  const node: any = { type: 'integer_literal', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function metavariable(text: string): NodeData<'metavariable'> & { render(): string; replace(target: { type: 'metavariable'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^\$[a-zA-Z_]\w*$/.test(text)) throw new Error(`Invalid metavariable: '${text}' does not match grammar pattern`);
  const node: any = { type: 'metavariable', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function mutableSpecifier(): NodeData<'mutable_specifier'> & { render(): string; replace(target: { type: 'mutable_specifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'mutable_specifier', fields: {}, text: 'mut' };
  node.render = () => 'mut';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'mut' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'mut' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'mut' }; };
  return node;
}

export function primitiveType(text: 'bool' | 'char' | 'f32' | 'f64' | 'i128' | 'i16' | 'i32' | 'i64' | 'i8' | 'isize' | 'str' | 'u128' | 'u16' | 'u32' | 'u64' | 'u8' | 'usize'): NodeData<'primitive_type'> & { render(): string; replace(target: { type: 'primitive_type'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'primitive_type', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function self(): NodeData<'self'> & { render(): string; replace(target: { type: 'self'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'self', fields: {}, text: 'self' };
  node.render = () => 'self';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'self' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'self' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'self' }; };
  return node;
}

export function shebang(text: string): NodeData<'shebang'> & { render(): string; replace(target: { type: 'shebang'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (!/^#![\r\f\t\v ]*([^\[\n].*)?\n$/.test(text)) throw new Error(`Invalid shebang: '${text}' does not match grammar pattern`);
  const node: any = { type: 'shebang', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function shorthandFieldIdentifier(text: string): NodeData<'shorthand_field_identifier'> & { render(): string; replace(target: { type: 'shorthand_field_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'shorthand_field_identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function stringContent(text: string): NodeData<'string_content'> & { render(): string; replace(target: { type: 'string_content'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'string_content', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

export function super_(): NodeData<'super'> & { render(): string; replace(target: { type: 'super'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  const node: any = { type: 'super', fields: {}, text: 'super' };
  node.render = () => 'super';
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: 'super' };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: 'super' };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' }; };
  return node;
}

export function typeIdentifier(text: string): NodeData<'type_identifier'> & { render(): string; replace(target: { type: 'type_identifier'; range(): { start: { index: number }; end: { index: number } } }): Edit } {
  if (RESERVED_KEYWORDS.has(text)) throw new Error(`'${text}' is a reserved keyword, not a valid type_identifier`);
  const node: any = { type: 'type_identifier', fields: {}, text };
  node.render = () => text;
  node.toEdit = (startOrRange: any, endPos?: number) => {
    if (typeof startOrRange === 'number') {
      if (endPos === undefined) throw new Error('endPos required when startPos is a number');
      return { startPos: startOrRange, endPos, insertedText: text };
    }
    return { startPos: startOrRange.start.index, endPos: startOrRange.end.index, insertedText: text };
  };
  node.replace = (target: any) => { const r = target.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; };
  return node;
}

/**
 * Create an editor for a target node. Returns a factory
 * for the target's kind, pre-loaded with its byte range.
 * Call .toEdit() with no args to produce the Edit.
 */
/**
 * Create an in-place editor for a parsed tree node.
 * Hydrates a factory from the target node's fields,
 * then allows fluent modification. .toEdit() uses the target's range.
 *
 * Usage: edit(matchedNode).returnType(ir.primitiveType("i64")).toEdit()
 */
export function edit<K extends string>(target: AssignableNode<K>): any {
  const factories: Record<string, { assign: (t: any) => any }> = {
    'abstract_type': abstractType,
    'arguments': arguments_,
    'array_expression': arrayExpression,
    'array_type': arrayType,
    'assignment_expression': assignmentExpression,
    'associated_type': associatedType,
    'async_block': asyncBlock,
    'attribute': attribute,
    'attribute_item': attributeItem,
    'await_expression': awaitExpression,
    'base_field_initializer': baseFieldInitializer,
    'binary_expression': binaryExpression,
    'block': block,
    'block_comment': blockComment,
    'bounded_type': boundedType,
    'bracketed_type': bracketedType,
    'break_expression': breakExpression,
    'call_expression': callExpression,
    'captured_pattern': capturedPattern,
    'closure_expression': closureExpression,
    'closure_parameters': closureParameters,
    'compound_assignment_expr': compoundAssignmentExpr,
    'const_block': constBlock,
    'const_item': constItem,
    'const_parameter': constParameter,
    'continue_expression': continueExpression,
    'declaration_list': declarationList,
    'dynamic_type': dynamicType,
    'else_clause': elseClause,
    'enum_item': enumItem,
    'enum_variant': enumVariant,
    'enum_variant_list': enumVariantList,
    'expression_statement': expressionStatement,
    'extern_crate_declaration': externCrateDeclaration,
    'extern_modifier': externModifier,
    'field_declaration': fieldDeclaration,
    'field_declaration_list': fieldDeclarationList,
    'field_expression': fieldExpression,
    'field_initializer': fieldInitializer,
    'field_initializer_list': fieldInitializerList,
    'field_pattern': fieldPattern,
    'for_expression': forExpression,
    'for_lifetimes': forLifetimes,
    'foreign_mod_item': foreignModItem,
    'function_item': functionItem,
    'function_modifiers': functionModifiers,
    'function_signature_item': functionSignatureItem,
    'function_type': functionType,
    'gen_block': genBlock,
    'generic_function': genericFunction,
    'generic_pattern': genericPattern,
    'generic_type': genericType,
    'generic_type_with_turbofish': genericTypeWithTurbofish,
    'higher_ranked_trait_bound': higherRankedTraitBound,
    'if_expression': ifExpression,
    'impl_item': implItem,
    'index_expression': indexExpression,
    'inner_attribute_item': innerAttributeItem,
    'label': label,
    'let_chain': letChain,
    'let_condition': letCondition,
    'let_declaration': letDeclaration,
    'lifetime': lifetime,
    'lifetime_parameter': lifetimeParameter,
    'line_comment': lineComment,
    'loop_expression': loopExpression,
    'macro_definition': macroDefinition,
    'macro_invocation': macroInvocation,
    'macro_rule': macroRule,
    'match_arm': matchArm,
    'match_block': matchBlock,
    'match_expression': matchExpression,
    'match_pattern': matchPattern,
    'mod_item': modItem,
    'mut_pattern': mutPattern,
    'negative_literal': negativeLiteral,
    'or_pattern': orPattern,
    'ordered_field_declaration_list': orderedFieldDeclarationList,
    'parameter': parameter,
    'parameters': parameters,
    'parenthesized_expression': parenthesizedExpression,
    'pointer_type': pointerType,
    'qualified_type': qualifiedType,
    'range_expression': rangeExpression,
    'range_pattern': rangePattern,
    'raw_string_literal': rawStringLiteral,
    'ref_pattern': refPattern,
    'reference_expression': referenceExpression,
    'reference_pattern': referencePattern,
    'reference_type': referenceType,
    'removed_trait_bound': removedTraitBound,
    'return_expression': returnExpression,
    'scoped_identifier': scopedIdentifier,
    'scoped_type_identifier': scopedTypeIdentifier,
    'scoped_use_list': scopedUseList,
    'self_parameter': selfParameter,
    'shorthand_field_initializer': shorthandFieldInitializer,
    'slice_pattern': slicePattern,
    'source_file': sourceFile,
    'static_item': staticItem,
    'string_literal': stringLiteral,
    'struct_expression': structExpression,
    'struct_item': structItem,
    'struct_pattern': structPattern,
    'token_binding_pattern': tokenBindingPattern,
    'token_repetition': tokenRepetition,
    'token_repetition_pattern': tokenRepetitionPattern,
    'token_tree': tokenTree,
    'token_tree_pattern': tokenTreePattern,
    'trait_bounds': traitBounds,
    'trait_item': traitItem,
    'try_block': tryBlock,
    'try_expression': tryExpression,
    'tuple_expression': tupleExpression,
    'tuple_pattern': tuplePattern,
    'tuple_struct_pattern': tupleStructPattern,
    'tuple_type': tupleType,
    'type_arguments': typeArguments,
    'type_binding': typeBinding,
    'type_cast_expression': typeCastExpression,
    'type_item': typeItem,
    'type_parameter': typeParameter,
    'type_parameters': typeParameters,
    'unary_expression': unaryExpression,
    'union_item': unionItem,
    'unsafe_block': unsafeBlock,
    'use_as_clause': useAsClause,
    'use_bounds': useBounds,
    'use_declaration': useDeclaration,
    'use_list': useList,
    'use_wildcard': useWildcard,
    'variadic_parameter': variadicParameter,
    'visibility_modifier': visibilityModifier,
    'where_clause': whereClause,
    'where_predicate': wherePredicate,
    'while_expression': whileExpression,
    'yield_expression': yieldExpression,
  };
  const factory = factories[target.type];
  if (!factory) throw new Error(`No factory for kind '${target.type}'`);
  return factory.assign(target);
}

// ---------------------------------------------------------------------------
// .from() resolution metadata
// ---------------------------------------------------------------------------

const FROM_FIELD_TYPES: Record<string, FromFieldInfo[]> = {
  'abstract_type': [
    { name: 'trait', namedTypes: ["type_identifier","scoped_type_identifier","removed_trait_bound","generic_type","function_type","tuple_type","bounded_type"], multiple: false },
  ],
  'arguments': [
  ],
  'array_expression': [
    { name: 'length', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'array_type': [
    { name: 'element', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'length', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'assignment_expression': [
    { name: 'left', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'right', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'associated_type': [
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'bounds', namedTypes: ["trait_bounds"], multiple: false },
  ],
  'async_block': [
  ],
  'attribute': [
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'arguments', namedTypes: ["token_tree"], multiple: false },
  ],
  'attribute_item': [
  ],
  'await_expression': [
  ],
  'base_field_initializer': [
  ],
  'binary_expression': [
    { name: 'left', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'operator', namedTypes: [], multiple: false, anonymousTokens: ["&&","||","&","|","^","==","!=","<","<=",">",">=","<<",">>","+","-","*","/","%"] },
    { name: 'right', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'block': [
  ],
  'block_comment': [
    { name: 'outer', namedTypes: ["outer_doc_comment_marker"], multiple: false },
    { name: 'inner', namedTypes: ["inner_doc_comment_marker"], multiple: false },
    { name: 'doc', namedTypes: ["doc_comment"], multiple: false },
  ],
  'bounded_type': [
  ],
  'bracketed_type': [
  ],
  'break_expression': [
  ],
  'call_expression': [
    { name: 'function', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block"], multiple: false },
    { name: 'arguments', namedTypes: ["arguments"], multiple: false },
  ],
  'captured_pattern': [
  ],
  'closure_expression': [
    { name: 'parameters', namedTypes: ["closure_parameters"], multiple: false },
    { name: 'return_type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'body', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression","_"], multiple: false },
  ],
  'closure_parameters': [
  ],
  'compound_assignment_expr': [
    { name: 'left', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'operator', namedTypes: [], multiple: false, anonymousTokens: ["+=","-=","*=","/=","%=","&=","|=","^=","<<=",">>="] },
    { name: 'right', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'const_block': [
    { name: 'body', namedTypes: ["block"], multiple: false },
  ],
  'const_item': [
    { name: 'name', namedTypes: ["identifier"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'const_parameter': [
    { name: 'name', namedTypes: ["identifier"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'value', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","block","identifier","negative_literal"], multiple: false },
  ],
  'continue_expression': [
  ],
  'declaration_list': [
  ],
  'dynamic_type': [
    { name: 'trait', namedTypes: ["type_identifier","higher_ranked_trait_bound","scoped_type_identifier","generic_type","function_type","tuple_type"], multiple: false },
  ],
  'else_clause': [
  ],
  'enum_item': [
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'body', namedTypes: ["enum_variant_list"], multiple: false },
  ],
  'enum_variant': [
    { name: 'name', namedTypes: ["identifier"], multiple: false },
    { name: 'body', namedTypes: ["field_declaration_list","ordered_field_declaration_list"], multiple: false },
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'enum_variant_list': [
  ],
  'expression_statement': [
  ],
  'extern_crate_declaration': [
    { name: 'name', namedTypes: ["identifier"], multiple: false },
    { name: 'alias', namedTypes: ["identifier"], multiple: false },
  ],
  'extern_modifier': [
  ],
  'field_declaration': [
    { name: 'name', namedTypes: ["field_identifier"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'field_declaration_list': [
  ],
  'field_expression': [
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'field', namedTypes: ["field_identifier","integer_literal"], multiple: false },
  ],
  'field_initializer': [
    { name: 'field', namedTypes: ["field_identifier","integer_literal"], multiple: false },
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'field_initializer_list': [
  ],
  'field_pattern': [
    { name: 'name', namedTypes: ["field_identifier","shorthand_field_identifier"], multiple: false },
    { name: 'pattern', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"], multiple: false },
  ],
  'for_expression': [
    { name: 'pattern', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"], multiple: false },
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'body', namedTypes: ["block"], multiple: false },
  ],
  'for_lifetimes': [
  ],
  'foreign_mod_item': [
    { name: 'body', namedTypes: ["declaration_list"], multiple: false },
  ],
  'function_item': [
    { name: 'name', namedTypes: ["identifier","metavariable"], multiple: false },
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'parameters', namedTypes: ["parameters"], multiple: false },
    { name: 'return_type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'body', namedTypes: ["block"], multiple: false },
  ],
  'function_modifiers': [
  ],
  'function_signature_item': [
    { name: 'name', namedTypes: ["identifier","metavariable"], multiple: false },
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'parameters', namedTypes: ["parameters"], multiple: false },
    { name: 'return_type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'function_type': [
    { name: 'trait', namedTypes: ["type_identifier","scoped_type_identifier"], multiple: false },
    { name: 'parameters', namedTypes: ["parameters"], multiple: false },
    { name: 'return_type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'gen_block': [
  ],
  'generic_function': [
    { name: 'function', namedTypes: ["identifier","scoped_identifier","field_expression"], multiple: false },
    { name: 'type_arguments', namedTypes: ["type_arguments"], multiple: false },
  ],
  'generic_pattern': [
    { name: 'type_arguments', namedTypes: ["type_arguments"], multiple: false },
  ],
  'generic_type': [
    { name: 'type', namedTypes: ["type_identifier","identifier","scoped_type_identifier"], multiple: false },
    { name: 'type_arguments', namedTypes: ["type_arguments"], multiple: false },
  ],
  'generic_type_with_turbofish': [
    { name: 'type', namedTypes: ["type_identifier","scoped_identifier"], multiple: false },
    { name: 'type_arguments', namedTypes: ["type_arguments"], multiple: false },
  ],
  'higher_ranked_trait_bound': [
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'if_expression': [
    { name: 'condition', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression","let_condition","let_chain"], multiple: false },
    { name: 'consequence', namedTypes: ["block"], multiple: false },
    { name: 'alternative', namedTypes: ["else_clause"], multiple: false },
  ],
  'impl_item': [
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'trait', namedTypes: ["type_identifier","scoped_type_identifier","generic_type"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'body', namedTypes: ["declaration_list"], multiple: false },
  ],
  'index_expression': [
  ],
  'inner_attribute_item': [
  ],
  'label': [
  ],
  'let_chain': [
  ],
  'let_condition': [
    { name: 'pattern', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"], multiple: false },
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'let_declaration': [
    { name: 'pattern', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'alternative', namedTypes: ["block"], multiple: false },
  ],
  'lifetime': [
  ],
  'lifetime_parameter': [
    { name: 'name', namedTypes: ["lifetime"], multiple: false },
    { name: 'bounds', namedTypes: ["trait_bounds"], multiple: false },
  ],
  'line_comment': [
    { name: 'outer', namedTypes: ["outer_doc_comment_marker"], multiple: false },
    { name: 'inner', namedTypes: ["inner_doc_comment_marker"], multiple: false },
    { name: 'doc', namedTypes: ["doc_comment"], multiple: false },
  ],
  'loop_expression': [
    { name: 'body', namedTypes: ["block"], multiple: false },
  ],
  'macro_definition': [
    { name: 'name', namedTypes: ["identifier"], multiple: false },
  ],
  'macro_invocation': [
    { name: 'macro', namedTypes: ["identifier","scoped_identifier"], multiple: false },
  ],
  'macro_rule': [
    { name: 'left', namedTypes: ["token_tree_pattern"], multiple: false },
    { name: 'right', namedTypes: ["token_tree"], multiple: false },
  ],
  'match_arm': [
    { name: 'pattern', namedTypes: ["match_pattern"], multiple: false },
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'match_block': [
  ],
  'match_expression': [
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'body', namedTypes: ["match_block"], multiple: false },
  ],
  'match_pattern': [
    { name: 'condition', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression","let_condition","let_chain"], multiple: false },
  ],
  'mod_item': [
    { name: 'name', namedTypes: ["identifier"], multiple: false },
    { name: 'body', namedTypes: ["declaration_list"], multiple: false },
  ],
  'mut_pattern': [
  ],
  'negative_literal': [
  ],
  'or_pattern': [
  ],
  'ordered_field_declaration_list': [
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: true },
  ],
  'parameter': [
    { name: 'pattern', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation","self"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'parameters': [
  ],
  'parenthesized_expression': [
  ],
  'pointer_type': [
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'qualified_type': [
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'alias', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'range_expression': [
  ],
  'range_pattern': [
    { name: 'left', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","self","identifier","metavariable","super","crate","scoped_identifier"], multiple: false },
    { name: 'right', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","self","identifier","metavariable","super","crate","scoped_identifier"], multiple: false },
  ],
  'raw_string_literal': [
  ],
  'ref_pattern': [
  ],
  'reference_expression': [
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'reference_pattern': [
  ],
  'reference_type': [
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'removed_trait_bound': [
  ],
  'return_expression': [
  ],
  'scoped_identifier': [
    { name: 'path', namedTypes: ["self","identifier","metavariable","super","crate","scoped_identifier","bracketed_type","generic_type"], multiple: false },
    { name: 'name', namedTypes: ["identifier","super"], multiple: false },
  ],
  'scoped_type_identifier': [
    { name: 'path', namedTypes: ["self","identifier","metavariable","super","crate","scoped_identifier","generic_type","bracketed_type"], multiple: false },
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
  ],
  'scoped_use_list': [
    { name: 'path', namedTypes: ["self","identifier","metavariable","super","crate","scoped_identifier"], multiple: false },
    { name: 'list', namedTypes: ["use_list"], multiple: false },
  ],
  'self_parameter': [
  ],
  'shorthand_field_initializer': [
  ],
  'slice_pattern': [
  ],
  'source_file': [
  ],
  'static_item': [
    { name: 'name', namedTypes: ["identifier"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
  ],
  'string_literal': [
  ],
  'struct_expression': [
    { name: 'name', namedTypes: ["type_identifier","scoped_type_identifier","generic_type_with_turbofish"], multiple: false },
    { name: 'body', namedTypes: ["field_initializer_list"], multiple: false },
  ],
  'struct_item': [
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'body', namedTypes: ["field_declaration_list","ordered_field_declaration_list"], multiple: false },
  ],
  'struct_pattern': [
    { name: 'type', namedTypes: ["type_identifier","scoped_type_identifier"], multiple: false },
  ],
  'token_binding_pattern': [
    { name: 'name', namedTypes: ["metavariable"], multiple: false },
    { name: 'type', namedTypes: ["fragment_specifier"], multiple: false },
  ],
  'token_repetition': [
  ],
  'token_repetition_pattern': [
  ],
  'token_tree': [
  ],
  'token_tree_pattern': [
  ],
  'trait_bounds': [
  ],
  'trait_item': [
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'bounds', namedTypes: ["trait_bounds"], multiple: false },
    { name: 'body', namedTypes: ["declaration_list"], multiple: false },
  ],
  'try_block': [
  ],
  'try_expression': [
  ],
  'tuple_expression': [
  ],
  'tuple_pattern': [
  ],
  'tuple_struct_pattern': [
    { name: 'type', namedTypes: ["identifier","scoped_identifier","generic_type"], multiple: false },
  ],
  'tuple_type': [
  ],
  'type_arguments': [
  ],
  'type_binding': [
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
    { name: 'type_arguments', namedTypes: ["type_arguments"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'type_cast_expression': [
    { name: 'value', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'type_item': [
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'type_parameter': [
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
    { name: 'bounds', namedTypes: ["trait_bounds"], multiple: false },
    { name: 'default_type', namedTypes: ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"], multiple: false },
  ],
  'type_parameters': [
  ],
  'unary_expression': [
  ],
  'union_item': [
    { name: 'name', namedTypes: ["type_identifier"], multiple: false },
    { name: 'type_parameters', namedTypes: ["type_parameters"], multiple: false },
    { name: 'body', namedTypes: ["field_declaration_list"], multiple: false },
  ],
  'unsafe_block': [
  ],
  'use_as_clause': [
    { name: 'path', namedTypes: ["self","identifier","metavariable","super","crate","scoped_identifier"], multiple: false },
    { name: 'alias', namedTypes: ["identifier"], multiple: false },
  ],
  'use_bounds': [
  ],
  'use_declaration': [
    { name: 'argument', namedTypes: ["self","identifier","metavariable","super","crate","scoped_identifier","use_as_clause","use_list","scoped_use_list","use_wildcard"], multiple: false },
  ],
  'use_list': [
  ],
  'use_wildcard': [
  ],
  'variadic_parameter': [
    { name: 'pattern', namedTypes: ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"], multiple: false },
  ],
  'visibility_modifier': [
  ],
  'where_clause': [
  ],
  'where_predicate': [
    { name: 'left', namedTypes: ["type_identifier","lifetime","scoped_type_identifier","generic_type","reference_type","pointer_type","tuple_type","array_type","higher_ranked_trait_bound","primitive_type"], multiple: false },
    { name: 'bounds', namedTypes: ["trait_bounds"], multiple: false },
  ],
  'while_expression': [
    { name: 'condition', namedTypes: ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression","let_condition","let_chain"], multiple: false },
    { name: 'body', namedTypes: ["block"], multiple: false },
  ],
  'yield_expression': [
  ],
};

const FROM_CHILDREN_TYPES: Record<string, string[]> = {
  'abstract_type': ["type_parameters"],
  'arguments': ["attribute_item","unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'array_expression': ["attribute_item","unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'associated_type': ["where_clause"],
  'async_block': ["block"],
  'attribute': ["self","identifier","metavariable","super","crate","scoped_identifier"],
  'attribute_item': ["attribute"],
  'await_expression': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'base_field_initializer': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'block': ["label","expression_statement","const_item","macro_invocation","macro_definition","empty_statement","attribute_item","inner_attribute_item","mod_item","foreign_mod_item","struct_item","union_item","enum_item","type_item","function_item","function_signature_item","impl_item","trait_item","associated_type","let_declaration","use_declaration","extern_crate_declaration","static_item","unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'bounded_type': ["lifetime","abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type","use_bounds"],
  'bracketed_type': ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type","qualified_type"],
  'break_expression': ["label","unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'captured_pattern': ["identifier","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"],
  'closure_parameters': ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation","parameter"],
  'const_item': ["visibility_modifier"],
  'continue_expression': ["label"],
  'declaration_list': ["const_item","macro_invocation","macro_definition","empty_statement","attribute_item","inner_attribute_item","mod_item","foreign_mod_item","struct_item","union_item","enum_item","type_item","function_item","function_signature_item","impl_item","trait_item","associated_type","let_declaration","use_declaration","extern_crate_declaration","static_item"],
  'else_clause': ["block","if_expression"],
  'enum_item': ["visibility_modifier","where_clause"],
  'enum_variant': ["visibility_modifier"],
  'enum_variant_list': ["attribute_item","enum_variant"],
  'expression_statement': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'extern_crate_declaration': ["visibility_modifier","crate"],
  'extern_modifier': ["string_literal"],
  'field_declaration': ["visibility_modifier"],
  'field_declaration_list': ["attribute_item","field_declaration"],
  'field_initializer': ["attribute_item"],
  'field_initializer_list': ["shorthand_field_initializer","field_initializer","base_field_initializer"],
  'field_pattern': ["mutable_specifier"],
  'for_expression': ["label"],
  'for_lifetimes': ["lifetime"],
  'foreign_mod_item': ["visibility_modifier","extern_modifier"],
  'function_item': ["visibility_modifier","function_modifiers","where_clause"],
  'function_modifiers': ["extern_modifier"],
  'function_signature_item': ["visibility_modifier","function_modifiers","where_clause"],
  'function_type': ["for_lifetimes","function_modifiers"],
  'gen_block': ["block"],
  'generic_pattern': ["identifier","scoped_identifier"],
  'impl_item': ["where_clause"],
  'index_expression': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'inner_attribute_item': ["attribute"],
  'label': ["identifier"],
  'let_chain': ["_expression","let_condition"],
  'let_declaration': ["mutable_specifier"],
  'lifetime': ["identifier"],
  'loop_expression': ["label"],
  'macro_definition': ["macro_rule"],
  'macro_invocation': ["token_tree"],
  'match_arm': ["attribute_item","inner_attribute_item"],
  'match_block': ["match_arm"],
  'match_pattern': ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"],
  'mod_item': ["visibility_modifier"],
  'mut_pattern': ["mutable_specifier","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"],
  'negative_literal': ["integer_literal","float_literal"],
  'or_pattern': ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"],
  'ordered_field_declaration_list': ["attribute_item","visibility_modifier"],
  'parameter': ["mutable_specifier"],
  'parameters': ["attribute_item","parameter","self_parameter","variadic_parameter","abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"],
  'parenthesized_expression': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'pointer_type': ["mutable_specifier"],
  'range_expression': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'raw_string_literal': ["string_content"],
  'ref_pattern': ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"],
  'reference_expression': ["mutable_specifier"],
  'reference_pattern': ["mutable_specifier","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"],
  'reference_type': ["lifetime","mutable_specifier"],
  'removed_trait_bound': ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"],
  'return_expression': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'self_parameter': ["lifetime","mutable_specifier","self"],
  'shorthand_field_initializer': ["attribute_item","identifier"],
  'slice_pattern': ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"],
  'source_file': ["shebang","expression_statement","const_item","macro_invocation","macro_definition","empty_statement","attribute_item","inner_attribute_item","mod_item","foreign_mod_item","struct_item","union_item","enum_item","type_item","function_item","function_signature_item","impl_item","trait_item","associated_type","let_declaration","use_declaration","extern_crate_declaration","static_item"],
  'static_item': ["visibility_modifier","mutable_specifier"],
  'string_literal': ["escape_sequence","string_content"],
  'struct_item': ["visibility_modifier","where_clause"],
  'struct_pattern': ["field_pattern","remaining_field_pattern"],
  'token_repetition': ["token_tree","token_repetition","metavariable","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","mutable_specifier","self","super","crate","primitive_type"],
  'token_repetition_pattern': ["token_tree_pattern","token_repetition_pattern","token_binding_pattern","metavariable","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","mutable_specifier","self","super","crate","primitive_type"],
  'token_tree': ["token_tree","token_repetition","metavariable","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","mutable_specifier","self","super","crate","primitive_type"],
  'token_tree_pattern': ["token_tree_pattern","token_repetition_pattern","token_binding_pattern","metavariable","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","mutable_specifier","self","super","crate","primitive_type"],
  'trait_bounds': ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type","lifetime","higher_ranked_trait_bound"],
  'trait_item': ["visibility_modifier","where_clause"],
  'try_block': ["block"],
  'try_expression': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'tuple_expression': ["attribute_item","unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'tuple_pattern': ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation","closure_expression"],
  'tuple_struct_pattern': ["string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","negative_literal","identifier","scoped_identifier","generic_pattern","tuple_pattern","tuple_struct_pattern","struct_pattern","ref_pattern","slice_pattern","captured_pattern","reference_pattern","remaining_field_pattern","mut_pattern","range_pattern","or_pattern","const_block","macro_invocation"],
  'tuple_type': ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type"],
  'type_arguments': ["abstract_type","reference_type","metavariable","pointer_type","generic_type","scoped_type_identifier","tuple_type","unit_type","array_type","function_type","type_identifier","macro_invocation","never_type","dynamic_type","bounded_type","removed_trait_bound","primitive_type","type_binding","lifetime","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","block","trait_bounds"],
  'type_item': ["visibility_modifier","where_clause"],
  'type_parameters': ["attribute_item","metavariable","type_parameter","lifetime_parameter","const_parameter"],
  'unary_expression': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
  'union_item': ["visibility_modifier","where_clause"],
  'unsafe_block': ["block"],
  'use_bounds': ["lifetime","type_identifier"],
  'use_declaration': ["visibility_modifier"],
  'use_list': ["self","identifier","metavariable","super","crate","scoped_identifier","use_as_clause","use_list","scoped_use_list","use_wildcard"],
  'use_wildcard': ["self","identifier","metavariable","super","crate","scoped_identifier"],
  'variadic_parameter': ["mutable_specifier"],
  'visibility_modifier': ["crate","self","super","identifier","metavariable","scoped_identifier"],
  'where_clause': ["where_predicate"],
  'while_expression': ["label"],
  'yield_expression': ["unary_expression","reference_expression","try_expression","binary_expression","assignment_expression","compound_assignment_expr","type_cast_expression","call_expression","return_expression","yield_expression","string_literal","raw_string_literal","char_literal","boolean_literal","integer_literal","float_literal","identifier","self","scoped_identifier","generic_function","await_expression","field_expression","array_expression","tuple_expression","macro_invocation","unit_expression","break_expression","continue_expression","index_expression","metavariable","closure_expression","parenthesized_expression","struct_expression","unsafe_block","async_block","gen_block","try_block","block","if_expression","match_expression","while_expression","loop_expression","for_expression","const_block","range_expression"],
};

const FROM_LEAF_SET = new Set(["boolean_literal","empty_statement","fragment_specifier","inner_doc_comment_marker","never_type","outer_doc_comment_marker","remaining_field_pattern","unit_expression","unit_type","char_literal","crate","doc_comment","escape_sequence","field_identifier","float_literal","identifier","integer_literal","metavariable","mutable_specifier","primitive_type","self","shebang","shorthand_field_identifier","string_content","super","type_identifier"]);
const FROM_BRANCH_SET = new Set(["abstract_type","arguments","array_expression","array_type","assignment_expression","associated_type","async_block","attribute","attribute_item","await_expression","base_field_initializer","binary_expression","block","block_comment","bounded_type","bracketed_type","break_expression","call_expression","captured_pattern","closure_expression","closure_parameters","compound_assignment_expr","const_block","const_item","const_parameter","continue_expression","declaration_list","dynamic_type","else_clause","enum_item","enum_variant","enum_variant_list","expression_statement","extern_crate_declaration","extern_modifier","field_declaration","field_declaration_list","field_expression","field_initializer","field_initializer_list","field_pattern","for_expression","for_lifetimes","foreign_mod_item","function_item","function_modifiers","function_signature_item","function_type","gen_block","generic_function","generic_pattern","generic_type","generic_type_with_turbofish","higher_ranked_trait_bound","if_expression","impl_item","index_expression","inner_attribute_item","label","let_chain","let_condition","let_declaration","lifetime","lifetime_parameter","line_comment","loop_expression","macro_definition","macro_invocation","macro_rule","match_arm","match_block","match_expression","match_pattern","mod_item","mut_pattern","negative_literal","or_pattern","ordered_field_declaration_list","parameter","parameters","parenthesized_expression","pointer_type","qualified_type","range_expression","range_pattern","raw_string_literal","ref_pattern","reference_expression","reference_pattern","reference_type","removed_trait_bound","return_expression","scoped_identifier","scoped_type_identifier","scoped_use_list","self_parameter","shorthand_field_initializer","slice_pattern","source_file","static_item","string_literal","struct_expression","struct_item","struct_pattern","token_binding_pattern","token_repetition","token_repetition_pattern","token_tree","token_tree_pattern","trait_bounds","trait_item","try_block","try_expression","tuple_expression","tuple_pattern","tuple_struct_pattern","tuple_type","type_arguments","type_binding","type_cast_expression","type_item","type_parameter","type_parameters","unary_expression","union_item","unsafe_block","use_as_clause","use_bounds","use_declaration","use_list","use_wildcard","variadic_parameter","visibility_modifier","where_clause","where_predicate","while_expression","yield_expression"]);

const FROM_LEAF_VALUES: Record<string, readonly string[]> = {
  'boolean_literal': ["false","true"],
  'fragment_specifier': ["block","expr","expr_2021","ident","item","lifetime","literal","meta","pat","pat_param","path","stmt","tt","ty","vis"],
  'primitive_type': ["bool","char","f32","f64","i128","i16","i32","i64","i8","isize","str","u128","u16","u32","u64","u8","usize"],
};

const FROM_LEAF_FACTORIES: Record<string, (text: string) => any> = {
  'boolean_literal': (t: string) => booleanLiteral(t as any),
  'empty_statement': () => emptyStatement(),
  'fragment_specifier': (t: string) => fragmentSpecifier(t as any),
  'inner_doc_comment_marker': (t: string) => innerDocCommentMarker(t as any),
  'never_type': () => neverType(),
  'outer_doc_comment_marker': (t: string) => outerDocCommentMarker(t as any),
  'remaining_field_pattern': () => remainingFieldPattern(),
  'unit_expression': () => unitExpression(),
  'unit_type': () => unitType(),
  'char_literal': (t: string) => charLiteral(t as any),
  'crate': () => crate(),
  'doc_comment': (t: string) => docComment(t as any),
  'escape_sequence': (t: string) => escapeSequence(t as any),
  'field_identifier': (t: string) => fieldIdentifier(t as any),
  'float_literal': (t: string) => floatLiteral(t as any),
  'identifier': (t: string) => identifier(t as any),
  'integer_literal': (t: string) => integerLiteral(t as any),
  'metavariable': (t: string) => metavariable(t as any),
  'mutable_specifier': () => mutableSpecifier(),
  'primitive_type': (t: string) => primitiveType(t as any),
  'self': () => self(),
  'shebang': (t: string) => shebang(t as any),
  'shorthand_field_identifier': (t: string) => shorthandFieldIdentifier(t as any),
  'string_content': (t: string) => stringContent(t as any),
  'super': () => super_(),
  'type_identifier': (t: string) => typeIdentifier(t as any),
};

const FROM_BRANCH_FACTORIES: Record<string, (config: any) => any> = {
  'abstract_type': (c: any) => abstractType(c),
  'arguments': (c: any) => arguments_(c),
  'array_expression': (c: any) => arrayExpression(c),
  'array_type': (c: any) => arrayType(c),
  'assignment_expression': (c: any) => assignmentExpression(c),
  'associated_type': (c: any) => associatedType(c),
  'async_block': (c: any) => asyncBlock(c),
  'attribute': (c: any) => attribute(c),
  'attribute_item': (c: any) => attributeItem(c),
  'await_expression': (c: any) => awaitExpression(c),
  'base_field_initializer': (c: any) => baseFieldInitializer(c),
  'binary_expression': (c: any) => binaryExpression(c),
  'block': (c: any) => block(c),
  'block_comment': (c: any) => blockComment(c),
  'bounded_type': (c: any) => boundedType(c),
  'bracketed_type': (c: any) => bracketedType(c),
  'break_expression': (c: any) => breakExpression(c),
  'call_expression': (c: any) => callExpression(c),
  'captured_pattern': (c: any) => capturedPattern(c),
  'closure_expression': (c: any) => closureExpression(c),
  'closure_parameters': (c: any) => closureParameters(c),
  'compound_assignment_expr': (c: any) => compoundAssignmentExpr(c),
  'const_block': (c: any) => constBlock(c),
  'const_item': (c: any) => constItem(c),
  'const_parameter': (c: any) => constParameter(c),
  'continue_expression': (c: any) => continueExpression(c),
  'declaration_list': (c: any) => declarationList(c),
  'dynamic_type': (c: any) => dynamicType(c),
  'else_clause': (c: any) => elseClause(c),
  'enum_item': (c: any) => enumItem(c),
  'enum_variant': (c: any) => enumVariant(c),
  'enum_variant_list': (c: any) => enumVariantList(c),
  'expression_statement': (c: any) => expressionStatement(c),
  'extern_crate_declaration': (c: any) => externCrateDeclaration(c),
  'extern_modifier': (c: any) => externModifier(c),
  'field_declaration': (c: any) => fieldDeclaration(c),
  'field_declaration_list': (c: any) => fieldDeclarationList(c),
  'field_expression': (c: any) => fieldExpression(c),
  'field_initializer': (c: any) => fieldInitializer(c),
  'field_initializer_list': (c: any) => fieldInitializerList(c),
  'field_pattern': (c: any) => fieldPattern(c),
  'for_expression': (c: any) => forExpression(c),
  'for_lifetimes': (c: any) => forLifetimes(c),
  'foreign_mod_item': (c: any) => foreignModItem(c),
  'function_item': (c: any) => functionItem(c),
  'function_modifiers': (c: any) => functionModifiers(c),
  'function_signature_item': (c: any) => functionSignatureItem(c),
  'function_type': (c: any) => functionType(c),
  'gen_block': (c: any) => genBlock(c),
  'generic_function': (c: any) => genericFunction(c),
  'generic_pattern': (c: any) => genericPattern(c),
  'generic_type': (c: any) => genericType(c),
  'generic_type_with_turbofish': (c: any) => genericTypeWithTurbofish(c),
  'higher_ranked_trait_bound': (c: any) => higherRankedTraitBound(c),
  'if_expression': (c: any) => ifExpression(c),
  'impl_item': (c: any) => implItem(c),
  'index_expression': (c: any) => indexExpression(c),
  'inner_attribute_item': (c: any) => innerAttributeItem(c),
  'label': (c: any) => label(c),
  'let_chain': (c: any) => letChain(c),
  'let_condition': (c: any) => letCondition(c),
  'let_declaration': (c: any) => letDeclaration(c),
  'lifetime': (c: any) => lifetime(c),
  'lifetime_parameter': (c: any) => lifetimeParameter(c),
  'line_comment': (c: any) => lineComment(c),
  'loop_expression': (c: any) => loopExpression(c),
  'macro_definition': (c: any) => macroDefinition(c),
  'macro_invocation': (c: any) => macroInvocation(c),
  'macro_rule': (c: any) => macroRule(c),
  'match_arm': (c: any) => matchArm(c),
  'match_block': (c: any) => matchBlock(c),
  'match_expression': (c: any) => matchExpression(c),
  'match_pattern': (c: any) => matchPattern(c),
  'mod_item': (c: any) => modItem(c),
  'mut_pattern': (c: any) => mutPattern(c),
  'negative_literal': (c: any) => negativeLiteral(c),
  'or_pattern': (c: any) => orPattern(c),
  'ordered_field_declaration_list': (c: any) => orderedFieldDeclarationList(c),
  'parameter': (c: any) => parameter(c),
  'parameters': (c: any) => parameters(c),
  'parenthesized_expression': (c: any) => parenthesizedExpression(c),
  'pointer_type': (c: any) => pointerType(c),
  'qualified_type': (c: any) => qualifiedType(c),
  'range_expression': (c: any) => rangeExpression(c),
  'range_pattern': (c: any) => rangePattern(c),
  'raw_string_literal': (c: any) => rawStringLiteral(c),
  'ref_pattern': (c: any) => refPattern(c),
  'reference_expression': (c: any) => referenceExpression(c),
  'reference_pattern': (c: any) => referencePattern(c),
  'reference_type': (c: any) => referenceType(c),
  'removed_trait_bound': (c: any) => removedTraitBound(c),
  'return_expression': (c: any) => returnExpression(c),
  'scoped_identifier': (c: any) => scopedIdentifier(c),
  'scoped_type_identifier': (c: any) => scopedTypeIdentifier(c),
  'scoped_use_list': (c: any) => scopedUseList(c),
  'self_parameter': (c: any) => selfParameter(c),
  'shorthand_field_initializer': (c: any) => shorthandFieldInitializer(c),
  'slice_pattern': (c: any) => slicePattern(c),
  'source_file': (c: any) => sourceFile(c),
  'static_item': (c: any) => staticItem(c),
  'string_literal': (c: any) => stringLiteral(c),
  'struct_expression': (c: any) => structExpression(c),
  'struct_item': (c: any) => structItem(c),
  'struct_pattern': (c: any) => structPattern(c),
  'token_binding_pattern': (c: any) => tokenBindingPattern(c),
  'token_repetition': (c: any) => tokenRepetition(c),
  'token_repetition_pattern': (c: any) => tokenRepetitionPattern(c),
  'token_tree': (c: any) => tokenTree(c),
  'token_tree_pattern': (c: any) => tokenTreePattern(c),
  'trait_bounds': (c: any) => traitBounds(c),
  'trait_item': (c: any) => traitItem(c),
  'try_block': (c: any) => tryBlock(c),
  'try_expression': (c: any) => tryExpression(c),
  'tuple_expression': (c: any) => tupleExpression(c),
  'tuple_pattern': (c: any) => tuplePattern(c),
  'tuple_struct_pattern': (c: any) => tupleStructPattern(c),
  'tuple_type': (c: any) => tupleType(c),
  'type_arguments': (c: any) => typeArguments(c),
  'type_binding': (c: any) => typeBinding(c),
  'type_cast_expression': (c: any) => typeCastExpression(c),
  'type_item': (c: any) => typeItem(c),
  'type_parameter': (c: any) => typeParameter(c),
  'type_parameters': (c: any) => typeParameters(c),
  'unary_expression': (c: any) => unaryExpression(c),
  'union_item': (c: any) => unionItem(c),
  'unsafe_block': (c: any) => unsafeBlock(c),
  'use_as_clause': (c: any) => useAsClause(c),
  'use_bounds': (c: any) => useBounds(c),
  'use_declaration': (c: any) => useDeclaration(c),
  'use_list': (c: any) => useList(c),
  'use_wildcard': (c: any) => useWildcard(c),
  'variadic_parameter': (c: any) => variadicParameter(c),
  'visibility_modifier': (c: any) => visibilityModifier(c),
  'where_clause': (c: any) => whereClause(c),
  'where_predicate': (c: any) => wherePredicate(c),
  'while_expression': (c: any) => whileExpression(c),
  'yield_expression': (c: any) => yieldExpression(c),
};

let _fromCtx: FromContext | null = null;
function getFromContext(): FromContext {
  if (_fromCtx) return _fromCtx;
  _fromCtx = {
    getFields: (k) => FROM_FIELD_TYPES[k],
    getChildrenTypes: (k) => FROM_CHILDREN_TYPES[k],
    isLeaf: (k) => FROM_LEAF_SET.has(k),
    isBranch: (k) => FROM_BRANCH_SET.has(k),
    getLeafValues: (k) => FROM_LEAF_VALUES[k],
    createLeaf: (kind, text) => {
      const f = FROM_LEAF_FACTORIES[kind];
      if (!f) throw new Error(`No leaf factory for kind '${kind}'`);
      return f(text);
    },
    createBranch: (kind, config) => {
      const f = FROM_BRANCH_FACTORIES[kind];
      if (!f) throw new Error(`No branch factory for kind '${kind}'`);
      return f(config);
    },
  };
  return _fromCtx;
}

export namespace abstractType {
  export function from(input: AbstractTypeFromInput): AbstractTypeNode {
    return resolveFromInput('abstract_type', input as any, getFromContext()) as any;
  }
}

export namespace arguments_ {
  export function from(input: ArgumentsFromInput): ArgumentsNode {
    return resolveFromInput('arguments', input as any, getFromContext()) as any;
  }
}

export namespace arrayExpression {
  export function from(input: ArrayExpressionFromInput): ArrayExpressionNode {
    return resolveFromInput('array_expression', input as any, getFromContext()) as any;
  }
}

export namespace arrayType {
  export function from(input: ArrayTypeFromInput): ArrayTypeNode {
    return resolveFromInput('array_type', input as any, getFromContext()) as any;
  }
}

export namespace assignmentExpression {
  export function from(input: AssignmentExpressionFromInput): AssignmentExpressionNode {
    return resolveFromInput('assignment_expression', input as any, getFromContext()) as any;
  }
}

export namespace associatedType {
  export function from(input: AssociatedTypeFromInput): AssociatedTypeNode {
    return resolveFromInput('associated_type', input as any, getFromContext()) as any;
  }
}

export namespace asyncBlock {
  export function from(input: AsyncBlockFromInput): AsyncBlockNode {
    return resolveFromInput('async_block', input as any, getFromContext()) as any;
  }
}

export namespace attribute {
  export function from(input: AttributeFromInput): AttributeNode {
    return resolveFromInput('attribute', input as any, getFromContext()) as any;
  }
}

export namespace attributeItem {
  export function from(input: AttributeItemFromInput): AttributeItemNode {
    return resolveFromInput('attribute_item', input as any, getFromContext()) as any;
  }
}

export namespace awaitExpression {
  export function from(input: AwaitExpressionFromInput): AwaitExpressionNode {
    return resolveFromInput('await_expression', input as any, getFromContext()) as any;
  }
}

export namespace baseFieldInitializer {
  export function from(input: BaseFieldInitializerFromInput): BaseFieldInitializerNode {
    return resolveFromInput('base_field_initializer', input as any, getFromContext()) as any;
  }
}

export namespace binaryExpression {
  export function from(input: BinaryExpressionFromInput): BinaryExpressionNode {
    return resolveFromInput('binary_expression', input as any, getFromContext()) as any;
  }
}

export namespace block {
  export function from(input: BlockFromInput): BlockNode {
    return resolveFromInput('block', input as any, getFromContext()) as any;
  }
}

export namespace blockComment {
  export function from(input: BlockCommentFromInput): BlockCommentNode {
    return resolveFromInput('block_comment', input as any, getFromContext()) as any;
  }
}

export namespace boundedType {
  export function from(input: BoundedTypeFromInput): BoundedTypeNode {
    return resolveFromInput('bounded_type', input as any, getFromContext()) as any;
  }
}

export namespace bracketedType {
  export function from(input: BracketedTypeFromInput): BracketedTypeNode {
    return resolveFromInput('bracketed_type', input as any, getFromContext()) as any;
  }
}

export namespace breakExpression {
  export function from(input: BreakExpressionFromInput): BreakExpressionNode {
    return resolveFromInput('break_expression', input as any, getFromContext()) as any;
  }
}

export namespace callExpression {
  export function from(input: CallExpressionFromInput): CallExpressionNode {
    return resolveFromInput('call_expression', input as any, getFromContext()) as any;
  }
}

export namespace capturedPattern {
  export function from(input: CapturedPatternFromInput): CapturedPatternNode {
    return resolveFromInput('captured_pattern', input as any, getFromContext()) as any;
  }
}

export namespace closureExpression {
  export function from(input: ClosureExpressionFromInput): ClosureExpressionNode {
    return resolveFromInput('closure_expression', input as any, getFromContext()) as any;
  }
}

export namespace closureParameters {
  export function from(input: ClosureParametersFromInput): ClosureParametersNode {
    return resolveFromInput('closure_parameters', input as any, getFromContext()) as any;
  }
}

export namespace compoundAssignmentExpr {
  export function from(input: CompoundAssignmentExprFromInput): CompoundAssignmentExprNode {
    return resolveFromInput('compound_assignment_expr', input as any, getFromContext()) as any;
  }
}

export namespace constBlock {
  export function from(input: ConstBlockFromInput): ConstBlockNode {
    return resolveFromInput('const_block', input as any, getFromContext()) as any;
  }
}

export namespace constItem {
  export function from(input: ConstItemFromInput): ConstItemNode {
    return resolveFromInput('const_item', input as any, getFromContext()) as any;
  }
}

export namespace constParameter {
  export function from(input: ConstParameterFromInput): ConstParameterNode {
    return resolveFromInput('const_parameter', input as any, getFromContext()) as any;
  }
}

export namespace continueExpression {
  export function from(input: ContinueExpressionFromInput): ContinueExpressionNode {
    return resolveFromInput('continue_expression', input as any, getFromContext()) as any;
  }
}

export namespace declarationList {
  export function from(input: DeclarationListFromInput): DeclarationListNode {
    return resolveFromInput('declaration_list', input as any, getFromContext()) as any;
  }
}

export namespace dynamicType {
  export function from(input: DynamicTypeFromInput): DynamicTypeNode {
    return resolveFromInput('dynamic_type', input as any, getFromContext()) as any;
  }
}

export namespace elseClause {
  export function from(input: ElseClauseFromInput): ElseClauseNode {
    return resolveFromInput('else_clause', input as any, getFromContext()) as any;
  }
}

export namespace enumItem {
  export function from(input: EnumItemFromInput): EnumItemNode {
    return resolveFromInput('enum_item', input as any, getFromContext()) as any;
  }
}

export namespace enumVariant {
  export function from(input: EnumVariantFromInput): EnumVariantNode {
    return resolveFromInput('enum_variant', input as any, getFromContext()) as any;
  }
}

export namespace enumVariantList {
  export function from(input: EnumVariantListFromInput): EnumVariantListNode {
    return resolveFromInput('enum_variant_list', input as any, getFromContext()) as any;
  }
}

export namespace expressionStatement {
  export function from(input: ExpressionStatementFromInput): ExpressionStatementNode {
    return resolveFromInput('expression_statement', input as any, getFromContext()) as any;
  }
}

export namespace externCrateDeclaration {
  export function from(input: ExternCrateDeclarationFromInput): ExternCrateDeclarationNode {
    return resolveFromInput('extern_crate_declaration', input as any, getFromContext()) as any;
  }
}

export namespace externModifier {
  export function from(input: ExternModifierFromInput): ExternModifierNode {
    return resolveFromInput('extern_modifier', input as any, getFromContext()) as any;
  }
}

export namespace fieldDeclaration {
  export function from(input: FieldDeclarationFromInput): FieldDeclarationNode {
    return resolveFromInput('field_declaration', input as any, getFromContext()) as any;
  }
}

export namespace fieldDeclarationList {
  export function from(input: FieldDeclarationListFromInput): FieldDeclarationListNode {
    return resolveFromInput('field_declaration_list', input as any, getFromContext()) as any;
  }
}

export namespace fieldExpression {
  export function from(input: FieldExpressionFromInput): FieldExpressionNode {
    return resolveFromInput('field_expression', input as any, getFromContext()) as any;
  }
}

export namespace fieldInitializer {
  export function from(input: FieldInitializerFromInput): FieldInitializerNode {
    return resolveFromInput('field_initializer', input as any, getFromContext()) as any;
  }
}

export namespace fieldInitializerList {
  export function from(input: FieldInitializerListFromInput): FieldInitializerListNode {
    return resolveFromInput('field_initializer_list', input as any, getFromContext()) as any;
  }
}

export namespace fieldPattern {
  export function from(input: FieldPatternFromInput): FieldPatternNode {
    return resolveFromInput('field_pattern', input as any, getFromContext()) as any;
  }
}

export namespace forExpression {
  export function from(input: ForExpressionFromInput): ForExpressionNode {
    return resolveFromInput('for_expression', input as any, getFromContext()) as any;
  }
}

export namespace forLifetimes {
  export function from(input: ForLifetimesFromInput): ForLifetimesNode {
    return resolveFromInput('for_lifetimes', input as any, getFromContext()) as any;
  }
}

export namespace foreignModItem {
  export function from(input: ForeignModItemFromInput): ForeignModItemNode {
    return resolveFromInput('foreign_mod_item', input as any, getFromContext()) as any;
  }
}

export namespace functionItem {
  export function from(input: FunctionItemFromInput): FunctionItemNode {
    return resolveFromInput('function_item', input as any, getFromContext()) as any;
  }
}

export namespace functionModifiers {
  export function from(input: FunctionModifiersFromInput): FunctionModifiersNode {
    return resolveFromInput('function_modifiers', input as any, getFromContext()) as any;
  }
}

export namespace functionSignatureItem {
  export function from(input: FunctionSignatureItemFromInput): FunctionSignatureItemNode {
    return resolveFromInput('function_signature_item', input as any, getFromContext()) as any;
  }
}

export namespace functionType {
  export function from(input: FunctionTypeFromInput): FunctionTypeNode {
    return resolveFromInput('function_type', input as any, getFromContext()) as any;
  }
}

export namespace genBlock {
  export function from(input: GenBlockFromInput): GenBlockNode {
    return resolveFromInput('gen_block', input as any, getFromContext()) as any;
  }
}

export namespace genericFunction {
  export function from(input: GenericFunctionFromInput): GenericFunctionNode {
    return resolveFromInput('generic_function', input as any, getFromContext()) as any;
  }
}

export namespace genericPattern {
  export function from(input: GenericPatternFromInput): GenericPatternNode {
    return resolveFromInput('generic_pattern', input as any, getFromContext()) as any;
  }
}

export namespace genericType {
  export function from(input: GenericTypeFromInput): GenericTypeNode {
    return resolveFromInput('generic_type', input as any, getFromContext()) as any;
  }
}

export namespace genericTypeWithTurbofish {
  export function from(input: GenericTypeWithTurbofishFromInput): GenericTypeWithTurbofishNode {
    return resolveFromInput('generic_type_with_turbofish', input as any, getFromContext()) as any;
  }
}

export namespace higherRankedTraitBound {
  export function from(input: HigherRankedTraitBoundFromInput): HigherRankedTraitBoundNode {
    return resolveFromInput('higher_ranked_trait_bound', input as any, getFromContext()) as any;
  }
}

export namespace ifExpression {
  export function from(input: IfExpressionFromInput): IfExpressionNode {
    return resolveFromInput('if_expression', input as any, getFromContext()) as any;
  }
}

export namespace implItem {
  export function from(input: ImplItemFromInput): ImplItemNode {
    return resolveFromInput('impl_item', input as any, getFromContext()) as any;
  }
}

export namespace indexExpression {
  export function from(input: IndexExpressionFromInput): IndexExpressionNode {
    return resolveFromInput('index_expression', input as any, getFromContext()) as any;
  }
}

export namespace innerAttributeItem {
  export function from(input: InnerAttributeItemFromInput): InnerAttributeItemNode {
    return resolveFromInput('inner_attribute_item', input as any, getFromContext()) as any;
  }
}

export namespace label {
  export function from(input: LabelFromInput): LabelNode {
    return resolveFromInput('label', input as any, getFromContext()) as any;
  }
}

export namespace letChain {
  export function from(input: LetChainFromInput): LetChainNode {
    return resolveFromInput('let_chain', input as any, getFromContext()) as any;
  }
}

export namespace letCondition {
  export function from(input: LetConditionFromInput): LetConditionNode {
    return resolveFromInput('let_condition', input as any, getFromContext()) as any;
  }
}

export namespace letDeclaration {
  export function from(input: LetDeclarationFromInput): LetDeclarationNode {
    return resolveFromInput('let_declaration', input as any, getFromContext()) as any;
  }
}

export namespace lifetime {
  export function from(input: LifetimeFromInput): LifetimeNode {
    return resolveFromInput('lifetime', input as any, getFromContext()) as any;
  }
}

export namespace lifetimeParameter {
  export function from(input: LifetimeParameterFromInput): LifetimeParameterNode {
    return resolveFromInput('lifetime_parameter', input as any, getFromContext()) as any;
  }
}

export namespace lineComment {
  export function from(input: LineCommentFromInput): LineCommentNode {
    return resolveFromInput('line_comment', input as any, getFromContext()) as any;
  }
}

export namespace loopExpression {
  export function from(input: LoopExpressionFromInput): LoopExpressionNode {
    return resolveFromInput('loop_expression', input as any, getFromContext()) as any;
  }
}

export namespace macroDefinition {
  export function from(input: MacroDefinitionFromInput): MacroDefinitionNode {
    return resolveFromInput('macro_definition', input as any, getFromContext()) as any;
  }
}

export namespace macroInvocation {
  export function from(input: MacroInvocationFromInput): MacroInvocationNode {
    return resolveFromInput('macro_invocation', input as any, getFromContext()) as any;
  }
}

export namespace macroRule {
  export function from(input: MacroRuleFromInput): MacroRuleNode {
    return resolveFromInput('macro_rule', input as any, getFromContext()) as any;
  }
}

export namespace matchArm {
  export function from(input: MatchArmFromInput): MatchArmNode {
    return resolveFromInput('match_arm', input as any, getFromContext()) as any;
  }
}

export namespace matchBlock {
  export function from(input: MatchBlockFromInput): MatchBlockNode {
    return resolveFromInput('match_block', input as any, getFromContext()) as any;
  }
}

export namespace matchExpression {
  export function from(input: MatchExpressionFromInput): MatchExpressionNode {
    return resolveFromInput('match_expression', input as any, getFromContext()) as any;
  }
}

export namespace matchPattern {
  export function from(input: MatchPatternFromInput): MatchPatternNode {
    return resolveFromInput('match_pattern', input as any, getFromContext()) as any;
  }
}

export namespace modItem {
  export function from(input: ModItemFromInput): ModItemNode {
    return resolveFromInput('mod_item', input as any, getFromContext()) as any;
  }
}

export namespace mutPattern {
  export function from(input: MutPatternFromInput): MutPatternNode {
    return resolveFromInput('mut_pattern', input as any, getFromContext()) as any;
  }
}

export namespace negativeLiteral {
  export function from(input: NegativeLiteralFromInput): NegativeLiteralNode {
    return resolveFromInput('negative_literal', input as any, getFromContext()) as any;
  }
}

export namespace orPattern {
  export function from(input: OrPatternFromInput): OrPatternNode {
    return resolveFromInput('or_pattern', input as any, getFromContext()) as any;
  }
}

export namespace orderedFieldDeclarationList {
  export function from(input: OrderedFieldDeclarationListFromInput): OrderedFieldDeclarationListNode {
    return resolveFromInput('ordered_field_declaration_list', input as any, getFromContext()) as any;
  }
}

export namespace parameter {
  export function from(input: ParameterFromInput): ParameterNode {
    return resolveFromInput('parameter', input as any, getFromContext()) as any;
  }
}

export namespace parameters {
  export function from(input: ParametersFromInput): ParametersNode {
    return resolveFromInput('parameters', input as any, getFromContext()) as any;
  }
}

export namespace parenthesizedExpression {
  export function from(input: ParenthesizedExpressionFromInput): ParenthesizedExpressionNode {
    return resolveFromInput('parenthesized_expression', input as any, getFromContext()) as any;
  }
}

export namespace pointerType {
  export function from(input: PointerTypeFromInput): PointerTypeNode {
    return resolveFromInput('pointer_type', input as any, getFromContext()) as any;
  }
}

export namespace qualifiedType {
  export function from(input: QualifiedTypeFromInput): QualifiedTypeNode {
    return resolveFromInput('qualified_type', input as any, getFromContext()) as any;
  }
}

export namespace rangeExpression {
  export function from(input: RangeExpressionFromInput): RangeExpressionNode {
    return resolveFromInput('range_expression', input as any, getFromContext()) as any;
  }
}

export namespace rangePattern {
  export function from(input: RangePatternFromInput): RangePatternNode {
    return resolveFromInput('range_pattern', input as any, getFromContext()) as any;
  }
}

export namespace rawStringLiteral {
  export function from(input: RawStringLiteralFromInput): RawStringLiteralNode {
    return resolveFromInput('raw_string_literal', input as any, getFromContext()) as any;
  }
}

export namespace refPattern {
  export function from(input: RefPatternFromInput): RefPatternNode {
    return resolveFromInput('ref_pattern', input as any, getFromContext()) as any;
  }
}

export namespace referenceExpression {
  export function from(input: ReferenceExpressionFromInput): ReferenceExpressionNode {
    return resolveFromInput('reference_expression', input as any, getFromContext()) as any;
  }
}

export namespace referencePattern {
  export function from(input: ReferencePatternFromInput): ReferencePatternNode {
    return resolveFromInput('reference_pattern', input as any, getFromContext()) as any;
  }
}

export namespace referenceType {
  export function from(input: ReferenceTypeFromInput): ReferenceTypeNode {
    return resolveFromInput('reference_type', input as any, getFromContext()) as any;
  }
}

export namespace removedTraitBound {
  export function from(input: RemovedTraitBoundFromInput): RemovedTraitBoundNode {
    return resolveFromInput('removed_trait_bound', input as any, getFromContext()) as any;
  }
}

export namespace returnExpression {
  export function from(input: ReturnExpressionFromInput): ReturnExpressionNode {
    return resolveFromInput('return_expression', input as any, getFromContext()) as any;
  }
}

export namespace scopedIdentifier {
  export function from(input: ScopedIdentifierFromInput): ScopedIdentifierNode {
    return resolveFromInput('scoped_identifier', input as any, getFromContext()) as any;
  }
}

export namespace scopedTypeIdentifier {
  export function from(input: ScopedTypeIdentifierFromInput): ScopedTypeIdentifierNode {
    return resolveFromInput('scoped_type_identifier', input as any, getFromContext()) as any;
  }
}

export namespace scopedUseList {
  export function from(input: ScopedUseListFromInput): ScopedUseListNode {
    return resolveFromInput('scoped_use_list', input as any, getFromContext()) as any;
  }
}

export namespace selfParameter {
  export function from(input: SelfParameterFromInput): SelfParameterNode {
    return resolveFromInput('self_parameter', input as any, getFromContext()) as any;
  }
}

export namespace shorthandFieldInitializer {
  export function from(input: ShorthandFieldInitializerFromInput): ShorthandFieldInitializerNode {
    return resolveFromInput('shorthand_field_initializer', input as any, getFromContext()) as any;
  }
}

export namespace slicePattern {
  export function from(input: SlicePatternFromInput): SlicePatternNode {
    return resolveFromInput('slice_pattern', input as any, getFromContext()) as any;
  }
}

export namespace sourceFile {
  export function from(input: SourceFileFromInput): SourceFileNode {
    return resolveFromInput('source_file', input as any, getFromContext()) as any;
  }
}

export namespace staticItem {
  export function from(input: StaticItemFromInput): StaticItemNode {
    return resolveFromInput('static_item', input as any, getFromContext()) as any;
  }
}

export namespace stringLiteral {
  export function from(input: StringLiteralFromInput): StringLiteralNode {
    return resolveFromInput('string_literal', input as any, getFromContext()) as any;
  }
}

export namespace structExpression {
  export function from(input: StructExpressionFromInput): StructExpressionNode {
    return resolveFromInput('struct_expression', input as any, getFromContext()) as any;
  }
}

export namespace structItem {
  export function from(input: StructItemFromInput): StructItemNode {
    return resolveFromInput('struct_item', input as any, getFromContext()) as any;
  }
}

export namespace structPattern {
  export function from(input: StructPatternFromInput): StructPatternNode {
    return resolveFromInput('struct_pattern', input as any, getFromContext()) as any;
  }
}

export namespace tokenBindingPattern {
  export function from(input: TokenBindingPatternFromInput): TokenBindingPatternNode {
    return resolveFromInput('token_binding_pattern', input as any, getFromContext()) as any;
  }
}

export namespace tokenRepetition {
  export function from(input: TokenRepetitionFromInput): TokenRepetitionNode {
    return resolveFromInput('token_repetition', input as any, getFromContext()) as any;
  }
}

export namespace tokenRepetitionPattern {
  export function from(input: TokenRepetitionPatternFromInput): TokenRepetitionPatternNode {
    return resolveFromInput('token_repetition_pattern', input as any, getFromContext()) as any;
  }
}

export namespace tokenTree {
  export function from(input: TokenTreeFromInput): TokenTreeNode {
    return resolveFromInput('token_tree', input as any, getFromContext()) as any;
  }
}

export namespace tokenTreePattern {
  export function from(input: TokenTreePatternFromInput): TokenTreePatternNode {
    return resolveFromInput('token_tree_pattern', input as any, getFromContext()) as any;
  }
}

export namespace traitBounds {
  export function from(input: TraitBoundsFromInput): TraitBoundsNode {
    return resolveFromInput('trait_bounds', input as any, getFromContext()) as any;
  }
}

export namespace traitItem {
  export function from(input: TraitItemFromInput): TraitItemNode {
    return resolveFromInput('trait_item', input as any, getFromContext()) as any;
  }
}

export namespace tryBlock {
  export function from(input: TryBlockFromInput): TryBlockNode {
    return resolveFromInput('try_block', input as any, getFromContext()) as any;
  }
}

export namespace tryExpression {
  export function from(input: TryExpressionFromInput): TryExpressionNode {
    return resolveFromInput('try_expression', input as any, getFromContext()) as any;
  }
}

export namespace tupleExpression {
  export function from(input: TupleExpressionFromInput): TupleExpressionNode {
    return resolveFromInput('tuple_expression', input as any, getFromContext()) as any;
  }
}

export namespace tuplePattern {
  export function from(input: TuplePatternFromInput): TuplePatternNode {
    return resolveFromInput('tuple_pattern', input as any, getFromContext()) as any;
  }
}

export namespace tupleStructPattern {
  export function from(input: TupleStructPatternFromInput): TupleStructPatternNode {
    return resolveFromInput('tuple_struct_pattern', input as any, getFromContext()) as any;
  }
}

export namespace tupleType {
  export function from(input: TupleTypeFromInput): TupleTypeNode {
    return resolveFromInput('tuple_type', input as any, getFromContext()) as any;
  }
}

export namespace typeArguments {
  export function from(input: TypeArgumentsFromInput): TypeArgumentsNode {
    return resolveFromInput('type_arguments', input as any, getFromContext()) as any;
  }
}

export namespace typeBinding {
  export function from(input: TypeBindingFromInput): TypeBindingNode {
    return resolveFromInput('type_binding', input as any, getFromContext()) as any;
  }
}

export namespace typeCastExpression {
  export function from(input: TypeCastExpressionFromInput): TypeCastExpressionNode {
    return resolveFromInput('type_cast_expression', input as any, getFromContext()) as any;
  }
}

export namespace typeItem {
  export function from(input: TypeItemFromInput): TypeItemNode {
    return resolveFromInput('type_item', input as any, getFromContext()) as any;
  }
}

export namespace typeParameter {
  export function from(input: TypeParameterFromInput): TypeParameterNode {
    return resolveFromInput('type_parameter', input as any, getFromContext()) as any;
  }
}

export namespace typeParameters {
  export function from(input: TypeParametersFromInput): TypeParametersNode {
    return resolveFromInput('type_parameters', input as any, getFromContext()) as any;
  }
}

export namespace unaryExpression {
  export function from(input: UnaryExpressionFromInput): UnaryExpressionNode {
    return resolveFromInput('unary_expression', input as any, getFromContext()) as any;
  }
}

export namespace unionItem {
  export function from(input: UnionItemFromInput): UnionItemNode {
    return resolveFromInput('union_item', input as any, getFromContext()) as any;
  }
}

export namespace unsafeBlock {
  export function from(input: UnsafeBlockFromInput): UnsafeBlockNode {
    return resolveFromInput('unsafe_block', input as any, getFromContext()) as any;
  }
}

export namespace useAsClause {
  export function from(input: UseAsClauseFromInput): UseAsClauseNode {
    return resolveFromInput('use_as_clause', input as any, getFromContext()) as any;
  }
}

export namespace useBounds {
  export function from(input: UseBoundsFromInput): UseBoundsNode {
    return resolveFromInput('use_bounds', input as any, getFromContext()) as any;
  }
}

export namespace useDeclaration {
  export function from(input: UseDeclarationFromInput): UseDeclarationNode {
    return resolveFromInput('use_declaration', input as any, getFromContext()) as any;
  }
}

export namespace useList {
  export function from(input: UseListFromInput): UseListNode {
    return resolveFromInput('use_list', input as any, getFromContext()) as any;
  }
}

export namespace useWildcard {
  export function from(input: UseWildcardFromInput): UseWildcardNode {
    return resolveFromInput('use_wildcard', input as any, getFromContext()) as any;
  }
}

export namespace variadicParameter {
  export function from(input: VariadicParameterFromInput): VariadicParameterNode {
    return resolveFromInput('variadic_parameter', input as any, getFromContext()) as any;
  }
}

export namespace visibilityModifier {
  export function from(input: VisibilityModifierFromInput): VisibilityModifierNode {
    return resolveFromInput('visibility_modifier', input as any, getFromContext()) as any;
  }
}

export namespace whereClause {
  export function from(input: WhereClauseFromInput): WhereClauseNode {
    return resolveFromInput('where_clause', input as any, getFromContext()) as any;
  }
}

export namespace wherePredicate {
  export function from(input: WherePredicateFromInput): WherePredicateNode {
    return resolveFromInput('where_predicate', input as any, getFromContext()) as any;
  }
}

export namespace whileExpression {
  export function from(input: WhileExpressionFromInput): WhileExpressionNode {
    return resolveFromInput('while_expression', input as any, getFromContext()) as any;
  }
}

export namespace yieldExpression {
  export function from(input: YieldExpressionFromInput): YieldExpressionNode {
    return resolveFromInput('yield_expression', input as any, getFromContext()) as any;
  }
}

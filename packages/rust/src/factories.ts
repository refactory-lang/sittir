// Auto-generated by @sittir/codegen — do not edit

import type {
  AbstractType,
  AbstractTypeConfig,
  AbstractTypeTree,
  Arguments,
  ArgumentsConfig,
  ArgumentsTree,
  ArrayExpression,
  ArrayExpressionConfig,
  ArrayExpressionTree,
  ArrayType,
  ArrayTypeConfig,
  ArrayTypeTree,
  AssignmentExpression,
  AssignmentExpressionConfig,
  AssignmentExpressionTree,
  AssociatedType,
  AssociatedTypeConfig,
  AssociatedTypeTree,
  AsyncBlock,
  AsyncBlockConfig,
  AsyncBlockTree,
  Attribute,
  AttributeConfig,
  AttributeItem,
  AttributeItemConfig,
  AttributeItemTree,
  AttributeTree,
  AwaitExpression,
  AwaitExpressionConfig,
  AwaitExpressionTree,
  BaseFieldInitializer,
  BaseFieldInitializerConfig,
  BaseFieldInitializerTree,
  BinaryExpression,
  BinaryExpressionConfig,
  BinaryExpressionTree,
  Block,
  BlockComment,
  BlockCommentConfig,
  BlockCommentContent,
  BlockCommentTree,
  BlockConfig,
  BlockTree,
  BooleanLiteral,
  BooleanLiteralTree,
  BoundedType,
  BoundedTypeConfig,
  BoundedTypeTree,
  BracketedType,
  BracketedTypeConfig,
  BracketedTypeTree,
  BreakExpression,
  BreakExpressionConfig,
  BreakExpressionTree,
  CallExpression,
  CallExpressionConfig,
  CallExpressionTree,
  CapturedPattern,
  CapturedPatternConfig,
  CapturedPatternTree,
  CharLiteral,
  CharLiteralTree,
  ClosureExpression,
  ClosureExpressionBody,
  ClosureExpressionBody2,
  ClosureExpressionBody2Config,
  ClosureExpressionBody2Tree,
  ClosureExpressionBodyConfig,
  ClosureExpressionBodyTree,
  ClosureExpressionConfig,
  ClosureExpressionTree,
  ClosureParameters,
  ClosureParametersConfig,
  ClosureParametersTree,
  Comment,
  CommentConfig,
  CommentTree,
  CompoundAssignmentExpr,
  CompoundAssignmentExprConfig,
  CompoundAssignmentExprTree,
  Condition,
  ConfigMap,
  ConstBlock,
  ConstBlockConfig,
  ConstBlockTree,
  ConstItem,
  ConstItemConfig,
  ConstItemTree,
  ConstParameter,
  ConstParameterConfig,
  ConstParameterTree,
  ContinueExpression,
  ContinueExpressionConfig,
  ContinueExpressionTree,
  Crate,
  CrateTree,
  DeclarationList,
  DeclarationListConfig,
  DeclarationListTree,
  DeclarationStatement,
  DelimTokenTree,
  DelimTokenTreeConfig,
  DelimTokenTreeTree,
  DelimTokens,
  DynamicType,
  DynamicTypeConfig,
  DynamicTypeTree,
  ElseClause,
  ElseClauseConfig,
  ElseClauseTree,
  EnumItem,
  EnumItemConfig,
  EnumItemTree,
  EnumVariant,
  EnumVariantConfig,
  EnumVariantList,
  EnumVariantListConfig,
  EnumVariantListTree,
  EnumVariantTree,
  EscapeSequence,
  EscapeSequenceTree,
  Expression,
  ExpressionEndingWithBlock,
  ExpressionExceptRange,
  ExpressionStatement,
  ExpressionStatementConfig,
  ExpressionStatementTree,
  ExternCrateDeclaration,
  ExternCrateDeclarationConfig,
  ExternCrateDeclarationTree,
  ExternModifier,
  ExternModifierConfig,
  ExternModifierTree,
  FieldDeclaration,
  FieldDeclarationConfig,
  FieldDeclarationList,
  FieldDeclarationListConfig,
  FieldDeclarationListTree,
  FieldDeclarationTree,
  FieldExpression,
  FieldExpressionConfig,
  FieldExpressionTree,
  FieldIdentifier,
  FieldIdentifierTree,
  FieldInitializer,
  FieldInitializerConfig,
  FieldInitializerList,
  FieldInitializerListConfig,
  FieldInitializerListTree,
  FieldInitializerTree,
  FieldPattern,
  FieldPatternColon,
  FieldPatternColonConfig,
  FieldPatternColonTree,
  FieldPatternConfig,
  FieldPatternName,
  FieldPatternNameConfig,
  FieldPatternNameTree,
  FieldPatternTree,
  FloatLiteral,
  FloatLiteralTree,
  ForExpression,
  ForExpressionConfig,
  ForExpressionTree,
  ForLifetimes,
  ForLifetimesConfig,
  ForLifetimesTree,
  ForeignModItem,
  ForeignModItemBody,
  ForeignModItemBodyConfig,
  ForeignModItemBodyTree,
  ForeignModItemConfig,
  ForeignModItemSemi,
  ForeignModItemSemiConfig,
  ForeignModItemSemiTree,
  ForeignModItemTree,
  FragmentSpecifier,
  FragmentSpecifierTree,
  FunctionItem,
  FunctionItemConfig,
  FunctionItemTree,
  FunctionModifiers,
  FunctionModifiersConfig,
  FunctionModifiersTree,
  FunctionSignatureItem,
  FunctionSignatureItemConfig,
  FunctionSignatureItemTree,
  FunctionType,
  FunctionTypeConfig,
  FunctionTypeTree,
  GenBlock,
  GenBlockConfig,
  GenBlockTree,
  GenericFunction,
  GenericFunctionConfig,
  GenericFunctionTree,
  GenericPattern,
  GenericPatternConfig,
  GenericPatternTree,
  GenericType,
  GenericTypeConfig,
  GenericTypeTree,
  GenericTypeWithTurbofish,
  GenericTypeWithTurbofishConfig,
  GenericTypeWithTurbofishTree,
  HigherRankedTraitBound,
  HigherRankedTraitBoundConfig,
  HigherRankedTraitBoundTree,
  Identifier,
  IdentifierTree,
  IfExpression,
  IfExpressionConfig,
  IfExpressionTree,
  ImplItem,
  ImplItemBody,
  ImplItemBodyConfig,
  ImplItemBodyTree,
  ImplItemConfig,
  ImplItemSemi,
  ImplItemSemiConfig,
  ImplItemSemiTree,
  ImplItemTree,
  IndexExpression,
  IndexExpressionConfig,
  IndexExpressionTree,
  InnerAttributeItem,
  InnerAttributeItemConfig,
  InnerAttributeItemTree,
  InnerDocCommentMarker,
  InnerDocCommentMarkerTree,
  IntegerLiteral,
  IntegerLiteralTree,
  KindMap,
  Label,
  LabelConfig,
  LabelTree,
  LastMatchArm,
  LastMatchArmConfig,
  LastMatchArmTree,
  LetChain,
  LetChainConfig,
  LetChainTree,
  LetCondition,
  LetConditionConfig,
  LetConditionTree,
  LetDeclaration,
  LetDeclarationConfig,
  LetDeclarationTree,
  Lifetime,
  LifetimeConfig,
  LifetimeParameter,
  LifetimeParameterConfig,
  LifetimeParameterTree,
  LifetimeTree,
  LineComment,
  LineCommentConfig,
  LineCommentTree,
  Literal,
  LiteralPattern,
  LoopExpression,
  LoopExpressionConfig,
  LoopExpressionTree,
  MacroDefinition,
  MacroDefinitionConfig,
  MacroDefinitionTree,
  MacroInvocation,
  MacroInvocationConfig,
  MacroInvocationTree,
  MacroRule,
  MacroRuleConfig,
  MacroRuleTree,
  MatchArm,
  MatchArmConfig,
  MatchArmTree,
  MatchBlock,
  MatchBlockConfig,
  MatchBlockTree,
  MatchExpression,
  MatchExpressionConfig,
  MatchExpressionTree,
  MatchPattern,
  MatchPatternConfig,
  MatchPatternTree,
  Metavariable,
  MetavariableTree,
  ModItem,
  ModItemBody,
  ModItemBodyConfig,
  ModItemBodyTree,
  ModItemConfig,
  ModItemSemi,
  ModItemSemiConfig,
  ModItemSemiTree,
  ModItemTree,
  MutPattern,
  MutPatternConfig,
  MutPatternTree,
  MutableSpecifier,
  MutableSpecifierTree,
  NegativeLiteral,
  NegativeLiteralConfig,
  NegativeLiteralTree,
  OrPattern,
  OrPatternConfig,
  OrPatternPipe,
  OrPatternPipe2,
  OrPatternPipe2Config,
  OrPatternPipe2Tree,
  OrPatternPipeConfig,
  OrPatternPipeTree,
  OrPatternTree,
  OrderedFieldDeclarationList,
  OrderedFieldDeclarationListConfig,
  OrderedFieldDeclarationListTree,
  OuterDocCommentMarker,
  OuterDocCommentMarkerTree,
  Parameter,
  ParameterConfig,
  ParameterTree,
  Parameters,
  ParametersConfig,
  ParametersTree,
  ParenthesizedExpression,
  ParenthesizedExpressionConfig,
  ParenthesizedExpressionTree,
  Path,
  Pattern,
  PointerType,
  PointerTypeConfig,
  PointerTypeTree,
  PrimitiveType,
  PrimitiveTypeTree,
  QualifiedType,
  QualifiedTypeConfig,
  QualifiedTypeTree,
  RangeExpression,
  RangeExpressionConfig,
  RangeExpressionOperator,
  RangeExpressionOperator2,
  RangeExpressionOperator2Config,
  RangeExpressionOperator2Tree,
  RangeExpressionOperatorConfig,
  RangeExpressionOperatorTree,
  RangeExpressionStart,
  RangeExpressionStart2,
  RangeExpressionStart2Config,
  RangeExpressionStart2Tree,
  RangeExpressionStartConfig,
  RangeExpressionStartTree,
  RangeExpressionTree,
  RangePattern,
  RangePatternConfig,
  RangePatternLeft,
  RangePatternLeftConfig,
  RangePatternLeftTree,
  RangePatternRight,
  RangePatternRightConfig,
  RangePatternRightTree,
  RangePatternTree,
  RawStringLiteral,
  RawStringLiteralConfig,
  RawStringLiteralContent,
  RawStringLiteralContentTree,
  RawStringLiteralTree,
  RefPattern,
  RefPatternConfig,
  RefPatternTree,
  ReferenceExpression,
  ReferenceExpressionConfig,
  ReferenceExpressionTree,
  ReferencePattern,
  ReferencePatternConfig,
  ReferencePatternTree,
  ReferenceType,
  ReferenceTypeConfig,
  ReferenceTypeTree,
  RemainingFieldPattern,
  RemovedTraitBound,
  RemovedTraitBoundConfig,
  RemovedTraitBoundTree,
  ReservedIdentifier,
  ReturnExpression,
  ReturnExpressionConfig,
  ReturnExpressionTree,
  ScopedIdentifier,
  ScopedIdentifierConfig,
  ScopedIdentifierTree,
  ScopedTypeIdentifier,
  ScopedTypeIdentifierConfig,
  ScopedTypeIdentifierInExpressionPosition,
  ScopedTypeIdentifierInExpressionPositionConfig,
  ScopedTypeIdentifierInExpressionPositionTree,
  ScopedTypeIdentifierTree,
  ScopedUseList,
  ScopedUseListConfig,
  ScopedUseListTree,
  Self,
  SelfParameter,
  SelfParameterConfig,
  SelfParameterTree,
  SelfTree,
  Shebang,
  ShebangTree,
  ShorthandFieldIdentifier,
  ShorthandFieldIdentifierTree,
  ShorthandFieldInitializer,
  ShorthandFieldInitializerConfig,
  ShorthandFieldInitializerTree,
  SlicePattern,
  SlicePatternConfig,
  SlicePatternTree,
  SourceFile,
  SourceFileConfig,
  SourceFileTree,
  Statement,
  StaticItem,
  StaticItemConfig,
  StaticItemTree,
  StringContent,
  StringContentTree,
  StringLiteral,
  StringLiteralConfig,
  StringLiteralTree,
  StructExpression,
  StructExpressionConfig,
  StructExpressionTree,
  StructItem,
  StructItemBody,
  StructItemBodyConfig,
  StructItemBodyTree,
  StructItemConfig,
  StructItemSemi,
  StructItemSemi2,
  StructItemSemi2Config,
  StructItemSemi2Tree,
  StructItemSemiConfig,
  StructItemSemiTree,
  StructItemTree,
  StructPattern,
  StructPatternConfig,
  StructPatternTree,
  Super,
  SuperTree,
  TokenBindingPattern,
  TokenBindingPatternConfig,
  TokenBindingPatternTree,
  TokenPattern,
  TokenRepetition,
  TokenRepetitionConfig,
  TokenRepetitionPattern,
  TokenRepetitionPatternConfig,
  TokenRepetitionPatternTree,
  TokenRepetitionTree,
  TokenTree,
  TokenTreeConfig,
  TokenTreePattern,
  TokenTreePatternConfig,
  TokenTreePatternTree,
  TokenTreeTree,
  Tokens,
  TraitBounds,
  TraitBoundsConfig,
  TraitBoundsTree,
  TraitItem,
  TraitItemConfig,
  TraitItemTree,
  TryBlock,
  TryBlockConfig,
  TryBlockTree,
  TryExpression,
  TryExpressionConfig,
  TryExpressionTree,
  TupleExpression,
  TupleExpressionConfig,
  TupleExpressionTree,
  TuplePattern,
  TuplePatternConfig,
  TuplePatternTree,
  TupleStructPattern,
  TupleStructPatternConfig,
  TupleStructPatternTree,
  TupleType,
  TupleTypeConfig,
  TupleTypeTree,
  TypeArguments,
  TypeArgumentsConfig,
  TypeArgumentsTree,
  TypeBinding,
  TypeBindingConfig,
  TypeBindingTree,
  TypeCastExpression,
  TypeCastExpressionConfig,
  TypeCastExpressionTree,
  TypeIdentifier,
  TypeIdentifierTree,
  TypeItem,
  TypeItemConfig,
  TypeItemTree,
  TypeParameter,
  TypeParameterConfig,
  TypeParameterTree,
  TypeParameters,
  TypeParametersConfig,
  TypeParametersTree,
  UnaryExpression,
  UnaryExpressionConfig,
  UnaryExpressionTree,
  UnionItem,
  UnionItemConfig,
  UnionItemTree,
  UnitExpression,
  UnitExpressionTree,
  UnitType,
  UnitTypeTree,
  UnsafeBlock,
  UnsafeBlockConfig,
  UnsafeBlockTree,
  UseAsClause,
  UseAsClauseConfig,
  UseAsClauseTree,
  UseBounds,
  UseBoundsConfig,
  UseBoundsTree,
  UseClause,
  UseDeclaration,
  UseDeclarationConfig,
  UseDeclarationTree,
  UseList,
  UseListConfig,
  UseListTree,
  UseWildcard,
  UseWildcardConfig,
  UseWildcardTree,
  VariadicParameter,
  VariadicParameterConfig,
  VariadicParameterTree,
  VisibilityModifier,
  VisibilityModifierConfig,
  VisibilityModifierCrate,
  VisibilityModifierCrateConfig,
  VisibilityModifierCrateTree,
  VisibilityModifierPub,
  VisibilityModifierPubConfig,
  VisibilityModifierPubTree,
  VisibilityModifierTree,
  WhereClause,
  WhereClauseConfig,
  WhereClauseTree,
  WherePredicate,
  WherePredicateConfig,
  WherePredicateTree,
  WhileExpression,
  WhileExpressionConfig,
  WhileExpressionTree,
  YieldExpression,
  YieldExpressionConfig,
  YieldExpressionTree,
  _FieldIdentifier,
  _LetChain,
  _Type,
  _TypeIdentifier,
} from './types.js';
import type { Edit, ByteRange, NonEmptyArray, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _assertNonEmpty<T>(
  arr: readonly T[],
  label: string,
): asserts arr is readonly [T, ...(readonly T[])] {
  if (arr.length === 0) {
    throw new Error(`${label}: requires at least one element`);
  }
}

const RESERVED_KEYWORDS: ReadonlySet<string> = new Set([
  '_',
  'as',
  'async',
  'await',
  'break',
  'const',
  'continue',
  'crate',
  'default',
  'dyn',
  'else',
  'enum',
  'extern',
  'fn',
  'for',
  'gen',
  'if',
  'impl',
  'in',
  'let',
  'loop',
  'match',
  'mod',
  'move',
  'mut',
  'pub',
  'raw',
  'ref',
  'return',
  'self',
  'static',
  'struct',
  'super',
  'trait',
  'try',
  'type',
  'union',
  'unsafe',
  'use',
  'where',
  'while',
  'yield',
]);

const _leafRe_identifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_shebang = /^(?:#![\r\f\t\v ]*([^\[\n].*)?\n)/u;
const _leafRe_metavariable = /^(?:\$[a-zA-Z_]\w*)/u;
const _leafRe_shorthandFieldIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_typeIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _leafRe_fieldIdentifier = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;
const _wordRe = /^(?:(r#)?[_\p{XID_Start}][_\p{XID_Continue}]*)/u;

export function sourceFile(config: SourceFileConfig) {
  const fields = {
    shebang: config?.shebang,
    statements: config?.statements,
  };
  return {
    type: 'source_file' as const,
    named: true as const,
    fields,
    shebang(shebang_?: Shebang | undefined) { return _fs(config, sourceFile, 'shebang', shebang_, fields.shebang); },
    statements(...statements_: Statement[]) { return _fsm(config, sourceFile, 'statements', statements_, fields.statements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SourceFileTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child: (Expression | ExpressionEndingWithBlock)) {
  const children = [child];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroDefinition(config: MacroDefinitionConfig) {
  const fields = {
    name: config?.name,
    rules: config?.rules,
  };
  return {
    type: 'macro_definition' as const,
    named: true as const,
    fields,
    name(name_?: Identifier | ReservedIdentifier) { return _fs(config, macroDefinition, 'name', name_, fields.name); },
    rules(rules_?: MacroRule) { return _fs(config, macroDefinition, 'rules', rules_, fields.rules); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroRule(config: MacroRuleConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'macro_rule' as const,
    named: true as const,
    fields,
    left(left_?: TokenTreePattern) { return _fs(config, macroRule, 'left', left_, fields.left); },
    right(right_?: TokenTree) { return _fs(config, macroRule, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroRuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenTreePattern(...children: TokenPattern[]) {
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenBindingPattern(config: TokenBindingPatternConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'token_binding_pattern' as const,
    named: true as const,
    fields,
    name(name_?: Metavariable) { return _fs(config, tokenBindingPattern, 'name', name_, fields.name); },
    typeField(type?: FragmentSpecifier) { return _fs(config, tokenBindingPattern, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenBindingPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetitionPattern(...children: TokenPattern[]) {
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fragmentSpecifier(text: 'block' | 'expr' | 'expr_2021' | 'ident' | 'item' | 'lifetime' | 'literal' | 'meta' | 'pat' | 'pat_param' | 'path' | 'stmt' | 'tt' | 'ty' | 'vis') {
  return {
    type: 'fragment_specifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FragmentSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function tokenTree(...children: Tokens[]) {
  return {
    type: 'token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetition(...children: Tokens[]) {
  return {
    type: 'token_repetition' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attributeItem(config: AttributeItemConfig) {
  const fields = {
    attribute: config?.attribute,
  };
  return {
    type: 'attribute_item' as const,
    named: true as const,
    fields,
    attribute(attribute_?: Attribute) { return _fs(config, attributeItem, 'attribute', attribute_, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function innerAttributeItem(config: InnerAttributeItemConfig) {
  const fields = {
    attribute: config?.attribute,
  };
  return {
    type: 'inner_attribute_item' as const,
    named: true as const,
    fields,
    attribute(attribute_?: Attribute) { return _fs(config, innerAttributeItem, 'attribute', attribute_, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InnerAttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: AttributeConfig) {
  const fields = {
    value: config?.value,
    arguments: config?.arguments,
  };
  const children = config?.children ?? [];
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    children,
    value(value_?: Expression | undefined) { return _fs(config, attribute, 'value', value_, fields.value); },
    arguments(arguments_?: DelimTokenTree | undefined) { return _fs(config, attribute, 'arguments', arguments_, fields.arguments); },
    getChild() { return children[0]; },
    setChild(child: Path) { return attribute({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function modItem(config: ModItemSemiConfig | ModItemBodyConfig) {
  if (config && 'visibilityModifier' in config && 'name' in config && 'body' in config) return modItemBody(config as ModItemBodyConfig);
  return modItemSemi(config as ModItemSemiConfig);
}
export function modItemSemi(config: ModItemSemiConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
  };
  return {
    type: 'mod_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, modItemSemi, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: Identifier) { return _fs(config, modItemSemi, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModItemSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function modItemBody(config: ModItemBodyConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'mod_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, modItemBody, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: Identifier) { return _fs(config, modItemBody, 'name', name_, fields.name); },
    body(body_?: DeclarationList) { return _fs(config, modItemBody, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModItemBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function foreignModItem(config: ForeignModItemSemiConfig | ForeignModItemBodyConfig) {
  if (config && 'visibilityModifier' in config && 'externModifier' in config && 'body' in config) return foreignModItemBody(config as ForeignModItemBodyConfig);
  return foreignModItemSemi(config as ForeignModItemSemiConfig);
}
export function foreignModItemSemi(config: ForeignModItemSemiConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    extern_modifier: config?.externModifier,
  };
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, foreignModItemSemi, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    externModifier(externModifier_?: ExternModifier) { return _fs(config, foreignModItemSemi, 'externModifier', externModifier_, fields.extern_modifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForeignModItemSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function foreignModItemBody(config: ForeignModItemBodyConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    extern_modifier: config?.externModifier,
    body: config?.body,
  };
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, foreignModItemBody, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    externModifier(externModifier_?: ExternModifier) { return _fs(config, foreignModItemBody, 'externModifier', externModifier_, fields.extern_modifier); },
    body(body_?: DeclarationList) { return _fs(config, foreignModItemBody, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForeignModItemBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declarationList(...children: DeclarationStatement[]) {
  return {
    type: 'declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structItem(config: StructItemBodyConfig | StructItemSemiConfig | StructItemSemi2Config) {
  if (config && 'name' in config && 'typeParameters' in config && 'body' in config) return structItemBody(config as StructItemBodyConfig);
  return structItemSemi2(config as StructItemSemi2Config);
}
export function structItemBody(config: StructItemBodyConfig) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    children,
    name(name_?: _TypeIdentifier) { return _fs(config, structItemBody, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, structItemBody, 'typeParameters', typeParameters_, fields.type_parameters); },
    body(body_?: FieldDeclarationList) { return _fs(config, structItemBody, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (VisibilityModifier | WhereClause)) { return structItemBody({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function structItemSemi(config: StructItemSemiConfig) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    children,
    name(name_?: _TypeIdentifier) { return _fs(config, structItemSemi, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, structItemSemi, 'typeParameters', typeParameters_, fields.type_parameters); },
    body(body_?: OrderedFieldDeclarationList) { return _fs(config, structItemSemi, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (VisibilityModifier | WhereClause)) { return structItemSemi({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function structItemSemi2(config: StructItemSemi2Config) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    variant: 'semi2' as const,
    fields,
    children,
    name(name_?: _TypeIdentifier) { return _fs(config, structItemSemi2, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, structItemSemi2, 'typeParameters', typeParameters_, fields.type_parameters); },
    getChild() { return children[0]; },
    setChild(child: VisibilityModifier) { return structItemSemi2({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemSemi2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionItem(config: UnionItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'union_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, unionItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: _TypeIdentifier) { return _fs(config, unionItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, unionItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: WhereClause | undefined) { return _fs(config, unionItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: FieldDeclarationList) { return _fs(config, unionItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumItem(config: EnumItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'enum_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, enumItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: _TypeIdentifier) { return _fs(config, enumItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, enumItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: WhereClause | undefined) { return _fs(config, enumItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: EnumVariantList) { return _fs(config, enumItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariantList(...children: (AttributeItem | EnumVariant)[]) {
  return {
    type: 'enum_variant_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariant(config: EnumVariantConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    body: config?.body,
    value: config?.value,
  };
  return {
    type: 'enum_variant' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, enumVariant, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: Identifier) { return _fs(config, enumVariant, 'name', name_, fields.name); },
    body(body_?: FieldDeclarationList | OrderedFieldDeclarationList | undefined) { return _fs(config, enumVariant, 'body', body_, fields.body); },
    value(value_?: Expression | undefined) { return _fs(config, enumVariant, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclarationList(...children: (AttributeItem | FieldDeclaration)[]) {
  return {
    type: 'field_declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclaration(config: FieldDeclarationConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'field_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, fieldDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: _FieldIdentifier) { return _fs(config, fieldDeclaration, 'name', name_, fields.name); },
    typeField(type?: _Type) { return _fs(config, fieldDeclaration, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orderedFieldDeclarationList(config: OrderedFieldDeclarationListConfig) {
  const fields = {
    type: config?.type,
  };
  return {
    type: 'ordered_field_declaration_list' as const,
    named: true as const,
    fields,
    typeField(...type: _Type[]) { return _fsm(config, orderedFieldDeclarationList, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrderedFieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externCrateDeclaration(config: ExternCrateDeclarationConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    crate: config?.crate,
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'extern_crate_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, externCrateDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    crate(crate_?: Crate) { return _fs(config, externCrateDeclaration, 'crate', crate_, fields.crate); },
    name(name_?: Identifier) { return _fs(config, externCrateDeclaration, 'name', name_, fields.name); },
    alias(alias_?: Identifier | undefined) { return _fs(config, externCrateDeclaration, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternCrateDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constItem(config: ConstItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'const_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, constItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: Identifier) { return _fs(config, constItem, 'name', name_, fields.name); },
    typeField(type?: _Type) { return _fs(config, constItem, 'type', type, fields.type); },
    value(value_?: Expression | undefined) { return _fs(config, constItem, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function staticItem(config: StaticItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    mutable_specifier: config?.mutableSpecifier,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'static_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, staticItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    mutableSpecifier(mutableSpecifier_?: "ref" | undefined) { return _fs(config, staticItem, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    name(name_?: Identifier) { return _fs(config, staticItem, 'name', name_, fields.name); },
    typeField(type?: _Type) { return _fs(config, staticItem, 'type', type, fields.type); },
    value(value_?: Expression | undefined) { return _fs(config, staticItem, 'value', value_, fields.value); },
    getChild() { return children[0]; },
    setChild(child: MutableSpecifier) { return staticItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StaticItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeItem(config: TypeItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    type: config?.type,
    trailing_where_clause: config?.trailingWhereClause,
  };
  return {
    type: 'type_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, typeItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: _TypeIdentifier) { return _fs(config, typeItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, typeItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: WhereClause | undefined) { return _fs(config, typeItem, 'whereClause', whereClause_, fields.where_clause); },
    typeField(type?: _Type) { return _fs(config, typeItem, 'type', type, fields.type); },
    trailingWhereClause(trailingWhereClause_?: WhereClause | undefined) { return _fs(config, typeItem, 'trailingWhereClause', trailingWhereClause_, fields.trailing_where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionItem(config: FunctionItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    function_modifiers: config?.functionModifiers,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'function_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, functionItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    functionModifiers(functionModifiers_?: FunctionModifiers | undefined) { return _fs(config, functionItem, 'functionModifiers', functionModifiers_, fields.function_modifiers); },
    name(name_?: Identifier | Metavariable) { return _fs(config, functionItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, functionItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: Parameters) { return _fs(config, functionItem, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: _Type | undefined) { return _fs(config, functionItem, 'returnType', returnType_, fields.return_type); },
    whereClause(whereClause_?: WhereClause | undefined) { return _fs(config, functionItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: Block) { return _fs(config, functionItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignatureItem(config: FunctionSignatureItemConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    function_modifiers: config?.functionModifiers,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    return_type: config?.returnType,
    where_clause: config?.whereClause,
  };
  return {
    type: 'function_signature_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, functionSignatureItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    functionModifiers(functionModifiers_?: FunctionModifiers | undefined) { return _fs(config, functionSignatureItem, 'functionModifiers', functionModifiers_, fields.function_modifiers); },
    name(name_?: Identifier | Metavariable) { return _fs(config, functionSignatureItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, functionSignatureItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: Parameters) { return _fs(config, functionSignatureItem, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: _Type | undefined) { return _fs(config, functionSignatureItem, 'returnType', returnType_, fields.return_type); },
    whereClause(whereClause_?: WhereClause | undefined) { return _fs(config, functionSignatureItem, 'whereClause', whereClause_, fields.where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionModifiers(config: FunctionModifiersConfig) {
  const fields = {
    async: config?.async,
    default: config?.default,
    const: config?.const,
    unsafe: config?.unsafe,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    fields,
    children,
    async(...async_: NonEmptyArray<"async">) { return _fsm(config, functionModifiers, 'async', async_, fields.async); },
    default(...default_: NonEmptyArray<"default">) { return _fsm(config, functionModifiers, 'default', default_, fields.default); },
    const(...const_: NonEmptyArray<"const">) { return _fsm(config, functionModifiers, 'const', const_, fields.const); },
    unsafe(...unsafe_: NonEmptyArray<"unsafe">) { return _fsm(config, functionModifiers, 'unsafe', unsafe_, fields.unsafe); },
    getChildren() { return children; },
    setChildren(...items: ExternModifier[]) {
      _assertNonEmpty(items, 'function_modifiers.children');
      return functionModifiers({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whereClause(...children: WherePredicate[]) {
  return {
    type: 'where_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhereClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function wherePredicate(config: WherePredicateConfig) {
  const fields = {
    left: config?.left,
    bounds: config?.bounds,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    fields,
    left(left_?: Lifetime | _TypeIdentifier | ScopedTypeIdentifier | GenericType | ReferenceType | PointerType | TupleType | ArrayType | HigherRankedTraitBound) { return _fs(config, wherePredicate, 'left', left_, fields.left); },
    bounds(bounds_?: TraitBounds) { return _fs(config, wherePredicate, 'bounds', bounds_, fields.bounds); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implItem(config: ImplItemBodyConfig | ImplItemSemiConfig) {
  if (config && 'typeParameters' in config && 'trait' in config && 'type' in config && 'body' in config) return implItemBody(config as ImplItemBodyConfig);
  return implItemSemi(config as ImplItemSemiConfig);
}
export function implItemBody(config: ImplItemBodyConfig) {
  const fields = {
    type_parameters: config?.typeParameters,
    trait: config?.trait,
    type: config?.type,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'impl_item' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    children,
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, implItemBody, 'typeParameters', typeParameters_, fields.type_parameters); },
    trait(trait_?: _TypeIdentifier | ScopedTypeIdentifier | GenericType | undefined) { return _fs(config, implItemBody, 'trait', trait_, fields.trait); },
    typeField(type?: _Type) { return _fs(config, implItemBody, 'type', type, fields.type); },
    body(body_?: DeclarationList) { return _fs(config, implItemBody, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: WhereClause) { return implItemBody({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplItemBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function implItemSemi(config: ImplItemSemiConfig) {
  const fields = {
    type_parameters: config?.typeParameters,
    trait: config?.trait,
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'impl_item' as const,
    named: true as const,
    variant: 'semi' as const,
    fields,
    children,
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, implItemSemi, 'typeParameters', typeParameters_, fields.type_parameters); },
    trait(trait_?: _TypeIdentifier | ScopedTypeIdentifier | GenericType | undefined) { return _fs(config, implItemSemi, 'trait', trait_, fields.trait); },
    typeField(type?: _Type) { return _fs(config, implItemSemi, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: WhereClause) { return implItemSemi({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplItemSemiTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitItem(config: TraitItemConfig) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    bounds: config?.bounds,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'trait_item' as const,
    named: true as const,
    fields,
    children,
    name(name_?: _TypeIdentifier) { return _fs(config, traitItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, traitItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    bounds(bounds_?: TraitBounds | undefined) { return _fs(config, traitItem, 'bounds', bounds_, fields.bounds); },
    body(body_?: DeclarationList) { return _fs(config, traitItem, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: (VisibilityModifier | WhereClause)) { return traitItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function associatedType(config: AssociatedTypeConfig) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    bounds: config?.bounds,
    where_clause: config?.whereClause,
  };
  return {
    type: 'associated_type' as const,
    named: true as const,
    fields,
    name(name_?: _TypeIdentifier) { return _fs(config, associatedType, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, associatedType, 'typeParameters', typeParameters_, fields.type_parameters); },
    bounds(bounds_?: TraitBounds | undefined) { return _fs(config, associatedType, 'bounds', bounds_, fields.bounds); },
    whereClause(whereClause_?: WhereClause | undefined) { return _fs(config, associatedType, 'whereClause', whereClause_, fields.where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssociatedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitBounds(...children: (_Type | Lifetime | HigherRankedTraitBound)[]) {
  _assertNonEmpty(children, 'trait_bounds.children');
  return {
    type: 'trait_bounds' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function higherRankedTraitBound(config: HigherRankedTraitBoundConfig) {
  const fields = {
    type_parameters: config?.typeParameters,
    type: config?.type,
  };
  return {
    type: 'higher_ranked_trait_bound' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: TypeParameters) { return _fs(config, higherRankedTraitBound, 'typeParameters', typeParameters_, fields.type_parameters); },
    typeField(type?: _Type) { return _fs(config, higherRankedTraitBound, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HigherRankedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function removedTraitBound(child: _Type) {
  const children = [child];
  return {
    type: 'removed_trait_bound' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RemovedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(...children: (AttributeItem | Metavariable | TypeParameter | LifetimeParameter | ConstParameter)[]) {
  _assertNonEmpty(children, 'type_parameters.children');
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constParameter(config: ConstParameterConfig) {
  const fields = {
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'const_parameter' as const,
    named: true as const,
    fields,
    name(name_?: Identifier) { return _fs(config, constParameter, 'name', name_, fields.name); },
    typeField(type?: _Type) { return _fs(config, constParameter, 'type', type, fields.type); },
    value(value_?: Block | Identifier | Literal | NegativeLiteral | undefined) { return _fs(config, constParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: TypeParameterConfig) {
  const fields = {
    name: config?.name,
    bounds: config?.bounds,
    default_type: config?.defaultType,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    name(name_?: _TypeIdentifier) { return _fs(config, typeParameter, 'name', name_, fields.name); },
    bounds(bounds_?: TraitBounds | undefined) { return _fs(config, typeParameter, 'bounds', bounds_, fields.bounds); },
    defaultType(defaultType_?: _Type | undefined) { return _fs(config, typeParameter, 'defaultType', defaultType_, fields.default_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetimeParameter(config: LifetimeParameterConfig) {
  const fields = {
    name: config?.name,
    bounds: config?.bounds,
  };
  return {
    type: 'lifetime_parameter' as const,
    named: true as const,
    fields,
    name(name_?: Lifetime) { return _fs(config, lifetimeParameter, 'name', name_, fields.name); },
    bounds(bounds_?: TraitBounds | undefined) { return _fs(config, lifetimeParameter, 'bounds', bounds_, fields.bounds); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letDeclaration(config: LetDeclarationConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
    type: config?.type,
    value: config?.value,
    alternative: config?.alternative,
  };
  return {
    type: 'let_declaration' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier | undefined) { return _fs(config, letDeclaration, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: Pattern) { return _fs(config, letDeclaration, 'pattern', pattern_, fields.pattern); },
    typeField(type?: _Type | undefined) { return _fs(config, letDeclaration, 'type', type, fields.type); },
    value(value_?: Expression | undefined) { return _fs(config, letDeclaration, 'value', value_, fields.value); },
    alternative(alternative_?: Block | undefined) { return _fs(config, letDeclaration, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useDeclaration(config: UseDeclarationConfig) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    argument: config?.argument,
  };
  return {
    type: 'use_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: VisibilityModifier | undefined) { return _fs(config, useDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    argument(argument_?: UseClause) { return _fs(config, useDeclaration, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedUseList(config: ScopedUseListConfig) {
  const fields = {
    path: config?.path,
    list: config?.list,
  };
  return {
    type: 'scoped_use_list' as const,
    named: true as const,
    fields,
    path(path_?: Path | undefined) { return _fs(config, scopedUseList, 'path', path_, fields.path); },
    list(list_?: UseList) { return _fs(config, scopedUseList, 'list', list_, fields.list); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedUseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useList(...children: UseClause[]) {
  return {
    type: 'use_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useAsClause(config: UseAsClauseConfig) {
  const fields = {
    path: config?.path,
    alias: config?.alias,
  };
  return {
    type: 'use_as_clause' as const,
    named: true as const,
    fields,
    path(path_?: Path) { return _fs(config, useAsClause, 'path', path_, fields.path); },
    alias(alias_?: Identifier) { return _fs(config, useAsClause, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseAsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useWildcard(config?: UseWildcardConfig) {
  const fields = {
    path: config?.path,
  };
  return {
    type: 'use_wildcard' as const,
    named: true as const,
    fields,
    path(path_?: Path | undefined) { return _fs(config, useWildcard, 'path', path_, fields.path); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseWildcardTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(...children: (AttributeItem | Parameter | SelfParameter | VariadicParameter | _Type)[]) {
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function selfParameter(config: SelfParameterConfig) {
  const fields = {
    lifetime: config?.lifetime,
    mutable_specifier: config?.mutableSpecifier,
    self: config?.self,
  };
  const children = config?.children ?? [];
  return {
    type: 'self_parameter' as const,
    named: true as const,
    fields,
    children,
    lifetime(lifetime_?: string) { return _fs(config, selfParameter, 'lifetime', lifetime_, fields.lifetime); },
    mutableSpecifier(mutableSpecifier_?: Lifetime | undefined) { return _fs(config, selfParameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    self(self_?: MutableSpecifier | undefined) { return _fs(config, selfParameter, 'self', self_, fields.self); },
    getChild() { return children[0]; },
    setChild(child: Self) { return selfParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SelfParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variadicParameter(config?: VariadicParameterConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'variadic_parameter' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier | undefined) { return _fs(config, variadicParameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: Pattern | undefined) { return _fs(config, variadicParameter, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariadicParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameter(config: ParameterConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
    type: config?.type,
  };
  return {
    type: 'parameter' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier | undefined) { return _fs(config, parameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: Pattern | Self) { return _fs(config, parameter, 'pattern', pattern_, fields.pattern); },
    typeField(type?: _Type) { return _fs(config, parameter, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externModifier(config?: ExternModifierConfig) {
  const fields = {
    string_literal: config?.stringLiteral,
  };
  return {
    type: 'extern_modifier' as const,
    named: true as const,
    fields,
    stringLiteral(stringLiteral_?: StringLiteral | undefined) { return _fs(config, externModifier, 'stringLiteral', stringLiteral_, fields.string_literal); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function visibilityModifier(config: VisibilityModifierCrateConfig | VisibilityModifierPubConfig) {
  if (config && 'pub' in config && 'in' in config) return visibilityModifierPub(config as VisibilityModifierPubConfig);
  return visibilityModifierCrate(config as VisibilityModifierCrateConfig);
}
export function visibilityModifierCrate(config: VisibilityModifierCrateConfig) {
  const children = config?.children ?? [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    variant: 'crate' as const,
    children,
    getChild() { return children[0]; },
    setChild(child: Crate) { return visibilityModifierCrate({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VisibilityModifierCrateTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function visibilityModifierPub(config: VisibilityModifierPubConfig) {
  const fields = {
    pub: config?.pub,
    in: config?.in,
  };
  const children = config?.children ?? [];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    variant: 'pub' as const,
    fields,
    children,
    pub(pub_?: "pub") { return _fs(config, visibilityModifierPub, 'pub', pub_, fields.pub); },
    in(in_?: "in" | undefined) { return _fs(config, visibilityModifierPub, 'in', in_, fields.in); },
    getChild() { return children[0]; },
    setChild(child: (Self | Super | Crate | Path)) { return visibilityModifierPub({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VisibilityModifierPubTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function bracketedType(child: (_Type | QualifiedType)) {
  const children = [child];
  return {
    type: 'bracketed_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BracketedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function qualifiedType(config: QualifiedTypeConfig) {
  const fields = {
    type: config?.type,
    alias: config?.alias,
  };
  return {
    type: 'qualified_type' as const,
    named: true as const,
    fields,
    typeField(type?: _Type) { return _fs(config, qualifiedType, 'type', type, fields.type); },
    alias(alias_?: _Type) { return _fs(config, qualifiedType, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: QualifiedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetime(config: LifetimeConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'lifetime' as const,
    named: true as const,
    fields,
    identifier(identifier_?: Identifier) { return _fs(config, lifetime, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: ArrayTypeConfig) {
  const fields = {
    element: config?.element,
    length: config?.length,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    element(element_?: _Type) { return _fs(config, arrayType, 'element', element_, fields.element); },
    length(length_?: Expression | undefined) { return _fs(config, arrayType, 'length', length_, fields.length); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forLifetimes(...children: Lifetime[]) {
  _assertNonEmpty(children, 'for_lifetimes.children');
  return {
    type: 'for_lifetimes' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForLifetimesTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: FunctionTypeConfig) {
  const fields = {
    for_lifetimes: config?.forLifetimes,
    trait: config?.trait,
    parameters: config?.parameters,
    return_type: config?.returnType,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    forLifetimes(forLifetimes_?: ForLifetimes | undefined) { return _fs(config, functionType, 'forLifetimes', forLifetimes_, fields.for_lifetimes); },
    trait(trait_?: _TypeIdentifier | ScopedTypeIdentifier) { return _fs(config, functionType, 'trait', trait_, fields.trait); },
    parameters(parameters_?: Parameters) { return _fs(config, functionType, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: _Type | undefined) { return _fs(config, functionType, 'returnType', returnType_, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(...children: _Type[]) {
  _assertNonEmpty(children, 'tuple_type.children');
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitType(text: string) {
  if (text.length === 0) throw new Error(`unit_type: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`unit_type: text '${text}' is a reserved keyword`);
  return {
    type: 'unit_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnitTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function genericFunction(config: GenericFunctionConfig) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_function' as const,
    named: true as const,
    fields,
    function(function_?: Identifier | ScopedIdentifier | FieldExpression) { return _fs(config, genericFunction, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: TypeArguments) { return _fs(config, genericFunction, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: GenericTypeConfig) {
  const fields = {
    type: config?.type,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    typeField(type?: _TypeIdentifier | ReservedIdentifier | ScopedTypeIdentifier) { return _fs(config, genericType, 'type', type, fields.type); },
    typeArguments(typeArguments_?: TypeArguments) { return _fs(config, genericType, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericTypeWithTurbofish(config: GenericTypeWithTurbofishConfig) {
  const fields = {
    type: config?.type,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type_with_turbofish' as const,
    named: true as const,
    fields,
    typeField(type?: _TypeIdentifier | ScopedIdentifier) { return _fs(config, genericTypeWithTurbofish, 'type', type, fields.type); },
    typeArguments(typeArguments_?: TypeArguments) { return _fs(config, genericTypeWithTurbofish, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeWithTurbofishTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function boundedType(config: BoundedTypeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'bounded_type' as const,
    named: true as const,
    fields,
    left(left_?: Lifetime | _Type | UseBounds) { return _fs(config, boundedType, 'left', left_, fields.left); },
    right(right_?: Lifetime | _Type | UseBounds) { return _fs(config, boundedType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BoundedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useBounds(...children: (Lifetime | _TypeIdentifier)[]) {
  return {
    type: 'use_bounds' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeArguments(config: TypeArgumentsConfig) {
  const fields = {
    bounds: config?.bounds,
  };
  const children = config?.children ?? [];
  return {
    type: 'type_arguments' as const,
    named: true as const,
    fields,
    children,
    bounds(...bounds_: TraitBounds[]) { return _fsm(config, typeArguments, 'bounds', bounds_, fields.bounds); },
    getChildren() { return children; },
    setChildren(...items: (_Type | TypeBinding | Lifetime | Literal | Block)[]) {
      _assertNonEmpty(items, 'type_arguments.children');
      return typeArguments({ ...(config ?? {}), children: items });
    },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeBinding(config: TypeBindingConfig) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
    type: config?.type,
  };
  return {
    type: 'type_binding' as const,
    named: true as const,
    fields,
    name(name_?: _TypeIdentifier) { return _fs(config, typeBinding, 'name', name_, fields.name); },
    typeArguments(typeArguments_?: TypeArguments | undefined) { return _fs(config, typeBinding, 'typeArguments', typeArguments_, fields.type_arguments); },
    typeField(type?: _Type) { return _fs(config, typeBinding, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeBindingTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceType(config: ReferenceTypeConfig) {
  const fields = {
    lifetime: config?.lifetime,
    mutable_specifier: config?.mutableSpecifier,
    type: config?.type,
  };
  return {
    type: 'reference_type' as const,
    named: true as const,
    fields,
    lifetime(lifetime_?: Lifetime | undefined) { return _fs(config, referenceType, 'lifetime', lifetime_, fields.lifetime); },
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier | undefined) { return _fs(config, referenceType, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    typeField(type?: _Type) { return _fs(config, referenceType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pointerType(config: PointerTypeConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'pointer_type' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: _Type) { return _fs(config, pointerType, 'type', type, fields.type); },
    getChild() { return children[0]; },
    setChild(child: MutableSpecifier) { return pointerType({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PointerTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractType(config: AbstractTypeConfig) {
  const fields = {
    type_parameters: config?.typeParameters,
    trait: config?.trait,
  };
  return {
    type: 'abstract_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: TypeParameters | undefined) { return _fs(config, abstractType, 'typeParameters', typeParameters_, fields.type_parameters); },
    trait(trait_?: _TypeIdentifier | ScopedTypeIdentifier | RemovedTraitBound | GenericType | FunctionType | TupleType | BoundedType) { return _fs(config, abstractType, 'trait', trait_, fields.trait); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dynamicType(config: DynamicTypeConfig) {
  const fields = {
    trait: config?.trait,
  };
  return {
    type: 'dynamic_type' as const,
    named: true as const,
    fields,
    trait(trait_?: HigherRankedTraitBound | _TypeIdentifier | ScopedTypeIdentifier | GenericType | FunctionType | TupleType) { return _fs(config, dynamicType, 'trait', trait_, fields.trait); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DynamicTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutableSpecifier() {
  return {
    type: 'mutable_specifier' as const,
    named: true as const,
    text: 'mut' as const,
    render: () => 'mut' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'mut' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'mut' as const },
    replace: (t: MutableSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'mut' as const }; },
  };
}

export function macroInvocation(config: MacroInvocationConfig) {
  const fields = {
    macro: config?.macro,
    token_tree: config?.tokenTree,
  };
  return {
    type: 'macro_invocation' as const,
    named: true as const,
    fields,
    macro(macro_?: ScopedIdentifier | Identifier | ReservedIdentifier) { return _fs(config, macroInvocation, 'macro', macro_, fields.macro); },
    tokenTree(tokenTree_?: DelimTokenTree) { return _fs(config, macroInvocation, 'tokenTree', tokenTree_, fields.token_tree); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroInvocationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function delimTokenTree(...children: DelimTokens[]) {
  return {
    type: 'delim_token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DelimTokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedIdentifier(config: ScopedIdentifierConfig) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_identifier' as const,
    named: true as const,
    fields,
    path(path_?: Path | BracketedType | GenericTypeWithTurbofish | undefined) { return _fs(config, scopedIdentifier, 'path', path_, fields.path); },
    name(name_?: Identifier | Super) { return _fs(config, scopedIdentifier, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifierInExpressionPosition(config: ScopedTypeIdentifierInExpressionPositionConfig) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_type_identifier_in_expression_position' as const,
    named: true as const,
    fields,
    path(path_?: Path | GenericTypeWithTurbofish | undefined) { return _fs(config, scopedTypeIdentifierInExpressionPosition, 'path', path_, fields.path); },
    name(name_?: _TypeIdentifier) { return _fs(config, scopedTypeIdentifierInExpressionPosition, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedTypeIdentifierInExpressionPositionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifier(config: ScopedTypeIdentifierConfig) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_type_identifier' as const,
    named: true as const,
    fields,
    path(path_?: Path | GenericTypeWithTurbofish | BracketedType | GenericType | undefined) { return _fs(config, scopedTypeIdentifier, 'path', path_, fields.path); },
    name(name_?: _TypeIdentifier) { return _fs(config, scopedTypeIdentifier, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpression(config: RangeExpressionStartConfig | RangeExpressionStart2Config | RangeExpressionOperatorConfig | RangeExpressionOperator2Config) {
  if (config && 'start' in config && 'operator' in config && 'end' in config) return rangeExpressionStart(config as RangeExpressionStartConfig);
  if (config && 'start' in config && 'operator' in config) return rangeExpressionStart2(config as RangeExpressionStart2Config);
  if (config && 'operator' in config && 'end' in config) return rangeExpressionOperator(config as RangeExpressionOperatorConfig);
  return rangeExpressionOperator2(config as RangeExpressionOperator2Config);
}
export function rangeExpressionStart(config: RangeExpressionStartConfig) {
  const fields = {
    start: config?.start,
    operator: config?.operator,
    end: config?.end,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'start' as const,
    fields,
    start(start_?: Expression) { return _fs(config, rangeExpressionStart, 'start', start_, fields.start); },
    operator(operator_?: ".." | "..." | "..=") { return _fs(config, rangeExpressionStart, 'operator', operator_, fields.operator); },
    end(end_?: Expression) { return _fs(config, rangeExpressionStart, 'end', end_, fields.end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionStartTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionStart2(config: RangeExpressionStart2Config) {
  const fields = {
    start: config?.start,
    operator: config?.operator,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'start2' as const,
    fields,
    start(start_?: Expression) { return _fs(config, rangeExpressionStart2, 'start', start_, fields.start); },
    operator(operator_?: "..") { return _fs(config, rangeExpressionStart2, 'operator', operator_, fields.operator); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionStart2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionOperator(config: RangeExpressionOperatorConfig) {
  const fields = {
    operator: config?.operator,
    end: config?.end,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'operator' as const,
    fields,
    operator(operator_?: "..") { return _fs(config, rangeExpressionOperator, 'operator', operator_, fields.operator); },
    end(end_?: Expression) { return _fs(config, rangeExpressionOperator, 'end', end_, fields.end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionOperatorTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangeExpressionOperator2(config: RangeExpressionOperator2Config) {
  const fields = {
    operator: config?.operator,
  };
  return {
    type: 'range_expression' as const,
    named: true as const,
    variant: 'operator2' as const,
    fields,
    operator(operator_?: "..") { return _fs(config, rangeExpressionOperator2, 'operator', operator_, fields.operator); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionOperator2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: UnaryExpressionConfig) {
  const fields = {
    operator: config?.operator,
    operand: config?.operand,
  };
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    operator(operator_?: "-" | "*" | "!") { return _fs(config, unaryExpression, 'operator', operator_, fields.operator); },
    operand(operand_?: Expression) { return _fs(config, unaryExpression, 'operand', operand_, fields.operand); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryExpression(config: TryExpressionConfig) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'try_expression' as const,
    named: true as const,
    fields,
    value(value_?: Expression) { return _fs(config, tryExpression, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceExpression(config: ReferenceExpressionConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    value: config?.value,
  };
  return {
    type: 'reference_expression' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier) { return _fs(config, referenceExpression, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    value(value_?: Expression) { return _fs(config, referenceExpression, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: BinaryExpressionConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(left_?: Expression) { return _fs(config, binaryExpression, 'left', left_, fields.left); },
    operator(operator_?: "&&" | "||" | "&" | "|" | "^" | "==" | "!=" | "<" | "<=" | ">" | ">=" | "<<" | ">>" | "+" | "-" | "*" | "/" | "%") { return _fs(config, binaryExpression, 'operator', operator_, fields.operator); },
    right(right_?: Expression) { return _fs(config, binaryExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: AssignmentExpressionConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: Expression) { return _fs(config, assignmentExpression, 'left', left_, fields.left); },
    right(right_?: Expression) { return _fs(config, assignmentExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function compoundAssignmentExpr(config: CompoundAssignmentExprConfig) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    fields,
    left(left_?: Expression) { return _fs(config, compoundAssignmentExpr, 'left', left_, fields.left); },
    operator(operator_?: "+=" | "-=" | "*=" | "/=" | "%=" | "&=" | "|=" | "^=" | "<<=" | ">>=") { return _fs(config, compoundAssignmentExpr, 'operator', operator_, fields.operator); },
    right(right_?: Expression) { return _fs(config, compoundAssignmentExpr, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeCastExpression(config: TypeCastExpressionConfig) {
  const fields = {
    value: config?.value,
    type: config?.type,
  };
  return {
    type: 'type_cast_expression' as const,
    named: true as const,
    fields,
    value(value_?: Expression) { return _fs(config, typeCastExpression, 'value', value_, fields.value); },
    typeField(type?: _Type) { return _fs(config, typeCastExpression, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeCastExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnExpression(child: Expression) {
  const children = [child];
  return {
    type: 'return_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(child: Expression) {
  const children = [child];
  return {
    type: 'yield_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: CallExpressionConfig) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ExpressionExceptRange) { return _fs(config, callExpression, 'function', function_, fields.function); },
    arguments(arguments_?: Arguments) { return _fs(config, callExpression, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arguments_(...children: (AttributeItem | Expression)[]) {
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayExpression(config: ArrayExpressionConfig) {
  const fields = {
    attributes: config?.attributes,
    elements: config?.elements,
  };
  return {
    type: 'array_expression' as const,
    named: true as const,
    fields,
    attributes(...attributes_: AttributeItem[]) { return _fsm(config, arrayExpression, 'attributes', attributes_, fields.attributes); },
    elements(elements_?: Expression | AttributeItem) { return _fs(config, arrayExpression, 'elements', elements_, fields.elements); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child: Expression) {
  const children = [child];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleExpression(config: TupleExpressionConfig) {
  const fields = {
    attributes: config?.attributes,
    first: config?.first,
    rest: config?.rest,
    trailing: config?.trailing,
  };
  return {
    type: 'tuple_expression' as const,
    named: true as const,
    fields,
    attributes(...attributes_: AttributeItem[]) { return _fsm(config, tupleExpression, 'attributes', attributes_, fields.attributes); },
    first(first_?: Expression) { return _fs(config, tupleExpression, 'first', first_, fields.first); },
    rest(...rest_: Expression[]) { return _fsm(config, tupleExpression, 'rest', rest_, fields.rest); },
    trailing(trailing_?: Expression | undefined) { return _fs(config, tupleExpression, 'trailing', trailing_, fields.trailing); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitExpression(text: string) {
  if (text.length === 0) throw new Error(`unit_expression: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`unit_expression: text '${text}' is a reserved keyword`);
  return {
    type: 'unit_expression' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnitExpressionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function structExpression(config: StructExpressionConfig) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'struct_expression' as const,
    named: true as const,
    fields,
    name(name_?: _TypeIdentifier | ScopedTypeIdentifierInExpressionPosition | GenericTypeWithTurbofish) { return _fs(config, structExpression, 'name', name_, fields.name); },
    body(body_?: FieldInitializerList) { return _fs(config, structExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializerList(...children: (ShorthandFieldInitializer | FieldInitializer | BaseFieldInitializer)[]) {
  return {
    type: 'field_initializer_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandFieldInitializer(config: ShorthandFieldInitializerConfig) {
  const fields = {
    attributes: config?.attributes,
    identifier: config?.identifier,
  };
  return {
    type: 'shorthand_field_initializer' as const,
    named: true as const,
    fields,
    attributes(...attributes_: AttributeItem[]) { return _fsm(config, shorthandFieldInitializer, 'attributes', attributes_, fields.attributes); },
    identifier(identifier_?: Identifier) { return _fs(config, shorthandFieldInitializer, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializer(config: FieldInitializerConfig) {
  const fields = {
    field: config?.field,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_initializer' as const,
    named: true as const,
    fields,
    children,
    field(field_?: _FieldIdentifier | IntegerLiteral) { return _fs(config, fieldInitializer, 'field', field_, fields.field); },
    value(value_?: Expression) { return _fs(config, fieldInitializer, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: AttributeItem[]) { return fieldInitializer({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function baseFieldInitializer(child: Expression) {
  const children = [child];
  return {
    type: 'base_field_initializer' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BaseFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifExpression(config: IfExpressionConfig) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_expression' as const,
    named: true as const,
    fields,
    condition(condition_?: Condition) { return _fs(config, ifExpression, 'condition', condition_, fields.condition); },
    consequence(consequence_?: Block) { return _fs(config, ifExpression, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: ElseClause | undefined) { return _fs(config, ifExpression, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letCondition(config: LetConditionConfig) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  return {
    type: 'let_condition' as const,
    named: true as const,
    fields,
    pattern(pattern_?: Pattern) { return _fs(config, letCondition, 'pattern', pattern_, fields.pattern); },
    value(value_?: Expression) { return _fs(config, letCondition, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetConditionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(child: (Block | IfExpression)) {
  const children = [child];
  return {
    type: 'else_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchExpression(config: MatchExpressionConfig) {
  const fields = {
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'match_expression' as const,
    named: true as const,
    fields,
    value(value_?: Expression) { return _fs(config, matchExpression, 'value', value_, fields.value); },
    body(body_?: MatchBlock) { return _fs(config, matchExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchBlock(...children: (MatchArm | LastMatchArm)[]) {
  return {
    type: 'match_block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchArm(config: MatchArmConfig) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: MatchPattern) { return _fs(config, matchArm, 'pattern', pattern_, fields.pattern); },
    value(value_?: Expression | ExpressionEndingWithBlock) { return _fs(config, matchArm, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: (AttributeItem | InnerAttributeItem)[]) { return matchArm({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lastMatchArm(config: LastMatchArmConfig) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'last_match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: MatchPattern) { return _fs(config, lastMatchArm, 'pattern', pattern_, fields.pattern); },
    value(value_?: Expression) { return _fs(config, lastMatchArm, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: (AttributeItem | InnerAttributeItem)[]) { return lastMatchArm({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LastMatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchPattern(config: MatchPatternConfig) {
  const fields = {
    condition: config?.condition,
  };
  const children = config?.children ?? [];
  return {
    type: 'match_pattern' as const,
    named: true as const,
    fields,
    children,
    condition(condition_?: Condition | undefined) { return _fs(config, matchPattern, 'condition', condition_, fields.condition); },
    getChild() { return children[0]; },
    setChild(child: Pattern) { return matchPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileExpression(config: WhileExpressionConfig) {
  const fields = {
    label: config?.label,
    condition: config?.condition,
    body: config?.body,
  };
  return {
    type: 'while_expression' as const,
    named: true as const,
    fields,
    label(label_?: Label | undefined) { return _fs(config, whileExpression, 'label', label_, fields.label); },
    condition(condition_?: Condition) { return _fs(config, whileExpression, 'condition', condition_, fields.condition); },
    body(body_?: Block) { return _fs(config, whileExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function loopExpression(config: LoopExpressionConfig) {
  const fields = {
    label: config?.label,
    body: config?.body,
  };
  return {
    type: 'loop_expression' as const,
    named: true as const,
    fields,
    label(label_?: Label | undefined) { return _fs(config, loopExpression, 'label', label_, fields.label); },
    body(body_?: Block) { return _fs(config, loopExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LoopExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forExpression(config: ForExpressionConfig) {
  const fields = {
    label: config?.label,
    pattern: config?.pattern,
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'for_expression' as const,
    named: true as const,
    fields,
    label(label_?: Label | undefined) { return _fs(config, forExpression, 'label', label_, fields.label); },
    pattern(pattern_?: Pattern) { return _fs(config, forExpression, 'pattern', pattern_, fields.pattern); },
    value(value_?: Expression) { return _fs(config, forExpression, 'value', value_, fields.value); },
    body(body_?: Block) { return _fs(config, forExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constBlock(config: ConstBlockConfig) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'const_block' as const,
    named: true as const,
    fields,
    body(body_?: Block) { return _fs(config, constBlock, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureExpression(config: ClosureExpressionBodyConfig | ClosureExpressionBody2Config) {
  if (config && 'static' in config && 'async' in config && 'move' in config && 'parameters' in config && 'returnType' in config && 'body' in config) return closureExpressionBody(config as ClosureExpressionBodyConfig);
  return closureExpressionBody2(config as ClosureExpressionBody2Config);
}
export function closureExpressionBody(config: ClosureExpressionBodyConfig) {
  const fields = {
    static: config?.static,
    async: config?.async,
    move: config?.move,
    parameters: config?.parameters,
    return_type: config?.returnType,
    body: config?.body,
  };
  return {
    type: 'closure_expression' as const,
    named: true as const,
    variant: 'body' as const,
    fields,
    static(static_?: "static" | undefined) { return _fs(config, closureExpressionBody, 'static', static_, fields.static); },
    async(async_?: "async" | undefined) { return _fs(config, closureExpressionBody, 'async', async_, fields.async); },
    move(move_?: "move" | undefined) { return _fs(config, closureExpressionBody, 'move', move_, fields.move); },
    parameters(parameters_?: ClosureParameters) { return _fs(config, closureExpressionBody, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: _Type | undefined) { return _fs(config, closureExpressionBody, 'returnType', returnType_, fields.return_type); },
    body(body_?: Block) { return _fs(config, closureExpressionBody, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureExpressionBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function closureExpressionBody2(config: ClosureExpressionBody2Config) {
  const fields = {
    static: config?.static,
    async: config?.async,
    move: config?.move,
    parameters: config?.parameters,
    body: config?.body,
  };
  return {
    type: 'closure_expression' as const,
    named: true as const,
    variant: 'body2' as const,
    fields,
    static(static_?: "static" | undefined) { return _fs(config, closureExpressionBody2, 'static', static_, fields.static); },
    async(async_?: "async" | undefined) { return _fs(config, closureExpressionBody2, 'async', async_, fields.async); },
    move(move_?: "move" | undefined) { return _fs(config, closureExpressionBody2, 'move', move_, fields.move); },
    parameters(parameters_?: ClosureParameters) { return _fs(config, closureExpressionBody2, 'parameters', parameters_, fields.parameters); },
    body(body_?: Expression) { return _fs(config, closureExpressionBody2, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureExpressionBody2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureParameters(...children: (Pattern | Parameter)[]) {
  return {
    type: 'closure_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function label(config: LabelConfig) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'label' as const,
    named: true as const,
    fields,
    identifier(identifier_?: Identifier) { return _fs(config, label, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabelTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakExpression(config?: BreakExpressionConfig) {
  const fields = {
    label: config?.label,
    expression: config?.expression,
  };
  return {
    type: 'break_expression' as const,
    named: true as const,
    fields,
    label(label_?: Label | undefined) { return _fs(config, breakExpression, 'label', label_, fields.label); },
    expression(expression_?: Expression | undefined) { return _fs(config, breakExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueExpression(config?: ContinueExpressionConfig) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'continue_expression' as const,
    named: true as const,
    fields,
    label(label_?: Label | undefined) { return _fs(config, continueExpression, 'label', label_, fields.label); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexExpression(config: IndexExpressionConfig) {
  const fields = {
    object: config?.object,
    index: config?.index,
  };
  return {
    type: 'index_expression' as const,
    named: true as const,
    fields,
    object(object_?: Expression) { return _fs(config, indexExpression, 'object', object_, fields.object); },
    index(index_?: Expression) { return _fs(config, indexExpression, 'index', index_, fields.index); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(child: Expression) {
  const children = [child];
  return {
    type: 'await_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldExpression(config: FieldExpressionConfig) {
  const fields = {
    value: config?.value,
    field: config?.field,
  };
  return {
    type: 'field_expression' as const,
    named: true as const,
    fields,
    value(value_?: Expression) { return _fs(config, fieldExpression, 'value', value_, fields.value); },
    field(field_?: _FieldIdentifier | IntegerLiteral) { return _fs(config, fieldExpression, 'field', field_, fields.field); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unsafeBlock(config: UnsafeBlockConfig) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'unsafe_block' as const,
    named: true as const,
    fields,
    block(block_?: Block) { return _fs(config, unsafeBlock, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnsafeBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asyncBlock(config: AsyncBlockConfig) {
  const fields = {
    block: config?.block,
  };
  const children = config?.children ?? [];
  return {
    type: 'async_block' as const,
    named: true as const,
    fields,
    children,
    block(block_?: "move" | undefined) { return _fs(config, asyncBlock, 'block', block_, fields.block); },
    getChild() { return children[0]; },
    setChild(child: Block) { return asyncBlock({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsyncBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genBlock(config: GenBlockConfig) {
  const fields = {
    block: config?.block,
  };
  const children = config?.children ?? [];
  return {
    type: 'gen_block' as const,
    named: true as const,
    fields,
    children,
    block(block_?: "move" | undefined) { return _fs(config, genBlock, 'block', block_, fields.block); },
    getChild() { return children[0]; },
    setChild(child: Block) { return genBlock({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryBlock(config: TryBlockConfig) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'try_block' as const,
    named: true as const,
    fields,
    block(block_?: Block) { return _fs(config, tryBlock, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(config: BlockConfig) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'block' as const,
    named: true as const,
    fields,
    children,
    label(label_?: Label | undefined) { return _fs(config, block, 'label', label_, fields.label); },
    getChildren() { return children; },
    setChildren(...items: (Statement | Expression)[]) { return block({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericPattern(config: GenericPatternConfig) {
  const fields = {
    type_arguments: config?.typeArguments,
  };
  const children = config?.children ?? [];
  return {
    type: 'generic_pattern' as const,
    named: true as const,
    fields,
    children,
    typeArguments(typeArguments_?: TypeArguments) { return _fs(config, genericPattern, 'typeArguments', typeArguments_, fields.type_arguments); },
    getChild() { return children[0]; },
    setChild(child: (Identifier | ScopedIdentifier)) { return genericPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(...children: (Pattern | ClosureExpression)[]) {
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slicePattern(...children: Pattern[]) {
  return {
    type: 'slice_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SlicePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleStructPattern(config: TupleStructPatternConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'tuple_struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: Identifier | ScopedIdentifier | GenericTypeWithTurbofish) { return _fs(config, tupleStructPattern, 'type', type, fields.type); },
    getChildren() { return children; },
    setChildren(...items: Pattern[]) { return tupleStructPattern({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleStructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structPattern(config: StructPatternConfig) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: _TypeIdentifier | ScopedTypeIdentifier) { return _fs(config, structPattern, 'type', type, fields.type); },
    getChildren() { return children; },
    setChildren(...items: (FieldPattern | RemainingFieldPattern)[]) { return structPattern({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldPattern(config: FieldPatternNameConfig | FieldPatternColonConfig) {
  if (config && 'mutableSpecifier' in config && 'name' in config && 'pattern' in config) return fieldPatternColon(config as FieldPatternColonConfig);
  return fieldPatternName(config as FieldPatternNameConfig);
}
export function fieldPatternName(config: FieldPatternNameConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    name: config?.name,
  };
  return {
    type: 'field_pattern' as const,
    named: true as const,
    variant: 'name' as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier | undefined) { return _fs(config, fieldPatternName, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    name(name_?: Identifier) { return _fs(config, fieldPatternName, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function fieldPatternColon(config: FieldPatternColonConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    name: config?.name,
    pattern: config?.pattern,
  };
  return {
    type: 'field_pattern' as const,
    named: true as const,
    variant: 'colon' as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier | undefined) { return _fs(config, fieldPatternColon, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    name(name_?: _FieldIdentifier) { return _fs(config, fieldPatternColon, 'name', name_, fields.name); },
    pattern(pattern_?: Pattern) { return _fs(config, fieldPatternColon, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternColonTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutPattern(config: MutPatternConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'mut_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier) { return _fs(config, mutPattern, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: Pattern) { return _fs(config, mutPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MutPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangePattern(config: RangePatternLeftConfig | RangePatternRightConfig) {
  if (config && 'left' in config && 'right' in config) return rangePatternLeft(config as RangePatternLeftConfig);
  return rangePatternRight(config as RangePatternRightConfig);
}
export function rangePatternLeft(config: RangePatternLeftConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'left' as const,
    fields,
    left(left_?: LiteralPattern | Path) { return _fs(config, rangePatternLeft, 'left', left_, fields.left); },
    right(right_?: LiteralPattern | Path) { return _fs(config, rangePatternLeft, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternLeftTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangePatternRight(config: RangePatternRightConfig) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    variant: 'right' as const,
    fields,
    right(right_?: LiteralPattern | Path) { return _fs(config, rangePatternRight, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternRightTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function refPattern(child: Pattern) {
  const children = [child];
  return {
    type: 'ref_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RefPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function capturedPattern(config: CapturedPatternConfig) {
  const fields = {
    identifier: config?.identifier,
    pattern: config?.pattern,
  };
  return {
    type: 'captured_pattern' as const,
    named: true as const,
    fields,
    identifier(identifier_?: Identifier) { return _fs(config, capturedPattern, 'identifier', identifier_, fields.identifier); },
    pattern(pattern_?: Pattern) { return _fs(config, capturedPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CapturedPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referencePattern(config: ReferencePatternConfig) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'reference_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: MutableSpecifier | undefined) { return _fs(config, referencePattern, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: Pattern) { return _fs(config, referencePattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferencePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orPattern(config: OrPatternPipeConfig | OrPatternPipe2Config) {
  if (config && 'left' in config && 'right' in config) return orPatternPipe(config as OrPatternPipeConfig);
  return orPatternPipe2(config as OrPatternPipe2Config);
}
export function orPatternPipe(config: OrPatternPipeConfig) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'or_pattern' as const,
    named: true as const,
    variant: 'pipe' as const,
    fields,
    left(left_?: Pattern) { return _fs(config, orPatternPipe, 'left', left_, fields.left); },
    right(right_?: Pattern) { return _fs(config, orPatternPipe, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrPatternPipeTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function orPatternPipe2(config: OrPatternPipe2Config) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'or_pattern' as const,
    named: true as const,
    variant: 'pipe2' as const,
    fields,
    right(right_?: Pattern) { return _fs(config, orPatternPipe2, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrPatternPipe2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function negativeLiteral(config: NegativeLiteralConfig) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'negative_literal' as const,
    named: true as const,
    fields,
    value(value_?: IntegerLiteral | FloatLiteral) { return _fs(config, negativeLiteral, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NegativeLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function integerLiteral(text: string) {
  if (text.length === 0) throw new Error(`integer_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`integer_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'integer_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IntegerLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringLiteral(...children: (EscapeSequence | StringContent)[]) {
  return {
    type: 'string_literal' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rawStringLiteral(config: RawStringLiteralConfig) {
  const fields = {
    raw_string_literal_start: config?.rawStringLiteralStart,
    string_content: config?.stringContent,
    raw_string_literal_end: config?.rawStringLiteralEnd,
  };
  return {
    type: 'raw_string_literal' as const,
    named: true as const,
    fields,
    rawStringLiteralStart(rawStringLiteralStart_?: string) { return _fs(config, rawStringLiteral, 'rawStringLiteralStart', rawStringLiteralStart_, fields.raw_string_literal_start); },
    stringContent(stringContent_?: RawStringLiteralContent) { return _fs(config, rawStringLiteral, 'stringContent', stringContent_, fields.string_content); },
    rawStringLiteralEnd(rawStringLiteralEnd_?: string) { return _fs(config, rawStringLiteral, 'rawStringLiteralEnd', rawStringLiteralEnd_, fields.raw_string_literal_end); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RawStringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function charLiteral(text: string) {
  if (text.length === 0) throw new Error(`char_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`char_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'char_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CharLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  if (text.length === 0) throw new Error(`escape_sequence: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`escape_sequence: text '${text}' is a reserved keyword`);
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function booleanLiteral(text: 'true' | 'false') {
  return {
    type: 'boolean_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: BooleanLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(child: (LineComment | BlockComment)) {
  const children = [child];
  return {
    type: 'comment' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lineComment(config: LineCommentConfig) {
  const fields = {
    outer: config?.outer,
    inner: config?.inner,
    doc: config?.doc,
  };
  return {
    type: 'line_comment' as const,
    named: true as const,
    fields,
    outer(outer_?: "/") { return _fs(config, lineComment, 'outer', outer_, fields.outer); },
    inner(inner_?: "!") { return _fs(config, lineComment, 'inner', inner_, fields.inner); },
    doc(doc_?: string) { return _fs(config, lineComment, 'doc', doc_, fields.doc); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LineCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function blockComment(config?: BlockCommentConfig) {
  const fields = {
    outer: config?.outer,
    inner: config?.inner,
    doc: config?.doc,
  };
  const children = config?.children ?? [];
  return {
    type: 'block_comment' as const,
    named: true as const,
    fields,
    children,
    outer(outer_?: string | undefined) { return _fs(config, blockComment, 'outer', outer_, fields.outer); },
    inner(inner_?: string | undefined) { return _fs(config, blockComment, 'inner', inner_, fields.inner); },
    doc(doc_?: BlockCommentContent | undefined) { return _fs(config, blockComment, 'doc', doc_, fields.doc); },
    getChild() { return children[0]; },
    setChild(child: BlockCommentContent) { return blockComment({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function identifier(text: string) {
  if (text.length === 0) throw new Error(`identifier: text must be non-empty`); if (!_leafRe_identifier.test(text)) throw new Error(`identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shebang(text: string) {
  if (text.length === 0) throw new Error(`shebang: text must be non-empty`); if (!_leafRe_shebang.test(text)) throw new Error(`shebang: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`shebang: text '${text}' is a reserved keyword`);
  return {
    type: 'shebang' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ShebangTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function self() {
  return {
    type: 'self' as const,
    named: true as const,
    text: 'self' as const,
    render: () => 'self' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'self' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'self' as const },
    replace: (t: SelfTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'self' as const }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function crate() {
  return {
    type: 'crate' as const,
    named: true as const,
    text: 'crate' as const,
    render: () => 'crate' as const,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'crate' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'crate' as const },
    replace: (t: CrateTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'crate' as const }; },
  };
}

export function metavariable(text: string) {
  if (text.length === 0) throw new Error(`metavariable: text must be non-empty`); if (!_leafRe_metavariable.test(text)) throw new Error(`metavariable: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`metavariable: text '${text}' is a reserved keyword`);
  return {
    type: 'metavariable' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: MetavariableTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringContent(text: string) {
  if (text.length === 0) throw new Error(`string_content: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`string_content: text '${text}' is a reserved keyword`);
  return {
    type: 'string_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function rawStringLiteralContent(text: string) {
  if (text.length === 0) throw new Error(`raw_string_literal_content: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`raw_string_literal_content: text '${text}' is a reserved keyword`);
  return {
    type: 'raw_string_literal_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RawStringLiteralContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function floatLiteral(text: string) {
  if (text.length === 0) throw new Error(`float_literal: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`float_literal: text '${text}' is a reserved keyword`);
  return {
    type: 'float_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FloatLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function primitiveType(text: 'u8' | 'i8' | 'u16' | 'i16' | 'u32' | 'i32' | 'u64' | 'i64' | 'u128' | 'i128' | 'isize' | 'usize' | 'f32' | 'f64' | 'bool' | 'str' | 'char') {
  return {
    type: 'primitive_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PrimitiveTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function letChain(...children: (_LetChain | LetCondition | Expression)[]) {
  return {
    type: 'let_chain' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | ByteRange, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandFieldIdentifier(text: string) {
  if (text.length === 0) throw new Error(`shorthand_field_identifier: text must be non-empty`); if (!_leafRe_shorthandFieldIdentifier.test(text)) throw new Error(`shorthand_field_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`shorthand_field_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'shorthand_field_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ShorthandFieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function outerDocCommentMarker(text: string) {
  if (text.length === 0) throw new Error(`outer_doc_comment_marker: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`outer_doc_comment_marker: text '${text}' is a reserved keyword`);
  return {
    type: 'outer_doc_comment_marker' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: OuterDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function innerDocCommentMarker(text: string) {
  if (text.length === 0) throw new Error(`inner_doc_comment_marker: text must be non-empty`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`inner_doc_comment_marker: text '${text}' is a reserved keyword`);
  return {
    type: 'inner_doc_comment_marker' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: InnerDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeIdentifier(text: string) {
  if (text.length === 0) throw new Error(`type_identifier: text must be non-empty`); if (!_leafRe_typeIdentifier.test(text)) throw new Error(`type_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`type_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function fieldIdentifier(text: string) {
  if (text.length === 0) throw new Error(`field_identifier: text must be non-empty`); if (!_leafRe_fieldIdentifier.test(text)) throw new Error(`field_identifier: text does not match pattern: ${text}`); if (_wordRe.test(text) && RESERVED_KEYWORDS.has(text)) throw new Error(`field_identifier: text '${text}' is a reserved keyword`);
  return {
    type: 'field_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | ByteRange, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "source_file": FluentNode<"source_file", SourceFileConfig>;
  "expression_statement": FluentNode<"expression_statement", ExpressionStatementConfig>;
  "macro_definition": FluentNode<"macro_definition", MacroDefinitionConfig>;
  "macro_rule": FluentNode<"macro_rule", MacroRuleConfig>;
  "token_tree_pattern": FluentNode<"token_tree_pattern", TokenTreePatternConfig>;
  "token_binding_pattern": FluentNode<"token_binding_pattern", TokenBindingPatternConfig>;
  "token_repetition_pattern": FluentNode<"token_repetition_pattern", TokenRepetitionPatternConfig>;
  "fragment_specifier": FragmentSpecifier;
  "token_tree": FluentNode<"token_tree", TokenTreeConfig>;
  "token_repetition": FluentNode<"token_repetition", TokenRepetitionConfig>;
  "attribute_item": FluentNode<"attribute_item", AttributeItemConfig>;
  "inner_attribute_item": FluentNode<"inner_attribute_item", InnerAttributeItemConfig>;
  "attribute": FluentNode<"attribute", AttributeConfig>;
  "mod_item": FluentNode<"mod_item", ModItemConfig>;
  "foreign_mod_item": FluentNode<"foreign_mod_item", ForeignModItemConfig>;
  "declaration_list": FluentNode<"declaration_list", DeclarationListConfig>;
  "struct_item": FluentNode<"struct_item", StructItemConfig>;
  "union_item": FluentNode<"union_item", UnionItemConfig>;
  "enum_item": FluentNode<"enum_item", EnumItemConfig>;
  "enum_variant_list": FluentNode<"enum_variant_list", EnumVariantListConfig>;
  "enum_variant": FluentNode<"enum_variant", EnumVariantConfig>;
  "field_declaration_list": FluentNode<"field_declaration_list", FieldDeclarationListConfig>;
  "field_declaration": FluentNode<"field_declaration", FieldDeclarationConfig>;
  "ordered_field_declaration_list": FluentNode<"ordered_field_declaration_list", OrderedFieldDeclarationListConfig>;
  "extern_crate_declaration": FluentNode<"extern_crate_declaration", ExternCrateDeclarationConfig>;
  "const_item": FluentNode<"const_item", ConstItemConfig>;
  "static_item": FluentNode<"static_item", StaticItemConfig>;
  "type_item": FluentNode<"type_item", TypeItemConfig>;
  "function_item": FluentNode<"function_item", FunctionItemConfig>;
  "function_signature_item": FluentNode<"function_signature_item", FunctionSignatureItemConfig>;
  "function_modifiers": FluentNode<"function_modifiers", FunctionModifiersConfig>;
  "where_clause": FluentNode<"where_clause", WhereClauseConfig>;
  "where_predicate": FluentNode<"where_predicate", WherePredicateConfig>;
  "impl_item": FluentNode<"impl_item", ImplItemConfig>;
  "trait_item": FluentNode<"trait_item", TraitItemConfig>;
  "associated_type": FluentNode<"associated_type", AssociatedTypeConfig>;
  "trait_bounds": FluentNode<"trait_bounds", TraitBoundsConfig>;
  "higher_ranked_trait_bound": FluentNode<"higher_ranked_trait_bound", HigherRankedTraitBoundConfig>;
  "removed_trait_bound": FluentNode<"removed_trait_bound", RemovedTraitBoundConfig>;
  "type_parameters": FluentNode<"type_parameters", TypeParametersConfig>;
  "const_parameter": FluentNode<"const_parameter", ConstParameterConfig>;
  "type_parameter": FluentNode<"type_parameter", TypeParameterConfig>;
  "lifetime_parameter": FluentNode<"lifetime_parameter", LifetimeParameterConfig>;
  "let_declaration": FluentNode<"let_declaration", LetDeclarationConfig>;
  "use_declaration": FluentNode<"use_declaration", UseDeclarationConfig>;
  "scoped_use_list": FluentNode<"scoped_use_list", ScopedUseListConfig>;
  "use_list": FluentNode<"use_list", UseListConfig>;
  "use_as_clause": FluentNode<"use_as_clause", UseAsClauseConfig>;
  "use_wildcard": FluentNode<"use_wildcard", UseWildcardConfig>;
  "parameters": FluentNode<"parameters", ParametersConfig>;
  "self_parameter": FluentNode<"self_parameter", SelfParameterConfig>;
  "variadic_parameter": FluentNode<"variadic_parameter", VariadicParameterConfig>;
  "parameter": FluentNode<"parameter", ParameterConfig>;
  "extern_modifier": FluentNode<"extern_modifier", ExternModifierConfig>;
  "visibility_modifier": FluentNode<"visibility_modifier", VisibilityModifierConfig>;
  "bracketed_type": FluentNode<"bracketed_type", BracketedTypeConfig>;
  "qualified_type": FluentNode<"qualified_type", QualifiedTypeConfig>;
  "lifetime": FluentNode<"lifetime", LifetimeConfig>;
  "array_type": FluentNode<"array_type", ArrayTypeConfig>;
  "for_lifetimes": FluentNode<"for_lifetimes", ForLifetimesConfig>;
  "function_type": FluentNode<"function_type", FunctionTypeConfig>;
  "tuple_type": FluentNode<"tuple_type", TupleTypeConfig>;
  "unit_type": UnitType;
  "generic_function": FluentNode<"generic_function", GenericFunctionConfig>;
  "generic_type": FluentNode<"generic_type", GenericTypeConfig>;
  "generic_type_with_turbofish": FluentNode<"generic_type_with_turbofish", GenericTypeWithTurbofishConfig>;
  "bounded_type": FluentNode<"bounded_type", BoundedTypeConfig>;
  "use_bounds": FluentNode<"use_bounds", UseBoundsConfig>;
  "type_arguments": FluentNode<"type_arguments", TypeArgumentsConfig>;
  "type_binding": FluentNode<"type_binding", TypeBindingConfig>;
  "reference_type": FluentNode<"reference_type", ReferenceTypeConfig>;
  "pointer_type": FluentNode<"pointer_type", PointerTypeConfig>;
  "abstract_type": FluentNode<"abstract_type", AbstractTypeConfig>;
  "dynamic_type": FluentNode<"dynamic_type", DynamicTypeConfig>;
  "mutable_specifier": MutableSpecifier;
  "macro_invocation": FluentNode<"macro_invocation", MacroInvocationConfig>;
  "delim_token_tree": FluentNode<"delim_token_tree", DelimTokenTreeConfig>;
  "scoped_identifier": FluentNode<"scoped_identifier", ScopedIdentifierConfig>;
  "scoped_type_identifier_in_expression_position": FluentNode<"scoped_type_identifier_in_expression_position", ScopedTypeIdentifierInExpressionPositionConfig>;
  "scoped_type_identifier": FluentNode<"scoped_type_identifier", ScopedTypeIdentifierConfig>;
  "range_expression": FluentNode<"range_expression", RangeExpressionConfig>;
  "unary_expression": FluentNode<"unary_expression", UnaryExpressionConfig>;
  "try_expression": FluentNode<"try_expression", TryExpressionConfig>;
  "reference_expression": FluentNode<"reference_expression", ReferenceExpressionConfig>;
  "binary_expression": FluentNode<"binary_expression", BinaryExpressionConfig>;
  "assignment_expression": FluentNode<"assignment_expression", AssignmentExpressionConfig>;
  "compound_assignment_expr": FluentNode<"compound_assignment_expr", CompoundAssignmentExprConfig>;
  "type_cast_expression": FluentNode<"type_cast_expression", TypeCastExpressionConfig>;
  "return_expression": FluentNode<"return_expression", ReturnExpressionConfig>;
  "yield_expression": FluentNode<"yield_expression", YieldExpressionConfig>;
  "call_expression": FluentNode<"call_expression", CallExpressionConfig>;
  "arguments": FluentNode<"arguments", ArgumentsConfig>;
  "array_expression": FluentNode<"array_expression", ArrayExpressionConfig>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", ParenthesizedExpressionConfig>;
  "tuple_expression": FluentNode<"tuple_expression", TupleExpressionConfig>;
  "unit_expression": UnitExpression;
  "struct_expression": FluentNode<"struct_expression", StructExpressionConfig>;
  "field_initializer_list": FluentNode<"field_initializer_list", FieldInitializerListConfig>;
  "shorthand_field_initializer": FluentNode<"shorthand_field_initializer", ShorthandFieldInitializerConfig>;
  "field_initializer": FluentNode<"field_initializer", FieldInitializerConfig>;
  "base_field_initializer": FluentNode<"base_field_initializer", BaseFieldInitializerConfig>;
  "if_expression": FluentNode<"if_expression", IfExpressionConfig>;
  "let_condition": FluentNode<"let_condition", LetConditionConfig>;
  "else_clause": FluentNode<"else_clause", ElseClauseConfig>;
  "match_expression": FluentNode<"match_expression", MatchExpressionConfig>;
  "match_block": FluentNode<"match_block", MatchBlockConfig>;
  "match_arm": FluentNode<"match_arm", MatchArmConfig>;
  "last_match_arm": FluentNode<"last_match_arm", LastMatchArmConfig>;
  "match_pattern": FluentNode<"match_pattern", MatchPatternConfig>;
  "while_expression": FluentNode<"while_expression", WhileExpressionConfig>;
  "loop_expression": FluentNode<"loop_expression", LoopExpressionConfig>;
  "for_expression": FluentNode<"for_expression", ForExpressionConfig>;
  "const_block": FluentNode<"const_block", ConstBlockConfig>;
  "closure_expression": FluentNode<"closure_expression", ClosureExpressionConfig>;
  "closure_parameters": FluentNode<"closure_parameters", ClosureParametersConfig>;
  "label": FluentNode<"label", LabelConfig>;
  "break_expression": FluentNode<"break_expression", BreakExpressionConfig>;
  "continue_expression": FluentNode<"continue_expression", ContinueExpressionConfig>;
  "index_expression": FluentNode<"index_expression", IndexExpressionConfig>;
  "await_expression": FluentNode<"await_expression", AwaitExpressionConfig>;
  "field_expression": FluentNode<"field_expression", FieldExpressionConfig>;
  "unsafe_block": FluentNode<"unsafe_block", UnsafeBlockConfig>;
  "async_block": FluentNode<"async_block", AsyncBlockConfig>;
  "gen_block": FluentNode<"gen_block", GenBlockConfig>;
  "try_block": FluentNode<"try_block", TryBlockConfig>;
  "block": FluentNode<"block", BlockConfig>;
  "generic_pattern": FluentNode<"generic_pattern", GenericPatternConfig>;
  "tuple_pattern": FluentNode<"tuple_pattern", TuplePatternConfig>;
  "slice_pattern": FluentNode<"slice_pattern", SlicePatternConfig>;
  "tuple_struct_pattern": FluentNode<"tuple_struct_pattern", TupleStructPatternConfig>;
  "struct_pattern": FluentNode<"struct_pattern", StructPatternConfig>;
  "field_pattern": FluentNode<"field_pattern", FieldPatternConfig>;
  "mut_pattern": FluentNode<"mut_pattern", MutPatternConfig>;
  "range_pattern": FluentNode<"range_pattern", RangePatternConfig>;
  "ref_pattern": FluentNode<"ref_pattern", RefPatternConfig>;
  "captured_pattern": FluentNode<"captured_pattern", CapturedPatternConfig>;
  "reference_pattern": FluentNode<"reference_pattern", ReferencePatternConfig>;
  "or_pattern": FluentNode<"or_pattern", OrPatternConfig>;
  "negative_literal": FluentNode<"negative_literal", NegativeLiteralConfig>;
  "integer_literal": IntegerLiteral;
  "string_literal": FluentNode<"string_literal", StringLiteralConfig>;
  "raw_string_literal": FluentNode<"raw_string_literal", RawStringLiteralConfig>;
  "char_literal": CharLiteral;
  "escape_sequence": EscapeSequence;
  "boolean_literal": BooleanLiteral;
  "comment": FluentNode<"comment", CommentConfig>;
  "line_comment": FluentNode<"line_comment", LineCommentConfig>;
  "block_comment": FluentNode<"block_comment", BlockCommentConfig>;
  "identifier": Identifier;
  "shebang": Shebang;
  "self": Self;
  "super": Super;
  "crate": Crate;
  "metavariable": Metavariable;
  "string_content": StringContent;
  "raw_string_literal_content": RawStringLiteralContent;
  "float_literal": FloatLiteral;
  "primitive_type": PrimitiveType;
  "let_chain": FluentNode<"let_chain", LetChainConfig>;
  "shorthand_field_identifier": ShorthandFieldIdentifier;
  "outer_doc_comment_marker": OuterDocCommentMarker;
  "inner_doc_comment_marker": InnerDocCommentMarker;
  "type_identifier": TypeIdentifier;
  "field_identifier": FieldIdentifier;
};

export const _factoryMap = {
  "source_file": sourceFile,
  "expression_statement": expressionStatement,
  "macro_definition": macroDefinition,
  "macro_rule": macroRule,
  "token_tree_pattern": tokenTreePattern,
  "token_binding_pattern": tokenBindingPattern,
  "token_repetition_pattern": tokenRepetitionPattern,
  "fragment_specifier": fragmentSpecifier,
  "token_tree": tokenTree,
  "token_repetition": tokenRepetition,
  "attribute_item": attributeItem,
  "inner_attribute_item": innerAttributeItem,
  "attribute": attribute,
  "mod_item": modItem,
  "foreign_mod_item": foreignModItem,
  "declaration_list": declarationList,
  "struct_item": structItem,
  "union_item": unionItem,
  "enum_item": enumItem,
  "enum_variant_list": enumVariantList,
  "enum_variant": enumVariant,
  "field_declaration_list": fieldDeclarationList,
  "field_declaration": fieldDeclaration,
  "ordered_field_declaration_list": orderedFieldDeclarationList,
  "extern_crate_declaration": externCrateDeclaration,
  "const_item": constItem,
  "static_item": staticItem,
  "type_item": typeItem,
  "function_item": functionItem,
  "function_signature_item": functionSignatureItem,
  "function_modifiers": functionModifiers,
  "where_clause": whereClause,
  "where_predicate": wherePredicate,
  "impl_item": implItem,
  "trait_item": traitItem,
  "associated_type": associatedType,
  "trait_bounds": traitBounds,
  "higher_ranked_trait_bound": higherRankedTraitBound,
  "removed_trait_bound": removedTraitBound,
  "type_parameters": typeParameters,
  "const_parameter": constParameter,
  "type_parameter": typeParameter,
  "lifetime_parameter": lifetimeParameter,
  "let_declaration": letDeclaration,
  "use_declaration": useDeclaration,
  "scoped_use_list": scopedUseList,
  "use_list": useList,
  "use_as_clause": useAsClause,
  "use_wildcard": useWildcard,
  "parameters": parameters,
  "self_parameter": selfParameter,
  "variadic_parameter": variadicParameter,
  "parameter": parameter,
  "extern_modifier": externModifier,
  "visibility_modifier": visibilityModifier,
  "bracketed_type": bracketedType,
  "qualified_type": qualifiedType,
  "lifetime": lifetime,
  "array_type": arrayType,
  "for_lifetimes": forLifetimes,
  "function_type": functionType,
  "tuple_type": tupleType,
  "unit_type": unitType,
  "generic_function": genericFunction,
  "generic_type": genericType,
  "generic_type_with_turbofish": genericTypeWithTurbofish,
  "bounded_type": boundedType,
  "use_bounds": useBounds,
  "type_arguments": typeArguments,
  "type_binding": typeBinding,
  "reference_type": referenceType,
  "pointer_type": pointerType,
  "abstract_type": abstractType,
  "dynamic_type": dynamicType,
  "mutable_specifier": mutableSpecifier,
  "macro_invocation": macroInvocation,
  "delim_token_tree": delimTokenTree,
  "scoped_identifier": scopedIdentifier,
  "scoped_type_identifier_in_expression_position": scopedTypeIdentifierInExpressionPosition,
  "scoped_type_identifier": scopedTypeIdentifier,
  "range_expression": rangeExpression,
  "unary_expression": unaryExpression,
  "try_expression": tryExpression,
  "reference_expression": referenceExpression,
  "binary_expression": binaryExpression,
  "assignment_expression": assignmentExpression,
  "compound_assignment_expr": compoundAssignmentExpr,
  "type_cast_expression": typeCastExpression,
  "return_expression": returnExpression,
  "yield_expression": yieldExpression,
  "call_expression": callExpression,
  "arguments": arguments_,
  "array_expression": arrayExpression,
  "parenthesized_expression": parenthesizedExpression,
  "tuple_expression": tupleExpression,
  "unit_expression": unitExpression,
  "struct_expression": structExpression,
  "field_initializer_list": fieldInitializerList,
  "shorthand_field_initializer": shorthandFieldInitializer,
  "field_initializer": fieldInitializer,
  "base_field_initializer": baseFieldInitializer,
  "if_expression": ifExpression,
  "let_condition": letCondition,
  "else_clause": elseClause,
  "match_expression": matchExpression,
  "match_block": matchBlock,
  "match_arm": matchArm,
  "last_match_arm": lastMatchArm,
  "match_pattern": matchPattern,
  "while_expression": whileExpression,
  "loop_expression": loopExpression,
  "for_expression": forExpression,
  "const_block": constBlock,
  "closure_expression": closureExpression,
  "closure_parameters": closureParameters,
  "label": label,
  "break_expression": breakExpression,
  "continue_expression": continueExpression,
  "index_expression": indexExpression,
  "await_expression": awaitExpression,
  "field_expression": fieldExpression,
  "unsafe_block": unsafeBlock,
  "async_block": asyncBlock,
  "gen_block": genBlock,
  "try_block": tryBlock,
  "block": block,
  "generic_pattern": genericPattern,
  "tuple_pattern": tuplePattern,
  "slice_pattern": slicePattern,
  "tuple_struct_pattern": tupleStructPattern,
  "struct_pattern": structPattern,
  "field_pattern": fieldPattern,
  "mut_pattern": mutPattern,
  "range_pattern": rangePattern,
  "ref_pattern": refPattern,
  "captured_pattern": capturedPattern,
  "reference_pattern": referencePattern,
  "or_pattern": orPattern,
  "negative_literal": negativeLiteral,
  "integer_literal": integerLiteral,
  "string_literal": stringLiteral,
  "raw_string_literal": rawStringLiteral,
  "char_literal": charLiteral,
  "escape_sequence": escapeSequence,
  "boolean_literal": booleanLiteral,
  "comment": comment,
  "line_comment": lineComment,
  "block_comment": blockComment,
  "identifier": identifier,
  "shebang": shebang,
  "self": self,
  "super": super_,
  "crate": crate,
  "metavariable": metavariable,
  "string_content": stringContent,
  "raw_string_literal_content": rawStringLiteralContent,
  "float_literal": floatLiteral,
  "primitive_type": primitiveType,
  "let_chain": letChain,
  "shorthand_field_identifier": shorthandFieldIdentifier,
  "outer_doc_comment_marker": outerDocCommentMarker,
  "inner_doc_comment_marker": innerDocCommentMarker,
  "type_identifier": typeIdentifier,
  "field_identifier": fieldIdentifier,
} as const;
export type _FactoryMap = typeof _factoryMap;

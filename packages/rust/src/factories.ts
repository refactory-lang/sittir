// Auto-generated by @sittir/codegen — do not edit

import type { AbstractType, AbstractTypeTree, Arguments, ArgumentsTree, ArrayExpression, ArrayExpressionTree, ArrayType, ArrayTypeTree, AssignmentExpression, AssignmentExpressionTree, AssociatedType, AssociatedTypeTree, AsyncBlock, AsyncBlockTree, Attribute, AttributeItem, AttributeItemTree, AttributeTree, AwaitExpression, AwaitExpressionTree, BaseFieldInitializer, BaseFieldInitializerTree, BinaryExpression, BinaryExpressionTree, Block, BlockComment, BlockCommentContent, BlockCommentContentTree, BlockCommentTree, BlockTree, BooleanLiteral, BooleanLiteralTree, BoundedType, BoundedTypeTree, BracketedType, BracketedTypeTree, BreakExpression, BreakExpressionTree, CallExpression, CallExpressionTree, CapturedPattern, CapturedPatternTree, CharLiteral, CharLiteralTree, ClosureExpression, ClosureExpressionBody, ClosureExpressionBody2, ClosureExpressionBody2Config, ClosureExpressionBody2Tree, ClosureExpressionBodyConfig, ClosureExpressionBodyTree, ClosureExpressionTree, ClosureParameters, ClosureParametersTree, Comment, CommentTree, CompoundAssignmentExpr, CompoundAssignmentExprTree, ConfigMap, ConstBlock, ConstBlockTree, ConstItem, ConstItemTree, ConstParameter, ConstParameterTree, ContinueExpression, ContinueExpressionTree, Crate, CrateTree, DeclarationList, DeclarationListTree, DelimTokenTree, DelimTokenTreeTree, DynamicType, DynamicTypeTree, ElseClause, ElseClauseTree, EnumItem, EnumItemTree, EnumVariant, EnumVariantList, EnumVariantListTree, EnumVariantTree, ErrorSentinel, ErrorSentinelTree, EscapeSequence, EscapeSequenceTree, ExpressionExceptRange, ExpressionExceptRangeTree, ExpressionStatement, ExpressionStatementTree, ExternCrateDeclaration, ExternCrateDeclarationTree, ExternModifier, ExternModifierTree, FieldDeclaration, FieldDeclarationList, FieldDeclarationListTree, FieldDeclarationTree, FieldExpression, FieldExpressionTree, FieldIdentifier, FieldIdentifierTree, FieldInitializer, FieldInitializerList, FieldInitializerListTree, FieldInitializerTree, FieldPattern, FieldPatternColon, FieldPatternColonConfig, FieldPatternColonTree, FieldPatternName, FieldPatternNameConfig, FieldPatternNameTree, FieldPatternTree, FloatLiteral, FloatLiteralTree, ForExpression, ForExpressionTree, ForLifetimes, ForLifetimesTree, ForeignModItem, ForeignModItemTree, FragmentSpecifier, FragmentSpecifierTree, FunctionItem, FunctionItemTree, FunctionModifiers, FunctionModifiersTree, FunctionSignatureItem, FunctionSignatureItemTree, FunctionType, FunctionTypeTree, GenBlock, GenBlockTree, GenericFunction, GenericFunctionTree, GenericPattern, GenericPatternTree, GenericType, GenericTypeTree, GenericTypeWithTurbofish, GenericTypeWithTurbofishTree, HiddenFieldIdentifier, HiddenFieldIdentifierTree, HiddenLetChain, HiddenLetChainTree, HiddenPath, HiddenPathTree, HiddenTypeIdentifier, HiddenTypeIdentifierTree, HigherRankedTraitBound, HigherRankedTraitBoundTree, Identifier, IdentifierTree, IfExpression, IfExpressionTree, ImplItem, ImplItemTree, IndexExpression, IndexExpressionTree, InnerAttributeItem, InnerAttributeItemTree, InnerDocCommentMarker, InnerDocCommentMarkerTree, IntegerLiteral, IntegerLiteralTree, KindMap, Label, LabelTree, LastMatchArm, LastMatchArmTree, LetChain, LetChainTree, LetCondition, LetConditionTree, LetDeclaration, LetDeclarationTree, Lifetime, LifetimeParameter, LifetimeParameterTree, LifetimeTree, LineComment, LineCommentTree, LoopExpression, LoopExpressionTree, MacroDefinition, MacroDefinitionTree, MacroInvocation, MacroInvocationTree, MacroRule, MacroRuleTree, MatchArm, MatchArmTree, MatchBlock, MatchBlockTree, MatchExpression, MatchExpressionTree, MatchPattern, MatchPatternTree, Metavariable, MetavariableTree, ModItem, ModItemTree, MutPattern, MutPatternTree, MutableSpecifier, MutableSpecifierTree, NegativeLiteral, NegativeLiteralTree, NonDelimToken, NonDelimTokenTree, NonSpecialToken, NonSpecialTokenTree, OrPattern, OrPatternTree, OrderedFieldDeclarationList, OrderedFieldDeclarationListTree, OuterDocCommentMarker, OuterDocCommentMarkerTree, Parameter, ParameterTree, Parameters, ParametersTree, ParenthesizedExpression, ParenthesizedExpressionTree, PointerType, PointerTypeTree, PrimitiveType, PrimitiveTypeTree, QualifiedType, QualifiedTypeTree, RangeExpression, RangeExpressionTree, RangePattern, RangePatternLeft, RangePatternLeftConfig, RangePatternLeftTree, RangePatternRight, RangePatternRightConfig, RangePatternRightTree, RangePatternTree, RawStringLiteral, RawStringLiteralContent, RawStringLiteralContentTree, RawStringLiteralTree, RefPattern, RefPatternTree, ReferenceExpression, ReferenceExpressionTree, ReferencePattern, ReferencePatternTree, ReferenceType, ReferenceTypeTree, RemovedTraitBound, RemovedTraitBoundTree, ReservedIdentifier, ReservedIdentifierTree, ReturnExpression, ReturnExpressionTree, ScopedIdentifier, ScopedIdentifierTree, ScopedTypeIdentifier, ScopedTypeIdentifierInExpressionPosition, ScopedTypeIdentifierInExpressionPositionTree, ScopedTypeIdentifierTree, ScopedUseList, ScopedUseListTree, Self, SelfParameter, SelfParameterTree, SelfTree, Shebang, ShebangTree, ShorthandFieldIdentifier, ShorthandFieldIdentifierTree, ShorthandFieldInitializer, ShorthandFieldInitializerTree, SlicePattern, SlicePatternTree, SourceFile, SourceFileTree, StaticItem, StaticItemTree, StringContent, StringContentTree, StringLiteral, StringLiteralTree, StructExpression, StructExpressionTree, StructItem, StructItemTree, StructPattern, StructPatternTree, Super, SuperTree, TokenBindingPattern, TokenBindingPatternTree, TokenRepetition, TokenRepetitionPattern, TokenRepetitionPatternTree, TokenRepetitionTree, TokenTree, TokenTreePattern, TokenTreePatternTree, TokenTreeTree, TraitBounds, TraitBoundsTree, TraitItem, TraitItemTree, TryBlock, TryBlockTree, TryExpression, TryExpressionTree, TupleExpression, TupleExpressionTree, TuplePattern, TuplePatternTree, TupleStructPattern, TupleStructPatternTree, TupleType, TupleTypeTree, TypeArguments, TypeArgumentsTree, TypeBinding, TypeBindingTree, TypeCastExpression, TypeCastExpressionTree, TypeIdentifier, TypeIdentifierTree, TypeItem, TypeItemTree, TypeParameter, TypeParameterTree, TypeParameters, TypeParametersTree, UnaryExpression, UnaryExpressionTree, UnionItem, UnionItemTree, UnitExpression, UnitExpressionTree, UnitType, UnitTypeTree, UnsafeBlock, UnsafeBlockTree, UseAsClause, UseAsClauseTree, UseBounds, UseBoundsTree, UseDeclaration, UseDeclarationTree, UseList, UseListTree, UseWildcard, UseWildcardTree, VariadicParameter, VariadicParameterTree, VisibilityModifier, VisibilityModifierTree, WhereClause, WhereClauseTree, WherePredicate, WherePredicateTree, WhileExpression, WhileExpressionTree, YieldExpression, YieldExpressionTree } from './types.js';
import type { Edit, ConfigOf, ChildOf, FluentNode } from '@sittir/types';
import { createRenderer } from '@sittir/core';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const { render, toEdit } = createRenderer(join(__dirname, '..', 'templates.yaml'));

function _fs<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: T[K] | undefined,
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v !== undefined ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}
function _fsm<T, R, K extends keyof T>(
  cfg: T | undefined,
  fn: (c: T) => R,
  key: K,
  v: readonly unknown[],
  cur: T[K] | undefined,
): T[K] | R | undefined {
  return v.length ? fn({ ...((cfg ?? {}) as T), [key]: v } as T) : cur;
}

const RESERVED_KEYWORDS = new Set([
  '_',
  'as',
  'async',
  'await',
  'break',
  'const',
  'continue',
  'crate',
  'default',
  'dyn',
  'else',
  'enum',
  'extern',
  'fn',
  'for',
  'gen',
  'if',
  'impl',
  'in',
  'let',
  'loop',
  'match',
  'mod',
  'move',
  'mut',
  'mutable_specifier',
  'pub',
  'raw',
  'ref',
  'return',
  'self',
  'static',
  'struct',
  'super',
  'trait',
  'try',
  'type',
  'union',
  'unsafe',
  'use',
  'where',
  'while',
  'yield',
]);

export function sourceFile(config: ConfigOf<SourceFile>) {
  const fields = {
    shebang: config?.shebang,
    statements: config?.statements,
  };
  return {
    type: 'source_file' as const,
    named: true as const,
    fields,
    shebang(shebang_?: ConfigOf<SourceFile>['shebang']) { return _fs(config, sourceFile, 'shebang', shebang_, fields.shebang); },
    statements(...statements_: NonNullable<ConfigOf<SourceFile>['statements']>) { return _fsm(config, sourceFile, 'statements', statements_, fields.statements); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SourceFileTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function expressionStatement(child: ChildOf<ExpressionStatement>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ExpressionStatement>['children'];
  return {
    type: 'expression_statement' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionStatementTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroDefinition(config: ConfigOf<MacroDefinition>) {
  const fields = {
    name: config?.name,
    rules: config?.rules,
  };
  return {
    type: 'macro_definition' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<MacroDefinition>['name']) { return _fs(config, macroDefinition, 'name', name_, fields.name); },
    rules(rules_?: ConfigOf<MacroDefinition>['rules']) { return _fs(config, macroDefinition, 'rules', rules_, fields.rules); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroDefinitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroRule(config: ConfigOf<MacroRule>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'macro_rule' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<MacroRule>['left']) { return _fs(config, macroRule, 'left', left_, fields.left); },
    right(right_?: ConfigOf<MacroRule>['right']) { return _fs(config, macroRule, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroRuleTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenTreePattern(..._children: ChildOf<TokenTreePattern>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TokenTreePattern>['children'];
  return {
    type: 'token_tree_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenBindingPattern(config: ConfigOf<TokenBindingPattern>) {
  const fields = {
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'token_binding_pattern' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TokenBindingPattern>['name']) { return _fs(config, tokenBindingPattern, 'name', name_, fields.name); },
    typeField(type?: ConfigOf<TokenBindingPattern>['type']) { return _fs(config, tokenBindingPattern, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenBindingPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetitionPattern(..._children: ChildOf<TokenRepetitionPattern>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TokenRepetitionPattern>['children'];
  return {
    type: 'token_repetition_pattern' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fragmentSpecifier(text: string) {
  if (!(['block', 'expr', 'expr_2021', 'ident', 'item', 'lifetime', 'literal', 'meta', 'pat', 'pat_param', 'path', 'stmt', 'tt', 'ty', 'vis'] as const as readonly string[]).includes(text)) throw new Error(`fragment_specifier: text '${text}' is not one of block|expr|expr_2021|ident|item|lifetime|literal|meta|pat|pat_param|path|stmt|tt|ty|vis`);
  return {
    type: 'fragment_specifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FragmentSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function tokenTree(..._children: ChildOf<TokenTree>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TokenTree>['children'];
  return {
    type: 'token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tokenRepetition(..._children: ChildOf<TokenRepetition>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TokenRepetition>['children'];
  return {
    type: 'token_repetition' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TokenRepetitionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonSpecialToken(child: ChildOf<NonSpecialToken>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<NonSpecialToken>['children'];
  return {
    type: '_non_special_token' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonSpecialTokenTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attributeItem(config: ConfigOf<AttributeItem>) {
  const fields = {
    attribute: config?.attribute,
  };
  return {
    type: 'attribute_item' as const,
    named: true as const,
    fields,
    attribute(attribute_?: ConfigOf<AttributeItem>['attribute']) { return _fs(config, attributeItem, 'attribute', attribute_, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function innerAttributeItem(config: ConfigOf<InnerAttributeItem>) {
  const fields = {
    attribute: config?.attribute,
  };
  return {
    type: 'inner_attribute_item' as const,
    named: true as const,
    fields,
    attribute(attribute_?: ConfigOf<InnerAttributeItem>['attribute']) { return _fs(config, innerAttributeItem, 'attribute', attribute_, fields.attribute); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: InnerAttributeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function attribute(config: ConfigOf<Attribute>) {
  const fields = {
    value: config?.value,
    arguments: config?.arguments,
  };
  const children = config?.children ?? [];
  return {
    type: 'attribute' as const,
    named: true as const,
    fields,
    children,
    value(value_?: ConfigOf<Attribute>['value']) { return _fs(config, attribute, 'value', value_, fields.value); },
    arguments(arguments_?: ConfigOf<Attribute>['arguments']) { return _fs(config, attribute, 'arguments', arguments_, fields.arguments); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<Attribute>) { return attribute({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AttributeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function modItem(config: ConfigOf<ModItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'mod_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<ModItem>['visibilityModifier']) { return _fs(config, modItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: ConfigOf<ModItem>['name']) { return _fs(config, modItem, 'name', name_, fields.name); },
    body(body_?: ConfigOf<ModItem>['body']) { return _fs(config, modItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function foreignModItem(config: ConfigOf<ForeignModItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    extern_modifier: config?.externModifier,
    body: config?.body,
  };
  return {
    type: 'foreign_mod_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<ForeignModItem>['visibilityModifier']) { return _fs(config, foreignModItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    externModifier(externModifier_?: ConfigOf<ForeignModItem>['externModifier']) { return _fs(config, foreignModItem, 'externModifier', externModifier_, fields.extern_modifier); },
    body(body_?: ConfigOf<ForeignModItem>['body']) { return _fs(config, foreignModItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForeignModItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function declarationList(..._children: ChildOf<DeclarationList>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<DeclarationList>['children'];
  return {
    type: 'declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structItem(config: ConfigOf<StructItem>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_item' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<StructItem>['name']) { return _fs(config, structItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: ConfigOf<StructItem>['typeParameters']) { return _fs(config, structItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    body(body_?: ConfigOf<StructItem>['body']) { return _fs(config, structItem, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<StructItem>) { return structItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unionItem(config: ConfigOf<UnionItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'union_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<UnionItem>['visibilityModifier']) { return _fs(config, unionItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: ConfigOf<UnionItem>['name']) { return _fs(config, unionItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: ConfigOf<UnionItem>['typeParameters']) { return _fs(config, unionItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: ConfigOf<UnionItem>['whereClause']) { return _fs(config, unionItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: ConfigOf<UnionItem>['body']) { return _fs(config, unionItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumItem(config: ConfigOf<EnumItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  return {
    type: 'enum_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<EnumItem>['visibilityModifier']) { return _fs(config, enumItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: ConfigOf<EnumItem>['name']) { return _fs(config, enumItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: ConfigOf<EnumItem>['typeParameters']) { return _fs(config, enumItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: ConfigOf<EnumItem>['whereClause']) { return _fs(config, enumItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: ConfigOf<EnumItem>['body']) { return _fs(config, enumItem, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariantList(..._children: ChildOf<EnumVariantList>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<EnumVariantList>['children'];
  return {
    type: 'enum_variant_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function enumVariant(config: ConfigOf<EnumVariant>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    body: config?.body,
    value: config?.value,
  };
  return {
    type: 'enum_variant' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<EnumVariant>['visibilityModifier']) { return _fs(config, enumVariant, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: ConfigOf<EnumVariant>['name']) { return _fs(config, enumVariant, 'name', name_, fields.name); },
    body(body_?: ConfigOf<EnumVariant>['body']) { return _fs(config, enumVariant, 'body', body_, fields.body); },
    value(value_?: ConfigOf<EnumVariant>['value']) { return _fs(config, enumVariant, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: EnumVariantTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclarationList(..._children: ChildOf<FieldDeclarationList>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<FieldDeclarationList>['children'];
  return {
    type: 'field_declaration_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldDeclaration(config: ConfigOf<FieldDeclaration>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type: config?.type,
  };
  return {
    type: 'field_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<FieldDeclaration>['visibilityModifier']) { return _fs(config, fieldDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: ConfigOf<FieldDeclaration>['name']) { return _fs(config, fieldDeclaration, 'name', name_, fields.name); },
    typeField(type?: ConfigOf<FieldDeclaration>['type']) { return _fs(config, fieldDeclaration, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orderedFieldDeclarationList(config?: ConfigOf<OrderedFieldDeclarationList>) {
  const fields = {
    attributes: config?.attributes,
    visibility_modifier: config?.visibilityModifier,
  };
  return {
    type: 'ordered_field_declaration_list' as const,
    named: true as const,
    fields,
    attributes(attributes_?: ConfigOf<OrderedFieldDeclarationList>['attributes']) { return _fs(config, orderedFieldDeclarationList, 'attributes', attributes_, fields.attributes); },
    visibilityModifier(visibilityModifier_?: ConfigOf<OrderedFieldDeclarationList>['visibilityModifier']) { return _fs(config, orderedFieldDeclarationList, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrderedFieldDeclarationListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externCrateDeclaration(config: ConfigOf<ExternCrateDeclaration>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    crate: config?.crate,
    name: config?.name,
    alias: config?.alias,
  };
  return {
    type: 'extern_crate_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<ExternCrateDeclaration>['visibilityModifier']) { return _fs(config, externCrateDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    crate(crate_?: ConfigOf<ExternCrateDeclaration>['crate']) { return _fs(config, externCrateDeclaration, 'crate', crate_, fields.crate); },
    name(name_?: ConfigOf<ExternCrateDeclaration>['name']) { return _fs(config, externCrateDeclaration, 'name', name_, fields.name); },
    alias(alias_?: ConfigOf<ExternCrateDeclaration>['alias']) { return _fs(config, externCrateDeclaration, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternCrateDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constItem(config: ConfigOf<ConstItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'const_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<ConstItem>['visibilityModifier']) { return _fs(config, constItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: ConfigOf<ConstItem>['name']) { return _fs(config, constItem, 'name', name_, fields.name); },
    typeField(type?: ConfigOf<ConstItem>['type']) { return _fs(config, constItem, 'type', type, fields.type); },
    value(value_?: ConfigOf<ConstItem>['value']) { return _fs(config, constItem, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function staticItem(config: ConfigOf<StaticItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    mutable_specifier: config?.mutableSpecifier,
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'static_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: ConfigOf<StaticItem>['visibilityModifier']) { return _fs(config, staticItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    mutableSpecifier(mutableSpecifier_?: ConfigOf<StaticItem>['mutableSpecifier']) { return _fs(config, staticItem, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    name(name_?: ConfigOf<StaticItem>['name']) { return _fs(config, staticItem, 'name', name_, fields.name); },
    typeField(type?: ConfigOf<StaticItem>['type']) { return _fs(config, staticItem, 'type', type, fields.type); },
    value(value_?: ConfigOf<StaticItem>['value']) { return _fs(config, staticItem, 'value', value_, fields.value); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<StaticItem>) { return staticItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StaticItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeItem(config: ConfigOf<TypeItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    name: config?.name,
    type_parameters: config?.typeParameters,
    where_clause: config?.whereClause,
    type: config?.type,
    trailing_where_clause: config?.trailingWhereClause,
  };
  return {
    type: 'type_item' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<TypeItem>['visibilityModifier']) { return _fs(config, typeItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    name(name_?: ConfigOf<TypeItem>['name']) { return _fs(config, typeItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: ConfigOf<TypeItem>['typeParameters']) { return _fs(config, typeItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    whereClause(whereClause_?: ConfigOf<TypeItem>['whereClause']) { return _fs(config, typeItem, 'whereClause', whereClause_, fields.where_clause); },
    typeField(type?: ConfigOf<TypeItem>['type']) { return _fs(config, typeItem, 'type', type, fields.type); },
    trailingWhereClause(trailingWhereClause_?: ConfigOf<TypeItem>['trailingWhereClause']) { return _fs(config, typeItem, 'trailingWhereClause', trailingWhereClause_, fields.trailing_where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionItem(config: ConfigOf<FunctionItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    function_modifiers: config?.functionModifiers,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    where_clause: config?.whereClause,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: ConfigOf<FunctionItem>['visibilityModifier']) { return _fs(config, functionItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    functionModifiers(functionModifiers_?: ConfigOf<FunctionItem>['functionModifiers']) { return _fs(config, functionItem, 'functionModifiers', functionModifiers_, fields.function_modifiers); },
    name(name_?: ConfigOf<FunctionItem>['name']) { return _fs(config, functionItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: ConfigOf<FunctionItem>['typeParameters']) { return _fs(config, functionItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: ConfigOf<FunctionItem>['parameters']) { return _fs(config, functionItem, 'parameters', parameters_, fields.parameters); },
    whereClause(whereClause_?: ConfigOf<FunctionItem>['whereClause']) { return _fs(config, functionItem, 'whereClause', whereClause_, fields.where_clause); },
    body(body_?: ConfigOf<FunctionItem>['body']) { return _fs(config, functionItem, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<FunctionItem>) { return functionItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionSignatureItem(config: ConfigOf<FunctionSignatureItem>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    function_modifiers: config?.functionModifiers,
    name: config?.name,
    type_parameters: config?.typeParameters,
    parameters: config?.parameters,
    where_clause: config?.whereClause,
  };
  const children = config?.children ?? [];
  return {
    type: 'function_signature_item' as const,
    named: true as const,
    fields,
    children,
    visibilityModifier(visibilityModifier_?: ConfigOf<FunctionSignatureItem>['visibilityModifier']) { return _fs(config, functionSignatureItem, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    functionModifiers(functionModifiers_?: ConfigOf<FunctionSignatureItem>['functionModifiers']) { return _fs(config, functionSignatureItem, 'functionModifiers', functionModifiers_, fields.function_modifiers); },
    name(name_?: ConfigOf<FunctionSignatureItem>['name']) { return _fs(config, functionSignatureItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: ConfigOf<FunctionSignatureItem>['typeParameters']) { return _fs(config, functionSignatureItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    parameters(parameters_?: ConfigOf<FunctionSignatureItem>['parameters']) { return _fs(config, functionSignatureItem, 'parameters', parameters_, fields.parameters); },
    whereClause(whereClause_?: ConfigOf<FunctionSignatureItem>['whereClause']) { return _fs(config, functionSignatureItem, 'whereClause', whereClause_, fields.where_clause); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<FunctionSignatureItem>) { return functionSignatureItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionSignatureItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionModifiers(..._children: ChildOf<FunctionModifiers>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<FunctionModifiers>['children'];
  return {
    type: 'function_modifiers' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionModifiersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whereClause(..._children: ChildOf<WhereClause>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<WhereClause>['children'];
  return {
    type: 'where_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhereClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function wherePredicate(config: ConfigOf<WherePredicate>) {
  const fields = {
    left: config?.left,
    bounds: config?.bounds,
  };
  return {
    type: 'where_predicate' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<WherePredicate>['left']) { return _fs(config, wherePredicate, 'left', left_, fields.left); },
    bounds(bounds_?: ConfigOf<WherePredicate>['bounds']) { return _fs(config, wherePredicate, 'bounds', bounds_, fields.bounds); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WherePredicateTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function implItem(config: ConfigOf<ImplItem>) {
  const fields = {
    type_parameters: config?.typeParameters,
    trait: config?.trait,
    type: config?.type,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'impl_item' as const,
    named: true as const,
    fields,
    children,
    typeParameters(typeParameters_?: ConfigOf<ImplItem>['typeParameters']) { return _fs(config, implItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    trait(trait_?: ConfigOf<ImplItem>['trait']) { return _fs(config, implItem, 'trait', trait_, fields.trait); },
    typeField(type?: ConfigOf<ImplItem>['type']) { return _fs(config, implItem, 'type', type, fields.type); },
    body(body_?: ConfigOf<ImplItem>['body']) { return _fs(config, implItem, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<ImplItem>) { return implItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ImplItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitItem(config: ConfigOf<TraitItem>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    bounds: config?.bounds,
    body: config?.body,
  };
  const children = config?.children ?? [];
  return {
    type: 'trait_item' as const,
    named: true as const,
    fields,
    children,
    name(name_?: ConfigOf<TraitItem>['name']) { return _fs(config, traitItem, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: ConfigOf<TraitItem>['typeParameters']) { return _fs(config, traitItem, 'typeParameters', typeParameters_, fields.type_parameters); },
    bounds(bounds_?: ConfigOf<TraitItem>['bounds']) { return _fs(config, traitItem, 'bounds', bounds_, fields.bounds); },
    body(body_?: ConfigOf<TraitItem>['body']) { return _fs(config, traitItem, 'body', body_, fields.body); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<TraitItem>) { return traitItem({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitItemTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function associatedType(config: ConfigOf<AssociatedType>) {
  const fields = {
    name: config?.name,
    type_parameters: config?.typeParameters,
    bounds: config?.bounds,
    where_clause: config?.whereClause,
  };
  return {
    type: 'associated_type' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<AssociatedType>['name']) { return _fs(config, associatedType, 'name', name_, fields.name); },
    typeParameters(typeParameters_?: ConfigOf<AssociatedType>['typeParameters']) { return _fs(config, associatedType, 'typeParameters', typeParameters_, fields.type_parameters); },
    bounds(bounds_?: ConfigOf<AssociatedType>['bounds']) { return _fs(config, associatedType, 'bounds', bounds_, fields.bounds); },
    whereClause(whereClause_?: ConfigOf<AssociatedType>['whereClause']) { return _fs(config, associatedType, 'whereClause', whereClause_, fields.where_clause); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssociatedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function traitBounds(..._children: ChildOf<TraitBounds>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TraitBounds>['children'];
  return {
    type: 'trait_bounds' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TraitBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function higherRankedTraitBound(config: ConfigOf<HigherRankedTraitBound>) {
  const fields = {
    type_parameters: config?.typeParameters,
    type: config?.type,
  };
  return {
    type: 'higher_ranked_trait_bound' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: ConfigOf<HigherRankedTraitBound>['typeParameters']) { return _fs(config, higherRankedTraitBound, 'typeParameters', typeParameters_, fields.type_parameters); },
    typeField(type?: ConfigOf<HigherRankedTraitBound>['type']) { return _fs(config, higherRankedTraitBound, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HigherRankedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function removedTraitBound(child: ChildOf<RemovedTraitBound>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<RemovedTraitBound>['children'];
  return {
    type: 'removed_trait_bound' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RemovedTraitBoundTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameters(..._children: ChildOf<TypeParameters>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TypeParameters>['children'];
  return {
    type: 'type_parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constParameter(config: ConfigOf<ConstParameter>) {
  const fields = {
    name: config?.name,
    type: config?.type,
    value: config?.value,
  };
  return {
    type: 'const_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<ConstParameter>['name']) { return _fs(config, constParameter, 'name', name_, fields.name); },
    typeField(type?: ConfigOf<ConstParameter>['type']) { return _fs(config, constParameter, 'type', type, fields.type); },
    value(value_?: ConfigOf<ConstParameter>['value']) { return _fs(config, constParameter, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeParameter(config: ConfigOf<TypeParameter>) {
  const fields = {
    name: config?.name,
    bounds: config?.bounds,
    default_type: config?.defaultType,
  };
  return {
    type: 'type_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TypeParameter>['name']) { return _fs(config, typeParameter, 'name', name_, fields.name); },
    bounds(bounds_?: ConfigOf<TypeParameter>['bounds']) { return _fs(config, typeParameter, 'bounds', bounds_, fields.bounds); },
    defaultType(defaultType_?: ConfigOf<TypeParameter>['defaultType']) { return _fs(config, typeParameter, 'defaultType', defaultType_, fields.default_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetimeParameter(config: ConfigOf<LifetimeParameter>) {
  const fields = {
    name: config?.name,
    bounds: config?.bounds,
  };
  return {
    type: 'lifetime_parameter' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<LifetimeParameter>['name']) { return _fs(config, lifetimeParameter, 'name', name_, fields.name); },
    bounds(bounds_?: ConfigOf<LifetimeParameter>['bounds']) { return _fs(config, lifetimeParameter, 'bounds', bounds_, fields.bounds); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letDeclaration(config: ConfigOf<LetDeclaration>) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
    type: config?.type,
    value: config?.value,
    alternative: config?.alternative,
  };
  return {
    type: 'let_declaration' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: ConfigOf<LetDeclaration>['mutableSpecifier']) { return _fs(config, letDeclaration, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: ConfigOf<LetDeclaration>['pattern']) { return _fs(config, letDeclaration, 'pattern', pattern_, fields.pattern); },
    typeField(type?: ConfigOf<LetDeclaration>['type']) { return _fs(config, letDeclaration, 'type', type, fields.type); },
    value(value_?: ConfigOf<LetDeclaration>['value']) { return _fs(config, letDeclaration, 'value', value_, fields.value); },
    alternative(alternative_?: ConfigOf<LetDeclaration>['alternative']) { return _fs(config, letDeclaration, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useDeclaration(config: ConfigOf<UseDeclaration>) {
  const fields = {
    visibility_modifier: config?.visibilityModifier,
    argument: config?.argument,
  };
  return {
    type: 'use_declaration' as const,
    named: true as const,
    fields,
    visibilityModifier(visibilityModifier_?: ConfigOf<UseDeclaration>['visibilityModifier']) { return _fs(config, useDeclaration, 'visibilityModifier', visibilityModifier_, fields.visibility_modifier); },
    argument(argument_?: ConfigOf<UseDeclaration>['argument']) { return _fs(config, useDeclaration, 'argument', argument_, fields.argument); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseDeclarationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedUseList(config: ConfigOf<ScopedUseList>) {
  const fields = {
    path: config?.path,
    list: config?.list,
  };
  return {
    type: 'scoped_use_list' as const,
    named: true as const,
    fields,
    path(path_?: ConfigOf<ScopedUseList>['path']) { return _fs(config, scopedUseList, 'path', path_, fields.path); },
    list(list_?: ConfigOf<ScopedUseList>['list']) { return _fs(config, scopedUseList, 'list', list_, fields.list); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedUseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useList(..._children: ChildOf<UseList>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<UseList>['children'];
  return {
    type: 'use_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useAsClause(config: ConfigOf<UseAsClause>) {
  const fields = {
    path: config?.path,
    alias: config?.alias,
  };
  return {
    type: 'use_as_clause' as const,
    named: true as const,
    fields,
    path(path_?: ConfigOf<UseAsClause>['path']) { return _fs(config, useAsClause, 'path', path_, fields.path); },
    alias(alias_?: ConfigOf<UseAsClause>['alias']) { return _fs(config, useAsClause, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseAsClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useWildcard(config?: ConfigOf<UseWildcard>) {
  const fields = {
    path: config?.path,
  };
  return {
    type: 'use_wildcard' as const,
    named: true as const,
    fields,
    path(path_?: ConfigOf<UseWildcard>['path']) { return _fs(config, useWildcard, 'path', path_, fields.path); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseWildcardTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameters(..._children: ChildOf<Parameters>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Parameters>['children'];
  return {
    type: 'parameters' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function selfParameter(config: ConfigOf<SelfParameter>) {
  const fields = {
    lifetime: config?.lifetime,
    mutable_specifier: config?.mutableSpecifier,
    self: config?.self,
  };
  const children = config?.children ?? [];
  return {
    type: 'self_parameter' as const,
    named: true as const,
    fields,
    children,
    lifetime(lifetime_?: ConfigOf<SelfParameter>['lifetime']) { return _fs(config, selfParameter, 'lifetime', lifetime_, fields.lifetime); },
    mutableSpecifier(mutableSpecifier_?: ConfigOf<SelfParameter>['mutableSpecifier']) { return _fs(config, selfParameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    self(self_?: ConfigOf<SelfParameter>['self']) { return _fs(config, selfParameter, 'self', self_, fields.self); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<SelfParameter>) { return selfParameter({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SelfParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function variadicParameter(config?: ConfigOf<VariadicParameter>) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'variadic_parameter' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: ConfigOf<VariadicParameter>['mutableSpecifier']) { return _fs(config, variadicParameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: ConfigOf<VariadicParameter>['pattern']) { return _fs(config, variadicParameter, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VariadicParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parameter(config: ConfigOf<Parameter>) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
    type: config?.type,
  };
  return {
    type: 'parameter' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: ConfigOf<Parameter>['mutableSpecifier']) { return _fs(config, parameter, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: ConfigOf<Parameter>['pattern']) { return _fs(config, parameter, 'pattern', pattern_, fields.pattern); },
    typeField(type?: ConfigOf<Parameter>['type']) { return _fs(config, parameter, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParameterTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function externModifier(config?: ConfigOf<ExternModifier>) {
  const fields = {
    string_literal: config?.stringLiteral,
  };
  return {
    type: 'extern_modifier' as const,
    named: true as const,
    fields,
    stringLiteral(stringLiteral_?: ConfigOf<ExternModifier>['stringLiteral']) { return _fs(config, externModifier, 'stringLiteral', stringLiteral_, fields.string_literal); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExternModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function visibilityModifier(child?: ChildOf<VisibilityModifier>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<VisibilityModifier>['children'];
  return {
    type: 'visibility_modifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: VisibilityModifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function bracketedType(child: ChildOf<BracketedType>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<BracketedType>['children'];
  return {
    type: 'bracketed_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BracketedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function qualifiedType(config: ConfigOf<QualifiedType>) {
  const fields = {
    type: config?.type,
    alias: config?.alias,
  };
  return {
    type: 'qualified_type' as const,
    named: true as const,
    fields,
    typeField(type?: ConfigOf<QualifiedType>['type']) { return _fs(config, qualifiedType, 'type', type, fields.type); },
    alias(alias_?: ConfigOf<QualifiedType>['alias']) { return _fs(config, qualifiedType, 'alias', alias_, fields.alias); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: QualifiedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lifetime(config: ConfigOf<Lifetime>) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'lifetime' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<Lifetime>['identifier']) { return _fs(config, lifetime, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LifetimeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayType(config: ConfigOf<ArrayType>) {
  const fields = {
    element: config?.element,
    length: config?.length,
  };
  return {
    type: 'array_type' as const,
    named: true as const,
    fields,
    element(element_?: ConfigOf<ArrayType>['element']) { return _fs(config, arrayType, 'element', element_, fields.element); },
    length(length_?: ConfigOf<ArrayType>['length']) { return _fs(config, arrayType, 'length', length_, fields.length); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forLifetimes(..._children: ChildOf<ForLifetimes>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<ForLifetimes>['children'];
  return {
    type: 'for_lifetimes' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForLifetimesTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function functionType(config: ConfigOf<FunctionType>) {
  const fields = {
    for_lifetimes: config?.forLifetimes,
    trait: config?.trait,
    parameters: config?.parameters,
    return_type: config?.returnType,
  };
  return {
    type: 'function_type' as const,
    named: true as const,
    fields,
    forLifetimes(forLifetimes_?: ConfigOf<FunctionType>['forLifetimes']) { return _fs(config, functionType, 'forLifetimes', forLifetimes_, fields.for_lifetimes); },
    trait(trait_?: ConfigOf<FunctionType>['trait']) { return _fs(config, functionType, 'trait', trait_, fields.trait); },
    parameters(parameters_?: ConfigOf<FunctionType>['parameters']) { return _fs(config, functionType, 'parameters', parameters_, fields.parameters); },
    returnType(returnType_?: ConfigOf<FunctionType>['returnType']) { return _fs(config, functionType, 'returnType', returnType_, fields.return_type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FunctionTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleType(..._children: ChildOf<TupleType>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<TupleType>['children'];
  return {
    type: 'tuple_type' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitType(text: string) {
  return {
    type: 'unit_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnitTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function genericFunction(config: ConfigOf<GenericFunction>) {
  const fields = {
    function: config?.function,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_function' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<GenericFunction>['function']) { return _fs(config, genericFunction, 'function', function_, fields.function); },
    typeArguments(typeArguments_?: ConfigOf<GenericFunction>['typeArguments']) { return _fs(config, genericFunction, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericFunctionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericType(config: ConfigOf<GenericType>) {
  const fields = {
    type: config?.type,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type' as const,
    named: true as const,
    fields,
    typeField(type?: ConfigOf<GenericType>['type']) { return _fs(config, genericType, 'type', type, fields.type); },
    typeArguments(typeArguments_?: ConfigOf<GenericType>['typeArguments']) { return _fs(config, genericType, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericTypeWithTurbofish(config: ConfigOf<GenericTypeWithTurbofish>) {
  const fields = {
    type: config?.type,
    type_arguments: config?.typeArguments,
  };
  return {
    type: 'generic_type_with_turbofish' as const,
    named: true as const,
    fields,
    typeField(type?: ConfigOf<GenericTypeWithTurbofish>['type']) { return _fs(config, genericTypeWithTurbofish, 'type', type, fields.type); },
    typeArguments(typeArguments_?: ConfigOf<GenericTypeWithTurbofish>['typeArguments']) { return _fs(config, genericTypeWithTurbofish, 'typeArguments', typeArguments_, fields.type_arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericTypeWithTurbofishTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function boundedType(config: ConfigOf<BoundedType>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'bounded_type' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<BoundedType>['left']) { return _fs(config, boundedType, 'left', left_, fields.left); },
    right(right_?: ConfigOf<BoundedType>['right']) { return _fs(config, boundedType, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BoundedTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function useBounds(..._children: ChildOf<UseBounds>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<UseBounds>['children'];
  return {
    type: 'use_bounds' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UseBoundsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeArguments(config: ConfigOf<TypeArguments>) {
  const fields = {
    bounds: config?.bounds,
  };
  const children = config?.children ?? [];
  return {
    type: 'type_arguments' as const,
    named: true as const,
    fields,
    children,
    bounds(...bounds_: NonNullable<ConfigOf<TypeArguments>['bounds']>) { return _fsm(config, typeArguments, 'bounds', bounds_, fields.bounds); },
    getChildren() { return children; },
    setChildren(...items: ChildOf<TypeArguments>[]) { return typeArguments({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeBinding(config: ConfigOf<TypeBinding>) {
  const fields = {
    name: config?.name,
    type_arguments: config?.typeArguments,
    type: config?.type,
  };
  return {
    type: 'type_binding' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<TypeBinding>['name']) { return _fs(config, typeBinding, 'name', name_, fields.name); },
    typeArguments(typeArguments_?: ConfigOf<TypeBinding>['typeArguments']) { return _fs(config, typeBinding, 'typeArguments', typeArguments_, fields.type_arguments); },
    typeField(type?: ConfigOf<TypeBinding>['type']) { return _fs(config, typeBinding, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeBindingTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceType(config: ConfigOf<ReferenceType>) {
  const fields = {
    lifetime: config?.lifetime,
    mutable_specifier: config?.mutableSpecifier,
    type: config?.type,
  };
  return {
    type: 'reference_type' as const,
    named: true as const,
    fields,
    lifetime(lifetime_?: ConfigOf<ReferenceType>['lifetime']) { return _fs(config, referenceType, 'lifetime', lifetime_, fields.lifetime); },
    mutableSpecifier(mutableSpecifier_?: ConfigOf<ReferenceType>['mutableSpecifier']) { return _fs(config, referenceType, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    typeField(type?: ConfigOf<ReferenceType>['type']) { return _fs(config, referenceType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function pointerType(config: ConfigOf<PointerType>) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    type: config?.type,
  };
  return {
    type: 'pointer_type' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: ConfigOf<PointerType>['mutableSpecifier']) { return _fs(config, pointerType, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    typeField(type?: ConfigOf<PointerType>['type']) { return _fs(config, pointerType, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: PointerTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function abstractType(config: ConfigOf<AbstractType>) {
  const fields = {
    type_parameters: config?.typeParameters,
    trait: config?.trait,
  };
  return {
    type: 'abstract_type' as const,
    named: true as const,
    fields,
    typeParameters(typeParameters_?: ConfigOf<AbstractType>['typeParameters']) { return _fs(config, abstractType, 'typeParameters', typeParameters_, fields.type_parameters); },
    trait(trait_?: ConfigOf<AbstractType>['trait']) { return _fs(config, abstractType, 'trait', trait_, fields.trait); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AbstractTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function dynamicType(config: ConfigOf<DynamicType>) {
  const fields = {
    trait: config?.trait,
  };
  return {
    type: 'dynamic_type' as const,
    named: true as const,
    fields,
    trait(trait_?: ConfigOf<DynamicType>['trait']) { return _fs(config, dynamicType, 'trait', trait_, fields.trait); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DynamicTypeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutableSpecifier() {
  return {
    type: 'mutable_specifier' as const,
    named: true as const,
    text: 'mut' as const,
    render: () => 'mut' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'mut' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'mut' as const },
    replace: (t: MutableSpecifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'mut' as const }; },
  };
}

export function expressionExceptRange(child: ChildOf<ExpressionExceptRange>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ExpressionExceptRange>['children'];
  return {
    type: '_expression_except_range' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ExpressionExceptRangeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function macroInvocation(config: ConfigOf<MacroInvocation>) {
  const fields = {
    macro: config?.macro,
    token_tree: config?.tokenTree,
  };
  return {
    type: 'macro_invocation' as const,
    named: true as const,
    fields,
    macro(macro_?: ConfigOf<MacroInvocation>['macro']) { return _fs(config, macroInvocation, 'macro', macro_, fields.macro); },
    tokenTree(tokenTree_?: ConfigOf<MacroInvocation>['tokenTree']) { return _fs(config, macroInvocation, 'tokenTree', tokenTree_, fields.token_tree); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MacroInvocationTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function delimTokenTree(..._children: ChildOf<DelimTokenTree>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<DelimTokenTree>['children'];
  return {
    type: 'delim_token_tree' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: DelimTokenTreeTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function nonDelimToken(child: ChildOf<NonDelimToken>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<NonDelimToken>['children'];
  return {
    type: '_non_delim_token' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NonDelimTokenTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedIdentifier(config: ConfigOf<ScopedIdentifier>) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_identifier' as const,
    named: true as const,
    fields,
    path(path_?: ConfigOf<ScopedIdentifier>['path']) { return _fs(config, scopedIdentifier, 'path', path_, fields.path); },
    name(name_?: ConfigOf<ScopedIdentifier>['name']) { return _fs(config, scopedIdentifier, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifierInExpressionPosition(config: ConfigOf<ScopedTypeIdentifierInExpressionPosition>) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_type_identifier_in_expression_position' as const,
    named: true as const,
    fields,
    path(path_?: ConfigOf<ScopedTypeIdentifierInExpressionPosition>['path']) { return _fs(config, scopedTypeIdentifierInExpressionPosition, 'path', path_, fields.path); },
    name(name_?: ConfigOf<ScopedTypeIdentifierInExpressionPosition>['name']) { return _fs(config, scopedTypeIdentifierInExpressionPosition, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedTypeIdentifierInExpressionPositionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function scopedTypeIdentifier(config: ConfigOf<ScopedTypeIdentifier>) {
  const fields = {
    path: config?.path,
    name: config?.name,
  };
  return {
    type: 'scoped_type_identifier' as const,
    named: true as const,
    fields,
    path(path_?: ConfigOf<ScopedTypeIdentifier>['path']) { return _fs(config, scopedTypeIdentifier, 'path', path_, fields.path); },
    name(name_?: ConfigOf<ScopedTypeIdentifier>['name']) { return _fs(config, scopedTypeIdentifier, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ScopedTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangeExpression(child: ChildOf<RangeExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<RangeExpression>['children'];
  return {
    type: 'range_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangeExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unaryExpression(config: ConfigOf<UnaryExpression>) {
  const fields = {
    operand: config?.operand,
  };
  const children = config?.children ?? [];
  return {
    type: 'unary_expression' as const,
    named: true as const,
    fields,
    children,
    operand(operand_?: ConfigOf<UnaryExpression>['operand']) { return _fs(config, unaryExpression, 'operand', operand_, fields.operand); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<UnaryExpression>) { return unaryExpression({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryExpression(config: ConfigOf<TryExpression>) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'try_expression' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<TryExpression>['value']) { return _fs(config, tryExpression, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referenceExpression(config: ConfigOf<ReferenceExpression>) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    value: config?.value,
  };
  return {
    type: 'reference_expression' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: ConfigOf<ReferenceExpression>['mutableSpecifier']) { return _fs(config, referenceExpression, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    value(value_?: ConfigOf<ReferenceExpression>['value']) { return _fs(config, referenceExpression, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferenceExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function binaryExpression(config: ConfigOf<BinaryExpression>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'binary_expression' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<BinaryExpression>['left']) { return _fs(config, binaryExpression, 'left', left_, fields.left); },
    operator(operator_?: ConfigOf<BinaryExpression>['operator']) { return _fs(config, binaryExpression, 'operator', operator_, fields.operator); },
    right(right_?: ConfigOf<BinaryExpression>['right']) { return _fs(config, binaryExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BinaryExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function assignmentExpression(config: ConfigOf<AssignmentExpression>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'assignment_expression' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<AssignmentExpression>['left']) { return _fs(config, assignmentExpression, 'left', left_, fields.left); },
    right(right_?: ConfigOf<AssignmentExpression>['right']) { return _fs(config, assignmentExpression, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AssignmentExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function compoundAssignmentExpr(config: ConfigOf<CompoundAssignmentExpr>) {
  const fields = {
    left: config?.left,
    operator: config?.operator,
    right: config?.right,
  };
  return {
    type: 'compound_assignment_expr' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<CompoundAssignmentExpr>['left']) { return _fs(config, compoundAssignmentExpr, 'left', left_, fields.left); },
    operator(operator_?: ConfigOf<CompoundAssignmentExpr>['operator']) { return _fs(config, compoundAssignmentExpr, 'operator', operator_, fields.operator); },
    right(right_?: ConfigOf<CompoundAssignmentExpr>['right']) { return _fs(config, compoundAssignmentExpr, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CompoundAssignmentExprTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function typeCastExpression(config: ConfigOf<TypeCastExpression>) {
  const fields = {
    value: config?.value,
    type: config?.type,
  };
  return {
    type: 'type_cast_expression' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<TypeCastExpression>['value']) { return _fs(config, typeCastExpression, 'value', value_, fields.value); },
    typeField(type?: ConfigOf<TypeCastExpression>['type']) { return _fs(config, typeCastExpression, 'type', type, fields.type); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TypeCastExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function returnExpression(child: ChildOf<ReturnExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ReturnExpression>['children'];
  return {
    type: 'return_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReturnExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function yieldExpression(child: ChildOf<YieldExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<YieldExpression>['children'];
  return {
    type: 'yield_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: YieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function callExpression(config: ConfigOf<CallExpression>) {
  const fields = {
    function: config?.function,
    arguments: config?.arguments,
  };
  return {
    type: 'call_expression' as const,
    named: true as const,
    fields,
    function(function_?: ConfigOf<CallExpression>['function']) { return _fs(config, callExpression, 'function', function_, fields.function); },
    arguments(arguments_?: ConfigOf<CallExpression>['arguments']) { return _fs(config, callExpression, 'arguments', arguments_, fields.arguments); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CallExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arguments_(..._children: ChildOf<Arguments>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<Arguments>['children'];
  return {
    type: 'arguments' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArgumentsTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function arrayExpression(config: ConfigOf<ArrayExpression>) {
  const fields = {
    attributes: config?.attributes,
    elements: config?.elements,
  };
  return {
    type: 'array_expression' as const,
    named: true as const,
    fields,
    attributes(...attributes_: NonNullable<ConfigOf<ArrayExpression>['attributes']>) { return _fsm(config, arrayExpression, 'attributes', attributes_, fields.attributes); },
    elements(elements_?: ConfigOf<ArrayExpression>['elements']) { return _fs(config, arrayExpression, 'elements', elements_, fields.elements); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ArrayExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function parenthesizedExpression(child: ChildOf<ParenthesizedExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ParenthesizedExpression>['children'];
  return {
    type: 'parenthesized_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ParenthesizedExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleExpression(config: ConfigOf<TupleExpression>) {
  const fields = {
    attributes: config?.attributes,
    first: config?.first,
    rest: config?.rest,
    trailing: config?.trailing,
  };
  return {
    type: 'tuple_expression' as const,
    named: true as const,
    fields,
    attributes(...attributes_: NonNullable<ConfigOf<TupleExpression>['attributes']>) { return _fsm(config, tupleExpression, 'attributes', attributes_, fields.attributes); },
    first(first_?: ConfigOf<TupleExpression>['first']) { return _fs(config, tupleExpression, 'first', first_, fields.first); },
    rest(...rest_: NonNullable<ConfigOf<TupleExpression>['rest']>) { return _fsm(config, tupleExpression, 'rest', rest_, fields.rest); },
    trailing(trailing_?: ConfigOf<TupleExpression>['trailing']) { return _fs(config, tupleExpression, 'trailing', trailing_, fields.trailing); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unitExpression(text: string) {
  return {
    type: 'unit_expression' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: UnitExpressionTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function structExpression(config: ConfigOf<StructExpression>) {
  const fields = {
    name: config?.name,
    body: config?.body,
  };
  return {
    type: 'struct_expression' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<StructExpression>['name']) { return _fs(config, structExpression, 'name', name_, fields.name); },
    body(body_?: ConfigOf<StructExpression>['body']) { return _fs(config, structExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializerList(..._children: ChildOf<FieldInitializerList>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<FieldInitializerList>['children'];
  return {
    type: 'field_initializer_list' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerListTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandFieldInitializer(config: ConfigOf<ShorthandFieldInitializer>) {
  const fields = {
    attributes: config?.attributes,
    identifier: config?.identifier,
  };
  return {
    type: 'shorthand_field_initializer' as const,
    named: true as const,
    fields,
    attributes(...attributes_: NonNullable<ConfigOf<ShorthandFieldInitializer>['attributes']>) { return _fsm(config, shorthandFieldInitializer, 'attributes', attributes_, fields.attributes); },
    identifier(identifier_?: ConfigOf<ShorthandFieldInitializer>['identifier']) { return _fs(config, shorthandFieldInitializer, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ShorthandFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldInitializer(config: ConfigOf<FieldInitializer>) {
  const fields = {
    field: config?.field,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'field_initializer' as const,
    named: true as const,
    fields,
    children,
    field(field_?: ConfigOf<FieldInitializer>['field']) { return _fs(config, fieldInitializer, 'field', field_, fields.field); },
    value(value_?: ConfigOf<FieldInitializer>['value']) { return _fs(config, fieldInitializer, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: ChildOf<FieldInitializer>[]) { return fieldInitializer({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function baseFieldInitializer(child: ChildOf<BaseFieldInitializer>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<BaseFieldInitializer>['children'];
  return {
    type: 'base_field_initializer' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BaseFieldInitializerTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function ifExpression(config: ConfigOf<IfExpression>) {
  const fields = {
    condition: config?.condition,
    consequence: config?.consequence,
    alternative: config?.alternative,
  };
  return {
    type: 'if_expression' as const,
    named: true as const,
    fields,
    condition(condition_?: ConfigOf<IfExpression>['condition']) { return _fs(config, ifExpression, 'condition', condition_, fields.condition); },
    consequence(consequence_?: ConfigOf<IfExpression>['consequence']) { return _fs(config, ifExpression, 'consequence', consequence_, fields.consequence); },
    alternative(alternative_?: ConfigOf<IfExpression>['alternative']) { return _fs(config, ifExpression, 'alternative', alternative_, fields.alternative); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IfExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function letCondition(config: ConfigOf<LetCondition>) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  return {
    type: 'let_condition' as const,
    named: true as const,
    fields,
    pattern(pattern_?: ConfigOf<LetCondition>['pattern']) { return _fs(config, letCondition, 'pattern', pattern_, fields.pattern); },
    value(value_?: ConfigOf<LetCondition>['value']) { return _fs(config, letCondition, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetConditionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenLetChain(child: ChildOf<HiddenLetChain>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<HiddenLetChain>['children'];
  return {
    type: '_let_chain' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenLetChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function elseClause(child: ChildOf<ElseClause>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<ElseClause>['children'];
  return {
    type: 'else_clause' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ElseClauseTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchExpression(config: ConfigOf<MatchExpression>) {
  const fields = {
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'match_expression' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<MatchExpression>['value']) { return _fs(config, matchExpression, 'value', value_, fields.value); },
    body(body_?: ConfigOf<MatchExpression>['body']) { return _fs(config, matchExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchBlock(..._children: ChildOf<MatchBlock>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<MatchBlock>['children'];
  return {
    type: 'match_block' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchArm(config: ConfigOf<MatchArm>) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: ConfigOf<MatchArm>['pattern']) { return _fs(config, matchArm, 'pattern', pattern_, fields.pattern); },
    value(value_?: ConfigOf<MatchArm>['value']) { return _fs(config, matchArm, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: ChildOf<MatchArm>[]) { return matchArm({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lastMatchArm(config: ConfigOf<LastMatchArm>) {
  const fields = {
    pattern: config?.pattern,
    value: config?.value,
  };
  const children = config?.children ?? [];
  return {
    type: 'last_match_arm' as const,
    named: true as const,
    fields,
    children,
    pattern(pattern_?: ConfigOf<LastMatchArm>['pattern']) { return _fs(config, lastMatchArm, 'pattern', pattern_, fields.pattern); },
    value(value_?: ConfigOf<LastMatchArm>['value']) { return _fs(config, lastMatchArm, 'value', value_, fields.value); },
    getChildren() { return children; },
    setChildren(...items: ChildOf<LastMatchArm>[]) { return lastMatchArm({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LastMatchArmTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function matchPattern(config: ConfigOf<MatchPattern>) {
  const fields = {
    pattern: config?.pattern,
    condition: config?.condition,
  };
  return {
    type: 'match_pattern' as const,
    named: true as const,
    fields,
    pattern(pattern_?: ConfigOf<MatchPattern>['pattern']) { return _fs(config, matchPattern, 'pattern', pattern_, fields.pattern); },
    condition(condition_?: ConfigOf<MatchPattern>['condition']) { return _fs(config, matchPattern, 'condition', condition_, fields.condition); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MatchPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function whileExpression(config: ConfigOf<WhileExpression>) {
  const fields = {
    label: config?.label,
    condition: config?.condition,
    body: config?.body,
  };
  return {
    type: 'while_expression' as const,
    named: true as const,
    fields,
    label(label_?: ConfigOf<WhileExpression>['label']) { return _fs(config, whileExpression, 'label', label_, fields.label); },
    condition(condition_?: ConfigOf<WhileExpression>['condition']) { return _fs(config, whileExpression, 'condition', condition_, fields.condition); },
    body(body_?: ConfigOf<WhileExpression>['body']) { return _fs(config, whileExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: WhileExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function loopExpression(config: ConfigOf<LoopExpression>) {
  const fields = {
    label: config?.label,
    body: config?.body,
  };
  return {
    type: 'loop_expression' as const,
    named: true as const,
    fields,
    label(label_?: ConfigOf<LoopExpression>['label']) { return _fs(config, loopExpression, 'label', label_, fields.label); },
    body(body_?: ConfigOf<LoopExpression>['body']) { return _fs(config, loopExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LoopExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function forExpression(config: ConfigOf<ForExpression>) {
  const fields = {
    label: config?.label,
    pattern: config?.pattern,
    value: config?.value,
    body: config?.body,
  };
  return {
    type: 'for_expression' as const,
    named: true as const,
    fields,
    label(label_?: ConfigOf<ForExpression>['label']) { return _fs(config, forExpression, 'label', label_, fields.label); },
    pattern(pattern_?: ConfigOf<ForExpression>['pattern']) { return _fs(config, forExpression, 'pattern', pattern_, fields.pattern); },
    value(value_?: ConfigOf<ForExpression>['value']) { return _fs(config, forExpression, 'value', value_, fields.value); },
    body(body_?: ConfigOf<ForExpression>['body']) { return _fs(config, forExpression, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ForExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function constBlock(config: ConfigOf<ConstBlock>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'const_block' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<ConstBlock>['body']) { return _fs(config, constBlock, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ConstBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureExpression(config: ConfigOf<ClosureExpressionBody> | ConfigOf<ClosureExpressionBody2>) {
  if (config && 'returnType' in config && 'body' in config) return closureExpressionBody(config as ConfigOf<ClosureExpressionBody>);
  return closureExpressionBody2(config as ConfigOf<ClosureExpressionBody2>);
}
export function closureExpressionBody(config: ConfigOf<ClosureExpressionBody>) {
  const fields = {
    return_type: config?.returnType,
    body: config?.body,
  };
  return {
    type: 'closure_expression' as const,
    named: true as const,
    fields,
    returnType(returnType_?: ConfigOf<ClosureExpressionBody>['returnType']) { return _fs(config, closureExpressionBody, 'returnType', returnType_, fields.return_type); },
    body(body_?: ConfigOf<ClosureExpressionBody>['body']) { return _fs(config, closureExpressionBody, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureExpressionBodyTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function closureExpressionBody2(config: ConfigOf<ClosureExpressionBody2>) {
  const fields = {
    body: config?.body,
  };
  return {
    type: 'closure_expression' as const,
    named: true as const,
    fields,
    body(body_?: ConfigOf<ClosureExpressionBody2>['body']) { return _fs(config, closureExpressionBody2, 'body', body_, fields.body); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureExpressionBody2Tree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function closureParameters(config?: ConfigOf<ClosureParameters>) {
  const fields = {
    pattern: config?.pattern,
  };
  const children = config?.children ?? [];
  return {
    type: 'closure_parameters' as const,
    named: true as const,
    fields,
    children,
    pattern(...pattern_: NonNullable<ConfigOf<ClosureParameters>['pattern']>) { return _fsm(config, closureParameters, 'pattern', pattern_, fields.pattern); },
    getChildren() { return children; },
    setChildren(...items: ChildOf<ClosureParameters>[]) { return closureParameters({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ClosureParametersTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function label(config: ConfigOf<Label>) {
  const fields = {
    identifier: config?.identifier,
  };
  return {
    type: 'label' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<Label>['identifier']) { return _fs(config, label, 'identifier', identifier_, fields.identifier); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LabelTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function breakExpression(config?: ConfigOf<BreakExpression>) {
  const fields = {
    label: config?.label,
    expression: config?.expression,
  };
  return {
    type: 'break_expression' as const,
    named: true as const,
    fields,
    label(label_?: ConfigOf<BreakExpression>['label']) { return _fs(config, breakExpression, 'label', label_, fields.label); },
    expression(expression_?: ConfigOf<BreakExpression>['expression']) { return _fs(config, breakExpression, 'expression', expression_, fields.expression); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BreakExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function continueExpression(config?: ConfigOf<ContinueExpression>) {
  const fields = {
    label: config?.label,
  };
  return {
    type: 'continue_expression' as const,
    named: true as const,
    fields,
    label(label_?: ConfigOf<ContinueExpression>['label']) { return _fs(config, continueExpression, 'label', label_, fields.label); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ContinueExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function indexExpression(config: ConfigOf<IndexExpression>) {
  const fields = {
    object: config?.object,
    index: config?.index,
  };
  return {
    type: 'index_expression' as const,
    named: true as const,
    fields,
    object(object_?: ConfigOf<IndexExpression>['object']) { return _fs(config, indexExpression, 'object', object_, fields.object); },
    index(index_?: ConfigOf<IndexExpression>['index']) { return _fs(config, indexExpression, 'index', index_, fields.index); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: IndexExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function awaitExpression(child: ChildOf<AwaitExpression>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<AwaitExpression>['children'];
  return {
    type: 'await_expression' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AwaitExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldExpression(config: ConfigOf<FieldExpression>) {
  const fields = {
    value: config?.value,
    field: config?.field,
  };
  return {
    type: 'field_expression' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<FieldExpression>['value']) { return _fs(config, fieldExpression, 'value', value_, fields.value); },
    field(field_?: ConfigOf<FieldExpression>['field']) { return _fs(config, fieldExpression, 'field', field_, fields.field); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldExpressionTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function unsafeBlock(config: ConfigOf<UnsafeBlock>) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'unsafe_block' as const,
    named: true as const,
    fields,
    block(block_?: ConfigOf<UnsafeBlock>['block']) { return _fs(config, unsafeBlock, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: UnsafeBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function asyncBlock(config: ConfigOf<AsyncBlock>) {
  const fields = {
    block: config?.block,
  };
  const children = config?.children ?? [];
  return {
    type: 'async_block' as const,
    named: true as const,
    fields,
    children,
    block(block_?: ConfigOf<AsyncBlock>['block']) { return _fs(config, asyncBlock, 'block', block_, fields.block); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<AsyncBlock>) { return asyncBlock({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: AsyncBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genBlock(config: ConfigOf<GenBlock>) {
  const fields = {
    block: config?.block,
  };
  const children = config?.children ?? [];
  return {
    type: 'gen_block' as const,
    named: true as const,
    fields,
    children,
    block(block_?: ConfigOf<GenBlock>['block']) { return _fs(config, genBlock, 'block', block_, fields.block); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<GenBlock>) { return genBlock({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tryBlock(config: ConfigOf<TryBlock>) {
  const fields = {
    block: config?.block,
  };
  return {
    type: 'try_block' as const,
    named: true as const,
    fields,
    block(block_?: ConfigOf<TryBlock>['block']) { return _fs(config, tryBlock, 'block', block_, fields.block); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TryBlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function block(config: ConfigOf<Block>) {
  const fields = {
    label: config?.label,
  };
  const children = config?.children ?? [];
  return {
    type: 'block' as const,
    named: true as const,
    fields,
    children,
    label(label_?: ConfigOf<Block>['label']) { return _fs(config, block, 'label', label_, fields.label); },
    getChildren() { return children; },
    setChildren(...items: ChildOf<Block>[]) { return block({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function genericPattern(config: ConfigOf<GenericPattern>) {
  const fields = {
    type_arguments: config?.typeArguments,
  };
  const children = config?.children ?? [];
  return {
    type: 'generic_pattern' as const,
    named: true as const,
    fields,
    children,
    typeArguments(typeArguments_?: ConfigOf<GenericPattern>['typeArguments']) { return _fs(config, genericPattern, 'typeArguments', typeArguments_, fields.type_arguments); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<GenericPattern>) { return genericPattern({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: GenericPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tuplePattern(config?: ConfigOf<TuplePattern>) {
  const fields = {
    pattern: config?.pattern,
  };
  const children = config?.children ?? [];
  return {
    type: 'tuple_pattern' as const,
    named: true as const,
    fields,
    children,
    pattern(...pattern_: NonNullable<ConfigOf<TuplePattern>['pattern']>) { return _fsm(config, tuplePattern, 'pattern', pattern_, fields.pattern); },
    getChildren() { return children; },
    setChildren(...items: ChildOf<TuplePattern>[]) { return tuplePattern({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TuplePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function slicePattern(config?: ConfigOf<SlicePattern>) {
  const fields = {
    pattern: config?.pattern,
  };
  return {
    type: 'slice_pattern' as const,
    named: true as const,
    fields,
    pattern(...pattern_: NonNullable<ConfigOf<SlicePattern>['pattern']>) { return _fsm(config, slicePattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: SlicePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function tupleStructPattern(config: ConfigOf<TupleStructPattern>) {
  const fields = {
    type: config?.type,
    pattern: config?.pattern,
  };
  return {
    type: 'tuple_struct_pattern' as const,
    named: true as const,
    fields,
    typeField(type?: ConfigOf<TupleStructPattern>['type']) { return _fs(config, tupleStructPattern, 'type', type, fields.type); },
    pattern(...pattern_: NonNullable<ConfigOf<TupleStructPattern>['pattern']>) { return _fsm(config, tupleStructPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: TupleStructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function structPattern(config: ConfigOf<StructPattern>) {
  const fields = {
    type: config?.type,
  };
  const children = config?.children ?? [];
  return {
    type: 'struct_pattern' as const,
    named: true as const,
    fields,
    children,
    typeField(type?: ConfigOf<StructPattern>['type']) { return _fs(config, structPattern, 'type', type, fields.type); },
    getChildren() { return children; },
    setChildren(...items: ChildOf<StructPattern>[]) { return structPattern({ ...(config ?? {}), children: items }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StructPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function fieldPattern(config: ConfigOf<FieldPatternName> | ConfigOf<FieldPatternColon>) {
  if (config && 'name' in config && 'pattern' in config) return fieldPatternColon(config as ConfigOf<FieldPatternColon>);
  return fieldPatternName(config as ConfigOf<FieldPatternName>);
}
export function fieldPatternName(config: ConfigOf<FieldPatternName>) {
  const fields = {
    name: config?.name,
  };
  return {
    type: 'field_pattern' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<FieldPatternName>['name']) { return _fs(config, fieldPatternName, 'name', name_, fields.name); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternNameTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function fieldPatternColon(config: ConfigOf<FieldPatternColon>) {
  const fields = {
    name: config?.name,
    pattern: config?.pattern,
  };
  return {
    type: 'field_pattern' as const,
    named: true as const,
    fields,
    name(name_?: ConfigOf<FieldPatternColon>['name']) { return _fs(config, fieldPatternColon, 'name', name_, fields.name); },
    pattern(pattern_?: ConfigOf<FieldPatternColon>['pattern']) { return _fs(config, fieldPatternColon, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: FieldPatternColonTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function mutPattern(config: ConfigOf<MutPattern>) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'mut_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: ConfigOf<MutPattern>['mutableSpecifier']) { return _fs(config, mutPattern, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: ConfigOf<MutPattern>['pattern']) { return _fs(config, mutPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: MutPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rangePattern(config: ConfigOf<RangePatternLeft> | ConfigOf<RangePatternRight>) {
  if (config && 'left' in config && 'right' in config) return rangePatternLeft(config as ConfigOf<RangePatternLeft>);
  return rangePatternRight(config as ConfigOf<RangePatternRight>);
}
export function rangePatternLeft(config: ConfigOf<RangePatternLeft>) {
  const fields = {
    left: config?.left,
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    fields,
    left(left_?: ConfigOf<RangePatternLeft>['left']) { return _fs(config, rangePatternLeft, 'left', left_, fields.left); },
    right(right_?: ConfigOf<RangePatternLeft>['right']) { return _fs(config, rangePatternLeft, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternLeftTree) { const r = target.range(); return toEdit(this, r); },
  };
}
export function rangePatternRight(config: ConfigOf<RangePatternRight>) {
  const fields = {
    right: config?.right,
  };
  return {
    type: 'range_pattern' as const,
    named: true as const,
    fields,
    right(right_?: ConfigOf<RangePatternRight>['right']) { return _fs(config, rangePatternRight, 'right', right_, fields.right); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RangePatternRightTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function refPattern(config: ConfigOf<RefPattern>) {
  const fields = {
    pattern: config?.pattern,
  };
  return {
    type: 'ref_pattern' as const,
    named: true as const,
    fields,
    pattern(pattern_?: ConfigOf<RefPattern>['pattern']) { return _fs(config, refPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RefPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function capturedPattern(config: ConfigOf<CapturedPattern>) {
  const fields = {
    identifier: config?.identifier,
    pattern: config?.pattern,
  };
  return {
    type: 'captured_pattern' as const,
    named: true as const,
    fields,
    identifier(identifier_?: ConfigOf<CapturedPattern>['identifier']) { return _fs(config, capturedPattern, 'identifier', identifier_, fields.identifier); },
    pattern(pattern_?: ConfigOf<CapturedPattern>['pattern']) { return _fs(config, capturedPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CapturedPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function referencePattern(config: ConfigOf<ReferencePattern>) {
  const fields = {
    mutable_specifier: config?.mutableSpecifier,
    pattern: config?.pattern,
  };
  return {
    type: 'reference_pattern' as const,
    named: true as const,
    fields,
    mutableSpecifier(mutableSpecifier_?: ConfigOf<ReferencePattern>['mutableSpecifier']) { return _fs(config, referencePattern, 'mutableSpecifier', mutableSpecifier_, fields.mutable_specifier); },
    pattern(pattern_?: ConfigOf<ReferencePattern>['pattern']) { return _fs(config, referencePattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: ReferencePatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function orPattern(config?: ConfigOf<OrPattern>) {
  const fields = {
    pattern: config?.pattern,
  };
  return {
    type: 'or_pattern' as const,
    named: true as const,
    fields,
    pattern(pattern_?: ConfigOf<OrPattern>['pattern']) { return _fs(config, orPattern, 'pattern', pattern_, fields.pattern); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: OrPatternTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function negativeLiteral(config: ConfigOf<NegativeLiteral>) {
  const fields = {
    value: config?.value,
  };
  return {
    type: 'negative_literal' as const,
    named: true as const,
    fields,
    value(value_?: ConfigOf<NegativeLiteral>['value']) { return _fs(config, negativeLiteral, 'value', value_, fields.value); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: NegativeLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function integerLiteral(text: string) {
  return {
    type: 'integer_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IntegerLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringLiteral(..._children: ChildOf<StringLiteral>[]) {
  const children = _children.filter(c => c && typeof c === "object" && "type" in c) as ConfigOf<StringLiteral>['children'];
  return {
    type: 'string_literal' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: StringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function rawStringLiteral(config: ConfigOf<RawStringLiteral>) {
  const fields = {
    raw_string_literal_start: config?.rawStringLiteralStart,
    string_content: config?.stringContent,
    raw_string_literal_end: config?.rawStringLiteralEnd,
  };
  return {
    type: 'raw_string_literal' as const,
    named: true as const,
    fields,
    rawStringLiteralStart(rawStringLiteralStart_?: ConfigOf<RawStringLiteral>['rawStringLiteralStart']) { return _fs(config, rawStringLiteral, 'rawStringLiteralStart', rawStringLiteralStart_, fields.raw_string_literal_start); },
    stringContent(stringContent_?: ConfigOf<RawStringLiteral>['stringContent']) { return _fs(config, rawStringLiteral, 'stringContent', stringContent_, fields.string_content); },
    rawStringLiteralEnd(rawStringLiteralEnd_?: ConfigOf<RawStringLiteral>['rawStringLiteralEnd']) { return _fs(config, rawStringLiteral, 'rawStringLiteralEnd', rawStringLiteralEnd_, fields.raw_string_literal_end); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: RawStringLiteralTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function charLiteral(text: string) {
  return {
    type: 'char_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: CharLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function escapeSequence(text: string) {
  return {
    type: 'escape_sequence' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: EscapeSequenceTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function booleanLiteral(text: string) {
  if (!(['true', 'false'] as const as readonly string[]).includes(text)) throw new Error(`boolean_literal: text '${text}' is not one of true|false`);
  return {
    type: 'boolean_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: BooleanLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function comment(child: ChildOf<Comment>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<Comment>['children'];
  return {
    type: 'comment' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: CommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function lineComment(config: ConfigOf<LineComment>) {
  const fields = {
    outer: config?.outer,
    inner: config?.inner,
    doc: config?.doc,
  };
  return {
    type: 'line_comment' as const,
    named: true as const,
    fields,
    outer(outer_?: ConfigOf<LineComment>['outer']) { return _fs(config, lineComment, 'outer', outer_, fields.outer); },
    inner(inner_?: ConfigOf<LineComment>['inner']) { return _fs(config, lineComment, 'inner', inner_, fields.inner); },
    doc(doc_?: ConfigOf<LineComment>['doc']) { return _fs(config, lineComment, 'doc', doc_, fields.doc); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LineCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function blockComment(config?: ConfigOf<BlockComment>) {
  const fields = {
    outer: config?.outer,
    inner: config?.inner,
    doc: config?.doc,
  };
  const children = config?.children ?? [];
  return {
    type: 'block_comment' as const,
    named: true as const,
    fields,
    children,
    outer(outer_?: ConfigOf<BlockComment>['outer']) { return _fs(config, blockComment, 'outer', outer_, fields.outer); },
    inner(inner_?: ConfigOf<BlockComment>['inner']) { return _fs(config, blockComment, 'inner', inner_, fields.inner); },
    doc(doc_?: ConfigOf<BlockComment>['doc']) { return _fs(config, blockComment, 'doc', doc_, fields.doc); },
    getChild() { return children[0]; },
    setChild(child: ChildOf<BlockComment>) { return blockComment({ ...(config ?? {}), children: [child] }); },
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: BlockCommentTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenPath(child: ChildOf<HiddenPath>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<HiddenPath>['children'];
  return {
    type: '_path' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenPathTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function identifier(text: string) {
  return {
    type: 'identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: IdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function shebang(text: string) {
  return {
    type: 'shebang' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ShebangTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function reservedIdentifier(text: string) {
  if (!(['default', 'union', 'gen'] as const as readonly string[]).includes(text)) throw new Error(`_reserved_identifier: text '${text}' is not one of default|union|gen`);
  return {
    type: '_reserved_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ReservedIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function hiddenTypeIdentifier(child: ChildOf<HiddenTypeIdentifier>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<HiddenTypeIdentifier>['children'];
  return {
    type: '_type_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenTypeIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function hiddenFieldIdentifier(child: ChildOf<HiddenFieldIdentifier>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<HiddenFieldIdentifier>['children'];
  return {
    type: '_field_identifier' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: HiddenFieldIdentifierTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function self() {
  return {
    type: 'self' as const,
    named: true as const,
    text: 'self' as const,
    render: () => 'self' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'self' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'self' as const },
    replace: (t: SelfTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'self' as const }; },
  };
}

export function super_() {
  return {
    type: 'super' as const,
    named: true as const,
    text: 'super' as const,
    render: () => 'super' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'super' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'super' as const },
    replace: (t: SuperTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'super' as const }; },
  };
}

export function crate() {
  return {
    type: 'crate' as const,
    named: true as const,
    text: 'crate' as const,
    render: () => 'crate' as const,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: 'crate' as const } : { startPos: s.start.index, endPos: s.end.index, insertedText: 'crate' as const },
    replace: (t: CrateTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: 'crate' as const }; },
  };
}

export function metavariable(text: string) {
  return {
    type: 'metavariable' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: MetavariableTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function stringContent(text: string) {
  return {
    type: 'string_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: StringContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function rawStringLiteralContent(text: string) {
  return {
    type: 'raw_string_literal_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: RawStringLiteralContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function floatLiteral(text: string) {
  return {
    type: 'float_literal' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FloatLiteralTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function blockCommentContent(text: string) {
  return {
    type: '_block_comment_content' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: BlockCommentContentTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function errorSentinel(text: string) {
  return {
    type: '_error_sentinel' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ErrorSentinelTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function primitiveType(text: string) {
  if (!(['u8', 'i8', 'u16', 'i16', 'u32', 'i32', 'u64', 'i64', 'u128', 'i128', 'isize', 'usize', 'f32', 'f64', 'bool', 'str', 'char'] as const as readonly string[]).includes(text)) throw new Error(`primitive_type: text '${text}' is not one of u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64|bool|str|char`);
  return {
    type: 'primitive_type' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: PrimitiveTypeTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function letChain(child: ChildOf<LetChain>) {
  const hasChild = child && typeof child === "object" && "type" in child;
  const children = (hasChild ? [child] : []) as ConfigOf<LetChain>['children'];
  return {
    type: 'let_chain' as const,
    named: true as const,
    children,
    render() { return render(this); },
    toEdit(startOrRange: number | { start: { index: number }; end: { index: number } }, endPos?: number) {
      if (typeof startOrRange === 'number') return toEdit(this, startOrRange, endPos!);
      return toEdit(this, startOrRange);
    },
    replace(target: LetChainTree) { const r = target.range(); return toEdit(this, r); },
  };
}

export function shorthandFieldIdentifier(text: string) {
  return {
    type: 'shorthand_field_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: ShorthandFieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function outerDocCommentMarker(text: string) {
  return {
    type: 'outer_doc_comment_marker' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: OuterDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function innerDocCommentMarker(text: string) {
  return {
    type: 'inner_doc_comment_marker' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: InnerDocCommentMarkerTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function typeIdentifier(text: string) {
  return {
    type: 'type_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: TypeIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export function fieldIdentifier(text: string) {
  return {
    type: 'field_identifier' as const,
    named: true as const,
    text: text,
    render: () => text,
    toEdit: (s: number | { start: { index: number }; end: { index: number } }, e?: number) => typeof s === 'number' ? { startPos: s, endPos: e!, insertedText: text } : { startPos: s.start.index, endPos: s.end.index, insertedText: text },
    replace: (t: FieldIdentifierTree) => { const r = t.range(); return { startPos: r.start.index, endPos: r.end.index, insertedText: text }; },
  };
}

export type FluentKindMap = {
  "source_file": FluentNode<"source_file", ConfigOf<SourceFile>>;
  "expression_statement": FluentNode<"expression_statement", ConfigOf<ExpressionStatement>>;
  "macro_definition": FluentNode<"macro_definition", ConfigOf<MacroDefinition>>;
  "macro_rule": FluentNode<"macro_rule", ConfigOf<MacroRule>>;
  "token_tree_pattern": FluentNode<"token_tree_pattern", ConfigOf<TokenTreePattern>>;
  "token_binding_pattern": FluentNode<"token_binding_pattern", ConfigOf<TokenBindingPattern>>;
  "token_repetition_pattern": FluentNode<"token_repetition_pattern", ConfigOf<TokenRepetitionPattern>>;
  "fragment_specifier": FragmentSpecifier;
  "token_tree": FluentNode<"token_tree", ConfigOf<TokenTree>>;
  "token_repetition": FluentNode<"token_repetition", ConfigOf<TokenRepetition>>;
  "_non_special_token": FluentNode<"_non_special_token", ConfigOf<NonSpecialToken>>;
  "attribute_item": FluentNode<"attribute_item", ConfigOf<AttributeItem>>;
  "inner_attribute_item": FluentNode<"inner_attribute_item", ConfigOf<InnerAttributeItem>>;
  "attribute": FluentNode<"attribute", ConfigOf<Attribute>>;
  "mod_item": FluentNode<"mod_item", ConfigOf<ModItem>>;
  "foreign_mod_item": FluentNode<"foreign_mod_item", ConfigOf<ForeignModItem>>;
  "declaration_list": FluentNode<"declaration_list", ConfigOf<DeclarationList>>;
  "struct_item": FluentNode<"struct_item", ConfigOf<StructItem>>;
  "union_item": FluentNode<"union_item", ConfigOf<UnionItem>>;
  "enum_item": FluentNode<"enum_item", ConfigOf<EnumItem>>;
  "enum_variant_list": FluentNode<"enum_variant_list", ConfigOf<EnumVariantList>>;
  "enum_variant": FluentNode<"enum_variant", ConfigOf<EnumVariant>>;
  "field_declaration_list": FluentNode<"field_declaration_list", ConfigOf<FieldDeclarationList>>;
  "field_declaration": FluentNode<"field_declaration", ConfigOf<FieldDeclaration>>;
  "ordered_field_declaration_list": FluentNode<"ordered_field_declaration_list", ConfigOf<OrderedFieldDeclarationList>>;
  "extern_crate_declaration": FluentNode<"extern_crate_declaration", ConfigOf<ExternCrateDeclaration>>;
  "const_item": FluentNode<"const_item", ConfigOf<ConstItem>>;
  "static_item": FluentNode<"static_item", ConfigOf<StaticItem>>;
  "type_item": FluentNode<"type_item", ConfigOf<TypeItem>>;
  "function_item": FluentNode<"function_item", ConfigOf<FunctionItem>>;
  "function_signature_item": FluentNode<"function_signature_item", ConfigOf<FunctionSignatureItem>>;
  "function_modifiers": FluentNode<"function_modifiers", ConfigOf<FunctionModifiers>>;
  "where_clause": FluentNode<"where_clause", ConfigOf<WhereClause>>;
  "where_predicate": FluentNode<"where_predicate", ConfigOf<WherePredicate>>;
  "impl_item": FluentNode<"impl_item", ConfigOf<ImplItem>>;
  "trait_item": FluentNode<"trait_item", ConfigOf<TraitItem>>;
  "associated_type": FluentNode<"associated_type", ConfigOf<AssociatedType>>;
  "trait_bounds": FluentNode<"trait_bounds", ConfigOf<TraitBounds>>;
  "higher_ranked_trait_bound": FluentNode<"higher_ranked_trait_bound", ConfigOf<HigherRankedTraitBound>>;
  "removed_trait_bound": FluentNode<"removed_trait_bound", ConfigOf<RemovedTraitBound>>;
  "type_parameters": FluentNode<"type_parameters", ConfigOf<TypeParameters>>;
  "const_parameter": FluentNode<"const_parameter", ConfigOf<ConstParameter>>;
  "type_parameter": FluentNode<"type_parameter", ConfigOf<TypeParameter>>;
  "lifetime_parameter": FluentNode<"lifetime_parameter", ConfigOf<LifetimeParameter>>;
  "let_declaration": FluentNode<"let_declaration", ConfigOf<LetDeclaration>>;
  "use_declaration": FluentNode<"use_declaration", ConfigOf<UseDeclaration>>;
  "scoped_use_list": FluentNode<"scoped_use_list", ConfigOf<ScopedUseList>>;
  "use_list": FluentNode<"use_list", ConfigOf<UseList>>;
  "use_as_clause": FluentNode<"use_as_clause", ConfigOf<UseAsClause>>;
  "use_wildcard": FluentNode<"use_wildcard", ConfigOf<UseWildcard>>;
  "parameters": FluentNode<"parameters", ConfigOf<Parameters>>;
  "self_parameter": FluentNode<"self_parameter", ConfigOf<SelfParameter>>;
  "variadic_parameter": FluentNode<"variadic_parameter", ConfigOf<VariadicParameter>>;
  "parameter": FluentNode<"parameter", ConfigOf<Parameter>>;
  "extern_modifier": FluentNode<"extern_modifier", ConfigOf<ExternModifier>>;
  "visibility_modifier": FluentNode<"visibility_modifier", ConfigOf<VisibilityModifier>>;
  "bracketed_type": FluentNode<"bracketed_type", ConfigOf<BracketedType>>;
  "qualified_type": FluentNode<"qualified_type", ConfigOf<QualifiedType>>;
  "lifetime": FluentNode<"lifetime", ConfigOf<Lifetime>>;
  "array_type": FluentNode<"array_type", ConfigOf<ArrayType>>;
  "for_lifetimes": FluentNode<"for_lifetimes", ConfigOf<ForLifetimes>>;
  "function_type": FluentNode<"function_type", ConfigOf<FunctionType>>;
  "tuple_type": FluentNode<"tuple_type", ConfigOf<TupleType>>;
  "unit_type": UnitType;
  "generic_function": FluentNode<"generic_function", ConfigOf<GenericFunction>>;
  "generic_type": FluentNode<"generic_type", ConfigOf<GenericType>>;
  "generic_type_with_turbofish": FluentNode<"generic_type_with_turbofish", ConfigOf<GenericTypeWithTurbofish>>;
  "bounded_type": FluentNode<"bounded_type", ConfigOf<BoundedType>>;
  "use_bounds": FluentNode<"use_bounds", ConfigOf<UseBounds>>;
  "type_arguments": FluentNode<"type_arguments", ConfigOf<TypeArguments>>;
  "type_binding": FluentNode<"type_binding", ConfigOf<TypeBinding>>;
  "reference_type": FluentNode<"reference_type", ConfigOf<ReferenceType>>;
  "pointer_type": FluentNode<"pointer_type", ConfigOf<PointerType>>;
  "abstract_type": FluentNode<"abstract_type", ConfigOf<AbstractType>>;
  "dynamic_type": FluentNode<"dynamic_type", ConfigOf<DynamicType>>;
  "mutable_specifier": MutableSpecifier;
  "_expression_except_range": FluentNode<"_expression_except_range", ConfigOf<ExpressionExceptRange>>;
  "macro_invocation": FluentNode<"macro_invocation", ConfigOf<MacroInvocation>>;
  "delim_token_tree": FluentNode<"delim_token_tree", ConfigOf<DelimTokenTree>>;
  "_non_delim_token": FluentNode<"_non_delim_token", ConfigOf<NonDelimToken>>;
  "scoped_identifier": FluentNode<"scoped_identifier", ConfigOf<ScopedIdentifier>>;
  "scoped_type_identifier_in_expression_position": FluentNode<"scoped_type_identifier_in_expression_position", ConfigOf<ScopedTypeIdentifierInExpressionPosition>>;
  "scoped_type_identifier": FluentNode<"scoped_type_identifier", ConfigOf<ScopedTypeIdentifier>>;
  "range_expression": FluentNode<"range_expression", ConfigOf<RangeExpression>>;
  "unary_expression": FluentNode<"unary_expression", ConfigOf<UnaryExpression>>;
  "try_expression": FluentNode<"try_expression", ConfigOf<TryExpression>>;
  "reference_expression": FluentNode<"reference_expression", ConfigOf<ReferenceExpression>>;
  "binary_expression": FluentNode<"binary_expression", ConfigOf<BinaryExpression>>;
  "assignment_expression": FluentNode<"assignment_expression", ConfigOf<AssignmentExpression>>;
  "compound_assignment_expr": FluentNode<"compound_assignment_expr", ConfigOf<CompoundAssignmentExpr>>;
  "type_cast_expression": FluentNode<"type_cast_expression", ConfigOf<TypeCastExpression>>;
  "return_expression": FluentNode<"return_expression", ConfigOf<ReturnExpression>>;
  "yield_expression": FluentNode<"yield_expression", ConfigOf<YieldExpression>>;
  "call_expression": FluentNode<"call_expression", ConfigOf<CallExpression>>;
  "arguments": FluentNode<"arguments", ConfigOf<Arguments>>;
  "array_expression": FluentNode<"array_expression", ConfigOf<ArrayExpression>>;
  "parenthesized_expression": FluentNode<"parenthesized_expression", ConfigOf<ParenthesizedExpression>>;
  "tuple_expression": FluentNode<"tuple_expression", ConfigOf<TupleExpression>>;
  "unit_expression": UnitExpression;
  "struct_expression": FluentNode<"struct_expression", ConfigOf<StructExpression>>;
  "field_initializer_list": FluentNode<"field_initializer_list", ConfigOf<FieldInitializerList>>;
  "shorthand_field_initializer": FluentNode<"shorthand_field_initializer", ConfigOf<ShorthandFieldInitializer>>;
  "field_initializer": FluentNode<"field_initializer", ConfigOf<FieldInitializer>>;
  "base_field_initializer": FluentNode<"base_field_initializer", ConfigOf<BaseFieldInitializer>>;
  "if_expression": FluentNode<"if_expression", ConfigOf<IfExpression>>;
  "let_condition": FluentNode<"let_condition", ConfigOf<LetCondition>>;
  "_let_chain": FluentNode<"_let_chain", ConfigOf<HiddenLetChain>>;
  "else_clause": FluentNode<"else_clause", ConfigOf<ElseClause>>;
  "match_expression": FluentNode<"match_expression", ConfigOf<MatchExpression>>;
  "match_block": FluentNode<"match_block", ConfigOf<MatchBlock>>;
  "match_arm": FluentNode<"match_arm", ConfigOf<MatchArm>>;
  "last_match_arm": FluentNode<"last_match_arm", ConfigOf<LastMatchArm>>;
  "match_pattern": FluentNode<"match_pattern", ConfigOf<MatchPattern>>;
  "while_expression": FluentNode<"while_expression", ConfigOf<WhileExpression>>;
  "loop_expression": FluentNode<"loop_expression", ConfigOf<LoopExpression>>;
  "for_expression": FluentNode<"for_expression", ConfigOf<ForExpression>>;
  "const_block": FluentNode<"const_block", ConfigOf<ConstBlock>>;
  "closure_expression": FluentNode<"closure_expression", ConfigOf<ClosureExpression>>;
  "closure_parameters": FluentNode<"closure_parameters", ConfigOf<ClosureParameters>>;
  "label": FluentNode<"label", ConfigOf<Label>>;
  "break_expression": FluentNode<"break_expression", ConfigOf<BreakExpression>>;
  "continue_expression": FluentNode<"continue_expression", ConfigOf<ContinueExpression>>;
  "index_expression": FluentNode<"index_expression", ConfigOf<IndexExpression>>;
  "await_expression": FluentNode<"await_expression", ConfigOf<AwaitExpression>>;
  "field_expression": FluentNode<"field_expression", ConfigOf<FieldExpression>>;
  "unsafe_block": FluentNode<"unsafe_block", ConfigOf<UnsafeBlock>>;
  "async_block": FluentNode<"async_block", ConfigOf<AsyncBlock>>;
  "gen_block": FluentNode<"gen_block", ConfigOf<GenBlock>>;
  "try_block": FluentNode<"try_block", ConfigOf<TryBlock>>;
  "block": FluentNode<"block", ConfigOf<Block>>;
  "generic_pattern": FluentNode<"generic_pattern", ConfigOf<GenericPattern>>;
  "tuple_pattern": FluentNode<"tuple_pattern", ConfigOf<TuplePattern>>;
  "slice_pattern": FluentNode<"slice_pattern", ConfigOf<SlicePattern>>;
  "tuple_struct_pattern": FluentNode<"tuple_struct_pattern", ConfigOf<TupleStructPattern>>;
  "struct_pattern": FluentNode<"struct_pattern", ConfigOf<StructPattern>>;
  "field_pattern": FluentNode<"field_pattern", ConfigOf<FieldPattern>>;
  "mut_pattern": FluentNode<"mut_pattern", ConfigOf<MutPattern>>;
  "range_pattern": FluentNode<"range_pattern", ConfigOf<RangePattern>>;
  "ref_pattern": FluentNode<"ref_pattern", ConfigOf<RefPattern>>;
  "captured_pattern": FluentNode<"captured_pattern", ConfigOf<CapturedPattern>>;
  "reference_pattern": FluentNode<"reference_pattern", ConfigOf<ReferencePattern>>;
  "or_pattern": FluentNode<"or_pattern", ConfigOf<OrPattern>>;
  "negative_literal": FluentNode<"negative_literal", ConfigOf<NegativeLiteral>>;
  "integer_literal": IntegerLiteral;
  "string_literal": FluentNode<"string_literal", ConfigOf<StringLiteral>>;
  "raw_string_literal": FluentNode<"raw_string_literal", ConfigOf<RawStringLiteral>>;
  "char_literal": CharLiteral;
  "escape_sequence": EscapeSequence;
  "boolean_literal": BooleanLiteral;
  "comment": FluentNode<"comment", ConfigOf<Comment>>;
  "line_comment": FluentNode<"line_comment", ConfigOf<LineComment>>;
  "block_comment": FluentNode<"block_comment", ConfigOf<BlockComment>>;
  "_path": FluentNode<"_path", ConfigOf<HiddenPath>>;
  "identifier": Identifier;
  "shebang": Shebang;
  "_reserved_identifier": ReservedIdentifier;
  "_type_identifier": FluentNode<"_type_identifier", ConfigOf<HiddenTypeIdentifier>>;
  "_field_identifier": FluentNode<"_field_identifier", ConfigOf<HiddenFieldIdentifier>>;
  "self": Self;
  "super": Super;
  "crate": Crate;
  "metavariable": Metavariable;
  "string_content": StringContent;
  "raw_string_literal_content": RawStringLiteralContent;
  "float_literal": FloatLiteral;
  "_block_comment_content": BlockCommentContent;
  "_error_sentinel": ErrorSentinel;
  "primitive_type": PrimitiveType;
  "let_chain": FluentNode<"let_chain", ConfigOf<LetChain>>;
  "shorthand_field_identifier": ShorthandFieldIdentifier;
  "outer_doc_comment_marker": OuterDocCommentMarker;
  "inner_doc_comment_marker": InnerDocCommentMarker;
  "type_identifier": TypeIdentifier;
  "field_identifier": FieldIdentifier;
};

export const _factoryMap = {
  "source_file": sourceFile,
  "expression_statement": expressionStatement,
  "macro_definition": macroDefinition,
  "macro_rule": macroRule,
  "token_tree_pattern": tokenTreePattern,
  "token_binding_pattern": tokenBindingPattern,
  "token_repetition_pattern": tokenRepetitionPattern,
  "fragment_specifier": fragmentSpecifier,
  "token_tree": tokenTree,
  "token_repetition": tokenRepetition,
  "_non_special_token": nonSpecialToken,
  "attribute_item": attributeItem,
  "inner_attribute_item": innerAttributeItem,
  "attribute": attribute,
  "mod_item": modItem,
  "foreign_mod_item": foreignModItem,
  "declaration_list": declarationList,
  "struct_item": structItem,
  "union_item": unionItem,
  "enum_item": enumItem,
  "enum_variant_list": enumVariantList,
  "enum_variant": enumVariant,
  "field_declaration_list": fieldDeclarationList,
  "field_declaration": fieldDeclaration,
  "ordered_field_declaration_list": orderedFieldDeclarationList,
  "extern_crate_declaration": externCrateDeclaration,
  "const_item": constItem,
  "static_item": staticItem,
  "type_item": typeItem,
  "function_item": functionItem,
  "function_signature_item": functionSignatureItem,
  "function_modifiers": functionModifiers,
  "where_clause": whereClause,
  "where_predicate": wherePredicate,
  "impl_item": implItem,
  "trait_item": traitItem,
  "associated_type": associatedType,
  "trait_bounds": traitBounds,
  "higher_ranked_trait_bound": higherRankedTraitBound,
  "removed_trait_bound": removedTraitBound,
  "type_parameters": typeParameters,
  "const_parameter": constParameter,
  "type_parameter": typeParameter,
  "lifetime_parameter": lifetimeParameter,
  "let_declaration": letDeclaration,
  "use_declaration": useDeclaration,
  "scoped_use_list": scopedUseList,
  "use_list": useList,
  "use_as_clause": useAsClause,
  "use_wildcard": useWildcard,
  "parameters": parameters,
  "self_parameter": selfParameter,
  "variadic_parameter": variadicParameter,
  "parameter": parameter,
  "extern_modifier": externModifier,
  "visibility_modifier": visibilityModifier,
  "bracketed_type": bracketedType,
  "qualified_type": qualifiedType,
  "lifetime": lifetime,
  "array_type": arrayType,
  "for_lifetimes": forLifetimes,
  "function_type": functionType,
  "tuple_type": tupleType,
  "unit_type": unitType,
  "generic_function": genericFunction,
  "generic_type": genericType,
  "generic_type_with_turbofish": genericTypeWithTurbofish,
  "bounded_type": boundedType,
  "use_bounds": useBounds,
  "type_arguments": typeArguments,
  "type_binding": typeBinding,
  "reference_type": referenceType,
  "pointer_type": pointerType,
  "abstract_type": abstractType,
  "dynamic_type": dynamicType,
  "mutable_specifier": mutableSpecifier,
  "_expression_except_range": expressionExceptRange,
  "macro_invocation": macroInvocation,
  "delim_token_tree": delimTokenTree,
  "_non_delim_token": nonDelimToken,
  "scoped_identifier": scopedIdentifier,
  "scoped_type_identifier_in_expression_position": scopedTypeIdentifierInExpressionPosition,
  "scoped_type_identifier": scopedTypeIdentifier,
  "range_expression": rangeExpression,
  "unary_expression": unaryExpression,
  "try_expression": tryExpression,
  "reference_expression": referenceExpression,
  "binary_expression": binaryExpression,
  "assignment_expression": assignmentExpression,
  "compound_assignment_expr": compoundAssignmentExpr,
  "type_cast_expression": typeCastExpression,
  "return_expression": returnExpression,
  "yield_expression": yieldExpression,
  "call_expression": callExpression,
  "arguments": arguments_,
  "array_expression": arrayExpression,
  "parenthesized_expression": parenthesizedExpression,
  "tuple_expression": tupleExpression,
  "unit_expression": unitExpression,
  "struct_expression": structExpression,
  "field_initializer_list": fieldInitializerList,
  "shorthand_field_initializer": shorthandFieldInitializer,
  "field_initializer": fieldInitializer,
  "base_field_initializer": baseFieldInitializer,
  "if_expression": ifExpression,
  "let_condition": letCondition,
  "_let_chain": hiddenLetChain,
  "else_clause": elseClause,
  "match_expression": matchExpression,
  "match_block": matchBlock,
  "match_arm": matchArm,
  "last_match_arm": lastMatchArm,
  "match_pattern": matchPattern,
  "while_expression": whileExpression,
  "loop_expression": loopExpression,
  "for_expression": forExpression,
  "const_block": constBlock,
  "closure_expression": closureExpression,
  "closure_parameters": closureParameters,
  "label": label,
  "break_expression": breakExpression,
  "continue_expression": continueExpression,
  "index_expression": indexExpression,
  "await_expression": awaitExpression,
  "field_expression": fieldExpression,
  "unsafe_block": unsafeBlock,
  "async_block": asyncBlock,
  "gen_block": genBlock,
  "try_block": tryBlock,
  "block": block,
  "generic_pattern": genericPattern,
  "tuple_pattern": tuplePattern,
  "slice_pattern": slicePattern,
  "tuple_struct_pattern": tupleStructPattern,
  "struct_pattern": structPattern,
  "field_pattern": fieldPattern,
  "mut_pattern": mutPattern,
  "range_pattern": rangePattern,
  "ref_pattern": refPattern,
  "captured_pattern": capturedPattern,
  "reference_pattern": referencePattern,
  "or_pattern": orPattern,
  "negative_literal": negativeLiteral,
  "integer_literal": integerLiteral,
  "string_literal": stringLiteral,
  "raw_string_literal": rawStringLiteral,
  "char_literal": charLiteral,
  "escape_sequence": escapeSequence,
  "boolean_literal": booleanLiteral,
  "comment": comment,
  "line_comment": lineComment,
  "block_comment": blockComment,
  "_path": hiddenPath,
  "identifier": identifier,
  "shebang": shebang,
  "_reserved_identifier": reservedIdentifier,
  "_type_identifier": hiddenTypeIdentifier,
  "_field_identifier": hiddenFieldIdentifier,
  "self": self,
  "super": super_,
  "crate": crate,
  "metavariable": metavariable,
  "string_content": stringContent,
  "raw_string_literal_content": rawStringLiteralContent,
  "float_literal": floatLiteral,
  "_block_comment_content": blockCommentContent,
  "_error_sentinel": errorSentinel,
  "primitive_type": primitiveType,
  "let_chain": letChain,
  "shorthand_field_identifier": shorthandFieldIdentifier,
  "outer_doc_comment_marker": outerDocCommentMarker,
  "inner_doc_comment_marker": innerDocCommentMarker,
  "type_identifier": typeIdentifier,
  "field_identifier": fieldIdentifier,
} as const;
export type _FactoryMap = typeof _factoryMap;

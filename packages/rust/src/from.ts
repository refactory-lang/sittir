// Auto-generated by @sittir/codegen — do not edit
// .from() API — shared resolvers, immutable ergonomic setters

import type { AbstractType, AbstractTypeConfig, Arguments, ArgumentsConfig, ArrayExpression, ArrayExpressionConfig, ArrayType, ArrayTypeConfig, AssignmentExpression, AssignmentExpressionConfig, AssociatedType, AssociatedTypeConfig, AsyncBlock, AsyncBlockConfig, Attribute, AttributeConfig, AttributeItem, AttributeItemConfig, AwaitExpression, AwaitExpressionConfig, BaseFieldInitializer, BaseFieldInitializerConfig, BinaryExpression, BinaryExpressionConfig, Block, BlockComment, BlockCommentConfig, BlockConfig, BoundedType, BoundedTypeConfig, BracketedType, BracketedTypeConfig, BreakExpression, BreakExpressionConfig, CallExpression, CallExpressionConfig, CapturedPattern, CapturedPatternConfig, ClosureExpression, ClosureExpressionConfig, ClosureParameters, ClosureParametersConfig, CompoundAssignmentExpr, CompoundAssignmentExprConfig, Condition, ConstBlock, ConstBlockConfig, ConstItem, ConstItemConfig, ConstParameter, ConstParameterConfig, ContinueExpression, ContinueExpressionConfig, DeclarationList, DeclarationListConfig, DeclarationStatement, DynamicType, DynamicTypeConfig, ElseClause, ElseClauseConfig, EnumItem, EnumItemConfig, EnumVariant, EnumVariantConfig, EnumVariantList, EnumVariantListConfig, Expression, ExpressionEndingWithBlock, ExpressionExceptRange, ExpressionStatement, ExpressionStatementConfig, ExternCrateDeclaration, ExternCrateDeclarationConfig, ExternModifier, ExternModifierConfig, FieldDeclaration, FieldDeclarationConfig, FieldDeclarationList, FieldDeclarationListConfig, FieldExpression, FieldExpressionConfig, FieldIdentifier, FieldInitializer, FieldInitializerConfig, FieldInitializerList, FieldInitializerListConfig, FieldPattern, FieldPatternConfig, ForExpression, ForExpressionConfig, ForLifetimes, ForLifetimesConfig, ForeignModItem, ForeignModItemConfig, FunctionItem, FunctionItemConfig, FunctionModifiers, FunctionModifiersConfig, FunctionSignatureItem, FunctionSignatureItemConfig, FunctionType, FunctionTypeConfig, GenBlock, GenBlockConfig, GenericFunction, GenericFunctionConfig, GenericPattern, GenericPatternConfig, GenericType, GenericTypeConfig, GenericTypeWithTurbofish, GenericTypeWithTurbofishConfig, HigherRankedTraitBound, HigherRankedTraitBoundConfig, IfExpression, IfExpressionConfig, ImplItem, ImplItemConfig, IndexExpression, IndexExpressionConfig, InnerAttributeItem, InnerAttributeItemConfig, Label, LabelConfig, LetChain, LetChainConfig, LetCondition, LetConditionConfig, LetDeclaration, LetDeclarationConfig, Lifetime, LifetimeConfig, LifetimeParameter, LifetimeParameterConfig, LineComment, LineCommentConfig, Literal, LiteralPattern, LoopExpression, LoopExpressionConfig, MacroDefinition, MacroDefinitionConfig, MacroInvocation, MacroInvocationConfig, MacroRule, MacroRuleConfig, MatchArm, MatchArmConfig, MatchBlock, MatchBlockConfig, MatchExpression, MatchExpressionConfig, MatchPattern, MatchPatternConfig, ModItem, ModItemConfig, MutPattern, MutPatternConfig, NegativeLiteral, NegativeLiteralConfig, OrPattern, OrPatternConfig, OrderedFieldDeclarationList, OrderedFieldDeclarationListConfig, Parameter, ParameterConfig, Parameters, ParametersConfig, ParenthesizedExpression, ParenthesizedExpressionConfig, Path, Pattern, PointerType, PointerTypeConfig, QualifiedType, QualifiedTypeConfig, RangeExpression, RangeExpressionConfig, RangePattern, RangePatternConfig, RawStringLiteral, RawStringLiteralConfig, RefPattern, RefPatternConfig, ReferenceExpression, ReferenceExpressionConfig, ReferencePattern, ReferencePatternConfig, ReferenceType, ReferenceTypeConfig, RemovedTraitBound, RemovedTraitBoundConfig, ReservedIdentifier, ReturnExpression, ReturnExpressionConfig, ScopedIdentifier, ScopedIdentifierConfig, ScopedTypeIdentifier, ScopedTypeIdentifierConfig, ScopedUseList, ScopedUseListConfig, SelfParameter, SelfParameterConfig, ShorthandFieldInitializer, ShorthandFieldInitializerConfig, SlicePattern, SlicePatternConfig, SourceFile, SourceFileConfig, Statement, StaticItem, StaticItemConfig, StringLiteral, StringLiteralConfig, StructExpression, StructExpressionConfig, StructItem, StructItemConfig, StructPattern, StructPatternConfig, TokenBindingPattern, TokenBindingPatternConfig, TokenRepetition, TokenRepetitionConfig, TokenRepetitionPattern, TokenRepetitionPatternConfig, TokenTree, TokenTreeConfig, TokenTreePattern, TokenTreePatternConfig, TraitBounds, TraitBoundsConfig, TraitItem, TraitItemConfig, TryBlock, TryBlockConfig, TryExpression, TryExpressionConfig, TupleExpression, TupleExpressionConfig, TuplePattern, TuplePatternConfig, TupleStructPattern, TupleStructPatternConfig, TupleType, TupleTypeConfig, Type, TypeArguments, TypeArgumentsConfig, TypeBinding, TypeBindingConfig, TypeCastExpression, TypeCastExpressionConfig, TypeIdentifier, TypeItem, TypeItemConfig, TypeParameter, TypeParameterConfig, TypeParameters, TypeParametersConfig, UnaryExpression, UnaryExpressionConfig, UnionItem, UnionItemConfig, UnsafeBlock, UnsafeBlockConfig, UseAsClause, UseAsClauseConfig, UseBounds, UseBoundsConfig, UseClause, UseDeclaration, UseDeclarationConfig, UseList, UseListConfig, UseWildcard, UseWildcardConfig, VariadicParameter, VariadicParameterConfig, VisibilityModifier, VisibilityModifierConfig, WhereClause, WhereClauseConfig, WherePredicate, WherePredicateConfig, WhileExpression, WhileExpressionConfig, YieldExpression, YieldExpressionConfig } from './types.js';
import type { KindMap, FromInputMap } from './types.js';
import type { FluentNodeOf } from '@sittir/types';
import { isNodeData, hasKind, resolveField } from './utils.js';

import { abstract_type_, arguments_, array_expression_, array_type_, assignment_expression_, associated_type_, async_block_, attribute_, attribute_item_, await_expression_, base_field_initializer_, binary_expression_, block_, block_comment_, boolean_literal_, bounded_type_, bracketed_type_, break_expression_, call_expression_, captured_pattern_, char_literal_, closure_expression_, closure_parameters_, compound_assignment_expr_, const_block_, const_item_, const_parameter_, continue_expression_, crate_, declaration_list_, doc_comment_, dynamic_type_, else_clause_, empty_statement_, enum_item_, enum_variant_, enum_variant_list_, escape_sequence_, expression_statement_, extern_crate_declaration_, extern_modifier_, field_declaration_, field_declaration_list_, field_expression_, field_identifier_, field_initializer_, field_initializer_list_, field_pattern_, float_literal_, for_expression_, for_lifetimes_, foreign_mod_item_, fragment_specifier_, function_item_, function_modifiers_, function_signature_item_, function_type_, gen_block_, generic_function_, generic_pattern_, generic_type_, generic_type_with_turbofish_, higher_ranked_trait_bound_, identifier_, if_expression_, impl_item_, index_expression_, inner_attribute_item_, inner_doc_comment_marker_, integer_literal_, label_, let_chain_, let_condition_, let_declaration_, lifetime_, lifetime_parameter_, line_comment_, loop_expression_, macro_definition_, macro_invocation_, macro_rule_, match_arm_, match_block_, match_expression_, match_pattern_, metavariable_, mod_item_, mut_pattern_, mutable_specifier_, negative_literal_, never_type_, or_pattern_, ordered_field_declaration_list_, outer_doc_comment_marker_, parameter_, parameters_, parenthesized_expression_, pointer_type_, primitive_type_, qualified_type_, range_expression_, range_pattern_, raw_string_literal_, ref_pattern_, reference_expression_, reference_pattern_, reference_type_, remaining_field_pattern_, removed_trait_bound_, return_expression_, scoped_identifier_, scoped_type_identifier_, scoped_use_list_, self_, self_parameter_, shebang_, shorthand_field_identifier_, shorthand_field_initializer_, slice_pattern_, source_file_, static_item_, string_content_, string_literal_, struct_expression_, struct_item_, struct_pattern_, super_, token_binding_pattern_, token_repetition_, token_repetition_pattern_, token_tree_, token_tree_pattern_, trait_bounds_, trait_item_, try_block_, try_expression_, tuple_expression_, tuple_pattern_, tuple_struct_pattern_, tuple_type_, type_arguments_, type_binding_, type_cast_expression_, type_identifier_, type_item_, type_parameter_, type_parameters_, unary_expression_, union_item_, unit_expression_, unit_type_, unsafe_block_, use_as_clause_, use_bounds_, use_declaration_, use_list_, use_wildcard_, variadic_parameter_, visibility_modifier_, where_clause_, where_predicate_, while_expression_, yield_expression_ } from './factories.js';
import type { AbstractTypeFromInput, ArgumentsFromInput, ArrayExpressionFromInput, ArrayTypeFromInput, AssignmentExpressionFromInput, AssociatedTypeFromInput, AsyncBlockFromInput, AttributeFromInput, AttributeItemFromInput, AwaitExpressionFromInput, BaseFieldInitializerFromInput, BinaryExpressionFromInput, BlockFromInput, BlockCommentFromInput, BoundedTypeFromInput, BracketedTypeFromInput, BreakExpressionFromInput, CallExpressionFromInput, CapturedPatternFromInput, ClosureExpressionFromInput, ClosureParametersFromInput, CompoundAssignmentExprFromInput, ConstBlockFromInput, ConstItemFromInput, ConstParameterFromInput, ContinueExpressionFromInput, DeclarationListFromInput, DynamicTypeFromInput, ElseClauseFromInput, EnumItemFromInput, EnumVariantFromInput, EnumVariantListFromInput, ExpressionStatementFromInput, ExternCrateDeclarationFromInput, ExternModifierFromInput, FieldDeclarationFromInput, FieldDeclarationListFromInput, FieldExpressionFromInput, FieldInitializerFromInput, FieldInitializerListFromInput, FieldPatternFromInput, ForExpressionFromInput, ForLifetimesFromInput, ForeignModItemFromInput, FunctionItemFromInput, FunctionModifiersFromInput, FunctionSignatureItemFromInput, FunctionTypeFromInput, GenBlockFromInput, GenericFunctionFromInput, GenericPatternFromInput, GenericTypeFromInput, GenericTypeWithTurbofishFromInput, HigherRankedTraitBoundFromInput, IfExpressionFromInput, ImplItemFromInput, IndexExpressionFromInput, InnerAttributeItemFromInput, LabelFromInput, LetChainFromInput, LetConditionFromInput, LetDeclarationFromInput, LifetimeFromInput, LifetimeParameterFromInput, LineCommentFromInput, LoopExpressionFromInput, MacroDefinitionFromInput, MacroInvocationFromInput, MacroRuleFromInput, MatchArmFromInput, MatchBlockFromInput, MatchExpressionFromInput, MatchPatternFromInput, ModItemFromInput, MutPatternFromInput, NegativeLiteralFromInput, OrPatternFromInput, OrderedFieldDeclarationListFromInput, ParameterFromInput, ParametersFromInput, ParenthesizedExpressionFromInput, PointerTypeFromInput, QualifiedTypeFromInput, RangeExpressionFromInput, RangePatternFromInput, RawStringLiteralFromInput, RefPatternFromInput, ReferenceExpressionFromInput, ReferencePatternFromInput, ReferenceTypeFromInput, RemovedTraitBoundFromInput, ReturnExpressionFromInput, ScopedIdentifierFromInput, ScopedTypeIdentifierFromInput, ScopedUseListFromInput, SelfParameterFromInput, ShorthandFieldInitializerFromInput, SlicePatternFromInput, SourceFileFromInput, StaticItemFromInput, StringLiteralFromInput, StructExpressionFromInput, StructItemFromInput, StructPatternFromInput, TokenBindingPatternFromInput, TokenRepetitionFromInput, TokenRepetitionPatternFromInput, TokenTreeFromInput, TokenTreePatternFromInput, TraitBoundsFromInput, TraitItemFromInput, TryBlockFromInput, TryExpressionFromInput, TupleExpressionFromInput, TuplePatternFromInput, TupleStructPatternFromInput, TupleTypeFromInput, TypeArgumentsFromInput, TypeBindingFromInput, TypeCastExpressionFromInput, TypeItemFromInput, TypeParameterFromInput, TypeParametersFromInput, UnaryExpressionFromInput, UnionItemFromInput, UnsafeBlockFromInput, UseAsClauseFromInput, UseBoundsFromInput, UseDeclarationFromInput, UseListFromInput, UseWildcardFromInput, VariadicParameterFromInput, VisibilityModifierFromInput, WhereClauseFromInput, WherePredicateFromInput, WhileExpressionFromInput, YieldExpressionFromInput } from './types.js';

/** Maps kind string to its typed .from() resolver. */
export type FromMap = {
  [K in keyof FromInputMap]: (input: KindMap[K] | FromInputMap[K]) => FluentNodeOf<KindMap[K]>;
};

/** @internal Map of kind string to .from() resolver function. */
export const _fromMap = {
  'abstract_type': abstractTypeFrom,
  'arguments': arguments_From,
  'array_expression': arrayExpressionFrom,
  'array_type': arrayTypeFrom,
  'assignment_expression': assignmentExpressionFrom,
  'associated_type': associatedTypeFrom,
  'async_block': asyncBlockFrom,
  'attribute': attributeFrom,
  'attribute_item': attributeItemFrom,
  'await_expression': awaitExpressionFrom,
  'base_field_initializer': baseFieldInitializerFrom,
  'binary_expression': binaryExpressionFrom,
  'block': blockFrom,
  'block_comment': blockCommentFrom,
  'bounded_type': boundedTypeFrom,
  'bracketed_type': bracketedTypeFrom,
  'break_expression': breakExpressionFrom,
  'call_expression': callExpressionFrom,
  'captured_pattern': capturedPatternFrom,
  'closure_expression': closureExpressionFrom,
  'closure_parameters': closureParametersFrom,
  'compound_assignment_expr': compoundAssignmentExprFrom,
  'const_block': constBlockFrom,
  'const_item': constItemFrom,
  'const_parameter': constParameterFrom,
  'continue_expression': continueExpressionFrom,
  'declaration_list': declarationListFrom,
  'dynamic_type': dynamicTypeFrom,
  'else_clause': elseClauseFrom,
  'enum_item': enumItemFrom,
  'enum_variant': enumVariantFrom,
  'enum_variant_list': enumVariantListFrom,
  'expression_statement': expressionStatementFrom,
  'extern_crate_declaration': externCrateDeclarationFrom,
  'extern_modifier': externModifierFrom,
  'field_declaration': fieldDeclarationFrom,
  'field_declaration_list': fieldDeclarationListFrom,
  'field_expression': fieldExpressionFrom,
  'field_initializer': fieldInitializerFrom,
  'field_initializer_list': fieldInitializerListFrom,
  'field_pattern': fieldPatternFrom,
  'for_expression': forExpressionFrom,
  'for_lifetimes': forLifetimesFrom,
  'foreign_mod_item': foreignModItemFrom,
  'function_item': functionItemFrom,
  'function_modifiers': functionModifiersFrom,
  'function_signature_item': functionSignatureItemFrom,
  'function_type': functionTypeFrom,
  'gen_block': genBlockFrom,
  'generic_function': genericFunctionFrom,
  'generic_pattern': genericPatternFrom,
  'generic_type': genericTypeFrom,
  'generic_type_with_turbofish': genericTypeWithTurbofishFrom,
  'higher_ranked_trait_bound': higherRankedTraitBoundFrom,
  'if_expression': ifExpressionFrom,
  'impl_item': implItemFrom,
  'index_expression': indexExpressionFrom,
  'inner_attribute_item': innerAttributeItemFrom,
  'label': labelFrom,
  'let_chain': letChainFrom,
  'let_condition': letConditionFrom,
  'let_declaration': letDeclarationFrom,
  'lifetime': lifetimeFrom,
  'lifetime_parameter': lifetimeParameterFrom,
  'line_comment': lineCommentFrom,
  'loop_expression': loopExpressionFrom,
  'macro_definition': macroDefinitionFrom,
  'macro_invocation': macroInvocationFrom,
  'macro_rule': macroRuleFrom,
  'match_arm': matchArmFrom,
  'match_block': matchBlockFrom,
  'match_expression': matchExpressionFrom,
  'match_pattern': matchPatternFrom,
  'mod_item': modItemFrom,
  'mut_pattern': mutPatternFrom,
  'negative_literal': negativeLiteralFrom,
  'or_pattern': orPatternFrom,
  'ordered_field_declaration_list': orderedFieldDeclarationListFrom,
  'parameter': parameterFrom,
  'parameters': parametersFrom,
  'parenthesized_expression': parenthesizedExpressionFrom,
  'pointer_type': pointerTypeFrom,
  'qualified_type': qualifiedTypeFrom,
  'range_expression': rangeExpressionFrom,
  'range_pattern': rangePatternFrom,
  'raw_string_literal': rawStringLiteralFrom,
  'ref_pattern': refPatternFrom,
  'reference_expression': referenceExpressionFrom,
  'reference_pattern': referencePatternFrom,
  'reference_type': referenceTypeFrom,
  'removed_trait_bound': removedTraitBoundFrom,
  'return_expression': returnExpressionFrom,
  'scoped_identifier': scopedIdentifierFrom,
  'scoped_type_identifier': scopedTypeIdentifierFrom,
  'scoped_use_list': scopedUseListFrom,
  'self_parameter': selfParameterFrom,
  'shorthand_field_initializer': shorthandFieldInitializerFrom,
  'slice_pattern': slicePatternFrom,
  'source_file': sourceFileFrom,
  'static_item': staticItemFrom,
  'string_literal': stringLiteralFrom,
  'struct_expression': structExpressionFrom,
  'struct_item': structItemFrom,
  'struct_pattern': structPatternFrom,
  'token_binding_pattern': tokenBindingPatternFrom,
  'token_repetition': tokenRepetitionFrom,
  'token_repetition_pattern': tokenRepetitionPatternFrom,
  'token_tree': tokenTreeFrom,
  'token_tree_pattern': tokenTreePatternFrom,
  'trait_bounds': traitBoundsFrom,
  'trait_item': traitItemFrom,
  'try_block': tryBlockFrom,
  'try_expression': tryExpressionFrom,
  'tuple_expression': tupleExpressionFrom,
  'tuple_pattern': tuplePatternFrom,
  'tuple_struct_pattern': tupleStructPatternFrom,
  'tuple_type': tupleTypeFrom,
  'type_arguments': typeArgumentsFrom,
  'type_binding': typeBindingFrom,
  'type_cast_expression': typeCastExpressionFrom,
  'type_item': typeItemFrom,
  'type_parameter': typeParameterFrom,
  'type_parameters': typeParametersFrom,
  'unary_expression': unaryExpressionFrom,
  'union_item': unionItemFrom,
  'unsafe_block': unsafeBlockFrom,
  'use_as_clause': useAsClauseFrom,
  'use_bounds': useBoundsFrom,
  'use_declaration': useDeclarationFrom,
  'use_list': useListFrom,
  'use_wildcard': useWildcardFrom,
  'variadic_parameter': variadicParameterFrom,
  'visibility_modifier': visibilityModifierFrom,
  'where_clause': whereClauseFrom,
  'where_predicate': wherePredicateFrom,
  'while_expression': whileExpressionFrom,
  'yield_expression': yieldExpressionFrom,
};

export function _resolveByKind(kind: string, rest: unknown) {
  const fn = (_fromMap as Record<string, (input: any) => unknown>)[kind];
  if (fn) return fn(rest);
  throw new Error(`Unknown kind for .from(): '${kind}'`);
}

function _resolveDeclarationStatement(v: unknown): DeclarationStatement;
function _resolveDeclarationStatement(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return (v === ';' ? empty_statement_() : (() => { throw new Error(`Expected ';' for empty_statement, got '${v}'`); })());
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: associated_type, attribute_item, const_item, enum_item, extern_crate_declaration, foreign_mod_item, function_item, function_signature_item, impl_item, inner_attribute_item, let_declaration, macro_definition, macro_invocation, mod_item, static_item, struct_item, trait_item, type_item, union_item, use_declaration. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveLiteral(v: unknown): Literal;
function _resolveLiteral(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['false', 'true'].includes(v)) return boolean_literal_(v);
    if (/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(v)) return char_literal_(v);
    if (/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(v)) return integer_literal_(v);
    return float_literal_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: raw_string_literal, string_literal. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpression(v: unknown): Expression;
function _resolveExpression(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['false', 'true'].includes(v)) return boolean_literal_(v);
    if (/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(v)) return char_literal_(v);
    if (/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(v)) return integer_literal_(v);
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^\(\)$/.test(v)) return (v === '()' ? unit_expression_() : (() => { throw new Error(`Expected '()' for unit_expression, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_expression, assignment_expression, async_block, await_expression, binary_expression, block, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, loop_expression, macro_invocation, match_expression, parenthesized_expression, range_expression, raw_string_literal, reference_expression, return_expression, scoped_identifier, string_literal, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveLiteralPattern(v: unknown): LiteralPattern;
function _resolveLiteralPattern(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['false', 'true'].includes(v)) return boolean_literal_(v);
    if (/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(v)) return char_literal_(v);
    if (/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(v)) return integer_literal_(v);
    return float_literal_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: negative_literal, raw_string_literal, string_literal. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePattern(v: unknown): Pattern;
function _resolvePattern(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['false', 'true'].includes(v)) return boolean_literal_(v);
    if (/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(v)) return char_literal_(v);
    if (/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(v)) return integer_literal_(v);
    if (/^\.\.$/.test(v)) return (v === '..' ? remaining_field_pattern_() : (() => { throw new Error(`Expected '..' for remaining_field_pattern, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: captured_pattern, const_block, generic_pattern, macro_invocation, mut_pattern, negative_literal, or_pattern, range_pattern, raw_string_literal, ref_pattern, reference_pattern, scoped_identifier, slice_pattern, string_literal, struct_pattern, tuple_pattern, tuple_struct_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveType(v: unknown): Type;
function _resolveType(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['bool', 'char', 'f32', 'f64', 'i128', 'i16', 'i32', 'i64', 'i8', 'isize', 'str', 'u128', 'u16', 'u32', 'u64', 'u8', 'usize'].includes(v)) return primitive_type_(v);
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^!$/.test(v)) return (v === '!' ? never_type_() : (() => { throw new Error(`Expected '!' for never_type, got '${v}'`); })());
    if (/^\(\)$/.test(v)) return (v === '()' ? unit_type_() : (() => { throw new Error(`Expected '()' for unit_type, got '${v}'`); })());
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: abstract_type, array_type, bounded_type, dynamic_type, function_type, generic_type, macro_invocation, pointer_type, reference_type, removed_trait_bound, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePath(v: unknown): Path;
function _resolvePath(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^crate$/.test(v)) return (v === 'crate' ? crate_() : (() => { throw new Error(`Expected 'crate' for crate, got '${v}'`); })());
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) return _resolveByKind('scoped_identifier', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('scoped_identifier', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveStatement(v: unknown): Statement;
function _resolveStatement(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return (v === ';' ? empty_statement_() : (() => { throw new Error(`Expected ';' for empty_statement, got '${v}'`); })());
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: associated_type, attribute_item, const_item, enum_item, expression_statement, extern_crate_declaration, foreign_mod_item, function_item, function_signature_item, impl_item, inner_attribute_item, let_declaration, macro_definition, macro_invocation, mod_item, static_item, struct_item, trait_item, type_item, union_item, use_declaration. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressionEndingWithBlock(v: unknown): ExpressionEndingWithBlock;
function _resolveExpressionEndingWithBlock(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: async_block, block, const_block, for_expression, gen_block, if_expression, loop_expression, match_expression, try_block, unsafe_block, while_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier(v: unknown): TypeIdentifier;
function _resolveTypeIdentifier(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressionExceptRange(v: unknown): ExpressionExceptRange;
function _resolveExpressionExceptRange(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['false', 'true'].includes(v)) return boolean_literal_(v);
    if (/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(v)) return char_literal_(v);
    if (/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(v)) return integer_literal_(v);
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^\(\)$/.test(v)) return (v === '()' ? unit_expression_() : (() => { throw new Error(`Expected '()' for unit_expression, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_expression, assignment_expression, async_block, await_expression, binary_expression, block, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, loop_expression, macro_invocation, match_expression, parenthesized_expression, raw_string_literal, reference_expression, return_expression, scoped_identifier, string_literal, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveFieldIdentifier(v: unknown): FieldIdentifier;
function _resolveFieldIdentifier(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return field_identifier_(v);
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveReservedIdentifier(v: unknown): ReservedIdentifier;
function _resolveReservedIdentifier(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return identifier_(v);
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveCondition(v: unknown): Condition;
function _resolveCondition(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['false', 'true'].includes(v)) return boolean_literal_(v);
    if (/^b?'(?:\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})|[^\\'])?'$/.test(v)) return char_literal_(v);
    if (/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(v)) return integer_literal_(v);
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^\(\)$/.test(v)) return (v === '()' ? unit_expression_() : (() => { throw new Error(`Expected '()' for unit_expression, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_expression, assignment_expression, async_block, await_expression, binary_expression, block, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, let_chain, let_condition, loop_expression, macro_invocation, match_expression, parenthesized_expression, range_expression, raw_string_literal, reference_expression, return_expression, scoped_identifier, string_literal, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveUseClause(v: unknown): UseClause;
function _resolveUseClause(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^crate$/.test(v)) return (v === 'crate' ? crate_() : (() => { throw new Error(`Expected 'crate' for crate, got '${v}'`); })());
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: scoped_identifier, scoped_use_list, use_as_clause, use_list, use_wildcard. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveType2(v: unknown): KindMap['type_identifier'] | KindMap['bounded_type'] | KindMap['function_type'] | KindMap['generic_type'] | KindMap['removed_trait_bound'] | KindMap['scoped_type_identifier'] | KindMap['tuple_type'];
function _resolveType2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: bounded_type, function_type, generic_type, removed_trait_bound, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveDeclarationStatement2(v: unknown): KindMap['attribute_item'];
function _resolveDeclarationStatement2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute_item, raw_string_literal, string_literal, array_expression, assignment_expression, async_block, await_expression, binary_expression, block, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, loop_expression, macro_invocation, match_expression, parenthesized_expression, range_expression, reference_expression, return_expression, scoped_identifier, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier2(v: unknown): KindMap['type_identifier'];
function _resolveTypeIdentifier2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) return _resolveTypeIdentifier(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveTypeIdentifier(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePath2(v: unknown): KindMap['crate'] | KindMap['identifier'] | KindMap['metavariable'] | KindMap['self'] | KindMap['super'] | KindMap['scoped_identifier'];
function _resolvePath2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^crate$/.test(v)) return (v === 'crate' ? crate_() : (() => { throw new Error(`Expected 'crate' for crate, got '${v}'`); })());
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: scoped_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier3(v: unknown): unknown;
function _resolveTypeIdentifier3(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['!=', '%', '&', '&&', '*', '+', '-', '/', '<', '<<', '<=', '==', '>', '>=', '>>', '^', '|', '||'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveStatement2(v: unknown): KindMap['expression_statement'];
function _resolveStatement2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: expression_statement, associated_type, attribute_item, const_item, enum_item, extern_crate_declaration, foreign_mod_item, function_item, function_signature_item, impl_item, inner_attribute_item, let_declaration, macro_definition, macro_invocation, mod_item, static_item, struct_item, trait_item, type_item, union_item, use_declaration, raw_string_literal, string_literal, array_expression, assignment_expression, async_block, await_expression, binary_expression, block, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, loop_expression, match_expression, parenthesized_expression, range_expression, reference_expression, return_expression, scoped_identifier, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rzt5kvo(v: unknown): KindMap['lifetime'] | KindMap['use_bounds'];
function _rzt5kvo(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: lifetime, use_bounds, abstract_type, array_type, bounded_type, dynamic_type, function_type, generic_type, macro_invocation, pointer_type, reference_type, removed_trait_bound, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _raoo9kn(v: unknown): KindMap['qualified_type'];
function _raoo9kn(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: qualified_type, abstract_type, array_type, bounded_type, dynamic_type, function_type, generic_type, macro_invocation, pointer_type, reference_type, removed_trait_bound, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressionExceptRange2(v: unknown): KindMap['identifier'] | KindMap['metavariable'] | KindMap['self'] | KindMap['unit_expression'] | KindMap['array_expression'] | KindMap['assignment_expression'] | KindMap['async_block'] | KindMap['await_expression'] | KindMap['binary_expression'] | KindMap['block'] | KindMap['break_expression'] | KindMap['call_expression'] | KindMap['closure_expression'] | KindMap['compound_assignment_expr'] | KindMap['const_block'] | KindMap['continue_expression'] | KindMap['field_expression'] | KindMap['for_expression'] | KindMap['gen_block'] | KindMap['generic_function'] | KindMap['if_expression'] | KindMap['index_expression'] | KindMap['loop_expression'] | KindMap['macro_invocation'] | KindMap['match_expression'] | KindMap['parenthesized_expression'] | KindMap['reference_expression'] | KindMap['return_expression'] | KindMap['scoped_identifier'] | KindMap['struct_expression'] | KindMap['try_block'] | KindMap['try_expression'] | KindMap['tuple_expression'] | KindMap['type_cast_expression'] | KindMap['unary_expression'] | KindMap['unsafe_block'] | KindMap['while_expression'] | KindMap['yield_expression'];
function _resolveExpressionExceptRange2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^\(\)$/.test(v)) return (v === '()' ? unit_expression_() : (() => { throw new Error(`Expected '()' for unit_expression, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_expression, assignment_expression, async_block, await_expression, binary_expression, block, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, loop_expression, macro_invocation, match_expression, parenthesized_expression, reference_expression, return_expression, scoped_identifier, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression, string_literal, raw_string_literal. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressionEndingWithBlock2(v: unknown): KindMap['block'];
function _resolveExpressionEndingWithBlock2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['_'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: block, raw_string_literal, string_literal, array_expression, assignment_expression, async_block, await_expression, binary_expression, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, loop_expression, macro_invocation, match_expression, parenthesized_expression, range_expression, reference_expression, return_expression, scoped_identifier, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier4(v: unknown): unknown;
function _resolveTypeIdentifier4(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['async'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier5(v: unknown): unknown;
function _resolveTypeIdentifier5(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['move'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier6(v: unknown): unknown;
function _resolveTypeIdentifier6(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['static'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1q9fx9f(v: unknown): KindMap['parameter'];
function _r1q9fx9f(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: parameter, negative_literal, raw_string_literal, string_literal, captured_pattern, const_block, generic_pattern, macro_invocation, mut_pattern, or_pattern, range_pattern, ref_pattern, reference_pattern, scoped_identifier, slice_pattern, struct_pattern, tuple_pattern, tuple_struct_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier7(v: unknown): unknown;
function _resolveTypeIdentifier7(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['%=', '&=', '*=', '+=', '-=', '/=', '<<=', '>>=', '^=', '|='].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1irud17(v: unknown): KindMap['identifier'] | KindMap['block'] | KindMap['negative_literal'];
function _r1irud17(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: block, negative_literal, raw_string_literal, string_literal. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rk93jcc(v: unknown): KindMap['type_identifier'] | KindMap['function_type'] | KindMap['generic_type'] | KindMap['higher_ranked_trait_bound'] | KindMap['scoped_type_identifier'] | KindMap['tuple_type'];
function _rk93jcc(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: function_type, generic_type, higher_ranked_trait_bound, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressionEndingWithBlock3(v: unknown): KindMap['block'] | KindMap['if_expression'];
function _resolveExpressionEndingWithBlock3(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: block, if_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r16kj8qi(v: unknown): KindMap['field_declaration_list'] | KindMap['ordered_field_declaration_list'];
function _r16kj8qi(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: field_declaration_list, ordered_field_declaration_list. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rmxz15l(v: unknown): KindMap['attribute_item'] | KindMap['enum_variant'];
function _rmxz15l(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute_item, enum_variant. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier8(v: unknown): unknown;
function _resolveTypeIdentifier8(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: raw_string_literal, string_literal, array_expression, assignment_expression, async_block, await_expression, binary_expression, block, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, loop_expression, macro_invocation, match_expression, parenthesized_expression, range_expression, reference_expression, return_expression, scoped_identifier, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveFieldIdentifier2(v: unknown): KindMap['field_identifier'];
function _resolveFieldIdentifier2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return field_identifier_(v);
  }
  if (Array.isArray(v)) return _resolveFieldIdentifier(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveFieldIdentifier(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1d1qsih(v: unknown): KindMap['attribute_item'] | KindMap['field_declaration'];
function _r1d1qsih(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute_item, field_declaration. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rkdlimm(v: unknown): KindMap['field_identifier'] | KindMap['integer_literal'];
function _rkdlimm(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'number') return integer_literal_(`${v}`);
  if (typeof v === 'string') {
    if (/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(v)) return integer_literal_(v);
    return field_identifier_(v);
  }
  if (Array.isArray(v)) return _resolveFieldIdentifier(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveFieldIdentifier(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1n0ectv(v: unknown): KindMap['base_field_initializer'] | KindMap['field_initializer'] | KindMap['shorthand_field_initializer'];
function _r1n0ectv(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: base_field_initializer, field_initializer, shorthand_field_initializer. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1gfzgqs(v: unknown): KindMap['field_identifier'] | KindMap['shorthand_field_identifier'];
function _r1gfzgqs(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return field_identifier_(v);
  }
  if (Array.isArray(v)) return _resolveFieldIdentifier(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveFieldIdentifier(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePath3(v: unknown): KindMap['identifier'] | KindMap['metavariable'];
function _resolvePath3(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    return identifier_(v);
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier9(v: unknown): unknown;
function _resolveTypeIdentifier9(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['default'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier10(v: unknown): unknown;
function _resolveTypeIdentifier10(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['const'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier11(v: unknown): unknown;
function _resolveTypeIdentifier11(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['unsafe'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveType3(v: unknown): KindMap['type_identifier'] | KindMap['scoped_type_identifier'];
function _resolveType3(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: scoped_type_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressionExceptRange3(v: unknown): KindMap['identifier'] | KindMap['field_expression'] | KindMap['scoped_identifier'];
function _resolveExpressionExceptRange3(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: field_expression, scoped_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePath4(v: unknown): KindMap['identifier'] | KindMap['scoped_identifier'];
function _resolvePath4(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return identifier_(v);
  }
  if (Array.isArray(v)) return _resolveByKind('scoped_identifier', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('scoped_identifier', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rhvptj6(v: unknown): KindMap['identifier'] | KindMap['type_identifier'] | KindMap['scoped_identifier'] | KindMap['scoped_type_identifier'];
function _rhvptj6(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: scoped_identifier, scoped_type_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rbvr4o6(v: unknown): KindMap['type_identifier'] | KindMap['scoped_identifier'];
function _rbvr4o6(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: scoped_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveCondition2(v: unknown): KindMap['let_chain'] | KindMap['let_condition'];
function _resolveCondition2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: let_chain, let_condition, unary_expression, reference_expression, try_expression, binary_expression, assignment_expression, compound_assignment_expr, type_cast_expression, call_expression, return_expression, yield_expression, string_literal, raw_string_literal, scoped_identifier, generic_function, await_expression, field_expression, array_expression, tuple_expression, macro_invocation, break_expression, continue_expression, index_expression, closure_expression, parenthesized_expression, struct_expression, unsafe_block, async_block, gen_block, try_block, block, if_expression, match_expression, while_expression, loop_expression, for_expression, const_block, range_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveType4(v: unknown): KindMap['type_identifier'] | KindMap['generic_type'] | KindMap['scoped_type_identifier'];
function _resolveType4(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: generic_type, scoped_type_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveCondition3(v: unknown): KindMap['let_condition'];
function _resolveCondition3(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: let_condition, raw_string_literal, string_literal, array_expression, assignment_expression, async_block, await_expression, binary_expression, block, break_expression, call_expression, closure_expression, compound_assignment_expr, const_block, continue_expression, field_expression, for_expression, gen_block, generic_function, if_expression, index_expression, loop_expression, macro_invocation, match_expression, parenthesized_expression, range_expression, reference_expression, return_expression, scoped_identifier, struct_expression, try_block, try_expression, tuple_expression, type_cast_expression, unary_expression, unsafe_block, while_expression, yield_expression. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveReservedIdentifier2(v: unknown): KindMap['identifier'];
function _resolveReservedIdentifier2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return identifier_(v);
  }
  if (Array.isArray(v)) return _resolveReservedIdentifier(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveReservedIdentifier(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePath5(v: unknown): KindMap['identifier'] | KindMap['scoped_identifier'];
function _resolvePath5(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: scoped_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveDeclarationStatement3(v: unknown): KindMap['attribute_item'] | KindMap['inner_attribute_item'];
function _resolveDeclarationStatement3(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute_item, inner_attribute_item. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier12(v: unknown): unknown;
function _resolveTypeIdentifier12(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['-'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveLiteral2(v: unknown): KindMap['float_literal'] | KindMap['integer_literal'];
function _resolveLiteral2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (/^(?:[0-9][0-9_]*|0x[0-9a-fA-F_]+|0b[01_]+|0o[0-7_]+)(?:u8|i8|u16|i16|u32|i32|u64|i64|u128|i128|isize|usize|f32|f64)?$/.test(v)) return integer_literal_(v);
    return float_literal_(v);
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1l6t7ly(v: unknown): KindMap['attribute_item'] | KindMap['visibility_modifier'];
function _r1l6t7ly(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute_item, visibility_modifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePath6(v: unknown): KindMap['self'];
function _resolvePath6(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
  }
  if (Array.isArray(v)) return _resolvePattern(v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolvePattern(v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rbdp9li(v: unknown): KindMap['attribute_item'] | KindMap['parameter'] | KindMap['self_parameter'] | KindMap['variadic_parameter'];
function _rbdp9li(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute_item, parameter, self_parameter, variadic_parameter, abstract_type, array_type, bounded_type, dynamic_type, function_type, generic_type, macro_invocation, pointer_type, reference_type, removed_trait_bound, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier13(v: unknown): unknown;
function _resolveTypeIdentifier13(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['..', '..=', '...'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePath7(v: unknown): KindMap['crate'] | KindMap['identifier'] | KindMap['metavariable'] | KindMap['self'] | KindMap['super'] | KindMap['scoped_identifier'];
function _resolvePath7(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^crate$/.test(v)) return (v === 'crate' ? crate_() : (() => { throw new Error(`Expected 'crate' for crate, got '${v}'`); })());
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: scoped_identifier, negative_literal, raw_string_literal, string_literal. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier14(v: unknown): unknown;
function _resolveTypeIdentifier14(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolvePath8(v: unknown): KindMap['identifier'] | KindMap['super'];
function _resolvePath8(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r16dnbom(v: unknown): KindMap['crate'] | KindMap['identifier'] | KindMap['metavariable'] | KindMap['self'] | KindMap['super'] | KindMap['bracketed_type'] | KindMap['generic_type'] | KindMap['scoped_identifier'];
function _r16dnbom(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^crate$/.test(v)) return (v === 'crate' ? crate_() : (() => { throw new Error(`Expected 'crate' for crate, got '${v}'`); })());
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: bracketed_type, generic_type, scoped_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _ra1kaw(v: unknown): KindMap['escape_sequence'] | KindMap['string_content'];
function _ra1kaw(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^\\(?:[^xu]|u[0-9a-fA-F]{4}|u\{[0-9a-fA-F]+\}|x[0-9a-fA-F]{2})$/.test(v)) return escape_sequence_(v);
    return string_content_(v);
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1315a75(v: unknown): KindMap['type_identifier'] | KindMap['generic_type_with_turbofish'] | KindMap['scoped_type_identifier'];
function _r1315a75(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: generic_type_with_turbofish, scoped_type_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rx4073x(v: unknown): KindMap['remaining_field_pattern'] | KindMap['field_pattern'];
function _rx4073x(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return (v === '..' ? remaining_field_pattern_() : (() => { throw new Error(`Expected '..' for remaining_field_pattern, got '${v}'`); })());
  }
  if (Array.isArray(v)) return _resolveByKind('field_pattern', v);
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    return _resolveByKind('field_pattern', v);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r18bpr9a(v: unknown): KindMap['crate'] | KindMap['identifier'] | KindMap['metavariable'] | KindMap['mutable_specifier'] | KindMap['primitive_type'] | KindMap['self'] | KindMap['super'] | KindMap['token_repetition'] | KindMap['token_tree'];
function _r18bpr9a(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['bool', 'char', 'f32', 'f64', 'i128', 'i16', 'i32', 'i64', 'i8', 'isize', 'str', 'u128', 'u16', 'u32', 'u64', 'u8', 'usize'].includes(v)) return primitive_type_(v);
    if (/^crate$/.test(v)) return (v === 'crate' ? crate_() : (() => { throw new Error(`Expected 'crate' for crate, got '${v}'`); })());
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^mut$/.test(v)) return (v === 'mut' ? mutable_specifier_() : (() => { throw new Error(`Expected 'mut' for mutable_specifier, got '${v}'`); })());
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: token_repetition, token_tree, raw_string_literal, string_literal. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rznjc70(v: unknown): KindMap['crate'] | KindMap['identifier'] | KindMap['metavariable'] | KindMap['mutable_specifier'] | KindMap['primitive_type'] | KindMap['self'] | KindMap['super'] | KindMap['token_binding_pattern'] | KindMap['token_repetition_pattern'] | KindMap['token_tree_pattern'];
function _rznjc70(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['bool', 'char', 'f32', 'f64', 'i128', 'i16', 'i32', 'i64', 'i8', 'isize', 'str', 'u128', 'u16', 'u32', 'u64', 'u8', 'usize'].includes(v)) return primitive_type_(v);
    if (/^crate$/.test(v)) return (v === 'crate' ? crate_() : (() => { throw new Error(`Expected 'crate' for crate, got '${v}'`); })());
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^mut$/.test(v)) return (v === 'mut' ? mutable_specifier_() : (() => { throw new Error(`Expected 'mut' for mutable_specifier, got '${v}'`); })());
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: token_binding_pattern, token_repetition_pattern, token_tree_pattern, raw_string_literal, string_literal. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1g3soma(v: unknown): KindMap['higher_ranked_trait_bound'] | KindMap['lifetime'];
function _r1g3soma(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: higher_ranked_trait_bound, lifetime, abstract_type, array_type, bounded_type, dynamic_type, function_type, generic_type, macro_invocation, pointer_type, reference_type, removed_trait_bound, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier15(v: unknown): unknown;
function _resolveTypeIdentifier15(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['?'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveExpressionExceptRange4(v: unknown): KindMap['closure_expression'];
function _resolveExpressionExceptRange4(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: closure_expression, negative_literal, raw_string_literal, string_literal, captured_pattern, const_block, generic_pattern, macro_invocation, mut_pattern, or_pattern, range_pattern, ref_pattern, reference_pattern, scoped_identifier, slice_pattern, struct_pattern, tuple_pattern, tuple_struct_pattern. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rnfan(v: unknown): KindMap['identifier'] | KindMap['generic_type'] | KindMap['scoped_identifier'];
function _rnfan(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: generic_type, scoped_identifier. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rzww8fe(v: unknown): KindMap['block'] | KindMap['lifetime'] | KindMap['trait_bounds'] | KindMap['type_binding'];
function _rzww8fe(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: block, lifetime, trait_bounds, type_binding, raw_string_literal, string_literal, abstract_type, array_type, bounded_type, dynamic_type, function_type, generic_type, macro_invocation, pointer_type, reference_type, removed_trait_bound, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _rrcyxzh(v: unknown): KindMap['metavariable'] | KindMap['attribute_item'] | KindMap['const_parameter'] | KindMap['lifetime_parameter'] | KindMap['type_parameter'];
function _rrcyxzh(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return metavariable_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: attribute_item, const_parameter, lifetime_parameter, type_parameter. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier16(v: unknown): unknown;
function _resolveTypeIdentifier16(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['-', '*', '!'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r1qv9yc5(v: unknown): KindMap['type_identifier'] | KindMap['lifetime'];
function _r1qv9yc5(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: lifetime. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveUseClause2(v: unknown): KindMap['crate'] | KindMap['identifier'] | KindMap['metavariable'] | KindMap['self'] | KindMap['super'] | KindMap['scoped_identifier'] | KindMap['scoped_use_list'] | KindMap['use_as_clause'] | KindMap['use_list'] | KindMap['use_wildcard'];
function _resolveUseClause2(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (/^crate$/.test(v)) return (v === 'crate' ? crate_() : (() => { throw new Error(`Expected 'crate' for crate, got '${v}'`); })());
    if (/^\$[a-zA-Z_]\w*$/.test(v)) return metavariable_(v);
    if (/^self$/.test(v)) return (v === 'self' ? self_() : (() => { throw new Error(`Expected 'self' for self, got '${v}'`); })());
    if (/^super$/.test(v)) return (v === 'super' ? super_() : (() => { throw new Error(`Expected 'super' for super, got '${v}'`); })());
    return identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: scoped_identifier, scoped_use_list, use_as_clause, use_list, use_wildcard. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier17(v: unknown): unknown;
function _resolveTypeIdentifier17(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['pub'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _resolveTypeIdentifier18(v: unknown): unknown;
function _resolveTypeIdentifier18(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'boolean') return boolean_literal_(v ? 'true' : 'false');
  if (typeof v === 'number') {
    return Number.isInteger(v) ? integer_literal_(`${v}`) : float_literal_(`${v}`);
  }
  if (typeof v === 'string') {
    if (['in'].includes(v)) return { type: v, text: v };
    throw new Error('Cannot resolve string value: no leaf types accepted for this field');
  }
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error('No branch types accepted for object value');
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

function _r17ua2b4(v: unknown): KindMap['primitive_type'] | KindMap['type_identifier'] | KindMap['array_type'] | KindMap['generic_type'] | KindMap['higher_ranked_trait_bound'] | KindMap['lifetime'] | KindMap['pointer_type'] | KindMap['reference_type'] | KindMap['scoped_type_identifier'] | KindMap['tuple_type'];
function _r17ua2b4(v: unknown) {
  if (isNodeData(v)) return v;
  if (typeof v === 'string') {
    if (['bool', 'char', 'f32', 'f64', 'i128', 'i16', 'i32', 'i64', 'i8', 'isize', 'str', 'u128', 'u16', 'u32', 'u64', 'u8', 'usize'].includes(v)) return primitive_type_(v);
    return type_identifier_(v);
  }
  if (Array.isArray(v)) throw new Error('Array value with ambiguous branch types — use {kind} to disambiguate');
  if (typeof v === 'object' && v !== null) {
    if (hasKind(v)) {
      const { kind: k, ...rest } = v;
      return _resolveByKind(k, rest);
    }
    throw new Error(`Multiple branch types possible for object with keys: ${Object.keys(v).join(', ')}. Candidates: array_type, generic_type, higher_ranked_trait_bound, lifetime, pointer_type, reference_type, scoped_type_identifier, tuple_type. Use { kind: '...' } to disambiguate.`);
  }
  throw new Error(`Cannot resolve .from() value: got ${typeof v}`);
}

export function abstractTypeFrom(input: AbstractType | AbstractTypeFromInput) {
  if (isNodeData(input)) {
    return abstract_type_({
      trait: input.fields?.trait,
      typeParameters: input.fields?.type_parameters,
    } as any);
  }
  const obj: any = input;
  return abstract_type_({
    trait: resolveField(obj.trait, _resolveType2),
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
  } as any);
}

export function arguments_From(input: Arguments | ArgumentsFromInput) {
  if (isNodeData(input)) {
    return arguments_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return arguments_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveDeclarationStatement2) : undefined,
  } as any);
}

export function arrayExpressionFrom(input: ArrayExpression | ArrayExpressionFromInput) {
  if (isNodeData(input)) {
    return array_expression_({
      length: input.fields?.length,
      attributes: input.fields?.attributes,
      elements: input.fields?.elements,
    } as any);
  }
  const obj: any = input;
  return array_expression_({
    length: obj.length !== undefined ? resolveField(obj.length, _resolveExpression) : undefined,
    attributes: obj.attributes !== undefined ? resolveField(obj.attributes, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('attribute_item', v) : v)) : undefined,
    elements: obj.elements !== undefined ? resolveField(obj.elements, _resolveDeclarationStatement2) : undefined,
  } as any);
}

export function arrayTypeFrom(input: ArrayType | ArrayTypeFromInput) {
  if (isNodeData(input)) {
    return array_type_({
      element: input.fields?.element,
      length: input.fields?.length,
    } as any);
  }
  const obj: any = input;
  return array_type_({
    element: resolveField(obj.element, _resolveType),
    length: obj.length !== undefined ? resolveField(obj.length, _resolveExpression) : undefined,
  } as any);
}

export function assignmentExpressionFrom(input: AssignmentExpression | AssignmentExpressionFromInput) {
  if (isNodeData(input)) {
    return assignment_expression_({
      left: input.fields?.left,
      right: input.fields?.right,
    } as any);
  }
  const obj: any = input;
  return assignment_expression_({
    left: resolveField(obj.left, _resolveExpression),
    right: resolveField(obj.right, _resolveExpression),
  } as any);
}

export function associatedTypeFrom(input: AssociatedType | AssociatedTypeFromInput) {
  if (isNodeData(input)) {
    return associated_type_({
      bounds: input.fields?.bounds,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      whereClause: input.fields?.where_clause,
    } as any);
  }
  const obj: any = input;
  return associated_type_({
    bounds: obj.bounds !== undefined ? resolveField(obj.bounds, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('trait_bounds', v) : v)) : undefined,
    name: resolveField(obj.name, _resolveTypeIdentifier2),
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function asyncBlockFrom(input: AsyncBlock | AsyncBlockFromInput) {
  if (isNodeData(input)) {
    return async_block_({
      block: input.fields?.block,
    } as any);
  }
  const obj: any = input;
  return async_block_({
    block: resolveField(obj.block, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
  } as any);
}

export function attributeFrom(input: Attribute | AttributeFromInput) {
  if (isNodeData(input)) {
    return attribute_({
      arguments: input.fields?.arguments,
      value: input.fields?.value,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return attribute_({
    arguments: obj.arguments !== undefined ? resolveField(obj.arguments, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('token_tree', v) : v)) : undefined,
    value: obj.value !== undefined ? resolveField(obj.value, _resolveExpression) : undefined,
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolvePath2) : undefined,
  } as any);
}

export function attributeItemFrom(input: AttributeItem | AttributeItemFromInput) {
  if (isNodeData(input)) {
    return attribute_item_({
      attribute: input.fields?.attribute,
    } as any);
  }
  const obj: any = input;
  return attribute_item_({
    attribute: resolveField(obj.attribute, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('attribute', v) : v)),
  } as any);
}

export function awaitExpressionFrom(input: AwaitExpression | AwaitExpressionFromInput) {
  if (isNodeData(input)) {
    return await_expression_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return await_expression_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveExpression) : undefined,
  } as any);
}

export function baseFieldInitializerFrom(input: BaseFieldInitializer | BaseFieldInitializerFromInput) {
  if (isNodeData(input)) {
    return base_field_initializer_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return base_field_initializer_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveExpression) : undefined,
  } as any);
}

export function binaryExpressionFrom(input: BinaryExpression | BinaryExpressionFromInput) {
  if (isNodeData(input)) {
    return binary_expression_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as any);
  }
  const obj: any = input;
  return binary_expression_({
    left: resolveField(obj.left, _resolveExpression),
    operator: resolveField(obj.operator, _resolveTypeIdentifier3),
    right: resolveField(obj.right, _resolveExpression),
  } as any);
}

export function blockFrom(input: Block | BlockFromInput) {
  if (isNodeData(input)) {
    return block_({
      label: input.fields?.label,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return block_({
    label: obj.label !== undefined ? resolveField(obj.label, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('label', v) : v)) : undefined,
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveStatement2) : undefined,
  } as any);
}

export function blockCommentFrom(input: BlockComment | BlockCommentFromInput) {
  if (isNodeData(input)) {
    return block_comment_({
      doc: input.fields?.doc,
      inner: input.fields?.inner,
      outer: input.fields?.outer,
    } as any);
  }
  const obj: any = input;
  return block_comment_({
    doc: obj.doc !== undefined ? resolveField(obj.doc, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? doc_comment_('' + v) : v)) : undefined,
    inner: obj.inner !== undefined ? resolveField(obj.inner, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? inner_doc_comment_marker_('' + v) : v)) : undefined,
    outer: obj.outer !== undefined ? resolveField(obj.outer, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? outer_doc_comment_marker_('' + v) : v)) : undefined,
  } as any);
}

export function boundedTypeFrom(input: BoundedType | BoundedTypeFromInput) {
  if (isNodeData(input)) {
    return bounded_type_({
      left: input.fields?.left,
      right: input.fields?.right,
    } as any);
  }
  const obj: any = input;
  return bounded_type_({
    left: resolveField(obj.left, _rzt5kvo),
    right: resolveField(obj.right, _rzt5kvo),
  } as any);
}

export function bracketedTypeFrom(input: BracketedType | BracketedTypeFromInput) {
  if (isNodeData(input)) {
    return bracketed_type_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return bracketed_type_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _raoo9kn) : undefined,
  } as any);
}

export function breakExpressionFrom(input: BreakExpression | BreakExpressionFromInput) {
  if (isNodeData(input)) {
    return break_expression_({
      label: input.fields?.label,
      expression: input.fields?.expression,
    } as any);
  }
  const obj: any = input;
  return break_expression_({
    label: obj.label !== undefined ? resolveField(obj.label, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('label', v) : v)) : undefined,
    expression: obj.expression !== undefined ? resolveField(obj.expression, _resolveExpression) : undefined,
  } as any);
}

export function callExpressionFrom(input: CallExpression | CallExpressionFromInput) {
  if (isNodeData(input)) {
    return call_expression_({
      arguments: input.fields?.arguments,
      function: input.fields?.function,
    } as any);
  }
  const obj: any = input;
  return call_expression_({
    arguments: resolveField(obj.arguments, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('arguments', v) : v)),
    function: resolveField(obj.function, _resolveExpressionExceptRange2),
  } as any);
}

export function capturedPatternFrom(input: CapturedPattern | CapturedPatternFromInput) {
  if (isNodeData(input)) {
    return captured_pattern_({
      identifier: input.fields?.identifier,
      pattern: input.fields?.pattern,
    } as any);
  }
  const obj: any = input;
  return captured_pattern_({
    identifier: resolveField(obj.identifier, _resolveReservedIdentifier),
    pattern: resolveField(obj.pattern, _resolvePattern),
  } as any);
}

export function closureExpressionFrom(input: ClosureExpression | ClosureExpressionFromInput) {
  if (isNodeData(input)) {
    return closure_expression_({
      body: input.fields?.body,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      async: input.fields?.async,
      move: input.fields?.move,
      static: input.fields?.static,
    } as any);
  }
  const obj: any = input;
  return closure_expression_({
    body: resolveField(obj.body, _resolveExpressionEndingWithBlock2),
    parameters: resolveField(obj.parameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('closure_parameters', v) : v)),
    returnType: obj.returnType !== undefined ? resolveField(obj.returnType, _resolveType) : undefined,
    async: obj.async !== undefined ? resolveField(obj.async, _resolveTypeIdentifier4) : undefined,
    move: obj.move !== undefined ? resolveField(obj.move, _resolveTypeIdentifier5) : undefined,
    static: obj.static !== undefined ? resolveField(obj.static, _resolveTypeIdentifier6) : undefined,
  } as any);
}

export function closureParametersFrom(input: ClosureParameters | ClosureParametersFromInput) {
  if (isNodeData(input)) {
    return closure_parameters_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return closure_parameters_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _r1q9fx9f) : undefined,
  } as any);
}

export function compoundAssignmentExprFrom(input: CompoundAssignmentExpr | CompoundAssignmentExprFromInput) {
  if (isNodeData(input)) {
    return compound_assignment_expr_({
      left: input.fields?.left,
      operator: input.fields?.operator,
      right: input.fields?.right,
    } as any);
  }
  const obj: any = input;
  return compound_assignment_expr_({
    left: resolveField(obj.left, _resolveExpression),
    operator: resolveField(obj.operator, _resolveTypeIdentifier7),
    right: resolveField(obj.right, _resolveExpression),
  } as any);
}

export function constBlockFrom(input: ConstBlock | ConstBlockFromInput) {
  if (isNodeData(input)) {
    return const_block_({
      body: input.fields?.body,
    } as any);
  }
  const obj: any = input;
  return const_block_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
  } as any);
}

export function constItemFrom(input: ConstItem | ConstItemFromInput) {
  if (isNodeData(input)) {
    return const_item_({
      name: input.fields?.name,
      type: input.fields?.type,
      value: input.fields?.value,
      visibilityModifier: input.fields?.visibility_modifier,
    } as any);
  }
  const obj: any = input;
  return const_item_({
    name: resolveField(obj.name, _resolveReservedIdentifier),
    type: resolveField(obj.type, _resolveType),
    value: obj.value !== undefined ? resolveField(obj.value, _resolveExpression) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
  } as any);
}

export function constParameterFrom(input: ConstParameter | ConstParameterFromInput) {
  if (isNodeData(input)) {
    return const_parameter_({
      name: input.fields?.name,
      type: input.fields?.type,
      value: input.fields?.value,
    } as any);
  }
  const obj: any = input;
  return const_parameter_({
    name: resolveField(obj.name, _resolveReservedIdentifier),
    type: resolveField(obj.type, _resolveType),
    value: obj.value !== undefined ? resolveField(obj.value, _r1irud17) : undefined,
  } as any);
}

export function continueExpressionFrom(input: ContinueExpression | ContinueExpressionFromInput) {
  if (isNodeData(input)) {
    return continue_expression_({
      label: input.fields?.label,
    } as any);
  }
  const obj: any = input;
  return continue_expression_({
    label: obj.label !== undefined ? resolveField(obj.label, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('label', v) : v)) : undefined,
  } as any);
}

export function declarationListFrom(input: DeclarationList | DeclarationListFromInput) {
  if (isNodeData(input)) {
    return declaration_list_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return declaration_list_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveDeclarationStatement) : undefined,
  } as any);
}

export function dynamicTypeFrom(input: DynamicType | DynamicTypeFromInput) {
  if (isNodeData(input)) {
    return dynamic_type_({
      trait: input.fields?.trait,
    } as any);
  }
  const obj: any = input;
  return dynamic_type_({
    trait: resolveField(obj.trait, _rk93jcc),
  } as any);
}

export function elseClauseFrom(input: ElseClause | ElseClauseFromInput) {
  if (isNodeData(input)) {
    return else_clause_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return else_clause_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveExpressionEndingWithBlock3) : undefined,
  } as any);
}

export function enumItemFrom(input: EnumItem | EnumItemFromInput) {
  if (isNodeData(input)) {
    return enum_item_({
      body: input.fields?.body,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      visibilityModifier: input.fields?.visibility_modifier,
      whereClause: input.fields?.where_clause,
    } as any);
  }
  const obj: any = input;
  return enum_item_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('enum_variant_list', v) : v)),
    name: resolveField(obj.name, _resolveTypeIdentifier2),
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function enumVariantFrom(input: EnumVariant | EnumVariantFromInput) {
  if (isNodeData(input)) {
    return enum_variant_({
      body: input.fields?.body,
      name: input.fields?.name,
      value: input.fields?.value,
      visibilityModifier: input.fields?.visibility_modifier,
    } as any);
  }
  const obj: any = input;
  return enum_variant_({
    body: obj.body !== undefined ? resolveField(obj.body, _r16kj8qi) : undefined,
    name: resolveField(obj.name, _resolveReservedIdentifier),
    value: obj.value !== undefined ? resolveField(obj.value, _resolveExpression) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
  } as any);
}

export function enumVariantListFrom(input: EnumVariantList | EnumVariantListFromInput) {
  if (isNodeData(input)) {
    return enum_variant_list_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return enum_variant_list_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _rmxz15l) : undefined,
  } as any);
}

export function expressionStatementFrom(input: ExpressionStatement | ExpressionStatementFromInput) {
  if (isNodeData(input)) {
    return expression_statement_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return expression_statement_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveTypeIdentifier8) : undefined,
  } as any);
}

export function externCrateDeclarationFrom(input: ExternCrateDeclaration | ExternCrateDeclarationFromInput) {
  if (isNodeData(input)) {
    return extern_crate_declaration_({
      alias: input.fields?.alias,
      name: input.fields?.name,
      visibilityModifier: input.fields?.visibility_modifier,
      crate: input.fields?.crate,
    } as any);
  }
  const obj: any = input;
  return extern_crate_declaration_({
    alias: obj.alias !== undefined ? resolveField(obj.alias, _resolveReservedIdentifier) : undefined,
    name: resolveField(obj.name, _resolveReservedIdentifier),
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    crate: resolveField(obj.crate, (v: unknown) => (typeof v === 'string' && v === 'crate' ? crate_() : v)),
  } as any);
}

export function externModifierFrom(input: ExternModifier | ExternModifierFromInput) {
  if (isNodeData(input)) {
    return extern_modifier_({
      stringLiteral: input.fields?.string_literal,
    } as any);
  }
  const obj: any = input;
  return extern_modifier_({
    stringLiteral: obj.stringLiteral !== undefined ? resolveField(obj.stringLiteral, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('string_literal', v) : v)) : undefined,
  } as any);
}

export function fieldDeclarationFrom(input: FieldDeclaration | FieldDeclarationFromInput) {
  if (isNodeData(input)) {
    return field_declaration_({
      name: input.fields?.name,
      type: input.fields?.type,
      visibilityModifier: input.fields?.visibility_modifier,
    } as any);
  }
  const obj: any = input;
  return field_declaration_({
    name: resolveField(obj.name, _resolveFieldIdentifier2),
    type: resolveField(obj.type, _resolveType),
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
  } as any);
}

export function fieldDeclarationListFrom(input: FieldDeclarationList | FieldDeclarationListFromInput) {
  if (isNodeData(input)) {
    return field_declaration_list_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return field_declaration_list_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _r1d1qsih) : undefined,
  } as any);
}

export function fieldExpressionFrom(input: FieldExpression | FieldExpressionFromInput) {
  if (isNodeData(input)) {
    return field_expression_({
      field: input.fields?.field,
      value: input.fields?.value,
    } as any);
  }
  const obj: any = input;
  return field_expression_({
    field: resolveField(obj.field, _rkdlimm),
    value: resolveField(obj.value, _resolveExpression),
  } as any);
}

export function fieldInitializerFrom(input: FieldInitializer | FieldInitializerFromInput) {
  if (isNodeData(input)) {
    return field_initializer_({
      field: input.fields?.field,
      value: input.fields?.value,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return field_initializer_({
    field: resolveField(obj.field, _rkdlimm),
    value: resolveField(obj.value, _resolveExpression),
    children: obj['children'] !== undefined ? resolveField(obj['children'], (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('attribute_item', v) : v)) : undefined,
  } as any);
}

export function fieldInitializerListFrom(input: FieldInitializerList | FieldInitializerListFromInput) {
  if (isNodeData(input)) {
    return field_initializer_list_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return field_initializer_list_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _r1n0ectv) : undefined,
  } as any);
}

export function fieldPatternFrom(input: FieldPattern | FieldPatternFromInput) {
  if (isNodeData(input)) {
    return field_pattern_({
      name: input.fields?.name,
      pattern: input.fields?.pattern,
      mutableSpecifier: input.fields?.mutable_specifier,
    } as any);
  }
  const obj: any = input;
  return field_pattern_({
    name: resolveField(obj.name, _r1gfzgqs),
    pattern: obj.pattern !== undefined ? resolveField(obj.pattern, _resolvePattern) : undefined,
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
  } as any);
}

export function forExpressionFrom(input: ForExpression | ForExpressionFromInput) {
  if (isNodeData(input)) {
    return for_expression_({
      body: input.fields?.body,
      pattern: input.fields?.pattern,
      value: input.fields?.value,
      label: input.fields?.label,
    } as any);
  }
  const obj: any = input;
  return for_expression_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
    pattern: resolveField(obj.pattern, _resolvePattern),
    value: resolveField(obj.value, _resolveExpression),
    label: obj.label !== undefined ? resolveField(obj.label, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('label', v) : v)) : undefined,
  } as any);
}

export function forLifetimesFrom(input: ForLifetimes | ForLifetimesFromInput) {
  if (isNodeData(input)) {
    return for_lifetimes_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return for_lifetimes_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('lifetime', v) : v)) : [],
  } as any);
}

export function foreignModItemFrom(input: ForeignModItem | ForeignModItemFromInput) {
  if (isNodeData(input)) {
    return foreign_mod_item_({
      body: input.fields?.body,
      visibilityModifier: input.fields?.visibility_modifier,
      externModifier: input.fields?.extern_modifier,
    } as any);
  }
  const obj: any = input;
  return foreign_mod_item_({
    body: obj.body !== undefined ? resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('declaration_list', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    externModifier: resolveField(obj.externModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('extern_modifier', v) : v)),
  } as any);
}

export function functionItemFrom(input: FunctionItem | FunctionItemFromInput) {
  if (isNodeData(input)) {
    return function_item_({
      body: input.fields?.body,
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
      visibilityModifier: input.fields?.visibility_modifier,
      functionModifiers: input.fields?.function_modifiers,
      whereClause: input.fields?.where_clause,
    } as any);
  }
  const obj: any = input;
  return function_item_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
    name: resolveField(obj.name, _resolvePath3),
    parameters: resolveField(obj.parameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('parameters', v) : v)),
    returnType: obj.returnType !== undefined ? resolveField(obj.returnType, _resolveType) : undefined,
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    functionModifiers: obj.functionModifiers !== undefined ? resolveField(obj.functionModifiers, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('function_modifiers', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function functionModifiersFrom(input: FunctionModifiers | FunctionModifiersFromInput) {
  if (isNodeData(input)) {
    return function_modifiers_({
      async: input.fields?.async,
      default: input.fields?.default,
      const: input.fields?.const,
      unsafe: input.fields?.unsafe,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return function_modifiers_({
    async: obj.async !== undefined ? resolveField(obj.async, _resolveTypeIdentifier4) : undefined,
    default: obj.default !== undefined ? resolveField(obj.default, _resolveTypeIdentifier9) : undefined,
    const: obj.const !== undefined ? resolveField(obj.const, _resolveTypeIdentifier10) : undefined,
    unsafe: obj.unsafe !== undefined ? resolveField(obj.unsafe, _resolveTypeIdentifier11) : undefined,
    children: obj['children'] !== undefined ? resolveField(obj['children'], (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('extern_modifier', v) : v)) : undefined,
  } as any);
}

export function functionSignatureItemFrom(input: FunctionSignatureItem | FunctionSignatureItemFromInput) {
  if (isNodeData(input)) {
    return function_signature_item_({
      name: input.fields?.name,
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      typeParameters: input.fields?.type_parameters,
      visibilityModifier: input.fields?.visibility_modifier,
      functionModifiers: input.fields?.function_modifiers,
      whereClause: input.fields?.where_clause,
    } as any);
  }
  const obj: any = input;
  return function_signature_item_({
    name: resolveField(obj.name, _resolvePath3),
    parameters: resolveField(obj.parameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('parameters', v) : v)),
    returnType: obj.returnType !== undefined ? resolveField(obj.returnType, _resolveType) : undefined,
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    functionModifiers: obj.functionModifiers !== undefined ? resolveField(obj.functionModifiers, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('function_modifiers', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function functionTypeFrom(input: FunctionType | FunctionTypeFromInput) {
  if (isNodeData(input)) {
    return function_type_({
      parameters: input.fields?.parameters,
      returnType: input.fields?.return_type,
      trait: input.fields?.trait,
      forLifetimes: input.fields?.for_lifetimes,
      functionModifiers: input.fields?.function_modifiers,
    } as any);
  }
  const obj: any = input;
  return function_type_({
    parameters: resolveField(obj.parameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('parameters', v) : v)),
    returnType: obj.returnType !== undefined ? resolveField(obj.returnType, _resolveType) : undefined,
    trait: obj.trait !== undefined ? resolveField(obj.trait, _resolveType3) : undefined,
    forLifetimes: obj.forLifetimes !== undefined ? resolveField(obj.forLifetimes, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('for_lifetimes', v) : v)) : undefined,
    functionModifiers: obj.functionModifiers !== undefined ? resolveField(obj.functionModifiers, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('function_modifiers', v) : v)) : undefined,
  } as any);
}

export function genBlockFrom(input: GenBlock | GenBlockFromInput) {
  if (isNodeData(input)) {
    return gen_block_({
      block: input.fields?.block,
    } as any);
  }
  const obj: any = input;
  return gen_block_({
    block: resolveField(obj.block, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
  } as any);
}

export function genericFunctionFrom(input: GenericFunction | GenericFunctionFromInput) {
  if (isNodeData(input)) {
    return generic_function_({
      function: input.fields?.function,
      typeArguments: input.fields?.type_arguments,
    } as any);
  }
  const obj: any = input;
  return generic_function_({
    function: resolveField(obj.function, _resolveExpressionExceptRange3),
    typeArguments: resolveField(obj.typeArguments, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_arguments', v) : v)),
  } as any);
}

export function genericPatternFrom(input: GenericPattern | GenericPatternFromInput) {
  if (isNodeData(input)) {
    return generic_pattern_({
      typeArguments: input.fields?.type_arguments,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return generic_pattern_({
    typeArguments: resolveField(obj.typeArguments, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_arguments', v) : v)),
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolvePath4) : undefined,
  } as any);
}

export function genericTypeFrom(input: GenericType | GenericTypeFromInput) {
  if (isNodeData(input)) {
    return generic_type_({
      type: input.fields?.type,
      typeArguments: input.fields?.type_arguments,
    } as any);
  }
  const obj: any = input;
  return generic_type_({
    type: resolveField(obj.type, _rhvptj6),
    typeArguments: resolveField(obj.typeArguments, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_arguments', v) : v)),
  } as any);
}

export function genericTypeWithTurbofishFrom(input: GenericTypeWithTurbofish | GenericTypeWithTurbofishFromInput) {
  if (isNodeData(input)) {
    return generic_type_with_turbofish_({
      type: input.fields?.type,
      typeArguments: input.fields?.type_arguments,
    } as any);
  }
  const obj: any = input;
  return generic_type_with_turbofish_({
    type: resolveField(obj.type, _rbvr4o6),
    typeArguments: resolveField(obj.typeArguments, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_arguments', v) : v)),
  } as any);
}

export function higherRankedTraitBoundFrom(input: HigherRankedTraitBound | HigherRankedTraitBoundFromInput) {
  if (isNodeData(input)) {
    return higher_ranked_trait_bound_({
      type: input.fields?.type,
      typeParameters: input.fields?.type_parameters,
    } as any);
  }
  const obj: any = input;
  return higher_ranked_trait_bound_({
    type: resolveField(obj.type, _resolveType),
    typeParameters: resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)),
  } as any);
}

export function ifExpressionFrom(input: IfExpression | IfExpressionFromInput) {
  if (isNodeData(input)) {
    return if_expression_({
      alternative: input.fields?.alternative,
      condition: input.fields?.condition,
      consequence: input.fields?.consequence,
    } as any);
  }
  const obj: any = input;
  return if_expression_({
    alternative: obj.alternative !== undefined ? resolveField(obj.alternative, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('else_clause', v) : v)) : undefined,
    condition: resolveField(obj.condition, _resolveCondition2),
    consequence: resolveField(obj.consequence, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
  } as any);
}

export function implItemFrom(input: ImplItem | ImplItemFromInput) {
  if (isNodeData(input)) {
    return impl_item_({
      body: input.fields?.body,
      trait: input.fields?.trait,
      type: input.fields?.type,
      typeParameters: input.fields?.type_parameters,
      whereClause: input.fields?.where_clause,
    } as any);
  }
  const obj: any = input;
  return impl_item_({
    body: obj.body !== undefined ? resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('declaration_list', v) : v)) : undefined,
    trait: obj.trait !== undefined ? resolveField(obj.trait, _resolveType4) : undefined,
    type: resolveField(obj.type, _resolveType),
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function indexExpressionFrom(input: IndexExpression | IndexExpressionFromInput) {
  if (isNodeData(input)) {
    return index_expression_({
      object: input.fields?.object,
      index: input.fields?.index,
    } as any);
  }
  const obj: any = input;
  return index_expression_({
    object: resolveField(obj.object, _resolveExpression),
    index: resolveField(obj.index, _resolveExpression),
  } as any);
}

export function innerAttributeItemFrom(input: InnerAttributeItem | InnerAttributeItemFromInput) {
  if (isNodeData(input)) {
    return inner_attribute_item_({
      attribute: input.fields?.attribute,
    } as any);
  }
  const obj: any = input;
  return inner_attribute_item_({
    attribute: resolveField(obj.attribute, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('attribute', v) : v)),
  } as any);
}

export function labelFrom(input: Label | LabelFromInput) {
  if (isNodeData(input)) {
    return label_({
      identifier: input.fields?.identifier,
    } as any);
  }
  const obj: any = input;
  return label_({
    identifier: resolveField(obj.identifier, _resolveReservedIdentifier),
  } as any);
}

export function letChainFrom(input: LetChain | LetChainFromInput) {
  if (isNodeData(input)) {
    return let_chain_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return let_chain_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveCondition3) : [],
  } as any);
}

export function letConditionFrom(input: LetCondition | LetConditionFromInput) {
  if (isNodeData(input)) {
    return let_condition_({
      pattern: input.fields?.pattern,
      value: input.fields?.value,
    } as any);
  }
  const obj: any = input;
  return let_condition_({
    pattern: resolveField(obj.pattern, _resolvePattern),
    value: resolveField(obj.value, _resolveExpression),
  } as any);
}

export function letDeclarationFrom(input: LetDeclaration | LetDeclarationFromInput) {
  if (isNodeData(input)) {
    return let_declaration_({
      alternative: input.fields?.alternative,
      pattern: input.fields?.pattern,
      type: input.fields?.type,
      value: input.fields?.value,
      mutableSpecifier: input.fields?.mutable_specifier,
    } as any);
  }
  const obj: any = input;
  return let_declaration_({
    alternative: obj.alternative !== undefined ? resolveField(obj.alternative, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)) : undefined,
    pattern: resolveField(obj.pattern, _resolvePattern),
    type: obj.type !== undefined ? resolveField(obj.type, _resolveType) : undefined,
    value: obj.value !== undefined ? resolveField(obj.value, _resolveExpression) : undefined,
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
  } as any);
}

export function lifetimeFrom(input: Lifetime | LifetimeFromInput) {
  if (isNodeData(input)) {
    return lifetime_({
      identifier: input.fields?.identifier,
    } as any);
  }
  const obj: any = input;
  return lifetime_({
    identifier: resolveField(obj.identifier, _resolveReservedIdentifier),
  } as any);
}

export function lifetimeParameterFrom(input: LifetimeParameter | LifetimeParameterFromInput) {
  if (isNodeData(input)) {
    return lifetime_parameter_({
      bounds: input.fields?.bounds,
      name: input.fields?.name,
    } as any);
  }
  const obj: any = input;
  return lifetime_parameter_({
    bounds: obj.bounds !== undefined ? resolveField(obj.bounds, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('trait_bounds', v) : v)) : undefined,
    name: resolveField(obj.name, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('lifetime', v) : v)),
  } as any);
}

export function lineCommentFrom(input: LineComment | LineCommentFromInput) {
  if (isNodeData(input)) {
    return line_comment_({
      doc: input.fields?.doc,
      inner: input.fields?.inner,
      outer: input.fields?.outer,
    } as any);
  }
  const obj: any = input;
  return line_comment_({
    doc: obj.doc !== undefined ? resolveField(obj.doc, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? doc_comment_('' + v) : v)) : undefined,
    inner: obj.inner !== undefined ? resolveField(obj.inner, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? inner_doc_comment_marker_('' + v) : v)) : undefined,
    outer: obj.outer !== undefined ? resolveField(obj.outer, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? outer_doc_comment_marker_('' + v) : v)) : undefined,
  } as any);
}

export function loopExpressionFrom(input: LoopExpression | LoopExpressionFromInput) {
  if (isNodeData(input)) {
    return loop_expression_({
      body: input.fields?.body,
      label: input.fields?.label,
    } as any);
  }
  const obj: any = input;
  return loop_expression_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
    label: obj.label !== undefined ? resolveField(obj.label, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('label', v) : v)) : undefined,
  } as any);
}

export function macroDefinitionFrom(input: MacroDefinition | MacroDefinitionFromInput) {
  if (isNodeData(input)) {
    return macro_definition_({
      name: input.fields?.name,
      rules: input.fields?.rules,
    } as any);
  }
  const obj: any = input;
  return macro_definition_({
    name: resolveField(obj.name, _resolveReservedIdentifier2),
    rules: obj.rules !== undefined ? resolveField(obj.rules, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('macro_rule', v) : v)) : [],
  } as any);
}

export function macroInvocationFrom(input: MacroInvocation | MacroInvocationFromInput) {
  if (isNodeData(input)) {
    return macro_invocation_({
      macro: input.fields?.macro,
      tokenTree: input.fields?.token_tree,
    } as any);
  }
  const obj: any = input;
  return macro_invocation_({
    macro: resolveField(obj.macro, _resolvePath5),
    tokenTree: resolveField(obj.tokenTree, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('token_tree', v) : v)),
  } as any);
}

export function macroRuleFrom(input: MacroRule | MacroRuleFromInput) {
  if (isNodeData(input)) {
    return macro_rule_({
      left: input.fields?.left,
      right: input.fields?.right,
    } as any);
  }
  const obj: any = input;
  return macro_rule_({
    left: resolveField(obj.left, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('token_tree_pattern', v) : v)),
    right: resolveField(obj.right, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('token_tree', v) : v)),
  } as any);
}

export function matchArmFrom(input: MatchArm | MatchArmFromInput) {
  if (isNodeData(input)) {
    return match_arm_({
      pattern: input.fields?.pattern,
      value: input.fields?.value,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return match_arm_({
    pattern: resolveField(obj.pattern, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('match_pattern', v) : v)),
    value: resolveField(obj.value, _resolveTypeIdentifier8),
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveDeclarationStatement3) : undefined,
  } as any);
}

export function matchBlockFrom(input: MatchBlock | MatchBlockFromInput) {
  if (isNodeData(input)) {
    return match_block_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return match_block_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('match_arm', v) : v)) : undefined,
  } as any);
}

export function matchExpressionFrom(input: MatchExpression | MatchExpressionFromInput) {
  if (isNodeData(input)) {
    return match_expression_({
      body: input.fields?.body,
      value: input.fields?.value,
    } as any);
  }
  const obj: any = input;
  return match_expression_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('match_block', v) : v)),
    value: resolveField(obj.value, _resolveExpression),
  } as any);
}

export function matchPatternFrom(input: MatchPattern | MatchPatternFromInput) {
  if (isNodeData(input)) {
    return match_pattern_({
      condition: input.fields?.condition,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return match_pattern_({
    condition: obj.condition !== undefined ? resolveField(obj.condition, _resolveCondition2) : undefined,
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolvePattern) : undefined,
  } as any);
}

export function modItemFrom(input: ModItem | ModItemFromInput) {
  if (isNodeData(input)) {
    return mod_item_({
      body: input.fields?.body,
      name: input.fields?.name,
      visibilityModifier: input.fields?.visibility_modifier,
    } as any);
  }
  const obj: any = input;
  return mod_item_({
    body: obj.body !== undefined ? resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('declaration_list', v) : v)) : undefined,
    name: resolveField(obj.name, _resolveReservedIdentifier),
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
  } as any);
}

export function mutPatternFrom(input: MutPattern | MutPatternFromInput) {
  if (isNodeData(input)) {
    return mut_pattern_({
      mutableSpecifier: input.fields?.mutable_specifier,
      pattern: input.fields?.pattern,
    } as any);
  }
  const obj: any = input;
  return mut_pattern_({
    mutableSpecifier: resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)),
    pattern: resolveField(obj.pattern, _resolvePattern),
  } as any);
}

export function negativeLiteralFrom(input: NegativeLiteral | NegativeLiteralFromInput) {
  if (isNodeData(input)) {
    return negative_literal_({
      operator: input.fields?.operator,
      value: input.fields?.value,
    } as any);
  }
  const obj: any = input;
  return negative_literal_({
    operator: resolveField(obj.operator, _resolveTypeIdentifier12),
    value: resolveField(obj.value, _resolveLiteral2),
  } as any);
}

export function orPatternFrom(input: OrPattern | OrPatternFromInput) {
  if (isNodeData(input)) {
    return or_pattern_({
      left: input.fields?.left,
      right: input.fields?.right,
    } as any);
  }
  const obj: any = input;
  return or_pattern_({
    left: resolveField(obj.left, _resolvePattern),
    right: obj.right !== undefined ? resolveField(obj.right, _resolvePattern) : undefined,
  } as any);
}

export function orderedFieldDeclarationListFrom(input: OrderedFieldDeclarationList | OrderedFieldDeclarationListFromInput) {
  if (isNodeData(input)) {
    return ordered_field_declaration_list_({
      type: input.fields?.type,
      attributes: input.fields?.attributes,
      visibilityModifier: input.fields?.visibility_modifier,
      declarations: input.fields?.declarations,
    } as any);
  }
  const obj: any = input;
  return ordered_field_declaration_list_({
    type: obj.type !== undefined ? resolveField(obj.type, _resolveType) : undefined,
    attributes: obj.attributes !== undefined ? resolveField(obj.attributes, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('attribute_item', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    declarations: obj.declarations !== undefined ? resolveField(obj.declarations, _r1l6t7ly) : undefined,
  } as any);
}

export function parameterFrom(input: Parameter | ParameterFromInput) {
  if (isNodeData(input)) {
    return parameter_({
      pattern: input.fields?.pattern,
      type: input.fields?.type,
      mutableSpecifier: input.fields?.mutable_specifier,
    } as any);
  }
  const obj: any = input;
  return parameter_({
    pattern: resolveField(obj.pattern, _resolvePath6),
    type: resolveField(obj.type, _resolveType),
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
  } as any);
}

export function parametersFrom(input: Parameters | ParametersFromInput) {
  if (isNodeData(input)) {
    return parameters_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return parameters_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _rbdp9li) : undefined,
  } as any);
}

export function parenthesizedExpressionFrom(input: ParenthesizedExpression | ParenthesizedExpressionFromInput) {
  if (isNodeData(input)) {
    return parenthesized_expression_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return parenthesized_expression_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveExpression) : undefined,
  } as any);
}

export function pointerTypeFrom(input: PointerType | PointerTypeFromInput) {
  if (isNodeData(input)) {
    return pointer_type_({
      type: input.fields?.type,
      mutableSpecifier: input.fields?.mutable_specifier,
    } as any);
  }
  const obj: any = input;
  return pointer_type_({
    type: resolveField(obj.type, _resolveType),
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
  } as any);
}

export function qualifiedTypeFrom(input: QualifiedType | QualifiedTypeFromInput) {
  if (isNodeData(input)) {
    return qualified_type_({
      alias: input.fields?.alias,
      type: input.fields?.type,
    } as any);
  }
  const obj: any = input;
  return qualified_type_({
    alias: resolveField(obj.alias, _resolveType),
    type: resolveField(obj.type, _resolveType),
  } as any);
}

export function rangeExpressionFrom(input: RangeExpression | RangeExpressionFromInput) {
  if (isNodeData(input)) {
    return range_expression_({
      start: input.fields?.start,
      operator: input.fields?.operator,
      end: input.fields?.end,
    } as any);
  }
  const obj: any = input;
  return range_expression_({
    start: obj.start !== undefined ? resolveField(obj.start, _resolveExpression) : undefined,
    operator: resolveField(obj.operator, _resolveTypeIdentifier13),
    end: obj.end !== undefined ? resolveField(obj.end, _resolveExpression) : undefined,
  } as any);
}

export function rangePatternFrom(input: RangePattern | RangePatternFromInput) {
  if (isNodeData(input)) {
    return range_pattern_({
      left: input.fields?.left,
      right: input.fields?.right,
    } as any);
  }
  const obj: any = input;
  return range_pattern_({
    left: obj.left !== undefined ? resolveField(obj.left, _resolvePath7) : undefined,
    right: obj.right !== undefined ? resolveField(obj.right, _resolvePath7) : undefined,
  } as any);
}

export function rawStringLiteralFrom(input: RawStringLiteral | RawStringLiteralFromInput) {
  if (isNodeData(input)) {
    return raw_string_literal_({
      rawStringLiteralStart: input.fields?.raw_string_literal_start,
      stringContent: input.fields?.string_content,
      rawStringLiteralEnd: input.fields?.raw_string_literal_end,
    } as any);
  }
  const obj: any = input;
  return raw_string_literal_({
    rawStringLiteralStart: resolveField(obj.rawStringLiteralStart, _resolveTypeIdentifier14),
    stringContent: resolveField(obj.stringContent, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? string_content_('' + v) : v)),
    rawStringLiteralEnd: resolveField(obj.rawStringLiteralEnd, _resolveTypeIdentifier14),
  } as any);
}

export function refPatternFrom(input: RefPattern | RefPatternFromInput) {
  if (isNodeData(input)) {
    return ref_pattern_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return ref_pattern_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolvePattern) : undefined,
  } as any);
}

export function referenceExpressionFrom(input: ReferenceExpression | ReferenceExpressionFromInput) {
  if (isNodeData(input)) {
    return reference_expression_({
      value: input.fields?.value,
      mutableSpecifier: input.fields?.mutable_specifier,
    } as any);
  }
  const obj: any = input;
  return reference_expression_({
    value: resolveField(obj.value, _resolveExpression),
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
  } as any);
}

export function referencePatternFrom(input: ReferencePattern | ReferencePatternFromInput) {
  if (isNodeData(input)) {
    return reference_pattern_({
      mutableSpecifier: input.fields?.mutable_specifier,
      pattern: input.fields?.pattern,
    } as any);
  }
  const obj: any = input;
  return reference_pattern_({
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
    pattern: resolveField(obj.pattern, _resolvePattern),
  } as any);
}

export function referenceTypeFrom(input: ReferenceType | ReferenceTypeFromInput) {
  if (isNodeData(input)) {
    return reference_type_({
      type: input.fields?.type,
      lifetime: input.fields?.lifetime,
      mutableSpecifier: input.fields?.mutable_specifier,
    } as any);
  }
  const obj: any = input;
  return reference_type_({
    type: resolveField(obj.type, _resolveType),
    lifetime: obj.lifetime !== undefined ? resolveField(obj.lifetime, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('lifetime', v) : v)) : undefined,
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
  } as any);
}

export function removedTraitBoundFrom(input: RemovedTraitBound | RemovedTraitBoundFromInput) {
  if (isNodeData(input)) {
    return removed_trait_bound_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return removed_trait_bound_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveType) : undefined,
  } as any);
}

export function returnExpressionFrom(input: ReturnExpression | ReturnExpressionFromInput) {
  if (isNodeData(input)) {
    return return_expression_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return return_expression_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveExpression) : undefined,
  } as any);
}

export function scopedIdentifierFrom(input: ScopedIdentifier | ScopedIdentifierFromInput) {
  if (isNodeData(input)) {
    return scoped_identifier_({
      name: input.fields?.name,
      path: input.fields?.path,
    } as any);
  }
  const obj: any = input;
  return scoped_identifier_({
    name: resolveField(obj.name, _resolvePath8),
    path: obj.path !== undefined ? resolveField(obj.path, _r16dnbom) : undefined,
  } as any);
}

export function scopedTypeIdentifierFrom(input: ScopedTypeIdentifier | ScopedTypeIdentifierFromInput) {
  if (isNodeData(input)) {
    return scoped_type_identifier_({
      name: input.fields?.name,
      path: input.fields?.path,
    } as any);
  }
  const obj: any = input;
  return scoped_type_identifier_({
    name: resolveField(obj.name, _resolveTypeIdentifier2),
    path: obj.path !== undefined ? resolveField(obj.path, _r16dnbom) : undefined,
  } as any);
}

export function scopedUseListFrom(input: ScopedUseList | ScopedUseListFromInput) {
  if (isNodeData(input)) {
    return scoped_use_list_({
      list: input.fields?.list,
      path: input.fields?.path,
    } as any);
  }
  const obj: any = input;
  return scoped_use_list_({
    list: resolveField(obj.list, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('use_list', v) : v)),
    path: obj.path !== undefined ? resolveField(obj.path, _resolvePath2) : undefined,
  } as any);
}

export function selfParameterFrom(input: SelfParameter | SelfParameterFromInput) {
  if (isNodeData(input)) {
    return self_parameter_({
      lifetime: input.fields?.lifetime,
      mutableSpecifier: input.fields?.mutable_specifier,
      self: input.fields?.self,
    } as any);
  }
  const obj: any = input;
  return self_parameter_({
    lifetime: obj.lifetime !== undefined ? resolveField(obj.lifetime, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('lifetime', v) : v)) : undefined,
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
    self: resolveField(obj.self, (v: unknown) => (typeof v === 'string' && v === 'self' ? self_() : v)),
  } as any);
}

export function shorthandFieldInitializerFrom(input: ShorthandFieldInitializer | ShorthandFieldInitializerFromInput) {
  if (isNodeData(input)) {
    return shorthand_field_initializer_({
      attributes: input.fields?.attributes,
      identifier: input.fields?.identifier,
    } as any);
  }
  const obj: any = input;
  return shorthand_field_initializer_({
    attributes: obj.attributes !== undefined ? resolveField(obj.attributes, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('attribute_item', v) : v)) : undefined,
    identifier: resolveField(obj.identifier, _resolveReservedIdentifier),
  } as any);
}

export function slicePatternFrom(input: SlicePattern | SlicePatternFromInput) {
  if (isNodeData(input)) {
    return slice_pattern_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return slice_pattern_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolvePattern) : undefined,
  } as any);
}

export function sourceFileFrom(input: SourceFile | SourceFileFromInput) {
  if (isNodeData(input)) {
    return source_file_({
      shebang: input.fields?.shebang,
      statements: input.fields?.statements,
    } as any);
  }
  const obj: any = input;
  return source_file_({
    shebang: obj.shebang !== undefined ? resolveField(obj.shebang, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? shebang_('' + v) : v)) : undefined,
    statements: obj.statements !== undefined ? resolveField(obj.statements, _resolveStatement) : undefined,
  } as any);
}

export function staticItemFrom(input: StaticItem | StaticItemFromInput) {
  if (isNodeData(input)) {
    return static_item_({
      name: input.fields?.name,
      type: input.fields?.type,
      value: input.fields?.value,
      visibilityModifier: input.fields?.visibility_modifier,
      mutableSpecifier: input.fields?.mutable_specifier,
    } as any);
  }
  const obj: any = input;
  return static_item_({
    name: resolveField(obj.name, _resolveReservedIdentifier),
    type: resolveField(obj.type, _resolveType),
    value: obj.value !== undefined ? resolveField(obj.value, _resolveExpression) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
  } as any);
}

export function stringLiteralFrom(input: StringLiteral | StringLiteralFromInput) {
  if (isNodeData(input)) {
    return string_literal_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return string_literal_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _ra1kaw) : undefined,
  } as any);
}

export function structExpressionFrom(input: StructExpression | StructExpressionFromInput) {
  if (isNodeData(input)) {
    return struct_expression_({
      body: input.fields?.body,
      name: input.fields?.name,
    } as any);
  }
  const obj: any = input;
  return struct_expression_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('field_initializer_list', v) : v)),
    name: resolveField(obj.name, _r1315a75),
  } as any);
}

export function structItemFrom(input: StructItem | StructItemFromInput) {
  if (isNodeData(input)) {
    return struct_item_({
      body: input.fields?.body,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      visibilityModifier: input.fields?.visibility_modifier,
      whereClause: input.fields?.where_clause,
    } as any);
  }
  const obj: any = input;
  return struct_item_({
    body: obj.body !== undefined ? resolveField(obj.body, _r16kj8qi) : undefined,
    name: resolveField(obj.name, _resolveTypeIdentifier2),
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function structPatternFrom(input: StructPattern | StructPatternFromInput) {
  if (isNodeData(input)) {
    return struct_pattern_({
      type: input.fields?.type,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return struct_pattern_({
    type: resolveField(obj.type, _resolveType3),
    children: obj['children'] !== undefined ? resolveField(obj['children'], _rx4073x) : undefined,
  } as any);
}

export function tokenBindingPatternFrom(input: TokenBindingPattern | TokenBindingPatternFromInput) {
  if (isNodeData(input)) {
    return token_binding_pattern_({
      name: input.fields?.name,
      type: input.fields?.type,
    } as any);
  }
  const obj: any = input;
  return token_binding_pattern_({
    name: resolveField(obj.name, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? metavariable_('' + v) : v)),
    type: resolveField(obj.type, (v: unknown) => (typeof v === 'string' || typeof v === 'number' || typeof v === 'boolean' ? fragment_specifier_('' + v) : v)),
  } as any);
}

export function tokenRepetitionFrom(input: TokenRepetition | TokenRepetitionFromInput) {
  if (isNodeData(input)) {
    return token_repetition_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return token_repetition_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _r18bpr9a) : undefined,
  } as any);
}

export function tokenRepetitionPatternFrom(input: TokenRepetitionPattern | TokenRepetitionPatternFromInput) {
  if (isNodeData(input)) {
    return token_repetition_pattern_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return token_repetition_pattern_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _rznjc70) : undefined,
  } as any);
}

export function tokenTreeFrom(input: TokenTree | TokenTreeFromInput) {
  if (isNodeData(input)) {
    return token_tree_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return token_tree_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _r18bpr9a) : undefined,
  } as any);
}

export function tokenTreePatternFrom(input: TokenTreePattern | TokenTreePatternFromInput) {
  if (isNodeData(input)) {
    return token_tree_pattern_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return token_tree_pattern_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _rznjc70) : undefined,
  } as any);
}

export function traitBoundsFrom(input: TraitBounds | TraitBoundsFromInput) {
  if (isNodeData(input)) {
    return trait_bounds_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return trait_bounds_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _r1g3soma) : [],
  } as any);
}

export function traitItemFrom(input: TraitItem | TraitItemFromInput) {
  if (isNodeData(input)) {
    return trait_item_({
      body: input.fields?.body,
      bounds: input.fields?.bounds,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      visibilityModifier: input.fields?.visibility_modifier,
      whereClause: input.fields?.where_clause,
    } as any);
  }
  const obj: any = input;
  return trait_item_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('declaration_list', v) : v)),
    bounds: obj.bounds !== undefined ? resolveField(obj.bounds, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('trait_bounds', v) : v)) : undefined,
    name: resolveField(obj.name, _resolveTypeIdentifier2),
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function tryBlockFrom(input: TryBlock | TryBlockFromInput) {
  if (isNodeData(input)) {
    return try_block_({
      block: input.fields?.block,
    } as any);
  }
  const obj: any = input;
  return try_block_({
    block: resolveField(obj.block, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
  } as any);
}

export function tryExpressionFrom(input: TryExpression | TryExpressionFromInput) {
  if (isNodeData(input)) {
    return try_expression_({
      value: input.fields?.value,
      operator: input.fields?.operator,
    } as any);
  }
  const obj: any = input;
  return try_expression_({
    value: resolveField(obj.value, _resolveExpression),
    operator: resolveField(obj.operator, _resolveTypeIdentifier15),
  } as any);
}

export function tupleExpressionFrom(input: TupleExpression | TupleExpressionFromInput) {
  if (isNodeData(input)) {
    return tuple_expression_({
      attributes: input.fields?.attributes,
      first: input.fields?.first,
      rest: input.fields?.rest,
      trailing: input.fields?.trailing,
    } as any);
  }
  const obj: any = input;
  return tuple_expression_({
    attributes: obj.attributes !== undefined ? resolveField(obj.attributes, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('attribute_item', v) : v)) : undefined,
    first: resolveField(obj.first, _resolveExpression),
    rest: obj.rest !== undefined ? resolveField(obj.rest, _resolveExpression) : undefined,
    trailing: obj.trailing !== undefined ? resolveField(obj.trailing, _resolveExpression) : undefined,
  } as any);
}

export function tuplePatternFrom(input: TuplePattern | TuplePatternFromInput) {
  if (isNodeData(input)) {
    return tuple_pattern_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return tuple_pattern_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveExpressionExceptRange4) : undefined,
  } as any);
}

export function tupleStructPatternFrom(input: TupleStructPattern | TupleStructPatternFromInput) {
  if (isNodeData(input)) {
    return tuple_struct_pattern_({
      type: input.fields?.type,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return tuple_struct_pattern_({
    type: resolveField(obj.type, _rnfan),
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolvePattern) : undefined,
  } as any);
}

export function tupleTypeFrom(input: TupleType | TupleTypeFromInput) {
  if (isNodeData(input)) {
    return tuple_type_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return tuple_type_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveType) : [],
  } as any);
}

export function typeArgumentsFrom(input: TypeArguments | TypeArgumentsFromInput) {
  if (isNodeData(input)) {
    return type_arguments_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return type_arguments_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _rzww8fe) : [],
  } as any);
}

export function typeBindingFrom(input: TypeBinding | TypeBindingFromInput) {
  if (isNodeData(input)) {
    return type_binding_({
      name: input.fields?.name,
      type: input.fields?.type,
      typeArguments: input.fields?.type_arguments,
    } as any);
  }
  const obj: any = input;
  return type_binding_({
    name: resolveField(obj.name, _resolveTypeIdentifier2),
    type: resolveField(obj.type, _resolveType),
    typeArguments: obj.typeArguments !== undefined ? resolveField(obj.typeArguments, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_arguments', v) : v)) : undefined,
  } as any);
}

export function typeCastExpressionFrom(input: TypeCastExpression | TypeCastExpressionFromInput) {
  if (isNodeData(input)) {
    return type_cast_expression_({
      type: input.fields?.type,
      value: input.fields?.value,
    } as any);
  }
  const obj: any = input;
  return type_cast_expression_({
    type: resolveField(obj.type, _resolveType),
    value: resolveField(obj.value, _resolveExpression),
  } as any);
}

export function typeItemFrom(input: TypeItem | TypeItemFromInput) {
  if (isNodeData(input)) {
    return type_item_({
      name: input.fields?.name,
      type: input.fields?.type,
      typeParameters: input.fields?.type_parameters,
      visibilityModifier: input.fields?.visibility_modifier,
      whereClause: input.fields?.where_clause,
      trailingWhereClause: input.fields?.trailing_where_clause,
    } as any);
  }
  const obj: any = input;
  return type_item_({
    name: resolveField(obj.name, _resolveTypeIdentifier2),
    type: resolveField(obj.type, _resolveType),
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
    trailingWhereClause: obj.trailingWhereClause !== undefined ? resolveField(obj.trailingWhereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function typeParameterFrom(input: TypeParameter | TypeParameterFromInput) {
  if (isNodeData(input)) {
    return type_parameter_({
      bounds: input.fields?.bounds,
      defaultType: input.fields?.default_type,
      name: input.fields?.name,
    } as any);
  }
  const obj: any = input;
  return type_parameter_({
    bounds: obj.bounds !== undefined ? resolveField(obj.bounds, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('trait_bounds', v) : v)) : undefined,
    defaultType: obj.defaultType !== undefined ? resolveField(obj.defaultType, _resolveType) : undefined,
    name: resolveField(obj.name, _resolveTypeIdentifier2),
  } as any);
}

export function typeParametersFrom(input: TypeParameters | TypeParametersFromInput) {
  if (isNodeData(input)) {
    return type_parameters_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return type_parameters_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _rrcyxzh) : [],
  } as any);
}

export function unaryExpressionFrom(input: UnaryExpression | UnaryExpressionFromInput) {
  if (isNodeData(input)) {
    return unary_expression_({
      operator: input.fields?.operator,
      operand: input.fields?.operand,
    } as any);
  }
  const obj: any = input;
  return unary_expression_({
    operator: resolveField(obj.operator, _resolveTypeIdentifier16),
    operand: resolveField(obj.operand, _resolveExpression),
  } as any);
}

export function unionItemFrom(input: UnionItem | UnionItemFromInput) {
  if (isNodeData(input)) {
    return union_item_({
      body: input.fields?.body,
      name: input.fields?.name,
      typeParameters: input.fields?.type_parameters,
      visibilityModifier: input.fields?.visibility_modifier,
      whereClause: input.fields?.where_clause,
    } as any);
  }
  const obj: any = input;
  return union_item_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('field_declaration_list', v) : v)),
    name: resolveField(obj.name, _resolveTypeIdentifier2),
    typeParameters: obj.typeParameters !== undefined ? resolveField(obj.typeParameters, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('type_parameters', v) : v)) : undefined,
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
    whereClause: obj.whereClause !== undefined ? resolveField(obj.whereClause, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_clause', v) : v)) : undefined,
  } as any);
}

export function unsafeBlockFrom(input: UnsafeBlock | UnsafeBlockFromInput) {
  if (isNodeData(input)) {
    return unsafe_block_({
      block: input.fields?.block,
    } as any);
  }
  const obj: any = input;
  return unsafe_block_({
    block: resolveField(obj.block, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
  } as any);
}

export function useAsClauseFrom(input: UseAsClause | UseAsClauseFromInput) {
  if (isNodeData(input)) {
    return use_as_clause_({
      alias: input.fields?.alias,
      path: input.fields?.path,
    } as any);
  }
  const obj: any = input;
  return use_as_clause_({
    alias: resolveField(obj.alias, _resolveReservedIdentifier),
    path: resolveField(obj.path, _resolvePath2),
  } as any);
}

export function useBoundsFrom(input: UseBounds | UseBoundsFromInput) {
  if (isNodeData(input)) {
    return use_bounds_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return use_bounds_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _r1qv9yc5) : undefined,
  } as any);
}

export function useDeclarationFrom(input: UseDeclaration | UseDeclarationFromInput) {
  if (isNodeData(input)) {
    return use_declaration_({
      argument: input.fields?.argument,
      visibilityModifier: input.fields?.visibility_modifier,
    } as any);
  }
  const obj: any = input;
  return use_declaration_({
    argument: resolveField(obj.argument, _resolveUseClause2),
    visibilityModifier: obj.visibilityModifier !== undefined ? resolveField(obj.visibilityModifier, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('visibility_modifier', v) : v)) : undefined,
  } as any);
}

export function useListFrom(input: UseList | UseListFromInput) {
  if (isNodeData(input)) {
    return use_list_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return use_list_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveUseClause) : undefined,
  } as any);
}

export function useWildcardFrom(input: UseWildcard | UseWildcardFromInput) {
  if (isNodeData(input)) {
    return use_wildcard_({
      path: input.fields?.path,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return use_wildcard_({
    path: obj.path !== undefined ? resolveField(obj.path, _resolvePath) : undefined,
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolvePath) : undefined,
  } as any);
}

export function variadicParameterFrom(input: VariadicParameter | VariadicParameterFromInput) {
  if (isNodeData(input)) {
    return variadic_parameter_({
      pattern: input.fields?.pattern,
      mutableSpecifier: input.fields?.mutable_specifier,
    } as any);
  }
  const obj: any = input;
  return variadic_parameter_({
    pattern: obj.pattern !== undefined ? resolveField(obj.pattern, _resolvePattern) : undefined,
    mutableSpecifier: obj.mutableSpecifier !== undefined ? resolveField(obj.mutableSpecifier, (v: unknown) => (typeof v === 'string' && v === 'mut' ? mutable_specifier_() : v)) : undefined,
  } as any);
}

export function visibilityModifierFrom(input: VisibilityModifier | VisibilityModifierFromInput) {
  if (isNodeData(input)) {
    return visibility_modifier_({
      pub: input.fields?.pub,
      in: input.fields?.in,
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return visibility_modifier_({
    pub: obj.pub !== undefined ? resolveField(obj.pub, _resolveTypeIdentifier17) : undefined,
    in: obj.in !== undefined ? resolveField(obj.in, _resolveTypeIdentifier18) : undefined,
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolvePath2) : undefined,
  } as any);
}

export function whereClauseFrom(input: WhereClause | WhereClauseFromInput) {
  if (isNodeData(input)) {
    return where_clause_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return where_clause_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('where_predicate', v) : v)) : undefined,
  } as any);
}

export function wherePredicateFrom(input: WherePredicate | WherePredicateFromInput) {
  if (isNodeData(input)) {
    return where_predicate_({
      bounds: input.fields?.bounds,
      left: input.fields?.left,
    } as any);
  }
  const obj: any = input;
  return where_predicate_({
    bounds: resolveField(obj.bounds, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('trait_bounds', v) : v)),
    left: resolveField(obj.left, _r17ua2b4),
  } as any);
}

export function whileExpressionFrom(input: WhileExpression | WhileExpressionFromInput) {
  if (isNodeData(input)) {
    return while_expression_({
      body: input.fields?.body,
      condition: input.fields?.condition,
      label: input.fields?.label,
    } as any);
  }
  const obj: any = input;
  return while_expression_({
    body: resolveField(obj.body, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('block', v) : v)),
    condition: resolveField(obj.condition, _resolveCondition2),
    label: obj.label !== undefined ? resolveField(obj.label, (v: any) => (typeof v === 'object' && v !== null ? _resolveByKind('label', v) : v)) : undefined,
  } as any);
}

export function yieldExpressionFrom(input: YieldExpression | YieldExpressionFromInput) {
  if (isNodeData(input)) {
    return yield_expression_({
      children: (input as any).children,
    } as any);
  }
  const obj: any = Array.isArray(input) ? { children: input } : input;
  return yield_expression_({
    children: obj['children'] !== undefined ? resolveField(obj['children'], _resolveExpression) : undefined,
  } as any);
}

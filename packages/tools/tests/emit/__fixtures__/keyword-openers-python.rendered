def f(a, b=(1, 2)):
    x = (1 + 2) * 3
    if (x):
        return (x)
    while (x):
        x = [1]
    for i in (a):
        pass
    assert (x)
    del (x)
    g = lambda: (x)
    y = not (x)
    z = x and (y) or [1]
    yield (x)
    raise (E)
    w = [i for i in (a) if (i)]
    v = a[(1)]
    u = {"k": (1), "j": [2]}
    s = f"a{(x)}"
    return (a, b)

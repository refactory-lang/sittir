/**
 * validate/node-types.ts — optional post-Link check (T021).
 *
 * validateAgainstNodeTypes and formatNodeTypesValidationReport were removed
 * in R8 (dead-code removal): no production callers, only test callers.
 */

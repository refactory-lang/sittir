// Auto-generated by @sittir/codegen — do not edit
import { describe, it, expect } from 'vitest';
import { ir } from '../src/ir.js';
import { TSKindId } from '../src/types.js';

describe('program', () => {
	it('factory produces correct type', () => {
		const node = ir.program();
		expect(node.$type).toBe(TSKindId.Program);
		expect(node.$source).toBe(2);
	});
	it('render does not throw on minimal config', () => {
		const node = ir.program();
		expect(() => node.$render!()).not.toThrow();
	});
});

describe('escape_sequence', () => {
	it('factory produces correct type', () => {
		const node = ir.escapeSequence('a');
		expect(node.$type).toBe(TSKindId.EscapeSequence);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.escapeSequence('a');
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});

describe('identifier', () => {
	it('factory produces correct type', () => {
		const node = ir.identifier('test');
		expect(node.$type).toBe(TSKindId.Identifier);
		expect(node.$source).toBe(2);
		expect(node.$text).toBe('test');
	});
});

describe('_immediate_identifier', () => {
	it('factory produces correct type', () => {
		const node = ir.immediateIdentifier('test');
		expect(node.$type).toBe(TSKindId.ImmediateIdentifier);
		expect(node.$source).toBe(2);
		expect(node.$text).toBe('test');
	});
});

describe('capture', () => {
	it('factory produces correct type', () => {
		const node = ir.capture({ $type: TSKindId.ImmediateIdentifier, $text: 'test', $source: 2, $named: true } as any);
		expect(node.$type).toBe(TSKindId.Capture);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.capture({ $type: TSKindId.ImmediateIdentifier, $text: 'test', $source: 2, $named: true } as any);
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
		expect(rendered).toContain('test');
	});
});

describe('string', () => {
	it('factory produces correct type', () => {
		const node = ir.string();
		expect(node.$type).toBe(TSKindId.String);
		expect(node.$source).toBe(2);
	});
	it('render does not throw on minimal config', () => {
		const node = ir.string();
		expect(() => node.$render!()).not.toThrow();
	});
});

describe('immediate_string', () => {
	it('factory produces correct type', () => {
		const node = ir.immediateString();
		expect(node.$type).toBe(TSKindId.ImmediateString);
		expect(node.$source).toBe(2);
	});
	it('render does not throw on minimal config', () => {
		const node = ir.immediateString();
		expect(() => node.$render!()).not.toThrow();
	});
});

describe('string_content', () => {
	it('factory produces correct type', () => {
		const node = ir.stringContent();
		expect(node.$type).toBe(TSKindId.StringContent);
		expect(node.$source).toBe(2);
	});
	it('render does not throw on minimal config', () => {
		const node = ir.stringContent();
		expect(() => node.$render!()).not.toThrow();
	});
});

describe('string_content sub-factories', () => {
	it('escapeSequence builds the escape_sequence form', () => {
		const node = ir.stringContent.escapeSequence('a');
		expect(node.$type).toBe(TSKindId.EscapeSequence);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
});

describe('parameters', () => {
	it('factory produces correct type', () => {
		const node = ir.parameters({ $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any);
		expect(node.$type).toBe(TSKindId.Parameters);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.parameters({ $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any);
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
		expect(rendered).toContain('test');
	});
});

describe('comment', () => {
	it('factory produces correct type', () => {
		const node = ir.comment('test');
		expect(node.$type).toBe(TSKindId.Comment);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.comment('test');
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});

describe('list', () => {
	it('factory produces correct type', () => {
		const node = ir.list({
			definitions: [{ $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any]
		});
		expect(node.$type).toBe(TSKindId.List);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.list({
			definitions: [{ $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any]
		});
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});

describe('list sub-factories', () => {
	it('capture builds the capture form', () => {
		const node = ir.list.capture({
			$type: TSKindId.ImmediateIdentifier,
			$text: 'test',
			$source: 2,
			$named: true
		} as any);
		expect(node.$type).toBe(TSKindId.Capture);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
});

describe('grouping', () => {
	it('factory produces correct type', () => {
		const node = ir.grouping({
			groupingGroup: [
				{
					$type: TSKindId.GroupingGroup,
					$text: 'test',
					$source: 2,
					$named: true,
					_group_expression: {
						$type: TSKindId.GroupExpressionArm,
						$text: 'test',
						$source: 2,
						$named: true,
						_left: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any,
						_right: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any
					} as any
				} as any
			]
		});
		expect(node.$type).toBe(TSKindId.Grouping);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.grouping({
			groupingGroup: [
				{
					$type: TSKindId.GroupingGroup,
					$text: 'test',
					$source: 2,
					$named: true,
					_group_expression: {
						$type: TSKindId.GroupExpressionArm,
						$text: 'test',
						$source: 2,
						$named: true,
						_left: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any,
						_right: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any
					} as any
				} as any
			]
		});
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});

describe('grouping sub-factories', () => {
	it('capture builds the capture form', () => {
		const node = ir.grouping.capture({
			$type: TSKindId.ImmediateIdentifier,
			$text: 'test',
			$source: 2,
			$named: true
		} as any);
		expect(node.$type).toBe(TSKindId.Capture);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
});

describe('missing_node', () => {
	it('factory produces correct type', () => {
		const node = ir.missingNode({});
		expect(node.$type).toBe(TSKindId.MissingNode);
		expect(node.$source).toBe(2);
	});
	it('render does not throw on minimal config', () => {
		const node = ir.missingNode({});
		expect(() => node.$render!()).not.toThrow();
	});
});

describe('missing_node sub-factories', () => {
	it('capture builds the capture form', () => {
		const node = ir.missingNode.capture({
			$type: TSKindId.ImmediateIdentifier,
			$text: 'test',
			$source: 2,
			$named: true
		} as any);
		expect(node.$type).toBe(TSKindId.Capture);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
});

describe('anonymous_node', () => {
	it('factory produces correct type', () => {
		const node = ir.anonymousNode({ name: { $type: TSKindId.String, $text: 'test', $source: 2, $named: true } as any });
		expect(node.$type).toBe(TSKindId.AnonymousNode);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.anonymousNode({ name: { $type: TSKindId.String, $text: 'test', $source: 2, $named: true } as any });
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});

describe('anonymous_node sub-factories', () => {
	it('capture builds the capture form', () => {
		const node = ir.anonymousNode.capture({
			$type: TSKindId.ImmediateIdentifier,
			$text: 'test',
			$source: 2,
			$named: true
		} as any);
		expect(node.$type).toBe(TSKindId.Capture);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
});

describe('named_node', () => {
	it('factory produces correct type', () => {
		const node = ir.namedNode({});
		expect(node.$type).toBe(TSKindId.NamedNode);
		expect(node.$source).toBe(2);
	});
	it('render does not throw on minimal config', () => {
		const node = ir.namedNode({});
		expect(() => node.$render!()).not.toThrow();
	});
});

describe('named_node sub-factories', () => {
	it('underscore builds the parent', () => {
		const node = ir.namedNode.underscore({});
		expect(node.$type).toBe(TSKindId.NamedNode);
		const seated = (node as any).name();
		expect(seated?.$text ?? seated).toBe(TSKindId.Underscore);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
	it('arm builds the parent', () => {
		const node = ir.namedNode.arm({
			namedNodeArm: {
				supertype: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any,
				name: { $type: TSKindId.ImmediateIdentifier, $text: 'test', $source: 2, $named: true } as any
			}
		});
		expect(node.$type).toBe(TSKindId.NamedNode);
		expect((node as any).namedNodeArm()?.$type).toBe(TSKindId.NamedNodeArm);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
	it('children builds the parent', () => {
		const node = ir.namedNode.children({
			namedNodeGroup: [
				{
					$type: TSKindId.NegatedField,
					$text: 'test',
					$source: 2,
					$named: true,
					_identifier: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any
				} as any
			]
		});
		expect(node.$type).toBe(TSKindId.NamedNode);
		expect((node as any).namedNodeGroup()?.$type).toBe(TSKindId.NamedNodeGroupChildren);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
	it('anchoredLast builds the parent', () => {
		const node = ir.namedNode.anchoredLast({
			last: {
				$type: TSKindId.NegatedField,
				$text: 'test',
				$source: 2,
				$named: true,
				_identifier: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any
			} as any
		});
		expect(node.$type).toBe(TSKindId.NamedNode);
		expect((node as any).namedNodeGroup()?.$type).toBe(TSKindId.NamedNodeGroupAnchoredLast);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
	it('capture builds the capture form', () => {
		const node = ir.namedNode.capture({
			$type: TSKindId.ImmediateIdentifier,
			$text: 'test',
			$source: 2,
			$named: true
		} as any);
		expect(node.$type).toBe(TSKindId.Capture);
		expect(node.$render!().length).toBeGreaterThan(0);
	});
});

describe('field_definition', () => {
	it('factory produces correct type', () => {
		const node = ir.fieldDefinition({
			name: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any,
			definition: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any
		});
		expect(node.$type).toBe(TSKindId.FieldDefinition);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.fieldDefinition({
			name: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any,
			definition: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any
		});
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});

describe('negated_field', () => {
	it('factory produces correct type', () => {
		const node = ir.negatedField({ $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any);
		expect(node.$type).toBe(TSKindId.NegatedField);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.negatedField({ $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any);
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
		expect(rendered).toContain('test');
	});
});

describe('predicate', () => {
	it('factory produces correct type', () => {
		const node = ir.predicate({
			content: '#',
			immediateIdentifier: { $type: TSKindId.ImmediateIdentifier, $text: 'test', $source: 2, $named: true } as any,
			type: '?'
		});
		expect(node.$type).toBe(TSKindId.Predicate);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.predicate({
			content: '#',
			immediateIdentifier: { $type: TSKindId.ImmediateIdentifier, $text: 'test', $source: 2, $named: true } as any,
			type: '?'
		});
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});

describe('group_expression_arm', () => {
	it('factory produces correct type', () => {
		const node = ir.groupExpressionArm({
			left: {
				$type: TSKindId.GroupExpressionArm,
				$text: 'test',
				$source: 2,
				$named: true,
				_left: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any,
				_right: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any
			} as any,
			right: {
				$type: TSKindId.GroupExpressionArm,
				$text: 'test',
				$source: 2,
				$named: true,
				_left: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any,
				_right: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any
			} as any
		});
		expect(node.$type).toBe(TSKindId.GroupExpressionArm);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.groupExpressionArm({
			left: {
				$type: TSKindId.GroupExpressionArm,
				$text: 'test',
				$source: 2,
				$named: true,
				_left: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any,
				_right: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any
			} as any,
			right: {
				$type: TSKindId.GroupExpressionArm,
				$text: 'test',
				$source: 2,
				$named: true,
				_left: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any,
				_right: { $type: TSKindId.NamedNode, $text: 'test', $source: 2, $named: true } as any
			} as any
		});
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});

describe('named_node_expression_arm', () => {
	it('factory produces correct type', () => {
		const node = ir.namedNodeExpressionArm({
			left: {
				$type: TSKindId.NegatedField,
				$text: 'test',
				$source: 2,
				$named: true,
				_identifier: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any
			} as any,
			right: {
				$type: TSKindId.NegatedField,
				$text: 'test',
				$source: 2,
				$named: true,
				_identifier: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any
			} as any
		});
		expect(node.$type).toBe(TSKindId.NamedNodeExpressionArm);
		expect(node.$source).toBe(2);
	});
	it('render produces non-empty string', () => {
		const node = ir.namedNodeExpressionArm({
			left: {
				$type: TSKindId.NegatedField,
				$text: 'test',
				$source: 2,
				$named: true,
				_identifier: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any
			} as any,
			right: {
				$type: TSKindId.NegatedField,
				$text: 'test',
				$source: 2,
				$named: true,
				_identifier: { $type: TSKindId.Identifier, $text: 'test', $source: 2, $named: true } as any
			} as any
		});
		const rendered = node.$render!();
		expect(rendered.length).toBeGreaterThan(0);
	});
});
